-- T60 follow-up shim — content moved to catept-plugin-architecture at module
-- path `CATEPTPluginArchitecture.Domains.SuperiorMethod`. Lean namespace
-- `CATEPTMain.Domains` is preserved verbatim inside the dep, so consumers
-- (Domains/{QM,GR,SR,ETH,VML}/Domain.lean, Bridges/SuperiorMethodBridges.lean,
-- Bridges/CrossDomainCompat.lean, etc.) see no change.
import CATEPTPluginArchitecture.Domains.SuperiorMethod
