-- T60 step 2 shim — content moved to catept-core at module path
-- `CATEPTMainExtracted.Core.Assumptions`.  Namespace `CATEPTMain` and the
-- `CATEPTAssumption` abbrev / `AssumptionId` registry are preserved verbatim
-- inside the dep.  See lakefile.lean `require «catept-core»`.
import CATEPTMainExtracted.Core.Assumptions
