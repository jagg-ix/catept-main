import NavierStokes.Bridges.EntropicTimeReparam

/-!
# Entropic Time Mechanics Bridge (Thin Re-export)

Canonical implementation moved to:
`NavierStokes/Bridges/EntropicTimeReparam.lean`.

This module is kept as a compatibility re-export so existing imports of
`NavierStokes.EntropicTimeMechanicsBridge` continue to work without path changes.
-/
