-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ln_series
-- PVSLib library: lnexp
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ln_series

Auto-generated stubs from PVSLib theory `ln_series`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ln_series

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Inf_sum : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (Integrable_p : pvs_τ4 → pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (Integral : pvs_τ7 → pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} (conv_powerseries_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} (conv_series_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (convergent_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (derivable_n_times_p : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (factorial : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (inf_sum : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (ln : pvs_τ19 → pvs_τ20)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (series : pvs_τ23 → pvs_τ24)

-- pvs2lean: ln_series.noa_posreal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[posreal]
theorem noa_posreal : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.noa_gt_m1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[{x: real | x > -1}]
theorem noa_gt_m1 : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.conn_gt_m1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[{x: real | x > -1}]
theorem conn_gt_m1 : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.conn_posreal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[posreal]
theorem conn_posreal : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.conn_abslt1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[abslt1]
theorem conn_abslt1 : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.noa_abslt1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[abslt1]
theorem noa_abslt1 : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.deriv_domain_gtm1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv_domain?[{x: real | x > -1}]
theorem deriv_domain_gtm1 : deriv_domain_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.nderiv_ln
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable_n_times?[posreal](ln, n)
theorem nderiv_ln : derivable_n_times_p ln n := by
  -- === TCCs for nderiv_ln ===
  -- TCC: nderiv_ln_TCC1
  -- Statement: deriv_domain?[posreal]
  have nderiv_ln_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_ln_TCC2
  -- Statement: not_one_element?[posreal]
  have nderiv_ln_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_ln_TCC1
  -- Statement: deriv_domain?[posreal]
  have nderiv_ln_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_ln_TCC2
  -- Statement: not_one_element?[posreal]
  have nderiv_ln_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.ln_nderiv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nderiv[posreal](n, ln) =
--  IF n = 0 THEN ln
--  ELSE (LAMBDA (x: posreal): -factorial(n - 1) / (-x) ^ n)
--  ENDIF
theorem ln_nderiv : nderiv n ln = if n = 0 then ln
else (fun (x : {x : ℝ // 0 < x})=> -factorial (n - 1) / (-x) ^ n) := by
  -- === TCCs for ln_nderiv ===
  -- TCC: ln_nderiv_TCC1
  -- Statement: FORALL (n: nat): derivable_n_times?[posreal](ln, n)
  have ln_nderiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC2
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES n - 1 >= 0
  have ln_nderiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC3
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES FORALL (x: posreal): (-x) /= 0 OR n >= 0
  have ln_nderiv_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC1
  -- Statement: FORALL (n: nat): derivable_n_times?[posreal](ln, n)
  have ln_nderiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC2
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES n - 1 >= 0
  have ln_nderiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC3
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES FORALL (x: posreal): (-x) /= 0 OR n >= 0
  have ln_nderiv_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_series.ln_estimate
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, n, (LAMBDA (nn: nat): IF nn = 0 THEN 0 ELSE -(-x) ^ nn / nn ENDIF))
axiom ln_estimate {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: ln_series.ln_taylors
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (c: between[{x: real | x > -1}](1, 1 + xgm1)):
--   ln(xgm1 + 1) = ln_estimate(xgm1, n) - (xgm1 / -c) ^ (n + 1) / (n + 1)
theorem ln_taylors : ∃ (c : {x : {x : ℝ // x > -1} // (1 < 1 + xgm1 → 1 < x ∧ x < 1 + xgm1) ∧
 (1 + xgm1 < 1 → 1 + xgm1 < x ∧ x < 1) ∧ (0 = xgm1 → x = 1)}), ln (xgm1 + 1) = ln_estimate xgm1 n - (xgm1 / -c) ^ (n + 1) / (n + 1) := by
  -- === TCCs for ln_taylors ===
  -- TCC: ln_taylors_TCC1
  -- Statement: connected?[{x: real | x > -1}]
  have ln_taylors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC2
  -- Statement: not_one_element?[{x: real | x > -1}]
  have ln_taylors_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC3
  -- Statement: FORALL (xgm1: {x: real | x > -1},
  --         c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   xgm1 + 1 >= 0 AND xgm1 + 1 > 0
  have ln_taylors_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC4
  -- Statement: FORALL (xgm1: {x: real | x > -1},
  --         c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   -c /= 0
  have ln_taylors_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC5
  -- Statement: FORALL (n: nat, xgm1: {x: real | x > -1},
  --         c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   (xgm1 / -c) /= 0 OR (n + 1) >= 0
  have ln_taylors_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC1
  -- Statement: connected?[{x: real | x > -1}]
  have ln_taylors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC2
  -- Statement: not_one_element?[{x: real | x > -1}]
  have ln_taylors_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC3
  -- Statement: FORALL (xgm1: Gt_m1, c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   xgm1 + 1 >= 0 AND xgm1 + 1 > 0
  have ln_taylors_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC4
  -- Statement: FORALL (xgm1: Gt_m1, c: between[{x: real | x > -1}](1, 1 + xgm1)): -c /= 0
  have ln_taylors_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC5
  -- Statement: FORALL (n: nat, xgm1: Gt_m1, c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   (xgm1 / -c) /= 0 OR (n + 1) >= 0
  have ln_taylors_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.ln_estimate_error_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (xp: posreal):
--   abs(ln(xp + 1) - ln_estimate(xp, n)) <= xp ^ (n + 1) / (n + 1)
theorem ln_estimate_error_bound : ∀ (xp : {x : {x : ℝ // x ≥ 0} // x > 0}), |ln (xp + 1) - ln_estimate xp n| ≤ xp ^ (n + 1) / (n + 1) := by
  -- === TCCs for ln_estimate_error_bound ===
  -- TCC: ln_estimate_error_bound_TCC1
  -- Statement: FORALL (n: nat, xp: posreal): xp /= 0 OR (n + 1) >= 0
  have ln_estimate_error_bound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.lnp1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: ln(1 + x1) = Integral(1, 1 + x1, (LAMBDA (t: posreal): 1 / t))
theorem lnp1 : ln (1 + x1) = Integral 1 (1 + x1) ((fun (t : {x : ℝ // 0 < x})=> 1 / t)) := by
  -- === TCCs for lnp1 ===
  -- TCC: lnp1_TCC1
  -- Statement: FORALL (x1: abslt1): 1 + x1 >= 0 AND 1 + x1 > 0
  have lnp1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lnp1_TCC2
  -- Statement: FORALL (x1: abslt1):
  --   Integrable?[posreal](1, 1 + x1, (LAMBDA (t: posreal): 1 / t))
  have lnp1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lnp1_TCC3
  -- Statement: connected?[posreal]
  have lnp1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_series.ln_estimate_increasing_odd
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (px, py: real):
--   px <= py IMPLIES ln_estimate(px, 2 * n + 1) <= ln_estimate(py, 2 * n + 1)
theorem ln_estimate_increasing_odd : ∀ (px : ℝ) (py : ℝ), px ≤ py → ln_estimate px (2 * n + 1) ≤ ln_estimate py (2 * n + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.ln_estimate_increasing_even
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x1, y1: absle1):
--   x1 <= y1 IMPLIES ln_estimate(x1, 2 * n) <= ln_estimate(y1, 2 * n)
theorem ln_estimate_increasing_even : ∀ (x1 : {x : ℝ // |x| ≤ 1}) (y1 : {x : ℝ // |x| ≤ 1}), x1 ≤ y1 → ln_estimate x1 (2 * n) ≤ ln_estimate y1 (2 * n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.lnp1_prep
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: Integrable?(0, x1, (LAMBDA (u: Gt_m1): 1 / (u + 1))) AND
--  Integral(1, 1 + x1, (LAMBDA (t: posreal): 1 / t)) =
--   Integral(0, x1, (LAMBDA (u: Gt_m1): 1 / (u + 1)))
theorem lnp1_prep : Integrable_p 0 x1 ((fun (u : Gt_m1)=> 1 / (u + 1))) ∧ Integral 1 (1 + x1) ((fun (t : {x : ℝ // 0 < x})=> 1 / t)) =
 Integral 0 x1 ((fun (u : Gt_m1)=> 1 / (u + 1))) := by
  -- === TCCs for lnp1_prep ===
  -- TCC: lnp1_prep_TCC1
  -- Statement: FORALL (x1: abslt1): x1 > -1
  have lnp1_prep_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lnp1_prep_TCC2
  -- Statement: FORALL (u: Gt_m1): (u + 1) /= 0
  have lnp1_prep_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.geom_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES
--  convergent?(series(LAMBDA n: (-x) ^ n)) AND
--   1 / (1 + x) = inf_sum((LAMBDA n: (-x) ^ n))
theorem geom_neg : |x| < 1 → convergent_p (series (fun n=>  (-x) ^ n)) ∧
 1 / (1 + x) = inf_sum ((fun n=>  (-x) ^ n)) := by
  -- === TCCs for geom_neg ===
  -- TCC: geom_neg_TCC1
  -- Statement: FORALL (x: real): abs(x) < 1 IMPLIES FORALL (n: nat): (-x) /= 0 OR n >= 0
  have geom_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: geom_neg_TCC2
  -- Statement: FORALL (x: real):
  --   convergent?(series(LAMBDA (n: nat): (-x) ^ n)) AND abs(x) < 1 IMPLIES
  --    (1 + x) /= 0
  have geom_neg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: geom_neg_TCC3
  -- Statement: FORALL (x: real):
  --   convergent?(series(LAMBDA (n: nat): (-x) ^ n)) AND abs(x) < 1 IMPLIES
  --    conv_series?(LAMBDA (n: nat): (-x) ^ n)
  have geom_neg_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.lnp1_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN 0 ELSE (-1) ^ (n + 1) / n ENDIF
noncomputable def lnp1_seq : sorry := by
  sorry  -- pvs2lean: definition stub for IF n = 0 THEN 0 ELSE (-1) ^ (n + 1) / n ENDIF

-- pvs2lean: ln_series.lnp1_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: conv_powerseries?[abslt1](lnp1_seq)
theorem lnp1_conv : conv_powerseries_p lnp1_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.int_geo_prep
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: Integrable?(0, x1, (LAMBDA (u: abslt1): 1 / (1 + u)))
theorem int_geo_prep : Integrable_p 0 x1 ((fun (u : abslt1)=> 1 / (1 + u))) := by
  -- === TCCs for int_geo_prep ===
  -- TCC: int_geo_prep_TCC1
  -- Statement: abs(0) < 1
  have int_geo_prep_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: int_geo_prep_TCC2
  -- Statement: FORALL (u: abslt1): (1 + u) /= 0
  have int_geo_prep_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_series.int_geo_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: conv_powerseries?[abslt1](lnp1_seq) AND
--  Integral(0, x1, (LAMBDA (u: {x: real | x > -1}): 1 / (u + 1))) =
--   Inf_sum(lnp1_seq)(x1)
theorem int_geo_neg : conv_powerseries_p lnp1_seq ∧ Integral 0 x1 ((fun (u : {x : ℝ // x > -1})=> 1 / (u + 1))) =
 Inf_sum lnp1_seq(x1) := by
  -- === TCCs for int_geo_neg ===
  -- TCC: int_geo_neg_TCC1
  -- Statement: FORALL (x1: abslt1):
  --   conv_powerseries?[abslt1](lnp1_seq) IMPLIES
  --    Integrable?[{x: real | x > -1}]
  --        (0, x1, (LAMBDA (u: {x: real | x > -1}): 1 / (u + 1)))
  have int_geo_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.ln_series