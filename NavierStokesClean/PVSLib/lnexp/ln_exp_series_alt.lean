-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ln_exp_series_alt
-- PVSLib library: lnexp
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ln_exp_series_alt

Auto-generated stubs from PVSLib theory `ln_exp_series_alt`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ln_exp_series_alt

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Integrable_p : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (Integral : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (continuous_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (conv_series_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (convergence : pvs_τ10 → pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} (convergent_p : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (derivable_n_times_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} (derivable_p : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (exp : pvs_τ17 → pvs_τ18)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (factorial : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (inf_sum : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (ln : pvs_τ28 → pvs_τ29)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (max : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (nderiv : pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (series : pvs_τ40 → pvs_τ41)

-- pvs2lean: ln_exp_series_alt.noa_posreal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[posreal]
theorem noa_posreal : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.conn_posreal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[posreal]
theorem conn_posreal : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.noa_gt_m1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[{x: real | x > -1}]
theorem noa_gt_m1 : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.conn_gt_m1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[{x: real | x > -1}]
theorem conn_gt_m1 : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.deriv_domain_gtm1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv_domain?[{x: real | x > -1}]
theorem deriv_domain_gtm1 : deriv_domain_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.nderiv_ln
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable_n_times?[posreal](ln, n)
theorem nderiv_ln : derivable_n_times_p ln n := by
  -- === TCCs for nderiv_ln ===
  -- TCC: nderiv_ln_TCC1
  -- Statement: deriv_domain?[posreal]
  have nderiv_ln_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_ln_TCC2
  -- Statement: not_one_element?[posreal]
  have nderiv_ln_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_ln_TCC1
  -- Statement: deriv_domain?[posreal]
  have nderiv_ln_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_ln_TCC2
  -- Statement: not_one_element?[posreal]
  have nderiv_ln_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_nderiv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nderiv[posreal](n, ln) =
--  IF n = 0 THEN ln
--  ELSE (LAMBDA (x: posreal): -factorial(n - 1) / (-x) ^ n)
--  ENDIF
theorem ln_nderiv : nderiv n ln = if n = 0 then ln
else (fun (x : {x : ℝ // 0 < x})=> -factorial (n - 1) / (-x) ^ n) := by
  -- === TCCs for ln_nderiv ===
  -- TCC: ln_nderiv_TCC1
  -- Statement: FORALL (n: nat): derivable_n_times?[posreal](ln, n)
  have ln_nderiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC2
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES n - 1 >= 0
  have ln_nderiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC3
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES FORALL (x: posreal): (-x) /= 0 OR n >= 0
  have ln_nderiv_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC1
  -- Statement: FORALL (n: nat): derivable_n_times?[posreal](ln, n)
  have ln_nderiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC2
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES n - 1 >= 0
  have ln_nderiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_nderiv_TCC3
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES FORALL (x: posreal): (-x) /= 0 OR n >= 0
  have ln_nderiv_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, n, (LAMBDA (nn: nat): IF nn = 0 THEN 0 ELSE -(-x) ^ nn / nn ENDIF))
axiom ln_estimate {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: continuous?((LAMBDA (t: posreal): (1 - t) ^ n / t))
theorem ln_estimate_scaf1 : continuous_p (fun (t : {x : {x : ℝ // x ≥ 0} // x > 0}) => (1 - t) ^ n / t) := by
  -- === TCCs for ln_estimate_scaf1 ===
  -- TCC: ln_estimate_scaf1_TCC1
  -- Statement: FORALL (n: nat, t: posreal): (1 - t) /= 0 OR n >= 0
  have ln_estimate_scaf1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: posreal):
--   Integrable?[posreal](1, x, (LAMBDA (t: posreal): (1 - t) ^ n / t))
theorem ln_estimate_scaf2 : ∀ (x : {x : {x : ℝ // x ≥ 0} // x > 0}), Integrable_p 1 x ((fun (t : {x : ℝ // 0 < x})=> (1 - t) ^ n / t)) := by
  -- === TCCs for ln_estimate_scaf2 ===
  -- TCC: ln_estimate_scaf2_TCC1
  -- Statement: connected?[posreal]
  have ln_estimate_scaf2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: posreal):
--   ln(x) = Integral(1, x, LAMBDA (t: posreal): (1 - t) ^ 0 / t)
theorem ln_estimate_scaf3 : ∀ (x : {x : {x : ℝ // x ≥ 0} // x > 0}), ln x = Integral 1 x (fun (t : {x : ℝ // 0 < x})=> (1 - t) ^ 0 / t) := by
  -- === TCCs for ln_estimate_scaf3 ===
  -- TCC: ln_estimate_scaf3_TCC1
  -- Statement: FORALL (t: posreal): (1 - t) /= 0 OR 0 >= 0
  have ln_estimate_scaf3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_estimate_scaf3_TCC2
  -- Statement: FORALL (x: posreal):
  --   Integrable?[posreal](1, x, LAMBDA (t: posreal): (1 - t) ^ 0 / t)
  have ln_estimate_scaf3_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf4
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= 1 IMPLIES
--  1 / (1 - x) = sigma(0, n, (LAMBDA (i: nat): x ^ i)) + x ^ (n + 1) / (1 - x)
theorem ln_estimate_scaf4 : x ≠ 1 → 1 / (1 - x) = Finset.sum (Finset.Icc 0 n) ((fun (i : ℕ)=> x ^ i)) + x ^ (n + 1) / (1 - x) := by
  -- === TCCs for ln_estimate_scaf4 ===
  -- TCC: ln_estimate_scaf4_TCC1
  -- Statement: FORALL (x: real): x /= 1 IMPLIES (1 - x) /= 0
  have ln_estimate_scaf4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_estimate_scaf4_TCC2
  -- Statement: FORALL (x: real): x /= 1 IMPLIES FORALL (i: nat): x /= 0 OR i >= 0
  have ln_estimate_scaf4_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_estimate_scaf4_TCC3
  -- Statement: FORALL (n: nat, x: real): x /= 1 IMPLIES x /= 0 OR (n + 1) >= 0
  have ln_estimate_scaf4_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf5
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 / nzx =
--  sigma(0, n, (LAMBDA (i: nat): (1 - nzx) ^ i)) + (1 - nzx) ^ (n + 1) / nzx
theorem ln_estimate_scaf5 : 1 / nzx = Finset.sum (Finset.Icc 0 n) ((fun (i : ℕ)=> (1 - nzx) ^ i)) + (1 - nzx) ^ (n + 1) / nzx := by
  -- === TCCs for ln_estimate_scaf5 ===
  -- TCC: ln_estimate_scaf5_TCC1
  -- Statement: FORALL (nzx: nzreal, i: nat): (1 - nzx) /= 0 OR i >= 0
  have ln_estimate_scaf5_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_estimate_scaf5_TCC2
  -- Statement: FORALL (n: nat, nzx: nzreal): (1 - nzx) /= 0 OR (n + 1) >= 0
  have ln_estimate_scaf5_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf6
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[posreal](LAMBDA px: ln_estimate(px - 1, n))
theorem ln_estimate_scaf6 : derivable_p (fun (px : {x : {x : ℝ // x ≥ 0} // x > 0}) => ln_estimate (px - 1) n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf7
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv[posreal](LAMBDA px: ln_estimate(px - 1, n + 1)) =
--  (LAMBDA px: sigma(0, n, (LAMBDA (i: nat): (1 - px) ^ i)))
theorem ln_estimate_scaf7 : deriv (fun px=>  ln_estimate (px - 1) (n + 1)) = (fun px=>  Finset.sum (Finset.Icc 0 n) ((fun (i : ℕ)=> (1 - px) ^ i))) := by
  -- === TCCs for ln_estimate_scaf7 ===
  -- TCC: ln_estimate_scaf7_TCC1
  -- Statement: FORALL (n: nat):
  --   derivable?[posreal](LAMBDA (px: posreal): ln_estimate(px - 1, n + 1))
  have ln_estimate_scaf7_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf8
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: ln(px) =
--  ln_estimate(px - 1, n + 1) +
--   Integral(1, px, LAMBDA (t: posreal): (1 - t) ^ (n + 1) / t)
theorem ln_estimate_scaf8 : ln px = ln_estimate (px - 1) (n + 1) +
 Integral 1 px (fun (t : {x : ℝ // 0 < x})=> (1 - t) ^ (n + 1) / t) := by
  -- === TCCs for ln_estimate_scaf8 ===
  -- TCC: ln_estimate_scaf8_TCC1
  -- Statement: FORALL (n: nat, t: posreal): (1 - t) /= 0 OR (n + 1) >= 0
  have ln_estimate_scaf8_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_estimate_scaf8_TCC2
  -- Statement: FORALL (n: nat, px: posreal):
  --   Integrable?[posreal](1, px, LAMBDA (t: posreal): (1 - t) ^ (n + 1) / t)
  have ln_estimate_scaf8_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf9
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: ln(px) =
--  ln_estimate(px - 1, n) +
--   Integral(1, px, LAMBDA (t: posreal): (1 - t) ^ n / t)
theorem ln_estimate_scaf9 : ln px = ln_estimate (px - 1) n +
 Integral 1 px (fun (t : {x : ℝ // 0 < x})=> (1 - t) ^ n / t) := by
  -- === TCCs for ln_estimate_scaf9 ===
  -- TCC: ln_estimate_scaf9_TCC1
  -- Statement: FORALL (n: nat, px: posreal):
  --   Integrable?[posreal](1, px, LAMBDA (t: posreal): (1 - t) ^ n / t)
  have ln_estimate_scaf9_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf10
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 <= px AND px <= 2 IMPLIES
--  abs(Integral(1, px, LAMBDA (t: posreal): (1 - t) ^ n / t)) <=
--   (px - 1) ^ (n + 1) / (n + 1)
theorem ln_estimate_scaf10 : 1 ≤ px ∧ px ≤ 2 → |Integral 1 px (fun (t : {x : ℝ // 0 < x})=> (1 - t) ^ n / t)| ≤
 (px - 1) ^ (n + 1) / (n + 1) := by
  -- === TCCs for ln_estimate_scaf10 ===
  -- TCC: ln_estimate_scaf10_TCC1
  -- Statement: FORALL (n: nat, px: posreal):
  --   px <= 2 AND 1 <= px IMPLIES (px - 1) /= 0 OR (n + 1) >= 0
  have ln_estimate_scaf10_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_scaf11
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: px < 1 IMPLIES
--  abs(Integral(1, px, LAMBDA (t: posreal): (1 - t) ^ n / t)) <=
--   (1 - px) ^ (n + 1) / ((n + 1) * px)
theorem ln_estimate_scaf11 : px < 1 → |Integral 1 px (fun (t : {x : ℝ // 0 < x})=> (1 - t) ^ n / t)| ≤
 (1 - px) ^ (n + 1) / ((n + 1) * px) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_estimate_bnd
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(ln(1 + z) - ln_estimate(z, n)) <=
--  abs(z) ^ (n + 1) / (IF z < 0 THEN 1 + z ELSE 1 ENDIF * (n + 1))
theorem ln_estimate_bnd : |ln (1 + z) - ln_estimate z n| ≤ |z| ^ (n + 1) / (if z < 0 then 1 + z else 1  * (n + 1)) := by
  -- === TCCs for ln_estimate_bnd ===
  -- TCC: ln_estimate_bnd_TCC1
  -- Statement: FORALL (z: real_gtm1_le1): 1 + z >= 0 AND 1 + z > 0
  have ln_estimate_bnd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_estimate_bnd_TCC2
  -- Statement: FORALL (n: nat, z: real_gtm1_le1): abs(z) /= 0 OR (n + 1) >= 0
  have ln_estimate_bnd_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_estimate_bnd_TCC3
  -- Statement: FORALL (n: nat, z: real_gtm1_le1):
  --   (IF z < 0 THEN 1 + z ELSE 1 ENDIF * (n + 1)) /= 0
  have ln_estimate_bnd_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.lnT
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: -(-x) ^ (n + 1) / (n + 1)
axiom lnT {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ30 → pvs_τ31

-- pvs2lean: ln_exp_series_alt.lnT_convergence
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(series(lnT(z)), ln(1 + z))
theorem lnT_convergence : convergence (series (lnT z)) (ln (1 + z)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.lnT_convergent
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(lnT(z)))
theorem lnT_convergent : convergent_p (series (lnT z)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_series_def
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: ln(1 + z) = inf_sum(lnT(z))
theorem ln_series_def : ln (1 + z) = inf_sum (lnT z) := by
  -- === TCCs for ln_series_def ===
  -- TCC: ln_series_def_TCC1
  -- Statement: FORALL (z: real_gtm1_le1): conv_series?(lnT(z))
  have ln_series_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.ln_taylors
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (c: between[{x: real | x > -1}](1, 1 + xgm1)):
--   ln(xgm1 + 1) = ln_estimate(xgm1, n) - (xgm1 / -c) ^ (n + 1) / (n + 1)
theorem ln_taylors : ∃ (c : {x : {x : ℝ // x > -1} // (1 < 1 + xgm1 → 1 < x ∧ x < 1 + xgm1) ∧
 (1 + xgm1 < 1 → 1 + xgm1 < x ∧ x < 1) ∧ (0 = xgm1 → x = 1)}), ln (xgm1 + 1) = ln_estimate xgm1 n - (xgm1 / -c) ^ (n + 1) / (n + 1) := by
  -- === TCCs for ln_taylors ===
  -- TCC: ln_taylors_TCC1
  -- Statement: connected?[{x: real | x > -1}]
  have ln_taylors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC2
  -- Statement: not_one_element?[{x: real | x > -1}]
  have ln_taylors_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC3
  -- Statement: FORALL (xgm1: {x: real | x > -1},
  --         c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   xgm1 + 1 >= 0 AND xgm1 + 1 > 0
  have ln_taylors_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC4
  -- Statement: FORALL (xgm1: {x: real | x > -1},
  --         c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   -c /= 0
  have ln_taylors_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC5
  -- Statement: FORALL (n: nat, xgm1: {x: real | x > -1},
  --         c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   (xgm1 / -c) /= 0 OR (n + 1) >= 0
  have ln_taylors_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC1
  -- Statement: connected?[{x: real | x > -1}]
  have ln_taylors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC2
  -- Statement: not_one_element?[{x: real | x > -1}]
  have ln_taylors_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC3
  -- Statement: FORALL (xgm1: Gt_m1, c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   xgm1 + 1 >= 0 AND xgm1 + 1 > 0
  have ln_taylors_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC4
  -- Statement: FORALL (xgm1: Gt_m1, c: between[{x: real | x > -1}](1, 1 + xgm1)): -c /= 0
  have ln_taylors_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ln_taylors_TCC5
  -- Statement: FORALL (n: nat, xgm1: Gt_m1, c: between[{x: real | x > -1}](1, 1 + xgm1)):
  --   (xgm1 / -c) /= 0 OR (n + 1) >= 0
  have ln_taylors_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.nderiv_exp
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable_n_times?(exp, n) AND nderiv(n, exp) = exp
theorem nderiv_exp : derivable_n_times_p exp n ∧ nderiv n exp = exp := by
  -- === TCCs for nderiv_exp ===
  -- TCC: nderiv_exp_TCC1
  -- Statement: deriv_domain?[real]
  have nderiv_exp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_exp_TCC2
  -- Statement: not_one_element?[real]
  have nderiv_exp_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.expT
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN 1 ELSE x ^ n / factorial(n) ENDIF
axiom expT {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20

-- pvs2lean: ln_exp_series_alt.exp_estimate
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, n, expT(x))
axiom exp_estimate {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ21 → pvs_τ22 → pvs_τ23

-- pvs2lean: ln_exp_series_alt.exp_taylors
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (c: between[real](0, x)):
--   exp(x) = exp_estimate(x, n) + exp(c) * x ^ (n + 1) / factorial(n + 1)
theorem exp_taylors : ∃ (c : {x_1 : ℝ // (0 < x → 0 < x_1 ∧ x_1 < x) ∧
 (x < 0 → x < x_1 ∧ x_1 < 0) ∧ (0 = x → x_1 = 0)}), exp x = exp_estimate x n + exp c * x ^ (n + 1) / factorial (n + 1) := by
  -- === TCCs for exp_taylors ===
  -- TCC: exp_taylors_TCC1
  -- Statement: FORALL (n: nat, x: real, c: between[real](0, x)): x /= 0 OR (n + 1) >= 0
  have exp_taylors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: exp_taylors_TCC1
  -- Statement: connected?[real]
  have exp_taylors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: exp_taylors_TCC2
  -- Statement: FORALL (n: nat, x: real, c: between[real](0, x)): x /= 0 OR (n + 1) >= 0
  have exp_taylors_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.exp_estimate_bnd
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(exp(x) - exp_estimate(x, n)) <=
--  max(exp(x), 1) * abs(x) ^ (n + 1) / factorial(n + 1)
theorem exp_estimate_bnd : |exp x - exp_estimate x n| ≤ max (exp x) 1 * |x| ^ (n + 1) / factorial (n + 1) := by
  -- === TCCs for exp_estimate_bnd ===
  -- TCC: exp_estimate_bnd_TCC1
  -- Statement: FORALL (n: nat, x: real): abs(x) /= 0 OR (n + 1) >= 0
  have exp_estimate_bnd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.exp_series_scaf2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sqrt(n) ^ (2 * n) <= factorial(2 * n)
theorem exp_series_scaf2 : Real.sqrt (n) ^ (2 * n) ≤ factorial (2 * n) := by
  -- === TCCs for exp_series_scaf2 ===
  -- TCC: exp_series_scaf2_TCC1
  -- Statement: FORALL (n: nat): sqrt(n) /= 0 OR (2 * n) >= 0
  have exp_series_scaf2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.exp_series_scaf3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sqrt(n) ^ (2 * n + 1) <= factorial(2 * n + 1)
theorem exp_series_scaf3 : Real.sqrt (n) ^ (2 * n + 1) ≤ factorial (2 * n + 1) := by
  -- === TCCs for exp_series_scaf3 ===
  -- TCC: exp_series_scaf3_TCC1
  -- Statement: FORALL (n: nat): sqrt(n) /= 0 OR (2 * n + 1) >= 0
  have exp_series_scaf3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.expT_convergence
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(series(expT(x)), exp(x))
theorem expT_convergence : convergence (series (expT x)) (exp x) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.expT_convergent
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(expT(x)))
theorem expT_convergent : convergent_p (series (expT x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_series_alt.exp_series
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: exp(x) = inf_sum(expT(x))
theorem exp_series : exp x = inf_sum (expT x) := by
  -- === TCCs for exp_series ===
  -- TCC: exp_series_TCC1
  -- Statement: conv_powerseries?[real](exp_seq)
  have exp_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: exp_series_TCC1
  -- Statement: FORALL (x: real): conv_series?(expT(x))
  have exp_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.ln_exp_series_alt