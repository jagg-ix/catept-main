-- Auto-generated by pvs2lean/translator.py
-- PVS theory: expt
-- PVSLib library: lnexp
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: expt

Auto-generated stubs from PVSLib theory `expt`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.expt

noncomputable section

variable {pvs_τ1 : Type*} (continuous_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (exp : pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (ln : pvs_τ4 → pvs_τ5)

-- pvs2lean: expt.^^
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF a = 0 THEN 0 ELSE exp(x * ln(a)) ENDIF
noncomputable def pvs_op_ea0166 : sorry := by
  sorry  -- pvs2lean: definition stub for IF a = 0 THEN 0 ELSE exp(x * ln(a)) ENDIF

-- pvs2lean: expt.hathat_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a ^^ (n + m) = (a ^^ n) * (a ^^ m)
theorem hathat_sum : a ^^ (n + m) = (a ^^ n) * (a ^^ m) := by
  -- === TCCs for hathat_sum ===
  -- TCC: hathat_sum_TCC1
  -- Statement: FORALL (a: posreal, m, n: nat): a /= 0 OR (n + m) /= 0
  have hathat_sum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: hathat_sum_TCC2
  -- Statement: FORALL (a: posreal, n: nat): a /= 0 OR n /= 0
  have hathat_sum_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_diff
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a ^^ (n - m) = (a ^^ n) / (a ^^ m)
theorem hathat_diff : a ^^ (n - m) = (a ^^ n) / (a ^^ m) := by
  -- === TCCs for hathat_diff ===
  -- TCC: hathat_diff_TCC1
  -- Statement: FORALL (a: posreal, m, n: nat): a /= 0 OR (n - m) /= 0
  have hathat_diff_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: hathat_diff_TCC2
  -- Statement: FORALL (a: posreal, m: nat): (a ^^ m) /= 0
  have hathat_diff_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_to_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: px ^^ 0 = 1
theorem hathat_to_0 : px ^^ 0 = 1 := by
  -- === TCCs for hathat_to_0 ===
  -- TCC: hathat_to_0_TCC1
  -- Statement: FORALL (px: posreal): px /= 0 OR 0 /= 0
  have hathat_to_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_to_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a ^^ 1 = a
theorem hathat_to_1 : a ^^ 1 = a := by
  -- === TCCs for hathat_to_1 ===
  -- TCC: hathat_to_1_TCC1
  -- Statement: FORALL (a: posreal): a /= 0 OR 1 /= 0
  have hathat_to_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 ^^ nzx = 0
theorem hathat_0 : 0 ^^ nzx = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 ^^ x = 1
theorem hathat_1 : 1 ^^ x = 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_nat
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a ^^ n = a ^ n
theorem hathat_nat : a ^^ n = a ^ n := by
  -- === TCCs for hathat_nat ===
  -- TCC: hathat_nat_TCC1
  -- Statement: FORALL (a: posreal, n: nat): a /= 0 OR n >= 0
  have hathat_nat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_lt_cross
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a ^^ (1 / px) < b IMPLIES a < b ^^ px
theorem hathat_lt_cross : a ^^ (1 / px) < b → a < b ^^ px := by
  -- === TCCs for hathat_lt_cross ===
  -- TCC: hathat_lt_cross_TCC1
  -- Statement: FORALL (a, px: posreal): a /= 0 OR (1 / px) /= 0
  have hathat_lt_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: hathat_lt_cross_TCC2
  -- Statement: FORALL (a, b, px: posreal): a ^^ (1 / px) < b IMPLIES b /= 0 OR px /= 0
  have hathat_lt_cross_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_gt_cross
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a ^^ (1 / px) > b IMPLIES a > b ^^ px
theorem hathat_gt_cross : a ^^ (1 / px) > b → a > b ^^ px := by
  -- === TCCs for hathat_gt_cross ===
  -- TCC: hathat_gt_cross_TCC1
  -- Statement: FORALL (a, b, px: posreal): a ^^ (1 / px) > b IMPLIES b /= 0 OR px /= 0
  have hathat_gt_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_eq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a ^^ nzx = 0 IFF a = 0
theorem hathat_eq_0 : a ^^ nzx = 0 ↔ a = 0 := by
  -- === TCCs for hathat_eq_0 ===
  -- TCC: hathat_eq_0_TCC1
  -- Statement: FORALL (a: posreal, nzx: nzreal): a /= 0 OR nzx /= 0
  have hathat_eq_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_eq_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: px ^^ x = 1 IFF (x = 0 OR px = 1)
theorem hathat_eq_1 : px ^^ x = 1 ↔ (x = 0 ∨ px = 1) := by
  -- === TCCs for hathat_eq_1 ===
  -- TCC: hathat_eq_1_TCC1
  -- Statement: FORALL (px: posreal, x: real): px /= 0 OR x /= 0
  have hathat_eq_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_cross
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a ^^ (1 / px) = b IMPLIES a = b ^^ px
theorem hathat_cross : a ^^ (1 / px) = b → a = b ^^ px := by
  -- === TCCs for hathat_cross ===
  -- TCC: hathat_cross_TCC1
  -- Statement: FORALL (a, b, px: posreal): a ^^ (1 / px) = b IMPLIES b /= 0 OR px /= 0
  have hathat_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (a ^^ px) ^^ py = a ^^ (px * py)
theorem hathat_mult : (a ^^ px) ^^ py = a ^^ (px * py) := by
  -- === TCCs for hathat_mult ===
  -- TCC: hathat_mult_TCC1
  -- Statement: FORALL (a, px: posreal): a /= 0 OR px /= 0
  have hathat_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: hathat_mult_TCC2
  -- Statement: FORALL (a, px, py: posreal): (a ^^ px) /= 0 OR py /= 0
  have hathat_mult_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: hathat_mult_TCC3
  -- Statement: FORALL (a, px, py: posreal): a /= 0 OR (px * py) /= 0
  have hathat_mult_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_div
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (a / px) ^^ x = a ^^ x / px ^^ x
theorem hathat_div : (a / px) ^^ x = a ^^ x / px ^^ x := by
  -- === TCCs for hathat_div ===
  -- TCC: hathat_div_TCC1
  -- Statement: FORALL (a, px: posreal, x: real): (a / px) /= 0 OR x /= 0
  have hathat_div_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: hathat_div_TCC2
  -- Statement: FORALL (px: posreal, x: real): px ^^ x /= 0
  have hathat_div_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(a ^^ x) = abs(a) ^^ x
theorem hathat_abs : |a ^^ x| = |a| ^^ x := by
  -- === TCCs for hathat_abs ===
  -- TCC: hathat_abs_TCC1
  -- Statement: FORALL (a: posreal, x: real): abs(a) /= 0 OR x /= 0
  have hathat_abs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_sum_posreal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (a ^^ px) * (a ^^ py) = a ^^ (px + py)
theorem hathat_sum_posreal : (a ^^ px) * (a ^^ py) = a ^^ (px + py) := by
  -- === TCCs for hathat_sum_posreal ===
  -- TCC: hathat_sum_posreal_TCC1
  -- Statement: FORALL (a, px, py: posreal): a /= 0 OR (px + py) /= 0
  have hathat_sum_posreal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_diff_posreal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (a ^^ px) / (a ^^ py) = a ^^ (px - py)
theorem hathat_diff_posreal : (a ^^ px) / (a ^^ py) = a ^^ (px - py) := by
  -- === TCCs for hathat_diff_posreal ===
  -- TCC: hathat_diff_posreal_TCC1
  -- Statement: FORALL (a, py: posreal): (a ^^ py) /= 0
  have hathat_diff_posreal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: hathat_diff_posreal_TCC2
  -- Statement: FORALL (a, px, py: posreal): a /= 0 OR (px - py) /= 0
  have hathat_diff_posreal_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: expt.hathat_cont
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: continuous?(LAMBDA x: a ^^ x)
theorem hathat_cont : continuous_p (fun (x : ℝ) => a ^^ x) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.expt