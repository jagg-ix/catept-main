-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sigma_lemmas
-- PVSLib library: linear_algebra
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sigma_lemmas

Auto-generated stubs from PVSLib theory `sigma_lemmas`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sigma_lemmas

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Index : pvs_τ1 → pvs_τ2)

-- pvs2lean: sigma_lemmas.sigma_restrict_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_restrict_dom : True := by
  -- === TCCs for sigma_restrict_dom ===
  -- TCC: sigma_restrict_dom_TCC1
  -- Statement: FORALL (m: posnat, i: below[m]): i < 1 + m
  have sigma_restrict_dom_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom_TCC2
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    0 >= 0 AND 0 < m OR EXISTS (j: below[m]): j <= 0
  have sigma_restrict_dom_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom_TCC3
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    j - 1 >= 0 AND j - 1 < m OR EXISTS (j_1: below[m...
  have sigma_restrict_dom_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom_TCC4
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    (FORALL (x, y: below[m]), (z: integer):
  --       x ...
  have sigma_restrict_dom_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom_TCC5
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    0 >= 0 AND 0 < 1 + m OR EXISTS (j: below[1 + m])...
  have sigma_restrict_dom_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom_TCC6
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    j - 1 >= 0 AND j - 1 < 1 + m OR EXISTS (j_1: bel...
  have sigma_restrict_dom_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom_TCC7
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    (FORALL (x, y: below[1 + m]), (z: integer):
  --     ...
  have sigma_restrict_dom_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.sigma_restrict_dom2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_restrict_dom2 : True := by
  -- === TCCs for sigma_restrict_dom2 ===
  -- TCC: sigma_restrict_dom2_TCC1
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j < m IMPLIES
  --    1 >= 0 AND 1 < m OR EXISTS (j: below[m]): j <= 1
  have sigma_restrict_dom2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom2_TCC2
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j < m IMPLIES
  --    j >= 0 AND j < m OR EXISTS (j_1: below[m]): j <= j_1
  have sigma_restrict_dom2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom2_TCC3
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j < m IMPLIES
  --    (FORALL (x, y: below[m]), (z: integer):
  --       x <...
  have sigma_restrict_dom2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom2_TCC4
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j < m IMPLIES
  --    1 >= 0 AND 1 < 1 + m OR EXISTS (j: below[1 + m]):...
  have sigma_restrict_dom2_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom2_TCC5
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j < m IMPLIES
  --    j >= 0 AND j < 1 + m OR EXISTS (j_1: below[1 + m]...
  have sigma_restrict_dom2_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_dom2_TCC6
  -- Statement: FORALL (j: posnat, m: posnat, F: [below[m] -> real],
  --         G: [below[m + 1] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j < m IMPLIES
  --    (FORALL (x, y: below[1 + m]), (z: integer):
  --      ...
  have sigma_restrict_dom2_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.sigma_below_shift
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_below_shift : True := by
  -- === TCCs for sigma_below_shift ===
  -- TCC: sigma_below_shift_TCC1
  -- Statement: FORALL (m: posnat): 1 >= 0 AND 1 < 1 + m OR EXISTS (j: below[1 + m]): j <= 1
  have sigma_below_shift_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_below_shift_TCC2
  -- Statement: FORALL (m: posnat): m >= 0 AND m < 1 + m OR EXISTS (j: below[1 + m]): m <= j
  have sigma_below_shift_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_below_shift_TCC3
  -- Statement: FORALL (m: posnat):
  --   (FORALL (x, y: below[1 + m]), (z: integer):
  --      x <= z AND z <= y IMPLIES z >= 0 AND z < 1 + m)
  have sigma_below_shift_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_below_shift_TCC4
  -- Statement: FORALL (m: posnat): 0 >= 0 AND 0 < m OR EXISTS (j: below[m]): j <= 0
  have sigma_below_shift_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_below_shift_TCC5
  -- Statement: FORALL (m: posnat):
  --   m - 1 >= 0 AND m - 1 < m OR EXISTS (j: below[m]): m - 1 <= j
  have sigma_below_shift_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_below_shift_TCC6
  -- Statement: FORALL (m: posnat, i: below[m]): i + 1 < 1 + m
  have sigma_below_shift_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_below_shift_TCC7
  -- Statement: FORALL (m: posnat):
  --   (FORALL (x, y: below[m]), (z: integer):
  --      x <= z AND z <= y IMPLIES z >= 0 AND z < m)
  have sigma_below_shift_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.sigma_eq_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_eq_index : True := by
  -- === TCCs for sigma_eq_index ===
  -- TCC: sigma_eq_index_TCC1
  -- Statement: FORALL (m, n: posnat): m = n IMPLIES FORALL (i: below[m]): i < n
  have sigma_eq_index_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_eq_index_TCC2
  -- Statement: FORALL (m, n: posnat, F: [below[m] -> real], G: [below[n] -> real]):
  --   (m = n AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    0 >= 0 AND 0 < n OR EXISTS (j: Index[n]): j <= 0
  have sigma_eq_index_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_eq_index_TCC3
  -- Statement: FORALL (m, n: posnat, F: [below[m] -> real], G: [below[n] -> real]):
  --   (m = n AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    n - 1 >= 0 AND n - 1 < n OR EXISTS (j: Index[n]): n - 1 <= j
  have sigma_eq_index_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_eq_index_TCC4
  -- Statement: FORALL (m, n: posnat, F: [below[m] -> real], G: [below[n] -> real]):
  --   (m = n AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    (FORALL (x, y: Index[n]), (z: integer):
  --       x <= z AND z <= y IMPLI...
  have sigma_eq_index_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.aux_sigma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem aux_sigma : True := by
  -- === TCCs for aux_sigma ===
  -- TCC: aux_sigma_TCC1
  -- Statement: FORALL (m: posnat, n: nat): n = 0 IMPLIES FORALL (i: below[m]): i < m + n
  have aux_sigma_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: aux_sigma_TCC2
  -- Statement: FORALL (m: posnat, n: nat, F: [below[m] -> real],
  --         G: [below[m + n] -> real]):
  --   (n = 0 AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    0 >= 0 AND 0 < m + n OR EXISTS (j: below[m + n]): j ...
  have aux_sigma_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: aux_sigma_TCC3
  -- Statement: FORALL (m: posnat, n: nat, F: [below[m] -> real],
  --         G: [below[m + n] -> real]):
  --   (n = 0 AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    m + n - 1 >= 0 AND m + n - 1 < m + n OR
  --     EXISTS (...
  have aux_sigma_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: aux_sigma_TCC4
  -- Statement: FORALL (m: posnat, n: nat, F: [below[m] -> real],
  --         G: [below[m + n] -> real]):
  --   (n = 0 AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    (FORALL (x, y: below[m + n]), (z: integer):
  --       x ...
  have aux_sigma_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.aux_sigma2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem aux_sigma2 : True := by
  -- === TCCs for aux_sigma2 ===
  -- TCC: aux_sigma2_TCC1
  -- Statement: FORALL (m, j: posnat, n: nat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (n = 0 AND j <= m AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    j - 1 >= 0 AND j - 1 < m OR EXISTS (j_...
  have aux_sigma2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: aux_sigma2_TCC2
  -- Statement: FORALL (m, j: posnat, n: nat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (n = 0 AND j <= m AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    0 >= 0 AND 0 < m + n OR EXISTS (j: bel...
  have aux_sigma2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: aux_sigma2_TCC3
  -- Statement: FORALL (m, j: posnat, n: nat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (n = 0 AND j <= m AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    j + n - 1 >= 0 AND j + n - 1 < m + n O...
  have aux_sigma2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: aux_sigma2_TCC4
  -- Statement: FORALL (m, j: posnat, n: nat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (n = 0 AND j <= m AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    FORALL (x, y: below[n + m]), (z: integ...
  have aux_sigma2_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.sigma_restrict_gen
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_restrict_gen : True := by
  -- === TCCs for sigma_restrict_gen ===
  -- TCC: sigma_restrict_gen_TCC1
  -- Statement: FORALL (m, n: posnat, i: below[m]): i < m + n
  have sigma_restrict_gen_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_gen_TCC2
  -- Statement: FORALL (j: posnat, m, n: posnat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    j - 1 >= 0 AND j - 1 < m OR EXISTS (j_1: belo...
  have sigma_restrict_gen_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_gen_TCC3
  -- Statement: FORALL (j: posnat, m, n: posnat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    0 >= 0 AND 0 < m + n OR EXISTS (j: below[n + ...
  have sigma_restrict_gen_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_gen_TCC4
  -- Statement: FORALL (j: posnat, m, n: posnat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    j - 1 >= 0 AND j - 1 < m + n OR EXISTS (j_1: ...
  have sigma_restrict_gen_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_gen_TCC5
  -- Statement: FORALL (j: posnat, m, n: posnat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m IMPLIES
  --    FORALL (x, y: below[n + m]), (z: integer):
  --   ...
  have sigma_restrict_gen_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.sigma_eq_index2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_eq_index2 : True := by
  -- === TCCs for sigma_eq_index2 ===
  -- TCC: sigma_eq_index2_TCC1
  -- Statement: FORALL (m, n, j: posnat, F: [below[m] -> real], G: [below[n] -> real]):
  --   (m = n AND j <= m AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    j - 1 >= 0 AND j - 1 < m OR EXISTS (j_1: Index[m]): j -...
  have sigma_eq_index2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_eq_index2_TCC2
  -- Statement: FORALL (m, n, j: posnat, F: [below[m] -> real], G: [below[n] -> real]):
  --   (m = n AND j <= m AND FORALL (i: below[m]): F(i) = G(i)) IMPLIES
  --    j - 1 >= 0 AND j - 1 < n OR EXISTS (j_1: Index[n]): j -...
  have sigma_eq_index2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.sigma_restrict_gen2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_restrict_gen2 : True := by
  -- === TCCs for sigma_restrict_gen2 ===
  -- TCC: sigma_restrict_gen2_TCC1
  -- Statement: FORALL (j: posnat, k: nat, m, n: posnat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m AND k < j IMPLIES
  --    k >= 0 AND k < m OR EXISTS ...
  have sigma_restrict_gen2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_restrict_gen2_TCC2
  -- Statement: FORALL (j: posnat, k: nat, m, n: posnat, F: [below[m] -> real],
  --         G: [below[n + m] -> real]):
  --   (FORALL (i: below[m]): F(i) = G(i)) AND j <= m AND k < j IMPLIES
  --    k >= 0 AND k < m + n OR EXI...
  have sigma_restrict_gen2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.sigma_eq_index3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_eq_index3 : True := by
  -- === TCCs for sigma_eq_index3 ===
  -- TCC: sigma_eq_index3_TCC1
  -- Statement: FORALL (m, n, j, k: posnat, F: [below[m] -> real], G: [below[n] -> real]):
  --   (m = n AND j <= m AND k < j AND FORALL (i: below[m]): F(i) = G(i))
  --    IMPLIES k >= 0 AND k < m OR EXISTS (j: Index[m]): ...
  have sigma_eq_index3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_eq_index3_TCC2
  -- Statement: FORALL (m, n, j, k: posnat, F: [below[m] -> real], G: [below[n] -> real]):
  --   (m = n AND j <= m AND k < j AND FORALL (i: below[m]): F(i) = G(i))
  --    IMPLIES k >= 0 AND k < n OR EXISTS (j: Index[n]): ...
  have sigma_eq_index3_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_lemmas.sigma_shift_gen
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_shift_gen : True := by
  -- === TCCs for sigma_shift_gen ===
  -- TCC: sigma_shift_gen_TCC1
  -- Statement: FORALL (j: posnat, m, n: posnat):
  --   j <= m IMPLIES
  --    j - 1 >= 0 AND j - 1 < m OR EXISTS (j_1: below[m]): j - 1 <= j_1
  have sigma_shift_gen_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_shift_gen_TCC2
  -- Statement: FORALL (j: posnat, m, n: posnat):
  --   j <= m IMPLIES FORALL (i: below[m]): i + n < m + n
  have sigma_shift_gen_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_shift_gen_TCC3
  -- Statement: FORALL (j: posnat, m, n: posnat):
  --   j <= m IMPLIES n >= 0 AND n < m + n OR EXISTS (j: below[n + m]): j <= n
  have sigma_shift_gen_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_shift_gen_TCC4
  -- Statement: FORALL (j: posnat, m, n: posnat):
  --   j <= m IMPLIES
  --    j - 1 + n >= 0 AND j - 1 + n < m + n OR
  --     EXISTS (j_1: below[n + m]): j - 1 + n <= j_1
  have sigma_shift_gen_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_shift_gen_TCC5
  -- Statement: FORALL (j: posnat, m, n: posnat):
  --   j <= m IMPLIES
  --    FORALL (x, y: below[n + m]), (z: integer):
  --      x <= z AND z <= y IMPLIES z >= 0 AND z < m + n
  have sigma_shift_gen_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.sigma_lemmas