-- Auto-generated by pvs2lean/translator.py
-- PVS theory: elementary_matrices
-- PVSLib library: linear_algebra
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: elementary_matrices

Auto-generated stubs from PVSLib theory `elementary_matrices`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.elementary_matrices

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (ELSIF : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (ElemMat : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (ElemMat1 : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (ElemMat2 : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (ElemMat3 : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (Mat : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (append : pvs_τ15 → pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (below : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (car : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (cdr : pvs_τ22 → pvs_τ23)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} (every : pvs_τ57 → pvs_τ58)
variable {pvs_τ59 : Type*} (invertible_p : pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (length : pvs_τ60 → pvs_τ61)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} (matrix : pvs_τ62 → pvs_τ63 → pvs_τ64)
variable {pvs_τ67 : Type*} (null_p : pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} (rowV : pvs_τ68 → pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} (squareMat_p : pvs_τ71 → pvs_τ72 → Prop)
variable {pvs_τ73 : Type*} (square_p : pvs_τ73 → Prop)

-- pvs2lean: elementary_matrices.zero_row?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom zero_row_p {pvs_τ74 : Type*} {pvs_τ75 : Type*} : pvs_τ74 → pvs_τ75 → Prop

-- pvs2lean: elementary_matrices.nonzero_row?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom nonzero_row_p {pvs_τ65 : Type*} {pvs_τ66 : Type*} : pvs_τ65 → pvs_τ66 → Prop

-- pvs2lean: elementary_matrices.zero_or_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem zero_or_nonzero : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.zero_isnt_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem zero_isnt_nonzero : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.nonzero_isnt_zero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem nonzero_isnt_zero : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elemM1?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemM1_p {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: elementary_matrices.elemM2?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemM2_p {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ36 → pvs_τ37 → pvs_τ38 → Prop

-- pvs2lean: elementary_matrices.elemM3?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemM3_p {pvs_τ43 : Type*} {pvs_τ44 : Type*} : pvs_τ43 → pvs_τ44 → Prop

-- pvs2lean: elementary_matrices.elemM?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemM_p {pvs_τ45 : Type*} : pvs_τ45 → Prop

-- pvs2lean: elementary_matrices.elemMat1?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemMat1_p {pvs_τ46 : Type*} {pvs_τ47 : Type*} : pvs_τ46 → pvs_τ47 → Prop

-- pvs2lean: elementary_matrices.elemMat2?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemMat2_p {pvs_τ48 : Type*} {pvs_τ49 : Type*} : pvs_τ48 → pvs_τ49 → Prop

-- pvs2lean: elementary_matrices.elemMat3?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemMat3_p {pvs_τ50 : Type*} {pvs_τ51 : Type*} : pvs_τ50 → pvs_τ51 → Prop

-- pvs2lean: elementary_matrices.elemMat?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemMat_p {pvs_τ52 : Type*} {pvs_τ53 : Type*} : pvs_τ52 → pvs_τ53 → Prop

-- pvs2lean: elementary_matrices.elemM1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemM1 {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28

-- pvs2lean: elementary_matrices.elemM2
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemM2 {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35

-- pvs2lean: elementary_matrices.elemM3
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elemM3 {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41 → pvs_τ42

-- pvs2lean: elementary_matrices.elemM1_invertible
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elemM1_invertible : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elemM2_invertible
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elemM2_invertible : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elemM3_invertible
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elemM3_invertible : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elemM1_prop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elemM1_prop : True := by
  -- === TCCs for elemM1_prop ===
  -- TCC: elemM1_prop_TCC1
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j, a: real,
  --         k: below(M`rows)):
  --   k < (elemM1(M`rows)(i, j)(a) * M)`rows
  have elemm1_prop_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM1_prop_TCC2
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j, a: real,
  --         k: below(M`rows)):
  --   elemM1(M`rows)(i, j)(a)`cols = M`rows
  have elemm1_prop_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM1_prop_TCC3
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j, a: real,
  --         k: below(M`rows)):
  --   k = i IMPLIES
  --    FORALL (x: nat): x < M`cols IFF x < (elemM1(M`rows)(i, j)(a) * M)`cols
  have elemm1_prop_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM1_prop_TCC4
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j, a: real,
  --         k: below(M`rows)):
  --   NOT k = i IMPLIES
  --    FORALL (x: nat): x < M`cols IFF x < (elemM1(M`rows)(i, j)(a) * M)`cols
  have elemm1_prop_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elemM2_prop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elemM2_prop : True := by
  -- === TCCs for elemM2_prop ===
  -- TCC: elemM2_prop_TCC1
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j,
  --         k: below(M`rows)):
  --   k < (elemM2(M`rows)(i, j) * M)`rows
  have elemm2_prop_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM2_prop_TCC2
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j,
  --         k: below(M`rows)):
  --   elemM2(M`rows)(i, j)`cols = M`rows
  have elemm2_prop_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM2_prop_TCC3
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j,
  --         k: below(M`rows)):
  --   k = i IMPLIES
  --    FORALL (x: nat): x < M`cols IFF x < (elemM2(M`rows)(i, j) * M)`cols
  have elemm2_prop_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM2_prop_TCC4
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j,
  --         k: below(M`rows)):
  --   NOT k = i AND k = j IMPLIES
  --    FORALL (x: nat): x < M`cols IFF x < (elemM2(M`rows)(i, j) * M)`cols
  have elemm2_prop_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM2_prop_TCC5
  -- Statement: FORALL (M: Matrix, i: below(M`rows), j: below(M`rows) | i /= j,
  --         k: below(M`rows)):
  --   NOT k = i AND NOT k = j IMPLIES
  --    FORALL (x: nat): x < M`cols IFF x < (elemM2(M`rows)(i, j) * M)`cols
  have elemm2_prop_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elemM3_prop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elemM3_prop : True := by
  -- === TCCs for elemM3_prop ===
  -- TCC: elemM3_prop_TCC1
  -- Statement: FORALL (M: Matrix, i: below(M`rows), c: nzreal, k: below(M`rows)):
  --   k < (elemM3(M`rows)(i)(c) * M)`rows
  have elemm3_prop_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM3_prop_TCC2
  -- Statement: FORALL (M: Matrix, i: below(M`rows), c: nzreal, k: below(M`rows)):
  --   elemM3(M`rows)(i)(c)`cols = M`rows
  have elemm3_prop_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM3_prop_TCC3
  -- Statement: FORALL (M: Matrix, i: below(M`rows), c: nzreal, k: below(M`rows)):
  --   k = i IMPLIES
  --    FORALL (x: nat): x < M`cols IFF x < (elemM3(M`rows)(i)(c) * M)`cols
  have elemm3_prop_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elemM3_prop_TCC4
  -- Statement: FORALL (M: Matrix, i: below(M`rows), c: nzreal, k: below(M`rows)):
  --   NOT k = i IMPLIES
  --    FORALL (x: nat): x < M`cols IFF x < (elemM3(M`rows)(i)(c) * M)`cols
  have elemm3_prop_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elem_prod
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elem_prod : True := by
  -- === TCCs for elem_prod ===
  -- TCC: elem_prod_TCC1
  -- Statement: FORALL (M: Matrix, e: ElemMat(M`rows)): e`cols = M`rows
  have elem_prod_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elem_product_left
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom elem_product_left {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} : pvs_τ54 → pvs_τ55 → pvs_τ56

-- pvs2lean: elementary_matrices.elem_prod_append_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elem_prod_append_aux : True := by
  -- === TCCs for elem_prod_append_aux ===
  -- TCC: elem_prod_append_aux_TCC1
  -- Statement: FORALL (n: posnat, M: Matrix | M`rows = n, el1: list[ElemMat(n)]):
  --   every[Matrix]
  --       (LAMBDA (x: Matrix):
  --          square?(x) AND squareMat?(M`rows)(x) AND elemMat?(M`rows)(x))
  --       (el1)
  have elem_prod_append_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elem_prod_append_aux_TCC2
  -- Statement: FORALL (n: posnat, M: Matrix | M`rows = n, el1, el2: list[ElemMat(n)]):
  --   every[Matrix]
  --       (LAMBDA (x: Matrix):
  --          square?(x) AND
  --           squareMat?(elem_product_left(M, el1)`rows)(x) AN...
  have elem_prod_append_aux_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elem_prod_append_aux_TCC3
  -- Statement: FORALL (n: posnat, M: Matrix | M`rows = n, el1, el2: list[ElemMat(n)]):
  --   every[Matrix]
  --       (LAMBDA (x: Matrix):
  --          square?(x) AND squareMat?(M`rows)(x) AND elemMat?(M`rows)(x))
  --       (appe...
  have elem_prod_append_aux_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elem_prod_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elem_prod_append : True := by
  -- === TCCs for elem_prod_append ===
  -- TCC: elem_prod_append_TCC1
  -- Statement: FORALL (M: Matrix, el1, el2: list[ElemMat(M`rows)]):
  --   every[Matrix]
  --       (LAMBDA (x: Matrix):
  --          square?(x) AND
  --           squareMat?(elem_product_left(M, el1)`rows)(x) AND
  --            elemMa...
  have elem_prod_append_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elem_prod_append_TCC2
  -- Statement: FORALL (M: Matrix, el1, el2: list[ElemMat(M`rows)]):
  --   every[Matrix]
  --       (LAMBDA (x: Matrix):
  --          square?(x) AND squareMat?(M`rows)(x) AND elemMat?(M`rows)(x))
  --       (append[Matrix](el1, el2))
  have elem_prod_append_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: elementary_matrices.elem_prod_append1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem elem_prod_append1 : True := by
  -- === TCCs for elem_prod_append1 ===
  -- TCC: elem_prod_append1_TCC1
  -- Statement: FORALL (M: Matrix, M1: Matrix | M1`rows = M`rows AND M1`cols = M`cols,
  --         e: ElemMat(M`rows), el: list[ElemMat(M`rows)]):
  --   elem_product_left(M, el) = M1 IMPLIES
  --    every[Matrix]
  --        (LAMBD...
  have elem_prod_append1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: elem_prod_append1_TCC2
  -- Statement: FORALL (M: Matrix, M1: Matrix | M1`rows = M`rows AND M1`cols = M`cols,
  --         e: ElemMat(M`rows), el: list[ElemMat(M`rows)]):
  --   elem_product_left(M, el) = M1 IMPLIES e`cols = M1`rows
  have elem_prod_append1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.elementary_matrices