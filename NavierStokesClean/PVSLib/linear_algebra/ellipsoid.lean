-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ellipsoid
-- PVSLib library: linear_algebra
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ellipsoid

Auto-generated stubs from PVSLib theory `ellipsoid`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ellipsoid

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (SquareMat : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (bijective_p : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (inv : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (squareMat_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} (square_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (transpose : pvs_τ11 → pvs_τ12)

-- pvs2lean: ellipsoid.ellipsoid
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem ellipsoid : True := by
  -- === TCCs for ellipsoid ===
  -- TCC: ellipsoid_TCC1
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n)): Q`rows = n AND Q`cols = n
  have ellipsoid_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC2
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n)):
  --   bijective?(n)(T(n, n)(Q)) IMPLIES M`rows = n AND M`cols = n
  have ellipsoid_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC3
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n)):
  --   bijective?(n)(T(n, n)(Q)) AND bijective?(n)(T(n, n)(M)) IMPLIES
  --    squareMat?(n)(Q) AND bijective?(n)(T(n, n)(Q))
  have ellipsoid_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC4
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n)):
  --   bijective?(n)(T(n, n)(Q)) AND bijective?(n)(T(n, n)(M)) IMPLIES
  --    FORALL (x2: nat): x2 < n IFF x2 < inv(n)(Q)`cols
  have ellipsoid_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC5
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n)):
  --   bijective?(n)(T(n, n)(Q)) AND bijective?(n)(T(n, n)(M)) IMPLIES
  --    FORALL (x2: nat): x2 < inv(n)(Q)`rows IFF x2 < n
  have ellipsoid_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC6
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n), x, c: Vector[n]):
  --   bijective?(n)(T(n, n)(Q)) AND
  --    bijective?(n)(T(n, n)(M)) AND (x - c) * (inv(n)(Q) * (x - c)) <= 1
  --    IMPLIES FORALL (x2: nat): x2 < n IF...
  have ellipsoid_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC7
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n), x, c: Vector[n]):
  --   bijective?(n)(T(n, n)(Q)) AND
  --    bijective?(n)(T(n, n)(M)) AND (x - c) * (inv(n)(Q) * (x - c)) <= 1
  --    IMPLIES FORALL (x2: nat): x2 < n IF...
  have ellipsoid_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC8
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n), x, c: Vector[n]):
  --   bijective?(n)(T(n, n)(Q)) AND
  --    bijective?(n)(T(n, n)(M)) AND (x - c) * (inv(n)(Q) * (x - c)) <= 1
  --    IMPLIES FORALL (x2: nat): x2 < M`ro...
  have ellipsoid_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC9
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n), x, y, b, c: Vector[n]):
  --   bijective?(n)(T(n, n)(Q)) AND
  --    bijective?(n)(T(n, n)(M)) AND
  --     (x - c) * (inv(n)(Q) * (x - c)) <= 1 AND y = M * x + b
  --    IMPLIES...
  have ellipsoid_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC10
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n), x, y, b, c: Vector[n]):
  --   bijective?(n)(T(n, n)(Q)) AND
  --    bijective?(n)(T(n, n)(M)) AND
  --     (x - c) * (inv(n)(Q) * (x - c)) <= 1 AND y = M * x + b
  --    IMPLIES...
  have ellipsoid_tcc10 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC11
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n), x, y, b, c: Vector[n]):
  --   bijective?(n)(T(n, n)(Q)) AND
  --    bijective?(n)(T(n, n)(M)) AND
  --     (x - c) * (inv(n)(Q) * (x - c)) <= 1 AND y = M * x + b
  --    IMPLIES...
  have ellipsoid_tcc11 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC12
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n), x, y, b, c: Vector[n]):
  --   bijective?(n)(T(n, n)(Q)) AND
  --    bijective?(n)(T(n, n)(M)) AND
  --     (x - c) * (inv(n)(Q) * (x - c)) <= 1 AND y = M * x + b
  --    IMPLIES...
  have ellipsoid_tcc12 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ellipsoid_TCC13
  -- Statement: FORALL (n: posnat, Q, M: SquareMat(n), x, y, b, c: Vector[n]):
  --   bijective?(n)(T(n, n)(Q)) AND
  --    bijective?(n)(T(n, n)(M)) AND
  --     (x - c) * (inv(n)(Q) * (x - c)) <= 1 AND y = M * x + b
  --    IMPLIES...
  have ellipsoid_tcc13 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.ellipsoid