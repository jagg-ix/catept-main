-- Auto-generated by pvs2lean/translator.py
-- PVS theory: interval_expr
-- PVSLib library: interval_arith
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: interval_expr

Auto-generated stubs from PVSLib theory `interval_expr`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.interval_expr

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Abs : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Add : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (CONST : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (Div : pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ14 : Type*} (HalfLeft : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (HalfRight : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (Includes_p : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (Inclusion_p : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (Mult : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (Neg : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (POW : pvs_τ26 → pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (Pow : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} (Precondition_p : pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} (ProperBox_p : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} (Proper_p : pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (Sq : pvs_τ35 → pvs_τ36)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (Sub : pvs_τ37 → pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (VARIDX : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} (Zeroless_p : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (pvs_op_d6fc54 : pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} (abs_p : pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} (add_p : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (append : pvs_τ47 → pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} (band_p : pvs_τ50 → Prop)
variable {pvs_τ51 : Type*} (bconst_p : pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (below : pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} (bimplies_p : pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} (bincludes_p : pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} (bite_p : pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} (bletin_p : pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} (bnot_p : pvs_τ58 → Prop)
variable {pvs_τ60 : Type*} (bor_p : pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} (brel_p : pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (car : pvs_τ62 → pvs_τ63)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (cdr : pvs_τ64 → pvs_τ65)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} (cons : pvs_τ66 → pvs_τ67 → pvs_τ68)
variable {pvs_τ69 : Type*} (const_p : pvs_τ69 → Prop)
variable {pvs_τ70 : Type*} (div_p : pvs_τ70 → Prop)
variable {pvs_τ74 : Type*} (fun2_p : pvs_τ74 → Prop)
variable {pvs_τ75 : Type*} (fun4_p : pvs_τ75 → Prop)
variable {pvs_τ76 : Type*} (fun_p : pvs_τ76 → Prop)
variable {pvs_τ77 : Type*} (length : pvs_τ77 → Prop)
variable {pvs_τ78 : Type*} (letin_p : pvs_τ78 → Prop)
variable {pvs_τ79 : Type*} {pvs_τ80 : Type*} (midpoint : pvs_τ79 → pvs_τ80)
variable {pvs_τ81 : Type*} (mult_p : pvs_τ81 → Prop)
variable {pvs_τ82 : Type*} (neg_p : pvs_τ82 → Prop)
variable {pvs_τ83 : Type*} {pvs_τ84 : Type*} (nth : pvs_τ83 → pvs_τ84 → Prop)
variable {pvs_τ85 : Type*} (null_p : pvs_τ85 → Prop)
variable {pvs_τ86 : Type*} {pvs_τ87 : Type*} (op : pvs_τ86 → pvs_τ87)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (op1 : pvs_τ88 → pvs_τ89)
variable {pvs_τ90 : Type*} {pvs_τ91 : Type*} (op2 : pvs_τ90 → pvs_τ91)
variable {pvs_τ92 : Type*} {pvs_τ93 : Type*} (op3 : pvs_τ92 → pvs_τ93)
variable {pvs_τ94 : Type*} {pvs_τ95 : Type*} (op4 : pvs_τ94 → pvs_τ95)
variable {pvs_τ96 : Type*} {pvs_τ97 : Type*} (opC : pvs_τ96 → pvs_τ97)
variable {pvs_τ98 : Type*} {pvs_τ99 : Type*} {pvs_τ100 : Type*} (opF : pvs_τ98 → pvs_τ99 → pvs_τ100)
variable {pvs_τ101 : Type*} {pvs_τ102 : Type*} {pvs_τ103 : Type*} {pvs_τ104 : Type*} (opF2 : pvs_τ101 → pvs_τ102 → pvs_τ103 → pvs_τ104)
variable {pvs_τ105 : Type*} {pvs_τ106 : Type*} {pvs_τ107 : Type*} {pvs_τ108 : Type*} {pvs_τ109 : Type*} {pvs_τ110 : Type*} (opF4 : pvs_τ105 → pvs_τ106 → pvs_τ107 → pvs_τ108 → pvs_τ109 → pvs_τ110)
variable {pvs_τ111 : Type*} {pvs_τ112 : Type*} {pvs_τ113 : Type*} (opc : pvs_τ111 → pvs_τ112 → pvs_τ113)
variable {pvs_τ114 : Type*} {pvs_τ115 : Type*} {pvs_τ116 : Type*} (opf : pvs_τ114 → pvs_τ115 → pvs_τ116)
variable {pvs_τ117 : Type*} {pvs_τ118 : Type*} {pvs_τ119 : Type*} {pvs_τ120 : Type*} (opf2 : pvs_τ117 → pvs_τ118 → pvs_τ119 → pvs_τ120)
variable {pvs_τ121 : Type*} {pvs_τ122 : Type*} (opf4 : pvs_τ121 → pvs_τ122)
variable {pvs_τ123 : Type*} (opn : pvs_τ123 → Prop)
variable {pvs_τ124 : Type*} (pow_p : pvs_τ124 → Prop)
variable {pvs_τ125 : Type*} {pvs_τ126 : Type*} (pre : pvs_τ125 → pvs_τ126 → Prop)
variable {pvs_τ127 : Type*} {pvs_τ128 : Type*} (pre1 : pvs_τ127 → pvs_τ128 → Prop)
variable {pvs_τ129 : Type*} {pvs_τ130 : Type*} {pvs_τ131 : Type*} (pre2 : pvs_τ129 → pvs_τ130 → pvs_τ131 → Prop)
variable {pvs_τ132 : Type*} {pvs_τ133 : Type*} {pvs_τ134 : Type*} {pvs_τ135 : Type*} (pre3 : pvs_τ132 → pvs_τ133 → pvs_τ134 → pvs_τ135 → Prop)
variable {pvs_τ136 : Type*} {pvs_τ137 : Type*} {pvs_τ138 : Type*} {pvs_τ139 : Type*} {pvs_τ140 : Type*} (pre4 : pvs_τ136 → pvs_τ137 → pvs_τ138 → pvs_τ139 → pvs_τ140 → Prop)
variable {pvs_τ142 : Type*} {pvs_τ143 : Type*} (restrict : pvs_τ142 → pvs_τ143)
variable {pvs_τ144 : Type*} {pvs_τ145 : Type*} (rin : pvs_τ144 → pvs_τ145)
variable {pvs_τ146 : Type*} {pvs_τ147 : Type*} (rlet : pvs_τ146 → pvs_τ147)
variable {pvs_τ150 : Type*} {pvs_τ151 : Type*} (sq : pvs_τ150 → pvs_τ151)
variable {pvs_τ152 : Type*} (sq_p : pvs_τ152 → Prop)
variable {pvs_τ153 : Type*} (sub_p : pvs_τ153 → Prop)
variable {pvs_τ154 : Type*} {pvs_τ155 : Type*} (subterm : pvs_τ154 → pvs_τ155 → Prop)
variable {pvs_τ156 : Type*} {pvs_τ157 : Type*} (upfrom : pvs_τ156 → pvs_τ157)
variable {pvs_τ158 : Type*} {pvs_τ159 : Type*} {pvs_τ160 : Type*} (v1 : pvs_τ158 → pvs_τ159 → pvs_τ160)
variable {pvs_τ161 : Type*} (varidx : pvs_τ161 → Prop)
variable {pvs_τ162 : Type*} (varidx_p : pvs_τ162 → Prop)
variable {pvs_τ163 : Type*} {pvs_τ164 : Type*} (vars_in_box_p : pvs_τ163 → pvs_τ164 → Prop)
variable {pvs_τ165 : Type*} {pvs_τ166 : Type*} (vs : pvs_τ165 → pvs_τ166)
variable {pvs_τ167 : Type*} (well_founded_p : pvs_τ167 → Prop)

-- pvs2lean: interval_expr.r2E
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CONST(LAMBDA (u: Unit): r, [|r|])
noncomputable def r2E : sorry := by
  sorry  -- pvs2lean: definition stub for CONST(LAMBDA (u: Unit): r, [|r|])

-- pvs2lean: interval_expr.I2E
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CONST(LAMBDA (u: Unit): midpoint(X), X)
noncomputable def I2E : sorry := by
  sorry  -- pvs2lean: definition stub for CONST(LAMBDA (u: Unit): midpoint(X), X)

-- pvs2lean: interval_expr.SQ2POW
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: POW(expr, 2)
noncomputable def SQ2POW : sorry := by
  sorry  -- pvs2lean: definition stub for POW(expr, 2)

-- pvs2lean: interval_expr.PreTrue
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (X: Interval): TRUE
noncomputable def PreTrue : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA (X: Interval): TRUE

-- pvs2lean: interval_expr.X
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: VARIDX(v)
noncomputable def X : sorry := by
  sorry  -- pvs2lean: definition stub for VARIDX(v)

-- pvs2lean: interval_expr.realexpr?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      const?(expr) OR varidx?(expr) OR add?(expr) OR abs?(expr) OR neg?(expr)
--  OR sub?(expr) OR mult?(expr) OR sq?(expr) OR pow?(expr) OR div?(expr)
--  OR fun?(expr) OR letin?(expr) OR fun2?(expr) OR fun
axiom realexpr_p {pvs_τ141 : Type*} : pvs_τ141 → Prop

-- pvs2lean: interval_expr.boolexpr?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      bconst?(expr) OR bnot?(expr) OR band?(expr) OR bor?(expr)
--  OR bimplies?(expr) OR brel?(expr) OR bincludes?(expr) OR bite?(expr)
--  OR bletin?(expr)
axiom boolexpr_p {pvs_τ59 : Type*} : pvs_τ59 → Prop

-- pvs2lean: interval_expr.real_bool_expr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (expr: IntervalExpr): realexpr?(expr) OR boolexpr?(expr)
theorem real_bool_expr : ∀ (expr : IntervalExpr), realexpr_p expr ∨ boolexpr_p expr := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.eval
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF const?(expr) THEN opc(expr)(unit)
-- ELSIF varidx?(expr) THEN vs(varidx(expr))
-- ELSIF add?(expr) THEN eval(op1(expr), vs, n) + eval(op2(expr), vs, n)
-- ELSIF abs?(expr) THEN abs(eval(op(expr), vs, n))
-- EL
axiom eval {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} : pvs_τ71 → pvs_τ72 → pvs_τ73 → Prop

-- pvs2lean: interval_expr.Eval
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF const?(expr) THEN opC(expr)
-- ELSIF varidx?(expr)
--   THEN IF varidx(expr) >= length(box) THEN EmptyInterval
--        ELSE nth(box, varidx(expr))
--        ENDIF
-- ELSIF add?(expr)
--   THEN LET op1 = Eval(op1(e
axiom Eval {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: interval_expr.well_typed?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      const?(expr) OR (varidx?(expr) AND varidx(expr) < length(box))
--  OR (add?(expr) AND
--       well_typed?(box)(op1(expr)) AND well_typed?(box)(op2(expr)))
--  OR (abs?(expr) AND well_typed?(box)(op(expr)
axiom well_typed_p {pvs_τ168 : Type*} {pvs_τ169 : Type*} : pvs_τ168 → pvs_τ169 → Prop

-- pvs2lean: interval_expr.well_typed?_weak_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [Box -> [RealExpr -> boolean]]):
--   (FORALL (box: Box, expr: RealExpr):
--           const?(expr) OR (varidx?(expr) AND varidx(expr) < length(box))
--       OR (add?(expr) AND P(box)(op1(expr)) AN
theorem well_typed_p_weak_induction : ∀ (P : List (ℝ × ℝ) → {x : IntervalExpr // const_p x ∨ varidx_p x ∨ add_p x ∨ abs_p x ∨ neg_p x ∨ sub_p x
 ∨ mult_p x ∨ sq_p x ∨ pow_p x ∨ div_p x ∨ fun_p x ∨ fun2_p x
 ∨ letin_p x ∨ fun4_p x} → Bool), ∀ (box : Box) (expr : RealExpr), const_p expr ∨ (varidx_p expr ∧ varidx expr < List.length box)
   ∨ (add_p expr ∧ P box(op1 expr) ∧ P box(op2 expr))
   ∨ (abs_p expr ∧ P box(op expr))
   ∨ (sub_p expr ∧ P box(op1 expr) ∧ P box(op2 expr))
   ∨ (neg_p expr ∧ P box(op expr))
   ∨ (mult_p expr ∧ P box(op1 expr) ∧ P box(op2 expr))
   ∨ (sq_p expr ∧ P box(op expr))
   ∨ (div_p expr ∧
        P box(op1 expr) ∧
         P box(op2 expr) ∧ Zeroless_p (Eval (op2 expr) box))
   ∨ (pow_p expr ∧ P box(op expr))
   ∨ (fun_p expr ∧ P box(op expr) ∧ pre expr(Eval (op expr) box))
   ∨ (fun2_p expr ∧
        P box(op1 expr) ∧
         P box(op2 expr) ∧
          pre1 expr(Eval (op1 expr) box) ∧
           pre2 expr(Eval (op1 expr) box, Eval (op2 expr) box))
   ∨ (fun4_p expr ∧
             P box(op1 expr) ∧ P box(op2 expr) ∧ P box(op3 expr)
         ∧ P box(op4 expr) ∧ pre1 expr(Eval (op1 expr) box)
         ∧ pre2 expr(Eval (op1 expr) box, Eval (op2 expr) box)
         ∧ pre3 expr
                 (Eval (op1 expr) box, Eval (op2 expr) box,
                  Eval (op3 expr) box)
         ∧ pre4 expr
                 (Eval (op1 expr) box, Eval (op2 expr) box,
                  Eval (op3 expr) box, Eval (op4 expr) box))
   ∨ (letin_p expr ∧
        P box(rlet expr) ∧
         P (append box ((: Eval (rlet expr) box :))) (rin expr))
   → P box(expr) → ∀ (box : Box) (expr : RealExpr), well_typed_p box(expr) → P box(expr) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.well_typed?_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [Box -> [RealExpr -> boolean]]):
--   (FORALL (box: Box, expr: RealExpr):
--           const?(expr) OR (varidx?(expr) AND varidx(expr) < length(box))
--       OR (add?(expr) AND
--            (well_typ
theorem well_typed_p_induction : ∀ (P : List (ℝ × ℝ) → {x : IntervalExpr // const_p x ∨ varidx_p x ∨ add_p x ∨ abs_p x ∨ neg_p x ∨ sub_p x
 ∨ mult_p x ∨ sq_p x ∨ pow_p x ∨ div_p x ∨ fun_p x ∨ fun2_p x
 ∨ letin_p x ∨ fun4_p x} → Bool), ∀ (box : Box) (expr : RealExpr), const_p expr ∨ (varidx_p expr ∧ varidx expr < List.length box)
   ∨ (add_p expr ∧
        (well_typed_p box(op1 expr) ∧ P box(op1 expr)) ∧
         well_typed_p box(op2 expr) ∧ P box(op2 expr))
   ∨ (abs_p expr ∧ well_typed_p box(op expr) ∧ P box(op expr))
   ∨ (sub_p expr ∧
        (well_typed_p box(op1 expr) ∧ P box(op1 expr)) ∧
         well_typed_p box(op2 expr) ∧ P box(op2 expr))
   ∨ (neg_p expr ∧ well_typed_p box(op expr) ∧ P box(op expr))
   ∨ (mult_p expr ∧
        (well_typed_p box(op1 expr) ∧ P box(op1 expr)) ∧
         well_typed_p box(op2 expr) ∧ P box(op2 expr))
   ∨ (sq_p expr ∧ well_typed_p box(op expr) ∧ P box(op expr))
   ∨ (div_p expr ∧
             well_typed_p box(op1 expr) ∧ P box(op1 expr)
         ∧ well_typed_p box(op2 expr) ∧ P box(op2 expr)
         ∧ Zeroless_p (Eval (op2 expr) box))
   ∨ (pow_p expr ∧ well_typed_p box(op expr) ∧ P box(op expr))
   ∨ (fun_p expr ∧
        (well_typed_p box(op expr) ∧ P box(op expr)) ∧
         pre expr(Eval (op expr) box))
   ∨ (fun2_p expr ∧
             well_typed_p box(op1 expr) ∧ P box(op1 expr)
         ∧ well_typed_p box(op2 expr) ∧ P box(op2 expr)
         ∧ pre1 expr(Eval (op1 expr) box)
         ∧ pre2 expr(Eval (op1 expr) box, Eval (op2 expr) box))
   ∨ (fun4_p expr ∧
             well_typed_p box(op1 expr) ∧ P box(op1 expr)
         ∧ well_typed_p box(op2 expr) ∧ P box(op2 expr)
         ∧ well_typed_p box(op3 expr) ∧ P box(op3 expr)
         ∧ well_typed_p box(op4 expr) ∧ P box(op4 expr)
         ∧ pre1 expr(Eval (op1 expr) box)
         ∧ pre2 expr(Eval (op1 expr) box, Eval (op2 expr) box)
         ∧ pre3 expr
                 (Eval (op1 expr) box, Eval (op2 expr) box,
                  Eval (op3 expr) box)
         ∧ pre4 expr
                 (Eval (op1 expr) box, Eval (op2 expr) box,
                  Eval (op3 expr) box, Eval (op4 expr) box))
   ∨ (letin_p expr ∧
        (well_typed_p box(rlet expr) ∧ P box(rlet expr)) ∧
         well_typed_p (append box ((: Eval (rlet expr) box :))) (rin expr)
          ∧ P (append box ((: Eval (rlet expr) box :))) (rin expr))
   → P box(expr) → ∀ (box : Box) (expr : RealExpr), well_typed_p box(expr) → P box(expr) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.Proper_well_typed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Proper?(Eval(expr, box)) IMPLIES well_typed?(box)(expr)
theorem Proper_well_typed : Proper_p (Eval expr box) → well_typed_p box(expr) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.Eval_inclusion
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (box: Box, vs: (vars_in_box?(box)), expr):
--   well_typed?(box)(expr) IMPLIES
--    eval(expr, vs, length(box)) ## Eval(expr, box)
theorem Eval_inclusion : ∀ (box : List (ℝ × ℝ)) (vs : {i : ℝ // i ≥ 0} → ℝ) (expr : {x : IntervalExpr // const_p x ∨ varidx_p x ∨ add_p x ∨ abs_p x ∨ neg_p x ∨ sub_p x
 ∨ mult_p x ∨ sq_p x ∨ pow_p x ∨ div_p x ∨ fun_p x ∨ fun2_p x
 ∨ letin_p x ∨ fun4_p x}), well_typed_p box(expr) → eval expr vs (List.length box) ∈ Eval expr box := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.Eval_inclusion_Proper
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (box: Box, vs: (vars_in_box?(box))) (expr):
--   LET E = Eval(expr, box) IN
--     Proper?(E) IMPLIES eval(expr, vs, length(box)) ## E
theorem Eval_inclusion_Proper : ∀ (box : List (ℝ × ℝ)) (vs : {i : ℝ // i ≥ 0} → ℝ), ∀ (expr : {x : IntervalExpr // const_p x ∨ varidx_p x ∨ add_p x ∨ abs_p x ∨ neg_p x ∨ sub_p x
 ∨ mult_p x ∨ sq_p x ∨ pow_p x ∨ div_p x ∨ fun_p x ∨ fun2_p x
 ∨ letin_p x ∨ fun4_p x}), fun (E : ℝ × ℝ) => Proper_p E → eval expr vs (List.length box) ∈ E (Eval expr box) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.well_typed_Proper
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_typed?(pox)(expr) IMPLIES Proper?(Eval(expr, pox))
theorem well_typed_Proper : well_typed_p pox(expr) → Proper_p (Eval expr pox) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.Eval_fundamental_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (expr):
--   Inclusion?(box, pox) IMPLIES
--    well_typed?(pox)(expr) AND Proper?(Eval(expr, box)) IMPLIES
--     Eval(expr, box) << Eval(expr, pox)
theorem Eval_fundamental_aux : ∀ (expr : {x : IntervalExpr // const_p x ∨ varidx_p x ∨ add_p x ∨ abs_p x ∨ neg_p x ∨ sub_p x
 ∨ mult_p x ∨ sq_p x ∨ pow_p x ∨ div_p x ∨ fun_p x ∨ fun2_p x
 ∨ letin_p x ∨ fun4_p x}), Inclusion_p box pox → well_typed_p pox(expr) ∧ Proper_p (Eval expr box) →
 Eval expr box pvs_lt_ord Eval expr pox := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.well_typed_Inclusion_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (expr):
--   Inclusion?(pox, box) IMPLIES
--    well_typed?(box)(expr) IMPLIES well_typed?(pox)(expr)
theorem well_typed_Inclusion_aux : ∀ (expr : {x : IntervalExpr // const_p x ∨ varidx_p x ∨ add_p x ∨ abs_p x ∨ neg_p x ∨ sub_p x
 ∨ mult_p x ∨ sq_p x ∨ pow_p x ∨ div_p x ∨ fun_p x ∨ fun2_p x
 ∨ letin_p x ∨ fun4_p x}), Inclusion_p pox box → well_typed_p box(expr) → well_typed_p pox(expr) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.well_typed_Inclusion
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Inclusion?(pox, box) IMPLIES
--  FORALL (expr): well_typed?(box)(expr) IMPLIES well_typed?(pox)(expr)
theorem well_typed_Inclusion : Inclusion_p pox box → ∀ (expr : _), well_typed_p box(expr) → well_typed_p pox(expr) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.Proper_Inclusion
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Inclusion?(pox, box) IMPLIES
--  FORALL (expr): Proper?(Eval(expr, box)) IMPLIES Proper?(Eval(expr, pox))
theorem Proper_Inclusion : Inclusion_p pox box → ∀ (expr : _), Proper_p (Eval expr box) → Proper_p (Eval expr pox) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.Eval_fundamental
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Inclusion?(pox, box) IMPLIES
--  FORALL (expr):
--    well_typed?(box)(expr) IMPLIES Eval(expr, pox) << Eval(expr, box)
theorem Eval_fundamental : Inclusion_p pox box → ∀ (expr : _), well_typed_p box(expr) → Eval expr pox pvs_lt_ord Eval expr box := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.Eval_fundamental_proper
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Inclusion?(pox, box) IMPLIES
--  FORALL (expr):
--    LET E = Eval(expr, box) IN Proper?(E) IMPLIES Eval(expr, pox) << E
theorem Eval_fundamental_proper : Inclusion_p pox box → ∀ (expr : _), (let E := Eval expr box; Proper_p E → Eval expr pox pvs_lt_ord E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.split
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(box) THEN (null, null)
-- ELSIF v = 0
--   THEN (cons(HalfLeft(car(box)), cdr(box)),
--         cons(HalfRight(car(box)), cdr(box)))
-- ELSE LET (lb, rb) = split(v - 1, cdr(box)) IN
--        (cons(car(box)
axiom split {pvs_τ148 : Type*} {pvs_τ149 : Type*} : pvs_τ148 → pvs_τ149 → Prop

-- pvs2lean: interval_expr.split_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (box: Box | length(box) <= v)):
--   split(v, box)`1 = box AND split(v, box)`2 = box
theorem split_eq : ∀ (v : {i : ℝ // i ≥ 0}) (box : {box : List (ℝ × ℝ) // List.length box ≤ v}), split v box.1 = box ∧ split v box.2 = box := by
  -- === TCCs for split_eq ===
  -- TCC: split_eq_TCC1
  -- Statement: FORALL (v: nat, (box: Box | length[Interval](box) <= v),
  --         v1:
  --           [d: [v1: nat, {box: Box | length(box) <= v1}] ->
  --              {lrb: [Box, Box] |
  --                 LET (lb, rb) = lrb IN...
  have split_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: split_eq_TCC2
  -- Statement: FORALL (v: nat, (box: Box | length[Interval](box) <= v),
  --         v1:
  --           [d: [v1: nat, {box: Box | length(box) <= v1}] ->
  --              {lrb: [Box, Box] |
  --                 LET (lb, rb) = lrb IN...
  have split_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: split_eq_TCC3
  -- Statement: FORALL (v: nat, (box: Box | length[Interval](box) <= v),
  --         v1:
  --           [d: [v1: nat, {box: Box | length(box) <= v1}] ->
  --              {lrb: [Box, Box] |
  --                 LET (lb, rb) = lrb IN...
  have split_eq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: split_eq_TCC4
  -- Statement: FORALL (v: nat, (box: Box | length[Interval](box) <= v),
  --         v1:
  --           [d: [v1: nat, {box: Box | length(box) <= v1}] ->
  --              {lrb: [Box, Box] |
  --                 LET (lb, rb) = lrb IN...
  have split_eq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: interval_expr.split_vars_in_box
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (box: Box, vs: (vars_in_box?(box)), v):
--   LET (bl, br) = split(v, box) IN
--     vars_in_box?(bl)(vs) OR vars_in_box?(br)(vs)
theorem split_vars_in_box : ∀ (box : List (ℝ × ℝ)) (vs : {i : ℝ // i ≥ 0} → ℝ) (v : {i : ℝ // i ≥ 0}), fun (bl : List (ℝ × ℝ)) (br : List (ℝ × ℝ)) => vars_in_box_p bl(vs) ∨ vars_in_box_p br(vs) (split v box) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.split_Inclusion
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (pox, v):
--   LET (bl, br) = split(v, pox) IN
--     Inclusion?(bl, pox) AND Inclusion?(br, pox)
theorem split_Inclusion : ∀ (pox : List (ℝ × ℝ)) (v : {i : ℝ // i ≥ 0}), fun (bl : List (ℝ × ℝ)) (br : List (ℝ × ℝ)) => Inclusion_p bl pox ∧ Inclusion_p br pox (split v pox) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interval_expr.length_split_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(split(v, pox)`1) = length(pox)
theorem length_split_1 : List.length (split v pox.1) = List.length pox := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: interval_expr.length_split_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(split(v, pox)`2) = length(pox)
theorem length_split_2 : List.length (split v pox.2) = List.length pox := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: interval_expr.eval_vs_idempotence
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: upfrom(i)):
--   (FORALL (subexpr: (varidx?)):
--      subterm(subexpr, expr) IMPLIES varidx(subexpr) < i)
--    IMPLIES eval(expr, vs, n) = eval(expr, vs WITH [(i) := x], n)
theorem eval_vs_idempotence : ∀ (n : {s : ℝ // s ≥ i}), (∀ (subexpr : (varidx_p)), subterm subexpr expr → varidx subexpr < i) → eval expr vs n = eval expr (Function.update vs i x) n := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.interval_expr