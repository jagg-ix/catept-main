-- Auto-generated by pvs2lean/translator.py
-- PVS theory: linear_dependence
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: linear_dependence

Auto-generated stubs from PVSLib theory `linear_dependence`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.linear_dependence

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (columns : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (row : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ12 : Type*} (rows : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (subrange : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (transpose : pvs_τ20 → pvs_τ21)

-- pvs2lean: linear_dependence.zerow
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (: zero(p) :)
axiom zerow {pvs_τ24 : Type*} {pvs_τ25 : Type*} : pvs_τ24 → pvs_τ25

-- pvs2lean: linear_dependence.zecolumn
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: transpose(zerow(p))
axiom zecolumn {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ22 → pvs_τ23

-- pvs2lean: linear_dependence.zerow_dim
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(zerow(p)) = 1 AND columns(zerow(p)) = p
theorem zerow_dim : rows (zerow p) = 1 ∧ columns (zerow p) = p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: linear_dependence.zecolumn_dim
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(zecolumn(p)) = p AND columns(zecolumn(p)) = 1
theorem zecolumn_dim : rows (zecolumn p) = p ∧ columns (zecolumn p) = 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: linear_dependence.row2mat
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (: row(A)(j) :)
axiom row2mat {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: linear_dependence.row2mat_dim
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(rows(A))):
--   rows(row2mat(A, j)) = 1 AND columns(row2mat(A, j)) = columns(A)
theorem row2mat_dim : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < rows A}), rows (row2mat A j) = 1 ∧ columns (row2mat A j) = columns A := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: linear_dependence.sum_rows
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF j = 0 THEN M + f(j) * row2mat(A, j)
-- ELSE sum_rows(A, f, j - 1, M + f(j) * row2mat(A, j))
-- ENDIF
axiom sum_rows {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → Prop

-- pvs2lean: linear_dependence.sum_rows_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(rows(A))):
--   (FORALL (k: subrange(0, j)): f(k) = g(k)) IMPLIES
--    sum_rows(A, f, j, M) = sum_rows(A, g, j, M)
theorem sum_rows_eq : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < rows A}), (∀ (k : Set.Icc 0 j), f k = g k) → sum_rows A f j M = sum_rows A g j M := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: linear_dependence.sum_rows_add_start
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(rows(A))):
--   sum_rows(A, f, j, M + N) = sum_rows(A, f, j, M) + N
theorem sum_rows_add_start : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < rows A}), sum_rows A f j (M + N) = sum_rows A f j M + N := by
  -- === TCCs for sum_rows_add_start ===
  -- TCC: sum_rows_add_start_TCC1
  -- Statement: FORALL (A, M, N: PosFullMatrix, j: below(rows(A))):
  --   (null?[list[real]](M + N) OR
  --     FORALL (i, j: below(length[list[real]](M + N))):
  --       length[real](nth[list[real]](M + N, i)) =
  --        length...
  have sum_rows_add_start_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: linear_dependence.subtract_same_scal
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(rows(A))):
--   (f(j) * row2mat(A, j) - f(j) * row2mat(A, j)) = 0 * row2mat(A, j)
theorem subtract_same_scal : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < rows A}), (f j * row2mat A j - f j * row2mat A j) = 0 * row2mat A j := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: linear_dependence.sum_lem_prep
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(rows(A)), k: below(j + 1)):
--   sum_rows(A, f WITH [k := 0], j, M) =
--    sum_rows(A, f, j, M) - f(k) * row2mat(A, k)
theorem sum_lem_prep : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < rows A}) (k : {s : {i : ℝ // i ≥ 0} // s < 1 + j}), sum_rows A (f WITH[k := 0]) j M = sum_rows A f j M - f k * row2mat A k := by
  -- === TCCs for sum_lem_prep ===
  -- TCC: sum_lem_prep_TCC1
  -- Statement: FORALL (A: PosFullMatrix, j: below(rows(A)), k: below(1 + j)): k < rows(A)
  have sum_lem_prep_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: linear_dependence.sum_lem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(rows(A))):
--   sum_rows(A, f WITH [j := 0]) = sum_rows(A, f) - f(j) * row2mat(A, j)
theorem sum_lem : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < rows A}), sum_rows A (f WITH[j := 0]) = sum_rows A f - f j * row2mat A j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: linear_dependence.row_dependent
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (sum_rows(A, f) = zerow(columns(A)))
axiom row_dependent {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: linear_dependence.row_dependence_lem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(rows(A)), (f: [nat -> real] | f(j) = -1)):
--   row_dependent(A, j, f) IMPLIES
--    row2mat(A, j) * M = sum_rows(A, f WITH [j := 0]) * M
theorem row_dependence_lem : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < rows A}) (f : {f : {i : ℝ // i ≥ 0} → ℝ // f j = -1}), row_dependent A j f → row2mat A j * M = sum_rows A (f WITH[j := 0]) * M := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: linear_dependence.row_dep
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: ((f(j) = -1 AND row_dependent(A, j, f)) OR (f = LAMBDA (i: nat): 0))
noncomputable def row_dep : sorry := by
  sorry  -- pvs2lean: definition stub for ((f(j) = -1 AND row_dependent(A, j, f)) OR (f = LAMBDA (i: nat): 0))

-- pvs2lean: linear_dependence.row_dep_fun
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: f WITH [j := f(j) + 1]
noncomputable def row_dep_fun : sorry := by
  sorry  -- pvs2lean: definition stub for f WITH [j := f(j) + 1]

end

end NavierStokesClean.PVSLib.linear_dependence