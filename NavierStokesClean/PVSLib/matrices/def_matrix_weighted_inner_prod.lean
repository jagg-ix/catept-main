-- Auto-generated by pvs2lean/translator.py
-- PVS theory: def_matrix_weighted_inner_prod
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: def_matrix_weighted_inner_prod

Auto-generated stubs from PVSLib theory `def_matrix_weighted_inner_prod`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.def_matrix_weighted_inner_prod

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (VectorN : pvs_τ1 → pvs_τ2)
variable {pvs_τ7 : Type*} (pos_defn_p : pvs_τ7 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (spos_defn_p : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} (zero_vec : pvs_τ15 → Prop)

-- pvs2lean: def_matrix_weighted_inner_prod.smat_ip
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: x * (A * y)
axiom smat_ip {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12

-- pvs2lean: def_matrix_weighted_inner_prod.smat_ip_scal_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (spos_defn?(n)), x, y: VectorN(n), c: real):
--   smat_ip(n, A)(c * x, y) = c * smat_ip(n, A)(x, y)
theorem smat_ip_scal_left : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}) (c : ℝ), smat_ip n A(c * x, y) = c * smat_ip n A(x, y) := by
  -- === TCCs for smat_ip_scal_left ===
  -- TCC: smat_ip_scal_left_TCC1
  -- Statement: FORALL (n: posnat, A: (spos_defn?(n)), x, y: VectorN(n), c: real):
  --   length[real](c * x) = n
  have smat_ip_scal_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.smat_ip_scal_right
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (spos_defn?(n)), x, y: VectorN(n), c: real):
--   smat_ip(n, A)(x, c * y) = c * smat_ip(n, A)(x, y)
theorem smat_ip_scal_right : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}) (c : ℝ), smat_ip n A(x, c * y) = c * smat_ip n A(x, y) := by
  -- === TCCs for smat_ip_scal_right ===
  -- TCC: smat_ip_scal_right_TCC1
  -- Statement: FORALL (n: posnat, A: (spos_defn?(n)), x, y: VectorN(n), c: real):
  --   length[real](c * y) = n
  have smat_ip_scal_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.smat_ip_sum_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (spos_defn?(n)), x, y, z: VectorN(n)):
--   smat_ip(n, A)(x + z, y) = smat_ip(n, A)(x, y) + smat_ip(n, A)(z, y)
theorem smat_ip_sum_left : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}) (z : {l : List ℝ // List.length l = n}), smat_ip n A(x + z, y) = smat_ip n A(x, y) + smat_ip n A(z, y) := by
  -- === TCCs for smat_ip_sum_left ===
  -- TCC: smat_ip_sum_left_TCC1
  -- Statement: FORALL (n: posnat, A: (spos_defn?(n)), x, y, z: VectorN(n)):
  --   length[real](x + z) = n
  have smat_ip_sum_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.smat_ip_sum_right
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (spos_defn?(n)), x, y, z: VectorN(n)):
--   smat_ip(n, A)(x, y + z) = smat_ip(n, A)(x, y) + smat_ip(n, A)(x, z)
theorem smat_ip_sum_right : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}) (z : {l : List ℝ // List.length l = n}), smat_ip n A(x, y + z) = smat_ip n A(x, y) + smat_ip n A(x, z) := by
  -- === TCCs for smat_ip_sum_right ===
  -- TCC: smat_ip_sum_right_TCC1
  -- Statement: FORALL (n: posnat, A: (spos_defn?(n)), x, y, z: VectorN(n)):
  --   length[real](y + z) = n
  have smat_ip_sum_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.smat_ip_sym
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (spos_defn?(n)), x, y: VectorN(n)):
--   smat_ip(n, A)(x, y) = smat_ip(n, A)(y, x)
theorem smat_ip_sym : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}), smat_ip n A(x, y) = smat_ip n A(y, x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.smat_ip_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (spos_defn?(n))):
--   LET x = zero_vec(n) IN smat_ip(n, A)(x, x) = 0
theorem smat_ip_0 : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}), fun (x : {l : List ℝ // List.length l = n}) => smat_ip n A(x, x) = 0 (zero_vec n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.smat_ip_geq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (spos_defn?(n)), x: VectorN(n)):
--   smat_ip(n, A)(x, x) >= 0
theorem smat_ip_geq_0 : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}), smat_ip n A(x, x) ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: smat_ip(n, A)(x, y)
axiom mat_ip {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip_scal_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (pos_defn?(n)), x, y: VectorN(n), c: real):
--   mat_ip(n, A)(c * x, y) = c * mat_ip(n, A)(x, y)
theorem mat_ip_scal_left : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}) (c : ℝ), mat_ip n A(c * x, y) = c * mat_ip n A(x, y) := by
  -- === TCCs for mat_ip_scal_left ===
  -- TCC: mat_ip_scal_left_TCC1
  -- Statement: FORALL (n: posnat, A: (pos_defn?(n)), x, y: VectorN(n), c: real):
  --   length[real](c * x) = n
  have mat_ip_scal_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip_scal_right
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (pos_defn?(n)), x, y: VectorN(n), c: real):
--   mat_ip(n, A)(x, c * y) = c * mat_ip(n, A)(x, y)
theorem mat_ip_scal_right : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}) (c : ℝ), mat_ip n A(x, c * y) = c * mat_ip n A(x, y) := by
  -- === TCCs for mat_ip_scal_right ===
  -- TCC: mat_ip_scal_right_TCC1
  -- Statement: FORALL (n: posnat, A: (pos_defn?(n)), x, y: VectorN(n), c: real):
  --   length[real](c * y) = n
  have mat_ip_scal_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip_sum_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (pos_defn?(n)), x, y, z: VectorN(n)):
--   mat_ip(n, A)(x + z, y) = mat_ip(n, A)(x, y) + mat_ip(n, A)(z, y)
theorem mat_ip_sum_left : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}) (z : {l : List ℝ // List.length l = n}), mat_ip n A(x + z, y) = mat_ip n A(x, y) + mat_ip n A(z, y) := by
  -- === TCCs for mat_ip_sum_left ===
  -- TCC: mat_ip_sum_left_TCC1
  -- Statement: FORALL (n: posnat, A: (pos_defn?(n)), x, y, z: VectorN(n)):
  --   length[real](x + z) = n
  have mat_ip_sum_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip_sum_right
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (pos_defn?(n)), x, y, z: VectorN(n)):
--   mat_ip(n, A)(x, y + z) = mat_ip(n, A)(x, y) + mat_ip(n, A)(x, z)
theorem mat_ip_sum_right : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}) (z : {l : List ℝ // List.length l = n}), mat_ip n A(x, y + z) = mat_ip n A(x, y) + mat_ip n A(x, z) := by
  -- === TCCs for mat_ip_sum_right ===
  -- TCC: mat_ip_sum_right_TCC1
  -- Statement: FORALL (n: posnat, A: (pos_defn?(n)), x, y, z: VectorN(n)):
  --   length[real](y + z) = n
  have mat_ip_sum_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip_sym
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (pos_defn?(n)), x, y: VectorN(n)):
--   mat_ip(n, A)(x, y) = mat_ip(n, A)(y, x)
theorem mat_ip_sym : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}) (y : {l : List ℝ // List.length l = n}), mat_ip n A(x, y) = mat_ip n A(y, x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (pos_defn?(n))):
--   LET x = zero_vec(n) IN x * (A * x) = 0
theorem mat_ip_0 : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}), fun (x : {l : List ℝ // List.length l = n}) => x * (A * x) = 0 (zero_vec n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip_geq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (pos_defn?(n)), x: VectorN(n)):
--   mat_ip(n, A)(x, x) >= 0
theorem mat_ip_geq_0 : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}), mat_ip n A(x, x) ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.mat_ip_gt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A: (pos_defn?(n)), x: VectorN(n)):
--   NOT x = zero_vec(n) IMPLIES mat_ip(n, A)(x, x) > 0
theorem mat_ip_gt_0 : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = n}) (x : {l : List ℝ // List.length l = n}), ¬ x = zero_vec n → mat_ip n A(x, x) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: def_matrix_weighted_inner_prod.norm_matn
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sqrt(mat_ip(n, A)(x, x))
noncomputable def norm_matn : sorry := by
  sorry  -- pvs2lean: definition stub for sqrt(mat_ip(n, A)(x, x))

-- pvs2lean: def_matrix_weighted_inner_prod.norm_matnsq
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: mat_ip(n, A)(x, x)
noncomputable def norm_matnsq : sorry := by
  sorry  -- pvs2lean: definition stub for mat_ip(n, A)(x, x)

end

end NavierStokesClean.PVSLib.def_matrix_weighted_inner_prod