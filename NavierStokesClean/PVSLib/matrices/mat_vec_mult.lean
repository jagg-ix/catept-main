-- Auto-generated by pvs2lean/translator.py
-- PVS theory: mat_vec_mult
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: mat_vec_mult

Auto-generated stubs from PVSLib theory `mat_vec_mult`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.mat_vec_mult

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (MatrixMN : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (VectorN : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (below : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (car : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (cdr : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (col : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (columns : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (cons : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (cons_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (entry : pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (length : pvs_τ24 → pvs_τ25)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (max : pvs_τ31 → pvs_τ32 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (min : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (norm_inf : pvs_τ38 → pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (nth : pvs_τ41 → pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} (row : pvs_τ44 → pvs_τ45 → pvs_τ46)
variable {pvs_τ47 : Type*} (rows : pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (v1 : pvs_τ48 → pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (zero_vec : pvs_τ54 → pvs_τ55)

-- pvs2lean: mat_vec_mult.MN_rc
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A: MatrixMN(m, n)):
--   rows(A) = m AND columns(A) = n OR rows(A) = 0 AND columns(A) = 0
theorem MN_rc : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}), rows A = m ∧ columns A = n ∨ rows A = 0 ∧ columns A = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.entry_extensionality
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: FullMatrix):
--   A = B IFF
--    (columns(A) = columns(B) AND rows(A) = rows(B)) AND
--     (FORALL (i: below(max(rows(A), rows(B))),
--              j: below(max(columns(A), columns(B)))):
--        
theorem entry_extensionality : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}), A = B ↔ (columns A = columns B ∧ rows A = rows B) ∧
 (∀ (i : Finset.range max (rows A (rows B))) (j : Finset.range max (columns A (columns B))), entry A(i, j) = entry B(i, j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.row_extensionality
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: FullMatrix):
--   A = B IFF
--    (columns(A) = columns(B) AND rows(A) = rows(B)) AND
--     FORALL (i: below(max(rows(A), rows(B)))): row(A)(i) = row(B)(i)
theorem row_extensionality : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}), A = B ↔ (columns A = columns B ∧ rows A = rows B) ∧
 ∀ (i : Finset.range max (rows A (rows B))), row A(i) = row B(i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.column_extensionality
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: FullMatrix):
--   A = B IFF
--    (columns(A) = columns(B) AND rows(A) = rows(B)) AND
--     FORALL (i: below(max(columns(A), columns(B)))): col(A)(i) = col(B)(i)
theorem column_extensionality : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}), A = B ↔ (columns A = columns B ∧ rows A = rows B) ∧
 ∀ (i : Finset.range max (columns A (columns B))), col A(i) = col B(i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.cdr_columns
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: {AA: FullMatrix | cons?(AA) AND cons?(cdr(AA))}):
--   columns(A) = columns(cdr(A))
theorem cdr_columns : ∀ (A : {AA : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // cons_p AA ∧ cons_p (cdr AA)}), columns A = columns (cdr A) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.cdr_columns_full
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: {AA: FullMatrix | cons?(AA)}):
--   FORALL (i, j: below(length(cdr(A)))):
--     length(nth(cdr(A), i)) = length(nth(cdr(A), j))
theorem cdr_columns_full : ∀ (A : {AA : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // cons_p AA}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length (cdr A)}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length (cdr A)}), List.length (nth (cdr A) i) = List.length (nth (cdr A) j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.matvec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = 0 THEN null ELSE cons(car(A) * v, matvec(m - 1, n, cdr(A), v)) ENDIF
axiom matvec {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ26 → pvs_τ27 → pvs_τ28 → pvs_τ29 → pvs_τ30

-- pvs2lean: mat_vec_mult.*
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: matvec(rows(A), columns(A), A, v)
noncomputable def pvs_op_3389da : sorry := by
  sorry  -- pvs2lean: definition stub for matvec(rows(A), columns(A), A, v)

-- pvs2lean: mat_vec_mult.matv_test
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: matvec(3, 0, (: null[real], null[real], null[real] :), null) = (: 0, 0, 0 :)
theorem matv_test : matvec 3 0 ((: null[real], null[real], null[real] :)) null = (: 0, 0, 0 :) := by
  -- === TCCs for matv_test ===
  -- TCC: matv_test_TCC1
  -- Statement: length[list[real]]((: null[real], null[real], null[real] :)) = 3 AND
  --  FORALL (i:
  --            below(length[list[real]]
  --                      ((: null[real], null[real], null[real] :)))):
  --    length[rea...
  have matv_test_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_nth
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: FullMatrix, v: VectorN(columns(A)), i: below(rows(A))):
--   nth(A * v, i) = row(A)(i) * v
theorem matv_nth : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (v : {l : List ℝ // List.length l = columns A}) (i : {s : {i : ℝ // i ≥ 0} // s < rows A}), nth (A * v) i = row A(i) * v := by
  -- === TCCs for matv_nth ===
  -- TCC: matv_nth_TCC1
  -- Statement: FORALL (A: FullMatrix, v: VectorN(columns(A)), i: below(rows(A))):
  --   i < length[real](A * v)
  have matv_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: mat_vec_mult.max_a
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF rows(A) = 0 OR columns(A) = 0 THEN 0
-- ELSE max(max(abs(car(A))), max_a(cdr(A)))
-- ENDIF
axiom max_a {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ33 → pvs_τ34

-- pvs2lean: mat_vec_mult.max_a_max
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: FullMatrix, j: below(columns(A)), i: below(rows(A))):
--   abs(nth(nth(A, i), j)) <= max_a(A)
theorem max_a_max : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (j : {s : {i : ℝ // i ≥ 0} // s < columns A}) (i : {s : {i : ℝ // i ≥ 0} // s < rows A}), |nth (nth A i) j| ≤ max_a A := by
  -- === TCCs for max_a_max ===
  -- TCC: max_a_max_TCC1
  -- Statement: FORALL (A: FullMatrix, j: below(columns(A)), i: below(rows(A))):
  --   i < length[list[real]](A)
  have max_a_max_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_a_max_TCC2
  -- Statement: FORALL (A: FullMatrix, j: below(columns(A)), i: below(rows(A))):
  --   j < length[real](nth[list[real]](A, i))
  have max_a_max_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.max_a_row
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: {AA: FullMatrix | columns(AA) > 0}, v: VectorN(columns(A)),
--         i: below(rows(A))):
--   abs(row(A)(i) * v) <= columns(A) * max_a(A) * norm_inf(columns(A))(v)
theorem max_a_row : ∀ (A : {AA : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // columns AA > 0}) (v : {l : List ℝ // List.length l = columns A}) (i : {s : {i : ℝ // i ≥ 0} // s < rows A}), |row A(i) * v| ≤ columns A * max_a A * norm_inf (columns A) (v) := by
  -- === TCCs for max_a_row ===
  -- TCC: max_a_row_TCC1
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0}, v: VectorN(columns(A)),
  --         i: below(rows(A))):
  --   cons?[real](v)
  have max_a_row_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_a_row_TCC2
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0}, v: VectorN(columns(A)),
  --         i: below(rows(A))):
  --   columns(A) > 0
  have max_a_row_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_norm_inf
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: {AA: FullMatrix | columns(AA) > 0}, v: VectorN(columns(A))):
--   norm_inf(rows(A))(A * v) <=
--    columns(A) * max_a(A) * norm_inf(columns(A))(v)
theorem matv_norm_inf : ∀ (A : {AA : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // columns AA > 0}) (v : {l : List ℝ // List.length l = columns A}), norm_inf (rows A) (A * v) ≤ columns A * max_a A * norm_inf (columns A) (v) := by
  -- === TCCs for matv_norm_inf ===
  -- TCC: matv_norm_inf_TCC1
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0}, v: VectorN(columns(A))):
  --   cons?[real](A * v)
  have matv_norm_inf_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: matv_norm_inf_TCC2
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0}, v: VectorN(columns(A))):
  --   rows(A) > 0
  have matv_norm_inf_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: matv_norm_inf_TCC3
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0}, v: VectorN(columns(A))):
  --   cons?[real](v)
  have matv_norm_inf_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: matv_norm_inf_TCC4
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0}, v: VectorN(columns(A))):
  --   columns(A) > 0
  have matv_norm_inf_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.vec2mat
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF v = null THEN null[list[real]]
-- ELSE cons((: car(v) :), vec2mat(cdr(v)))
-- ENDIF
axiom vec2mat {pvs_τ53 : Type*} : pvs_τ53 → Prop

-- pvs2lean: mat_vec_mult.vec2mat_id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: Vector): col(vec2mat(v))(0) = v
theorem vec2mat_id : ∀ (v : List ℝ), col (vec2mat v) (0) = v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_matmat
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: FullMatrix, v: VectorN(columns(A))):
--   A * v = col(A * vec2mat(v))(0)
theorem matv_matmat : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (v : {l : List ℝ // List.length l = columns A}), A * v = col (A * vec2mat v) (0) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: mat_vec_mult.vec2mat_mult
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: FullMatrix, v: VectorN(columns(A))):
--   columns(A) > 0 IMPLIES vec2mat(A * v) = A * vec2mat(v)
theorem vec2mat_mult : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (v : {l : List ℝ // List.length l = columns A}), columns A > 0 → vec2mat (A * v) = A * vec2mat v := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_dist_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: FullMatrix, v1, v2: VectorN(columns(A))):
--   A * (v1 + v2) = A * v1 + A * v2
theorem matv_dist_sum : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (v1 : {l : List ℝ // List.length l = columns A}) (v2 : {l : List ℝ // List.length l = columns A}), A * (v1 + v2) = A * v1 + A * v2 := by
  -- === TCCs for matv_dist_sum ===
  -- TCC: matv_dist_sum_TCC1
  -- Statement: FORALL (A: FullMatrix, v1, v2: VectorN(columns(A))):
  --   length[real]((v1 + v2)) = columns(A)
  have matv_dist_sum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_dist_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: FullMatrix, v: VectorN(columns(A)), c: real):
--   A * (c * v) = c * (A * v)
theorem matv_dist_scal : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (v : {l : List ℝ // List.length l = columns A}) (c : ℝ), A * (c * v) = c * (A * v) := by
  -- === TCCs for matv_dist_scal ===
  -- TCC: matv_dist_scal_TCC1
  -- Statement: FORALL (A: FullMatrix, v: VectorN(columns(A)), c: real):
  --   length[real]((c * v)) = columns(A)
  have matv_dist_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_dist_sub
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: FullMatrix, v1, v2: VectorN(columns(A))):
--   A * (v1 - v2) = A * v1 - A * v2
theorem matv_dist_sub : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}) (v1 : {l : List ℝ // List.length l = columns A}) (v2 : {l : List ℝ // List.length l = columns A}), A * (v1 - v2) = A * v1 - A * v2 := by
  -- === TCCs for matv_dist_sub ===
  -- TCC: matv_dist_sub_TCC1
  -- Statement: FORALL (A: FullMatrix, v1, v2: VectorN(columns(A))):
  --   length[real]((v1 - v2)) = columns(A)
  have matv_dist_sub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_asso
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: {AA: FullMatrix | columns(AA) > 0},
--         B: {BB: FullMatrix | rows(BB) = columns(A) AND columns(BB) > 0},
--         v: VectorN(columns(B))):
--   A * (B * v) = (A * B) * v
theorem matv_asso : ∀ (A : {AA : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // columns AA > 0}) (B : {BB : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows BB = columns A ∧ columns BB > 0}) (v : {l : List ℝ // List.length l = columns B}), A * (B * v) = (A * B) * v := by
  -- === TCCs for matv_asso ===
  -- TCC: matv_asso_TCC1
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0},
  --         B: {BB: FullMatrix | rows(BB) = columns(A) AND columns(BB) > 0},
  --         v: VectorN(columns(B))):
  --   length[real]((B * v)) = columns(A)
  have matv_asso_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: matv_asso_TCC2
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0},
  --         B: {BB: FullMatrix | rows(BB) = columns(A) AND columns(BB) > 0},
  --         v: VectorN(columns(B))):
  --   length[real](v) = columns((A * B))
  have matv_asso_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_add_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: {AA: FullMatrix | columns(AA) > 0 AND rows(AA) > 0},
--         B: {BB: FullMatrix | rows(BB) = columns(A) AND columns(BB) > 0},
--         y1: VectorN(rows(A)), y2: VectorN(columns(B)),
--         
theorem matv_add_zero : ∀ (A : {AA : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // columns AA > 0 ∧ rows AA > 0}) (B : {BB : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows BB = columns A ∧ columns BB > 0}) (y1 : {l : List ℝ // List.length l = rows A}) (y2 : {l : List ℝ // List.length l = columns B}) (y3 : {l : List ℝ // List.length l = columns A}), y1 - (A * B * y2) = y1 - (A * y3) + A * (y3 - B * y2) := by
  -- === TCCs for matv_add_zero ===
  -- TCC: matv_add_zero_TCC1
  -- Statement: FORALL (A: {AA: FullMatrix | columns(AA) > 0 AND rows(AA) > 0},
  --         B: {BB: FullMatrix | rows(BB) = columns(A) AND columns(BB) > 0},
  --         y1: VectorN(rows(A)), y2: VectorN(columns(B)),
  --      ...
  have matv_add_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: mat_vec_mult.matv_zerovec
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: FullMatrix): A * zero_vec(columns(A)) = zero_vec(rows(A))
theorem matv_zerovec : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)}), A * zero_vec (columns A) = zero_vec (rows A) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.mat_vec_mult