-- Auto-generated by pvs2lean/translator.py
-- PVS theory: matrices
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: matrices

Auto-generated stubs from PVSLib theory `matrices`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.matrices

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (FF : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (GG : pvs_τ3 → pvs_τ4)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (MatrixMN : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (VectorN : pvs_τ9 → pvs_τ10)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (append : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (below : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} (car : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (cdr : pvs_τ22 → pvs_τ23)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (cons : pvs_τ27 → pvs_τ28 → pvs_τ29)
variable {pvs_τ38 : Type*} (length : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (max : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (min : pvs_τ41 → pvs_τ42 → pvs_τ43)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (nth : pvs_τ47 → pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} (null_p : pvs_τ50 → Prop)

-- pvs2lean: matrices.matrixmn?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(M) = m AND FORALL (i: below(length(M))): length(nth(M, i)) = n
noncomputable def matrixmn_p : sorry := by
  sorry  -- pvs2lean: definition stub for length(M) = m AND FORALL (i: below(length(M))): length(nth(M, i)) = n

-- pvs2lean: matrices.fullmatrix?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: null?(M) OR
--  FORALL (i, j: below(length(M))): length(nth(M, i)) = length(nth(M, j))
noncomputable def fullmatrix_p : sorry := by
  sorry  -- pvs2lean: definition stub for null?(M) OR  FORALL (i, j: below(length(M))): length(nth(M, i)) = length(nth(M, 

-- pvs2lean: matrices.zero
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: array2list[real](n)(LAMBDA (p: nat): 0)
noncomputable def zero : sorry := by
  sorry  -- pvs2lean: definition stub for array2list[real](n)(LAMBDA (p: nat): 0)

-- pvs2lean: matrices.MEx
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (: (: 1, 2 :), (: 3, 4, 5 :), (: 6 :) :)
noncomputable def MEx : sorry := by
  sorry  -- pvs2lean: definition stub for (: (: 1, 2 :), (: 3, 4, 5 :), (: 6 :) :)

-- pvs2lean: matrices.length_matrix_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nn: nat, LL: list[listn[real](nn)]):
--   length[list[real]](LL) = length[listn[real](nn)](LL)
theorem length_matrix_eq : ∀ (nn : {i : ℝ // i ≥ 0}) (LL : {x : List (List ℝ) // every (fun (x1 : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
             (x1)
         ∧ List.length x1 = nn)
     (x)}), List.length LL = List.length LL := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.nth_matrix_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nn: nat, LL: list[listn[real](nn)],
--         i: below[length[list[real]](LL)]):
--   nth[listn[real](nn)](LL, i) = nth[list[real]](LL, i)
theorem nth_matrix_eq : ∀ (nn : {i : ℝ // i ≥ 0}) (LL : {x : List (List ℝ) // every (fun (x1 : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
             (x1)
         ∧ List.length x1 = nn)
     (x)}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length LL}), nth LL i = nth LL i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.length_matrix_equiv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nn: nat, LL: list[listn[real](nn)]):
--   length[list[real]](LL) = length[listn[real](nn)](LL)
theorem length_matrix_equiv : ∀ (nn : {i : ℝ // i ≥ 0}) (LL : {x : List (List ℝ) // every (fun (x1 : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
             (x1)
         ∧ List.length x1 = nn)
     (x)}), List.length LL = List.length LL := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.length_matrix_nth
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nn: nat, LL: list[listn[real](nn)],
--         i: below[length[list[real]](LL)]):
--   length[real](nth[list[real]](LL, i)) = nn
theorem length_matrix_nth : ∀ (nn : {i : ℝ // i ≥ 0}) (LL : {x : List (List ℝ) // every (fun (x1 : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
             (x1)
         ∧ List.length x1 = nn)
     (x)}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length LL}), List.length (nth[list[real]](LL, i)) = nn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.matrix_listn_nth
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nn: nat, LL: list[listn[real](nn)],
--         i: below[length[list[real]](LL)],
--         j: below[length[real](nth[listn[real](nn)](LL, i))]):
--   nth[real](nth[listn[real](nn)](LL, i), j) =
--    nth
theorem matrix_listn_nth : ∀ (nn : {i : ℝ // i ≥ 0}) (LL : {x : List (List ℝ) // every (fun (x1 : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
             (x1)
         ∧ List.length x1 = nn)
     (x)}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length LL}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length (nth[listn[real](nn)](LL, i))}), nth (nth[listn[real](nn)](LL, i)) j = nth (nth[list[real]](LL, i)) j := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.access
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < length(v1) THEN nth(v1, i) ELSE 0 ENDIF
axiom access {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13

-- pvs2lean: matrices.rows
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(M)
axiom rows {pvs_τ54 : Type*} : pvs_τ54 → Prop

-- pvs2lean: matrices.length_rows
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(M) = rows(M)
theorem length_rows : List.length M = rows M := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.columns
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(M) THEN 0 ELSE max(length(car(M)), columns(cdr(M))) ENDIF
axiom columns {pvs_τ26 : Type*} : pvs_τ26 → Prop

-- pvs2lean: matrices.row
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i >= length(M) THEN null[real] ELSE nth(M, i) ENDIF
axiom row {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} : pvs_τ51 → pvs_τ52 → pvs_τ53

-- pvs2lean: matrices.col
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(M) THEN null[real]
-- ELSE cons(access(car(M))(i), col(cdr(M))(i))
-- ENDIF
axiom col {pvs_τ24 : Type*} {pvs_τ25 : Type*} : pvs_τ24 → pvs_τ25 → Prop

-- pvs2lean: matrices.col_def
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(col(M)(i)) = rows(M) AND
--  FORALL (j: below(rows(M))):
--    nth(col(M)(i), j) =
--     IF i < length(nth(M, j)) THEN nth(nth(M, j), i) ELSE 0 ENDIF
theorem col_def : List.length (col M(i)) = rows M ∧ ∀ (j : Finset.range rows M), nth (col M(i)) j =
   if i < List.length (nth M j) then nth (nth M j) i else 0 := by
  -- === TCCs for col_def ===
  -- TCC: col_def_TCC1
  -- Statement: FORALL (M: Matrix, i: nat):
  --   length(col(M)(i)) = rows(M) IMPLIES
  --    FORALL (j: below(rows(M))): j < length[real](col(M)(i))
  have col_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: col_def_TCC2
  -- Statement: FORALL (M: Matrix, i: nat):
  --   length(col(M)(i)) = rows(M) IMPLIES
  --    FORALL (j: below(rows(M))): j < length[list[real]](M)
  have col_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.col_zero
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i >= columns(M) IMPLIES col(M)(i) = zero(rows(M))
theorem col_zero : i ≥ columns M → col M(i) = zero (rows M) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.access_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: access(zero(n))(i) = 0
theorem access_zero : access (zero n) (i) = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.nonempty?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(M) > 0 AND columns(M) > 0
noncomputable def nonempty_p : sorry := by
  sorry  -- pvs2lean: definition stub for rows(M) > 0 AND columns(M) > 0

-- pvs2lean: matrices.entry
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: access(row(M)(i))(j)
axiom entry {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop

-- pvs2lean: matrices.entry_test
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry((: (: 1, 2, 0 :), (: 3, 4, 5 :), (: 6, 0, 0 :) :))(1, 1) = 4
theorem entry_test : entry ((: (: 1, 2, 0 :), (: 3, 4, 5 :), (: 6, 0, 0 :) :)) (1, 1) = 4 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.access_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: access(row(M)(i))(j) = entry(M)(i, j)
theorem access_row : access (row M(i)) (j) = entry M(i, j) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.access_col
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: access(col(M)(j))(i) = entry(M)(i, j)
theorem access_col : access (col M(j)) (i) = entry M(i, j) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.coltest
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: col(MEx)(1) = (: 2, 4, 0 :)
theorem coltest : col MEx(1) = (: 2, 4, 0 :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.full_matrix_columns
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (null?(SM) AND columns(SM) = 0) OR columns(SM) = length(car(SM))
theorem full_matrix_columns : (null_p SM ∧ columns SM = 0) ∨ columns SM = List.length (car SM) := by
  -- === TCCs for full_matrix_columns ===
  -- TCC: full_matrix_columns_TCC1
  -- Statement: FORALL (SM: FullMatrix):
  --   NOT (null?(SM) AND columns(SM) = 0) IMPLIES cons?[list[real]](SM)
  have full_matrix_columns_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.rows_mn
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (M: MatrixMN(m, n)): rows(M) = m
theorem rows_mn : ∀ (M : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}), rows M = m := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.columns_mn
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (pm: posnat, n: nat, M: MatrixMN(pm, n)): columns(M) = n
theorem columns_mn : ∀ (pm : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : ℝ // i ≥ 0}) (M : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = pm ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}), columns M = n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.length_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(SM) IMPLIES length(row(SM)(i)) = columns(SM)
theorem length_row : i < List.length SM → List.length (row SM(i)) = columns SM := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.length_col
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(col(SM)(i)) = rows(SM)
theorem length_col : List.length (col SM(i)) = rows SM := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.columns_0_entry
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(M) = 0 IMPLIES entry(M)(i, j) = 0
theorem columns_0_entry : columns M = 0 → entry M(i, j) = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.rows_0_entry
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(M) = 0 IMPLIES entry(M)(i, j) = 0
theorem rows_0_entry : rows M = 0 → entry M(i, j) = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.entry_eq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: i >= rows(M) OR j >= columns(M) IMPLIES entry(M)(i, j) = 0
theorem entry_eq_0 : i ≥ rows M ∨ j ≥ columns M → entry M(i, j) = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.add
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(v1) THEN v2
-- ELSIF null?(v2) THEN v1
-- ELSE cons(car(v1) + car(v2), add(cdr(v1), cdr(v2)))
-- ENDIF
axiom add {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16

-- pvs2lean: matrices.scal
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(v2) THEN v2 ELSE cons(r * car(v2), scal(r, cdr(v2))) ENDIF
axiom scal {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} : pvs_τ55 → pvs_τ56 → pvs_τ57

-- pvs2lean: matrices.sub
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: add(v1, scal(-1, v2))
axiom sub {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} : pvs_τ58 → pvs_τ59 → pvs_τ60

-- pvs2lean: matrices.dot
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(v1) OR null?(v2) THEN 0
-- ELSE car(v1) * car(v2) + dot(cdr(v1), cdr(v2))
-- ENDIF
axiom dot {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: matrices.super_dot
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(v1) OR null?(v2) THEN 0
-- ELSIF car(v1) = 0 THEN dot(cdr(v1), cdr(v2))
-- ELSE car(v1) * car(v2) + dot(cdr(v1), cdr(v2))
-- ENDIF
noncomputable def super_dot : sorry := by
  sorry  -- pvs2lean: definition stub for IF null?(v1) OR null?(v2) THEN 0 ELSIF car(v1) = 0 THEN dot(cdr(v1), cdr(v2)) EL

-- pvs2lean: matrices.super_duper_dot
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF kz = 0 THEN (IF FF(0) = 0 THEN 0 ELSE FF(0) * GG(0) ENDIF)
-- ELSE super_duper_dot(FF, GG, kz - 1) +
--       (IF FF(kz) = 0 THEN 0 ELSE FF(kz) * GG(kz) ENDIF)
-- ENDIF
axiom super_duper_dot {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} : pvs_τ61 → pvs_τ62 → pvs_τ63 → pvs_τ64

-- pvs2lean: matrices.+
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: add(v1, v2)
noncomputable def pvs_op_26b172 : sorry := by
  sorry  -- pvs2lean: definition stub for add(v1, v2)

-- pvs2lean: matrices.*
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: scal(r, v2)
noncomputable def pvs_op_3389da : sorry := by
  sorry  -- pvs2lean: definition stub for scal(r, v2)

-- pvs2lean: matrices.-
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: sub(v1, v2)
noncomputable def pvs_op_336d5e : sorry := by
  sorry  -- pvs2lean: definition stub for sub(v1, v2)

-- pvs2lean: matrices.access_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: access(v1 + v2)(i) = access(v1)(i) + access(v2)(i)
theorem access_sum : access (v1 + v2) (i) = access v1(i) + access v2(i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.access_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: access(r * v)(i) = r * access(v)(i)
theorem access_scal : access (r * v) (i) = r * access v(i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.vect_scal_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 * v = v
theorem vect_scal_1 : 1 * v = v := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.dot_eq_sigma
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: dot(v1, v2) =
--  sigma(0, min(length(v1) - 1, length(v2) - 1),
--        LAMBDA (k: nat): access(v1)(k) * access(v2)(k))
theorem dot_eq_sigma : dot v1 v2 = sigma 0 (min (List.length v1 - 1) (List.length v2 - 1)) (fun (k : ℕ)=> access v1(k) * access v2(k)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.dot_zero_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: v * zero(n) = 0
theorem dot_zero_right : v * zero n = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.dot_commutes
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: v1 * v2 = v2 * v1
theorem dot_commutes : v1 * v2 = v2 * v1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.dot_zero_left
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: zero(n) * v = 0
theorem dot_zero_left : zero n * v = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.length_add_vect
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(v1 + v2) = max(length(v1), length(v2))
theorem length_add_vect : List.length (v1 + v2) = max (List.length v1) (List.length v2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.length_add_vect_same
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(v1) = length(v2) IMPLIES length(v1 + v2) = length(v1)
theorem length_add_vect_same : List.length v1 = List.length v2 → List.length (v1 + v2) = List.length v1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.length_scal_vect
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(r * v1) = length(v1)
theorem length_scal_vect : List.length (r * v1) = List.length v1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.form_matrix
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: array2list[listn[real](n)]
--     (m)(LAMBDA (k: nat): array2list[real](n)(LAMBDA (p: nat): F(k, p)))
axiom form_matrix {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} : pvs_τ35 → pvs_τ36 → pvs_τ37 → Prop

-- pvs2lean: matrices.columns_form_matrix
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (F: [[nat, nat] -> real]): m = 0 OR columns(form_matrix(F, m, n)) = n
theorem columns_form_matrix : ∀ (F : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → ℝ), m = 0 ∨ columns (form_matrix F m n) = n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.rows_form_matrix
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (F: [[nat, nat] -> real]): rows(form_matrix(F, m, n)) = m
theorem rows_form_matrix : ∀ (F : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → ℝ), rows (form_matrix F m n) = m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.form_matrix_empty
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, F: [[nat, nat] -> real]):
--   m = 0 IMPLIES null?(form_matrix(F, m, n))
theorem form_matrix_empty : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → ℝ), m = 0 → null_p (form_matrix F m n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.FEx
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN (IF j = 0 THEN 1 ELSIF j = 1 THEN 2 ELSE 0 ENDIF)
-- ELSIF i = 1 THEN IF j = 0 THEN 3 ELSIF j = 1 THEN 4 ELSE 5 ENDIF
-- ELSIF i = 2 AND j = 0 THEN 6
-- ELSE 0
-- ENDIF
noncomputable def FEx : sorry := by
  sorry  -- pvs2lean: definition stub for IF i = 0 THEN (IF j = 0 THEN 1 ELSIF j = 1 THEN 2 ELSE 0 ENDIF) ELSIF i = 1 THEN

-- pvs2lean: matrices.form_matrix_test1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(FEx, 3, 3) = (: (: 1, 2, 0 :), (: 3, 4, 5 :), (: 6, 0, 0 :) :)
theorem form_matrix_test1 : form_matrix FEx 3 3 = (: (: 1, 2, 0 :), (: 3, 4, 5 :), (: 6, 0, 0 :) :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.full_matrix_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: SM1 = SM2 IFF
--  (rows(SM1) = rows(SM2) AND
--    columns(SM1) = columns(SM2) AND
--     FORALL (i: below(rows(SM1)), j: below(columns(SM1))):
--       entry(SM1)(i, j) = entry(SM2)(i, j))
theorem full_matrix_eq : SM1 = SM2 ↔ (rows SM1 = rows SM2 ∧
  columns SM1 = columns SM2 ∧
   ∀ (i : Finset.range rows SM1) (j : Finset.range columns SM1), entry SM1(i, j) = entry SM2(i, j)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.matrix2array
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: SM = form_matrix(entry(SM), rows(SM), columns(SM))
theorem matrix2array : SM = form_matrix (entry SM) (rows SM) (columns SM) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.entry_form_matrix
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(form_matrix(F, m, n))(i, j) =
--  IF i < m AND j < n THEN F(i, j) ELSE 0 ENDIF
theorem entry_form_matrix : entry (form_matrix F m n) (i, j) = if i < m ∧ j < n then F i j else 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.entry_form_matrix2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < m AND j < n IMPLIES entry(form_matrix(F, m, n))(i, j) = F(i, j)
theorem entry_form_matrix2 : i < m ∧ j < n → entry (form_matrix F m n) (i, j) = F i j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.form_matrix_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(F, m, n) = form_matrix(G, m, n) IFF
--  FORALL (i, j): i < m AND j < n IMPLIES F(i, j) = G(i, j)
theorem form_matrix_eq : form_matrix F m n = form_matrix G m n ↔ ∀ (i : _) (j : _), i < m ∧ j < n → F i j = G i j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.matrix_reduce_prop
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [Matrix -> bool]):
--   (FORALL (F): P(form_matrix(F, m, n))) IMPLIES
--    (FORALL (M: MatrixMN(m, n)): P(M))
theorem matrix_reduce_prop : ∀ (P : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} → Bool), (∀ (F : _), P (form_matrix F m n)) → (∀ (M : MatrixMN m n), P M) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.mult
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (i, j: nat): dot(row(M)(i), col(N)(j)), rows(M),
--             columns(N))
axiom mult {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} : pvs_τ44 → pvs_τ45 → pvs_τ46

-- pvs2lean: matrices.mult_null_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: null[list[real]] * M = null[list[real]]
theorem mult_null_left : null[list[real]] * M = null[list[real]] := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.mult_null_right
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET N = M * null[list[real]] IN
--   length(N) = length(M) AND
--    FORALL (i: below(length(N))): nth(N, i) = null[real]
theorem mult_null_right : fun (N : {x : {A : {M_1 : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M_1 = rows M ∧
 ∀ (i : Finset.range List.length M_1), List.length (nth M_1 i) = columns null[list[real]]} // ∀ (i : ℕ) (j : ℕ), entry A(i, j) = row M(i) * col null[list[real]](j)} // null_p x ∨
 ∀ (i : _) (j : Finset.range List.length x), List.length (nth x i) = List.length (nth x j)}) => List.length N = List.length M ∧ ∀ (i : Finset.range List.length N), nth N i = null[real] (M * null[list[real]]) := by
  -- === TCCs for mult_null_right ===
  -- TCC: mult_null_right_TCC1
  -- Statement: FORALL (M: Matrix):
  --   null?[list[real]](M * null[list[real]]) OR
  --    FORALL (i, j: below(length[list[real]](M * null[list[real]]))):
  --      length[real](nth[list[real]](M * null[list[real]], i)) =
  --    ...
  have mult_null_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.columns_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (NOT null?(M)) IMPLIES columns(M * N) = columns(N)
theorem columns_mult : (¬ null_p M) → columns (M * N) = columns N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.rows_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(M * N) = rows(M)
theorem rows_mult : rows (M * N) = rows M := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.columns_append
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(append(M, N)) = max(columns(M), columns(N))
theorem columns_append : columns (append M N) = max (columns M) (columns N) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.append_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (NOT null?(M)) IMPLIES append(M, N) * A = append(M * A, N * A)
theorem append_mult : (¬ null_p M) → append M N * A = append (M * A) (N * A) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.matrix_mult_assoc
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (NOT null?(N)) IMPLIES (M * N) * A = M * (N * A)
theorem matrix_mult_assoc : (¬ null_p N) → (M * N) * A = M * (N * A) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.entry_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(M * N)(i, j) =
--  IF i < rows(M) AND j < columns(N) THEN row(M)(i) * col(N)(j) ELSE 0 ENDIF
theorem entry_mult : entry (M * N) (i, j) = if i < rows M ∧ j < columns N then row M(i) * col N(j) else 0 := by
  -- === TCCs for entry_mult ===
  -- TCC: entry_mult_TCC1
  -- Statement: FORALL (l1, l2: (cons?[real])):
  --   NOT length(l1) = 1 AND NOT length(l2) = 1 IMPLIES
  --    cons?[real](cdr[real](l2))
  have entry_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: entry_mult_TCC2
  -- Statement: FORALL (l1, l2: (cons?[real])):
  --   NOT length(l1) = 1 AND NOT length(l2) = 1 IMPLIES
  --    length[real](cdr[real](l1)) + length[real](cdr[real](l2)) <
  --     length[real](l1) + length[real](l2)
  have entry_mult_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.form_matrix_mult
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (F, G: [[nat, nat] -> real], k, m, n: nat):
--   m > 0 AND k > 0 IMPLIES
--    form_matrix(F, k, m) * form_matrix(G, m, n) =
--     form_matrix(LAMBDA (i, j: nat):
--                   IF i > k OR j > n TH
theorem form_matrix_mult : ∀ (F : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → ℝ) (G : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → ℝ) (k : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}), m > 0 ∧ k > 0 → form_matrix F k m * form_matrix G m n =
 form_matrix (fun (i : _) (j : ℕ)=> if i > k ∨ j > n then 0
               else Finset.sum (Finset.Icc 0 m - 1) (fun (d : ℕ)=> F i d * G d j)) k n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrices.columns_add
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(M + N) = max(columns(M), columns(N))
theorem columns_add : columns (M + N) = max (columns M) (columns N) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.rows_add
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(M + N) = max(rows(M), rows(N))
theorem rows_add : rows (M + N) = max (rows M) (rows N) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.matrix_add_assoc
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (M + N) + A = M + (N + A)
theorem matrix_add_assoc : (M + N) + A = M + (N + A) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.matrix_add_comm
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: M + N = N + M
theorem matrix_add_comm : M + N = N + M := by
  exact Matrix.add_comm

-- pvs2lean: matrices.columns_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(r * M) = columns(M)
theorem columns_scal : columns (r * M) = columns M := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.rows_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(r * M) = rows(M)
theorem rows_scal : rows (r * M) = rows M := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.rows_sub
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(M - N) = max(rows(M), rows(N))
theorem rows_sub : rows (M - N) = max (rows M) (rows N) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.columns_sub
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(M - N) = max(columns(M), columns(N))
theorem columns_sub : columns (M - N) = max (columns M) (columns N) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.matrix_sub_test
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (: (: 1, 2, 3 :), (: 4, 5, 6 :), (: 7, 8, 9 :) :) -
--  (: (: 9, 8, 7 :), (: 6, 5, 4 :), (: 3, 2, 1 :) :)
--  = (: (: -8, -6, -4 :), (: -2, 0, 2 :), (: 4, 6, 8 :) :)
theorem matrix_sub_test : (: (: 1, 2, 3 :), (: 4, 5, 6 :), (: 7, 8, 9 :) :) -
 (: (: 9, 8, 7 :), (: 6, 5, 4 :), (: 3, 2, 1 :) :) = (: (: -8, -6, -4 :), (: -2, 0, 2 :), (: 4, 6, 8 :) :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.Id
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (i, j: nat): IF i = j THEN 1 ELSE 0 ENDIF, pm, pm)
axiom Id {pvs_τ5 : Type*} : pvs_τ5 → Prop

-- pvs2lean: matrices.mult_Id_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D1) = pm IMPLIES Id(pm) * D1 = D1
theorem mult_Id_left : rows D1 = pm → Id pm * D1 = D1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.mult_Id_right
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D1) = pm IMPLIES D1 * Id(pm) = D1
theorem mult_Id_right : columns D1 = pm → D1 * Id pm = D1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.rows_Id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(Id(pm)) = pm
theorem rows_Id : rows (Id pm) = pm := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.columns_Id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(Id(pm)) = pm
theorem columns_Id : columns (Id pm) = pm := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.entry_Id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(Id(pm))(i, j) = IF i >= pm OR j >= pm OR i /= j THEN 0 ELSE 1 ENDIF
theorem entry_Id : entry (Id pm) (i, j) = if i ≥ pm ∨ j ≥ pm ∨ i ≠ j then 0 else 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.transpose
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (i, j: nat): entry(PFM)(j, i), columns(PFM), rows(PFM))
axiom transpose {pvs_τ65 : Type*} : pvs_τ65 → Prop

-- pvs2lean: matrices.rows_transpose
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(transpose(PFM)) = columns(PFM)
theorem rows_transpose : rows (transpose PFM) = columns PFM := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.columns_transpose
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(transpose(PFM)) = rows(PFM)
theorem columns_transpose : columns (transpose PFM) = rows PFM := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.entry_transpose
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(transpose(PFM))(i, j) = entry(PFM)(j, i)
theorem entry_transpose : entry (transpose PFM) (i, j) = entry PFM(j, i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.transpose_transpose
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: transpose(transpose(PFM)) = PFM
theorem transpose_transpose : transpose (transpose PFM) = PFM := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.transpose_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D1) = rows(D2) IMPLIES
--  transpose(D1 * D2) = transpose(D2) * transpose(D1)
theorem transpose_mult : columns D1 = rows D2 → transpose (D1 * D2) = transpose D2 * transpose D1 := by
  -- === TCCs for transpose_mult ===
  -- TCC: transpose_mult_TCC1
  -- Statement: FORALL (D1, D2: PosFullMatrix):
  --   columns(D1) = rows(D2) IMPLIES rows(D1 * D2) > 0 AND columns(D1 * D2) > 0
  have transpose_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.transpose_Id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: transpose(Id(pm)) = Id(pm)
theorem transpose_Id : transpose (Id pm) = Id pm := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrices.vect2matrix
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (i, j): IF i = 0 THEN access(v)(j) ELSE 0 ENDIF, 1,
--             length(v))
axiom vect2matrix {pvs_τ66 : Type*} : pvs_τ66 → Prop

-- pvs2lean: matrices.vect2matrix_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(v1) > 0 AND length(v2) > 0 AND vect2matrix(v1) = vect2matrix(v2)
--  IMPLIES v1 = v2
theorem vect2matrix_eq : List.length v1 > 0 ∧ List.length v2 > 0 ∧ vect2matrix v1 = vect2matrix v2 → v1 = v2 := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.matrices