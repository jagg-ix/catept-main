-- Auto-generated by pvs2lean/translator.py
-- PVS theory: alt_inverse_mat
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: alt_inverse_mat

Auto-generated stubs from PVSLib theory `alt_inverse_mat`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.alt_inverse_mat

noncomputable section

variable {pvs_τ1 : Type*} (Id : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (SquareMatrix : pvs_τ2 → pvs_τ3)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (length : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (rows : pvs_τ8 → Prop)

-- pvs2lean: alt_inverse_mat.inv?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (Iv: Square):
--   rows(Iv) = rows(A) AND Iv * A = Id(rows(A)) AND A * Iv = Id(rows(A))
axiom inv_p {pvs_τ5 : Type*} : pvs_τ5 → Prop

-- pvs2lean: alt_inverse_mat.inv
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom inv {pvs_τ4 : Type*} : pvs_τ4 → Prop

-- pvs2lean: alt_inverse_mat.inv_unique
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: Square, invA: SquareMatrix(length(A))):
--   (invA * A = Id(rows(A)) AND A * invA = Id(rows(A))) IMPLIES inv(A) = invA
theorem inv_unique : ∀ (A : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D}) (invA : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = List.length A}), (invA * A = Id (rows A) ∧ A * invA = Id (rows A)) → inv A = invA := by
  -- === TCCs for inv_unique ===
  -- TCC: inv_unique_TCC1
  -- Statement: FORALL (A: Square, invA: SquareMatrix(length[list[real]](A))): rows(A) > 0
  have inv_unique_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inv_unique_TCC2
  -- Statement: FORALL (A: Square, invA: SquareMatrix(length[list[real]](A))):
  --   invA * A = Id(rows(A)) AND A * invA = Id(rows(A)) IMPLIES inv?(A)
  have inv_unique_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.alt_inverse_mat