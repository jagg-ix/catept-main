-- Auto-generated by pvs2lean/translator.py
-- PVS theory: norms
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: norms

Auto-generated stubs from PVSLib theory `norms`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.norms

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (VectorN : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (below : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (car : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (cdr : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (cons : pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (cons_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (constant_vec : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ20 : Type*} (length : pvs_τ20 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (nn : pvs_τ23 → pvs_τ24)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (nth : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (rho : pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (zero_vec : pvs_τ40 → pvs_τ41)

-- pvs2lean: norms.norm?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (v1: VectorN(m)): rho(v1) >= 0) AND
--  (FORALL (v1: VectorN(m), c: real): rho(c * v1) = abs(c) * rho(v1)) AND
--   (FORALL (v1: VectorN(m)): (rho(v1) = 0 IFF v1 = zero_vec(m))) AND
--    (FORALL (v1, 
axiom norm_p {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ33 → pvs_τ34 → Prop

-- pvs2lean: norms.max
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(ll) = 1 THEN car(ll) ELSE max(car(ll), max(cdr(ll))) ENDIF
axiom max {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: norms.max_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)): EXISTS (i: below(length(ll))): max(ll) = nth(ll, i)
theorem max_nth : ∀ (ll : List ℝ), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < List.length ll}), max ll = nth ll i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.entry_mult
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(l1) = 1 THEN (: car(l1) * car(l2) :)
-- ELSIF length(l2) = 1 THEN (: car(l1) * car(l2) :)
-- ELSE cons(car(l1) * car(l2), entry_mult(cdr(l1), cdr(l2)))
-- ENDIF
axiom entry_mult {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: norms.entry_sum
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(ll) = 1 THEN car(ll) ELSE car(ll) + entry_sum(cdr(ll)) ENDIF
axiom entry_sum {pvs_τ19 : Type*} : pvs_τ19 → Prop

-- pvs2lean: norms.abs
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(ll) = 1 THEN cons(abs(car(ll)), null)
-- ELSE cons(abs(car(ll)), abs(cdr(ll)))
-- ENDIF
noncomputable def abs : sorry := by
  sorry  -- pvs2lean: definition stub for IF length(ll) = 1 THEN cons(abs(car(ll)), null) ELSE cons(abs(car(ll)), abs(cdr(

-- pvs2lean: norms.abs_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)):
--   FORALL (i: below(length(ll))): abs(nth(ll, i)) = nth(abs(ll), i)
theorem abs_nth : ∀ (ll : List ℝ), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length ll}), |nth ll i| = nth |ll| i := by
  -- === TCCs for abs_nth ===
  -- TCC: abs_nth_TCC1
  -- Statement: FORALL (ll: (cons?[real]), i: below(length[real](ll))):
  --   i < length[real](abs(ll))
  have abs_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.test_max_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max((: -3, -4, 1 :)) = 1
theorem test_max_1 : max ((: -3, -4, 1 :)) = 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: norms.test_abs_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs((: -3, -4, 1 :)) = (: 3, 4, 1 :)
theorem test_abs_1 : |(: -3, -4, 1 :)| = (: 3, 4, 1 :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: norms.test_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry_sum((: -1, -10, 4 :)) = -7
theorem test_sum : entry_sum ((: -1, -10, 4 :)) = -7 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.test_entry_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry_mult((: -1, -2, 4 :), (: -1, -2, 4 :)) = (: 1, 4, 16 :)
theorem test_entry_mult : entry_mult ((: -1, -2, 4 :)) ((: -1, -2, 4 :)) = (: 1, 4, 16 :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.test_entry_mult_2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry_mult((: -1, -2 :), (: -1, -2, 4 :)) = (: 1, 4 :)
theorem test_entry_mult_2 : entry_mult ((: -1, -2 :)) ((: -1, -2, 4 :)) = (: 1, 4 :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.abs_max_lists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?), c: real): max(abs(c * ll)) = abs(c) * max(abs(ll))
theorem abs_max_lists : ∀ (ll : List ℝ) (c : ℝ), max (|c * ll|) = |c| * max |ll| := by
  -- === TCCs for abs_max_lists ===
  -- TCC: abs_max_lists_TCC1
  -- Statement: FORALL (ll: (cons?[real]), c: real): cons?[real](c * ll)
  have abs_max_lists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: abs_max_lists_TCC2
  -- Statement: FORALL (ll: (cons?[real]), c: real): cons?[real](abs(c * ll))
  have abs_max_lists_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: abs_max_lists_TCC3
  -- Statement: FORALL (ll: (cons?[real])): cons?[real](abs(ll))
  have abs_max_lists_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.abs_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)): max(abs(ll)) >= 0
theorem abs_pos : ∀ (ll : List ℝ), max |ll| ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.abs_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)):
--   max(abs(ll)) = 0 IFF EXISTS (m: posnat): ll = zero_vec(m)
theorem abs_zero : ∀ (ll : List ℝ), max |ll| = 0 ↔ ∃ (m : {n : ℕ // 0 < n}), ll = zero_vec m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.abs_max_triag
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)):
--   length(l1) = length(l2) IMPLIES
--    max(abs(l1 + l2)) <= max(abs(l1)) + max(abs(l2))
theorem abs_max_triag : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → max (|l1 + l2|) ≤ max |l1| + max |l2| := by
  -- === TCCs for abs_max_triag ===
  -- TCC: abs_max_triag_TCC1
  -- Statement: FORALL (l1, l2: (cons?[real])):
  --   length(l1) = length(l2) IMPLIES cons?[real](l1 + l2)
  have abs_max_triag_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: abs_max_triag_TCC2
  -- Statement: FORALL (l1, l2: (cons?[real])):
  --   length(l1) = length(l2) IMPLIES cons?[real](abs(l1 + l2))
  have abs_max_triag_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: abs_max_triag_TCC3
  -- Statement: FORALL (l1, l2: (cons?[real])):
  --   length(l1) = length(l2) IMPLIES cons?[real](abs(l2))
  have abs_max_triag_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.sum_abs_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)): entry_sum(abs(ll)) >= 0
theorem sum_abs_pos : ∀ (ll : List ℝ), entry_sum |ll| ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.sum_abs_lists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?), c: real):
--   entry_sum(abs(c * ll)) = abs(c) * entry_sum(abs(ll))
theorem sum_abs_lists : ∀ (ll : List ℝ) (c : ℝ), entry_sum (|c * ll|) = |c| * entry_sum |ll| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.sum_abs_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)):
--   entry_sum(abs(ll)) = 0 IFF EXISTS (m: posnat): ll = zero_vec(m)
theorem sum_abs_zero : ∀ (ll : List ℝ), entry_sum |ll| = 0 ↔ ∃ (m : {n : ℕ // 0 < n}), ll = zero_vec m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.sum_abs_triag
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)):
--   length(l1) = length(l2) IMPLIES
--    entry_sum(abs(l1 + l2)) <= entry_sum(abs(l1)) + entry_sum(abs(l2))
theorem sum_abs_triag : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → entry_sum (|l1 + l2|) ≤ entry_sum |l1| + entry_sum |l2| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (ll: (cons?)): max(abs(ll))
axiom norm_inf {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} : pvs_τ30 → pvs_τ31 → pvs_τ32

-- pvs2lean: norms.norm_1
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (ll: (cons?)): entry_sum(abs(ll))
axiom norm_1 {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ25 → pvs_τ26 → pvs_τ27

-- pvs2lean: norms.norm_2sq
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: ll * ll
axiom norm_2sq {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ28 → pvs_τ29

-- pvs2lean: norms.norm_2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (v: (cons?)): sqrt(norm_2sq(v))
noncomputable def norm_2 : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA (v: (cons?)): sqrt(norm_2sq(v))

-- pvs2lean: norms.norm_inf_recurse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat):
--   FORALL (u: VectorN(m)):
--     IF m > 1
--       THEN norm_inf(m)(u) = max(abs(car(u)), norm_inf(m - 1)(cdr(u)))
--     ELSE norm_inf(m)(u) = abs(car(u))
--     ENDIF
theorem norm_inf_recurse : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}), ∀ (u : {l : List ℝ // List.length l = m}), IF (m > 1) (norm_inf m(u) = max (|car u|) (norm_inf (m - 1) (cdr u))) (norm_inf m(u) = |car u|) := by
  -- === TCCs for norm_inf_recurse ===
  -- TCC: norm_inf_recurse_TCC1
  -- Statement: FORALL (m: posnat, u: VectorN(m)): m > 1 IMPLIES cons?[real](cdr[real](u))
  have norm_inf_recurse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: norm_inf_recurse_TCC2
  -- Statement: FORALL (m: posnat, u: VectorN(m)): m > 1 IMPLIES m - 1 >= 0 AND m - 1 > 0
  have norm_inf_recurse_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_one_recurse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat):
--   FORALL (u: VectorN(m)):
--     IF m > 1 THEN norm_1(m)(u) = abs(car(u)) + norm_1(m - 1)(cdr(u))
--     ELSE norm_1(m)(u) = abs(car(u))
--     ENDIF
theorem norm_one_recurse : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}), ∀ (u : {l : List ℝ // List.length l = m}), IF (m > 1) (norm_1 m(u) = |car u| + norm_1 (m - 1) (cdr u)) (norm_1 m(u) = |car u|) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.test_abs
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: norm_inf((: -1, -10, 4 :)) = 10
theorem test_abs : norm_inf ((: -1, -10, 4 :)) = 10 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.test_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: norm_1((: -1, -10, 4 :)) = 15
theorem test_1 : norm_1 ((: -1, -10, 4 :)) = 15 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.test_2sq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: norm_2sq((: -1, -10, 4 :)) = 117
theorem test_2sq : norm_2sq ((: -1, -10, 4 :)) = 117 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.big_kahuna
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max((: abs(5 - 3 * norm_inf((: -1, -10, 4 :) + (: 5, 7, 2 :))), 4 :)) = 13
theorem big_kahuna : max ((: |5 - 3 * norm_inf ((: -1, -10, 4 :) + (: 5, 7, 2 :))|, 4 :)) = 13 := by
  -- === TCCs for big_kahuna ===
  -- TCC: big_kahuna_TCC1
  -- Statement: cons?[real]((: -1, -10, 4 :) + (: 5, 7, 2 :))
  have big_kahuna_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf_constant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat, c: real): norm_inf(m)(constant_vec(c, m)) = abs(c)
theorem norm_inf_constant : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (c : ℝ), norm_inf m(constant_vec c m) = |c| := by
  -- === TCCs for norm_inf_constant ===
  -- TCC: norm_inf_constant_TCC1
  -- Statement: FORALL (m: posnat, c: real): cons?[real](constant_vec(c, m))
  have norm_inf_constant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_reflex
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat, nn: (norm?(m)), v1, v2: VectorN(m)):
--   nn(v1 - v2) = nn(v2 - v1)
theorem norm_reflex : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (nn : List ℝ → {x : ℝ // x ≥ 0}) (v1 : {l : List ℝ // List.length l = m}) (v2 : {l : List ℝ // List.length l = m}), nn (v1 - v2) = nn (v2 - v1) := by
  -- === TCCs for norm_reflex ===
  -- TCC: norm_reflex_TCC1
  -- Statement: FORALL (m: posnat, nn: (norm?(m)), v1, v2: VectorN(m)): cons?[real](v1 - v2)
  have norm_reflex_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: norm_reflex_TCC2
  -- Statement: FORALL (m: posnat, nn: (norm?(m)), v1, v2: VectorN(m)): cons?[real](v2 - v1)
  have norm_reflex_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.reverse_triag
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat, u, v: VectorN(m), nn: (norm?(m))):
--   abs(nn(u) - nn(v)) <= nn(u - v)
theorem reverse_triag : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (u : {l : List ℝ // List.length l = m}) (v : {l : List ℝ // List.length l = m}) (nn : List ℝ → {x : ℝ // x ≥ 0}), |nn u - nn v| ≤ nn (u - v) := by
  -- === TCCs for reverse_triag ===
  -- TCC: reverse_triag_TCC1
  -- Statement: FORALL (m: posnat, u, v: VectorN(m), nn: (norm?(m))): cons?[real](v)
  have reverse_triag_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)):
--   EXISTS (i: below(length(ll))): norm_inf(ll) = abs(nth(ll, i))
theorem norm_inf_nth : ∀ (ll : List ℝ), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < List.length ll}), norm_inf ll = |nth ll i| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf_nth_m
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat, u: VectorN(m)):
--   EXISTS (i: below(m)): norm_inf(m)(u) = abs(nth(u, i))
theorem norm_inf_nth_m : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (u : {l : List ℝ // List.length l = m}), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < m}), norm_inf m(u) = |nth u i| := by
  -- === TCCs for norm_inf_nth_m ===
  -- TCC: norm_inf_nth_m_TCC1
  -- Statement: FORALL (m: posnat, u: VectorN(m), i: below(m)): i < length[real](u)
  have norm_inf_nth_m_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf_nth_ineq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)):
--   FORALL (i: below(length(ll))): norm_inf(ll) >= abs(nth(ll, i))
theorem norm_inf_nth_ineq : ∀ (ll : List ℝ), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length ll}), norm_inf ll ≥ |nth ll i| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf_nth_ineq_m
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat, u: VectorN(m)):
--   FORALL (i: below(m)): norm_inf(m)(u) >= abs(nth(u, i))
theorem norm_inf_nth_ineq_m : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (u : {l : List ℝ // List.length l = m}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < m}), norm_inf m(u) ≥ |nth u i| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf_canc
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat, v1, v2, v3: VectorN(m)):
--   norm_inf(m)((v1 - v2) + (v2 - v3)) = norm_inf(m)(v1 - v3)
theorem norm_inf_canc : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (v1 : {l : List ℝ // List.length l = m}) (v2 : {l : List ℝ // List.length l = m}) (v3 : {l : List ℝ // List.length l = m}), norm_inf m((v1 - v2) + (v2 - v3)) = norm_inf m(v1 - v3) := by
  -- === TCCs for norm_inf_canc ===
  -- TCC: norm_inf_canc_TCC1
  -- Statement: FORALL (m: posnat, v1, v2, v3: VectorN(m)):
  --   cons?[real]((v1 - v2) + (v2 - v3))
  have norm_inf_canc_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: norm_inf_canc_TCC2
  -- Statement: FORALL (m: posnat, v1, v2, v3: VectorN(m)): cons?[real](v1 - v3)
  have norm_inf_canc_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf_e
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, i: below(n)): norm_inf(n)(e(n, i)) = 1
theorem norm_inf_e : ∀ (n : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < n}), norm_inf n(e n i) = 1 := by
  -- === TCCs for norm_inf_e ===
  -- TCC: norm_inf_e_TCC1
  -- Statement: FORALL (n: nat, i: below(n)): cons?[real](e(n, i))
  have norm_inf_e_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: norm_inf_e_TCC2
  -- Statement: FORALL (n: nat, i: below(n)): n > 0
  have norm_inf_e_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.norm_inf_e_scal
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, i: below(n), c: real): norm_inf(n)(c * e(n, i)) = abs(c)
theorem norm_inf_e_scal : ∀ (n : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < n}) (c : ℝ), norm_inf n(c * e n i) = |c| := by
  -- === TCCs for norm_inf_e_scal ===
  -- TCC: norm_inf_e_scal_TCC1
  -- Statement: FORALL (n: nat, i: below(n), c: real): cons?[real](c * e(n, i))
  have norm_inf_e_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.dot_max_ineq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   length(v) = length(u) IMPLIES
--    abs(v * u) <= length(v) * norm_inf(v) * norm_inf(u)
theorem dot_max_ineq : ∀ (u : List ℝ) (v : List ℝ), List.length v = List.length u → |v * u| ≤ List.length v * norm_inf v * norm_inf u := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: norms.dot_equality_delta
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?), delta: posreal):
--   (length(u) = length(v) AND
--     (FORALL (w:
--                {ww: VectorN(length(u)) | norm_inf(length(u))(ww) < delta}):
--        u * w = v * w))
--    IMPLIES u = v
theorem dot_equality_delta : ∀ (u : List ℝ) (v : List ℝ) (delta : {x : {x : ℝ // x ≥ 0} // x > 0}), (List.length u = List.length v ∧
  (∀ (w : {x : VectorN (List.length u) // norm_inf (List.List.length u) (x) < delta}), u * w = v * w)) → u = v := by
  -- === TCCs for dot_equality_delta ===
  -- TCC: dot_equality_delta_TCC1
  -- Statement: FORALL (u, v: (cons?[real])):
  --   length(u) = length(v) IMPLIES
  --    FORALL (ww: VectorN(length[real](u))): cons?[real](ww)
  have dot_equality_delta_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dot_equality_delta_TCC2
  -- Statement: FORALL (u, v: (cons?[real])):
  --   length(u) = length(v) IMPLIES
  --    FORALL (ww: VectorN(length[real](u))): length[real](u) > 0
  have dot_equality_delta_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.norms