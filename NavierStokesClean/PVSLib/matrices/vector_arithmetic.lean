-- Auto-generated by pvs2lean/translator.py
-- PVS theory: vector_arithmetic
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: vector_arithmetic

Auto-generated stubs from PVSLib theory `vector_arithmetic`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.vector_arithmetic

noncomputable section

variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (VectorN : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (below : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (car : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (cdr : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (cons : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (cons_p : pvs_τ15 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (every : pvs_τ22 → pvs_τ23)
variable {pvs_τ28 : Type*} (integer_pred : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} (length : pvs_τ29 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (min : pvs_τ34 → pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (nth : pvs_τ36 → pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} (number_field_pred : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} (rational_pred : pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} (real_pred : pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (restrict : pvs_τ41 → pvs_τ42)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} (scal : pvs_τ43 → pvs_τ44 → pvs_τ45)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (subrange : pvs_τ46 → pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} (well_founded_p : pvs_τ49 → Prop)

-- pvs2lean: vector_arithmetic.wf_1
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_founded?(LAMBDA (x: (cons?), y: (cons?)):
--                 restrict
--                     [[list[real], list[real]],
--                      [(cons?[real]), (cons?[real])], boolean]
--                    
theorem wf_1 : well_founded_p (fun (x : List ℝ) (y : List ℝ) => restrict pvs_op_9c1628 x y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.zero_vec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = 0 THEN null ELSE cons(0, zero_vec(m - 1)) ENDIF
axiom zero_vec {pvs_τ50 : Type*} : pvs_τ50 → Prop

-- pvs2lean: vector_arithmetic.zero_vec0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, v: VectorN(m)):
--   v = zero_vec(m) IFF FORALL (i: below(m)): nth(v, i) = 0
theorem zero_vec0 : ∀ (m : {i : ℝ // i ≥ 0}) (v : {l : List ℝ // List.length l = m}), v = zero_vec m ↔ ∀ (i : Finset.range m), nth v i = 0 := by
  -- === TCCs for zero_vec0 ===
  -- TCC: zero_vec0_TCC1
  -- Statement: FORALL (m: nat, v: VectorN(m), i: below(m)): i < length[real](v)
  have zero_vec0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.nth_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat): FORALL (i: below(m)): nth(zero_vec(m), i) = 0
theorem nth_zero : ∀ (m : {i : ℝ // i ≥ 0}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < m}), nth (zero_vec m) i = 0 := by
  -- === TCCs for nth_zero ===
  -- TCC: nth_zero_TCC1
  -- Statement: FORALL (m: nat, i: below(m)): i < length[real](zero_vec(m))
  have nth_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.zero_cdr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat): cdr(zero_vec(m)) = zero_vec(m - 1)
theorem zero_cdr : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}), cdr (zero_vec m) = zero_vec (m - 1) := by
  -- === TCCs for zero_cdr ===
  -- TCC: zero_cdr_TCC1
  -- Statement: FORALL (m: posnat): cons?[real](zero_vec(m))
  have zero_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zero_cdr_TCC2
  -- Statement: FORALL (m: posnat): m - 1 >= 0
  have zero_cdr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.every_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat):
--   every[number]
--       (LAMBDA (x: number):
--               number_field_pred(x) AND real_pred(x) AND rational_pred(x)
--           AND integer_pred(x) AND (x >= 0))
--       (zero_vec(n))
theorem every_zero : ∀ (n : {i : ℝ // i ≥ 0}), every (fun (x : ℝ) => number_field_pred x ∧ real_pred x ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (zero_vec n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.constant_vec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = 0 THEN null ELSE cons(c, constant_vec(c, m - 1)) ENDIF
axiom constant_vec {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: vector_arithmetic.e
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = 0 THEN null
-- ELSIF i > 0 THEN cons(0, e(m - 1, i - 1))
-- ELSIF i = 0 THEN cons(1, zero_vec(m - 1))
-- ELSE zero_vec(m)
-- ENDIF
noncomputable def e : sorry := by
  sorry  -- pvs2lean: definition stub for IF m = 0 THEN null ELSIF i > 0 THEN cons(0, e(m - 1, i - 1)) ELSIF i = 0 THEN co

-- pvs2lean: vector_arithmetic.e_cdr
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat, i: below(m)):
--   i > 0 IMPLIES cdr(e(m, i)) = e(m - 1, i - 1)
theorem e_cdr : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : {s : {i : ℝ // i ≥ 0} // s < m}), i > 0 → cdr (e m i) = e (m - 1) (i - 1) := by
  -- === TCCs for e_cdr ===
  -- TCC: e_cdr_TCC1
  -- Statement: FORALL (m: posnat, i: below(m)): i > 0 IMPLIES cons?[real](e(m, i))
  have e_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: e_cdr_TCC2
  -- Statement: FORALL (m: posnat, i: below(m)): i > 0 IMPLIES i - 1 >= 0 AND i - 1 < m - 1
  have e_cdr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: vector_arithmetic.nth_e
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, i: below(m)):
--   FORALL (j: below(m)):
--     (j = i IMPLIES nth(e(m, i), j) = 1) AND
--      (j /= i IMPLIES nth(e(m, i), j) = 0)
theorem nth_e : ∀ (m : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < m}), ∀ (j : {s : {i : ℝ // i ≥ 0} // s < m}), (j = i → nth (e m i) j = 1) ∧ (j ≠ i → nth (e m i) j = 0) := by
  -- === TCCs for nth_e ===
  -- TCC: nth_e_TCC1
  -- Statement: FORALL (m: nat, i: below(m), j: below(m)):
  --   j = i IMPLIES j < length[real](e(m, i))
  have nth_e_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_e_TCC2
  -- Statement: FORALL (m: nat, i: below(m), j: below(m)):
  --   (j = i IMPLIES nth(e(m, i), j) = 1) AND j /= i IMPLIES
  --    j < length[real](e(m, i))
  have nth_e_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: vector_arithmetic.every_e
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, k: {ii: nat | ii < n}):
--   every[number]
--       (LAMBDA (x: number):
--               number_field_pred(x) AND real_pred(x) AND rational_pred(x)
--           AND integer_pred(x) AND (x >= 0))
-- 
theorem every_e : ∀ (n : {i : ℝ // i ≥ 0}) (k : {ii : {i : ℝ // i ≥ 0} // ii < n}), every (fun (x : ℝ) => number_field_pred x ∧ real_pred x ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (e n k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.e_not_zero
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, i: below(m)): e(m, i) /= zero_vec(m)
theorem e_not_zero : ∀ (m : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < m}), e m i ≠ zero_vec m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.test_zero1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: zero_vec(3) = (: 0, 0, 0 :)
theorem test_zero1 : zero_vec 3 = (: 0, 0, 0 :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.test_zero2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: zero_vec(4) /= (: 1, 0, 0, 0 :)
theorem test_zero2 : zero_vec 4 ≠ (: 1, 0, 0, 0 :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.test_e1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: e(2, 0) = (: 1, 0 :)
theorem test_e1 : e 2 0 = (: 1, 0 :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: vector_arithmetic.test_e2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: e(9, 3) = (: 0, 0, 0, 1, 0, 0, 0, 0, 0 :)
theorem test_e2 : e 9 3 = (: 0, 0, 0, 1, 0, 0, 0, 0, 0 :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: vector_arithmetic.test_e3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: e(9, 8) = (: 0, 0, 0, 0, 0, 0, 0, 0, 1 :)
theorem test_e3 : e 9 8 = (: 0, 0, 0, 0, 0, 0, 0, 0, 1 :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: vector_arithmetic.test_constant_vec1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: constant_vec(2, 3) = (: 2, 2, 2 :)
theorem test_constant_vec1 : constant_vec 2 3 = (: 2, 2, 2 :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.test_constant_vec2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real): constant_vec(c, 3) = (: c, c, c :)
theorem test_constant_vec2 : ∀ (c : ℝ), constant_vec c 3 = (: c, c, c :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.greater?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF cdr(l2) = null AND cdr(l1) = null THEN car(l1) > car(l2)
-- ELSIF cdr(l1) = null OR cdr(l2) = null THEN FALSE
-- ELSE (car(l1) > car(l2)) AND greater?(cdr(l1), cdr(l2))
-- ENDIF
axiom greater_p {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ26 → pvs_τ27 → Prop

-- pvs2lean: vector_arithmetic.>
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: greater?(l1, l2)
noncomputable def pvs_op_cedf8d : sorry := by
  sorry  -- pvs2lean: definition stub for greater?(l1, l2)

-- pvs2lean: vector_arithmetic.greater_eq?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF cdr(l2) = null AND cdr(l1) = null THEN car(l1) >= car(l2)
-- ELSIF cdr(l1) = null OR cdr(l2) = null THEN FALSE
-- ELSE (car(l1) >= car(l2)) AND greater_eq?(cdr(l1), cdr(l2))
-- ENDIF
axiom greater_eq_p {pvs_τ24 : Type*} {pvs_τ25 : Type*} : pvs_τ24 → pvs_τ25 → Prop

-- pvs2lean: vector_arithmetic.>=
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: greater_eq?(l1, l2)
noncomputable def pvs_op_3a1e9b : sorry := by
  sorry  -- pvs2lean: definition stub for greater_eq?(l1, l2)

-- pvs2lean: vector_arithmetic.less?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF cdr(l2) = null AND cdr(l1) = null THEN car(l1) < car(l2)
-- ELSIF cdr(l1) = null OR cdr(l2) = null THEN FALSE
-- ELSE (car(l1) < car(l2)) AND less?(cdr(l1), cdr(l2))
-- ENDIF
axiom less_p {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ32 → pvs_τ33 → Prop

-- pvs2lean: vector_arithmetic.<
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: less?(l1, l2)
noncomputable def pvs_op_524a50 : sorry := by
  sorry  -- pvs2lean: definition stub for less?(l1, l2)

-- pvs2lean: vector_arithmetic.less_eq?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF cdr(l2) = null AND cdr(l1) = null THEN car(l1) <= car(l2)
-- ELSIF cdr(l1) = null OR cdr(l2) = null THEN FALSE
-- ELSE (car(l1) <= car(l2)) AND less_eq?(cdr(l1), cdr(l2))
-- ENDIF
axiom less_eq_p {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: vector_arithmetic.<=
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: less_eq?(l1, l2)
noncomputable def pvs_op_a7eeae : sorry := by
  sorry  -- pvs2lean: definition stub for less_eq?(l1, l2)

-- pvs2lean: vector_arithmetic.g_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: zero_vec(3) > (: -1, -1, -10 :)
theorem g_1 : zero_vec 3 > (: -1, -1, -10 :) := by
  -- === TCCs for g_1 ===
  -- TCC: g_1_TCC1
  -- Statement: cons?[real](zero_vec(3))
  have g_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.ge_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: greater_eq?(zero_vec(3), (: 0, -9, -1000 :))
theorem ge_1 : greater_eq_p (zero_vec 3) ((: 0, -9, -1000 :)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.l_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT less?(zero_vec(3), (: -1, -1, -10 :))
theorem l_1 : ¬(less_p (zero_vec 3) ((: -1, -1, -10 :))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.cdr_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real, ll: (cons?)): cdr(c * ll) = c * cdr(ll)
theorem cdr_scal : ∀ (c : ℝ) (ll : List ℝ), cdr (c * ll) = c * cdr ll := by
  -- === TCCs for cdr_scal ===
  -- TCC: cdr_scal_TCC1
  -- Statement: FORALL (c: real, ll: (cons?[real])): cons?[real](c * ll)
  have cdr_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.cdr_sub
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)): cdr(l1 - l2) = cdr(l1) - cdr(l2)
theorem cdr_sub : ∀ (l1 : List ℝ) (l2 : List ℝ), cdr (l1 - l2) = cdr l1 - cdr l2 := by
  -- === TCCs for cdr_sub ===
  -- TCC: cdr_sub_TCC1
  -- Statement: FORALL (l1, l2: (cons?[real])): cons?[real](l1 - l2)
  have cdr_sub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.cdr_add
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)): cdr(l1 + l2) = cdr(l1) + cdr(l2)
theorem cdr_add : ∀ (l1 : List ℝ) (l2 : List ℝ), cdr (l1 + l2) = cdr l1 + cdr l2 := by
  -- === TCCs for cdr_add ===
  -- TCC: cdr_add_TCC1
  -- Statement: FORALL (l1, l2: (cons?[real])): cons?[real](l1 + l2)
  have cdr_add_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.car_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real, ll: (cons?)): car(c * ll) = c * car(ll)
theorem car_scal : ∀ (c : ℝ) (ll : List ℝ), car (c * ll) = c * car ll := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.car_sub
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)): car(l1 - l2) = car(l1) - car(l2)
theorem car_sub : ∀ (l1 : List ℝ) (l2 : List ℝ), car (l1 - l2) = car l1 - car l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.neg_1_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?)):
--   cons(-1 * (-1 * car(ll)), scal(-1, scal(-1, cdr(ll)))) = ll
theorem neg_1_scal : ∀ (ll : List ℝ), cons (-1 * (-1 * car ll)) (scal -1 (scal -1 (cdr ll))) = ll := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.add_commute
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)): l1 + l2 = l2 + l1
theorem add_commute : ∀ (l1 : List ℝ) (l2 : List ℝ), l1 + l2 = l2 + l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.sub_idx
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)): l1 - l2 = l1 + (-1 * l2)
theorem sub_idx : ∀ (l1 : List ℝ) (l2 : List ℝ), l1 - l2 = l1 + (-1 * l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.asso_1_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: (cons?), a, b: real):
--   scal(a, scal(b, ll)) = scal(b, scal(a, ll))
theorem asso_1_scal : ∀ (ll : List ℝ) (a : ℝ) (b : ℝ), scal a (scal b ll) = scal b (scal a ll) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.scal_asso
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?), c: nzreal): (l1 * (c * l2)) = c * (l1 * l2)
theorem scal_asso : ∀ (l1 : List ℝ) (l2 : List ℝ) (c : {r : ℝ // r ≠ 0}), (l1 * (c * l2)) = c * (l1 * l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.asso_v
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c1, c2: real, ll: (cons?)): c1 * (c2 * ll) = (c1 * c2) * ll
theorem asso_v : ∀ (c1 : ℝ) (c2 : ℝ) (ll : List ℝ), c1 * (c2 * ll) = (c1 * c2) * ll := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.asso_dot
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real, l1, l2: (cons?)): (c * l1) * l2 = c * (l1 * l2)
theorem asso_dot : ∀ (c : ℝ) (l1 : List ℝ) (l2 : List ℝ), (c * l1) * l2 = c * (l1 * l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.asso_add
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) IMPLIES
--    l1 + l2 + l3 = l1 + (l2 + l3)
theorem asso_add : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) → l1 + l2 + l3 = l1 + (l2 + l3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.asso_sub_commute
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) IMPLIES
--    l1 - l3 + l2 = l1 + (l2 - l3)
theorem asso_sub_commute : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) → l1 - l3 + l2 = l1 + (l2 - l3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.scal_cancel
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?), c: nzreal):
--   length(l1) = length(l2) IMPLIES (c * l1 = c * l2 IMPLIES l1 = l2)
theorem scal_cancel : ∀ (l1 : List ℝ) (l2 : List ℝ) (c : {r : ℝ // r ≠ 0}), List.length l1 = List.length l2 → (c * l1 = c * l2 → l1 = l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.add_zero
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)):
--   length(l1) = length(l2) IMPLIES
--    (l1 + l2 = l2 IFF l1 = zero_vec(length(l1)))
theorem add_zero : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → (l1 + l2 = l2 ↔ l1 = zero_vec (List.length l1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.zero_id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1: list): l1 + zero_vec(length(l1)) = l1
theorem zero_id : ∀ (l1 : List ℝ), l1 + zero_vec (List.length l1) = l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.scal_zero
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real, m: nat): c * zero_vec(m) = zero_vec(m)
theorem scal_zero : ∀ (c : ℝ) (m : {i : ℝ // i ≥ 0}), c * zero_vec m = zero_vec m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?), m: nat): l * zero_vec(m) = 0
theorem dot_zero : ∀ (l : List ℝ) (m : {i : ℝ // i ≥ 0}), l * zero_vec m = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.zero_mult
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real, l: (cons?)):
--   c /= 0 AND l /= zero_vec(length(l)) IMPLIES c * l /= zero_vec(length(l))
theorem zero_mult : ∀ (c : ℝ) (l : List ℝ), c ≠ 0 ∧ l ≠ zero_vec (List.length l) → c * l ≠ zero_vec (List.length l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.add_cancel
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   length(l1) = length(l2) AND length(l3) = length(l1) AND l1 + l2 = l3 + l2
--    IMPLIES l1 = l3
theorem add_cancel : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), List.length l1 = List.length l2 ∧ List.length l3 = List.length l1 ∧ l1 + l2 = l3 + l2 → l1 = l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.sub_zero
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?)): l - zero_vec(length(l)) = l
theorem sub_zero : ∀ (l : List ℝ), l - zero_vec (List.length l) = l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.mult_zero
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?)): 0 * l = zero_vec(length(l))
theorem mult_zero : ∀ (l : List ℝ), 0 * l = zero_vec (List.length l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.add_zero_trick
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   length(l1) = length(l2) AND length(l2) = length(l3) IMPLIES
--    l1 - l2 + (l2 - l3) = l1 - l3
theorem add_zero_trick : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), List.length l1 = List.length l2 ∧ List.length l2 = List.length l3 → l1 - l2 + (l2 - l3) = l1 - l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.diff_zero
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, l1, l2: (cons?)):
--   length(l1) = length(l2) IMPLIES (l1 - l2 = zero_vec(m) IMPLIES l1 = l2)
theorem diff_zero : ∀ (m : {i : ℝ // i ≥ 0}) (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → (l1 - l2 = zero_vec m → l1 = l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.add_zero_imp_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, l1, l2: (cons?)):
--   length(l1) = length(l2) IMPLIES
--    (l1 + l2 = zero_vec(m) IMPLIES l1 = (-1) * l2)
theorem add_zero_imp_eq : ∀ (m : {i : ℝ // i ≥ 0}) (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → (l1 + l2 = zero_vec m → l1 = (-1) * l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.one_idx
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?)): 1 * l = l
theorem one_idx : ∀ (l : List ℝ), 1 * l = l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.zero_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?), c: nzreal):
--   c * l = zero_vec(length(l)) IMPLIES l = zero_vec(length(l))
theorem zero_scal : ∀ (l : List ℝ) (c : {r : ℝ // r ≠ 0}), c * l = zero_vec (List.length l) → l = zero_vec (List.length l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.sub_self
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1: (cons?)): l1 - l1 = zero_vec(length(l1))
theorem sub_self : ∀ (l1 : List ℝ), l1 - l1 = zero_vec (List.length l1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.sub_sandwich
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)):
--   length(l1) = length(l2) IMPLIES (l1 + l2 - l1) = l2
theorem sub_sandwich : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → (l1 + l2 - l1) = l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.sub_equal_id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)): length(l1) = length(l2) IMPLIES l1 + l2 - l2 = l1
theorem sub_equal_id : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → l1 + l2 - l2 = l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.sub_equal_id1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)): length(l1) = length(l2) IMPLIES l1 - l2 + l2 = l1
theorem sub_equal_id1 : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → l1 - l2 + l2 = l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.neg_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)): -1 * (l1 - l2) = l2 - l1
theorem neg_1 : ∀ (l1 : List ℝ) (l2 : List ℝ), -1 * (l1 - l2) = l2 - l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dist_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real, l1, l2: (cons?)): c * (l1 - l2) = c * l1 - c * l2
theorem dist_1 : ∀ (c : ℝ) (l1 : List ℝ) (l2 : List ℝ), c * (l1 - l2) = c * l1 - c * l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dist
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real, l1, l2: (cons?)): c * (l1 + l2) = c * l1 + c * l2
theorem dist : ∀ (c : ℝ) (l1 : List ℝ) (l2 : List ℝ), c * (l1 + l2) = c * l1 + c * l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dist_scalar
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c1, c2: real, ll: (cons?)): (c1 + c2) * ll = c1 * ll + c2 * ll
theorem dist_scalar : ∀ (c1 : ℝ) (c2 : ℝ) (ll : List ℝ), (c1 + c2) * ll = c1 * ll + c2 * ll := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dist_scalar_sub
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c1, c2: real, ll: (cons?)): (c1 - c2) * ll = c1 * ll - c2 * ll
theorem dist_scalar_sub : ∀ (c1 : ℝ) (c2 : ℝ) (ll : List ℝ), (c1 - c2) * ll = c1 * ll - c2 * ll := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dist_list
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)): l1 * (l2 + l3) = l1 * l2 + l1 * l3
theorem dist_list : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), l1 * (l2 + l3) = l1 * l2 + l1 * l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.nth_dist
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?), c: real):
--   FORALL (i: below(length(l))): nth(c * l, i) = c * nth(l, i)
theorem nth_dist : ∀ (l : List ℝ) (c : ℝ), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length l}), nth (c * l) i = c * nth l i := by
  -- === TCCs for nth_dist ===
  -- TCC: nth_dist_TCC1
  -- Statement: FORALL (l: (cons?[real]), c: real, i: below(length[real](l))):
  --   i < length[real](c * l)
  have nth_dist_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.nth_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)):
--   length(l1) = length(l2) IMPLIES
--    FORALL (i: below(length(l1))): nth(l1 + l2, i) = nth(l1, i) + nth(l2, i)
theorem nth_sum : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → ∀ (i : Finset.range List.length l1), nth (l1 + l2) i = nth l1 i + nth l2 i := by
  -- === TCCs for nth_sum ===
  -- TCC: nth_sum_TCC1
  -- Statement: FORALL (l1, l2: (cons?[real])):
  --   length(l1) = length(l2) IMPLIES
  --    FORALL (i: below(length[real](l1))): i < length[real](l1 + l2)
  have nth_sum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_sum_TCC2
  -- Statement: FORALL (l1, l2: (cons?[real])):
  --   length(l1) = length(l2) IMPLIES
  --    FORALL (i: below(length[real](l1))): i < length[real](l2)
  have nth_sum_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.nth_subtract
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?)):
--   length(l1) = length(l2) IMPLIES
--    FORALL (i: below(length(l1))): nth(l1 - l2, i) = nth(l1, i) - nth(l2, i)
theorem nth_subtract : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length l1 = List.length l2 → ∀ (i : Finset.range List.length l1), nth (l1 - l2) i = nth l1 i - nth l2 i := by
  -- === TCCs for nth_subtract ===
  -- TCC: nth_subtract_TCC1
  -- Statement: FORALL (l1, l2: (cons?[real])):
  --   length(l1) = length(l2) IMPLIES
  --    FORALL (i: below(length[real](l1))): i < length[real](l1 - l2)
  have nth_subtract_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.sub_dist
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) IMPLIES
--    l1 - (l2 + l3) = l1 - l2 - l3
theorem sub_dist : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) → l1 - (l2 + l3) = l1 - l2 - l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_linear
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?), c: real):
--   l1 * (l2 + c * l3) = l1 * l2 + c * l1 * l3
theorem dot_linear : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ) (c : ℝ), l1 * (l2 + c * l3) = l1 * l2 + c * l1 * l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_short
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN car(u) * car(v)
-- ELSE nth(u, i) * nth(v, i) + dot_short(u, v, i - 1)
-- ENDIF
axiom dot_short {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21 → Prop

-- pvs2lean: vector_arithmetic.F_sum
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < min(length(u), length(v)) THEN nth(u, i) * nth(v, i) ELSE 0 ENDIF
axiom F_sum {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4

-- pvs2lean: vector_arithmetic.dot_short_is_sig
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   FORALL (i: below(min(length(u), length(v)))):
--     dot_short(u, v, i) = sigma(0, i, F_sum(u, v))
theorem dot_short_is_sig : ∀ (u : List ℝ) (v : List ℝ), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < min (List.length u) (List.length v)}), dot_short u v i = Finset.sum (Finset.Icc 0 i) (F_sum u v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.F_shift
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   (cons?(cdr(u)) AND cons?(cdr(v))) IMPLIES
--    FORALL (i: subrange(0, min(length(u), length(v)))):
--      F_sum(cdr(u), cdr(v))(i) = F_sum(u, v)(i + 1)
theorem F_shift : ∀ (u : List ℝ) (v : List ℝ), (cons_p (cdr u) ∧ cons_p (cdr v)) → ∀ (i : Set.Icc 0 min (List.length u (List.length v))), F_sum (cdr u) (cdr v) (i) = F_sum u v(i + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.F_sum_shift
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   (cons?(cdr(u)) AND cons?(cdr(v))) IMPLIES
--    sigma(1, min(length(u), length(v)), F_sum(u, v)) =
--     sigma(0, min(length(cdr(u)), length(cdr(v))), F_sum(cdr(u), cdr(v)))
theorem F_sum_shift : ∀ (u : List ℝ) (v : List ℝ), (cons_p (cdr u) ∧ cons_p (cdr v)) → sigma 1 (min (List.length u) (List.length v)) (F_sum u v) =
 sigma 0 (min (List.length (cdr u)) (List.length (cdr v))) (F_sum (cdr u) (cdr v)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_is_sig
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   u * v = sigma(0, min(length(u), length(v)), F_sum(u, v))
theorem dot_is_sig : ∀ (u : List ℝ) (v : List ℝ), u * v = sigma 0 (min (List.length u) (List.length v)) (F_sum u v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_is_sig2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   u * v = sigma(0, min(length(u) - 1, length(v) - 1), F_sum(u, v))
theorem dot_is_sig2 : ∀ (u : List ℝ) (v : List ℝ), u * v = sigma 0 (min (List.length u - 1) (List.length v - 1)) (F_sum u v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_short_is_dot
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   length(u) = length(v) IMPLIES dot_short(u, v, length(u) - 1) = u * v
theorem dot_short_is_dot : ∀ (u : List ℝ) (v : List ℝ), List.length u = List.length v → dot_short u v (List.length u - 1) = u * v := by
  -- === TCCs for dot_short_is_dot ===
  -- TCC: dot_short_is_dot_TCC1
  -- Statement: FORALL (u, v: (cons?[real])):
  --   length(u) = length(v) IMPLIES
  --    length[real](u) - 1 >= 0 AND
  --     length[real](u) - 1 < min(length[real](u), length[real](v))
  have dot_short_is_dot_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_is_sigma
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   length(u) = length(v) IMPLIES
--    u * v =
--     sigma(0, length(u) - 1,
--           LAMBDA (i: nat):
--             IF i < length(u) THEN nth(u, i) * nth(v, i) ELSE 0 ENDIF)
theorem dot_is_sigma : ∀ (u : List ℝ) (v : List ℝ), List.length u = List.length v → u * v =
 sigma 0 (List.length u - 1) (fun (i : ℕ)=> if i < List.length u then nth u i * nth v i else 0) := by
  -- === TCCs for dot_is_sigma ===
  -- TCC: dot_is_sigma_TCC1
  -- Statement: FORALL (u, v: (cons?[real])):
  --   length(u) = length(v) IMPLIES
  --    FORALL (i: nat): i < length(u) IMPLIES i < length[real](v)
  have dot_is_sigma_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_prod_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, v: list[real]): v * zero_vec(n) = 0
theorem dot_prod_zero : ∀ (n : {i : ℝ // i ≥ 0}) (v : List ℝ), v * zero_vec n = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_prod_e
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, i: below(n), v: {vv: list | length(vv) > i}):
--   v * e(n, i) = nth(v, i)
theorem dot_prod_e : ∀ (n : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < n}) (v : {vv : List ℝ // List.length vv > i}), v * e n i = nth v i := by
  -- === TCCs for dot_prod_e ===
  -- TCC: dot_prod_e_TCC1
  -- Statement: FORALL (n: nat, i: below(n), v: {vv: list[real] | length[real](vv) > i}):
  --   i < length[real](v)
  have dot_prod_e_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_equality
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   (length(u) = length(v) AND (FORALL (w: (cons?)): u * w = v * w)) IMPLIES
--    u = v
theorem dot_equality : ∀ (u : List ℝ) (v : List ℝ), (List.length u = List.length v ∧ (∀ (w : (cons_p)), u * w = v * w)) → u = v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_equality1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   (length(u) = length(v) AND
--     (FORALL (w: VectorN(length(u))): u * w = v * w))
--    IMPLIES u = v
theorem dot_equality1 : ∀ (u : List ℝ) (v : List ℝ), (List.length u = List.length v ∧ (∀ (w : VectorN (List.length u)), u * w = v * w)) → u = v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_equality_e
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: (cons?)):
--   (length(u) = length(v) AND
--     FORALL (i: below(length(u))):
--       u * e(length(u), i) = v * e(length(u), i))
--    IMPLIES u = v
theorem dot_equality_e : ∀ (u : List ℝ) (v : List ℝ), (List.length u = List.length v ∧
  ∀ (i : Finset.range List.length u), u * e (List.length u) i = v * e (List.length u) i) → u = v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_scalar1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?), c: real): l1 * (c * l2) = c * (l1 * l2)
theorem dot_scalar1 : ∀ (l1 : List ℝ) (l2 : List ℝ) (c : ℝ), l1 * (c * l2) = c * (l1 * l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_dist
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)): l1 * (l2 + l3) = l1 * l2 + l1 * l3
theorem dot_dist : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), l1 * (l2 + l3) = l1 * l2 + l1 * l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.dot_dist_sub
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) IMPLIES
--    l1 * (l2 - l3) = l1 * l2 - l1 * l3
theorem dot_dist_sub : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) → l1 * (l2 - l3) = l1 * l2 - l1 * l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.quad_cancel
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) IMPLIES
--    l1 - l2 - (l3 - l2) = l1 - l3
theorem quad_cancel : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) → l1 - l2 - (l3 - l2) = l1 - l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.quad_cancel_plus
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) IMPLIES
--    (l1 - l2) + (l2 - l3) = l1 - l3
theorem quad_cancel_plus : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) → (l1 - l2) + (l2 - l3) = l1 - l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.quad_move
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3, l4: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) AND
--    length(l3) = length(l4)
--    IMPLIES (l1 + l2) - (l3 + l4) = l1 - l3 + (l2 - l4)
theorem quad_move : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ) (l4 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) ∧
 List.length l3 = List.length l4 → (l1 + l2) - (l3 + l4) = l1 - l3 + (l2 - l4) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.quad_cancel_p_m
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) IMPLIES
--    (l1 + l2) - (l1 + l3) = l2 - l3
theorem quad_cancel_p_m : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) → (l1 + l2) - (l1 + l3) = l2 - l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.quad_cancel_tail
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: (cons?)):
--   (length(l1) = length(l2) AND length(l2) = length(l3)) IMPLIES
--    l1 - l2 = l1 - l2 - l3 + l3
theorem quad_cancel_tail : ∀ (l1 : List ℝ) (l2 : List ℝ) (l3 : List ℝ), (List.length l1 = List.length l2 ∧ List.length l2 = List.length l3) → l1 - l2 = l1 - l2 - l3 + l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.add_sub_zero
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u: (cons?), c: real): c * u = u + (c - 1) * u
theorem add_sub_zero : ∀ (u : List ℝ) (c : ℝ), c * u = u + (c - 1) * u := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vector_arithmetic.-
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 * v
noncomputable def pvs_op_336d5e : sorry := by
  sorry  -- pvs2lean: definition stub for -1 * v

end

end NavierStokesClean.PVSLib.vector_arithmetic