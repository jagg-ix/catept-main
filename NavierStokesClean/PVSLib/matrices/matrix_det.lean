-- Auto-generated by pvs2lean/translator.py
-- PVS theory: matrix_det
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: matrix_det

Auto-generated stubs from PVSLib theory `matrix_det`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.matrix_det

noncomputable section

variable {pvs_τ11 : Type*} (FM : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (GM : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} (Id : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (SquareMatrix : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} (columns : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (det : pvs_τ18 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (entry : pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (form_matrix : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (max : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} (null_p : pvs_τ39 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (remove : pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (replace_row : pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} (row : pvs_τ53 → pvs_τ54 → pvs_τ55)
variable {pvs_τ56 : Type*} (rows : pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} (swap : pvs_τ57 → pvs_τ58 → pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (transpose : pvs_τ61 → pvs_τ62)

-- pvs2lean: matrix_det.Esr
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (k, j: nat):
--               IF k /= j THEN 0 ELSIF k /= i THEN 1 ELSE r ENDIF,
--             pn, pn)
axiom Esr {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10 → Prop

-- pvs2lean: matrix_det.entry_Esr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(Esr(pn)(i, r))(k, j) =
--  IF k /= j OR k >= pn OR j >= pn THEN 0 ELSIF k /= i THEN 1 ELSE r ENDIF
theorem entry_Esr : entry (Esr pn(i, r)) (k, j) = if k ≠ j ∨ k ≥ pn ∨ j ≥ pn then 0 ELSIF k ≠ i then 1 else r := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.rows_Esr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(Esr(pn)(i, r)) = pn
theorem rows_Esr : rows (Esr pn(i, r)) = pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.columns_Esr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(Esr(pn)(i, r)) = pn
theorem columns_Esr : columns (Esr pn(i, r)) = pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.det_Esr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < pn IMPLIES det(Esr(pn)(i, r)) = r
theorem det_Esr : i < pn → det (Esr pn(i, r)) = r := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.transpose_Esr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: transpose(Esr(pn)(i, r)) = Esr(pn)(i, r)
theorem transpose_Esr : transpose (Esr pn(i, r)) = Esr pn(i, r) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.mult_Esr_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) = pn AND i < pn IMPLIES
--  Esr(pn)(i, r) * D = replace_row(i, r * row(D)(i))(D)
theorem mult_Esr_left : rows D = pn ∧ i < pn → Esr pn(i, r) * D = replace_row i (r * row D(i)) (D) := by
  -- === TCCs for mult_Esr_left ===
  -- TCC: mult_Esr_left_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i: nat, pn: posnat, r: real):
  --   i < pn AND rows(D) = pn IMPLIES columns(D) = length[real](r * row(D)(i))
  have mult_esr_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.mult_Esr_Esr_inv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: r /= 0 AND i < pn IMPLIES Esr(pn)(i, r) * Esr(pn)(i, 1 / r) = Id(pn)
theorem mult_Esr_Esr_inv : r ≠ 0 ∧ i < pn → Esr pn(i, r) * Esr pn(i, 1 / r) = Id pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.Ers
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (k, p: nat):
--               IF k = i AND p = j THEN 1
--               ELSIF k = j AND p = i THEN 1
--               ELSIF k /= i AND k /= j AND k = p THEN 1
--               ELSE 0
--          
axiom Ers {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7 → Prop

-- pvs2lean: matrix_det.entry_Ers
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(Ers(pn)(i, j))(k, p) =
--  IF k >= pn OR p >= pn THEN 0
--  ELSIF k = i AND p = j THEN 1
--  ELSIF k = j AND p = i THEN 1
--  ELSIF k /= i AND k /= j AND k = p THEN 1
--  ELSE 0
--  ENDIF
theorem entry_Ers : entry (Ers pn(i, j)) (k, p) = if k ≥ pn ∨ p ≥ pn then 0
ELSIF k = i ∧ p = j then 1
ELSIF k = j ∧ p = i then 1
ELSIF k ≠ i ∧ k ≠ j ∧ k = p then 1
else 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.rows_Ers
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(Ers(pn)(i, j)) = pn
theorem rows_Ers : rows (Ers pn(i, j)) = pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.columns_Ers
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(Ers(pn)(i, j)) = pn
theorem columns_Ers : columns (Ers pn(i, j)) = pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.mult_Ers_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) = pn AND i < pn AND j < pn IMPLIES Ers(pn)(i, j) * D = swap(i, j)(D)
theorem mult_Ers_left : rows D = pn ∧ i < pn ∧ j < pn → Ers pn(i, j) * D = swap i j(D) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.transpose_Ers
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: transpose(Ers(pn)(i, j)) = Ers(pn)(j, i)
theorem transpose_Ers : transpose (Ers pn(i, j)) = Ers pn(j, i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.det_Ers
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < pn AND j < pn IMPLIES det(Ers(pn)(i, j)) = IF i = j THEN 1 ELSE -1 ENDIF
theorem det_Ers : i < pn ∧ j < pn → det (Ers pn(i, j)) = if i = j then 1 else -1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.mult_Ers_Ers_inv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < pn AND j < pn IMPLIES Ers(pn)(i, j) * Ers(pn)(i, j) = Id(pn)
theorem mult_Ers_Ers_inv : i < pn ∧ j < pn → Ers pn(i, j) * Ers pn(i, j) = Id pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.Easr
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (k, p: nat):
--               IF k = i
--                 THEN (IF j = i AND p = i THEN 1 + r
--                       ELSIF p = i THEN 1
--                       ELSIF p = j THEN r
--            
axiom Easr {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: matrix_det.entry_Easr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(Easr(pn)(i, j, r))(k, p) =
--  IF k >= pn OR p >= pn THEN 0
--  ELSIF k = i
--    THEN (IF j = i AND p = i THEN 1 + r
--          ELSIF p = i THEN 1
--          ELSIF p = j THEN r
--          ELSE 0
--          ENDI
theorem entry_Easr : entry (Easr pn(i, j, r)) (k, p) = if k ≥ pn ∨ p ≥ pn then 0
ELSIF k = i
  then (if j = i ∧ p = i then 1 + r
        ELSIF p = i then 1
        ELSIF p = j then r
        else 0
        )
ELSIF k = p then 1
else 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.rows_Easr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(Easr(pn)(i, j, r)) = pn
theorem rows_Easr : rows (Easr pn(i, j, r)) = pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.columns_Easr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(Easr(pn)(i, j, r)) = pn
theorem columns_Easr : columns (Easr pn(i, j, r)) = pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.mult_Easr_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) = pn AND i < pn AND j < pn IMPLIES
--  Easr(pn)(i, j, r) * D = replace_row(i, row(D)(i) + r * row(D)(j))(D)
theorem mult_Easr_left : rows D = pn ∧ i < pn ∧ j < pn → Easr pn(i, j, r) * D = replace_row i (row D(i) + r * row D(j)) (D) := by
  -- === TCCs for mult_Easr_left ===
  -- TCC: mult_Easr_left_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i, j: nat, pn: posnat, r: real):
  --   j < pn AND i < pn AND rows(D) = pn IMPLIES
  --    columns(D) = length[real](row(D)(i) + r * row(D)(j))
  have mult_easr_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.transpose_Easr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: transpose(Easr(pn)(i, j, r)) = Easr(pn)(j, i, r)
theorem transpose_Easr : transpose (Easr pn(i, j, r)) = Easr pn(j, i, r) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.det_Easr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < pn AND j < pn IMPLIES
--  det(Easr(pn)(i, j, r)) = IF i = j THEN 1 + r ELSE 1 ENDIF
theorem det_Easr : i < pn ∧ j < pn → det (Easr pn(i, j, r)) = if i = j then 1 + r else 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.mult_Easr_Easr_inv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < pn AND j < pn AND i /= j IMPLIES
--  Easr(pn)(i, j, r) * Easr(pn)(i, j, -r) = Id(pn)
theorem mult_Easr_Easr_inv : i < pn ∧ j < pn ∧ i ≠ j → Easr pn(i, j, r) * Easr pn(i, j, -r) = Id pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_det.ZERO
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (i, j): 0, pn, pn)
noncomputable def ZERO : sorry := by
  sorry  -- pvs2lean: definition stub for form_matrix(LAMBDA (i, j): 0, pn, pn)

-- pvs2lean: matrix_det.upper_triangular?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j): i > j AND i < rows(M) IMPLIES entry(M)(i, j) = 0
axiom upper_triangular_p {pvs_τ63 : Type*} : pvs_τ63 → Prop

-- pvs2lean: matrix_det.prod_diag
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i >= rows(M) - 1 THEN entry(M)(rows(M) - 1, rows(M) - 1)
-- ELSE entry(M)(i, i) * prod_diag(M, i + 1)
-- ENDIF
axiom prod_diag {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ40 → pvs_τ41 → Prop

-- pvs2lean: matrix_det.prod_diag_remove_0_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: k + 1 < rows(S) IMPLIES prod_diag(remove(S, 0, 0), k) = prod_diag(S, k + 1)
theorem prod_diag_remove_0_0 : k + 1 < rows S → prod_diag (remove S 0 0) k = prod_diag S (k + 1) := by
  -- === TCCs for prod_diag_remove_0_0 ===
  -- TCC: prod_diag_remove_0_0_TCC1
  -- Statement: FORALL (S: Square, k: nat):
  --   k + 1 < rows(S) IMPLIES rows(remove(S, 0, 0)) > 0
  have prod_diag_remove_0_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.diagonal?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j): i /= j IMPLIES entry(M)(i, j) = 0
axiom diagonal_p {pvs_τ19 : Type*} : pvs_τ19 → Prop

-- pvs2lean: matrix_det.diagonal_upper_triangular
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: diagonal?(M) IMPLIES upper_triangular?(M)
theorem diagonal_upper_triangular : diagonal_p M → upper_triangular_p M := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.det_upper_triangular
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: upper_triangular?(S) IMPLIES det(S) = prod_diag(S, 0)
theorem det_upper_triangular : upper_triangular_p S → det S = prod_diag S 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.det_upper_triangular_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: upper_triangular?(S) IMPLIES
--  (det(S) = 0 IFF (EXISTS (i): i < rows(S) AND entry(S)(i, i) = 0))
theorem det_upper_triangular_zero : upper_triangular_p S → (det S = 0 ↔ (∃ (i : _), i < rows S ∧ entry S(i, i) = 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.upper_triangular_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: upper_triangular?(S) AND upper_triangular?(R) IMPLIES
--  upper_triangular?(S * R)
theorem upper_triangular_mult : upper_triangular_p S ∧ upper_triangular_p R → upper_triangular_p (S * R) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.lower_triangular?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j): i < j AND i < rows(M) IMPLIES entry(M)(i, j) = 0
axiom lower_triangular_p {pvs_τ36 : Type*} : pvs_τ36 → Prop

-- pvs2lean: matrix_det.lower_triangular_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: lower_triangular?(S) AND lower_triangular?(R) IMPLIES
--  lower_triangular?(S * R)
theorem lower_triangular_mult : lower_triangular_p S ∧ lower_triangular_p R → lower_triangular_p (S * R) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.upper_triangular_Id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: upper_triangular?(Id(pn))
theorem upper_triangular_Id : upper_triangular_p (Id pn) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.lower_triangular_Id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: lower_triangular?(Id(pn))
theorem lower_triangular_Id : lower_triangular_p (Id pn) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.upper_triangular_Easr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < j IMPLIES upper_triangular?(Easr(pn)(i, j, r))
theorem upper_triangular_Easr : i < j → upper_triangular_p (Easr pn(i, j, r)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.lower_triangular_Easr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i > j IMPLIES lower_triangular?(Easr(pn)(i, j, r))
theorem lower_triangular_Easr : i > j → lower_triangular_p (Easr pn(i, j, r)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.prod_mat
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF k = 0 THEN FM(0) ELSE prod_mat(pn)(FM, k - 1) * FM(k) ENDIF
axiom prod_mat {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} : pvs_τ42 → pvs_τ43 → pvs_τ44 → Prop

-- pvs2lean: matrix_det.prod_mat_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (FM, GM: [nat -> SquareMatrix(pn)]):
--   (FORALL (i): i <= k IMPLIES FM(i) = GM(i)) IMPLIES
--    prod_mat(pn)(FM, k) = prod_mat(pn)(GM, k)
theorem prod_mat_eq : ∀ (FM : {i : ℝ // i ≥ 0} → {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}) (GM : {i : ℝ // i ≥ 0} → {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), (∀ (i : _), i ≤ k → FM i = GM i) → prod_mat pn(FM, k) = prod_mat pn(GM, k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.mult_prod_mat
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (FM, GM: [nat -> SquareMatrix(pn)]):
--   prod_mat(pn)(FM, k) * prod_mat(pn)(GM, p) =
--    prod_mat(pn)
--            (LAMBDA (i: nat): IF i <= k THEN FM(i) ELSE GM(i - k - 1) ENDIF,
--             k + p 
theorem mult_prod_mat : ∀ (FM : {i : ℝ // i ≥ 0} → {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}) (GM : {i : ℝ // i ≥ 0} → {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), prod_mat pn(FM, k) * prod_mat pn(GM, p) = prod_mat pn
        (fun (i : ℕ)=> if i ≤ k then FM i else GM (i - k - 1) ,
         k + p + 1) := by
  -- === TCCs for mult_prod_mat ===
  -- TCC: mult_prod_mat_TCC1
  -- Statement: FORALL (k: nat, pn: posnat, FM, GM: [nat -> SquareMatrix(pn)], i: nat):
  --   NOT i <= k IMPLIES i - k - 1 >= 0
  have mult_prod_mat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.prod_mat_expand_left
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (FM: [nat -> SquareMatrix(pn)]):
--   k > 0 IMPLIES
--    prod_mat(pn)(FM, k) = FM(0) * prod_mat(pn)(LAMBDA (i): FM(i + 1), k - 1)
theorem prod_mat_expand_left : ∀ (FM : {i : ℝ // i ≥ 0} → {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), k > 0 → prod_mat pn(FM, k) = FM 0 * prod_mat pn(fun (i : _)=> FM (i + 1), k - 1) := by
  -- === TCCs for prod_mat_expand_left ===
  -- TCC: prod_mat_expand_left_TCC1
  -- Statement: FORALL (k: nat, pn: posnat, FM: [nat -> SquareMatrix(pn)]):
  --   k > 0 IMPLIES k - 1 >= 0
  have prod_mat_expand_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.transpose_prod_mat
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (FM: [nat -> SquareMatrix(pn)]):
--   transpose(prod_mat(pn)(FM, k)) =
--    prod_mat(pn)
--            (LAMBDA (i):
--               IF i <= k THEN transpose(FM(k - i)) ELSE FM(i) ENDIF,
--             k)
theorem transpose_prod_mat : ∀ (FM : {i : ℝ // i ≥ 0} → {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), transpose (prod_mat pn(FM, k)) = prod_mat pn
        (fun (i : _)=> if i ≤ k then transpose (FM (k - i)) else FM i ,
         k) := by
  -- === TCCs for transpose_prod_mat ===
  -- TCC: transpose_prod_mat_TCC1
  -- Statement: FORALL (k: nat, pn: posnat, FM: [nat -> SquareMatrix(pn)], i: nat):
  --   i <= k IMPLIES k - i >= 0
  have transpose_prod_mat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: transpose_prod_mat_TCC2
  -- Statement: FORALL (k: nat, pn: posnat, FM: [nat -> SquareMatrix(pn)], i: nat):
  --   i <= k IMPLIES
  --    rows(transpose(FM(k - i))) = columns(transpose(FM(k - i))) AND
  --     rows(transpose(FM(k - i))) = pn
  have transpose_prod_mat_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.is_simple_seq?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p):
--   FM(p) = Id(pn) OR
--    (include_swaps AND
--      EXISTS (i, j): i < pn AND j < pn AND FM(p) = Ers(pn)(i, j))
--     OR
--     (EXISTS (i, j, r):
--        i < pn AND j < pn AND i /= j AND FM(p) = Eas
axiom is_simple_seq_p {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35 → Prop

-- pvs2lean: matrix_det.is_simple_prod?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (FM: [nat -> SquareMatrix(pn)], k):
--   is_simple_seq?(pn)(FM, k, include_scals, include_swaps) AND
--    SQ = prod_mat(pn)(FM, k)
axiom is_simple_prod_p {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ27 → pvs_τ28 → pvs_τ29 → pvs_τ30 → Prop

-- pvs2lean: matrix_det.mult_simple_prod
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (R, S: SquareMatrix(pn)):
--   is_simple_prod?(pn, include_scals, include_swaps)(R) AND
--    is_simple_prod?(pn, include_scals, include_swaps)(S)
--    IMPLIES is_simple_prod?(pn, include_scals, includ
theorem mult_simple_prod : ∀ (R : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}) (S : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), is_simple_prod_p pn include_scals include_swaps(R) ∧
 is_simple_prod_p pn include_scals include_swaps(S) → is_simple_prod_p pn include_scals include_swaps(R * S) := by
  -- === TCCs for mult_simple_prod ===
  -- TCC: mult_simple_prod_TCC1
  -- Statement: FORALL (include_scals, include_swaps: bool, pn: posnat,
  --         R, S: SquareMatrix(pn)):
  --   is_simple_prod?(pn, include_scals, include_swaps)(R) AND
  --    is_simple_prod?(pn, include_scals, include_swa...
  have mult_simple_prod_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.Id_simple_prod
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_simple_prod?(pn, include_scals, include_swaps)(Id(pn))
theorem Id_simple_prod : is_simple_prod_p pn include_scals include_swaps (Id pn) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.Esr_simple_prod
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < pn AND include_scals IMPLIES
--  is_simple_prod?(pn, include_scals, include_swaps)(Esr(pn)(i, r))
theorem Esr_simple_prod : i < pn ∧ include_scals → is_simple_prod_p pn include_scals include_swaps(Esr pn(i, r)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.Ers_simple_prod
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < pn AND j < pn AND include_swaps IMPLIES
--  is_simple_prod?(pn, include_scals, include_swaps)(Ers(pn)(i, j))
theorem Ers_simple_prod : i < pn ∧ j < pn ∧ include_swaps → is_simple_prod_p pn include_scals include_swaps(Ers pn(i, j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.Easr_simple_prod
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < pn AND j < pn AND i /= j IMPLIES
--  is_simple_prod?(pn, include_scals, include_swaps)(Easr(pn)(i, j, r))
theorem Easr_simple_prod : i < pn ∧ j < pn ∧ i ≠ j → is_simple_prod_p pn include_scals include_swaps(Easr pn(i, j, r)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.det_simple_prod_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: SquareMatrix(pn)):
--   is_simple_prod?(pn, FALSE, FALSE)(S) IMPLIES det(S) = 1
theorem det_simple_prod_1 : ∀ (S : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), is_simple_prod_p pn False False(S) → det S = 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.det_mult_simple_prod_left
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S, R: SquareMatrix(pn)):
--   is_simple_prod?(pn, FALSE, FALSE)(S) IMPLIES det(S * R) = det(R)
theorem det_mult_simple_prod_left : ∀ (S : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}) (R : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), is_simple_prod_p pn False False(S) → det (S * R) = det R := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.Easr_null
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(Easr(pn)(i, j, r))
theorem Easr_null : ¬(null_p (Easr pn i j r)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.transpose_simple_prod
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: SquareMatrix(pn)):
--   is_simple_prod?(pn, include_scals, include_swaps)(S) IFF
--    is_simple_prod?(pn, include_scals, include_swaps)(transpose(S))
theorem transpose_simple_prod : ∀ (S : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), is_simple_prod_p pn include_scals include_swaps(S) ↔ is_simple_prod_p pn include_scals include_swaps(transpose S) := by
  -- === TCCs for transpose_simple_prod ===
  -- TCC: transpose_simple_prod_TCC1
  -- Statement: FORALL (pn: posnat, S: SquareMatrix(pn)):
  --   rows(transpose(S)) = columns(transpose(S)) AND rows(transpose(S)) = pn
  have transpose_simple_prod_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.diagonal_simple_prod
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: SquareMatrix(pn)):
--   diagonal?(S) IMPLIES is_simple_prod?(pn, TRUE, include_swaps)(S)
theorem diagonal_simple_prod : ∀ (S : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}), diagonal_p S → is_simple_prod_p pn True include_swaps(S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_det.is_simple_prod_implic
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: SquareMatrix(pn), include_scals2, include_swaps2: bool):
--   (include_scals IMPLIES include_scals2) AND
--    (include_swaps IMPLIES include_swaps2) AND
--     is_simple_prod?(pn, include_scals, in
theorem is_simple_prod_implic : ∀ (S : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = pn}) (include_scals2 : Bool) (include_swaps2 : Bool), (include_scals → include_scals2) ∧
 (include_swaps → include_swaps2) ∧
  is_simple_prod_p pn include_scals include_swaps(S) → is_simple_prod_p pn include_scals2 include_swaps2(S) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.matrix_det