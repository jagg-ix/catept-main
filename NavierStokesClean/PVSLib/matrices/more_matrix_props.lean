-- Auto-generated by pvs2lean/translator.py
-- PVS theory: more_matrix_props
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: more_matrix_props

Auto-generated stubs from PVSLib theory `more_matrix_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.more_matrix_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (MatrixMN : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (VectorN : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (below : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (col : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (columns : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (entry : pvs_τ12 → pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (length : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (min : pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (nth : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (row : pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} (rows : pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (transpose : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (vect2matrix : pvs_τ30 → pvs_τ31)

-- pvs2lean: more_matrix_props.mat_entry
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A, B: MatrixMN(m, n)):
--   A = B IFF
--    FORALL (i: below(m), j: below(n)): entry(A)(i, j) = entry(B)(i, j)
theorem mat_entry : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}), A = B ↔ ∀ (i : Finset.range m) (j : Finset.range n), entry A(i, j) = entry B(i, j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.dist_dist_left_gen
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, z: Vector): u * (v + z) = u * v + u * z
theorem dist_dist_left_gen : ∀ (u : List ℝ) (v : List ℝ) (z : List ℝ), u * (v + z) = u * v + u * z := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.nth_nth_add
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: Vector, i: below(min(length(u), length(v)))):
--   nth(u + v, i) = nth(u, i) + nth(v, i)
theorem nth_nth_add : ∀ (u : List ℝ) (v : List ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < min (List.length u) (List.length v)}), nth (u + v) i = nth u i + nth v i := by
  -- === TCCs for nth_nth_add ===
  -- TCC: nth_nth_add_TCC1
  -- Statement: FORALL (u, v: Vector, i: below(min(length[real](u), length[real](v)))):
  --   i < length[real](u + v)
  have nth_nth_add_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_nth_add_TCC2
  -- Statement: FORALL (u, v: Vector, i: below(min(length[real](u), length[real](v)))):
  --   i < length[real](u)
  have nth_nth_add_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_nth_add_TCC3
  -- Statement: FORALL (u, v: Vector, i: below(min(length[real](u), length[real](v)))):
  --   i < length[real](v)
  have nth_nth_add_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.MN_null
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, A: MatrixMN(0, n)): A = null
theorem MN_null : ∀ (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = 0 ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}), A = null := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: more_matrix_props.sum_length
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A, B: MatrixMN(m, n)):
--   rows(A + B) = rows(A) AND rows(A + B) = rows(B)
theorem sum_length : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}), rows (A + B) = rows A ∧ rows (A + B) = rows B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.sum_cols
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A, B: MatrixMN(m, n)):
--   columns(A + B) = columns(A) AND columns(A + B) = columns(B)
theorem sum_cols : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}), columns (A + B) = columns A ∧ columns (A + B) = columns B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.sum_entry
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A, B: MatrixMN(m, n), i, j: nat):
--   entry(A + B)(i, j) = entry(A)(i, j) + entry(B)(i, j)
theorem sum_entry : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), entry (A + B) (i, j) = entry A(i, j) + entry B(i, j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.row_lengths
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A, B: MatrixMN(m, n), i: nat):
--   length(row(A + B)(i)) = length(row(A)(i) + row(B)(i))
theorem row_lengths : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (i : {i : ℝ // i ≥ 0}), List.length (row (A + B) (i)) = List.length (row A(i) + row B(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.col_lengths
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A, B: MatrixMN(m, n), i: nat):
--   length(col(A + B)(i)) = length(col(A)(i) + col(B)(i))
theorem col_lengths : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (i : {i : ℝ // i ≥ 0}), List.length (col (A + B) (i)) = List.length (col A(i) + col B(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.row_add
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A, B: MatrixMN(m, n), i: nat):
--   row(A + B)(i) = row(A)(i) + row(B)(i)
theorem row_add : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (i : {i : ℝ // i ≥ 0}), row (A + B) (i) = row A(i) + row B(i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.col_add
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A, B: MatrixMN(m, n), i: nat):
--   col(A + B)(i) = col(A)(i) + col(B)(i)
theorem col_add : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (i : {i : ℝ // i ≥ 0}), col (A + B) (i) = col A(i) + col B(i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.entry_dist_left
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n, p: nat, A: MatrixMN(m, n), B, C: MatrixMN(n, p), i, j: nat):
--   entry(A * (B + C))(i, j) =
--    IF i < rows(A) AND j < columns(B) THEN row(A)(i) * col(B + C)(j)
--    ELSE 0
--    ENDIF
theorem entry_dist_left : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (p : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = n ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = p}) (C : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = n ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = p}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), entry (A * (B + C)) (i, j) = if i < rows A ∧ j < columns B then row A(i) * col (B + C) (j)
else 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.entries_match_col
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n, p: nat, A: MatrixMN(m, n), B, C: MatrixMN(n, p)):
--   columns(B) = columns(B + C)
theorem entries_match_col : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (p : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = n ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = p}) (C : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = n ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = p}), columns B = columns (B + C) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.entry_dist_left2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n, p: nat, A: MatrixMN(m, n), B, C: MatrixMN(n, p), i, j: nat):
--   entry(A * (B + C))(i, j) =
--    IF i < rows(A) AND j < columns(B)
--      THEN row(A)(i) * (col(B)(j) + col(C)(j))
--    ELSE 0
--    
theorem entry_dist_left2 : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (p : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = n ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = p}) (C : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = n ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = p}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), entry (A * (B + C)) (i, j) = if i < rows A ∧ j < columns B then row A(i) * (col B(j) + col C(j))
else 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.mult_dist_pos
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n, p: posnat, A: MatrixMN(m, n), B, C: MatrixMN(n, p)):
--   A * (B + C) = A * B + A * C
theorem mult_dist_pos : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (p : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (B : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = n ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = p}) (C : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = n ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = p}), A * (B + C) = A * B + A * C := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.scal_mat_row_length
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A: MatrixMN(m, n), c: real, i: nat):
--   length(row(c * A)(i)) = length(c * row(A)(i))
theorem scal_mat_row_length : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (c : ℝ) (i : {i : ℝ // i ≥ 0}), List.length (row (c * A) (i)) = List.length (c * row A(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.scal_mat_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A: MatrixMN(m, n), c: real, i: nat):
--   row(c * A)(i) = c * row(A)(i)
theorem scal_mat_row : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (c : ℝ) (i : {i : ℝ // i ≥ 0}), row (c * A) (i) = c * row A(i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.scal_mat_col
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: nat, A: MatrixMN(m, n), c: real, i: nat):
--   row(c * A)(i) = c * row(A)(i)
theorem scal_mat_col : ∀ (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (c : ℝ) (i : {i : ℝ // i ≥ 0}), row (c * A) (i) = c * row A(i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.col_transpose
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A: MatrixMN(m, n), i: below(rows(A))):
--   row(A)(i) = col(transpose(A))(i)
theorem col_transpose : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (i : {s : {i : ℝ // i ≥ 0} // s < rows A}), row A(i) = col (transpose A) (i) := by
  -- === TCCs for col_transpose ===
  -- TCC: col_transpose_TCC1
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), i: below(rows(A))):
  --   (null?[list[real]](A) OR
  --     FORALL (i, j: below(length[list[real]](A))):
  --       length[real](nth[list[real]](A, i)) =
  --        length[re...
  have col_transpose_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.nth_vect2matrix
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, v: VectorN(n)): nth(vect2matrix(v), 0) = v
theorem nth_vect2matrix : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (v : {l : List ℝ // List.length l = n}), nth (vect2matrix v) 0 = v := by
  -- === TCCs for nth_vect2matrix ===
  -- TCC: nth_vect2matrix_TCC1
  -- Statement: FORALL (n: posnat, v: VectorN(n)): length[real](v) > 0
  have nth_vect2matrix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_vect2matrix_TCC2
  -- Statement: FORALL (n: posnat, v: VectorN(n)): 0 < length[list[real]](vect2matrix(v))
  have nth_vect2matrix_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: more_matrix_props.dot_to_matrix_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, v1, v2: VectorN(n)):
--   v1 * v2 = nth(nth(vect2matrix(v1) * transpose(vect2matrix(v2)), 0), 0)
theorem dot_to_matrix_mult : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (v1 : {l : List ℝ // List.length l = n}) (v2 : {l : List ℝ // List.length l = n}), v1 * v2 = nth (nth (vect2matrix v1 * transpose (vect2matrix v2)) 0) 0 := by
  -- === TCCs for dot_to_matrix_mult ===
  -- TCC: dot_to_matrix_mult_TCC1
  -- Statement: FORALL (n: posnat, v1, v2: VectorN(n)): length[real](v2) > 0
  have dot_to_matrix_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dot_to_matrix_mult_TCC2
  -- Statement: FORALL (n: posnat, v1, v2: VectorN(n)):
  --   0 < length[list[real]](vect2matrix(v1) * transpose(vect2matrix(v2)))
  have dot_to_matrix_mult_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dot_to_matrix_mult_TCC3
  -- Statement: FORALL (n: posnat, v1, v2: VectorN(n)):
  --   0 <
  --    length[real]
  --        (nth[list[real]](vect2matrix(v1) * transpose(vect2matrix(v2)), 0))
  have dot_to_matrix_mult_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.matrix_vector_to_matrix_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A: MatrixMN(m, n), v: VectorN(n)):
--   vect2matrix(A * v) = transpose(A * transpose(vect2matrix(v)))
theorem matrix_vector_to_matrix_mult : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (v : {l : List ℝ // List.length l = n}), vect2matrix (A * v) = transpose (A * transpose (vect2matrix v)) := by
  -- === TCCs for matrix_vector_to_matrix_mult ===
  -- TCC: matrix_vector_to_matrix_mult_TCC1
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), v: VectorN(n)):
  --   length[real](A * v) > 0
  have matrix_vector_to_matrix_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: matrix_vector_to_matrix_mult_TCC2
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), v: VectorN(n)):
  --   rows(A * transpose(vect2matrix(v))) > 0 AND
  --    columns(A * transpose(vect2matrix(v))) > 0
  have matrix_vector_to_matrix_mult_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: more_matrix_props.transpose_scalar_matrix
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: MatrixMN(1, 1)): A = transpose(A)
theorem transpose_scalar_matrix : ∀ (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = 1 ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = 1}), A = transpose A := by
  -- === TCCs for transpose_scalar_matrix ===
  -- TCC: transpose_scalar_matrix_TCC1
  -- Statement: FORALL (A: MatrixMN(1, 1)):
  --   (null?[list[real]](A) OR
  --     FORALL (i, j: below(length[list[real]](A))):
  --       length[real](nth[list[real]](A, i)) =
  --        length[real](nth[list[real]](A, j)))
  --    AN...
  have transpose_scalar_matrix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.more_matrix_props