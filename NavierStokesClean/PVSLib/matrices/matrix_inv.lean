-- Auto-generated by pvs2lean/translator.py
-- PVS theory: matrix_inv
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: matrix_inv

Auto-generated stubs from PVSLib theory `matrix_inv`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.matrix_inv

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Easr : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (Esr : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (Id : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (MatrixMN : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (SquareMatrix : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (below : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} (columns : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} (det : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (diag : pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (entry : pvs_τ23 → pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (form_matrix : pvs_τ26 → pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (is_simple_prod_p : pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} (row : pvs_τ36 → pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} (rows : pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (transpose : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} (upper_triangular_p : pvs_τ42 → Prop)

-- pvs2lean: matrix_inv.left_inv
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET dg = diag(TRUE, FALSE, S),
--     R = dg`multip,
--     newT =
--       form_matrix(LAMBDA (i, j):
--                     IF i /= j OR entry(R * S)(i, i) = 0 THEN 0
--                     ELSE 1 / entry(R * S)(i
axiom left_inv {pvs_τ35 : Type*} : pvs_τ35 → Prop

-- pvs2lean: matrix_inv.mult_left_inv_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: det(S) /= 0 IMPLIES S * left_inv(S) = Id(rows(S))
theorem mult_left_inv_right : det S ≠ 0 → S * left_inv S = Id (rows S) := by
  -- === TCCs for mult_left_inv_right ===
  -- TCC: mult_left_inv_right_TCC1
  -- Statement: FORALL (S: Square): det(S) /= 0 IMPLIES rows(S) > 0
  have mult_left_inv_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.invertible?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (IQ):
--   rows(IQ) = rows(SQ) AND IQ * SQ = Id(rows(SQ)) AND SQ * IQ = Id(rows(SQ))
axiom invertible_p {pvs_τ30 : Type*} : pvs_τ30 → Prop

-- pvs2lean: matrix_inv.invertible_rew
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: invertible?(SQ) IFF
--  (det(SQ) /= 0 OR
--    (EXISTS (IQ): rows(IQ) = rows(SQ) AND IQ * SQ = Id(rows(SQ))) OR
--     (EXISTS (IQ): rows(IQ) = rows(SQ) AND SQ * IQ = Id(rows(SQ))))
theorem invertible_rew : invertible_p SQ ↔ (det SQ ≠ 0 ∨
  (∃ (IQ : _), rows IQ = rows SQ ∧ IQ * SQ = Id (rows SQ)) ∨
   (∃ (IQ : _), rows IQ = rows SQ ∧ SQ * IQ = Id (rows SQ))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.inverse
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: left_inv(SQ)
axiom inverse {pvs_τ29 : Type*} : pvs_τ29 → Prop

-- pvs2lean: matrix_inv.mult_inverse_left
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: invertible?(SQ) IMPLIES inverse(SQ) * SQ = Id(rows(SQ))
theorem mult_inverse_left : invertible_p SQ → inverse SQ * SQ = Id (rows SQ) := by
  -- === TCCs for mult_inverse_left ===
  -- TCC: mult_inverse_left_TCC1
  -- Statement: FORALL (SQ: Square): invertible?(SQ) IMPLIES det(SQ) /= 0 OR invertible?(SQ)
  have mult_inverse_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_inverse_left_TCC2
  -- Statement: FORALL (SQ: Square): invertible?(SQ) IMPLIES rows(SQ) > 0
  have mult_inverse_left_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.mult_inverse_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: invertible?(SQ) IMPLIES SQ * inverse(SQ) = Id(rows(SQ))
theorem mult_inverse_right : invertible_p SQ → SQ * inverse SQ = Id (rows SQ) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.inverse_unique
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(IQ) = rows(SQ) AND (IQ * SQ = Id(rows(SQ)) OR SQ * IQ = Id(rows(SQ)))
--  IMPLIES
--  (invertible?(SQ) AND
--    det(SQ) /= 0 AND
--     IQ = inverse(SQ) AND IQ * SQ = Id(rows(SQ)) AND SQ * IQ = Id(rows(SQ))
theorem inverse_unique : rows IQ = rows SQ ∧ (IQ * SQ = Id (rows SQ) ∨ SQ * IQ = Id (rows SQ)) → (invertible_p SQ ∧
  det SQ ≠ 0 ∧
   IQ = inverse SQ ∧ IQ * SQ = Id (rows SQ) ∧ SQ * IQ = Id (rows SQ)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.invertible_det
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: invertible?(SQ) IFF det(SQ) /= 0
theorem invertible_det : invertible_p SQ ↔ det SQ ≠ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.invertible_mult
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(S) = rows(R) AND
--  (det(S) /= 0 OR invertible?(S)) AND (det(R) /= 0 OR invertible?(R))
--  IMPLIES (det(S * R) /= 0 AND invertible?(S * R))
theorem invertible_mult : rows S = rows R ∧
 (det S ≠ 0 ∨ invertible_p S) ∧ (det R ≠ 0 ∨ invertible_p R) → (det (S * R) ≠ 0 ∧ invertible_p (S * R)) := by
  -- === TCCs for invertible_mult ===
  -- TCC: invertible_mult_TCC1
  -- Statement: FORALL (R, S: Square):
  --   det(S * R) /= 0 AND
  --    (det(R) /= 0 OR invertible?(R)) AND
  --     (det(S) /= 0 OR invertible?(S)) AND rows(S) = rows(R)
  --    IMPLIES
  --    (rows(S * R) > 0 AND columns(S * R) > 0) ...
  have invertible_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.inverse_mult
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(S) = rows(R) AND
--  (det(S) /= 0 OR invertible?(S)) AND (det(R) /= 0 OR invertible?(R))
--  IMPLIES inverse(S * R) = inverse(R) * inverse(S)
theorem inverse_mult : rows S = rows R ∧
 (det S ≠ 0 ∨ invertible_p S) ∧ (det R ≠ 0 ∨ invertible_p R) → inverse (S * R) = inverse R * inverse S := by
  -- === TCCs for inverse_mult ===
  -- TCC: inverse_mult_TCC1
  -- Statement: FORALL (R, S: Square):
  --   (det(R) /= 0 OR invertible?(R)) AND
  --    (det(S) /= 0 OR invertible?(S)) AND rows(S) = rows(R)
  --    IMPLIES
  --    (rows(S * R) > 0 AND columns(S * R) > 0) AND
  --     rows(S * R) = co...
  have inverse_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.det_inverse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: det(S) /= 0 OR invertible?(S) IMPLIES det(inverse(S)) = 1 / det(S)
theorem det_inverse : det S ≠ 0 ∨ invertible_p S → det (inverse S) = 1 / det S := by
  -- === TCCs for det_inverse ===
  -- TCC: det_inverse_TCC1
  -- Statement: FORALL (S: Square): det(S) /= 0 OR invertible?(S) IMPLIES det(S) /= 0
  have det_inverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.inverse_invertible
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: det(S) /= 0 OR invertible?(S) IMPLIES invertible?(inverse(S))
theorem inverse_invertible : det S ≠ 0 ∨ invertible_p S → invertible_p (inverse S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.inverse_inverse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: det(S) /= 0 OR invertible?(S) IMPLIES inverse(inverse(S)) = S
theorem inverse_inverse : det S ≠ 0 ∨ invertible_p S → inverse (inverse S) = S := by
  -- === TCCs for inverse_inverse ===
  -- TCC: inverse_inverse_TCC1
  -- Statement: FORALL (S: Square):
  --   det(S) /= 0 OR invertible?(S) IMPLIES
  --    det(inverse(S)) /= 0 OR invertible?(inverse(S))
  have inverse_inverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.GH
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: form_matrix(LAMBDA (i, j):
--               IF i < j THEN i ^ 2 + j ^ 3 + 3
--               ELSE i * j - 2 + i ^ 3 + 0.1 ^ i
--               ENDIF,
--             pn, pn)
noncomputable def GH : sorry := by
  sorry  -- pvs2lean: definition stub for form_matrix(LAMBDA (i, j):               IF i < j THEN i ^ 2 + j ^ 3 + 3        

-- pvs2lean: matrix_inv.det_nonzero_simple_prod
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: det(S) /= 0 IMPLIES is_simple_prod?(rows(S), TRUE, FALSE)(S)
theorem det_nonzero_simple_prod : det S ≠ 0 → is_simple_prod_p (rows S) True False(S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.mult_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [SquareMatrix(rows(S)) -> bool]):
--        (FORALL (i, j, r):
--           i < rows(S) AND j < rows(S) AND i /= j IMPLIES
--            P(Easr(rows(S))(i, j, r)))
--    AND (FORALL (i, r): i < rows(S)
theorem mult_induction : ∀ (P : {SQ : {D : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0} // rows D = columns D} // rows SQ = rows S} → Bool), (∀ (i : _) (j : _) (r : _), i < rows S ∧ j < rows S ∧ i ≠ j →
         P (Easr (rows S) (i, j, r)))
 ∧ (∀ (i : _) (r : _), i < rows S ∧ r ≠ 0 → P (Esr (rows S) (i, r)))
 ∧ P (Id (rows S))
 ∧ (∀ (Q : _) (R : SquareMatrix (rows S)), P Q ∧ P R → P (Q * R))
 ∧ (det S ≠ 0) → P S := by
  -- === TCCs for mult_induction ===
  -- TCC: mult_induction_TCC1
  -- Statement: FORALL (S: Square, i: nat, j: nat):
  --   i < rows(S) AND j < rows(S) AND i /= j IMPLIES rows(S) > 0
  have mult_induction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_induction_TCC2
  -- Statement: FORALL (S: Square, P: [SquareMatrix(rows(S)) -> bool]):
  --   (FORALL (i: nat, j: nat, r: real):
  --      i < rows(S) AND j < rows(S) AND i /= j IMPLIES
  --       P(Easr(rows(S))(i, j, r)))
  --    IMPLIES
  --    FORAL...
  have mult_induction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_induction_TCC3
  -- Statement: FORALL (S: Square, P: [SquareMatrix(rows(S)) -> bool]):
  --   ((FORALL (i: nat, j: nat, r: real):
  --       i < rows(S) AND j < rows(S) AND i /= j IMPLIES
  --        P(Easr(rows(S))(i, j, r)))
  --     AND
  --     FORA...
  have mult_induction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_induction_TCC4
  -- Statement: FORALL (S: Square, P: [SquareMatrix(rows(S)) -> bool]):
  --   (FORALL (i: nat, j: nat, r: real):
  --      i < rows(S) AND j < rows(S) AND i /= j IMPLIES
  --       P(Easr(rows(S))(i, j, r)))
  --    AND
  --    (FORALL (...
  have mult_induction_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_inv.det_transpose
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (SM: PosFullMatrix): det(transpose(SM)) = det(SM)
theorem det_transpose : ∀ (SM : {SM : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // null_p M ∨
 ∀ (i : _) (j : Finset.range List.length M), List.length (nth M i) = List.length (nth M j)} // rows SM > 0 ∧ columns SM > 0}), det (transpose SM) = det SM := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.matrix_inv