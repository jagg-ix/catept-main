-- Auto-generated by pvs2lean/translator.py
-- PVS theory: matrix_props
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: matrix_props

Auto-generated stubs from PVSLib theory `matrix_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.matrix_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Id : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (access : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (below : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (car : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (cdr : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (col : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (columns : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (cons : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (entry : pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (form_matrix : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} (length : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (max : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (nth : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} (null_p : pvs_τ35 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (row : pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} (rows : pvs_τ44 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (transpose : pvs_τ50 → pvs_τ51)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} (v1 : pvs_τ52 → pvs_τ53 → pvs_τ54 → pvs_τ55)

-- pvs2lean: matrix_props.matrix_2x2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) = 2 AND columns(D) = 2 IMPLIES
--  D =
--   (: (: entry(D)(0, 0), entry(D)(0, 1) :),
--      (: entry(D)(1, 0), entry(D)(1, 1) :) :)
theorem matrix_2x2 : rows D = 2 ∧ columns D = 2 → D =
 (: (: entry D(0, 0), entry D(0, 1) :),
    (: entry D(1, 0), entry D(1, 1) :) :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.length_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(row(SM)(i)) = IF i >= rows(SM) THEN 0 ELSE columns(SM) ENDIF
theorem length_row : List.length (row SM(i)) = if i ≥ rows SM then 0 else columns SM := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.length_nth_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(SM) IMPLIES length(nth(SM, i)) = columns(SM)
theorem length_nth_row : i < List.length SM → List.length (nth SM i) = columns SM := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.columns_cdr
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) > 1 IMPLIES columns(cdr(D)) = columns(D)
theorem columns_cdr : rows D > 1 → columns (cdr D) = columns D := by
  -- === TCCs for columns_cdr ===
  -- TCC: columns_cdr_TCC1
  -- Statement: FORALL (D: PosFullMatrix): rows(D) > 1 IMPLIES cons?[list[real]](D)
  have columns_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.columns_cons
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(cons(v, M)) = max(length(v), columns(M))
theorem columns_cons : columns (cons v M) = max (List.length v) (columns M) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.access_col
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: access(col(SM)(i))(j) = access(row(SM)(j))(i)
theorem access_col : access (col SM(i)) (j) = access (row SM(j)) (i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.remove
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF rows(M) = 0 OR columns(M) = 0 THEN null[list[real]]
-- ELSE form_matrix(LAMBDA (m, n):
--                    LET newm =
--                          IF m >= i OR i >= rows(M) THEN m + 1 ELSE m ENDIF,
--        
axiom remove {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ36 → pvs_τ37 → pvs_τ38 → Prop

-- pvs2lean: matrix_props.rows_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(remove(M, i, j)) =
--  IF rows(M) = 0 OR columns(M) = 0 THEN 0 ELSE rows(M) - 1 ENDIF
theorem rows_remove : rows (remove M i j) = if rows M = 0 ∨ columns M = 0 then 0 else rows M - 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.columns_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(remove(M, i, j)) =
--  IF rows(M) <= 1 OR columns(M) = 0 THEN 0 ELSE columns(M) - 1 ENDIF
theorem columns_remove : columns (remove M i j) = if rows M ≤ 1 ∨ columns M = 0 then 0 else columns M - 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.remove_remove_1_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (rows(D) > 2) AND (columns(D) > 2) AND (m <= n)
--  AND (n < columns(D) - 1) AND (m < columns(D) - 1)
--  IMPLIES remove(remove(D, 1, n + 1), 0, m) = remove(remove(D, 0, m), 0, n)
theorem remove_remove_1_0 : (rows D > 2) ∧ (columns D > 2) ∧ (m ≤ n)
 ∧ (n < columns D - 1) ∧ (m < columns D - 1) → remove (remove D 1 (n + 1)) 0 m = remove (remove D 0 m) 0 n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.remove_remove_1_0_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) > 2 AND columns(D) > 2 AND n < columns(D) - 1 IMPLIES
--  remove(remove(D, 1, 0), 0, n) = remove(remove(D, 0, 1 + n), 0, 0)
theorem remove_remove_1_0_0 : rows D > 2 ∧ columns D > 2 ∧ n < columns D - 1 → remove (remove D 1 0) 0 n = remove (remove D 0 (1 + n)) 0 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.remove_remove_1_n
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) > 2 AND columns(D) > 2 AND n + m < columns(D) - 1 IMPLIES
--  remove(remove(D, 1, n), 0, m + n) = remove(remove(D, 0, 1 + m + n), 0, n)
theorem remove_remove_1_n : rows D > 2 ∧ columns D > 2 ∧ n + m < columns D - 1 → remove (remove D 1 n) 0 (m + n) = remove (remove D 0 (1 + m + n)) 0 n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.entry_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(remove(M, i, j))(m, n) =
--  LET newm = IF m >= i OR i >= rows(M) THEN m + 1 ELSE m ENDIF,
--      newn = IF n >= j OR j >= columns(M) THEN n + 1 ELSE n ENDIF
--    IN entry(M)(newm, newn)
theorem entry_remove : entry (remove M i j) (m, n) = (let newm := if m ≥ i ∨ i ≥ rows M then m + 1 else m ; let newn := if n ≥ j ∨ j ≥ columns M then n + 1 else n ; entry M(newm, newn)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.remove_Id_0_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: pm > 1 IMPLIES remove(Id(pm), 0, 0) = Id(pm - 1)
theorem remove_Id_0_0 : pm > 1 → remove (Id pm) 0 0 = Id (pm - 1) := by
  -- === TCCs for remove_Id_0_0 ===
  -- TCC: remove_Id_0_0_TCC1
  -- Statement: FORALL (pm: posnat): pm > 1 IMPLIES pm - 1 >= 0 AND pm - 1 > 0
  have remove_id_0_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.remove_test
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: remove((: (: 1, 2 :), (: 3, 4 :) :), 1, 1) = (: (: 1 :) :)
theorem remove_test : remove ((: (: 1, 2 :), (: 3, 4 :) :)) 1 1 = (: (: 1 :) :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.det
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(M) OR rows(M) /= columns(M) THEN 0
-- ELSIF rows(M) = 1 THEN entry(M)(0, 0)
-- ELSE sigma(0, columns(M) - 1,
--            LAMBDA (k): (-1) ^ k * entry(M)(0, k) * det(remove(M, 0, k)))
-- ENDIF
axiom det {pvs_τ19 : Type*} : pvs_τ19 → Prop

-- pvs2lean: matrix_props.det_test
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c, d: real): det((: (: a, b :), (: c, d :) :)) = a * d - b * c
theorem det_test : ∀ (a : ℝ) (b : ℝ) (c : ℝ) (d : ℝ), det ((: (: a, b :), (: c, d :) :)) = a * d - b * c := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.det_size_noteq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(M) /= columns(M) IMPLIES det(M) = 0
theorem det_size_noteq : rows M ≠ columns M → det M = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.swap
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF k = i THEN F(j, p) ELSIF k = j THEN F(i, p) ELSE F(k, p) ENDIF
axiom swap {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} : pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48 → pvs_τ49 → Prop

-- pvs2lean: matrix_props.swap_fun_test
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET FF = (LAMBDA (i, j: nat): 0) IN swap(0, 1)(FF)(1, 1) = 0
theorem swap_fun_test : fun (FF : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {x : {s : {i : ℝ // i ≥ 0} // s < exp2 8} // even_p x}) => swap 0 1(FF) (1, 1) = 0 (fun (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}) => 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.entry_swap
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: entry(swap(i, j)(D))(k, p) =
--  IF k < rows(D) AND p < columns(D) THEN swap(i, j)(entry(D))(k, p)
--  ELSE 0
--  ENDIF
theorem entry_swap : entry (swap i j(D)) (k, p) = if k < rows D ∧ p < columns D then swap i j(entry D) (k, p)
else 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.swap_test
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: swap(0, 1)((: (: 1, 2 :), (: 3, 4 :) :)) = (: (: 3, 4 :), (: 1, 2 :) :)
theorem swap_test : swap 0 1((: (: 1, 2 :), (: 3, 4 :) :)) = (: (: 3, 4 :), (: 1, 2 :) :) := by
  -- === TCCs for swap_test ===
  -- TCC: swap_test_TCC1
  -- Statement: (null?[list[real]]((: (: 1, 2 :), (: 3, 4 :) :)) OR
  --   FORALL (i, j: below(length[list[real]]((: (: 1, 2 :), (: 3, 4 :) :)))):
  --     length[real](nth[list[real]]((: (: 1, 2 :), (: 3, 4 :) :), i)) =
  --   ...
  have swap_test_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.remove_swap_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) >= 2 AND columns(D) >= 2 IMPLIES
--  remove(swap(0, 1)(D), 0, n) = remove(D, 1, n)
theorem remove_swap_1 : rows D ≥ 2 ∧ columns D ≥ 2 → remove (swap 0 1(D)) 0 n = remove D 1 n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.remove_swap_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (i /= 0) AND (j /= 0) AND (i < rows(D)) AND (j < rows(D))
--  AND (rows(D) = columns(D))
--  IMPLIES remove(swap(i, j)(D), 0, n) = swap(i - 1, j - 1)(remove(D, 0, n))
theorem remove_swap_2 : (i ≠ 0) ∧ (j ≠ 0) ∧ (i < rows D) ∧ (j < rows D)
 ∧ (rows D = columns D) → remove (swap i j(D)) 0 n = swap (i - 1) (j - 1) (remove D 0 n) := by
  -- === TCCs for remove_swap_2 ===
  -- TCC: remove_swap_2_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i, j, n: nat):
  --        (rows(D) = columns(D)) AND (j < rows(D)) AND (i < rows(D))
  --    AND (j /= 0) AND (i /= 0)
  --    IMPLIES rows(remove(D, 0, n)) > 0 AND columns(remove(D, 0,...
  have remove_swap_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_swap_2_TCC2
  -- Statement: FORALL (D: PosFullMatrix, i, j: nat):
  --        (rows(D) = columns(D)) AND (j < rows(D)) AND (i < rows(D))
  --    AND (j /= 0) AND (i /= 0)
  --    IMPLIES i - 1 >= 0
  have remove_swap_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_swap_2_TCC3
  -- Statement: FORALL (D: PosFullMatrix, i, j: nat):
  --        (rows(D) = columns(D)) AND (j < rows(D)) AND (i < rows(D))
  --    AND (j /= 0) AND (i /= 0)
  --    IMPLIES j - 1 >= 0
  have remove_swap_2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.swap_sym
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: swap(i, j)(D) = swap(j, i)(D)
theorem swap_sym : swap i j(D) = swap j i(D) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.det_swap_0_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) > 1 IMPLIES det(swap(0, 1)(D)) = -det(D)
theorem det_swap_0_1 : rows D > 1 → det (swap 0 1(D)) = -det D := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.swap_swap_matrix
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < rows(D) AND j < rows(D) IMPLIES swap(i, j)(swap(i, j)(D)) = D
theorem swap_swap_matrix : i < rows D ∧ j < rows D → swap i j(swap i j(D)) = D := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.swap_similar
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (i < rows(D)) AND (j < rows(D)) AND (k < rows(D)) AND (i /= j)
--  AND (j /= k) AND (k /= i)
--  IMPLIES swap(k, j)(D) = swap(i, j)(swap(k, i)(swap(i, j)(D)))
theorem swap_similar : (i < rows D) ∧ (j < rows D) ∧ (k < rows D) ∧ (i ≠ j)
 ∧ (j ≠ k) ∧ (k ≠ i) → swap k j(D) = swap i j(swap k i(swap i j(D))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.det_swap
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i /= j AND i < rows(D) AND j < rows(D) IMPLIES det(swap(i, j)(D)) = -det(D)
theorem det_swap : i ≠ j ∧ i < rows D ∧ j < rows D → det (swap i j(D)) = -det D := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.row_swap
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < rows(D) AND j < rows(D) IMPLIES
--  row(swap(i, j)(D))(k) =
--   row(D)(IF k = i THEN j ELSIF k = j THEN i ELSE k ENDIF)
theorem row_swap : i < rows D ∧ j < rows D → row (swap i j(D)) (k) =
 row D(if k = i then j ELSIF k = j then i else k ) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.rows_swap
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(swap(i, j)(D)) = rows(D)
theorem rows_swap : rows (swap i j(D)) = rows D := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.columns_swap
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(swap(i, j)(D)) = columns(D)
theorem columns_swap : columns (swap i j(D)) = columns D := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.swap_id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: swap(i, i)(D) = D
theorem swap_id : swap i i(D) = D := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.swap_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: row(D)(i) = row(D)(j) IMPLIES swap(i, j)(D) = D
theorem swap_eq : row D(i) = row D(j) → swap i j(D) = D := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.det_rows_eq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < rows(D) AND j < rows(D) AND row(D)(i) = row(D)(j) AND i /= j IMPLIES
--  det(D) = 0
theorem det_rows_eq_0 : i < rows D ∧ j < rows D ∧ row D(i) = row D(j) ∧ i ≠ j → det D = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.replace_row
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i >= rows(D) THEN D
-- ELSIF rows(D) = 1 THEN cons(v, null[list[real]])
-- ELSIF i = 0 THEN cons(v, cdr(D))
-- ELSE cons(car(D), replace_row(i - 1, v)(cdr(D)))
-- ENDIF
axiom replace_row {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41 → Prop

-- pvs2lean: matrix_props.entry_replace_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D) = length(v) IMPLIES
--  entry(replace_row(i, v)(D))(j, k) =
--   (IF j < rows(D) AND j = i THEN access(v)(k) ELSE entry(D)(j, k) ENDIF)
theorem entry_replace_row : columns D = List.length v → entry (replace_row i v(D)) (j, k) =
 (if j < rows D ∧ j = i then access v(k) else entry D(j, k) ) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.rows_replace_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D) = length(v) IMPLIES rows(replace_row(i, v)(D)) = rows(D)
theorem rows_replace_row : columns D = List.length v → rows (replace_row i v(D)) = rows D := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.columns_replace_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D) = length(v) IMPLIES columns(replace_row(i, v)(D)) = columns(D)
theorem columns_replace_row : columns D = List.length v → columns (replace_row i v(D)) = columns D := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.remove_replace_row
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D) = length(v) AND rows(D) > 1 AND columns(D) > 1 IMPLIES
--  remove(replace_row(k, v)(D), k, j) = remove(D, k, j)
theorem remove_replace_row : columns D = List.length v ∧ rows D > 1 ∧ columns D > 1 → remove (replace_row k v(D)) k j = remove D k j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.swap_replace_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D) = length(v) AND i < rows(D) AND j < rows(D) AND k < rows(D)
--  IMPLIES
--  swap(i, j)(replace_row(k, v)(D)) =
--   (IF i = k THEN replace_row(j, v)(swap(i, j)(D))
--    ELSIF j = k THEN replace_row(i,
theorem swap_replace_row : columns D = List.length v ∧ i < rows D ∧ j < rows D ∧ k < rows D → swap i j(replace_row k v(D)) =
 (if i = k then replace_row j v(swap i j(D))
  ELSIF j = k then replace_row i v(swap i j(D))
  else replace_row k v(swap i j(D))
  ) := by
  -- === TCCs for swap_replace_row ===
  -- TCC: swap_replace_row_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i, j, k: nat, v: Vector):
  --        (i = k) AND (k < rows(D)) AND (j < rows(D)) AND (i < rows(D))
  --    AND (columns(D) = length(v))
  --    IMPLIES columns(swap(i, j)(D)) = length[r...
  have swap_replace_row_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: swap_replace_row_TCC2
  -- Statement: FORALL (D: PosFullMatrix, i, j, k: nat, v: Vector):
  --        (j = k) AND (NOT i = k) AND (k < rows(D)) AND (j < rows(D))
  --    AND (i < rows(D)) AND (columns(D) = length(v))
  --    IMPLIES columns(swap(i, j...
  have swap_replace_row_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: swap_replace_row_TCC3
  -- Statement: FORALL (D: PosFullMatrix, i, j, k: nat, v: Vector):
  --        (NOT j = k) AND (NOT i = k) AND (k < rows(D)) AND (j < rows(D))
  --    AND (i < rows(D)) AND (columns(D) = length(v))
  --    IMPLIES columns(swap(...
  have swap_replace_row_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.row_replace_row
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D) = length(v) IMPLIES
--  row(replace_row(i, v)(D))(j) =
--   IF j < rows(D) AND j = i THEN v ELSE row(D)(j) ENDIF
theorem row_replace_row : columns D = List.length v → row (replace_row i v(D)) (j) =
 if j < rows D ∧ j = i then v else row D(j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.remove_replace_row_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D) = length(v) IMPLIES
--  remove(replace_row(i, v)(D), i, j) = remove(D, i, j)
theorem remove_replace_row_eq : columns D = List.length v → remove (replace_row i v(D)) i j = remove D i j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.det_replace_row_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(v1) = length(v2) AND columns(D) = length(v1) AND i < rows(D) IMPLIES
--  det(replace_row(i, v1 + v2)(D)) =
--   det(replace_row(i, v1)(D)) + det(replace_row(i, v2)(D))
theorem det_replace_row_sum : List.length v1 = List.length v2 ∧ columns D = List.length v1 ∧ i < rows D → det (replace_row i (v1 + v2) (D)) =
 det (replace_row i v1(D)) + det (replace_row i v2(D)) := by
  -- === TCCs for det_replace_row_sum ===
  -- TCC: det_replace_row_sum_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i: nat, v1, v2: Vector):
  --   i < rows(D) AND columns(D) = length(v1) AND length(v1) = length(v2)
  --    IMPLIES columns(D) = length[real](v1 + v2)
  have det_replace_row_sum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: det_replace_row_sum_TCC2
  -- Statement: FORALL (D: PosFullMatrix, i: nat, v1, v2: Vector):
  --   i < rows(D) AND columns(D) = length(v1) AND length(v1) = length(v2)
  --    IMPLIES columns(D) = length[real](v2)
  have det_replace_row_sum_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.det_replace_row_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: columns(D) = length(v) AND i < rows(D) IMPLIES
--  det(replace_row(i, r * v)(D)) = r * det(replace_row(i, v)(D))
theorem det_replace_row_scal : columns D = List.length v ∧ i < rows D → det (replace_row i (r * v) (D)) = r * det (replace_row i v(D)) := by
  -- === TCCs for det_replace_row_scal ===
  -- TCC: det_replace_row_scal_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i: nat, r: real, v: Vector):
  --   i < rows(D) AND columns(D) = length(v) IMPLIES
  --    columns(D) = length[real](r * v)
  have det_replace_row_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.replace_row_id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < rows(D) IMPLIES replace_row(i, row(D)(i))(D) = D
theorem replace_row_id : i < rows D → replace_row i (row D(i)) (D) = D := by
  -- === TCCs for replace_row_id ===
  -- TCC: replace_row_id_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i: nat):
  --   i < rows(D) IMPLIES columns(D) = length[real](row(D)(i))
  have replace_row_id_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.det_replace_row_sum_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < rows(D) AND j < rows(D) AND i /= j IMPLIES
--  det(replace_row(i, row(D)(i) + r * row(D)(j))(D)) = det(D)
theorem det_replace_row_sum_scal : i < rows D ∧ j < rows D ∧ i ≠ j → det (replace_row i (row D(i) + r * row D(j)) (D)) = det D := by
  -- === TCCs for det_replace_row_sum_scal ===
  -- TCC: det_replace_row_sum_scal_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i, j: nat, r: real):
  --   i /= j AND j < rows(D) AND i < rows(D) IMPLIES
  --    columns(D) = length[real](row(D)(i) + r * row(D)(j))
  have det_replace_row_sum_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.replace_row_sum_to_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < rows(D) AND j < rows(D) IMPLIES
--  replace_row(i, row(D)(i) + row(D)(j))(D) =
--   replace_row(i, row(D)(i) + 1 * row(D)(j))(D)
theorem replace_row_sum_to_scal : i < rows D ∧ j < rows D → replace_row i (row D(i) + row D(j)) (D) =
 replace_row i (row D(i) + 1 * row D(j)) (D) := by
  -- === TCCs for replace_row_sum_to_scal ===
  -- TCC: replace_row_sum_to_scal_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i, j: nat):
  --   j < rows(D) AND i < rows(D) IMPLIES
  --    columns(D) = length[real](row(D)(i) + row(D)(j))
  have replace_row_sum_to_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_row_sum_to_scal_TCC2
  -- Statement: FORALL (D: PosFullMatrix, i, j: nat):
  --   j < rows(D) AND i < rows(D) IMPLIES
  --    columns(D) = length[real](row(D)(i) + 1 * row(D)(j))
  have replace_row_sum_to_scal_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.det_Id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: det(Id(pn)) = 1
theorem det_Id : det (Id pn) = 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_props.det_first_column_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i): i < rows(D) IMPLIES entry(D)(i, 0) = 0) IMPLIES det(D) = 0
theorem det_first_column_zero : (∀ (i : _), i < rows D → entry D(i, 0) = 0) → det D = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_props.remove_transpose
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rows(D) > 1 AND columns(D) > 1 IMPLIES
--  remove(transpose(D), i, j) = transpose(remove(D, j, i))
theorem remove_transpose : rows D > 1 ∧ columns D > 1 → remove (transpose D) i j = transpose (remove D j i) := by
  -- === TCCs for remove_transpose ===
  -- TCC: remove_transpose_TCC1
  -- Statement: FORALL (D: PosFullMatrix, i, j: nat):
  --   columns(D) > 1 AND rows(D) > 1 IMPLIES
  --    rows(remove(D, j, i)) > 0 AND columns(remove(D, j, i)) > 0
  have remove_transpose_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.matrix_props