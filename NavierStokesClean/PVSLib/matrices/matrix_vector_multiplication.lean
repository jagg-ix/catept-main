-- Auto-generated by pvs2lean/translator.py
-- PVS theory: matrix_vector_multiplication
-- PVSLib library: matrices
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: matrix_vector_multiplication

Auto-generated stubs from PVSLib theory `matrix_vector_multiplication`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.matrix_vector_multiplication

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Id : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (MatrixMN : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (VectorN : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (append : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (below : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (car : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (cdr : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (choose : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (cons : pvs_τ19 → pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} (cons_p : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (dot : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ29 : Type*} (length : pvs_τ29 → Prop)
variable {pvs_τ39 : Type*} (nonempty_p : pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} (norm_2 : pvs_τ40 → pvs_τ41 → pvs_τ42)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} (norm_inf : pvs_τ43 → pvs_τ44 → pvs_τ45)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (nth : pvs_τ46 → pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} (null_p : pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (row : pvs_τ50 → pvs_τ51 → pvs_τ52)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (zero_vec : pvs_τ53 → pvs_τ54)

-- pvs2lean: matrix_vector_multiplication.mult
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(v) OR null?(A) THEN null
-- ELSE cons(dot(car(A), v), mult(cdr(A), v))
-- ENDIF
axiom mult {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38

-- pvs2lean: matrix_vector_multiplication.mult_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: Matrix, v: Vector, m: nat):
--   (length(A) = m AND length(v) > 0) IMPLIES length(mult(A, v)) = m
theorem mult_length : ∀ (A : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)}) (v : List ℝ) (m : {i : ℝ // i ≥ 0}), (List.length A = m ∧ List.length v > 0) → List.length (mult A v) = m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.*
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: mult(A, v)
noncomputable def pvs_op_3389da : sorry := by
  sorry  -- pvs2lean: definition stub for mult(A, v)

-- pvs2lean: matrix_vector_multiplication.mmult_nth
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A: MatrixMN(m, n), v: VectorN(n)):
--   FORALL (i: below(m)): nth(mult(m, n, A, v), i) = row(A)(i) * v
theorem mmult_nth : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (v : {l : List ℝ // List.length l = n}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < m}), nth (mult m n A v) i = row A(i) * v := by
  -- === TCCs for mmult_nth ===
  -- TCC: mmult_nth_TCC1
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), v: VectorN(n), i: below(m)):
  --   i < length[real](mult(m, n, A, v))
  have mmult_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.max_row_2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A: MatrixMN(m, n)):
--   EXISTS (i: below(m)):
--     FORALL (j: below(m)): norm_2(n)(row(A)(j)) <= norm_2(n)(row(A)(i))
theorem max_row_2 : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < m}), ∀ (j : {s : {i : ℝ // i ≥ 0} // s < m}), norm_2 n(row A(j)) ≤ norm_2 n(row A(i)) := by
  -- === TCCs for max_row_2 ===
  -- TCC: max_row_2_TCC1
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), i: below(m), j: below(m)):
  --   cons?[real](row(A)(j))
  have max_row_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_row_2_TCC2
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), i: below(m), j: below(m)):
  --   cons?[real](row(A)(i))
  have max_row_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_row_2_TCC3
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), j: below(m)):
  --   cons?[real](row(A)(j))
  have max_row_2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_row_2_TCC4
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n)):
  --   nonempty?[nonneg_real]
  --       ({c: nonneg_real |
  --           (FORALL (j: below(m)): norm_2(n)(row(A)(j)) <= c) AND
  --            EXISTS (i: below(m)): c = norm...
  have max_row_2_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.mmult_inf_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A: MatrixMN(m, n), v: VectorN(n)):
--   norm_inf(m)(mult(m, n, A, v)) <= (max_row_2(m, n, A)) * norm_2(n)(v)
theorem mmult_inf_bound : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (v : {l : List ℝ // List.length l = n}), norm_inf m(mult m n A v) ≤ (max_row_2 m n A) * norm_2 n(v) := by
  -- === TCCs for mmult_inf_bound ===
  -- TCC: mmult_inf_bound_TCC1
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), v: VectorN(n)):
  --   cons?[real](mult(m, n, A, v))
  have mmult_inf_bound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mmult_inf_bound_TCC2
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), v: VectorN(n)): cons?[real](v)
  have mmult_inf_bound_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.mmult_dist
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A: MatrixMN(m, n), v1, v2: VectorN(n)):
--   mult(m, n, A, v1 + v2) = mult(m, n, A, v1) + mult(m, n, A, v2)
theorem mmult_dist : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (v1 : {l : List ℝ // List.length l = n}) (v2 : {l : List ℝ // List.length l = n}), mult m n A (v1 + v2) = mult m n A v1 + mult m n A v2 := by
  -- === TCCs for mmult_dist ===
  -- TCC: mmult_dist_TCC1
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), v1, v2: VectorN(n)):
  --   length[real](v1 + v2) = n
  have mmult_dist_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.mmult_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A: MatrixMN(m, n), v1: VectorN(n), c: real):
--   c * mult(m, n, A, v1) = mult(m, n, A, c * v1)
theorem mmult_scal : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (v1 : {l : List ℝ // List.length l = n}) (c : ℝ), c * mult m n A v1 = mult m n A (c * v1) := by
  -- === TCCs for mmult_scal ===
  -- TCC: mmult_scal_TCC1
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), v1: VectorN(n), c: real):
  --   length[real](c * v1) = n
  have mmult_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.mmult_sub_dist
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, n: posnat, A: MatrixMN(m, n), v1, v2: VectorN(n)):
--   mult(m, n, A, v1 - v2) = mult(m, n, A, v1) - mult(m, n, A, v2)
theorem mmult_sub_dist : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A : {M : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x) (x))
     (x)} // List.length M = m ∧ ∀ (i : Finset.range List.length M), List.length (nth M i) = n}) (v1 : {l : List ℝ // List.length l = n}) (v2 : {l : List ℝ // List.length l = n}), mult m n A (v1 - v2) = mult m n A v1 - mult m n A v2 := by
  -- === TCCs for mmult_sub_dist ===
  -- TCC: mmult_sub_dist_TCC1
  -- Statement: FORALL (m, n: posnat, A: MatrixMN(m, n), v1, v2: VectorN(n)):
  --   length[real](v1 - v2) = n
  have mmult_sub_dist_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.eye
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = 0 OR n = 0 THEN null
-- ELSIF m > n THEN append(eye(m - 1, n), (: zero_vec(n) :))
-- ELSE append(eye(m - 1, n), (: e(n, m - 1) :))
-- ENDIF
axiom eye {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ26 → pvs_τ27 → pvs_τ28

-- pvs2lean: matrix_vector_multiplication.mult_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: mult((: (: 1, 2, 3 :), (: 4, 5, 6 :), (: 7, 8, 9 :) :), (: 1, 1, 1 :)) =
--  (: 6, 15, 24 :)
theorem mult_1 : mult ((: (: 1, 2, 3 :), (: 4, 5, 6 :), (: 7, 8, 9 :) :)) ((: 1, 1, 1 :)) = (: 6, 15, 24 :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.eye_1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: eye(2, 2) = (: (: 1, 0 :), (: 0, 1 :) :)
theorem eye_1 : eye 2 2 = (: (: 1, 0 :), (: 0, 1 :) :) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.eye_2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: VectorN(3)): eye(3, 3) * v = v
theorem eye_2 : ∀ (v : {l : List ℝ // List.length l = 3}), eye 3 3 * v = v := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.eye_3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: VectorN(3)): Id(3) * v = v
theorem eye_3 : ∀ (v : {l : List ℝ // List.length l = 3}), Id 3 * v = v := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: matrix_vector_multiplication.mult_3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: mult((: (: 1, 2, 3 :), (: 4, 5, 6 :), (: 7, 8, 9 :) :), (: 1, 1 :)) =
--  (: 3, 9, 15 :)
theorem mult_3 : mult ((: (: 1, 2, 3 :), (: 4, 5, 6 :), (: 7, 8, 9 :) :)) ((: 1, 1 :)) = (: 3, 9, 15 :) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.matrix_vector_multiplication