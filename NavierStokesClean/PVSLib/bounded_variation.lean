-- Auto-generated by pvs2lean/translator.py
-- PVS theory: bounded_variation
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: bounded_variation

Auto-generated stubs from PVSLib theory `bounded_variation`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.bounded_variation

noncomputable section

-- pvs2lean: bounded_variation.T_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: bounded_variation.connected_domain
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[T]
theorem connected_domain : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.not_one_element
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[T]
theorem not_one_element : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.variation_on
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma[below(P`length - 1)]
--     (0, P`length - 2,
--      LAMBDA (n: below(P`length - 1)): abs(f(P`seq(n + 1)) - f(P`seq(n))))
noncomputable def variation_on : sorry := by
  sorry  -- pvs2lean: definition stub for sigma[below(P`length - 1)]     (0, P`length - 2,      LAMBDA (n: below(P`length 

-- pvs2lean: bounded_variation.variation_on_strictly_sort
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition(a, b)):
--    variation_on(a, b, P) = variation_on(a, b, strictly_sort(P))
theorem variation_on_strictly_sort : a < b → ∀ (P : partition a b), variation_on a b P = variation_on a b (strictly_sort P) := by
  -- === TCCs for variation_on_strictly_sort ===
  -- TCC: variation_on_strictly_sort_TCC1
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --           (strictly_sort[T, restrict[[real, real], [T, T], bool](<=)]
  --                (P)`length
  --             > 1)
  --       AND (stri...
  have variation_on_strictly_sort_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.bounded_variation?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (M: nnreal):
--    (FORALL (P: partition(a, b)): variation_on(a, b, P)(f) <= M))
noncomputable def bounded_variation_p : sorry := by
  sorry  -- pvs2lean: definition stub for (EXISTS (M: nnreal):    (FORALL (P: partition(a, b)): variation_on(a, b, P)(f) <

-- pvs2lean: bounded_variation.monotonic_BV
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (bb: T | a < bb):
--   monotonic?[(LAMBDA (x: T): a <= x AND x <= bb)]
--       (restrict[T, (LAMBDA (x: T): a <= x AND x <= bb), real](f))
--    IMPLIES bounded_variation?(a, bb)(f)
theorem monotonic_BV : ∀ (bb : {bb : {x : {x : {x : ℝ // _} // _} // _} // a < bb}), monotonic_p[(fun (x : T)=> a ≤ x ∧ x ≤ bb)]
    (restrict (f)) → bounded_variation_p a bb(f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.BV_bounded
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND bounded_variation?(a, b)(f) IMPLIES bounded_on?(a, b, f)
theorem BV_bounded : a < b ∧ bounded_variation_p a b(f) → bounded_on_p a b f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.BV_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND (FORALL (P: partition(a, b)): variation_on(a, b, P)(f) <= M)
--  IMPLIES
--  (FORALL (x: (LAMBDA (x: T): a <= x AND x <= b)):
--     abs(f(x)) <= abs(f(a)) + M)
theorem BV_bound : a < b ∧ (∀ (P : partition a b), variation_on a b P(f) ≤ M) → (∀ (x : (fun (x : T) =>  a ≤ x ∧ x ≤ b)), |f x| ≤ |f a| + M) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.BV_add
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND bounded_variation?(a, b)(f) AND bounded_variation?(a, b)(g)
--  IMPLIES bounded_variation?(a, b)((LAMBDA (x: T): f(x) + g(x)))
theorem BV_add : a < b ∧ bounded_variation_p a b(f) ∧ bounded_variation_p a b(g) → bounded_variation_p a b((fun (x : T)=> f x + g x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.BV_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND bounded_variation?(a, b)(f) IMPLIES
--  bounded_variation?(a, b)(LAMBDA (x: T): D * f(x))
theorem BV_scal : a < b ∧ bounded_variation_p a b(f) → bounded_variation_p a b(fun (x : T)=> D * f x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.BV_sub
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND bounded_variation?(a, b)(f) AND bounded_variation?(a, b)(g)
--  IMPLIES bounded_variation?(a, b)((LAMBDA (x: T): f(x) - g(x)))
theorem BV_sub : a < b ∧ bounded_variation_p a b(f) ∧ bounded_variation_p a b(g) → bounded_variation_p a b((fun (x : T)=> f x - g x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.BV_mult
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND bounded_variation?(a, b)(f) AND bounded_variation?(a, b)(g)
--  IMPLIES bounded_variation?(a, b)(LAMBDA (x: T): f(x) * g(x))
theorem BV_mult : a < b ∧ bounded_variation_p a b(f) ∧ bounded_variation_p a b(g) → bounded_variation_p a b(fun (x : T)=> f x * g x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.variation_on_subset
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a <= c AND c < d AND d <= b IMPLIES
--  FORALL (Pcd: partition(c, d)):
--    EXISTS (Pab: partition(a, b)):
--      variation_on(c, d, Pcd)(f) <= variation_on(a, b, Pab)(f)
theorem variation_on_subset : a ≤ c ∧ c < d ∧ d ≤ b → ∀ (Pcd : partition c d), ∃ (Pab : partition a b), variation_on c d Pcd(f) ≤ variation_on a b Pab(f) := by
  -- === TCCs for variation_on_subset ===
  -- TCC: variation_on_subset_TCC1
  -- Statement: FORALL (a, b, c, d: T):
  --   d <= b AND c < d AND a <= c IMPLIES
  --    FORALL (Pcd: partition[T](c, d), Pab: partition[T](a, b)): a < b
  have variation_on_subset_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.BV_subset
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a <= c AND c < d AND d <= b AND bounded_variation?(a, b)(f) IMPLIES
--  bounded_variation?(c, d)(f)
theorem BV_subset : a ≤ c ∧ c < d ∧ d ≤ b ∧ bounded_variation_p a b(f) → bounded_variation_p c d(f) := by
  -- === TCCs for BV_subset ===
  -- TCC: BV_subset_TCC1
  -- Statement: FORALL (a, b, c, d: T): d <= b AND c < d AND a <= c IMPLIES a < b
  have bv_subset_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.total_variation
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def total_variation : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: bounded_variation.total_variation_approx
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND bounded_variation?(a, b)(f) IMPLIES
--  FORALL (x: (LAMBDA (x: T): a <= x AND x <= b)):
--    a /= x IMPLIES
--     FORALL (epsi: posreal):
--       EXISTS (P: partition(a, x)):
--         variation_on(a, 
theorem total_variation_approx : a < b ∧ bounded_variation_p a b(f) → ∀ (x : (fun (x : T) =>  a ≤ x ∧ x ≤ b)), a ≠ x →
   ∀ (epsi : {x : ℝ // 0 < x}), ∃ (P : partition a x), variation_on a x P(f) > total_variation a b f(x) - epsi := by
  -- === TCCs for total_variation_approx ===
  -- TCC: total_variation_approx_TCC1
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   bounded_variation?(a, b)(f) AND a < b IMPLIES
  --    FORALL (x: (LAMBDA (x: T): a <= x AND x <= b)):
  --      a /= x IMPLIES FORALL (P: partition[T](a, x)): a < x
  have total_variation_approx_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_variation.BV_decomposition
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  (bounded_variation?(a, b)(f) IFF
--    LET CI = LAMBDA (x: T): a <= x AND x <= b IN
--      EXISTS (g, h):
--        f = (LAMBDA (x: T): g(x) - h(x)) AND
--         increasing?[(CI)](restrict[T, (C
theorem BV_decomposition : a < b → (bounded_variation_p a b(f) ↔
  (let CI := fun (x : T)=> a ≤ x ∧ x ≤ b; ∃ (g : _) (h : _), f = (fun (x : T)=> g x - h x) ∧
       increasing_p[(CI)](restrict (g)) ∧
        increasing_p[(CI)](restrict (h)))) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.bounded_variation