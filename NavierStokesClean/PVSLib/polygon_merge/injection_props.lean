-- Auto-generated by pvs2lean/translator.py
-- PVS theory: injection_props
-- PVSLib library: polygon_merge
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: injection_props

Auto-generated stubs from PVSLib theory `injection_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.injection_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (are_segments_overlapping_p : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (below : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (ccw_vertex_order_p : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (edge_of_polygon_p : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (edges_of_polygon : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (injected_edge : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (injected_vertices : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (max_overlap_point : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (member : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (min_overlap_point : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (point_in_polygon_p : pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (point_on_arb_ray_p : pvs_τ34 → pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (point_on_polygon_perimeter_p : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (point_on_segment_p : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (point_outside_polygon_p : pvs_τ41 → pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (polygon_contained_p : pvs_τ43 → pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (reverse_polygon : pvs_τ45 → pvs_τ46)
variable {pvs_τ47 : Type*} (reverse_segment : pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} (rotated_vertices : pvs_τ48 → pvs_τ49 → pvs_τ50 → pvs_τ51)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (segment_endpoint_p : pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (seq : pvs_τ56 → pvs_τ57)
variable {pvs_τ61 : Type*} (simple_polygon_2d_p : pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (subsegment_p : pvs_τ62 → pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (topleft_vertex_idx : pvs_τ64 → pvs_τ65)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} (vertex_p : pvs_τ66 → pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} (vertices : pvs_τ68 → Prop)

-- pvs2lean: injection_props.merge_pre_condition
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (B: simple_polygon_2d):
--   EXISTS (p: point_2d): point_in_polygon?(p, A) AND point_in_polygon?(p, B)
axiom merge_pre_condition {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: injection_props.merge_pre_condition_symmetric
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   merge_pre_condition(A)(B) IFF merge_pre_condition(B)(A)
theorem merge_pre_condition_symmetric : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), merge_pre_condition A(B) ↔ merge_pre_condition B(A) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.inject_post_condition?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: C =
--  (# num_vertices := injected_vertices(A, B, A`num_vertices)`length,
--     vertices := injected_vertices(A, B, A`num_vertices)`seq #)
axiom inject_post_condition_p {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: injection_props.inject_vertices_into_polygon
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom inject_vertices_into_polygon {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: injection_props.vertex_on_edge_in_injected_edge_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d,
--           i, j: nat):
--          ((Am, Bm) = inject_vertices_into_po
theorem vertex_on_edge_in_injected_edge_set : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (j < A.num_vertices) ∧ (i < Am.num_vertices)
 ∧ (s = edges_of_polygon Am(i)) ∧ (e = edges_of_polygon A(j))
 ∧ point_on_segment_p s.p1 e ∧ (s.p1 ≠ e.p2) → member s.p1 (injected_edge e B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.point_on_injected_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)), p: point_2d,
--         i, j: nat):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--          ((Am, Bm) = inject_vertices_into_polygon(A
theorem point_on_injected_segment : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (i < A.num_vertices) ∧ (j = injected_vertices A B i.List.length)
 ∧ (edges_of_polygon A(i).p1 =
       injected_vertices A B A.num_vertices.seq j)
 ∧ point_on_segment_p p (edges_of_polygon A(i)) → ∃ (e : (edge_of_polygon_p Am)), point_on_segment_p p e := by
  -- === TCCs for point_on_injected_segment ===
  -- TCC: point_on_injected_segment_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)), i: nat,
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND i < A`num_vertices
  --    I...
  have point_on_injected_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_injected_segment_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)), i, j: nat,
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    i < A`num_vertice...
  have point_on_injected_segment_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.A_vertex_is_Am_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, p: point_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND vertex?(A)(p) IM
theorem A_vertex_is_Am_vertex : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), (Am, Bm) = inject_vertices_into_polygon A B ∧ vertex_p A(p) → vertex_p Am(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_vertex_index_in_range
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d,
--           i, j: nat):
--          ((Am, Bm) = inject_vertices_into_po
theorem injected_vertex_index_in_range : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (j < A.num_vertices) ∧ (i < Am.num_vertices)
 ∧ (s = edges_of_polygon Am(i)) ∧ (e = edges_of_polygon A(j))
 ∧ point_on_segment_p s.p1 e ∧ (s.p1 = e.p1 ∨ s.p2 = e.p2) → i ≥ injected_vertices A B j.List.length ∧
 i < injected_vertices A B (j + 1).List.length := by
  -- === TCCs for injected_vertex_index_in_range ===
  -- TCC: injected_vertex_index_in_range_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d,
  --         i, j: nat):
  --        ((Am, Bm) = inject_vertices_into_polygo...
  have injected_vertex_index_in_range_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertex_index_in_range_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d,
  --         i, j: nat):
  --        ((Am, Bm) = inject_vertices_into_polygo...
  have injected_vertex_index_in_range_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_p2_eq_imp_p1_on_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           i: below(A`num_vertices), j: below(Am`num_vertices),
--           s, e: segme
theorem injected_p2_eq_imp_p1_on_segment : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < Am.num_vertices}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (Am, Bm) = inject_vertices_into_polygon A B ∧
 s.p2 = e.p2 ∧ s = edges_of_polygon A(i) ∧ e = edges_of_polygon Am(j) → point_on_segment_p e.p1 s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.edge_overlaps_with_another?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (e: (edge_of_polygon?(A))): are_segments_overlapping?(s)(e)
noncomputable def edge_overlaps_with_another_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS (e: (edge_of_polygon?(A))): are_segments_overlapping?(s)(e)

-- pvs2lean: injection_props.no_crossings_between_overlap_endpoints
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, s, e: segment_2d,
--         i: below(B`num_vertices)):
--   edge_of_polygon?(A)(s) AND
--    e = edges_of_polygon(B)(i) AND are_segments_overlapping?(s)(e)
--    IMPLIES
--    LET p
theorem no_crossings_between_overlap_endpoints : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}), edge_of_polygon_p A(s) ∧
 e = edges_of_polygon B(i) ∧ are_segments_overlapping_p s(e) → (let p := min_overlap_point s e; let q := max_overlap_point s e; ¬ ∃ (r : point_2d) (j : Finset.range B.num_vertices), (i ≠ j) ∧ (r ≠ p) ∧ (r ≠ q)
         ∧ point_on_segment_p r (edges_of_polygon B(j))
         ∧ point_on_segment_p r ((# p1 := p, p2 := q #))) := by
  -- === TCCs for no_crossings_between_overlap_endpoints ===
  -- TCC: no_crossings_between_overlap_endpoints_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, s, e: segment_2d,
  --         i: below(B`num_vertices)):
  --   edge_of_polygon?(A)(s) AND
  --    e = edges_of_polygon(B)(i) AND are_segments_overlapping?(s)(e)
  --    IMPLIES
  --    FO...
  have no_crossings_between_overlap_endpoints_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_vertex_on_edge_imp_subsegment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d,
--           j: nat):
--          ((Am, Bm) = inject_vertices_into_polyg
theorem injected_vertex_on_edge_imp_subsegment : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (j : {i : ℝ // i ≥ 0}), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (j < A.num_vertices) ∧ edge_of_polygon_p Am(s)
 ∧ (e = edges_of_polygon A(j)) ∧ point_on_segment_p s.p1 e
 ∧ (s.p1 ≠ e.p2) → subsegment_p s e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_edge_same_dir_as_original
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           i: below(A`num_vertices), j: below(Am`num_vertices),
--           s, e: segme
theorem injected_edge_same_dir_as_original : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < Am.num_vertices}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (Am, Bm) = inject_vertices_into_polygon A B ∧
 s = edges_of_polygon A(i) ∧
  e = edges_of_polygon Am(j) ∧ (s.p1 = e.p1 ∨ s.p2 = e.p2) → ^(s.p2 - s.p1) = ^(e.p2 - e.p1) := by
  -- === TCCs for injected_edge_same_dir_as_original ===
  -- TCC: injected_edge_same_dir_as_original_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         i: below(A`num_vertices), j: below(Am`num_vertices),
  --         s, e: segment_2...
  have injected_edge_same_dir_as_original_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_edge_same_dir_as_original_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         i: below(A`num_vertices), j: below(Am`num_vertices),
  --         s, e: segment_2...
  have injected_edge_same_dir_as_original_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_perimeter_points_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, p: point_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      (point_
theorem injected_perimeter_points_same : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), (Am, Bm) = inject_vertices_into_polygon A B → (point_on_polygon_perimeter_p A(p) ↔ point_on_polygon_perimeter_p Am(p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_interior_points_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, p: point_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      (point_
theorem injected_interior_points_same : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), (Am, Bm) = inject_vertices_into_polygon A B → (point_in_polygon_p p A ↔ point_in_polygon_p p Am) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_exterior_points_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, p: point_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      (point_
theorem injected_exterior_points_same : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), (Am, Bm) = inject_vertices_into_polygon A B → (point_outside_polygon_p p A ↔ point_outside_polygon_p p Am) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_topleft_vertex_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      A`vertices(topleft_v
theorem injected_topleft_vertex_same : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → A.vertices (topleft_vertex_idx A) = Am.vertices (topleft_vertex_idx Am) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.same_edge_or_vertex_intersection?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF are_segments_overlapping?(s)(e) THEN s = e OR s = reverse_segment(e)
-- ELSE FORALL (p: point_2d):
--        point_on_segment?(p, s) AND point_on_segment?(p, e) IMPLIES
--         segment_endpoint?(s)(p) AN
axiom same_edge_or_vertex_intersection_p {pvs_τ52 : Type*} {pvs_τ53 : Type*} : pvs_τ52 → pvs_τ53 → Prop

-- pvs2lean: injection_props.same_edge_or_vertex_intersection_symm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   same_edge_or_vertex_intersection?(s, e) IFF
--    same_edge_or_vertex_intersection?(e, s)
theorem same_edge_or_vertex_intersection_symm : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), same_edge_or_vertex_intersection_p s e ↔ same_edge_or_vertex_intersection_p e s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.only_vertex_intersections?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: (edge_of_polygon?(G))):
--   IF are_segments_overlapping?(s)(e) THEN s = e OR s = reverse_segment(e)
--   ELSE FORALL (p: point_2d):
--          point_on_segment?(p, s) AND point_on_segment?(p, e) I
axiom only_vertex_intersections_p {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: injection_props.only_vertex_intersections_symm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G, H: simple_polygon_2d):
--   (FORALL (e: (edge_of_polygon?(H))): only_vertex_intersections?(G, H, e))
--    IMPLIES
--    FORALL (s: (edge_of_polygon?(G))): only_vertex_intersections?(H, G, s)
theorem only_vertex_intersections_symm : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (H : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (e : (edge_of_polygon_p H)), only_vertex_intersections_p G H e) → ∀ (s : (edge_of_polygon_p G)), only_vertex_intersections_p H G s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.only_vertex_intersections_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G, H: simple_polygon_2d, s: segment_2d):
--   edge_of_polygon?(reverse_polygon(H))(s) AND
--    only_vertex_intersections?(G, H, reverse_segment(s))
--    IMPLIES
--    only_vertex_intersections?(reverse_
theorem only_vertex_intersections_reverse : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (H : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), edge_of_polygon_p (reverse_polygon H) (s) ∧
 only_vertex_intersections_p G H (reverse_segment s) → only_vertex_intersections_p (reverse_polygon G) (reverse_polygon H) s := by
  -- === TCCs for only_vertex_intersections_reverse ===
  -- TCC: only_vertex_intersections_reverse_TCC1
  -- Statement: FORALL (H: simple_polygon_2d, s: segment_2d):
  --   edge_of_polygon?(reverse_polygon(H))(s) IMPLIES
  --    edge_of_polygon?(H)(reverse_segment(s))
  have only_vertex_intersections_reverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: only_vertex_intersections_reverse_TCC2
  -- Statement: FORALL (G, H: simple_polygon_2d, s: segment_2d):
  --   edge_of_polygon?(reverse_polygon(H))(s) AND
  --    only_vertex_intersections?(G, H, reverse_segment(s))
  --    IMPLIES simple_polygon_2d?(reverse_polygon(G))
  have only_vertex_intersections_reverse_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: only_vertex_intersections_reverse_TCC3
  -- Statement: FORALL (G, H: simple_polygon_2d, s: segment_2d):
  --   edge_of_polygon?(reverse_polygon(H))(s) AND
  --    only_vertex_intersections?(G, H, reverse_segment(s))
  --    IMPLIES simple_polygon_2d?(reverse_polygon(H))
  have only_vertex_intersections_reverse_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.seq_only_vertex_intersections?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(V`length - 1)):
--   LET s = (# p1 := V`seq(i), p2 := V`seq(i + 1) #) IN
--     same_edge_or_vertex_intersection?(s, e)
axiom seq_only_vertex_intersections_p {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} : pvs_τ58 → pvs_τ59 → pvs_τ60 → Prop

-- pvs2lean: injection_props.only_vertex_int_seq_imp_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (V:
--           {V: finseq[point_2d] |
--              FORALL (i: below(V`length - 1)): V`seq(i) /= V`seq(i + 1)},
--         G, H: simple_polygon_2d, e: (edge_of_polygon?(H))):
--   seq_only_vertex_inter
theorem only_vertex_int_seq_imp_polygon : ∀ (V : {V : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // ∀ (i : Finset.range V.List.length - 1), ¬ V.seq i = V.seq (1 + i)}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (H : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), seq_only_vertex_intersections_p V H e ∧
 V.List.length = G.num_vertices + 1 ∧
  (V ^ (0, V.List.length - 2)).seq = G.vertices ∧
   V.seq (V.List.length - 1) = G.vertices 0 → only_vertex_intersections_p G H e := by
  -- === TCCs for only_vertex_int_seq_imp_polygon ===
  -- TCC: only_vertex_int_seq_imp_polygon_TCC1
  -- Statement: FORALL (V:
  --           {V: finseq[point_2d] |
  --              FORALL (i: below(V`length - 1)): V`seq(i) /= V`seq(i + 1)},
  --         G, H: simple_polygon_2d, e: (edge_of_polygon?(H))):
  --   seq_only_vertex_in...
  have only_vertex_int_seq_imp_polygon_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: only_vertex_int_seq_imp_polygon_TCC2
  -- Statement: FORALL (V:
  --           {V: finseq[point_2d] |
  --              FORALL (i: below(V`length - 1)): V`seq(i) /= V`seq(i + 1)},
  --         G, H: simple_polygon_2d, e: (edge_of_polygon?(H))):
  --   seq_only_vertex_in...
  have only_vertex_int_seq_imp_polygon_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: only_vertex_int_seq_imp_polygon_TCC3
  -- Statement: FORALL (V:
  --           {V: finseq[point_2d] |
  --              FORALL (i: below(V`length - 1)): V`seq(i) /= V`seq(i + 1)},
  --         G, H: simple_polygon_2d, e: (edge_of_polygon?(H))):
  --   seq_only_vertex_in...
  have only_vertex_int_seq_imp_polygon_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: only_vertex_int_seq_imp_polygon_TCC4
  -- Statement: FORALL (V:
  --           {V: finseq[point_2d] |
  --              FORALL (i: below(V`length - 1)): V`seq(i) /= V`seq(i + 1)},
  --         G, H: simple_polygon_2d, e: (edge_of_polygon?(H))):
  --   seq_only_vertex_in...
  have only_vertex_int_seq_imp_polygon_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.edges_included_imp_edge_sets_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (s: segment_2d):
--      edge_of_polygon?(A)(s) IMPLIES edge_of_polygon?(B)(s))
--    IMPLIES
--    A`num_vertices = B`num_vertices AND
--     FORALL (e: segment_2d):
--  
theorem edges_included_imp_edge_sets_eq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (s : segment_2d), edge_of_polygon_p A(s) → edge_of_polygon_p B(s)) → A.num_vertices = B.num_vertices ∧
 ∀ (e : segment_2d), edge_of_polygon_p B(e) → edge_of_polygon_p A(e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.edges_included_imp_vertex_sets_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (s: segment_2d):
--      edge_of_polygon?(A)(s) IMPLIES edge_of_polygon?(B)(s))
--    IMPLIES
--    FORALL (i: below(A`num_vertices)):
--      EXISTS (j: below(B`num_ve
theorem edges_included_imp_vertex_sets_eq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (s : segment_2d), edge_of_polygon_p A(s) → edge_of_polygon_p B(s)) → ∀ (i : Finset.range A.num_vertices), ∃ (j : Finset.range B.num_vertices), A.vertices i = B.vertices j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.edges_included_imp_rotated_vertices
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (s: segment_2d):
--      edge_of_polygon?(A)(s) IMPLIES edge_of_polygon?(B)(s))
--    IMPLIES
--    EXISTS (n: nat):
--      A`vertices = rotated_vertices(B`num_vertice
theorem edges_included_imp_rotated_vertices : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (s : segment_2d), edge_of_polygon_p A(s) → edge_of_polygon_p B(s)) → ∃ (n : ℕ), A.vertices = rotated_vertices B.num_vertices(B.vertices, n) := by
  -- === TCCs for edges_included_imp_rotated_vertices ===
  -- TCC: edges_included_imp_rotated_vertices_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d):
  --   (FORALL (s: segment_2d):
  --      edge_of_polygon?(A)(s) IMPLIES edge_of_polygon?(B)(s))
  --    IMPLIES FORALL (x: nat): x < B`num_vertices IFF x < A`num_vertices
  have edges_included_imp_rotated_vertices_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.edges_included_imp_topleft_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (s: segment_2d):
--      edge_of_polygon?(A)(s) IMPLIES edge_of_polygon?(B)(s))
--    IMPLIES
--    A`vertices(topleft_vertex_idx(A)) = B`vertices(topleft_vertex_idx
theorem edges_included_imp_topleft_eq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (s : segment_2d), edge_of_polygon_p A(s) → edge_of_polygon_p B(s)) → A.vertices (topleft_vertex_idx A) = B.vertices (topleft_vertex_idx B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.edges_included_imp_ccw_eqv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (s: segment_2d):
--      edge_of_polygon?(A)(s) IMPLIES edge_of_polygon?(B)(s))
--    IMPLIES (ccw_vertex_order?(A) IFF ccw_vertex_order?(B))
theorem edges_included_imp_ccw_eqv : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (s : segment_2d), edge_of_polygon_p A(s) → edge_of_polygon_p B(s)) → (ccw_vertex_order_p A ↔ ccw_vertex_order_p B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.outer_polygon_has_pt_left_of_inner_perim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (p: point_2d):
--      point_on_polygon_perimeter?(A)(p) IMPLIES
--       point_outside_polygon?(p, B))
--    AND
--    (EXISTS (u: point_2d):
--       point_in_polygon?(u
theorem outer_polygon_has_pt_left_of_inner_perim : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (p : point_2d), point_on_polygon_perimeter_p A(p) → point_outside_polygon_p p B)
 ∧
 (∃ (u : point_2d), point_in_polygon_p u A ∧
     (point_in_polygon_p u B ∨ point_on_polygon_perimeter_p B(u))) → ∃ (q : point_2d), vertex_p A(q) ∧
   ∀ (r : point_2d), point_on_polygon_perimeter_p B(r) → q.x < r.x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.inject_vertices_into_polygon_comm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     merge_pre_condition(A)(B) AND
--      (Am, Bm) = inject_vertices_into_polygon(A, B
theorem inject_vertices_into_polygon_comm : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), merge_pre_condition A(B) ∧ (Am, Bm) = inject_vertices_into_polygon A B → (Bm, Am) = inject_vertices_into_polygon B A := by
  -- === TCCs for inject_vertices_into_polygon_comm ===
  -- TCC: inject_vertices_into_polygon_comm_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d):
  --   merge_pre_condition(A)(B) AND
  --    (Am, Bm) = inject_vertices_into_polygon(A, B)
  --   ...
  have inject_vertices_into_polygon_comm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.intersections_only_at_vertices
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d,
--           p: point_2d):
--          ((Am, Bm) = inject_vertices_into_
theorem intersections_only_at_vertices : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ edge_of_polygon_p A(s) ∧ edge_of_polygon_p B(e)
 ∧ (¬ are_segments_overlapping_p s(e)) ∧ point_on_segment_p p s
 ∧ point_on_segment_p p e → vertex_p Am(p) ∧ vertex_p Bm(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_intersections_only_at_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d,
--           p: point_2d):
--          ((Am, Bm) = inject_vertices_into_
theorem injected_intersections_only_at_endpoints : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ edge_of_polygon_p Am(s) ∧ edge_of_polygon_p Bm(e)
 ∧ (¬ are_segments_overlapping_p s(e)) ∧ point_on_segment_p p s
 ∧ point_on_segment_p p e → segment_endpoint_p s(p) ∧ segment_endpoint_p e(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_intersections_only_at_vertices
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d,
--           p: point_2d):
--          ((Am, Bm) = inject_vertices_into_
theorem injected_intersections_only_at_vertices : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ edge_of_polygon_p Am(s) ∧ edge_of_polygon_p Bm(e)
 ∧ (¬ are_segments_overlapping_p s(e)) ∧ point_on_segment_p p s
 ∧ point_on_segment_p p e → vertex_p Am(p) ∧ vertex_p Bm(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.overlapping_injected_edge_p1_is_MOP
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      edge_o
theorem overlapping_injected_edge_p1_is_MOP : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (Am, Bm) = inject_vertices_into_polygon A B ∧
 edge_of_polygon_p Am(s) ∧
  edge_of_polygon_p Bm(e) ∧ are_segments_overlapping_p s(e) → s.p1 = min_overlap_point s e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.overlapping_injected_edge_p2_is_MOP
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      edge_o
theorem overlapping_injected_edge_p2_is_MOP : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (Am, Bm) = inject_vertices_into_polygon A B ∧
 edge_of_polygon_p Am(s) ∧
  edge_of_polygon_p Bm(e) ∧ are_segments_overlapping_p s(e) → s.p2 = max_overlap_point s e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.overlapping_edges_share_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      edge_o
theorem overlapping_edges_share_endpoints : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (Am, Bm) = inject_vertices_into_polygon A B ∧
 edge_of_polygon_p Am(s) ∧
  edge_of_polygon_p Bm(e) ∧ are_segments_overlapping_p s(e) → if ^(s.p2 - s.p1) = ^(e.p2 - e.p1) then s.p1 = e.p1 ∧ s.p2 = e.p2
else s.p1 = e.p2 ∧ s.p2 = e.p1 := by
  -- === TCCs for overlapping_edges_share_endpoints ===
  -- TCC: overlapping_edges_share_endpoints_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    edge_of_po...
  have overlapping_edges_share_endpoints_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: overlapping_edges_share_endpoints_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    edge_of_po...
  have overlapping_edges_share_endpoints_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_polygons_contained
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      (polygon_contained?(
theorem injected_polygons_contained : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → (polygon_contained_p A B ↔ polygon_contained_p Am Bm) ∧
 (polygon_contained_p B A ↔ polygon_contained_p Bm Am) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_overlaps_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      edge_o
theorem injected_overlaps_equal : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (Am, Bm) = inject_vertices_into_polygon A B ∧
 edge_of_polygon_p Am(s) ∧
  edge_of_polygon_p Bm(e) ∧ are_segments_overlapping_p s(e) → s = e ∨ s = reverse_segment e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_only_vertex_intersections
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      (FORALL (e: (edge_of
theorem injected_only_vertex_intersections : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → (∀ (e : (edge_of_polygon_p Bm)), only_vertex_intersections_p Am Bm e)
 ∧
 (∀ (s : (edge_of_polygon_p Am)), only_vertex_intersections_p Bm Am s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_p2_on_ray_imp_edges_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
--          ((Am, Bm) = inject_vertices_into_polygon(A, B))
--      AND
theorem injected_p2_on_ray_imp_edges_eq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ edge_of_polygon_p Am(s) ∧ edge_of_polygon_p Bm(e) ∧ (s.p1 = e.p1)
 ∧ point_on_arb_ray_p s.p2 e.p1 (^(e.p2 - e.p1)) → s = e := by
  -- === TCCs for injected_p2_on_ray_imp_edges_eq ===
  -- TCC: injected_p2_on_ray_imp_edges_eq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    edge_of_po...
  have injected_p2_on_ray_imp_edges_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: injection_props.injected_p1_on_ray_imp_edges_eq_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
--          ((Am, Bm) = inject_vertices_into_polygon(A, B))
--      AND
theorem injected_p1_on_ray_imp_edges_eq_rev : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ edge_of_polygon_p Am(s) ∧ edge_of_polygon_p Bm(e) ∧ (s.p1 = e.p2)
 ∧ point_on_arb_ray_p e.p1 s.p1 (^(s.p2 - s.p1)) → reverse_segment s = e := by
  -- === TCCs for injected_p1_on_ray_imp_edges_eq_rev ===
  -- TCC: injected_p1_on_ray_imp_edges_eq_rev_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, s, e: segment_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    edge_of_po...
  have injected_p1_on_ray_imp_edges_eq_rev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.injection_props