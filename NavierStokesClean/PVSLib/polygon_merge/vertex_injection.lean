-- Auto-generated by pvs2lean/translator.py
-- PVS theory: vertex_injection
-- PVSLib library: polygon_merge
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: vertex_injection

Auto-generated stubs from PVSLib theory `vertex_injection`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.vertex_injection

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (are_segments_overlapping_p : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (below : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (card : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} (choose : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (edge_of_polygon_p : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (edges_of_polygon : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} (empty_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (find_seq_index : pvs_τ12 → pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (is_point_on_segment_p : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (max_overlap_point : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (member : pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (min : pvs_τ34 → pvs_τ35)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (min_overlap_point : pvs_τ36 → pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (next_index : pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} (nonempty_p : pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} (norm : pvs_τ41 → Prop)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} (point_on_segment_p : pvs_τ46 → pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} (polygon_edges_do_not_cross_p : pvs_τ48 → pvs_τ49 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (rest : pvs_τ53 → pvs_τ54)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} (segment_endpoint_p : pvs_τ55 → pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} (seq : pvs_τ57 → Prop)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (simple_polygon_2d_p : pvs_τ60 → pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} (singleton_seq : pvs_τ62 → Prop)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} (subsegment_p : pvs_τ63 → pvs_τ64 → Prop)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (subset_p : pvs_τ65 → pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (uniq_vertex_list_p : pvs_τ67 → pvs_τ68 → Prop)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (upto : pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} (vertices : pvs_τ71 → pvs_τ72)

-- pvs2lean: vertex_injection.injected_edge
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: {p: point_2d |
--    p /= e`p2 AND
--     (p = e`p1 OR
--       EXISTS (s: (edge_of_polygon?(B))):
--         IF are_segments_overlapping?(e)(s)
--           THEN p = min_overlap_point(e, s) OR p = max_overlap_point
axiom injected_edge {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} : pvs_τ15 → pvs_τ16 → pvs_τ17 → Prop

-- pvs2lean: vertex_injection.injected_edge_seq_rec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(P) THEN empty_seq
-- ELSIF card(P) = 1 THEN singleton_seq(choose(P))
-- ELSE LET u = choose(P),
--          S = injected_edge_seq_rec(e, rest(P)),
--          L = S`length,
--          A = {j: below(L) | n
axiom injected_edge_seq_rec {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ22 → pvs_τ23 → Prop

-- pvs2lean: vertex_injection.injected_edge_seq_props
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, P: finite_set[point_2d], S: finseq[point_2d]):
--   (FORALL (u: point_2d): P(u) IMPLIES point_on_segment?(u, e)) AND
--    S = injected_edge_seq_rec(e, P)
--    IMPLIES
--    S`length = car
theorem injected_edge_seq_props : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (P : ℝ × ℝ → Bool) (S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), (∀ (u : point_2d), P u → point_on_segment_p u e) ∧
 S = injected_edge_seq_rec e P → S.List.length = card P ∧
 (∀ (v : point_2d), P v → ∃ (i : ℕ), i < S.List.length ∧ v = S.seq i)
  ∧
  (∀ (i : ℕ), i < S.List.length → member (S.seq i) P) ∧
   (∀ (i : ℕ) (j : Finset.range i), i < S.List.length → norm (S.seq j - e.p1) < norm (S.seq i - e.p1)) := by
  -- === TCCs for injected_edge_seq_props ===
  -- TCC: injected_edge_seq_props_TCC1
  -- Statement: FORALL (e: segment_2d, P: finite_set[point_2d]):
  --   (FORALL (u: point_2d): P(u) IMPLIES point_on_segment?(u, e)) IMPLIES
  --    FORALL (u: (P)): point_on_segment?(u, e)
  have injected_edge_seq_props_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_edge_seq_props_TCC2
  -- Statement: FORALL (e: segment_2d, P: finite_set[point_2d], S: finseq[point_2d]):
  --   (     (FORALL (u: point_2d): P(u) IMPLIES point_on_segment?(u, e))
  --     AND (S = injected_edge_seq_rec(e, P)) AND (S`length = ...
  have injected_edge_seq_props_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_edge_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom injected_edge_seq {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ20 → pvs_τ21 → Prop

-- pvs2lean: vertex_injection.injected_vertices
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF index = 0 THEN empty_seq
-- ELSE LET e = edges_of_polygon(A)(index - 1) IN
--        injected_vertices(A, B, index - 1) o
--         injected_edge_seq(e, injected_edge(e, B))
-- ENDIF
axiom injected_vertices {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: vertex_injection.seq_injected_edge_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom seq_injected_edge_seq {pvs_τ58 : Type*} {pvs_τ59 : Type*} : pvs_τ58 → pvs_τ59 → Prop

-- pvs2lean: vertex_injection.reduce_seq_injected_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET S = seq_injected_edge_seq(A, B) IN
--   IF index = 0 THEN empty_seq
--   ELSE reduce_seq_injected_seq(A, B, index - 1) o
--         seq_injected_edge_seq(A, B)`seq(index - 1)
--   ENDIF
axiom reduce_seq_injected_seq {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} : pvs_τ50 → pvs_τ51 → pvs_τ52 → Prop

-- pvs2lean: vertex_injection.num_injected_edge_vertices
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN 0
-- ELSE num_injected_edge_vertices(A, B, i - 1) +
--       card(injected_edge(edges_of_polygon(A)(i - 1), B))
-- ENDIF
axiom num_injected_edge_vertices {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} : pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45

-- pvs2lean: vertex_injection.injected_edge_indices
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := A`num_vertices + 1,
--    seq
--      := LAMBDA (i: upto(A`num_vertices)):
--           num_injected_edge_vertices(A, B, i) #)
axiom injected_edge_indices {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ18 → pvs_τ19 → Prop

-- pvs2lean: vertex_injection.length_injected_vertices_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, k: nat):
--   k <= A`num_vertices IMPLIES k <= injected_vertices(A, B, k)`length
theorem length_injected_vertices_bound : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices → k ≤ injected_vertices A B k.List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_edge_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, k: nat):
--   k < A`num_vertices IMPLIES
--    nonempty?(injected_edge(edges_of_polygon(A)(k), B))
theorem injected_edge_nonempty : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k < A.num_vertices → nonempty_p (injected_edge (edges_of_polygon A(k)) B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.length_injected_vertices_incr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, k: nat):
--   k < A`num_vertices IMPLIES
--    injected_vertices(A, B, k)`length < injected_vertices(A, B, k + 1)`length
theorem length_injected_vertices_incr : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k < A.num_vertices → injected_vertices A B k.List.length < injected_vertices A B (k + 1).List.length := by
  -- === TCCs for length_injected_vertices_incr ===
  -- TCC: length_injected_vertices_incr_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, k: nat):
  --   k < A`num_vertices IMPLIES k <= A`num_vertices
  have length_injected_vertices_incr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: length_injected_vertices_incr_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, k: nat):
  --   k < A`num_vertices IMPLIES k + 1 <= A`num_vertices
  have length_injected_vertices_incr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.length_injected_vertices_ordered
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j, k: nat):
--   j < k AND k <= A`num_vertices IMPLIES
--    injected_vertices(A, B, j)`length < injected_vertices(A, B, k)`length
theorem length_injected_vertices_ordered : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), j < k ∧ k ≤ A.num_vertices → injected_vertices A B j.List.length < injected_vertices A B k.List.length := by
  -- === TCCs for length_injected_vertices_ordered ===
  -- TCC: length_injected_vertices_ordered_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, j, k: nat):
  --   j < k AND k <= A`num_vertices IMPLIES j <= A`num_vertices
  have length_injected_vertices_ordered_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.len_injected_vert_eq_num_inj_vert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, k: nat):
--   k <= A`num_vertices IMPLIES
--    injected_vertices(A, B, k)`length = num_injected_edge_vertices(A, B, k)
theorem len_injected_vert_eq_num_inj_vert : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices → injected_vertices A B k.List.length = num_injected_edge_vertices A B k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_edge_indices_eq_len_inj_vert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, k: nat):
--   k <= A`num_vertices IMPLIES
--    injected_vertices(A, B, k)`length = injected_edge_indices(A, B)`seq(k)
theorem injected_edge_indices_eq_len_inj_vert : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices → injected_vertices A B k.List.length = injected_edge_indices A B.seq k := by
  -- === TCCs for injected_edge_indices_eq_len_inj_vert ===
  -- TCC: injected_edge_indices_eq_len_inj_vert_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, k: nat):
  --   k <= A`num_vertices IMPLIES k < injected_edge_indices(A, B)`length
  have injected_edge_indices_eq_len_inj_vert_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_edge_indices_incr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, k: nat):
--   k < A`num_vertices IMPLIES
--    injected_edge_indices(A, B)`seq(k) <
--     injected_edge_indices(A, B)`seq(k + 1)
theorem injected_edge_indices_incr : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k < A.num_vertices → injected_edge_indices A B.seq k < injected_edge_indices A B.seq (k + 1) := by
  -- === TCCs for injected_edge_indices_incr ===
  -- TCC: injected_edge_indices_incr_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, k: nat):
  --   k < A`num_vertices IMPLIES k < injected_edge_indices(A, B)`length
  have injected_edge_indices_incr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_edge_indices_incr_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, k: nat):
  --   k < A`num_vertices IMPLIES k + 1 < injected_edge_indices(A, B)`length
  have injected_edge_indices_incr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_edge_indices_ordered
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j, k: nat):
--   j < k AND k <= A`num_vertices IMPLIES
--    injected_edge_indices(A, B)`seq(j) < injected_edge_indices(A, B)`seq(k)
theorem injected_edge_indices_ordered : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), j < k ∧ k ≤ A.num_vertices → injected_edge_indices A B.seq j < injected_edge_indices A B.seq k := by
  -- === TCCs for injected_edge_indices_ordered ===
  -- TCC: injected_edge_indices_ordered_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, j, k: nat):
  --   j < k AND k <= A`num_vertices IMPLIES
  --    j < injected_edge_indices(A, B)`length
  have injected_edge_indices_ordered_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertices_subseq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j, k, last: nat):
--   0 < j AND
--    j <= k AND
--     k <= A`num_vertices AND last = injected_vertices(A, B, j)`length - 1
--    IMPLIES
--    injected_vertices(A, B, j) = injecte
theorem injected_vertices_subseq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}) (last : {i : ℝ // i ≥ 0}), 0 < j ∧
 j ≤ k ∧
  k ≤ A.num_vertices ∧ last = injected_vertices A B j.List.length - 1 → injected_vertices A B j = injected_vertices A B k ^ (0, last) := by
  -- === TCCs for injected_vertices_subseq ===
  -- TCC: injected_vertices_subseq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, j, k: nat):
  --   0 < j AND j <= k AND k <= A`num_vertices IMPLIES j <= A`num_vertices
  have injected_vertices_subseq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertices_tail_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i, k: nat):
--   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
--    injected_vertices(A, B, k)`seq(i) =
--     injected_vertices(A, B, A`num_vertices)`
theorem injected_vertices_tail_eq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices ∧ i < injected_vertices A B k.List.length → injected_vertices A B k.seq i =
 injected_vertices A B A.num_vertices.seq i := by
  -- === TCCs for injected_vertices_tail_eq ===
  -- TCC: injected_vertices_tail_eq_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, i, k: nat):
  --   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
  --    i < injected_vertices(A, B, A`num_vertices)`length
  have injected_vertices_tail_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertices_tail_eq_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, i, k: nat):
  --   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
  --    A`num_vertices <= A`num_vertices
  have injected_vertices_tail_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vert_seq_fns_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, k: nat):
--   k <= A`num_vertices IMPLIES
--    reduce_seq_injected_seq(A, B, k) = injected_vertices(A, B, k)
theorem injected_vert_seq_fns_equiv : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices → reduce_seq_injected_seq A B k = injected_vertices A B k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.inj_edge_seq_len_eq_diff_inj_indices
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j: nat):
--   j < A`num_vertices IMPLIES
--    seq_injected_edge_seq(A, B)`seq(j)`length =
--     injected_edge_indices(A, B)`seq(1 + j) -
--      injected_edge_indices(A, B)`seq(
theorem inj_edge_seq_len_eq_diff_inj_indices : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {i : ℝ // i ≥ 0}), j < A.num_vertices → seq_injected_edge_seq A B.seq j.List.length =
 injected_edge_indices A B.seq (1 + j) - injected_edge_indices A B.seq j := by
  -- === TCCs for inj_edge_seq_len_eq_diff_inj_indices ===
  -- TCC: inj_edge_seq_len_eq_diff_inj_indices_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, j: nat):
  --   j < A`num_vertices IMPLIES j < seq_injected_edge_seq(A, B)`length
  have inj_edge_seq_len_eq_diff_inj_indices_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inj_edge_seq_len_eq_diff_inj_indices_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, j: nat):
  --   j < A`num_vertices IMPLIES 1 + j < injected_edge_indices(A, B)`length
  have inj_edge_seq_len_eq_diff_inj_indices_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vert_seq_elements_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i, k: nat):
--   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
--    EXISTS (j: below(k), m, n: nat):
--           (m = injected_edge_indices(A, B)`seq(
theorem injected_vert_seq_elements_eq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices ∧ i < injected_vertices A B k.List.length → ∃ (j : Finset.range k) (m : _) (n : ℕ), (m = injected_edge_indices A B.seq j)
   ∧ (n = injected_edge_indices A B.seq (j + 1)) ∧ (m ≤ i) ∧ (i < n)
   ∧ (injected_vertices A B (j + 1).seq i =
         seq_injected_edge_seq A B.seq j.seq (i - m)) := by
  -- === TCCs for injected_vert_seq_elements_eq ===
  -- TCC: injected_vert_seq_elements_eq_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, i, k: nat):
  --   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
  --    FORALL (j: below(k)): j < injected_edge_indices(A, B)`length
  have injected_vert_seq_elements_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vert_seq_elements_eq_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, i, k: nat):
  --   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
  --    FORALL (j: below(k), m: nat):
  --      m = injected_edge_indices(A, B)`seq(j) IMP...
  have injected_vert_seq_elements_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vert_seq_elements_eq_TCC3
  -- Statement: FORALL (A, B: simple_polygon_2d, i, k: nat):
  --   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
  --    FORALL (j: below(k), m, n: nat):
  --      m = injected_edge_indices(A, B)`seq(j) ...
  have injected_vert_seq_elements_eq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vert_seq_elements_eq_TCC4
  -- Statement: FORALL (A, B: simple_polygon_2d, i, k: nat):
  --   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
  --    FORALL (j: below(k), m, n: nat):
  --      m = injected_edge_indices(A, B)`seq(j) ...
  have injected_vert_seq_elements_eq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vert_seq_elements_eq_TCC5
  -- Statement: FORALL (A, B: simple_polygon_2d, i, k: nat):
  --   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
  --    FORALL (j: below(k), m, n: nat):
  --      m = injected_edge_indices(A, B)`seq(j) ...
  have injected_vert_seq_elements_eq_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vert_seq_elements_eq_TCC6
  -- Statement: FORALL (A, B: simple_polygon_2d, i, k: nat):
  --   k <= A`num_vertices AND i < injected_vertices(A, B, k)`length IMPLIES
  --    FORALL (j: below(k), m, n: nat):
  --      m = injected_edge_indices(A, B)`seq(j) ...
  have injected_vert_seq_elements_eq_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_edge_seq_elements_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat):
--   LET IV = injected_vertices(A, B, A`num_vertices),
--       s = edges_of_polygon(A)(j),
--       L = injected_vertices(A, B, j)`length,
--  
theorem injected_edge_seq_elements_eq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (i : {i : ℝ // i ≥ 0}), fun (IV : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) => fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (L : {i : ℝ // i ≥ 0}) => fun (Q : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length = card (injected_edge s B) ∧
 (∀ (v : (injected_edge s B)), ∃ (i : Finset.range S.List.length), v = S.seq i)
  ∧
  (∀ (i : Finset.range S.List.length), member (S.seq i) (injected_edge s B)) ∧
   ∀ (i : Finset.range S.List.length) (j : Finset.range i), norm (S.seq j - s.p1) < norm (S.seq i - s.p1)}) => i < IV.List.length ∧ i ≥ L ∧ i < Q.List.length + L → IV.seq i = Q.seq (i - L) (injected_edge_seq s (injected_edge s B)) (injected_vertices A B j.List.length) (edges_of_polygon A j) (injected_vertices A B A.num_vertices) := by
  -- === TCCs for injected_edge_seq_elements_eq ===
  -- TCC: injected_edge_seq_elements_eq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, j: below(A`num_vertices)):
  --   A`num_vertices <= A`num_vertices
  have injected_edge_seq_elements_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_edge_seq_elements_eq_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices),
  --         IV: finseq[point_2d], s: segment_2d):
  --   IV = injected_vertices(A, B, A`num_vertices) AND
  --    s = edges_of_polygon(A)(j)
  --    IMPLIES ...
  have injected_edge_seq_elements_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_edge_seq_elements_eq_TCC3
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat,
  --         IV: finseq[point_2d], s: segment_2d, L: nat,
  --         Q:
  --           {S: finseq[point_2d] |
  --              S`length = card[poin...
  have injected_edge_seq_elements_eq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.member_injected_edge_on_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, e: segment_2d, B: simple_polygon_2d):
--   member(v, injected_edge(e, B)) IMPLIES point_on_segment?(v, e)
theorem member_injected_edge_on_segment : ∀ (v : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), member v (injected_edge e B) → point_on_segment_p v e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.length_injected_edge_seq_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, A, B: simple_polygon_2d, j: nat):
--   j < A`num_vertices AND e = edges_of_polygon(A)(j) IMPLIES
--    injected_edge_seq(e, injected_edge(e, B))`length > 0
theorem length_injected_edge_seq_pos : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {i : ℝ // i ≥ 0}), j < A.num_vertices ∧ e = edges_of_polygon A(j) → injected_edge_seq e (injected_edge e B).List.length > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertices_length_max
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i: nat):
--   i <= A`num_vertices AND
--    injected_vertices(A, B, i)`length =
--     injected_vertices(A, B, A`num_vertices)`length
--    IMPLIES A`num_vertices = i
theorem injected_vertices_length_max : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}), i ≤ A.num_vertices ∧
 injected_vertices A B i.List.length =
  injected_vertices A B A.num_vertices.List.length → A.num_vertices = i := by
  -- === TCCs for injected_vertices_length_max ===
  -- TCC: injected_vertices_length_max_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, i: nat):
  --   i <= A`num_vertices IMPLIES A`num_vertices <= A`num_vertices
  have injected_vertices_length_max_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertices_distinct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, A, B: simple_polygon_2d, S: finite_set[point_2d],
--         Q: finseq[point_2d], k: nat):
--   k < A`num_vertices AND
--    e = edges_of_polygon(A)(k) AND
--     subset?(S, injected_edge(e
theorem injected_vertices_distinct : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (S : ℝ × ℝ → Bool) (Q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k < A.num_vertices ∧
 e = edges_of_polygon A(k) ∧
  subset_p S (injected_edge e B) ∧ Q = injected_edge_seq e S → ∀ (i : _) (j : Finset.range Q.List.length), Q.seq i = Q.seq j → i = j := by
  -- === TCCs for injected_vertices_distinct ===
  -- TCC: injected_vertices_distinct_TCC1
  -- Statement: FORALL (e: segment_2d, A, B: simple_polygon_2d, S: finite_set[point_2d],
  --         k: nat):
  --   k < A`num_vertices AND
  --    e = edges_of_polygon(A)(k) AND subset?(S, injected_edge(e, B))
  --    IMPLIES FORAL...
  have injected_vertices_distinct_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.member_injected_index_exists
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, e: segment_2d, A, B: simple_polygon_2d,
--         S: finite_set[point_2d], Q: finseq[point_2d], i, k: nat):
--        (k < A`num_vertices) AND (e = edges_of_polygon(A)(k))
--    AND (S = 
theorem member_injected_index_exists : ∀ (v : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (S : ℝ × ℝ → Bool) (Q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), (k < A.num_vertices) ∧ (e = edges_of_polygon A(k))
 ∧ (S = injected_edge e B) ∧ member v S
 ∧ (Q = injected_edge_seq e S) → find_seq_index v Q Q.List.length < Q.List.length := by
  -- === TCCs for member_injected_index_exists ===
  -- TCC: member_injected_index_exists_TCC1
  -- Statement: FORALL (v: point_2d, e: segment_2d, A, B: simple_polygon_2d,
  --         S: finite_set[point_2d], k: nat):
  --   k < A`num_vertices AND
  --    e = edges_of_polygon(A)(k) AND S = injected_edge(e, B) AND member(...
  have member_injected_index_exists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: member_injected_index_exists_TCC2
  -- Statement: FORALL (v: point_2d, e: segment_2d, A, B: simple_polygon_2d,
  --         S: finite_set[point_2d], Q: finseq[point_2d], k: nat):
  --        (k < A`num_vertices) AND (e = edges_of_polygon(A)(k))
  --    AND (S = ...
  have member_injected_index_exists_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.member_injected_edge_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, e: segment_2d, A, B: simple_polygon_2d,
--         S: finite_set[point_2d], Q: finseq[point_2d], j: nat):
--   j < A`num_vertices AND
--    e = edges_of_polygon(A)(j) AND
--     subset?(S, in
theorem member_injected_edge_index : ∀ (v : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (S : ℝ × ℝ → Bool) (Q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (j : {i : ℝ // i ≥ 0}), j < A.num_vertices ∧
 e = edges_of_polygon A(j) ∧
  subset_p S (injected_edge e B) ∧ Q = injected_edge_seq e S → (member v S ↔ ∃ (i : ℕ), i < Q.List.length ∧ v = Q.seq i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_edge_seq_starts_with_p1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, A, B: simple_polygon_2d, j: nat):
--   j < A`num_vertices AND e = edges_of_polygon(A)(j) IMPLIES
--    injected_edge_seq(e, injected_edge(e, B))`seq(0) = e`p1
theorem injected_edge_seq_starts_with_p1 : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {i : ℝ // i ≥ 0}), j < A.num_vertices ∧ e = edges_of_polygon A(j) → injected_edge_seq e (injected_edge e B).seq 0 = e.p1 := by
  -- === TCCs for injected_edge_seq_starts_with_p1 ===
  -- TCC: injected_edge_seq_starts_with_p1_TCC1
  -- Statement: FORALL (e: segment_2d, A, B: simple_polygon_2d, j: nat):
  --   j < A`num_vertices AND e = edges_of_polygon(A)(j) IMPLIES
  --    0 < injected_edge_seq(e, injected_edge(e, B))`length
  have injected_edge_seq_starts_with_p1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertex_p1_is_seq_element
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j, k: nat):
--   j < k AND k <= A`num_vertices IMPLIES
--    edges_of_polygon(A)(j)`p1 =
--     injected_vertices(A, B, k)`seq(injected_vertices(A, B, j)`length)
theorem injected_vertex_p1_is_seq_element : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), j < k ∧ k ≤ A.num_vertices → edges_of_polygon A(j).p1 =
 injected_vertices A B k.seq (injected_vertices A B j.List.length) := by
  -- === TCCs for injected_vertex_p1_is_seq_element ===
  -- TCC: injected_vertex_p1_is_seq_element_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, j, k: nat):
  --   j < k AND k <= A`num_vertices IMPLIES j < A`num_vertices
  have injected_vertex_p1_is_seq_element_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertex_p1_is_seq_element_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, j, k: nat):
  --   j < k AND k <= A`num_vertices IMPLIES
  --    injected_vertices(A, B, j)`length < injected_vertices(A, B, k)`length
  have injected_vertex_p1_is_seq_element_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertex_in_injected_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, A, B: simple_polygon_2d, k: nat):
--   k <= A`num_vertices IMPLIES
--    ((EXISTS (i: nat):
--        i < injected_vertices(A, B, k)`length AND
--         injected_vertices(A, B, k)`seq(i) = 
theorem injected_vertex_in_injected_edge : ∀ (v : ℝ × ℝ) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices → ((∃ (i : ℕ), i < injected_vertices A B k.List.length ∧
     injected_vertices A B k.seq i = v)
  ↔
  (∃ (j : ℕ), j < k ∧ member v (injected_edge (edges_of_polygon A(j)) B))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertex_in_injected_edge_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, A, B: simple_polygon_2d, i, k: nat):
--   k <= A`num_vertices AND
--    i < injected_vertices(A, B, k)`length AND
--     injected_vertices(A, B, k)`seq(i) = v
--    IMPLIES
--    EXISTS (j: nat)
theorem injected_vertex_in_injected_edge_2 : ∀ (v : ℝ × ℝ) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices ∧
 i < injected_vertices A B k.List.length ∧
  injected_vertices A B k.seq i = v → ∃ (j : ℕ), j < k ∧ member v (injected_edge (edges_of_polygon A(j)) B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.edge_seq_point_distinct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, e: segment_2d, IV: finseq[point_2d],
--         i, j, k: nat):
--        (k < A`num_vertices) AND (IV = injected_vertices(A, B, k))
--    AND (e = edges_of_polygon(A)(k)) AND (
theorem edge_seq_point_distinct : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (IV : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), (k < A.num_vertices) ∧ (IV = injected_vertices A B k)
 ∧ (e = edges_of_polygon A(k)) ∧ (i < IV.List.length) ∧ (j ≥ IV.List.length)
 ∧ (j < injected_edge_seq e (injected_edge e B).List.length + IV.List.length) → IV.seq i ≠ injected_edge_seq e (injected_edge e B).seq (j - IV.List.length) := by
  -- === TCCs for edge_seq_point_distinct ===
  -- TCC: edge_seq_point_distinct_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, e: segment_2d, IV: finseq[point_2d],
  --         i, j, k: nat):
  --        (k < A`num_vertices) AND (IV = injected_vertices(A, B, k))
  --    AND (e = edges_of_polygon(A)(k)) AN...
  have edge_seq_point_distinct_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.all_injected_vertices_distinct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, IV: finseq[point_2d], k: nat):
--   k <= A`num_vertices AND IV = injected_vertices(A, B, k) IMPLIES
--    FORALL (i, j: below(IV`length)): IV`seq(i) = IV`seq(j) IMPLIES i = 
theorem all_injected_vertices_distinct : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (IV : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), k ≤ A.num_vertices ∧ IV = injected_vertices A B k → ∀ (i : _) (j : Finset.range IV.List.length), IV.seq i = IV.seq j → i = j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertex_on_original_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat):
--   i >= injected_edge_indices(A, B)`seq(j) AND
--    i < injected_edge_indices(A, B)`seq(j + 1)
--    IMPLIES
--    point_on_segment?(injected
theorem injected_vertex_on_original_edge : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (i : {i : ℝ // i ≥ 0}), i ≥ injected_edge_indices A B.seq j ∧
 i < injected_edge_indices A B.seq (j + 1) → point_on_segment_p (injected_vertices A B (j + 1).seq i) (edges_of_polygon A(j)) := by
  -- === TCCs for injected_vertex_on_original_edge ===
  -- TCC: injected_vertex_on_original_edge_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices)):
  --   j < injected_edge_indices(A, B)`length
  have injected_vertex_on_original_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertex_on_original_edge_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat):
  --   i >= injected_edge_indices(A, B)`seq(j) IMPLIES
  --    j + 1 < injected_edge_indices(A, B)`length
  have injected_vertex_on_original_edge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertex_on_original_edge_TCC3
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat):
  --   i >= injected_edge_indices(A, B)`seq(j) AND
  --    i < injected_edge_indices(A, B)`seq(j + 1)
  --    IMPLIES i < injected_vertices(A, B...
  have injected_vertex_on_original_edge_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertex_on_original_edge_TCC4
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat):
  --   i >= injected_edge_indices(A, B)`seq(j) AND
  --    i < injected_edge_indices(A, B)`seq(j + 1)
  --    IMPLIES j + 1 <= A`num_vertices
  have injected_vertex_on_original_edge_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_edge_subsegment_of_original
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_edge_subsegment_of_original : True := by
  -- === TCCs for injected_edge_subsegment_of_original ===
  -- TCC: injected_edge_subsegment_of_original_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, s: segment_2d):
  --   A`num_vertices <= A`num_vertices
  have injected_edge_subsegment_of_original_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_edge_subsegment_of_original_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, s: segment_2d, IV: finseq[point_2d],
  --         Am:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --   IV = injected_vertices...
  have injected_edge_subsegment_of_original_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.point_on_injected_on_original
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_on_injected_on_original : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.injected_p2_in_injected_edge_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_p2_in_injected_edge_set : True := by
  -- === TCCs for injected_p2_in_injected_edge_set ===
  -- TCC: injected_p2_in_injected_edge_set_TCC1
  -- Statement: FORALL (A: simple_polygon_2d): A`num_vertices <= A`num_vertices
  have injected_p2_in_injected_edge_set_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_p2_in_injected_edge_set_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, i: nat, IV: finseq[point_2d], next: nat):
  --   IV = injected_vertices(A, B, A`num_vertices) AND
  --    next = IF i = IV`length - 1 THEN 0 ELSE i + 1 ENDIF AND i < IV`lengt...
  have injected_p2_in_injected_edge_set_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.point_on_injected_edge_on_original
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_on_injected_edge_on_original : True := by
  -- === TCCs for point_on_injected_edge_on_original ===
  -- TCC: point_on_injected_edge_on_original_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, i: nat, IV: finseq[point_2d],
  --         C:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --   IV = injected_vertices(A, B, A...
  have point_on_injected_edge_on_original_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_injected_edge_on_original_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, i: nat, IV: finseq[point_2d],
  --         C:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --   IV = injected_vertices(A, B, A...
  have point_on_injected_edge_on_original_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.point_in_injected_edge_seq_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, Q: finseq[point_2d], e: segment_2d,
--         v: point_2d, j: below(A`num_vertices), i, l: nat):
--        (j < A`num_vertices) AND (e = edges_of_polygon(A)(j))
--    AND (i <
theorem point_in_injected_edge_seq_index : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (i : {i : ℝ // i ≥ 0}) (l : {i : ℝ // i ≥ 0}), (j < A.num_vertices) ∧ (e = edges_of_polygon A(j))
 ∧ (i < injected_vertices A B (j + 1).List.length)
 ∧ (l = injected_vertices A B j.List.length)
 ∧ (v = injected_vertices A B A.num_vertices.seq i)
 ∧ point_on_segment_p v e ∧ (v ≠ e.p2)
 ∧ (Q = injected_edge_seq e (injected_edge e B)) → ∃ (m : Finset.range Q.List.length), v = Q.seq m ∧ i = l + m := by
  -- === TCCs for point_in_injected_edge_seq_index ===
  -- TCC: point_in_injected_edge_seq_index_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, e: segment_2d, j: below(A`num_vertices)):
  --   j < A`num_vertices AND e = edges_of_polygon(A)(j) IMPLIES
  --    j + 1 <= A`num_vertices
  have point_in_injected_edge_seq_index_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_in_injected_edge_seq_index_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, e: segment_2d, j: below(A`num_vertices),
  --         i: nat):
  --   j < A`num_vertices AND
  --    e = edges_of_polygon(A)(j) AND i < injected_vertices(A, B, j + 1)`length
  --    IM...
  have point_in_injected_edge_seq_index_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_in_injected_edge_seq_index_TCC3
  -- Statement: FORALL (A, B: simple_polygon_2d, e: segment_2d, j: below(A`num_vertices),
  --         i, l: nat):
  --   j < A`num_vertices AND
  --    e = edges_of_polygon(A)(j) AND
  --     i < injected_vertices(A, B, j + 1)`lengt...
  have point_in_injected_edge_seq_index_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertex_on_edge_index_bounds
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i, k: nat,
--         v: point_2d):
--        (k <= A`num_vertices) AND (j < k)
--    AND (i < injected_vertices(A, B, k)`length)
--    AND (v /= edges_o
theorem injected_vertex_on_edge_index_bounds : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (i : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}) (v : ℝ × ℝ), (k ≤ A.num_vertices) ∧ (j < k)
 ∧ (i < injected_vertices A B k.List.length)
 ∧ (v ≠ edges_of_polygon A(j).p2)
 ∧ (v = injected_vertices A B A.num_vertices.seq i)
 ∧ point_on_segment_p v (edges_of_polygon A(j)) → i ≥ injected_vertices A B j.List.length ∧
 i < injected_vertices A B (j + 1).List.length := by
  -- === TCCs for injected_vertex_on_edge_index_bounds ===
  -- TCC: injected_vertex_on_edge_index_bounds_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i, k: nat,
  --         v: point_2d):
  --        (k <= A`num_vertices) AND (j < k)
  --    AND (i < injected_vertices(A, B, k)`length)
  --    AND (v /= edge...
  have injected_vertex_on_edge_index_bounds_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertex_on_edge_index_bounds_all
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat,
--         v: point_2d):
--   j < A`num_vertices AND
--    i < injected_vertices(A, B, A`num_vertices)`length AND
--     v = injected_vertices(A,
theorem injected_vertex_on_edge_index_bounds_all : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (i : {i : ℝ // i ≥ 0}) (v : ℝ × ℝ), j < A.num_vertices ∧
 i < injected_vertices A B A.num_vertices.List.length ∧
  v = injected_vertices A B A.num_vertices.seq i ∧
   point_on_segment_p v (edges_of_polygon A(j)) → if v ≠ edges_of_polygon A(j).p2
  then i ≥ injected_vertices A B j.List.length ∧
        i < injected_vertices A B (j + 1).List.length
ELSIF j = A.num_vertices - 1 then i = 0
else i = injected_vertices A B (j + 1).List.length := by
  -- === TCCs for injected_vertex_on_edge_index_bounds_all ===
  -- TCC: injected_vertex_on_edge_index_bounds_all_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat,
  --         v: point_2d):
  --        (j < A`num_vertices)
  --    AND (i < injected_vertices(A, B, A`num_vertices)`length)
  --    AND (v = injected...
  have injected_vertex_on_edge_index_bounds_all_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertex_on_edge_index_bounds_all_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat,
  --         v: point_2d):
  --        (j < A`num_vertices)
  --    AND (i < injected_vertices(A, B, A`num_vertices)`length)
  --    AND (v = injected...
  have injected_vertex_on_edge_index_bounds_all_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertex_on_edge_index_bounds_all_TCC3
  -- Statement: FORALL (A, B: simple_polygon_2d, j: below(A`num_vertices), i: nat,
  --         v: point_2d):
  --        (j < A`num_vertices)
  --    AND (i < injected_vertices(A, B, A`num_vertices)`length)
  --    AND (v = injected...
  have injected_vertex_on_edge_index_bounds_all_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.injected_vertices_length_rollover
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_vertices_length_rollover : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.injected_p2_on_same_original_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, IV: finseq[point_2d], i, j, next: nat):
--        (j < A`num_vertices)
--    AND (IV = injected_vertices(A, B, A`num_vertices)) AND (i < IV`length)
--    AND (next = IF i = IV`
theorem injected_p2_on_same_original_edge : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (IV : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}) (next : {i : ℝ // i ≥ 0}), (j < A.num_vertices) ∧ (IV = injected_vertices A B A.num_vertices)
 ∧ (i < IV.List.length)
 ∧ (next = if i = IV.List.length - 1 then 0 else i + 1 )
 ∧ point_on_segment_p (IV.seq i) (edges_of_polygon A(j))
 ∧ (IV.seq i ≠ edges_of_polygon A(j).p2) → point_on_segment_p (IV.seq next) (edges_of_polygon A(j)) := by
  -- === TCCs for injected_p2_on_same_original_edge ===
  -- TCC: injected_p2_on_same_original_edge_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, IV: finseq[point_2d], i, j, next: nat):
  --        (j < A`num_vertices)
  --    AND (IV = injected_vertices(A, B, A`num_vertices)) AND (i < IV`length)
  --    AND (next = IF i = ...
  have injected_p2_on_same_original_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_injection.next_injected_vertex_on_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem next_injected_vertex_on_edge : True := by
  -- === TCCs for next_injected_vertex_on_edge ===
  -- TCC: next_injected_vertex_on_edge_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: nat, IV: finseq[point_2d],
  --         Am:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --        (IV = injected_vertic...
  have next_injected_vertex_on_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_injected_vertex_on_edge_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: nat, IV: finseq[point_2d],
  --         Am:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --        (IV = injected_vertic...
  have next_injected_vertex_on_edge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_injected_vertex_on_edge_TCC3
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: nat, IV: finseq[point_2d],
  --         Am:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --        (IV = injected_vertic...
  have next_injected_vertex_on_edge_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_injected_vertex_on_edge_TCC4
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: nat, IV: finseq[point_2d],
  --         Am:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --        (IV = injected_vertic...
  have next_injected_vertex_on_edge_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.normalized_edge_endpoint_diffs_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem normalized_edge_endpoint_diffs_eq : True := by
  -- === TCCs for normalized_edge_endpoint_diffs_eq ===
  -- TCC: normalized_edge_endpoint_diffs_eq_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: nat, IV: finseq[point_2d],
  --         Am:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --        (IV = injected_vertic...
  have normalized_edge_endpoint_diffs_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: normalized_edge_endpoint_diffs_eq_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: nat, IV: finseq[point_2d],
  --         Am:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] #]):
  --        (IV = injected_vertic...
  have normalized_edge_endpoint_diffs_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.injected_edge_p1_neq_original_p2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_edge_p1_neq_original_p2 : True := by
  -- === TCCs for injected_edge_p1_neq_original_p2 ===
  -- TCC: injected_edge_p1_neq_original_p2_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, Am: polygon_2d, IV: finseq[point_2d],
  --         i: below(A`num_vertices), j: nat, s, e: segment_2d):
  --   IV = injected_vertices(A, B, A`num_vertices) AND
  --    Am = (# num...
  have injected_edge_p1_neq_original_p2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.injected_edge_has_same_dir_as_original
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_edge_has_same_dir_as_original : True := by
  -- === TCCs for injected_edge_has_same_dir_as_original ===
  -- TCC: injected_edge_has_same_dir_as_original_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, Am: polygon_2d, IV: finseq[point_2d],
  --         i: below(A`num_vertices), j: nat, s, e: segment_2d):
  --        (IV = injected_vertices(A, B, A`num_vertices))
  --    AND (Am ...
  have injected_edge_has_same_dir_as_original_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_edge_has_same_dir_as_original_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, Am: polygon_2d, IV: finseq[point_2d],
  --         i: below(A`num_vertices), j: nat, s, e: segment_2d):
  --        (IV = injected_vertices(A, B, A`num_vertices))
  --    AND (Am ...
  have injected_edge_has_same_dir_as_original_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.injected_edge_p2_neq_original_p1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_edge_p2_neq_original_p1 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.nonlast_injected_vertex_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem nonlast_injected_vertex_index : True := by
  -- === TCCs for nonlast_injected_vertex_index ===
  -- TCC: nonlast_injected_vertex_index_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, Am: polygon_2d, IV: finseq[point_2d],
  --         i: below(A`num_vertices), j: nat, s, e: segment_2d):
  --        (IV = injected_vertices(A, B, A`num_vertices))
  --    AND (Am ...
  have nonlast_injected_vertex_index_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nonlast_injected_vertex_index_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, Am: polygon_2d, IV: finseq[point_2d],
  --         i: below(A`num_vertices), j: nat, s, e: segment_2d):
  --        (IV = injected_vertices(A, B, A`num_vertices))
  --    AND (Am ...
  have nonlast_injected_vertex_index_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.injected_edge_endpoint_or_interior
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_edge_endpoint_or_interior : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.overlapping_injected_edges_are_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem overlapping_injected_edges_are_equal : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.same_injected_edge_imp_no_crossings
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem same_injected_edge_imp_no_crossings : True := by
  -- === TCCs for same_injected_edge_imp_no_crossings ===
  -- TCC: same_injected_edge_imp_no_crossings_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: nat, k: below(A`num_vertices),
  --         IV: finseq[point_2d],
  --         C:
  --           [# num_vertices: nat,
  --              vertices: [below[IV`length] -> point_2d] ...
  have same_injected_edge_imp_no_crossings_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.injected_vertex_edges_do_not_cross
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_vertex_edges_do_not_cross : True := by
  -- === TCCs for injected_vertex_edges_do_not_cross ===
  -- TCC: injected_vertex_edges_do_not_cross_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, IV: finseq[point_2d]):
  --   IV = injected_vertices(A, B, A`num_vertices) IMPLIES IV`length >= 3
  have injected_vertex_edges_do_not_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: injected_vertex_edges_do_not_cross_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, IV: finseq[point_2d]):
  --   IV = injected_vertices(A, B, A`num_vertices) IMPLIES
  --    uniq_vertex_list?(IV`length)(IV`seq)
  have injected_vertex_edges_do_not_cross_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_injection.injected_vertices_form_simple_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem injected_vertices_form_simple_polygon : True := by
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.vertex_injection