-- Auto-generated by pvs2lean/translator.py
-- PVS theory: merge_well_formed
-- PVSLib library: polygon_merge
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: merge_well_formed

Auto-generated stubs from PVSLib theory `merge_well_formed`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.merge_well_formed

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (are_segments_overlapping_p : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (below : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (between_rays_p : pvs_τ5 → pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ10 : Type*} (ccw_vertex_order_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (connector_segment : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (edge_of_polygon_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (edges_of_polygon : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (find_index : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (find_index_any : pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (first_vertex_merge : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (inject_vertices_into_polygon : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (leftward_nearby_points_inside_p : pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (merge_pre_condition : pvs_τ34 → pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (merge_prefix : pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (merge_seq : pvs_τ41 → pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (next_index : pvs_τ43 → pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (next_merge_vertex : pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (on_perimeter_vertex_path_p : pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} (only_vertex_intersections_p : pvs_τ53 → pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} (outward_edge_offset_dist : pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} (point_AB_vtx_p : pvs_τ61 → pvs_τ62 → pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} (point_between_rays_p : pvs_τ64 → pvs_τ65 → pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (point_in_polygon_p : pvs_τ67 → pvs_τ68 → Prop)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} {pvs_τ72 : Type*} (point_in_vertex_wedge_p : pvs_τ69 → pvs_τ70 → pvs_τ71 → pvs_τ72 → Prop)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} (point_on_arb_ray_p : pvs_τ73 → pvs_τ74 → pvs_τ75 → Prop)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} (point_on_polygon_perimeter_p : pvs_τ76 → pvs_τ77 → Prop)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} (point_on_segment_p : pvs_τ78 → pvs_τ79 → Prop)
variable {pvs_τ80 : Type*} {pvs_τ81 : Type*} (point_outside_polygon_p : pvs_τ80 → pvs_τ81 → Prop)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} (polygon_contained_p : pvs_τ82 → pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} (prev_0 : pvs_τ84 → pvs_τ85)
variable {pvs_τ86 : Type*} {pvs_τ87 : Type*} {pvs_τ88 : Type*} (prev_index : pvs_τ86 → pvs_τ87 → pvs_τ88)
variable {pvs_τ89 : Type*} {pvs_τ90 : Type*} (rem : pvs_τ89 → pvs_τ90 → Prop)
variable {pvs_τ91 : Type*} {pvs_τ92 : Type*} (reverse_segment : pvs_τ91 → pvs_τ92)
variable {pvs_τ93 : Type*} {pvs_τ94 : Type*} (rightward_nearby_points_outside_p : pvs_τ93 → pvs_τ94 → Prop)
variable {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} (rightward_segment_offset : pvs_τ95 → pvs_τ96 → pvs_τ97)
variable {pvs_τ98 : Type*} {pvs_τ99 : Type*} (segment_endpoint_p : pvs_τ98 → pvs_τ99 → Prop)
variable {pvs_τ104 : Type*} (segments_do_not_cross_p : pvs_τ104 → Prop)
variable {pvs_τ105 : Type*} (seq : pvs_τ105 → Prop)
variable {pvs_τ106 : Type*} {pvs_τ107 : Type*} {pvs_τ108 : Type*} (seq_only_vertex_intersections_p : pvs_τ106 → pvs_τ107 → pvs_τ108 → Prop)
variable {pvs_τ109 : Type*} {pvs_τ110 : Type*} (topleft_vertex_idx : pvs_τ109 → pvs_τ110)
variable {pvs_τ111 : Type*} {pvs_τ112 : Type*} (uniq_vertex_list_p : pvs_τ111 → pvs_τ112 → Prop)
variable {pvs_τ113 : Type*} {pvs_τ114 : Type*} (upfrom : pvs_τ113 → pvs_τ114)
variable {pvs_τ115 : Type*} {pvs_τ116 : Type*} (upto : pvs_τ115 → pvs_τ116)
variable {pvs_τ117 : Type*} {pvs_τ118 : Type*} (vertex_p : pvs_τ117 → pvs_τ118 → Prop)
variable {pvs_τ119 : Type*} (vertices : pvs_τ119 → Prop)

-- pvs2lean: merge_well_formed.ccw_merge_pre_condition
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: merge_pre_condition(A)(B)
axiom ccw_merge_pre_condition {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9

-- pvs2lean: merge_well_formed.segment_enters_polygon_alt?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (pt: point_2d): point_on_segment?(pt, s) AND point_in_polygon?(pt, G)
axiom segment_enters_polygon_alt_p {pvs_τ100 : Type*} {pvs_τ101 : Type*} : pvs_τ100 → pvs_τ101 → Prop

-- pvs2lean: merge_well_formed.segment_exits_polygon?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (pt: point_2d):
--   point_on_segment?(pt, s) AND point_outside_polygon?(pt, G)
axiom segment_exits_polygon_p {pvs_τ102 : Type*} {pvs_τ103 : Type*} : pvs_τ102 → pvs_τ103 → Prop

-- pvs2lean: merge_well_formed.segment_does_not_enter_own_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d):
--   FORALL (e: (edge_of_polygon?(G))): NOT segment_enters_polygon_alt?(G, e)
theorem segment_does_not_enter_own_polygon : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬(segment_enters_polygon_alt_p G e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.rightward_segment_offset_on_connector
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices)):
--   LET e = edges_of_polygon(G)(i), c = outward_edge_offset_dist(G) IN
--     point_on_segment?(rightward_segment_offset(e, c),
--                    
theorem rightward_segment_offset_on_connector : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (c : {x : {x : ℝ // x ≥ 0} // x > 0}) => point_on_segment_p (rightward_segment_offset e c) (connector_segment G i) (outward_edge_offset_dist G) (edges_of_polygon G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.connector_points_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices), r: point_2d):
--   point_on_segment?(r, connector_segment(G, i)) IMPLIES
--    point_outside_polygon?(r, G)
theorem connector_points_outside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (r : ℝ × ℝ), point_on_segment_p r (connector_segment G i) → point_outside_polygon_p r G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.rightward_nearby_ccw_points_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices)):
--   rightward_nearby_points_outside?(G, i)
theorem rightward_nearby_ccw_points_outside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), rightward_nearby_points_outside_p G i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.leftward_nearby_ccw_points_inside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices)):
--   leftward_nearby_points_inside?(G, i)
theorem leftward_nearby_ccw_points_inside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), leftward_nearby_points_inside_p G i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.injected_polygon_has_ccw_order
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND ccw_vertex_order?(A)
--      IMP
theorem injected_polygon_has_ccw_order : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ ccw_vertex_order_p A → ccw_vertex_order_p Am := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.injected_polygons_have_ccw_order
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      ccw_vertex_order?(A) AND
theorem injected_polygons_have_ccw_order : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧
 ccw_vertex_order_p A ∧ ccw_vertex_order_p B → ccw_vertex_order_p Am ∧ ccw_vertex_order_p Bm := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.injected_polygon_has_merge_precondition
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      merge_pre_condition(
theorem injected_polygon_has_merge_precondition : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → merge_pre_condition Am(Bm) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.next_vertex_neq_between_point
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, A, B: simple_polygon_2d, i, j: int):
--        (i = find_index(A, u)) AND (i >= 0) AND (j = find_index(B, u))
--    AND (j >= 0) AND (edges_of_polygon(A)(i)`p1 = edges_of_polygon(B)(
theorem next_vertex_neq_between_point : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : ℝ) (j : ℝ), (i = find_index A u) ∧ (i ≥ 0) ∧ (j = find_index B u)
 ∧ (j ≥ 0) ∧ (edges_of_polygon A(i).p1 = edges_of_polygon B(j).p1)
 ∧ point_between_rays_p (edges_of_polygon A(i)) (edges_of_polygon B(j)) (v) → v ≠ A.vertices (next_index A i) ∧ v ≠ B.vertices (next_index B j) := by
  -- === TCCs for next_vertex_neq_between_point ===
  -- TCC: next_vertex_neq_between_point_TCC1
  -- Statement: FORALL (u: point_2d, A, B: simple_polygon_2d, i, j: int):
  --   i = find_index(A, u) AND i >= 0 AND j = find_index(B, u) AND j >= 0
  --    IMPLIES i < A`num_vertices
  have next_vertex_neq_between_point_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_vertex_neq_between_point_TCC2
  -- Statement: FORALL (u: point_2d, A, B: simple_polygon_2d, i, j: int):
  --   i = find_index(A, u) AND i >= 0 AND j = find_index(B, u) AND j >= 0
  --    IMPLIES j < B`num_vertices
  have next_vertex_neq_between_point_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.incoming_edge_of_C?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET e = edges_of_polygon(A)(i) IN
--   EXISTS (j: below(C`num_vertices), q: point_2d):
--     e`p2 = C`vertices(j) AND
--      q /= e`p2 AND
--       point_on_segment?(q, e) AND point_in_vertex_wedge?(TRUE, C, j)
axiom incoming_edge_of_C_p {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: merge_well_formed.outgoing_edge_of_C?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET e = edges_of_polygon(A)(i) IN
--   EXISTS (j: below(C`num_vertices), q: point_2d):
--     e`p1 = C`vertices(j) AND
--      q /= e`p1 AND
--       point_on_segment?(q, e) AND point_in_vertex_wedge?(TRUE, C, j)
axiom outgoing_edge_of_C_p {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} : pvs_τ56 → pvs_τ57 → pvs_τ58 → Prop

-- pvs2lean: merge_well_formed.int_index
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rem(A`num_vertices)(A`num_vertices + n)
axiom int_index {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31

-- pvs2lean: merge_well_formed.inc_or_outg_edge_invariant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, j: below(A`num_vertices)):
--   (FORALL (e: (edge_of_polygon?(A))): only_vertex_intersections?(C, A, e))
--    AND incoming_edge_of_C?(A, C, j)
--    IMPLIES
--    FORALL (k: bel
theorem inc_or_outg_edge_invariant : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}), (∀ (e : (edge_of_polygon_p A)), only_vertex_intersections_p C A e)
 ∧ incoming_edge_of_C_p A C j → ∀ (k : Finset.range A.num_vertices), (∃ (l : {k : ℕ // k ≤ k}), outgoing_edge_of_C_p A C (int_index A(j - l))) ∨
   (∀ (l : {k : ℕ // k ≤ k}), ¬ outgoing_edge_of_C_p A C (int_index A(j - l)) ∧
       point_outside_polygon_p (A.vertices (int_index A(j - l))) C) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.inc_edge_imp_outg_edge_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, j: below(A`num_vertices)):
--   (FORALL (e: (edge_of_polygon?(A))): only_vertex_intersections?(C, A, e))
--    AND incoming_edge_of_C?(A, C, j)
--    IMPLIES
--    EXISTS (k: bel
theorem inc_edge_imp_outg_edge_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}), (∀ (e : (edge_of_polygon_p A)), only_vertex_intersections_p C A e)
 ∧ incoming_edge_of_C_p A C j → ∃ (k : Finset.range A.num_vertices), outgoing_edge_of_C_p A C (int_index A(j - k)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.incoming_edge_imp_outgoing_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, s: (edge_of_polygon?(A)),
--         i: below(C`num_vertices)):
--   s`p2 = C`vertices(i) AND
--    (EXISTS (p: point_2d):
--       point_on_segment?(p, s) AND
--        p /= s`p2 A
theorem incoming_edge_imp_outgoing_edge : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < C.num_vertices}), s.p2 = C.vertices i ∧
 (∃ (p : point_2d), point_on_segment_p p s ∧
     p ≠ s.p2 ∧ point_in_vertex_wedge_p True C i(p))
  ∧
  (∀ (e : (edge_of_polygon_p A)), only_vertex_intersections_p C A e) → ∃ (e : (edge_of_polygon_p A)) (j : Finset.range C.num_vertices) (q : point_2d), e.p1 = C.vertices j ∧
   point_on_segment_p q e ∧
    q ≠ e.p1 ∧ point_in_vertex_wedge_p True C j(q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.A_intersects_C_imp_outgoing_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, i: below(C`num_vertices), s: segment_2d,
--         p: point_2d):
--        (FORALL (e: (edge_of_polygon?(A))):
--           only_vertex_intersections?(C, A, e))
--    AND vertex
theorem A_intersects_C_imp_outgoing_edge : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < C.num_vertices}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), (∀ (e : (edge_of_polygon_p A)), only_vertex_intersections_p C A e)
 ∧ vertex_p A(p) ∧ point_outside_polygon_p p C
 ∧ edge_of_polygon_p A(s) ∧ (s.p1 = C.vertices i)
 ∧ (are_segments_overlapping_p s(edges_of_polygon C(i)) ∨
       are_segments_overlapping_p s(edges_of_polygon C(prev_index C i))
        ∨
        ∃ (u : point_2d), point_on_segment_p u s ∧
           u ≠ s.p1 ∧ point_in_vertex_wedge_p False C i(u)) → ∃ (e : (edge_of_polygon_p A)) (j : Finset.range C.num_vertices) (q : point_2d), e.p1 = C.vertices j ∧
   q ≠ e.p1 ∧
    point_on_segment_p q e ∧ point_in_vertex_wedge_p True C j(q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.A_intersects_C_imp_outgoing_edge_exists
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon):
--   (FORALL (e: (edge_of_polygon?(A))): only_vertex_intersections?(C, A, e))
--    AND
--    (EXISTS (p, q: (vertex?(A))):
--       point_outside_polygon?(p, C) AND vertex?(C)(
theorem A_intersects_C_imp_outgoing_edge_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (e : (edge_of_polygon_p A)), only_vertex_intersections_p C A e)
 ∧
 (∃ (p : _) (q : (vertex_p A)), point_outside_polygon_p p C ∧ vertex_p C(q)) → ∃ (e : (edge_of_polygon_p A)) (j : Finset.range C.num_vertices) (u : point_2d), e.p1 = C.vertices j ∧
   u ≠ e.p1 ∧
    point_on_segment_p u e ∧ point_in_vertex_wedge_p True C j(u) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.first_merge_vertex_is_topleft
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
--         M: finseq[point_2d]):
--   M = merge_seq(A, B) IMPLIES
--    FORALL (k: below(M`length)):
--      M`seq(0)`x < M`seq(k)`x OR
--       M`seq(
theorem first_merge_vertex_is_topleft : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), M = merge_seq A B → ∀ (k : Finset.range M.List.length), M.seq 0.x < M.seq k.x ∨
   M.seq 0.x = M.seq k.x ∧ M.seq k.y ≤ M.seq 0.y := by
  -- === TCCs for first_merge_vertex_is_topleft ===
  -- TCC: first_merge_vertex_is_topleft_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
  --   merge_pre_condition(A)(B)
  have first_merge_vertex_is_topleft_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_merge_vertex_is_topleft_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d]):
  --   M = merge_seq(A, B) IMPLIES FORALL (k: below(M`length)): 0 < M`length
  have first_merge_vertex_is_topleft_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_seq_topleft_vertices
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   LET v0 = first_vertex_merge(A, B),
--       (Am, Bm) = inject_vertices_into_polygon(A, B)
--     IN
--     (find_index(Am, v0) >= 0 IMPLIES
--       f
theorem merge_seq_topleft_vertices : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (v0 : ℝ × ℝ) => fun (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) => (find_index Am v0 ≥ 0 →
  find_index Am v0 = topleft_vertex_idx Am) ∧ (find_index Bm v0 ≥ 0 →
  find_index Bm v0 = topleft_vertex_idx Bm) (inject_vertices_into_polygon A B) (first_vertex_merge A B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.injected_topleft_vertices_shared
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, v: point_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      merge_s
theorem injected_topleft_vertices_shared : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (v : ℝ × ℝ), (Am, Bm) = inject_vertices_into_polygon A B → merge_seq A B.seq 0 = v ∧
 find_index Am v ≥ 0 ∧ find_index Bm v ≥ 0
 →
 Am.vertices (topleft_vertex_idx Am) = v ∧
  Bm.vertices (topleft_vertex_idx Bm) = v := by
  -- === TCCs for injected_topleft_vertices_shared ===
  -- TCC: injected_topleft_vertices_shared_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
  --    0 < merge_seq(A, B)`length
  have injected_topleft_vertices_shared_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.first_merge_vertex_is_topleft_all
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           M: finseq[point_2d], p: point_2d):
--     (Am, Bm) = inject_vertices_int
theorem first_merge_vertex_is_topleft_all : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (p : ℝ × ℝ), (Am, Bm) = inject_vertices_into_polygon A B ∧
 M = merge_seq A B ∧
  (point_on_polygon_perimeter_p Am(p) ∨ point_on_polygon_perimeter_p Bm(p)) → M.seq 0.x < p.x ∨ M.seq 0.x = p.x ∧ p.y ≤ M.seq 0.y := by
  -- === TCCs for first_merge_vertex_is_topleft_all ===
  -- TCC: first_merge_vertex_is_topleft_all_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, M: finseq[point_2d],
  --         p: point_2d):
  --   (Am, Bm) = inject_vertices_into_po...
  have first_merge_vertex_is_topleft_all_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_seq_length_neq_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   merge_seq(A, B)`length /= 2
theorem merge_seq_length_neq_2 : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), merge_seq A B.List.length ≠ 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_seq_has_min_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   merge_seq(A, B)`length > 2
theorem merge_seq_has_min_length : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), merge_seq A B.List.length > 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.next_merge_vertex_neq_prev_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem next_merge_vertex_neq_prev_vertex : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merge_well_formed.prev_edge_not_on_arb_ray
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem prev_edge_not_on_arb_ray : True := by
  -- === TCCs for prev_edge_not_on_arb_ray ===
  -- TCC: prev_edge_not_on_arb_ray_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         vertex, prev_vtx: (point_AB_vtx?(Am, Bm)), e: segment_2d):
  --        ((Am, Bm) ...
  have prev_edge_not_on_arb_ray_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: prev_edge_not_on_arb_ray_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         vertex, prev_vtx: (point_AB_vtx?(Am, Bm)), e: segment_2d):
  --        ((Am, Bm) ...
  have prev_edge_not_on_arb_ray_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merge_well_formed.next_edge_on_ray_imp_next_vertices_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem next_edge_on_ray_imp_next_vertices_eq : True := by
  -- === TCCs for next_edge_on_ray_imp_next_vertices_eq ===
  -- TCC: next_edge_on_ray_imp_next_vertices_eq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         vertex, next_vtx: (point_AB_vtx?(Am, Bm)), e: segment_2d):
  --   (Am, Bm) = inje...
  have next_edge_on_ray_imp_next_vertices_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_edge_on_ray_imp_next_vertices_eq_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         vertex, next_vtx: (point_AB_vtx?(Am, Bm)), e: segment_2d):
  --        ((Am, Bm) ...
  have next_edge_on_ray_imp_next_vertices_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merge_well_formed.next_merge_vertex_comm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem next_merge_vertex_comm : True := by
  -- === TCCs for next_merge_vertex_comm ===
  -- TCC: next_merge_vertex_comm_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         vertex, prev_vtx: (point_AB_vtx?(Am, Bm))):
  --   (Am, Bm) = inject_vertices_int...
  have next_merge_vertex_comm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merge_well_formed.next_merge_vertex_comm_prev_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      LET first_vtx = firs
theorem next_merge_vertex_comm_prev_0 : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → (let first_vtx := first_vertex_merge A B; next_merge_vertex Am Bm first_vtx (prev_0 first_vtx) =
   next_merge_vertex Bm Am first_vtx (prev_0 first_vtx)) := by
  -- === TCCs for next_merge_vertex_comm_prev_0 ===
  -- TCC: next_merge_vertex_comm_prev_0_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, first_vtx: point_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    first_v...
  have next_merge_vertex_comm_prev_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_merge_vertex_comm_prev_0_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, first_vtx: point_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    first_v...
  have next_merge_vertex_comm_prev_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_prefix_comm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am, Bm: simple_polygon_2d, k: nat):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      LET first_vtx = first_vertex_me
theorem merge_prefix_comm : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : ℝ // i ≥ 0}), (Am, Bm) = inject_vertices_into_polygon A B → (let first_vtx := first_vertex_merge A B; merge_prefix Am Bm first_vtx k = merge_prefix Bm Am first_vtx k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_seq_commutative
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   merge_seq(A, B) = merge_seq(B, A)
theorem merge_seq_commutative : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), merge_seq A B = merge_seq B A := by
  -- === TCCs for merge_seq_commutative ===
  -- TCC: merge_seq_commutative_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
  --   merge_pre_condition(B)(A)
  have merge_seq_commutative_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.A_vertex_imp_B_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, v: point_2d):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND vertex?(A)(v) AND point_on_polygon_perimeter?(B)(v)
--    IMPLIES vertex?(
theorem A_vertex_imp_B_vertex : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (v : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ vertex_p A(v) ∧ point_on_polygon_perimeter_p B(v) → vertex_p B(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.B_vertex_imp_A_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, v: point_2d):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND vertex?(B)(v) AND point_on_polygon_perimeter?(A)(v)
--    IMPLIES vertex?(
theorem B_vertex_imp_A_vertex : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (v : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ vertex_p B(v) ∧ point_on_polygon_perimeter_p A(v) → vertex_p A(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.outside_endpoint_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: ccw_simple_polygon,
--         i: below(B`num_vertices), e: segment_2d):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND
--    e`p1 = B`ver
theorem outside_endpoint_between_rays : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧
 e.p1 = B.vertices i ∧
  point_outside_polygon_p e.p2 B ∧
   (edge_of_polygon_p A(e) ∨ edge_of_polygon_p A(reverse_segment e)) → between_rays_p (edges_of_polygon B(prev_index B i)) (edges_of_polygon B(i))
             (e.p2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.inside_endpoint_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: ccw_simple_polygon,
--         i: below(B`num_vertices), e: segment_2d):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND
--    e`p1 = B`ver
theorem inside_endpoint_between_rays : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧
 e.p1 = B.vertices i ∧
  point_in_polygon_p e.p2 B ∧
   (edge_of_polygon_p A(e) ∨ edge_of_polygon_p A(reverse_segment e)) → point_between_rays_p (edges_of_polygon B(i)) (reverse_segment (edges_of_polygon B(prev_index B i)))
                   (e.p2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.contained_imp_endpoint_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: ccw_simple_polygon, i: below(B`num_vertices), e: segment_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B, A, h))
--    AND polygon_contained?(A, B) AN
theorem contained_imp_endpoint_between_rays : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ polygon_contained_p A B ∧ (e.p1 = B.vertices i)
 ∧ (e.p2 ≠ B.vertices (next_index B i))
 ∧ (e.p2 ≠ B.vertices (prev_index B i))
 ∧ (edge_of_polygon_p A(e) ∨ edge_of_polygon_p A(reverse_segment e)) → point_between_rays_p (edges_of_polygon B(i)) (reverse_segment (edges_of_polygon B(prev_index B i)))
                   (e.p2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.non_vertex_points_outside_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, e: segment_2d, q: point_2d):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND
--    edge_of_polygon?(A)(e) AND
--     (point_outside_polygon
theorem non_vertex_points_outside_polygon : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧
 edge_of_polygon_p A(e) ∧
  (point_outside_polygon_p e.p1 B ∧ q ≠ e.p2 ∨
    point_outside_polygon_p e.p2 B ∧ q ≠ e.p1)
   ∧ point_on_segment_p q e → point_outside_polygon_p q B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.non_vertex_points_inside_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, e: segment_2d, q: point_2d):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND
--    edge_of_polygon?(A)(e) AND
--     (point_in_polygon?(e`p
theorem non_vertex_points_inside_polygon : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧
 edge_of_polygon_p A(e) ∧
  (point_in_polygon_p e.p1 B ∧ q ≠ e.p2 ∨
    point_in_polygon_p e.p2 B ∧ q ≠ e.p1)
   ∧ point_on_segment_p q e → point_in_polygon_p q B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.non_endpoints_outside_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, e: segment_2d, p, q: point_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B, A, h))
--    AND edge_of_polygon?(A)(e) AND point_outsi
theorem non_endpoints_outside_polygon : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ edge_of_polygon_p A(e) ∧ point_outside_polygon_p p B
 ∧ point_on_segment_p p e ∧ point_on_segment_p q e
 ∧ (¬ segment_endpoint_p e(p)) ∧ ¬ segment_endpoint_p e(q) → point_outside_polygon_p q B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.non_endpoints_inside_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, e: segment_2d, p, q: point_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B, A, h))
--    AND edge_of_polygon?(A)(e) AND point_in_po
theorem non_endpoints_inside_polygon : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ edge_of_polygon_p A(e) ∧ point_in_polygon_p p B
 ∧ point_on_segment_p p e ∧ point_on_segment_p q e
 ∧ (¬ segment_endpoint_p e(p)) ∧ ¬ segment_endpoint_p e(q) → point_in_polygon_p q B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.endpoint_outside_or_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, e: segment_2d, p, v: point_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B, A, h))
--    AND edge_of_polygon?(A)(e) AND point_outsi
theorem endpoint_outside_or_on_perimeter : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (v : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ edge_of_polygon_p A(e) ∧ point_outside_polygon_p p B
 ∧ segment_endpoint_p e(v) ∧ point_on_segment_p p e → point_outside_polygon_p v B ∨ vertex_p B(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.endpoint_inside_or_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, e: segment_2d, p, v: point_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B, A, h))
--    AND edge_of_polygon?(A)(e) AND point_in_po
theorem endpoint_inside_or_on_perimeter : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (v : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ edge_of_polygon_p A(e) ∧ point_in_polygon_p p B
 ∧ segment_endpoint_p e(v) ∧ point_on_segment_p p e → point_in_polygon_p v B ∨ vertex_p B(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.edge_endpoint_inside_or_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (B: simple_polygon_2d, e: segment_2d, p, v: point_2d):
--   segment_endpoint?(e)(v) AND
--    (FORALL (q: point_2d), (s: (edge_of_polygon?(B))):
--       point_on_segment?(q, e) AND point_on_segment?(q,
theorem edge_endpoint_inside_or_on_perimeter : ∀ (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (v : ℝ × ℝ), segment_endpoint_p e(v) ∧
 (∀ (q: point_2d), (s: (edge_of_polygon_p B)):
    point_on_segment_p q e ∧ point_on_segment_p q s →
     segment_endpoint_p e(q) ∧ segment_endpoint_p s(q))
  ∧ point_in_polygon_p p B ∧ point_on_segment_p p e → point_in_polygon_p v B ∨ vertex_p B(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.outside_point_on_segment_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: ccw_simple_polygon, p, q: point_2d,
--         i: below(B`num_vertices), e: segment_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B
theorem outside_point_on_segment_between_rays : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ edge_of_polygon_p A(e) ∧ (p ≠ e.p1) ∧ (q ≠ e.p1)
 ∧ (e.p1 = B.vertices i) ∧ point_outside_polygon_p p B
 ∧ point_on_segment_p p e ∧ point_on_segment_p q e → between_rays_p (edges_of_polygon B(prev_index B i)) (edges_of_polygon B(i))
             (q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.inside_point_on_segment_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: ccw_simple_polygon, p, q: point_2d,
--         i: below(B`num_vertices), e: segment_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B
theorem inside_point_on_segment_between_rays : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ edge_of_polygon_p A(e) ∧ (p ≠ e.p1) ∧ (q ≠ e.p1)
 ∧ (e.p1 = B.vertices i) ∧ point_in_polygon_p p B
 ∧ point_on_segment_p p e ∧ point_on_arb_ray_p q e.p1 (^(e.p2 - e.p1)) → between_rays_p (reverse_segment (edges_of_polygon B(i))) (reverse_segment (edges_of_polygon B(prev_index B i)))
             (q) := by
  -- === TCCs for inside_point_on_segment_between_rays ===
  -- TCC: inside_point_on_segment_between_rays_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: ccw_simple_polygon, p, q: point_2d,
  --         i: below(B`num_vertices), e: segment_2d):
  --        (FORALL (h: (edge_of_polygon?(A))):
  --           only_vertex_intersections...
  have inside_point_on_segment_between_rays_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.extend_vertices_outside_polygon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i: below(A`num_vertices), n: nat):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND
--    (FORALL (k: below(n)):
--       NOT vertex?(B)(A`v
theorem extend_vertices_outside_polygon : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (n : {i : ℝ // i ≥ 0}), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧
 (∀ (k : Finset.range n), ¬ vertex_p B(A.vertices (rem A.num_vertices(k + i))))
  ∧ point_outside_polygon_p (A.vertices i) B → ∀ (k : Finset.range n), point_outside_polygon_p (A.vertices (rem A.num_vertices(k + i))) B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.in_out_intersection_vertex_invariant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i, j: below(A`num_vertices)):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND
--    point_outside_polygon?(A`vertices(i), B) AND
--     poi
theorem in_out_intersection_vertex_invariant : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧
 point_outside_polygon_p (A.vertices i) B ∧
  point_in_polygon_p (A.vertices j) B → (let m := A.num_vertices; ∀ (k : Finset.range rem (A.num_vertices (j - i))), (∀ (l : {k : ℕ // k ≤ k}), ¬ point_in_polygon_p (A.vertices (rem m(l + i))) B)
     ∨
     (∃ (l : {k : ℕ // k ≤ k}), 0 < l ∧ vertex_p B(A.vertices (rem m(l + i))))) := by
  -- === TCCs for in_out_intersection_vertex_invariant ===
  -- TCC: in_out_intersection_vertex_invariant_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: below(A`num_vertices)):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND
  --    point_outside_polygon?(A`vertices(i), B) AND
  --     ...
  have in_out_intersection_vertex_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: in_out_intersection_vertex_invariant_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, i, j: below(A`num_vertices)):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND
  --    point_outside_polygon?(A`vertices(i), B) AND
  --     ...
  have in_out_intersection_vertex_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.in_out_intersection_vertex_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i, j: below(A`num_vertices)):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND
--    point_outside_polygon?(A`vertices(i), B) AND
--     poi
theorem in_out_intersection_vertex_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧
 point_outside_polygon_p (A.vertices i) B ∧
  point_in_polygon_p (A.vertices j) B → ∃ (k : Finset.range rem (A.num_vertices (j - i))), 0 < k ∧ vertex_p B(A.vertices (rem A.num_vertices(k + i))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.seq_in_out_intersection_vertex_invariant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S:
--           {S: finseq[point_2d] |
--              FORALL (i: below(S`length - 1)): S`seq(i) /= S`seq(i + 1)},
--         B: simple_polygon_2d, j: below(S`length - 1), i: below(j - 1)):
--   (FORALL (
theorem seq_in_out_intersection_vertex_invariant : ∀ (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 1}) (i : {s : {i : ℝ // i ≥ 0} // s < j - 1}), (∀ (h : (edge_of_polygon_p B)), seq_only_vertex_intersections_p S B h)
 ∧ point_outside_polygon_p (S.seq i) B ∧ point_in_polygon_p (S.seq j) B → ∀ (k : {k : ℕ // k ≤ j - i}), (∀ (l : {k : ℕ // k ≤ k}), ¬ point_in_polygon_p (S.seq (l + i)) B) ∨
   (∃ (l : {k : ℕ // k ≤ k}), 0 < l ∧ vertex_p B(S.seq (l + i))) := by
  -- === TCCs for seq_in_out_intersection_vertex_invariant ===
  -- TCC: seq_in_out_intersection_vertex_invariant_TCC1
  -- Statement: FORALL (S: finseq[point_2d], i: below(S`length - 1)): i < S`length
  have seq_in_out_intersection_vertex_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: seq_in_out_intersection_vertex_invariant_TCC2
  -- Statement: FORALL (S: finseq[point_2d], i: below(S`length - 1)): i + 1 < S`length
  have seq_in_out_intersection_vertex_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: seq_in_out_intersection_vertex_invariant_TCC3
  -- Statement: FORALL (S:
  --           {S: finseq[point_2d] |
  --              FORALL (i: below(S`length - 1)): S`seq(i) /= S`seq(i + 1)},
  --         B: simple_polygon_2d, j: below(S`length - 1), i: below(j - 1)):
  --   (FORAL...
  have seq_in_out_intersection_vertex_invariant_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: seq_in_out_intersection_vertex_invariant_TCC4
  -- Statement: FORALL (S:
  --           {S: finseq[point_2d] |
  --              FORALL (i: below(S`length - 1)): S`seq(i) /= S`seq(i + 1)},
  --         B: simple_polygon_2d, j: below(S`length - 1), i: below(j - 1)):
  --   (FORAL...
  have seq_in_out_intersection_vertex_invariant_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: seq_in_out_intersection_vertex_invariant_TCC5
  -- Statement: FORALL (S:
  --           {S: finseq[point_2d] |
  --              FORALL (i: below(S`length - 1)): S`seq(i) /= S`seq(i + 1)},
  --         B: simple_polygon_2d, j: below(S`length - 1), i: below(j - 1)):
  --   (FORAL...
  have seq_in_out_intersection_vertex_invariant_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.seq_in_out_intersection_vertex_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S:
--           {S: finseq[point_2d] |
--              FORALL (i: below(S`length - 1)): S`seq(i) /= S`seq(i + 1)},
--         B: simple_polygon_2d, j: below(S`length - 1), i: below(j - 1)):
--   (FORALL (
theorem seq_in_out_intersection_vertex_exists : ∀ (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 1}) (i : {s : {i : ℝ // i ≥ 0} // s < j - 1}), (∀ (h : (edge_of_polygon_p B)), seq_only_vertex_intersections_p S B h)
 ∧ point_outside_polygon_p (S.seq i) B ∧ point_in_polygon_p (S.seq j) B → ∃ (k : Finset.range j - i), 0 < k ∧ vertex_p B(S.seq (k + i)) := by
  -- === TCCs for seq_in_out_intersection_vertex_exists ===
  -- TCC: seq_in_out_intersection_vertex_exists_TCC1
  -- Statement: FORALL (S:
  --           {S: finseq[point_2d] |
  --              FORALL (i: below(S`length - 1)): S`seq(i) /= S`seq(i + 1)},
  --         B: simple_polygon_2d, j: below(S`length - 1), i: below(j - 1)):
  --   (FORAL...
  have seq_in_out_intersection_vertex_exists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.path_to_enter_B_invariant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(p, B) AND point_in_po
theorem path_to_enter_B_invariant : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p p B ∧ point_in_polygon_p q B → (let i := next_index A (find_index_any A p); let m := A.num_vertices; ∀ (k : Finset.range A.num_vertices), (∀ (l : {k : ℕ // k ≤ k}), ¬ point_in_polygon_p (A.vertices (rem m(l + i))) B)
     ∨
     (∃ (l : {k : ℕ // k ≤ k}), vertex_p B(A.vertices (rem m(l + i))) ∧
         segment_enters_polygon_alt_p B (edges_of_polygon A(rem m(l + i))))) := by
  -- === TCCs for path_to_enter_B_invariant ===
  -- TCC: path_to_enter_B_invariant_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(p, B) AND point_in...
  have path_to_enter_b_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_to_enter_B_invariant_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(p, B) AND point_in...
  have path_to_enter_b_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_to_enter_B_invariant_TCC3
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(p, B) AND point_in...
  have path_to_enter_b_invariant_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.path_to_enter_B_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(p, B) AND point_in_po
theorem path_to_enter_B_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p p B ∧ point_in_polygon_p q B → (let i := next_index A (find_index_any A p); let m := A.num_vertices; ∃ (k : Finset.range A.num_vertices), vertex_p B(A.vertices (rem m(k + i))) ∧
     segment_enters_polygon_alt_p B (edges_of_polygon A(rem m(k + i)))) := by
  -- === TCCs for path_to_enter_B_exists ===
  -- TCC: path_to_enter_B_exists_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(p, B) AND point_in...
  have path_to_enter_b_exists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.first_path_to_enter_B_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(p, B) AND point_in_po
theorem first_path_to_enter_B_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p p B ∧ point_in_polygon_p q B → (let i := next_index A (find_index_any A p); let m := A.num_vertices; ∃ (k : Finset.range A.num_vertices), (∀ (l : Finset.range k), ¬ point_in_polygon_p (A.vertices (rem m(l + i))) B)
     ∧
     vertex_p B(A.vertices (rem m(k + i))) ∧
      segment_enters_polygon_alt_p B (edges_of_polygon A(rem m(k + i)))) := by
  -- === TCCs for first_path_to_enter_B_exists ===
  -- TCC: first_path_to_enter_B_exists_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(p, B) AND point_in...
  have first_path_to_enter_b_exists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.path_to_exit_B_invariant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(q, B) AND point_in_po
theorem path_to_exit_B_invariant : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p q B ∧ point_in_polygon_p p B → (let i := next_index A (find_index_any A p); let m := A.num_vertices; ∀ (k : Finset.range A.num_vertices), (∀ (l : {k : ℕ // k ≤ k}), ¬ point_outside_polygon_p (A.vertices (rem m(l + i))) B)
     ∨
     (∃ (l : {k : ℕ // k ≤ k}), vertex_p B(A.vertices (rem m(l + i))) ∧
         segment_exits_polygon_p B (edges_of_polygon A(rem m(l + i))))) := by
  -- === TCCs for path_to_exit_B_invariant ===
  -- TCC: path_to_exit_B_invariant_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND point_in...
  have path_to_exit_b_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_to_exit_B_invariant_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND point_in...
  have path_to_exit_b_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_to_exit_B_invariant_TCC3
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND point_in...
  have path_to_exit_b_invariant_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.path_to_exit_B_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(q, B) AND point_in_po
theorem path_to_exit_B_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p q B ∧ point_in_polygon_p p B → (let i := next_index A (find_index_any A p); let m := A.num_vertices; ∃ (k : Finset.range A.num_vertices), vertex_p B(A.vertices (rem m(k + i))) ∧
     segment_exits_polygon_p B (edges_of_polygon A(rem m(k + i)))) := by
  -- === TCCs for path_to_exit_B_exists ===
  -- TCC: path_to_exit_B_exists_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND point_in...
  have path_to_exit_b_exists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.first_path_to_exit_B_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(q, B) AND point_in_po
theorem first_path_to_exit_B_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p q B ∧ point_in_polygon_p p B → (let i := next_index A (find_index_any A p); let m := A.num_vertices; ∃ (k : Finset.range A.num_vertices), (∀ (l : Finset.range k), ¬ point_outside_polygon_p (A.vertices (rem m(l + i))) B)
     ∧
     vertex_p B(A.vertices (rem m(k + i))) ∧
      segment_exits_polygon_p B (edges_of_polygon A(rem m(k + i)))) := by
  -- === TCCs for first_path_to_exit_B_exists ===
  -- TCC: first_path_to_exit_B_exists_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND point_in...
  have first_path_to_exit_b_exists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.path_to_exit_B_from_vertex_invariant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (vertex?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(q, B) AND vertex?(B)(p)
--    IMPLIES
--    LET
theorem path_to_exit_B_from_vertex_invariant : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p q B ∧ vertex_p B(p) → (let i := find_index A p; let m := A.num_vertices; ∀ (k : Finset.range A.num_vertices), (∀ (l : {k : ℕ // k ≤ k}), ¬ point_outside_polygon_p (A.vertices (rem m(l + i))) B)
     ∨
     (∃ (l : {k : ℕ // k ≤ k}), vertex_p B(A.vertices (rem m(l + i))) ∧
         segment_exits_polygon_p B (edges_of_polygon A(rem m(l + i))))) := by
  -- === TCCs for path_to_exit_B_from_vertex_invariant ===
  -- TCC: path_to_exit_B_from_vertex_invariant_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (vertex?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND vertex?(B)(p)
  --    IMPLIES
  --    ...
  have path_to_exit_b_from_vertex_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_to_exit_B_from_vertex_invariant_TCC2
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (vertex?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND vertex?(B)(p)
  --    IMPLIES
  --    ...
  have path_to_exit_b_from_vertex_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.path_to_exit_B_from_vertex_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (vertex?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(q, B) AND vertex?(B)(p)
--    IMPLIES
--    LET
theorem path_to_exit_B_from_vertex_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p q B ∧ vertex_p B(p) → (let i := find_index A p; let m := A.num_vertices; ∃ (k : Finset.range A.num_vertices), vertex_p B(A.vertices (rem m(k + i))) ∧
     segment_exits_polygon_p B (edges_of_polygon A(rem m(k + i)))) := by
  -- === TCCs for path_to_exit_B_from_vertex_exists ===
  -- TCC: path_to_exit_B_from_vertex_exists_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (vertex?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND vertex?(B)(p)
  --    IMPLIES
  --    ...
  have path_to_exit_b_from_vertex_exists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.first_path_to_exit_B_from_vertex_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p, q: (vertex?(A))):
--   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
--    AND point_outside_polygon?(q, B) AND vertex?(B)(p)
--    IMPLIES
--    LET
theorem first_path_to_exit_B_from_vertex_exists : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ point_outside_polygon_p q B ∧ vertex_p B(p) → (let i := find_index A p; let m := A.num_vertices; ∃ (k : Finset.range A.num_vertices), (∀ (l : Finset.range k), ¬ point_outside_polygon_p (A.vertices (rem m(l + i))) B)
     ∧
     vertex_p B(A.vertices (rem m(k + i))) ∧
      segment_exits_polygon_p B (edges_of_polygon A(rem m(k + i)))) := by
  -- === TCCs for first_path_to_exit_B_from_vertex_exists ===
  -- TCC: first_path_to_exit_B_from_vertex_exists_TCC1
  -- Statement: FORALL (A, B: simple_polygon_2d, p, q: (vertex?(A))):
  --   (FORALL (h: (edge_of_polygon?(A))): only_vertex_intersections?(B, A, h))
  --    AND point_outside_polygon?(q, B) AND vertex?(B)(p)
  --    IMPLIES
  --    ...
  have first_path_to_exit_b_from_vertex_exists_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.in_out_for_overlapping_polygons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: ccw_simple_polygon, p, q: point_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B, A, h))
--    AND (NOT polygon_contained?(B, A)) AND point_in_polygon?
theorem in_out_for_overlapping_polygons : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ (¬ polygon_contained_p B A) ∧ point_in_polygon_p p A
 ∧ point_in_polygon_p p B ∧ vertex_p A(q)
 ∧ point_outside_polygon_p q B → ∃ (u : _) (v : point_2d) (i : _) (j : Finset.range B.num_vertices) (e : (edge_of_polygon_p A)), vertex_p A(u) ∧ (u = B.vertices i) ∧ vertex_p A(v)
   ∧ (v = B.vertices j) ∧ (v = e.p1) ∧ (v ≠ u) ∧ (i ≠ j)
   ∧ between_rays_p (edges_of_polygon B(prev_index B j)) (edges_of_polygon B(j))
                    (e.p2)
   ∧ ∀ (w : (vertex_p A)), on_perimeter_vertex_path_p A u v(w) →
          ¬ point_outside_polygon_p w B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_seq_vertices_always_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
--         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--         M: finseq[point_2d], e: segment_2d, i: below(M`length - 1),
--  
theorem merge_seq_vertices_always_endpoints : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}) (j : {s : {i : ℝ // i ≥ 0} // s < M.List.length}), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B) ∧ (e.p1 = M.seq i) ∧ (e.p2 = M.seq (i + 1))
 ∧ point_on_segment_p (M.seq j) e → M.seq j = M.seq i ∨ M.seq j = M.seq (i + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_seq_vertices_always_endpoints_Am
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
--         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--         M: finseq[point_2d], e: segment_2d, i: below(Am`num_vertices)
theorem merge_seq_vertices_always_endpoints_Am : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < Am.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < M.List.length}), (Am, Bm) = inject_vertices_into_polygon A B ∧
 M = merge_seq A B ∧
  e = edges_of_polygon Am(i) ∧ point_on_segment_p (M.seq j) e → M.seq j = Am.vertices i ∨ M.seq j = Am.vertices (next_index Am i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_seq_vertices_always_endpoints_Bm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
--         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--         M: finseq[point_2d], e: segment_2d, i: below(Bm`num_vertices)
theorem merge_seq_vertices_always_endpoints_Bm : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < Bm.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < M.List.length}), (Am, Bm) = inject_vertices_into_polygon A B ∧
 M = merge_seq A B ∧
  e = edges_of_polygon Bm(i) ∧ point_on_segment_p (M.seq j) e → M.seq j = Bm.vertices i ∨ M.seq j = Bm.vertices (next_index Bm i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_seq_vertices_always_endpoints_M
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem merge_seq_vertices_always_endpoints_M : True := by
  -- === TCCs for merge_seq_vertices_always_endpoints_M ===
  -- TCC: merge_seq_vertices_always_endpoints_M_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], e: segment_2d, k: below(M`length),
  --   ...
  have merge_seq_vertices_always_endpoints_m_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_vertices_always_endpoints_M_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], e: segment_2d, k: below(M`length),
  --   ...
  have merge_seq_vertices_always_endpoints_m_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_vertices_always_endpoints_M_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], e: segment_2d, k: below(M`length),
  --   ...
  have merge_seq_vertices_always_endpoints_m_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_vertices_always_endpoints_M_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], e: segment_2d, k: below(M`length),
  --   ...
  have merge_seq_vertices_always_endpoints_m_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merge_well_formed.no_outgoing_edges_from_M
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], s, e: segment_2d, i: below(M`length
theorem no_outgoing_edges_from_M : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B) ∧ (i > 0)
 ∧ (s = (# p1 := M.seq (i - 1), p2 := M.seq i #))
 ∧ (e = (# p1 := M.seq i, p2 := M.seq (i + 1) #)) → ¬ ∃ (h : segment_2d), between_rays_p s e(h.p2) ∧
       M.seq i = h.p1 ∧
        (edge_of_polygon_p Am(h) ∨ edge_of_polygon_p Bm(h)) := by
  -- === TCCs for no_outgoing_edges_from_M ===
  -- TCC: no_outgoing_edges_from_M_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], s, e: segment_2d, i: below(M`length -...
  have no_outgoing_edges_from_m_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.no_outgoing_edges_from_M_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], s, e: segment_2d, w: point_2d):
--    
theorem no_outgoing_edges_from_M_0 : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (w : ℝ × ℝ), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B) ∧ (w = M.seq 0 + (# x := 0, y := 1 #))
 ∧ (s = (# p1 := w, p2 := M.seq 0 #))
 ∧ (e = (# p1 := M.seq 0, p2 := M.seq 1 #)) → ¬ ∃ (h : segment_2d), between_rays_p s e(h.p2) ∧
       M.seq 0 = h.p1 ∧
        (edge_of_polygon_p Am(h) ∨ edge_of_polygon_p Bm(h)) := by
  -- === TCCs for no_outgoing_edges_from_M_0 ===
  -- TCC: no_outgoing_edges_from_M_0_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], s, e: segment_2d):
  --   (Am, Bm) = injec...
  have no_outgoing_edges_from_m_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_outgoing_edges_from_M_0_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], s, e: segment_2d, w: point_2d):
  --   (Am...
  have no_outgoing_edges_from_m_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_initial_segments_do_not_cross
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), k: posnat):
--   LET M = merge_seq(A, B) ^ (0, k - 1) IN
--     k <= merge_seq(A, B)`length AND uniq_vertex_list?(k)(M`seq) IMPLIES
--      segme
theorem merge_initial_segments_do_not_cross : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (k : {i : {i : ℝ // i ≥ 0} // i > 0}), fun (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) => k ≤ merge_seq A B.List.length ∧ uniq_vertex_list_p k(M.seq) → segments_do_not_cross_p M (merge_seq A B ^ (0, k - 1)) := by
  -- === TCCs for merge_initial_segments_do_not_cross ===
  -- TCC: merge_initial_segments_do_not_cross_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), k: posnat):
  --   k - 1 >= 0
  have merge_initial_segments_do_not_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_initial_segments_do_not_cross_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), k: posnat,
  --         M: finseq[point_2d]):
  --   M = merge_seq(A, B) ^ (0, k - 1) AND k <= merge_seq(A, B)`length IMPLIES
  --    FORALL (x: nat...
  have merge_initial_segments_do_not_cross_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_initial_segments_do_not_cross_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), k: posnat,
  --         M: finseq[point_2d]):
  --   M = merge_seq(A, B) ^ (0, k - 1) AND
  --    k <= merge_seq(A, B)`length AND uniq_vertex_list?(...
  have merge_initial_segments_do_not_cross_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_well_formed.merge_wrapped_segments_do_not_cross
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), i0: nat,
--         k: posnat):
--   LET M = merge_seq(A, B) IN
--     k < M`length AND
--      i0 + 3 <= k AND
--       uniq_vertex_list?(k)((M ^ (0, 
theorem merge_wrapped_segments_do_not_cross : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i0 : {i : ℝ // i ≥ 0}) (k : {i : {i : ℝ // i ≥ 0} // i > 0}), fun (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) => k < M.List.length ∧
 i0 + 3 ≤ k ∧
  uniq_vertex_list_p k((M ^ (0, k - 1)).seq) ∧ M.seq k = M.seq i0 → segments_do_not_cross_p (M ^ (0, k)) (merge_seq A B) := by
  -- === TCCs for merge_wrapped_segments_do_not_cross ===
  -- TCC: merge_wrapped_segments_do_not_cross_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), i0: nat,
  --         k: posnat, M: finseq[point_2d]):
  --   M = merge_seq(A, B) AND k < M`length AND i0 + 3 <= k IMPLIES
  --    FORALL (x: nat): ...
  have merge_wrapped_segments_do_not_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_wrapped_segments_do_not_cross_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), i0: nat,
  --         k: posnat, M: finseq[point_2d]):
  --   M = merge_seq(A, B) AND
  --    k < M`length AND
  --     i0 + 3 <= k AND uniq_vertex_list?...
  have merge_wrapped_segments_do_not_cross_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_wrapped_segments_do_not_cross_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), i0: nat,
  --         k: posnat, M: finseq[point_2d]):
  --        (M = merge_seq(A, B)) AND (k < M`length) AND (i0 + 3 <= k)
  --    AND uniq_verte...
  have merge_wrapped_segments_do_not_cross_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.merge_well_formed