-- Auto-generated by pvs2lean/translator.py
-- PVS theory: merged_polygon_closed
-- PVSLib library: polygon_merge
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: merged_polygon_closed

Auto-generated stubs from PVSLib theory `merged_polygon_closed`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.merged_polygon_closed

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (between_rays_p : pvs_τ3 → pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (ccw_merge_pre_condition : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (ccw_vertex_order_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (edge_of_polygon_p : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (edges_of_polygon : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (empty_p : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (inject_vertices_into_polygon : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} (loop_back_intercept : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (merge_pre_condition : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (merge_seq : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (min : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (mostly_uniq_point_list_p : pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (next_index : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ42 : Type*} (norm : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} (on_perimeter_vertex_path_p : pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (only_vertex_intersections_p : pvs_τ47 → pvs_τ48 → pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (point_on_polygon_perimeter_p : pvs_τ50 → pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (point_on_segment_p : pvs_τ52 → pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (point_on_segment_seq_p : pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (point_outside_polygon_p : pvs_τ56 → pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (polygon_contained_p : pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} (polygon_edges_do_not_cross_p : pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} (prev_index : pvs_τ61 → pvs_τ62 → pvs_τ63)
variable {pvs_τ64 : Type*} (real_pred : pvs_τ64 → Prop)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (reverse_segment : pvs_τ65 → pvs_τ66)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (segment_endpoint_p : pvs_τ67 → pvs_τ68 → Prop)
variable {pvs_τ69 : Type*} (segments_do_not_cross_p : pvs_τ69 → Prop)
variable {pvs_τ70 : Type*} (seq : pvs_τ70 → Prop)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} (seq_only_vertex_intersections_p : pvs_τ71 → pvs_τ72 → pvs_τ73 → Prop)
variable {pvs_τ74 : Type*} (single_loop_back_seq_p : pvs_τ74 → Prop)
variable {pvs_τ75 : Type*} {pvs_τ76 : Type*} (singleton_seq : pvs_τ75 → pvs_τ76)
variable {pvs_τ77 : Type*} (sqv : pvs_τ77 → Prop)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} (uniq_vertex_list_p : pvs_τ78 → pvs_τ79 → Prop)
variable {pvs_τ80 : Type*} {pvs_τ81 : Type*} (upto : pvs_τ80 → pvs_τ81)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} (vertex_p : pvs_τ82 → pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} (vertices : pvs_τ84 → Prop)

-- pvs2lean: merged_polygon_closed.min_edge_sep_vertex_A
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET e = edges_of_polygon(B)(j),
--     v = e`p2 - e`p1,
--     vtx = A`vertices(i),
--     t_min = (vtx - e`p1) * v / sqv(v)
--   IN
--   IF t_min < 0 THEN norm(vtx - e`p1)
--   ELSIF t_min > 1 THEN norm(vtx - e`p2)
--   
axiom min_edge_sep_vertex_A {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27 → Prop

-- pvs2lean: merged_polygon_closed.min_edge_separation_A
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min[posreal, restrict[[real, real], [posreal, posreal], bool](<=)]
--     ({v_min: posreal |
--         EXISTS (i: below(A`num_vertices), j: below(B`num_vertices)):
--           NOT point_on_segment?(A`vertice
axiom min_edge_separation_A {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ28 → pvs_τ29 → Prop

-- pvs2lean: merged_polygon_closed.min_edge_separation_AB
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min(min_edge_separation_A(A, B), min_edge_separation_A(B, A))
axiom min_edge_separation_AB {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: merged_polygon_closed.min_edge_sep_vertex_A_le_dist_to_vtx
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i: below(A`num_vertices),
--         j: below(B`num_vertices)):
--   point_on_segment?(A`vertices(i), edges_of_polygon(B)(j)) OR
--    FORALL (p: point_2d):
--      point_on_segme
theorem min_edge_sep_vertex_A_le_dist_to_vtx : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}), point_on_segment_p (A.vertices i) (edges_of_polygon B(j)) ∨ ∀ (p : point_2d), point_on_segment_p p (edges_of_polygon B(j)) →
   min_edge_sep_vertex_A A B i j ≤ norm (A.vertices i - p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.min_edge_separation_A_is_min
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i: below(A`num_vertices),
--         j: below(B`num_vertices)):
--   point_on_segment?(A`vertices(i), edges_of_polygon(B)(j)) OR
--    FORALL (p: point_2d):
--      point_on_segme
theorem min_edge_separation_A_is_min : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}), point_on_segment_p (A.vertices i) (edges_of_polygon B(j)) ∨ ∀ (p : point_2d), point_on_segment_p p (edges_of_polygon B(j)) →
   min_edge_separation_A A B ≤ norm (A.vertices i - p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.min_edge_sep_AB_all_points
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, i: below(A`num_vertices),
--         j: below(B`num_vertices), p, q: point_2d):
--   (FORALL (u: point_2d):
--      NOT point_on_segment?(u, edges_of_polygon(A)(i)) OR
--       NO
theorem min_edge_sep_AB_all_points : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < A.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < B.num_vertices}) (p : ℝ × ℝ) (q : ℝ × ℝ), (∀ (u : point_2d), ¬ point_on_segment_p u (edges_of_polygon A(i)) ∨
    ¬ point_on_segment_p u (edges_of_polygon B(j)))
 ∧
 point_on_segment_p p (edges_of_polygon A(i)) ∧
  point_on_segment_p q (edges_of_polygon B(j)) → min_edge_separation_AB A B ≤ norm (p - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.extend_do_not_cross_off_seq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (M: segment_endpoint_seq, w: point_2d, S2: finseq[point_2d]):
--   w = M`seq(0) + (# x := 0, y := 1 #) AND
--    (FORALL (p: point_2d):
--       point_on_segment_seq?(p, M) IMPLIES
--        M`seq(0)`x < p
theorem extend_do_not_cross_off_seq : ∀ (M : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (w : ℝ × ℝ) (S2 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), w = M.seq 0 + (# x := 0, y := 1 #) ∧
 (∀ (p : point_2d), point_on_segment_seq_p p M →
     M.seq 0.x < p.x ∨ M.seq 0.x = p.x ∧ p.y ≤ M.seq 0.y)
  ∧ S2 = singleton_seq w ∘ M ∧ segments_do_not_cross_p M → segments_do_not_cross_p S2 := by
  -- === TCCs for extend_do_not_cross_off_seq ===
  -- TCC: extend_do_not_cross_off_seq_TCC1
  -- Statement: FORALL (M: segment_endpoint_seq): 0 < M`length
  have extend_do_not_cross_off_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: extend_do_not_cross_off_seq_TCC2
  -- Statement: FORALL (M: segment_endpoint_seq, w: point_2d, S2: finseq[point_2d]):
  --   w = M`seq(0) + (# x := 0, y := 1 #) AND
  --    (FORALL (p: point_2d):
  --       point_on_segment_seq?(p, M) IMPLIES
  --        M`seq(0)`x ...
  have extend_do_not_cross_off_seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.subseq_does_not_cross
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (M: segment_endpoint_seq, k, l, i, j: below(M`length)):
--   k <= i AND i <= j AND j <= l AND segments_do_not_cross?(M ^ (k, l))
--    IMPLIES segments_do_not_cross?(M ^ (i, j))
theorem subseq_does_not_cross : ∀ (M : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (k : {s : {i : ℝ // i ≥ 0} // s < M.List.length}) (l : {s : {i : ℝ // i ≥ 0} // s < M.List.length}) (i : {s : {i : ℝ // i ≥ 0} // s < M.List.length}) (j : {s : {i : ℝ // i ≥ 0} // s < M.List.length}), k ≤ i ∧ i ≤ j ∧ j ≤ l ∧ segments_do_not_cross_p (M ^ (k, l)) → segments_do_not_cross_p (M ^ (i, j)) := by
  -- === TCCs for subseq_does_not_cross ===
  -- TCC: subseq_does_not_cross_TCC1
  -- Statement: FORALL (M: segment_endpoint_seq, k, l, i, j: below(M`length)):
  --   k <= i AND i <= j AND j <= l IMPLIES
  --    (^[point_2d](M, (k, l)))`length > 0 AND
  --     FORALL (i: below((^[point_2d](M, (k, l)))`length...
  have subseq_does_not_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subseq_does_not_cross_TCC2
  -- Statement: FORALL (M: segment_endpoint_seq, k, l, i, j: below(M`length)):
  --   k <= i AND i <= j AND j <= l AND segments_do_not_cross?(M ^ (k, l))
  --    IMPLIES
  --    (^[point_2d](M, (i, j)))`length > 0 AND
  --     FORALL...
  have subseq_does_not_cross_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.extend_do_not_cross_Am_edge
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], j: below(M`length), S2: finseq[poin
theorem extend_do_not_cross_Am_edge : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < M.List.length}) (S2 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (j > 0) ∧ ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B) ∧ edge_of_polygon_p Am(e) ∧ (M.seq j = e.p2)
 ∧ (¬ segment_endpoint_p e(M.seq (j - 1)))
 ∧ (S2 = (M ^ (0, j)) ∘ singleton_seq e.p1)
 ∧ segments_do_not_cross_p (M ^ (0, j))
 ∧ uniq_vertex_list_p (j + 1) ((M ^ (0, j)).seq) → segments_do_not_cross_p S2 := by
  -- === TCCs for extend_do_not_cross_Am_edge ===
  -- TCC: extend_do_not_cross_Am_edge_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), e: segment_2d):
  --  ...
  have extend_do_not_cross_am_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: extend_do_not_cross_Am_edge_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), e: segment_2d):
  --  ...
  have extend_do_not_cross_am_edge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: extend_do_not_cross_Am_edge_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), S2: finseq[point_...
  have extend_do_not_cross_am_edge_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: extend_do_not_cross_Am_edge_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), S2: finseq[point_...
  have extend_do_not_cross_am_edge_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: extend_do_not_cross_Am_edge_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), S2: finseq[point_...
  have extend_do_not_cross_am_edge_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.left_side_loop_back_is_ccw
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], C: simple_polygon_2d, j: below(M`le
theorem left_side_loop_back_is_ccw : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < M.List.length}) (i : {s : {i : ℝ // i ≥ 0} // s < j - 2}) (S2 : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (w : ℝ × ℝ) (r : {s : {i : ℝ // i ≥ 0} // s < Am.num_vertices}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (j > 0) ∧ ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B) ∧ uniq_vertex_list_p j((M ^ (0, j - 1)).seq)
 ∧ (S2.List.length = j + 2)
 ∧ (S2 =
       singleton_seq w ∘ (M ^ (0, j - 1)) ∘ singleton_seq (Am.vertices r))
 ∧ (M.seq i = Am.vertices r)
 ∧ (M.seq (j - 1) = Am.vertices (next_index Am r))
 ∧ (M.seq i = S2.seq (j + 1)) ∧ (w = M.seq 0 + (# x := 0, y := 1 #))
 ∧ (s =
       (# p1 := if i = 0 then w else M.seq (i - 1) , p2 := M.seq i #))
 ∧ (e = (# p1 := M.seq i, p2 := M.seq (1 + i) #))
 ∧ between_rays_p (reverse_segment e) (reverse_segment s)
                  (Am.vertices (next_index Am r))
 ∧ (∀ (p : point_2d), (point_on_polygon_perimeter_p Am(p) ∨
          point_on_polygon_perimeter_p Bm(p))
         → M.seq 0.x < p.x ∨ M.seq 0.x = p.x ∧ p.y ≤ M.seq 0.y)
 ∧ single_loop_back_seq_p S2
 ∧ mostly_uniq_point_list_p S2.List.length (loop_back_intercept S2) (S2.seq)
 ∧ segments_do_not_cross_p S2 ∧ (C.num_vertices = j - i)
 ∧ (∀ (k : Finset.range C.num_vertices), C.vertices k = M.seq (i + k)) → ccw_vertex_order_p C := by
  -- === TCCs for left_side_loop_back_is_ccw ===
  -- TCC: left_side_loop_back_is_ccw_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC7
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC8
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC9
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC10
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], j: below(M`length), i: below(j - 2),
  -- ...
  have left_side_loop_back_is_ccw_tcc10 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: left_side_loop_back_is_ccw_TCC11
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], C: simple_polygon_2d, j: below(M`leng...
  have left_side_loop_back_is_ccw_tcc11 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.no_incoming_edge_at_i
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET w = M`seq(0) + (# x := 0, y := 1 #) IN
--   NOT EXISTS (s, e, h: segment_2d):
--              (s =
--                (# p1 := IF i = 0 THEN w ELSE M`seq(i - 1) ENDIF,
--                   p2 := M`seq(i) #))
--  
axiom no_incoming_edge_at_i {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ38 → pvs_τ39 → pvs_τ40 → pvs_τ41 → Prop

-- pvs2lean: merged_polygon_closed.no_min_inc_back_side_edge_from_M_contained
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem no_min_inc_back_side_edge_from_M_contained : True := by
  -- === TCCs for no_min_inc_back_side_edge_from_M_contained ===
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC7
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC8
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC9
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC10
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc10 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC11
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc11 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC12
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc12 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_contained_TCC13
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         C: ccw_simple_polygon, M: finseq[point_2d], i: below(M`len...
  have no_min_inc_back_side_edge_from_m_contained_tcc13 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merged_polygon_closed.no_min_inc_back_side_edge_from_M
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], i: below(M`length - 1), w: point_2d
theorem no_min_inc_back_side_edge_from_M : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}) (w : ℝ × ℝ) (r : {s : {i : ℝ // i ≥ 0} // s < Am.num_vertices}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B)
 ∧ uniq_vertex_list_p (2 + i) ((M ^ (0, 1 + i)).seq)
 ∧ (M.seq 0 + (# x := 0, y := 1 #) = w)
 ∧ (∀ (k : Finset.range Am.num_vertices), w ≠ Am.vertices k)
 ∧ (∀ (p : point_2d), (point_on_polygon_perimeter_p Am(p) ∨
          point_on_polygon_perimeter_p Bm(p))
         → M.seq 0.x < p.x ∨ M.seq 0.x = p.x ∧ p.y ≤ M.seq 0.y)
 ∧ (∀ (u : point_2d), point_on_segment_p u ((# p1 := w, p2 := M.seq 0 #)) →
         u = M.seq 0 ∨ ¬ point_on_segment_seq_p u M)
 ∧ (∀ (i : Finset.range M.List.length - 1), ¬ M.seq i = M.seq (1 + i))
 ∧ (∀ (k : Finset.range M.List.length), w ≠ M.seq k)
 ∧ (∀ (k : Finset.range M.List.length), M.seq 0.x < M.seq k.x ∨
         M.seq 0.x = M.seq k.x ∧ M.seq k.y ≤ M.seq 0.y)
 ∧ (M.seq i = Am.vertices (next_index Am r))
 ∧ (s =
       (# p1 := if i = 0 then w else M.seq (i - 1) , p2 := M.seq i #))
 ∧ (e = (# p1 := M.seq i, p2 := M.seq (1 + i) #))
 ∧ between_rays_p s e(Am.vertices r) → ∀ (k : {k : ℕ // k ≤ i}), Am.vertices r ≠ M.seq k := by
  -- === TCCs for no_min_inc_back_side_edge_from_M ===
  -- TCC: no_min_inc_back_side_edge_from_M_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         r: be...
  have no_min_inc_back_side_edge_from_m_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         r: be...
  have no_min_inc_back_side_edge_from_m_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         r: be...
  have no_min_inc_back_side_edge_from_m_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), w: point_2d,
  -- ...
  have no_min_inc_back_side_edge_from_m_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), w: point_2d,
  -- ...
  have no_min_inc_back_side_edge_from_m_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), w: point_2d,
  -- ...
  have no_min_inc_back_side_edge_from_m_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC7
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), w: point_2d,
  -- ...
  have no_min_inc_back_side_edge_from_m_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC8
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), w: point_2d,
  -- ...
  have no_min_inc_back_side_edge_from_m_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC9
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), w: point_2d,
  -- ...
  have no_min_inc_back_side_edge_from_m_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC10
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), w: point_2d,
  -- ...
  have no_min_inc_back_side_edge_from_m_tcc10 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_back_side_edge_from_M_TCC11
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), w: point_2d,
  -- ...
  have no_min_inc_back_side_edge_from_m_tcc11 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.no_min_inc_edge_to_M_Am_no_cross
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], i: below(M`length - 1),
--           S
theorem no_min_inc_edge_to_M_Am_no_cross : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}) (S2 : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (w : ℝ × ℝ) (r : {s : {i : ℝ // i ≥ 0} // s < Am.num_vertices}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B)
 ∧ uniq_vertex_list_p (2 + i) ((M ^ (0, 1 + i)).seq) ∧ (S2.List.length = i + 4)
 ∧ single_loop_back_seq_p S2
 ∧ mostly_uniq_point_list_p S2.List.length (loop_back_intercept S2) (S2.seq)
 ∧ (∀ (i : Finset.range S2.List.length - 1), ¬ S2.seq i = S2.seq (1 + i))
 ∧ (∀ (k : Finset.range i + 4), S2.seq k =
         if k = 0 then w
         ELSIF k = i + 2 then Am.vertices r
         ELSIF k = i + 3 then Am.vertices (prev_index Am r)
         else M.seq (k - 1)
         )
 ∧ (M.seq 0 + (# x := 0, y := 1 #) = w)
 ∧ (∀ (k : Finset.range Am.num_vertices), w ≠ Am.vertices k)
 ∧ (∀ (p : point_2d), (point_on_polygon_perimeter_p Am(p) ∨
          point_on_polygon_perimeter_p Bm(p))
         → M.seq 0.x < p.x ∨ M.seq 0.x = p.x ∧ p.y ≤ M.seq 0.y)
 ∧ (∀ (u : point_2d), point_on_segment_p u ((# p1 := w, p2 := M.seq 0 #)) →
         u = M.seq 0 ∨ ¬ point_on_segment_seq_p u M)
 ∧ (∀ (i : Finset.range M.List.length - 1), ¬ M.seq i = M.seq (1 + i))
 ∧ (∀ (k : Finset.range M.List.length), w ≠ M.seq k)
 ∧ (∀ (k : Finset.range M.List.length), M.seq 0.x < M.seq k.x ∨
         M.seq 0.x = M.seq k.x ∧ M.seq k.y ≤ M.seq 0.y)
 ∧ (M.seq i = Am.vertices (next_index Am r))
 ∧ (s =
       (# p1 := if i = 0 then w else M.seq (i - 1) , p2 := M.seq i #))
 ∧ (e = (# p1 := M.seq i, p2 := M.seq (1 + i) #))
 ∧ between_rays_p s e(Am.vertices r) → segments_do_not_cross_p S2 := by
  -- === TCCs for no_min_inc_edge_to_M_Am_no_cross ===
  -- TCC: no_min_inc_edge_to_M_Am_no_cross_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_no_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_no_cross_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_no_cross_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_no_cross_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_no_cross_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_no_cross_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_no_cross_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_no_cross_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_no_cross_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_no_cross_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_no_cross_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.no_min_inc_edge_to_M_Am_uniq_list
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], i: below(M`length - 1),
--           S
theorem no_min_inc_edge_to_M_Am_uniq_list : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}) (S2 : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (w : ℝ × ℝ) (r : {s : {i : ℝ // i ≥ 0} // s < Am.num_vertices}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B)
 ∧ uniq_vertex_list_p (2 + i) ((M ^ (0, 1 + i)).seq)
 ∧ single_loop_back_seq_p S2 ∧ (S2.List.length = i + 4)
 ∧ (∀ (i : Finset.range S2.List.length - 1), ¬ S2.seq i = S2.seq (1 + i))
 ∧ (∀ (k : Finset.range i + 4), S2.seq k =
         if k = 0 then w
         ELSIF k = i + 2 then Am.vertices r
         ELSIF k = i + 3 then Am.vertices (prev_index Am r)
         else M.seq (k - 1)
         )
 ∧ (M.seq 0 + (# x := 0, y := 1 #) = w)
 ∧ (∀ (k : Finset.range Am.num_vertices), w ≠ Am.vertices k)
 ∧ (∀ (p : point_2d), (point_on_polygon_perimeter_p Am(p) ∨
          point_on_polygon_perimeter_p Bm(p))
         → M.seq 0.x < p.x ∨ M.seq 0.x = p.x ∧ p.y ≤ M.seq 0.y)
 ∧ (∀ (u : point_2d), point_on_segment_p u ((# p1 := w, p2 := M.seq 0 #)) →
         u = M.seq 0 ∨ ¬ point_on_segment_seq_p u M)
 ∧ (∀ (i : Finset.range M.List.length - 1), ¬ M.seq i = M.seq (1 + i))
 ∧ (∀ (k : Finset.range M.List.length), w ≠ M.seq k)
 ∧ (∀ (k : Finset.range M.List.length), M.seq 0.x < M.seq k.x ∨
         M.seq 0.x = M.seq k.x ∧ M.seq k.y ≤ M.seq 0.y)
 ∧ (edges_of_polygon Am(r) = h)
 ∧ (s =
       (# p1 := if i = 0 then w else M.seq (i - 1) , p2 := M.seq i #))
 ∧ (e = (# p1 := M.seq i, p2 := M.seq (1 + i) #))
 ∧ between_rays_p s e(h.p1) ∧ (M.seq i = h.p2) → mostly_uniq_point_list_p S2.List.length (loop_back_intercept S2) (S2.seq) := by
  -- === TCCs for no_min_inc_edge_to_M_Am_uniq_list ===
  -- TCC: no_min_inc_edge_to_M_Am_uniq_list_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_uniq_list_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_uniq_list_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_uniq_list_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_uniq_list_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_uniq_list_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_uniq_list_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_uniq_list_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_uniq_list_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_uniq_list_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_inc_edge_to_M_Am_uniq_list_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1),
  --         S2: s...
  have no_min_inc_edge_to_m_am_uniq_list_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.no_min_incoming_edge_to_M_Am
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], i: below(M`length - 1), s, e, h: se
theorem no_min_incoming_edge_to_M_Am : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (w : ℝ × ℝ), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B)
 ∧ uniq_vertex_list_p (i + 2) ((M ^ (0, i + 1)).seq)
 ∧ (∀ (k : Finset.range i), no_incoming_edge_at_i Am Bm M k)
 ∧ (w = M.seq 0 + (# x := 0, y := 1 #))
 ∧ (s =
       (# p1 := if i = 0 then w else M.seq (i - 1) , p2 := M.seq i #))
 ∧ (e = (# p1 := M.seq i, p2 := M.seq (1 + i) #))
 ∧ between_rays_p s e(h.p1) ∧ (M.seq i = h.p2) → ¬ edge_of_polygon_p Am(h) := by
  -- === TCCs for no_min_incoming_edge_to_M_Am ===
  -- TCC: no_min_incoming_edge_to_M_Am_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), s, e, h: segm...
  have no_min_incoming_edge_to_m_am_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_Am_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), s, e, h: segm...
  have no_min_incoming_edge_to_m_am_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_Am_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), s, e, h: segm...
  have no_min_incoming_edge_to_m_am_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_Am_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), s, e, h: segm...
  have no_min_incoming_edge_to_m_am_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_Am_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), s, e, h: segm...
  have no_min_incoming_edge_to_m_am_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_Am_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), s, e, h: segm...
  have no_min_incoming_edge_to_m_am_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_Am_TCC7
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), s, e, h: segm...
  have no_min_incoming_edge_to_m_am_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_Am_TCC8
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1), s, e, h: segm...
  have no_min_incoming_edge_to_m_am_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.no_min_incoming_edge_to_M
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], i: below(M`length - 1)):
--     (Am, B
theorem no_min_incoming_edge_to_M : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}), (Am, Bm) = inject_vertices_into_polygon A B ∧
 M = merge_seq A B ∧
  uniq_vertex_list_p (i + 2) ((M ^ (0, i + 1)).seq) ∧
   (∀ (k : Finset.range i), no_incoming_edge_at_i Am Bm M k) → no_incoming_edge_at_i Am Bm M i := by
  -- === TCCs for no_min_incoming_edge_to_M ===
  -- TCC: no_min_incoming_edge_to_M_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1)):
  --   merge_pre_...
  have no_min_incoming_edge_to_m_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1)):
  --   (Am, Bm) =...
  have no_min_incoming_edge_to_m_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1)):
  --   (Am, Bm) =...
  have no_min_incoming_edge_to_m_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1)):
  --   (Am, Bm) =...
  have no_min_incoming_edge_to_m_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_min_incoming_edge_to_M_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: below(M`length - 1)):
  --   ((Am, Bm) ...
  have no_min_incoming_edge_to_m_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.no_incoming_edges_to_M
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], i: nat):
--     (Am, Bm) = inject_vert
theorem no_incoming_edges_to_M : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}), (Am, Bm) = inject_vertices_into_polygon A B ∧
 M = merge_seq A B ∧
  i < M.List.length - 2 ∧ uniq_vertex_list_p (i + 2) ((M ^ (0, i + 1)).seq) → no_incoming_edge_at_i Am Bm M i := by
  -- === TCCs for no_incoming_edges_to_M ===
  -- TCC: no_incoming_edges_to_M_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
  --   merge_pre_condition(A)(B)
  have no_incoming_edges_to_m_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_incoming_edges_to_M_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: nat):
  --   (Am, Bm) = inject_vertices...
  have no_incoming_edges_to_m_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_incoming_edges_to_M_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: nat):
  --   (Am, Bm) = inject_vertices...
  have no_incoming_edges_to_m_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_incoming_edges_to_M_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i: nat):
  --   (Am, Bm) = inject_vertices...
  have no_incoming_edges_to_m_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.no_right_side_loop_back_on_M
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (M: finseq[point_2d], s, e, h: segment_2d, j: below(M`length),
--           i: below(j - 2)):
--          (M = merge_seq(A, B)) AND 
theorem no_right_side_loop_back_on_M : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (j : {s : {i : ℝ // i ≥ 0} // s < M.List.length}) (i : {s : {i : ℝ // i ≥ 0} // s < j - 2}), (M = merge_seq A B) ∧ (i > 0)
 ∧ (s = (# p1 := M.seq (i - 1), p2 := M.seq i #))
 ∧ (e = (# p1 := M.seq i, p2 := M.seq (i + 1) #))
 ∧ (h = (# p1 := M.seq (j - 1), p2 := M.seq j #))
 ∧ uniq_vertex_list_p j((M ^ (0, j - 1)).seq) ∧ (M.seq i = M.seq j) → ¬ between_rays_p s e(h.p1) ∨ M.seq i ≠ h.p2 := by
  -- === TCCs for no_right_side_loop_back_on_M ===
  -- TCC: no_right_side_loop_back_on_M_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d], s, e, h: segment_2d, j: below(M`length),
  --         i: below(j - 2)):
  --   merge_pre_condition(A)(B)
  have no_right_side_loop_back_on_m_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_right_side_loop_back_on_M_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d], s, e, h: segment_2d, j: below(M`length),
  --         i: below(j - 2)):
  --   M = merge_seq(A, B) AND i > 0 IMPLI...
  have no_right_side_loop_back_on_m_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_right_side_loop_back_on_M_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d], s, e, h: segment_2d, j: below(M`length),
  --         i: below(j - 2)):
  --   M = merge_seq(A, B) AND i > 0 IMPLI...
  have no_right_side_loop_back_on_m_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_right_side_loop_back_on_M_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d], s, e, h: segment_2d, j: below(M`length),
  --         i: below(j - 2)):
  --   M = merge_seq(A, B) AND
  --    i > 0 AN...
  have no_right_side_loop_back_on_m_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_right_side_loop_back_on_M_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d], s, e, h: segment_2d, j: below(M`length),
  --         i: below(j - 2)):
  --   M = merge_seq(A, B) AND
  --    i > 0 AN...
  have no_right_side_loop_back_on_m_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_right_side_loop_back_on_M_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d], s, e, h: segment_2d, j: below(M`length),
  --         i: below(j - 2)):
  --        (M = merge_seq(A, B)) AND (i >...
  have no_right_side_loop_back_on_m_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_right_side_loop_back_on_M_TCC7
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d], s, e, h: segment_2d, j: below(M`length),
  --         i: below(j - 2)):
  --        (M = merge_seq(A, B)) AND (i >...
  have no_right_side_loop_back_on_m_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.edge_on_perim_imp_outgoing_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: ccw_simple_polygon, q: point_2d,
--         e: segment_2d):
--        (FORALL (h: (edge_of_polygon?(A))):
--           only_vertex_intersections?(B, A, h))
--    AND edge_of_polyg
theorem edge_on_perim_imp_outgoing_edge : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (q : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (∀ (h : (edge_of_polygon_p A)), only_vertex_intersections_p B A h)
 ∧ edge_of_polygon_p A(e) ∧ edge_of_polygon_p B(e) ∧ vertex_p A(q)
 ∧ point_outside_polygon_p q B → ∃ (v : point_2d) (i : Finset.range B.num_vertices) (s : (edge_of_polygon_p A)), vertex_p A(v) ∧ (v = B.vertices i) ∧ (v = s.p1)
   ∧ between_rays_p (edges_of_polygon B(prev_index B i)) (edges_of_polygon B(i))
                    (s.p2)
   ∧ ∀ (u : (vertex_p A)), on_perimeter_vertex_path_p A e.p2 v(u) →
          ¬ point_outside_polygon_p u B := by
  -- === TCCs for edge_on_perim_imp_outgoing_edge ===
  -- TCC: edge_on_perim_imp_outgoing_edge_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: ccw_simple_polygon, q: point_2d,
  --         e: segment_2d):
  --        (FORALL (h: (edge_of_polygon?(A))):
  --           only_vertex_intersections?(B, A, h))
  --    AND edge_of_po...
  have edge_on_perim_imp_outgoing_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.ccw_shared_edges_imp_ccw
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
--         C: simple_polygon_2d, M: finseq[point_2d],
--         i0: below(M`length - 1)):
--        (M = merge_seq(A, B)) AND (0 < i0)
--    AND (C
theorem ccw_shared_edges_imp_ccw : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i0 : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}), (M = merge_seq A B) ∧ (0 < i0) ∧ (C.num_vertices < M.List.length - i0)
 ∧ (∀ (k : Finset.range C.num_vertices), C.vertices k = M.seq (i0 + k))
 ∧ uniq_vertex_list_p (i0 + C.num_vertices)
                      ((M ^ (0, i0 + C.num_vertices - 1)).seq)
 ∧ (M.seq (i0 + C.num_vertices) = M.seq i0) → ccw_vertex_order_p C := by
  -- === TCCs for ccw_shared_edges_imp_ccw ===
  -- TCC: ccw_shared_edges_imp_ccw_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d], i0: below(M`length - 1)):
  --   merge_pre_condition(A)(B)
  have ccw_shared_edges_imp_ccw_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ccw_shared_edges_imp_ccw_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         C: simple_polygon_2d, M: finseq[point_2d],
  --         i0: below(M`length - 1)):
  --   M = merge_seq(A, B) AND 0 < i0 AND C`num_verti...
  have ccw_shared_edges_imp_ccw_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ccw_shared_edges_imp_ccw_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         C: simple_polygon_2d, M: finseq[point_2d],
  --         i0: below(M`length - 1)):
  --   (M = merge_seq(A, B) AND
  --     0 < i0 AND
  --      C...
  have ccw_shared_edges_imp_ccw_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ccw_shared_edges_imp_ccw_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         C: simple_polygon_2d, M: finseq[point_2d],
  --         i0: below(M`length - 1)):
  --   (M = merge_seq(A, B) AND
  --     0 < i0 AND
  --      C...
  have ccw_shared_edges_imp_ccw_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ccw_shared_edges_imp_ccw_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         C: simple_polygon_2d, M: finseq[point_2d],
  --         i0: below(M`length - 1)):
  --        (M = merge_seq(A, B)) AND (0 < i0)
  --    AND...
  have ccw_shared_edges_imp_ccw_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ccw_shared_edges_imp_ccw_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         C: simple_polygon_2d, M: finseq[point_2d],
  --         i0: below(M`length - 1)):
  --        (M = merge_seq(A, B)) AND (0 < i0)
  --    AND...
  have ccw_shared_edges_imp_ccw_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merged_polygon_closed.merge_subseq_edges_do_not_cross
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem merge_subseq_edges_do_not_cross : True := by
  -- === TCCs for merge_subseq_edges_do_not_cross ===
  -- TCC: merge_subseq_edges_do_not_cross_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
  --   merge_pre_condition(A)(B)
  have merge_subseq_edges_do_not_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_subseq_edges_do_not_cross_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), i, j: nat,
  --         M: finseq[point_2d]):
  --        (M = merge_seq(A, B) ^ (i, j)) AND (i < j)
  --    AND (j < merge_seq(A, B)`length - 1) AN...
  have merge_subseq_edges_do_not_cross_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_subseq_edges_do_not_cross_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), i, j: nat,
  --         M: finseq[point_2d]):
  --        (M = merge_seq(A, B) ^ (i, j)) AND (i < j)
  --    AND (j < merge_seq(A, B)`length - 1) AN...
  have merge_subseq_edges_do_not_cross_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_subseq_edges_do_not_cross_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)), i, j: nat,
  --         M: finseq[point_2d]):
  --        (M = merge_seq(A, B) ^ (i, j)) AND (i < j)
  --    AND (j < merge_seq(A, B)`length - 1) AN...
  have merge_subseq_edges_do_not_cross_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merged_polygon_closed.merge_seq_uniq_case_Am
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           M: finseq[point_2d], i0: below(M`length - 1),
--           
theorem merge_seq_uniq_case_Am : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i0 : {s : {i : ℝ // i ≥ 0} // s < M.List.length - 1}) (j0 : {s : {i : ℝ // i ≥ 0} // s < M.List.length}) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (M = merge_seq A B) ∧ (i0 > 0) ∧ (j0 - i0 > 2)
 ∧ (M.seq i0 = M.seq j0)
 ∧ (C =
       (# num_vertices := j0 - i0,
          vertices := fun (k : Finset.range j0 - i0)=> M.seq (k + i0) #))
 ∧ edge_of_polygon_p Am((# p1 := M.seq (i0 - 1), p2 := M.seq i0 #))
 ∧ point_outside_polygon_p (M.seq (i0 - 1)) C
 ∧ (∀ (k : Finset.range j0 - i0), edges_of_polygon C(k) =
         (# p1 := M.seq (k + i0), p2 := M.seq (1 + k + i0) #))
 ∧ (∀ (k : Finset.range j0 - i0), C.vertices k = M.seq (k + i0))
 ∧ (min
          ({j: Finset.range M.List.length |
              ∃ (l : ℕ), l < j ∧ M.seq j = M.seq l})
       = j0)
 ∧ (choose
          ({i: Finset.range M.List.length | i < j0 ∧ M.seq i = M.seq j0})
       = i0)
 ∧ (∀ (e : (edge_of_polygon_p Am)), seq_only_vertex_intersections_p (merge_seq A B) Am e)
 ∧ (∀ (e : (edge_of_polygon_p Bm)), seq_only_vertex_intersections_p (merge_seq A B) Bm e) → i0 = 0 ∨ polygon_contained_p A B ∨ polygon_contained_p B A := by
  -- === TCCs for merge_seq_uniq_case_Am ===
  -- TCC: merge_seq_uniq_case_Am_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC3
  -- Statement: FORALL (num_vertices: number_field, A: ccw_simple_polygon,
  --         B: (ccw_merge_pre_condition(A)), Am: ccw_simple_polygon,
  --         Bm: (ccw_merge_pre_condition(Am)), M: finseq[point_2d],
  --         i...
  have merge_seq_uniq_case_am_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC6
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC7
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC8
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC9
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC10
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc10 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC11
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc11 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_uniq_case_Am_TCC12
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], i0: below(M`length - 1), j0: below(M`...
  have merge_seq_uniq_case_am_tcc12 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.merged_polygon_closed