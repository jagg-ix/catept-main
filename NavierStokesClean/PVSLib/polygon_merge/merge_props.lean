-- Auto-generated by pvs2lean/translator.py
-- PVS theory: merge_props
-- PVSLib library: polygon_merge
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: merge_props

Auto-generated stubs from PVSLib theory `merge_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.merge_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (ccw_merge_pre_condition : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (ccw_vertex_order_p : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (edge_of_polygon_p : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (edges_of_polygon : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (find_index : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (first_vertex_merge : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (inject_vertices_into_polygon : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} (leftmost_vertex_val : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (merge_pre_condition : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (merge_prefix : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (merge_seq : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (next_index : pvs_τ27 → pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (next_merge_vertex : pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (no_edges_in_outward_wedges_p : pvs_τ34 → pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (norm : pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (only_vertex_intersections_p : pvs_τ38 → pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (point_AB_vtx_p : pvs_τ41 → pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (point_between_edges_p : pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} (point_between_rays_p : pvs_τ48 → pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (point_in_polygon_p : pvs_τ50 → pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (point_on_polygon_perimeter_p : pvs_τ52 → pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (point_on_segment_p : pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (point_outside_polygon_p : pvs_τ56 → pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (polygon_contained_p : pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (polygon_edges_do_not_cross_p : pvs_τ60 → pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (prev_0 : pvs_τ62 → pvs_τ63)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} (prev_index : pvs_τ64 → pvs_τ65 → pvs_τ66)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} (rem : pvs_τ67 → pvs_τ68 → pvs_τ69)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} (reverse_segment : pvs_τ70 → pvs_τ71)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} {pvs_τ76 : Type*} (rotated_vertices : pvs_τ72 → pvs_τ73 → pvs_τ74 → pvs_τ75 → pvs_τ76)
variable {pvs_τ77 : Type*} (seq : pvs_τ77 → Prop)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} (seq_prev_vtx : pvs_τ78 → pvs_τ79 → pvs_τ80 → Prop)
variable {pvs_τ83 : Type*} (simple_polygon_2d_p : pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} (topleft_vertex_idx : pvs_τ84 → Prop)
variable {pvs_τ85 : Type*} {pvs_τ86 : Type*} (topleft_vertex_val : pvs_τ85 → pvs_τ86)
variable {pvs_τ87 : Type*} {pvs_τ88 : Type*} (uniq_vertex_list_p : pvs_τ87 → pvs_τ88 → Prop)
variable {pvs_τ89 : Type*} {pvs_τ90 : Type*} (upto : pvs_τ89 → pvs_τ90)
variable {pvs_τ91 : Type*} {pvs_τ92 : Type*} (vertex_wedge_radius : pvs_τ91 → pvs_τ92)
variable {pvs_τ93 : Type*} (vertices : pvs_τ93 → Prop)

-- pvs2lean: merge_props.contains_imp_topleft_first_vtx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   polygon_contained?(B, A) IMPLIES
--    first_vertex_merge(A, B) = A`vertices(topleft_vertex_idx(A))
theorem contains_imp_topleft_first_vtx : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p B A → first_vertex_merge A B = A.vertices (topleft_vertex_idx A) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.contains_imp_topleft_first_vtx_Am
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
--         Am, Bm: simple_polygon_2d):
--   polygon_contained?(B, A) AND
--    (Am, Bm) = inject_vertices_into_polygon(A, B)
--    IMPLIES first_vertex_m
theorem contains_imp_topleft_first_vtx_Am : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p B A ∧ (Am, Bm) = inject_vertices_into_polygon A B → first_vertex_merge A B = Am.vertices (topleft_vertex_idx Am) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.contained_next_merge_vtx_eq_next_Am_vtx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     polygon_contained?(Bm, Am) AND
--      (Am, Bm) = inject_vertice
theorem contained_next_merge_vtx_eq_next_Am_vtx : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p Bm Am ∧ (Am, Bm) = inject_vertices_into_polygon A B → ∀ (i : Finset.range Am.num_vertices), next_merge_vertex Am Bm (Am.vertices i) (Am.vertices (prev_index Am i))
   = Am.vertices (next_index Am i) := by
  -- === TCCs for contained_next_merge_vtx_eq_next_Am_vtx ===
  -- TCC: contained_next_merge_vtx_eq_next_Am_vtx_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
  --   polygon_contained?(Bm, Am) IMPLIES merge_pre_condition(A)(B)
  have contained_next_merge_vtx_eq_next_am_vtx_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: contained_next_merge_vtx_eq_next_Am_vtx_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
  --   polygon_contained?(Bm, Am) AND
  --    (Am, Bm) = inject_vertices_in...
  have contained_next_merge_vtx_eq_next_am_vtx_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.contained_next_merge_vtx_eq_next_Am_topleft
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     polygon_contained?(Bm, Am) AND
--      (Am, Bm) = inject_vertice
theorem contained_next_merge_vtx_eq_next_Am_topleft : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p Bm Am ∧ (Am, Bm) = inject_vertices_into_polygon A B → (let Am_0 := topleft_vertex_idx Am; next_merge_vertex Am Bm (Am.vertices Am_0) (prev_0 (Am.vertices Am_0)) =
   Am.vertices (next_index Am Am_0)) := by
  -- === TCCs for contained_next_merge_vtx_eq_next_Am_topleft ===
  -- TCC: contained_next_merge_vtx_eq_next_Am_topleft_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
  --   polygon_contained?(Bm, Am) IMPLIES
  --    FORALL (Am_0:
  --            ...
  have contained_next_merge_vtx_eq_next_am_topleft_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_prefix_contained_rotated_vertices_A
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           first_vtx: point_2d, k: nat):
--          (k < Am`num_verti
theorem merge_prefix_contained_rotated_vertices_A : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (first_vtx : ℝ × ℝ) (k : {i : ℝ // i ≥ 0}), (k < Am.num_vertices) ∧ polygon_contained_p B A
 ∧ (first_vtx = first_vertex_merge A B)
 ∧ ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (∀ (e : (edge_of_polygon_p Am)), only_vertex_intersections_p Bm Am e) → merge_prefix Am Bm first_vtx k.seq =
 fun (i : {k : ℕ // k ≤ k})=> rotated_vertices Am.num_vertices(Am.vertices, topleft_vertex_idx Am) (i) := by
  -- === TCCs for merge_prefix_contained_rotated_vertices_A ===
  -- TCC: merge_prefix_contained_rotated_vertices_A_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         first_vtx: point_2d, k: nat):
  --   k < Am`num_vertices AND
  --   ...
  have merge_prefix_contained_rotated_vertices_a_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_prefix_contained_rotated_vertices_A_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         first_vtx: point_2d, k: nat):
  --        (k < Am`num_vertices)...
  have merge_prefix_contained_rotated_vertices_a_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_prefix_contained_rotated_vertices_A_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         first_vtx: point_2d, k: nat):
  --        (k < Am`num_vertices)...
  have merge_prefix_contained_rotated_vertices_a_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_prefix_contained_rotated_vertices_A_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         first_vtx: point_2d, k: nat):
  --        (k < Am`num_vertices)...
  have merge_prefix_contained_rotated_vertices_a_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.contained_next_merge_vtx_is_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     polygon_contained?(Bm, Am) AND
--      (Am, Bm) = inject_vertice
theorem contained_next_merge_vtx_is_first : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p Bm Am ∧ (Am, Bm) = inject_vertices_into_polygon A B → next_merge_vertex Am Bm (Am.vertices
                      (rem Am.num_vertices(topleft_vertex_idx Am - 1))) (Am.vertices
                      (rem Am.num_vertices(topleft_vertex_idx Am - 2)))
 = first_vertex_merge A B := by
  -- === TCCs for contained_next_merge_vtx_is_first ===
  -- TCC: contained_next_merge_vtx_is_first_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
  --   polygon_contained?(Bm, Am) AND
  --    (Am, Bm) = inject_vertices_in...
  have contained_next_merge_vtx_is_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.contained_next_merge_vtx_neq_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           L: nat):
--     polygon_contained?(Bm, Am) AND
--      (Am, Bm
theorem contained_next_merge_vtx_neq_first : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (L : {i : ℝ // i ≥ 0}), polygon_contained_p Bm Am ∧
 (Am, Bm) = inject_vertices_into_polygon A B ∧
  merge_seq A B.List.length = L ∧ Am.num_vertices > L → next_merge_vertex Am Bm (merge_seq A B.seq (L - 1)) (seq_prev_vtx (merge_seq A B) (L - 1) 1)
 ≠ first_vertex_merge A B := by
  -- === TCCs for contained_next_merge_vtx_neq_first ===
  -- TCC: contained_next_merge_vtx_neq_first_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)), L: nat):
  --   polygon_contained?(Bm, Am) AND
  --    (Am, Bm) = inject_ver...
  have contained_next_merge_vtx_neq_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: contained_next_merge_vtx_neq_first_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)), L: nat):
  --   polygon_contained?(Bm, Am) AND
  --    (Am, Bm) = inject_ver...
  have contained_next_merge_vtx_neq_first_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: contained_next_merge_vtx_neq_first_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)), L: nat):
  --   polygon_contained?(Bm, Am) AND
  --    (Am, Bm) = inject_ver...
  have contained_next_merge_vtx_neq_first_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: contained_next_merge_vtx_neq_first_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)), L: nat):
  --   polygon_contained?(Bm, Am) AND
  --    (Am, Bm) = inject_ver...
  have contained_next_merge_vtx_neq_first_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_seq_contained_rotated_vertices_A
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      polygo
theorem merge_seq_contained_rotated_vertices_A : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ polygon_contained_p B A → merge_seq A B.List.length = Am.num_vertices ∧
 merge_seq A B.seq =
  rotated_vertices Am.num_vertices(Am.vertices, topleft_vertex_idx Am) := by
  -- === TCCs for merge_seq_contained_rotated_vertices_A ===
  -- TCC: merge_seq_contained_rotated_vertices_A_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
  --   merge_pre_condition(A)(B)
  have merge_seq_contained_rotated_vertices_a_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_contained_rotated_vertices_A_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    polygon_co...
  have merge_seq_contained_rotated_vertices_a_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_seq_contained_rotated_vertices_B
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      polygo
theorem merge_seq_contained_rotated_vertices_B : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ polygon_contained_p A B → merge_seq A B.List.length = Bm.num_vertices ∧
 merge_seq A B.seq =
  rotated_vertices Bm.num_vertices(Bm.vertices, topleft_vertex_idx Bm) := by
  -- === TCCs for merge_seq_contained_rotated_vertices_B ===
  -- TCC: merge_seq_contained_rotated_vertices_B_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    polygon_co...
  have merge_seq_contained_rotated_vertices_b_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_seq_not_contained_has_uniq_vertex_list
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   LET M = merge_seq(A, B) IN
--     uniq_vertex_list?(M`length)(M`seq) OR
--      polygon_contained?(A, B) OR polygon_contained?(B, A)
theorem merge_seq_not_contained_has_uniq_vertex_list : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) => uniq_vertex_list_p M.List.length(M.seq) ∨ polygon_contained_p A B ∨ polygon_contained_p B A (merge_seq A B) := by
  -- === TCCs for merge_seq_not_contained_has_uniq_vertex_list ===
  -- TCC: merge_seq_not_contained_has_uniq_vertex_list_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
  --   merge_pre_condition(A)(B)
  have merge_seq_not_contained_has_uniq_vertex_list_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_seq_has_uniq_vertex_list
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   LET M = merge_seq(A, B) IN uniq_vertex_list?(M`length)(M`seq)
theorem merge_seq_has_uniq_vertex_list : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) => uniq_vertex_list_p M.List.length M.seq (merge_seq A B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merged_polygon_is_closed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           L: finseq[point_2d]):
--     (Am, Bm) = inject_vertices_int
theorem merged_polygon_is_closed : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (L : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ L = merge_seq A B → (let last_vtx := L.seq (L.List.length - 1); let prev_vtx := L.seq (L.List.length - 2); L.seq 0 = next_merge_vertex Am Bm last_vtx prev_vtx) := by
  -- === TCCs for merged_polygon_is_closed ===
  -- TCC: merged_polygon_is_closed_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_po...
  have merged_polygon_is_closed_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merged_polygon_is_closed_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         L: finseq[point_2d], last_vtx: point_2d):
  --   (Am, Bm) = inj...
  have merged_polygon_is_closed_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merged_polygon_is_closed_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         L: finseq[point_2d], last_vtx: point_2d, prev_vtx: point_2...
  have merged_polygon_is_closed_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merged_polygon_is_closed_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         L: finseq[point_2d], last_vtx: point_2d, prev_vtx: point_2...
  have merged_polygon_is_closed_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_path_props_edges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem merge_path_props_edges : True := by
  -- === TCCs for merge_path_props_edges ===
  -- TCC: merge_path_props_edges_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         L: finseq[point_2d], i: below(L`length), next: nat):
  --   (Am...
  have merge_path_props_edges_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_path_props_edges_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         L: finseq[point_2d], i: below(L`length), next: nat):
  --   (Am...
  have merge_path_props_edges_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merge_props.merge_seq_edges_do_not_cross
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem merge_seq_edges_do_not_cross : True := by
  -- === TCCs for merge_seq_edges_do_not_cross ===
  -- TCC: merge_seq_edges_do_not_cross_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d]):
  --   M = merge_seq(A, B) IMPLIES M`length >= 3
  have merge_seq_edges_do_not_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_edges_do_not_cross_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d]):
  --   M = merge_seq(A, B) IMPLIES uniq_vertex_list?(M`length)(M`seq)
  have merge_seq_edges_do_not_cross_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: merge_props.simple_merged_polygon
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET M = merge_seq(A, B) IN (# num_vertices := M`length, vertices := M`seq #)
axiom simple_merged_polygon {pvs_τ81 : Type*} {pvs_τ82 : Type*} : pvs_τ81 → pvs_τ82 → Prop

-- pvs2lean: merge_props.merge_seq_topleft_index_is_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   topleft_vertex_idx(simple_merged_polygon(A, B)) = 0
theorem merge_seq_topleft_index_is_0 : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), topleft_vertex_idx (simple_merged_polygon A B) = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.first_merge_vertex_in_one_injected_polygon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   LET v0 = first_vertex_merge(A, B),
--       (Am, Bm) = inject_vertices_into_polygon(A, B)
--     IN find_index(Am, v0) >= 0 OR find_index(B
theorem first_merge_vertex_in_one_injected_polygon : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (v0 : ℝ × ℝ) => fun (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) => find_index Am v0 ≥ 0 ∨ find_index Bm v0 ≥ 0 (inject_vertices_into_polygon A B) (first_vertex_merge A B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.first_two_merge_vertices
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
--         B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           v0: point_2d):
--     (Am, Bm
theorem first_two_merge_vertices : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (v0 : ℝ × ℝ), (Am, Bm) = inject_vertices_into_polygon A B ∧
 C = simple_merged_polygon A B ∧ v0 = first_vertex_merge A B → C.vertices 0 = v0 ∧
 C.vertices 1 = next_merge_vertex Am Bm v0 (prev_0 v0) := by
  -- === TCCs for first_two_merge_vertices ===
  -- TCC: first_two_merge_vertices_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
  --         B: (ccw_merge_pre_condition(A)), Am: ccw_simple_polygon,
  --         Bm: (ccw_merge_pre_condition(Am)), v0: point_2d):
  --   (Am, Bm) = inject_v...
  have first_two_merge_vertices_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_two_merge_vertices_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
  --         B: (ccw_merge_pre_condition(A)), Am: ccw_simple_polygon,
  --         Bm: (ccw_merge_pre_condition(Am)), v0: point_2d):
  --   (Am, Bm) = inject_v...
  have first_two_merge_vertices_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_two_merge_vertices_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
  --         B: (ccw_merge_pre_condition(A)), Am: ccw_simple_polygon,
  --         Bm: (ccw_merge_pre_condition(Am)), v0: point_2d):
  --   (Am, Bm) = inject_v...
  have first_two_merge_vertices_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.first_merged_edge_is_topleft_Am_or_Bm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
--         B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into
theorem first_merged_edge_is_topleft_Am_or_Bm : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧
 C = simple_merged_polygon A B → (let ti_Am := topleft_vertex_idx Am; let ti_Bm := topleft_vertex_idx Bm; (Am.vertices ti_Am = C.vertices 0 ∨
    Bm.vertices ti_Bm = C.vertices 0)
   ∧
   (edges_of_polygon Am(ti_Am) = edges_of_polygon C(0) ∨
     edges_of_polygon Bm(ti_Bm) = edges_of_polygon C(0))) := by
  -- === TCCs for first_merged_edge_is_topleft_Am_or_Bm ===
  -- TCC: first_merged_edge_is_topleft_Am_or_Bm_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
  --         B: (ccw_merge_pre_condition(A)), Am: ccw_simple_polygon,
  --         Bm: (ccw_merge_pre_condition(Am)),
  --         ti_Am:
  --           {i: below(A...
  have first_merged_edge_is_topleft_am_or_bm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_vertex_seq_full
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
--         B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into
theorem merge_vertex_seq_full : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧
 C = simple_merged_polygon A B → ∀ (i : Finset.range C.num_vertices), (let prev_vtx := seq_prev_vtx (merge_seq A B) i 1; C.vertices (next_index C i) =
     next_merge_vertex Am Bm (C.vertices i) prev_vtx) := by
  -- === TCCs for merge_vertex_seq_full ===
  -- TCC: merge_vertex_seq_full_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
  --         B: (ccw_merge_pre_condition(A)), Am: ccw_simple_polygon,
  --         Bm: (ccw_merge_pre_condition(Am))):
  --   (Am, Bm) = inject_vertices_into_p...
  have merge_vertex_seq_full_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_vertex_seq_full_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
  --         B: (ccw_merge_pre_condition(A)), Am: ccw_simple_polygon,
  --         Bm: (ccw_merge_pre_condition(Am)), i: below(C`num_vertices),
  --         pr...
  have merge_vertex_seq_full_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_path_edges_AB
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
--         B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into
theorem merge_path_edges_AB : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧
 C = simple_merged_polygon A B → ∀ (i : Finset.range C.num_vertices), (let S := edges_of_polygon C(i); edge_of_polygon_p Am(S) ∨ edge_of_polygon_p Bm(S)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.last_merged_edge_is_prev_topleft_Am_or_Bm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
--         B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into
theorem last_merged_edge_is_prev_topleft_Am_or_Bm : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧
 C = simple_merged_polygon A B → (let prev_i_Am := prev_index Am (topleft_vertex_idx Am); let prev_i_Bm := prev_index Bm (topleft_vertex_idx Bm); let last_i_C := C.num_vertices - 1; (Am.vertices prev_i_Am = C.vertices last_i_C ∨
    Bm.vertices prev_i_Bm = C.vertices last_i_C)
   ∧
   (edges_of_polygon Am(prev_i_Am) = edges_of_polygon C(last_i_C) ∨
     edges_of_polygon Bm(prev_i_Bm) = edges_of_polygon C(last_i_C))) := by
  -- === TCCs for last_merged_edge_is_prev_topleft_Am_or_Bm ===
  -- TCC: last_merged_edge_is_prev_topleft_Am_or_Bm_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, C: simple_polygon_2d,
  --         B: (ccw_merge_pre_condition(A)), Am: ccw_simple_polygon,
  --         Bm: (ccw_merge_pre_condition(Am)),
  --         prev_i_Am: below(Am`num_vert...
  have last_merged_edge_is_prev_topleft_am_or_bm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_seq_meets_ccw_condition_A_B
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      LE
theorem merge_seq_meets_ccw_condition_A_B : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → (let M := merge_seq A B; let C := simple_merged_polygon A B; let r := M.seq 0 - (# x := 1, y := 0 #); edge_of_polygon_p Am((# p1 := M.seq 0, p2 := M.seq 1 #)) ∧
   edge_of_polygon_p Bm((# p1 := M.seq (M.List.length - 1), p2 := M.seq 0 #))
   → point_between_edges_p True C 0(r)) := by
  -- === TCCs for merge_seq_meets_ccw_condition_A_B ===
  -- TCC: merge_seq_meets_ccw_condition_A_B_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], C: simple_polygon_2d):
  --   (Am, Bm) = i...
  have merge_seq_meets_ccw_condition_a_b_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_meets_ccw_condition_A_B_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], C: simple_polygon_2d, r: Vector):
  --   (...
  have merge_seq_meets_ccw_condition_a_b_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_meets_ccw_condition_A_B_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], C: simple_polygon_2d, r: Vector):
  --   (...
  have merge_seq_meets_ccw_condition_a_b_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_meets_ccw_condition_A_B_TCC4
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], C: simple_polygon_2d, r: Vector):
  --    ...
  have merge_seq_meets_ccw_condition_a_b_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_meets_ccw_condition_A_B_TCC5
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         M: finseq[point_2d], C: simple_polygon_2d, r: Vector):
  --    ...
  have merge_seq_meets_ccw_condition_a_b_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_seq_meets_ccw_condition
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   LET M = merge_seq(A, B),
--       C = (# num_vertices := M`length, vertices := M`seq #)
--     IN point_between_edges?(TRUE, C, 0)(M`seq(0)
theorem merge_seq_meets_ccw_condition : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (M : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) => fun (C : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < M.List.length} → ℝ × ℝ)) => point_between_edges_p True C 0 (M.seq 0 - (# x := 1, y := 0 #)) (# num_vertices := M.List.length) (vertices := M.seq #) (merge_seq A B) := by
  -- === TCCs for merge_seq_meets_ccw_condition ===
  -- TCC: merge_seq_meets_ccw_condition_TCC1
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d],
  --         C:
  --           [# num_vertices: nat,
  --              vertices: [below[M`length] -> point_2d] #]):
  --   M...
  have merge_seq_meets_ccw_condition_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_meets_ccw_condition_TCC2
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d],
  --         C:
  --           [# num_vertices: nat,
  --              vertices: [below[M`length] -> point_2d] #]):
  --   M...
  have merge_seq_meets_ccw_condition_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_meets_ccw_condition_TCC3
  -- Statement: FORALL (A: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         M: finseq[point_2d],
  --         C:
  --           [# num_vertices: nat,
  --              vertices: [below[M`length] -> point_2d] #]):
  --   M...
  have merge_seq_meets_ccw_condition_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merged_polygon
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: simple_merged_polygon(A, B)
axiom merged_polygon {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: merge_props.merged_polygon_has_finseq_prop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: merge_pred, A: ccw_simple_polygon,
--         B: (ccw_merge_pre_condition(A))):
--   LET L = merge_seq(A, B), M = merged_polygon(A, B) IN
--     P(L`length, L`seq) IMPLIES P(M`num_vertices, M`vertic
theorem merged_polygon_has_finseq_prop : ∀ (P : _ × ({s : {i : ℝ // i ≥ 0} // s < L} → ℝ × ℝ) → Bool) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (L : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) => fun (M : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) => P L.List.length L.seq → P M.num_vertices M.vertices (merged_polygon A B) (merge_seq A B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_only_vertex_intersections
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      C =
theorem merge_only_vertex_intersections : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧
 C = merged_polygon A B ∧
  (∀ (s : (edge_of_polygon_p C)), edge_of_polygon_p Am(s) ∨ edge_of_polygon_p Bm(s)) → (∀ (e : (edge_of_polygon_p Am)), only_vertex_intersections_p C Am e)
 ∧
 (∀ (e : (edge_of_polygon_p Bm)), only_vertex_intersections_p C Bm e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.no_outgoing_edges_in_noninitial_wedges
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      C =
theorem no_outgoing_edges_in_noninitial_wedges : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ C = merged_polygon A B → (∀ (i : Finset.range C.num_vertices) (e : (edge_of_polygon_p Am)), ¬ (C.vertices i = e.p1 ∧
         i > 0 ∧ point_between_edges_p True C i(e.p2)))
 ∧
 (∀ (i : Finset.range C.num_vertices) (e : (edge_of_polygon_p Bm)), ¬ (C.vertices i = e.p1 ∧
          i > 0 ∧ point_between_edges_p True C i(e.p2))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.last_edge_is_leftmost_subcase_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           e, h: segment_2d, r, q: point_2d):
--          ((Am, Bm)
theorem last_edge_is_leftmost_subcase_0 : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (r : ℝ × ℝ) (q : ℝ × ℝ), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ edge_of_polygon_p Bm(e) ∧ edge_of_polygon_p Bm(h)
 ∧ (C = simple_merged_polygon A B) ∧ (e.p2 = C.vertices 0)
 ∧ (h.p1 = C.vertices 0) ∧ point_on_segment_p r e
 ∧ point_between_edges_p True C 0(r) ∧ point_outside_polygon_p r C
 ∧ (norm (r - C.vertices 0) ≤ vertex_wedge_radius C)
 ∧ point_on_segment_p q h ∧ point_between_edges_p True C 0(q)
 ∧ point_outside_polygon_p q C → r = e.p2 ∨ q = h.p1 := by
  -- === TCCs for last_edge_is_leftmost_subcase_0 ===
  -- TCC: last_edge_is_leftmost_subcase_0_TCC1
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         e, h: segment_2d):
  --   (Am, Bm) = inject_vertices_into_po...
  have last_edge_is_leftmost_subcase_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.last_edge_is_leftmost_incoming_edge
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           s, e: segment_2d):
--          ((Am, Bm) = inject_vertic
theorem last_edge_is_leftmost_incoming_edge : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (C = merged_polygon A B)
 ∧ (edge_of_polygon_p Am(s) ∧ edge_of_polygon_p Bm(e) ∨
       edge_of_polygon_p Bm(s) ∧ edge_of_polygon_p Am(e))
 ∧ (s.p2 = e.p2) ∧ (s = edges_of_polygon C(C.num_vertices - 1)) → ¬ point_between_rays_p (reverse_segment e) (reverse_segment s)
                       (C.vertices 0 - (# x := 1, y := 0 #)) := by
  -- === TCCs for last_edge_is_leftmost_incoming_edge ===
  -- TCC: last_edge_is_leftmost_incoming_edge_TCC1
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         s, e: segment_2d):
  --   (Am, Bm) = inject_vertices_into_po...
  have last_edge_is_leftmost_incoming_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: last_edge_is_leftmost_incoming_edge_TCC2
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         s, e: segment_2d):
  --        ((Am, Bm) = inject_vertices_i...
  have last_edge_is_leftmost_incoming_edge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.no_outgoing_edges_case_i_eq_0_A
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           e: segment_2d, i, j: nat):
--          ((Am, Bm) = injec
theorem no_outgoing_edges_case_i_eq_0_A : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (C = merged_polygon A B) ∧ (i = 0) ∧ (i < C.num_vertices)
 ∧ (find_index Am (C.vertices i) = j) ∧ (j ≥ 0)
 ∧ (find_index Bm (C.vertices i) ≥ 0)
 ∧ (C.vertices (next_index C i) =
       Bm.vertices (next_index Bm (find_index Bm (C.vertices i))))
 ∧ (e = (# p1 := Am.vertices j, p2 := Am.vertices (next_index Am j) #))
 ∧ (C.vertices i = e.p1)
 ∧ point_between_rays_p (reverse_segment (edges_of_polygon C
                                                         (prev_index C i))) (edges_of_polygon C(i))
                        (e.p2) → point_between_rays_p (edges_of_polygon Bm(find_index Bm (C.vertices i))) (edges_of_polygon Am(j))
                   (seq_prev_vtx (merge_seq A B) i 1) := by
  -- === TCCs for no_outgoing_edges_case_i_eq_0_A ===
  -- TCC: no_outgoing_edges_case_i_eq_0_A_TCC1
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         e: segment_2d, i, j: nat):
  --        ((Am, Bm) = inject_ve...
  have no_outgoing_edges_case_i_eq_0_a_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_outgoing_edges_case_i_eq_0_A_TCC2
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         e: segment_2d, i, j: nat):
  --        ((Am, Bm) = inject_ve...
  have no_outgoing_edges_case_i_eq_0_a_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_outgoing_edges_case_i_eq_0_A_TCC3
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         e: segment_2d, i, j: nat):
  --        ((Am, Bm) = inject_ve...
  have no_outgoing_edges_case_i_eq_0_a_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.no_outgoing_edges_case_i_eq_0_B
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
--           e: segment_2d, i, j: nat):
--          ((Am, Bm) = injec
theorem no_outgoing_edges_case_i_eq_0_B : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (C = merged_polygon A B) ∧ (i = 0) ∧ (j ≥ 0)
 ∧ (find_index Am (C.vertices i) ≥ 0)
 ∧ (find_index Bm (C.vertices i) = j)
 ∧ point_between_rays_p (edges_of_polygon Bm(j)) (edges_of_polygon Am
                                         (find_index Am (C.vertices i)))
                        (seq_prev_vtx (merge_seq A B) i 1)
 ∧ (C.vertices (next_index C i) =
       Am.vertices (next_index Am (find_index Am (C.vertices i))))
 ∧ (e = (# p1 := Bm.vertices j, p2 := Bm.vertices (next_index Bm j) #))
 ∧ (C.vertices i = e.p1) → ¬ point_between_rays_p (reverse_segment (edges_of_polygon C
                                                        (prev_index C i))) (edges_of_polygon C(i))
                       (e.p2) := by
  -- === TCCs for no_outgoing_edges_case_i_eq_0_B ===
  -- TCC: no_outgoing_edges_case_i_eq_0_B_TCC1
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         e: segment_2d, i, j: nat):
  --   (Am, Bm) = inject_vertices...
  have no_outgoing_edges_case_i_eq_0_b_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_outgoing_edges_case_i_eq_0_B_TCC2
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         e: segment_2d, i, j: nat):
  --        ((Am, Bm) = inject_ve...
  have no_outgoing_edges_case_i_eq_0_b_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_outgoing_edges_case_i_eq_0_B_TCC3
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         e: segment_2d, i, j: nat):
  --        ((Am, Bm) = inject_ve...
  have no_outgoing_edges_case_i_eq_0_b_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_outgoing_edges_case_i_eq_0_B_TCC4
  -- Statement: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A)),
  --         Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am)),
  --         e: segment_2d, i, j: nat):
  --        ((Am, Bm) = inject_ve...
  have no_outgoing_edges_case_i_eq_0_b_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.no_outgoing_edges_in_outward_wedges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      C =
theorem no_outgoing_edges_in_outward_wedges : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ C = merged_polygon A B → (∀ (i : Finset.range C.num_vertices) (e : (edge_of_polygon_p Am)), ¬ (C.vertices i = e.p1 ∧ point_between_edges_p True C i(e.p2)))
 ∧
 (∀ (i : Finset.range C.num_vertices) (e : (edge_of_polygon_p Bm)), ¬ (C.vertices i = e.p1 ∧ point_between_edges_p True C i(e.p2))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.no_incoming_edges_in_outward_wedges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      C =
theorem no_incoming_edges_in_outward_wedges : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ C = merged_polygon A B → (∀ (i : Finset.range C.num_vertices) (e : (edge_of_polygon_p Am)), ¬ (C.vertices i = e.p2 ∧ point_between_edges_p True C i(e.p1)))
 ∧
 (∀ (i : Finset.range C.num_vertices) (e : (edge_of_polygon_p Bm)), ¬ (C.vertices i = e.p2 ∧ point_between_edges_p True C i(e.p1))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_no_edges_in_outward_wedges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      C =
theorem merge_no_edges_in_outward_wedges : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ C = merged_polygon A B → no_edges_in_outward_wedges_p C Am ∧ no_edges_in_outward_wedges_p C Bm := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merge_props.merge_intersecting_polygons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, B: (ccw_merge_pre_condition(A))):
--   FORALL (Am: ccw_simple_polygon, Bm: (ccw_merge_pre_condition(Am))):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      C =
theorem merge_intersecting_polygons : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧
 C = merged_polygon A B ∧
  (∀ (s : (edge_of_polygon_p C)), edge_of_polygon_p Am(s) ∨ edge_of_polygon_p Bm(s)) → (∃ (q : point_2d), point_on_polygon_perimeter_p Am(q) ∧
    (point_on_polygon_perimeter_p C(q) ∨ point_in_polygon_p q C))
 ∧
 (∃ (q : point_2d), point_on_polygon_perimeter_p Bm(q) ∧
     (point_on_polygon_perimeter_p C(q) ∨ point_in_polygon_p q C)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.merge_props