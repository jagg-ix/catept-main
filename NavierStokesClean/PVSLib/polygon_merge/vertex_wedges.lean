-- Auto-generated by pvs2lean/translator.py
-- PVS theory: vertex_wedges
-- PVSLib library: polygon_merge
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: vertex_wedges

Auto-generated stubs from PVSLib theory `vertex_wedges`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.vertex_wedges

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (between_rays_p : pvs_τ3 → pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (cross : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (dir_parallel_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (edge_of_polygon_p : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (edges_of_polygon : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} (empty_p : pvs_τ14 → Prop)
variable {pvs_τ19 : Type*} (injective_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (length : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (min : pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} (min_edge_sep : pvs_τ24 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (next_arb_intersection_p : pvs_τ28 → pvs_τ29 → pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (next_index : pvs_τ31 → pvs_τ32 → pvs_τ33)
variable {pvs_τ36 : Type*} (norm : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (only_vertex_intersections_p : pvs_τ37 → pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (parallel_p : pvs_τ40 → pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} (perp_left : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (perp_right : pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (point_between_edges_p : pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} (point_in_polygon_p : pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} (point_on_arb_ray_p : pvs_τ55 → pvs_τ56 → pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (point_on_polygon_perimeter_p : pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (point_on_segment_p : pvs_τ60 → pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (point_outside_polygon_p : pvs_τ62 → pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (polygon_contained_p : pvs_τ64 → pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} (prev_index : pvs_τ66 → pvs_τ67 → pvs_τ68)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} (rem : pvs_τ69 → pvs_τ70 → pvs_τ71)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} (reverse_segment : pvs_τ72 → pvs_τ73)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} (segment_endpoint_p : pvs_τ74 → pvs_τ75 → Prop)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} (sines_of_edge_angles : pvs_τ76 → pvs_τ77)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} (sq : pvs_τ78 → pvs_τ79)
variable {pvs_τ80 : Type*} (surjective_p : pvs_τ80 → Prop)
variable {pvs_τ81 : Type*} {pvs_τ82 : Type*} (topleft_vertex_idx : pvs_τ81 → pvs_τ82)
variable {pvs_τ89 : Type*} (vertices : pvs_τ89 → Prop)

-- pvs2lean: vertex_wedges.vertex_wedge_radius
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min_edge_sep(G) / 3
axiom vertex_wedge_radius {pvs_τ88 : Type*} : pvs_τ88 → Prop

-- pvs2lean: vertex_wedges.point_in_vertex_wedge?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: point_between_edges?(outward, G, i)(p) AND
--  norm(p - G`vertices(i)) <= vertex_wedge_radius(G)
axiom point_in_vertex_wedge_p {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} : pvs_τ51 → pvs_τ52 → pvs_τ53 → pvs_τ54 → Prop

-- pvs2lean: vertex_wedges.vertex_bisect_vector
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET v = G`vertices(i),
--     u = G`vertices(prev_index(G, i)),
--     w = G`vertices(next_index(G, i)),
--     uv = u - v,
--     wv = w - v,
--     c = cross(uv, wv),
--     a = norm(wv) * uv + norm(uv) * wv
--   IN IF 
axiom vertex_bisect_vector {pvs_τ83 : Type*} {pvs_τ84 : Type*} : pvs_τ83 → pvs_τ84 → Prop

-- pvs2lean: vertex_wedges.vertex_bisect_vector_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   vertex_bisect_vector(G, i) /= zero
theorem vertex_bisect_vector_nonzero : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), vertex_bisect_vector G i ≠ zero := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.vertex_wedge_apex
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET v = G`vertices(i),
--     d = vertex_wedge_radius(G),
--     b = ^(vertex_bisect_vector(G, i))
--   IN v + d * (IF outward THEN b ELSE -b ENDIF)
axiom vertex_wedge_apex {pvs_τ85 : Type*} {pvs_τ86 : Type*} {pvs_τ87 : Type*} : pvs_τ85 → pvs_τ86 → pvs_τ87 → Prop

-- pvs2lean: vertex_wedges.wedge_connector_sine
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min(sines_of_edge_angles(G)) / 2
axiom wedge_connector_sine {pvs_τ93 : Type*} : pvs_τ93 → Prop

-- pvs2lean: vertex_wedges.wedge_connector_sine_sq_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d): 1 > sq(wedge_connector_sine(G))
theorem wedge_connector_sine_sq_bound : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), 1 > (wedge_connector_sine G^2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.wedge_connector_offset
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vertex_wedge_radius(G) * wedge_connector_sine(G)
noncomputable def wedge_connector_offset : sorry := by
  sorry  -- pvs2lean: definition stub for vertex_wedge_radius(G) * wedge_connector_sine(G)

-- pvs2lean: vertex_wedges.wedge_connector_endpoint
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET u = G`vertices(i),
--     v = G`vertices(next_index(G, i)),
--     r = vertex_wedge_radius(G),
--     sin_a = wedge_connector_sine(G),
--     cos_a = sqrt(1 - sq(sin_a)),
--     perp_vect = r * sin_a * ^(perp_le
axiom wedge_connector_endpoint {pvs_τ90 : Type*} {pvs_τ91 : Type*} {pvs_τ92 : Type*} : pvs_τ90 → pvs_τ91 → pvs_τ92 → Prop

-- pvs2lean: vertex_wedges.no_edges_in_outward_wedges?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(G`num_vertices)):
--   LET v = G`vertices(i) IN
--     FORALL (e: (edge_of_polygon?(H))):
--       NOT (v = e`p1 AND point_between_edges?(TRUE, G, i)(e`p2) OR
--             v = e`p2 AND point_be
axiom no_edges_in_outward_wedges_p {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ34 → pvs_τ35 → Prop

-- pvs2lean: vertex_wedges.index_from_offset
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rem(G`num_vertices)(i + offset)
axiom index_from_offset {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: vertex_wedges.index_from_offset_injective
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, offset: below(G`num_vertices)):
--   injective?(index_from_offset(G, offset))
theorem index_from_offset_injective : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (offset : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), injective_p (index_from_offset G offset) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.index_from_offset_surjective
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, offset: below(G`num_vertices)):
--   surjective?(index_from_offset(G, offset))
theorem index_from_offset_surjective : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (offset : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), surjective_p (index_from_offset G offset) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.in_out_on_segment_have_crossing_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p, q: point_2d, e: segment_2d):
--   point_in_polygon?(p, G) AND
--    point_outside_polygon?(q, G) AND
--     point_on_segment?(p, e) AND point_on_segment?(q, e)
--    IMPLIES
--    EX
theorem in_out_on_segment_have_crossing_between : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_in_polygon_p p G ∧
 point_outside_polygon_p q G ∧
  point_on_segment_p p e ∧ point_on_segment_p q e → ∃ (v : point_2d), point_on_segment_p v e ∧
   point_on_polygon_perimeter_p G(v) ∧
    0 < norm (v - p) ∧ norm (v - p) < norm (q - p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.in_out_have_crossing_on_subsegment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem in_out_have_crossing_on_subsegment : True := by
  -- === TCCs for in_out_have_crossing_on_subsegment ===
  -- TCC: in_out_have_crossing_on_subsegment_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p, q: point_2d):
  --   NOT point_outside_polygon?(p, G) AND point_outside_polygon?(q, G) IMPLIES
  --    q /= p
  have in_out_have_crossing_on_subsegment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.nearest_perimeter_offset
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom nearest_perimeter_offset {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ25 → pvs_τ26 → pvs_τ27 → Prop

-- pvs2lean: vertex_wedges.nearest_perimeter_offset_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p, q: point_2d, e: segment_2d):
--        (NOT point_outside_polygon?(p, G)) AND point_outside_polygon?(q, G)
--    AND point_on_segment?(p, e) AND point_on_segment?(q, e) AND 
theorem nearest_perimeter_offset_bound : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (¬ point_outside_polygon_p p G) ∧ point_outside_polygon_p q G
 ∧ point_on_segment_p p e ∧ point_on_segment_p q e ∧ (p ≠ q) → nearest_perimeter_offset G(p, q) ≤ norm (p - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.between_points_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, G: simple_polygon_2d, e: segment_2d):
--   NOT point_outside_polygon?(p, G) AND
--    point_outside_polygon?(q, G) AND
--     point_on_segment?(p, e) AND point_on_segment?(q, e)
--    IMPL
theorem between_points_on_perimeter : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ point_outside_polygon_p p G ∧
 point_outside_polygon_p q G ∧
  point_on_segment_p p e ∧ point_on_segment_p q e → ∃ (v : point_2d) (i : Finset.range G.num_vertices), point_on_segment_p v (edges_of_polygon G(i)) ∧
   point_on_segment_p v e ∧
    ∀ (u : point_2d) (d : {x : ℝ // 0 < x}), u = v + d * (q - v) ∧ d ≤ 1 → point_outside_polygon_p u G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.extend_points_between_edges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d, outward: bool, G: simple_polygon_2d,
--         i: below(G`num_vertices)):
--   e`p1 = G`vertices(i) AND
--    p /= e`p1 AND
--     point_between_edges?(outward, G, i)(p) AND p
theorem extend_points_between_edges : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (outward : Bool) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), e.p1 = G.vertices i ∧
 p ≠ e.p1 ∧
  point_between_edges_p outward G i(p) ∧ point_on_segment_p p e → ∀ (q : point_2d), point_on_segment_p q e ∧ q ≠ e.p1 →
   point_between_edges_p outward G i(q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.extend_points_between_edges_ray
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d, outward: bool, G: simple_polygon_2d,
--         i: below(G`num_vertices)):
--   e`p1 = G`vertices(i) AND
--    p /= e`p1 AND
--     point_between_edges?(outward, G, i)(p) AND
--  
theorem extend_points_between_edges_ray : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (outward : Bool) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), e.p1 = G.vertices i ∧
 p ≠ e.p1 ∧
  point_between_edges_p outward G i(p) ∧
   point_on_arb_ray_p p e.p1 (^(e.p2 - e.p1)) → ∀ (q : point_2d), point_on_arb_ray_p q e.p1 (^(e.p2 - e.p1)) ∧ q ≠ e.p1 →
   point_between_edges_p outward G i(q) := by
  -- === TCCs for extend_points_between_edges_ray ===
  -- TCC: extend_points_between_edges_ray_TCC1
  -- Statement: FORALL (p: point_2d, e: segment_2d, outward: bool, G: simple_polygon_2d,
  --         i: below(G`num_vertices)):
  --   e`p1 = G`vertices(i) AND
  --    p /= e`p1 AND point_between_edges?(outward, G, i)(p)
  --    IMP...
  have extend_points_between_edges_ray_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.all_interior_points_inside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q: point_2d, G, H: ccw_simple_polygon, e: (edge_of_polygon?(H))):
--   only_vertex_intersections?(G, H, e) AND
--    point_in_polygon?(q, G) AND
--     point_on_segment?(q, e) AND NOT segment_endpoint?
theorem all_interior_points_inside : ∀ (q : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (H : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), only_vertex_intersections_p G H e ∧
 point_in_polygon_p q G ∧
  point_on_segment_p q e ∧ ¬ segment_endpoint_p e(q) → ∀ (p : point_2d), point_on_segment_p p e ∧ ¬ segment_endpoint_p e(p) →
   point_in_polygon_p p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.opposite_wedge_apex_point
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), outward: bool):
--   vertex_wedge_apex(NOT outward, G, i) =
--    2 * G`vertices(i) - vertex_wedge_apex(outward, G, i)
theorem opposite_wedge_apex_point : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (outward : Bool), vertex_wedge_apex (¬ outward) G i = 2 * G.vertices i - vertex_wedge_apex outward G i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.opposite_points_between_narrow
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p, v: point_2d):
--   cross(s`p1 - v, e`p2 - v) >= 0 AND
--    v = s`p2 AND v = e`p1 AND between_rays?(s, e)(p)
--    IMPLIES between_rays?(reverse_segment(e), reverse_segment(s))(2 *
theorem opposite_points_between_narrow : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (v : ℝ × ℝ), cross (s.p1 - v) (e.p2 - v) ≥ 0 ∧
 v = s.p2 ∧ v = e.p1 ∧ between_rays_p s e(p) → between_rays_p (reverse_segment e) (reverse_segment s) (2 * v - p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.opposite_points_between_wide
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p, v: point_2d):
--   cross(s`p1 - v, e`p2 - v) < 0 AND
--    v = s`p2 AND
--     v = e`p1 AND
--      between_rays?((# p1 := 2 * v - e`p2, p2 := v #),
--                    (# p1 := v, p2 
theorem opposite_points_between_wide : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (v : ℝ × ℝ), cross (s.p1 - v) (e.p2 - v) < 0 ∧
 v = s.p2 ∧
  v = e.p1 ∧
   between_rays_p ((# p1 := 2 * v - e.p2, p2 := v #)) ((# p1 := v, p2 := 2 * v - s.p1 #))
                (p) → between_rays_p (reverse_segment e) (reverse_segment s) (2 * v - p) := by
  -- === TCCs for opposite_points_between_wide ===
  -- TCC: opposite_points_between_wide_TCC1
  -- Statement: FORALL (s, e: segment_2d, v: point_2d):
  --   cross(s`p1 - v, e`p2 - v) < 0 AND v = s`p2 AND v = e`p1 IMPLIES
  --    v /= 2 * v - e`p2
  have opposite_points_between_wide_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: opposite_points_between_wide_TCC2
  -- Statement: FORALL (s, e: segment_2d, v: point_2d):
  --   cross(s`p1 - v, e`p2 - v) < 0 AND v = s`p2 AND v = e`p1 IMPLIES
  --    2 * v - s`p1 /= v
  have opposite_points_between_wide_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.cross_wide_opposite_narrow
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d):
--   cross(u - v, w - v) < 0 IMPLIES cross(v - w, v - u) > 0
theorem cross_wide_opposite_narrow : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), cross (u - v) (w - v) < 0 → cross (v - w) (v - u) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.cross_wide_opposite_vertex_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d):
--   cross(u - v, w - v) < 0 IMPLIES
--    between_rays?((# p1 := u, p2 := v #), (# p1 := v, p2 := 2 * v - u #))
--                 (2 * v - w)
theorem cross_wide_opposite_vertex_between : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), cross (u - v) (w - v) < 0 → between_rays_p ((# p1 := u, p2 := v #)) ((# p1 := v, p2 := 2 * v - u #))
             (2 * v - w) := by
  -- === TCCs for cross_wide_opposite_vertex_between ===
  -- TCC: cross_wide_opposite_vertex_between_TCC1
  -- Statement: FORALL (u, v, w: point_2d): cross(u - v, w - v) < 0 IMPLIES v /= u
  have cross_wide_opposite_vertex_between_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cross_wide_opposite_vertex_between_TCC2
  -- Statement: FORALL (u, v, w: point_2d): cross(u - v, w - v) < 0 IMPLIES 2 * v - u /= v
  have cross_wide_opposite_vertex_between_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.cross_wide_point_between_opposite_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q, u, v, w: point_2d):
--   cross(u - v, w - v) < 0 AND
--    between_rays?((# p1 := 2 * v - w, p2 := v #),
--                  (# p1 := v, p2 := 2 * v - u #))
--                 (q)
--    IMPLIES
--    between
theorem cross_wide_point_between_opposite_1 : ∀ (q : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), cross (u - v) (w - v) < 0 ∧
 between_rays_p ((# p1 := 2 * v - w, p2 := v #)) ((# p1 := v, p2 := 2 * v - u #))
              (q) → between_rays_p ((# p1 := u, p2 := v #)) ((# p1 := v, p2 := 2 * v - u #)) (q) := by
  -- === TCCs for cross_wide_point_between_opposite_1 ===
  -- TCC: cross_wide_point_between_opposite_1_TCC1
  -- Statement: FORALL (u, v, w: point_2d): cross(u - v, w - v) < 0 IMPLIES v /= 2 * v - w
  have cross_wide_point_between_opposite_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.cross_wide_point_between_opposite_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q, u, v, w: point_2d):
--   cross(u - v, w - v) < 0 AND
--    between_rays?((# p1 := u, p2 := v #), (# p1 := v, p2 := 2 * v - u #))(q)
--    IMPLIES between_rays?((# p1 := u, p2 := v #), (# p1 := v, p2
theorem cross_wide_point_between_opposite_2 : ∀ (q : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), cross (u - v) (w - v) < 0 ∧
 between_rays_p ((# p1 := u, p2 := v #)) ((# p1 := v, p2 := 2 * v - u #)) (q) → between_rays_p ((# p1 := u, p2 := v #)) ((# p1 := v, p2 := w #)) (q) := by
  -- === TCCs for cross_wide_point_between_opposite_2 ===
  -- TCC: cross_wide_point_between_opposite_2_TCC1
  -- Statement: FORALL (q, u, v, w: point_2d):
  --   cross(u - v, w - v) < 0 AND
  --    between_rays?((# p1 := u, p2 := v #), (# p1 := v, p2 := 2 * v - u #))(q)
  --    IMPLIES w /= v
  have cross_wide_point_between_opposite_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.cross_wide_point_between_opposite_3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q, u, v, w: point_2d):
--   cross(u - v, w - v) < 0 AND
--    between_rays?((# p1 := 2 * v - w, p2 := v #), (# p1 := v, p2 := w #))(q)
--    IMPLIES between_rays?((# p1 := u, p2 := v #), (# p1 := v, p2
theorem cross_wide_point_between_opposite_3 : ∀ (q : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), cross (u - v) (w - v) < 0 ∧
 between_rays_p ((# p1 := 2 * v - w, p2 := v #)) ((# p1 := v, p2 := w #)) (q) → between_rays_p ((# p1 := u, p2 := v #)) ((# p1 := v, p2 := w #)) (q) := by
  -- === TCCs for cross_wide_point_between_opposite_3 ===
  -- TCC: cross_wide_point_between_opposite_3_TCC1
  -- Statement: FORALL (u, v, w: point_2d): cross(u - v, w - v) < 0 IMPLIES w /= v
  have cross_wide_point_between_opposite_3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.cross_wide_point_between_opposite_4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q, u, v, w: point_2d):
--   cross(u - v, w - v) < 0 AND
--    between_rays?((# p1 := 2 * v - w, p2 := v #),
--                  (# p1 := v, p2 := 2 * v - u #))
--                 (q)
--    IMPLIES
--    between
theorem cross_wide_point_between_opposite_4 : ∀ (q : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), cross (u - v) (w - v) < 0 ∧
 between_rays_p ((# p1 := 2 * v - w, p2 := v #)) ((# p1 := v, p2 := 2 * v - u #))
              (q) → between_rays_p ((# p1 := 2 * v - w, p2 := v #)) ((# p1 := v, p2 := w #)) (q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.cross_wide_point_between_opposite_5
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q, u, v, w: point_2d):
--   cross(u - v, w - v) < 0 AND
--    between_rays?((# p1 := u, p2 := v #), (# p1 := v, p2 := w #))(q)
--    IMPLIES
--    between_rays?((# p1 := 2 * v - w, p2 := v #), (# p1 := v,
theorem cross_wide_point_between_opposite_5 : ∀ (q : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), cross (u - v) (w - v) < 0 ∧
 between_rays_p ((# p1 := u, p2 := v #)) ((# p1 := v, p2 := w #)) (q) → between_rays_p ((# p1 := 2 * v - w, p2 := v #)) ((# p1 := v, p2 := w #)) (q) ∨
 between_rays_p ((# p1 := u, p2 := v #)) ((# p1 := v, p2 := 2 * v - u #)) (q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.weighted_vector_sum_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, a, b: posreal):
--   LET u = s`p1, v = e`p1, w = e`p2, uv = u - v, wv = w - v IN
--     cross(uv, wv) /= 0 IMPLIES
--      IF cross(uv, wv) < 0 THEN between_rays?(s, e)(v - b * uv - a
theorem weighted_vector_sum_between : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (a : {x : {x : ℝ // x ≥ 0} // x > 0}) (b : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : {p : ℝ × ℝ // p ≠ e.p1}) => fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => cross uv wv ≠ 0 → if cross uv wv < 0 then between_rays_p s e(v - b * uv - a * wv)
else between_rays_p s e(v + b * uv + a * wv) (w - v) (u - v) e.p2 e.p1 s.p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.weighted_vector_sum_between_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, a, b: negreal):
--   LET u = s`p1, v = e`p1, w = e`p2, uv = u - v, wv = w - v IN
--     cross(uv, wv) /= 0 IMPLIES
--      IF cross(uv, wv) < 0 THEN between_rays?(s, e)(v + b * uv + a
theorem weighted_vector_sum_between_neg : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (a : {x : {x : ℝ // x ≤ 0} // x < 0}) (b : {x : {x : ℝ // x ≤ 0} // x < 0}), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : {p : ℝ × ℝ // p ≠ e.p1}) => fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => cross uv wv ≠ 0 → if cross uv wv < 0 then between_rays_p s e(v + b * uv + a * wv)
else between_rays_p s e(v - b * uv - a * wv) (w - v) (u - v) e.p2 e.p1 s.p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.bisect_vector_between
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   LET s = edges_of_polygon(G)(prev_index(G, i)),
--       e = edges_of_polygon(G)(i),
--       bv = vertex_bisect_vector(G, i)
--     IN between_rays?(s
theorem bisect_vector_between : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (bv : ℝ × ℝ) => between_rays_p s e (e.p1 + bv) (vertex_bisect_vector G i) (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.bisect_vector_between_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   LET s = edges_of_polygon(G)(prev_index(G, i)),
--       e = edges_of_polygon(G)(i),
--       bv = vertex_bisect_vector(G, i)
--     IN between_rays?(r
theorem bisect_vector_between_rev : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (bv : ℝ × ℝ) => between_rays_p (reverse_segment e) (reverse_segment s) (e.p1 - bv) (vertex_bisect_vector G i) (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.apex_point_neq_next_apex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), outward: bool):
--   vertex_wedge_apex(outward, G, i) /=
--    vertex_wedge_apex(outward, G, next_index(G, i))
theorem apex_point_neq_next_apex : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (outward : Bool), vertex_wedge_apex outward G i ≠ vertex_wedge_apex outward G (next_index G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_endpoints_neq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   wedge_connector_endpoint(G, i, TRUE) /=
--    wedge_connector_endpoint(G, i, FALSE)
theorem connector_endpoints_neq : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), wedge_connector_endpoint G i True ≠ wedge_connector_endpoint G i False := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_segment
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# p1 := wedge_connector_endpoint(G, i, TRUE),
--    p2 := wedge_connector_endpoint(G, i, FALSE) #)
noncomputable def connector_segment : sorry := by
  sorry  -- pvs2lean: definition stub for (# p1 := wedge_connector_endpoint(G, i, TRUE),    p2 := wedge_connector_endpoint

-- pvs2lean: vertex_wedges.acute_angle_coeff_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (uv, wv: Nz_vector, a, b, normu, normw: posreal):
--   a * (normu * normw) < cross(uv, wv) AND b * (normu * normw) > uv * wv
--    IMPLIES
--    a * cross(uv, perp_left(wv)) + b * cross(uv, wv) > 0 OR
--  
theorem acute_angle_coeff_between : ∀ (uv : {v : ℝ × ℝ // v ≠ zero}) (wv : {v : ℝ × ℝ // v ≠ zero}) (a : {x : {x : ℝ // x ≥ 0} // x > 0}) (b : {x : {x : ℝ // x ≥ 0} // x > 0}) (normu : {x : {x : ℝ // x ≥ 0} // x > 0}) (normw : {x : {x : ℝ // x ≥ 0} // x > 0}), a * (normu * normw) < cross uv wv ∧ b * (normu * normw) > uv * wv → a * cross uv (perp_left wv) + b * cross uv wv > 0 ∨
 cross uv wv < 0 ∨ uv * wv < 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.pos_coeff_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, a, b: posreal):
--   LET u = s`p1, v = e`p1, w = e`p2, uv = u - v, wv = w - v IN
--     u /= w AND
--      s`p2 = e`p1 AND
--       (cross(uv, wv) < 0 OR
--         uv * wv < 0 OR (a < cros
theorem pos_coeff_between : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (a : {x : {x : ℝ // x ≥ 0} // x > 0}) (b : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : {p : ℝ × ℝ // p ≠ e.p1}) => fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => u ≠ w ∧
 s.p2 = e.p1 ∧
  (cross uv wv < 0 ∨
    uv * wv < 0 ∨ (a < cross ^(uv) ^(wv) ∧ b > ^(uv) * ^(wv))) → between_rays_p s e(v + a * ^(perp_left wv) + b * ^(wv)) (w - v) (u - v) e.p2 e.p1 s.p1 := by
  -- === TCCs for pos_coeff_between ===
  -- TCC: pos_coeff_between_TCC1
  -- Statement: FORALL (s, e: segment_2d, u: point_2d, v: point_2d,
  --         w: {p: point_2d | p /= e`p1}, uv: Vector, wv: Vector):
  --        (u = s`p1) AND (v = e`p1) AND (w = e`p2) AND (uv = u - v)
  --    AND (wv = w - ...
  have pos_coeff_between_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: pos_coeff_between_TCC2
  -- Statement: FORALL (s, e: segment_2d, u: point_2d, v: point_2d,
  --         w: {p: point_2d | p /= e`p1}, uv: Vector, wv: Vector):
  --        (u = s`p1) AND (v = e`p1) AND (w = e`p2) AND (uv = u - v)
  --    AND (wv = w - ...
  have pos_coeff_between_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: pos_coeff_between_TCC3
  -- Statement: FORALL (s, e: segment_2d, a, b: posreal, u: point_2d, v: point_2d,
  --         w: {p: point_2d | p /= e`p1}, uv: Vector, wv: Vector):
  --        (u = s`p1) AND (v = e`p1) AND (w = e`p2) AND (uv = u - v)
  --   ...
  have pos_coeff_between_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: pos_coeff_between_TCC4
  -- Statement: FORALL (s, e: segment_2d, a, b: posreal, u: point_2d, v: point_2d,
  --         w: {p: point_2d | p /= e`p1}, uv: Vector, wv: Vector):
  --        (u = s`p1) AND (v = e`p1) AND (w = e`p2) AND (uv = u - v)
  --   ...
  have pos_coeff_between_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.neg_coeff_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, a, b: posreal):
--   LET u = s`p1, v = e`p1, w = e`p2, uv = u - v, wv = w - v IN
--     u /= w AND
--      s`p2 = e`p1 AND
--       (cross(uv, wv) < 0 OR
--         uv * wv < 0 OR (a < cros
theorem neg_coeff_between : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (a : {x : {x : ℝ // x ≥ 0} // x > 0}) (b : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : {p : ℝ × ℝ // p ≠ e.p1}) => fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => u ≠ w ∧
 s.p2 = e.p1 ∧
  (cross uv wv < 0 ∨
    uv * wv < 0 ∨ (a < cross ^(uv) ^(wv) ∧ b > ^(uv) * ^(wv))) → between_rays_p s e(v + b * ^(uv) - a * ^(perp_left uv)) (w - v) (u - v) e.p2 e.p1 s.p1 := by
  -- === TCCs for neg_coeff_between ===
  -- TCC: neg_coeff_between_TCC1
  -- Statement: FORALL (s, e: segment_2d, a, b: posreal, u: point_2d, v: point_2d,
  --         w: {p: point_2d | p /= e`p1}, uv: Vector, wv: Vector):
  --        (u = s`p1) AND (v = e`p1) AND (w = e`p2) AND (uv = u - v)
  --   ...
  have neg_coeff_between_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: neg_coeff_between_TCC2
  -- Statement: FORALL (s, e: segment_2d, a, b: posreal, u: point_2d, v: point_2d,
  --         w: {p: point_2d | p /= e`p1}, uv: Vector, wv: Vector):
  --        (u = s`p1) AND (v = e`p1) AND (w = e`p2) AND (uv = u - v)
  --   ...
  have neg_coeff_between_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_endpoint_between
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem connector_endpoint_between : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.apex_point_between
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), outward: bool):
--   point_between_edges?(outward, G, i)(vertex_wedge_apex(outward, G, i))
theorem apex_point_between : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (outward : Bool), point_between_edges_p outward G i (vertex_wedge_apex outward G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.apex_point_between_rev_out
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   LET s = edges_of_polygon(G)(prev_index(G, i)),
--       e = edges_of_polygon(G)(i),
--       p = vertex_wedge_apex(TRUE, G, i),
--       v = e`p1
--     
theorem apex_point_between_rev_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (p : ℝ × ℝ) => fun (v : ℝ × ℝ) => cross (s.p1 - v) (e.p2 - v) < 0 → between_rays_p ((# p1 := 2 * v - e.p2, p2 := v #)) ((# p1 := v, p2 := 2 * v - s.p1 #))
             (p) e.p1 (vertex_wedge_apex True G i) (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  -- === TCCs for apex_point_between_rev_out ===
  -- TCC: apex_point_between_rev_out_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), s: segment_2d,
  --         e: segment_2d, p: point_2d, v: point_2d):
  --        (s = edges_of_polygon(G)(prev_index(G, i)))
  --    AND (e = edges_of_poly...
  have apex_point_between_rev_out_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: apex_point_between_rev_out_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), s: segment_2d,
  --         e: segment_2d, p: point_2d, v: point_2d):
  --        (s = edges_of_polygon(G)(prev_index(G, i)))
  --    AND (e = edges_of_poly...
  have apex_point_between_rev_out_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.apex_point_between_rev_in
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   LET s = edges_of_polygon(G)(prev_index(G, i)),
--       e = edges_of_polygon(G)(i),
--       p = vertex_wedge_apex(FALSE, G, i),
--       v = e`p1
--    
theorem apex_point_between_rev_in : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (p : ℝ × ℝ) => fun (v : ℝ × ℝ) => cross (s.p1 - v) (e.p2 - v) > 0 → between_rays_p ((# p1 := 2 * v - s.p1, p2 := v #)) ((# p1 := v, p2 := 2 * v - e.p2 #))
             (p) e.p1 (vertex_wedge_apex False G i) (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  -- === TCCs for apex_point_between_rev_in ===
  -- TCC: apex_point_between_rev_in_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), s: segment_2d,
  --         e: segment_2d, p: point_2d, v: point_2d):
  --        (s = edges_of_polygon(G)(prev_index(G, i)))
  --    AND (e = edges_of_poly...
  have apex_point_between_rev_in_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: apex_point_between_rev_in_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), s: segment_2d,
  --         e: segment_2d, p: point_2d, v: point_2d):
  --        (s = edges_of_polygon(G)(prev_index(G, i)))
  --    AND (e = edges_of_poly...
  have apex_point_between_rev_in_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.opposite_apex_point_between
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, v: point_2d, G: simple_polygon_2d, i: below(G`num_vertices)):
--   LET s = edges_of_polygon(G)(prev_index(G, i)), e = edges_of_polygon(G)(i)
--     IN
--     v = e`p1 AND p = vertex_wedge_apex(TRUE,
theorem opposite_apex_point_between : ∀ (p : ℝ × ℝ) (v : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => v = e.p1 ∧ p = vertex_wedge_apex True G i → between_rays_p (reverse_segment e) (reverse_segment s) (2 * v - p) (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.in_wedge_or_on_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices)):
--   norm(p - G`vertices(i)) <= vertex_wedge_radius(G) IMPLIES
--    point_in_vertex_wedge?(TRUE, G, i)(p) OR
--     point_in_vertex_wedge
theorem in_wedge_or_on_edge : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), norm (p - G.vertices i) ≤ vertex_wedge_radius G → point_in_vertex_wedge_p True G i(p) ∨
 point_in_vertex_wedge_p False G i(p) ∨
  point_on_segment_p p (edges_of_polygon G(i)) ∨
   point_on_segment_p p (edges_of_polygon G(prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.wedge_points_not_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices),
--         outward: bool):
--   point_in_vertex_wedge?(outward, G, i)(p) IMPLIES
--    NOT point_on_polygon_perimeter?(G)(p)
theorem wedge_points_not_on_perimeter : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (outward : Bool), point_in_vertex_wedge_p outward G i(p) → ¬ point_on_polygon_perimeter_p G(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.apex_in_wedge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices), outward: bool):
--   point_in_vertex_wedge?(outward, G, i)(vertex_wedge_apex(outward, G, i))
theorem apex_in_wedge : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (outward : Bool), point_in_vertex_wedge_p outward G i (vertex_wedge_apex outward G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_endpoint_in_wedge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem connector_endpoint_in_wedge : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.no_intersections_with_prev_edge_wide
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, s, e: segment_2d):
--        (cross(s`p1 - e`p1, e`p2 - e`p1) < 0) AND (s`p2 = e`p1) AND (p /= q)
--    AND (cross(e`p2 - e`p1, p - e`p1) < 0) AND parallel?(e`p2 - e`p1, q - p)
--    IM
theorem no_intersections_with_prev_edge_wide : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (cross (s.p1 - e.p1) (e.p2 - e.p1) < 0) ∧ (s.p2 = e.p1) ∧ (p ≠ q)
 ∧ (cross (e.p2 - e.p1) (p - e.p1) < 0) ∧ parallel_p (e.p2 - e.p1) (q - p) → ∀ (r : point_2d), point_on_segment_p r ((# p1 := p, p2 := q #)) →
   ¬ point_on_segment_p r s := by
  -- === TCCs for no_intersections_with_prev_edge_wide ===
  -- TCC: no_intersections_with_prev_edge_wide_TCC1
  -- Statement: FORALL (p, q: point_2d, s, e: segment_2d):
  --        (cross(s`p1 - e`p1, e`p2 - e`p1) < 0) AND (s`p2 = e`p1) AND (p /= q)
  --    AND (cross(e`p2 - e`p1, p - e`p1) < 0) AND parallel?(e`p2 - e`p1, q - p)
  --   ...
  have no_intersections_with_prev_edge_wide_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.other_end_of_parallel_segment_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, s, e: segment_2d):
--        (s`p2 = e`p1) AND (p /= q)
--    AND (NOT dir_parallel?(e`p1 - e`p2, s`p2 - s`p1))
--    AND (cross(p - e`p1, q - e`p1) > 0) AND between_rays?(s, e)(p)
--    A
theorem other_end_of_parallel_segment_between : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p2 = e.p1) ∧ (p ≠ q)
 ∧ (¬ dir_parallel_p (e.p1 - e.p2) (s.p2 - s.p1))
 ∧ (cross (p - e.p1) (q - e.p1) > 0) ∧ between_rays_p s e(p)
 ∧ dir_parallel_p (e.p2 - e.p1) (q - p)
 ∧ (∃ (a : {x : ℝ // 0 < x}) (c : NNReal), q - a * perp_left (e.p2 - e.p1) = e.p1 + c * (e.p2 - e.p1)) → between_rays_p s e(q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.other_end_of_parallel_segment_between_far
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, s, e: segment_2d):
--        (s`p2 = e`p1) AND (p /= q)
--    AND (NOT dir_parallel?(e`p1 - e`p2, s`p2 - s`p1))
--    AND (cross(p - s`p1, q - s`p1) > 0) AND between_rays?(s, e)(q)
--    A
theorem other_end_of_parallel_segment_between_far : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p2 = e.p1) ∧ (p ≠ q)
 ∧ (¬ dir_parallel_p (e.p1 - e.p2) (s.p2 - s.p1))
 ∧ (cross (p - s.p1) (q - s.p1) > 0) ∧ between_rays_p s e(q)
 ∧ dir_parallel_p (s.p1 - s.p2) (p - q)
 ∧ (∃ (a : {x : ℝ // 0 < x}) (c : NNReal), p - a * perp_left (s.p2 - s.p1) = s.p2 + c * (s.p1 - s.p2)) → between_rays_p s e(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.interior_segment_points_in_wedge
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices),
--         outward: bool):
--   LET s = edges_of_polygon(G)(prev_index(G, i)), e = edges_of_polygon(G)(i)
--     IN
--     IF outward THEN 
theorem interior_segment_points_in_wedge : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (outward : Bool), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => if outward then cross (s.p1 - e.p1) (e.p2 - e.p1) ≥ 0
else cross (e.p2 - e.p1) (s.p1 - e.p1) ≥ 0

 ∧
 p ≠ q ∧
  point_in_vertex_wedge_p outward G i(p) ∧
   point_in_vertex_wedge_p outward G i(q) → ∀ (u : point_2d), point_on_segment_p u ((# p1 := p, p2 := q #)) →
   point_in_vertex_wedge_p outward G i(u) (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  -- === TCCs for interior_segment_points_in_wedge ===
  -- TCC: interior_segment_points_in_wedge_TCC1
  -- Statement: FORALL (p, q: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices),
  --         outward: bool, s: segment_2d, e: segment_2d):
  --        (s = edges_of_polygon(G)(prev_index(G, i)))
  --    AND (e = edges_o...
  have interior_segment_points_in_wedge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.pos_cross_to_between_point_narrow
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices)):
--   LET s = edges_of_polygon(G)(prev_index(G, i)), e = edges_of_polygon(G)(i)
--     IN
--     cross(s`p1 - e`p1, e`p2 - e`p1) > 0 AND be
theorem pos_cross_to_between_point_narrow : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => cross (s.p1 - e.p1) (e.p2 - e.p1) > 0 ∧ between_rays_p s e(p) → cross (e.p1 - e.p2) (p - e.p2) > 0 (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.pos_cross_to_between_point_wide
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices)):
--   LET s = edges_of_polygon(G)(prev_index(G, i)), e = edges_of_polygon(G)(i)
--     IN
--     cross(s`p1 - e`p1, e`p2 - e`p1) <= 0 AND
--  
theorem pos_cross_to_between_point_wide : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => cross (s.p1 - e.p1) (e.p2 - e.p1) ≤ 0 ∧ cross (e.p1 - e.p2) (p - e.p1) > 0 → cross (e.p1 - e.p2) (p - e.p2) > 0 (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_parallel_to_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   LET p = wedge_connector_endpoint(G, i, TRUE),
--       q = wedge_connector_endpoint(G, i, FALSE),
--       e = edges_of_polygon(G)(i)
--     IN dir_pa
theorem connector_parallel_to_edge : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (p : ℝ × ℝ) => fun (q : ℝ × ℝ) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => dir_parallel_p (e.p2 - e.p1) (q - p) (edges_of_polygon G i) (wedge_connector_endpoint G i False) (wedge_connector_endpoint G i True) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_near_endpoint_cross_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   LET p = wedge_connector_endpoint(G, i, TRUE), e = edges_of_polygon(G)(i)
--     IN cross(e`p2 - e`p1, p - e`p1) < 0
theorem connector_near_endpoint_cross_neg : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (p : ℝ × ℝ) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => cross (e.p2 - e.p1) (p - e.p1) < 0 (edges_of_polygon G i) (wedge_connector_endpoint G i True) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_segment_point_cross_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), q: point_2d):
--   LET p = wedge_connector_endpoint(G, i, TRUE),
--       r = wedge_connector_endpoint(G, i, FALSE),
--       e = edges_of_polygon(G)(i)
-- 
theorem connector_segment_point_cross_neg : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (q : ℝ × ℝ), fun (p : ℝ × ℝ) => fun (r : ℝ × ℝ) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => p ≠ q ∧ point_on_segment_p q ((# p1 := p, p2 := r #)) → cross (e.p2 - e.p1) (q - e.p1) < 0 (edges_of_polygon G i) (wedge_connector_endpoint G i False) (wedge_connector_endpoint G i True) := by
  -- === TCCs for connector_segment_point_cross_neg ===
  -- TCC: connector_segment_point_cross_neg_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), q: point_2d,
  --         p: point_2d, r: point_2d, e: segment_2d):
  --   p = wedge_connector_endpoint(G, i, TRUE) AND
  --    r = wedge_connector_endpoint...
  have connector_segment_point_cross_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_segment_points_cross_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), q: point_2d):
--   LET p = wedge_connector_endpoint(G, i, TRUE),
--       r = wedge_connector_endpoint(G, i, FALSE),
--       e = edges_of_polygon(G)(i)
-- 
theorem connector_segment_points_cross_pos : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (q : ℝ × ℝ), fun (p : ℝ × ℝ) => fun (r : ℝ × ℝ) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => p ≠ q ∧ point_on_segment_p q ((# p1 := p, p2 := r #)) → cross (p - e.p1) (q - e.p1) > 0 (edges_of_polygon G i) (wedge_connector_endpoint G i False) (wedge_connector_endpoint G i True) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.connector_not_on_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem connector_not_on_edge : True := by
  -- === TCCs for connector_not_on_edge ===
  -- TCC: connector_not_on_edge_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), p: point_2d,
  --         q: point_2d, e: segment_2d):
  --   p = wedge_connector_endpoint(G, i, TRUE) AND
  --    q = wedge_connector_endpoint(G, i, FALSE)...
  have connector_not_on_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.connector_not_on_prev_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem connector_not_on_prev_edge : True := by
  -- === TCCs for connector_not_on_prev_edge ===
  -- TCC: connector_not_on_prev_edge_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), p: point_2d,
  --         q: point_2d, e: segment_2d):
  --   p = wedge_connector_endpoint(G, i, TRUE) AND
  --    q = wedge_connector_endpoint(G, i, FALSE)...
  have connector_not_on_prev_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.connector_not_on_next_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem connector_not_on_next_edge : True := by
  -- === TCCs for connector_not_on_next_edge ===
  -- TCC: connector_not_on_next_edge_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), p: point_2d,
  --         q: point_2d, e: segment_2d):
  --   p = wedge_connector_endpoint(G, i, TRUE) AND
  --    q = wedge_connector_endpoint(G, i, FALSE)...
  have connector_not_on_next_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.connector_not_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem connector_not_on_perimeter : True := by
  -- === TCCs for connector_not_on_perimeter ===
  -- TCC: connector_not_on_perimeter_TCC1
  -- Statement: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices), p: point_2d,
  --         q: point_2d):
  --   p = wedge_connector_endpoint(G, i, TRUE) AND
  --    q = wedge_connector_endpoint(G, i, FALSE)
  --    IMPLIES q ...
  have connector_not_on_perimeter_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.apex_to_apex_not_on_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem apex_to_apex_not_on_edge : True := by
  -- === TCCs for apex_to_apex_not_on_edge ===
  -- TCC: apex_to_apex_not_on_edge_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), p: point_2d,
  --         q: point_2d, e: segment_2d):
  --   p = vertex_wedge_apex(TRUE, G, i) AND
  --    q = vertex_wedge_apex(TRUE, G, next_index(G, i))...
  have apex_to_apex_not_on_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.vertex_to_vertex_length_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i, j: below(G`num_vertices), e: segment_2d):
--   e`p1 = G`vertices(i) AND e`p2 = G`vertices(j) IMPLIES
--    min_edge_sep(G) <= length(e)
theorem vertex_to_vertex_length_bound : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), e.p1 = G.vertices i ∧ e.p2 = G.vertices j → min_edge_sep G ≤ List.length e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.vertex_to_vertex_in_wedge_neq_prev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, q: point_2d, i, j: below(G`num_vertices),
--         e: segment_2d):
--   e`p1 = G`vertices(i) AND
--    e`p2 = G`vertices(j) AND
--     point_on_segment?(q, e) AND point_in_vertex_
theorem vertex_to_vertex_in_wedge_neq_prev : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (q : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), e.p1 = G.vertices i ∧
 e.p2 = G.vertices j ∧
  point_on_segment_p q e ∧ point_in_vertex_wedge_p True G j(q) → j ≠ prev_index G i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.segment_points_in_wedge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem segment_points_in_wedge : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: vertex_wedges.between_opposite_apex_points_in_wedges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices), p: point_2d,
--         d: nnreal):
--   p =
--    G`vertices(i) +
--     (d - vertex_wedge_radius(G)) * ^(vertex_bisect_vector(G, i))
--    AND d /= vertex_w
theorem between_opposite_apex_points_in_wedges : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (p : ℝ × ℝ) (d : {x : ℝ // x ≥ 0}), p =
 G.vertices i +
  (d - vertex_wedge_radius G) * ^(vertex_bisect_vector G i)
 ∧ d ≠ vertex_wedge_radius G ∧ d < 2 * vertex_wedge_radius G → point_in_vertex_wedge_p True G i(p) ∨
 point_in_vertex_wedge_p False G i(p) := by
  -- === TCCs for between_opposite_apex_points_in_wedges ===
  -- TCC: between_opposite_apex_points_in_wedges_TCC1
  -- Statement: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices)):
  --   vertex_bisect_vector(G, i) /= zero
  have between_opposite_apex_points_in_wedges_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.wedge_point_out_imp_all_wedge_points_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices)):
--   point_in_vertex_wedge?(TRUE, G, i)(p) AND
--    point_in_vertex_wedge?(TRUE, G, i)(q) AND point_outside_polygon?(p, G)
--    IMPLI
theorem wedge_point_out_imp_all_wedge_points_out : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_in_vertex_wedge_p True G i(p) ∧
 point_in_vertex_wedge_p True G i(q) ∧ point_outside_polygon_p p G → point_outside_polygon_p q G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.outward_apex_outside_inward_inside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices)):
--   point_outside_polygon?(vertex_wedge_apex(TRUE, G, i), G) IMPLIES
--    point_in_polygon?(vertex_wedge_apex(FALSE, G, i), G)
theorem outward_apex_outside_inward_inside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_outside_polygon_p (vertex_wedge_apex True G i) G → point_in_polygon_p (vertex_wedge_apex False G i) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.westward_point_outside
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, c: posreal):
--   LET r = (# x := c, y := 0 #) IN
--     point_outside_polygon?(G`vertices(topleft_vertex_idx(G)) - r, G)
theorem westward_point_outside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (c : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (r : {x : {x : ℝ // x ≥ 0} // x > 0} × {x : {s : {i : ℝ // i ≥ 0} // s < exp2 8} // even_p x}) => point_outside_polygon_p (G.vertices (topleft_vertex_idx G) - r) G (# x := c) (y := 0 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.topleft_outward_apex_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon):
--   LET a = vertex_wedge_apex(TRUE, G, topleft_vertex_idx(G)) IN
--     point_outside_polygon?(a, G)
theorem topleft_outward_apex_outside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (a : ℝ × ℝ) => point_outside_polygon_p a G (vertex_wedge_apex True G (topleft_vertex_idx G)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.wedge_apex_points_outside_rec
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices)):
--   LET j = index_from_offset(G, topleft_vertex_idx(G))(i) IN
--     point_outside_polygon?(vertex_wedge_apex(TRUE, G, j), G)
theorem wedge_apex_points_outside_rec : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (j : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => point_outside_polygon_p (vertex_wedge_apex True G j) G (index_from_offset G (topleft_vertex_idx G) i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.wedge_apex_points_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices)):
--   point_outside_polygon?(vertex_wedge_apex(TRUE, G, i), G)
theorem wedge_apex_points_outside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_outside_polygon_p (vertex_wedge_apex True G i) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.wedge_apex_points_inside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: ccw_simple_polygon, i: below(G`num_vertices)):
--   point_in_polygon?(vertex_wedge_apex(FALSE, G, i), G)
theorem wedge_apex_points_inside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_in_polygon_p (vertex_wedge_apex False G i) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.all_wedge_points_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices)):
--   point_in_vertex_wedge?(TRUE, G, i)(p) IMPLIES point_outside_polygon?(p, G)
theorem all_wedge_points_outside : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_in_vertex_wedge_p True G i(p) → point_outside_polygon_p p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.all_wedge_points_inside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: ccw_simple_polygon, i: below(G`num_vertices)):
--   point_in_vertex_wedge?(FALSE, G, i)(p) IMPLIES point_in_polygon?(p, G)
theorem all_wedge_points_inside : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_in_vertex_wedge_p False G i(p) → point_in_polygon_p p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.point_on_spanning_edge_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: ccw_simple_polygon, i, j: below(C`num_vertices),
--         s: segment_2d, q: point_2d):
--        (edge_of_polygon?(A)(s) OR edge_of_polygon?(A)(reverse_segment(s)))
--    AND (C`vertices(i) = s
theorem point_on_spanning_edge_between_rays : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < C.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < C.num_vertices}) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (q : ℝ × ℝ), (edge_of_polygon_p A(s) ∨ edge_of_polygon_p A(reverse_segment s))
 ∧ (C.vertices i = s.p1) ∧ (C.vertices j = s.p2)
 ∧ point_on_segment_p q s
 ∧ (∀ (e : (edge_of_polygon_p A)), only_vertex_intersections_p C A e)
 ∧ (norm (q - C.vertices j) ≤ vertex_wedge_radius C)
 ∧ between_rays_p (edges_of_polygon C(prev_index C j)) (edges_of_polygon C(j))
                  (q) → between_rays_p (edges_of_polygon C(prev_index C i)) (edges_of_polygon C(i))
             (q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.point_outside_imp_edge_partly_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G, H: simple_polygon_2d):
--   (FORALL (e: (edge_of_polygon?(H))): only_vertex_intersections?(G, H, e))
--    AND
--    (EXISTS (p: point_2d):
--       point_on_polygon_perimeter?(H)(p) AND
--        NOT poi
theorem point_outside_imp_edge_partly_outside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (H : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (e : (edge_of_polygon_p H)), only_vertex_intersections_p G H e)
 ∧
 (∃ (p : point_2d), point_on_polygon_perimeter_p H(p) ∧ ¬ point_outside_polygon_p p G)
  ∧
  (∃ (u : point_2d), point_on_polygon_perimeter_p H(u) ∧ point_outside_polygon_p u G) → ∃ (i: Finset.range H.num_vertices), (q, r: point_2d):
       point_on_segment_p q (edges_of_polygon H(i))
   ∧ point_on_segment_p r (edges_of_polygon H(i))
   ∧ point_outside_polygon_p q G ∧ point_on_polygon_perimeter_p G(r)
   ∧ (norm (q - r) ≤ vertex_wedge_radius G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vertex_wedges.contained_if_no_protruding_edges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G, H: ccw_simple_polygon):
--   (EXISTS (q: point_2d):
--      point_on_polygon_perimeter?(H)(q) AND
--       (point_on_polygon_perimeter?(G)(q) OR point_in_polygon?(q, G)))
--    AND
--    no_edges_in_outwa
theorem contained_if_no_protruding_edges : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (H : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∃ (q : point_2d), point_on_polygon_perimeter_p H(q) ∧
    (point_on_polygon_perimeter_p G(q) ∨ point_in_polygon_p q G))
 ∧
 no_edges_in_outward_wedges_p G H ∧
  (∀ (e : (edge_of_polygon_p H)), only_vertex_intersections_p G H e) → polygon_contained_p H G := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.vertex_wedges