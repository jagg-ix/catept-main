-- Auto-generated by pvs2lean/translator.py
-- PVS theory: polygon_merge
-- PVSLib library: polygon_merge
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: polygon_merge

Auto-generated stubs from PVSLib theory `polygon_merge`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.polygon_merge

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (edge_of_polygon_p : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (edges_of_polygon : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (find_index : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (inject_post_condition_p : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (inject_vertices_into_polygon : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (leftmost_vertex_val : pvs_τ15 → pvs_τ16)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (merge_pre_condition : pvs_τ24 → pvs_τ25)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (next_index : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} (point_between_rays_p : pvs_τ42 → pvs_τ43 → pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (point_on_polygon_perimeter_p : pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ51 : Type*} (seq : pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} (seq_only_vertex_intersections_p : pvs_τ52 → pvs_τ53 → pvs_τ54 → Prop)
variable {pvs_τ58 : Type*} (singleton_seq : pvs_τ58 → Prop)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} (topleft_vertex_idx : pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (topleft_vertex_val : pvs_τ61 → pvs_τ62)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} (upto : pvs_τ63 → pvs_τ64)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (vertex_p : pvs_τ65 → pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (vertices : pvs_τ67 → pvs_τ68)

-- pvs2lean: polygon_merge.point_AB_vtx?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vertex?(Am)(p) OR vertex?(Bm)(p)
axiom point_AB_vtx_p {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41 → Prop

-- pvs2lean: polygon_merge.point_seq_AB_vtx?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(s`length)): point_AB_vtx?(Am, Bm)(s`seq(i))
axiom point_seq_AB_vtx_p {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} : pvs_τ47 → pvs_τ48 → pvs_τ49 → Prop

-- pvs2lean: polygon_merge.uniq_seq_AB_vtx?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(s`length)): point_AB_vtx?(Am, Bm)(s`seq(i))
noncomputable def uniq_seq_AB_vtx_p : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL (i: below(s`length)): point_AB_vtx?(Am, Bm)(s`seq(i))

-- pvs2lean: polygon_merge.seq_AB_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am, Bm: simple_polygon_2d), (p: (point_AB_vtx?(Am, Bm))):
--   point_seq_AB_vtx?(Am, Bm)(singleton_seq(p))
theorem seq_AB_singleton : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), point_seq_AB_vtx_p Am Bm (singleton_seq p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.seq_AB_concat
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am, Bm: simple_polygon_2d), (x, y: finseq[point_2d]):
--   point_seq_AB_vtx?(Am, Bm)(x) AND point_seq_AB_vtx?(Am, Bm)(y) IFF
--    point_seq_AB_vtx?(Am, Bm)(x o y)
theorem seq_AB_concat : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (x : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (y : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), point_seq_AB_vtx_p Am Bm(x) ∧ point_seq_AB_vtx_p Am Bm(y) ↔ point_seq_AB_vtx_p Am Bm(x ∘ y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.next_merge_vertex
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET idx_A = find_index(Am, vertex) IN
--   LET idx_B = find_index(Bm, vertex) IN
--     IF idx_A >= 0
--       THEN IF idx_B >= 0
--              THEN LET e2: segment_2d = edges_of_polygon(Am)(idx_A) IN
--          
axiom next_merge_vertex {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → Prop

-- pvs2lean: polygon_merge.prev_0
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: topleft + (# x := -1, y := 0 #)
axiom prev_0 {pvs_τ50 : Type*} : pvs_τ50 → Prop

-- pvs2lean: polygon_merge.seq_prev_vtx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i >= k AND i - k < S`length THEN S`seq(i - k) ELSE prev_0(S`seq(0)) ENDIF
axiom seq_prev_vtx {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} : pvs_τ55 → pvs_τ56 → pvs_τ57 → Prop

-- pvs2lean: polygon_merge.next_vertex_distinct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am, Bm: simple_polygon_2d, vertex: (point_AB_vtx?(Am, Bm)),
--         prev_vtx: point_2d):
--   next_merge_vertex(Am, Bm, vertex, prev_vtx) /= vertex
theorem next_vertex_distinct : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (vertex : ℝ × ℝ) (prev_vtx : ℝ × ℝ), next_merge_vertex Am Bm vertex prev_vtx ≠ vertex := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.next_merge_vertex_on_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem next_merge_vertex_on_edge : True := by
  -- === TCCs for next_merge_vertex_on_edge ===
  -- TCC: next_merge_vertex_on_edge_TCC1
  -- Statement: FORALL (Am, Bm: simple_polygon_2d, vertex: (point_AB_vtx?(Am, Bm)),
  --         prev_vtx: point_2d, next: (point_AB_vtx?(Am, Bm)),
  --         edge:
  --           [# p1: (point_AB_vtx?(Am, Bm)), p2: (point_AB_...
  have next_merge_vertex_on_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: polygon_merge.merge_helper
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET next_vtx: point_2d = next_merge_vertex(Am, Bm, vertex, prev_vtx) IN
--   IF next_vtx = first_vtx OR vtx_num = Am`num_vertices + Bm`num_vertices
--     THEN empty_seq
--   ELSE singleton_seq(next_vtx) o
--    
axiom merge_helper {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23

-- pvs2lean: polygon_merge.first_vertex_merge
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET start_with_A: bool =
--       IF leftmost_vertex_val(A) = leftmost_vertex_val(B)
--         THEN topleft_vertex_val(A) >= topleft_vertex_val(B)
--       ELSE leftmost_vertex_val(A) < leftmost_vertex_val(B)
axiom first_vertex_merge {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ9 → pvs_τ10 → Prop

-- pvs2lean: polygon_merge.first_vertex_AB
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      point_AB_vtx?(Am, Bm
theorem first_vertex_AB : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → point_AB_vtx_p Am Bm(first_vertex_merge A B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.first_vertex_merge_comm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   first_vertex_merge(A, B) = first_vertex_merge(B, A)
theorem first_vertex_merge_comm : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), first_vertex_merge A B = first_vertex_merge B A := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (Am, Bm) = inject_vertices_into_polygon(A, B) IN
--   LET first_vtx = first_vertex_merge(A, B) IN
--     singleton_seq(first_vtx) o
--      merge_helper(Am, Bm, first_vtx, first_vtx, prev_0(first_vtx), 0)
axiom merge_seq {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: polygon_merge.merge_seq_length_gt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   merge_seq(A, B)`length > 0
theorem merge_seq_length_gt_0 : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), merge_seq A B.List.length > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_helper_max_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--         first_vtx, vertex: (point_AB_vtx?(Am, Bm)), prev_vtx: point_2d,
--         k: upto(Am`num_vertices + Bm`num_vertices)):
--   LET n = Am`num_vert
theorem merge_helper_max_length : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (first_vtx : ℝ × ℝ) (vertex : ℝ × ℝ) (prev_vtx : ℝ × ℝ) (k : {s : {i : ℝ // i ≥ 0} // s ≤ Am.num_vertices + Bm.num_vertices}), fun (n : {i : {i : ℝ // i ≥ 0} // i > 0}) => merge_helper Am Bm first_vtx vertex prev_vtx (n - k).List.length ≤ k (Am.num_vertices + Bm.num_vertices) := by
  -- === TCCs for merge_helper_max_length ===
  -- TCC: merge_helper_max_length_TCC1
  -- Statement: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         first_vtx, vertex: (point_AB_vtx?(Am, Bm)),
  --         k: upto(Am`num_vertices + Bm`num_vertices), n: posint):
  --   n = Am`num_vertices + Bm`...
  have merge_helper_max_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_seq_max_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      merge_seq(A, B)`leng
theorem merge_seq_max_length : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → merge_seq A B.List.length ≤ Am.num_vertices + Bm.num_vertices + 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_prefix
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF k = 0 THEN singleton_seq(first_vtx)
-- ELSE LET S = merge_prefix(Am, Bm, first_vtx, k - 1),
--          prev_vtx = seq_prev_vtx(S, k, 2)
--        IN
--        S o singleton_seq(next_merge_vertex(Am, Bm, S`seq
axiom merge_prefix {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ26 → pvs_τ27 → pvs_τ28 → pvs_τ29 → Prop

-- pvs2lean: polygon_merge.merge_prefix_initial_element
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat):
--   merge_prefix(Am, Bm, first_vtx, k)`seq(0) = first_vtx
theorem merge_prefix_initial_element : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (first_vtx : ℝ × ℝ) (k : {i : ℝ // i ≥ 0}), merge_prefix Am Bm first_vtx k.seq 0 = first_vtx := by
  -- === TCCs for merge_prefix_initial_element ===
  -- TCC: merge_prefix_initial_element_TCC1
  -- Statement: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat):
  --   0 < merge_prefix(Am, Bm, first_vtx, k)`length
  have merge_prefix_initial_element_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_prefix_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat):
--   merge_prefix(Am, Bm, first_vtx, k)`length = k + 1
theorem merge_prefix_length : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (first_vtx : ℝ × ℝ) (k : {i : ℝ // i ≥ 0}), merge_prefix Am Bm first_vtx k.List.length = k + 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_prefix_prev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat, i: upto(k)):
--   merge_prefix(Am, Bm, first_vtx, k)`seq(i) =
--    merge_prefix(Am, Bm, first_vtx, 
theorem merge_prefix_prev : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (first_vtx : ℝ × ℝ) (k : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s ≤ k}), merge_prefix Am Bm first_vtx k.seq i = merge_prefix Am Bm first_vtx (k + 1).seq i := by
  -- === TCCs for merge_prefix_prev ===
  -- TCC: merge_prefix_prev_TCC1
  -- Statement: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat, i: upto(k)):
  --   i < merge_prefix(Am, Bm, first_vtx, k)`length
  have merge_prefix_prev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_prefix_prev_TCC2
  -- Statement: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat, i: upto(k)):
  --   i < merge_prefix(Am, Bm, first_vtx, 1 + k)`length
  have merge_prefix_prev_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_prefix_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat, d: nat, i: upto(k)):
--   merge_prefix(Am, Bm, first_vtx, k)`seq(i) =
--    merge_prefix(Am, Bm, fir
theorem merge_prefix_prefix : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (first_vtx : ℝ × ℝ) (k : {i : ℝ // i ≥ 0}) (d : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s ≤ k}), merge_prefix Am Bm first_vtx k.seq i = merge_prefix Am Bm first_vtx (k + d).seq i := by
  -- === TCCs for merge_prefix_prefix ===
  -- TCC: merge_prefix_prefix_TCC1
  -- Statement: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat, d: nat, i: upto(k)):
  --   i < merge_prefix(Am, Bm, first_vtx, d + k)`length
  have merge_prefix_prefix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_prefix_AB
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--         first_vtx: (point_AB_vtx?(Am, Bm)), k: nat):
--   FORALL (i: upto(k)):
--     point_AB_vtx?(Am, Bm)(merge_prefix(Am, Bm, first_vtx, k)`seq(i))
theorem merge_prefix_AB : ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (first_vtx : ℝ × ℝ) (k : {i : ℝ // i ≥ 0}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s ≤ k}), point_AB_vtx_p Am Bm (merge_prefix Am Bm first_vtx k.seq i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_vertex_AB
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           L: finseq[point_2d]):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AN
theorem merge_vertex_AB : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (L : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ L = merge_seq A B → ∀ (i : Finset.range L.List.length), point_AB_vtx_p Am Bm(L.seq i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_helper_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      LET first_vtx = firs
theorem merge_helper_seq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → (let first_vtx := first_vertex_merge A B; let L := merge_seq A B; ∀ (i : Finset.range L.List.length), (let P := merge_prefix Am Bm first_vtx i; let prev_vtx := seq_prev_vtx P i 1; L = P ∘ merge_helper Am Bm first_vtx (P.seq i) prev_vtx i)) := by
  -- === TCCs for merge_helper_seq ===
  -- TCC: merge_helper_seq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, first_vtx: point_2d,
  --         L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into...
  have merge_helper_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_helper_seq_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, first_vtx: point_2d,
  --         L: finseq[point_2d], i: below(L`length),
  --         P: {s:...
  have merge_helper_seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_helper_seq_TCC3
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, first_vtx: point_2d,
  --         L: finseq[point_2d], i: below(L`length),
  --         P: {s:...
  have merge_helper_seq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_helper_seq_TCC4
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, first_vtx: point_2d,
  --         L: finseq[point_2d], i: below(L`length),
  --         P: {s:...
  have merge_helper_seq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_helper_seq_TCC5
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, first_vtx: point_2d,
  --         L: finseq[point_2d], i: below(L`length),
  --         P: {s:...
  have merge_helper_seq_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.extend_merge_prefix_merge_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, n: nat):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      LET first_vt
theorem extend_merge_prefix_merge_seq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (n : {i : ℝ // i ≥ 0}), (Am, Bm) = inject_vertices_into_polygon A B → (let first_vtx := first_vertex_merge A B; let L := merge_seq A B; L.List.length ≤ n + 1 →
   ∀ (i : Finset.range L.List.length), L.seq i = merge_prefix Am Bm first_vtx n.seq i) := by
  -- === TCCs for extend_merge_prefix_merge_seq ===
  -- TCC: extend_merge_prefix_merge_seq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, n: nat,
  --         first_vtx: point_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_verti...
  have extend_merge_prefix_merge_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_seq_eq_merge_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      merge_seq(A, B) =
--   
theorem merge_seq_eq_merge_prefix : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → merge_seq A B =
 merge_prefix Am Bm (first_vertex_merge A B) (merge_seq A B.List.length - 1) := by
  -- === TCCs for merge_seq_eq_merge_prefix ===
  -- TCC: merge_seq_eq_merge_prefix_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
  --    point_AB_vtx?(Am, Bm)(fi...
  have merge_seq_eq_merge_prefix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_eq_merge_prefix_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
  --    merge_seq(A, B)`length -...
  have merge_seq_eq_merge_prefix_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.next_merge_vtx_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           L: finseq[point_2d], n: nat):
--     (Am, Bm) = inject_vertices_into_polygon(
theorem next_merge_vtx_seq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (L : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (n : {i : ℝ // i ≥ 0}), (Am, Bm) = inject_vertices_into_polygon A B ∧
 L = merge_prefix Am Bm (first_vertex_merge A B) n → ∀ (i : Finset.range L.List.length - 1), (let prev_vtx := seq_prev_vtx L i 1; L.seq (i + 1) = next_merge_vertex Am Bm (L.seq i) prev_vtx) := by
  -- === TCCs for next_merge_vtx_seq ===
  -- TCC: next_merge_vtx_seq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d],
  --         n: nat):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B...
  have next_merge_vtx_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_merge_vtx_seq_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d],
  --         n: nat, i: below(L`length - 1), prev_vtx: point_2d):
  --   ...
  have next_merge_vtx_seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_merge_vtx_seq_TCC3
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d],
  --         n: nat, i: below(L`length - 1), prev_vtx: point_2d):
  --   ...
  have next_merge_vtx_seq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_merge_vtx_seq_TCC4
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d],
  --         n: nat, i: below(L`length - 1), prev_vtx: point_2d):
  --   ...
  have next_merge_vtx_seq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_vertex_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           L: finseq[point_2d]):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AN
theorem merge_vertex_seq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (L : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ L = merge_seq A B → ∀ (i : Finset.range L.List.length - 1), (let prev_vtx := seq_prev_vtx L i 1; L.seq (i + 1) = next_merge_vertex Am Bm (L.seq i) prev_vtx) := by
  -- === TCCs for merge_vertex_seq ===
  -- TCC: merge_vertex_seq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND L = merge_...
  have merge_vertex_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_vertex_seq_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d],
  --         i: below(L`length - 1), prev_vtx: point_2d):
  --   (Am, Bm)...
  have merge_vertex_seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_vertex_seq_TCC3
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d],
  --         i: below(L`length - 1), prev_vtx: point_2d):
  --   (Am, Bm)...
  have merge_vertex_seq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_vertex_seq_TCC4
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d],
  --         i: below(L`length - 1), prev_vtx: point_2d):
  --   (Am, Bm)...
  have merge_vertex_seq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_seq_vertices_wrap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           L: finseq[point_2d]):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AN
theorem merge_seq_vertices_wrap : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (L : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧
 L = merge_seq A B ∧ L.List.length - 1 < Am.num_vertices + Bm.num_vertices → (let prev_vtx := seq_prev_vtx L (L.List.length - 1) 1; next_merge_vertex Am Bm (L.seq (L.List.length - 1)) prev_vtx =
   first_vertex_merge A B) := by
  -- === TCCs for merge_seq_vertices_wrap ===
  -- TCC: merge_seq_vertices_wrap_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    L = mer...
  have merge_seq_vertices_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_vertices_wrap_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    L = mer...
  have merge_seq_vertices_wrap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_vertices_wrap_TCC3
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    L = mer...
  have merge_seq_vertices_wrap_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_vertices_wrap_TCC4
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    L = mer...
  have merge_seq_vertices_wrap_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_path_props
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem merge_path_props : True := by
  -- === TCCs for merge_path_props ===
  -- TCC: merge_path_props_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND L = merge_...
  have merge_path_props_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_path_props_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND L = merge_...
  have merge_path_props_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_path_props_TCC3
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, L: finseq[point_2d]):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND L = merge_...
  have merge_path_props_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: polygon_merge.adj_merge_seq_vertices_distinct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           L: finseq[point_2d]):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AN
theorem adj_merge_seq_vertices_distinct : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (L : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B ∧ L = merge_seq A B → ∀ (i : Finset.range L.List.length - 1), L.seq i ≠ L.seq (i + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.next_is_first_merge_seq_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           first_vtx, vertex: (point_AB_vtx?(Am, Bm)), prev_vtx: point_2d,
--           
theorem next_is_first_merge_seq_length : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (first_vtx : ℝ × ℝ) (vertex : ℝ × ℝ) (prev_vtx : ℝ × ℝ) (k : {i : {i : ℝ // i ≥ 0} // i > 0}), ((Am, Bm) = inject_vertices_into_polygon A B)
 ∧ (first_vtx = first_vertex_merge A B) ∧ (k < merge_seq A B.List.length)
 ∧ (vertex = merge_prefix Am Bm first_vtx k.seq k)
 ∧ (prev_vtx = merge_prefix Am Bm first_vtx k.seq (k - 1))
 ∧ (next_merge_vertex Am Bm vertex prev_vtx = first_vtx) → merge_seq A B.List.length = k + 1 := by
  -- === TCCs for next_is_first_merge_seq_length ===
  -- TCC: next_is_first_merge_seq_length_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         first_vtx, vertex: (point_AB_vtx?(Am, Bm)), k: posnat):
  --   (Am, Bm) = inject_...
  have next_is_first_merge_seq_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_is_first_merge_seq_length_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d,
  --         first_vtx, vertex: (point_AB_vtx?(Am, Bm)), k: posnat):
  --   (Am, Bm) = inject_...
  have next_is_first_merge_seq_length_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_seq_only_vertex_intersections
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      (FORALL (e: (edge_of
theorem merge_seq_only_vertex_intersections : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → (∀ (e : (edge_of_polygon_p Am)), seq_only_vertex_intersections_p (merge_seq A B) Am e)
 ∧
 (∀ (e : (edge_of_polygon_p Bm)), seq_only_vertex_intersections_p (merge_seq A B) Bm e) := by
  -- === TCCs for merge_seq_only_vertex_intersections ===
  -- TCC: merge_seq_only_vertex_intersections_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
  --    FORALL (e: (edge_of_poly...
  have merge_seq_only_vertex_intersections_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_only_vertex_intersections_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d):
  --   (Am, Bm) = inject_vertices_into_polygon(A, B) AND
  --    (FORALL (e: (edge_of_polygon...
  have merge_seq_only_vertex_intersections_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.merge_seq_only_vertex_int_subseq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d,
--           S: finseq[point_2d], i, j: below(merge_seq(A, B)`length)):
--     (Am, Bm) = 
theorem merge_seq_only_vertex_int_subseq : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < merge_seq A B.List.length}) (j : {s : {i : ℝ // i ≥ 0} // s < merge_seq A B.List.length}), (Am, Bm) = inject_vertices_into_polygon A B ∧
 S = merge_seq A B ^ (i, j) → (∀ (e : (edge_of_polygon_p Am)), seq_only_vertex_intersections_p S Am e)
 ∧
 (∀ (e : (edge_of_polygon_p Bm)), seq_only_vertex_intersections_p S Bm e) := by
  -- === TCCs for merge_seq_only_vertex_int_subseq ===
  -- TCC: merge_seq_only_vertex_int_subseq_TCC1
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, S: finseq[point_2d],
  --         i, j: below(merge_seq(A, B)`length)):
  --   (Am, Bm) = inje...
  have merge_seq_only_vertex_int_subseq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_seq_only_vertex_int_subseq_TCC2
  -- Statement: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A)),
  --         Am: simple_polygon_2d, Bm: simple_polygon_2d, S: finseq[point_2d],
  --         i, j: below(merge_seq(A, B)`length)):
  --   ((Am, Bm) = inj...
  have merge_seq_only_vertex_int_subseq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.first_vertex_merge_is_topleft_Am_or_Bm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     LET first_vtx = first_vertex_merge(A, B) IN
--       (Am, Bm) = inject_vertices_in
theorem first_vertex_merge_is_topleft_Am_or_Bm : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (first_vtx : ℝ × ℝ) => (Am, Bm) = inject_vertices_into_polygon A B → (vertex_p Am(first_vtx) →
  first_vtx = Am.vertices (topleft_vertex_idx Am))
 ∧
 (vertex_p Bm(first_vtx) →
   first_vtx = Bm.vertices (topleft_vertex_idx Bm)) (first_vertex_merge A B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.first_vertex_x_le_vertices
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) IMPLIES
--      LET first_vtx = firs
theorem first_vertex_x_le_vertices : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (Am, Bm) = inject_vertices_into_polygon A B → (let first_vtx := first_vertex_merge A B; (∀ (i : Finset.range Am.num_vertices), first_vtx.x ≤ Am.vertices i.x) ∧
   (∀ (i : Finset.range Bm.num_vertices), first_vtx.x ≤ Bm.vertices i.x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_merge.prev_0_first_vtx_not_on_Am_or_Bm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: simple_polygon_2d, B: (merge_pre_condition(A))):
--   FORALL (Am: simple_polygon_2d, Bm: simple_polygon_2d, p: point_2d):
--     (Am, Bm) = inject_vertices_into_polygon(A, B) AND
--      (point_on_p
theorem prev_0_first_vtx_not_on_Am_or_Bm : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ∀ (Am : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (Bm : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), (Am, Bm) = inject_vertices_into_polygon A B ∧
 (point_on_polygon_perimeter_p Am(p) ∨ point_on_polygon_perimeter_p Bm(p)) → prev_0 (first_vertex_merge A B) ≠ p := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.polygon_merge