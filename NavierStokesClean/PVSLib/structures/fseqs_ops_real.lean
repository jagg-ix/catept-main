-- Auto-generated by pvs2lean/translator.py
-- PVS theory: fseqs_ops_real
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: fseqs_ops_real

Auto-generated stubs from PVSLib theory `fseqs_ops_real`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.fseqs_ops_real

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (seq : pvs_τ1 → pvs_τ2)

-- pvs2lean: fseqs_ops_real.mmin
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m > n THEN n ELSE m ENDIF
noncomputable def mmin : sorry := by
  sorry  -- pvs2lean: definition stub for IF m > n THEN n ELSE m ENDIF

-- pvs2lean: fseqs_ops_real.+
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET len = IF fs`length > gs`length THEN gs`length ELSE fs`length ENDIF IN
--   (# length := len,
--      seq
--        := (LAMBDA n:
--              IF n < len THEN fs`seq(n) + gs`seq(n) ELSE default ENDIF) #)
noncomputable def pvs_op_26b172 : sorry := by
  sorry  -- pvs2lean: definition stub for LET len = IF fs`length > gs`length THEN gs`length ELSE fs`length ENDIF IN   (# l

-- pvs2lean: fseqs_ops_real.-
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET len = IF fs`length > gs`length THEN gs`length ELSE fs`length ENDIF IN
--   (# length := len,
--      seq
--        := (LAMBDA n:
--              IF n < len THEN fs`seq(n) - gs`seq(n) ELSE default ENDIF) #)
noncomputable def pvs_op_336d5e : sorry := by
  sorry  -- pvs2lean: definition stub for LET len = IF fs`length > gs`length THEN gs`length ELSE fs`length ENDIF IN   (# l

-- pvs2lean: fseqs_ops_real.*
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET len = IF fs`length > gs`length THEN gs`length ELSE fs`length ENDIF IN
--   (# length := len,
--      seq
--        := (LAMBDA n:
--              IF n < len THEN fs`seq(n) * gs`seq(n) ELSE default ENDIF) #)
noncomputable def pvs_op_3389da : sorry := by
  sorry  -- pvs2lean: definition stub for LET len = IF fs`length > gs`length THEN gs`length ELSE fs`length ENDIF IN   (# l

-- pvs2lean: fseqs_ops_real./
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET len = IF fs`length > nzgs`length THEN nzgs`length ELSE fs`length ENDIF
--   IN
--   (# length := len,
--      seq
--        := (LAMBDA n:
--              IF n < len THEN fs`seq(n) / nzgs`seq(n) ELSE default ENDI
noncomputable def pvs_op_6666cd : sorry := by
  sorry  -- pvs2lean: definition stub for LET len = IF fs`length > nzgs`length THEN nzgs`length ELSE fs`length ENDIF   IN 

end

end NavierStokesClean.PVSLib.fseqs_ops_real