-- Auto-generated by pvs2lean/translator.py
-- PVS theory: fseqs
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: fseqs

Auto-generated stubs from PVSLib theory `fseqs`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.fseqs

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (choose : pvs_τ3 → pvs_τ4)
variable {pvs_τ9 : Type*} (nonempty_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (seq : pvs_τ10 → pvs_τ11 → pvs_τ12)

-- pvs2lean: fseqs.default
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: choose({t: T | TRUE})
noncomputable def default : sorry := by
  sorry  -- pvs2lean: definition stub for choose({t: T | TRUE})

-- pvs2lean: fseqs.member
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (i: below(fs`length)): x = fs`seq(i)
axiom member {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: fseqs.empty_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := 0, seq := (LAMBDA (x: nat): default) #)
noncomputable def empty_seq : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := 0, seq := (LAMBDA (x: nat): default) #)

-- pvs2lean: fseqs.emptyseq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty_seq
noncomputable def emptyseq : sorry := by
  sorry  -- pvs2lean: definition stub for empty_seq

-- pvs2lean: fseqs.singleton
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := 1,
--    seq := (LAMBDA (n: nat): IF n = 0 THEN x ELSE default ENDIF) #)
axiom singleton {pvs_τ13 : Type*} {pvs_τ14 : Type*} : pvs_τ13 → pvs_τ14

-- pvs2lean: fseqs.singleton?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (x: T): fs = singleton(x)
axiom singleton_p {pvs_τ15 : Type*} : pvs_τ15 → Prop

-- pvs2lean: fseqs.tofs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA (i: nat): IF i < len THEN f(i) ELSE default ENDIF)
noncomputable def tofs : sorry := by
  sorry  -- pvs2lean: definition stub for (LAMBDA (i: nat): IF i < len THEN f(i) ELSE default ENDIF)

-- pvs2lean: fseqs.tofseq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := f`length,
--    seq
--      := LAMBDA (i: nat): IF i < f`length THEN f`seq(i) ELSE default ENDIF #)
noncomputable def tofseq : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := f`length,    seq      := LAMBDA (i: nat): IF i < f`length THEN f`se

-- pvs2lean: fseqs.singleton_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: singleton?(fs) IFF fs`length = 1
theorem singleton_length : singleton_p fs ↔ fs.List.length = 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: fseqs.fseqs_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fs1 = fs2 IFF
--  fs1`length = fs2`length AND
--   (FORALL (n: nat): n < fs1`length IMPLIES fs1`seq(n) = fs2`seq(n))
theorem fseqs_eq : fs1 = fs2 ↔ fs1.List.length = fs2.List.length ∧
 (∀ (n : ℕ), n < fs1.List.length → fs1.seq n = fs2.seq n) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs.fseq_length_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: fs`length = 0 IMPLIES fs = empty_seq
theorem fseq_length_zero : fs.List.length = 0 → fs = empty_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: fseqs.O
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET l1 = fs1`length, l2 = fs2`length IN
--   (# length := l1 + l2,
--      seq
--        := (LAMBDA (n: nat):
--              IF n < l1 THEN fs1`seq(n)
--              ELSIF n < l1 + l2 THEN fs2`seq(n - l1)
--         
noncomputable def O : sorry := by
  sorry  -- pvs2lean: definition stub for LET l1 = fs1`length, l2 = fs2`length IN   (# length := l1 + l2,      seq        

-- pvs2lean: fseqs.concat
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: o(fs1, fs2)
noncomputable def concat : sorry := by
  sorry  -- pvs2lean: definition stub for o(fs1, fs2)

-- pvs2lean: fseqs.concat_length_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (fs1 o fs2)`length = fs1`length + fs2`length
theorem concat_length_add : (fs1 ∘ fs2).List.length = fs1.List.length + fs2.List.length := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs.member_composition
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(x, fs1 o fs2) IFF member(x, fs1) OR member(x, fs2)
theorem member_composition : member x (fs1 ∘ fs2) ↔ member x fs1 ∨ member x fs2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs.concat_right_emptyseq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fs o empty_seq = fs
theorem concat_right_emptyseq : fs ∘ empty_seq = fs := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: fseqs.concat_left_emptyseq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty_seq o fs = fs
theorem concat_left_emptyseq : empty_seq ∘ fs = fs := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: fseqs.member_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(x, singleton(y)) IFF x = y
theorem member_singleton : member x (singleton y) ↔ x = y := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs.mmin
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m > n THEN n ELSE m ENDIF
noncomputable def mmin : sorry := by
  sorry  -- pvs2lean: definition stub for IF m > n THEN n ELSE m ENDIF

-- pvs2lean: fseqs.^
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (m, n) = p IN
--   IF m > n OR m >= fs`length THEN empty_seq
--   ELSE LET len =
--              IF n - m + 1 > fs`length - m THEN fs`length - m
--              ELSE n - m + 1
--              ENDIF
--          IN
--  
noncomputable def pvs_op_7e6a2a : sorry := by
  sorry  -- pvs2lean: definition stub for LET (m, n) = p IN   IF m > n OR m >= fs`length THEN empty_seq   ELSE LET len =  

-- pvs2lean: fseqs.extract1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fs`seq(0)
noncomputable def extract1 : sorry := by
  sorry  -- pvs2lean: definition stub for fs`seq(0)

-- pvs2lean: fseqs.o_assoc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (fs1 o (fs2 o fs3)) = ((fs1 o fs2) o fs3)
theorem o_assoc : (fs1 ∘ (fs2 ∘ fs3)) = ((fs1 ∘ fs2) ∘ fs3) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs.fseq1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := 1,
--    seq := (LAMBDA (n: nat): IF n < 1 THEN t ELSE default ENDIF) #)
axiom fseq1 {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6

-- pvs2lean: fseqs.fseq2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := 2,
--    seq
--      := (LAMBDA (i: nat):
--            IF i = 0 THEN u ELSIF i = 1 THEN v ELSE default ENDIF) #)
noncomputable def fseq2 : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := 2,    seq      := (LAMBDA (i: nat):            IF i = 0 THEN u ELSI

-- pvs2lean: fseqs.fseq1_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fseq1(x)`seq(0) = x
theorem fseq1_def : fseq1 x.seq 0 = x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs.const_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := n,
--    seq := (LAMBDA (i: nat): IF i < n THEN c ELSE default ENDIF) #)
noncomputable def const_seq : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := n,    seq := (LAMBDA (i: nat): IF i < n THEN c ELSE default ENDIF) 

-- pvs2lean: fseqs.rev
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := fs`length,
--    seq
--      := (LAMBDA (i: nat):
--            IF i < fs`length THEN seq(fs)(fs`length - 1 - i)
--            ELSE default
--            ENDIF) #)
noncomputable def rev : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := fs`length,    seq      := (LAMBDA (i: nat):            IF i < fs`le

end

end NavierStokesClean.PVSLib.fseqs