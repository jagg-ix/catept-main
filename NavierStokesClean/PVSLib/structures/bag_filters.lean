-- Auto-generated by pvs2lean/translator.py
-- PVS theory: bag_filters
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: bag_filters

Auto-generated stubs from PVSLib theory `bag_filters`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.bag_filters

noncomputable section

variable {pvs_τ1 : Type*} (card : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (disjoint_p : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} (empty_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (extract : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (insert : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (intersection : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} (is_finite : pvs_τ14 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (leq : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (max : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (member : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (min : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (purge : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (singleton_bag : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (subbag_p : pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (union : pvs_τ34 → pvs_τ35 → pvs_τ36)

-- pvs2lean: bag_filters.l_filter
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA i: IF leq(i, v) THEN X(i) ELSE 0 ENDIF
axiom l_filter {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ15 → pvs_τ16 → Prop

-- pvs2lean: bag_filters.u_filter
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA i: IF leq(v, i) THEN X(i) ELSE 0 ENDIF
axiom u_filter {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ32 → pvs_τ33 → Prop

-- pvs2lean: bag_filters.finite_l_filter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(A) IMPLIES is_finite(l_filter(A, i))
theorem finite_l_filter : is_finite A → is_finite (l_filter A i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.finite_u_filter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(A) IMPLIES is_finite(u_filter(A, i))
theorem finite_u_filter : is_finite A → is_finite (u_filter A i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_extract
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subbag?(extract(v, X), l_filter(X, v))
theorem l_filter_extract : subbag_p (extract v X) (l_filter X v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_extract
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subbag?(extract(v, X), u_filter(X, v))
theorem u_filter_extract : subbag_p (extract v X) (u_filter X v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: l_filter(singleton_bag(v), v) = singleton_bag(v)
theorem l_filter_singleton : l_filter (singleton_bag v) v = singleton_bag v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: u_filter(singleton_bag(v), v) = singleton_bag(v)
theorem u_filter_singleton : u_filter (singleton_bag v) v = singleton_bag v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_subbag
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subbag?(l_filter(X, v), X)
theorem l_filter_subbag : subbag_p (l_filter X v) X := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_subbag
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subbag?(u_filter(X, v), X)
theorem u_filter_subbag : subbag_p (u_filter X v) X := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_orders
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: leq(i, v) IMPLIES subbag?(l_filter(X, i), l_filter(X, v))
theorem l_filter_orders : leq i v → subbag_p (l_filter X i) (l_filter X v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_orders
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: leq(v, i) IMPLIES subbag?(u_filter(X, i), u_filter(X, v))
theorem u_filter_orders : leq v i → subbag_p (u_filter X i) (u_filter X v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_orders_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (leq(i, v) AND i /= v) IMPLIES
--  subbag?(l_filter(X, i), purge(v, l_filter(X, v)))
theorem l_filter_orders_2 : (leq i v ∧ i ≠ v) → subbag_p (l_filter X i) (purge v (l_filter X v)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_orders_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (leq(v, i) AND i /= v) IMPLIES
--  subbag?(u_filter(X, i), purge(v, u_filter(X, v)))
theorem u_filter_orders_2 : (leq v i ∧ i ≠ v) → subbag_p (u_filter X i) (purge v (u_filter X v)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_insert_assoc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: l_filter(insert(i, X), i) = insert(i, l_filter(X, i))
theorem l_filter_insert_assoc : l_filter (insert i X) i = insert i (l_filter X i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_insert_assoc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: u_filter(insert(i, X), i) = insert(i, u_filter(X, i))
theorem u_filter_insert_assoc : u_filter (insert i X) i = insert i (u_filter X i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_insert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subbag?(l_filter(X, v), l_filter(insert(i, X), v))
theorem l_filter_insert : subbag_p (l_filter X v) (l_filter (insert i X) v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_insert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subbag?(u_filter(X, v), u_filter(insert(i, X), v))
theorem u_filter_insert : subbag_p (u_filter X v) (u_filter (insert i X) v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_purge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (leq(i, v) AND i /= v) IMPLIES subbag?(l_filter(X, i), purge(v, X))
theorem l_filter_purge : (leq i v ∧ i ≠ v) → subbag_p (l_filter X i) (purge v X) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_purge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (leq(i, v) AND i /= v) IMPLIES subbag?(u_filter(X, v), purge(i, X))
theorem u_filter_purge : (leq i v ∧ i ≠ v) → subbag_p (u_filter X v) (purge i X) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_u_diff_disj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (leq(v, i) AND i /= v) IMPLIES disjoint?(l_filter(X, v), u_filter(X, i))
theorem l_u_diff_disj : (leq v i ∧ i ≠ v) → disjoint_p (l_filter X v) (u_filter X i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_u_fullset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: union(purge(i, l_filter(X, i)), u_filter(X, i)) = X
theorem l_u_fullset : union (purge i (l_filter X i)) (u_filter X i) = X := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_u_fullset2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: union(purge(i, u_filter(X, i)), l_filter(X, i)) = X
theorem l_u_fullset2 : union (purge i (u_filter X i)) (l_filter X i) = X := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_u_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: card(l_filter(A, i)) + card(u_filter(A, i)) = card(A) + A(i)
theorem l_u_diff : card (l_filter A i) + card (u_filter A i) = card A + A i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.subset_diff_elements
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (leq(v, i) AND i /= v) IMPLIES
--  subbag?(union(l_filter(X, v), u_filter(X, i)), X)
theorem subset_diff_elements : (leq v i ∧ i ≠ v) → subbag_p (union (l_filter X v) (u_filter X i)) X := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.card_diff_elements
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (leq(v, i) AND i /= v) IMPLIES
--  card(l_filter(A, v)) + card(u_filter(A, i)) <= card(A)
theorem card_diff_elements : (leq v i ∧ i ≠ v) → card (l_filter A v) + card (u_filter A i) ≤ card A := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.card_plus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: card(A) + card(B) = card(union(A, B)) + card(intersection(A, B))
theorem card_plus : card A + card B = card (union A B) + card (intersection A B) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bag_filters.pigeonhole
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: card(A) + card(B) > card(union(A, B)) IMPLIES
--  EXISTS x: member(x, A) AND member(x, B)
theorem pigeonhole : card A + card B > card (union A B) → ∃ x,  member x A ∧ member x B := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bag_filters.maj_pigeonhole
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 * card(A) > card(C) AND
--  subbag?(A, C) AND 2 * card(B) >= card(C) AND subbag?(B, C)
--  IMPLIES EXISTS x: member(x, A) AND member(x, B)
theorem maj_pigeonhole : 2 * card A > card C ∧
 subbag_p A C ∧ 2 * card B ≥ card C ∧ subbag_p B C → ∃ x,  member x A ∧ member x B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 * card(l_filter(A, i)) > card(A) IMPLIES NOT empty?(l_filter(A, i))
theorem l_filter_nonempty : 2 * card (l_filter A i) > card A → ¬ empty_p (l_filter A i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 * card(u_filter(A, i)) > card(A) IMPLIES NOT empty?(u_filter(A, i))
theorem u_filter_nonempty : 2 * card (u_filter A i) > card A → ¬ empty_p (u_filter A i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_max
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT empty?(l_filter(A, i)) IMPLIES
--  l_filter(A, i) = l_filter(A, max(l_filter(A, i)))
theorem l_filter_max : ¬ empty_p (l_filter A i) → l_filter A i = l_filter A (max (l_filter A i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_min
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT empty?(u_filter(A, i)) IMPLIES
--  u_filter(A, i) = u_filter(A, min(u_filter(A, i)))
theorem u_filter_min : ¬ empty_p (u_filter A i) → u_filter A i = u_filter A (min (u_filter A i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.l_filter_max_purge
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT empty?(purge(i, l_filter(A, i))) IMPLIES
--  l_filter(A, max(purge(i, l_filter(A, i)))) = purge(i, l_filter(A, i))
theorem l_filter_max_purge : ¬ empty_p (purge i (l_filter A i)) → l_filter A (max (purge i (l_filter A i))) = purge i (l_filter A i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.u_filter_min_purge
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT empty?(purge(i, u_filter(A, i))) IMPLIES
--  u_filter(A, min(purge(i, u_filter(A, i)))) = purge(i, u_filter(A, i))
theorem u_filter_min_purge : ¬ empty_p (purge i (u_filter A i)) → u_filter A (min (purge i (u_filter A i))) = purge i (u_filter A i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bag_filters.filter_exists
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS i:
--    2 * card(l_filter(A, i)) > card(A) AND
--     2 * card(u_filter(A, i)) >= card(A))
--  OR empty?(A)
theorem filter_exists : (∃ i, 
   2 * card (l_filter A i) > card A ∧
    2 * card (u_filter A i) ≥ card A) ∨ empty_p A := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.bag_filters