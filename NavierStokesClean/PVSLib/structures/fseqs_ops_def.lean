-- Auto-generated by pvs2lean/translator.py
-- PVS theory: fseqs_ops_def
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: fseqs_ops_def

Auto-generated stubs from PVSLib theory `fseqs_ops_def`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.fseqs_ops_def

noncomputable section

variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (length : pvs_τ12 → pvs_τ13)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (seq : pvs_τ16 → pvs_τ17 → pvs_τ18)

-- pvs2lean: fseqs_ops_def.first
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fs`seq(0)
axiom first {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ7 → pvs_τ8

-- pvs2lean: fseqs_ops_def.rest
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fs ^ (1, fs`length)
axiom rest {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ14 → pvs_τ15

-- pvs2lean: fseqs_ops_def.delete
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n < nefs`length
--   THEN (# length := nefs`length - 1,
--           seq
--             := (LAMBDA i:
--                   (IF i < n THEN nefs`seq(i)
--                    ELSE nefs`seq(i + 1)
--                    
axiom delete {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6

-- pvs2lean: fseqs_ops_def.insert
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n <= fs`length
--   THEN (# length := fs`length + 1,
--           seq
--             := (LAMBDA i:
--                   IF i < n THEN fs`seq(i)
--                   ELSIF i = n THEN x
--                   ELSE fs`se
axiom insert {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: fseqs_ops_def.add
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: insert(x, 0, fs)
axiom add {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3

-- pvs2lean: fseqs_ops_def.addend
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := fs`length + 1,
--    seq
--      := (LAMBDA (ii: nat):
--            IF ii < fs`length THEN seq(fs)(ii)
--            ELSIF ii < fs`length + 1 THEN x
--            ELSE default
--            ENDIF) #)
noncomputable def addend : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := fs`length + 1,    seq      := (LAMBDA (ii: nat):            IF ii <

-- pvs2lean: fseqs_ops_def.insert_delete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n < nefs`length IMPLIES insert(nefs`seq(n), n, delete(n, nefs)) = nefs
theorem insert_delete : n < nefs.List.length → insert (nefs.seq n) n (delete n nefs) = nefs := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs_ops_def.add_first_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: add(first(nefs), rest(nefs)) = nefs
theorem add_first_rest : add (first nefs) (rest nefs) = nefs := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs_ops_def.every
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL n: n < fs`length IMPLIES p(fs`seq(n)))
noncomputable def every : sorry := by
  sorry  -- pvs2lean: definition stub for (FORALL n: n < fs`length IMPLIES p(fs`seq(n)))

-- pvs2lean: fseqs_ops_def.some
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS n: n < fs`length IMPLIES p(fs`seq(n)))
axiom pvs_some {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21

-- pvs2lean: fseqs_ops_def.some_p_trivial
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: some(p, fs)
theorem some_p_trivial : some p fs := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseqs_ops_def.map
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := length(fs),
--    seq
--      := (LAMBDA i:
--            IF i < length(fs) THEN F(fs`seq(i)) ELSE default ENDIF) #)
noncomputable def map : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := length(fs),    seq      := (LAMBDA i:            IF i < length(fs) 

end

end NavierStokesClean.PVSLib.fseqs_ops_def