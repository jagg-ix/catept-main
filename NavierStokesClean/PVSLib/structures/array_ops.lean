-- Auto-generated by pvs2lean/translator.py
-- PVS theory: array_ops
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: array_ops

Auto-generated stubs from PVSLib theory `array_ops`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.array_ops

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (a_m : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (a_n : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (barr : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (below : pvs_τ7 → pvs_τ8)

-- pvs2lean: array_ops.fill
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA (ii: below(n)): t)
axiom fill {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ9 → pvs_τ10 → Prop

-- pvs2lean: array_ops.caret_fill0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(n)):
--   i <= j IMPLIES fill(n, t) ^ (i, j) = fill(j - i + 1, t)
theorem caret_fill0 : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < n}), i ≤ j → fill n t ^ (i, j) = fill (j - i + 1) t := by
  -- === TCCs for caret_fill0 ===
  -- TCC: caret_fill0_TCC1
  -- Statement: FORALL (i, n: nat, j: below(n)): i <= j IMPLIES j - i + 1 >= 0
  have caret_fill0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_fill0_TCC2
  -- Statement: FORALL (i, n: nat, j: below(n)):
  --   i <= j IMPLIES
  --    FORALL (x: nat):
  --      x < 1 + j - i IFF x < IF i > j THEN 0 ELSE 1 + j - i ENDIF
  have caret_fill0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: array_ops.caret_fill1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(n)):
--   i <= j IMPLIES fill(n, t) ^ (i, j) = fill(j - i + 1, t)
theorem caret_fill1 : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < n}), i ≤ j → fill n t ^ (i, j) = fill (j - i + 1) t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: array_ops.caret_concat_bot
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(n), A_n: barr(n), B_m: barr(m)):
--   j < n IMPLIES (A_n o B_m) ^ (i, j) = A_n ^ (i, j)
theorem caret_concat_bot : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < n}) (A_n : {s : {i : ℝ // i ≥ 0} // s < n} → T) (B_m : {s : {i : ℝ // i ≥ 0} // s < m} → T), j < n → (A_n ∘ B_m) ^ (i, j) = A_n ^ (i, j) := by
  -- === TCCs for caret_concat_bot ===
  -- TCC: caret_concat_bot_TCC1
  -- Statement: FORALL (m, n: nat, j: below(n), A_n: barr(n), B_m: barr(m)):
  --   j < n IMPLIES j < m + n
  have caret_concat_bot_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.caret_concat_top
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A_n: barr(n), B_m: barr(m)):
--   i >= n AND j >= n AND j < n + m IMPLIES
--    (A_n o B_m) ^ (i, j) = B_m ^ (i - n, j - n)
theorem caret_concat_top : ∀ (A_n : {s : {i : ℝ // i ≥ 0} // s < n} → T) (B_m : {s : {i : ℝ // i ≥ 0} // s < m} → T), i ≥ n ∧ j ≥ n ∧ j < n + m → (A_n ∘ B_m) ^ (i, j) = B_m ^ (i - n, j - n) := by
  -- === TCCs for caret_concat_top ===
  -- TCC: caret_concat_top_TCC1
  -- Statement: FORALL (i, j, m, n: nat, A_n: barr(n), B_m: barr(m)):
  --   i >= n AND j >= n AND j < n + m IMPLIES j < m + n
  have caret_concat_top_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_concat_top_TCC2
  -- Statement: FORALL (i, j, m, n: nat, A_n: barr(n), B_m: barr(m)):
  --   i >= n AND j >= n AND j < n + m IMPLIES i - n >= 0
  have caret_concat_top_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_concat_top_TCC3
  -- Statement: FORALL (i, j, m, n: nat, A_n: barr(n), B_m: barr(m)):
  --   i >= n AND j >= n AND j < n + m IMPLIES j - n >= 0 AND j - n < m
  have caret_concat_top_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_concat_top_TCC4
  -- Statement: FORALL (i, j, m, n: nat, A_n: barr(n), B_m: barr(m)):
  --   i >= n AND j >= n AND j < n + m IMPLIES
  --    FORALL (x: nat):
  --      x < IF i - n > j - n THEN 0 ELSE 1 + j - i ENDIF IFF
  --       x < IF i > j THEN...
  have caret_concat_top_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.caret_concat_all
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(n), A_n: barr(n), B_m: barr(m)):
--   i <= n AND n <= j IMPLIES
--    (A_n o B_m) ^ (i, j) = A_n ^ (0, i - n) o B_m ^ (j, n - 1)
theorem caret_concat_all : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < n}) (A_n : {s : {i : ℝ // i ≥ 0} // s < n} → T) (B_m : {s : {i : ℝ // i ≥ 0} // s < m} → T), i ≤ n ∧ n ≤ j → (A_n ∘ B_m) ^ (i, j) = A_n ^ (0, i - n) ∘ B_m ^ (j, n - 1) := by
  -- === TCCs for caret_concat_all ===
  -- TCC: caret_concat_all_TCC1
  -- Statement: FORALL (i, m, n: nat, j: below(n), A_n: barr(n), B_m: barr(m)):
  --   i <= n AND n <= j IMPLIES j < m + n
  have caret_concat_all_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_concat_all_TCC2
  -- Statement: FORALL (i, m, n: nat, j: below(n), A_n: barr(n), B_m: barr(m)):
  --   i <= n AND n <= j IMPLIES i - n >= 0 AND i - n < n
  have caret_concat_all_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_concat_all_TCC3
  -- Statement: FORALL (i, m, n: nat, j: below(n), A_n: barr(n), B_m: barr(m)):
  --   i <= n AND n <= j IMPLIES n - 1 >= 0 AND n - 1 < m
  have caret_concat_all_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_concat_all_TCC4
  -- Statement: FORALL (i, m, n: nat, j: below(n), A_n: barr(n), B_m: barr(m)):
  --   i <= n AND n <= j AND NOT 0 > i - n IMPLIES 1 + i - n >= 0
  have caret_concat_all_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_concat_all_TCC5
  -- Statement: FORALL (i, m, n: nat, j: below(n), A_n: barr(n), B_m: barr(m)):
  --   i <= n AND n <= j AND NOT j > n - 1 IMPLIES n - j >= 0
  have caret_concat_all_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_concat_all_TCC6
  -- Statement: FORALL (i, m, n: nat, j: below(n), A_n: barr(n), B_m: barr(m)):
  --   i <= n AND n <= j IMPLIES
  --    FORALL (x: nat):
  --      x <
  --       IF j > n - 1 THEN 0 ELSE n - j ENDIF +
  --        IF 0 > i - n THEN 0 ELSE...
  have caret_concat_all_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.array_decomp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, k: below(n - 1), A_n: barr(n)):
--   (A_n ^ (0, k)) o (A_n ^ (k + 1, n - 1)) = A_n
theorem array_decomp : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (k : {s : {i : ℝ // i ≥ 0} // s < n - 1}) (A_n : {s : {i : ℝ // i ≥ 0} // s < n} → T), (A_n ^ (0, k)) ∘ (A_n ^ (k + 1, n - 1)) = A_n := by
  -- === TCCs for array_decomp ===
  -- TCC: array_decomp_TCC1
  -- Statement: FORALL (n: posnat, k: below(n - 1), A_n: barr(n)): k < n
  have array_decomp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: array_decomp_TCC2
  -- Statement: FORALL (n: posnat, k: below(n - 1), A_n: barr(n)): n - 1 >= 0 AND n - 1 < n
  have array_decomp_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: array_decomp_TCC3
  -- Statement: FORALL (n: posnat, k: below(n - 1), A_n: barr(n)):
  --   NOT 1 + k > n - 1 IMPLIES n - 1 - k >= 0
  have array_decomp_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: array_decomp_TCC4
  -- Statement: FORALL (n: posnat, k: below(n - 1), A_n: barr(n)):
  --   FORALL (x: nat):
  --     x < n IFF
  --      x <
  --       IF 1 + k > n - 1 THEN 0 ELSE n - 1 - k ENDIF +
  --        IF 0 > k THEN 0 ELSE 1 + k ENDIF
  have array_decomp_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.caret_bot_all
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, A_n: barr(n), B_m: barr(m)):
--   (A_n o B_m) ^ (0, n - 1) = A_n
theorem caret_bot_all : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (A_n : {s : {i : ℝ // i ≥ 0} // s < n} → T) (B_m : {s : {i : ℝ // i ≥ 0} // s < m} → T), (A_n ∘ B_m) ^ (0, n - 1) = A_n := by
  -- === TCCs for caret_bot_all ===
  -- TCC: caret_bot_all_TCC1
  -- Statement: FORALL (m: nat, n: posnat, A_n: barr(n), B_m: barr(m)):
  --   n - 1 >= 0 AND n - 1 < n + m
  have caret_bot_all_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_bot_all_TCC2
  -- Statement: FORALL (m: nat, n: posnat, A_n: barr(n), B_m: barr(m)):
  --   FORALL (x: nat): x < n IFF x < IF 0 > n - 1 THEN 0 ELSE n ENDIF
  have caret_bot_all_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.caret_top_all
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A_n: barr(n), B_m: barr(m)):
--   n + m > 0 IMPLIES (A_n o B_m) ^ (n, n + m - 1) = B_m
theorem caret_top_all : ∀ (A_n : {s : {i : ℝ // i ≥ 0} // s < n} → T) (B_m : {s : {i : ℝ // i ≥ 0} // s < m} → T), n + m > 0 → (A_n ∘ B_m) ^ (n, n + m - 1) = B_m := by
  -- === TCCs for caret_top_all ===
  -- TCC: caret_top_all_TCC1
  -- Statement: FORALL (m, n: nat, A_n: barr(n), B_m: barr(m)):
  --   n + m > 0 IMPLIES n + m - 1 >= 0 AND n + m - 1 < m + n
  have caret_top_all_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_top_all_TCC2
  -- Statement: FORALL (m, n: nat, A_n: barr(n), B_m: barr(m)):
  --   n + m > 0 IMPLIES
  --    FORALL (x: nat): x < m IFF x < IF n > m + n - 1 THEN 0 ELSE m ENDIF
  have caret_top_all_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.concat_array_bot
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: below(m), a_m: below_array[m, T], a_n: below_array[n, T]):
--    (a_m o a_n)(i) = a_m(i))
theorem concat_array_bot : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < m}) (a_m : {s : {i : ℝ // i ≥ 0} // s < m} → T) (a_n : {s : {i : ℝ // i ≥ 0} // s < n} → T), (a_m ∘ a_n) (i) = a_m i := by
  -- === TCCs for concat_array_bot ===
  -- TCC: concat_array_bot_TCC1
  -- Statement: FORALL (m, n: nat, i: below(m), a_m: below_array[m, T],
  --         a_n: below_array[n, T]):
  --   i < m + n
  have concat_array_bot_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: concat_array_bot_TCC1
  -- Statement: FORALL (i: below(n)): i < m + n
  have concat_array_bot_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.concat_array_top
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (j: {i: int | i >= m AND i < n + m}, a_m: below_array[m, T],
--          a_n: below_array[n, T]):
--    (a_m o a_n)(j) = a_n(j - m))
theorem concat_array_top : ∀ (j : {i : ℝ // i ≥ m ∧ i < m + n}) (a_m : {s : {i : ℝ // i ≥ 0} // s < m} → T) (a_n : {s : {i : ℝ // i ≥ 0} // s < n} → T), (a_m ∘ a_n) (j) = a_n (j - m) := by
  -- === TCCs for concat_array_top ===
  -- TCC: concat_array_top_TCC1
  -- Statement: FORALL (m, n: nat, j: {i: int | i >= m AND i < m + n},
  --         a_m: below_array[m, T], a_n: below_array[n, T]):
  --   j >= 0 AND j < m + n
  have concat_array_top_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: concat_array_top_TCC2
  -- Statement: FORALL (m, n: nat, j: {i: int | i >= m AND i < m + n},
  --         a_m: below_array[m, T], a_n: below_array[n, T]):
  --   j - m >= 0 AND j - m < n
  have concat_array_top_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: concat_array_top_TCC1
  -- Statement: FORALL (j: {i: int | i >= n AND i < m + n}): j >= 0 AND j < m + n
  have concat_array_top_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: concat_array_top_TCC2
  -- Statement: FORALL (j: {i: int | i >= n AND i < m + n}): j - n >= 0 AND j - n < m
  have concat_array_top_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.concat_array_r
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (a_n: below_array[n, T]): a_n o empty_array[T] = a_n)
theorem concat_array_r : ∀ (a_n : {s : {i : ℝ // i ≥ 0} // s < n} → T), a_n ∘ empty_array = a_n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.concat_array_l
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (a_n: below_array[n, T]): empty_array[T] o a_n = a_n)
theorem concat_array_l : ∀ (a_n : {s : {i : ℝ // i ≥ 0} // s < n} → T), empty_array ∘ a_n = a_n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: array_ops.concat_array_assoc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (a_m: below_array[m, T]),
--         (a_n: below_array[n, T]), (a_j: below_array[j, T]):
--    a_m o (a_n o a_j) = (a_m o a_n) o a_j)
theorem concat_array_assoc : ∀ (a_m : {s : {i : ℝ // i ≥ 0} // s < m} → T) (a_n : {s : {i : ℝ // i ≥ 0} // s < n} → T) (a_j : {s : {i : ℝ // i ≥ 0} // s < j} → T), a_m ∘ (a_n ∘ a_j) = (a_m ∘ a_n) ∘ a_j := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.array_ops