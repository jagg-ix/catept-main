-- Auto-generated by pvs2lean/translator.py
-- PVS theory: indexed_list
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: indexed_list

Auto-generated stubs from PVSLib theory `indexed_list`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.indexed_list

noncomputable section

variable {pvs_τ1 : Type*} (car_ol : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (cons : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (last_ol : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (length : pvs_τ9 → pvs_τ10)
variable {pvs_τ13 : Type*} (null_ol_p : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} (null_p : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} (ordered_list_p : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} (strict_order_p : pvs_τ16 → Prop)

-- pvs2lean: indexed_list.lt_idx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: t1`1 < t2`1
axiom lt_idx {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12 → Prop

-- pvs2lean: indexed_list.le_idx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: t1`1 = t2`1 OR lt_idx(t1, t2)
noncomputable def le_idx : sorry := by
  sorry  -- pvs2lean: definition stub for t1`1 = t2`1 OR lt_idx(t1, t2)

-- pvs2lean: indexed_list.lt_idx_strict_order
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_order?(lt_idx[T])
theorem lt_idx_strict_order : strict_order_p lt_idx := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: indexed_list.olidx_car_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ol: ordered_list[[nat, T], lt_idx[T]]):
--   NOT null?(ol) IMPLIES car_ol(ol)`1 <= last_ol(ol)`1
theorem olidx_car_last : ∀ (ol : {l : List ({i : ℝ // i ≥ 0} × T) // ordered_list_p l}), ¬ null_p ol → car_ol ol.1 ≤ last_ol ol.1 := by
  -- === TCCs for olidx_car_last ===
  -- TCC: olidx_car_last_TCC1
  -- Statement: strict_order?[[nat, T]](lt_idx[T])
  have olidx_car_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: olidx_car_last_TCC2
  -- Statement: FORALL (ol: ordered_list[[nat, T], lt_idx[T]]):
  --   NOT null?(ol) IMPLIES NOT null_ol?[[nat, T], lt_idx[T]](ol)
  have olidx_car_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: indexed_list.last_idx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF NOT null?(l) THEN last_ol(l)`1 ELSE -1 ENDIF
axiom last_idx {pvs_τ6 : Type*} : pvs_τ6 → Prop

-- pvs2lean: indexed_list.first
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES l
--   OF null: null,
--      cons(h, t):
--        IF h`1 < n THEN cons_ol[[nat, T], lt_idx[T]](h, first(t, n))
--        ELSE null
--        ENDIF
--   ENDCASES
axiom first {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ4 → pvs_τ5 → Prop

-- pvs2lean: indexed_list.first_is_ol
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: ordered_list[[nat, T], lt_idx[T]]):
--   ordered_list?[[nat, T], lt_idx[T]](first[T](l, n))
theorem first_is_ol : ∀ (l : {l : List ({i : ℝ // i ≥ 0} × T) // ordered_list_p l}), ordered_list_p (first l n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: indexed_list.last_up_to_idx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: ordered_list[[nat, T], lt_idx[T]]):
--   NOT null?(first[T](l, n)) IMPLIES
--    last_ol[[nat, T], lt_idx[T]](first[T](l, n))`1 < n
theorem last_up_to_idx : ∀ (l : {l : List ({i : ℝ // i ≥ 0} × T) // ordered_list_p l}), ¬ null_p (first l n) → last_ol (first[T](l, n)).1 < n := by
  -- === TCCs for last_up_to_idx ===
  -- TCC: last_up_to_idx_TCC1
  -- Statement: FORALL (n: nat, l: ordered_list[[nat, T], lt_idx[T]]):
  --   NOT null?(first[T](l, n)) IMPLIES
  --    ordered_list?[[nat, T], lt_idx[T]](first[T](l, n))
  have last_up_to_idx_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: indexed_list.first_zero_prop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: ordered_list[[nat, T], lt_idx[T]]): first(l, 0) = null
theorem first_zero_prop : ∀ (l : {l : List ({i : ℝ // i ≥ 0} × T) // ordered_list_p l}), first l 0 = null := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: indexed_list.first_all_prop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: ordered_list[[nat, T], lt_idx[T]]):
--   n > last_idx(l) IMPLIES first(l, n) = l
theorem first_all_prop : ∀ (l : {l : List ({i : ℝ // i ≥ 0} × T) // ordered_list_p l}), n > last_idx l → first l n = l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: indexed_list.car_up_to_idx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: ordered_list[[nat, T], lt_idx[T]]):
--   NOT null?(first[T](l, n)) AND NOT null_ol?(l) AND m < car_ol(l)`1 IMPLIES
--    m < car_ol[[nat, T], lt_idx[T]](first(l, n))`1
theorem car_up_to_idx : ∀ (l : {l : List ({i : ℝ // i ≥ 0} × T) // ordered_list_p l}), ¬ null_p (first l n) ∧ ¬ null_ol_p l ∧ m < car_ol l.1 → m < car_ol (first l n).1 := by
  -- === TCCs for car_up_to_idx ===
  -- TCC: car_up_to_idx_TCC1
  -- Statement: FORALL (m, n: nat, l: ordered_list[[nat, T], lt_idx[T]]):
  --   NOT null?(first[T](l, n)) AND NOT null_ol?(l) AND m < car_ol(l)`1 IMPLIES
  --    ordered_list?[[nat, T], lt_idx[T]](first(l, n)) AND
  --     NOT ...
  have car_up_to_idx_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.indexed_list