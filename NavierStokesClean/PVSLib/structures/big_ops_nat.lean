-- Auto-generated by pvs2lean/translator.py
-- PVS theory: big_ops_nat
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: big_ops_nat

Auto-generated stubs from PVSLib theory `big_ops_nat`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.big_ops_nat

noncomputable section

variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (pvs_for : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (sig : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (subrange : pvs_τ10 → pvs_τ11 → pvs_τ12)

-- pvs2lean: big_ops_nat.big_ops_nat
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: for(i, j, id, LAMBDA (k: subrange(i, j), t: T): t o F(k))
axiom big_ops_nat {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop

-- pvs2lean: big_ops_nat.big_ops_nat_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (k: nat): i <= k AND k <= j IMPLIES F(k) = G(k)) IMPLIES
--  big_ops_nat(i, j, F) = big_ops_nat(i, j, G)
theorem big_ops_nat_eq : (∀ (k : ℕ), i ≤ k ∧ k ≤ j → F k = G k) → big_ops_nat i j F = big_ops_nat i j G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_expand_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: big_ops_nat(i, j, F) =
--  (IF i <= j THEN big_ops_nat(i, j - 1, F) o F(j) ELSE id ENDIF)
theorem big_ops_nat_expand_right : big_ops_nat i j F = (if i ≤ j then big_ops_nat i (j - 1) F ∘ F j else id ) := by
  -- === TCCs for big_ops_nat_expand_right ===
  -- TCC: big_ops_nat_expand_right_TCC1
  -- Statement: FORALL (i: nat, j: int): i <= j IMPLIES j >= 0
  have big_ops_nat_expand_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_expand_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: big_ops_nat(i, j, F) =
--  (IF i <= j THEN F(i) o big_ops_nat(i + 1, j, F) ELSE id ENDIF)
theorem big_ops_nat_expand_left : big_ops_nat i j F = (if i ≤ j then F i ∘ big_ops_nat (i + 1) j F else id ) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_split
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i - 1 <= z AND z <= j IMPLIES
--  big_ops_nat(i, j, F) = big_ops_nat(i, z, F) o big_ops_nat(z + 1, j, F)
theorem big_ops_nat_split : i - 1 ≤ z ∧ z ≤ j → big_ops_nat i j F = big_ops_nat i z F ∘ big_ops_nat (z + 1) j F := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_shift
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: i + p >= 0 IMPLIES
--  big_ops_nat(i + p, j + p, F) =
--   big_ops_nat(i, j,
--               LAMBDA (k: nat): IF k + p >= 0 THEN F(k + p) ELSE id ENDIF)
theorem big_ops_nat_shift : i + p ≥ 0 → big_ops_nat (i + p) (j + p) F =
 big_ops_nat i j (fun (k : ℕ)=> if k + p ≥ 0 then F (k + p) else id) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_reverse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: commutative?[T](o) IMPLIES
--  big_ops_nat(i, j, F) =
--   big_ops_nat(i, j,
--               LAMBDA (k: nat):
--                 IF i + j - k >= 0 THEN F(i + j - k) ELSE id ENDIF)
theorem big_ops_nat_reverse : commutative_p o → big_ops_nat i j F =
 big_ops_nat i j (fun (k : ℕ)=> if i + j - k ≥ 0 then F (i + j - k) else id) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_id
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (k: nat): i <= k AND k <= j IMPLIES F(k) = id) IMPLIES
--  big_ops_nat(i, j, F) = id
theorem big_ops_nat_id : (∀ (k : ℕ), i ≤ k ∧ k ≤ j → F k = id) → big_ops_nat i j F = id := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_comp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem big_ops_nat_comp : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: big_ops_nat.big_ops_nat_tolambda
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: big_ops_nat(i, j, F) = big_ops_nat(i, j, LAMBDA (k: nat): F(k))
theorem big_ops_nat_tolambda : big_ops_nat i j F = big_ops_nat i j (fun (k : ℕ)=> F k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_distrib
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= j AND (FORALL (t1, t2, t3: T): t1 * (t2 o t3) = (t1 * t2) o (t1 * t3))
--  IMPLIES
--  c * big_ops_nat(i, j, F) = big_ops_nat(i, j, LAMBDA (k: nat): c * F(k))
theorem big_ops_nat_distrib : i ≤ j ∧ (∀ (t1 : _) (t2 : _) (t3 : T), t1 * (t2 ∘ t3) = (t1 * t2) ∘ (t1 * t3)) → c * big_ops_nat i j F = big_ops_nat i j (fun (k : ℕ)=> c * F k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_eq_one_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= z AND
--  z <= j AND
--   (FORALL (k: subrange(i, z - 1)): F(k) = id) AND
--    (FORALL (k: subrange(z + 1, j)): F(k) = id)
--  IMPLIES big_ops_nat(i, j, F) = F(z)
theorem big_ops_nat_eq_one_arg : i ≤ z ∧
 z ≤ j ∧
  (∀ (k : Set.Icc i z - 1), F k = id) ∧
   (∀ (k : Set.Icc z + 1 j), F k = id) → big_ops_nat i j F = F z := by
  -- === TCCs for big_ops_nat_eq_one_arg ===
  -- TCC: big_ops_nat_eq_one_arg_TCC1
  -- Statement: FORALL (i: nat, j: int, z: nat):
  --   z <= j AND i <= z IMPLIES FORALL (k: subrange(i, z - 1)): k >= 0
  have big_ops_nat_eq_one_arg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: big_ops_nat_eq_one_arg_TCC2
  -- Statement: FORALL (F: [nat -> T], i: nat, j: int, z: nat):
  --   (FORALL (k: subrange(i, z - 1)): F(k) = id) AND z <= j AND i <= z IMPLIES
  --    FORALL (k: subrange(1 + z, j)): k >= 0
  have big_ops_nat_eq_one_arg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: big_ops_nat.big_ops_nat_bij
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem big_ops_nat_bij : True := by
  -- === TCCs for big_ops_nat_bij ===
  -- TCC: big_ops_nat_bij_TCC1
  -- Statement: FORALL (i: nat, j: int, k: subrange(i, j)): k >= 0
  have big_ops_nat_bij_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: big_ops_nat_bij_TCC2
  -- Statement: FORALL (i: nat, j: int, sig: [nat -> nat]):
  --   (FORALL (k: subrange(i, j)): i <= sig(k) AND sig(k) <= j) IMPLIES
  --    FORALL (k: subrange(i, j), z: subrange(i, j)): z >= 0
  have big_ops_nat_bij_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: big_ops_nat.big_ops_nat_eq_involution
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem big_ops_nat_eq_involution : True := by
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.big_ops_nat