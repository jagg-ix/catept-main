-- Auto-generated by pvs2lean/translator.py
-- PVS theory: doubletons
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: doubletons

Auto-generated stubs from PVSLib theory `doubletons`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.doubletons

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (PROJ_1 : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (PROJ_2 : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (below : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} (card : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} (choose : pvs_τ7 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (is_finite : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} (length : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} (nonempty_p : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (rest : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (seq : pvs_τ18 → pvs_τ19 → pvs_τ20)

-- pvs2lean: doubletons.dbl
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {t: T | t = x OR t = y}
axiom dbl {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: doubletons.doubleton?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS x, y: x /= y AND S = dbl(x, y))
axiom doubleton_p {pvs_τ11 : Type*} : pvs_τ11 → Prop

-- pvs2lean: doubletons.dbl_comm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dbl(x, y) = dbl(y, x)
theorem dbl_comm : dbl x y = dbl y x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: doubletons.doubleton_irreflexive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT doubleton?(dbl(x, x))
theorem doubleton_irreflexive : ¬(doubleton_p (dbl x x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.doubleton_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(D)
theorem doubleton_nonempty : nonempty_p D := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.doubleton_rest_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(rest(D))
theorem doubleton_rest_nonempty : nonempty_p (rest D) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.dbl_choose
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: choose(dbl(x, y)) = x OR choose(dbl(x, y)) = y
theorem dbl_choose : choose (dbl x y) = x ∨ choose (dbl x y) = y := by
  -- === TCCs for dbl_choose ===
  -- TCC: dbl_choose_TCC1
  -- Statement: FORALL (x, y: T): nonempty?[T](dbl(x, y))
  have dbl_choose_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: doubletons.dbl_rest_choose
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= y IMPLIES
--  (choose(dbl(x, y)) /= choose(rest(dbl(x, y)))) AND
--   (choose(rest(dbl(x, y))) = x OR choose(rest(dbl(x, y))) = y)
theorem dbl_rest_choose : x ≠ y → (choose (dbl x y) ≠ choose (rest (dbl x y))) ∧
 (choose (rest (dbl x y)) = x ∨ choose (rest (dbl x y)) = y) := by
  -- === TCCs for dbl_rest_choose ===
  -- TCC: dbl_rest_choose_TCC1
  -- Statement: FORALL (x, y: T): x /= y IMPLIES nonempty?[T](rest[T](dbl(x, y)))
  have dbl_rest_choose_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: doubletons.dbl_to_pair
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom dbl_to_pair {pvs_τ10 : Type*} : pvs_τ10 → Prop

-- pvs2lean: doubletons.dbl_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dbl(PROJ_1(dbl_to_pair(D)), PROJ_2(dbl_to_pair(D))) = D
theorem dbl_def : dbl (PROJ_1 (dbl_to_pair D)) (PROJ_2 (dbl_to_pair D)) = D := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.dbl_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: D = dbl(x, y) IMPLIES D(x) AND D(y)
theorem dbl_in : D = dbl x y → D x ∧ D y := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: doubletons.dbl_not_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: D = dbl(x, y) AND z /= x AND z /= y IMPLIES NOT D(z)
theorem dbl_not_in : D = dbl x y ∧ z ≠ x ∧ z ≠ y → ¬ D z := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: doubletons.dbl_to_pair_lem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= y IMPLIES
--  dbl_to_pair(dbl(x, y)) = (x, y) OR dbl_to_pair(dbl(x, y)) = (y, x)
theorem dbl_to_pair_lem : x ≠ y → dbl_to_pair (dbl x y) = (x, y) ∨ dbl_to_pair (dbl x y) = (y, x) := by
  -- === TCCs for dbl_to_pair_lem ===
  -- TCC: dbl_to_pair_lem_TCC1
  -- Statement: FORALL (x, y: T):
  --   x /= y IMPLIES
  --    EXISTS (x_1: T), (y_1: T): x_1 /= y_1 AND dbl(x, y) = dbl(x_1, y_1)
  have dbl_to_pair_lem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.dbl_to_pair_inverse_l
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dbl(dbl_to_pair(D)) = D
theorem dbl_to_pair_inverse_l : dbl (dbl_to_pair D) = D := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.dbl_proj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: D(PROJ_1(dbl_to_pair(D))) AND D(PROJ_2(dbl_to_pair(D)))
theorem dbl_proj : D (PROJ_1 (dbl_to_pair D)) ∧ D (PROJ_2 (dbl_to_pair D)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.proj_dbl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (PROJ_1(dbl_to_pair(D)) = v OR PROJ_2(dbl_to_pair(D)) = v) IMPLIES D(v)
theorem proj_dbl : (PROJ_1 (dbl_to_pair D) = v ∨ PROJ_2 (dbl_to_pair D) = v) → D v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.dbl_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dbl(x, y) = dbl(v, z) IMPLIES (x = v AND y = z) OR (x = z AND y = v)
theorem dbl_eq : dbl x y = dbl v z → (x = v ∧ y = z) ∨ (x = z ∧ y = v) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: doubletons.finite_dbl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(dbl(x, y))
theorem finite_dbl : is_finite (dbl x y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.finite_doubleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(e)
theorem finite_doubleton : is_finite e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: doubletons.card_dbl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= y IMPLIES card(dbl(x, y)) = 2
theorem card_dbl : x ≠ y → card (dbl x y) = 2 := by
  -- === TCCs for card_dbl ===
  -- TCC: card_dbl_TCC1
  -- Statement: FORALL (x, y: T): x /= y IMPLIES is_finite[T](dbl(x, y))
  have card_dbl_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: doubletons.rev
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := length(fs),
--    seq := (LAMBDA (i: below(length(fs))): seq(fs)(length(fs) - 1 - i)) #)
noncomputable def rev : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := length(fs),    seq := (LAMBDA (i: below(length(fs))): seq(fs)(lengt

end

end NavierStokesClean.PVSLib.doubletons