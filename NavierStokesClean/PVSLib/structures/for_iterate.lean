-- Auto-generated by pvs2lean/translator.py
-- PVS theory: for_iterate
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: for_iterate

Auto-generated stubs from PVSLib theory `for_iterate`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.for_iterate

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (ForBody : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (IterateBody : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (below : pvs_τ7 → pvs_τ8)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (inv : pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (iterate : pvs_τ46 → pvs_τ47 → pvs_τ48 → pvs_τ49)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} (max : pvs_τ68 → pvs_τ69 → Prop)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} {pvs_τ72 : Type*} (subrange : pvs_τ70 → pvs_τ71 → pvs_τ72)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} (upfrom : pvs_τ73 → pvs_τ74)
variable {pvs_τ75 : Type*} {pvs_τ76 : Type*} (upto : pvs_τ75 → pvs_τ76)

-- pvs2lean: for_iterate.for_def
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i > j THEN a ELSE f(j, for_def(i, j - 1, a, f)) ENDIF
axiom for_def {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: for_iterate.for_def_inv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: int, n: nat, a: T, f: [[int, T] -> T]):
--   for_def(i, n + i, a, f) = for_def(1 + i, n + i, f(i, a), f)
theorem for_def_inv : ∀ (i : ℝ) (n : {i : ℝ // i ≥ 0}) (a : T) (f : ℝ × T → T), for_def i (n + i) a f = for_def (1 + i) (n + i) (f i a) f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_shift
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, shift: int, a: T, f: [[int, T] -> T]):
--   for_def(i, j, a, f) =
--    for_def(i + shift, j + shift, a, LAMBDA (k: int, t: T): f(k - shift, t))
theorem for_shift : ∀ (i : ℝ) (j : ℝ) (shift : ℝ) (a : T) (f : ℝ × T → T), for_def i j a f = for_def (i + shift) (j + shift) a (fun (k : ℤ) (t : T)=> f (k - shift) t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_def_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, a: T, f, g: [[int, T] -> T]):
--   (FORALL (x: subrange(i, j), t: T): f(x, t) = g(x, t)) IMPLIES
--    for_def(i, j, a, f) = for_def(i, j, a, g)
theorem for_def_ext : ∀ (i : ℝ) (j : ℝ) (a : T) (f : ℝ × T → T) (g : ℝ × T → T), (∀ (x : Set.Icc i j) (t : T), f x t = g x t) → for_def i j a f = for_def i j a g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_def_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, a: T, f: [[int, T] -> T], inv: PRED[[nat, T]]):
--   (inv(0, a) AND
--     FORALL (k: subrange(0, j - i), ak: T):
--       inv(k, ak) IMPLIES inv(k + 1, f(i + k, ak)))
--    IMPLIES inv(max(0, 
theorem for_def_induction : ∀ (i : ℝ) (j : ℝ) (a : T) (f : ℝ × T → T) (inv : {i : ℝ // i ≥ 0} × T → Bool), (inv 0 a ∧
  ∀ (k : Set.Icc 0 j - i) (ak : T), inv k ak → inv (k + 1) (f (i + k) ak)) → inv (max 0 (j - i + 1)) (for_def i j a f) := by
  -- === TCCs for for_def_induction ===
  -- TCC: for_def_induction_TCC1
  -- Statement: FORALL (i, j: int, a: T, f: [[int, T] -> T], inv: PRED[[nat, T]]):
  --   inv(0, a) IMPLIES FORALL (k: subrange(0, j - i), ak: T): k >= 0
  have for_def_induction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_def_induction_TCC2
  -- Statement: FORALL (i, j: int, a: T, f: [[int, T] -> T], inv: PRED[[nat, T]]):
  --   inv(0, a) IMPLIES
  --    FORALL (k: subrange(0, j - i), ak: T): inv(k, ak) IMPLIES k + 1 >= 0
  have for_def_induction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_def_induction_TCC3
  -- Statement: FORALL (i, j: int, a: T, f: [[int, T] -> T], inv: PRED[[nat, T]]):
  --   (inv(0, a) AND
  --     FORALL (k: subrange(0, j - i), ak: T):
  --       inv(k, ak) IMPLIES inv(k + 1, f(i + k, ak)))
  --    IMPLIES max(0, j...
  have for_def_induction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.ext2int
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF upfrom <= i AND i <= upto THEN f(i, a) ELSE a ENDIF
axiom ext2int {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13

-- pvs2lean: for_iterate.for_it
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i > upto THEN a ELSE for_it(upfrom, i + 1, upto, f(i, a), f) ENDIF
axiom for_it {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37

-- pvs2lean: for_iterate.for
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: for_it(m, m, n, init, f)
axiom pvs_for {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: for_iterate.for_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, a: T, f: ForBody(i, j)):
--   for(i, j, a, f) = for_def(i, j, a, ext2int(i, j, f))
theorem for_eq : ∀ (i : ℝ) (j : ℝ) (a : T) (f : {k : ℝ // i ≤ k ∧ k ≤ j} × T → T), for i j a f = for_def i j a (ext2int i j f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_induction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
--         inv: PRED[[upto(n - m + 1), T]]):
--   (inv(0, init) AND
--     FORALL (k: subrange(0, n - m), ak: T):
--       inv(k, ak) IMPLIES inv(
theorem for_induction : ∀ (m : ℝ) (n : {n : ℝ // n ≥ m - 1}) (init : T) (f : {k : ℝ // m ≤ k ∧ k ≤ n} × T → T) (inv : {s : {i : ℝ // i ≥ 0} // s ≤ 1 + n - m} × T → Bool), (inv 0 init ∧
  ∀ (k : Set.Icc 0 n - m) (ak : T), inv k ak → inv (k + 1) (f (m + k) ak)) → inv (n - m + 1) (for m n init f) := by
  -- === TCCs for for_induction ===
  -- TCC: for_induction_TCC1
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n)):
  --   0 <= 1 + n - m
  have for_induction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_induction_TCC2
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[upto(1 + n - m), T]]):
  --   inv(0, init) IMPLIES
  --    FORALL (k: subrange(0, n - m), ak: T): k >= 0 AND k <= 1 + n - m
  have for_induction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_induction_TCC3
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[upto(1 + n - m), T]]):
  --   inv(0, init) IMPLIES
  --    FORALL (k: subrange(0, n - m), ak: T):
  --      inv(k, ak) IMPLIES ...
  have for_induction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_induction_TCC4
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[upto(1 + n - m), T]]):
  --   inv(0, init) IMPLIES
  --    FORALL (k: subrange(0, n - m), ak: T):
  --      inv(k, ak) IMPLIES ...
  have for_induction_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_induction_TCC5
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[upto(1 + n - m), T]]):
  --   (inv(0, init) AND
  --     FORALL (k: subrange(0, n - m), ak: T):
  --       inv(k, ak) IMPLIES i...
  have for_induction_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: for_iterate.for_invariant
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
--         inv: PRED[[subrange(m, n + 1), T]]):
--   (inv(m, init) AND
--     FORALL (k: subrange(m, n), ak: T):
--       inv(k, ak) IMPLIES inv(k
theorem for_invariant : ∀ (m : ℝ) (n : {n : ℝ // n ≥ m - 1}) (init : T) (f : {k : ℝ // m ≤ k ∧ k ≤ n} × T → T) (inv : {k : ℝ // m ≤ k ∧ k ≤ 1 + n} × T → Bool), (inv m init ∧
  ∀ (k : Set.Icc m n) (ak : T), inv k ak → inv (k + 1) (f k ak)) → inv (n + 1) (for m n init f) := by
  -- === TCCs for for_invariant ===
  -- TCC: for_invariant_TCC1
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n)):
  --   m <= m AND m <= 1 + n
  have for_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_invariant_TCC2
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[subrange(m, 1 + n), T]]):
  --   inv(m, init) IMPLIES
  --    FORALL (k: subrange(m, n), ak: T): m <= k AND k <= 1 + n
  have for_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_invariant_TCC3
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[subrange(m, 1 + n), T]]):
  --   inv(m, init) IMPLIES
  --    FORALL (k: subrange(m, n), ak: T):
  --      inv(k, ak) IMPLIES m...
  have for_invariant_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_invariant_TCC4
  -- Statement: FORALL (m: int, (n: int | n >= m - 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[subrange(m, 1 + n), T]]):
  --   (inv(m, init) AND
  --     FORALL (k: subrange(m, n), ak: T):
  --       inv(k, ak) IMPLIES in...
  have for_invariant_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: for_iterate.iter
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF k = 0 OR m > n THEN init
-- ELSIF k - 1 <= n - m THEN f(m + k - 1, iter(m, n)(k - 1)(init, f))
-- ELSE f(n, iter(m, n)(n - m)(init, f))
-- ENDIF
axiom iter {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} : pvs_τ40 → pvs_τ41 → pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45

-- pvs2lean: for_iterate.iter_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: int, n: upfrom(m), k: posnat) (init: T, f: ForBody(m, n)):
--   iter(m, n)(n - m + k)(init, f) = f(n, iter(m, n)(n - m)(init, f))
theorem iter_ge : ∀ (m : ℝ) (n : {s : ℝ // s ≥ m}) (k : {i : {i : ℝ // i ≥ 0} // i > 0}), ∀ (init : T) (f : {k : ℝ // m ≤ k ∧ k ≤ n} × T → T), iter m n(n - m + k) (init, f) = f n (iter m n(n - m) (init, f)) := by
  -- === TCCs for iter_ge ===
  -- TCC: iter_ge_TCC1
  -- Statement: FORALL (m: int, n: upfrom(m), k: posnat, init: T, f: ForBody(m, n)):
  --   n - m + k >= 0
  have iter_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iter_ge_TCC2
  -- Statement: FORALL (m: int, n: upfrom(m), init: T, f: ForBody(m, n)): m <= n AND n <= n
  have iter_ge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iter_ge_TCC3
  -- Statement: FORALL (m: int, n: upfrom(m), init: T, f: ForBody(m, n)): n - m >= 0
  have iter_ge_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: for_iterate.iterate_eq_iter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: nat, m: upfrom(k - 1), f: [T -> T], init: T):
--   iterate(f, k)(init) = iter(0, m)(k)(init, LAMBDA (i: upto(m), a: T): f(a))
theorem iterate_eq_iter : ∀ (k : {i : ℝ // i ≥ 0}) (m : {s : ℝ // s ≥ k - 1}) (f : T → T) (init : T), iterate f k(init) = iter 0 m(k) (init, fun (i : {k : ℕ // k ≤ m}) (a : T)=> f a) := by
  -- === TCCs for iterate_eq_iter ===
  -- TCC: iterate_eq_iter_TCC1
  -- Statement: FORALL (k: nat, m: upfrom(k - 1), f: [T -> T], init: T):
  --   FORALL (x: int): x >= 0 AND x <= m IFF 0 <= x AND x <= m
  have iterate_eq_iter_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: for_iterate.for_iterate
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, a: T, f: ForBody(i, j)):
--   for(i, j, a, f) = iter(i, j)(max(j + 1 - i, 0))(a, f)
theorem for_iterate : ∀ (i : ℝ) (j : ℝ) (a : T) (f : {k : ℝ // i ≤ k ∧ k ≤ j} × T → T), for i j a f = iter i j(max (j + 1 - i) 0) (a, f) := by
  -- === TCCs for for_iterate ===
  -- TCC: for_iterate_TCC1
  -- Statement: FORALL (i, j: int, a: T, f: ForBody(i, j)): max(j + 1 - i, 0) >= 0
  have for_iterate_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_down_def
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < j THEN a ELSE f(j, for_down_def(i, j + 1, a, f)) ENDIF
axiom for_down_def {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ28 → pvs_τ29 → pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: for_iterate.for_down_def_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, a: T, f, g: [[int, T] -> T]):
--   (FORALL (x: subrange(j, i), t: T): f(x, t) = g(x, t)) IMPLIES
--    for_down_def(i, j, a, f) = for_down_def(i, j, a, g)
theorem for_down_def_ext : ∀ (i : ℝ) (j : ℝ) (a : T) (f : ℝ × T → T) (g : ℝ × T → T), (∀ (x : Set.Icc j i) (t : T), f x t = g x t) → for_down_def i j a f = for_down_def i j a g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_down_up
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, a: T, f: [[int, T] -> T]):
--   for_down_def(i, j, a, f) =
--    for_def(j, i, a, LAMBDA (k: int, t: T): f(i - k + j, t))
theorem for_down_up : ∀ (i : ℝ) (j : ℝ) (a : T) (f : ℝ × T → T), for_down_def i j a f = for_def j i a (fun (k : ℤ) (t : T)=> f (i - k + j) t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_down_def_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, a: T, f: [[int, T] -> T], inv: PRED[[nat, T]]):
--   (inv(0, a) AND
--     FORALL (k: subrange(0, i - j), ak: T):
--       inv(k, ak) IMPLIES inv(k + 1, f(i - k, ak)))
--    IMPLIES inv(max(0, 
theorem for_down_def_induction : ∀ (i : ℝ) (j : ℝ) (a : T) (f : ℝ × T → T) (inv : {i : ℝ // i ≥ 0} × T → Bool), (inv 0 a ∧
  ∀ (k : Set.Icc 0 i - j) (ak : T), inv k ak → inv (k + 1) (f (i - k) ak)) → inv (max 0 (i - j + 1)) (for_down_def i j a f) := by
  -- === TCCs for for_down_def_induction ===
  -- TCC: for_down_def_induction_TCC1
  -- Statement: FORALL (i, j: int, a: T, f: [[int, T] -> T], inv: PRED[[nat, T]]):
  --   inv(0, a) IMPLIES FORALL (k: subrange(0, i - j), ak: T): k >= 0
  have for_down_def_induction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_def_induction_TCC2
  -- Statement: FORALL (i, j: int, a: T, f: [[int, T] -> T], inv: PRED[[nat, T]]):
  --   inv(0, a) IMPLIES
  --    FORALL (k: subrange(0, i - j), ak: T): inv(k, ak) IMPLIES k + 1 >= 0
  have for_down_def_induction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_def_induction_TCC3
  -- Statement: FORALL (i, j: int, a: T, f: [[int, T] -> T], inv: PRED[[nat, T]]):
  --   (inv(0, a) AND
  --     FORALL (k: subrange(0, i - j), ak: T):
  --       inv(k, ak) IMPLIES inv(k + 1, f(i - k, ak)))
  --    IMPLIES max(0, i...
  have for_down_def_induction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_down
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: for(m, n, init, LAMBDA (k: subrange(m, n), a: T): f(n + m - k, a))
axiom for_down {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27

-- pvs2lean: for_iterate.for_down_induction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
--         inv: PRED[[upto(n - m + 1), T]]):
--   (inv(0, init) AND
--     FORALL (k: subrange(0, n - m), ak: T):
--       inv(k, ak) IMPLIES inv(
theorem for_down_induction : ∀ (n : ℝ) (m : {m : ℝ // m ≤ n + 1}) (init : T) (f : {k : ℝ // m ≤ k ∧ k ≤ n} × T → T) (inv : {s : {i : ℝ // i ≥ 0} // s ≤ 1 + n - m} × T → Bool), (inv 0 init ∧
  ∀ (k : Set.Icc 0 n - m) (ak : T), inv k ak → inv (k + 1) (f (n - k) ak)) → inv (n - m + 1) (for_down n m init f) := by
  -- === TCCs for for_down_induction ===
  -- TCC: for_down_induction_TCC1
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n)):
  --   0 <= 1 + n - m
  have for_down_induction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_induction_TCC2
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[upto(1 + n - m), T]]):
  --   inv(0, init) IMPLIES
  --    FORALL (k: subrange(0, n - m), ak: T): k >= 0 AND k <= 1 + n - m
  have for_down_induction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_induction_TCC3
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[upto(1 + n - m), T]]):
  --   inv(0, init) IMPLIES
  --    FORALL (k: subrange(0, n - m), ak: T):
  --      inv(k, ak) IMPLIES ...
  have for_down_induction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_induction_TCC4
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[upto(1 + n - m), T]]):
  --   inv(0, init) IMPLIES
  --    FORALL (k: subrange(0, n - m), ak: T):
  --      inv(k, ak) IMPLIES ...
  have for_down_induction_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_induction_TCC5
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[upto(1 + n - m), T]]):
  --   (inv(0, init) AND
  --     FORALL (k: subrange(0, n - m), ak: T):
  --       inv(k, ak) IMPLIES i...
  have for_down_induction_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_down_invariant
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
--         inv: PRED[[subrange(m - 1, n), T]]):
--   (inv(n, init) AND
--     FORALL (k: subrange(m, n), ak: T):
--       inv(k, ak) IMPLIES inv(k
theorem for_down_invariant : ∀ (n : ℝ) (m : {m : ℝ // m ≤ n + 1}) (init : T) (f : {k : ℝ // m ≤ k ∧ k ≤ n} × T → T) (inv : {k : ℝ // m - 1 ≤ k ∧ k ≤ n} × T → Bool), (inv n init ∧
  ∀ (k : Set.Icc m n) (ak : T), inv k ak → inv (k - 1) (f k ak)) → inv (m - 1) (for_down n m init f) := by
  -- === TCCs for for_down_invariant ===
  -- TCC: for_down_invariant_TCC1
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n)):
  --   m - 1 <= n AND n <= n
  have for_down_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_invariant_TCC2
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[subrange(m - 1, n), T]]):
  --   inv(n, init) IMPLIES
  --    FORALL (k: subrange(m, n), ak: T): m - 1 <= k AND k <= n
  have for_down_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_invariant_TCC3
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[subrange(m - 1, n), T]]):
  --   inv(n, init) IMPLIES
  --    FORALL (k: subrange(m, n), ak: T):
  --      inv(k, ak) IMPLIES m...
  have for_down_invariant_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: for_down_invariant_TCC4
  -- Statement: FORALL (n: int, (m: int | m <= n + 1), init: T, f: ForBody(m, n),
  --         inv: PRED[[subrange(m - 1, n), T]]):
  --   (inv(n, init) AND
  --     FORALL (k: subrange(m, n), ak: T):
  --       inv(k, ak) IMPLIES in...
  have for_down_invariant_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.for_down_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: int, a: T, f: ForBody(j, i)):
--   for_down(i, j, a, f) = for_down_def(i, j, a, ext2int(j, i, f))
theorem for_down_eq : ∀ (i : ℝ) (j : ℝ) (a : T) (f : {k : ℝ // j ≤ k ∧ k ≤ i} × T → T), for_down i j a f = for_down_def i j a (ext2int j i f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: for_iterate.iterate_left_def
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = j THEN f(i) ELSE iterate_left_def(i, j - 1, f, o) o f(j) ENDIF
axiom iterate_left_def {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} : pvs_τ55 → pvs_τ56 → pvs_τ57 → pvs_τ58 → Prop

-- pvs2lean: for_iterate.iterate_left_def_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem iterate_left_def_ext : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: for_iterate.iterate_left_id
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: for(i, j, id, LAMBDA (k: subrange(i, j), t: T): t o f(k))
noncomputable def iterate_left_id : sorry := by
  sorry  -- pvs2lean: definition stub for for(i, j, id, LAMBDA (k: subrange(i, j), t: T): t o f(k))

-- pvs2lean: for_iterate.iterate_left
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: for(m + 1, n, f(m), LAMBDA (k_1: subrange(1 + m, n), t: T): t o f(k_1))
axiom iterate_left {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} : pvs_τ50 → pvs_τ51 → pvs_τ52 → pvs_τ53 → pvs_τ54

-- pvs2lean: for_iterate.iterate_left_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem iterate_left_eq : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: for_iterate.iterate_left_induction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem iterate_left_induction : True := by
  -- === TCCs for iterate_left_induction ===
  -- TCC: iterate_left_induction_TCC1
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T]):
  --   0 <= n - m
  have iterate_left_induction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_left_induction_TCC2
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[upto(n - m), T]]):
  --   inv(0, f(m)) IMPLIES FORALL (k: below(n - m), ak: T): k <= n - m
  have iterate_left_induction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_left_induction_TCC3
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[upto(n - m), T]]):
  --   inv(0, f(m)) IMPLIES
  --    FORALL (k: below(n - m), ak: T): inv(k, ak) IMPLIES k + 1 <= n - m
  have iterate_left_induction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_left_induction_TCC4
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[upto(n - m), T]]):
  --   inv(0, f(m)) IMPLIES
  --    FORALL (k: below(n - m), ak: T):
  --      inv(k, ak) IMPLIES m <= m...
  have iterate_left_induction_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_left_induction_TCC5
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[upto(n - m), T]]):
  --   (inv(0, f(m)) AND
  --     FORALL (k: below(n - m), ak: T):
  --       inv(k, ak) IMPLIES inv(k +...
  have iterate_left_induction_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: for_iterate.iterate_left_invariant
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem iterate_left_invariant : True := by
  -- === TCCs for iterate_left_invariant ===
  -- TCC: iterate_left_invariant_TCC1
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[subrange(m, n), T]]):
  --   inv(m, f(m)) IMPLIES
  --    FORALL (k: subrange(m, n - 1), ak: T): m <= k AND k <= n
  have iterate_left_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_left_invariant_TCC2
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[subrange(m, n), T]]):
  --   inv(m, f(m)) IMPLIES
  --    FORALL (k: subrange(m, n - 1), ak: T):
  --      inv(k, ak) IMPLI...
  have iterate_left_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_left_invariant_TCC3
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[subrange(m, n), T]]):
  --   (inv(m, f(m)) AND
  --     FORALL (k: subrange(m, n - 1), ak: T):
  --       inv(k, ak) IMPLIE...
  have iterate_left_invariant_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: for_iterate.iterate_right_def
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = j THEN f(j) ELSE f(i) o iterate_right_def(i + 1, j, f, o) ENDIF
axiom iterate_right_def {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} : pvs_τ64 → pvs_τ65 → pvs_τ66 → pvs_τ67 → Prop

-- pvs2lean: for_iterate.iterate_right_def_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem iterate_right_def_ext : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: for_iterate.iterate_right_id
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: for_down(j, i, id, LAMBDA (k: subrange(i, j), t: T): f(k) o t)
noncomputable def iterate_right_id : sorry := by
  sorry  -- pvs2lean: definition stub for for_down(j, i, id, LAMBDA (k: subrange(i, j), t: T): f(k) o t)

-- pvs2lean: for_iterate.iterate_right
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: for_down(n - 1, m, f(n), LAMBDA (k_1: subrange(m, n - 1), t: T): f(k_1) o t)
axiom iterate_right {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} : pvs_τ59 → pvs_τ60 → pvs_τ61 → pvs_τ62 → pvs_τ63

-- pvs2lean: for_iterate.iterate_right_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem iterate_right_eq : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: for_iterate.iterate_right_induction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem iterate_right_induction : True := by
  -- === TCCs for iterate_right_induction ===
  -- TCC: iterate_right_induction_TCC1
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[upto(n - m), T]]):
  --   inv(0, f(n)) IMPLIES FORALL (k: below(n - m), ak: T): k <= n - m
  have iterate_right_induction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_right_induction_TCC2
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[upto(n - m), T]]):
  --   inv(0, f(n)) IMPLIES
  --    FORALL (k: below(n - m), ak: T): inv(k, ak) IMPLIES k + 1 <= n - m
  have iterate_right_induction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_right_induction_TCC3
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[upto(n - m), T]]):
  --   inv(0, f(n)) IMPLIES
  --    FORALL (k: below(n - m), ak: T):
  --      inv(k, ak) IMPLIES m <= n...
  have iterate_right_induction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: iterate_right_induction_TCC4
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[upto(n - m), T]]):
  --   (inv(0, f(n)) AND
  --     FORALL (k: below(n - m), ak: T):
  --       inv(k, ak) IMPLIES inv(k +...
  have iterate_right_induction_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: for_iterate.iterate_right_invariant
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem iterate_right_invariant : True := by
  -- === TCCs for iterate_right_invariant ===
  -- TCC: iterate_right_invariant_TCC1
  -- Statement: FORALL (m: int, n: upfrom(m), f: IterateBody(m, n), O: [[T, T] -> T],
  --         inv: PRED[[subrange(m, n), T]]):
  --   inv(n, f(n)) IMPLIES
  --    FORALL (k: subrange(m + 1, n), ak: T):
  --      inv(k, ak) IMPLI...
  have iterate_right_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.for_iterate