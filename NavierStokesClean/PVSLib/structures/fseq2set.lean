-- Auto-generated by pvs2lean/translator.py
-- PVS theory: fseq2set
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: fseq2set

Auto-generated stubs from PVSLib theory `fseq2set`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.fseq2set

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (card : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (empty_p : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (is_finite : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (length : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (max : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} (min : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} (nonempty_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (seq : pvs_τ12 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (sort : pvs_τ18 → pvs_τ19)

-- pvs2lean: fseq2set.seq2set
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {s: T | (EXISTS (kk: below(length(fs))): fs`seq(kk) = s)}
axiom seq2set {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15

-- pvs2lean: fseq2set.seq2set_lem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (ii: below(length(fs))): seq2set(fs)(fs`seq(ii)))
theorem seq2set_lem : ∀ (ii : {s : {i : ℝ // i ≥ 0} // s < List.length fs}), seq2set fs (fs.seq ii) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseq2set.card_seq2set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: card[T](seq2set(fs)) <= length(fs)
theorem card_seq2set : card (seq2set fs) ≤ List.length fs := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseq2set.minmax_seq2set
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min[T, <=](seq2set(ne_fs)) = min(ne_fs) AND
--  max[T, <=](seq2set(ne_fs)) = max(ne_fs)
theorem minmax_seq2set : min (seq2set ne_fs) = min ne_fs ∧ max (seq2set ne_fs) = max ne_fs := by
  -- === TCCs for minmax_seq2set ===
  -- TCC: minmax_seq2set_TCC1
  -- Statement: FORALL (ne_fs: ne_fseq[T]): NOT empty?[T](seq2set(ne_fs))
  have minmax_seq2set_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: fseq2set.set2seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom set2seq {pvs_τ16 : Type*} {pvs_τ17 : Type*} : pvs_τ16 → pvs_τ17

-- pvs2lean: fseq2set.set2seq_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(set2seq(S)) = card(S)
theorem set2seq_length : List.length (set2seq S) = card S := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseq2set.set2seq_lem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fs = set2seq(S) IN FORALL (ii: below(length(fs))): S(fs`seq(ii))
theorem set2seq_lem : fun (fs : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → T // ∀ (i : ℕ), i ≥ List.length → f i = default}) // List.length fs = card S ∧
 (∀ (ii : Finset.range List.length fs), S (fs.seq ii)) ∧
  (∀ (x : T), S x → ∃ (ii : Finset.range List.length fs), fs.seq ii = x)
   ∧
   (∀ (ii : _) (jj : Finset.range List.length fs), ii ≠ jj → fs.seq ii ≠ fs.seq jj)}) => ∀ (ii : {s : {i : ℝ // i ≥ 0} // s < List.length fs}), S (fs.seq ii) (set2seq S) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseq2set.set2seq_exists
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fs = set2seq(S) IN
--   S(x) IMPLIES EXISTS (ii: below(length(fs))): fs`seq(ii) = x
theorem set2seq_exists : fun (fs : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → T // ∀ (i : ℕ), i ≥ List.length → f i = default}) // List.length fs = card S ∧
 (∀ (ii : Finset.range List.length fs), S (fs.seq ii)) ∧
  (∀ (x : T), S x → ∃ (ii : Finset.range List.length fs), fs.seq ii = x)
   ∧
   (∀ (ii : _) (jj : Finset.range List.length fs), ii ≠ jj → fs.seq ii ≠ fs.seq jj)}) => S x → ∃ (ii : Finset.range List.length fs), fs.seq ii = x (set2seq S) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseq2set.minmax_set2seq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: card(S) > 0 IMPLIES
--  min(set2seq(S)) = min[T, <=](S) AND max(set2seq(S)) = max[T, <=](S)
theorem minmax_set2seq : card S > 0 → min (set2seq S) = min S ∧ max (set2seq S) = max S := by
  -- === TCCs for minmax_set2seq ===
  -- TCC: minmax_set2seq_TCC1
  -- Statement: FORALL (S: finite_set[T]): card(S) > 0 IMPLIES length(set2seq(S)) > 0
  have minmax_set2seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: minmax_set2seq_TCC2
  -- Statement: FORALL (S: finite_set[T]): card(S) > 0 IMPLIES NOT empty?[T](S)
  have minmax_set2seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: fseq2set.set2seq_neq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fs = set2seq(S) IN
--   FORALL (ii, jj: below(length(fs))):
--     ii /= jj IMPLIES fs`seq(ii) /= fs`seq(jj)
theorem set2seq_neq : fun (fs : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → T // ∀ (i : ℕ), i ≥ List.length → f i = default}) // List.length fs = card S ∧
 (∀ (ii : Finset.range List.length fs), S (fs.seq ii)) ∧
  (∀ (x : T), S x → ∃ (ii : Finset.range List.length fs), fs.seq ii = x)
   ∧
   (∀ (ii : _) (jj : Finset.range List.length fs), ii ≠ jj → fs.seq ii ≠ fs.seq jj)}) => ∀ (ii : {s : {i : ℝ // i ≥ 0} // s < List.length fs}) (jj : {s : {i : ℝ // i ≥ 0} // s < List.length fs}), ii ≠ jj → fs.seq ii ≠ fs.seq jj (set2seq S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: fseq2set.set2seq_all
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fs = sort(set2seq(S)) IN
--   FORALL (ii: below(length(fs) - 1)):
--     fs`seq(ii) <= x AND
--      x <= fs`seq(ii + 1) AND fs`seq(ii) /= x AND x /= fs`seq(ii + 1)
--      IMPLIES NOT S(x)
theorem set2seq_all : fun (fs : {ss : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → T // ∀ (i : ℕ), i ≥ List.length → f i = default}) // permutation_p (set2seq S) ss ∧ increasing_p ss}) => ∀ (ii : {s : {i : ℝ // i ≥ 0} // s < List.length fs - 1}), fs.seq ii ≤ x ∧
 x ≤ fs.seq (ii + 1) ∧ fs.seq ii ≠ x ∧ x ≠ fs.seq (ii + 1) → ¬ S x (sort (set2seq S)) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.fseq2set