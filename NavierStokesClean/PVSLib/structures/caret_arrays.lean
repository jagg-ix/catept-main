-- Auto-generated by pvs2lean/translator.py
-- PVS theory: caret_arrays
-- PVSLib library: structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: caret_arrays

Auto-generated stubs from PVSLib theory `caret_arrays`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.caret_arrays

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)

-- pvs2lean: caret_arrays.^
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (m, n) = p IN
--   IF m <= n THEN (LAMBDA (x: below[n - m + 1]): A(x + m))
--   ELSE empty_array
--   ENDIF
noncomputable def pvs_op_7e6a2a : sorry := by
  sorry  -- pvs2lean: definition stub for LET (m, n) = p IN   IF m <= n THEN (LAMBDA (x: below[n - m + 1]): A(x + m))   EL

-- pvs2lean: caret_arrays.caret_all
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: N > 0 IMPLIES A ^ (0, N - 1) = A
theorem caret_all : N > 0 → A ^ (0, N - 1) = A := by
  -- === TCCs for caret_all ===
  -- TCC: caret_all_TCC1
  -- Statement: N > 0 IMPLIES N - 1 >= 0 AND N - 1 < N
  have caret_all_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_all_TCC2
  -- Statement: N > 0 IMPLIES
  --  FORALL (x: nat): x < N IFF x < IF 0 > N - 1 THEN 0 ELSE N ENDIF
  have caret_all_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: caret_arrays.caret_ii_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (A ^ (i, i))(0) = A(i)
theorem caret_ii_0 : (A ^ (i, i)) (0) = A i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: caret_arrays.caret_elim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= j AND k < i - j + 1 IMPLIES (A ^ (i, j))(k) = A(j + k)
theorem caret_elim : i ≤ j ∧ k < i - j + 1 → (A ^ (i, j)) (k) = A (j + k) := by
  -- === TCCs for caret_elim ===
  -- TCC: caret_elim_TCC1
  -- Statement: FORALL (i, j, k: below(N)):
  --   k < i - j + 1 AND i <= j IMPLIES k < IF i > j THEN 0 ELSE 1 + j - i ENDIF
  have caret_elim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_elim_TCC2
  -- Statement: FORALL (i, j, k: below(N)): k < i - j + 1 AND i <= j IMPLIES j + k < N
  have caret_elim_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.caret_arrays