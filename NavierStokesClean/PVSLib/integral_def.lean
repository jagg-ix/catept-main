-- Auto-generated by pvs2lean/translator.py
-- PVS theory: integral_def
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: integral_def

Auto-generated stubs from PVSLib theory `integral_def`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.integral_def

noncomputable section

-- pvs2lean: integral_def.T_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_def.connected_domain
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[T]
theorem connected_domain : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.not_one_element
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[T]
theorem not_one_element : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.width
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET xx = seq(P), N = length(P) IN
--   max({l: real | EXISTS (ii: below(N - 1)): l = xx(ii + 1) - xx(ii)})
noncomputable def width : sorry := by
  sorry  -- pvs2lean: definition stub for LET xx = seq(P), N = length(P) IN   max({l: real | EXISTS (ii: below(N - 1)): l 

-- pvs2lean: integral_def.width_lem
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (a: T),
--         (b: {x: T | a < x}),
--         (P: partition(a, b)), (ii: below(length(P) - 1)):
--    width(a, b, P) >=
--     finseq_appl[closed_interval[T](a, b)](P)(ii + 1) -
--      finseq_appl[clos
theorem width_lem : ∀ (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) (ii : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1}), width a b P ≥ finseq_appl (P) (ii + 1) -
 finseq_appl (P) (ii) := by
  -- === TCCs for width_lem ===
  -- TCC: width_lem_TCC1
  -- Statement: FORALL (a: T, b: {x: T | a < x}, P: partition(a, b),
  --         ii: below(length(P) - 1)):
  --   ii + 1 < length(P)
  have width_lem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: width_lem_TCC2
  -- Statement: FORALL (a: T, b: {x: T | a < x}, P: partition(a, b),
  --         ii: below(length(P) - 1)):
  --   ii < length(P)
  have width_lem_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.parts_order
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: partition(a, b), ii, jj: below(length(P))):
--   ii < jj IMPLIES seq(P)(ii) < seq(P)(jj)
theorem parts_order : ∀ (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) (ii : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P}) (jj : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P}), ii < jj → seq P(ii) < seq P(jj) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.parts_order_le
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: partition(a, b), ii, jj: below(length(P))):
--   ii <= jj IMPLIES seq(P)(ii) <= seq(P)(jj)
theorem parts_order_le : ∀ (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) (ii : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P}) (jj : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P}), ii ≤ jj → seq P(ii) ≤ seq P(jj) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.parts_disjoint
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: partition(a, b), ii, jj: below(length(P) - 1)):
--   seq(P)(ii) < x AND
--    x < seq(P)(1 + ii) AND seq(P)(jj) < x AND x < seq(P)(1 + jj)
--    IMPLIES jj = ii
theorem parts_disjoint : ∀ (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) (ii : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1}) (jj : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1}), seq P(ii) < x ∧
 x < seq P(1 + ii) ∧ seq P(jj) < x ∧ x < seq P(1 + jj) → jj = ii := by
  -- === TCCs for parts_disjoint ===
  -- TCC: parts_disjoint_TCC1
  -- Statement: FORALL (a, b: T, P: partition(a, b), ii, jj: below(length(P) - 1)):
  --   ii < P`length
  have parts_disjoint_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: parts_disjoint_TCC2
  -- Statement: FORALL (a, b, x: T, P: partition(a, b), ii, jj: below(length(P) - 1)):
  --   seq(P)(ii) < x IMPLIES 1 + ii < P`length
  have parts_disjoint_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: parts_disjoint_TCC3
  -- Statement: FORALL (a, b, x: T, P: partition(a, b), ii, jj: below(length(P) - 1)):
  --   seq(P)(ii) < x AND x < seq(P)(1 + ii) IMPLIES jj < P`length
  have parts_disjoint_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: parts_disjoint_TCC4
  -- Statement: FORALL (a, b, x: T, P: partition(a, b), ii, jj: below(length(P) - 1)):
  --   seq(P)(ii) < x AND x < seq(P)(1 + ii) AND seq(P)(jj) < x IMPLIES
  --    1 + jj < P`length
  have parts_disjoint_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.in_part?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (ii: below(length(P) - 1)): xx = seq(P)(ii)
noncomputable def in_part_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS (ii: below(length(P) - 1)): xx = seq(P)(ii)

-- pvs2lean: integral_def.in_sect?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: seq(P)(ii) < xx AND xx < seq(P)(ii + 1)
noncomputable def in_sect_p : sorry := by
  sorry  -- pvs2lean: definition stub for seq(P)(ii) < xx AND xx < seq(P)(ii + 1)

-- pvs2lean: integral_def.part_in
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: partition(a, b)):
--   a < b AND a <= x AND x <= b IMPLIES
--    (EXISTS (ii: below(length(P) - 1)):
--       seq(P)(ii) <= x AND x <= seq(P)(ii + 1))
theorem part_in : ∀ (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}), a < b ∧ a ≤ x ∧ x ≤ b → (∃ (ii : Finset.range length (P - 1)), seq P(ii) ≤ x ∧ x ≤ seq P(ii + 1)) := by
  -- === TCCs for part_in ===
  -- TCC: part_in_TCC1
  -- Statement: FORALL (a, b, x: T, P: partition(a, b)):
  --   a < b AND a <= x AND x <= b IMPLIES
  --    FORALL (ii: below(length(P) - 1)): ii < P`length
  have part_in_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: part_in_TCC2
  -- Statement: FORALL (a, b, x: T, P: partition(a, b)):
  --   a < b AND a <= x AND x <= b IMPLIES
  --    FORALL (ii: below(length(P) - 1)):
  --      seq(P)(ii) <= x IMPLIES ii + 1 < P`length
  have part_in_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: integral_def.part_not_in
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition(a, b)):
--    FORALL (ii, jj: below(length(P) - 1)):
--      seq(P)(ii) < x AND x < seq(P)(ii + 1) IMPLIES x /= seq(P)(jj)
theorem part_not_in : a < b → ∀ (P : partition a b), ∀ (ii : _) (jj : Finset.range length (P - 1)), seq P(ii) < x ∧ x < seq P(ii + 1) → x ≠ seq P(jj) := by
  -- === TCCs for part_not_in ===
  -- TCC: part_not_in_TCC1
  -- Statement: FORALL (a, b, x: T):
  --   a < b IMPLIES
  --    FORALL (P: partition(a, b), ii, jj: below(length(P) - 1)):
  --      seq(P)(ii) < x IMPLIES ii + 1 < P`length
  have part_not_in_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: part_not_in_TCC2
  -- Statement: FORALL (a, b, x: T):
  --   a < b IMPLIES
  --    FORALL (P: partition(a, b), ii, jj: below(length(P) - 1)):
  --      seq(P)(ii) < x AND x < seq(P)(ii + 1) IMPLIES jj < P`length
  have part_not_in_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.part_induction
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (P: partition(a, b)):
--    (FORALL (x: closed_interval(a, b)):
--       LET xx = seq(P), N = length(P) IN
--         (FORALL (ii: below(N - 1)):
--            xx(ii) <= x AND x <= xx(ii + 1) IMPLIES Prop
theorem part_induction : ∀ (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}), ∀ (x : {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b}), fun (xx : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < P.length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b}) => fun (N : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}) => (∀ (ii : Finset.range N - 1), xx ii ≤ x ∧ x ≤ xx (ii + 1) → Prop x) → Prop x (length P) (seq P) := by
  -- === TCCs for part_induction ===
  -- TCC: part_induction_TCC1
  -- Statement: FORALL (a, b: T, P: partition(a, b), x: closed_interval[T](a, b),
  --         xx: [below[P`length] -> closed_interval[T](a, b)], N: nat):
  --   xx = seq(P) AND N = length(P) IMPLIES
  --    FORALL (ii: below(N ...
  have part_induction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: part_induction_TCC2
  -- Statement: FORALL (a, b: T, P: partition(a, b), x: closed_interval[T](a, b),
  --         xx: [below[P`length] -> closed_interval[T](a, b)], N: nat):
  --   xx = seq(P) AND N = length(P) IMPLIES
  --    FORALL (ii: below(N ...
  have part_induction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.eq_partition
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := N,
--    seq := (LAMBDA (ii: below(N)): a + ii * (b - a) / (N - 1)) #)
noncomputable def eq_partition : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := N,    seq := (LAMBDA (ii: below(N)): a + ii * (b - a) / (N - 1)) #)

-- pvs2lean: integral_def.xis?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (ii: below(length(P) - 1)):
--    finseq_appl[closed_interval[T](a, b)](P)(ii) <= fs(ii) AND
--     fs(ii) <= finseq_appl[closed_interval[T](a, b)](P)(ii + 1))
noncomputable def xis_p : sorry := by
  sorry  -- pvs2lean: definition stub for (FORALL (ii: below(length(P) - 1)):    finseq_appl[closed_interval[T](a, b)](P)(

-- pvs2lean: integral_def.xis_lem
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: T, b: {x: T | a < x}, P: partition(a, b),
--         (xis: (xis?(a, b, P))), (ii: below(length(P) - 1))):
--   finseq_appl[closed_interval[T](a, b)](P)(ii) <= xis(ii) AND
--    xis(ii) <= finseq_app
theorem xis_lem : ∀ (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) (xis : {x : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} // _}) (ii : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1}), finseq_appl (P) (ii) ≤ xis ii ∧ xis ii ≤ finseq_appl (P) (ii + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.Rie_sum
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET N = length(P) - 1 IN
--   sigma[below(N)]
--       (0, N - 1,
--        (LAMBDA (n: below(N)):
--           (finseq_appl[closed_interval[T](a, b)](P)(n + 1) -
--             finseq_appl[closed_interval[T](a, b)]
noncomputable def Rie_sum : sorry := by
  sorry  -- pvs2lean: definition stub for LET N = length(P) - 1 IN   sigma[below(N)]       (0, N - 1,        (LAMBDA (n: b

-- pvs2lean: integral_def.Rie_sec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN 0 ELSE (seq(P)(n) - seq(P)(n - 1)) * f(xis(n - 1)) ENDIF
noncomputable def Rie_sec : sorry := by
  sorry  -- pvs2lean: definition stub for IF n = 0 THEN 0 ELSE (seq(P)(n) - seq(P)(n - 1)) * f(xis(n - 1)) ENDIF

-- pvs2lean: integral_def.Rie_sum_alt
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET N = length(P) - 1 IN
--   sigma[upto(N)](1, N, (LAMBDA (n: upto(N)): Rie_sec(a, b, P, xis, f, n)))
noncomputable def Rie_sum_alt : sorry := by
  sorry  -- pvs2lean: definition stub for LET N = length(P) - 1 IN   sigma[upto(N)](1, N, (LAMBDA (n: upto(N)): Rie_sec(a,

-- pvs2lean: integral_def.Rie_sum_alt_lem
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: T, b: {x: T | a < x}, P: partition(a, b),
--         (xis: (xis?(a, b, P)))):
--   Rie_sum(a, b, P, xis, f) = Rie_sum_alt(a, b, P, xis, f)
theorem Rie_sum_alt_lem : ∀ (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) (xis : {x : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} // _}), Rie_sum a b P xis f = Rie_sum_alt a b P xis f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.Riemann_sum?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (xis: (xis?(a, b, P))):
--    LET N = length(P) - 1 IN S = Rie_sum(a, b, P, xis, f))
noncomputable def Riemann_sum_p : sorry := by
  sorry  -- pvs2lean: definition stub for (EXISTS (xis: (xis?(a, b, P))):    LET N = length(P) - 1 IN S = Rie_sum(a, b, P,

-- pvs2lean: integral_def.integral?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (epsi: posreal):
--    (EXISTS (delta: posreal):
--       (FORALL (P: partition(a, b)):
--          width(a, b, P) < delta IMPLIES
--           (FORALL (R: (Riemann_sum?(a, b, P, f))): abs(S - R) < epsi))
noncomputable def integral_p : sorry := by
  sorry  -- pvs2lean: definition stub for (FORALL (epsi: posreal):    (EXISTS (delta: posreal):       (FORALL (P: partitio

-- pvs2lean: integral_def.x_in
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def x_in : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_def.pick_one
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: x_in(finseq_appl[closed_interval[T](a, b)](P)(ii),
--      finseq_appl[closed_interval[T](a, b)](P)(ii + 1))
noncomputable def pick_one : sorry := by
  sorry  -- pvs2lean: definition stub for x_in(finseq_appl[closed_interval[T](a, b)](P)(ii),      finseq_appl[closed_inter

-- pvs2lean: integral_def.gen_xis
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: finseq_appl[real]((# length := length(P) - 1, seq := pick_one(a, b, P) #))
noncomputable def gen_xis : sorry := by
  sorry  -- pvs2lean: definition stub for finseq_appl[real]((# length := length(P) - 1, seq := pick_one(a, b, P) #))

-- pvs2lean: integral_def.len_eq_part
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES length(eq_partition(a, b, N)) = N
theorem len_eq_part : a < b → length (eq_partition a b N) = N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.eq_part_lem_a
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES seq(eq_partition(a, b, N))(0) = a
theorem eq_part_lem_a : a < b → seq (eq_partition a b N) (0) = a := by
  -- === TCCs for eq_part_lem_a ===
  -- TCC: eq_part_lem_a_TCC1
  -- Statement: FORALL (N: above(1), a, b: T):
  --   a < b IMPLIES 0 < eq_partition(a, b, N)`length
  have eq_part_lem_a_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.eq_part_lem_b
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES seq(eq_partition(a, b, N))(N - 1) = b
theorem eq_part_lem_b : a < b → seq (eq_partition a b N) (N - 1) = b := by
  -- === TCCs for eq_part_lem_b ===
  -- TCC: eq_part_lem_b_TCC1
  -- Statement: FORALL (N: above(1), a, b: T):
  --   a < b IMPLIES N - 1 >= 0 AND N - 1 < eq_partition(a, b, N)`length
  have eq_part_lem_b_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.width_eq_part
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES width(a, b, eq_partition(a, b, N)) = (b - a) / (N - 1)
theorem width_eq_part : a < b → width a b (eq_partition a b N) = (b - a) / (N - 1) := by
  -- === TCCs for width_eq_part ===
  -- TCC: width_eq_part_TCC1
  -- Statement: FORALL (N: above(1), a, b: T): a < b IMPLIES (N - 1) /= 0
  have width_eq_part_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.N_from_delta
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  LET N = 2 + floor((b - a) / delta), EP = eq_partition(a, b, N) IN
--    width(a, b, EP) < delta
theorem N_from_delta : a < b → (let N := 2 + floor ((b - a) / delta); let EP := eq_partition a b N; width a b EP < delta) := by
  -- === TCCs for N_from_delta ===
  -- TCC: N_from_delta_TCC1
  -- Statement: FORALL (a, b: T, delta: posreal):
  --   a < b IMPLIES
  --    FORALL (N: int): N = 2 + floor((b - a) / delta) IMPLIES N > 1
  have n_from_delta_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.Riemann?_Rie
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: T, b: {x: T | a < x}, P: partition(a, b), xis: (xis?(a, b, P))):
--   Riemann_sum?(a, b, P, f)(Rie_sum(a, b, P, xis, f))
theorem Riemann_p_Rie : ∀ (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) (xis : {x : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} // _}), Riemann_sum_p ((a, b, P, f)) (Rie_sum ((a, b, P, xis, f))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.integral_unique
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND integral?(a, b, f, A1) AND integral?(a, b, f, A2) IMPLIES A1 = A2
theorem integral_unique : a < b ∧ integral_p a b f A1 ∧ integral_p a b f A2 → A1 = A2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.integrable?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (S: real): integral?(a, b, f, S))
noncomputable def integrable_p : sorry := by
  sorry  -- pvs2lean: definition stub for (EXISTS (S: real): integral?(a, b, f, S))

-- pvs2lean: integral_def.integral
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def integral : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_def.integral_def
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  ((integrable?(a, b, f) AND integral(a, b, f) = s) IFF
--    integral?(a, b, f, s))
theorem integral_def : a < b → ((integrable_p a b f ∧ integral a b f = s) ↔ integral_p a b f s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.integral_restrict_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND
--  (FORALL x: a <= x AND x <= b IMPLIES f(x) = g(x)) AND integrable?(a, b, f)
--  IMPLIES integrable?(a, b, g) AND integral(a, b, g) = integral(a, b, f)
theorem integral_restrict_eq : a < b ∧
 (∀ x,  a ≤ x ∧ x ≤ b → f x = g x) ∧ integrable_p a b f → integrable_p a b g ∧ integral a b g = integral a b f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.Integrable?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (a = b) OR
--  (a < b AND integrable?(a, b, f)) OR (b < a AND integrable?(b, a, f))
noncomputable def Integrable_p : sorry := by
  sorry  -- pvs2lean: definition stub for (a = b) OR  (a < b AND integrable?(a, b, f)) OR (b < a AND integrable?(b, a, f))

-- pvs2lean: integral_def.Integral?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (a = b AND S = 0) OR (a < b AND integral?(a, b, f, S))
noncomputable def Integral_p : sorry := by
  sorry  -- pvs2lean: definition stub for (a = b AND S = 0) OR (a < b AND integral?(a, b, f, S))

-- pvs2lean: integral_def.Integral
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF a = b THEN 0
-- ELSIF a < b THEN integral(a, b, f)
-- ELSE -integral(b, a, f)
-- ENDIF
noncomputable def Integral : sorry := by
  sorry  -- pvs2lean: definition stub for IF a = b THEN 0 ELSIF a < b THEN integral(a, b, f) ELSE -integral(b, a, f) ENDIF

-- pvs2lean: integral_def.Integrable?_rew
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND Integrable?(a, b, f) IMPLIES integrable?(a, b, f)
theorem Integrable_p_rew : a < b ∧ Integrable_p a b f → integrable_p a b f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_def.Integral_rew
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND Integrable?(a, b, f) IMPLIES Integral(a, b, f) = integral(a, b, f)
theorem Integral_rew : a < b ∧ Integrable_p a b f → Integral a b f = integral a b f := by
  -- === TCCs for Integral_rew ===
  -- TCC: Integral_rew_TCC1
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   Integrable?(a, b, f) AND a < b IMPLIES integrable?(a, b, f)
  have integral_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.integral_def