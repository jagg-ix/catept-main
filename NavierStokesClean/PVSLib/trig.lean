-- Auto-generated root for PVSLib sub-library: trig
-- Imports all theory files in NavierStokesClean/PVSLib/trig/
-- DO NOT EDIT: regenerated by scripts/gen_pvs_roots.py

import trig.acos
import trig.arccos
import trig.arcsin
import trig.asin
import trig.atan
import trig.atan2
import trig.atan2_props
import trig.atan_approx
import trig.atan_integral
import trig.atan_values
import trig.deriv_sincos
import trig.exp_term
import trig.infinite_limits
import trig.integral_sincos
import trig.integral_sincos_lin
import trig.jatan2
import trig.law_cosines
import trig.merten
import trig.pi_def
import trig.sincos
import trig.sincos_def
import trig.tan_approx
import trig.to2pi
import trig.trig_approx
import trig.trig_basic
import trig.trig_degree
import trig.trig_extra
import trig.trig_ineq
import trig.trig_inverses
import trig.trig_rew
import trig.trig_safe_ops
import trig.trig_types
import trig.trig_values
