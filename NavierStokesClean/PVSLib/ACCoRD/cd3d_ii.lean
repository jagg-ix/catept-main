-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cd3d_ii
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cd3d_ii

Auto-generated stubs from PVSLib theory `cd3d_ii`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cd3d_ii

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (FlightPlanRelevant2 : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (FlightTimesLimited2 : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (FlightTimesRelevant2 : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ16 : Type*} (flp : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} (fp : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (fp2 : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (min : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (segment : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} (sq : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (sqv : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (vect2 : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (velocity : pvs_τ28 → pvs_τ29)

-- pvs2lean: cd3d_ii.conflict_3D_ii?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (tt: FlightTimesRelevant2(fp, fp2)):
--   sqv(vect2(location_at[N](fp)(tt) - location_at[M](fp2)(tt))) < sq(D) AND
--    abs(location_at[N](fp)(tt)`z - location_at[M](fp2)(tt)`z) < H
axiom conflict_3D_ii_p {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ14 → pvs_τ15 → Prop

-- pvs2lean: cd3d_ii.seg_allowable
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
--         t1: FlightTimesLimited2(fp, fp2), j: below[N]):
--   j = segment(fp)(t1) IMPLIES
--    j < N - 1 AND
--     min(fp(1 + j)`time - fp(j)`time, fp(
theorem seg_allowable : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}) (t1 : {aa : {a : ℝ // start_time fp ≤ a ∧ a ≤ end_time fp} // ∃ (j : Fin N - 1), (aa = fp j.time) ∧ (fp (1 + j).time > fp2 0.time)
   ∧ (aa < fp 0.time + T) ∧ (aa < fp (N - 1).time)
   ∧ (aa < fp2 (M - 1).time)}) (j : {s : {i : ℝ // i ≥ 0} // s < N}), j = segment fp(t1) → j < N - 1 ∧
 min (fp (1 + j).time - fp j.time) (fp 0.time - fp j.time + T) > 0 := by
  -- === TCCs for seg_allowable ===
  -- TCC: seg_allowable_TCC1
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         t1: FlightTimesLimited2(fp, fp2), j: below[N]):
  --   j = segment(fp)(t1) AND j < N - 1 IMPLIES 1 + j < N
  have seg_allowable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cd3d_ii.time_in_range
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
--         t1: FlightTimesLimited2(fp, fp2)):
--   LET j = segment(fp)(t1) IN
--     FORALL (t2:
--               FlightTimesRelevant
--                   [D,
theorem time_in_range : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}) (t1 : {aa : {a : ℝ // start_time fp ≤ a ∧ a ≤ end_time fp} // ∃ (j : Fin N - 1), (aa = fp j.time) ∧ (fp (1 + j).time > fp2 0.time)
   ∧ (aa < fp 0.time + T) ∧ (aa < fp (N - 1).time)
   ∧ (aa < fp2 (M - 1).time)}), fun (j : {s : {i : ℝ // i ≥ 0} // s < N}) => ∀ (t2 : {aa : {a : ℝ // start_time fp2 ≤ a ∧ a ≤ end_time fp2} // fp j.time ≤ aa ∧
 aa ≤
  fp j.time + min (fp (1 + j).time - fp j.time) (fp 0.time - fp j.time + T)}), t2 ≤ fp (j + 1).time ∧ t2 ≤ fp 0.time + T ∧ fp 0.time ≤ t2 ∧ t1 = fp j.time (segment fp t1) := by
  -- === TCCs for time_in_range ===
  -- TCC: time_in_range_TCC1
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         t1: FlightTimesLimited2(fp, fp2), j: below[N]):
  --   j = segment(fp)(t1) IMPLIES 1 + j < N
  have time_in_range_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: time_in_range_TCC2
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         t1: FlightTimesLimited2(fp, fp2), j: below[N]):
  --   j = segment(fp)(t1) IMPLIES
  --    min(fp(1 + j)`time - fp(j)`time, fp(0)`time - fp(j)...
  have time_in_range_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: time_in_range_TCC3
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         t1: FlightTimesLimited2(fp, fp2), j: below[N]):
  --   j = segment(fp)(t1) IMPLIES
  --    FORALL (t2:
  --              FlightTimesRelevant
  --       ...
  have time_in_range_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cd3d_ii.limited_bounds
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
--         jt: FlightTimesLimited2(fp, fp2), j: below[N]):
--   segment(fp)(jt) = j IMPLIES
--    fp2(0)`time <
--     jt + min(fp(j + 1)`time - fp(j)`time
theorem limited_bounds : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}) (jt : {aa : {a : ℝ // start_time fp ≤ a ∧ a ≤ end_time fp} // ∃ (j : Fin N - 1), (aa = fp j.time) ∧ (fp (1 + j).time > fp2 0.time)
   ∧ (aa < fp 0.time + T) ∧ (aa < fp (N - 1).time)
   ∧ (aa < fp2 (M - 1).time)}) (j : {s : {i : ℝ // i ≥ 0} // s < N}), segment fp(jt) = j → fp2 0.time <
 jt + min (fp (j + 1).time - fp j.time) (fp 0.time - fp j.time + T)
 ∧ jt < fp2 (M - 1).time := by
  -- === TCCs for limited_bounds ===
  -- TCC: limited_bounds_TCC1
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         jt: FlightTimesLimited2(fp, fp2), j: below[N]):
  --   segment(fp)(jt) = j IMPLIES j + 1 < N
  have limited_bounds_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: limited_bounds_TCC2
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         jt: FlightTimesLimited2(fp, fp2), j: below[N]):
  --   segment(fp)(jt) = j AND
  --    fp2(0)`time <
  --     jt + min(fp(j + 1)`time - fp(j)`time,...
  have limited_bounds_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cd3d_ii.upper_seg_bounds
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
--         jt: FlightTimesLimited2(fp, fp2), j: below[N]):
--   segment(fp)(jt) = j IMPLIES
--    min(fp(1 + j)`time - fp(j)`time, fp(0)`time - fp(j)`ti
theorem upper_seg_bounds : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}) (jt : {aa : {a : ℝ // start_time fp ≤ a ∧ a ≤ end_time fp} // ∃ (j : Fin N - 1), (aa = fp j.time) ∧ (fp (1 + j).time > fp2 0.time)
   ∧ (aa < fp 0.time + T) ∧ (aa < fp (N - 1).time)
   ∧ (aa < fp2 (M - 1).time)}) (j : {s : {i : ℝ // i ≥ 0} // s < N}), segment fp(jt) = j → min (fp (1 + j).time - fp j.time) (fp 0.time - fp j.time + T) + jt ≤
 fp (j + 1).time := by
  -- === TCCs for upper_seg_bounds ===
  -- TCC: upper_seg_bounds_TCC1
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         jt: FlightTimesLimited2(fp, fp2), j: below[N]):
  --   segment(fp)(jt) = j IMPLIES 1 + j < N
  have upper_seg_bounds_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cd3d_ii.cd3d_ii_rew
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp)):
--   conflict_3D_ii?(fp, fp2) IFF
--    EXISTS (tt: FlightTimesLimited2(fp, fp2)):
--      LET j = segment(fp)(tt),
--          relT = T - (fp(j)`time - f
theorem cd3d_ii_rew : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}), conflict_3D_ii_p fp fp2 ↔ ∃ (tt : FlightTimesLimited2 fp fp2), (let j := segment fp(tt); let relT := T - (fp j.time - fp 0.time); let t := min (fp (j + 1).time - fp j.time) relT; let so := fp j.position; let vo := velocity fp(j); conflict_3D_p so tt vo fp2) := by
  -- === TCCs for cd3d_ii_rew ===
  -- TCC: cd3d_ii_rew_TCC1
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         tt: FlightTimesLimited2(fp, fp2), j: below[N], relT: real):
  --   j = segment(fp)(tt) AND relT = T - (fp(j)`time - fp(0)`time) IMPLIES
  --  ...
  have cd3d_ii_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cd3d_ii_rew_TCC2
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         tt: FlightTimesLimited2(fp, fp2), j: below[N], relT: real,
  --         t: {p: real | p <= fp(1 + j)`time - fp(j)`time AND p <= relT},
  --   ...
  have cd3d_ii_rew_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cd3d_ii_rew_TCC3
  -- Statement: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
  --         tt: FlightTimesLimited2(fp, fp2), j: below[N], relT: real,
  --         t: {p: real | p <= fp(1 + j)`time - fp(j)`time AND p <= relT},
  --   ...
  have cd3d_ii_rew_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ii.cd3d_ii_ind?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET relT = T - (fp(j)`time - fp(0)`time),
--     t = min(fp(j + 1)`time - fp(j)`time, relT),
--     so = fp(j)`position,
--     to = fp(j)`time,
--     vo = velocity[N](fp)(j)
--   IN
--   IF relT > 0 AND
--       (fp2(0)
axiom cd3d_ii_ind_p {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: cd3d_ii.cd3d_ii_ind?_weak_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
--         P: [below[N - 1] -> bool]):
--   (FORALL (j: below[N - 1]):
--      IF T - (fp(j)`time - fp(0)`time) > 0 AND
--          (fp2(0)`time <
--         
theorem cd3d_ii_ind_p_weak_induction : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}) (P : {s : {i : ℝ // i ≥ 0} // s < N - 1} → Bool), ∀ (j : Fin N - 1), if T - (fp j.time - fp 0.time) > 0 ∧
      (fp2 0.time <
        min (fp (j + 1).time) (fp j.time + (T - (fp j.time - fp 0.time)))
        ∧ fp2 (M - 1).time > fp j.time)
    then cd3d_si_p
             (fp j.position, velocity fp(j), fp j.time, fp2)
  else False
  
   ∨ (j > 0 ∧ P (j - 1))
   → P j → ∀ (j : Fin N - 1), cd3d_ii_ind_p fp fp2(j) → P j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ii.cd3d_ii_ind?_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp),
--         P: [below[N - 1] -> bool]):
--   (FORALL (j: below[N - 1]):
--      IF T - (fp(j)`time - fp(0)`time) > 0 AND
--          (fp2(0)`time <
--         
theorem cd3d_ii_ind_p_induction : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}) (P : {s : {i : ℝ // i ≥ 0} // s < N - 1} → Bool), ∀ (j : Fin N - 1), if T - (fp j.time - fp 0.time) > 0 ∧
      (fp2 0.time <
        min (fp (j + 1).time) (fp j.time + (T - (fp j.time - fp 0.time)))
        ∧ fp2 (M - 1).time > fp j.time)
    then cd3d_si_p
             (fp j.position, velocity fp(j), fp j.time, fp2)
  else False
  
   ∨ (j > 0 ∧ cd3d_ii_ind_p fp fp2(j - 1) ∧ P (j - 1))
   → P j → ∀ (j : Fin N - 1), cd3d_ii_ind_p fp fp2(j) → P j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ii.some_cd3d_ii
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp), i: below[N - 1]):
--   cd3d_ii_ind?(fp, fp2)(i) IFF
--    EXISTS (j: below[N - 1]): j <= i AND cd3d_ii_ind?(fp, fp2)(j)
theorem some_cd3d_ii : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}) (i : {s : {i : ℝ // i ≥ 0} // s < N - 1}), cd3d_ii_ind_p fp fp2(i) ↔ ∃ (j : Fin N - 1), j ≤ i ∧ cd3d_ii_ind_p fp fp2(j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ii.cd3d_ii?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d_ii_ind?(fp, fp2)(N - 2)
axiom cd3d_ii_p {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: cd3d_ii.cd3d_ii_ind_correct
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp)):
--   (FORALL (i: below[N - 1]):
--      cd3d_ii_ind?(fp, fp2)(i) IMPLIES
--       EXISTS (j: below[N - 1]):
--         LET relT = T - (fp(j)`time - fp(0)`
theorem cd3d_ii_ind_correct : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < N - 1}), cd3d_ii_ind_p fp fp2(i) → ∃ (j : Fin N - 1), (let relT := T - (fp j.time - fp 0.time); let t := min (fp (j + 1).time - fp j.time) relT; let so := fp j.position; let to := fp j.time; let vo := velocity fp(j); (j ≤ i ∧
      if relT > 0 ∧
          fp2 0.time < min (fp (j + 1).time) (fp j.time + relT) ∧
           fp2 (M - 1).time > fp j.time
        then conflict_3D_p so to vo fp2
      else False
      )) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ii.cd3d_ii_ind_complete
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp)):
--   (FORALL (i: below[N - 1]):
--      EXISTS (j: below[N - 1]):
--        LET relT = T - (fp(j)`time - fp(0)`time),
--            t = min(fp(j + 1)`time
theorem cd3d_ii_ind_complete : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < N - 1}), ∃ (j : {s : {i : ℝ // i ≥ 0} // s < N - 1}), fun (relT : ℝ) => fun (t : {p : ℝ // p ≤ fp (1 + j).time - fp j.time ∧ p ≤ relT}) => fun (so : ℝ × ℝ × ℝ) => fun (to : ℝ) => fun (vo : ℝ × ℝ × ℝ) => (j ≤ i ∧
  if relT > 0 ∧
      fp2 0.time < min (fp (j + 1).time) (fp j.time + relT) ∧
       fp2 (M - 1).time > fp j.time
    then conflict_3D_p so to vo fp2
  else False
  ) → cd3d_ii_ind_p fp fp2(i) (velocity fp j) (fp j.time) (fp j.position) (min (fp (j + 1).time - fp j.time) relT) (T - (fp j.time - fp 0.time)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ii.cd3d_si_complete_parameters
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, t1: posreal, fp: FlightPlan[N],
--         fp2: FlightPlanRelevant2(fp)):
--   LET t2 = t1 IN
--     fp2(0)`time < t2 + to AND
--      to < fp2(M - 1)`time AND conflict_3D?[D, H, 
theorem cd3d_si_complete_parameters : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (t1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}), fun (t2 : {x : {x : ℝ // x ≥ 0} // x > 0}) => fp2 0.time < t2 + to ∧
 to < fp2 (M - 1).time ∧ conflict_3D_p so to vo fp2 → cd3d_si_p so vo to fp2 t1 := by
  -- === TCCs for cd3d_si_complete_parameters ===
  -- TCC: cd3d_si_complete_parameters_TCC1
  -- Statement: FORALL (to: real, t1: posreal, fp: FlightPlan[N],
  --         fp2: FlightPlanRelevant2(fp), t2: posreal):
  --   t2 = t1 AND fp2(0)`time < t2 + to IMPLIES M - 1 >= 0 AND M - 1 < M
  have cd3d_si_complete_parameters_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cd3d_si_complete_parameters_TCC2
  -- Statement: FORALL (so, vo: Vect3, to: real, t1: posreal, fp: FlightPlan[N],
  --         fp2: FlightPlanRelevant2(fp), t2: posreal):
  --   t2 = t1 AND
  --    fp2(0)`time < t2 + to AND
  --     to < fp2(M - 1)`time AND conflict...
  have cd3d_si_complete_parameters_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ii.cd3d_ii_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp)):
--   cd3d_ii?(fp, fp2) IMPLIES conflict_3D_ii?(fp, fp2)
theorem cd3d_ii_correct : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}), cd3d_ii_p fp fp2 → conflict_3D_ii_p fp fp2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ii.cd3d_ii_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fp: FlightPlan[N], fp2: FlightPlanRelevant2(fp)):
--   conflict_3D_ii?(fp, fp2) IMPLIES cd3d_ii?(fp, fp2)
theorem cd3d_ii_complete : ∀ (fp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time}) (fp2 : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < M} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin M), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < fp (N - 1).time ∧
 fp 0.time < flp (M - 1).time ∧ flp 0.time < fp 0.time + T}), conflict_3D_ii_p fp fp2 → cd3d_ii_p fp fp2 := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cd3d_ii