-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cd2d_shape
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cd2d_shape

Auto-generated stubs from PVSLib theory `cd2d_shape`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cd2d_shape

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Circle : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (CircularSegment : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (Rectangle : pvs_τ5 → pvs_τ6)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (hmd_tangent_point : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (horizontal_tca : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (mk_circle : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (mk_circular_segment : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (mk_rectangle : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (norm : pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} (perpL : pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (point_strictly_in_p : pvs_τ31 → pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (point_strictly_in_rectangle_p : pvs_τ34 → pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} (sq : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} (sqv : pvs_τ37 → Prop)

-- pvs2lean: cd2d_shape.cd2d_rectangle
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, si: Vect2, v: Nz_vect2, T: posreal):
--   LET s = so - si,
--       vD = hmd_tangent_point[D](v),
--       A = so + vD,
--       B = so - vD,
--       vT = T * v
--     IN
--     point_strictly_in_rectangle?(m
theorem cd2d_rectangle : ∀ (so : ℝ × ℝ) (si : ℝ × ℝ) (v : {v : ℝ × ℝ // v ≠ zero}) (T : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (s : ℝ × ℝ) => fun (vD : {v : ℝ × ℝ // v ≠ zero}) => fun (A : ℝ × ℝ) => fun (B : ℝ × ℝ) => fun (vT : {v : ℝ × ℝ // v ≠ zero}) => point_strictly_in_rectangle_p (mk_rectangle A B (-norm vT)) (si) ↔ |s * perpL ^(v)| < D ∧ 0 < -s * v < T * sqv v (T * v) (so - vD) (so + vD) (hmd_tangent_point v) (so - si) := by
  -- === TCCs for cd2d_rectangle ===
  -- TCC: cd2d_rectangle_TCC1
  -- Statement: FORALL (so, si: Vect2, v: Nz_vect2, T: posreal, s: Vector, vD: Nz_vect2,
  --         A: Vector, B: Vector, vT: Nz_vector):
  --        (s = so - si) AND (vD = hmd_tangent_point[D](v)) AND (A = so + vD)
  --    A...
  have cd2d_rectangle_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd2d_shape.cd2d_rectangle_tcpa
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, si: Vect2, v: Nz_vect2, T: posreal):
--   LET s = so - si,
--       vD = hmd_tangent_point[D](v),
--       A = so + vD,
--       B = so - vD,
--       vT = T * v
--     IN
--     point_strictly_in_rectangle?(m
theorem cd2d_rectangle_tcpa : ∀ (so : ℝ × ℝ) (si : ℝ × ℝ) (v : {v : ℝ × ℝ // v ≠ zero}) (T : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (s : ℝ × ℝ) => fun (vD : {v : ℝ × ℝ // v ≠ zero}) => fun (A : ℝ × ℝ) => fun (B : ℝ × ℝ) => fun (vT : {v : ℝ × ℝ // v ≠ zero}) => point_strictly_in_rectangle_p (mk_rectangle A B (-norm vT)) (si) ↔ (let tcpa := horizontal_tca s v; sqv (s + tcpa * v) < (D)^2 ∧ 0 < tcpa < T) (T * v) (so - vD) (so + vD) (hmd_tangent_point v) (so - si) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd2d_shape.cd2d_circular_segment_divergent
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, si: Vect2, v: Nz_vect2):
--   LET s = so - si, vD = hmd_tangent_point[D](v), A = so + vD, B = so - vD
--     IN
--     point_strictly_in?(mk_circular_segment(A, so, B), FALSE)(si) IFF
--      (sqv((s)
theorem cd2d_circular_segment_divergent : ∀ (so : ℝ × ℝ) (si : ℝ × ℝ) (v : {v : ℝ × ℝ // v ≠ zero}), fun (s : ℝ × ℝ) => fun (vD : {v : ℝ × ℝ // v ≠ zero}) => fun (A : ℝ × ℝ) => fun (B : ℝ × ℝ) => point_strictly_in_p (mk_circular_segment A so B) False(si) ↔ (sqv (s) < (D)^2 ∧ s * v ≥ 0) (so - vD) (so + vD) (hmd_tangent_point v) (so - si) := by
  -- === TCCs for cd2d_circular_segment_divergent ===
  -- TCC: cd2d_circular_segment_divergent_TCC1
  -- Statement: FORALL (so, si: Vect2, v: Nz_vect2, s: Vector, vD: Nz_vect2, A: Vector,
  --         B: Vector):
  --   s = so - si AND
  --    vD = hmd_tangent_point[D](v) AND A = so + vD AND B = so - vD
  --    IMPLIES (A /= so AND...
  have cd2d_circular_segment_divergent_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd2d_shape.cd2d_circular_segment_convergent
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, si: Vect2, v: Nz_vect2):
--   LET s = so - si, vD = hmd_tangent_point[D](v), A = so - vD, B = so + vD
--     IN
--     point_strictly_in?(mk_circular_segment(A, so, B), FALSE)(si) IFF
--      (sqv((s)
theorem cd2d_circular_segment_convergent : ∀ (so : ℝ × ℝ) (si : ℝ × ℝ) (v : {v : ℝ × ℝ // v ≠ zero}), fun (s : ℝ × ℝ) => fun (vD : {v : ℝ × ℝ // v ≠ zero}) => fun (A : ℝ × ℝ) => fun (B : ℝ × ℝ) => point_strictly_in_p (mk_circular_segment A so B) False(si) ↔ (sqv (s) < (D)^2 ∧ s * v ≤ 0) (so + vD) (so - vD) (hmd_tangent_point v) (so - si) := by
  -- === TCCs for cd2d_circular_segment_convergent ===
  -- TCC: cd2d_circular_segment_convergent_TCC1
  -- Statement: FORALL (so, si: Vect2, v: Nz_vect2, s: Vector, vD: Nz_vect2, A: Vector,
  --         B: Vector):
  --   s = so - si AND
  --    vD = hmd_tangent_point[D](v) AND A = so - vD AND B = so + vD
  --    IMPLIES (A /= so AND...
  have cd2d_circular_segment_convergent_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd2d_shape.cd2d_hazard_zone
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF T = 0 OR v = zero THEN (: Circle(mk_circle(so, D)) :)
-- ELSE LET vD = hmd_tangent_point[D](v),
--          A = so + vD,
--          B = so - vD,
--          vT = T * v,
--          soT = so + vT,
--          C = so
axiom cd2d_hazard_zone {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10

-- pvs2lean: cd2d_shape.cd2d_haz_soundness
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo, si, vi: Vect2, T: nnreal, ss: shapes_2D):
--   LET s = so - si, v = vo - vi, ss = cd2d_hazard_zone(so, v, T) IN
--     point_strictly_in?(ss)(si) IMPLIES
--      EXISTS (t: Lookahead[0, T]): sq
theorem cd2d_haz_soundness : ∀ (so : ℝ × ℝ) (vo : ℝ × ℝ) (si : ℝ × ℝ) (vi : ℝ × ℝ) (T : {x : ℝ // x ≥ 0}) (ss : List Shape2D), fun (s : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (ss : List Shape2D) => point_strictly_in_p ss(si) → ∃ (t : Lookahead 0 T), sqv (s + t * v) < (D)^2 (cd2d_hazard_zone so v T) (vo - vi) (so - si) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd2d_shape.cd2d_haz_completeness
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo, si, vi: Vect2, T: nnreal, ss: shapes_2D):
--   LET s = so - si, v = vo - vi, ss = cd2d_hazard_zone(so, v, T) IN
--     (EXISTS (t: Lookahead[0, T]): sqv(s + t * v) < sq(D)) IMPLIES
--      poin
theorem cd2d_haz_completeness : ∀ (so : ℝ × ℝ) (vo : ℝ × ℝ) (si : ℝ × ℝ) (vi : ℝ × ℝ) (T : {x : ℝ // x ≥ 0}) (ss : List Shape2D), fun (s : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (ss : List Shape2D) => (∃ (t : Lookahead 0 T), sqv (s + t * v) < (D)^2) → point_strictly_in_p ss(si) (cd2d_hazard_zone so v T) (vo - vi) (so - si) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd2d_shape.cd2d_haz_correctness
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo, si, vi: Vect2, T: nnreal, ss: shapes_2D):
--   LET s = so - si, v = vo - vi, ss = cd2d_hazard_zone(so, v, T) IN
--     point_strictly_in?(ss)(si) IFF
--      EXISTS (t: Lookahead[0, T]): sqv(s 
theorem cd2d_haz_correctness : ∀ (so : ℝ × ℝ) (vo : ℝ × ℝ) (si : ℝ × ℝ) (vi : ℝ × ℝ) (T : {x : ℝ // x ≥ 0}) (ss : List Shape2D), fun (s : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (ss : List Shape2D) => point_strictly_in_p ss(si) ↔ ∃ (t : Lookahead 0 T), sqv (s + t * v) < (D)^2 (cd2d_hazard_zone so v T) (vo - vi) (so - si) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cd2d_shape