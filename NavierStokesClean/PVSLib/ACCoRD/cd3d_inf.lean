-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cd3d_inf
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cd3d_inf

Auto-generated stubs from PVSLib theory `cd3d_inf`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cd3d_inf

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Delta : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (Theta_D : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (Theta_H : pvs_τ7 → pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (conflict_p : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (max : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (min : pvs_τ19 → pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (sign : pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} (sq : pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} (sqv : pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} (vect2 : pvs_τ26 → Prop)

-- pvs2lean: cd3d_inf.conflict_3D_2D_inf_stable
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: v`z /= 0 AND max(-H - sign(v`z) * s`z, 0) < H - sign(v`z) * s`z IMPLIES
--  (conflict?(s, v) IFF
--    conflict_2D?
--        [D * abs(v`z), max(-H - sign(v`z) * s`z, 0), H - sign(v`z) * s`z]
--        (vect2(abs
theorem conflict_3D_2D_inf_stable : v.z ≠ 0 ∧ max (-H - sign v.z * s.z) 0 < H - sign v.z * s.z → (conflict_p s v ↔
  conflict_2D_p
      (vect2 (|v.z| * s), vect2 v)) := by
  -- === TCCs for conflict_3D_2D_inf_stable ===
  -- TCC: conflict_3D_2D_inf_stable_TCC1
  -- Statement: FORALL (s, v: Vect3):
  --   max(-H - sign(v`z) * s`z, 0) < H - sign(v`z) * s`z AND v`z /= 0 IMPLIES
  --    D * abs(v`z) > 0
  have conflict_3d_2d_inf_stable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: conflict_3D_2D_inf_stable_TCC2
  -- Statement: FORALL (s, v: Vect3):
  --   max(-H - sign(v`z) * s`z, 0) < H - sign(v`z) * s`z AND v`z /= 0 IMPLIES
  --    H - sign(v`z) * s`z >= 0 AND
  --     H - sign(v`z) * s`z > 0 AND
  --      H - sign(v`z) * s`z > max(-H - s...
  have conflict_3d_2d_inf_stable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_inf.conflict_vz_swap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem conflict_vz_swap : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: cd3d_inf.conflict_on_open_interval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict?(s, v) IFF
--  EXISTS (topen: posreal):
--    sqv(vect2(s) + topen * vect2(v)) < sq(D) AND abs(s`z + topen * v`z) < H
theorem conflict_on_open_interval : conflict_p s v ↔ ∃ (topen : {x : ℝ // 0 < x}), sqv (vect2 s + topen * vect2 v) < (D)^2 ∧ |s.z + topen * v.z| < H := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_inf.detection_inf
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF vect2(nzv) = zero AND sqv(vect2(s)) < sq(D)
--   THEN (max(Theta_H(s`z, nzv`z, -1), 0), max(Theta_H(s`z, nzv`z, 1), 0))
-- ELSIF Delta(vect2(s), vect2(nzv)) > 0
--   THEN LET td1 = Theta_D(vect2(s), vect2(n
axiom detection_inf {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16

-- pvs2lean: cd3d_inf.detection_inf_correct
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (tin, tout) = detection_inf(s, nzv) IN
--   tin < t AND t < tout IMPLIES
--    abs(s`z + t * nzv`z) < H AND sqv(vect2(s) + t * vect2(nzv)) < sq(D)
theorem detection_inf_correct : fun (tin : {x : ℝ // x ≥ 0}) (tout : {x : ℝ // x ≥ 0}) => tin < t ∧ t < tout → |s.z + t * nzv.z| < H ∧ sqv (vect2 s + t * vect2 nzv) < (D)^2 (detection_inf s nzv) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_inf.detection_inf_complete
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (tin, tout) = detection_inf(s, nzv) IN
--   abs(s`z + t * nzv`z) < H AND sqv(vect2(s) + t * vect2(nzv)) < sq(D)
--    IMPLIES tin <= t AND t <= tout AND tin < tout
theorem detection_inf_complete : fun (tin : {x : ℝ // x ≥ 0}) (tout : {x : ℝ // x ≥ 0}) => |s.z + t * nzv.z| < H ∧ sqv (vect2 s + t * vect2 nzv) < (D)^2 → tin ≤ t ∧ t ≤ tout ∧ tin < tout (detection_inf s nzv) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_inf.conflict_detection_inf
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (tin, tout) = detection_inf(s, nzv) IN conflict?(s, nzv) IFF tin < tout
theorem conflict_detection_inf : fun (tin : {x : ℝ // x ≥ 0}) (tout : {x : ℝ // x ≥ 0}) => conflict_p s nzv ↔ tin < tout (detection_inf s nzv) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_inf.cd3d_inf?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF v`z = 0 AND abs(s`z) < H THEN cd2d_inf?[D](vect2(s), vect2(v))
-- ELSIF v`z /= 0 AND max(-H - sign(v`z) * s`z, 0) < H - sign(v`z) * s`z
--   THEN cd2d?[D * abs(v`z), max(-H - sign(v`z) * s`z, 0),
--        
axiom cd3d_inf_p {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: cd3d_inf.cd3d_inf_rewrite
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d_inf?(s, v) IFF
--  IF v`z = 0 AND abs(s`z) < H THEN cd2d_inf?[D](vect2(s), vect2(v))
--  ELSIF v`z /= 0 AND max(Theta_H(s`z, v`z, -1), 0) < Theta_H(s`z, v`z, 1)
--    THEN cd2d?[D, max(Theta_H(s`z, v`z, -
theorem cd3d_inf_rewrite : cd3d_inf_p s v ↔ if v.z = 0 ∧ |s.z| < H then cd2d_inf_p (vect2 s) (vect2 v)
ELSIF v.z ≠ 0 ∧ max (Theta_H s.z v.z -1) 0 < Theta_H s.z v.z 1
  then cd2d_p
           (vect2 s, vect2 v)
else False := by
  -- === TCCs for cd3d_inf_rewrite ===
  -- TCC: cd3d_inf_rewrite_TCC1
  -- Statement: FORALL (s, v: Vect3):
  --   max(Theta_H(s`z, v`z, -1), 0) < Theta_H(s`z, v`z, 1) AND
  --    v`z /= 0 AND NOT (v`z = 0 AND abs(s`z) < H)
  --    IMPLIES
  --    Theta_H[H](s`z, v`z, 1) >= 0 AND
  --     Theta_H[H](s`z, v`...
  have cd3d_inf_rewrite_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_inf.cd3d_inf_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d_inf?(s, v) IMPLIES conflict?(s, v)
theorem cd3d_inf_correct : cd3d_inf_p s v → conflict_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_inf.cd3d_inf_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict?(s, v) IMPLIES cd3d_inf?(s, v)
theorem cd3d_inf_complete : conflict_p s v → cd3d_inf_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_inf.cd3d_inf
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict?(s, v) IFF cd3d_inf?(s, v)
theorem cd3d_inf : conflict_p s v ↔ cd3d_inf_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cd3d_inf