-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cd3d_ever
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cd3d_ever

Auto-generated stubs from PVSLib theory `cd3d_ever`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cd3d_ever

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Theta_H : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (cd2d_ever : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (conflict_p : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (max : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (sign : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} (sq : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (sqv : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (vect2 : pvs_τ19 → pvs_τ20)

-- pvs2lean: cd3d_ever.conflict_2D_stable
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: v`z /= 0 AND max(-H - sign(v`z) * s`z, 0) < H - sign(v`z) * s`z IMPLIES
--  (conflict?(s, v) IFF
--    conflict_2D?
--        [D * abs(v`z), max(-H - sign(v`z) * s`z, 0), H - sign(v`z) * s`z]
--        (vect2(abs
theorem conflict_2D_stable : v.z ≠ 0 ∧ max (-H - sign v.z * s.z) 0 < H - sign v.z * s.z → (conflict_p s v ↔
  conflict_2D_p
      (vect2 (|v.z| * s), vect2 v)) := by
  -- === TCCs for conflict_2D_stable ===
  -- TCC: conflict_2D_stable_TCC1
  -- Statement: FORALL (s, v: Vect3):
  --   max(-H - sign(v`z) * s`z, 0) < H - sign(v`z) * s`z AND v`z /= 0 IMPLIES
  --    D * abs(v`z) > 0
  have conflict_2d_stable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: conflict_2D_stable_TCC2
  -- Statement: FORALL (s, v: Vect3):
  --   max(-H - sign(v`z) * s`z, 0) < H - sign(v`z) * s`z AND v`z /= 0 IMPLIES
  --    H - sign(v`z) * s`z >= 0 AND
  --     H - sign(v`z) * s`z > 0 AND
  --      H - sign(v`z) * s`z > max(-H - s...
  have conflict_2d_stable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ever.conflict_vz_swap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem conflict_vz_swap : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: cd3d_ever.conflict_on_open_interval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict?(s, v) IFF
--  EXISTS (topen: posreal):
--    sqv(vect2(s) + topen * vect2(v)) < sq(D) AND abs(s`z + topen * v`z) < H
theorem conflict_on_open_interval : conflict_p s v ↔ ∃ (topen : {x : ℝ // 0 < x}), sqv (vect2 s + topen * vect2 v) < (D)^2 ∧ |s.z + topen * v.z| < H := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ever.cd3d_ever?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF v`z = 0 AND abs(s`z) < H THEN cd2d_ever(D, vect2(s), vect2(v))
-- ELSIF v`z /= 0 AND max(-H - sign(v`z) * s`z, 0) < H - sign(v`z) * s`z
--   THEN cd2d?[D * abs(v`z), max(-H - sign(v`z) * s`z, 0),
--        
axiom cd3d_ever_p {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: cd3d_ever.cd3d_ever_rewrite
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d_ever?(s, v) IFF
--  IF v`z = 0 AND abs(s`z) < H THEN cd2d_ever(D, vect2(s), vect2(v))
--  ELSIF v`z /= 0 AND max(Theta_H(s`z, v`z, -1), 0) < Theta_H(s`z, v`z, 1)
--    THEN cd2d?[D, max(Theta_H(s`z, v`z, 
theorem cd3d_ever_rewrite : cd3d_ever_p s v ↔ if v.z = 0 ∧ |s.z| < H then cd2d_ever D (vect2 s) (vect2 v)
ELSIF v.z ≠ 0 ∧ max (Theta_H s.z v.z -1) 0 < Theta_H s.z v.z 1
  then cd2d_p
           (vect2 s, vect2 v)
else False := by
  -- === TCCs for cd3d_ever_rewrite ===
  -- TCC: cd3d_ever_rewrite_TCC1
  -- Statement: FORALL (s, v: Vect3):
  --   max(Theta_H(s`z, v`z, -1), 0) < Theta_H(s`z, v`z, 1) AND
  --    v`z /= 0 AND NOT (v`z = 0 AND abs(s`z) < H)
  --    IMPLIES
  --    Theta_H[H](s`z, v`z, 1) >= 0 AND
  --     Theta_H[H](s`z, v`...
  have cd3d_ever_rewrite_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ever.cd3d_ever_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d_ever?(s, v) IMPLIES conflict?(s, v)
theorem cd3d_ever_correct : cd3d_ever_p s v → conflict_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ever.cd3d_ever_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict?(s, v) IMPLIES cd3d_ever?(s, v)
theorem cd3d_ever_complete : conflict_p s v → cd3d_ever_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_ever.cd3d_ever
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict?(s, v) IFF cd3d_ever?(s, v)
theorem cd3d_ever : conflict_p s v ↔ cd3d_ever_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cd3d_ever