-- Auto-generated by pvs2lean/translator.py
-- PVS theory: bands_3D
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: bands_3D

Auto-generated stubs from PVSLib theory `bands_3D`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.bands_3D

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Delta : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (Theta_D : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (Theta_H : pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (Vgs_3D : pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (Vs : pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (Vtrk_3D : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (add : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (cd3d_p : pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (conflict_3D_p : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (max : pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (min : pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (seq : pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} (sq : pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} (sqv : pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} (subrange : pvs_τ56 → pvs_τ57 → pvs_τ58)
variable {pvs_τ62 : Type*} (vect2 : pvs_τ62 → Prop)

-- pvs2lean: bands_3D.trk_bands_3D
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF vo`z = vi`z AND abs(s`z) < H
--   THEN trk_bands[D, B, T, gsmin, gsmax](vect2(s), vect2(vo), vect2(vi))
-- ELSIF vo`z /= vi`z
--   THEN LET BB = max(Theta_H(s`z, vo`z - vi`z, -1), B),
--            TT = min(Th
axiom trk_bands_3D {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} : pvs_τ59 → pvs_τ60 → pvs_τ61 → Prop

-- pvs2lean: bands_3D.gs_bands_3D
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF vo`z = vi`z AND abs(s`z) < H
--   THEN gs_bands[D, B, T, gsmin, gsmax](vect2(s), vect2(vo), vect2(vi))
-- ELSIF vo`z /= vi`z
--   THEN LET BB = max(Theta_H(s`z, vo`z - vi`z, -1), B),
--            TT = min(The
axiom gs_bands_3D {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} : pvs_τ30 → pvs_τ31 → pvs_τ32 → Prop

-- pvs2lean: bands_3D.vs_bands_3D
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF vect2(vo - vi) = zero AND sqv(vect2(s)) < sq(D)
--   THEN vs_bands[H, B, T, vsmin, vsmax](s`z, vi`z)
-- ELSIF Delta(vect2(s), vect2(vo - vi)) > 0
--   THEN LET BB = max(Theta_D(vect2(s), vect2(vo - vi), -1)
axiom vs_bands_3D {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} : pvs_τ63 → pvs_τ64 → pvs_τ65 → Prop

-- pvs2lean: bands_3D.trk_bands_3D_not_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: trk_bands_3D(s, vo, vi)`length > 1
theorem trk_bands_3D_not_empty : trk_bands_3D s vo vi.List.length > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.trk_bands_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET trkb = trk_bands_3D(s, vo, vi) IN trkb`seq(trkb`length - 1) = 2 * pi
theorem trk_bands_last : fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => trkb.seq (trkb.List.length - 1) = 2 * pi (trk_bands_3D s vo vi) := by
  -- === TCCs for trk_bands_last ===
  -- TCC: trk_bands_last_TCC1
  -- Statement: FORALL (s: Vect3, vi, vo: Nzv2_vect3,
  --         trkb: {fs: (trk_fseq?[gsmin, gsmax]) | increasing?[real, <=](fs)}):
  --   trkb = trk_bands_3D(s, vo, vi) IMPLIES trkb`length - 1 >= 0
  have trk_bands_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.trk_bands_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET trkb = trk_bands_3D(s, vo, vi) IN trkb`seq(0) = 0
theorem trk_bands_first : fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => trkb.seq 0 = 0 (trk_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.gs_bands_3D_not_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: gs_bands_3D(s, vo, vi)`length > 1
theorem gs_bands_3D_not_empty : gs_bands_3D s vo vi.List.length > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.gs_bands_last
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET gsb = gs_bands_3D(s, vo, vi) IN gsb`seq(gsb`length - 1) = gsmax
theorem gs_bands_last : fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => gsb.seq (gsb.List.length - 1) = gsmax (gs_bands_3D s vo vi) := by
  -- === TCCs for gs_bands_last ===
  -- TCC: gs_bands_last_TCC1
  -- Statement: FORALL (s: Vect3, vi, vo: Nzv2_vect3,
  --         gsb: {fs: (gs_fseq?[gsmin, gsmax]) | increasing?[real, <=](fs)}):
  --   gsb = gs_bands_3D(s, vo, vi) IMPLIES gsb`length - 1 >= 0
  have gs_bands_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.gs_bands_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET gsb = gs_bands_3D(s, vo, vi) IN gsb`seq(0) = gsmin
theorem gs_bands_first : fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => gsb.seq 0 = gsmin (gs_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.vs_bands_3D_not_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vs_bands_3D(s, vo, vi)`length > 1
theorem vs_bands_3D_not_empty : vs_bands_3D s vo vi.List.length > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.vs_bands_last
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET vsb = vs_bands_3D(s, vo, vi) IN vsb`seq(vsb`length - 1) = vsmax
theorem vs_bands_last : fun (vsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => vsb.seq (vsb.List.length - 1) = vsmax (vs_bands_3D s vo vi) := by
  -- === TCCs for vs_bands_last ===
  -- TCC: vs_bands_last_TCC1
  -- Statement: FORALL (s: Vect3, vi, vo: Nzv2_vect3,
  --         vsb: {fs: (vs_fseq?[vsmin, vsmax]) | increasing?[real, <=](fs)}):
  --   vsb = vs_bands_3D(s, vo, vi) IMPLIES vsb`length - 1 >= 0
  have vs_bands_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.vs_bands_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET vsb = vs_bands_3D(s, vo, vi) IN vsb`seq(0) = vsmin
theorem vs_bands_first : fun (vsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => vsb.seq 0 = vsmin (vs_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.red_trk_band_3D?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mp = (trkb`seq(i) + trkb`seq(i + 1)) / 2 IN
--   cd3d?(s, Vtrk_3D(vo, vi)(mp))
axiom red_trk_band_3D_p {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} : pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46 → Prop

-- pvs2lean: bands_3D.red_gs_band_3D?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mp = (gsb`seq(i) + gsb`seq(i + 1)) / 2 IN cd3d?(s, Vgs_3D(vo, vi)(mp))
axiom red_gs_band_3D_p {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ37 → pvs_τ38 → pvs_τ39 → pvs_τ40 → pvs_τ41 → Prop

-- pvs2lean: bands_3D.red_vs_band_3D?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mp = (vsb`seq(i) + vsb`seq(i + 1)) / 2 IN cd3d?(s, Vs(vo, vi)(mp))
axiom red_vs_band_3D_p {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} : pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → pvs_τ51 → Prop

-- pvs2lean: bands_3D.trk_red_bands_3D
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET trkb = trk_bands_3D(s, vo, vi) IN
--   FORALL (i: subrange(0, trkb`length - 2)
--               | trkb`seq(i) /= trkb`seq(i + 1)):
--     red_trk_band_3D?(s, vo, vi, trkb)(i) IFF
--      (FORALL (x: real | tr
theorem trk_red_bands_3D : fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ trkb.List.length - 2} // trkb.seq i ≠ trkb.seq (i + 1)}), red_trk_band_3D_p s vo vi trkb(i) ↔ (∀ (x : real | trkb.seq i < x ∧ x < trkb.seq (i + 1)), conflict_3D_p s (Vtrk_3D vo vi(x))) (trk_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.trk_green_bands_3D
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET trkb = trk_bands_3D(s, vo, vi) IN
--   FORALL (i: subrange(0, trkb`length - 2)
--               | trkb`seq(i) /= trkb`seq(i + 1)):
--     NOT red_trk_band_3D?(s, vo, vi, trkb)(i) IFF
--      (FORALL (x: real 
theorem trk_green_bands_3D : fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ trkb.List.length - 2} // trkb.seq i ≠ trkb.seq (i + 1)}), ¬ red_trk_band_3D_p s vo vi trkb(i) ↔ (∀ (x : real | trkb.seq i < x ∧ x < trkb.seq (i + 1)), ¬ conflict_3D_p s (Vtrk_3D vo vi(x))) (trk_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.trk_bands_equivalence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET trkb = trk_bands_3D(s, vo, vi) IN
--   FORALL (x, y):
--     (0 <= x AND
--       x <= y AND
--        y <= 2 * pi AND
--         (FORALL (i: subrange(0, trkb`length - 1)):
--            trkb`seq(i) < x OR y < trkb
theorem trk_bands_equivalence : fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (x : ℝ) (y : ℝ), (0 ≤ x ∧
  x ≤ y ∧
   y ≤ 2 * pi ∧
    (∀ (i : Set.Icc 0 trkb.List.length - 1), trkb.seq i < x ∨ y < trkb.seq i)) → (conflict_3D_p s (Vtrk_3D vo vi(x)) ↔
  conflict_3D_p s (Vtrk_3D vo vi(y))) (trk_bands_3D s vo vi) := by
  -- === TCCs for trk_bands_equivalence ===
  -- TCC: trk_bands_equivalence_TCC1
  -- Statement: FORALL (s: Vect3, vi, vo: Nzv2_vect3,
  --         trkb: {fs: (trk_fseq?[gsmin, gsmax]) | increasing?[real, <=](fs)}):
  --   trkb = trk_bands_3D(s, vo, vi) IMPLIES
  --    FORALL (x: real, y: real):
  --      0 <= x ...
  have trk_bands_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.gs_red_bands_3D
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET gsb = gs_bands_3D(s, vo, vi) IN
--   FORALL (i: subrange(0, gsb`length - 2) | gsb`seq(i) /= gsb`seq(i + 1)):
--     red_gs_band_3D?(s, vo, vi, gsb)(i) IFF
--      (FORALL (x: real | gsb`seq(i) < x AND x < 
theorem gs_red_bands_3D : fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ gsb.List.length - 2} // gsb.seq i ≠ gsb.seq (i + 1)}), red_gs_band_3D_p s vo vi gsb(i) ↔ (∀ (x : real | gsb.seq i < x ∧ x < gsb.seq (i + 1)), conflict_3D_p s (Vgs_3D vo vi(x))) (gs_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.gs_green_bands_3D
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET gsb = gs_bands_3D(s, vo, vi) IN
--   FORALL (i: subrange(0, gsb`length - 2) | gsb`seq(i) /= gsb`seq(i + 1)):
--     NOT red_gs_band_3D?(s, vo, vi, gsb)(i) IFF
--      (FORALL (x: real | gsb`seq(i) < x AND 
theorem gs_green_bands_3D : fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ gsb.List.length - 2} // gsb.seq i ≠ gsb.seq (i + 1)}), ¬ red_gs_band_3D_p s vo vi gsb(i) ↔ (∀ (x : real | gsb.seq i < x ∧ x < gsb.seq (i + 1)), ¬ conflict_3D_p s (Vgs_3D vo vi(x))) (gs_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.gs_bands_equivalence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET gsb = gs_bands_3D(s, vo, vi) IN
--   FORALL (x, y):
--     (gsmin <= x AND
--       x <= y AND
--        y <= gsmax AND
--         (FORALL (i: subrange(0, gsb`length - 1)):
--            gsb`seq(i) < x OR y < gsb`s
theorem gs_bands_equivalence : fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (x : ℝ) (y : ℝ), (gsmin ≤ x ∧
  x ≤ y ∧
   y ≤ gsmax ∧
    (∀ (i : Set.Icc 0 gsb.List.length - 1), gsb.seq i < x ∨ y < gsb.seq i)) → (conflict_3D_p s (Vgs_3D vo vi(x)) ↔ conflict_3D_p s (Vgs_3D vo vi(y))) (gs_bands_3D s vo vi) := by
  -- === TCCs for gs_bands_equivalence ===
  -- TCC: gs_bands_equivalence_TCC1
  -- Statement: FORALL (s: Vect3, vi, vo: Nzv2_vect3,
  --         gsb: {fs: (gs_fseq?[gsmin, gsmax]) | increasing?[real, <=](fs)}):
  --   gsb = gs_bands_3D(s, vo, vi) IMPLIES
  --    FORALL (x: real, y: real):
  --      gsmin <= x ...
  have gs_bands_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.vs_red_bands_3D
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET vsb = vs_bands_3D(s, vo, vi) IN
--   FORALL (i: subrange(0, vsb`length - 2) | vsb`seq(i) /= vsb`seq(i + 1)):
--     red_vs_band_3D?(s, vo, vi, vsb)(i) IFF
--      (FORALL (x: real | vsb`seq(i) < x AND x < 
theorem vs_red_bands_3D : fun (vsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ vsb.List.length - 2} // vsb.seq i ≠ vsb.seq (i + 1)}), red_vs_band_3D_p s vo vi vsb(i) ↔ (∀ (x : real | vsb.seq i < x ∧ x < vsb.seq (i + 1)), conflict_3D_p s (Vs vo vi(x))) (vs_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.vs_green_bands_3D
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET vsb = vs_bands_3D(s, vo, vi) IN
--   FORALL (i: subrange(0, vsb`length - 2) | vsb`seq(i) /= vsb`seq(i + 1)):
--     NOT red_vs_band_3D?(s, vo, vi, vsb)(i) IFF
--      (FORALL (x: real | vsb`seq(i) < x AND 
theorem vs_green_bands_3D : fun (vsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ vsb.List.length - 2} // vsb.seq i ≠ vsb.seq (i + 1)}), ¬ red_vs_band_3D_p s vo vi vsb(i) ↔ (∀ (x : real | vsb.seq i < x ∧ x < vsb.seq (i + 1)), ¬ conflict_3D_p s (Vs vo vi(x))) (vs_bands_3D s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_3D.vs_bands_equivalence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET vsb = vs_bands_3D(s, vo, vi) IN
--   FORALL (x, y):
--     (vsmin <= x AND
--       x <= y AND
--        y <= vsmax AND
--         (FORALL (i: subrange(0, vsb`length - 1)):
--            vsb`seq(i) < x OR y < vsb`s
theorem vs_bands_equivalence : fun (vsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (x : ℝ) (y : ℝ), (vsmin ≤ x ∧
  x ≤ y ∧
   y ≤ vsmax ∧
    (∀ (i : Set.Icc 0 vsb.List.length - 1), vsb.seq i < x ∨ y < vsb.seq i)) → (conflict_3D_p s (Vs vo vi(x)) ↔ conflict_3D_p s (Vs vo vi(y))) (vs_bands_3D s vo vi) := by
  -- === TCCs for vs_bands_equivalence ===
  -- TCC: vs_bands_equivalence_TCC1
  -- Statement: FORALL (s: Vect3, vi, vo: Nzv2_vect3,
  --         vsb: {fs: (vs_fseq?[vsmin, vsmax]) | increasing?[real, <=](fs)}):
  --   vsb = vs_bands_3D(s, vo, vi) IMPLIES
  --    FORALL (x: real, y: real):
  --      vsmin <= x ...
  have vs_bands_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.bands_3D