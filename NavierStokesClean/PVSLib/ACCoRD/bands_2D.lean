-- Auto-generated by pvs2lean/translator.py
-- PVS theory: bands_2D
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: bands_2D

Auto-generated stubs from PVSLib theory `bands_2D`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.bands_2D

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Vgs : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (Vtrk : pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (addend : pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (cd2d_p : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (conflict_2D_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} (gs : pvs_τ16 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (gs_critical_p : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (gs_line_eps : pvs_τ27 → pvs_τ28 → pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} (gs_only_circle : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (member : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (seq : pvs_τ51 → pvs_τ52)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (singleton : pvs_τ53 → pvs_τ54)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} (sort : pvs_τ55 → pvs_τ56 → pvs_τ57)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (sq : pvs_τ58 → pvs_τ59)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (sqv : pvs_τ60 → pvs_τ61)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} (subrange : pvs_τ62 → pvs_τ63 → pvs_τ64)
variable {pvs_τ65 : Type*} (to2pi : pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} (track : pvs_τ66 → pvs_τ67)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} {pvs_τ76 : Type*} {pvs_τ77 : Type*} (trk_critical_p : pvs_τ74 → pvs_τ75 → pvs_τ76 → pvs_τ77 → Prop)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} {pvs_τ81 : Type*} {pvs_τ82 : Type*} {pvs_τ83 : Type*} (trk_line_eps_irt : pvs_τ78 → pvs_τ79 → pvs_τ80 → pvs_τ81 → pvs_τ82 → pvs_τ83)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} {pvs_τ86 : Type*} {pvs_τ87 : Type*} {pvs_τ88 : Type*} {pvs_τ89 : Type*} {pvs_τ90 : Type*} (trk_only_circle : pvs_τ84 → pvs_τ85 → pvs_τ86 → pvs_τ87 → pvs_τ88 → pvs_τ89 → pvs_τ90)

-- pvs2lean: bands_2D.gs_min_lt_max
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: gsmin < gsmax
theorem gs_min_lt_max : gsmin < gsmax := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bands_2D.trk_critical
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF nvo /= zero THEN addend(track(nvo), empty_seq) ELSE empty_seq ENDIF
axiom trk_critical {pvs_τ72 : Type*} {pvs_τ73 : Type*} : pvs_τ72 → pvs_τ73

-- pvs2lean: bands_2D.trk_critical_rew
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: trk_critical(nvo) =
--  IF nvo /= zero THEN singleton(track(nvo)) ELSE empty_seq ENDIF
theorem trk_critical_rew : trk_critical nvo = if nvo ≠ zero then singleton (track nvo) else empty_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.member_trk_critical
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(x, trk_critical(nvo)) IMPLIES x = track(nvo)
theorem member_trk_critical : member x (trk_critical nvo) → x = track nvo := by
  -- === TCCs for member_trk_critical ===
  -- TCC: member_trk_critical_TCC1
  -- Statement: FORALL (nvo: Vect2, x: real):
  --   member(x, trk_critical(nvo)) IMPLIES nvo /= zero
  have member_trk_critical_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.trk_critical_composition
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (trkfs: fseq, nvo: Vect2, x: real):
--   member(x, trkfs o trk_critical(nvo)) IFF
--    ((nvo /= zero AND x = track(nvo)) OR member(x, trkfs))
theorem trk_critical_composition : ∀ (trkfs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default})) (nvo : ℝ × ℝ) (x : ℝ), member x (trkfs ∘ trk_critical nvo) ↔ ((nvo ≠ zero ∧ x = track nvo) ∨ member x trkfs) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.trk_bands
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sort(LET fs2: (trk_fseq?) =
--            IF sqv(s) >= sq(D)
--              THEN trk_critical(trk_line_eps_irt(s, vo, vi, -1, -1)) o
--                    trk_critical(trk_line_eps_irt(s, vo, vi, -1, 1))
--     
axiom trk_bands {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} : pvs_τ68 → pvs_τ69 → pvs_τ70 → pvs_τ71

-- pvs2lean: bands_2D.trk_bands_not_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: trk_bands(s, vo, vi)`length > 1
theorem trk_bands_not_empty : trk_bands s vo vi.List.length > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.trk_bands_critical
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (trk_critical?(s, vo, vi)(trk2) OR to2pi(trk2) = 0) IFF
--  member(trk2, trk_bands(s, vo, vi))
theorem trk_bands_critical : (trk_critical_p s vo vi(trk2) ∨ to2pi trk2 = 0) ↔ member trk2 (trk_bands s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.gs_critical
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF nvo /= zero AND gsmin <= gs(nvo) AND gs(nvo) <= gsmax
--   THEN addend(gs(nvo), empty_seq)
-- ELSE empty_seq
-- ENDIF
axiom gs_critical {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22

-- pvs2lean: bands_2D.gs_critical_rew
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nvo: Vect2):
--   gs_critical(nvo) =
--    IF nvo /= zero AND gsmin <= gs(nvo) AND gs(nvo) <= gsmax
--      THEN singleton(gs(nvo))
--    ELSE empty_seq
--    ENDIF
theorem gs_critical_rew : ∀ (nvo : ℝ × ℝ), gs_critical nvo = if nvo ≠ zero ∧ gsmin ≤ gs nvo ∧ gs nvo ≤ gsmax
  then singleton (gs nvo)
else empty_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.member_gs_critical
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nvo: Vect2, x: real):
--   member(x, gs_critical(nvo)) IMPLIES x = gs(nvo)
theorem member_gs_critical : ∀ (nvo : ℝ × ℝ) (x : ℝ), member x (gs_critical nvo) → x = gs nvo := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.gs_critical_composition
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (gsfs: (gs_fseq?), nvo: Vect2, x: real):
--   member(x, gsfs o gs_critical(nvo)) IFF
--    ((nvo /= zero AND x = gs(nvo) AND gsmin <= gs(nvo) AND gs(nvo) <= gsmax)
--      OR member(x, gsfs))
theorem gs_critical_composition : ∀ (gsfs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default})) (nvo : ℝ × ℝ) (x : ℝ), member x (gsfs ∘ gs_critical nvo) ↔ ((nvo ≠ zero ∧ x = gs nvo ∧ gsmin ≤ gs nvo ∧ gs nvo ≤ gsmax) ∨
  member x gsfs) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.gs_bands
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sort(LET fs2: (gs_fseq?) =
--            IF sqv(s) >= sq(D)
--              THEN gs_critical(gs_line_eps(s, vo, vi, -1)) o
--                    gs_critical(gs_line_eps(s, vo, vi, 1))
--                    o gs_c
axiom gs_bands {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20

-- pvs2lean: bands_2D.gs_bands_not_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: gs_bands(s, vo, vi)`length > 1
theorem gs_bands_not_empty : gs_bands s vo vi.List.length > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.gs_bands_critical
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (gs_critical?(s, vo, vi)(gsp) OR gsp = gsmin OR gsp = gsmax) IFF
--  member(gsp, gs_bands(s, vo, vi))
theorem gs_bands_critical : (gs_critical_p s vo vi(gsp) ∨ gsp = gsmin ∨ gsp = gsmax) ↔ member gsp (gs_bands s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.red_trk_band?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mp = (trkb`seq(i) + trkb`seq(i + 1)) / 2 IN cd2d?(s, Vtrk(vo, vi)(mp))
axiom red_trk_band_p {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ46 → pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → Prop

-- pvs2lean: bands_2D.trk_red_bands
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET trkb = trk_bands(s, vo, vi) IN
--   FORALL (i:
--             {ii: subrange(0, trkb`length - 2) |
--                trkb`seq(ii) /= trkb`seq(ii + 1)}):
--     red_trk_band?(s, vo, vi, trkb)(i) IFF
--      (FORAL
theorem trk_red_bands : fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {ii : {k : ℝ // 0 ≤ k ∧ k ≤ trkb.List.length - 2} // ¬ trkb.seq ii = trkb.seq (1 + ii)}), red_trk_band_p s vo vi trkb(i) ↔ (∀ (x : real | trkb.seq i < x ∧ x < trkb.seq (i + 1)), conflict_2D_p s (Vtrk vo vi(x))) (trk_bands s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.trk_green_bands
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET trkb = trk_bands(s, vo, vi) IN
--   FORALL (i:
--             {ii: subrange(0, trkb`length - 2) |
--                trkb`seq(ii) /= trkb`seq(ii + 1)}):
--     NOT red_trk_band?(s, vo, vi, trkb)(i) IFF
--      (F
theorem trk_green_bands : fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {ii : {k : ℝ // 0 ≤ k ∧ k ≤ trkb.List.length - 2} // ¬ trkb.seq ii = trkb.seq (1 + ii)}), ¬ red_trk_band_p s vo vi trkb(i) ↔ (∀ (x : real | trkb.seq i < x ∧ x < trkb.seq (i + 1)), ¬ conflict_2D_p s (Vtrk vo vi(x))) (trk_bands s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.red_gs_band?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mp = (gsb`seq(i) + gsb`seq(i + 1)) / 2 IN cd2d?(s, Vgs(vo, vi)(mp))
axiom red_gs_band_p {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} : pvs_τ41 → pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45 → Prop

-- pvs2lean: bands_2D.gs_red_bands
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET gsb = gs_bands(s, vo, vi) IN
--   FORALL (i:
--             {ii: subrange(0, gsb`length - 2) |
--                gsb`seq(ii) /= gsb`seq(ii + 1)}):
--     red_gs_band?(s, vo, vi, gsb)(i) IFF
--      (FORALL (x: r
theorem gs_red_bands : fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {ii : {k : ℝ // 0 ≤ k ∧ k ≤ gsb.List.length - 2} // ¬ gsb.seq ii = gsb.seq (1 + ii)}), red_gs_band_p s vo vi gsb(i) ↔ (∀ (x : real | gsb.seq i < x ∧ x < gsb.seq (i + 1)), conflict_2D_p s (Vgs vo vi(x))) (gs_bands s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_2D.gs_green_bands
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET gsb = gs_bands(s, vo, vi) IN
--   FORALL (i:
--             {ii: subrange(0, gsb`length - 2) |
--                gsb`seq(ii) /= gsb`seq(ii + 1)}):
--     NOT red_gs_band?(s, vo, vi, gsb)(i) IFF
--      (FORALL (
theorem gs_green_bands : fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {ii : {k : ℝ // 0 ≤ k ∧ k ≤ gsb.List.length - 2} // ¬ gsb.seq ii = gsb.seq (1 + ii)}), ¬ red_gs_band_p s vo vi gsb(i) ↔ (∀ (x : real | gsb.seq i < x ∧ x < gsb.seq (i + 1)), ¬ conflict_2D_p s (Vgs vo vi(x))) (gs_bands s vo vi) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.bands_2D