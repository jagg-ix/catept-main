-- Auto-generated by pvs2lean/translator.py
-- PVS theory: circle_optimum_2D
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: circle_optimum_2D

Auto-generated stubs from PVSLib theory `circle_optimum_2D`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.circle_optimum_2D

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (ELSIF : pvs_τ1 → pvs_τ2)
variable {pvs_τ22 : Type*} (norm : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (orthogonal_p : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (orthonormal_p : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} (perpR : pvs_τ27 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (sign : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (sq : pvs_τ32 → pvs_τ33)
variable {pvs_τ34 : Type*} (sqv : pvs_τ34 → Prop)

-- pvs2lean: circle_optimum_2D.circle?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: norm(w - c) = R
axiom circle_p {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5 → Prop

-- pvs2lean: circle_optimum_2D.segment?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (t): w = t * v
axiom segment_p {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ28 → pvs_τ29 → Prop

-- pvs2lean: circle_optimum_2D.intersects_circle_fun?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (v /= zero AND Delta[R](-c, v) >= 0 AND (c * v >= 0 OR sqv(-c) <= sq(R)))
--  OR (v = zero AND sqv(c) = sq(R))
axiom intersects_circle_fun_p {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15 → Prop

-- pvs2lean: circle_optimum_2D.intersects_circle_fun_scal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: intersects_circle_fun?(v, c, R) IFF intersects_circle_fun?(k * v, c, R)
theorem intersects_circle_fun_scal : intersects_circle_fun_p v c R ↔ intersects_circle_fun_p (k * v) c R := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.intersects_circle_fun_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: intersects_circle_fun?(v, c, R) IFF
--  (EXISTS (w): circle?(c, R)(w) AND segment?(v)(w))
theorem intersects_circle_fun_def : intersects_circle_fun_p v c R ↔ (∃ (w : _), circle_p c R(w) ∧ segment_p v(w)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.intersects_circle_fun_inc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: v /= zero AND
--  u /= zero AND intersects_circle_fun?(v, c, R) AND ^(v) * c <= ^(u) * c
--  IMPLIES intersects_circle_fun?(u, c, R)
theorem intersects_circle_fun_inc : v ≠ zero ∧
 u ≠ zero ∧ intersects_circle_fun_p v c R ∧ ^(v) * c ≤ ^(u) * c → intersects_circle_fun_p u c R := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.inter_circle_max_time
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF v /= zero AND intersects_circle_fun?(v, c, R) THEN Theta_D[R](-c, v, 1)
-- ELSE 0
-- ENDIF
axiom inter_circle_max_time {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12

-- pvs2lean: circle_optimum_2D.inter_circle_max_time_scal
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (pt: posreal):
--   inter_circle_max_time(pt * v, c, R) =
--    (1 / pt) * inter_circle_max_time(v, c, R)
theorem inter_circle_max_time_scal : ∀ (pt : {x : {x : ℝ // x ≥ 0} // x > 0}), inter_circle_max_time (pt * v) c R = (1 / pt) * inter_circle_max_time v c R := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.inter_circle_max_time_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET t = inter_circle_max_time(v, c, R) IN
--   (intersects_circle_fun?(v, c, R) IMPLIES circle?(c, R)(t * v)) AND
--    (FORALL (nnt: nnreal):
--       circle?(c, R)(nnt * v) IMPLIES
--        norm(nnt * v) <= no
theorem inter_circle_max_time_def : fun (t : {x : ℝ // x ≥ 0}) => (intersects_circle_fun_p v c R → circle_p c R(t * v)) ∧ (∀ (nnt : NNReal), circle_p c R(nnt * v) →
    norm (nnt * v) ≤ norm (t * v) ∧
     (nnt ≠ t ∧ v ≠ zero → norm (nnt * v) < norm (t * v))) (inter_circle_max_time v c R) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.inter_circle_max_norm
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF intersects_circle_fun?(v, c, R)
--   THEN inter_circle_max_time(v, c, R) * norm(v)
-- ELSE 0
-- ENDIF
axiom inter_circle_max_norm {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ6 → pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: circle_optimum_2D.inter_circle_max_norm_scal
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (pt: posreal):
--   inter_circle_max_norm(pt * v, c, R) = inter_circle_max_norm(v, c, R)
theorem inter_circle_max_norm_scal : ∀ (pt : {x : {x : ℝ // x ≥ 0} // x > 0}), inter_circle_max_norm (pt * v) c R = inter_circle_max_norm v c R := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_norm_increasing
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (v /= zero AND u /= zero AND ^(v) * c <= ^(u) * c) OR
--  ((v = zero OR u = zero) AND norm(v) <= norm(u))
--  IMPLIES inter_circle_max_norm(v, c, R) <= inter_circle_max_norm(u, c, R)
theorem max_norm_increasing : (v ≠ zero ∧ u ≠ zero ∧ ^(v) * c ≤ ^(u) * c) ∨
 ((v = zero ∨ u = zero) ∧ norm v ≤ norm u) → inter_circle_max_norm v c R ≤ inter_circle_max_norm u c R := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_circle_point_in_slice
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF c = p AND perpR(e1) * e2 >= 0 THEN p + (R / norm(e1)) * perpR(e1)
-- ELSIF c = p THEN p - (R / norm(e1)) * perpR(e1)
-- ELSIF (c - p) * e1 >= 0 AND (c - p) * e2 >= 0
--   THEN p + (1 + R / norm(c - p)) * (c
axiom max_circle_point_in_slice {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21

-- pvs2lean: circle_optimum_2D.lem1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v1, v2: Vect2):
--   norm(v1) = 1 AND norm(v2) = 1 IMPLIES
--    ((v1 * v2 >= 0 IFF norm(v1 - v2) <= sqrt(2)) AND
--      (v1 * v2 > 0 IFF norm(v1 - v2) < sqrt(2)))
theorem lem1 : ∀ (v1 : ℝ × ℝ) (v2 : ℝ × ℝ), norm v1 = 1 ∧ norm v2 = 1 → ((v1 * v2 ≥ 0 ↔ norm (v1 - v2) ≤ Real.sqrt (2)) ∧
  (v1 * v2 > 0 ↔ norm (v1 - v2) < Real.sqrt (2))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.lem2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v1, v2, v3: Vect2):
--   norm(v1) = 1 AND norm(v2) = 1 AND norm(v3) = 1 IMPLIES
--    ((v1 * v3 <= v2 * v3) IFF norm(v1 - v3) >= norm(v2 - v3)) AND
--     ((v1 * v3 < v2 * v3) IFF norm(v1 - v3) > norm(
theorem lem2 : ∀ (v1 : ℝ × ℝ) (v2 : ℝ × ℝ) (v3 : ℝ × ℝ), norm v1 = 1 ∧ norm v2 = 1 ∧ norm v3 = 1 → ((v1 * v3 ≤ v2 * v3) ↔ norm (v1 - v3) ≥ norm (v2 - v3)) ∧
 ((v1 * v3 < v2 * v3) ↔ norm (v1 - v3) > norm (v2 - v3)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_circle_point_in_slice_intersection
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (aa, bb, cc, dd, ap, bp, cp, dp, h, g: Nz_vect2):
--        orthonormal?(aa, ap) AND orthonormal?(bb, bp)
--    AND orthonormal?(cc, cp) AND orthonormal?(dd, dp) AND (norm(h) = 1)
--    AND (norm(g) = 1
theorem max_circle_point_in_slice_intersection : ∀ (aa : {v : ℝ × ℝ // v ≠ zero}) (bb : {v : ℝ × ℝ // v ≠ zero}) (cc : {v : ℝ × ℝ // v ≠ zero}) (dd : {v : ℝ × ℝ // v ≠ zero}) (ap : {v : ℝ × ℝ // v ≠ zero}) (bp : {v : ℝ × ℝ // v ≠ zero}) (cp : {v : ℝ × ℝ // v ≠ zero}) (dp : {v : ℝ × ℝ // v ≠ zero}) (h : {v : ℝ × ℝ // v ≠ zero}) (g : {v : ℝ × ℝ // v ≠ zero}), orthonormal_p aa ap ∧ orthonormal_p bb bp ∧ orthonormal_p cc cp
 ∧ orthonormal_p dd dp ∧ (norm h = 1) ∧ (norm g = 1)
 ∧ (bp * aa ≥ 0) ∧ (ap * bb ≥ 0) ∧ (cp * dd ≥ 0) ∧ (dp * cc ≥ 0)
 ∧ (aa = bb → ap ≠ bp) ∧ (h * aa > 0) ∧ (h * bb > 0)
 ∧ (h * cc > 0) ∧ (h * dd > 0) ∧ (g * cc > 0) ∧ (g * dd > 0)
 ∧ (¬ (g * aa ≥ 0 ∧ g * bb ≥ 0)) → (ap * cc > 0 ∧ ap * dd > 0) ∨ (bp * cc > 0 ∧ bp * dd > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_circle_point_in_slice_union
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (aa, bb, cc, dd, ap, bp, cp, dp, h, g: Nz_vect2):
--        orthonormal?(aa, ap) AND orthonormal?(bb, bp)
--    AND orthonormal?(cc, cp) AND orthonormal?(dd, dp) AND (norm(h) = 1)
--    AND (norm(g) = 1
theorem max_circle_point_in_slice_union : ∀ (aa : {v : ℝ × ℝ // v ≠ zero}) (bb : {v : ℝ × ℝ // v ≠ zero}) (cc : {v : ℝ × ℝ // v ≠ zero}) (dd : {v : ℝ × ℝ // v ≠ zero}) (ap : {v : ℝ × ℝ // v ≠ zero}) (bp : {v : ℝ × ℝ // v ≠ zero}) (cp : {v : ℝ × ℝ // v ≠ zero}) (dp : {v : ℝ × ℝ // v ≠ zero}) (h : {v : ℝ × ℝ // v ≠ zero}) (g : {v : ℝ × ℝ // v ≠ zero}), orthonormal_p aa ap ∧ orthonormal_p bb bp ∧ orthonormal_p cc cp
 ∧ orthonormal_p dd dp ∧ (norm h = 1) ∧ (norm g = 1)
 ∧ (bp * aa ≥ 0) ∧ (ap * bb ≥ 0) ∧ (cp * dd ≥ 0) ∧ (dp * cc ≥ 0)
 ∧ (aa = bb → ap ≠ bp) ∧ (h * aa > 0) ∧ (h * bb > 0)
 ∧ (g * cc = g * dd) ∧ (h * cc > 0 ∨ h * dd > 0)
 ∧ (g * cc > 0 ∨ g * dd > 0) ∧ (¬ (g * aa ≥ 0 ∧ g * bb ≥ 0)) → (ap * cc > 0 ∨ ap * dd > 0) ∨ (bp * cc > 0 ∨ bp * dd > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.dot_gt_dot_equals_slice_intersection
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (g, rp: Vect2):
--   rp * g >= 0 AND norm(g) = 1 AND norm(rp) = 1 IMPLIES
--    (EXISTS (vv1, vv2: Nz_vect2):
--       norm(vv1) = 1 AND
--        norm(vv2) = 1 AND
--         g * vv1 = g * vv2 AND
--          (
theorem dot_gt_dot_equals_slice_intersection : ∀ (g : ℝ × ℝ) (rp : ℝ × ℝ), rp * g ≥ 0 ∧ norm g = 1 ∧ norm rp = 1 → (∃ (vv1 : _) (vv2 : Nz_vect2), norm vv1 = 1 ∧
    norm vv2 = 1 ∧
     g * vv1 = g * vv2 ∧
      (∀ (ww : Nz_vect2), norm ww = 1 →
          (ww * g > rp * g ↔ (ww * vv1 > 0 ∧ ww * vv2 > 0)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.dot_gt_dot_equals_slice_union
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (g, rp: Vect2):
--   rp * g < 0 AND norm(g) = 1 AND norm(rp) = 1 AND rp /= -g IMPLIES
--    (EXISTS (vv1, vv2: Nz_vect2):
--       norm(vv1) = 1 AND
--        norm(vv2) = 1 AND
--         g * vv1 = g * vv2 AN
theorem dot_gt_dot_equals_slice_union : ∀ (g : ℝ × ℝ) (rp : ℝ × ℝ), rp * g < 0 ∧ norm g = 1 ∧ norm rp = 1 ∧ rp ≠ -g → (∃ (vv1 : _) (vv2 : Nz_vect2), norm vv1 = 1 ∧
    norm vv2 = 1 ∧
     g * vv1 = g * vv2 ∧
      (∀ (ww : Nz_vect2), norm ww = 1 →
          (ww * g > rp * g ↔ (ww * vv1 > 0 ∨ ww * vv2 > 0)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_circle_point_in_slice_lem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (r, s, g, h, rp, sp: Vect2):
--         (norm(r) = 1) AND (norm(s) = 1) AND (norm(g) = 1) AND (norm(h) = 1)
--     AND (norm(rp) = 1) AND (norm(sp) = 1)
--     AND (NOT (g * r >= 0 AND g * s >= 0)) AND
theorem max_circle_point_in_slice_lem : ∀ (r : ℝ × ℝ) (s : ℝ × ℝ) (g : ℝ × ℝ) (h : ℝ × ℝ) (rp : ℝ × ℝ) (sp : ℝ × ℝ), (norm r = 1) ∧ (norm s = 1) ∧ (norm g = 1) ∧ (norm h = 1)
 ∧ (norm rp = 1) ∧ (norm sp = 1) ∧ (¬ (g * r ≥ 0 ∧ g * s ≥ 0))
 ∧ (sp * g ≤ rp * g) ∧ (h * r ≥ 0) ∧ (h * s ≥ 0)
 ∧ orthonormal_p r rp ∧ orthonormal_p s sp ∧ (rp * s ≥ 0)
 ∧ (sp * r ≥ 0) ∧ (r = s → rp ≠ sp)
 ∧ (¬ (sp * g ≤ 0 ∧ r = -s ∧ rp = sp ∧ h = -sp)) → h * g ≤ rp * g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_circle_point_in_slice_lem_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (r, s, g, h, rp, sp: Vect2):
--         (norm(r) = 1) AND (norm(s) = 1) AND (norm(g) = 1) AND (norm(h) = 1)
--     AND (norm(rp) = 1) AND (norm(sp) = 1)
--     AND (NOT (g * r >= 0 AND g * s >= 0)) AND
theorem max_circle_point_in_slice_lem_eq : ∀ (r : ℝ × ℝ) (s : ℝ × ℝ) (g : ℝ × ℝ) (h : ℝ × ℝ) (rp : ℝ × ℝ) (sp : ℝ × ℝ), (norm r = 1) ∧ (norm s = 1) ∧ (norm g = 1) ∧ (norm h = 1)
 ∧ (norm rp = 1) ∧ (norm sp = 1) ∧ (¬ (g * r ≥ 0 ∧ g * s ≥ 0))
 ∧ (sp * g ≤ rp * g) ∧ (h * r ≥ 0) ∧ (h * s ≥ 0)
 ∧ orthonormal_p r rp ∧ orthonormal_p s sp ∧ (rp * s ≥ 0)
 ∧ (sp * r ≥ 0) ∧ (r = s) ∧ (rp = -sp) → h * g ≤ rp * g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_circle_point_in_slide_sym_lem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (vv: Vect2, e1perp, e2perp: Nz_vect2):
--        (vv = p + inter_circle_max_time(e1perp, c - p, R) * e1perp)
--    AND circle?(c, R)(w) AND ((w - p) * e1 >= 0) AND ((w - p) * e2 >= 0)
--    AND orthogon
theorem max_circle_point_in_slide_sym_lem : ∀ (vv : ℝ × ℝ) (e1perp : {v : ℝ × ℝ // v ≠ zero}) (e2perp : {v : ℝ × ℝ // v ≠ zero}), (vv = p + inter_circle_max_time e1perp (c - p) R * e1perp)
 ∧ circle_p c R(w) ∧ ((w - p) * e1 ≥ 0) ∧ ((w - p) * e2 ≥ 0)
 ∧ orthogonal_p e1 e1perp ∧ orthogonal_p e2 e2perp
 ∧ (e1perp * e2 ≥ 0) ∧ (e2perp * e1 ≥ 0)
 ∧ ((e2perp ≠ zero ∧
        e1perp ≠ zero ∧
         ^(e2perp) * (c - p) ≤ ^(e1perp) * (c - p) ∧
          (c - p ≠ zero ∧
            (¬ (^(c - p) * ^(e1) ≥ 0 ∧ ^(c - p) * ^(e2) ≥ 0)) ∧
             ^(e1) = ^(e2) ∧ ^(e1perp) = -^(e2perp)
            ∨
            (c - p ≠ zero ∧
              ¬ (^(c - p) * ^(e1) ≥ 0 ∧ ^(c - p) * ^(e2) ≥ 0))
             ∧
             (^(e1) = ^(e2) → ^(e1perp) ≠ ^(e2perp)) ∧
              (¬ (^(e2perp) * ^(c - p) ≤ 0 ∧
                     ^(e1) = -^(e2) ∧
                      ^(e1perp) = ^(e2perp) ∧
                       w - p ≠ zero ∧ ^(w - p) = -^(e2perp)))))
       ∨
       ((e2perp = zero ∨ e1perp = zero) ∧
         norm (perpR e2) ≤ norm (perpR e1))) → (((c - p) * e1 ≥ 0 ∧ (c - p) * e2 ≥ 0) ∨
  norm (w - p) ≤ norm (vv - p) ∨ c = p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_circle_point_in_slice_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET vv = max_circle_point_in_slice(p, c, R, e1, e2) IN
--   (intersects_circle_fun?(vv - p, c - p, R) IMPLIES
--     circle?(c, R)(vv) AND (vv - p) * e1 >= 0 AND (vv - p) * e2 >= 0)
--    AND
--    (FORALL (w):
--  
theorem max_circle_point_in_slice_def : fun (vv : ℝ × ℝ) => (intersects_circle_fun_p (vv - p) (c - p) R →
  circle_p c R(vv) ∧ (vv - p) * e1 ≥ 0 ∧ (vv - p) * e2 ≥ 0) ∧ (∀ (w : _), circle_p c R(w) ∧ (w - p) * e1 ≥ 0 ∧ (w - p) * e2 ≥ 0 →
    norm (w - p) ≤ norm (vv - p)) (max_circle_point_in_slice p c R e1 e2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circle_optimum_2D.max_circle_point_in_slice_complete
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET vv = max_circle_point_in_slice(p, c, R, e1, e2) IN
--   FORALL (w):
--     circle?(c, R)(w) AND (w - p) * e1 >= 0 AND (w - p) * e2 >= 0 IMPLIES
--      intersects_circle_fun?(vv - p, c - p, R)
theorem max_circle_point_in_slice_complete : fun (vv : ℝ × ℝ) => ∀ (w : ℝ × ℝ), circle_p c R(w) ∧ (w - p) * e1 ≥ 0 ∧ (w - p) * e2 ≥ 0 → intersects_circle_fun_p (vv - p) (c - p) R (max_circle_point_in_slice p c R e1 e2) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.circle_optimum_2D