-- Auto-generated by pvs2lean/translator.py
-- PVS theory: bands_si
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: bands_si

Auto-generated stubs from PVSLib theory `bands_si`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.bands_si

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (FlightPlanRelevant_nz : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (conflict_3D_p : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (flp : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (fp : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (gs : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (gs2v3 : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (max : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (member : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (min : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} (number_field_pred : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} (real_pred : pvs_τ34 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} (seg_lh_bottom : pvs_τ53 → pvs_τ54 → pvs_τ55 → pvs_τ56)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} (seg_lh_top : pvs_τ57 → pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (seq : pvs_τ60 → pvs_τ61)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (sort : pvs_τ62 → pvs_τ63)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} (subrange : pvs_τ64 → pvs_τ65 → pvs_τ66)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (trk : pvs_τ67 → pvs_τ68)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (trk2v3 : pvs_τ69 → pvs_τ70 → Prop)
variable {pvs_τ81 : Type*} {pvs_τ82 : Type*} (trk_only_p : pvs_τ81 → pvs_τ82 → Prop)
variable {pvs_τ83 : Type*} (vect2 : pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} {pvs_τ86 : Type*} (velocity : pvs_τ84 → pvs_τ85 → pvs_τ86)
variable {pvs_τ87 : Type*} {pvs_τ88 : Type*} (vs : pvs_τ87 → pvs_τ88)
variable {pvs_τ89 : Type*} {pvs_τ90 : Type*} (vs2v3 : pvs_τ89 → pvs_τ90 → Prop)
variable {pvs_τ102 : Type*} (vs_only_p : pvs_τ102 → Prop)

-- pvs2lean: bands_si.trk_bands_si_i
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fi_st = fp(i)`time,
--     fi_et = fp(i + 1)`time,
--     vi = velocity(fp)(i),
--     Bo = B + to,
--     To = T + to
--   IN
--   IF fi_et <= Bo OR fi_st >= To THEN empty_seq
--   ELSE LET st = max(fi_st, Bo),
--      
axiom trk_bands_si_i {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} : pvs_τ76 → pvs_τ77 → pvs_τ78 → pvs_τ79 → pvs_τ80 → Prop

-- pvs2lean: bands_si.expand_trk_bands_si_i
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below[N - 1]):
--   fp(i + 1)`time > B + to AND fp(i)`time < T + to IMPLIES
--    trk_bands_si_i(so, vo, to, fp)(i) =
--     LET LB = max(fp(i)`time, B + to) - to,
--         UB = min(fp(1 + i)`time, T
theorem expand_trk_bands_si_i : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < N - 1}), fp (i + 1).time > B + to ∧ fp i.time < T + to → trk_bands_si_i so vo to fp(i) =
 (let LB := max (fp i.time) (B + to) - to; let UB := min (fp (1 + i).time) (T + to) - to; trk_bands_3D
       (so - (fp i.position - (fp i.time - to) * velocity fp(i)), vo,
        velocity fp(i))) := by
  -- === TCCs for expand_trk_bands_si_i ===
  -- TCC: expand_trk_bands_si_i_TCC1
  -- Statement: FORALL (i: below[N - 1]): i + 1 < N
  have expand_trk_bands_si_i_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expand_trk_bands_si_i_TCC2
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: below[N - 1]):
  --   fp(i + 1)`time > B + to IMPLIES i < N
  have expand_trk_bands_si_i_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expand_trk_bands_si_i_TCC3
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: below[N - 1]):
  --   fp(i + 1)`time > B + to AND fp(i)`time < T + to IMPLIES
  --    FORALL (LB: real): LB = max(fp(i)`time, B + to) - to IMPLIES 1 + i < N
  have expand_trk_bands_si_i_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expand_trk_bands_si_i_TCC4
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: below[N - 1]):
  --   fp(i + 1)`time > B + to AND fp(i)`time < T + to IMPLIES
  --    FORALL (LB: real, UB: real):
  --      LB = max(fp(i)`time, B + to) - to AND
  --       ...
  have expand_trk_bands_si_i_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expand_trk_bands_si_i_TCC5
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: below[N - 1]):
  --   fp(i + 1)`time > B + to AND fp(i)`time < T + to IMPLIES
  --    FORALL (LB: real, UB: real):
  --      LB = max(fp(i)`time, B + to) - to AND
  --       ...
  have expand_trk_bands_si_i_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN trk_bands_si_i(so, vo, to, fp)(i)
-- ELSE sort(trk_bands_si_i(so, vo, to, fp)(i) o
--            trk_bands_si(so, vo, to, fp)(i - 1))
-- ENDIF
axiom trk_bands_si {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} : pvs_τ71 → pvs_τ72 → pvs_τ73 → pvs_τ74 → pvs_τ75 → Prop

-- pvs2lean: bands_si.gs_bands_si_i
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fi_st = fp(i)`time,
--     fi_et = fp(i + 1)`time,
--     vi = velocity(fp)(i),
--     Bo = B + to,
--     To = T + to
--   IN
--   IF fi_et <= Bo OR fi_st >= To THEN empty_seq
--   ELSE LET st = max(fi_st, Bo),
--      
axiom gs_bands_si_i {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24

-- pvs2lean: bands_si.gs_bands_si
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN gs_bands_si_i(so, vo, to, fp)(i)
-- ELSE sort(gs_bands_si_i(so, vo, to, fp)(i) o
--            gs_bands_si(so, vo, to, fp)(i - 1))
-- ENDIF
axiom gs_bands_si {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18 → Prop

-- pvs2lean: bands_si.vs_bands_si_i
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fi_st = fp(i)`time,
--     fi_et = fp(i + 1)`time,
--     vi = velocity(fp)(i),
--     Bo = B + to,
--     To = T + to
--   IN
--   IF fi_et <= Bo OR fi_st >= To THEN empty_seq
--   ELSE LET st = max(fi_st, Bo),
--      
axiom vs_bands_si_i {pvs_τ96 : Type*} {pvs_τ97 : Type*} {pvs_τ98 : Type*} {pvs_τ99 : Type*} {pvs_τ100 : Type*} {pvs_τ101 : Type*} : pvs_τ96 → pvs_τ97 → pvs_τ98 → pvs_τ99 → pvs_τ100 → pvs_τ101

-- pvs2lean: bands_si.vs_bands_si
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN vs_bands_si_i(so, vo, to, fp)(i)
-- ELSE sort(vs_bands_si_i(so, vo, to, fp)(i) o
--            vs_bands_si(so, vo, to, fp)(i - 1))
-- ENDIF
axiom vs_bands_si {pvs_τ91 : Type*} {pvs_τ92 : Type*} {pvs_τ93 : Type*} {pvs_τ94 : Type*} {pvs_τ95 : Type*} : pvs_τ91 → pvs_τ92 → pvs_τ93 → pvs_τ94 → pvs_τ95 → Prop

-- pvs2lean: bands_si.red_trk_band_fp?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mp = (trkb`seq(i) + trkb`seq(i + 1)) / 2, trk = trk2v3(vo)(mp) IN
--   conflict_3D?(so, to, trk, fp)
axiom red_trk_band_fp_p {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} : pvs_τ41 → pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46 → Prop

-- pvs2lean: bands_si.red_gs_band_fp?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mp = (gsb`seq(i) + gsb`seq(i + 1)) / 2, gs = gs2v3(vo)(mp) IN
--   conflict_3D?(so, to, gs, fp)
axiom red_gs_band_fp_p {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} : pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39 → pvs_τ40 → Prop

-- pvs2lean: bands_si.red_vs_band_fp?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mp = (vsb`seq(i) + vsb`seq(i + 1)) / 2, vs = vs2v3(vo)(mp) IN
--   conflict_3D?(so, to, vs, fp)
axiom red_vs_band_fp_p {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} : pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52 → Prop

-- pvs2lean: bands_si.segdefs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: subrange(0, N - 2)):
--   seg_lh_bottom(fp, to)(j) + fp(j)`time - to = max(fp(j)`time, B + to) - to
--    AND
--    seg_lh_top(fp, to)(j) + fp(j)`time - to =
--     min(fp(1 + j)`time, T + to) - to
theorem segdefs : ∀ (j : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}), seg_lh_bottom fp to(j) + fp j.time - to = max (fp j.time) (B + to) - to ∧ seg_lh_top fp to(j) + fp j.time - to = min (fp (1 + j).time) (T + to) - to := by
  -- === TCCs for segdefs ===
  -- TCC: segdefs_TCC1
  -- Statement: FORALL (j: subrange(0, N - 2)): j >= 0 AND j < N - 1
  have segdefs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: segdefs_TCC2
  -- Statement: FORALL (j: subrange(0, N - 2)): j >= 0 AND j < N
  have segdefs_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: segdefs_TCC3
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, j: subrange(0, N - 2)):
  --   seg_lh_bottom(fp, to)(j) + fp(j)`time - to = max(fp(j)`time, B + to) - to
  --    IMPLIES 1 + j >= 0 AND 1 + j < N
  have segdefs_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si_combines
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2), x: real):
--   member(x, trk_bands_si(so, vo, to, fp)(i)) IFF
--    EXISTS (j: subrange(0, i)): member(x, trk_bands_si_i(so, vo, to, fp)(j))
theorem trk_bands_si_combines : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}) (x : ℝ), member x (trk_bands_si so vo to fp(i)) ↔ ∃ (j : Set.Icc 0 i), member x (trk_bands_si_i so vo to fp(j)) := by
  -- === TCCs for trk_bands_si_combines ===
  -- TCC: trk_bands_si_combines_TCC1
  -- Statement: FORALL (i: subrange(0, N - 2), j: subrange(0, i)): j >= 0 AND j < N - 1
  have trk_bands_si_combines_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.empty_no_members
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(x, empty_seq)
theorem empty_no_members : ¬(member x empty_seq) := by
  -- === TCCs for empty_no_members ===
  -- TCC: empty_no_members_TCC1
  -- Statement: FORALL (i: nat):
  --   i >= empty_seq[real]`length IMPLIES empty_seq[real]`seq(i) = default[real]
  have empty_no_members_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si_component
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2), x: real):
--   member(x, trk_bands_si(so, vo, to, fp)(i)) IFF
--    EXISTS (j: subrange(0, i)):
--      LET LB = max(fp(j)`time, B + to) - to,
--          UB = min(fp(1 + j)`time, T
theorem trk_bands_si_component : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}) (x : ℝ), member x (trk_bands_si so vo to fp(i)) ↔ ∃ (j : Set.Icc 0 i), (let LB := max (fp j.time) (B + to) - to; let UB := min (fp (1 + j).time) (T + to) - to; fp (j + 1).time > B + to ∧
     fp j.time < T + to ∧
      member x (trk_bands_3D
                 (so -
                   (fp j.position - (fp j.time - to) * velocity fp(j)),
                  vo, velocity fp(j)))) := by
  -- === TCCs for trk_bands_si_component ===
  -- TCC: trk_bands_si_component_TCC1
  -- Statement: FORALL (i: subrange(0, N - 2), j: subrange(0, i)): j >= 0 AND j < N
  have trk_bands_si_component_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: trk_bands_si_component_TCC2
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: subrange(0, N - 2),
  --         j: subrange(0, i), LB: real):
  --   LB = max(fp(j)`time, B + to) - to IMPLIES 1 + j >= 0 AND 1 + j < N
  have trk_bands_si_component_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: trk_bands_si_component_TCC3
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: subrange(0, N - 2),
  --         j: subrange(0, i), LB: real, UB: real):
  --   LB = max(fp(j)`time, B + to) - to AND
  --    UB = min(fp(1 + j)`time, T + to) - to
  --    IM...
  have trk_bands_si_component_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: trk_bands_si_component_TCC4
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: subrange(0, N - 2),
  --         j: subrange(0, i), LB: real, UB: real):
  --   LB = max(fp(j)`time, B + to) - to AND
  --    UB = min(fp(1 + j)`time, T + to) - to AND
  --  ...
  have trk_bands_si_component_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: trk_bands_si_component_TCC5
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: subrange(0, N - 2),
  --         j: subrange(0, i), LB: real, UB: real):
  --   LB = max(fp(j)`time, B + to) - to AND
  --    UB = min(fp(1 + j)`time, T + to) - to AND
  --  ...
  have trk_bands_si_component_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2), j: subrange(0, i)):
--   trk_bands_si(so, vo, to, fp)(i)`length >=
--    trk_bands_si_i(so, vo, to, fp)(j)`length
theorem trk_bands_si_length : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}) (j : {k : ℝ // 0 ≤ k ∧ k ≤ i}), trk_bands_si so vo to fp(i).List.length ≥ trk_bands_si_i so vo to fp(j).List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si_i_relevant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2)):
--   trk_bands_si_i(so, vo, to, fp)(i)`length > 0 IFF
--    fp(i)`time < to + T AND to + B < fp(i + 1)`time
theorem trk_bands_si_i_relevant : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}), trk_bands_si_i so vo to fp(i).List.length > 0 ↔ fp i.time < to + T ∧ to + B < fp (i + 1).time := by
  -- === TCCs for trk_bands_si_i_relevant ===
  -- TCC: trk_bands_si_i_relevant_TCC1
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: subrange(0, N - 2)):
  --   fp(i)`time < to + T IMPLIES i + 1 >= 0 AND i + 1 < N
  have trk_bands_si_i_relevant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si_i_relevant_has_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2)):
--   trk_bands_si_i(so, vo, to, fp)(i)`length > 0 IFF
--    trk_bands_si_i(so, vo, to, fp)(i)`length > 1
theorem trk_bands_si_i_relevant_has_2 : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}), trk_bands_si_i so vo to fp(i).List.length > 0 ↔ trk_bands_si_i so vo to fp(i).List.length > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si_relevant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2)):
--   trk_bands_si(so, vo, to, fp)(i)`length > 0 IFF
--    fp(0)`time < to + T AND to + B < fp(i + 1)`time
theorem trk_bands_si_relevant : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}), trk_bands_si so vo to fp(i).List.length > 0 ↔ fp 0.time < to + T ∧ to + B < fp (i + 1).time := by
  -- === TCCs for trk_bands_si_relevant ===
  -- TCC: trk_bands_si_relevant_TCC1
  -- Statement: FORALL (fp: FlightPlan_nz[N], to: real, i: subrange(0, N - 2)):
  --   fp(0)`time < to + T IMPLIES i + 1 >= 0 AND i + 1 < N
  have trk_bands_si_relevant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si_relevant_has_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2)):
--   trk_bands_si(so, vo, to, fp)(i)`length > 0 IFF
--    trk_bands_si(so, vo, to, fp)(i)`length > 1
theorem trk_bands_si_relevant_has_2 : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}), trk_bands_si so vo to fp(i).List.length > 0 ↔ trk_bands_si so vo to fp(i).List.length > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_bands_si_equivalence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET trkb = trk_bands_si(so, vo, to, fpr)(N - 2), trk = trk2v3(vo) IN
--     FORALL (x, y):
--       (0 <= x AND
--         x <= y AND
--          y <= 2 * pi AND
--        
theorem trk_bands_si_equivalence : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => fun (trk : ℝ → {vo2 : ℝ × ℝ × ℝ // trk_only_p (vect2 vo) (vect2 vo2) ∧ vo.z = vo2.z}) => ∀ (x : ℝ) (y : ℝ), (0 ≤ x ∧
  x ≤ y ∧
   y ≤ 2 * pi ∧
    (∀ (i : Set.Icc 0 trkb.List.length - 1), trkb.seq i < x ∨ y < trkb.seq i)) → (conflict_3D_p so to (trk x) fpr ↔ conflict_3D_p so to (trk y) fpr) (trk2v3 vo) (trk_bands_si so vo to fpr (N - 2)) := by
  -- === TCCs for trk_bands_si_equivalence ===
  -- TCC: trk_bands_si_equivalence_TCC1
  -- Statement: N - 2 >= 0 AND N - 2 < N - 1
  have trk_bands_si_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: trk_bands_si_equivalence_TCC2
  -- Statement: FORALL (so: Vect3, to: real, vo: Nzv2_vect3,
  --         fpr: FlightPlanRelevant_nz(to),
  --         trkb: {fs: (trk_fseq?[gsmin, gsmax]) | increasing?[real, <=](fs)},
  --         trk:
  --           [real ->
  --      ...
  have trk_bands_si_equivalence_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_red_bands_si
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET trkb = trk_bands_si(so, vo, to, fpr)(N - 2) IN
--     FORALL (i: subrange(0, trkb`length - 2)
--                 | trkb`seq(i) /= trkb`seq(i + 1)):
--       red_t
theorem trk_red_bands_si : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ trkb.List.length - 2} // trkb.seq i ≠ trkb.seq (i + 1)}), red_trk_band_fp_p so vo to fpr trkb(i) ↔ (∀ (x : real | trkb.seq i < x ∧ x < trkb.seq (i + 1)), (let trk := trk2v3 vo(x); conflict_3D_p so to trk fpr)) (trk_bands_si so vo to fpr (N - 2)) := by
  -- === TCCs for trk_red_bands_si ===
  -- TCC: trk_red_bands_si_TCC1
  -- Statement: FORALL (so: Vect3, to: real, vo: Nzv2_vect3,
  --         fpr: FlightPlanRelevant_nz(to),
  --         trkb: {fs: (trk_fseq?[gsmin, gsmax]) | increasing?[real, <=](fs)}):
  --   trkb = trk_bands_si(so, vo, to, fp...
  have trk_red_bands_si_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.trk_green_bands_si
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET trkb = trk_bands_si(so, vo, to, fpr)(N - 2) IN
--     FORALL (i: subrange(0, trkb`length - 2)
--                 | trkb`seq(i) /= trkb`seq(i + 1)):
--       NOT r
theorem trk_green_bands_si : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (trkb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ trkb.List.length - 2} // trkb.seq i ≠ trkb.seq (i + 1)}), ¬ red_trk_band_fp_p so vo to fpr trkb(i) ↔ (∀ (x : real | trkb.seq i < x ∧ x < trkb.seq (i + 1)), (let trk := trk2v3 vo(x); ¬ conflict_3D_p so to trk fpr)) (trk_bands_si so vo to fpr (N - 2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.gs_bands_si_combines
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2), x: real):
--   member(x, gs_bands_si(so, vo, to, fp)(i)) IFF
--    EXISTS (j: subrange(0, i)): member(x, gs_bands_si_i(so, vo, to, fp)(j))
theorem gs_bands_si_combines : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}) (x : ℝ), member x (gs_bands_si so vo to fp(i)) ↔ ∃ (j : Set.Icc 0 i), member x (gs_bands_si_i so vo to fp(j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.gs_bands_si_component
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2), x: real):
--   member(x, gs_bands_si(so, vo, to, fp)(i)) IFF
--    EXISTS (j: subrange(0, i)):
--      LET LB = max(fp(j)`time, B + to) - to,
--          UB = min(fp(1 + j)`time, T 
theorem gs_bands_si_component : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}) (x : ℝ), member x (gs_bands_si so vo to fp(i)) ↔ ∃ (j : Set.Icc 0 i), (let LB := max (fp j.time) (B + to) - to; let UB := min (fp (1 + j).time) (T + to) - to; fp (j + 1).time > B + to ∧
     fp j.time < T + to ∧
      member x (gs_bands_3D
                 (so -
                   (fp j.position - (fp j.time - to) * velocity fp(j)),
                  vo, velocity fp(j)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.gs_bands_si_equivalence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET gsb = gs_bands_si(so, vo, to, fpr)(N - 2), gs = gs2v3(vo) IN
--     FORALL (x, y):
--       (gsmin <= x AND
--         x <= y AND
--          y <= gsmax AND
--         
theorem gs_bands_si_equivalence : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => fun (gs : ℝ → ℝ × ℝ × ℝ) => ∀ (x : ℝ) (y : ℝ), (gsmin ≤ x ∧
  x ≤ y ∧
   y ≤ gsmax ∧
    (∀ (i : Set.Icc 0 gsb.List.length - 1), gsb.seq i < x ∨ y < gsb.seq i)) → (conflict_3D_p so to (gs x) fpr ↔ conflict_3D_p so to (gs y) fpr) (gs2v3 vo) (gs_bands_si so vo to fpr (N - 2)) := by
  -- === TCCs for gs_bands_si_equivalence ===
  -- TCC: gs_bands_si_equivalence_TCC1
  -- Statement: FORALL (so: Vect3, to: real, vo: Nzv2_vect3,
  --         fpr: FlightPlanRelevant_nz(to),
  --         gsb: {fs: (gs_fseq?[gsmin, gsmax]) | increasing?[real, <=](fs)},
  --         gs: [real -> Vect3]):
  --   gsb = g...
  have gs_bands_si_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.gs_red_bands_si
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET gsb = gs_bands_si(so, vo, to, fpr)(N - 2) IN
--     FORALL (i: subrange(0, gsb`length - 2) | gsb`seq(i) /= gsb`seq(i + 1)):
--       red_gs_band_fp?(so, vo, to
theorem gs_red_bands_si : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ gsb.List.length - 2} // gsb.seq i ≠ gsb.seq (i + 1)}), red_gs_band_fp_p so vo to fpr gsb(i) ↔ (∀ (x : real | gsb.seq i < x ∧ x < gsb.seq (i + 1)), (let gs := gs2v3 vo(x); conflict_3D_p so to gs fpr)) (gs_bands_si so vo to fpr (N - 2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.gs_green_bands_si
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET gsb = gs_bands_si(so, vo, to, fpr)(N - 2) IN
--     FORALL (i: subrange(0, gsb`length - 2) | gsb`seq(i) /= gsb`seq(i + 1)):
--       NOT red_gs_band_fp?(so, vo
theorem gs_green_bands_si : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (gsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ gsb.List.length - 2} // gsb.seq i ≠ gsb.seq (i + 1)}), ¬ red_gs_band_fp_p so vo to fpr gsb(i) ↔ (∀ (x : real | gsb.seq i < x ∧ x < gsb.seq (i + 1)), (let gs := gs2v3 vo(x); ¬ conflict_3D_p so to gs fpr)) (gs_bands_si so vo to fpr (N - 2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.vs_bands_si_combines
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2), x: real):
--   member(x, vs_bands_si(so, vo, to, fp)(i)) IFF
--    EXISTS (j: subrange(0, i)): member(x, vs_bands_si_i(so, vo, to, fp)(j))
theorem vs_bands_si_combines : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}) (x : ℝ), member x (vs_bands_si so vo to fp(i)) ↔ ∃ (j : Set.Icc 0 i), member x (vs_bands_si_i so vo to fp(j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.vs_bands_si_component
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange(0, N - 2), x: real):
--   member(x, vs_bands_si(so, vo, to, fp)(i)) IFF
--    EXISTS (j: subrange(0, i)):
--      LET LB = max(fp(j)`time, B + to) - to,
--          UB = min(fp(1 + j)`time, T 
theorem vs_bands_si_component : ∀ (i : {k : ℝ // 0 ≤ k ∧ k ≤ N - 2}) (x : ℝ), member x (vs_bands_si so vo to fp(i)) ↔ ∃ (j : Set.Icc 0 i), (let LB := max (fp j.time) (B + to) - to; let UB := min (fp (1 + j).time) (T + to) - to; fp (j + 1).time > B + to ∧
     fp j.time < T + to ∧
      member x (vs_bands_3D
                 (so -
                   (fp j.position - (fp j.time - to) * velocity fp(j)),
                  vo, velocity fp(j)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.vs_bands_si_equivalence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET vsb = vs_bands_si(so, vo, to, fpr)(N - 2), vs = vs2v3(vo) IN
--     FORALL (x, y):
--       (vsmin <= x AND
--         x <= y AND
--          y <= vsmax AND
--         
theorem vs_bands_si_equivalence : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (vsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => fun (vs : ℝ → ℝ × ℝ × ℝ) => ∀ (x : ℝ) (y : ℝ), (vsmin ≤ x ∧
  x ≤ y ∧
   y ≤ vsmax ∧
    (∀ (i : Set.Icc 0 vsb.List.length - 1), vsb.seq i < x ∨ y < vsb.seq i)) → (conflict_3D_p so to (vs x) fpr ↔ conflict_3D_p so to (vs y) fpr) (vs2v3 vo) (vs_bands_si so vo to fpr (N - 2)) := by
  -- === TCCs for vs_bands_si_equivalence ===
  -- TCC: vs_bands_si_equivalence_TCC1
  -- Statement: FORALL (so: Vect3, to: real, vo: Nzv2_vect3,
  --         fpr: FlightPlanRelevant_nz(to),
  --         vsb: {fs: (vs_fseq?[vsmin, vsmax]) | increasing?[real, <=](fs)},
  --         vs: [real -> (vs_only?(vo))]):
  -- ...
  have vs_bands_si_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.vs_red_bands_si
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET vsb = vs_bands_si(so, vo, to, fpr)(N - 2) IN
--     FORALL (i: subrange(0, vsb`length - 2) | vsb`seq(i) /= vsb`seq(i + 1)):
--       red_vs_band_fp?(so, vo, to
theorem vs_red_bands_si : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (vsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ vsb.List.length - 2} // vsb.seq i ≠ vsb.seq (i + 1)}), red_vs_band_fp_p so vo to fpr vsb(i) ↔ (∀ (x : real | vsb.seq i < x ∧ x < vsb.seq (i + 1)), (let vs := vs2v3 vo(x); conflict_3D_p so to vs fpr)) (vs_bands_si so vo to fpr (N - 2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bands_si.vs_green_bands_si
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fpr: FlightPlanRelevant_nz(to)):
--   LET vsb = vs_bands_si(so, vo, to, fpr)(N - 2) IN
--     FORALL (i: subrange(0, vsb`length - 2) | vsb`seq(i) /= vsb`seq(i + 1)):
--       NOT red_vs_band_fp?(so, vo
theorem vs_green_bands_si : ∀ (fpr : {flp : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time} // ∀ (i : Fin N - 1), vect2 (velocity flp(i)) ≠ zero}), fun (vsb : {fs : {i : ℝ // i ≥ 0} × ({f : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), i ≥ List.length → f i = default}) // increasing_p fs}) => ∀ (i : {i : {k : ℝ // 0 ≤ k ∧ k ≤ vsb.List.length - 2} // vsb.seq i ≠ vsb.seq (i + 1)}), ¬ red_vs_band_fp_p so vo to fpr vsb(i) ↔ (∀ (x : real | vsb.seq i < x ∧ x < vsb.seq (i + 1)), (let vs := vs2v3 vo(x); ¬ conflict_3D_p so to vs fpr)) (vs_bands_si so vo to fpr (N - 2)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.bands_si