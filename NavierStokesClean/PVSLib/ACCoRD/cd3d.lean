-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cd3d
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cd3d

Auto-generated stubs from PVSLib theory `cd3d`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cd3d

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Delta : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Theta_D : pvs_τ3 → pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (Theta_H : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (circle_solution_2D_p : pvs_τ12 → pvs_τ13 → pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (horizontal_conflict_p : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (max : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (min : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (sign : pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} (sq : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} (sqv : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} (vect2 : pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (vertical_conflict_p : pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (vertical_solution_p : pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → Prop)

-- pvs2lean: cd3d.conflict_3D?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (t: Lookahead[B, T]):
--   abs(s`z + t * v`z) < H AND sqv(vect2(s) + t * vect2(v)) < sq(D)
axiom conflict_3D_p {pvs_τ16 : Type*} {pvs_τ17 : Type*} : pvs_τ16 → pvs_τ17 → Prop

-- pvs2lean: cd3d.conflict_3D_2D_stable
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: v`z /= 0 AND
--  max(-H - sign(v`z) * s`z, B * abs(v`z)) <
--   min(H - sign(v`z) * s`z, T * abs(v`z))
--  IMPLIES
--  (conflict_3D?(s, v) IFF
--    conflict_2D?
--        [D * abs(v`z), max(-H - sign(v`z) * s`z, B * a
theorem conflict_3D_2D_stable : v.z ≠ 0 ∧
 max (-H - sign v.z * s.z) (B * |v.z|) <
  min (H - sign v.z * s.z) (T * |v.z|) → (conflict_3D_p s v ↔
  conflict_2D_p
      (vect2 (|v.z| * s), vect2 v)) := by
  -- === TCCs for conflict_3D_2D_stable ===
  -- TCC: conflict_3D_2D_stable_TCC1
  -- Statement: FORALL (s, v: Vect3):
  --   max(-H - sign(v`z) * s`z, B * abs(v`z)) <
  --    min(H - sign(v`z) * s`z, T * abs(v`z))
  --    AND v`z /= 0
  --    IMPLIES D * abs(v`z) > 0
  have conflict_3d_2d_stable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: conflict_3D_2D_stable_TCC2
  -- Statement: FORALL (s, v: Vect3):
  --   max(-H - sign(v`z) * s`z, B * abs(v`z)) <
  --    min(H - sign(v`z) * s`z, T * abs(v`z))
  --    AND v`z /= 0
  --    IMPLIES max(-H - sign(v`z) * s`z, B * abs(v`z)) >= 0
  have conflict_3d_2d_stable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: conflict_3D_2D_stable_TCC3
  -- Statement: FORALL (s, v: Vect3):
  --   max(-H - sign(v`z) * s`z, B * abs(v`z)) <
  --    min(H - sign(v`z) * s`z, T * abs(v`z))
  --    AND v`z /= 0
  --    IMPLIES
  --    min(H - sign(v`z) * s`z, T * abs(v`z)) >= 0 AND
  --     min(H -...
  have conflict_3d_2d_stable_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.conflict_3D_vz_swap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem conflict_3D_vz_swap : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: cd3d.conflict_3D_on_open_interval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict_3D?(s, v) IFF
--  EXISTS (topen: Lookahead[B, T]):
--    B < topen AND
--     topen < T AND
--      sqv(vect2(s) + topen * vect2(v)) < sq(D) AND abs(s`z + topen * v`z) < H
theorem conflict_3D_on_open_interval : conflict_3D_p s v ↔ ∃ (topen : Lookahead B T), B < topen ∧
   topen < T ∧
    sqv (vect2 s + topen * vect2 v) < (D)^2 ∧ |s.z + topen * v.z| < H := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.conflict_3D_vertical
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: vect2(v) /= zero AND
--  Delta(vect2(s), vect2(v)) >= 0 AND
--   max(Theta_D(vect2(s), vect2(v), -1), B) <
--    min(Theta_D(vect2(s), vect2(v), 1), T)
--  IMPLIES
--  (conflict_3D?(s, v) IFF
--    conflict_vertical?
--  
theorem conflict_3D_vertical : vect2 v ≠ zero ∧
 Delta (vect2 s) (vect2 v) ≥ 0 ∧
  max (Theta_D (vect2 s) (vect2 v) -1) B <
   min (Theta_D (vect2 s) (vect2 v) 1) T → (conflict_3D_p s v ↔
  conflict_vertical_p
      (s.z, v.z)) := by
  -- === TCCs for conflict_3D_vertical ===
  -- TCC: conflict_3D_vertical_TCC1
  -- Statement: FORALL (s, v: Vect3):
  --   max(Theta_D(vect2(s), vect2(v), -1), B) <
  --    min(Theta_D(vect2(s), vect2(v), 1), T)
  --    AND Delta(vect2(s), vect2(v)) >= 0 AND vect2(v) /= zero
  --    IMPLIES max(Theta_D[D](vect...
  have conflict_3d_vertical_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: conflict_3D_vertical_TCC2
  -- Statement: FORALL (s, v: Vect3):
  --   max(Theta_D(vect2(s), vect2(v), -1), B) <
  --    min(Theta_D(vect2(s), vect2(v), 1), T)
  --    AND Delta(vect2(s), vect2(v)) >= 0 AND vect2(v) /= zero
  --    IMPLIES
  --    min(Theta_D[D](v...
  have conflict_3d_vertical_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.detection
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF vect2(v) = zero AND sqv(vect2(s)) < sq(D)
--   THEN IF v`z /= 0
--          THEN (min(max(Theta_H(s`z, v`z, -1), B), T),
--                max(min(Theta_H(s`z, v`z, 1), T), B))
--        ELSIF abs(s`z) < H THE
axiom detection {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ18 → pvs_τ19 → pvs_τ20

-- pvs2lean: cd3d.detection_correct
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (tin, tout) = detection(s, v) IN
--   tin < t AND t < tout IMPLIES
--    abs(s`z + t * v`z) < H AND sqv(vect2(s) + t * vect2(v)) < sq(D)
theorem detection_correct : fun (tin : {t : {x : ℝ // x ≥ 0} // B ≤ t ∧ t ≤ T}) (tout : {t : {x : ℝ // x ≥ 0} // B ≤ t ∧ t ≤ T}) => tin < t ∧ t < tout → |s.z + t * v.z| < H ∧ sqv (vect2 s + t * vect2 v) < (D)^2 (detection s v) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cd3d.detection_complete
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (tin, tout) = detection(s, v) IN
--   abs(s`z + t * v`z) < H AND sqv(vect2(s) + t * vect2(v)) < sq(D) IMPLIES
--    tin <= t AND t <= tout AND tin < tout
theorem detection_complete : fun (tin : {t : {x : ℝ // x ≥ 0} // B ≤ t ∧ t ≤ T}) (tout : {t : {x : ℝ // x ≥ 0} // B ≤ t ∧ t ≤ T}) => |s.z + t * v.z| < H ∧ sqv (vect2 s + t * vect2 v) < (D)^2 → tin ≤ t ∧ t ≤ tout ∧ tin < tout (detection s v) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cd3d.conflict_detection
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (tin, tout) = detection(s, v) IN conflict_3D?(s, v) IFF tin < tout
theorem conflict_detection : fun (tin : {t : {x : ℝ // x ≥ 0} // B ≤ t ∧ t ≤ T}) (tout : {t : {x : ℝ // x ≥ 0} // B ≤ t ∧ t ≤ T}) => conflict_3D_p s v ↔ tin < tout (detection s v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.cd3d?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF v`z = 0 AND abs(s`z) < H THEN cd2d?[D, B, T](vect2(s), vect2(v))
-- ELSIF v`z /= 0 AND
--        max(-H - sign(v`z) * s`z, B * abs(v`z)) <
--         min(H - sign(v`z) * s`z, T * abs(v`z))
--   THEN cd2d?[D * 
axiom cd3d_p {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: cd3d.cd3d_rewrite
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d?(s, v) IFF
--  IF v`z = 0 AND abs(s`z) < H THEN cd2d?[D, B, T](vect2(s), vect2(v))
--  ELSIF v`z /= 0 AND
--         max(Theta_H(s`z, v`z, -1), B) < min(Theta_H(s`z, v`z, 1), T)
--    THEN cd2d?[D, max(Theta
theorem cd3d_rewrite : cd3d_p s v ↔ if v.z = 0 ∧ |s.z| < H then cd2d_p (vect2 s) (vect2 v)
ELSIF v.z ≠ 0 ∧
       max (Theta_H s.z v.z -1) B < min (Theta_H s.z v.z 1) T
  then cd2d_p
           (vect2 s, vect2 v)
else False := by
  -- === TCCs for cd3d_rewrite ===
  -- TCC: cd3d_rewrite_TCC1
  -- Statement: FORALL (s, v: Vect3):
  --   max(Theta_H(s`z, v`z, -1), B) < min(Theta_H(s`z, v`z, 1), T) AND
  --    v`z /= 0 AND NOT (v`z = 0 AND abs(s`z) < H)
  --    IMPLIES max(Theta_H[H](s`z, v`z, -1), B) >= 0
  have cd3d_rewrite_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cd3d_rewrite_TCC2
  -- Statement: FORALL (s, v: Vect3):
  --   max(Theta_H(s`z, v`z, -1), B) < min(Theta_H(s`z, v`z, 1), T) AND
  --    v`z /= 0 AND NOT (v`z = 0 AND abs(s`z) < H)
  --    IMPLIES
  --    min(Theta_H[H](s`z, v`z, 1), T) >= 0 AND
  --     mi...
  have cd3d_rewrite_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.cd3d_test
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nvo, vi: Vect3):
--        (D = 10000.8) AND (H = 283.464) AND (B = 0) AND (T = 300)
--    AND (s = (# x := 0, y := 110733.65, z := 0 #))
--    AND (nvo = (# x := 0, y := -236.48, z := 0 #))
--    AND (vi
theorem cd3d_test : ∀ (nvo : ℝ × ℝ × ℝ) (vi : ℝ × ℝ × ℝ), (D = 10000.8) ∧ (H = 283.464) ∧ (B = 0) ∧ (T = 300)
 ∧ (s = (# x := 0, y := 110733.65, z := 0 #))
 ∧ (nvo = (# x := 0, y := -236.48, z := 0 #))
 ∧ (vi = (# x := 0, y := 236.48, z := 0 #)) → cd3d_p s (nvo - vi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.cd3d_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d?(s, v) IMPLIES conflict_3D?(s, v)
theorem cd3d_correct : cd3d_p s v → conflict_3D_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.cd3d_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict_3D?(s, v) IMPLIES cd3d?(s, v)
theorem cd3d_complete : conflict_3D_p s v → cd3d_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.cd3d
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict_3D?(s, v) IFF cd3d?(s, v)
theorem cd3d : conflict_3D_p s v ↔ cd3d_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.cd3d_rewrite_vertical
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d?(s, v) IFF
--  (vect2(v) = zero AND
--    sqv(vect2(s)) < sq(D) AND cd_vertical?[H, B, T](s`z, v`z))
--   OR
--   (Delta(vect2(s), vect2(v)) > 0 AND
--     max(Theta_D(vect2(s), vect2(v), -1), B) <
--      min(The
theorem cd3d_rewrite_vertical : cd3d_p s v ↔ (vect2 v = zero ∧
  sqv (vect2 s) < (D)^2 ∧ cd_vertical_p s.z v.z)
 ∨
 (Delta (vect2 s) (vect2 v) > 0 ∧
   max (Theta_D (vect2 s) (vect2 v) -1) B <
    min (Theta_D (vect2 s) (vect2 v) 1) T
    ∧
    cd_vertical_p
        (s.z, v.z)) := by
  -- === TCCs for cd3d_rewrite_vertical ===
  -- TCC: cd3d_rewrite_vertical_TCC1
  -- Statement: FORALL (s, v: Vect3):
  --   Delta(vect2(s), vect2(v)) > 0 AND
  --    NOT (vect2(v) = zero AND
  --          sqv(vect2(s)) < sq(D) AND cd_vertical?[H, B, T](s`z, v`z))
  --    IMPLIES vect2(v) /= zero AND Delta[D](ve...
  have cd3d_rewrite_vertical_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cd3d_rewrite_vertical_TCC2
  -- Statement: FORALL (s, v: Vect3):
  --   max(Theta_D(vect2(s), vect2(v), -1), B) <
  --    min(Theta_D(vect2(s), vect2(v), 1), T)
  --    AND
  --    Delta(vect2(s), vect2(v)) > 0 AND
  --     NOT (vect2(v) = zero AND
  --           sqv(ve...
  have cd3d_rewrite_vertical_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cd3d_rewrite_vertical_TCC3
  -- Statement: FORALL (s, v: Vect3):
  --   max(Theta_D(vect2(s), vect2(v), -1), B) <
  --    min(Theta_D(vect2(s), vect2(v), 1), T)
  --    AND
  --    Delta(vect2(s), vect2(v)) > 0 AND
  --     NOT (vect2(v) = zero AND
  --           sqv(ve...
  have cd3d_rewrite_vertical_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.vertical_solution_not_conflict_3D
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vertical_solution?(s`z, v`z, T, -1) IMPLIES NOT conflict_3D?(s, v)
theorem vertical_solution_not_conflict_3D : vertical_solution_p s.z v.z T -1 → ¬ conflict_3D_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.conflict_3D_horizontal_conflict
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict_3D?(s, v) IMPLIES horizontal_conflict?(vect2(s), vect2(v))
theorem conflict_3D_horizontal_conflict : conflict_3D_p s v → horizontal_conflict_p (vect2 s) (vect2 v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.conflict_3D_vertical_conflict
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: conflict_3D?(s, v) IMPLIES vertical_conflict?(s`z, v`z)
theorem conflict_3D_vertical_conflict : conflict_3D_p s v → vertical_conflict_p s.z v.z := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d.circle_solution_2D_not_conflict_3D
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: circle_solution_2D?(vect2(s), vect2(v), T, -1) IMPLIES
--  NOT conflict_3D?(s, v)
theorem circle_solution_2D_not_conflict_3D : circle_solution_2D_p (vect2 s) (vect2 v) T -1 → ¬ conflict_3D_p s v := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cd3d