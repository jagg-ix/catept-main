-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cd3d_si
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cd3d_si

Auto-generated stubs from PVSLib theory `cd3d_si`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cd3d_si

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (FlightPlanRelevant : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (FlightTimesRelevant : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (flp : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} (flpl : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (location_at : pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (max : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (min : pvs_τ28 → pvs_τ29 → pvs_τ30)
variable {pvs_τ37 : Type*} (sq : pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (sqv : pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (vect2 : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} (velocity : pvs_τ42 → pvs_τ43 → pvs_τ44)

-- pvs2lean: cd3d_si.conflict_3D?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (tt: FlightTimesRelevant(to, flp)):
--   sqv(vect2((so + (tt - to) * vo) - location_at(flp)(tt))) < sq(D) AND
--    abs(so`z + (tt - to) * vo`z - location_at(flp)(tt)`z) < H
axiom conflict_3D_p {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18 → Prop

-- pvs2lean: cd3d_si.seg_lh_top
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min(flp(j + 1)`time, to + T) - flp(j)`time
axiom seg_lh_top {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} : pvs_τ34 → pvs_τ35 → pvs_τ36 → Prop

-- pvs2lean: cd3d_si.seg_lh_bottom
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max(flp(j)`time, to + B) - flp(j)`time
axiom seg_lh_bottom {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ31 → pvs_τ32 → pvs_τ33 → Prop

-- pvs2lean: cd3d_si.seg_lh_top_positive
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below[N - 1], to: real, flpl: FlightPlanRelevant(to)):
--   seg_lh_bottom(flpl, to)(j) < seg_lh_top(flpl, to)(j) IMPLIES
--    seg_lh_top(flpl, to)(j) > 0
theorem seg_lh_top_positive : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < N - 1}) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), seg_lh_bottom flpl to(j) < seg_lh_top flpl to(j) → seg_lh_top flpl to(j) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.lh_start_position
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: flp(j)`position +
--  (seg_lh_bottom(flp, to)(j) - flp(j)`time) * velocity(flp)(j)
noncomputable def lh_start_position : sorry := by
  sorry  -- pvs2lean: definition stub for flp(j)`position +  (seg_lh_bottom(flp, to)(j) - flp(j)`time) * velocity(flp)(j)

-- pvs2lean: cd3d_si.nontrivial_lookahead
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: below[N], to: real, flpl: FlightPlanRelevant(to)):
--   to + B <= flpl(k)`time AND flpl(k)`time <= to + T IMPLIES
--    (k < N - 1 AND seg_lh_top(flpl, to)(k) - seg_lh_bottom(flpl, to)(k) > 0)
--   
theorem nontrivial_lookahead : ∀ (k : {s : {i : ℝ // i ≥ 0} // s < N}) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), to + B ≤ flpl k.time ∧ flpl k.time ≤ to + T → (k < N - 1 ∧ seg_lh_top flpl to(k) - seg_lh_bottom flpl to(k) > 0) ∨
 (k > 0 ∧
   seg_lh_top flpl to(k - 1) - seg_lh_bottom flpl to(k - 1) > 0) := by
  -- === TCCs for nontrivial_lookahead ===
  -- TCC: nontrivial_lookahead_TCC1
  -- Statement: FORALL (k: below[N], to: real, flpl: FlightPlanRelevant(to)):
  --   to + B <= flpl(k)`time AND
  --    flpl(k)`time <= to + T AND
  --     NOT (k < N - 1 AND
  --           seg_lh_top(flpl, to)(k) - seg_lh_bottom(flp...
  have nontrivial_lookahead_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.conflict_3D_rew
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to)):
--   conflict_3D?(so, to, vo, flpl) IFF
--    EXISTS (j: below[N - 1]):
--      (seg_lh_bottom(flpl, to)(j) < seg_lh_top(flpl, to)(j) AND
--        
theorem conflict_3D_rew : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), conflict_3D_p so to vo flpl ↔ ∃ (j : Fin N - 1), (seg_lh_bottom flpl to(j) < seg_lh_top flpl to(j) ∧
    conflict_3D_p
        ((so + (flpl j.time - to) * vo) - flpl j.position,
         vo - velocity flpl(j))) := by
  -- === TCCs for conflict_3D_rew ===
  -- TCC: conflict_3D_rew_TCC1
  -- Statement: FORALL (to: real, flpl: FlightPlanRelevant(to), j: below[N - 1]):
  --   seg_lh_bottom(flpl, to)(j) < seg_lh_top(flpl, to)(j) IMPLIES
  --    seg_lh_top(flpl, to)(j) >= 0 AND
  --     seg_lh_top(flpl, to)(j) > 0 ...
  have conflict_3d_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.conflict_3D_flightplan_open_interval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to)):
--   conflict_3D?(so, to, vo, flpl) IFF
--    (EXISTS (tt: FlightTimesRelevant(to, flpl)):
--            (to + B < tt) AND (tt < to + T) AND (tt 
theorem conflict_3D_flightplan_open_interval : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), conflict_3D_p so to vo flpl ↔ (∃ (tt : FlightTimesRelevant to flpl), (to + B < tt) ∧ (tt < to + T) ∧ (tt < flpl (N - 1).time)
    ∧ (sqv (vect2 ((so + (tt - to) * vo) - location_at flpl(tt))) < (D)^2)
    ∧ (|so.z + (tt - to) * vo.z - location_at flpl(tt).z| < H)) := by
  -- === TCCs for conflict_3D_flightplan_open_interval ===
  -- TCC: conflict_3D_flightplan_open_interval_TCC1
  -- Statement: FORALL (to: real, flpl: FlightPlanRelevant(to),
  --         tt: FlightTimesRelevant(to, flpl)):
  --   to + B < tt AND tt < to + T IMPLIES N - 1 >= 0 AND N - 1 < N
  have conflict_3d_flightplan_open_interval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.conflict_3D_rew_absolute_time
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to)):
--   conflict_3D?(so, to, vo, flpl) IFF
--    EXISTS (j: below[N - 1]):
--      LET lb = seg_lh_bottom(flpl, to)(j) + flpl(j)`time - to,
--         
theorem conflict_3D_rew_absolute_time : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), conflict_3D_p so to vo flpl ↔ ∃ (j : Fin N - 1), (let lb := seg_lh_bottom flpl to(j) + flpl j.time - to; let ub := seg_lh_top flpl to(j) + flpl j.time - to; (lb < ub ∧
      conflict_3D_p
          (so -
            (flpl j.position - (flpl j.time - to) * velocity flpl(j)),
           vo - velocity flpl(j)))) := by
  -- === TCCs for conflict_3D_rew_absolute_time ===
  -- TCC: conflict_3D_rew_absolute_time_TCC1
  -- Statement: FORALL (to: real, flpl: FlightPlanRelevant(to), j: below[N - 1], lb: real,
  --         ub: real):
  --   lb = seg_lh_bottom(flpl, to)(j) + flpl(j)`time - to AND
  --    ub = seg_lh_top(flpl, to)(j) + flpl(j)`tim...
  have conflict_3d_rew_absolute_time_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: conflict_3D_rew_absolute_time_TCC2
  -- Statement: FORALL (to: real, flpl: FlightPlanRelevant(to), j: below[N - 1], lb: real,
  --         ub: real):
  --   lb = seg_lh_bottom(flpl, to)(j) + flpl(j)`time - to AND
  --    ub = seg_lh_top(flpl, to)(j) + flpl(j)`tim...
  have conflict_3d_rew_absolute_time_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.cd3d_ind?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (seg_lh_bottom(flpl, to)(j) < seg_lh_top(flpl, to)(j) AND
--   cd3d?[D, H, seg_lh_bottom(flpl, to)(j), seg_lh_top(flpl, to)(j)]
--       ((so + (flpl(j)`time - to) * vo) - flpl(j)`position,
--        vo - velo
axiom cd3d_ind_p {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10 → Prop

-- pvs2lean: cd3d_si.cd3d_ind?_weak_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to),
--         P: [below[N - 1] -> boolean]):
--   (FORALL (j: below[N - 1]):
--      (seg_lh_bottom(flpl, to)(j) < seg_lh_top(flpl, to)(j) AND
--       
theorem cd3d_ind_p_weak_induction : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}) (P : {s : {i : ℝ // i ≥ 0} // s < N - 1} → Bool), ∀ (j : Fin N - 1), (seg_lh_bottom flpl to(j) < seg_lh_top flpl to(j) ∧
    cd3d_p
        ((so + (flpl j.time - to) * vo) - flpl j.position,
         vo - velocity flpl(j)))
   ∨ (j > 0 ∧ P (j - 1))
   → P j → ∀ (j : Fin N - 1), cd3d_ind_p so vo to flpl(j) → P j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.cd3d_ind?_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to),
--         P: [below[N - 1] -> boolean]):
--   (FORALL (j: below[N - 1]):
--      (seg_lh_bottom(flpl, to)(j) < seg_lh_top(flpl, to)(j) AND
--       
theorem cd3d_ind_p_induction : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}) (P : {s : {i : ℝ // i ≥ 0} // s < N - 1} → Bool), ∀ (j : Fin N - 1), (seg_lh_bottom flpl to(j) < seg_lh_top flpl to(j) ∧
    cd3d_p
        ((so + (flpl j.time - to) * vo) - flpl j.position,
         vo - velocity flpl(j)))
   ∨ (j > 0 ∧ cd3d_ind_p so vo to flpl(j - 1) ∧ P (j - 1))
   → P j → ∀ (j : Fin N - 1), cd3d_ind_p so vo to flpl(j) → P j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.cd3d_si?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cd3d_ind?(so, vo, to, flpl)(N - 2)
axiom cd3d_si_p {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14 → Prop

-- pvs2lean: cd3d_si.cd3d_ind_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to)):
--   (FORALL (i: below[N - 1]):
--      cd3d_ind?(so, vo, to, flpl)(i) IMPLIES
--       EXISTS (j: below[N - 1]):
--         (j <= i AND
--           s
theorem cd3d_ind_correct : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < N - 1}), cd3d_ind_p so vo to flpl(i) → ∃ (j : Fin N - 1), (j ≤ i ∧
    seg_lh_bottom flpl to(j) < seg_lh_top flpl to(j) ∧
     conflict_3D_p
         ((so + (flpl j.time - to) * vo) - flpl j.position,
          vo - velocity flpl(j))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.cd3d_ind_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to)):
--   (FORALL (i: below[N - 1]):
--      (EXISTS (j: below[N - 1]):
--         (j <= i AND
--           seg_lh_bottom(flpl, to)(j) < seg_lh_top(flpl,
theorem cd3d_ind_complete : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < N - 1}), (∃ (j : Fin N - 1), (j ≤ i ∧
     seg_lh_bottom flpl to(j) < seg_lh_top flpl to(j) ∧
      conflict_3D_p
          ((so + (flpl j.time - to) * vo) - flpl j.position,
           vo - velocity flpl(j)))) → cd3d_ind_p so vo to flpl(i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.cd3d_si_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to)):
--   cd3d_si?(so, vo, to, flpl) IMPLIES conflict_3D?(so, to, vo, flpl)
theorem cd3d_si_correct : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), cd3d_si_p so vo to flpl → conflict_3D_p so to vo flpl := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cd3d_si.cd3d_si_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (so, vo: Vect3, to: real, flpl: FlightPlanRelevant(to)):
--   conflict_3D?(so, to, vo, flpl) IMPLIES cd3d_si?(so, vo, to, flpl)
theorem cd3d_si_complete : ∀ (so : ℝ × ℝ × ℝ) (vo : ℝ × ℝ × ℝ) (to : ℝ) (flpl : {flp : {fp : {s : {i : ℝ // i ≥ 0} // s < N} → (ℝ × ℝ × ℝ) × ℝ // ∀ (i : Fin N), i - 1 ≥ 0 → fp i.time > fp (i - 1).time} // flp 0.time < to + T ∧ to + B < flp (N - 1).time}), conflict_3D_p so to vo flpl → cd3d_si_p so vo to flpl := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cd3d_si