-- Auto-generated by pvs2lean/translator.py
-- PVS theory: delay_3D
-- PVSLib library: ACCoRD
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: delay_3D

Auto-generated stubs from PVSLib theory `delay_3D`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.delay_3D

noncomputable section

-- pvs2lean: delay_3D.delay_conflict_free_rel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nnt <= rt AND
--  conflict?[D, H](s, v) AND
--   (NOT conflict_3D?[D, H, 0, rt](s, v)) AND
--    (NOT conflict?[D, H](s + rt * v, nv))
--  IMPLIES
--  (NOT conflict_3D?[D, H, 0, nnt](s, v)) AND
--   (NOT conflict?[D, H
theorem delay_conflict_free_rel : nnt ≤ rt ∧
 conflict_p s v ∧
  (¬ conflict_3D_p s v) ∧
   (¬ conflict_p (s + rt * v) nv) → (¬ conflict_3D_p s v) ∧
 (¬ conflict_p (s + nnt * v) nv) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: delay_3D.delay_conflict_free
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nnt <= rt AND
--  conflict?[D, H](so - si, vo - vi) AND
--   (NOT conflict_3D?[D, H, 0, rt](so - si, vo - vi)) AND
--    (NOT conflict?[D, H]((so + rt * vo) - (si + rt * vi), nvo - vi))
--  IMPLIES
--  (NOT conflict
theorem delay_conflict_free : nnt ≤ rt ∧
 conflict_p (so - si) (vo - vi) ∧
  (¬ conflict_3D_p (so - si) (vo - vi)) ∧
   (¬ conflict_p ((so + rt * vo) - (si + rt * vi)) (nvo - vi)) → (¬ conflict_3D_p (so - si) (vo - vi)) ∧
 (¬ conflict_p ((so + nnt * vo) - (si + nnt * vi)) (nvo - vi)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.delay_3D