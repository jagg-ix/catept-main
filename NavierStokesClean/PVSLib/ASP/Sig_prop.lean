-- Auto-generated by pvs2lean/translator.py
-- PVS theory: Sig_prop
-- PVSLib library: ASP
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: Sig_prop

Auto-generated stubs from PVSLib theory `Sig_prop`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.Sig_prop

noncomputable section

variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (complement : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (intersection : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (member : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} (partial_order_p : pvs_τ17 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (singleton : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (subset_p : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (union : pvs_τ24 → pvs_τ25 → pvs_τ26)

-- pvs2lean: Sig_prop.s3?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x <= 2
axiom s3_p {pvs_τ18 : Type*} : pvs_τ18 → Prop

-- pvs2lean: Sig_prop.s2?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: s3?(x) AND (x = 0 OR x = 2)
noncomputable def s2_p : sorry := by
  sorry  -- pvs2lean: definition stub for s3?(x) AND (x = 0 OR x = 2)

-- pvs2lean: Sig_prop.RH
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (t: T): IF i(t) = 2 THEN 2 ELSE 0 ENDIF
noncomputable def RH : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA (t: T): IF i(t) = 2 THEN 2 ELSE 0 ENDIF

-- pvs2lean: Sig_prop.RT
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (t: T): IF i(t) = 0 THEN 0 ELSE 2 ENDIF
axiom RT {pvs_τ1 : Type*} : pvs_τ1 → Prop

-- pvs2lean: Sig_prop.product
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (t: T):
--   IF (i(t) = 0 AND j(t) = 0) THEN 0
--   ELSE IF (i(t) = 2 AND j(t) = 2) THEN 2 ELSE 1 ENDIF
--   ENDIF
noncomputable def product : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA (t: T):   IF (i(t) = 0 AND j(t) = 0) THEN 0   ELSE IF (i(t) = 2 AND j(t) 

-- pvs2lean: Sig_prop.R_ord
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF ((FORALL (t: T): (i(t) <= j(t))) AND RT(i) = RT(j)) THEN TRUE
-- ELSE FALSE
-- ENDIF
axiom R_ord {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ2 → pvs_τ3 → Prop

-- pvs2lean: Sig_prop.R_reflexive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: I): R_ord(i, i)
theorem R_reflexive : ∀ (i : T → {i : ℝ // i ≥ 0}), R_ord i i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.R_antisymmetric
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: I): (R_ord(i, j) AND R_ord(j, i)) IMPLIES i = j
theorem R_antisymmetric : ∀ (i : T → {i : ℝ // i ≥ 0}) (j : T → {i : ℝ // i ≥ 0}), (R_ord i j ∧ R_ord j i) → i = j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.R_transitive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j, k: I): (R_ord(i, j) AND R_ord(j, k)) IMPLIES R_ord(i, k)
theorem R_transitive : ∀ (i : T → {i : ℝ // i ≥ 0}) (j : T → {i : ℝ // i ≥ 0}) (k : T → {i : ℝ // i ≥ 0}), (R_ord i j ∧ R_ord j k) → R_ord i k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.R_partial_order
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: partial_order?[I](R_ord)
theorem R_partial_order : partial_order_p R_ord := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.RT_ord
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: I): R_ord(i, RT(i))
theorem RT_ord : ∀ (i : T → {i : ℝ // i ≥ 0}), R_ord i (RT i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.RT_classic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: IC): RT(i) = i
theorem RT_classic : ∀ (i : {i : T → {i : ℝ // i ≥ 0} // ∀ (t : T), (i t = 0 ∨ i t = 2)}), RT i = i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Sig_prop.RT_ord_mon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: I): R_ord(i, j) IMPLIES RT(i) = RT(j)
theorem RT_ord_mon : ∀ (i : T → {i : ℝ // i ≥ 0}) (j : T → {i : ℝ // i ≥ 0}), R_ord i j → RT i = RT j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.RT_ord_uniq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: I, j: IC): R_ord(i, j) IMPLIES RT(i) = j
theorem RT_ord_uniq : ∀ (i : T → {i : ℝ // i ≥ 0}) (j : {i : T → {i : ℝ // i ≥ 0} // ∀ (t : T), (i t = 0 ∨ i t = 2)}), R_ord i j → RT i = j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.R_ord2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (FORALL (t: T): (i(t) <= j(t)) AND (i(t) = 0 IMPLIES j(t) = 0))
--   THEN TRUE
-- ELSE FALSE
-- ENDIF
axiom R_ord2 {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ4 → pvs_τ5 → Prop

-- pvs2lean: Sig_prop.R_ord_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: I): (R_ord(i, j) IFF R_ord2(i, j))
theorem R_ord_same : ∀ (i : T → {i : ℝ // i ≥ 0}) (j : T → {i : ℝ // i ≥ 0}), R_ord i j ↔ R_ord2 i j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.comp
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {i: I | NOT member(i, S)}
axiom comp {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ9 → pvs_τ10

-- pvs2lean: Sig_prop.comp_eq_complement
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: set[I]): comp(X) = complement(X)
theorem comp_eq_complement : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool), comp X = complement X := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Sig_prop.classic
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {ic: IC | member(ic, S)}
axiom classic {pvs_τ8 : Type*} : pvs_τ8 → Prop

-- pvs2lean: Sig_prop.bottom
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {i: I | EXISTS (j: I): (member(j, S) AND R_ord2(i, j))}
axiom bottom {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ6 → pvs_τ7

-- pvs2lean: Sig_prop.top
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {i: I | EXISTS (j: I): (member(j, S) AND R_ord2(j, i))}
noncomputable def top : sorry := by
  sorry  -- pvs2lean: definition stub for {i: I | EXISTS (j: I): (member(j, S) AND R_ord2(j, i))}

-- pvs2lean: Sig_prop.classic_charact
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: set[I]):
--   classic(X) =
--    restrict[I, IC, bool]
--        (intersection(X, extend[I, IC, bool, FALSE](fullset[IC])))
theorem classic_charact : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool), classic X = restrict
    (intersection X (extend Set.univ)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Sig_prop.subset_comp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]): subset?(comp(X), comp(Y)) IFF subset?(Y, X)
theorem subset_comp : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p (comp X) (comp Y) ↔ subset_p Y X := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.subset_classic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: set[IC]): classic(extend[I, IC, bool, FALSE](X)) = X
theorem subset_classic : ∀ (X : {i : T → {i : ℝ // i ≥ 0} // ∀ (t : T), (i t = 0 ∨ i t = 2)} → Bool), classic (extend X) = X := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Sig_prop.union_classic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]): classic(union(X, Y)) = union(classic(X), classic(Y))
theorem union_classic : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), classic (union X Y) = union (classic X) (classic Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.intersection_classic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]):
--   classic(intersection(X, Y)) = intersection(classic(X), classic(Y))
theorem intersection_classic : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), classic (intersection X Y) = intersection (classic X) (classic Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.union_bottom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]): bottom(union(X, Y)) = union(bottom(X), bottom(Y))
theorem union_bottom : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), bottom (union X Y) = union (bottom X) (bottom Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.union_top
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]): top(union(X, Y)) = union(top(X), top(Y))
theorem union_top : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), top (union X Y) = union (top X) (top Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.union_bottom_cla
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[IC]):
--   bottom(extend[I, IC, bool, FALSE](union(X, Y))) =
--    union(bottom(extend[I, IC, bool, FALSE](X)),
--          bottom(extend[I, IC, bool, FALSE](Y)))
theorem union_bottom_cla : ∀ (X : {i : T → {i : ℝ // i ≥ 0} // ∀ (t : T), (i t = 0 ∨ i t = 2)} → Bool) (Y : {i : T → {i : ℝ // i ≥ 0} // ∀ (t : T), (i t = 0 ∨ i t = 2)} → Bool), bottom (extend (union X Y)) = union (bottom (extend X)) (bottom (extend Y)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.union_bottom_classic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]):
--   bottom(extend[I, IC, bool, FALSE](classic(union(X, Y)))) =
--    union(bottom(extend[I, IC, bool, FALSE](classic(X))),
--          bottom(extend[I, IC, bool, FALSE](classic((Y)))))
theorem union_bottom_classic : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), bottom (extend (classic (union X Y))) = union (bottom (extend (classic X))) (bottom (extend (classic (Y)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.union_top_classic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]):
--   top(extend[I, IC, bool, FALSE](classic(union(X, Y)))) =
--    union(top(extend[I, IC, bool, FALSE](classic(X))),
--          top(extend[I, IC, bool, FALSE](classic((Y)))))
theorem union_top_classic : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), top (extend (classic (union X Y))) = union (top (extend (classic X))) (top (extend (classic (Y)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.intersection_bottom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]):
--   subset?(bottom(intersection(X, Y)), intersection(bottom(X), bottom(Y)))
theorem intersection_bottom : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p (bottom (intersection X Y)) (intersection (bottom X) (bottom Y)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.intersection_top
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]):
--   subset?(top(intersection(X, Y)), intersection(top(X), top(Y)))
theorem intersection_top : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p (top (intersection X Y)) (intersection (top X) (top Y)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.classic_include
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: set[I]): subset?(classic(X), restrict[I, IC, bool](X))
theorem classic_include : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p (classic X) (restrict X) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.classic_monot
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]): subset?(X, Y) IMPLIES subset?(classic(X), classic(Y))
theorem classic_monot : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p X Y → subset_p (classic X) (classic Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.bottom_include
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: set[I]): subset?(X, bottom(X))
theorem bottom_include : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p X (bottom X) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.bottom_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I], i: I):
--   subset?(X, Y) IMPLIES subset?(bottom(X), bottom(Y))
theorem bottom_subset : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool) (i : T → {i : ℝ // i ≥ 0}), subset_p X Y → subset_p (bottom X) (bottom Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.bottom_monot
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]):
--   subset?(X, Y) IMPLIES
--    subset?(bottom(extend[I, IC, bool, FALSE](classic(X))),
--            bottom(extend[I, IC, bool, FALSE](classic(Y))))
theorem bottom_monot : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p X Y → subset_p (bottom (extend (classic X))) (bottom (extend (classic Y))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.classic_bottom_classic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: set[I]):
--   classic(bottom(extend[I, IC, bool, FALSE](classic(X)))) = classic(X)
theorem classic_bottom_classic : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool), classic (bottom (extend (classic X))) = classic X := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Sig_prop.top_include
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: set[I]): subset?(X, top(X))
theorem top_include : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p X (top X) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.top_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I], i: I): subset?(X, Y) IMPLIES subset?(top(X), top(Y))
theorem top_subset : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool) (i : T → {i : ℝ // i ≥ 0}), subset_p X Y → subset_p (top X) (top Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.top_monot
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]):
--   subset?(X, Y) IMPLIES
--    subset?(top(extend[I, IC, bool, FALSE](classic(X))),
--            top(extend[I, IC, bool, FALSE](classic(Y))))
theorem top_monot : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p X Y → subset_p (top (extend (classic X))) (top (extend (classic Y))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.simplificat
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]): union(X, intersection(Y, X)) = X
theorem simplificat : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), union X (intersection Y X) = X := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.union_monot
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y, Z: set[I]):
--   subset?(X, Y) IMPLIES subset?(union(Z, X), union(Z, Y))
theorem union_monot : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool) (Z : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p X Y → subset_p (union Z X) (union Z Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.union_sub_tot
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]):
--   subset?(X, Y) IFF FORALL (i: I): member(i, union(comp(X), Y))
theorem union_sub_tot : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p X Y ↔ ∀ (i : I), member i (union (comp X) Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.charact_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X, Y: set[I]): subset?(X, Y) IFF intersection(X, comp(Y)) = emptyset
theorem charact_sub : ∀ (X : (T → {i : ℝ // i ≥ 0}) → Bool) (Y : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p X Y ↔ intersection X (comp Y) = emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.intersection_monot2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B, C, D: set[I]):
--   subset?(A, B) AND subset?(C, D) IMPLIES
--    subset?(intersection(A, C), intersection(B, D))
theorem intersection_monot2 : ∀ (A : (T → {i : ℝ // i ≥ 0}) → Bool) (B : (T → {i : ℝ // i ≥ 0}) → Bool) (C : (T → {i : ℝ // i ≥ 0}) → Bool) (D : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p A B ∧ subset_p C D → subset_p (intersection A C) (intersection B D) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.classic_top_uni
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: I): classic(top(singleton(i))) = singleton(RT(i))
theorem classic_top_uni : ∀ (i : T → {i : ℝ // i ≥ 0}), classic (top (singleton i)) = singleton (RT i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Sig_prop.classic_total
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I], i: I):
--   (member(i, bottom(extend[I, IC, bool, FALSE](classic(S))))) IFF
--    (member(RT(i), S))
theorem classic_total : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool) (i : T → {i : ℝ // i ≥ 0}), (member i (bottom (extend (classic S)))) ↔ (member (RT i) S) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Sig_prop.classic_top
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[IC], i: I):
--   (member(i, bottom(extend[I, IC, bool, FALSE](S)))) IFF (member(RT(i), S))
theorem classic_top : ∀ (S : {i : T → {i : ℝ // i ≥ 0} // ∀ (t : T), (i t = 0 ∨ i t = 2)} → Bool) (i : T → {i : ℝ // i ≥ 0}), (member i (bottom (extend S))) ↔ (member (RT i) S) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Sig_prop.total_closed?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: I): (member(i, S)) IMPLIES (member(RT(i), S))
axiom total_closed_p {pvs_τ23 : Type*} : pvs_τ23 → Prop

-- pvs2lean: Sig_prop.total_closed_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I]):
--   total_closed?(S) IMPLIES
--    subset?(S, bottom(extend[I, IC, bool, FALSE](classic(S))))
theorem total_closed_1 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool), total_closed_p S → subset_p S (bottom (extend (classic S))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.total_closed_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I]):
--   subset?(S, bottom(extend[I, IC, bool, FALSE](classic(S)))) IMPLIES
--    classic(top(S)) = classic(S)
theorem total_closed_2 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool), subset_p S (bottom (extend (classic S))) → classic (top S) = classic S := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.total_closed_3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I]): classic(top(S)) = classic(S) IMPLIES total_closed?(S)
theorem total_closed_3 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool), classic (top S) = classic S → total_closed_p S := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.total_closed_caract1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I]):
--   total_closed?(S) IFF
--    subset?(S, bottom(extend[I, IC, bool, FALSE](classic(S))))
theorem total_closed_caract1 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool), total_closed_p S ↔ subset_p S (bottom (extend (classic S))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.total_closed_caract2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I]): total_closed?(S) IFF classic(top(S)) = classic(S)
theorem total_closed_caract2 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool), total_closed_p S ↔ classic (top S) = classic S := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.total_closed_caract4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I], i: I):
--   subset?(classic(top(S)), restrict[I, IC, bool](S)) IMPLIES
--    total_closed?(S)
theorem total_closed_caract4 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool) (i : T → {i : ℝ // i ≥ 0}), subset_p (classic (top S)) (restrict S) → total_closed_p S := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.total_closed_caract5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I], i: I):
--   subset?(classic(top(S)), classic(S)) IFF total_closed?(S)
theorem total_closed_caract5 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool) (i : T → {i : ℝ // i ≥ 0}), subset_p (classic (top S)) (classic S) ↔ total_closed_p S := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.bottom_classic_com_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I], i: I):
--   subset?(bottom(extend[I, IC, bool, FALSE](classic(comp(S)))),
--           comp(bottom(extend[I, IC, bool, FALSE](classic(S)))))
theorem bottom_classic_com_1 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool) (i : T → {i : ℝ // i ≥ 0}), subset_p (bottom (extend (classic (comp S)))) (comp (bottom (extend (classic S)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Sig_prop.bottom_classic_com_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: set[I], i: I):
--   total_closed?(S) IMPLIES
--    subset?(bottom(extend[I, IC, bool, FALSE](classic(comp(S)))), comp(S))
theorem bottom_classic_com_2 : ∀ (S : (T → {i : ℝ // i ≥ 0}) → Bool) (i : T → {i : ℝ // i ≥ 0}), total_closed_p S → subset_p (bottom (extend (classic (comp S)))) (comp S) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.Sig_prop