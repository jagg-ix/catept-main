-- Auto-generated by pvs2lean/translator.py
-- PVS theory: integral_step
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: integral_step

Auto-generated stubs from PVSLib theory `integral_step`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.integral_step

noncomputable section

-- pvs2lean: integral_step.T_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_step.connected_domain
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[T]
theorem connected_domain : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.not_one_element
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[T]
theorem not_one_element : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.sigma_0_m1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, -1, fn) = 0
theorem sigma_0_m1 : Finset.sum (Finset.Icc 0 -1) (fn) = 0 := by
  -- === TCCs for sigma_0_m1 ===
  -- TCC: sigma_0_m1_TCC1
  -- Statement: 0 >= 0 OR EXISTS (j: nat): j <= 0
  have sigma_0_m1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_0_m1_TCC2
  -- Statement: -1 >= 0 OR EXISTS (j: nat): -1 <= j
  have sigma_0_m1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_0_m1_TCC3
  -- Statement: (FORALL (x, y: nat), (z: integer): x <= z AND z <= y IMPLIES z >= 0)
  have sigma_0_m1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: integral_step.pick
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: choose({t: T | seq(P)(j) < t AND t < seq(P)(j + 1)})
noncomputable def pick : sorry := by
  sorry  -- pvs2lean: definition stub for choose({t: T | seq(P)(j) < t AND t < seq(P)(j + 1)})

-- pvs2lean: integral_step.val_in
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: f(pick(a, b, P, j))
noncomputable def val_in : sorry := by
  sorry  -- pvs2lean: definition stub for f(pick(a, b, P, j))

-- pvs2lean: integral_step.stp_sect
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF jj >= length(P) - 1 THEN 0
-- ELSIF x = seq(P)(jj) THEN f(x)
-- ELSIF seq(P)(jj) < x AND x < seq(P)(jj + 1) THEN val_in(a, b, P, jj, f)
-- ELSE 0
-- ENDIF
noncomputable def stp_sect : sorry := by
  sorry  -- pvs2lean: definition stub for IF jj >= length(P) - 1 THEN 0 ELSIF x = seq(P)(jj) THEN f(x) ELSIF seq(P)(jj) < 

-- pvs2lean: integral_step.stp_sect_lem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition(a, b)):
--    FORALL (ii, jj: upto(length(P) - 1)):
--      x > seq(P)(jj) AND ii < jj IMPLIES stp_sect(a, b, P, ii, f)(x) = 0
theorem stp_sect_lem : a < b → ∀ (P : partition a b), ∀ (ii : _) (jj : {k : ℕ // k ≤ length P}, x > seq P(jj) ∧ ii < jj → stp_sect a b P ii f(x) = 0 := by
  -- === TCCs for stp_sect_lem ===
  -- TCC: stp_sect_lem_TCC1
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b), jj: upto(length(P) - 1)): jj < P`length
  have stp_sect_lem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.sigma_stp_sect
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition(a, b)):
--    FORALL (j: nat):
--      j <= length(P) - 2 AND x > seq(P)(1 + j) IMPLIES
--       sigma(0, j,
--             LAMBDA (ii: nat):
--               IF ii >= 1 + j THEN 
theorem sigma_stp_sect : a < b → ∀ (P : partition a b), ∀ (j : ℕ), j ≤ length P - 2 ∧ x > seq P(1 + j) →
     Finset.sum (Finset.Icc 0 j) (fun (ii : ℕ)=> if ii ≥ 1 + j then 0 else stp_sect a b P ii f(x) )
      = 0 := by
  -- === TCCs for sigma_stp_sect ===
  -- TCC: sigma_stp_sect_TCC1
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b), j: nat):
  --      j <= length(P) - 2 IMPLIES 1 + j < P`length
  have sigma_stp_sect_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_stp_sect_TCC2
  -- Statement: FORALL (a, b, x: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b), j: nat):
  --      j <= length(P) - 2 AND x > seq(P)(1 + j) IMPLIES
  --       j >= 0 OR EXISTS (j_1: nat): j <= j_1
  have sigma_stp_sect_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.integral_stp_sect
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: T, b: {x: T | a < x}, P: partition[T](a, b),
--         j: below(length(P) - 1)):
--   integral?(a, b, stp_sect(a, b, P, j, f),
--             val_in(a, b, P, j, f) * (seq(P)(j + 1) - seq(P)(j)))
theorem integral_stp_sect : ∀ (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) (j : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1}), integral_p ((a, b, stp_sect a b P j f,
 val_in a b P j f * (seq P(j + 1) - seq P(j)))) := by
  -- === TCCs for integral_stp_sect ===
  -- TCC: integral_stp_sect_TCC1
  -- Statement: FORALL (a: T, b: {x: T | a < x}, P: partition[T](a, b),
  --         j: below(length(P) - 1)):
  --   j + 1 < P`length
  have integral_stp_sect_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_stp_sect_TCC2
  -- Statement: FORALL (a: T, b: {x: T | a < x}, P: partition[T](a, b),
  --         j: below(length(P) - 1)):
  --   j < P`length
  have integral_stp_sect_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.sumof
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma[nat](0, n, (LAMBDA (ii: nat): IF ii > n THEN 0 ELSE ff(ii)(x) ENDIF))
noncomputable def sumof : sorry := by
  sorry  -- pvs2lean: definition stub for sigma[nat](0, n, (LAMBDA (ii: nat): IF ii > n THEN 0 ELSE ff(ii)(x) ENDIF))

-- pvs2lean: integral_step.sigma_sumof
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: T, b: {x: T | a < x}, P: partition[T](a, b)):
--   step_function_on?(a, b, f, P) IMPLIES
--    (FORALL (n: nat):
--       n <= length(P) - 2 IMPLIES
--        (FORALL (x: closed_interval[T](a, b)):
--    
theorem sigma_sumof : ∀ (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}), step_function_on_p a b f P → (∀ (n : ℕ), n ≤ length P - 2 →
    (∀ (x : closed_interval (a, b)), x ≤ seq P(n + 1) →
        sumof n ((fun (jj : {k : ℕ // k ≤ n})=> stp_sect a b P jj f)) (x) +
         if x = seq P(n + 1) then f x else 0 
         = f x)) := by
  -- === TCCs for sigma_sumof ===
  -- TCC: sigma_sumof_TCC1
  -- Statement: FORALL (f: [T -> real], a: T, b: {x: T | a < x}, P: partition[T](a, b)):
  --   step_function_on?(a, b, f, P) IMPLIES
  --    FORALL (n: nat):
  --      n <= length(P) - 2 IMPLIES
  --       FORALL (x: closed_interval...
  have sigma_sumof_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.integrable_sumof
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, a: T, b: {x: T | a < x}, P: partition[T](a, b)):
--   n <= length(P) - 2 IMPLIES
--    integrable?(a, b,
--                sumof(n, (LAMBDA (jj: upto(n)): stp_sect(a, b, P, jj, f))))
theorem integrable_sumof : ∀ (n : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}) (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}), n ≤ length P - 2 → integrable_p a b (sumof n ((fun (jj : {k : ℕ // k ≤ n})=> stp_sect a b P jj f))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.integral_sumof
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, a: T, b: {x: T | a < x}, P: partition[T](a, b)):
--   n <= length(P) - 2 IMPLIES
--    integral(a, b,
--             sumof(n, (LAMBDA (jj: upto(n)): stp_sect(a, b, P, jj, f))))
--     =
--     sigma(
theorem integral_sumof : ∀ (n : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}) (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}), n ≤ length P - 2 → integral a b (sumof n ((fun (jj : {k : ℕ // k ≤ n})=> stp_sect a b P jj f)))
 =
 Finset.sum (Finset.Icc 0 n) ((fun (ii : Finset.range length (P - 1))=> val_in a b P ii f *
           (finseq_appl (P) (ii + 1) -
             finseq_appl (P) (ii)))) := by
  -- === TCCs for integral_sumof ===
  -- TCC: integral_sumof_TCC1
  -- Statement: FORALL (f: [T -> real], n: nat, a: T, b: {x: T | a < x},
  --         P: partition[T](a, b)):
  --   n <= length(P) - 2 IMPLIES
  --    integrable?[T]
  --        (a, b, sumof(n, LAMBDA (jj: upto(n)): stp_sect(a, b, P...
  have integral_sumof_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_sumof_TCC2
  -- Statement: FORALL (n: nat, a: T, b: {x: T | a < x}, P: partition[T](a, b)):
  --   n <= length(P) - 2 IMPLIES
  --    0 >= 0 AND 0 < length(P) - 1 OR EXISTS (j: below(length(P) - 1)): j <= 0
  have integral_sumof_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_sumof_TCC3
  -- Statement: FORALL (n: nat, a: T, b: {x: T | a < x}, P: partition[T](a, b)):
  --   n <= length(P) - 2 IMPLIES
  --    n >= 0 AND n < length(P) - 1 OR EXISTS (j: below(length(P) - 1)): n <= j
  have integral_sumof_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_sumof_TCC4
  -- Statement: FORALL (n: nat, a: T, b: {x: T | a < x}, P: partition[T](a, b)):
  --   n <= length(P) - 2 IMPLIES
  --    FORALL (x, y: below(length(P) - 1)), (z: integer):
  --      x <= z AND z <= y IMPLIES z >= 0 AND z < len...
  have integral_sumof_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.step_fun_is_sumof_n
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, a: T, b: {x: T | a < x}, P: partition[T](a, b)):
--   n <= length(P) - 2 AND step_function_on?(a, b, f, P) IMPLIES
--    LET endx = seq(P)(n + 1) IN
--      (FORALL (x: closed_interval[T](a, en
theorem step_fun_is_sumof_n : ∀ (n : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}) (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}), n ≤ length P - 2 ∧ step_function_on_p a b f P → (let endx := seq P(n + 1); (∀ (x : closed_interval (a, endx)), sumof n ((fun (jj : {k : ℕ // k ≤ n})=> stp_sect a b P jj f))
       WITH[endx := f endx]
         (x)
      = f x)) := by
  -- === TCCs for step_fun_is_sumof_n ===
  -- TCC: step_fun_is_sumof_n_TCC1
  -- Statement: FORALL (f: [T -> real], n: nat, a: T, b: {x: T | a < x},
  --         P: partition[T](a, b)):
  --   n <= length(P) - 2 AND step_function_on?(a, b, f, P) IMPLIES
  --    n + 1 < P`length
  have step_fun_is_sumof_n_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.step_fun_is_sumof
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: T, b: {x: T | a < x}, P: partition[T](a, b)):
--   step_function_on?(a, b, f, P) IMPLIES
--    (FORALL (x: closed_interval[T](a, b)):
--       LET N = length(P) - 2 IN
--         sumof(N, (LAMBDA (jj: 
theorem step_fun_is_sumof : ∀ (a : {x : {x : {x : ℝ // _} // _} // _}) (b : {x : {x : {x : {x : ℝ // _} // _} // _} // a < x}) (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}), step_function_on_p a b f P → (∀ (x : closed_interval (a, b)), (let N := length P - 2; sumof N ((fun (jj : {k : ℕ // k ≤ N})=> stp_sect a b P jj f))
       WITH[b := f b]
         (x)
      = f x)) := by
  -- === TCCs for step_fun_is_sumof ===
  -- TCC: step_fun_is_sumof_TCC1
  -- Statement: FORALL (f: [T -> real], a: T, b: {x: T | a < x}, P: partition[T](a, b)):
  --   step_function_on?(a, b, f, P) IMPLIES
  --    FORALL (x: closed_interval[T](a, b), N: int):
  --      N = length(P) - 2 IMPLIES N >= 0
  have step_fun_is_sumof_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.step_function_integrable?
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND step_function?(a, b, f) IMPLIES integrable?(a, b, f)
theorem step_function_integrable_p : a < b ∧ step_function_p a b f → integrable_p a b f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.step_function_on_integral
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition[T](a, b)):
--    step_function_on?(a, b, f, P) IMPLIES
--     integral(a, b, f) =
--      LET N = length(P) IN
--        sigma(0, N - 2,
--              (LAMBDA (ii: below(N - 1))
theorem step_function_on_integral : a < b → ∀ (P : partition (a, b)), step_function_on_p a b f P →
   integral a b f =
    (let N := length P; Finset.sum (Finset.Icc 0 N - 2) ((fun (ii : Finset.range N - 1)=> val_in a b P ii f *
                (finseq_appl (P) (ii + 1) -
                  finseq_appl (P) (ii))))) := by
  -- === TCCs for step_function_on_integral ===
  -- TCC: step_function_on_integral_TCC1
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      step_function_on?(a, b, f, P) IMPLIES integrable?[T](a, b, f)
  have step_function_on_integral_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: step_function_on_integral_TCC2
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      step_function_on?(a, b, f, P) IMPLIES
  --       FORALL (N: nat):
  --         N = length(P) IMPLIES
  --          0 >= 0 ...
  have step_function_on_integral_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: step_function_on_integral_TCC3
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      step_function_on?(a, b, f, P) IMPLIES
  --       FORALL (N: nat):
  --         N = length(P) IMPLIES
  --          N - 2 >...
  have step_function_on_integral_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: step_function_on_integral_TCC4
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      step_function_on?(a, b, f, P) IMPLIES
  --       FORALL (N: nat):
  --         N = length(P) IMPLIES FORALL (ii: belo...
  have step_function_on_integral_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: step_function_on_integral_TCC5
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      step_function_on?(a, b, f, P) IMPLIES
  --       FORALL (N: nat):
  --         N = length(P) IMPLIES FORALL (ii: belo...
  have step_function_on_integral_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: step_function_on_integral_TCC6
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      step_function_on?(a, b, f, P) IMPLIES
  --       FORALL (N: nat):
  --         N = length(P) IMPLIES FORALL (ii: belo...
  have step_function_on_integral_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: step_function_on_integral_TCC7
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      step_function_on?(a, b, f, P) IMPLIES
  --       FORALL (N: nat):
  --         N = length(P) IMPLIES
  --          FORALL ...
  have step_function_on_integral_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_step.step_to_integrable
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND
--  (FORALL (eps: posreal):
--     (EXISTS (f1, f2: [T -> real]):
--             step_function?(a, b, f1) AND step_function?(a, b, f2)
--         AND (FORALL (xx: closed_interval(a, b)):
--                
theorem step_to_integrable : a < b ∧
 (∀ (eps : {x : ℝ // 0 < x}), (∃ (f1 : _) (f2 : T → ℝ), step_function_p a b f1 ∧ step_function_p a b f2
        ∧ (∀ (xx : closed_interval a b), f1 xx ≤ f xx ∧ f xx ≤ f2 xx)
        ∧ integrable_p a b (f2 - f1) ∧ (integral a b (f2 - f1) < eps))) → integrable_p a b f := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.integral_step