-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cauchy_numerical
-- PVSLib library: exact_real_arith
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cauchy_numerical

Auto-generated stubs from PVSLib theory `cauchy_numerical`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cauchy_numerical

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Eval : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (Eval_real : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (Some : pvs_τ8 → pvs_τ9)
variable {pvs_τ14 : Type*} (some_p : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (val : pvs_τ15 → pvs_τ16 → Prop)

-- pvs2lean: cauchy_numerical.numerical
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET cx = Eval(zero_prec)(expr, vars) IN
--   IF some?(cx)
--     THEN (# ans := Some(val(cx)(precision) / 2 ^ precision),
--             p := precision #)
--   ELSE (# ans := None, p := precision #)
--   ENDIF
axiom numerical {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: cauchy_numerical.numerical_soundness
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET res = numerical(precision, zero_prec)(expr, vars) IN
--   some?(res`ans) IMPLIES
--    val(res`ans) * 2 ^ res`p - 1 < val(Eval_real(expr, vars)) * 2 ^ res`p
--     AND
--     val(Eval_real(expr, vars)) * 2 ^ 
theorem numerical_soundness : fun (res : Maybe ℝ × {i : ℝ // i ≥ 0}) => some_p res.ans → val res.ans * 2 ^ res.p - 1 < val (Eval_real expr vars) * 2 ^ res.p ∧
 val (Eval_real expr vars) * 2 ^ res.p < val res.ans * 2 ^ res.p + 1 (numerical precision zero_prec expr vars) := by
  -- === TCCs for numerical_soundness ===
  -- TCC: numerical_soundness_TCC1
  -- Statement: FORALL (expr: CauchyExpr, precision: nat, vars: list[rat], zero_prec: nat,
  --         res: Output):
  --   res = numerical(precision, zero_prec)(expr, vars) AND some?(res`ans)
  --    IMPLIES some?[real](Eval_r...
  have numerical_soundness_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cauchy_numerical