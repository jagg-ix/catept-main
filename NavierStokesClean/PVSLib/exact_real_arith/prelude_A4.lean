-- Auto-generated by pvs2lean/translator.py
-- PVS theory: prelude_A4
-- PVSLib library: exact_real_arith
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: prelude_A4

Auto-generated stubs from PVSLib theory `prelude_A4`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.prelude_A4

noncomputable section

variable {pvs_τ1 : Type*} (even_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} (odd_p : pvs_τ2 → Prop)

-- pvs2lean: prelude_A4.expt_neg_even
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: even?(n) => nx ^ n = (-nx) ^ n
theorem expt_neg_even : even_p n → nx ^ n = (-nx) ^ n := by
  -- === TCCs for expt_neg_even ===
  -- TCC: expt_neg_even_TCC1
  -- Statement: FORALL (n: nat, nx: negreal): even?(n) IMPLIES nx /= 0 OR n >= 0
  have expt_neg_even_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expt_neg_even_TCC2
  -- Statement: FORALL (n: nat, nx: negreal): even?(n) IMPLIES (-nx) /= 0 OR n >= 0
  have expt_neg_even_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: prelude_A4.expt_neg_odd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: odd?(n) => nx ^ n = -((-nx) ^ n)
theorem expt_neg_odd : odd_p n → nx ^ n = -((-nx) ^ n) := by
  -- === TCCs for expt_neg_odd ===
  -- TCC: expt_neg_odd_TCC1
  -- Statement: FORALL (n: nat, nx: negreal): odd?(n) IMPLIES nx /= 0 OR n >= 0
  have expt_neg_odd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expt_neg_odd_TCC2
  -- Statement: FORALL (n: nat, nx: negreal): odd?(n) IMPLIES (-nx) /= 0 OR n >= 0
  have expt_neg_odd_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: prelude_A4.expt_0pn
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 ^ pn = 0
theorem expt_0pn : 0 ^ pn = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.expt_inverse_inv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (1 / n0x) ^ i = 1 / (n0x ^ i)
theorem expt_inverse_inv : (1 / n0x) ^ i = 1 / (n0x ^ i) := by
  -- === TCCs for expt_inverse_inv ===
  -- TCC: expt_inverse_inv_TCC1
  -- Statement: FORALL (i: int, n0x: nzreal): n0x /= 0 OR i >= 0
  have expt_inverse_inv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.expt_product_n0i
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x * y) ^ pn = x ^ pn * y ^ pn
theorem expt_product_n0i : (x * y) ^ pn = x ^ pn * y ^ pn := by
  -- === TCCs for expt_product_n0i ===
  -- TCC: expt_product_n0i_TCC1
  -- Statement: FORALL (pn: posnat, x, y: real): (x * y) /= 0 OR pn >= 0
  have expt_product_n0i_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expt_product_n0i_TCC2
  -- Statement: FORALL (pn: posnat, x: real): x /= 0 OR pn >= 0
  have expt_product_n0i_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.expt_division
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x / n0y) ^ pn = x ^ pn / n0y ^ pn
theorem expt_division : (x / n0y) ^ pn = x ^ pn / n0y ^ pn := by
  -- === TCCs for expt_division ===
  -- TCC: expt_division_TCC1
  -- Statement: FORALL (n0y: nzreal, pn: posnat, x: real): (x / n0y) /= 0 OR pn >= 0
  have expt_division_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expt_division_TCC2
  -- Statement: FORALL (n0y: nzreal, pn: posnat): n0y /= 0 OR pn >= 0
  have expt_division_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expt_division_TCC1
  -- Statement: FORALL (i: int, n0x, n0y: nzreal): (n0x / n0y) /= 0 OR i >= 0
  have expt_division_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < y => 0 <= n / (n + 1 + y) AND n / (n + 1 + y) < 1
theorem A4_0 : -1 < y → 0 ≤ n / (n + 1 + y) ∧ n / (n + 1 + y) < 1 := by
  -- === TCCs for A4_0 ===
  -- TCC: A4_0_TCC1
  -- Statement: FORALL (n: nat, y: real): -1 < y IMPLIES (n + 1 + y) /= 0
  have a4_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < y => 0 < pn / (pn + 1 + y) AND pn / (pn + 1 + y) < 1
theorem A4_1 : -1 < y → 0 < pn / (pn + 1 + y) ∧ pn / (pn + 1 + y) < 1 := by
  -- === TCCs for A4_1 ===
  -- TCC: A4_1_TCC1
  -- Statement: FORALL (pn: posnat, y: real): -1 < y IMPLIES (pn + 1 + y) /= 0
  have a4_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < y AND z = n * y / (n + 1 + y) => -1 < z
theorem A4_2 : -1 < y ∧ z = n * y / (n + 1 + y) → -1 < z := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_3_n
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < y AND y < 0 AND z = pn * y / (pn + 1 + y) => y < z
theorem A4_3_n : -1 < y ∧ y < 0 ∧ z = pn * y / (pn + 1 + y) → y < z := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_3_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: y = 0 AND z = pn * y / (pn + 1 + y) => z = 0
theorem A4_3_0 : y = 0 ∧ z = pn * y / (pn + 1 + y) → z = 0 := by
  -- === TCCs for A4_3_0 ===
  -- TCC: A4_3_0_TCC1
  -- Statement: FORALL (pn: posnat, y: real): y = 0 IMPLIES (pn + 1 + y) /= 0
  have a4_3_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_3_p
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 < y AND z = pn * y / (pn + 1 + y) => z < y
theorem A4_3_p : 0 < y ∧ z = pn * y / (pn + 1 + y) → z < y := by
  -- === TCCs for A4_3_p ===
  -- TCC: A4_3_p_TCC1
  -- Statement: FORALL (pn: posnat, y: real): 0 < y IMPLIES (pn + 1 + y) /= 0
  have a4_3_p_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < y AND z = pn * y / (pn + 1 + y) =>
--  (pn * x * (1 + z) < z <=> (pn + 1) * x * (1 + y) < y)
theorem A4_4 : -1 < y ∧ z = pn * y / (pn + 1 + y) → (pn * x * (1 + z) < z ≤> (pn + 1) * x * (1 + y) < y) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_5pp
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 < x =>
--  (FORALL n:
--     (FORALL y: 0 < y => ((1 + y) * n * x < y => (1 + x) ^ n < 1 + y)))
theorem A4_5pp : 0 < x → (∀ n, 
   (∀ y,  0 < y → ((1 + y) * n * x < y → (1 + x) ^ n < 1 + y))) := by
  -- === TCCs for A4_5pp ===
  -- TCC: A4_5pp_TCC1
  -- Statement: FORALL (x: real):
  --   0 < x IMPLIES
  --    FORALL (n: nat, y: real):
  --      0 < y AND (1 + y) * n * x < y IMPLIES (1 + x) /= 0 OR n >= 0
  have a4_5pp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_5nn
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < x AND x < 0 =>
--  (FORALL pn:
--     (FORALL y:
--        -1 < y AND y < 0 => ((1 + y) * pn * x < y => (1 + x) ^ pn < 1 + y)))
theorem A4_5nn : -1 < x ∧ x < 0 → (∀ pn, 
   (∀ y, 
      -1 < y ∧ y < 0 → ((1 + y) * pn * x < y → (1 + x) ^ pn < 1 + y))) := by
  -- === TCCs for A4_5nn ===
  -- TCC: A4_5nn_TCC1
  -- Statement: FORALL (x: real):
  --   x < 0 AND -1 < x IMPLIES
  --    FORALL (pn: posnat, y: real):
  --      -1 < y AND y < 0 AND (1 + y) * pn * x < y IMPLIES
  --       (1 + x) /= 0 OR pn >= 0
  have a4_5nn_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_5nn_general
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < x AND x < 0 AND y < 0 AND (1 + y) * n * x < y => (1 + x) ^ n < 1 + y
theorem A4_5nn_general : -1 < x ∧ x < 0 ∧ y < 0 ∧ (1 + y) * n * x < y → (1 + x) ^ n < 1 + y := by
  -- === TCCs for A4_5nn_general ===
  -- TCC: A4_5nn_general_TCC1
  -- Statement: FORALL (n: nat, x, y: real):
  --   (1 + y) * n * x < y AND y < 0 AND x < 0 AND -1 < x IMPLIES
  --    (1 + x) /= 0 OR n >= 0
  have a4_5nn_general_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < x AND -1 < y AND (1 + y) * pn * x < y => (1 + x) ^ pn < 1 + y
theorem A4_5 : -1 < x ∧ -1 < y ∧ (1 + y) * pn * x < y → (1 + x) ^ pn < 1 + y := by
  -- === TCCs for A4_5 ===
  -- TCC: A4_5_TCC1
  -- Statement: FORALL (pn: posnat, x, y: real):
  --   (1 + y) * pn * x < y AND -1 < y AND -1 < x IMPLIES (1 + x) /= 0 OR pn >= 0
  have a4_5_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_6pp
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 < x =>
--  (FORALL n:
--     (FORALL y: 0 < y => (y * (1 + x) < n * x => 1 + y < (1 + x) ^ n)))
theorem A4_6pp : 0 < x → (∀ n, 
   (∀ y,  0 < y → (y * (1 + x) < n * x → 1 + y < (1 + x) ^ n))) := by
  -- === TCCs for A4_6pp ===
  -- TCC: A4_6pp_TCC1
  -- Statement: FORALL (x: real):
  --   0 < x IMPLIES
  --    FORALL (n: nat, y: real):
  --      0 < y AND y * (1 + x) < n * x IMPLIES (1 + x) /= 0 OR n >= 0
  have a4_6pp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_6pp_general
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 < x AND y * (1 + x) < n * x => 1 + y < (1 + x) ^ n
theorem A4_6pp_general : 0 < x ∧ y * (1 + x) < n * x → 1 + y < (1 + x) ^ n := by
  -- === TCCs for A4_6pp_general ===
  -- TCC: A4_6pp_general_TCC1
  -- Statement: FORALL (n: nat, x, y: real):
  --   y * (1 + x) < n * x AND 0 < x IMPLIES (1 + x) /= 0 OR n >= 0
  have a4_6pp_general_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_6nn
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < x AND x < 0 =>
--  (FORALL n:
--     (FORALL y:
--        -1 < y AND y < 0 => (y * (1 + x) < n * x => 1 + y < (1 + x) ^ n)))
theorem A4_6nn : -1 < x ∧ x < 0 → (∀ n, 
   (∀ y, 
      -1 < y ∧ y < 0 → (y * (1 + x) < n * x → 1 + y < (1 + x) ^ n))) := by
  -- === TCCs for A4_6nn ===
  -- TCC: A4_6nn_TCC1
  -- Statement: FORALL (x: real):
  --   x < 0 AND -1 < x IMPLIES
  --    FORALL (n: nat, y: real):
  --      -1 < y AND y < 0 AND y * (1 + x) < n * x IMPLIES (1 + x) /= 0 OR n >= 0
  have a4_6nn_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_6nn_general
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 < x AND x < 0 AND y < 0 AND y * (1 + x) < n * x => 1 + y < (1 + x) ^ n
theorem A4_6nn_general : -1 < x ∧ x < 0 ∧ y < 0 ∧ y * (1 + x) < n * x → 1 + y < (1 + x) ^ n := by
  -- === TCCs for A4_6nn_general ===
  -- TCC: A4_6nn_general_TCC1
  -- Statement: FORALL (n: nat, x, y: real):
  --   y * (1 + x) < n * x AND y < 0 AND x < 0 AND -1 < x IMPLIES
  --    (1 + x) /= 0 OR n >= 0
  have a4_6nn_general_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_sqrt_ineq1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 / 2 < 2 - sqrt(2)
theorem A4_sqrt_ineq1 : 1 / 2 < 2 - Real.sqrt (2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_sqrt_ineq2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 / sqrt(2) + 2 ^ (-3) < 1
theorem A4_sqrt_ineq2 : 1 / Real.sqrt (2) + 2 ^ (-3) < 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_sqrt_ineq3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 < 1 / sqrt(2) - 2 ^ (-3)
theorem A4_sqrt_ineq3 : 0 < 1 / Real.sqrt (2) - 2 ^ (-3) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_logsize
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 <= pn AND 1 <= pn * 2 ^ -l AND pn * 2 ^ -l < 2 => 1 <= l
theorem A4_logsize : 2 ≤ pn ∧ 1 ≤ pn * 2 ^ -l ∧ pn * 2 ^ -l < 2 → 1 ≤ l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_ineq1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 <= n AND 3 <= pn AND 2 ^ -n / sqrt(2) <= x AND x < 2 ^ -n =>
--  x ^ pn + 2 ^ -n < 2 * x
theorem A4_lemma_ineq1 : 1 ≤ n ∧ 3 ≤ pn ∧ 2 ^ -n / Real.sqrt (2) ≤ x ∧ x < 2 ^ -n → x ^ pn + 2 ^ -n < 2 * x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_ineq2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (3 <= pn) AND (2 ^ -n <= x) AND (x < 1) AND (1 <= pn * 2 ^ -l)
--  AND (pn * 2 ^ -l < 2)
--  => x ^ pn + 2 ^ -n < 2 * x
theorem A4_lemma_ineq2 : (3 ≤ pn) ∧ (2 ^ -n ≤ x) ∧ (x < 1) ∧ (1 ≤ pn * 2 ^ -l)
 ∧ (pn * 2 ^ -l < 2) → x ^ pn + 2 ^ -n < 2 * x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_large_UB_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (3 <= pn) AND (1 / sqrt(2) <= x) AND (x < 1) AND (1 <= pn * 2 ^ -l)
--  AND (pn * 2 ^ -l < 2)
--  => (x + 2 ^ -(l + 2)) ^ pn < x ^ pn + 1
theorem A4_lemma_large_UB_0 : (3 ≤ pn) ∧ (1 / Real.sqrt (2) ≤ x) ∧ (x < 1) ∧ (1 ≤ pn * 2 ^ -l)
 ∧ (pn * 2 ^ -l < 2) → (x + 2 ^ -(l + 2)) ^ pn < x ^ pn + 1 := by
  -- === TCCs for A4_lemma_large_UB_0 ===
  -- TCC: A4_lemma_large_UB_0_TCC1
  -- Statement: FORALL (l: nat, pn: posnat, x: real):
  --        (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l) AND (x < 1)
  --    AND (1 / sqrt(2) <= x) AND (3 <= pn)
  --    IMPLIES NOT (x + 2 ^ -(2 + l)) = 0 OR pn >= 0
  have a4_lemma_large_ub_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_large_UB
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (3 <= pn) AND (e2 / sqrt(2) <= x) AND (x < 1) AND (1 <= pn * 2 ^ -l)
--  AND (pn * 2 ^ -l < 2) AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  => (x + e1) ^ pn < x ^ pn + e2
theorem A4_lemma_large_UB : (3 ≤ pn) ∧ (e2 / Real.sqrt (2) ≤ x) ∧ (x < 1) ∧ (1 ≤ pn * 2 ^ -l)
 ∧ (pn * 2 ^ -l < 2) ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_large_UB ===
  -- TCC: A4_lemma_large_UB_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x < 1) AND (e2 / sqrt(2) <= x) AND (3 <=...
  have a4_lemma_large_ub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_large_LB
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (3 <= pn) AND (e2 / sqrt(2) <= x) AND (x < 1) AND (1 <= pn * 2 ^ -l)
--  AND (pn * 2 ^ -l < 2) AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  => x ^ pn - e2 < (x - e1) ^ pn
theorem A4_lemma_large_LB : (3 ≤ pn) ∧ (e2 / Real.sqrt (2) ≤ x) ∧ (x < 1) ∧ (1 ≤ pn * 2 ^ -l)
 ∧ (pn * 2 ^ -l < 2) ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn := by
  -- === TCCs for A4_lemma_large_LB ===
  -- TCC: A4_lemma_large_LB_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x < 1) AND (e2 / sqrt(2) <= x) AND (3 <=...
  have a4_lemma_large_lb_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_large_ge3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (3 <= pn) AND (e2 / sqrt(2) <= x) AND (x < 1) AND (1 <= pn * 2 ^ -l)
--  AND (pn * 2 ^ -l < 2) AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  => x ^ pn - e2 < (x - e1) ^ pn AND (x + e1) ^ pn < x ^ p
theorem A4_lemma_large_ge3 : (3 ≤ pn) ∧ (e2 / Real.sqrt (2) ≤ x) ∧ (x < 1) ∧ (1 ≤ pn * 2 ^ -l)
 ∧ (pn * 2 ^ -l < 2) ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_large_lt3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (pn = 1 OR pn = 2) AND (e2 / sqrt(2) <= x) AND (x < 1)
--  AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2) AND (e1 = 2 ^ -(n + l + 2))
--  AND (e2 = 2 ^ -n)
--  => x ^ pn - e2 < (x - e1) ^ pn AND (x + e1) ^ 
theorem A4_lemma_large_lt3 : (pn = 1 ∨ pn = 2) ∧ (e2 / Real.sqrt (2) ≤ x) ∧ (x < 1)
 ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2) ∧ (e1 = 2 ^ -(n + l + 2))
 ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_large_lt3 ===
  -- TCC: A4_lemma_large_lt3_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x < 1) AND (e2 / sqrt(2) <= x)
  --    AND (p...
  have a4_lemma_large_lt3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: A4_lemma_large_lt3_TCC2
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x - e1) ^ pn) AND (e2 = 2 ^ -n)
  --    AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l)
  --    AND (x <...
  have a4_lemma_large_lt3_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_small_UB
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (2 <= pn) AND (0 < x) AND (x < e2 / sqrt(2)) AND (1 <= l)
--  AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  => (x + e1) ^ pn < x ^ pn + e2
theorem A4_lemma_small_UB : (2 ≤ pn) ∧ (0 < x) ∧ (x < e2 / Real.sqrt (2)) ∧ (1 ≤ l)
 ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_small_UB ===
  -- TCC: A4_lemma_small_UB_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (1 <= l)
  --    AND (x < e2 / sqrt(2)) AND (0 < x) AND (2 <= pn)
  --    IMPLIES NOT e1 + x = 0 OR...
  have a4_lemma_small_ub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_small_LB
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (2 <= pn) AND (0 < x) AND (x < e2 / sqrt(2)) AND (1 <= l)
--  AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  => x ^ pn - e2 < (x - e1) ^ pn
theorem A4_lemma_small_LB : (2 ≤ pn) ∧ (0 < x) ∧ (x < e2 / Real.sqrt (2)) ∧ (1 ≤ l)
 ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn := by
  -- === TCCs for A4_lemma_small_LB ===
  -- TCC: A4_lemma_small_LB_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (1 <= l)
  --    AND (x < e2 / sqrt(2)) AND (0 < x) AND (2 <= pn)
  --    IMPLIES (x - e1) /= 0 OR ...
  have a4_lemma_small_lb_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_small
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (0 < x) AND (x < e2 / sqrt(2)) AND (1 <= pn * 2 ^ -l)
--  AND (pn * 2 ^ -l < 2) AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  => x ^ pn - e2 < (x - e1) ^ pn AND (x + e1) ^ pn < x ^ pn + e2
theorem A4_lemma_small : (0 < x) ∧ (x < e2 / Real.sqrt (2)) ∧ (1 ≤ pn * 2 ^ -l)
 ∧ (pn * 2 ^ -l < 2) ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_small ===
  -- TCC: A4_lemma_small_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x < e2 / sqrt(2)) AND (0 < x)
  --    IMPLIES...
  have a4_lemma_small_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: A4_lemma_small_TCC2
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x - e1) ^ pn) AND (e2 = 2 ^ -n)
  --    AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l)
  --    AND (x <...
  have a4_lemma_small_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_px
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (e1 <= x) AND (x < 1) AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2)
--  AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  => x ^ pn - e2 < (x - e1) ^ pn AND (x + e1) ^ pn < x ^ pn + e2
theorem A4_lemma_px : (e1 ≤ x) ∧ (x < 1) ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2)
 ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_px ===
  -- TCC: A4_lemma_px_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x < 1) AND (e1 <= x)
  --    IMPLIES (x - e1)...
  have a4_lemma_px_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: A4_lemma_px_TCC2
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x - e1) ^ pn) AND (e2 = 2 ^ -n)
  --    AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l)
  --    AND (x <...
  have a4_lemma_px_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_p
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (e1 <= x) AND (x < 1) AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2)
--  AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  =>
--  x ^ pn - e2 < (x - e1) ^ pn AND
--   x ^ pn - e2 < (x + e1) ^ pn AND
--    (x - e1
theorem A4_lemma_p : (e1 ≤ x) ∧ (x < 1) ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2)
 ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧
 x ^ pn - e2 < (x + e1) ^ pn ∧
  (x - e1) ^ pn < x ^ pn + e2 ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_0_px
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (0 < x) AND (x < e1) AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2)
--  AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  => x ^ pn - e2 < (x - e1) ^ pn AND (x + e1) ^ pn < x ^ pn + e2
theorem A4_lemma_0_px : (0 < x) ∧ (x < e1) ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2)
 ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_0_px ===
  -- TCC: A4_lemma_0_px_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x < e1) AND (0 < x)
  --    IMPLIES (x - e1) ...
  have a4_lemma_0_px_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: A4_lemma_0_px_TCC2
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x - e1) ^ pn) AND (e2 = 2 ^ -n)
  --    AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l)
  --    AND (x <...
  have a4_lemma_0_px_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_0_p
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (0 < x) AND (x < e1) AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2)
--  AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  =>
--  x ^ pn - e2 < (x - e1) ^ pn AND
--   x ^ pn - e2 < (x + e1) ^ pn AND
--    (x - e1)
theorem A4_lemma_0_p : (0 < x) ∧ (x < e1) ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2)
 ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧
 x ^ pn - e2 < (x + e1) ^ pn ∧
  (x - e1) ^ pn < x ^ pn + e2 ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (-e1 < x) AND (x < e1) AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2)
--  AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  =>
--  x ^ pn - e2 < (x - e1) ^ pn AND
--   x ^ pn - e2 < (x + e1) ^ pn AND
--    (x - e
theorem A4_lemma_0 : (-e1 < x) ∧ (x < e1) ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2)
 ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧
 x ^ pn - e2 < (x + e1) ^ pn ∧
  (x - e1) ^ pn < x ^ pn + e2 ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_0 ===
  -- TCC: A4_lemma_0_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x < e1) AND (-e1 < x)
  --    IMPLIES (x - e1...
  have a4_lemma_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: A4_lemma_0_TCC2
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x - e1) ^ pn) AND (e2 = 2 ^ -n)
  --    AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l)
  --    AND (x <...
  have a4_lemma_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_n_even
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      even?(pn) AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n) AND (-1 < x)
--  AND (x <= -e1) AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2)
--  => x ^ pn - e2 < (x + e1) ^ pn AND (x - e1) ^ pn < x ^ pn + e2
theorem A4_lemma_n_even : even_p pn ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) ∧ (-1 < x)
 ∧ (x ≤ -e1) ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2) → x ^ pn - e2 < (x + e1) ^ pn ∧ (x - e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_n_even ===
  -- TCC: A4_lemma_n_even_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l) AND (x <= -e1) AND (-1 < x)
  --    AND (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND even?(pn)
  --    ...
  have a4_lemma_n_even_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: A4_lemma_n_even_TCC2
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x + e1) ^ pn) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x <= -e1) AND (-1 < x) AND (e2 = 2 ^ -n)
  --    AND (e1 ...
  have a4_lemma_n_even_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_n_odd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      odd?(pn) AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n) AND (-1 < x)
--  AND (x <= -e1) AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2)
--  => x ^ pn - e2 < (x - e1) ^ pn AND (x + e1) ^ pn < x ^ pn + e2
theorem A4_lemma_n_odd : odd_p pn ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) ∧ (-1 < x)
 ∧ (x ≤ -e1) ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2) → x ^ pn - e2 < (x - e1) ^ pn ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_n_odd ===
  -- TCC: A4_lemma_n_odd_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l) AND (x <= -e1) AND (-1 < x)
  --    AND (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND odd?(pn)
  --    I...
  have a4_lemma_n_odd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: A4_lemma_n_odd_TCC2
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x - e1) ^ pn) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x <= -e1) AND (-1 < x) AND (e2 = 2 ^ -n)
  --    AND (e1 ...
  have a4_lemma_n_odd_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: prelude_A4.A4_lemma_n
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (-1 < x) AND (x <= -e1) AND (1 <= pn * 2 ^ -l) AND (pn * 2 ^ -l < 2)
--  AND (e1 = 2 ^ -(n + l + 2)) AND (e2 = 2 ^ -n)
--  =>
--  x ^ pn - e2 < (x - e1) ^ pn AND
--   x ^ pn - e2 < (x + e1) ^ pn AND
--    (x - 
theorem A4_lemma_n : (-1 < x) ∧ (x ≤ -e1) ∧ (1 ≤ pn * 2 ^ -l) ∧ (pn * 2 ^ -l < 2)
 ∧ (e1 = 2 ^ -(n + l + 2)) ∧ (e2 = 2 ^ -n) → x ^ pn - e2 < (x - e1) ^ pn ∧
 x ^ pn - e2 < (x + e1) ^ pn ∧
  (x - e1) ^ pn < x ^ pn + e2 ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for A4_lemma_n ===
  -- TCC: A4_lemma_n_TCC1
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2)
  --    AND (1 <= pn * 2 ^ -l) AND (x <= -e1) AND (-1 < x)
  --    IMPLIES (x - e...
  have a4_lemma_n_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: A4_lemma_n_TCC2
  -- Statement: FORALL (e1, e2: real, l, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x - e1) ^ pn) AND (e2 = 2 ^ -n)
  --    AND (e1 = 2 ^ -(n + l + 2)) AND (pn * 2 ^ -l < 2) AND (1 <= pn * 2 ^ -l)
  --    AND (x <...
  have a4_lemma_n_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.prelude_A4