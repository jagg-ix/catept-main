-- Auto-generated by pvs2lean/translator.py
-- PVS theory: inv
-- PVSLib library: exact_real_arith
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: inv

Auto-generated stubs from PVSLib theory `inv`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.inv

noncomputable section

variable {pvs_τ5 : Type*} (cauchy_nzreal_p : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (cauchy_prop : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (min : pvs_τ8 → pvs_τ9)
variable {pvs_τ15 : Type*} (nonempty_p : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} (nzcx : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} (round : pvs_τ17 → Prop)

-- pvs2lean: inv.expt_x2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ 2 = x * x
theorem expt_x2 : x ^ 2 = x * x := by
  -- === TCCs for expt_x2 ===
  -- TCC: expt_x2_TCC1
  -- Statement: FORALL (x: real): x /= 0 OR 2 >= 0
  have expt_x2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.expt_times2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nzx ^ (2 * s) = nzx ^ s * nzx ^ s
theorem expt_times2 : nzx ^ (2 * s) = nzx ^ s * nzx ^ s := by
  -- === TCCs for expt_times2 ===
  -- TCC: expt_times2_TCC1
  -- Statement: FORALL (nzx: nzreal, s: nat): nzx /= 0 OR (2 * s) >= 0
  have expt_times2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: expt_times2_TCC2
  -- Statement: FORALL (nzx: nzreal, s: nat): nzx /= 0 OR s >= 0
  have expt_times2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.minimum_inv_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?({s | 3 <= abs(nzcx(s))})
theorem minimum_inv_exists : nonempty_p (fun (s : {i : ℝ // i ≥ 0}) => 3 ≤ |nzcx s|) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: inv.minimum_inv
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min_nat.min({s | 3 <= abs(nzcx(s))})
axiom minimum_inv {pvs_τ10 : Type*} : pvs_τ10 → Prop

-- pvs2lean: inv.minimum_inv_prop0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: s = minimum_inv(nzcx) => 3 <= abs(nzcx(s))
theorem minimum_inv_prop0 : s = minimum_inv nzcx → 3 ≤ |nzcx s| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: inv.minimum_inv_prop1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: s = minimum_inv(nzcx) AND cauchy_prop(x, nzcx) => 2 <= abs(x) * 2 ^ s
theorem minimum_inv_prop1 : s = minimum_inv nzcx ∧ cauchy_prop x nzcx → 2 ≤ |x| * 2 ^ s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: inv.minimum_inv_prop2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 <= abs(nzx) * 2 ^ s AND
--  n - 1 < nzx * 2 ^ (p + 2 * s + 2) AND nzx * 2 ^ (p + 2 * s + 2) < n + 1
--  => n /= 0
theorem minimum_inv_prop2 : 2 ≤ |nzx| * 2 ^ s ∧
 n - 1 < nzx * 2 ^ (p + 2 * s + 2) ∧ nzx * 2 ^ (p + 2 * s + 2) < n + 1 → n ≠ 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.minimum_inv_aux
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF 3 <= abs(nzcx(s)) THEN s ELSE minimum_inv_aux(nzcx, s + 1) ENDIF
axiom minimum_inv_aux {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13

-- pvs2lean: inv.minimum_inv_impl
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: minimum_inv_aux(nzcx, 0)
axiom minimum_inv_impl {pvs_τ14 : Type*} : pvs_τ14 → Prop

-- pvs2lean: inv.minimum_inv_impl_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: minimum_inv(nzcx) = minimum_inv_impl(nzcx)
theorem minimum_inv_impl_def : minimum_inv nzcx = minimum_inv_impl nzcx := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: inv.inv_p0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 ^ (p + s + 3) - 1 < pn => 6 <= pn
theorem inv_p0 : 2 ^ (p + s + 3) - 1 < pn → 6 ≤ pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 ^ (p + s + 3) - 1 < pn => 2 ^ (2 * p + 2 * s + 3) < pn * (pn - 1)
theorem inv_p1 : 2 ^ (p + s + 3) - 1 < pn → 2 ^ (2 * p + 2 * s + 3) < pn * (pn - 1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 ^ (p + s + 3) - 1 < pn => 2 ^ (2 * p + 2 * s + 3) < pn * (pn + 1)
theorem inv_p2 : 2 ^ (p + s + 3) - 1 < pn → 2 ^ (2 * p + 2 * s + 3) < pn * (pn + 1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 ^ (p + s + 3) - 1 < pn => 2 ^ (2 * p + 2 * s + 2) / pn - (pn - 1) / 2 < 0
theorem inv_p3 : 2 ^ (p + s + 3) - 1 < pn → 2 ^ (2 * p + 2 * s + 2) / pn - (pn - 1) / 2 < 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 ^ (p + s + 3) - 1 < pn => 2 ^ (2 * p + 2 * s + 2) / pn - (pn + 1) / 2 < 0
theorem inv_p4 : 2 ^ (p + s + 3) - 1 < pn → 2 ^ (2 * p + 2 * s + 2) / pn - (pn + 1) / 2 < 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r = round(2 ^ (2 * p + 2 * s + 2) / pn) AND 2 ^ (p + s + 3) - 1 < pn =>
--  (r - 1) * (pn + 1) < 2 ^ (2 * p + 2 * s + 2)
theorem inv_p5 : r = round (2 ^ (2 * p + 2 * s + 2) / pn) ∧ 2 ^ (p + s + 3) - 1 < pn → (r - 1) * (pn + 1) < 2 ^ (2 * p + 2 * s + 2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p6
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r = round(2 ^ (2 * p + 2 * s + 2) / pn) AND 2 ^ (p + s + 3) - 1 < pn =>
--  2 ^ (2 * p + 2 * s + 2) < (r + 1) * (pn - 1)
theorem inv_p6 : r = round (2 ^ (2 * p + 2 * s + 2) / pn) ∧ 2 ^ (p + s + 3) - 1 < pn → 2 ^ (2 * p + 2 * s + 2) < (r + 1) * (pn - 1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p7
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r = round(2 ^ (2 * p + 2 * s + 2) / pn) AND 2 ^ (p + s + 3) - 1 < pn =>
--  (r - 1) / 2 ^ p < 2 ^ (p + 2 * s + 2) / (pn + 1) AND
--   2 ^ (p + 2 * s + 2) / (pn - 1) < (r + 1) / 2 ^ p
theorem inv_p7 : r = round (2 ^ (2 * p + 2 * s + 2) / pn) ∧ 2 ^ (p + s + 3) - 1 < pn → (r - 1) / 2 ^ p < 2 ^ (p + 2 * s + 2) / (pn + 1) ∧
 2 ^ (p + 2 * s + 2) / (pn - 1) < (r + 1) / 2 ^ p := by
  -- === TCCs for inv_p7 ===
  -- TCC: inv_p7_TCC1
  -- Statement: FORALL (p: nat, pn: posnat, r: int, s: nat):
  --   (r - 1) / 2 ^ p < 2 ^ (p + 2 * s + 2) / (pn + 1) AND
  --    2 ^ (p + s + 3) - 1 < pn AND r = round(2 ^ (2 * p + 2 * s + 2) / pn)
  --    IMPLIES (pn - 1) /= 0
  have inv_p7_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p8
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 <= px * 2 ^ s AND
--  pn - 1 < px * 2 ^ (p + 2 * s + 2) AND px * 2 ^ (p + 2 * s + 2) < pn + 1
--  => 2 ^ (p + s + 3) - 1 < pn
theorem inv_p8 : 2 ≤ px * 2 ^ s ∧
 pn - 1 < px * 2 ^ (p + 2 * s + 2) ∧ px * 2 ^ (p + 2 * s + 2) < pn + 1 → 2 ^ (p + s + 3) - 1 < pn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p9
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 <= px * 2 ^ s AND
--  r = round(2 ^ (2 * p + 2 * s + 2) / pn) AND
--   pn - 1 < px * 2 ^ (p + 2 * s + 2) AND px * 2 ^ (p + 2 * s + 2) < pn + 1
--  => (r - 1) * px < 2 ^ p AND 2 ^ p < (r + 1) * px
theorem inv_p9 : 2 ≤ px * 2 ^ s ∧
 r = round (2 ^ (2 * p + 2 * s + 2) / pn) ∧
  pn - 1 < px * 2 ^ (p + 2 * s + 2) ∧ px * 2 ^ (p + 2 * s + 2) < pn + 1 → (r - 1) * px < 2 ^ p ∧ 2 ^ p < (r + 1) * px := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_n5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r = round(2 ^ (2 * p + 2 * s + 2) / nn) AND 2 ^ (p + s + 3) - 1 < -1 * nn
--  => (r - 1) < 2 ^ (2 * p + 2 * s + 2) / (nn + 1)
theorem inv_n5 : r = round (2 ^ (2 * p + 2 * s + 2) / nn) ∧ 2 ^ (p + s + 3) - 1 < -1 * nn → (r - 1) < 2 ^ (2 * p + 2 * s + 2) / (nn + 1) := by
  -- === TCCs for inv_n5 ===
  -- TCC: inv_n5_TCC1
  -- Statement: FORALL (nn: negint, p: nat, r: int, s: nat):
  --   2 ^ (p + s + 3) - 1 < -1 * nn AND r = round(2 ^ (2 * p + 2 * s + 2) / nn)
  --    IMPLIES (nn + 1) /= 0
  have inv_n5_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_n6
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r = round(2 ^ (2 * p + 2 * s + 2) / nn) AND 2 ^ (p + s + 3) - 1 < -1 * nn
--  => 2 ^ (2 * p + 2 * s + 2) / (nn - 1) < (r + 1)
theorem inv_n6 : r = round (2 ^ (2 * p + 2 * s + 2) / nn) ∧ 2 ^ (p + s + 3) - 1 < -1 * nn → 2 ^ (2 * p + 2 * s + 2) / (nn - 1) < (r + 1) := by
  -- === TCCs for inv_n6 ===
  -- TCC: inv_n6_TCC1
  -- Statement: FORALL (nn: negint, p: nat, r: int, s: nat):
  --   2 ^ (p + s + 3) - 1 < -1 * nn AND r = round(2 ^ (2 * p + 2 * s + 2) / nn)
  --    IMPLIES (nn - 1) /= 0
  have inv_n6_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_n7
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r = round(2 ^ (2 * p + 2 * s + 2) / nn) AND 2 ^ (p + s + 3) - 1 < -nn =>
--  (r - 1) / 2 ^ p < 2 ^ (p + 2 * s + 2) / (nn + 1) AND
--   2 ^ (p + 2 * s + 2) / (nn - 1) < (r + 1) / 2 ^ p
theorem inv_n7 : r = round (2 ^ (2 * p + 2 * s + 2) / nn) ∧ 2 ^ (p + s + 3) - 1 < -nn → (r - 1) / 2 ^ p < 2 ^ (p + 2 * s + 2) / (nn + 1) ∧
 2 ^ (p + 2 * s + 2) / (nn - 1) < (r + 1) / 2 ^ p := by
  -- === TCCs for inv_n7 ===
  -- TCC: inv_n7_TCC1
  -- Statement: FORALL (nn: negint, p: nat, r: int, s: nat):
  --   2 ^ (p + s + 3) - 1 < -nn AND r = round(2 ^ (2 * p + 2 * s + 2) / nn)
  --    IMPLIES (nn + 1) /= 0
  have inv_n7_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inv_n7_TCC2
  -- Statement: FORALL (nn: negint, p: nat, r: int, s: nat):
  --   (r - 1) / 2 ^ p < 2 ^ (p + 2 * s + 2) / (nn + 1) AND
  --    2 ^ (p + s + 3) - 1 < -nn AND r = round(2 ^ (2 * p + 2 * s + 2) / nn)
  --    IMPLIES (nn - 1) /= 0
  have inv_n7_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_n8
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 <= -nx * 2 ^ s AND
--  nn - 1 < nx * 2 ^ (p + 2 * s + 2) AND nx * 2 ^ (p + 2 * s + 2) < nn + 1
--  => 2 ^ (p + s + 3) - 1 < -nn
theorem inv_n8 : 2 ≤ -nx * 2 ^ s ∧
 nn - 1 < nx * 2 ^ (p + 2 * s + 2) ∧ nx * 2 ^ (p + 2 * s + 2) < nn + 1 → 2 ^ (p + s + 3) - 1 < -nn := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_n9
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 <= -nx * 2 ^ s AND
--  r = round(2 ^ (2 * p + 2 * s + 2) / nn) AND
--   nn - 1 < nx * 2 ^ (p + 2 * s + 2) AND nx * 2 ^ (p + 2 * s + 2) < nn + 1
--  => (r - 1) < 2 ^ p / nx AND 2 ^ p / nx < (r + 1)
theorem inv_n9 : 2 ≤ -nx * 2 ^ s ∧
 r = round (2 ^ (2 * p + 2 * s + 2) / nn) ∧
  nn - 1 < nx * 2 ^ (p + 2 * s + 2) ∧ nx * 2 ^ (p + 2 * s + 2) < nn + 1 → (r - 1) < 2 ^ p / nx ∧ 2 ^ p / nx < (r + 1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.inv_p
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 <= abs(nzx) * 2 ^ s AND
--  r = round(2 ^ (2 * p + 2 * s + 2) / nzn) AND
--   nzn - 1 < nzx * 2 ^ (p + 2 * s + 2) AND
--    nzx * 2 ^ (p + 2 * s + 2) < nzn + 1
--  => (r - 1) < 2 ^ p / nzx AND 2 ^ p / nzx < (r 
theorem inv_p : 2 ≤ |nzx| * 2 ^ s ∧
 r = round (2 ^ (2 * p + 2 * s + 2) / nzn) ∧
  nzn - 1 < nzx * 2 ^ (p + 2 * s + 2) ∧
   nzx * 2 ^ (p + 2 * s + 2) < nzn + 1 → (r - 1) < 2 ^ p / nzx ∧ 2 ^ p / nzx < (r + 1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: inv.minimum_inv_prop3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: s = minimum_inv(nzcx) AND cauchy_prop(nzx, nzcx) =>
--  cauchy_prop(1 / nzx,
--              LAMBDA p: round(2 ^ (2 * p + 2 * s + 2) / nzcx(p + 2 * s + 2)))
theorem minimum_inv_prop3 : s = minimum_inv nzcx ∧ cauchy_prop nzx nzcx → cauchy_prop (1 / nzx) (fun p=>  round (2 ^ (2 * p + 2 * s + 2) / nzcx (p + 2 * s + 2))) := by
  -- === TCCs for minimum_inv_prop3 ===
  -- TCC: minimum_inv_prop3_TCC1
  -- Statement: FORALL (nzcx: cauchy_nzreal, nzx: nzreal, s: nat):
  --   cauchy_prop(nzx, nzcx) AND s = minimum_inv(nzcx) IMPLIES
  --    FORALL (p: nat): nzcx(p + 2 * s + 2) /= 0
  have minimum_inv_prop3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: inv.cauchy_nz_inv
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA p:
--    LET s = minimum_inv_impl(nzcx) IN
--      round(2 ^ (2 * p + 2 * s + 2) / nzcx(p + 2 * s + 2)))
axiom cauchy_nz_inv {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ3 → pvs_τ4

-- pvs2lean: inv.inv_nz_lemma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cauchy_prop(nzx, nzcx) => cauchy_prop(1 / nzx, cauchy_nz_inv(nzcx))
theorem inv_nz_lemma : cauchy_prop nzx nzcx → cauchy_prop (1 / nzx) (cauchy_nz_inv nzcx) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: inv.cauchy_inv
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cauchy_nz_inv(nzcx)
axiom cauchy_inv {pvs_τ1 : Type*} {pvs_τ2 : Type*} : pvs_τ1 → pvs_τ2

-- pvs2lean: inv.inv
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 / nzx
noncomputable def inv : sorry := by
  sorry  -- pvs2lean: definition stub for 1 / nzx

-- pvs2lean: inv.inv_lemma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cauchy_prop(nzx, nzcx) => cauchy_prop(1 / nzx, cauchy_inv(nzcx))
theorem inv_lemma : cauchy_prop nzx nzcx → cauchy_prop (1 / nzx) (cauchy_inv nzcx) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.inv