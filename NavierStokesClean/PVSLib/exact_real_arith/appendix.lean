-- Auto-generated by pvs2lean/translator.py
-- PVS theory: appendix
-- PVSLib library: exact_real_arith
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: appendix

Auto-generated stubs from PVSLib theory `appendix`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.appendix

noncomputable section

variable {pvs_τ1 : Type*} (floor : pvs_τ1 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (log2 : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (round : pvs_τ11 → Prop)

-- pvs2lean: appendix.lemma_A1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: m * m <= n AND n < (m + 1) * (m + 1) IFF m = floor(sqrt(n))
theorem lemma_A1 : m * m ≤ n ∧ n < (m + 1) * (m + 1) ↔ m = floor (Real.sqrt (n)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: appendix.lemma_A2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r = round(p / q) IFF (r - 1 / 2) * q <= p AND p < (r + 1 / 2) * q
theorem lemma_A2 : r = round (p / q) ↔ (r - 1 / 2) * q ≤ p ∧ p < (r + 1 / 2) * q := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: appendix.lemma_A3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: i = floor(log2(px)) IFF 2 ^ i <= px AND px < 2 ^ (i + 1)
theorem lemma_A3 : i = floor (log2 px) ↔ 2 ^ i ≤ px ∧ px < 2 ^ (i + 1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: appendix.lemma_A4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: e1 = 2 ^ -(n + floor(log2(pn)) + 2) AND e2 = 2 ^ -n AND -1 < x AND x < 1 =>
--  x ^ pn - e2 < (x - e1) ^ pn AND
--   x ^ pn - e2 < (x + e1) ^ pn AND
--    (x - e1) ^ pn < x ^ pn + e2 AND (x + e1) ^ pn < x ^ pn
theorem lemma_A4 : e1 = 2 ^ -(n + floor (log2 pn) + 2) ∧ e2 = 2 ^ -n ∧ -1 < x ∧ x < 1 → x ^ pn - e2 < (x - e1) ^ pn ∧
 x ^ pn - e2 < (x + e1) ^ pn ∧
  (x - e1) ^ pn < x ^ pn + e2 ∧ (x + e1) ^ pn < x ^ pn + e2 := by
  -- === TCCs for lemma_A4 ===
  -- TCC: lemma_A4_TCC1
  -- Statement: FORALL (e1, e2: real, n: nat, pn: posnat, x: real):
  --   x < 1 AND -1 < x AND e2 = 2 ^ -n AND e1 = 2 ^ -(n + floor(log2(pn)) + 2)
  --    IMPLIES x /= 0 OR pn >= 0
  have lemma_a4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lemma_A4_TCC2
  -- Statement: FORALL (e1, e2: real, n: nat, pn: posnat, x: real):
  --   x < 1 AND -1 < x AND e2 = 2 ^ -n AND e1 = 2 ^ -(n + floor(log2(pn)) + 2)
  --    IMPLIES (x - e1) /= 0 OR pn >= 0
  have lemma_a4_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lemma_A4_TCC3
  -- Statement: FORALL (e1, e2: real, n: nat, pn: posnat, x: real):
  --        (x ^ pn - e2 < (x - e1) ^ pn) AND (x < 1) AND (-1 < x)
  --    AND (e2 = 2 ^ -n) AND (e1 = 2 ^ -(n + floor(log2(pn)) + 2))
  --    IMPLIES NOT e1 + ...
  have lemma_a4_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: appendix.epsilon_log2_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat): EXISTS (p: nat): n < 2 ^ p
theorem epsilon_log2_aux : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}), ∃ (p : {i : ℝ // i ≥ 0}), n < 2 ^ p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: appendix.epsilon_log2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (p: nat): 1 / 2 ^ p < px
theorem epsilon_log2 : ∃ (p : {i : ℝ // i ≥ 0}), 1 / 2 ^ p < px := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: appendix.floor_sqrt_aux
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (IF (m + 1) * (m + 1) <= n THEN 1 + floor_sqrt_aux(n, m + 1) ELSE 0 ENDIF)
axiom floor_sqrt_aux {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ6 → pvs_τ7 → pvs_τ8

-- pvs2lean: appendix.floor_sqrt
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: floor_sqrt_aux(n, 0)
axiom floor_sqrt {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ4 → pvs_τ5

-- pvs2lean: appendix.floor_sqrt_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: floor_sqrt(n) = floor(sqrt(n))
theorem floor_sqrt_def : floor_sqrt n = floor (Real.sqrt (n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: appendix.floor_log2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (IF n >= 2 THEN 1 + floor_log2(floor(n / 2)) ELSE 0 ENDIF)
axiom floor_log2 {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ2 → pvs_τ3

-- pvs2lean: appendix.floor_log2_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: floor_log2(pn) = floor(log2(pn))
theorem floor_log2_def : floor_log2 pn = floor (log2 pn) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.appendix