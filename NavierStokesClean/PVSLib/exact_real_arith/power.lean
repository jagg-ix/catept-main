-- Auto-generated by pvs2lean/translator.py
-- PVS theory: power
-- PVSLib library: exact_real_arith
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: power

Auto-generated stubs from PVSLib theory `power`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.power

noncomputable section

variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (cauchy_prop : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (cauchy_real_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (cx : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (even_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (floor : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (floor_log2 : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (log2 : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} (odd_p : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} (round : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (scx : pvs_τ20 → pvs_τ21)

-- pvs2lean: power.abs_ge0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 <= abs(x)
theorem abs_ge0 : 0 ≤ |x| := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.abs_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(-x) = abs(x)
theorem abs_neg : |-x| = |x| := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.abs_interval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x - y) < a <=> x - a < y AND y < x + a
theorem abs_interval : |x - y| < a <=> x - a < y ∧ y < x + a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.abs_interval1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < a <=> -a < x AND x < a
theorem abs_interval1 : |x| < a <=> -a < x ∧ x < a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.abs_interval2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) <= a <=> -a <= x AND x <= a
theorem abs_interval2 : |x| ≤ a <=> -a ≤ x ∧ x ≤ a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.triangle_open
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x - y) < a AND abs(y - z) < b => abs(x - z) < a + b
theorem triangle_open : |x - y| < a ∧ |y - z| < b → |x - z| < a + b := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.abs_error
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x - y) < a AND abs(w - z) < b => abs(x + w - (y + z)) < a + b
theorem abs_error : |x - y| < a ∧ |w - z| < b → |x + w - (y + z)| < a + b := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.lemma_A2_generalized
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r = round(x) => r - 1 / 2 <= x AND x < r + 1 / 2
theorem lemma_A2_generalized : r = round x → r - 1 / 2 ≤ x ∧ x < r + 1 / 2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_n_odd
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      odd?(pn) AND (sx + e1 < 0) AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2 ^ p AND sx ^ p
theorem cauchy_power_lt1_n_odd : odd_p pn ∧ (sx + e1 < 0) ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_n_odd ===
  -- TCC: cauchy_power_lt1_n_odd_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND sx + e1 < 0 AND odd?(pn) IMPLIES
  --    (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_n_odd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_n_odd_TCC2
  -- Statement: FORALL (Y: int, e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --        (sx / e1 < rx + 1) AND (rx - 1 < sx / e1)
  --    AND (Y = round((rx * e1) ^ pn * 2 ^ p))
  --    AND (e1 = 2 ^ -(p + floor(lo...
  have cauchy_power_lt1_n_odd_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_n_even
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      even?(pn) AND (sx + e1 < 0) AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2 ^ p AND sx ^ 
theorem cauchy_power_lt1_n_even : even_p pn ∧ (sx + e1 < 0) ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_n_even ===
  -- TCC: cauchy_power_lt1_n_even_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND sx + e1 < 0 AND even?(pn) IMPLIES
  --    (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_n_even_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_n_even_TCC2
  -- Statement: FORALL (Y: int, e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --        (sx / e1 < rx + 1) AND (rx - 1 < sx / e1)
  --    AND (Y = round((rx * e1) ^ pn * 2 ^ p))
  --    AND (e1 = 2 ^ -(p + floor(lo...
  have cauchy_power_lt1_n_even_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_snz_odd
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      odd?(pn) AND (sx < 0) AND (sx + e1 = 0)
--  AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2 
theorem cauchy_power_lt1_snz_odd : odd_p pn ∧ (sx < 0) ∧ (sx + e1 = 0)
 ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_snz_odd ===
  -- TCC: cauchy_power_lt1_snz_odd_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND
  --    sx + e1 = 0 AND sx < 0 AND odd?(pn)
  --    IMPLIES (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_snz_odd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_snz_odd_TCC2
  -- Statement: FORALL (Y: int, e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --        (sx / e1 < rx + 1) AND (rx - 1 < sx / e1)
  --    AND (Y = round((rx * e1) ^ pn * 2 ^ p))
  --    AND (e1 = 2 ^ -(p + floor(lo...
  have cauchy_power_lt1_snz_odd_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_snz_even
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      even?(pn) AND (sx < 0) AND (sx + e1 = 0)
--  AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2
theorem cauchy_power_lt1_snz_even : even_p pn ∧ (sx < 0) ∧ (sx + e1 = 0)
 ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_snz_even ===
  -- TCC: cauchy_power_lt1_snz_even_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND
  --    sx + e1 = 0 AND sx < 0 AND even?(pn)
  --    IMPLIES (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_snz_even_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_snz_even_TCC2
  -- Statement: FORALL (Y: int, e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --        (sx / e1 < rx + 1) AND (rx - 1 < sx / e1)
  --    AND (Y = round((rx * e1) ^ pn * 2 ^ p))
  --    AND (e1 = 2 ^ -(p + floor(lo...
  have cauchy_power_lt1_snz_even_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_sn_odd
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      odd?(pn) AND (sx < 0) AND (0 < sx + e1)
--  AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2 
theorem cauchy_power_lt1_sn_odd : odd_p pn ∧ (sx < 0) ∧ (0 < sx + e1)
 ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_sn_odd ===
  -- TCC: cauchy_power_lt1_sn_odd_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND
  --    0 < sx + e1 AND sx < 0 AND odd?(pn)
  --    IMPLIES (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_sn_odd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_sn_odd_TCC2
  -- Statement: FORALL (Y: int, e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --        (sx / e1 < rx + 1) AND (rx - 1 < sx / e1)
  --    AND (Y = round((rx * e1) ^ pn * 2 ^ p))
  --    AND (e1 = 2 ^ -(p + floor(lo...
  have cauchy_power_lt1_sn_odd_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_sn_even
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      even?(pn) AND (sx < 0) AND (0 < sx + e1)
--  AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2
theorem cauchy_power_lt1_sn_even : even_p pn ∧ (sx < 0) ∧ (0 < sx + e1)
 ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_sn_even ===
  -- TCC: cauchy_power_lt1_sn_even_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND
  --    0 < sx + e1 AND sx < 0 AND even?(pn)
  --    IMPLIES (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_sn_even_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_sn_even_TCC2
  -- Statement: FORALL (Y: int, e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --        (sx / e1 < rx + 1) AND (rx - 1 < sx / e1)
  --    AND (Y = round((rx * e1) ^ pn * 2 ^ p))
  --    AND (e1 = 2 ^ -(p + floor(lo...
  have cauchy_power_lt1_sn_even_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_z
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (sx = 0) AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2 ^ p AND sx ^ pn * 2 ^ p < 1 + Y
theorem cauchy_power_lt1_z : (sx = 0) ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_z ===
  -- TCC: cauchy_power_lt1_z_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND sx = 0 IMPLIES
  --    (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_z_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_z_TCC2
  -- Statement: FORALL (Y: int, e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --        (sx / e1 < rx + 1) AND (rx - 1 < sx / e1)
  --    AND (Y = round((rx * e1) ^ pn * 2 ^ p))
  --    AND (e1 = 2 ^ -(p + floor(lo...
  have cauchy_power_lt1_z_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_sp_odd
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      odd?(pn) AND (0 < sx) AND (sx - e1 < 0)
--  AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2 
theorem cauchy_power_lt1_sp_odd : odd_p pn ∧ (0 < sx) ∧ (sx - e1 < 0)
 ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_sp_odd ===
  -- TCC: cauchy_power_lt1_sp_odd_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND
  --    sx - e1 < 0 AND 0 < sx AND odd?(pn)
  --    IMPLIES (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_sp_odd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_sp_even
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      even?(pn) AND (0 < sx) AND (sx - e1 < 0)
--  AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2
theorem cauchy_power_lt1_sp_even : even_p pn ∧ (0 < sx) ∧ (sx - e1 < 0)
 ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_sp_even ===
  -- TCC: cauchy_power_lt1_sp_even_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND
  --    sx - e1 < 0 AND 0 < sx AND even?(pn)
  --    IMPLIES (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_sp_even_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_pz
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (sx - e1 = 0) AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2 ^ p AND sx ^ pn * 2 ^ p < 1
theorem cauchy_power_lt1_pz : (sx - e1 = 0) ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_pz ===
  -- TCC: cauchy_power_lt1_pz_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND sx - e1 = 0 IMPLIES
  --    (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_pz_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_p
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (0 < sx - e1) AND (e1 = 2 ^ -(p + floor(log2(pn)) + 3))
--  AND (Y = round((rx * e1) ^ pn * 2 ^ p)) AND (rx - 1 < sx / e1)
--  AND (sx / e1 < rx + 1)
--  => Y - 1 < sx ^ pn * 2 ^ p AND sx ^ pn * 2 ^ p < 1
theorem cauchy_power_lt1_p : (0 < sx - e1) ∧ (e1 = 2 ^ -(p + floor (log2 pn) + 3))
 ∧ (Y = round ((rx * e1) ^ pn * 2 ^ p)) ∧ (rx - 1 < sx / e1)
 ∧ (sx / e1 < rx + 1) → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_p ===
  -- TCC: cauchy_power_lt1_p_TCC1
  -- Statement: FORALL (e1: posreal, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   e1 = 2 ^ -(p + floor(log2(pn)) + 3) AND 0 < sx - e1 IMPLIES
  --    (rx * e1) /= 0 OR pn >= 0
  have cauchy_power_lt1_p_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_main_generalized
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: Y = round((rx * 2 ^ -(p + floor(log2(pn)) + 3)) ^ pn * 2 ^ p) AND
--  rx - 1 < sx * 2 ^ (p + floor(log2(pn)) + 3) AND
--   sx * 2 ^ (p + floor(log2(pn)) + 3) < rx + 1
--  => Y - 1 < sx ^ pn * 2 ^ p AND sx ^ pn
theorem cauchy_power_lt1_main_generalized : Y = round ((rx * 2 ^ -(p + floor (log2 pn) + 3)) ^ pn * 2 ^ p) ∧
 rx - 1 < sx * 2 ^ (p + floor (log2 pn) + 3) ∧
  sx * 2 ^ (p + floor (log2 pn) + 3) < rx + 1 → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_main_generalized ===
  -- TCC: cauchy_power_lt1_main_generalized_TCC1
  -- Statement: FORALL (p: nat, pn: posnat, rx: rat):
  --   NOT (rx * 2 ^ -(3 + p + floor(log2(pn)))) = 0 OR pn >= 0
  have cauchy_power_lt1_main_generalized_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_main_generalized_TCC2
  -- Statement: FORALL (Y: int, p: nat, pn: posnat, rx: rat, sx: smallreal):
  --   sx * 2 ^ (p + floor(log2(pn)) + 3) < rx + 1 AND
  --    rx - 1 < sx * 2 ^ (p + floor(log2(pn)) + 3) AND
  --     Y = round((rx * 2 ^ -(p + floor...
  have cauchy_power_lt1_main_generalized_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_main
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: Y = round((X * 2 ^ -(p + floor(log2(pn)) + 3)) ^ pn * 2 ^ p) AND
--  X - 1 < sx * 2 ^ (p + floor(log2(pn)) + 3) AND
--   sx * 2 ^ (p + floor(log2(pn)) + 3) < X + 1
--  => Y - 1 < sx ^ pn * 2 ^ p AND sx ^ pn * 
theorem cauchy_power_lt1_main : Y = round ((X * 2 ^ -(p + floor (log2 pn) + 3)) ^ pn * 2 ^ p) ∧
 X - 1 < sx * 2 ^ (p + floor (log2 pn) + 3) ∧
  sx * 2 ^ (p + floor (log2 pn) + 3) < X + 1 → Y - 1 < sx ^ pn * 2 ^ p ∧ sx ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_lt1_main ===
  -- TCC: cauchy_power_lt1_main_TCC1
  -- Statement: FORALL (X: int, p: nat, pn: posnat):
  --   NOT (X * 2 ^ -(3 + p + floor(log2(pn)))) = 0 OR pn >= 0
  have cauchy_power_lt1_main_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_main_TCC2
  -- Statement: FORALL (X, Y: int, p: nat, pn: posnat, sx: smallreal):
  --   sx * 2 ^ (p + floor(log2(pn)) + 3) < X + 1 AND
  --    X - 1 < sx * 2 ^ (p + floor(log2(pn)) + 3) AND
  --     Y = round((X * 2 ^ -(p + floor(log2(pn)...
  have cauchy_power_lt1_main_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1_isreal
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cauchy_real?(LAMBDA p:
--                round((scx(p + floor(log2(pn)) + 3) *
--                        2 ^ -(p + floor(log2(pn)) + 3))
--                       ^ pn
--                       * 2 ^ p))
theorem cauchy_power_lt1_isreal : cauchy_real_p (fun (p : {i : ℝ // i ≥ 0}) => round ((scx (p + floor (log2 pn) + 3) * 2 ^ -(p + floor (log2 pn) + 3)) ^ pn * 2 ^ p)) := by
  -- === TCCs for cauchy_power_lt1_isreal ===
  -- TCC: cauchy_power_lt1_isreal_TCC1
  -- Statement: FORALL (pn: posnat, p: nat): p + floor(log2(pn)) + 3 >= 0
  have cauchy_power_lt1_isreal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_isreal_TCC2
  -- Statement: FORALL (pn: posnat, p: nat): 3 + p + floor(log2(pn)) >= 0
  have cauchy_power_lt1_isreal_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_lt1_isreal_TCC3
  -- Statement: FORALL (pn: posnat, scx: cauchy_smallreal, p: nat):
  --   NOT (scx(3 + p + floor(log2(pn))) * 2 ^ -(3 + p + floor(log2(pn)))) = 0
  --    OR pn >= 0
  have cauchy_power_lt1_isreal_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_lt1
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA p:
--    round((scx(p + floor(log2(pn)) + 3) * 2 ^ -(p + floor(log2(pn)) + 3)) ^
--           pn
--           * 2 ^ p))
axiom cauchy_power_lt1 {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6

-- pvs2lean: power.power_lemma_lt1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cauchy_prop(sx, scx) => cauchy_prop(sx ^ pn, cauchy_power_lt1(scx, pn))
theorem power_lemma_lt1 : cauchy_prop sx scx → cauchy_prop (sx ^ pn) (cauchy_power_lt1 scx pn) := by
  -- === TCCs for power_lemma_lt1 ===
  -- TCC: power_lemma_lt1_TCC1
  -- Statement: FORALL (pn: posnat, scx: cauchy_smallreal, sx: smallreal):
  --   cauchy_prop(sx, scx) IMPLIES sx /= 0 OR pn >= 0
  have power_lemma_lt1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power_div1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: X0 - 1 < x AND x < X0 + 1 AND p = floor(log2(abs(X0) + 1)) + 1 =>
--  -(2 ^ p) < x AND x < 2 ^ p
theorem cauchy_power_div1 : X0 - 1 < x ∧ x < X0 + 1 ∧ p = floor (log2 (|X0| + 1)) + 1 → -(2 ^ p) < x ∧ x < 2 ^ p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.cauchy_power_main
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (X0 - 1 < x) AND (x < X0 + 1) AND (q = 1 + floor(log2(abs(X0) + 1)))
--  AND (p1 = p + floor(log2(pn)) + 3 + pn * q)
--  AND (Y = round((X / 2 ^ p1) ^ pn * 2 ^ p)) AND (X - 1 < x * 2 ^ p1)
--  AND (x * 2 
theorem cauchy_power_main : (X0 - 1 < x) ∧ (x < X0 + 1) ∧ (q = 1 + floor (log2 (|X0| + 1)))
 ∧ (p1 = p + floor (log2 pn) + 3 + pn * q)
 ∧ (Y = round ((X / 2 ^ p1) ^ pn * 2 ^ p)) ∧ (X - 1 < x * 2 ^ p1)
 ∧ (x * 2 ^ p1 < X + 1) → Y - 1 < x ^ pn * 2 ^ p ∧ x ^ pn * 2 ^ p < 1 + Y := by
  -- === TCCs for cauchy_power_main ===
  -- TCC: cauchy_power_main_TCC1
  -- Statement: FORALL (X, X0: int, p, p1: nat, pn: posnat, q: nat, x: real):
  --   p1 = p + floor(log2(pn)) + 3 + pn * q AND
  --    q = 1 + floor(log2(abs(X0) + 1)) AND x < X0 + 1 AND X0 - 1 < x
  --    IMPLIES (X / 2 ^ p1) /...
  have cauchy_power_main_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cauchy_power_main_TCC2
  -- Statement: FORALL (X, X0, Y: int, p, p1: nat, pn: posnat, q: nat, x: real):
  --        (x * 2 ^ p1 < X + 1) AND (X - 1 < x * 2 ^ p1)
  --    AND (Y = round((X / 2 ^ p1) ^ pn * 2 ^ p))
  --    AND (p1 = p + floor(log2(pn)) ...
  have cauchy_power_main_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: power.cauchy_power_isreal
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cauchy_real?(LAMBDA p:
--                LET p1 =
--                      p + floor_log2(pn) + 3 +
--                       pn * (1 + floor_log2(abs(cx(0)) + 1))
--                  IN round((cx(p1) * 2 ^ -(p1)) 
theorem cauchy_power_isreal : cauchy_real_p (fun (p : {i : ℝ // i ≥ 0}) => fun (p1 : {i : {i : ℝ // i ≥ 0} // i > 0}) => round ((cx p1 * 2 ^ -(p1)) ^ pn * 2 ^ p) (p + floor_log2 pn + 3 + pn * (1 + floor_log2 (|cx 0| + 1)))) := by
  -- === TCCs for cauchy_power_isreal ===
  -- TCC: cauchy_power_isreal_TCC1
  -- Statement: FORALL (cx: cauchy_real, pn: posnat, p: nat, p1: posint):
  --   p1 = p + floor_log2(pn) + 3 + pn * (1 + floor_log2(abs(cx(0)) + 1))
  --    IMPLIES (cx(p1) * 2 ^ -p1) /= 0 OR pn >= 0
  have cauchy_power_isreal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: power.cauchy_power
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA p:
--    LET p1 = p + floor_log2(pn) + 3 + pn * (1 + floor_log2(abs(cx(0)) + 1))
--      IN round((cx(p1) * 2 ^ -(p1)) ^ pn * 2 ^ p))
axiom cauchy_power {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3

-- pvs2lean: power.power_lemma
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cauchy_prop(x, cx) => cauchy_prop(x ^ pn, cauchy_power(cx, pn))
theorem power_lemma : cauchy_prop x cx → cauchy_prop (x ^ pn) (cauchy_power cx pn) := by
  -- === TCCs for power_lemma ===
  -- TCC: power_lemma_TCC1
  -- Statement: FORALL (cx: cauchy_real, pn: posnat, x: real):
  --   cauchy_prop(x, cx) IMPLIES x /= 0 OR pn >= 0
  have power_lemma_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.power