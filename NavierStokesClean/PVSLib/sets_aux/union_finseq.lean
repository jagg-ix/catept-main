-- Auto-generated by pvs2lean/translator.py
-- PVS theory: union_finseq
-- PVSLib library: sets_aux
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: union_finseq

Auto-generated stubs from PVSLib theory `union_finseq`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.union_finseq

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (is_finite : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (length : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (seq : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (union : pvs_τ6 → pvs_τ7 → pvs_τ8)

-- pvs2lean: union_finseq.card_union_ineq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: finite_set[T]):
--   card[T](union(A, B)) <= card[T](A) + card[T](B)
theorem card_union_ineq : ∀ (A : T → Bool) (B : T → Bool), card (union A B) ≤ card A + card B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: union_finseq.union_finseq_rec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 OR fs`length < n THEN emptyset[T]
-- ELSE union(union_finseq_rec(fs, n - 1), fs`seq(n - 1))
-- ENDIF
axiom union_finseq_rec {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: union_finseq.union_finseq
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: union_finseq_rec(fs, fs`length)
axiom union_finseq {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12

-- pvs2lean: union_finseq.union_finseq_subset
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: T, fs: finseq[finite_set[T]]):
--   member[T](a, union_finseq(fs)) <=>
--    EXISTS (i: nat | i < fs`length):
--      member[T](a, finseq_appl[finite_set[T]](fs)(i))
theorem union_finseq_subset : ∀ (a : T) (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool)), member a (union_finseq fs) <=> ∃ (i : nat | i < fs.List.length), member a finseq_appl[finite_set[T]](fs) (i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: union_finseq.union_card_seq
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := fs`length,
--    seq
--      := LAMBDA (i: nat | i < fs`length):
--           card[T](finseq_appl[finite_set[T]](fs)(i)) #)
axiom union_card_seq {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ9 → pvs_τ10

-- pvs2lean: union_finseq.union_finseq_ineq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fs: finseq[finite_set[T]]):
--   IF fs`length = 0 THEN card[T](union_finseq[T](fs)) = 0
--   ELSE card[T](union_finseq[T](fs)) <=
--         sigma(0, fs`length - 1, finseq_appl[real](union_card_seq(fs)
theorem union_finseq_ineq : ∀ (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool)), IF (fs.List.length = 0) (card union_finseq[T](fs) = 0) (card union_finseq[T](fs) ≤
  Finset.sum (Finset.Icc 0 fs.List.length - 1) (finseq_appl (union_card_seq fs))) := by
  -- === TCCs for union_finseq_ineq ===
  -- TCC: union_finseq_ineq_TCC1
  -- Statement: FORALL (fs: finseq[finite_set[T]]):
  --   fs`length = 0 IMPLIES is_finite[T](union_finseq[T](fs))
  have union_finseq_ineq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: union_finseq_ineq_TCC2
  -- Statement: FORALL (fs: finseq[finite_set[T]]):
  --   NOT fs`length = 0 IMPLIES is_finite[T](union_finseq[T](fs))
  have union_finseq_ineq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: union_finseq_ineq_TCC3
  -- Statement: FORALL (fs: finseq[finite_set[T]]):
  --   NOT fs`length = 0 IMPLIES
  --    0 >= 0 AND 0 < length(union_card_seq(fs)) OR
  --     EXISTS (j: below[length(union_card_seq(fs))]): j <= 0
  have union_finseq_ineq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: union_finseq_ineq_TCC4
  -- Statement: FORALL (fs: finseq[finite_set[T]]):
  --   NOT fs`length = 0 IMPLIES
  --    fs`length - 1 >= 0 AND fs`length - 1 < length(union_card_seq(fs)) OR
  --     EXISTS (j: below[length(union_card_seq(fs))]): fs`length ...
  have union_finseq_ineq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: union_finseq_ineq_TCC5
  -- Statement: FORALL (fs: finseq[finite_set[T]]):
  --   NOT fs`length = 0 IMPLIES
  --    FORALL (x, y: below[length(union_card_seq(fs))]), (z: integer):
  --      x <= z AND z <= y IMPLIES z >= 0 AND z < length(union_card_se...
  have union_finseq_ineq_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: union_finseq.union_finseq_card_sum
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 OR fs`length < n THEN 0
-- ELSE card[T](finseq_appl[finite_set[T]](fs)(n - 1)) +
--       union_finseq_card_sum(fs, n - 1)
-- ENDIF
axiom union_finseq_card_sum {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15

-- pvs2lean: union_finseq.union_finseq_ineq2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fs: finseq[finite_set[T]]):
--   card[T](union_finseq[T](fs)) <= union_finseq_card_sum(fs, fs`length)
theorem union_finseq_ineq2 : ∀ (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool)), card union_finseq[T](fs) ≤ union_finseq_card_sum fs fs.List.length := by
  -- === TCCs for union_finseq_ineq2 ===
  -- TCC: union_finseq_ineq2_TCC1
  -- Statement: FORALL (fs: finseq[finite_set[T]]): is_finite[T](union_finseq[T](fs))
  have union_finseq_ineq2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.union_finseq