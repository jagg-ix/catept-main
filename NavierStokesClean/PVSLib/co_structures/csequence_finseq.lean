-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_finseq
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_finseq

Auto-generated stubs from PVSLib theory `csequence_finseq`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_finseq

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (index_p : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (indexes : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (length : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (nth : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (seq : pvs_τ15 → pvs_τ16)

-- pvs2lean: csequence_finseq.from_finseq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF fs`length = 0 THEN empty_cseq
-- ELSE add(fs`seq(0),
--          from_finseq((# length := fs`length - 1,
--                         seq
--                           := LAMBDA (x: below[fs`length - 1]):
--        
axiom from_finseq {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ4 → pvs_τ5

-- pvs2lean: csequence_finseq.from_finseq_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fs: length(from_finseq(fs)) = fs`length
theorem from_finseq_length : ∀ (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), List.length (from_finseq fs) = fs.List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_finseq.from_finseq_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fs, n: index?(from_finseq(fs))(n) IFF n < fs`length
theorem from_finseq_index : ∀ (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (n : {i : ℝ // i ≥ 0}), index_p (from_finseq fs) (n) ↔ n < fs.List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_finseq.from_finseq_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fs, (n: below[fs`length]): nth(from_finseq(fs), n) = fs`seq(n)
theorem from_finseq_nth : ∀ (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (n : {s : {i : ℝ // i ≥ 0} // s < fs.List.length}), nth (from_finseq fs) n = fs.seq n := by
  -- === TCCs for from_finseq_nth ===
  -- TCC: from_finseq_nth_TCC1
  -- Statement: FORALL (fs, n: below[fs`length]): index?[T](from_finseq(fs))(n)
  have from_finseq_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_finseq.to_finseq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := length(fseq),
--    seq := LAMBDA (x: below[length(fseq)]): nth(fseq, x) #)
axiom to_finseq {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ17 → pvs_τ18

-- pvs2lean: csequence_finseq.to_finseq_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq: to_finseq(fseq)`length = length(fseq)
theorem to_finseq_length : ∀ (fseq : csequence T), to_finseq fseq.List.length = List.length fseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_finseq.to_finseq_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, n: n < to_finseq(fseq)`length IFF index?(fseq)(n)
theorem to_finseq_index : ∀ (fseq : csequence T) (n : {i : ℝ // i ≥ 0}), n < to_finseq fseq.List.length ↔ index_p fseq(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_finseq.to_finseq_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, (n: indexes(fseq)): to_finseq(fseq)`seq(n) = nth(fseq, n)
theorem to_finseq_nth : ∀ (fseq : csequence T) (n : {i : ℝ // i ≥ 0}), to_finseq fseq.seq n = nth fseq n := by
  -- === TCCs for to_finseq_nth ===
  -- TCC: to_finseq_nth_TCC1
  -- Statement: FORALL (fseq, n: indexes[T](fseq)): n < to_finseq(fseq)`length
  have to_finseq_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_finseq.to_from_finseq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fs: to_finseq(from_finseq(fs)) = fs
theorem to_from_finseq : ∀ (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), to_finseq (from_finseq fs) = fs := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_finseq.from_to_finseq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq: from_finseq(to_finseq(fseq)) = fseq
theorem from_to_finseq : ∀ (fseq : csequence T), from_finseq (to_finseq fseq) = fseq := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_finseq