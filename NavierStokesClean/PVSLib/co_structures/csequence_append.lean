-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_append
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_append

Auto-generated stubs from PVSLib theory `csequence_append`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_append

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (coreduce : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (empty_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (every : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (first : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (index_p : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (indexes : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (inj_add : pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (insert : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} (is_finite : pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (last : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (length : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} (nonempty_p : pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} (nth : pvs_τ33 → pvs_τ34 → pvs_τ35)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (rest : pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (pvs_some : pvs_τ38 → pvs_τ39 → pvs_τ40)

-- pvs2lean: csequence_append.append_struct
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF state`3 THEN inj_empty_cseq
-- ELSIF empty?(state`1) THEN inj_add(state`2, (state`1, state`2, TRUE))
-- ELSE inj_add(first(state`1), (rest(state`1), state`2, FALSE))
-- ENDIF
noncomputable def append_struct : sorry := by
  sorry  -- pvs2lean: definition stub for IF state`3 THEN inj_empty_cseq ELSIF empty?(state`1) THEN inj_add(state`2, (stat

-- pvs2lean: csequence_append.append
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: coreduce(append_struct)(cseq, t, FALSE)
axiom append {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ4 → pvs_τ5 → Prop

-- pvs2lean: csequence_append.append_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq:
--   first(append(t, cseq)) = IF empty?(cseq) THEN t ELSE first(cseq) ENDIF
theorem append_first : ∀ (t : T) (cseq : csequence T), first (append t cseq) = if empty_p cseq then t else first cseq := by
  -- === TCCs for append_first ===
  -- TCC: append_first_TCC1
  -- Statement: FORALL (t, cseq): NOT empty?(cseq) IMPLIES nonempty?[T](cseq)
  have append_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_append.append_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, nseq: rest(append(t, nseq)) = append(t, rest(nseq))
theorem append_rest : ∀ (t : T) (nseq : csequence T), rest (append t nseq) = append t (rest nseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_append.append_finite_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, fseq: append(t, fseq) = insert(t, length(fseq), fseq)
theorem append_finite_def : ∀ (t : T) (fseq : csequence T), append t fseq = insert t (List.length fseq) fseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_append.append_infinite_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, iseq: append(t, iseq) = iseq
theorem append_infinite_def : ∀ (t : T) (iseq : {cseq : csequence T // ¬ is_finite cseq}), append t iseq = iseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_append.append_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, fseq: length(append(t, fseq)) = length(fseq) + 1
theorem append_length : ∀ (t : T) (fseq : csequence T), List.length (append t fseq) = List.length fseq + 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_append.append_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq, n:
--   index?(append(t, cseq))(n) IFF
--    (is_finite(cseq) AND n = length(cseq)) OR index?(cseq)(n)
theorem append_index : ∀ (t : T) (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p (append t cseq) (n) ↔ (is_finite cseq ∧ n = List.length cseq) ∨ index_p cseq(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_append.append_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq, (n: indexes(cseq)): nth(append(t, cseq), n) = nth(cseq, n)
theorem append_nth : ∀ (t : T) (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), nth (append t cseq) n = nth cseq n := by
  -- === TCCs for append_nth ===
  -- TCC: append_nth_TCC1
  -- Statement: FORALL (t, cseq, n: indexes[T](cseq)): index?[T](append(t, cseq))(n)
  have append_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_append.append_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, u, cseq: add(u, append(t, cseq)) = append(t, add(u, cseq))
theorem append_add : ∀ (t : T) (u : T) (cseq : csequence T), add u (append t cseq) = append t (add u cseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_append.append_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, fseq: last(append(t, fseq)) = t
theorem append_last : ∀ (t : T) (fseq : csequence T), last (append t fseq) = t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_append.append_extensionality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq1, cseq2:
--   append(t, cseq1) = append(t, cseq2) IMPLIES cseq1 = cseq2
theorem append_extensionality : ∀ (t : T) (cseq1 : csequence T) (cseq2 : csequence T), append t cseq1 = append t cseq2 → cseq1 = cseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_append.append_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq, p:
--   some(p)(append(t, cseq)) IFF some(p)(cseq) OR (is_finite(cseq) AND p(t))
theorem append_some : ∀ (t : T) (cseq : csequence T) (p : T → Bool), some p(append t cseq) ↔ some p(cseq) ∨ (is_finite cseq ∧ p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_append.append_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq, p:
--   every(p)(append(t, cseq)) IFF
--    every(p)(cseq) AND (NOT is_finite(cseq) OR p(t))
theorem append_every : ∀ (t : T) (cseq : csequence T) (p : T → Bool), every p(append t cseq) ↔ every p(cseq) ∧ (¬ is_finite cseq ∨ p t) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_append