-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_add
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_add

Auto-generated stubs from PVSLib theory `csequence_add`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_add

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (empty_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (every : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (index_p : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (indexes : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (is_finite : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (last : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (length : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (nth : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (pvs_some : pvs_τ20 → pvs_τ21 → pvs_τ22)

-- pvs2lean: csequence_add.add_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, fseq: length(add(t, fseq)) = length(fseq) + 1
theorem add_length : ∀ (t : T) (fseq : csequence T), List.length (add t fseq) = List.length fseq + 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_add.add_index
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq, n: index?(add(t, cseq))(n) IFF n = 0 OR index?(cseq)(n - 1)
theorem add_index : ∀ (t : T) (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p (add t cseq) (n) ↔ n = 0 ∨ index_p cseq(n - 1) := by
  -- === TCCs for add_index ===
  -- TCC: add_index_TCC1
  -- Statement: FORALL (t, n): NOT n = 0 IMPLIES n - 1 >= 0
  have add_index_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_add.add_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq, (n: indexes(cseq)): nth(add(t, cseq), n + 1) = nth(cseq, n)
theorem add_nth : ∀ (t : T) (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), nth (add t cseq) (n + 1) = nth cseq n := by
  -- === TCCs for add_nth ===
  -- TCC: add_nth_TCC1
  -- Statement: FORALL (t, cseq, n: indexes[T](cseq)): index?[T](add[T](t, cseq))(n + 1)
  have add_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_add.add_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, fseq:
--   last(add(t, fseq)) = IF empty?(fseq) THEN t ELSE last(fseq) ENDIF
theorem add_last : ∀ (t : T) (fseq : csequence T), last (add t fseq) = if empty_p fseq then t else last fseq := by
  -- === TCCs for add_last ===
  -- TCC: add_last_TCC1
  -- Statement: FORALL (t, fseq): NOT empty?(fseq) IMPLIES nonempty?[T](fseq)
  have add_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_add.add_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq, p: some(p)(add(t, cseq)) IFF p(t) OR some(p)(cseq)
theorem add_some : ∀ (t : T) (cseq : csequence T) (p : T → Bool), some p(add t cseq) ↔ p t ∨ some p(cseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_add.add_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq, p: every(p)(add(t, cseq)) IFF p(t) AND every(p)(cseq)
theorem add_every : ∀ (t : T) (cseq : csequence T) (p : T → Bool), every p(add t cseq) ↔ p t ∧ every p(cseq) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_add