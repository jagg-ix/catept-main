-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_split
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_split

Auto-generated stubs from PVSLib theory `csequence_split`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_split

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (ceiling : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (coreduce : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (empty_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} (even_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (every : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (first : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (floor : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (index_p : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (indexes : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (inj_add : pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} (last : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (length : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} (nonempty_p : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (nth : pvs_τ27 → pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} (odd_p : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} (rest : pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (pvs_some : pvs_τ32 → pvs_τ33 → pvs_τ34)

-- pvs2lean: csequence_split.split_left_struct
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(cseq) THEN inj_empty_cseq
-- ELSE inj_add(first(cseq),
--              IF empty?(rest(cseq)) THEN empty_cseq
--              ELSE rest(rest(cseq))
--              ENDIF)
-- ENDIF
noncomputable def split_left_struct : sorry := by
  sorry  -- pvs2lean: definition stub for IF empty?(cseq) THEN inj_empty_cseq ELSE inj_add(first(cseq),              IF em

-- pvs2lean: csequence_split.split_right_struct
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(cseq) OR empty?(rest(cseq)) THEN inj_empty_cseq
-- ELSE inj_add(first(rest(cseq)), rest(rest(cseq)))
-- ENDIF
noncomputable def split_right_struct : sorry := by
  sorry  -- pvs2lean: definition stub for IF empty?(cseq) OR empty?(rest(cseq)) THEN inj_empty_cseq ELSE inj_add(first(res

-- pvs2lean: csequence_split.split
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (coreduce(split_left_struct)(cseq), coreduce(split_right_struct)(cseq))
axiom split {pvs_τ35 : Type*} : pvs_τ35 → Prop

-- pvs2lean: csequence_split.split_empty_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: empty?(split(cseq)`1) IFF empty?(cseq)
theorem split_empty_left : ∀ (cseq : csequence T), empty_p (split cseq.1) ↔ empty_p cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_empty_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: empty?(split(cseq)`2) IFF empty?(cseq) OR empty?(rest(cseq))
theorem split_empty_right : ∀ (cseq : csequence T), empty_p (split cseq.2) ↔ empty_p cseq ∨ empty_p (rest cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_nonempty_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: nonempty?(split(cseq)`1) IFF nonempty?(cseq)
theorem split_nonempty_left : ∀ (cseq : csequence T), nonempty_p (split cseq.1) ↔ nonempty_p cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_nonempty_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   nonempty?(split(cseq)`2) IFF nonempty?(cseq) AND nonempty?(rest(cseq))
theorem split_nonempty_right : ∀ (cseq : csequence T), nonempty_p (split cseq.2) ↔ nonempty_p cseq ∧ nonempty_p (rest cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_first_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq: first(split(nseq)`1) = first(nseq)
theorem split_first_left : ∀ (nseq : csequence T), first (split nseq.1) = first nseq := by
  -- === TCCs for split_first_left ===
  -- TCC: split_first_left_TCC1
  -- Statement: FORALL (nseq): nonempty?[T](split(nseq)`1)
  have split_first_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_first_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq:
--   nonempty?(rest(nseq)) IMPLIES first(split(nseq)`2) = first(rest(nseq))
theorem split_first_right : ∀ (nseq : csequence T), nonempty_p (rest nseq) → first (split nseq.2) = first (rest nseq) := by
  -- === TCCs for split_first_right ===
  -- TCC: split_first_right_TCC1
  -- Statement: FORALL (nseq): nonempty?(rest(nseq)) IMPLIES nonempty?[T](split(nseq)`2)
  have split_first_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_rest_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq:
--   rest(split(nseq)`1) =
--    IF empty?(rest(nseq)) THEN empty_cseq
--    ELSE split(rest(rest(nseq)))`1
--    ENDIF
theorem split_rest_left : ∀ (nseq : csequence T), rest (split nseq.1) = if empty_p (rest nseq) then empty_cseq else split (rest (rest nseq)).1 := by
  -- === TCCs for split_rest_left ===
  -- TCC: split_rest_left_TCC1
  -- Statement: FORALL (nseq): NOT empty?(rest(nseq)) IMPLIES nonempty?[T](rest[T](nseq))
  have split_rest_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_rest_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq:
--   nonempty?(rest(nseq)) IMPLIES
--    rest(split(nseq)`2) = split(rest(rest(nseq)))`2
theorem split_rest_right : ∀ (nseq : csequence T), nonempty_p (rest nseq) → rest (split nseq.2) = split (rest (rest nseq)).2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_rest_swap_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq: split(rest(nseq))`1 = split(nseq)`2
theorem split_rest_swap_left : ∀ (nseq : csequence T), split (rest nseq).1 = split nseq.2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_rest_swap_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq: split(rest(nseq))`2 = rest(split(nseq)`1)
theorem split_rest_swap_right : ∀ (nseq : csequence T), split (rest nseq).2 = rest (split nseq.1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_length_left
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq: length(split(fseq)`1) = ceiling(length(fseq) / 2)
theorem split_length_left : ∀ (fseq : csequence T), List.length (split fseq.1) = ceiling (List.length fseq / 2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_length_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq: length(split(fseq)`2) = floor(length(fseq) / 2)
theorem split_length_right : ∀ (fseq : csequence T), List.length (split fseq.2) = floor (List.length fseq / 2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq: length(fseq) = length(split(fseq)`1) + length(split(fseq)`2)
theorem split_length : ∀ (fseq : csequence T), List.length fseq = List.length (split fseq.1) + List.length (split fseq.2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_index_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n: index?(split(cseq)`1)(n) IFF index?(cseq)(2 * n)
theorem split_index_left : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p (split cseq.1) (n) ↔ index_p cseq(2 * n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_index_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n: index?(split(cseq)`2)(n) IFF index?(cseq)(2 * n + 1)
theorem split_index_right : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p (split cseq.2) (n) ↔ index_p cseq(2 * n + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_nth_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, (n: indexes(split(cseq)`1)):
--   nth(split(cseq)`1, n) = nth(cseq, 2 * n)
theorem split_nth_left : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), nth (split cseq.1) n = nth cseq (2 * n) := by
  -- === TCCs for split_nth_left ===
  -- TCC: split_nth_left_TCC1
  -- Statement: FORALL (cseq, n: indexes[T](split(cseq)`1)): index?[T](cseq)(2 * n)
  have split_nth_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_nth_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, (n: indexes(split(cseq)`2)):
--   nth(split(cseq)`2, n) = nth(cseq, 2 * n + 1)
theorem split_nth_right : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), nth (split cseq.2) n = nth cseq (2 * n + 1) := by
  -- === TCCs for split_nth_right ===
  -- TCC: split_nth_right_TCC1
  -- Statement: FORALL (cseq, n: indexes[T](split(cseq)`2)): index?[T](cseq)(2 * n + 1)
  have split_nth_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, t: split(add(t, cseq)) = (add(t, split(cseq)`2), split(cseq)`1)
theorem split_add : ∀ (cseq : csequence T) (t : T), split (add t cseq) = (add t (split cseq.2), split cseq.1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_last_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq:
--   nonempty?(split(fseq)`1) IMPLIES
--    last(split(fseq)`1) =
--     IF odd?(length(fseq)) THEN last(fseq)
--     ELSE nth(fseq, length(fseq) - 2)
--     ENDIF
theorem split_last_left : ∀ (fseq : csequence T), nonempty_p (split fseq.1) → last (split fseq.1) =
 if odd_p (List.length fseq) then last fseq
 else nth fseq (List.length fseq - 2) := by
  -- === TCCs for split_last_left ===
  -- TCC: split_last_left_TCC1
  -- Statement: FORALL (fseq):
  --   nonempty?(split(fseq)`1) AND odd?(length(fseq)) IMPLIES nonempty?[T](fseq)
  have split_last_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: split_last_left_TCC2
  -- Statement: FORALL (fseq):
  --   nonempty?(split(fseq)`1) AND NOT odd?(length(fseq)) IMPLIES
  --    length[T](fseq) - 2 >= 0 AND index?[T](fseq)(length[T](fseq) - 2)
  have split_last_left_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_last_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq:
--   nonempty?(split(fseq)`2) IMPLIES
--    last(split(fseq)`2) =
--     IF even?(length(fseq)) THEN last(fseq)
--     ELSE nth(fseq, length(fseq) - 2)
--     ENDIF
theorem split_last_right : ∀ (fseq : csequence T), nonempty_p (split fseq.2) → last (split fseq.2) =
 if even_p (List.length fseq) then last fseq
 else nth fseq (List.length fseq - 2) := by
  -- === TCCs for split_last_right ===
  -- TCC: split_last_right_TCC1
  -- Statement: FORALL (fseq):
  --   nonempty?(split(fseq)`2) AND even?(length(fseq)) IMPLIES
  --    nonempty?[T](fseq)
  have split_last_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: split_last_right_TCC2
  -- Statement: FORALL (fseq):
  --   nonempty?(split(fseq)`2) AND NOT even?(length(fseq)) IMPLIES
  --    length[T](fseq) - 2 >= 0 AND index?[T](fseq)(length[T](fseq) - 2)
  have split_last_right_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_split.split_extensionality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2: split(cseq1) = split(cseq2) IMPLIES cseq1 = cseq2
theorem split_extensionality : ∀ (cseq1 : csequence T) (cseq2 : csequence T), split cseq1 = split cseq2 → cseq1 = cseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, p:
--   some(p)(cseq) IFF some(p)(split(cseq)`1) OR some(p)(split(cseq)`2)
theorem split_some : ∀ (cseq : csequence T) (p : T → Bool), some p(cseq) ↔ some p(split cseq.1) ∨ some p(split cseq.2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_split.split_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, p:
--   every(p)(cseq) IFF every(p)(split(cseq)`1) AND every(p)(split(cseq)`2)
theorem split_every : ∀ (cseq : csequence T) (p : T → Bool), every p(cseq) ↔ every p(split cseq.1) ∧ every p(split cseq.2) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_split