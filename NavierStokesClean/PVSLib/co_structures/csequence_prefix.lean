-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_prefix
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_prefix

Auto-generated stubs from PVSLib theory `csequence_prefix`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_prefix

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (empty_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (every : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} (first : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (index_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (indexes : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (is_finite : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (length : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (min : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} (nonempty_p : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (nth : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (partial_order_p : pvs_τ20 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (rest : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (restrict : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (pvs_some : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} (total_order_p : pvs_τ33 → Prop)

-- pvs2lean: csequence_prefix.prefix?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty?(cseq1) OR
--  (nonempty?(cseq2) AND
--    first(cseq1) = first(cseq2) AND prefix?(rest(cseq1), rest(cseq2)))
axiom prefix_p {pvs_τ24 : Type*} {pvs_τ25 : Type*} : pvs_τ24 → pvs_τ25 → Prop

-- pvs2lean: csequence_prefix.prefix?_weak_coinduction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [[csequence[T], csequence[T]] -> boolean]):
--   (FORALL (cseq1, cseq2: csequence[T]):
--      P(cseq1, cseq2) IMPLIES
--       empty?(cseq1) OR
--        (nonempty?(cseq2) AND
--          first(cseq1) = 
theorem prefix_p_weak_coinduction : ∀ (P : csequence T × csequence T → Bool), ∀ (cseq1 : _) (cseq2 : csequence T), P cseq1 cseq2 →
   empty_p cseq1 ∨
    (nonempty_p cseq2 ∧
      first cseq1 = first cseq2 ∧ P (rest cseq1) (rest cseq2)) → ∀ (cseq1 : _) (cseq2 : csequence T), P cseq1 cseq2 → prefix_p cseq1 cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_coinduction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [[csequence[T], csequence[T]] -> boolean]):
--   (FORALL (cseq1, cseq2: csequence[T]):
--      P(cseq1, cseq2) IMPLIES
--       empty?(cseq1) OR
--        (nonempty?(cseq2) AND
--          first(cseq1) = 
theorem prefix_p_coinduction : ∀ (P : csequence T × csequence T → Bool), ∀ (cseq1 : _) (cseq2 : csequence T), P cseq1 cseq2 →
   empty_p cseq1 ∨
    (nonempty_p cseq2 ∧
      first cseq1 = first cseq2 ∧
       (prefix_p (rest cseq1) (rest cseq2) ∨ P (rest cseq1) (rest cseq2))) → ∀ (cseq1 : _) (cseq2 : csequence T), P cseq1 cseq2 → prefix_p cseq1 cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, fseq: prefix?(cseq, fseq) IMPLIES is_finite(cseq)
theorem prefix_p_finite : ∀ (cseq : csequence T) (fseq : csequence T), prefix_p cseq fseq → is_finite cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_infinite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL iseq, cseq: prefix?(iseq, cseq) IFF iseq = cseq
theorem prefix_p_infinite : ∀ (iseq : {cseq : csequence T // ¬ is_finite cseq}) (cseq : csequence T), prefix_p iseq cseq ↔ iseq = cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, eseq: prefix?(cseq, eseq) IFF empty?(cseq)
theorem prefix_p_empty : ∀ (cseq : csequence T) (eseq : csequence T), prefix_p cseq eseq ↔ empty_p cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_empty_is_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL eseq, cseq: prefix?(eseq, cseq)
theorem prefix_p_empty_is_prefix : ∀ (eseq : csequence T) (cseq : csequence T), prefix_p eseq cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq1, nseq2:
--   prefix?(nseq1, nseq2) IMPLIES first(nseq1) = first(nseq2)
theorem prefix_p_first : ∀ (nseq1 : csequence T) (nseq2 : csequence T), prefix_p nseq1 nseq2 → first nseq1 = first nseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq1, nseq2:
--   prefix?(nseq1, nseq2) IMPLIES prefix?(rest(nseq1), rest(nseq2))
theorem prefix_p_rest : ∀ (nseq1 : csequence T) (nseq2 : csequence T), prefix_p nseq1 nseq2 → prefix_p (rest nseq1) (rest nseq2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq1, fseq2:
--   prefix?(fseq1, fseq2) IMPLIES length(fseq1) <= length(fseq2)
theorem prefix_p_length : ∀ (fseq1 : csequence T) (fseq2 : csequence T), prefix_p fseq1 fseq2 → List.length fseq1 ≤ List.length fseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_length_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq1, fseq2:
--   prefix?(fseq1, fseq2) AND length(fseq1) = length(fseq2) IMPLIES
--    fseq1 = fseq2
theorem prefix_p_length_eq : ∀ (fseq1 : csequence T) (fseq2 : csequence T), prefix_p fseq1 fseq2 ∧ List.length fseq1 = List.length fseq2 → fseq1 = fseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, n:
--   prefix?(cseq1, cseq2) AND index?(cseq1)(n) IMPLIES index?(cseq2)(n)
theorem prefix_p_index : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), prefix_p cseq1 cseq2 ∧ index_p cseq1(n) → index_p cseq2(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, (n: indexes(cseq1)):
--   prefix?(cseq1, cseq2) IMPLIES nth(cseq1, n) = nth(cseq2, n)
theorem prefix_p_nth : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), prefix_p cseq1 cseq2 → nth cseq1 n = nth cseq2 n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_concatenate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2: prefix?(cseq1, cseq1 o cseq2)
theorem prefix_p_concatenate : ∀ (cseq1 : csequence T) (cseq2 : csequence T), prefix_p cseq1 (cseq1 ∘ cseq2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   prefix?(cseq1, cseq2) IFF (EXISTS cseq: cseq1 o cseq = cseq2)
theorem prefix_p_def : ∀ (cseq1 : csequence T) (cseq2 : csequence T), prefix_p cseq1 cseq2 ↔ (∃ cseq,  cseq1 ∘ cseq = cseq2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_total_order
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   total_order?(restrict
--                    [[csequence, csequence],
--                     [(LAMBDA (cseq1: csequence[T]): prefix?(cseq1, cseq)),
--                      (LAMBDA (cseq1: csequen
theorem prefix_p_total_order : ∀ (cseq : csequence T), total_order_p (restrict prefix_p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 OR empty?(cseq) THEN empty_cseq
-- ELSE add(first(cseq), prefix(rest(cseq), n - 1))
-- ENDIF
axiom pvs_prefix {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ21 → pvs_τ22 → pvs_τ23

-- pvs2lean: csequence_prefix.prefix_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: empty?(prefix(cseq, 0))
theorem prefix_0 : ∀ (cseq : csequence T), empty_p (prefix cseq 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_extract
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n: prefix(cseq, n + 1) = cseq ^ (0, n)
theorem prefix_extract : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), prefix cseq (n + 1) = cseq ^ (0, n) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq, n: prefix(rest(nseq), n) = rest(prefix(nseq, n + 1))
theorem prefix_rest : ∀ (nseq : csequence T) (n : {i : ℝ // i ≥ 0}), prefix (rest nseq) n = rest (prefix nseq (n + 1)) := by
  -- === TCCs for prefix_rest ===
  -- TCC: prefix_rest_TCC1
  -- Statement: FORALL (nseq, n): nonempty?[T](prefix(nseq, n + 1))
  have prefix_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_prefix
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n, m: prefix(prefix(cseq, n), m) = prefix(cseq, min(n, m))
theorem prefix_prefix : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}), prefix (prefix cseq n) m = prefix cseq (min n m) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n:
--   length(prefix(cseq, n)) =
--    IF is_finite(cseq) THEN min(n, length(cseq)) ELSE n ENDIF
theorem prefix_length : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), List.length (prefix cseq n) = if is_finite cseq then min n (List.length cseq) else n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n, m: index?(prefix(cseq, n))(m) IFF index?(cseq)(m) AND m < n
theorem prefix_index : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}), index_p (prefix cseq n) (m) ↔ index_p cseq(m) ∧ m < n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_full
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n: index?(cseq)(n) OR prefix(cseq, n) = cseq
theorem prefix_full : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p cseq(n) ∨ prefix cseq n = cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_concatenate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, n:
--   prefix(cseq1 o cseq2, n) =
--    IF index?(cseq1)(n) THEN prefix(cseq1, n)
--    ELSE cseq1 o prefix(cseq2, n - length(cseq1))
--    ENDIF
theorem prefix_concatenate : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), prefix (cseq1 ∘ cseq2) n = if index_p cseq1(n) then prefix cseq1 n
else cseq1 ∘ prefix cseq2 (n - List.length cseq1) := by
  -- === TCCs for prefix_concatenate ===
  -- TCC: prefix_concatenate_TCC1
  -- Statement: FORALL (cseq1, n): NOT index?(cseq1)(n) IMPLIES is_finite[T](cseq1)
  have prefix_concatenate_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: prefix_concatenate_TCC2
  -- Statement: FORALL (cseq1, n): NOT index?(cseq1)(n) IMPLIES n - length[T](cseq1) >= 0
  have prefix_concatenate_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   prefix?(cseq1, cseq2) IFF
--    cseq1 = cseq2 OR (EXISTS n: cseq1 = prefix(cseq2, n))
theorem prefix_p_prefix : ∀ (cseq1 : csequence T) (cseq2 : csequence T), prefix_p cseq1 cseq2 ↔ cseq1 = cseq2 ∨ (∃ n,  cseq1 = prefix cseq2 n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n, p:
--   some(p)(prefix(cseq, n)) IFF
--    (EXISTS (i: indexes(cseq)): i < n AND p(nth(cseq, i)))
theorem prefix_some : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (p : T → Bool), some p(prefix cseq n) ↔ (∃ (i : indexes cseq), i < n ∧ p (nth cseq i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n, p:
--   every(p)(prefix(cseq, n)) IFF
--    (FORALL (i: indexes(cseq)): i < n IMPLIES p(nth(cseq, i)))
theorem prefix_every : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (p : T → Bool), every p(prefix cseq n) ↔ (∀ (i : indexes cseq), i < n → p (nth cseq i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_prefix.prefix?_order
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n, m: n <= m IMPLIES prefix?(prefix(cseq, n), prefix(cseq, m))
theorem prefix_p_order : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}), n ≤ m → prefix_p (prefix cseq n) (prefix cseq m) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_prefix