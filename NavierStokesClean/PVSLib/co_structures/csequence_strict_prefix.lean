-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_strict_prefix
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_strict_prefix

Auto-generated stubs from PVSLib theory `csequence_strict_prefix`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_strict_prefix

noncomputable section

variable {pvs_τ1 : Type*} (empty_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} (first : pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (index_p : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (indexes : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (is_finite : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (length : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (nonempty_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (nth : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (pvs_prefix : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (prefix_p : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (rest : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (restrict : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} (strict_order_p : pvs_τ21 → Prop)
variable {pvs_τ24 : Type*} (well_ordered_p : pvs_τ24 → Prop)

-- pvs2lean: csequence_strict_prefix.strict_prefix?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(cseq2) AND
--  (empty?(cseq1) OR
--    (first(cseq1) = first(cseq2) AND
--      strict_prefix?(rest(cseq1), rest(cseq2))))
axiom strict_prefix_p {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ22 → pvs_τ23 → Prop

-- pvs2lean: csequence_strict_prefix.strict_prefix?_weak_induction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [[csequence[T], csequence[T]] -> boolean]):
--   (FORALL (cseq1, cseq2: csequence[T]):
--      nonempty?(cseq2) AND
--       (empty?(cseq1) OR
--         (first(cseq1) = first(cseq2) AND P(rest(cseq1),
theorem strict_prefix_p_weak_induction : ∀ (P : csequence T × csequence T → Bool), ∀ (cseq1 : _) (cseq2 : csequence T), nonempty_p cseq2 ∧
   (empty_p cseq1 ∨
     (first cseq1 = first cseq2 ∧ P (rest cseq1) (rest cseq2)))
   → P cseq1 cseq2 → ∀ (cseq1 : _) (cseq2 : csequence T), strict_prefix_p cseq1 cseq2 → P cseq1 cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_induction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [[csequence[T], csequence[T]] -> boolean]):
--   (FORALL (cseq1, cseq2: csequence[T]):
--      nonempty?(cseq2) AND
--       (empty?(cseq1) OR
--         (first(cseq1) = first(cseq2) AND
--           stri
theorem strict_prefix_p_induction : ∀ (P : csequence T × csequence T → Bool), ∀ (cseq1 : _) (cseq2 : csequence T), nonempty_p cseq2 ∧
   (empty_p cseq1 ∨
     (first cseq1 = first cseq2 ∧
       strict_prefix_p (rest cseq1) (rest cseq2) ∧
        P (rest cseq1) (rest cseq2)))
   → P cseq1 cseq2 → ∀ (cseq1 : _) (cseq2 : csequence T), strict_prefix_p cseq1 cseq2 → P cseq1 cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_prefix?
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   strict_prefix?(cseq1, cseq2) IFF prefix?(cseq1, cseq2) AND cseq1 /= cseq2
theorem strict_prefix_p_prefix_p : ∀ (cseq1 : csequence T) (cseq2 : csequence T), strict_prefix_p cseq1 cseq2 ↔ prefix_p cseq1 cseq2 ∧ cseq1 ≠ cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2: strict_prefix?(cseq1, cseq2) IMPLIES is_finite(cseq1)
theorem strict_prefix_p_finite : ∀ (cseq1 : csequence T) (cseq2 : csequence T), strict_prefix_p cseq1 cseq2 → is_finite cseq1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq, cseq:
--   strict_prefix?(nseq, cseq) IMPLIES first(nseq) = first(cseq)
theorem strict_prefix_p_first : ∀ (nseq : csequence T) (cseq : csequence T), strict_prefix_p nseq cseq → first nseq = first cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq, cseq:
--   strict_prefix?(nseq, cseq) IMPLIES strict_prefix?(rest(nseq), rest(cseq))
theorem strict_prefix_p_rest : ∀ (nseq : csequence T) (cseq : csequence T), strict_prefix_p nseq cseq → strict_prefix_p (rest nseq) (rest cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, fseq:
--   strict_prefix?(cseq, fseq) IMPLIES length(cseq) < length(fseq)
theorem strict_prefix_p_length : ∀ (cseq : csequence T) (fseq : csequence T), strict_prefix_p cseq fseq → List.length cseq < List.length fseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, n:
--   strict_prefix?(cseq1, cseq2) AND index?(cseq1)(n) IMPLIES index?(cseq2)(n)
theorem strict_prefix_p_index : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), strict_prefix_p cseq1 cseq2 ∧ index_p cseq1(n) → index_p cseq2(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, (n: indexes(cseq1)):
--   strict_prefix?(cseq1, cseq2) IMPLIES nth(cseq1, n) = nth(cseq2, n)
theorem strict_prefix_p_nth : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), strict_prefix_p cseq1 cseq2 → nth cseq1 n = nth cseq2 n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_concatenate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, nseq: strict_prefix?(fseq, fseq o nseq)
theorem strict_prefix_p_concatenate : ∀ (fseq : csequence T) (nseq : csequence T), strict_prefix_p fseq (fseq ∘ nseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   strict_prefix?(cseq1, cseq2) IFF
--    is_finite(cseq1) AND (EXISTS nseq: cseq1 o nseq = cseq2)
theorem strict_prefix_p_def : ∀ (cseq1 : csequence T) (cseq2 : csequence T), strict_prefix_p cseq1 cseq2 ↔ is_finite cseq1 ∧ (∃ nseq,  cseq1 ∘ nseq = cseq2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_well_ordered
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   well_ordered?(restrict
--                     [[csequence, csequence],
--                      [(LAMBDA (cseq1: csequence[T]):
--                          strict_prefix?(cseq1, cseq)),
--          
theorem strict_prefix_p_well_ordered : ∀ (cseq : csequence T), well_ordered_p (restrict strict_prefix_p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_strict_prefix.strict_prefix?_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   strict_prefix?(cseq1, cseq2) IFF
--    (EXISTS n:
--       cseq1 = prefix(cseq2, n) AND
--        (is_finite(cseq2) IMPLIES n < length(cseq2)))
theorem strict_prefix_p_prefix : ∀ (cseq1 : csequence T) (cseq2 : csequence T), strict_prefix_p cseq1 cseq2 ↔ (∃ n, 
   cseq1 = prefix cseq2 n ∧
    (is_finite cseq2 → n < List.length cseq2)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_strict_prefix