-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_remove
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_remove

Auto-generated stubs from PVSLib theory `csequence_remove`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_remove

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (empty_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (every : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} (first : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (index_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (indexes : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (is_finite : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (last : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (length : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} (nonempty_p : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (nth : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ21 : Type*} (rest : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (pvs_some : pvs_τ22 → pvs_τ23 → pvs_τ24)

-- pvs2lean: csequence_remove.remove
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(cseq) THEN cseq
-- ELSIF index = 0 THEN rest(cseq)
-- ELSE add(first(cseq), remove(rest(cseq), index - 1))
-- ENDIF
axiom remove {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: csequence_remove.remove_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq: remove(nseq, 0) = rest(nseq)
theorem remove_0 : ∀ (nseq : csequence T), remove nseq 0 = rest nseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_remove.remove_empty
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, index:
--   empty?(remove(cseq, index)) IFF
--    empty?(cseq) OR (index = 0 AND empty?(rest(cseq)))
theorem remove_empty : ∀ (cseq : csequence T) (index : {i : ℝ // i ≥ 0}), empty_p (remove cseq index) ↔ empty_p cseq ∨ (index = 0 ∧ empty_p (rest cseq)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_remove.remove_nonempty
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, index:
--   nonempty?(remove(cseq, index)) IFF
--    nonempty?(cseq) AND (index > 0 OR nonempty?(rest(cseq)))
theorem remove_nonempty : ∀ (cseq : csequence T) (index : {i : ℝ // i ≥ 0}), nonempty_p (remove cseq index) ↔ nonempty_p cseq ∧ (index > 0 ∨ nonempty_p (rest cseq)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_remove.remove_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, index:
--   nonempty?(remove(cseq, index)) IMPLIES
--    first(remove(cseq, index)) =
--     IF index = 0 THEN first(rest(cseq)) ELSE first(cseq) ENDIF
theorem remove_first : ∀ (cseq : csequence T) (index : {i : ℝ // i ≥ 0}), nonempty_p (remove cseq index) → first (remove cseq index) =
 if index = 0 then first (rest cseq) else first cseq := by
  -- === TCCs for remove_first ===
  -- TCC: remove_first_TCC1
  -- Statement: FORALL (cseq, index):
  --   nonempty?(remove(cseq, index)) AND index = 0 IMPLIES nonempty?[T](cseq)
  have remove_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_first_TCC2
  -- Statement: FORALL (cseq, index):
  --   nonempty?(remove(cseq, index)) AND index = 0 IMPLIES
  --    nonempty?[T](rest[T](cseq))
  have remove_first_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_first_TCC3
  -- Statement: FORALL (cseq, index):
  --   nonempty?(remove(cseq, index)) AND NOT index = 0 IMPLIES
  --    nonempty?[T](cseq)
  have remove_first_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_remove.remove_rest
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq, index:
--   nonempty?(remove(nseq, index)) IMPLIES
--    rest(remove(nseq, index)) =
--     IF index = 0 THEN rest(rest(nseq))
--     ELSE remove(rest(nseq), index - 1)
--     ENDIF
theorem remove_rest : ∀ (nseq : csequence T) (index : {i : ℝ // i ≥ 0}), nonempty_p (remove nseq index) → rest (remove nseq index) =
 if index = 0 then rest (rest nseq) else remove (rest nseq) (index - 1) := by
  -- === TCCs for remove_rest ===
  -- TCC: remove_rest_TCC1
  -- Statement: FORALL (nseq, index):
  --   nonempty?(remove(nseq, index)) AND index = 0 IMPLIES
  --    nonempty?[T](rest[T](nseq))
  have remove_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_rest_TCC2
  -- Statement: FORALL (nseq, index):
  --   nonempty?(remove(nseq, index)) AND NOT index = 0 IMPLIES index - 1 >= 0
  have remove_rest_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_remove.remove_upfrom_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, (index: upfrom[length(fseq)]): remove(fseq, index) = fseq
theorem remove_upfrom_length : ∀ (fseq : csequence T) (index : {s : ℝ // s ≥ List.length fseq}), remove fseq index = fseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_remove.remove_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, index:
--   length(remove(fseq, index)) =
--    length(fseq) - IF index?(fseq)(index) THEN 1 ELSE 0 ENDIF
theorem remove_length : ∀ (fseq : csequence T) (index : {i : ℝ // i ≥ 0}), List.length (remove fseq index) = List.length fseq - if index_p fseq(index) then 1 else 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_remove.remove_index
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL index, cseq, n:
--   index?(remove(cseq, index))(n) IFF
--    index?(cseq)(n) AND
--     NOT (is_finite(cseq) AND n = length(cseq) - 1 AND index?(cseq)(index))
theorem remove_index : ∀ (index : {i : ℝ // i ≥ 0}) (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p (remove cseq index) (n) ↔ index_p cseq(n) ∧
 ¬ (is_finite cseq ∧ n = List.length cseq - 1 ∧ index_p cseq(index)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_remove.remove_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n, (m: indexes(remove(cseq, n))):
--   nth(remove(cseq, n), m) = nth(cseq, m + IF m < n THEN 0 ELSE 1 ENDIF)
theorem remove_nth : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}), nth (remove cseq n) m = nth cseq (m + if m < n then 0 else 1) := by
  -- === TCCs for remove_nth ===
  -- TCC: remove_nth_TCC1
  -- Statement: FORALL (cseq, n, m: indexes[T](remove(cseq, n))):
  --   index?[T](cseq)(m + IF m < n THEN 0 ELSE 1 ENDIF)
  have remove_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_remove.remove_add
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, index, t:
--   add(t, remove(cseq, index)) = remove(add(t, cseq), index + 1)
theorem remove_add : ∀ (cseq : csequence T) (index : {i : ℝ // i ≥ 0}) (t : T), add t (remove cseq index) = remove (add t cseq) (index + 1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_remove.remove_last
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, index:
--   nonempty?(remove(fseq, index)) IMPLIES
--    last(remove(fseq, index)) =
--     IF index = length(fseq) - 1 THEN nth(fseq, index - 1)
--     ELSE last(fseq)
--     ENDIF
theorem remove_last : ∀ (fseq : csequence T) (index : {i : ℝ // i ≥ 0}), nonempty_p (remove fseq index) → last (remove fseq index) =
 if index = List.length fseq - 1 then nth fseq (index - 1) else last fseq := by
  -- === TCCs for remove_last ===
  -- TCC: remove_last_TCC1
  -- Statement: FORALL (fseq, index):
  --   nonempty?(remove(fseq, index)) AND index = length(fseq) - 1 IMPLIES
  --    index - 1 >= 0 AND index?[T](fseq)(index - 1)
  have remove_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_last_TCC2
  -- Statement: FORALL (fseq, index):
  --   nonempty?(remove(fseq, index)) AND NOT index = length(fseq) - 1 IMPLIES
  --    nonempty?[T](fseq)
  have remove_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_remove.remove_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n, m:
--   remove(remove(cseq, n), m) =
--    IF n <= m THEN remove(remove(cseq, m + 1), n)
--    ELSE remove(remove(cseq, m), n - 1)
--    ENDIF
theorem remove_remove : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}), remove (remove cseq n) m = if n ≤ m then remove (remove cseq (m + 1)) n
else remove (remove cseq m) (n - 1) := by
  -- === TCCs for remove_remove ===
  -- TCC: remove_remove_TCC1
  -- Statement: FORALL (n, m): NOT n <= m IMPLIES n - 1 >= 0
  have remove_remove_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_remove.remove_extensionality
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, n:
--   remove(cseq1, n) = remove(cseq2, n) AND
--    ((NOT index?(cseq1)(n) AND NOT index?(cseq2)(n)) OR
--      (index?(cseq1)(n) AND
--        index?(cseq2)(n) AND nth(cseq1, n) = nth(cseq
theorem remove_extensionality : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), remove cseq1 n = remove cseq2 n ∧
 ((¬ index_p cseq1(n) ∧ ¬ index_p cseq2(n)) ∨
   (index_p cseq1(n) ∧
     index_p cseq2(n) ∧ nth cseq1 n = nth cseq2 n)) → cseq1 = cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_remove.remove_some
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, index, p:
--   some(p)(remove(cseq, index)) IFF
--    (EXISTS (n: indexes(cseq)): n /= index AND p(nth(cseq, n)))
theorem remove_some : ∀ (cseq : csequence T) (index : {i : ℝ // i ≥ 0}) (p : T → Bool), some p(remove cseq index) ↔ (∃ (n : indexes cseq), n ≠ index ∧ p (nth cseq n)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_remove.remove_every
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, index, p:
--   every(p)(remove(cseq, index)) IFF
--    (FORALL (n: indexes(cseq)): n = index OR p(nth(cseq, n)))
theorem remove_every : ∀ (cseq : csequence T) (index : {i : ℝ // i ≥ 0}) (p : T → Bool), every p(remove cseq index) ↔ (∀ (n : indexes cseq), n = index ∨ p (nth cseq n)) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_remove