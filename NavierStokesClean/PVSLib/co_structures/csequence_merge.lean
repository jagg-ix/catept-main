-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_merge
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_merge

Auto-generated stubs from PVSLib theory `csequence_merge`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_merge

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (coreduce : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (empty_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (even_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (every : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (first : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (index_p : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (indexes : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (inj_add : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} (integer_pred : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} (is_finite : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (last : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} (length : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (length_eq : pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (length_lt : pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ30 : Type*} (nonempty_p : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (nth : pvs_τ31 → pvs_τ32 → pvs_τ33)
variable {pvs_τ34 : Type*} (rest : pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (pvs_some : pvs_τ35 → pvs_τ36 → pvs_τ37)

-- pvs2lean: csequence_merge.merge_struct
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(state`1)
--   THEN IF empty?(state`2) THEN inj_empty_cseq
--        ELSE inj_add(first(state`2), (rest(state`2), state`1))
--        ENDIF
-- ELSE inj_add(first(state`1), (state`2, rest(state`1)))
-- ENDIF
noncomputable def merge_struct : sorry := by
  sorry  -- pvs2lean: definition stub for IF empty?(state`1)   THEN IF empty?(state`2) THEN inj_empty_cseq        ELSE inj

-- pvs2lean: csequence_merge.merge
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: coreduce(merge_struct)(cseq1, cseq2)
axiom merge {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ28 → pvs_τ29 → Prop

-- pvs2lean: csequence_merge.merge_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   empty?(merge(cseq1, cseq2)) IFF empty?(cseq1) AND empty?(cseq2)
theorem merge_empty : ∀ (cseq1 : csequence T) (cseq2 : csequence T), empty_p (merge cseq1 cseq2) ↔ empty_p cseq1 ∧ empty_p cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   nonempty?(merge(cseq1, cseq2)) IFF nonempty?(cseq1) OR nonempty?(cseq2)
theorem merge_nonempty : ∀ (cseq1 : csequence T) (cseq2 : csequence T), nonempty_p (merge cseq1 cseq2) ↔ nonempty_p cseq1 ∨ nonempty_p cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_empty_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL eseq, cseq: merge(eseq, cseq) = cseq
theorem merge_empty_left : ∀ (eseq : csequence T) (cseq : csequence T), merge eseq cseq = cseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_merge.merge_empty_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, eseq: merge(cseq, eseq) = cseq
theorem merge_empty_right : ∀ (cseq : csequence T) (eseq : csequence T), merge cseq eseq = cseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_merge.merge_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   nonempty?(merge(cseq1, cseq2)) IMPLIES
--    first(merge(cseq1, cseq2)) =
--     IF nonempty?(cseq1) THEN first(cseq1) ELSE first(cseq2) ENDIF
theorem merge_first : ∀ (cseq1 : csequence T) (cseq2 : csequence T), nonempty_p (merge cseq1 cseq2) → first (merge cseq1 cseq2) =
 if nonempty_p cseq1 then first cseq1 else first cseq2 := by
  -- === TCCs for merge_first ===
  -- TCC: merge_first_TCC1
  -- Statement: FORALL (cseq1, cseq2):
  --   nonempty?(merge(cseq1, cseq2)) AND NOT nonempty?(cseq1) IMPLIES
  --    nonempty?[T](cseq2)
  have merge_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   nonempty?(merge(cseq1, cseq2)) IMPLIES
--    rest(merge(cseq1, cseq2)) =
--     IF nonempty?(cseq1) THEN merge(cseq2, rest(cseq1))
--     ELSE rest(cseq2)
--     ENDIF
theorem merge_rest : ∀ (cseq1 : csequence T) (cseq2 : csequence T), nonempty_p (merge cseq1 cseq2) → rest (merge cseq1 cseq2) =
 if nonempty_p cseq1 then merge cseq2 (rest cseq1) else rest cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_finiteness
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   is_finite(merge(cseq1, cseq2)) IMPLIES
--    is_finite(cseq1) AND is_finite(cseq2)
theorem merge_finiteness : ∀ (cseq1 : csequence T) (cseq2 : csequence T), is_finite (merge cseq1 cseq2) → is_finite cseq1 ∧ is_finite cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq1, fseq2:
--   length(merge(fseq1, fseq2)) = length(fseq1) + length(fseq2)
theorem merge_length : ∀ (fseq1 : csequence T) (fseq2 : csequence T), List.length (merge fseq1 fseq2) = List.length fseq1 + List.length fseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_merge.merge_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, n:
--   index?(merge(cseq1, cseq2))(n) IFF
--    index?(cseq1)(n) OR
--     (is_finite(cseq1) IMPLIES index?(cseq2)(n - length(cseq1)))
theorem merge_index : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), index_p (merge cseq1 cseq2) (n) ↔ index_p cseq1(n) ∨
 (is_finite cseq1 → index_p cseq2(n - List.length cseq1)) := by
  -- === TCCs for merge_index ===
  -- TCC: merge_index_TCC1
  -- Statement: FORALL (cseq1, n):
  --   NOT index?(cseq1)(n) AND is_finite(cseq1) IMPLIES
  --    n - length[T](cseq1) >= 0
  have merge_index_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, (n: indexes(merge(cseq1, cseq2))):
--   nth(merge(cseq1, cseq2), n) =
--    IF length_lt(cseq1, cseq2) AND n >= length(cseq1) * 2
--      THEN nth(cseq2, n - length(cseq1))
--    ELSIF length
theorem merge_nth : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), nth (merge cseq1 cseq2) n = if length_lt cseq1 cseq2 ∧ n ≥ List.length cseq1 * 2
  then nth cseq2 (n - List.length cseq1)
ELSIF length_lt cseq2 cseq1 ∧ n ≥ List.length cseq2 * 2
  then nth cseq1 (n - List.length cseq2)
ELSIF even_p n then nth cseq1 (n / 2)
else nth cseq2 ((n - 1) / 2) := by
  -- === TCCs for merge_nth ===
  -- TCC: merge_nth_TCC1
  -- Statement: FORALL (cseq1, cseq2, n: indexes[T](merge(cseq1, cseq2))):
  --   length_lt(cseq1, cseq2) IMPLIES is_finite[T](cseq1)
  have merge_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_nth_TCC2
  -- Statement: FORALL (cseq1, cseq2, n: indexes[T](merge(cseq1, cseq2))):
  --   length_lt(cseq1, cseq2) AND n >= length(cseq1) * 2 IMPLIES
  --    n - length[T](cseq1) >= 0 AND index?[T](cseq2)(n - length[T](cseq1))
  have merge_nth_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_nth_TCC3
  -- Statement: FORALL (cseq1, cseq2, n: indexes[T](merge(cseq1, cseq2))):
  --   NOT (length_lt(cseq1, cseq2) AND n >= length(cseq1) * 2) AND
  --    length_lt(cseq2, cseq1)
  --    IMPLIES is_finite[T](cseq2)
  have merge_nth_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_nth_TCC4
  -- Statement: FORALL (cseq1, cseq2, n: indexes[T](merge(cseq1, cseq2))):
  --   NOT (length_lt(cseq1, cseq2) AND n >= length(cseq1) * 2) AND
  --    length_lt(cseq2, cseq1) AND n >= length(cseq2) * 2
  --    IMPLIES
  --    n - len...
  have merge_nth_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_nth_TCC5
  -- Statement: FORALL (cseq1, cseq2, n: indexes[T](merge(cseq1, cseq2))):
  --   NOT (length_lt(cseq1, cseq2) AND n >= length(cseq1) * 2) AND
  --    NOT (length_lt(cseq2, cseq1) AND n >= length(cseq2) * 2) AND even?(n)
  --   ...
  have merge_nth_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_nth_TCC6
  -- Statement: FORALL (cseq1, cseq2, n: indexes[T](merge(cseq1, cseq2))):
  --   NOT (length_lt(cseq1, cseq2) AND n >= length(cseq1) * 2) AND
  --    NOT (length_lt(cseq2, cseq1) AND n >= length(cseq2) * 2) AND
  --     NOT eve...
  have merge_nth_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq1, cseq2:
--   add(t, merge(cseq1, cseq2)) = merge(add(t, cseq2), cseq1)
theorem merge_add : ∀ (t : T) (cseq1 : csequence T) (cseq2 : csequence T), add t (merge cseq1 cseq2) = merge (add t cseq2) cseq1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_merge.merge_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq1, fseq2:
--   nonempty?(merge(fseq1, fseq2)) IMPLIES
--    last(merge(fseq1, fseq2)) =
--     IF length(fseq1) <= length(fseq2) THEN last(fseq2)
--     ELSE last(fseq1)
--     ENDIF
theorem merge_last : ∀ (fseq1 : csequence T) (fseq2 : csequence T), nonempty_p (merge fseq1 fseq2) → last (merge fseq1 fseq2) =
 if List.length fseq1 ≤ List.length fseq2 then last fseq2 else last fseq1 := by
  -- === TCCs for merge_last ===
  -- TCC: merge_last_TCC1
  -- Statement: FORALL (fseq1, fseq2):
  --   nonempty?(merge(fseq1, fseq2)) AND length(fseq1) <= length(fseq2) IMPLIES
  --    nonempty?[T](fseq2)
  have merge_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_last_TCC2
  -- Statement: FORALL (fseq1, fseq2):
  --   nonempty?(merge(fseq1, fseq2)) AND NOT length(fseq1) <= length(fseq2)
  --    IMPLIES nonempty?[T](fseq1)
  have merge_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_extensionality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, cseq3, cseq4:
--   length_eq(cseq1, cseq2) AND
--    length_eq(cseq3, cseq4) AND merge(cseq1, cseq2) = merge(cseq3, cseq4)
--    IMPLIES cseq1 = cseq3 AND cseq2 = cseq4
theorem merge_extensionality : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (cseq3 : csequence T) (cseq4 : csequence T), length_eq cseq1 cseq2 ∧
 length_eq cseq3 cseq4 ∧ merge cseq1 cseq2 = merge cseq3 cseq4 → cseq1 = cseq3 ∧ cseq2 = cseq4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_merge.merge_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL p, cseq1, cseq2:
--   some(p)(merge(cseq1, cseq2)) IFF some(p)(cseq1) OR some(p)(cseq2)
theorem merge_some : ∀ (p : T → Bool) (cseq1 : csequence T) (cseq2 : csequence T), some p(merge cseq1 cseq2) ↔ some p(cseq1) ∨ some p(cseq2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_merge.merge_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL p, cseq1, cseq2:
--   every(p)(merge(cseq1, cseq2)) IFF every(p)(cseq1) AND every(p)(cseq2)
theorem merge_every : ∀ (p : T → Bool) (cseq1 : csequence T) (cseq2 : csequence T), every p(merge cseq1 cseq2) ↔ every p(cseq1) ∧ every p(cseq2) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_merge