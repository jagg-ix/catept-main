-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_concatenate_extract
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_concatenate_extract

Auto-generated stubs from PVSLib theory `csequence_concatenate_extract`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_concatenate_extract

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (length : pvs_τ1 → pvs_τ2)

-- pvs2lean: csequence_concatenate_extract.o_extract
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, i, j, k:
--   i <= j AND j <= k IMPLIES
--    (fseq ^ (i, j)) o (fseq ^ (j + 1, k)) = fseq ^ (i, k)
theorem o_extract : ∀ (fseq : csequence T) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), i ≤ j ∧ j ≤ k → (fseq ^ (i, j)) ∘ (fseq ^ (j + 1, k)) = fseq ^ (i, k) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate_extract.o_extract_eta
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, i, j:
--   i < j AND j >= length(fseq) - 1 IMPLIES
--    (fseq ^ (0, i)) o (fseq ^ (i + 1, j)) = fseq
theorem o_extract_eta : ∀ (fseq : csequence T) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), i < j ∧ j ≥ List.length fseq - 1 → (fseq ^ (0, i)) ∘ (fseq ^ (i + 1, j)) = fseq := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_concatenate_extract