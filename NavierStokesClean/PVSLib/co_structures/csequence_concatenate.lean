-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_concatenate
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_concatenate

Auto-generated stubs from PVSLib theory `csequence_concatenate`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_concatenate

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (coreduce : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (empty_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (every : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} (first : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (index_p : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (indexes : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (inj_add : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (is_finite : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (last : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} (length : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (nonempty_p : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (nth : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} (rest : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (pvs_some : pvs_τ27 → pvs_τ28 → pvs_τ29)

-- pvs2lean: csequence_concatenate.concatenate_struct
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(cseq1)
--   THEN IF empty?(cseq2) THEN inj_empty_cseq
--        ELSE inj_add(first(cseq2), (cseq1, rest(cseq2)))
--        ENDIF
-- ELSE inj_add(first(cseq1), (rest(cseq1), cseq2))
-- ENDIF
noncomputable def concatenate_struct : sorry := by
  sorry  -- pvs2lean: definition stub for IF empty?(cseq1)   THEN IF empty?(cseq2) THEN inj_empty_cseq        ELSE inj_add

-- pvs2lean: csequence_concatenate.O
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: coreduce(concatenate_struct)(cseq1, cseq2)
noncomputable def O : sorry := by
  sorry  -- pvs2lean: definition stub for coreduce(concatenate_struct)(cseq1, cseq2)

-- pvs2lean: csequence_concatenate.o_finiteness
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   is_finite(cseq1 o cseq2) IMPLIES is_finite(cseq1) AND is_finite(cseq2)
theorem o_finiteness : ∀ (cseq1 : csequence T) (cseq2 : csequence T), is_finite (cseq1 ∘ cseq2) → is_finite cseq1 ∧ is_finite cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   empty?(cseq1 o cseq2) IFF empty?(cseq1) AND empty?(cseq2)
theorem o_empty : ∀ (cseq1 : csequence T) (cseq2 : csequence T), empty_p (cseq1 ∘ cseq2) ↔ empty_p cseq1 ∧ empty_p cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   nonempty?(cseq1 o cseq2) IFF nonempty?(cseq1) OR nonempty?(cseq2)
theorem o_nonempty : ∀ (cseq1 : csequence T) (cseq2 : csequence T), nonempty_p (cseq1 ∘ cseq2) ↔ nonempty_p cseq1 ∨ nonempty_p cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_empty_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL eseq, cseq: eseq o cseq = cseq
theorem o_empty_left : ∀ (eseq : csequence T) (cseq : csequence T), eseq ∘ cseq = cseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_empty_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, eseq: cseq o eseq = cseq
theorem o_empty_right : ∀ (cseq : csequence T) (eseq : csequence T), cseq ∘ eseq = cseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   nonempty?(cseq1 o cseq2) IMPLIES
--    first(cseq1 o cseq2) =
--     IF empty?(cseq1) THEN first(cseq2) ELSE first(cseq1) ENDIF
theorem o_first : ∀ (cseq1 : csequence T) (cseq2 : csequence T), nonempty_p (cseq1 ∘ cseq2) → first (cseq1 ∘ cseq2) =
 if empty_p cseq1 then first cseq2 else first cseq1 := by
  -- === TCCs for o_first ===
  -- TCC: o_first_TCC1
  -- Statement: FORALL (cseq1, cseq2):
  --   nonempty?(cseq1 o cseq2) AND empty?(cseq1) IMPLIES nonempty?[T](cseq2)
  have o_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   nonempty?(cseq1 o cseq2) IMPLIES
--    rest(cseq1 o cseq2) =
--     IF empty?(cseq1) THEN rest(cseq2) ELSE rest(cseq1) o cseq2 ENDIF
theorem o_rest : ∀ (cseq1 : csequence T) (cseq2 : csequence T), nonempty_p (cseq1 ∘ cseq2) → rest (cseq1 ∘ cseq2) =
 if empty_p cseq1 then rest cseq2 else rest cseq1 ∘ cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_first_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq1, nseq2, cseq:
--   first(nseq1) = first(nseq2) AND rest(nseq1) o cseq = rest(nseq2) IMPLIES
--    nseq1 o cseq = nseq2
theorem o_first_rest : ∀ (nseq1 : csequence T) (nseq2 : csequence T) (cseq : csequence T), first nseq1 = first nseq2 ∧ rest nseq1 ∘ cseq = rest nseq2 → nseq1 ∘ cseq = nseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq1, fseq2: length(fseq1 o fseq2) = length(fseq1) + length(fseq2)
theorem o_length : ∀ (fseq1 : csequence T) (fseq2 : csequence T), List.length (fseq1 ∘ fseq2) = List.length fseq1 + List.length fseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, n:
--   index?(cseq1 o cseq2)(n) IFF
--    (is_finite(cseq1) AND is_finite(cseq2) IMPLIES
--      n < length(cseq1) + length(cseq2))
theorem o_index : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), index_p (cseq1 ∘ cseq2) (n) ↔ (is_finite cseq1 ∧ is_finite cseq2 →
  n < List.length cseq1 + List.length cseq2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, (n: indexes(cseq1 o cseq2)):
--   nth(cseq1 o cseq2, n) =
--    IF is_finite(cseq1) AND n >= length(cseq1)
--      THEN nth(cseq2, n - length(cseq1))
--    ELSE nth(cseq1, n)
--    ENDIF
theorem o_nth : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (n : {i : ℝ // i ≥ 0}), nth (cseq1 ∘ cseq2) n = if is_finite cseq1 ∧ n ≥ List.length cseq1
  then nth cseq2 (n - List.length cseq1)
else nth cseq1 n := by
  -- === TCCs for o_nth ===
  -- TCC: o_nth_TCC1
  -- Statement: FORALL (cseq1, cseq2, n: indexes[T](cseq1 o cseq2)):
  --   is_finite(cseq1) AND n >= length(cseq1) IMPLIES
  --    n - length[T](cseq1) >= 0 AND index?[T](cseq2)(n - length[T](cseq1))
  have o_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: o_nth_TCC2
  -- Statement: FORALL (cseq1, cseq2, n: indexes[T](cseq1 o cseq2)):
  --   NOT (is_finite(cseq1) AND n >= length(cseq1)) IMPLIES index?[T](cseq1)(n)
  have o_nth_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, t: add(t, cseq1) o cseq2 = add(t, cseq1 o cseq2)
theorem o_add : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (t : T), add t cseq1 ∘ cseq2 = add t (cseq1 ∘ cseq2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   is_finite(cseq1 o cseq2) AND nonempty?(cseq1 o cseq2) IMPLIES
--    last(cseq1 o cseq2) =
--     IF empty?(cseq2) THEN last(cseq1) ELSE last(cseq2) ENDIF
theorem o_last : ∀ (cseq1 : csequence T) (cseq2 : csequence T), is_finite (cseq1 ∘ cseq2) ∧ nonempty_p (cseq1 ∘ cseq2) → last (cseq1 ∘ cseq2) =
 if empty_p cseq2 then last cseq1 else last cseq2 := by
  -- === TCCs for o_last ===
  -- TCC: o_last_TCC1
  -- Statement: FORALL (cseq1, cseq2):
  --   is_finite(cseq1 o cseq2) AND nonempty?(cseq1 o cseq2) AND empty?(cseq2)
  --    IMPLIES is_finite[T](cseq1) AND nonempty?[T](cseq1)
  have o_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: o_last_TCC2
  -- Statement: FORALL (cseq1, cseq2):
  --   is_finite(cseq1 o cseq2) AND
  --    nonempty?(cseq1 o cseq2) AND NOT empty?(cseq2)
  --    IMPLIES is_finite[T](cseq2) AND nonempty?[T](cseq2)
  have o_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_infinite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL iseq, cseq: iseq o cseq = iseq
theorem o_infinite : ∀ (iseq : {cseq : csequence T // ¬ is_finite cseq}) (cseq : csequence T), iseq ∘ cseq = iseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_assoc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, cseq1, cseq2: cseq o (cseq1 o cseq2) = (cseq o cseq1) o cseq2
theorem o_assoc : ∀ (cseq : csequence T) (cseq1 : csequence T) (cseq2 : csequence T), cseq ∘ (cseq1 ∘ cseq2) = (cseq ∘ cseq1) ∘ cseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_extensionality_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, cseq1, cseq2: fseq o cseq1 = fseq o cseq2 IMPLIES cseq1 = cseq2
theorem o_extensionality_left : ∀ (fseq : csequence T) (cseq1 : csequence T) (cseq2 : csequence T), fseq ∘ cseq1 = fseq ∘ cseq2 → cseq1 = cseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_extensionality_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, cseq1, cseq2: cseq1 o fseq = cseq2 o fseq IMPLIES cseq1 = cseq2
theorem o_extensionality_right : ∀ (fseq : csequence T) (cseq1 : csequence T) (cseq2 : csequence T), cseq1 ∘ fseq = cseq2 ∘ fseq → cseq1 = cseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, p:
--   some(p)(cseq1 o cseq2) IFF
--    some(p)(cseq1) OR (is_finite(cseq1) AND some(p)(cseq2))
theorem o_some : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (p : T → Bool), some p(cseq1 ∘ cseq2) ↔ some p(cseq1) ∨ (is_finite cseq1 ∧ some p(cseq2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_concatenate.o_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, p:
--   every(p)(cseq1 o cseq2) IFF
--    every(p)(cseq1) AND (NOT is_finite[T](cseq1) OR every(p)(cseq2))
theorem o_every : ∀ (cseq1 : csequence T) (cseq2 : csequence T) (p : T → Bool), every p(cseq1 ∘ cseq2) ↔ every p(cseq1) ∧ (¬ is_finite cseq1 ∨ every p(cseq2)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_concatenate