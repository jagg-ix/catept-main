-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_flatten
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_flatten

Auto-generated stubs from PVSLib theory `csequence_flatten`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_flatten

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (coreduce : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (empty_p : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (every : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (filter : pvs_τ10 → pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} (first : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (first_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ17 : Type*} (is_finite : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (length : pvs_τ18 → Prop)
variable {pvs_τ20 : Type*} (nonempty_p : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (nth : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} (rest : pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (pvs_some : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (suffix : pvs_τ28 → pvs_τ29 → pvs_τ30)

-- pvs2lean: csequence_flatten.some_every_empty
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: every(empty?)(cseq) IFF NOT some(nonempty?)(cseq)
theorem some_every_empty : ∀ (cseq : csequence (csequence T)), every empty_p(cseq) ↔ ¬ some nonempty_p(cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_struct
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(nseq) THEN inj_empty_cseq[T, nonempty_seq_seq]
-- ELSE inj_add[T, nonempty_seq_seq]
--          (first(first(nseq)),
--           IF empty?(rest(first(nseq))) THEN rest(nseq)
--           ELSE add(rest(
noncomputable def flatten_struct : sorry := by
  sorry  -- pvs2lean: definition stub for IF empty?(nseq) THEN inj_empty_cseq[T, nonempty_seq_seq] ELSE inj_add[T, nonempt

-- pvs2lean: csequence_flatten.flatten
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: coreduce(flatten_struct)(filter(cseq, nonempty?))
axiom flatten {pvs_τ16 : Type*} : pvs_τ16 → Prop

-- pvs2lean: csequence_flatten.flatten_empty
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: empty?(flatten(cseq)) IFF every(empty?)(cseq)
theorem flatten_empty : ∀ (cseq : csequence (csequence T)), empty_p (flatten cseq) ↔ every empty_p(cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_empty_cseq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL eseq: empty?(flatten(eseq))
theorem flatten_empty_cseq : ∀ (eseq : csequence (csequence T)), empty_p (flatten eseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: nonempty?(flatten(cseq)) IFF some(nonempty?)(cseq)
theorem flatten_nonempty : ∀ (cseq : csequence (csequence T)), nonempty_p (flatten cseq) ↔ some nonempty_p(cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_filter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: flatten(filter(cseq, nonempty?)) = flatten(cseq)
theorem flatten_filter : ∀ (cseq : csequence (csequence T)), flatten (filter cseq nonempty_p) = flatten cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_reduce
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   empty?(cseq) OR
--    nonempty?(first(cseq)) OR flatten(cseq) = flatten(rest(cseq))
theorem flatten_reduce : ∀ (cseq : csequence (csequence T)), empty_p cseq ∨ nonempty_p (first cseq) ∨ flatten cseq = flatten (rest cseq) := by
  -- === TCCs for flatten_reduce ===
  -- TCC: flatten_reduce_TCC1
  -- Statement: FORALL (cseq): NOT empty?(cseq) IMPLIES nonempty?[csequence[T]](cseq)
  have flatten_reduce_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   nonempty?(flatten(cseq)) IMPLIES
--    rest(flatten(cseq)) =
--     flatten(add(rest(first(filter(cseq, nonempty?))),
--                 rest(filter(cseq, nonempty?))))
theorem flatten_rest : ∀ (cseq : csequence (csequence T)), nonempty_p (flatten cseq) → rest (flatten cseq) =
 flatten (add (rest (first (filter cseq nonempty_p))) (rest (filter cseq nonempty_p))) := by
  -- === TCCs for flatten_rest ===
  -- TCC: flatten_rest_TCC1
  -- Statement: FORALL (cseq):
  --   nonempty?(flatten(cseq)) IMPLIES
  --    nonempty?[csequence[T]](filter[csequence[T]](cseq, nonempty?[T]))
  have flatten_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: flatten_rest_TCC2
  -- Statement: FORALL (cseq):
  --   nonempty?(flatten(cseq)) IMPLIES
  --    nonempty?[T]
  --        (first[csequence[T]](filter[csequence[T]](cseq, nonempty?[T])))
  have flatten_rest_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_rest2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   empty?(cseq) OR
--    empty?(first(cseq)) OR
--     rest(flatten(cseq)) = flatten(add(rest(first(cseq)), rest(cseq)))
theorem flatten_rest2 : ∀ (cseq : csequence (csequence T)), empty_p cseq ∨ empty_p (first cseq) ∨
 rest (flatten cseq) = flatten (add (rest (first cseq)) (rest cseq)) := by
  -- === TCCs for flatten_rest2 ===
  -- TCC: flatten_rest2_TCC1
  -- Statement: FORALL (cseq):
  --   NOT empty?(cseq) AND NOT empty?(first(cseq)) IMPLIES
  --    nonempty?[T](flatten(cseq))
  have flatten_rest2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: flatten_rest2_TCC2
  -- Statement: FORALL (cseq):
  --   NOT empty?(cseq) AND NOT empty?(first(cseq)) IMPLIES
  --    nonempty?[T](first[csequence[T]](cseq))
  have flatten_rest2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   nonempty?(flatten(cseq)) IMPLIES
--    first(flatten(cseq)) = first(first(filter(cseq, nonempty?)))
theorem flatten_first : ∀ (cseq : csequence (csequence T)), nonempty_p (flatten cseq) → first (flatten cseq) = first (first (filter cseq nonempty_p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_suffix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   some(nonempty?)(cseq) IMPLIES
--    flatten(cseq) = flatten(suffix(cseq, first_p(nonempty?, cseq)))
theorem flatten_suffix : ∀ (cseq : csequence (csequence T)), some nonempty_p(cseq) → flatten cseq = flatten (suffix cseq (first_p nonempty_p cseq)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_concatenate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   nonempty?(cseq) IMPLIES flatten(cseq) = first(cseq) o flatten(rest(cseq))
theorem flatten_concatenate : ∀ (cseq : csequence (csequence T)), nonempty_p cseq → flatten cseq = first cseq ∘ flatten (rest cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_rest_suffix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   nonempty?(flatten(cseq)) IMPLIES
--    LET first = first_p(nonempty?, cseq) IN
--      rest(flatten(cseq)) =
--       flatten(add(rest(nth(cseq, first)), suffix(cseq, first + 1)))
theorem flatten_rest_suffix : ∀ (cseq : csequence (csequence T)), nonempty_p (flatten cseq) → (let first := first_p nonempty_p cseq; rest (flatten cseq) =
   flatten (add (rest (nth cseq first)) (suffix cseq (first + 1)))) := by
  -- === TCCs for flatten_rest_suffix ===
  -- TCC: flatten_rest_suffix_TCC1
  -- Statement: FORALL (cseq):
  --   nonempty?(flatten(cseq)) IMPLIES
  --    csequence_codt[csequence[T]].some(nonempty?[T])(cseq)
  have flatten_rest_suffix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: flatten_rest_suffix_TCC2
  -- Statement: FORALL (cseq):
  --   nonempty?(flatten(cseq)) IMPLIES
  --    FORALL (first: indexes[csequence[T]](cseq)):
  --      first = first_p(nonempty?, cseq) IMPLIES
  --       nonempty?[T](nth[csequence[T]](cseq, first))
  have flatten_rest_suffix_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_finite
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   is_finite(flatten(cseq)) IFF
--    is_finite(filter(cseq, nonempty?)) AND every(is_finite)(cseq)
theorem flatten_finite : ∀ (cseq : csequence (csequence T)), is_finite (flatten cseq) ↔ is_finite (filter cseq nonempty_p) ∧ every is_finite(cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_infinite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   NOT is_finite(flatten(cseq)) IFF
--    NOT is_finite(filter(cseq, nonempty?)) OR
--     some(LAMBDA (cseq: csequence[T]): NOT is_finite(cseq))(cseq)
theorem flatten_infinite : ∀ (cseq : csequence (csequence T)), ¬ is_finite (flatten cseq) ↔ ¬ is_finite (filter cseq nonempty_p) ∨
 some (fun (cseq : csequence T)=> ¬ is_finite cseq) (cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.length_of_flatten
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(flatten(fseq)) THEN 0
-- ELSE length(nth(fseq, first_p(nonempty?, fseq))) +
--       length_of_flatten(suffix(fseq, first_p(nonempty?, fseq) + 1))
-- ENDIF
axiom length_of_flatten {pvs_τ19 : Type*} : pvs_τ19 → Prop

-- pvs2lean: csequence_flatten.length_of_flatten_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, tseq:
--   is_finite(tseq) IMPLIES
--    length_of_flatten(add(tseq, fseq)) =
--     length(tseq) + length_of_flatten(fseq)
theorem length_of_flatten_add : ∀ (fseq : {cseq : csequence (csequence T) // is_finite (flatten cseq)}) (tseq : csequence T), is_finite tseq → length_of_flatten (add tseq fseq) = List.length tseq + length_of_flatten fseq := by
  -- === TCCs for length_of_flatten_add ===
  -- TCC: length_of_flatten_add_TCC1
  -- Statement: FORALL (fseq, tseq):
  --   is_finite(tseq) IMPLIES
  --    is_finite[T](flatten(add[csequence[T]](tseq, fseq)))
  have length_of_flatten_add_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   is_finite(flatten(cseq)) IMPLIES
--    length(flatten(cseq)) = length_of_flatten(cseq)
theorem flatten_length : ∀ (cseq : csequence (csequence T)), is_finite (flatten cseq) → List.length (flatten cseq) = length_of_flatten cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, p: some(p)(flatten(cseq)) IMPLIES some(some(p))(cseq)
theorem flatten_some : ∀ (cseq : csequence (csequence T)) (p : T → Bool), some p(flatten cseq) → some (some p) (cseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_some_finite
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, p:
--   every(is_finite)(cseq) IMPLIES
--    (some(p)(flatten(cseq)) IFF some(some(p))(cseq))
theorem flatten_some_finite : ∀ (cseq : csequence (csequence T)) (p : T → Bool), every is_finite(cseq) → (some p(flatten cseq) ↔ some (some p) (cseq)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, p: every(every(p))(cseq) IMPLIES every(p)(flatten(cseq))
theorem flatten_every : ∀ (cseq : csequence (csequence T)) (p : T → Bool), every (every p) (cseq) → every p(flatten cseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_flatten.flatten_every_finite
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, p:
--   every(is_finite)(cseq) IMPLIES
--    (every(p)(flatten(cseq)) IFF every(every(p))(cseq))
theorem flatten_every_finite : ∀ (cseq : csequence (csequence T)) (p : T → Bool), every is_finite(cseq) → (every p(flatten cseq) ↔ every (every p) (cseq)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_flatten