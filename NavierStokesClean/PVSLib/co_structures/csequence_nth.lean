-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_nth
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_nth

Auto-generated stubs from PVSLib theory `csequence_nth`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_nth

noncomputable section

variable {pvs_τ1 : Type*} (empty_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (every : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (first : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} (full_p : pvs_τ6 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (indexes : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (is_finite : pvs_τ11 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (length : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (nonempty_p : pvs_τ15 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (rest : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (pvs_some : pvs_τ21 → pvs_τ22 → pvs_τ23)

-- pvs2lean: csequence_nth.index?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(cseq) AND (n = 0 OR index?(rest(cseq))(n - 1))
axiom index_p {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: csequence_nth.index?_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: nonempty?(cseq) IFF index?(cseq)(0)
theorem index_p_0 : ∀ (cseq : csequence T), nonempty_p cseq ↔ index_p cseq(0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.index?_ub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n: index?(cseq)(n) OR is_finite(cseq)
theorem index_p_ub : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p cseq(n) ∨ is_finite cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.index?_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, (i: indexes(cseq)), n: n < i IMPLIES index?(cseq)(n)
theorem index_p_lt : ∀ (cseq : csequence T) (i : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}), n < i → index_p cseq(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.index?_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, n: index?(fseq)(n) IFF n < length(fseq)
theorem index_p_finite : ∀ (fseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p fseq(n) ↔ n < List.length fseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.index?_finite_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq: EXISTS n: NOT index?(fseq)(n)
theorem index_p_finite_bound : ∀ (fseq : csequence T), ∃ (n : {i : ℝ // i ≥ 0}), ¬(index_p fseq n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.index?_infinite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL iseq, n: index?(iseq)(n)
theorem index_p_infinite : ∀ (iseq : {cseq : csequence T // ¬ is_finite cseq}) (n : {i : ℝ // i ≥ 0}), index_p iseq n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.index?_infinite_full
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: NOT is_finite[T](cseq) IFF full?(index?(cseq))
theorem index_p_infinite_full : ∀ (cseq : csequence T), ¬ is_finite cseq ↔ full_p (index_p cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.index?_prop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n:
--   index?(cseq)(n) IFF (is_finite(cseq) IMPLIES n < length(cseq))
theorem index_p_prop : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p cseq(n) ↔ (is_finite cseq → n < List.length cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.index?_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, (n: indexes(cseq)): nonempty?(cseq)
theorem index_p_nonempty : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), nonempty_p cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.nth
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN first(cseq) ELSE nth(rest(cseq), n - 1) ENDIF
axiom nth {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: csequence_nth.nth_extensionality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   index?(cseq1) = index?(cseq2) AND
--    (FORALL (n: indexes(cseq1)): nth(cseq1, n) = nth(cseq2, n))
--    IMPLIES cseq1 = cseq2
theorem nth_extensionality : ∀ (cseq1 : csequence T) (cseq2 : csequence T), index_p cseq1 = index_p cseq2 ∧
 (∀ (n : indexes cseq1), nth cseq1 n = nth cseq2 n) → cseq1 = cseq2 := by
  -- === TCCs for nth_extensionality ===
  -- TCC: nth_extensionality_TCC1
  -- Statement: FORALL (cseq1, cseq2):
  --   index?(cseq1) = index?(cseq2) IMPLIES
  --    FORALL (n: indexes(cseq1)): index?(cseq2)(n)
  have nth_extensionality_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_nth.nth_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, p:
--   every(p)(cseq) IFF (FORALL (i: indexes(cseq)): p(nth(cseq, i)))
theorem nth_every : ∀ (cseq : csequence T) (p : T → Bool), every p(cseq) ↔ (∀ (i : indexes cseq), p (nth cseq i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_nth.nth_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, p:
--   some(p)(cseq) IFF (EXISTS (i: indexes(cseq)): p(nth(cseq, i)))
theorem nth_some : ∀ (cseq : csequence T) (p : T → Bool), some p(cseq) ↔ (∃ (i : indexes cseq), p (nth cseq i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_nth.last
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nth(nfseq, length(nfseq) - 1)
axiom last {pvs_τ12 : Type*} : pvs_τ12 → Prop

-- pvs2lean: csequence_nth.last_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nfseq: empty?(rest(nfseq)) OR last(rest(nfseq)) = last(nfseq)
theorem last_rest : ∀ (nfseq : {cseq : csequence T // nonempty_p cseq}), empty_p (rest nfseq) ∨ last (rest nfseq) = last nfseq := by
  -- === TCCs for last_rest ===
  -- TCC: last_rest_TCC1
  -- Statement: FORALL (nfseq):
  --   NOT empty?(rest(nfseq)) IMPLIES
  --    is_finite[T](rest[T](nfseq)) AND nonempty?[T](rest[T](nfseq))
  have last_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_nth