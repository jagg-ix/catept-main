-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_limit
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_limit

Auto-generated stubs from PVSLib theory `csequence_limit`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_limit

noncomputable section

variable {pvs_τ1 : Type*} (ascending_chain_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (chain : pvs_τ2 → pvs_τ3)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (coreduce : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (empty_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (first : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (index_p : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (inj_add : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (is_finite : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (least_upperbound_p : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (min : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} (nonempty_p : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (nth : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (pvs_prefix : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (prefix_p : pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} (rest : pvs_τ32 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (suffix : pvs_τ35 → pvs_τ36 → pvs_τ37)

-- pvs2lean: csequence_limit.prefix_chain
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA n: prefix(cseq, n)
axiom prefix_chain {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ28 → pvs_τ29

-- pvs2lean: csequence_limit.suffix_chain
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA m: suffix(chain(m), n)
axiom suffix_chain {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} : pvs_τ38 → pvs_τ39 → pvs_τ40

-- pvs2lean: csequence_limit.rest_chain
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA n: IF empty?(chain(n)) THEN empty_cseq ELSE rest(chain(n)) ENDIF
axiom rest_chain {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ33 → pvs_τ34

-- pvs2lean: csequence_limit.ascending_chain?_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain, n, m, k:
--   n <= m AND index?(chain(n))(k) IMPLIES
--    index?(chain(m))(k) AND nth(chain(n), k) = nth(chain(m), k)
theorem ascending_chain_p_nth : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), n ≤ m ∧ index_p (chain n) (k) → index_p (chain m) (k) ∧ nth (chain n) k = nth (chain m) k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_struct
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (FORALL n: empty?(chain(n))) THEN inj_empty_cseq
-- ELSE inj_add(first(chain(min({n | nonempty?(chain(n))}))),
--              rest_chain(chain))
-- ENDIF
noncomputable def limit_struct : sorry := by
  sorry  -- pvs2lean: definition stub for IF (FORALL n: empty?(chain(n))) THEN inj_empty_cseq ELSE inj_add(first(chain(min

-- pvs2lean: csequence_limit.limit
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: coreduce(limit_struct)(chain)
axiom limit {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ18 → pvs_τ19

-- pvs2lean: csequence_limit.limit_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain: empty?(limit(chain)) IFF (FORALL n: empty?(chain(n)))
theorem limit_empty : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T), empty_p (limit chain) ↔ (∀ n,  empty_p (chain n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain: nonempty?(limit(chain)) IFF (EXISTS n: nonempty?(chain(n)))
theorem limit_nonempty : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T), nonempty_p (limit chain) ↔ (∃ n,  nonempty_p (chain n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain, n:
--   nonempty?(chain(n)) IMPLIES first(chain(n)) = first(limit(chain))
theorem limit_first : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T) (n : {i : ℝ // i ≥ 0}), nonempty_p (chain n) → first (chain n) = first (limit chain) := by
  -- === TCCs for limit_first ===
  -- TCC: limit_first_TCC1
  -- Statement: FORALL (chain, n): nonempty?(chain(n)) IMPLIES nonempty?[T](limit(chain))
  have limit_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_rest_chain
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain:
--   nonempty?(limit(chain)) IMPLIES
--    rest(limit(chain)) = limit(rest_chain(chain))
theorem limit_rest_chain : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T), nonempty_p (limit chain) → rest (limit chain) = limit (rest_chain chain) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_suffix_chain
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain, n: suffix(limit(chain), n) = limit(suffix_chain(chain, n))
theorem limit_suffix_chain : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T) (n : {i : ℝ // i ≥ 0}), suffix (limit chain) n = limit (suffix_chain chain n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_lub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain: least_upperbound?(chain)(limit(chain))
theorem limit_lub : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T), least_upperbound_p chain (limit chain) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain, cseq, n, m:
--   index?(chain(n))(m) IMPLIES
--    index?(limit(chain))(m) AND nth(chain(n), m) = nth(limit(chain), m)
theorem limit_nth : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T) (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}), index_p (chain n) (m) → index_p (limit chain) (m) ∧ nth (chain n) m = nth (limit chain) m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain, cseq:
--   limit(chain) = cseq IFF
--    (FORALL t, n:
--       index?(cseq)(n) AND nth(cseq, n) = t IFF
--        (EXISTS m: index?(chain(m))(n) AND nth(chain(m), n) = t))
theorem limit_def : ∀ (chain : {i : ℝ // i ≥ 0} → csequence T) (cseq : csequence T), limit chain = cseq ↔ (∀ t n, 
   index_p cseq(n) ∧ nth cseq n = t ↔
    (∃ m,  index_p (chain m) (n) ∧ nth (chain m) n = t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_prefix_chain
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq: limit(prefix_chain(cseq)) = cseq
theorem limit_prefix_chain : ∀ (cseq : csequence T), limit (prefix_chain cseq) = cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_prefix_compact
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, n, chain:
--   (n = 0 OR index?(cseq)(n - 1)) IMPLIES
--    (prefix?(prefix(cseq, n), limit(chain)) IMPLIES
--      (EXISTS m: prefix?(prefix(cseq, n), chain(m))))
theorem limit_prefix_compact : ∀ (cseq : csequence T) (n : {i : ℝ // i ≥ 0}) (chain : {i : ℝ // i ≥ 0} → csequence T), (n = 0 ∨ index_p cseq(n - 1)) → (prefix_p (prefix cseq n) (limit chain) →
  (∃ m,  prefix_p (prefix cseq n) (chain m))) := by
  -- === TCCs for limit_prefix_compact ===
  -- TCC: limit_prefix_compact_TCC1
  -- Statement: FORALL (n, chain): NOT n = 0 IMPLIES n - 1 >= 0
  have limit_prefix_compact_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.limit_finite_compact
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq:
--   is_finite(cseq) IFF
--    (FORALL chain:
--       prefix?(cseq, limit(chain)) IMPLIES
--        (EXISTS m: prefix?(cseq, chain(m))))
theorem limit_finite_compact : ∀ (cseq : csequence T), is_finite cseq ↔ (∀ chain, 
   prefix_p cseq (limit chain) → (∃ m,  prefix_p cseq (chain m))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_limit.continuous?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL chain: (FORALL n: p(chain(n))) IMPLIES p(limit(chain))
axiom continuous_p {pvs_τ4 : Type*} : pvs_τ4 → Prop

-- pvs2lean: csequence_limit.continuous?_infinite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL p:
--   continuous?(p) AND (FORALL fseq: p(fseq)) IMPLIES (FORALL cseq: p(cseq))
theorem continuous_p_infinite : ∀ (p : csequence T → Bool), continuous_p p ∧ (∀ fseq,  p fseq) → (∀ cseq,  p cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_limit