-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_zip
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_zip

Auto-generated stubs from PVSLib theory `csequence_zip`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_zip

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (coreduce : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (empty_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (every : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (first : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (index_p : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (indexes : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (inj_add : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} (is_finite : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} (last : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (length : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (length_eq : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (length_gt : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (length_lt : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (min : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} (nonempty_p : pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} (nth : pvs_τ33 → pvs_τ34 → pvs_τ35)
variable {pvs_τ36 : Type*} (p1 : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} (p2 : pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (rest : pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} (pvs_some : pvs_τ40 → pvs_τ41 → pvs_τ42)

-- pvs2lean: csequence_zip.zip_struct
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(state`1) OR empty?(state`2) THEN inj_empty_cseq
-- ELSE inj_add((first(state`1), first(state`2)),
--              (rest(state`1), rest(state`2)))
-- ENDIF
noncomputable def zip_struct : sorry := by
  sorry  -- pvs2lean: definition stub for IF empty?(state`1) OR empty?(state`2) THEN inj_empty_cseq ELSE inj_add((first(st

-- pvs2lean: csequence_zip.zip
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: coreduce(zip_struct)(cseq1, cseq2)
axiom zip {pvs_τ43 : Type*} {pvs_τ44 : Type*} : pvs_τ43 → pvs_τ44 → Prop

-- pvs2lean: csequence_zip.zip_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   empty?(zip(cseq1, cseq2)) IFF empty?(cseq1) OR empty?(cseq2)
theorem zip_empty : ∀ (cseq1 : csequence T1) (cseq2 : csequence T2), empty_p (zip cseq1 cseq2) ↔ empty_p cseq1 ∨ empty_p cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_zip.zip_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq1, nseq2: first(zip(nseq1, nseq2)) = (first(nseq1), first(nseq2))
theorem zip_first : ∀ (nseq1 : csequence T1) (nseq2 : csequence T2), first (zip nseq1 nseq2) = (first nseq1, first nseq2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_zip.zip_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq1, nseq2: rest(zip(nseq1, nseq2)) = zip(rest(nseq1), rest(nseq2))
theorem zip_rest : ∀ (nseq1 : csequence T1) (nseq2 : csequence T2), rest (zip nseq1 nseq2) = zip (rest nseq1) (rest nseq2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_zip.zip_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, (a: T1), (b: T2):
--   zip(add(a, cseq1), add(b, cseq2)) = add((a, b), zip(cseq1, cseq2))
theorem zip_add : ∀ (cseq1 : csequence T1) (cseq2 : csequence T2) (a : T1) (b : T2), zip (add a cseq1) (add b cseq2) = add ((a, b)) (zip cseq1 cseq2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_zip.zip_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   is_finite(zip(cseq1, cseq2)) IMPLIES
--    length(zip(cseq1, cseq2)) =
--     IF is_finite(cseq1)
--       THEN IF is_finite(cseq2) THEN min(length(cseq1), length(cseq2))
--            ELSE
theorem zip_length : ∀ (cseq1 : csequence T1) (cseq2 : csequence T2), is_finite (zip cseq1 cseq2) → List.length (zip cseq1 cseq2) =
 if is_finite cseq1
   then if is_finite cseq2 then min (List.length cseq1) (List.length cseq2)
        else List.length cseq1
        
 else List.length cseq2 := by
  -- === TCCs for zip_length ===
  -- TCC: zip_length_TCC1
  -- Statement: FORALL (cseq1, cseq2):
  --   is_finite(zip(cseq1, cseq2)) AND NOT is_finite(cseq1) IMPLIES
  --    is_finite[T2](cseq2)
  have zip_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_zip.zip_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, n:
--   index?(zip(cseq1, cseq2))(n) IFF index?(cseq1)(n) AND index?(cseq2)(n)
theorem zip_index : ∀ (cseq1 : csequence T1) (cseq2 : csequence T2) (n : {i : ℝ // i ≥ 0}), index_p (zip cseq1 cseq2) (n) ↔ index_p cseq1(n) ∧ index_p cseq2(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_zip.zip_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, (n: indexes(zip(cseq1, cseq2))):
--   nth(zip(cseq1, cseq2), n) = (nth(cseq1, n), nth(cseq2, n))
theorem zip_nth : ∀ (cseq1 : csequence T1) (cseq2 : csequence T2) (n : {i : ℝ // i ≥ 0}), nth (zip cseq1 cseq2) n = (nth cseq1 n, nth cseq2 n) := by
  -- === TCCs for zip_nth ===
  -- TCC: zip_nth_TCC1
  -- Statement: FORALL (cseq1, cseq2, n: indexes[[T1, T2]](zip(cseq1, cseq2))):
  --   index?[T1](cseq1)(n)
  have zip_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_nth_TCC2
  -- Statement: FORALL (cseq1, cseq2, n: indexes[[T1, T2]](zip(cseq1, cseq2))):
  --   index?[T2](cseq2)(n)
  have zip_nth_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_zip.zip_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nseq1, nseq2:
--   is_finite(zip(nseq1, nseq2)) IMPLIES
--    last(zip(nseq1, nseq2)) =
--     COND length_lt(nseq1, nseq2) ->
--            (last(nseq1), nth(nseq2, length(nseq1) - 1)),
--          length_gt
theorem zip_last : ∀ (nseq1 : csequence T1) (nseq2 : csequence T2), is_finite (zip nseq1 nseq2) → last (zip nseq1 nseq2) =
 COND length_lt nseq1 nseq2 ->
        (last nseq1, nth nseq2 (List.length nseq1 - 1)),
      length_gt nseq1 nseq2 ->
        (nth nseq1 (List.length nseq2 - 1), last nseq2),
      else -> (last nseq1, last nseq2)
 ENDCOND := by
  -- === TCCs for zip_last ===
  -- TCC: zip_last_TCC1
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) AND length_lt(nseq1, nseq2) IMPLIES
  --    is_finite[T1](nseq1) AND nonempty?[T1](nseq1)
  have zip_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_last_TCC2
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) AND length_lt(nseq1, nseq2) IMPLIES
  --    is_finite[T1](nseq1)
  have zip_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_last_TCC3
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) AND length_lt(nseq1, nseq2) IMPLIES
  --    length[T1](nseq1) - 1 >= 0 AND index?[T2](nseq2)(length[T1](nseq1) - 1)
  have zip_last_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_last_TCC4
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) AND
  --    NOT length_lt(nseq1, nseq2) AND length_gt(nseq1, nseq2)
  --    IMPLIES is_finite[T2](nseq2)
  have zip_last_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_last_TCC5
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) AND
  --    NOT length_lt(nseq1, nseq2) AND length_gt(nseq1, nseq2)
  --    IMPLIES
  --    length[T2](nseq2) - 1 >= 0 AND index?[T1](nseq1)(length[T2](nseq2)...
  have zip_last_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_last_TCC6
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) AND
  --    NOT length_lt(nseq1, nseq2) AND length_gt(nseq1, nseq2)
  --    IMPLIES is_finite[T2](nseq2) AND nonempty?[T2](nseq2)
  have zip_last_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_last_TCC7
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) AND
  --    NOT length_lt(nseq1, nseq2) AND NOT length_gt(nseq1, nseq2)
  --    IMPLIES is_finite[T1](nseq1) AND nonempty?[T1](nseq1)
  have zip_last_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_last_TCC8
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) AND
  --    NOT length_lt(nseq1, nseq2) AND NOT length_gt(nseq1, nseq2)
  --    IMPLIES is_finite[T2](nseq2) AND nonempty?[T2](nseq2)
  have zip_last_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_last_TCC9
  -- Statement: FORALL (nseq1, nseq2):
  --   is_finite(zip(nseq1, nseq2)) IMPLIES
  --    NOT (length_lt[T1, T2](nseq1, nseq2) AND length_gt[T1, T2](nseq1, nseq2))
  have zip_last_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_zip.zip_extensionality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cseq1, cseq3: csequence[T1]), (cseq2, cseq4: csequence[T2]):
--   length_eq(cseq1, cseq2) AND
--    length_eq(cseq3, cseq4) AND zip(cseq1, cseq2) = zip(cseq3, cseq4)
--    IMPLIES cseq1 = cseq3 AND cse
theorem zip_extensionality : ∀ (cseq1 : csequence T1) (cseq3 : csequence T1) (cseq2 : csequence T2) (cseq4 : csequence T2), length_eq cseq1 cseq2 ∧
 length_eq cseq3 cseq4 ∧ zip cseq1 cseq2 = zip cseq3 cseq4 → cseq1 = cseq3 ∧ cseq2 = cseq4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_zip.zip_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, p1, p2:
--   some(LAMBDA t: p1(t`1) OR p2(t`2))(zip(cseq1, cseq2)) IFF
--    COND length_lt(cseq1, cseq2) ->
--           some(p1)(cseq1) OR
--            (EXISTS (i: indexes(cseq2)):
--        
theorem zip_some : ∀ (cseq1 : csequence T1) (cseq2 : csequence T2) (p1 : T1 → Bool) (p2 : T2 → Bool), some (fun t=>  p1 t.1 ∨ p2 t.2) (zip cseq1 cseq2) ↔ COND length_lt cseq1 cseq2: indexes cseq2):
           i < List.length cseq1 ∧ p2 (nth cseq2 i)),
     length_gt cseq1 cseq2: indexes cseq1):
          i < List.length cseq2 ∧ p1 (nth cseq1 i))
        ∨ some p2(cseq2),
     else -> some p1(cseq1) ∨ some p2(cseq2)
ENDCOND := by
  -- === TCCs for zip_some ===
  -- TCC: zip_some_TCC1
  -- Statement: FORALL (cseq1, cseq2, p1):
  --   length_lt(cseq1, cseq2) AND NOT some(p1)(cseq1) IMPLIES
  --    FORALL (i: indexes[T2](cseq2)): is_finite[T1](cseq1)
  have zip_some_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_some_TCC2
  -- Statement: FORALL (cseq1, cseq2):
  --   NOT length_lt(cseq1, cseq2) AND length_gt(cseq1, cseq2) IMPLIES
  --    FORALL (i: indexes[T1](cseq1)): is_finite[T2](cseq2)
  have zip_some_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zip_some_TCC3
  -- Statement: FORALL (cseq1, cseq2):
  --   NOT (length_lt[T1, T2](cseq1, cseq2) AND length_gt[T1, T2](cseq1, cseq2))
  have zip_some_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_zip.zip_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2, p1, p2:
--   every(LAMBDA t: p1(t`1) AND p2(t`2))(zip(cseq1, cseq2)) IFF
--    COND length_lt(cseq1, cseq2) ->
--           every(p1)(cseq1) AND
--            (FORALL (i: indexes(cseq2)):
--    
theorem zip_every : ∀ (cseq1 : csequence T1) (cseq2 : csequence T2) (p1 : T1 → Bool) (p2 : T2 → Bool), every (fun t=>  p1 t.1 ∧ p2 t.2) (zip cseq1 cseq2) ↔ COND length_lt cseq1 cseq2: indexes cseq2):
           i < List.length cseq1 → p2 (nth cseq2 i)),
     length_gt cseq1 cseq2: indexes cseq1):
          i < List.length cseq2 → p1 (nth cseq1 i))
        ∧ every p2(cseq2),
     else -> every p1(cseq1) ∧ every p2(cseq2)
ENDCOND := by
  -- === TCCs for zip_every ===
  -- TCC: zip_every_TCC1
  -- Statement: FORALL (cseq1, cseq2, p1):
  --   length_lt(cseq1, cseq2) AND every(p1)(cseq1) IMPLIES
  --    FORALL (i: indexes[T2](cseq2)): is_finite[T1](cseq1)
  have zip_every_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_zip