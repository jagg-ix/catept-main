-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_filter_of
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_filter_of

Auto-generated stubs from PVSLib theory `csequence_filter_of`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_filter_of

noncomputable section

variable {pvs_τ1 : Type*} (empty_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (every : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ7 : Type*} (first : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (first_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} (is_finite : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} (nonempty_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (nth : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (pred_AND : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (pred_IMPLIES : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (rest : pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (pvs_some : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (suffix : pvs_τ26 → pvs_τ27 → pvs_τ28)

-- pvs2lean: csequence_filter_of.filter_of?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (empty?(filt_seq) AND NOT some(p)(cseq)) OR
--  (nonempty?(filt_seq) AND
--    some(p)(cseq) AND
--     IF p(first(cseq))
--       THEN first(filt_seq) = first(cseq) AND
--             filter_of?(rest(filt_seq), res
axiom filter_of_p {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: csequence_filter_of.filter_of?_weak_coinduction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, P: [[csequence[T], csequence[T]] -> boolean]):
--   (FORALL (filt_seq, cseq: csequence[T]):
--      P(filt_seq, cseq) IMPLIES
--       (empty?(filt_seq) AND NOT some(p)(cseq)) OR
--        (nonempty?(f
theorem filter_of_p_weak_coinduction : ∀ (p : T → Bool) (P : csequence T × csequence T → Bool), ∀ (filt_seq : _) (cseq : csequence T), P filt_seq cseq →
   (empty_p filt_seq ∧ ¬ some p(cseq)) ∨
    (nonempty_p filt_seq ∧
      some p(cseq) ∧
       if p (first cseq)
         then first filt_seq = first cseq ∧
               P (rest filt_seq) (rest cseq)
       else P filt_seq (rest cseq)
       ) → ∀ (filt_seq : _) (cseq : csequence T), P filt_seq cseq → filter_of_p filt_seq cseq p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_coinduction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, P: [[csequence[T], csequence[T]] -> boolean]):
--   (FORALL (filt_seq, cseq: csequence[T]):
--      P(filt_seq, cseq) IMPLIES
--       (empty?(filt_seq) AND NOT some(p)(cseq)) OR
--        (nonempty?(f
theorem filter_of_p_coinduction : ∀ (p : T → Bool) (P : csequence T × csequence T → Bool), ∀ (filt_seq : _) (cseq : csequence T), P filt_seq cseq →
   (empty_p filt_seq ∧ ¬ some p(cseq)) ∨
    (nonempty_p filt_seq ∧
      some p(cseq) ∧
       if p (first cseq)
         then first filt_seq = first cseq ∧
               (filter_of_p (rest filt_seq) (rest cseq) p ∨
                 P (rest filt_seq) (rest cseq))
       else filter_of_p filt_seq (rest cseq) p ∨ P filt_seq (rest cseq)
       ) → ∀ (filt_seq : _) (cseq : csequence T), P filt_seq cseq → filter_of_p filt_seq cseq p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq, cseq, p:
--   filter_of?(filt_seq, cseq, p) IMPLIES
--    (empty?(filt_seq) IFF NOT some(p)(cseq))
theorem filter_of_p_empty : ∀ (filt_seq : csequence T) (cseq : csequence T) (p : T → Bool), filter_of_p filt_seq cseq p → (empty_p filt_seq ↔ ¬ some p(cseq)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq, cseq, p:
--   filter_of?(filt_seq, cseq, p) IMPLIES
--    (nonempty?(filt_seq) IFF some(p)(cseq))
theorem filter_of_p_nonempty : ∀ (filt_seq : csequence T) (cseq : csequence T) (p : T → Bool), filter_of_p filt_seq cseq p → (nonempty_p filt_seq ↔ some p(cseq)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq, cseq, p:
--   filter_of?(filt_seq, cseq, p) AND is_finite(cseq) IMPLIES
--    is_finite(filt_seq)
theorem filter_of_p_finite : ∀ (filt_seq : csequence T) (cseq : csequence T) (p : T → Bool), filter_of_p filt_seq cseq p ∧ is_finite cseq → is_finite filt_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_def
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq, cseq, p:
--   filter_of?(filt_seq, cseq, p) IMPLIES every(p)(filt_seq)
theorem filter_of_p_def : ∀ (filt_seq : csequence T) (cseq : csequence T) (p : T → Bool), filter_of_p filt_seq cseq p → every p(filt_seq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_first_p
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq, cseq, p:
--   some(p)(cseq) AND filter_of?(filt_seq, cseq, p) IMPLIES
--    nth(cseq, first_p(p, cseq)) = first(filt_seq)
theorem filter_of_p_first_p : ∀ (filt_seq : csequence T) (cseq : csequence T) (p : T → Bool), some p(cseq) ∧ filter_of_p filt_seq cseq p → nth cseq (first_p p cseq) = first filt_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_suffix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq, cseq, p:
--   some(p)(cseq) AND filter_of?(filt_seq, cseq, p) IMPLIES
--    filter_of?(rest(filt_seq), suffix(cseq, 1 + first_p(p, cseq)), p)
theorem filter_of_p_suffix : ∀ (filt_seq : csequence T) (cseq : csequence T) (p : T → Bool), some p(cseq) ∧ filter_of_p filt_seq cseq p → filter_of_p (rest filt_seq) (suffix cseq (1 + first_p p cseq)) p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_injective
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq1, filt_seq2, cseq, p:
--   filter_of?(filt_seq1, cseq, p) AND filter_of?(filt_seq2, cseq, p) IMPLIES
--    filt_seq1 = filt_seq2
theorem filter_of_p_injective : ∀ (filt_seq1 : csequence T) (filt_seq2 : csequence T) (cseq : csequence T) (p : T → Bool), filter_of_p filt_seq1 cseq p ∧ filter_of_p filt_seq2 cseq p → filt_seq1 = filt_seq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_concatenate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq1, filt_seq2, cseq1, cseq2, p:
--   is_finite(cseq1) AND
--    filter_of?(filt_seq1, cseq1, p) AND filter_of?(filt_seq2, cseq2, p)
--    IMPLIES filter_of?(filt_seq1 o filt_seq2, cseq1 o cseq2, 
theorem filter_of_p_concatenate : ∀ (filt_seq1 : csequence T) (filt_seq2 : csequence T) (cseq1 : csequence T) (cseq2 : csequence T) (p : T → Bool), is_finite cseq1 ∧
 filter_of_p filt_seq1 cseq1 p ∧ filter_of_p filt_seq2 cseq2 p → filter_of_p (filt_seq1 ∘ filt_seq2) (cseq1 ∘ cseq2) p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_implication
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq, cseq, p, q:
--   (FORALL t: pred_IMPLIES(p, q)(t)) AND
--    some(p)(cseq) AND filter_of?(filt_seq, cseq, q)
--    IMPLIES some(p)(filt_seq)
theorem filter_of_p_implication : ∀ (filt_seq : csequence T) (cseq : csequence T) (p : T → Bool) (q : T → Bool), (∀ t,  pred_IMPLIES p q(t)) ∧
 some p(cseq) ∧ filter_of_p filt_seq cseq q → some p(filt_seq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_implication_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq, cseq, p, q:
--   filter_of?(filt_seq, cseq, p) AND some(q)(filt_seq) IMPLIES some(q)(cseq)
theorem filter_of_p_implication_rev : ∀ (filt_seq : csequence T) (cseq : csequence T) (p : T → Bool) (q : T → Bool), filter_of_p filt_seq cseq p ∧ some q(filt_seq) → some q(cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq1, filt_seq2, cseq, p, q:
--   filter_of?(filt_seq1, cseq, p) AND filter_of?(filt_seq2, filt_seq1, q)
--    IMPLIES
--    (some(p)(cseq) AND some(q)(filt_seq1) IFF some(pred_AND(p, q))(cseq))
theorem filter_of_p_some : ∀ (filt_seq1 : csequence T) (filt_seq2 : csequence T) (cseq : csequence T) (p : T → Bool) (q : T → Bool), filter_of_p filt_seq1 cseq p ∧ filter_of_p filt_seq2 filt_seq1 q → (some p(cseq) ∧ some q(filt_seq1) ↔ some (pred_AND p q) (cseq)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_filter_of.filter_of?_filter_of?
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL filt_seq1, filt_seq2, cseq, p, q:
--   filter_of?(filt_seq1, cseq, p) AND filter_of?(filt_seq2, filt_seq1, q)
--    IMPLIES filter_of?(filt_seq2, cseq, pred_AND(p, q))
theorem filter_of_p_filter_of_p : ∀ (filt_seq1 : csequence T) (filt_seq2 : csequence T) (cseq : csequence T) (p : T → Bool) (q : T → Bool), filter_of_p filt_seq1 cseq p ∧ filter_of_p filt_seq2 filt_seq1 q → filter_of_p filt_seq2 cseq (pred_AND p q) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_filter_of