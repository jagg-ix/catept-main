-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_map_props
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_map_props

Auto-generated stubs from PVSLib theory `csequence_map_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_map_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (empty_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (every : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (first : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (index_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (indexes : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (injective_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (is_finite : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (last : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (length : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (map : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} (nonempty_p : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (nth : pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (rest : pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (pvs_some : pvs_τ27 → pvs_τ28 → pvs_τ29)

-- pvs2lean: csequence_map_props.map_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq: empty?(map(f, cseq)) IFF empty?(cseq)
theorem map_empty : ∀ (f : T1 → T2) (cseq : csequence T1), empty_p (map f cseq) ↔ empty_p cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_map_props.map_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq: nonempty?(map(f, cseq)) IFF nonempty?(cseq)
theorem map_nonempty : ∀ (f : T1 → T2) (cseq : csequence T1), nonempty_p (map f cseq) ↔ nonempty_p cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_map_props.map_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, nseq: first(map(f, nseq)) = f(first(nseq))
theorem map_first : ∀ (f : T1 → T2) (nseq : csequence T1), first (map f nseq) = f (first nseq) := by
  -- === TCCs for map_first ===
  -- TCC: map_first_TCC1
  -- Statement: FORALL (f, nseq): nonempty?[T2](map[T1, T2](f, nseq))
  have map_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_map_props.map_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, nseq: rest(map(f, nseq)) = map(f, rest(nseq))
theorem map_rest : ∀ (f : T1 → T2) (nseq : csequence T1), rest (map f nseq) = map f (rest nseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_map_props.map_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, fseq: length(map(f, fseq)) = length(fseq)
theorem map_length : ∀ (f : T1 → T2) (fseq : csequence T1), List.length (map f fseq) = List.length fseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_map_props.map_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq: index?(map(f, cseq)) = index?(cseq)
theorem map_index : ∀ (f : T1 → T2) (cseq : csequence T1), index_p (map f cseq) = index_p cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_map_props.map_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq, (n: indexes(cseq)): nth(map(f, cseq), n) = f(nth(cseq, n))
theorem map_nth : ∀ (f : T1 → T2) (cseq : csequence T1) (n : {i : ℝ // i ≥ 0}), nth (map f cseq) n = f (nth cseq n) := by
  -- === TCCs for map_nth ===
  -- TCC: map_nth_TCC1
  -- Statement: FORALL (f, cseq, n: indexes[T1](cseq)): index?[T2](map[T1, T2](f, cseq))(n)
  have map_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_map_props.map_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq, t: add(f(t), map(f, cseq)) = map(f, add(t, cseq))
theorem map_add : ∀ (f : T1 → T2) (cseq : csequence T1) (t : T1), add (f t) (map f cseq) = map f (add t cseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_map_props.map_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, nfseq: last(map(f, nfseq)) = f(last(nfseq))
theorem map_last : ∀ (f : T1 → T2) (nfseq : {cseq : csequence T1 // nonempty_p cseq}), last (map f nfseq) = f (last nfseq) := by
  -- === TCCs for map_last ===
  -- TCC: map_last_TCC1
  -- Statement: FORALL (f, nfseq): nonempty?[T2](map[T1, T2](f, nfseq))
  have map_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_map_props.map_map
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq: map(f)(cseq) = map(f, cseq)
theorem map_map : ∀ (f : T1 → T2) (cseq : csequence T1), map f(cseq) = map f cseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_map_props.map_injective
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f: injective?(f) IMPLIES injective?(map(f))
theorem map_injective : ∀ (f : T1 → T2), injective_p f → injective_p (map f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_map_props.map_extensionality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq1, cseq2:
--   injective?(f) AND map(f, cseq1) = map(f, cseq2) IMPLIES cseq1 = cseq2
theorem map_extensionality : ∀ (f : T1 → T2) (cseq1 : csequence T1) (cseq2 : csequence T1), injective_p f ∧ map f cseq1 = map f cseq2 → cseq1 = cseq2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_map_props.map_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq, p: some(p)(map(f, cseq)) IFF some(p o f)(cseq)
theorem map_some : ∀ (f : T1 → T2) (cseq : csequence T1) (p : T2 → Bool), some p(map f cseq) ↔ some (p ∘ f) (cseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_map_props.map_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL f, cseq, p: every(p)(map(f, cseq)) IFF every(p o f)(cseq)
theorem map_every : ∀ (f : T1 → T2) (cseq : csequence T1) (p : T2 → Bool), every p(map f cseq) ↔ every (p ∘ f) (cseq) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_map_props