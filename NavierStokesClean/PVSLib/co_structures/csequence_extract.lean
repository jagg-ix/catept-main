-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_extract
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_extract

Auto-generated stubs from PVSLib theory `csequence_extract`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_extract

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (empty_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (every : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} (first : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (index_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (indexes : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (is_finite : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (last : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (length : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (max : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (min : pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} (nonempty_p : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (nth : pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} (rest : pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (pvs_some : pvs_τ26 → pvs_τ27 → pvs_τ28)

-- pvs2lean: csequence_extract.^
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (m, n) = range IN
--   IF empty?(cseq) OR m > n THEN empty_cseq
--   ELSIF m > 0 THEN rest(cseq) ^ (m - 1, n - 1)
--   ELSE add(first(cseq),
--            IF n = 0 THEN empty_cseq ELSE rest(cseq) ^ (m, n - 1)
noncomputable def pvs_op_7e6a2a : sorry := by
  sorry  -- pvs2lean: definition stub for LET (m, n) = range IN   IF empty?(cseq) OR m > n THEN empty_cseq   ELSIF m > 0 T

-- pvs2lean: csequence_extract.extract_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, range:
--   empty?(cseq ^ range) IFF
--    range`1 > range`2 OR (is_finite(cseq) AND NOT index?(cseq)(range`1))
theorem extract_empty : ∀ (cseq : csequence T) (range : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0}), empty_p (cseq ^ range) ↔ range.1 > range.2 ∨ (is_finite cseq ∧ ¬ index_p cseq(range.1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, range:
--   nonempty?(cseq ^ range) IFF
--    range`1 <= range`2 AND (NOT is_finite(cseq) OR index?(cseq)(range`1))
theorem extract_nonempty : ∀ (cseq : csequence T) (range : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0}), nonempty_p (cseq ^ range) ↔ range.1 ≤ range.2 ∧ (¬ is_finite cseq ∨ index_p cseq(range.1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, range:
--   length(cseq ^ range) =
--    IF (range`1 > range`2 OR (is_finite(cseq) AND range`1 >= length(cseq)))
--      THEN 0
--    ELSIF is_finite(cseq) AND range`2 >= length(cseq)
--      THEN lengt
theorem extract_length : ∀ (cseq : csequence T) (range : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0}), List.length (cseq ^ range) = if (range.1 > range.2 ∨ (is_finite cseq ∧ range.1 ≥ List.length cseq))
  then 0
ELSIF is_finite cseq ∧ range.2 ≥ List.length cseq
  then List.length cseq - range.1
else range.2 - range.1 + 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nfseq, n: n >= length(nfseq) - 1 IMPLIES nfseq ^ (0, n) = nfseq
theorem extract_def : ∀ (nfseq : {cseq : csequence T // nonempty_p cseq}) (n : {i : ℝ // i ≥ 0}), n ≥ List.length nfseq - 1 → nfseq ^ (0, n) = nfseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_extract.extract_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL nfseq: nfseq ^ (1, length(nfseq) - 1) = rest(nfseq)
theorem extract_rest : ∀ (nfseq : {cseq : csequence T // nonempty_p cseq}), nfseq ^ (1, List.length nfseq - 1) = rest nfseq := by
  -- === TCCs for extract_rest ===
  -- TCC: extract_rest_TCC1
  -- Statement: FORALL (nfseq): length[T](nfseq) - 1 >= 0
  have extract_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_extract.extract_rest2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, m, n:
--   nonempty?(cseq ^ (m, n)) IMPLIES rest(cseq ^ (m, n)) = cseq ^ (m + 1, n)
theorem extract_rest2 : ∀ (cseq : csequence T) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}), nonempty_p (cseq ^ (m, n)) → rest (cseq ^ (m, n)) = cseq ^ (m + 1, n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_extract
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, range, range2:
--   (cseq ^ range) ^ range2 =
--    cseq ^ (range`1 + range2`1, min(range`2, range`1 + range2`2))
theorem extract_extract : ∀ (cseq : csequence T) (range : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0}) (range2 : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0}), (cseq ^ range) ^ range2 = cseq ^ (range.1 + range2.1, min range.2 (range.1 + range2.2)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_extract.extract_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, m, n, index:
--   index?(cseq ^ (m, n))(index) IFF
--    m + index <= n AND index?(cseq)(m + index)
theorem extract_index : ∀ (cseq : csequence T) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (index : {i : ℝ // i ≥ 0}), index_p (cseq ^ (m, n)) (index) ↔ m + index ≤ n ∧ index_p cseq(m + index) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, m, n:
--   nonempty?(cseq ^ (m, n)) IMPLIES first(cseq ^ (m, n)) = nth(cseq, m)
theorem extract_first : ∀ (cseq : csequence T) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}), nonempty_p (cseq ^ (m, n)) → first (cseq ^ (m, n)) = nth cseq m := by
  -- === TCCs for extract_first ===
  -- TCC: extract_first_TCC1
  -- Statement: FORALL (cseq, m, n): nonempty?(cseq ^ (m, n)) IMPLIES index?[T](cseq)(m)
  have extract_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, (m: indexes(cseq)), (n: nat), (i: indexes(cseq ^ (m, n))):
--   nth(cseq ^ (m, n), i) = nth(cseq, m + i)
theorem extract_nth : ∀ (cseq : csequence T) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (i : {i : ℝ // i ≥ 0}), nth (cseq ^ (m, n)) i = nth cseq (m + i) := by
  -- === TCCs for extract_nth ===
  -- TCC: extract_nth_TCC1
  -- Statement: FORALL (cseq, m: indexes[T](cseq), n: nat, i: indexes[T](cseq ^ (m, n))):
  --   index?[T](cseq)(m + i)
  have extract_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_extract.extract_add
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, t, m, n:
--   add(t, cseq) ^ (m, n) =
--    IF m > n THEN empty_cseq
--    ELSIF m > 0 THEN cseq ^ (m - 1, max(0, n - 1))
--    ELSIF n > 0 THEN add(t, cseq ^ (m, n - 1))
--    ELSE add(t, empty_cseq)
--  
theorem extract_add : ∀ (cseq : csequence T) (t : T) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}), add t cseq ^ (m, n) = if m > n then empty_cseq
ELSIF m > 0 then cseq ^ (m - 1, max 0 (n - 1))
ELSIF n > 0 then add t (cseq ^ (m, n - 1))
else add t empty_cseq := by
  -- === TCCs for extract_add ===
  -- TCC: extract_add_TCC1
  -- Statement: FORALL (t, m, n): NOT m > n AND m > 0 IMPLIES m - 1 >= 0
  have extract_add_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: extract_add_TCC2
  -- Statement: FORALL (t, m, n): NOT m > n AND m > 0 IMPLIES max(0, n - 1) >= 0
  have extract_add_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: extract_add_TCC3
  -- Statement: FORALL (t, m, n): NOT m > n AND NOT m > 0 AND n > 0 IMPLIES n - 1 >= 0
  have extract_add_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, m, n:
--   nonempty?(cseq ^ (m, n)) IMPLIES
--    last(cseq ^ (m, n)) =
--     IF index?(cseq)(n) THEN nth(cseq, n) ELSE last(cseq) ENDIF
theorem extract_last : ∀ (cseq : csequence T) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}), nonempty_p (cseq ^ (m, n)) → last (cseq ^ (m, n)) =
 if index_p cseq(n) then nth cseq n else last cseq := by
  -- === TCCs for extract_last ===
  -- TCC: extract_last_TCC1
  -- Statement: FORALL (cseq, m, n):
  --   nonempty?(cseq ^ (m, n)) AND NOT index?(cseq)(n) IMPLIES
  --    is_finite[T](cseq) AND nonempty?[T](cseq)
  have extract_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_extensionality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq1, cseq2:
--   (FORALL range: cseq1 ^ range = cseq2 ^ range) IMPLIES cseq1 = cseq2
theorem extract_extensionality : ∀ (cseq1 : csequence T) (cseq2 : csequence T), (∀ range,  cseq1 ^ range = cseq2 ^ range) → cseq1 = cseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_extract.extract_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, m, n, p:
--   some(p)(cseq ^ (m, n)) IFF
--    (EXISTS index:
--       m <= index AND
--        index <= n AND index?(cseq)(index) AND p(nth(cseq, index)))
theorem extract_some : ∀ (cseq : csequence T) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (p : T → Bool), some p(cseq ^ (m, n)) ↔ (∃ index, 
   m ≤ index ∧
    index ≤ n ∧ index_p cseq(index) ∧ p (nth cseq index)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_extract.extract_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, m, n, p:
--   every(p)(cseq ^ (m, n)) IFF
--    (FORALL index:
--       m <= index AND index <= n AND index?(cseq)(index) IMPLIES
--        p(nth(cseq, index)))
theorem extract_every : ∀ (cseq : csequence T) (m : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}) (p : T → Bool), every p(cseq ^ (m, n)) ↔ (∀ index, 
   m ≤ index ∧ index ≤ n ∧ index_p cseq(index) →
    p (nth cseq index)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_extract