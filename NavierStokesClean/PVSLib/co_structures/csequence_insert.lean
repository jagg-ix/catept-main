-- Auto-generated by pvs2lean/translator.py
-- PVS theory: csequence_insert
-- PVSLib library: co_structures
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: csequence_insert

Auto-generated stubs from PVSLib theory `csequence_insert`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.csequence_insert

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (empty_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (every : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (first : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (index_p : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (indexes : pvs_τ11 → pvs_τ12)
variable {pvs_τ16 : Type*} (is_finite : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (last : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (length : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (nth : pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (rest : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (pvs_some : pvs_τ25 → pvs_τ26 → pvs_τ27)

-- pvs2lean: csequence_insert.insert
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF index = 0 OR empty?(cseq) THEN add(t, cseq)
-- ELSE add(first(cseq), insert(t, index - 1, rest(cseq)))
-- ENDIF
axiom insert {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15 → Prop

-- pvs2lean: csequence_insert.insert_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, index, cseq:
--   first(insert(t, index, cseq)) =
--    IF index = 0 OR empty?(cseq) THEN t ELSE first(cseq) ENDIF
theorem insert_first : ∀ (t : T) (index : {i : ℝ // i ≥ 0}) (cseq : csequence T), first (insert t index cseq) = if index = 0 ∨ empty_p cseq then t else first cseq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_insert.insert_rest
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, index, cseq:
--   rest(insert(t, index, cseq)) =
--    IF index = 0 OR empty?(cseq) THEN cseq
--    ELSE insert(t, index - 1, rest(cseq))
--    ENDIF
theorem insert_rest : ∀ (t : T) (index : {i : ℝ // i ≥ 0}) (cseq : csequence T), rest (insert t index cseq) = if index = 0 ∨ empty_p cseq then cseq
else insert t (index - 1) (rest cseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_insert.insert_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL fseq, t, index: length(insert(t, index, fseq)) = length(fseq) + 1
theorem insert_length : ∀ (fseq : csequence T) (t : T) (index : {i : ℝ // i ≥ 0}), List.length (insert t index fseq) = List.length fseq + 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_insert.insert_index
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, index, cseq, n:
--   index?(insert(t, index, cseq))(n) IFF n = 0 OR index?(cseq)(n - 1)
theorem insert_index : ∀ (t : T) (index : {i : ℝ // i ≥ 0}) (cseq : csequence T) (n : {i : ℝ // i ≥ 0}), index_p (insert t index cseq) (n) ↔ n = 0 ∨ index_p cseq(n - 1) := by
  -- === TCCs for insert_index ===
  -- TCC: insert_index_TCC1
  -- Statement: FORALL (t, n): NOT n = 0 IMPLIES n - 1 >= 0
  have insert_index_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_insert.insert_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL cseq, t, index, (n: indexes(cseq)):
--   nth(cseq, n) =
--    nth(insert(t, index, cseq), n + IF n < index THEN 0 ELSE 1 ENDIF)
theorem insert_nth : ∀ (cseq : csequence T) (t : T) (index : {i : ℝ // i ≥ 0}) (n : {i : ℝ // i ≥ 0}), nth cseq n = nth (insert t index cseq) (n + if n < index then 0 else 1) := by
  -- === TCCs for insert_nth ===
  -- TCC: insert_nth_TCC1
  -- Statement: FORALL (cseq, t, index, n: indexes[T](cseq)):
  --   index?[T](insert(t, index, cseq))(n + IF n < index THEN 0 ELSE 1 ENDIF)
  have insert_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_insert.insert_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, cseq: insert(t, 0, cseq) = add(t, cseq)
theorem insert_0 : ∀ (t : T) (cseq : csequence T), insert t 0 cseq = add t cseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_insert.insert_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, u, index, cseq:
--   add(u, insert(t, index, cseq)) = insert(t, index + 1, add(u, cseq))
theorem insert_add : ∀ (t : T) (u : T) (index : {i : ℝ // i ≥ 0}) (cseq : csequence T), add u (insert t index cseq) = insert t (index + 1) (add u cseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_insert.insert_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, index, fseq:
--   last(insert(t, index, fseq)) =
--    IF index?(fseq)(index) THEN last(fseq) ELSE t ENDIF
theorem insert_last : ∀ (t : T) (index : {i : ℝ // i ≥ 0}) (fseq : csequence T), last (insert t index fseq) = if index_p fseq(index) then last fseq else t := by
  -- === TCCs for insert_last ===
  -- TCC: insert_last_TCC1
  -- Statement: FORALL (t, index, fseq): index?(fseq)(index) IMPLIES nonempty?[T](fseq)
  have insert_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_insert.insert_beyond
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, index, fseq:
--   index > length(fseq) IMPLIES
--    insert(t, index, fseq) = insert(t, length(fseq), fseq)
theorem insert_beyond : ∀ (t : T) (index : {i : ℝ // i ≥ 0}) (fseq : csequence T), index > List.length fseq → insert t index fseq = insert t (List.length fseq) fseq := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_insert.insert_insert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, u, n, m, cseq:
--   insert(t, n, insert(u, m, cseq)) =
--    IF is_finite(cseq) AND n > length(cseq) AND m > length(cseq)
--      THEN insert(u, length(cseq), insert(t, n, cseq))
--    ELSIF n <= m THEN
theorem insert_insert : ∀ (t : T) (u : T) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}) (cseq : csequence T), insert t n (insert u m cseq) = if is_finite cseq ∧ n > List.length cseq ∧ m > List.length cseq
  then insert u (List.length cseq) (insert t n cseq)
ELSIF n ≤ m then insert u (m + 1) (insert t n cseq)
else insert u m (insert t (n - 1) cseq) := by
  -- === TCCs for insert_insert ===
  -- TCC: insert_insert_TCC1
  -- Statement: FORALL (t, u, n, m, cseq):
  --   NOT (is_finite(cseq) AND n > length(cseq) AND m > length(cseq)) AND
  --    NOT n <= m
  --    IMPLIES n - 1 >= 0
  have insert_insert_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: csequence_insert.insert_extensionality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, index, cseq1, cseq2:
--   insert(t, index, cseq1) = insert(t, index, cseq2) IMPLIES cseq1 = cseq2
theorem insert_extensionality : ∀ (t : T) (index : {i : ℝ // i ≥ 0}) (cseq1 : csequence T) (cseq2 : csequence T), insert t index cseq1 = insert t index cseq2 → cseq1 = cseq2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_insert.insert_some
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, index, cseq:
--   some(p)(insert(t, index, cseq)) IFF p(t) OR some(p)(cseq)
theorem insert_some : ∀ (t : T) (index : {i : ℝ // i ≥ 0}) (cseq : csequence T), some p(insert t index cseq) ↔ p t ∨ some p(cseq) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: csequence_insert.insert_every
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, index, cseq:
--   every(p)(insert(t, index, cseq)) IFF p(t) AND every(p)(cseq)
theorem insert_every : ∀ (t : T) (index : {i : ℝ // i ≥ 0}) (cseq : csequence T), every p(insert t index cseq) ↔ p t ∧ every p(cseq) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.csequence_insert