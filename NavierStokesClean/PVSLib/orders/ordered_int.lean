-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ordered_int
-- PVSLib library: orders
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ordered_int

Auto-generated stubs from PVSLib theory `ordered_int`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ordered_int

noncomputable section

variable {pvs_τ7 : Type*} (empty_p : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (full_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (has_greatest_p : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (has_least_p : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (is_finite : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (unrelated : pvs_τ14 → pvs_τ15 → pvs_τ16)

-- pvs2lean: ordered_int.upto
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: upto(i, restrict[[real, real], [int, int], bool](<=))
axiom upto {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ20 → pvs_τ21 → pvs_τ22

-- pvs2lean: ordered_int.below
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: below(i, restrict[[real, real], [int, int], bool](<=))
axiom below {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6

-- pvs2lean: ordered_int.above
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: above(i, restrict[[real, real], [int, int], bool](<=))
axiom above {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3

-- pvs2lean: ordered_int.upfrom
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: upfrom(i, restrict[[real, real], [int, int], bool](<=))
axiom upfrom {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19

-- pvs2lean: ordered_int.prefix_empty
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL pre: is_finite(pre) IMPLIES empty?(pre)
theorem prefix_empty : ∀ (pre : {S : ℝ → Bool // prefix_p S (restrict ≤)}), is_finite pre → empty_p pre := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_int.prefix_below
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL pre:
--   empty?(pre) OR
--    full?(pre) OR
--     (EXISTS i: pre = below(i, restrict[[real, real], [int, int], bool](<=)))
theorem prefix_below : ∀ (pre : {S : ℝ → Bool // prefix_p S (restrict ≤)}), empty_p pre ∨ full_p pre ∨
 (∃ i,  pre = Finset.range i, restrict ≤) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_int.prefix_upto
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL pre:
--   empty?(pre) OR
--    full?(pre) OR
--     (EXISTS i: pre = upto(i, restrict[[real, real], [int, int], bool](<=)))
theorem prefix_upto : ∀ (pre : {S : ℝ → Bool // prefix_p S (restrict ≤)}), empty_p pre ∨ full_p pre ∨
 (∃ i,  pre = upto i (restrict ≤)) := by
  -- === TCCs for prefix_upto ===
  -- TCC: prefix_upto_TCC1
  -- Statement: FORALL (pre):
  --   is_finite(pre) AND NOT empty?(pre) IMPLIES card[nat](pre) - 1 >= 0
  have prefix_upto_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_int.suffix_empty
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL suf: is_finite(suf) IMPLIES empty?(suf)
theorem suffix_empty : ∀ (suf : {S : ℝ → Bool // suffix_p S (restrict ≤)}), is_finite suf → empty_p suf := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_int.suffix_upfrom
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL suf:
--   empty?(suf) OR
--    full?(suf) OR
--     (EXISTS i:
--        suf = upfrom(i, restrict[[real, real], [int, int], bool](<=)))
theorem suffix_upfrom : ∀ (suf : {S : ℝ → Bool // suffix_p S (restrict ≤)}), empty_p suf ∨ full_p suf ∨
 (∃ i,  suf = upfrom i (restrict ≤)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_int.suffix_above
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL suf:
--   empty?(suf) OR
--    full?(suf) OR
--     (EXISTS i: suf = above(i, restrict[[real, real], [int, int], bool](<=)))
theorem suffix_above : ∀ (suf : {S : ℝ → Bool // suffix_p S (restrict ≤)}), empty_p suf ∨ full_p suf ∨
 (∃ i,  suf = above i (restrict ≤)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_int.unrelated_empty
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL i: empty?(unrelated(i, restrict[[real, real], [int, int], bool](<=)))
theorem unrelated_empty : ∀ (i : ℝ), empty_p (unrelated i (restrict ≤)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.ordered_int