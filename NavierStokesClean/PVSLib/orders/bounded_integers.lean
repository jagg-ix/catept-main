-- Auto-generated by pvs2lean/translator.py
-- PVS theory: bounded_integers
-- PVSLib library: orders
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: bounded_integers

Auto-generated stubs from PVSLib theory `bounded_integers`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.bounded_integers

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (disjoint_p : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (has_greatest_p : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (has_least_p : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (intersection : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (nonempty_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (remove : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (subset_p : pvs_τ14 → pvs_τ15 → Prop)

-- pvs2lean: bounded_integers.T_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: bounded_integers.non_empty_bounded_above_has_greatest_lem
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: S(i) &
--  upper_bound?[T](j, S, restrict[[real, real], [T, T], bool](<=)) &
--   c = j - i
--  => has_greatest?(S, restrict[[real, real], [T, T], bool](<=))
theorem non_empty_bounded_above_has_greatest_lem : S i &
 upper_bound_p j S (restrict[[real, real], [T, T], bool](≤)) & c = j - i → has_greatest_p S (restrict ≤) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_integers.non_empty_bounded_below_has_least_lem
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: S(i) &
--  lower_bound?[T](j, S, restrict[[real, real], [T, T], bool](<=)) &
--   c = i - j
--  => has_least?(S, restrict[[real, real], [T, T], bool](<=))
theorem non_empty_bounded_below_has_least_lem : S i &
 lower_bound_p j S (restrict[[real, real], [T, T], bool](≤)) & c = i - j → has_least_p S (restrict ≤) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_integers.remove_least
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(remove(least[T](restrict[[real, real], [T, T], bool](<=))(Sl),
--                  Sl))
--  =>
--  least[T](restrict[[real, real], [T, T], bool](<=))(Sl) <
--   least[T]
--       (restrict[[real, real], [T
theorem remove_least : nonempty_p (remove (least (restrict[[real, real], [T, T], bool](≤)) (Sl)) Sl) → least (restrict[[real, real], [T, T], bool](≤)) (Sl) <
 least
     (restrict ≤)
     (remove (least (restrict[[real, real], [T, T], bool](≤)) (Sl)) Sl) := by
  -- === TCCs for remove_least ===
  -- TCC: remove_least_TCC1
  -- Statement: FORALL (Sl: (non_empty_bounded_below?)):
  --   nonempty?(remove(least[T](restrict[[real, real], [T, T], bool](<=))(Sl),
  --                    Sl))
  --    IMPLIES
  --    has_least?[T]
  --        (remove[T]
  --            ...
  have remove_least_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_integers.remove_least_monotone
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Sl1, Sl2) =>
--  subset?(remove(least[T](restrict[[real, real], [T, T], bool](<=))(Sl1),
--                 Sl1),
--          remove(least[T](restrict[[real, real], [T, T], bool](<=))(Sl2),
--            
theorem remove_least_monotone : subset_p Sl1 Sl2 → subset_p (remove (least (restrict[[real, real], [T, T], bool](≤)) (Sl1)) Sl1) (remove (least (restrict[[real, real], [T, T], bool](≤)) (Sl2)) Sl2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_integers.remove_greatest
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(remove(greatest[T](restrict[[real, real], [T, T], bool](<=))(Su),
--                  Su))
--  =>
--  greatest[T]
--      (restrict[[real, real], [T, T], bool](<=))
--      (remove(greatest[T](restrict[[re
theorem remove_greatest : nonempty_p (remove (greatest (restrict[[real, real], [T, T], bool](≤)) (Su)) Su) → greatest
    (restrict ≤)
    (remove (greatest (restrict[[real, real], [T, T], bool](≤)) (Su)) Su)
 < greatest (restrict[[real, real], [T, T], bool](≤)) (Su) := by
  -- === TCCs for remove_greatest ===
  -- TCC: remove_greatest_TCC1
  -- Statement: FORALL (Su: (non_empty_bounded_above?)):
  --   nonempty?(remove(greatest[T]
  --                        (restrict[[real, real], [T, T], bool](<=))(Su),
  --                    Su))
  --    IMPLIES
  --    has_greatest?[T]...
  have remove_greatest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_integers.remove_greatest_monotone
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Su1, Su2) =>
--  subset?(remove(greatest[T](restrict[[real, real], [T, T], bool](<=))(Su1),
--                 Su1),
--          remove(greatest[T](restrict[[real, real], [T, T], bool](<=))(Su2),
--      
theorem remove_greatest_monotone : subset_p Su1 Su2 → subset_p (remove (greatest (restrict[[real, real], [T, T], bool](≤)) (Su1)) Su1) (remove (greatest (restrict[[real, real], [T, T], bool](≤)) (Su2)) Su2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_integers.intersection_greatest
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(Su1, Su2) OR
--  has_greatest?(intersection(Su1, Su2),
--                restrict[[real, real], [T, T], bool](<=))
theorem intersection_greatest : disjoint_p Su1 Su2 ∨ has_greatest_p (intersection Su1 Su2) (restrict ≤) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_integers.intersection_least
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(Sl1, Sl2) OR
--  has_least?(intersection(Sl1, Sl2),
--             restrict[[real, real], [T, T], bool](<=))
theorem intersection_least : disjoint_p Sl1 Sl2 ∨ has_least_p (intersection Sl1 Sl2) (restrict ≤) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.bounded_integers