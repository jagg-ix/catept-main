-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ordered_nat
-- PVSLib library: orders
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ordered_nat

Auto-generated stubs from PVSLib theory `ordered_nat`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ordered_nat

noncomputable section

variable {pvs_τ7 : Type*} (bijective_p : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (card : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (empty_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (extend : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} (full_p : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} (greatest : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (has_greatest_p : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (has_least_p : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} (is_finite : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (least : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} (surjective_p : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (unrelated : pvs_τ23 → pvs_τ24 → pvs_τ25)

-- pvs2lean: ordered_nat.lesseq
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: restrict[[real, real], [nat, nat], boolean](<=)
noncomputable def lesseq : sorry := by
  sorry  -- pvs2lean: definition stub for restrict[[real, real], [nat, nat], boolean](<=)

-- pvs2lean: ordered_nat.upto
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: upto(n, restrict[[real, real], [nat, nat], boolean](<=))
axiom upto {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31

-- pvs2lean: ordered_nat.below
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: below(n, restrict[[real, real], [nat, nat], boolean](<=))
axiom below {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6

-- pvs2lean: ordered_nat.above
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: above(n, restrict[[real, real], [nat, nat], boolean](<=))
axiom above {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3

-- pvs2lean: ordered_nat.upfrom
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: upfrom(n, restrict[[real, real], [nat, nat], boolean](<=))
axiom upfrom {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ26 → pvs_τ27 → pvs_τ28

-- pvs2lean: ordered_nat.nonzero_below_greatest
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   n > 0 IMPLIES
--    has_greatest?(below(n, restrict[[real, real], [nat, nat], boolean](<=)),
--                  restrict[[real, real], [nat, nat], boolean](<=))
--     AND
--     greatest(restrict[[r
theorem nonzero_below_greatest : ∀ (n : {i : ℝ // i ≥ 0}), n > 0 → has_greatest_p (Finset.range n) (restrict ≤) (restrict ≤)
 ∧
 greatest (restrict ≤)
         (Finset.range n, restrict ≤)
  = n - 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.upto_greatest
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   greatest(restrict[[real, real], [nat, nat], boolean](<=))
--           (upto(n, restrict[[real, real], [nat, nat], boolean](<=)))
--    = n
theorem upto_greatest : ∀ (n : {i : ℝ // i ≥ 0}), greatest (restrict ≤)
        (upto n (restrict ≤)) = n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_nat.above_least
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   least(restrict[[real, real], [nat, nat], boolean](<=))
--        (above(n, restrict[[real, real], [nat, nat], boolean](<=)))
--    = n + 1
theorem above_least : ∀ (n : {i : ℝ // i ≥ 0}), least (restrict ≤)
     (above n (restrict ≤)) = n + 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_nat.upfrom_least
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   least(restrict[[real, real], [nat, nat], boolean](<=))
--        (upfrom(n, restrict[[real, real], [nat, nat], boolean](<=)))
--    = n
theorem upfrom_least : ∀ (n : {i : ℝ // i ≥ 0}), least (restrict ≤)
     (upfrom n (restrict ≤)) = n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_nat.below_prelude
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   below(n, restrict[[real, real], [nat, nat], boolean](<=)) =
--    extend[nat, below(n), bool, FALSE](fullset[below(n)])
theorem below_prelude : ∀ (n : {i : ℝ // i ≥ 0}), Finset.range n, restrict ≤ = extend Set.univ := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_nat.upto_prelude
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   upto(n, restrict[[real, real], [nat, nat], boolean](<=)) =
--    extend[nat, upto(n), bool, FALSE](fullset[upto(n)])
theorem upto_prelude : ∀ (n : {i : ℝ // i ≥ 0}), upto n (restrict ≤) = extend Set.univ := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_nat.prefix_finite
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL pre: is_finite(pre) OR full?(pre)
theorem prefix_finite : ∀ (pre : {S : {i : ℝ // i ≥ 0} → Bool // prefix_p S (restrict ≤)}), is_finite pre ∨ full_p pre := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.prefix_below
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL pre:
--   is_finite(pre) IMPLIES
--    pre = below(card(pre), restrict[[real, real], [nat, nat], boolean](<=))
theorem prefix_below : ∀ (pre : {S : {i : ℝ // i ≥ 0} → Bool // prefix_p S (restrict ≤)}), is_finite pre → pre = Finset.range card pre (restrict ≤) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.prefix_upto
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL pre:
--   is_finite(pre) IMPLIES
--    empty?(pre) OR
--     pre =
--      upto(card(pre) - 1, restrict[[real, real], [nat, nat], boolean](<=))
theorem prefix_upto : ∀ (pre : {S : {i : ℝ // i ≥ 0} → Bool // prefix_p S (restrict ≤)}), is_finite pre → empty_p pre ∨
 pre = upto (card pre - 1) (restrict ≤) := by
  -- === TCCs for prefix_upto ===
  -- TCC: prefix_upto_TCC1
  -- Statement: FORALL (pre):
  --   is_finite(pre) AND NOT empty?(pre) IMPLIES card[nat](pre) - 1 >= 0
  have prefix_upto_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.below_prelude_is_finite
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(fullset[below(n)])
theorem below_prelude_is_finite : is_finite fullset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.below_card
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   card(below(n, restrict[[real, real], [nat, nat], boolean](<=))) = n
theorem below_card : ∀ (n : {i : ℝ // i ≥ 0}), card (Finset.range n) (restrict ≤) = n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_nat.upto_card
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   card(upto(n, restrict[[real, real], [nat, nat], boolean](<=))) = 1 + n
theorem upto_card : ∀ (n : {i : ℝ // i ≥ 0}), card (upto n (restrict ≤)) = 1 + n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_nat.below_prelude_card
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n: card(extend[nat, below(n), bool, FALSE](fullset[below(n)])) = n
theorem below_prelude_card : ∀ (n : {i : ℝ // i ≥ 0}), card (extend Set.univ) = n := by
  -- === TCCs for below_prelude_card ===
  -- TCC: below_prelude_card_TCC1
  -- Statement: FORALL (n):
  --   is_finite[nat](extend[nat, below(n), bool, FALSE](fullset[below(n)]))
  have below_prelude_card_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_nat.suffix_empty
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL suf: is_finite(suf) IMPLIES empty?(suf)
theorem suffix_empty : ∀ (suf : {S : {i : ℝ // i ≥ 0} → Bool // suffix_p S (restrict ≤)}), is_finite suf → empty_p suf := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.suffix_upfrom
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL suf:
--   empty?(suf) OR
--    (EXISTS n:
--       suf = upfrom(n, restrict[[real, real], [nat, nat], boolean](<=)))
theorem suffix_upfrom : ∀ (suf : {S : {i : ℝ // i ≥ 0} → Bool // suffix_p S (restrict ≤)}), empty_p suf ∨ (∃ n,  suf = upfrom n (restrict ≤)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.suffix_above
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL suf:
--   empty?(suf) OR
--    full?(suf) OR
--     (EXISTS n:
--        suf = above(n, restrict[[real, real], [nat, nat], boolean](<=)))
theorem suffix_above : ∀ (suf : {S : {i : ℝ // i ≥ 0} → Bool // suffix_p S (restrict ≤)}), empty_p suf ∨ full_p suf ∨
 (∃ n,  suf = above n (restrict ≤)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.unrelated_empty
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL n:
--   empty?(unrelated(n, restrict[[real, real], [nat, nat], boolean](<=)))
theorem unrelated_empty : ∀ (n : {i : ℝ // i ≥ 0}), empty_p (unrelated n (restrict ≤)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.prefix_finite_full
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL pre1, pre2:
--   is_finite(pre1) AND full?(pre2) IMPLIES
--    (FORALL (f: [(pre1) -> (pre2)]): NOT surjective?(f))
theorem prefix_finite_full : ∀ (pre1 : {S : {i : ℝ // i ≥ 0} → Bool // prefix_p S (restrict ≤)}) (pre2 : {S : {i : ℝ // i ≥ 0} → Bool // prefix_p S (restrict ≤)}), is_finite pre1 ∧ full_p pre2 → (∀ (f: [(pre1): ¬ surjective_p f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_nat.prefix_bijective
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL pre1, pre2:
--   (EXISTS (f: [(pre1) -> (pre2)]): bijective?(f)) IMPLIES pre1 = pre2
theorem prefix_bijective : ∀ (pre1 : {S : {i : ℝ // i ≥ 0} → Bool // prefix_p S (restrict ≤)}) (pre2 : {S : {i : ℝ // i ≥ 0} → Bool // prefix_p S (restrict ≤)}), (∃ (f: [(pre1): bijective_p f) → pre1 = pre2 := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.ordered_nat