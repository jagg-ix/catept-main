-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cycles_
-- PVSLib library: digraphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cycles_

Auto-generated stubs from PVSLib theory `cycles_`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cycles

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (VertCycles : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Walk : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (below : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (card : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (circuit_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (eq_circuit_p : pvs_τ12 → pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (is_finite : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} (length : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (rest : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (sub_walk_p : pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (vert : pvs_τ23 → pvs_τ24)

-- pvs2lean: cycles_.concretedigraph_implements_abstractdigraph
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: digraphs_abs.correct_implementation
theorem concretedigraph_implements_abstractdigraph : correct_implementation := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cycles_.cycle?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: circuit?(G, w) AND
--  (FORALL (i, j: below(length(w) - 1)):
--     i /= j IMPLIES finseq_appl[T](w)(i) /= finseq_appl[T](w)(j))
axiom cycle_p {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: cycles_.cycle_at?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cycle?(G, w) AND finseq_appl[T](w)(0) = v
noncomputable def cycle_at_p : sorry := by
  sorry  -- pvs2lean: definition stub for cycle?(G, w) AND finseq_appl[T](w)(0) = v

-- pvs2lean: cycles_.cycle_is_circuit
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G, w): cycle?(G, w) IMPLIES circuit?(G, w)
theorem cycle_is_circuit : ∀ (G : ConcreteDigraph) (w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0}), cycle_p G w → circuit_p G w := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cycles_.circuit_subwalk_cycle
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: circuit?(G, w) IMPLIES
--  EXISTS (w1: prewalk): sub_walk?(G, w, w1) AND cycle?(G, w1)
theorem circuit_subwalk_cycle : circuit_p G w → ∃ (w1 : prewalk), sub_walk_p G w w1 ∧ cycle_p G w1 := by
  -- === TCCs for circuit_subwalk_cycle ===
  -- TCC: circuit_subwalk_cycle_TCC1
  -- Statement: FORALL (G: ConcreteDigraph,
  --         w: prewalk[T, ConcreteDigraph, vert, edges, size, make]):
  --   circuit?(G, w) IMPLIES
  --    FORALL (w1: prewalk[T, ConcreteDigraph, vert, edges, size, make]):
  --      wal...
  have circuit_subwalk_cycle_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cycles_.cycle_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: circuit?(G, w) IMPLIES
--  (cycle?(G, w) OR
--    EXISTS (w1, w2: prewalk): eq_circuit?(G, w, w1 o w2) AND cycle?(G, w1))
theorem cycle_prefix : circuit_p G w → (cycle_p G w ∨
  ∃ (w1 : _) (w2 : prewalk), eq_circuit_p G w (w1 ∘ w2) ∧ cycle_p G w1) := by
  -- === TCCs for cycle_prefix ===
  -- TCC: cycle_prefix_TCC1
  -- Statement: FORALL (G: ConcreteDigraph,
  --         w: prewalk[T, ConcreteDigraph, vert, edges, size, make]):
  --   NOT cycle?(G, w) AND circuit?(G, w) IMPLIES
  --    FORALL (w1, w2: prewalk[T, ConcreteDigraph, vert, edge...
  have cycle_prefix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cycles_.cycle_o_circuit
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: circuit?(G, w) IMPLIES
--  (cycle?(G, w) OR
--    EXISTS (w1, w2: prewalk):
--      eq_circuit?(G, w, w1 o rest(w2)) AND cycle?(G, w1) AND circuit?(G, w2))
theorem cycle_o_circuit : circuit_p G w → (cycle_p G w ∨
  ∃ (w1 : _) (w2 : prewalk), eq_circuit_p G w (w1 ∘ rest w2) ∧ cycle_p G w1 ∧ circuit_p G w2) := by
  -- === TCCs for cycle_o_circuit ===
  -- TCC: cycle_o_circuit_TCC1
  -- Statement: FORALL (G: ConcreteDigraph,
  --         w: prewalk[T, ConcreteDigraph, vert, edges, size, make]):
  --   NOT cycle?(G, w) AND circuit?(G, w) IMPLIES
  --    FORALL (w1, w2: prewalk[T, ConcreteDigraph, vert, edge...
  have cycle_o_circuit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cycles_.Pigeon_hole
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Walk(GF)):
--   length(w) > card(vert(GF)) IMPLIES
--    (EXISTS (i, j: below(length(w))):
--       i /= j AND finseq_appl[T](w)(i) = finseq_appl[T](w)(j))
theorem Pigeon_hole : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p GF w}), List.length w > card (vert GF) → (∃ (i : _) (j : Finset.range List.length w), i ≠ j ∧ finseq_appl w(i) = finseq_appl w(j)) := by
  -- === TCCs for Pigeon_hole ===
  -- TCC: Pigeon_hole_TCC1
  -- Statement: FORALL (GF: Digraph[T, ConcreteDigraph, vert, edges, size, make],
  --         w: Walk[T, ConcreteDigraph, vert, edges, size, make](GF)):
  --   is_finite[T](vert(GF))
  have pigeon_hole_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cycles