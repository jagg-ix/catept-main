-- Auto-generated by pvs2lean/translator.py
-- PVS theory: circuit_decomp_
-- PVSLib library: digraphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: circuit_decomp_

Auto-generated stubs from PVSLib theory `circuit_decomp_`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.circuit_decomp

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Walk : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (below : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (car : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (cdr : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (circuit_at_p : pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (cycle_at_p : pvs_τ12 → pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (edge_p : pvs_τ22 → pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (gen_seq1 : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (last : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} (length : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (list2finseq : pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} (map : pvs_τ33 → pvs_τ34 → pvs_τ35)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} (nth : pvs_τ36 → pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (seq : pvs_τ39 → pvs_τ40)
variable {pvs_τ44 : Type*} (trunc1 : pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (v1 : pvs_τ45 → pvs_τ46 → pvs_τ47)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} (vert : pvs_τ48 → pvs_τ49 → pvs_τ50)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (vert_circ : pvs_τ54 → pvs_τ55)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (walk_decomp : pvs_τ56 → pvs_τ57)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (walk_p : pvs_τ61 → pvs_τ62 → Prop)

-- pvs2lean: circuit_decomp_.concretedigraph_implements_abstractdigraph
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: digraphs_abs.correct_implementation
theorem concretedigraph_implements_abstractdigraph : correct_implementation := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.drop_circuits
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: list2finseq(map(LAMBDA (vc: vert_circ(G)): vc`v)(cd))
axiom drop_circuits {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ20 → pvs_τ21 → Prop

-- pvs2lean: circuit_decomp_.cdr_decomp
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G)):
--   (length(cd) > 0 AND NOT length(cd) = 1) IMPLIES
--    length(cdr(cd)) > 0 AND
--     walk?(G, list2finseq(map(LAMBDA (vc: vert_circ(G)): vc`v)(cdr(cd))))
theorem cdr_decomp : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}), (List.length cd > 0 ∧ ¬ List.length cd = 1) → List.length (cdr cd) > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cdr cd))) := by
  -- === TCCs for cdr_decomp ===
  -- TCC: cdr_decomp_TCC1
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G)):
  --   length(cd) > 0 AND NOT length(cd) = 1 IMPLIES cons?[vert_circ(G)](cd)
  have cdr_decomp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cdr_decomp_TCC2
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G)):
  --   length(cd) > 0 AND NOT length(cd) = 1 AND length(cdr(cd)) > 0 IMPLIES
  --    length
  --      (list2finseq[vert[T, ConcreteDigraph, vert, edges, size, make...
  have cdr_decomp_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.decomp_to_walk
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(cd) = 1 THEN car(cd)`c
-- ELSE car(cd)`c o decomp_to_walk(G, cdr(cd))
-- ENDIF
axiom decomp_to_walk {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ15 → pvs_τ16 → Prop

-- pvs2lean: circuit_decomp_.decomp_to_walk_length
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(cd) = 1 THEN car(cd)`c`length
-- ELSE car(cd)`c`length + decomp_to_walk_length(G, cdr(cd))
-- ENDIF
axiom decomp_to_walk_length {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19

-- pvs2lean: circuit_decomp_.decomp_to_walk_rw
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G)):
--   length(decomp_to_walk(G, cd)) = decomp_to_walk_length(G, cd)
theorem decomp_to_walk_rw : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}), List.length (decomp_to_walk G cd) = decomp_to_walk_length G cd := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.decomp_to_walk_length_bnd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((cd: walk_decomp(G)), (n: below(length(cd)))):
--   decomp_to_walk_length(G, cd) >= nth(cd, n)`c`length
theorem decomp_to_walk_length_bnd : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (n : {s : {i : ℝ // i ≥ 0} // s < List.length cd}), decomp_to_walk_length G cd ≥ nth cd n.c.List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.caret_decomp
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i, j: nat):
--   i <= j AND j < length(cd) IMPLIES
--    length(^(cd, (i, j))) > 0 AND
--     walk?(G,
--           list2finseq(map(LAMBDA (vc: vert_circ(G)): vc`v)(^(cd, (i, j)))))
theorem caret_decomp : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), i ≤ j ∧ j < List.length cd → List.length (^(cd, (i, j))) > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (^(cd, (i, j))))) := by
  -- === TCCs for caret_decomp ===
  -- TCC: caret_decomp_TCC1
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G), i, j: nat):
  --   i <= j AND j < length(cd) AND length((cd ^ (i, j))) > 0 IMPLIES
  --    length
  --      (list2finseq[vert[T, ConcreteDigraph, vert, edges, size,...
  have caret_decomp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.walk_index
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN length(car(cd)`c)
-- ELSE length(car(cd)`c) + walk_index(G, cdr(cd), i - 1)
-- ENDIF
axiom walk_index {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} : pvs_τ58 → pvs_τ59 → pvs_τ60 → Prop

-- pvs2lean: circuit_decomp_.walk_index_ub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), j: below(length(cd))):
--   walk_index(G, cd, j) <= decomp_to_walk(G, cd)`length
theorem walk_index_ub : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length cd}), walk_index G cd j ≤ decomp_to_walk G cd.List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.walk_index_incr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i, j: below(length(cd))):
--   i <= j IMPLIES walk_index(G, cd, i) <= walk_index(G, cd, j)
theorem walk_index_incr : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length cd}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length cd}), i ≤ j → walk_index G cd i ≤ walk_index G cd j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.walk_index_sincr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i, j: below(length(cd))):
--   i < j IMPLIES walk_index(G, cd, i) < walk_index(G, cd, j)
theorem walk_index_sincr : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length cd}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length cd}), i < j → walk_index G cd i < walk_index G cd j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.walk_index_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i: below(length(cd) - 1)):
--   walk_index(G, cd, i + 1) = walk_index(G, cd, i) + nth(cd, 1 + i)`c`length
theorem walk_index_last : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length cd - 1}), walk_index G cd (i + 1) = walk_index G cd i + nth cd (1 + i).c.List.length := by
  -- === TCCs for walk_index_last ===
  -- TCC: walk_index_last_TCC1
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G),
  --         i: below(length[vert_circ(G)](cd) - 1)):
  --   i + 1 < length[vert_circ(G)](cd)
  have walk_index_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walk_index_last_TCC2
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G),
  --         i: below(length[vert_circ(G)](cd) - 1)):
  --   i < length[vert_circ(G)](cd)
  have walk_index_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walk_index_last_TCC3
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G),
  --         i: below(length[vert_circ(G)](cd) - 1)):
  --   1 + i < length[vert_circ(G)](cd)
  have walk_index_last_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.walk_index_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i, j: nat):
--   i <= j AND j < length(cd) IMPLIES
--    walk_index(G, cd, i) - nth[vert_circ(G)](cd, i)`c`length >= 0
theorem walk_index_bound : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), i ≤ j ∧ j < List.length cd → walk_index G cd i - nth cd i.c.List.length ≥ 0 := by
  -- === TCCs for walk_index_bound ===
  -- TCC: walk_index_bound_TCC1
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G), i, j: nat):
  --   i <= j AND j < length(cd) IMPLIES i < length[vert_circ(G)](cd)
  have walk_index_bound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.walk_index_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i, j: nat):
--   i <= j AND j < length(cd) IMPLIES walk_index(G, cd, j) - 1 >= 0
theorem walk_index_pos : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), i ≤ j ∧ j < List.length cd → walk_index G cd j - 1 ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.decomp_walk_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i: below(length(cd)),
--         x: below(nth(cd, i)`c`length)):
--   nth(cd, i)`c`seq(x) =
--    decomp_to_walk(G, cd)`seq(walk_index(G, cd, i) - nth(cd, i)`c`length + x)
theorem decomp_walk_index : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length cd}) (x : {s : {i : ℝ // i ≥ 0} // s < nth cd i.c.List.length}), nth cd i.c.seq x = decomp_to_walk G cd.seq (walk_index G cd i - nth cd i.c.List.length + x) := by
  -- === TCCs for decomp_walk_index ===
  -- TCC: decomp_walk_index_TCC1
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G),
  --         i: below(length[vert_circ(G)](cd)),
  --         x: below(nth[vert_circ(G)](cd, i)`c`length)):
  --   walk_index(G, cd, i) - nth[vert_circ(G)](cd, i)`c...
  have decomp_walk_index_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.caret_commute
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((cd: walk_decomp(G)), (i, j: nat)):
--   i <= j AND j < length(cd) IMPLIES
--    decomp_to_walk(G, cd ^ (i, j)) =
--     decomp_to_walk(G, cd) ^
--      (walk_index(G, cd, i) - nth(cd, i)`c`length, walk_i
theorem caret_commute : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), i ≤ j ∧ j < List.length cd → decomp_to_walk G (cd ^ (i, j)) =
 decomp_to_walk G cd ^
  (walk_index G cd i - nth cd i.c.List.length, walk_index G cd j - 1) := by
  -- === TCCs for caret_commute ===
  -- TCC: caret_commute_TCC1
  -- Statement: FORALL (G: ConcreteDigraph, (cd: walk_decomp(G)), (i, j: nat)):
  --   i <= j AND j < length(cd) IMPLIES
  --    length[vert_circ(G)](^[vert_circ(G)](cd, (i, j))) > 0 AND
  --     walk?[T, ConcreteDigraph, vert, ...
  have caret_commute_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_commute_TCC2
  -- Statement: FORALL (G: ConcreteDigraph, (cd: walk_decomp(G)), (i, j: nat)):
  --   i <= j AND j < length(cd) IMPLIES
  --    walk_index(G, cd, i) - nth[vert_circ(G)](cd, i)`c`length >= 0
  have caret_commute_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: caret_commute_TCC3
  -- Statement: FORALL (G: ConcreteDigraph, (cd: walk_decomp(G)), (i, j: nat)):
  --   i <= j AND j < length(cd) IMPLIES walk_index(G, cd, j) - 1 >= 0
  have caret_commute_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.append_decomp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: <unparse-error: SIMPLE-ERROR>
theorem append_decomp : ∀ (cd1 : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (cd2 : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}), edge_p G(nth cd1 (List.length cd1 - 1).v, nth cd2 0.v) → List.length (append[vert_circ G](cd1, cd2)) > 0 ∧
 walk_p G (list2finseq[vert[T, ConcreteDigraph, vert, edges, size, make](G)]
           (map[vert_circ G,
                vert[T, ConcreteDigraph, vert, edges, size, make](G)]
                (fun (vc : vert_circ G)=> vc.v)
                (append cd1 cd2))) := by
  -- === TCCs for append_decomp ===
  -- TCC: append_decomp_TCC1
  -- Statement: <unparse-error: SIMPLE-ERROR>
  have append_decomp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: append_decomp_TCC2
  -- Statement: <unparse-error: SIMPLE-ERROR>
  have append_decomp_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: append_decomp_TCC3
  -- Statement: <unparse-error: SIMPLE-ERROR>
  have append_decomp_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: circuit_decomp_.append_commute
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: <unparse-error: SIMPLE-ERROR>
theorem append_commute : ∀ (cd1 : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (cd2 : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}), edge_p G(nth cd1 (List.length cd1 - 1).v, nth cd2 0.v) → decomp_to_walk G (append cd1 cd2) =
 decomp_to_walk G cd1 ∘ decomp_to_walk G cd2 := by
  -- === TCCs for append_commute ===
  -- TCC: append_commute_TCC1
  -- Statement: <unparse-error: SIMPLE-ERROR>
  have append_commute_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: circuit_decomp_.circuit_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i: below(length(cd))):
--   nth(cd, i)`c`seq(0) = nth(cd, i)`v
theorem circuit_vertex : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length cd}), nth cd i.c.seq 0 = nth cd i.v := by
  -- === TCCs for circuit_vertex ===
  -- TCC: circuit_vertex_TCC1
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G),
  --         i: below(length[vert_circ(G)](cd))):
  --   0 < nth[vert_circ(G)](cd, i)`c`length
  have circuit_vertex_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.circuit_first_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), i: below(length(cd))):
--   nth(cd, i)`c`seq(0) = nth(cd, i)`c`seq(nth(cd, i)`c`length - 1)
theorem circuit_first_last : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length cd}), nth cd i.c.seq 0 = nth cd i.c.seq (nth cd i.c.List.length - 1) := by
  -- === TCCs for circuit_first_last ===
  -- TCC: circuit_first_last_TCC1
  -- Statement: FORALL (G: ConcreteDigraph, cd: walk_decomp(G),
  --         i: below(length[vert_circ(G)](cd))):
  --   nth[vert_circ(G)](cd, i)`c`length - 1 >= 0 AND
  --    nth[vert_circ(G)](cd, i)`c`length - 1 < nth[vert_cir...
  have circuit_first_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.shrink_repeat
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (cd: walk_decomp(G), (W: Walk(G) | W = decomp_to_walk(G, cd))):
--   LET ww = drop_circuits(G, cd) IN
--     (EXISTS (i, j: below(length(ww))):
--        i < j AND finseq_appl[T](ww)(i) = finseq_appl[T]
theorem shrink_repeat : ∀ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (W : {W : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w} // W = decomp_to_walk G cd}), fun (ww : {W : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w} // List.length W = List.length cd}) => (∃ (i : _) (j : Finset.range List.length ww), i < j ∧ finseq_appl ww(i) = finseq_appl ww(j)) → (∃ (scd : walk_decomp G), W = decomp_to_walk G scd ∧
    List.length (drop_circuits G scd) < List.length ww) (drop_circuits G cd) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.circuit_decomp_prep
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: vert(G), w: Walk(G), cd: walk_decomp(G), n: posnat):
--   n = length(cd) AND decomp_to_walk(G, cd) = w IMPLIES
--    EXISTS (scd: walk_decomp(G)):
--      (decomp_to_walk(G, scd) = w AND
--        LET 
theorem circuit_decomp_prep : ∀ (v : T) (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}), n = List.length cd ∧ decomp_to_walk G cd = w → ∃ (scd : walk_decomp G), (decomp_to_walk G scd = w ∧
    (let path := drop_circuits G scd; ∀ (i : _) (j : Finset.range List.length path), i < j → finseq_appl path(i) ≠ finseq_appl path(j))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.vert2vc
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# v := v, c := gen_seq1(G, v) #)
axiom vert2vc {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} : pvs_τ51 → pvs_τ52 → pvs_τ53

-- pvs2lean: circuit_decomp_.trivial_decomp
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(w) = 1 THEN (: vert2vc(G, finseq_appl[T](w)(0)) :)
-- ELSE cons[vert_circ(G)]
--          (vert2vc(G, finseq_appl[T](w)(0)),
--           trivial_decomp(G, w ^ (1, length(w) - 1)))
-- ENDIF
axiom trivial_decomp {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} : pvs_τ41 → pvs_τ42 → pvs_τ43

-- pvs2lean: circuit_decomp_.trivial_decomp_idemp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Walk(G)): w = decomp_to_walk(G, trivial_decomp(G, w))
theorem trivial_decomp_idemp : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}), w = decomp_to_walk G (trivial_decomp G w) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.trivial_decomp_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Walk(G)): length(trivial_decomp(G, w)) = length(w)
theorem trivial_decomp_length : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}), List.length (trivial_decomp G w) = List.length w := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.trivial_decomp_circuit_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Walk(G), i: below(length(w))):
--   nth(trivial_decomp(G, w), i)`c`length = 1
theorem trivial_decomp_circuit_length : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length w}), nth (trivial_decomp G w) i.c.List.length = 1 := by
  -- === TCCs for trivial_decomp_circuit_length ===
  -- TCC: trivial_decomp_circuit_length_TCC1
  -- Statement: FORALL (G: ConcreteDigraph,
  --         w: Walk[T, ConcreteDigraph, vert, edges, size, make](G),
  --         i: below(length(w))):
  --   i < length[vert_circ(G)](trivial_decomp(G, w))
  have trivial_decomp_circuit_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.trunk_decomp
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Walk(G), cd: walk_decomp(G)):
--   w`length > 1 AND decomp_to_walk(G, cd) = trunc1(w) IMPLIES
--    length[vert_circ(G)]
--        (append[vert_circ(G)](cd, (: vert2vc(G, last(w)) :)))
--     > 0
--     A
theorem trunk_decomp : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}), w.List.length > 1 ∧ decomp_to_walk G cd = trunc1 w → List.length (append[vert_circ G](cd, (: vert2vc G (last w) :))) >
 0
 ∧
 walk_p G (list2finseq[vert[T, ConcreteDigraph, vert, edges, size, make](G)]
           (map[vert_circ G,
                vert[T, ConcreteDigraph, vert, edges, size, make](G)]
                (fun (vc : vert_circ G)=> vc.v)
                (append cd ((: vert2vc G (last w) :))))) := by
  -- === TCCs for trunk_decomp ===
  -- TCC: trunk_decomp_TCC1
  -- Statement: FORALL (G: ConcreteDigraph,
  --         w: Walk[T, ConcreteDigraph, vert, edges, size, make](G),
  --         cd: walk_decomp(G)):
  --   w`length > 1 IMPLIES length(w) >= 2
  have trunk_decomp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: trunk_decomp_TCC2
  -- Statement: FORALL (G: ConcreteDigraph,
  --         w: Walk[T, ConcreteDigraph, vert, edges, size, make](G),
  --         cd: walk_decomp(G)):
  --   w`length > 1 AND decomp_to_walk(G, cd) = trunc1(w) IMPLIES
  --    vert?[T, Co...
  have trunk_decomp_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: trunk_decomp_TCC3
  -- Statement: FORALL (G: ConcreteDigraph,
  --         w: Walk[T, ConcreteDigraph, vert, edges, size, make](G),
  --         cd: walk_decomp(G)):
  --   w`length > 1 AND
  --    decomp_to_walk(G, cd) = trunc1(w) AND
  --     length[vert...
  have trunk_decomp_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.trunc_decomp_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Walk(G), cd: walk_decomp(G)):
--   w`length > 1 AND decomp_to_walk(G, cd) = trunc1(w) IMPLIES
--    decomp_to_walk(G, append[vert_circ(G)](cd, (: vert2vc(G, last(w)) :))) =
--     w
theorem trunc_decomp_def : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}), w.List.length > 1 ∧ decomp_to_walk G cd = trunc1 w → decomp_to_walk G (append cd ((: vert2vc G (last w) :))) = w := by
  -- === TCCs for trunc_decomp_def ===
  -- TCC: trunc_decomp_def_TCC1
  -- Statement: FORALL (G: ConcreteDigraph,
  --         w: Walk[T, ConcreteDigraph, vert, edges, size, make](G),
  --         cd: walk_decomp(G)):
  --   w`length > 1 AND decomp_to_walk(G, cd) = trunc1(w) IMPLIES
  --    length[vert...
  have trunc_decomp_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circuit_decomp_.circuit_decomposition
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: vert(G), (circ: prewalk | circuit_at?(G, circ, v))):
--   EXISTS (cd: walk_decomp(G)):
--     (decomp_to_walk(G, cd) = circ AND
--       cycle_at?(G, drop_circuits(G, cd), v) AND
--        FORALL (i: b
theorem circuit_decomposition : ∀ (v : T) (circ : {circ : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // circuit_at_p G circ v}), ∃ (cd : {cd : {x : List (({w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) × T) // every ({vc:
         [# c: Walk G,
            v: vert G #] |
         vc.c = gen_seq1 G vc.v ∨ circuit_at_p G vc.c vc.v})
     (x)} // List.length cd > 0 ∧
 walk_p G (list2finseq (map (fun (vc : vert_circ G)=> vc.v) (cd)))}), decomp_to_walk G cd = circ ∧ cycle_at_p G (drop_circuits G cd) v ∧
 ∀ (i : Finset.range List.length cd), nth cd i.c.List.length < List.length circ := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.circuit_decomp