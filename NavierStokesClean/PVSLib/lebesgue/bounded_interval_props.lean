-- Auto-generated by pvs2lean/translator.py
-- PVS theory: bounded_interval_props
-- PVSLib library: lebesgue
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: bounded_interval_props

Auto-generated stubs from PVSLib theory `bounded_interval_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.bounded_interval_props

noncomputable section

variable {pvs_τ1 : Type*} (bounded_interval_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} (inf : pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (sup : pvs_τ3 → pvs_τ4)

-- pvs2lean: bounded_interval_props.bounded_interval1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: A(a) => inf(A) <= a AND a <= sup(A)
theorem bounded_interval1 : A a → inf A ≤ a ∧ a ≤ sup A := by
  -- === TCCs for bounded_interval1 ===
  -- TCC: bounded_interval1_TCC1
  -- Statement: FORALL (A: bounded_interval, a: real):
  --   A(a) IMPLIES nonempty?[real](A) AND below_bounded[real](A)
  have bounded_interval1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: bounded_interval1_TCC2
  -- Statement: FORALL (A: bounded_interval, a: real):
  --   inf(A) <= a AND A(a) IMPLIES nonempty?[real](A) AND above_bounded[real](A)
  have bounded_interval1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bounded_interval_props.bounded_interval2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?[real](A) AND inf(A) < a AND a < sup(A) => A(a)
theorem bounded_interval2 : nonempty_p A ∧ inf A < a ∧ a < sup A → A a := by
  -- === TCCs for bounded_interval2 ===
  -- TCC: bounded_interval2_TCC1
  -- Statement: FORALL (A: bounded_interval):
  --   nonempty?[real](A) IMPLIES below_bounded[real](A)
  have bounded_interval2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: bounded_interval2_TCC2
  -- Statement: FORALL (A: bounded_interval, a: real):
  --   inf(A) < a AND nonempty?[real](A) IMPLIES above_bounded[real](A)
  have bounded_interval2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_interval_props.bounded_interval3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?[real](A) => inf(A) <= sup(A)
theorem bounded_interval3 : nonempty_p A → inf A ≤ sup A := by
  -- === TCCs for bounded_interval3 ===
  -- TCC: bounded_interval3_TCC1
  -- Statement: FORALL (A: bounded_interval):
  --   nonempty?[real](A) IMPLIES above_bounded[real](A)
  have bounded_interval3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_interval_props.bounded_interval4
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?[real](A, B) AND A(a) AND B(b) AND a <= b => sup(A) <= inf(B)
theorem bounded_interval4 : disjoint_p A B ∧ A a ∧ B b ∧ a ≤ b → sup A ≤ inf B := by
  -- === TCCs for bounded_interval4 ===
  -- TCC: bounded_interval4_TCC1
  -- Statement: FORALL (A, B: bounded_interval, a, b: real):
  --   a <= b AND B(b) AND A(a) AND disjoint?[real](A, B) IMPLIES
  --    nonempty?[real](A) AND above_bounded[real](A)
  have bounded_interval4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: bounded_interval4_TCC2
  -- Statement: FORALL (A, B: bounded_interval, a, b: real):
  --   a <= b AND B(b) AND A(a) AND disjoint?[real](A, B) IMPLIES
  --    nonempty?[real](B) AND below_bounded[real](B)
  have bounded_interval4_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_interval_props.bounded_interval5
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?[real](A, B) AND A(a) AND B(b) AND a <= b =>
--  FORALL (x: (A), y: (B)): x < y
theorem bounded_interval5 : disjoint_p A B ∧ A a ∧ B b ∧ a ≤ b → ∀ (x : (A)) (y : (B)), x < y := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_interval_props.bounded_interval_disjoint_union1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt:      disjoint?[real](A, B) AND bounded_interval?(union[real](A, B)) AND A(a)
--  AND B(b) AND (a <= b)
--  => sup(A) = inf(B)
theorem bounded_interval_disjoint_union1 : disjoint_p A B ∧ bounded_interval_p (union A B) ∧ A a
 ∧ B b ∧ (a ≤ b) → sup A = inf B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_interval_props.bounded_interval_disjoint_union2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt:      disjoint?[real](A, B) AND bounded_interval?(union[real](A, B)) AND A(a)
--  AND B(b) AND (a <= b)
--  => (A(sup(A)) OR B(inf(B)))
theorem bounded_interval_disjoint_union2 : disjoint_p A B ∧ bounded_interval_p (union A B) ∧ A a
 ∧ B b ∧ (a ≤ b) → (A (sup A) ∨ B (inf B)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_interval_props.bounded_interval_disjoint_union3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt:      disjoint?[real](A, B) AND bounded_interval?(union[real](A, B)) AND A(a)
--  AND B(b) AND (a <= b)
--  => inf(union[real](A, B)) = inf(A)
theorem bounded_interval_disjoint_union3 : disjoint_p A B ∧ bounded_interval_p (union A B) ∧ A a
 ∧ B b ∧ (a ≤ b) → inf (union A B) = inf A := by
  -- === TCCs for bounded_interval_disjoint_union3 ===
  -- TCC: bounded_interval_disjoint_union3_TCC1
  -- Statement: FORALL (A, B: bounded_interval, a, b: real):
  --        (a <= b) AND B(b) AND A(a) AND bounded_interval?(union[real](A, B))
  --    AND disjoint?[real](A, B)
  --    IMPLIES
  --    nonempty?[real](union[real](A, B))...
  have bounded_interval_disjoint_union3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bounded_interval_props.bounded_interval_disjoint_union4
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt:      disjoint?[real](A, B) AND bounded_interval?(union[real](A, B)) AND A(a)
--  AND B(b) AND (a <= b)
--  => sup(union[real](A, B)) = sup(B)
theorem bounded_interval_disjoint_union4 : disjoint_p A B ∧ bounded_interval_p (union A B) ∧ A a
 ∧ B b ∧ (a ≤ b) → sup (union A B) = sup B := by
  -- === TCCs for bounded_interval_disjoint_union4 ===
  -- TCC: bounded_interval_disjoint_union4_TCC1
  -- Statement: FORALL (A, B: bounded_interval, a, b: real):
  --        (a <= b) AND B(b) AND A(a) AND bounded_interval?(union[real](A, B))
  --    AND disjoint?[real](A, B)
  --    IMPLIES
  --    nonempty?[real](union[real](A, B))...
  have bounded_interval_disjoint_union4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: bounded_interval_disjoint_union4_TCC2
  -- Statement: FORALL (A, B: bounded_interval, a, b: real):
  --        (a <= b) AND B(b) AND A(a) AND bounded_interval?(union[real](A, B))
  --    AND disjoint?[real](A, B)
  --    IMPLIES nonempty?[real](B) AND above_bounded[...
  have bounded_interval_disjoint_union4_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.bounded_interval_props