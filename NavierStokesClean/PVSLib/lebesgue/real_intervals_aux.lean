-- Auto-generated by pvs2lean/translator.py
-- PVS theory: real_intervals_aux
-- PVSLib library: lebesgue
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: real_intervals_aux

Auto-generated stubs from PVSLib theory `real_intervals_aux`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.real_intervals_aux

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (above_bounded : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (ball : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (below_bounded : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (bounded_interval_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (bounded_open_interval_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} (empty_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (inf : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (inf_open : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (interval_p : pvs_τ15 → Prop)
variable {pvs_τ19 : Type*} (metric_closed_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (metric_open_p : pvs_τ20 → Prop)
variable {pvs_τ22 : Type*} (nonempty_p : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (open_inf : pvs_τ23 → pvs_τ24)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (sup : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} (unbounded_p : pvs_τ28 → Prop)

-- pvs2lean: real_intervals_aux.closed_interval?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: interval?(A) AND metric_closed?(A)
noncomputable def closed_interval_p : sorry := by
  sorry  -- pvs2lean: definition stub for interval?(A) AND metric_closed?(A)

-- pvs2lean: real_intervals_aux.open_interval?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: interval?(A) AND metric_open?(A)
axiom open_interval_p {pvs_τ25 : Type*} : pvs_τ25 → Prop

-- pvs2lean: real_intervals_aux.bounded_open_interval_def
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: bounded_open_interval?(A) <=> empty?(A) OR EXISTS x, r: A = ball(x, r)
theorem bounded_open_interval_def : bounded_open_interval_p A <=> empty_p A ∨ ∃ x r,  A = ball x r := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: real_intervals_aux.unbounded_open_interval_def
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (unbounded?(A) AND open_interval?(A)) <=>
--  (A = fullset[real] OR
--    (EXISTS a: A = open_inf(a)) OR (EXISTS a: A = inf_open(a)))
theorem unbounded_open_interval_def : (unbounded_p A ∧ open_interval_p A) <=> (A = Set.univ ∨
  (∃ a,  A = open_inf a) ∨ (∃ a,  A = inf_open a)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: real_intervals_aux.length
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?[real](b) THEN 0 ELSE sup(b) - inf(b) ENDIF
axiom length {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ17 → pvs_τ18

-- pvs2lean: real_intervals_aux.length_closed
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?[real](b) => length(b) = length({x | inf(b) <= x AND x <= sup(b)})
theorem length_closed : nonempty_p b → List.length b = List.length ({x | inf b ≤ x ∧ x ≤ sup b}) := by
  -- === TCCs for length_closed ===
  -- TCC: length_closed_TCC1
  -- Statement: FORALL (b: bounded_interval):
  --   nonempty?[real](b) IMPLIES below_bounded[real](b)
  have length_closed_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: length_closed_TCC2
  -- Statement: FORALL (b: bounded_interval):
  --   nonempty?[real](b) IMPLIES
  --    FORALL (x: real): inf(b) <= x IMPLIES above_bounded[real](b)
  have length_closed_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: length_closed_TCC3
  -- Statement: FORALL (b: bounded_interval):
  --   nonempty?[real](b) IMPLIES
  --    bounded_interval?({x: real | inf[real](b) <= x AND x <= sup[real](b)})
  have length_closed_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: real_intervals_aux.length_open
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?[real](b) => length(b) = length({x | inf(b) < x AND x < sup(b)})
theorem length_open : nonempty_p b → List.length b = List.length ({x | inf b < x ∧ x < sup b}) := by
  -- === TCCs for length_open ===
  -- TCC: length_open_TCC1
  -- Statement: FORALL (b: bounded_interval):
  --   nonempty?[real](b) IMPLIES
  --    FORALL (x: real): inf(b) < x IMPLIES above_bounded[real](b)
  have length_open_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: length_open_TCC2
  -- Statement: FORALL (b: bounded_interval):
  --   nonempty?[real](b) IMPLIES
  --    bounded_interval?({x: real | inf[real](b) < x AND x < sup[real](b)})
  have length_open_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: real_intervals_aux.length_empty_rew
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(emptyset[real]) = 0
theorem length_empty_rew : List.length ((∅ : Set ℝ)) = 0 := by
  -- === TCCs for length_empty_rew ===
  -- TCC: length_empty_rew_TCC1
  -- Statement: bounded_interval?(emptyset[real])
  have length_empty_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_intervals_aux.length_closed_rew
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: {x | a <= x}): length({x | a <= x AND x <= b}) = b - a
theorem length_closed_rew : ∀ (b : {x : ℝ // a ≤ x}), List.length ({x | a ≤ x ∧ x ≤ b}) = b - a := by
  -- === TCCs for length_closed_rew ===
  -- TCC: length_closed_rew_TCC1
  -- Statement: FORALL (a: real, b: {x: real | a <= x}):
  --   bounded_interval?({x: real | a <= x AND x <= b})
  have length_closed_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_intervals_aux.length_open_rew
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: {x | a <= x}): length({x | a < x AND x < b}) = b - a
theorem length_open_rew : ∀ (b : {x : ℝ // a ≤ x}), List.length ({x | a < x ∧ x < b}) = b - a := by
  -- === TCCs for length_open_rew ===
  -- TCC: length_open_rew_TCC1
  -- Statement: FORALL (a: real, b: {x: real | a <= x}):
  --   bounded_interval?({x: real | a < x AND x < b})
  have length_open_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_intervals_aux.left_closed_right_open_interval?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(A) AND
--  interval?(A) AND
--   (above_bounded(A) => NOT A(sup(A))) AND (below_bounded(A) => A(inf(A)))
axiom left_closed_right_open_interval_p {pvs_τ16 : Type*} : pvs_τ16 → Prop

-- pvs2lean: real_intervals_aux.nonempty_interval?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(A) AND interval?(A)
axiom nonempty_interval_p {pvs_τ21 : Type*} : pvs_τ21 → Prop

end

end NavierStokesClean.PVSLib.real_intervals_aux