-- Auto-generated by pvs2lean/translator.py
-- PVS theory: lebesgue_fundamental
-- PVSLib library: lebesgue
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: lebesgue_fundamental

Auto-generated stubs from PVSLib theory `lebesgue_fundamental`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.lebesgue_fundamental

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (closed : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} (deriv : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (integrable_p : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (integral : pvs_τ7 → pvs_τ8 → Prop)

-- pvs2lean: lebesgue_fundamental.*
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA x: extend[real, (closed(a, b)), real, 0](f)(x) * g(x)
noncomputable def pvs_op_3389da : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA x: extend[real, (closed(a, b)), real, 0](f)(x) * g(x)

-- pvs2lean: lebesgue_fundamental.fundamental_lebesgue
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv(F) = restrict[real, (closed(a, b)), real](f) =>
--  integrable?(closed(a, b))(f) AND integral(closed(a, b), f) = F(b) - F(a)
theorem fundamental_lebesgue : deriv F = restrict f → integrable_p (closed a b) (f) ∧ integral (closed a b) f = F b - F a := by
  -- === TCCs for fundamental_lebesgue ===
  -- TCC: fundamental_lebesgue_TCC1
  -- Statement: FORALL (F: deriv_fun[(closed(a, b))], f: continuous_on[(closed(a, b))]):
  --   integrable?(closed(a, b))(f) AND
  --    deriv(F) = restrict[real, (closed(a, b)), real](f)
  --    IMPLIES closed(a, b)(b)
  have fundamental_lebesgue_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fundamental_lebesgue_TCC2
  -- Statement: FORALL (F: deriv_fun[(closed(a, b))], f: continuous_on[(closed(a, b))]):
  --   integrable?(closed(a, b))(f) AND
  --    deriv(F) = restrict[real, (closed(a, b)), real](f)
  --    IMPLIES closed(a, b)(a)
  have fundamental_lebesgue_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: lebesgue_fundamental.integration_by_parts
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (deriv(F) = restrict[real, (closed(a, b)), real](f) AND
--   deriv(G) = restrict[real, (closed(a, b)), real](g))
--  =>
--  (integrable?(closed(a, b))
--              (LAMBDA (x: real) -> real:
--                 ex
theorem integration_by_parts : (deriv F = restrict f ∧
  deriv G = restrict g) → (integrable_p (closed a b)
            (fun (x : ℝ)=> extend F(x) * g x)
  ∧
  integrable_p (closed a b)
             (fun (x : ℝ)=> extend G(x) * f x)
   ∧
   integral (closed a b) (fun (x : ℝ)=> extend F(x) * g x)
    =
    F b * G b - F a * G a -
     integral (closed a b) (fun (x : ℝ)=> extend G(x) * f x)) := by
  -- === TCCs for integration_by_parts ===
  -- TCC: integration_by_parts_TCC1
  -- Statement: FORALL (F, G: deriv_fun[(closed(a, b))],
  --         f, g: continuous_on[(closed(a, b))]):
  --   integrable?(closed(a, b))
  --              (LAMBDA (x: real) -> real:
  --                 extend[real, (closed(a, b)...
  have integration_by_parts_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integration_by_parts_TCC2
  -- Statement: FORALL (F, G: deriv_fun[(closed(a, b))],
  --         f, g: continuous_on[(closed(a, b))]):
  --   integrable?(closed(a, b))
  --              (LAMBDA (x: real) -> real:
  --                 extend[real, (closed(a, b)...
  have integration_by_parts_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.lebesgue_fundamental