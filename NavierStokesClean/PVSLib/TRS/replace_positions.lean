-- Auto-generated by pvs2lean/translator.py
-- PVS theory: replace_positions
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: replace_positions

Auto-generated stubs from PVSLib theory `replace_positions`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.replace_positions

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Pos_var : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (SPP : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} (SPP_p : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (add_first : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (add_last : pvs_τ10 → pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (delete : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (equivalent : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (ext : pvs_τ19 → pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (finseq_appl : pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (is_countably_infinite : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (is_finite : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} (length : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (parallel : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (positionsOF : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} (positions_p : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (replaceTerm : pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (rest : pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (set2seq : pvs_τ45 → pvs_τ46)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (subtermOF : pvs_τ47 → pvs_τ48 → pvs_τ49)

-- pvs2lean: replace_positions.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.replace_pos
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(fssp) = 0 THEN s
-- ELSE replace_pos(replaceTerm(s, t,
--                              finseq_appl[position[variable, symbol, arity]]
--                                  (fssp)(0)),
--                  
axiom replace_pos {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} : pvs_τ40 → pvs_τ41 → pvs_τ42 → Prop

-- pvs2lean: replace_positions.replace_pos_is_stable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss: SPP(s)), sig:
--   ext(sig)(replace_pos(s, t, fss)) =
--    replace_pos(ext(sig)(s), ext(sig)(t), fss)
theorem replace_pos_is_stable : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL), ext sig(replace_pos s t fss) = replace_pos (ext sig(s)) (ext sig(t)) fss := by
  -- === TCCs for replace_pos_is_stable ===
  -- TCC: replace_pos_is_stable_TCC1
  -- Statement: FORALL (s, fss: SPP[variable, symbol, arity](s), sig):
  --   SPP?[variable, symbol, arity](ext[variable, symbol, arity](sig)(s))(fss)
  have replace_pos_is_stable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.preserv_first_position
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss: SPP(s)), (q: positions?(s)):
--   (FORALL (i: below[length(fss)]):
--      parallel(finseq_appl[position[variable, symbol, arity]](fss)(i), q))
--    => positionsOF(replace_pos(s, t, add_fir
theorem preserv_first_position : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), (∀ (i : Fin List.length fss), parallel (finseq_appl fss(i)) q) → positionsOF (replace_pos s t (add_first q fss)) (q) := by
  -- === TCCs for preserv_first_position ===
  -- TCC: preserv_first_position_TCC1
  -- Statement: FORALL (s, fss: SPP[variable, symbol, arity](s),
  --         q: positions?[variable, symbol, arity](s)):
  --   (FORALL (i: below[length(fss)]):
  --      parallel(finseq_appl[position[variable, symbol, arity]](...
  have preserv_first_position_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.preserv_all_parallel_positions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss: SPP(s)), (q: positions?(s)):
--   (FORALL (i: below[length(fss)]):
--      parallel(finseq_appl[position[variable, symbol, arity]](fss)(i), q))
--    => positionsOF(replace_pos(s, t, fss))(q
theorem preserv_all_parallel_positions : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), (∀ (i : Fin List.length fss), parallel (finseq_appl fss(i)) q) → positionsOF (replace_pos s t fss) (q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.preserv_all_replaced_positions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fssp: SPP(s)), (i: below[fssp`length]):
--   positionsOF(replace_pos(s, t, fssp))
--              (finseq_appl[position[variable, symbol, arity]](fssp)(i))
theorem preserv_all_replaced_positions : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fssp : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (i : {s : {i : ℝ // i ≥ 0} // s < fssp.List.length}), positionsOF (replace_pos s t fssp) (finseq_appl fssp i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.preserv_unchanged_subterms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss: SPP(s)), (q: positions?(s)):
--   (FORALL (i: below[length(fss)]):
--      parallel(finseq_appl[position[variable, symbol, arity]](fss)(i), q))
--    => subtermOF(replace_pos(s, t, fss), q) 
theorem preserv_unchanged_subterms : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), (∀ (i : Fin List.length fss), parallel (finseq_appl fss(i)) q) → subtermOF (replace_pos s t fss) q = subtermOF s q := by
  -- === TCCs for preserv_unchanged_subterms ===
  -- TCC: preserv_unchanged_subterms_TCC1
  -- Statement: FORALL (t, s, fss: SPP[variable, symbol, arity](s),
  --         q: positions?[variable, symbol, arity](s)):
  --   (FORALL (i: below[length(fss)]):
  --      parallel(finseq_appl[position[variable, symbol, arity...
  have preserv_unchanged_subterms_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.replace_pos_subterm_to_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss: SPP(s)):
--   (FORALL (i: below[length(fss)]):
--      subtermOF(s, finseq_appl[position[variable, symbol, arity]](fss)(i)) =
--       t)
--    IMPLIES replace_pos(s, t, fss) = s
theorem replace_pos_subterm_to_term : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))), (∀ (i : Fin List.length fss), subtermOF s (finseq_appl fss(i)) = t) → replace_pos s t fss = s := by
  -- === TCCs for replace_pos_subterm_to_term ===
  -- TCC: replace_pos_subterm_to_term_TCC1
  -- Statement: FORALL (s, fss: SPP[variable, symbol, arity](s), i: below[length(fss)]):
  --   positionsOF[variable, symbol, arity]
  --       (s)(finseq_appl[position[variable, symbol, arity]](fss)(i))
  have replace_pos_subterm_to_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.replace_pos_to_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: replace_pos(s, x, set2seq(Pos_var(s, x))) = s
theorem replace_pos_to_term : replace_pos s x (set2seq (Pos_var s x)) = s := by
  -- === TCCs for replace_pos_to_term ===
  -- TCC: replace_pos_to_term_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], x: (V)):
  --   is_finite[position[variable, symbol, arity]]
  --       (Pos_var[variable, symbol, arity](s, x))
  have replace_pos_to_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_pos_to_term_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], x: (V)):
  --   SPP?[variable, symbol, arity]
  --       (s)
  --       (set2seq[position[variable, symbol, arity]]
  --            (Pos_var[variable, symbol, arity](s, x)))
  have replace_pos_to_term_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.CP_lemma_aux2a
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss: SPP(s)), (q: positions?(s)):
--   (FORALL (i: below[length(fss)]):
--      parallel(finseq_appl[position[variable, symbol, arity]](fss)(i), q))
--    =>
--    replace_pos(s, t, add_last(fss, q)
theorem CP_lemma_aux2a : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), (∀ (i : Fin List.length fss), parallel (finseq_appl fss(i)) q) → replace_pos s t (add_last fss q) =
 replaceTerm (replace_pos s t fss) t q := by
  -- === TCCs for CP_lemma_aux2a ===
  -- TCC: CP_lemma_aux2a_TCC1
  -- Statement: FORALL (s, fss: SPP[variable, symbol, arity](s),
  --         q: positions?[variable, symbol, arity](s)):
  --   (FORALL (i: below[length(fss)]):
  --      parallel(finseq_appl[position[variable, symbol, arity]](...
  have cp_lemma_aux2a_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.CP_lemma_aux2a1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss: SPP(s)), (q: positions?(s)):
--   (FORALL (i: below[length(fss)]):
--      parallel(finseq_appl[position[variable, symbol, arity]](fss)(i), q))
--    =>
--    replace_pos(s, t, add_first(q, fss
theorem CP_lemma_aux2a1 : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), (∀ (i : Fin List.length fss), parallel (finseq_appl fss(i)) q) → replace_pos s t (add_first q fss) =
 replaceTerm (replace_pos s t fss) t q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.CP_lemma_aux2a2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss: SPP(s)), (i: below[length(fss)]):
--   replace_pos(s, t, fss) =
--    replaceTerm(replace_pos(s, t, delete(fss, i)), t,
--                finseq_appl[position[variable, symbol, arity]](fss)(
theorem CP_lemma_aux2a2 : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length fss}), replace_pos s t fss = replaceTerm (replace_pos s t (delete fss i)) t (finseq_appl fss(i)) := by
  -- === TCCs for CP_lemma_aux2a2 ===
  -- TCC: CP_lemma_aux2a2_TCC1
  -- Statement: FORALL (s, fss: SPP[variable, symbol, arity](s), i: below[length(fss)]):
  --   SPP?[variable, symbol, arity]
  --       (s)(delete[position[variable, symbol, arity]](fss, i))
  have cp_lemma_aux2a2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CP_lemma_aux2a2_TCC2
  -- Statement: FORALL (t, s, fss: SPP[variable, symbol, arity](s), i: below[length(fss)]):
  --   positionsOF[variable, symbol, arity]
  --       (replace_pos(s, t, delete[position[variable, symbol, arity]](fss, i)))
  --      ...
  have cp_lemma_aux2a2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replace_positions.CP_lemma_aux2a3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, (fss1, fss2: SPP(s)):
--   equivalent(fss1, fss2) =>
--    replace_pos(s, t, fss1) = replace_pos(s, t, fss2)
theorem CP_lemma_aux2a3 : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (fss1 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (fss2 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))), equivalent fss1 fss2 → replace_pos s t fss1 = replace_pos s t fss2 := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.replace_positions