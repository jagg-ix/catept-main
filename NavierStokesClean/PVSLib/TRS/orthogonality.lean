-- Auto-generated by pvs2lean/translator.py
-- PVS theory: orthogonality
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: orthogonality

Auto-generated stubs from PVSLib theory `orthogonality`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.orthogonality

noncomputable section

variable {pvs_τ1 : Type*} (Orthogonal_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} (PP_p : pvs_τ2 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (SPP_p : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (add_first : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (complement_pos : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (confluent_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (diamond_property_p : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (ext : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (is_countably_infinite : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} (length : pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (lhs : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (member : pvs_τ28 → pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (member_seq : pvs_τ31 → pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (parallel_reduction_fix_p : pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (parallel_reduction_p : pvs_τ37 → pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (positionsOF : pvs_τ40 → pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} (reduction_p : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (rest : pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (rhs : pvs_τ45 → pvs_τ46)
variable {pvs_τ47 : Type*} (seq : pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} (seq2set : pvs_τ48 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (subseq : pvs_τ51 → pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (subtermOF : pvs_τ53 → pvs_τ54 → Prop)

-- pvs2lean: orthogonality.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.sub_pos
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(fsp) = 0 THEN empty_seq[position[variable, symbol, arity]]
-- ELSIF p <= fsp`seq(0) AND p /= fsp`seq(0)
--   THEN add_first(fsp`seq(0), sub_pos(rest(fsp), p))
-- ELSE sub_pos(rest(fsp), p)
-- ENDIF
axiom sub_pos {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ49 → pvs_τ50 → Prop

-- pvs2lean: orthogonality.Pos_Over
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (IF length(fsp1) = 0 THEN empty_seq[position[variable, symbol, arity]]
--  ELSE (IF (length(sub_pos(fsp2, fsp1`seq(0))) > 0 OR
--             PP?(add_first(fsp1`seq(0), fsp2)))
--          THEN add_first(fsp1`
axiom Pos_Over {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7

-- pvs2lean: orthogonality.Pos_Under
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(fsp2) = 0 THEN empty_seq[position[variable, symbol, arity]]
-- ELSE sub_pos(fsp1, fsp2`seq(0)) o Pos_Under(fsp1, rest(fsp2))
-- ENDIF
axiom Pos_Under {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10

-- pvs2lean: orthogonality.Pos_Equal
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(fsp1) = 0 THEN empty_seq[position[variable, symbol, arity]]
-- ELSIF member_seq(fsp1`seq(0), fsp2)
--   THEN add_first(fsp1`seq(0), Pos_Equal(rest(fsp1), fsp2))
-- ELSE Pos_Equal(rest(fsp1), fsp2)
-- EN
axiom Pos_Equal {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: orthogonality.sub_pos_element
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) & member_seq(p1 o p2, fsp) & p2`length /= 0 IMPLIES
--  sub_pos(fsp, p1)`length /= 0 & member_seq(p1 o p2, sub_pos(fsp, p1))
theorem sub_pos_element : PP_p fsp & member_seq (p1 ∘ p2) fsp & p2.List.length ≠ 0 → sub_pos fsp p1.List.length ≠ 0 & member_seq (p1 ∘ p2) (sub_pos fsp p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.parallel_pos_are_dif
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) IMPLIES
--  FORALL (i, j: below[fsp`length]):
--    i /= j =>
--     finseq_appl[position[variable, symbol, arity]](fsp)(i) /=
--      finseq_appl[position[variable, symbol, arity]](fsp)(j)
theorem parallel_pos_are_dif : PP_p fsp → ∀ (i : _) (j : Fin fsp.List.length), i ≠ j →
   finseq_appl fsp(i) ≠
    finseq_appl fsp(j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.same_pos_in_Pos_Equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) & member_seq(p, fsp1) & member_seq(p, fsp2) IMPLIES
--  member_seq(p, Pos_Equal(fsp1, fsp2))
theorem same_pos_in_Pos_Equal : PP_p fsp1 & PP_p fsp2 & member_seq p fsp1 & member_seq p fsp2 → member_seq p (Pos_Equal fsp1 fsp2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Over_is_sub_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) IMPLIES subseq(Pos_Over(fsp1, fsp2), fsp1)
theorem Pos_Over_is_sub_seq : PP_p fsp1 & PP_p fsp2 → subseq (Pos_Over fsp1 fsp2) fsp1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Equal_is_sub_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) IMPLIES subseq(Pos_Equal(fsp1, fsp2), fsp1)
theorem Pos_Equal_is_sub_seq : PP_p fsp1 & PP_p fsp2 → subseq (Pos_Equal fsp1 fsp2) fsp1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.sub_pos_is_sub_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) IMPLIES subseq(sub_pos(fsp, p), fsp)
theorem sub_pos_is_sub_seq : PP_p fsp → subseq (sub_pos fsp p) fsp := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Under_is_sub_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) IMPLIES subseq(Pos_Under(fsp1, fsp2), fsp1)
theorem Pos_Under_is_sub_seq : PP_p fsp1 & PP_p fsp2 → subseq (Pos_Under fsp1 fsp2) fsp1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Over_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) IMPLIES PP?(Pos_Over(fsp1, fsp2))
theorem Pos_Over_is_PP : PP_p fsp1 & PP_p fsp2 → PP_p (Pos_Over fsp1 fsp2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Over_is_SPP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(s)(fsp1) & SPP?(s)(fsp2) IMPLIES SPP?(s)(Pos_Over(fsp1, fsp2))
theorem Pos_Over_is_SPP : SPP_p s(fsp1) & SPP_p s(fsp2) → SPP_p s(Pos_Over fsp1 fsp2) := by
  -- === TCCs for Pos_Over_is_SPP ===
  -- TCC: Pos_Over_is_SPP_TCC1
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   SPP?(s)(fsp2) AND SPP?(s)(fsp1) IMPLIES PP?[variable, symbol, arity](fsp1)
  have pos_over_is_spp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Pos_Over_is_SPP_TCC2
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   SPP?(s)(fsp2) AND SPP?(s)(fsp1) IMPLIES PP?[variable, symbol, arity](fsp2)
  have pos_over_is_spp_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.pos_up_in_Pos_Over
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:    PP?(fsp1) & PP?(fsp2) & (p2`length /= 0) & member_seq(p1, fsp1)
--  & member_seq(p1 o p2, fsp2)
--  IMPLIES member_seq(p1, Pos_Over(fsp1, fsp2))
theorem pos_up_in_Pos_Over : PP_p fsp1 & PP_p fsp2 & (p2.List.length ≠ 0) & member_seq p1 fsp1
 & member_seq (p1 ∘ p2) fsp2 → member_seq p1 (Pos_Over fsp1 fsp2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.parallel_pos_in_Pos_Over
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) & member_seq(p, fsp1) & PP?(add_first(p, fsp2))
--  IMPLIES member_seq(p, Pos_Over(fsp1, fsp2))
theorem parallel_pos_in_Pos_Over : PP_p fsp1 & PP_p fsp2 & member_seq p fsp1 & PP_p (add_first p fsp2) → member_seq p (Pos_Over fsp1 fsp2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.sub_pos_in_Pos_Under_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) & member_seq(p2, fsp) & p1 /= p2 & p1 <= p2 IMPLIES
--  member_seq(p2, sub_pos(fsp, p1))
theorem sub_pos_in_Pos_Under_aux : PP_p fsp & member_seq p2 fsp & p1 ≠ p2 & p1 ≤ p2 → member_seq p2 (sub_pos fsp p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.sub_pos_in_Pos_Under
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:    PP?(fsp1) & PP?(fsp2) & member_seq(p1, fsp1) & member_seq(p2, fsp2)
--  & (p1 /= p2) & (p2 <= p1)
--  IMPLIES member_seq(p1, Pos_Under(fsp1, fsp2))
theorem sub_pos_in_Pos_Under : PP_p fsp1 & PP_p fsp2 & member_seq p1 fsp1 & member_seq p2 fsp2
 & (p1 ≠ p2) & (p2 ≤ p1) → member_seq p1 (Pos_Under fsp1 fsp2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.sub_pos_is_under
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) & member_seq(p1, sub_pos(fsp, p)) IMPLIES p <= p1 & p /= p1
theorem sub_pos_is_under : PP_p fsp & member_seq p1 (sub_pos fsp p) → p ≤ p1 & p ≠ p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.sub_pos_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) IMPLIES PP?(sub_pos(fsp, p))
theorem sub_pos_is_PP : PP_p fsp → PP_p (sub_pos fsp p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.sub_pos_is_SPP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(s)(fsp) IMPLIES SPP?(s)(sub_pos(fsp, p))
theorem sub_pos_is_SPP : SPP_p s(fsp) → SPP_p s(sub_pos fsp p) := by
  -- === TCCs for sub_pos_is_SPP ===
  -- TCC: sub_pos_is_SPP_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   SPP?(s)(fsp) IMPLIES PP?[variable, symbol, arity](fsp)
  have sub_pos_is_spp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Under_character
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem Pos_Under_character : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: orthogonality.union_positions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) IMPLIES
--  seq2set(fsp1) =
--   seq2set(Pos_Over(fsp1, fsp2) o Pos_Equal(fsp1, fsp2) o
--            Pos_Under(fsp1, fsp2))
theorem union_positions : PP_p fsp1 & PP_p fsp2 → seq2set fsp1 =
 seq2set (Pos_Over fsp1 fsp2 ∘ Pos_Equal fsp1 fsp2 ∘ Pos_Under fsp1 fsp2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Over_character
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) =>
--  FORALL (i: below[Pos_Over(fsp1, fsp2)`length]):
--    PP?(add_first(Pos_Over(fsp1, fsp2)`seq(i), fsp2)) OR
--     EXISTS (j: below[fsp2`length]):
--       Pos_Over(fsp1, fsp2)`seq(i) 
theorem Pos_Over_character : PP_p fsp1 & PP_p fsp2 → ∀ (i : Fin Pos_Over fsp1 fsp2.List.length), PP_p (add_first (Pos_Over fsp1 fsp2.seq i) fsp2) ∨
   ∃ (j : Fin fsp2.List.length), Pos_Over fsp1 fsp2.seq i ≤ fsp2.seq j &
      Pos_Over fsp1 fsp2.seq i ≠ fsp2.seq j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Equal_character
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) =>
--  FORALL (i: below[Pos_Equal(fsp1, fsp2)`length]):
--    EXISTS (j: below[fsp2`length]):
--      Pos_Equal(fsp1, fsp2)`seq(i) = fsp2`seq(j)
theorem Pos_Equal_character : PP_p fsp1 & PP_p fsp2 → ∀ (i : Fin Pos_Equal fsp1 fsp2.List.length), ∃ (j : Fin fsp2.List.length), Pos_Equal fsp1 fsp2.seq i = fsp2.seq j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Equal_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) => PP?(Pos_Equal(fsp1, fsp2))
theorem Pos_Equal_is_PP : PP_p fsp1 & PP_p fsp2 → PP_p (Pos_Equal fsp1 fsp2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Over_and_Pos_Equal_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) & PP?(fsp2) =>
--  PP?(Pos_Over(fsp1, fsp2) o Pos_Over(fsp2, fsp1) o Pos_Equal(fsp1, fsp2))
theorem Pos_Over_and_Pos_Equal_is_PP : PP_p fsp1 & PP_p fsp2 → PP_p (Pos_Over fsp1 fsp2 ∘ Pos_Over fsp2 fsp1 ∘ Pos_Equal fsp1 fsp2) := by
  -- === TCCs for Pos_Over_and_Pos_Equal_is_PP ===
  -- TCC: Pos_Over_and_Pos_Equal_is_PP_TCC1
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]]):
  --   PP?(fsp2) AND PP?(fsp1) IMPLIES
  --    is_finite[position[variable, symbol, arity]]
  --        (union[position[variable, symbol, arity]]
  --   ...
  have pos_over_and_pos_equal_is_pp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Pos_Over_and_Pos_Equal_dominance
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp1) & SPP?(t)(fsp2) IMPLIES
--  (FORALL (i: below[fsp1`length]):
--     EXISTS (j:
--               below[(Pos_Over(fsp1, fsp2) o Pos_Over(fsp2, fsp1) o
--                       Pos_Equal(fsp1, fsp2))`l
theorem Pos_Over_and_Pos_Equal_dominance : SPP_p t(fsp1) & SPP_p t(fsp2) → (∀ (i : Fin fsp1.List.length), ∃ (j : below[(Pos_Over fsp1 fsp2 ∘ Pos_Over fsp2 fsp1 ∘ Pos_Equal fsp1 fsp2).List.length]), (Pos_Over fsp1 fsp2 ∘ Pos_Over fsp2 fsp1 ∘ Pos_Equal fsp1 fsp2).seq
         (j)
      ≤ fsp1.seq i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.divergence_in_Pos_Over_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp2) &
--  parallel_reduction_fix?(E)(t, t2, fsp2) &
--   positionsOF(t)(p) & member_seq(p, Pos_Over( #(p), fsp2))
--  =>
--  parallel_reduction_fix?(E)
--                         (subtermOF(t, p), subtermO
theorem divergence_in_Pos_Over_aux : SPP_p t(fsp2) &
 parallel_reduction_fix_p E(t, t2, fsp2) &
  positionsOF t(p) & member_seq p (Pos_Over #(p) fsp2) → parallel_reduction_fix_p E
                       (subtermOF t p, subtermOF t2 p,
                        complement_pos p fsp2) := by
  -- === TCCs for divergence_in_Pos_Over_aux ===
  -- TCC: divergence_in_Pos_Over_aux_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t2: term[variable, symbol, a...
  have divergence_in_pos_over_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: divergence_in_Pos_Over_aux_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t2: term[variable, symbol, a...
  have divergence_in_pos_over_aux_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: divergence_in_Pos_Over_aux_TCC3
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t2: term[variable, symbol, a...
  have divergence_in_pos_over_aux_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.divergence_in_Pos_Over
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:    SPP?(t)(fsp1) & SPP?(t)(fsp2) & parallel_reduction_fix?(E)(t, t1, fsp1)
--  & parallel_reduction_fix?(E)(t, t2, fsp2)
--  & member_seq(p, Pos_Over(fsp1, fsp2))
--  =>
--  (EXISTS ((e: rewrite_rule[variable, sy
theorem divergence_in_Pos_Over : SPP_p t(fsp1) & SPP_p t(fsp2) & parallel_reduction_fix_p E(t, t1, fsp1)
 & parallel_reduction_fix_p E(t, t2, fsp2)
 & member_seq p (Pos_Over fsp1 fsp2) → (∃ ((e : rewrite_rule[variable, symbol, arity] | member e E), sigma), subtermOF t p = ext sigma(lhs e) &
    subtermOF t1 p = ext sigma(rhs e) &
     parallel_reduction_p E(subtermOF t p, subtermOF t2 p)) := by
  -- === TCCs for divergence_in_Pos_Over ===
  -- TCC: divergence_in_Pos_Over_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t1, t2: term[variable,...
  have divergence_in_pos_over_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: divergence_in_Pos_Over_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t1, t2: term[variable,...
  have divergence_in_pos_over_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: divergence_in_Pos_Over_TCC3
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t1, t2: term[variable,...
  have divergence_in_pos_over_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.subterm_joinability
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem subterm_joinability : True := by
  -- === TCCs for subterm_joinability ===
  -- TCC: subterm_joinability_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        (fsp =
  --          ...
  have subterm_joinability_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_joinability_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        (fsp =
  --          ...
  have subterm_joinability_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_joinability_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reduction_fi...
  have subterm_joinability_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_joinability_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        (fsp =
  --          ...
  have subterm_joinability_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_joinability_TCC3
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        (fsp =
  --          ...
  have subterm_joinability_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: orthogonality.subterms_joinability
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem subterms_joinability : True := by
  -- === TCCs for subterms_joinability ===
  -- TCC: subterms_joinability_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reducti...
  have subterms_joinability_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterms_joinability_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reducti...
  have subterms_joinability_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterms_joinability_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reducti...
  have subterms_joinability_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterms_joinability_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reducti...
  have subterms_joinability_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: orthogonality.parallel_reduction_has_DP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Orthogonal?(E) => diamond_property?(parallel_reduction?(E))
theorem parallel_reduction_has_DP : Orthogonal_p E → diamond_property_p (parallel_reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality.Orthogonal_implies_confluent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (E: Orthogonal): LET RRE = reduction?(E) IN confluent?(RRE)
theorem Orthogonal_implies_confluent : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool), fun (RRE : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) => confluent_p RRE (reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.orthogonality