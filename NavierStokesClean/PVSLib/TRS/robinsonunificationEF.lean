-- Auto-generated by pvs2lean/translator.py
-- PVS theory: robinsonunificationEF
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: robinsonunificationEF

Auto-generated stubs from PVSLib theory `robinsonunificationEF`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.robinsonunificationEF

noncomputable section

variable {pvs_τ1 : Type*} (Card : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Dom : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (IUnion : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (VRan : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (Vars : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (add_last : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (arity : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (below : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (child : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (comp : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (delete : pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (difference : pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (ext : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (first_diff : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} (is_countably_infinite : pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (is_finite : pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} (last : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (left_pos : pvs_τ34 → pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (left_without_children : pvs_τ36 → pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} (length : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (lex2 : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (link_of_frst_diff : pvs_τ41 → pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (member : pvs_τ43 → pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (mgu : pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48)
variable {pvs_τ52 : Type*} (nonempty_p : pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (positionsOF : pvs_τ53 → pvs_τ54 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (singleton : pvs_τ62 → pvs_τ63)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (strict_subset_p : pvs_τ64 → pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} (subset_p : pvs_τ66 → pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} (subtermOF : pvs_τ68 → pvs_τ69 → Prop)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} (unifiable : pvs_τ70 → pvs_τ71 → Prop)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} (union : pvs_τ72 → pvs_τ73 → Prop)
variable {pvs_τ74 : Type*} (vars_p : pvs_τ74 → Prop)

-- pvs2lean: robinsonunificationEF.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.var_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(V)
theorem var_nonempty : nonempty_p V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.symbol_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?({f: symbol | arity(f) = 1})
theorem symbol_nonempty : nonempty_p (fun (f : symbol) => arity f = 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.right_pos
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF NOT positionsOF(s)(p) THEN emptyset
-- ELSE IF p = empty_seq THEN only_empty_seq
--      ELSE LET p1 = delete(p, length(p) - 1) IN
--             LET i: nat = last(p) IN
--               LET n: nat = arity(f(s
axiom right_pos {pvs_τ55 : Type*} {pvs_τ56 : Type*} : pvs_τ55 → pvs_τ56 → Prop

-- pvs2lean: robinsonunificationEF.next_position
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF p = empty_seq THEN empty_seq
-- ELSE LET pi0 = delete(p, length(p) - 1) IN
--        IF f(subtermOF(s, pi0)) /= f(subtermOF(t, pi0)) THEN pi0
--        ELSE LET pi = add_last(delete(p, length(p) - 1), last(
axiom next_position {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} : pvs_τ49 → pvs_τ50 → pvs_τ51 → Prop

-- pvs2lean: robinsonunificationEF.right_pos_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, p: position | positionsOF(s)(p)):
--   subset?(right_pos(s, p), positionsOF(s))
theorem right_pos_subset : ∀ (s : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), subset_p (right_pos s p) (positionsOF s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.next_position_commute
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t: term, p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   next_position(s, t, p) = next_position(t, s, p)
theorem next_position_commute : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), next_position s t p = next_position t s p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.next_position_is_position
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t: term, p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   positionsOF(s)(next_position(s, t, p))
theorem next_position_is_position : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), positionsOF s (next_position s t p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.next_pos_length_and_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t: term,
--         p: position
--             | positionsOF(s)(p) AND positionsOF(t)(p) AND p /= empty_seq):
--   length(next_position(s, t, p)) < length(p) OR
--    (length(next_position(s, t, p)) = 
theorem next_pos_length_and_last : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p) ∧ p ≠ empty_seq}), List.length (next_position s t p) < List.length p ∨ (List.length (next_position s t p) = List.length p ∧
  last p < last (next_position s t p)) := by
  -- === TCCs for next_pos_length_and_last ===
  -- TCC: next_pos_length_and_last_TCC1
  -- Statement: FORALL (s, t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                positionsOF[variable, symbol, a...
  have next_pos_length_and_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_pos_length_and_last_TCC2
  -- Statement: FORALL (s, t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                positionsOF[variable, symbol, a...
  have next_pos_length_and_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.next_pos_is_a_diff_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t: term, p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   (NOT p = empty_seq) IMPLIES next_position(s, t, p) /= p
theorem next_pos_is_a_diff_pos : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), (¬ p = empty_seq) → next_position s t p ≠ p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.member_right_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, p: position | positionsOF(s)(p),
--         q: position | positionsOF(s)(q)):
--   (member(q, right_pos(s, p)) AND p /= q) IFF left_pos(p, q)
theorem member_right_pos : ∀ (s : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}) (q : {q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(q)}), (member q (right_pos s p) ∧ p ≠ q) ↔ left_pos p q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.next_pos_member_right_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t: term,
--         p: position
--             | positionsOF(s)(p) AND positionsOF(t)(p) AND p /= empty_seq):
--   member(next_position(s, t, p), right_pos(s, p))
theorem next_pos_member_right_pos : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p) ∧ p ≠ empty_seq}), member (next_position s t p) (right_pos s p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.equal_right_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, p: position | positionsOF(s)(p),
--         q: position | positionsOF(s)(q)):
--   right_pos(s, p) = right_pos(s, q) IMPLIES p = q
theorem equal_right_pos : ∀ (s : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}) (q : {q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(q)}), right_pos s p = right_pos s q → p = q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.subset_right_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, p: position | positionsOF(s)(p),
--         q: position | positionsOF(s)(q)):
--   member(q, right_pos(s, p)) AND q /= p IMPLIES
--    strict_subset?(right_pos(s, q), right_pos(s, p))
theorem subset_right_pos : ∀ (s : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}) (q : {q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(q)}), member q (right_pos s p) ∧ q ≠ p → strict_subset_p (right_pos s q) (right_pos s p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.next_pos_to_the_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND positionsOF(t)(p) AND p /= empty_seq):
--   Card(right_pos(s, next_position(s, t, p))) < Card(right_pos(s, p))
theorem next_pos_to_the_right : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p) ∧ p ≠ empty_seq}), Card (right_pos s (next_position s t p)) < Card (right_pos s p) := by
  -- === TCCs for next_pos_to_the_right ===
  -- TCC: next_pos_to_the_right_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have next_pos_to_the_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_pos_to_the_right_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have next_pos_to_the_right_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.ext_link_remove_x
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET sig = link_of_frst_diff(subtermOF(s, p
theorem ext_link_remove_x : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => (¬ sig = fail ∧ Dom sig(x)) → (¬ member x (Vars (ext sig(s)))) ∧ (¬ member x (Vars (ext sig(t)))) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) := by
  -- === TCCs for ext_link_remove_x ===
  -- TCC: ext_link_remove_x_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have ext_link_remove_x_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.vars_ext_link_s_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET sig = link_of_frst_diff(subtermOF(s, p
theorem vars_ext_link_s_subset : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → subset_p (Vars (ext sig(s))) (union (Vars s) (Vars t)) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.vars_ext_link_t_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET sig = link_of_frst_diff(subtermOF(s, p
theorem vars_ext_link_t_subset : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → subset_p (Vars (ext sig(t))) (union (Vars s) (Vars t)) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.union_vars_ext_link_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET sig = link_of_frst_diff(subtermOF(s, p
theorem union_vars_ext_link_subterm : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → union (Vars (ext sig(s))) (Vars (ext sig(t))) =
 difference (union (Vars s) (Vars t)) (Dom sig) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.termination_lemma_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET sig = link_of_frst_diff(subtermOF(s, p
theorem termination_lemma_subterm : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → Card (union (Vars (ext sig(s))) (Vars (ext sig(t)))) <
 Card (union (Vars s) (Vars t)) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) := by
  -- === TCCs for termination_lemma_subterm ===
  -- TCC: termination_lemma_subterm_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have termination_lemma_subterm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: termination_lemma_subterm_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have termination_lemma_subterm_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.subtermOF_next_position
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   LET q = next_position(s, t, p) IN subtermOF(s, q) /= subtermOF(t, q)
theorem subtermOF_next_position : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), fun (q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => subtermOF s q ≠ subtermOF t q (next_position s t p) := by
  -- === TCCs for subtermOF_next_position ===
  -- TCC: subtermOF_next_position_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AN...
  have subtermof_next_position_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subtermOF_next_position_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AN...
  have subtermof_next_position_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.np_o_fd_is_position
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   LET q = next_position(s, t, p) IN
--     LET q1 = first_diff(subtermOF(s, q), subtermOF(t, q)) IN
--     
theorem np_o_fd_is_position : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), fun (q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => fun (q1 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => positionsOF t (q O q1) (first_diff (subtermOF s q) (subtermOF t q)) (next_position s t p) := by
  -- === TCCs for np_o_fd_is_position ===
  -- TCC: np_o_fd_is_position_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AN...
  have np_o_fd_is_position_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.child_np_child_p
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   (FORALL (q: position | positionsOF(s)(q) AND positionsOF(t)(q)):
--      child(next_position(s, t, p), q) IMPLI
theorem child_np_child_p : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), ∀ (q : {q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(q) ∧ positionsOF t(q)}), child (next_position s t p) q → child p q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.next_pos_empty_equal_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND positionsOF(t)(p) AND p /= empty_seq):
--   (next_position(s, t, p) = empty_seq AND
--     FORALL (p1: position | positionsO
theorem next_pos_empty_equal_subterm : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p) ∧ p ≠ empty_seq}), (next_position s t p = empty_seq ∧
  ∀ (p1 : position | positionsOF s(p1) ∧ positionsOF t(p1)), child p p1 → f (subtermOF s p1) = f (subtermOF t p1)) → (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children p q → subtermOF s q = subtermOF t q) := by
  -- === TCCs for next_pos_empty_equal_subterm ===
  -- TCC: next_pos_empty_equal_subterm_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have next_pos_empty_equal_subterm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_pos_empty_equal_subterm_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have next_pos_empty_equal_subterm_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.next_pos_equal_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND positionsOF(t)(p) AND p /= empty_seq):
--   ((FORALL (p1: position | positionsOF(s)(p1) AND positionsOF(t)(p1)):
--       ch
theorem next_pos_equal_subterm : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p) ∧ p ≠ empty_seq}), ((∀ (p1 : position | positionsOF s(p1) ∧ positionsOF t(p1)), child p p1 → f (subtermOF s p1) = f (subtermOF t p1))
  ∧
  (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q p → subtermOF s q = subtermOF t q)
   ∧
   subtermOF s p = subtermOF t p ∧
    next_position s t p ≠ empty_seq) → (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q (next_position s t p) →
    subtermOF s q = subtermOF t q) := by
  -- === TCCs for next_pos_equal_subterm ===
  -- TCC: next_pos_equal_subterm_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have next_pos_equal_subterm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_pos_equal_subterm_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have next_pos_equal_subterm_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.fd_equal_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   LET fd = first_diff(s, t) IN
--     left_without_children(p, fd) IMPLIES subtermOF(s, p) = subtermOF(t
theorem fd_equal_subterm : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), fun (fd : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => left_without_children p fd → subtermOF s p = subtermOF t p (first_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.child_p_o_fd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET fd = first_diff(subtermOF(s, p), subte
theorem child_p_o_fd : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (fd : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), child p q → f (subtermOF s q) = f (subtermOF t q)) → (∀ (q : position
             | positionsOF (ext sig(s)) (q) ∧
                positionsOF (ext sig(t)) (q)), child (p ∘ fd) q →
    f (subtermOF (ext sig(s)) q) = f (subtermOF (ext sig(t)) q)) (first_diff (subtermOF s p) (subtermOF t p)) := by
  -- === TCCs for child_p_o_fd ===
  -- TCC: child_p_o_fd_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have child_p_o_fd_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: child_p_o_fd_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have child_p_o_fd_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: child_p_o_fd_TCC3
  -- Statement: FORALL (sig: Sub[variable, symbol, arity],
  --         s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variabl...
  have child_p_o_fd_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: child_p_o_fd_TCC4
  -- Statement: FORALL (sig: Sub[variable, symbol, arity],
  --         s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variabl...
  have child_p_o_fd_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.separation_lwc_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: left_without_children(q, p o p1) AND NOT left_without_children(q, p)
--  IMPLIES EXISTS q1: q = p o q1 AND left_without_children(q1, p1)
theorem separation_lwc_pos : left_without_children q (p ∘ p1) ∧ ¬ left_without_children q p → ∃ q1,  q = p ∘ q1 ∧ left_without_children q1 p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.lwc_o_fd_empty_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET fd = first_diff(s, t) IN
--     (left_without_children(p, fd) AND
--       positionsOF(s)(p) AND
--        p = p1 o p2 AND positionsOF(t)(p1) AND vars?(subtermOF(t, p1
theorem lwc_o_fd_empty_seq : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (fd : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => (left_without_children p fd ∧
  positionsOF s(p) ∧
   p = p1 ∘ p2 ∧ positionsOF t(p1) ∧ vars_p (subtermOF t p1)) → p2 = empty_seq (first_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.lwc_p_o_fd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET fd = first_diff(subtermOF(s, p), subte
theorem lwc_p_o_fd : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (fd : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q p → subtermOF s q = subtermOF t q) → (∀ (q : position
             | positionsOF (ext sig(s)) (q) ∧
                positionsOF (ext sig(t)) (q)), left_without_children q (p ∘ fd) →
    subtermOF (ext sig(s)) q = subtermOF (ext sig(t)) q) (first_diff (subtermOF s p) (subtermOF t p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.np_p_o_fd_empty_unifiable_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET fd = first_diff(subtermOF(s, p), subte
theorem np_p_o_fd_empty_unifiable_term : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (fd : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => (¬ sig = fail ∧
  next_position (ext sig(s)) (ext sig(t)) (p ∘ fd) = empty_seq ∧
   (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q p → subtermOF s q = subtermOF t q)
    ∧
    (∀ (p1 : position | positionsOF s(p1) ∧ positionsOF t(p1)), child p p1 → f (subtermOF s p1) = f (subtermOF t p1))) → ext sig(s) = ext sig(t) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) (first_diff (subtermOF s p) (subtermOF t p)) := by
  -- === TCCs for np_p_o_fd_empty_unifiable_term ===
  -- TCC: np_p_o_fd_empty_unifiable_term_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have np_p_o_fd_empty_unifiable_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.np_p_o_fd_equal_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET fd = first_diff(subtermOF(s, p), subte
theorem np_p_o_fd_equal_subterm : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (fd : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => (¬ sig = fail ∧
  next_position (ext sig(s)) (ext sig(t)) (p ∘ fd) ≠ empty_seq ∧
   (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q p → subtermOF s q = subtermOF t q)
    ∧
    (∀ (p1 : position | positionsOF s(p1) ∧ positionsOF t(p1)), child p p1 → f (subtermOF s p1) = f (subtermOF t p1))) → (∀ (q : position
             | positionsOF (ext sig(s)) (q) ∧
                positionsOF (ext sig(t)) (q)), left_without_children q (next_position (ext sig(s)) (ext sig(t)) (p ∘ fd))
    → subtermOF (ext sig(s)) q = subtermOF (ext sig(t)) q) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) (first_diff (subtermOF s p) (subtermOF t p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.robinson_unification_algorithm_aux
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF subtermOF(s, p) = subtermOF(t, p)
--   THEN LET pi = next_position(s, t, p) IN
--          IF pi = empty_seq THEN identity
--          ELSE robinson_unification_algorithm_aux(s, t, pi)
--          ENDIF
-- ELSE L
axiom robinson_unification_algorithm_aux {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} : pvs_τ59 → pvs_τ60 → pvs_τ61 → Prop

-- pvs2lean: robinsonunificationEF.robinson_unification_algorithm_EF
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: robinson_unification_algorithm_aux(s, t, empty_seq)
axiom robinson_unification_algorithm_EF {pvs_τ57 : Type*} {pvs_τ58 : Type*} : pvs_τ57 → pvs_τ58 → Prop

-- pvs2lean: robinsonunificationEF.unifiable_implies_not_fail1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET sig = link_of_frst_diff(subtermOF(s, p
theorem unifiable_implies_not_fail1 : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => unifiable s t → ¬ sig = fail (link_of_frst_diff (subtermOF s p) (subtermOF t p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.preserving_generality1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   member(rho, U(s, t)) IMPLIES
--    LET sig = 
theorem preserving_generality1 : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), member rho (U s t) → (let sig := link_of_frst_diff (subtermOF s p) (subtermOF t p); ∃ theta,  rho = comp theta sig) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.unifiable_preserves_unifiability1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET sig = link_of_frst_diff(subtermOF(s, p
theorem unifiable_preserves_unifiability1 : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => unifiable s t → unifiable (ext sig(s)) ((ext sig(t))) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.dom_ruaEF_subset_union_vars_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   unifiable(s, t) IMPLIES
--    LET sigma = robinson_unification_algorithm_aux(s, t, p) IN
--      subset?(Dom(sigma
theorem dom_ruaEF_subset_union_vars_aux : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), unifiable s t → (let sigma := robinson_unification_algorithm_aux s t p; subset_p (Dom sigma) (union (Vars s) (Vars t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.dom_ruaEF_subset_union_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifiable(s, t) IMPLIES
--  LET sigma = robinson_unification_algorithm_EF(s, t) IN
--    subset?(Dom(sigma), union(Vars(s), Vars(t)))
theorem dom_ruaEF_subset_union_vars : unifiable s t → (let sigma := robinson_unification_algorithm_EF s t; subset_p (Dom sigma) (union (Vars s) (Vars t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.vran_ruaEF_subset_union_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position
--             | positionsOF(s)(p) AND
--                positionsOF(t)(p) AND subtermOF(s, p) /= subtermOF(t, p)):
--   LET pi = first_diff(subtermOF(s, p), subte
theorem vran_ruaEF_subset_union_aux : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧
 positionsOF t(p) ∧ subtermOF s p ≠ subtermOF t p}), fun (pi : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => fun (sig1 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => fun (np : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => fun (sig2 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => unifiable s t → subset_p (VRan (comp sig2 sig1)) (union (VRan sig2) (difference (VRan sig1) (Dom sig2))) (robinson_unification_algorithm_aux (ext sig1(s)) (ext sig1(t)) np) (next_position (ext sig1(s)) (ext sig1(t)) (p ∘ pi)) (link_of_frst_diff (subtermOF s p) (subtermOF t p)) (first_diff (subtermOF s p) (subtermOF t p)) := by
  -- === TCCs for vran_ruaEF_subset_union_aux ===
  -- TCC: vran_ruaEF_subset_union_aux_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have vran_ruaef_subset_union_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: vran_ruaEF_subset_union_aux_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have vran_ruaef_subset_union_aux_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.dom_ran_ruaEF_disjoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   unifiable(s, t) IMPLIES
--    LET sigma = robinson_unification_algorithm_aux(s, t, p) IN
--      subset?(VRan(sigm
theorem dom_ran_ruaEF_disjoint : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), unifiable s t → (let sigma := robinson_unification_algorithm_aux s t p; subset_p (VRan sigma) (difference (union (Vars s) (Vars t)) (Dom sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.ruaEF_fails_iff_non_unifiable_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   (FORALL (q: position | positionsOF(s)(q) AND positionsOF(t)(q)):
--      left_without_children(q, p) => subterm
theorem ruaEF_fails_iff_non_unifiable_aux : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q p → subtermOF s q = subtermOF t q)
 ∧
 (∀ (p1 : position | positionsOF s(p1) ∧ positionsOF t(p1)), child p p1 → f (subtermOF s p1) = f (subtermOF t p1)) → (¬ unifiable s t ↔ robinson_unification_algorithm_aux s t p = fail) := by
  -- === TCCs for ruaEF_fails_iff_non_unifiable_aux ===
  -- TCC: ruaEF_fails_iff_non_unifiable_aux_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have ruaef_fails_iff_non_unifiable_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ruaEF_fails_iff_non_unifiable_aux_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have ruaef_fails_iff_non_unifiable_aux_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.ruaEF_gives_unifier_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   (FORALL (q: position | positionsOF(s)(q) AND positionsOF(t)(q)):
--      left_without_children(q, p) => subterm
theorem ruaEF_gives_unifier_aux : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q p → subtermOF s q = subtermOF t q)
 ∧
 (∀ (p1 : position | positionsOF s(p1) ∧ positionsOF t(p1)), child p p1 → f (subtermOF s p1) = f (subtermOF t p1)) → (unifiable s t ↔
  member (robinson_unification_algorithm_aux s t p) (U s t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.ruaEF_gives_mg_subs_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   (FORALL (q: position | positionsOF(s)(q) AND positionsOF(t)(q)):
--      left_without_children(q, p) => subterm
theorem ruaEF_gives_mg_subs_aux : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q p → subtermOF s q = subtermOF t q)
 ∧
 (∀ (p1 : position | positionsOF s(p1) ∧ positionsOF t(p1)), child p p1 → f (subtermOF s p1) = f (subtermOF t p1)) → (member rho (U s t) →
  robinson_unification_algorithm_aux s t p ≤ rho) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.ruaEF_fails_iff_non_unifiable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT unifiable(s, t) IFF robinson_unification_algorithm_EF(s, t) = fail
theorem ruaEF_fails_iff_non_unifiable : ¬ unifiable s t ↔ robinson_unification_algorithm_EF s t = fail := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.ruaEF_gives_unifier
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifiable(s, t) IFF member(robinson_unification_algorithm_EF(s, t), U(s, t))
theorem ruaEF_gives_unifier : unifiable s t ↔ member (robinson_unification_algorithm_EF s t) (U s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.ruaEF_gives_mg_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(rho, U(s, t)) IMPLIES robinson_unification_algorithm_EF(s, t) <= rho
theorem ruaEF_gives_mg_subs : member rho (U s t) → robinson_unification_algorithm_EF s t ≤ rho := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunificationEF.completeness_ruaEF
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem completeness_ruaEF : True := by
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.robinsonunificationEF