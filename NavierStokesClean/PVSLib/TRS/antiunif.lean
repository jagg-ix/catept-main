-- Auto-generated by pvs2lean/translator.py
-- PVS theory: antiunif
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: antiunif

Auto-generated stubs from PVSLib theory `antiunif`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.antiunif

noncomputable section

variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (add : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (app : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (app_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (append : pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (arg : pvs_τ23 → pvs_τ24)
variable {pvs_τ27 : Type*} (car : pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (card : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (cdr : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (cons : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} (cons_p : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (disjoint_p : pvs_τ36 → pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (dom : pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} (empty_p : pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (epsilon : pvs_τ41 → pvs_τ42)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (img : pvs_τ50 → pvs_τ51)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} (intersection : pvs_τ52 → pvs_τ53 → pvs_τ54)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} (is_finite : pvs_τ55 → pvs_τ56)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} (label : pvs_τ57 → pvs_τ58)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (length : pvs_τ61 → pvs_τ62)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} (map : pvs_τ67 → pvs_τ68 → pvs_τ69)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} (member : pvs_τ78 → pvs_τ79 → Prop)
variable {pvs_τ80 : Type*} (nice_p : pvs_τ80 → Prop)
variable {pvs_τ83 : Type*} {pvs_τ84 : Type*} {pvs_τ85 : Type*} (nth : pvs_τ83 → pvs_τ84 → pvs_τ85)
variable {pvs_τ86 : Type*} (null_p : pvs_τ86 → Prop)
variable {pvs_τ87 : Type*} {pvs_τ88 : Type*} {pvs_τ89 : Type*} (pair : pvs_τ87 → pvs_τ88 → pvs_τ89)
variable {pvs_τ90 : Type*} (pair_p : pvs_τ90 → Prop)
variable {pvs_τ94 : Type*} {pvs_τ95 : Type*} (singleton : pvs_τ94 → pvs_τ95)
variable {pvs_τ97 : Type*} {pvs_τ98 : Type*} (subs : pvs_τ97 → pvs_τ98 → Prop)
variable {pvs_τ99 : Type*} {pvs_τ100 : Type*} (subset_p : pvs_τ99 → pvs_τ100 → Prop)
variable {pvs_τ101 : Type*} {pvs_τ102 : Type*} (sum : pvs_τ101 → pvs_τ102)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} (supset_dom : pvs_τ103 → pvs_τ104)
variable {pvs_τ105 : Type*} {pvs_τ106 : Type*} (sym : pvs_τ105 → pvs_τ106)
variable {pvs_τ107 : Type*} {pvs_τ108 : Type*} (term1 : pvs_τ107 → pvs_τ108)
variable {pvs_τ109 : Type*} {pvs_τ110 : Type*} (term2 : pvs_τ109 → pvs_τ110)
variable {pvs_τ111 : Type*} {pvs_τ112 : Type*} {pvs_τ113 : Type*} (union : pvs_τ111 → pvs_τ112 → pvs_τ113)
variable {pvs_τ118 : Type*} {pvs_τ119 : Type*} (pvs_variable : pvs_τ118 → pvs_τ119)

-- pvs2lean: antiunif.makeAUT
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# lhs := lhs, rhs := rhs, label := label #)
axiom makeAUT {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} : pvs_τ63 → pvs_τ64 → pvs_τ65 → pvs_τ66

-- pvs2lean: antiunif.vars
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vars(eq`lhs) ∪ vars(eq`rhs)
axiom vars {pvs_τ120 : Type*} {pvs_τ121 : Type*} : pvs_τ120 → pvs_τ121

-- pvs2lean: antiunif.validAUT?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ¬member(eq`label, vars(eq))
axiom validAUT_p {pvs_τ114 : Type*} : pvs_τ114 → Prop

-- pvs2lean: antiunif.size
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: size(eq`lhs) + size(eq`rhs)
axiom size {pvs_τ96 : Type*} : pvs_τ96 → Prop

-- pvs2lean: antiunif.match_DecF?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET lhs = eq`lhs, rhs = eq`rhs IN
--   (app?(lhs) ∧ app?(rhs)) ∧ sym(lhs) = sym(rhs)
axiom match_DecF_p {pvs_τ71 : Type*} : pvs_τ71 → Prop

-- pvs2lean: antiunif.match_DecP?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET lhs = eq`lhs, rhs = eq`rhs IN pair?(lhs) ∧ pair?(rhs)
axiom match_DecP_p {pvs_τ73 : Type*} : pvs_τ73 → Prop

-- pvs2lean: antiunif.match_Synt?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET lhs = eq`lhs, rhs = eq`rhs IN ¬app?(lhs) ∧ ¬pair?(lhs) ∧ lhs = rhs
axiom match_Synt_p {pvs_τ77 : Type*} : pvs_τ77 → Prop

-- pvs2lean: antiunif.match_Sol?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT (match_DecF?(eq) ∨ match_DecP?(eq) ∨ match_Synt?(eq))
axiom match_Sol_p {pvs_τ75 : Type*} : pvs_τ75 → Prop

-- pvs2lean: antiunif.AUT_classification
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (eq: AUT):
--   (match_DecF?(eq) ∨ match_DecP?(eq) ∨ match_Synt?(eq) ∨ match_Sol?(eq)) ∧
--    ((match_DecF?(eq) ∧
--       ¬(match_DecP?(eq) ∨ match_Synt?(eq) ∨ match_Sol?(eq)))
--      ∨
--      (match_DecP
theorem AUT_classification : ∀ (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))), (match_DecF_p eq ∨ match_DecP_p eq ∨ match_Synt_p eq ∨ match_Sol_p eq) ∧ ((match_DecF_p eq ∧ ¬(match_DecP_p eq ∨ match_Synt_p eq ∨ match_Sol_p eq))
  ∨
  (match_DecP_p eq ∧ ¬(match_DecF_p eq ∨ match_Synt_p eq ∨ match_Sol_p eq))
   ∨
   (match_Synt_p eq ∧
     ¬(match_DecF_p eq ∨ match_DecP_p eq ∨ match_Sol_p eq))
    ∨
    (match_Sol_p eq ∧
      ¬(match_DecF_p eq ∨ match_DecP_p eq ∨ match_Synt_p eq))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.vars_in_append_List_AUT
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: List_AUT): vars(append(s1, s2)) = union(vars(s1), vars(s2))
theorem vars_in_append_List_AUT : ∀ (s1 : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (s2 : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), vars (append s1 s2) = union (vars s1) (vars s2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.subset_vars_AUT_AUTs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: List_AUT, eq: AUT):
--   member(eq, s) IMPLIES subset?(vars(eq), vars(s))
theorem subset_vars_AUT_AUTs : ∀ (s : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))), member eq s → subset_p (vars eq) (vars s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.labels
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(s) THEN ∅ ELSE add(car(s)`label, labels(cdr(s))) ENDIF
axiom labels {pvs_τ59 : Type*} {pvs_τ60 : Type*} : pvs_τ59 → pvs_τ60

-- pvs2lean: antiunif.AUT_member_AUT_label_in_labels
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (le: List_AUT, eq: AUT):
--   member(eq, le) IMPLIES member(eq`label, labels(le))
theorem AUT_member_AUT_label_in_labels : ∀ (le : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))), member eq le → member eq.label (labels le) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.AUTs_label_AUT_this_label
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (le: List_AUT, lbl: variable):
--   member(lbl, labels(le)) IMPLIES
--    EXISTS (eq: AUT): member(eq, le) AND eq`label = lbl
theorem AUTs_label_AUT_this_label : ∀ (le : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (lbl : {i : ℝ // i ≥ 0}), member lbl (labels le) → ∃ (eq : AUT), member eq le ∧ eq.label = lbl := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.card_lbls_in_List_AUT
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (le: List_AUT): card(labels(le)) <= length(le)
theorem card_lbls_in_List_AUT : ∀ (le : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), card (labels le) ≤ List.length le := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.append_labels_is_union_labels
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts1, auts2: List_AUT):
--   labels(append(auts1, auts2)) = union(labels(auts1), labels(auts2))
theorem append_labels_is_union_labels : ∀ (auts1 : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (auts2 : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), labels (append auts1 auts2) = union (labels auts1) (labels auts2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.valid_AUTs?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty?(vars(s) ∩ labels(s)) ∧ card(labels(s)) = length(s)
axiom valid_AUTs_p {pvs_τ117 : Type*} : pvs_τ117 → Prop

-- pvs2lean: antiunif.disjoint_valid_append_valid_AUTs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts1, auts2: List_AUT):
--   valid_AUTs?(append(auts1, auts2)) IMPLIES
--    disjoint?(labels(auts1), labels(auts2))
theorem disjoint_valid_append_valid_AUTs : ∀ (auts1 : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (auts2 : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), valid_AUTs_p (append auts1 auts2) → disjoint_p (labels auts1) (labels auts2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.non_member_label_valid_AUTs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?) | cons?(auts)):
--   NOT member(car(auts)`label, labels(cdr(auts)))
theorem non_member_label_valid_AUTs : ∀ (auts : {auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) // cons_p auts}), ¬(member (car auts.label) (labels (cdr auts))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.validity_cdr_valid_AUTs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?) | cons?(auts)): valid_AUTs?(cdr(auts))
theorem validity_cdr_valid_AUTs : ∀ (auts : {auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) // cons_p auts}), valid_AUTs_p (cdr auts) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.validity_AUT_in_valid_AUTs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?), i: below[length(auts)]):
--   validAUT?(nth(auts, i))
theorem validity_AUT_in_valid_AUTs : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length auts}), validAUT_p (nth auts i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.non_member_var_nth_label
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?), i: below[length(auts)], v: variable):
--   NOT member(v, labels(auts)) IMPLIES v /= label(nth(auts, i))
theorem non_member_var_nth_label : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length auts}) (v : {i : ℝ // i ≥ 0}), ¬ member v (labels auts) → v ≠ label (nth auts i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.validity_append_valid_AUTs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: List_AUT):
--   valid_AUTs?(append(l1, l2)) IMPLIES valid_AUTs?(l1) AND valid_AUTs?(l2)
theorem validity_append_valid_AUTs : ∀ (l1 : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (l2 : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), valid_AUTs_p (append l1 l2) → valid_AUTs_p l1 ∧ valid_AUTs_p l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.repeated_AUT?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eq1`lhs = eq2`lhs ∧ eq1`rhs = eq2`rhs
axiom repeated_AUT_p {pvs_τ92 : Type*} {pvs_τ93 : Type*} : pvs_τ92 → pvs_τ93 → Prop

-- pvs2lean: antiunif.AUT_repeated_in?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cons?(s) ∧ (repeated_AUT?(eq, car(s)) ∨ AUT_repeated_in?(eq, cdr(s)))
axiom AUT_repeated_in_p {pvs_τ1 : Type*} {pvs_τ2 : Type*} : pvs_τ1 → pvs_τ2 → Prop

-- pvs2lean: antiunif.first_AUT_repeated?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cons?(s) ∧ AUT_repeated_in?(car(s), cdr(s))
axiom first_AUT_repeated_p {pvs_τ43 : Type*} : pvs_τ43 → Prop

-- pvs2lean: antiunif.cdr_first_AUT_repeated
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: (first_AUT_repeated?)): cons?(cdr(s))
theorem cdr_first_AUT_repeated : ∀ (s : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), cons_p (cdr s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.rep_AUT_in
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF repeated_AUT?(car(s), car(cdr(s))) THEN car(cdr(s))
-- ELSE rep_AUT_in(cons(car(s), cdr(cdr(s))))
-- ENDIF
axiom rep_AUT_in {pvs_τ91 : Type*} : pvs_τ91 → Prop

-- pvs2lean: antiunif.repeated_AUT_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: (first_AUT_repeated?)):
--   rep_AUT_in(s)`lhs = car(s)`lhs AND rep_AUT_in(s)`rhs = car(s)`rhs
theorem repeated_AUT_correct : ∀ (s : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), rep_AUT_in s.lhs = car s.lhs ∧ rep_AUT_in s.rhs = car s.rhs := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.rep_AUT_in_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: (first_AUT_repeated?)): member(rep_AUT_in(s), cdr(s))
theorem rep_AUT_in_cdr : ∀ (s : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), member (rep_AUT_in s) (cdr s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.nonrepeated?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(s) <= 1 ∨ (NOT first_AUT_repeated?(s) ∧ nonrepeated?(cdr(s)))
axiom nonrepeated_p {pvs_τ81 : Type*} : pvs_τ81 → Prop

-- pvs2lean: antiunif.allSolvedAUTs?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null?(s) ∨ (match_Sol?(car(s)) ∧ allSolvedAUTs?(cdr(s)))
axiom allSolvedAUTs_p {pvs_τ14 : Type*} : pvs_τ14 → Prop

-- pvs2lean: antiunif.validSolvedAUTs?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: allSolvedAUTs?(s) ∧ nonrepeated?(s)
axiom validSolvedAUTs_p {pvs_τ116 : Type*} : pvs_τ116 → Prop

-- pvs2lean: antiunif.validConfiguration?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET allAUTs = append(c`unsolved, c`solved) IN
--   valid_AUTs?(allAUTs) ∧
--    empty?(supset_dom(c`substitution) ∩ labels(allAUTs)) ∧
--     validSolvedAUTs?(c`solved) ∧
--      empty?(supset_dom(c`substitution)
axiom validConfiguration_p {pvs_τ115 : Type*} : pvs_τ115 → Prop

-- pvs2lean: antiunif.disjoint_labels_unsolved_solved
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   disjoint?(labels(c`unsolved), labels(c`solved))
theorem disjoint_labels_unsolved_solved : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), disjoint_p (labels c.unsolved) (labels c.solved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.validity_car_conf_unsolved
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved)):
--   validAUT?(car(c`unsolved))
theorem validity_car_conf_unsolved : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}), validAUT_p (car c.unsolved) := by
  -- === TCCs for validity_car_conf_unsolved ===
  -- TCC: validity_car_conf_unsolved_TCC1
  -- Statement: FORALL (c: (validConfiguration?) | cons?[AUT](c`unsolved)):
  --   cons?[AUT](c`unsolved)
  have validity_car_conf_unsolved_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.validity_cdr_conf_unsolved
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved), eq: AUT):
--   member(eq, cdr(c`unsolved)) IMPLIES validAUT?(eq)
theorem validity_cdr_conf_unsolved : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}) (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))), member eq (cdr c.unsolved) → validAUT_p eq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.invariance_labels_in_validConf
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   LET allAUTs = append(c`unsolved, c`solved) IN
--     FORALL (l: (labels(allAUTs))):
--       subs(c`substitution)(variable(l)) = variable(l)
theorem invariance_labels_in_validConf : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (allAUTs : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => ∀ (l : {i : ℝ // i ≥ 0}), subs c.substitution(variable l) = variable l (append c.unsolved c.solved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.cdr_is_validConf
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem cdr_is_validConf : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: antiunif.labels_allAUTs_as_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   labels(append(c`unsolved, c`solved)) =
--    union(labels(c`unsolved), labels(c`solved))
theorem labels_allAUTs_as_union : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), labels (append c.unsolved c.solved) = union (labels c.unsolved) (labels c.solved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshLabel
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: epsilon((LAMBDA (v: variable): NOT member(v, V)))
axiom freshLabel {pvs_τ44 : Type*} {pvs_τ45 : Type*} : pvs_τ44 → pvs_τ45 → Prop

-- pvs2lean: antiunif.freshness_membship
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (V: finite_set[variable]): NOT member(freshLabel(V), V)
theorem freshness_membship : ∀ (V : {i : ℝ // i ≥ 0} → Bool), ¬(member (freshLabel V) V) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_epsilon_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?), V: finite_set[variable]):
--   NOT member(freshLabel(c, V), union(vars(c), V))
theorem freshness_epsilon_ext : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (V : {i : ℝ // i ≥ 0} → Bool), ¬(member (freshLabel c V) (union (vars c) V)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_epsilon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)): NOT member(freshLabel(c), vars(c))
theorem freshness_epsilon : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ¬(member (freshLabel c) (vars c)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   NOT member(freshLabel(c), vars(append(c`unsolved, c`solved)))
theorem freshness_vars : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ¬(member (freshLabel c) (vars (append c.unsolved c.solved))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_labels
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   NOT member(freshLabel(c), labels(append(c`unsolved, c`solved)))
theorem freshness_labels : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ¬(member (freshLabel c) (labels (append c.unsolved c.solved))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_nth_label
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?),
--         i: below[length(c`unsolved) + length(c`solved)]):
--   NOT freshLabel(c) = (nth(append(c`unsolved, c`solved), i))`label
theorem freshness_nth_label : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length c.solved + List.length c.unsolved}), ¬(freshLabel c = (nth (append c.unsolved c.solved) i).label) := by
  -- === TCCs for freshness_nth_label ===
  -- TCC: freshness_nth_label_TCC1
  -- Statement: FORALL (c: (validConfiguration?),
  --         i: below[length[AUT](c`unsolved) + length[AUT](c`solved)]):
  --   i < length[AUT](append[AUT](c`unsolved, c`solved))
  have freshness_nth_label_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_car_label
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved)):
--   NOT freshLabel(c) = label(car(c`unsolved))
theorem freshness_car_label : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}), ¬(freshLabel c = label (car c.unsolved)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_labels_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?), V: finite_set[variable]):
--   NOT member(freshLabel(c, V), labels(append(c`unsolved, c`solved)))
theorem freshness_labels_ext : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (V : {i : ℝ // i ≥ 0} → Bool), ¬(member (freshLabel c V) (labels (append c.unsolved c.solved))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   NOT member(freshLabel(c),
--              union(supset_dom(c`substitution), vars(img(c`substitution))))
theorem freshness_subs : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ¬(member (freshLabel c) (union (supset_dom c.substitution) (vars (img c.substitution)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_subs_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?), V: finite_set[variable]):
--   NOT member(freshLabel(c, V),
--              union(supset_dom(c`substitution), vars(img(c`substitution))))
theorem freshness_subs_ext : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (V : {i : ℝ // i ≥ 0} → Bool), ¬(member (freshLabel c V) (union (supset_dom c.substitution) (vars (img c.substitution)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.freshness_subs_dom_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?), V: finite_set[variable]):
--   NOT member(freshLabel(c, V), dom(c`substitution))
theorem freshness_subs_dom_ext : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (V : {i : ℝ // i ≥ 0} → Bool), ¬(member (freshLabel c V) (dom c.substitution)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.car_lbl_fresh_in_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved)):
--   NOT member(label(car(c`unsolved)),
--              labels(append(cdr(c`unsolved), c`solved)))
theorem car_lbl_fresh_in_cdr : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}), ¬(member (label (car c.unsolved)) (labels (append (cdr c.unsolved) c.solved))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.car_lbl_fresh_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved)):
--   NOT member(label(car(c`unsolved)), dom(c`substitution))
theorem car_lbl_fresh_dom : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}), ¬(member (label (car c.unsolved)) (dom c.substitution)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.emptyness_conf_vars_with_lbls_and_fresh_variables
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved),
--         set_lbl: finite_set[variable]):
--   (FORALL (lbl: variable):
--      member(lbl, set_lbl) IMPLIES NOT member(lbl, vars(c)))
--    IMPLIES
--    empty
theorem emptyness_conf_vars_with_lbls_and_fresh_variables : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}) (set_lbl : {i : ℝ // i ≥ 0} → Bool), (∀ (lbl : variable), member lbl set_lbl → ¬ member lbl (vars c)) → empty_p (intersection (vars (append c.unsolved c.solved)) (union set_lbl (labels (append (cdr c.unsolved) c.solved)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.emptyness_conf_supdom_with_lbls_and_fresh_variables
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved),
--         set_lbl: finite_set[variable]):
--   (FORALL (lbl: variable):
--      member(lbl, set_lbl) IMPLIES NOT member(lbl, vars(c)))
--    IMPLIES
--    empty
theorem emptyness_conf_supdom_with_lbls_and_fresh_variables : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}) (set_lbl : {i : ℝ // i ≥ 0} → Bool), (∀ (lbl : variable), member lbl set_lbl → ¬ member lbl (vars c)) → empty_p (intersection (supset_dom c.substitution) (union set_lbl (labels (append (cdr c.unsolved) c.solved)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.emptyness_conf_supdom_with_car_lbls_and_fresh_variables
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved),
--         set_lbl: finite_set[variable]):
--   LET eq = car(c`unsolved) IN
--     (FORALL (lbl: variable):
--        member(lbl, set_lbl) IMPLIES NOT member(
theorem emptyness_conf_supdom_with_car_lbls_and_fresh_variables : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}) (set_lbl : {i : ℝ // i ≥ 0} → Bool), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => (∀ (lbl : variable), member lbl set_lbl → ¬ member lbl (vars c)) → empty_p (intersection (union (singleton eq.label) (supset_dom c.substitution)) (union set_lbl (labels (append (cdr c.unsolved) c.solved)))) (car c.unsolved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.emptyness_conf_var_with_lbls_DecF
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved), lbl: variable):
--   NOT member(lbl, vars(c)) IMPLIES
--    empty?(intersection(vars(append(c`unsolved, c`solved)),
--                        add(lbl, labe
theorem emptyness_conf_var_with_lbls_DecF : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}) (lbl : {i : ℝ // i ≥ 0}), ¬ member lbl (vars c) → empty_p (intersection (vars (append c.unsolved c.solved)) (add lbl (labels (append (cdr c.unsolved) c.solved)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.emptyness_conf_var_with_lbls_DecP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved)):
--   LET lbl1 = freshLabel(c) IN
--     LET lbl2 = freshLabel(union(vars(c), singleton(lbl1))) IN
--       empty?(intersection(vars(append(c`unsolved, c`s
theorem emptyness_conf_var_with_lbls_DecP : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}), fun (lbl1 : {i : ℝ // i ≥ 0}) => fun (lbl2 : {i : ℝ // i ≥ 0}) => empty_p (intersection (vars (append c.unsolved c.solved)) (add lbl1 (add lbl2 (labels (append (cdr c.unsolved) c.solved))))) (freshLabel (union (vars c) (singleton lbl1))) (freshLabel c) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.emptyness_conf_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved)):
--   empty?(intersection(vars(append(c`unsolved, c`solved)),
--                       (labels(append(cdr(c`unsolved), c`solved)))))
theorem emptyness_conf_var : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}), empty_p (intersection (vars (append c.unsolved c.solved)) ((labels (append (cdr c.unsolved) c.solved)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.niceness_preserv_conditions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?) | cons?(c`unsolved),
--         set_lbl: finite_set[variable]):
--   LET eq = car(c`unsolved) IN
--     (FORALL (lbl: variable):
--        member(lbl, set_lbl) IMPLIES NOT member(
theorem niceness_preserv_conditions : ∀ (c : {c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) // cons_p c.unsolved}) (set_lbl : {i : ℝ // i ≥ 0} → Bool), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => (∀ (lbl : variable), member lbl set_lbl → ¬ member lbl (vars c)) → (¬ member eq.label (union set_lbl (supset_dom c.substitution)) ∧
  empty_p (intersection set_lbl (union (supset_dom c.substitution) (vars (img c.substitution))))) (car c.unsolved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.match_DecF_conf?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: match_DecF?(c`unsolved)
axiom match_DecF_conf_p {pvs_τ70 : Type*} : pvs_τ70 → Prop

-- pvs2lean: antiunif.niceness_preserv_conditions_DecF
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_DecF_conf?)):
--   LET eq = car(c`unsolved) IN
--     LET lbl = freshLabel(c) IN
--       NOT member(eq`label,
--                  union(singleton(lbl), supset_dom(c`substitution)))
--        AND
--  
theorem niceness_preserv_conditions_DecF : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => fun (lbl : {i : ℝ // i ≥ 0}) => ¬ member eq.label (union (singleton lbl) (supset_dom c.substitution)) ∧ empty_p (intersection (singleton lbl) (union (supset_dom c.substitution) (vars (img c.substitution)))) (freshLabel c) (car c.unsolved) := by
  -- === TCCs for niceness_preserv_conditions_DecF ===
  -- TCC: niceness_preserv_conditions_DecF_TCC1
  -- Statement: FORALL (c: (match_DecF_conf?)): cons?[AUT](c`unsolved)
  have niceness_preserv_conditions_decf_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.nice_sub_DecF
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_DecF_conf?)):
--   LET eq = car(c`unsolved), lbl = freshLabel(c), sigma = c`substitution IN
--     nice?(cons((eq`label, app(sym(eq`lhs), variable(lbl))), sigma))
theorem nice_sub_DecF : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => fun (lbl : {i : ℝ // i ≥ 0}) => fun (sigma : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => nice_p (cons ((eq.label, app (sym eq.lhs) (variable lbl))) sigma) c.substitution (freshLabel c) (car c.unsolved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.uns_solv_vars_match_DecF_conf
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_DecF_conf?)):
--   LET eq = car(c`unsolved), lhs = eq`lhs, rhs = eq`rhs IN
--     vars(append(c`unsolved, c`solved)) =
--      union(vars(arg(lhs)),
--            union(vars(arg(rhs)), vars(appe
theorem uns_solv_vars_match_DecF_conf : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => fun (lhs : first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => fun (rhs : first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => vars (append c.unsolved c.solved) = union (vars (arg lhs)) (union (vars (arg rhs)) (vars (append (cdr c.unsolved) c.solved))) eq.rhs eq.lhs (car c.unsolved) := by
  -- === TCCs for uns_solv_vars_match_DecF_conf ===
  -- TCC: uns_solv_vars_match_DecF_conf_TCC1
  -- Statement: FORALL (c: (match_DecF_conf?), eq: AUT, lhs: Term, rhs: Term):
  --   eq = car(c`unsolved) AND lhs = eq`lhs AND rhs = eq`rhs IMPLIES
  --    app?[constant, variable, f_symbol](lhs)
  have uns_solv_vars_match_decf_conf_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: uns_solv_vars_match_DecF_conf_TCC2
  -- Statement: FORALL (c: (match_DecF_conf?), eq: AUT, lhs: Term, rhs: Term):
  --   eq = car(c`unsolved) AND lhs = eq`lhs AND rhs = eq`rhs IMPLIES
  --    app?[constant, variable, f_symbol](rhs)
  have uns_solv_vars_match_decf_conf_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.DecF
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET eq = car(c`unsolved), lhs = eq`lhs, rhs = eq`rhs, lbl = freshLabel(c)
--   IN
--   c
--     WITH [unsolved
--             := cons(makeAUT(arg(lhs), arg(rhs), lbl), cdr(c`unsolved)),
--           substitution
--    
axiom DecF {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ3 → pvs_τ4

-- pvs2lean: antiunif.match_DecP_conf?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: match_DecP?(c`unsolved)
axiom match_DecP_conf_p {pvs_τ72 : Type*} : pvs_τ72 → Prop

-- pvs2lean: antiunif.niceness_preserv_conditions_DecP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_DecP_conf?)):
--   LET eq = car(c`unsolved),
--       lbl1 = freshLabel(c),
--       lbl2 = freshLabel(union(vars(c), singleton(lbl1))),
--       set_lbl = {x: variable | x = lbl1 OR x = lbl2}
--  
theorem niceness_preserv_conditions_DecP : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => fun (lbl1 : {i : ℝ // i ≥ 0}) => fun (lbl2 : {i : ℝ // i ≥ 0}) => fun (set_lbl : {i : ℝ // i ≥ 0} → Bool) => ¬ member eq.label (union set_lbl (supset_dom c.substitution)) ∧ empty_p (intersection set_lbl (union (supset_dom c.substitution) (vars (img c.substitution)))) (fun (x : {i : ℝ // i ≥ 0}) => x = lbl1 ∨ x = lbl2) (freshLabel (union (vars c) (singleton lbl1))) (freshLabel c) (car c.unsolved) := by
  -- === TCCs for niceness_preserv_conditions_DecP ===
  -- TCC: niceness_preserv_conditions_DecP_TCC1
  -- Statement: FORALL (c: (match_DecP_conf?)): cons?[AUT](c`unsolved)
  have niceness_preserv_conditions_decp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.nice_sub_DecP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_DecP_conf?)):
--   LET eq = car(c`unsolved),
--       lbl1 = freshLabel(c),
--       lbl2 = freshLabel(union(vars(c), singleton(lbl1))),
--       sigma = c`substitution
--     IN nice?(cons((eq`lab
theorem nice_sub_DecP : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => fun (lbl1 : {i : ℝ // i ≥ 0}) => fun (lbl2 : {i : ℝ // i ≥ 0}) => fun (sigma : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : number_field // i ≥ 0} // s < List.length} → character))))) => nice_p (cons ((eq.label, pair (variable lbl1) (variable lbl2))) sigma) c.substitution (freshLabel (union (vars c) (singleton lbl1))) (freshLabel c) (car c.unsolved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.uns_solv_vars_match_DecP_conf
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_DecP_conf?)):
--   LET eq = car(c`unsolved), lhs = eq`lhs, rhs = eq`rhs IN
--     vars(append(c`unsolved, c`solved)) =
--      union(vars(term1(lhs)),
--            union(vars(term1(rhs)),
--      
theorem uns_solv_vars_match_DecP_conf : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => fun (lhs : first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => fun (rhs : first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => vars (append c.unsolved c.solved) = union (vars (term1 lhs)) (union (vars (term1 rhs)) (union (vars (term2 lhs)) (union (vars (term2 rhs)) (vars (append (cdr c.unsolved) c.solved))))) eq.rhs eq.lhs (car c.unsolved) := by
  -- === TCCs for uns_solv_vars_match_DecP_conf ===
  -- TCC: uns_solv_vars_match_DecP_conf_TCC1
  -- Statement: FORALL (c: (match_DecP_conf?), eq: AUT, lhs: Term, rhs: Term):
  --   eq = car(c`unsolved) AND lhs = eq`lhs AND rhs = eq`rhs IMPLIES
  --    pair?[constant, variable, f_symbol](lhs)
  have uns_solv_vars_match_decp_conf_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: uns_solv_vars_match_DecP_conf_TCC2
  -- Statement: FORALL (c: (match_DecP_conf?), eq: AUT, lhs: Term, rhs: Term):
  --   eq = car(c`unsolved) AND lhs = eq`lhs AND rhs = eq`rhs IMPLIES
  --    pair?[constant, variable, f_symbol](rhs)
  have uns_solv_vars_match_decp_conf_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.DecP
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET eq = car(c`unsolved),
--     lhs = eq`lhs,
--     rhs = eq`rhs,
--     lbl1 = freshLabel(c),
--     lbl2 = freshLabel(union(vars(c), singleton(lbl1)))
--   IN
--   c
--     WITH [unsolved
--             := cons(makeAUT(t
axiom DecP {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6

-- pvs2lean: antiunif.match_Synt_conf?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: match_Synt?(c`unsolved)
axiom match_Synt_conf_p {pvs_τ76 : Type*} : pvs_τ76 → Prop

-- pvs2lean: antiunif.nice_sub_Synt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_Synt_conf?)):
--   LET eq = car(c`unsolved) IN
--     nice?(cons((eq`label, eq`lhs), c`substitution))
theorem nice_sub_Synt : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) => nice_p (cons ((eq.label, eq.lhs)) c.substitution) (car c.unsolved) := by
  -- === TCCs for nice_sub_Synt ===
  -- TCC: nice_sub_Synt_TCC1
  -- Statement: FORALL (c: (match_Synt_conf?)): cons?[AUT](c`unsolved)
  have nice_sub_synt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.Synt
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET eq = car(c`unsolved), lhs = eq`lhs IN
--   c
--     WITH [unsolved := cdr(c`unsolved),
--           substitution := cons((eq`label, lhs), c`substitution)]
axiom Synt {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ9 → pvs_τ10

-- pvs2lean: antiunif.syntAUT_inter_vars_unsolv_labels_solv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_Synt_conf?)):
--   empty?(intersection(vars(car(c`unsolved)), labels(Synt(c)`solved)))
theorem syntAUT_inter_vars_unsolv_labels_solv : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), empty_p (intersection (vars (car c.unsolved)) (labels (Synt c.solved))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.labels_Synt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_Synt_conf?)): labels(Synt(c)`solved) = labels(c`solved)
theorem labels_Synt : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), labels (Synt c.solved) = labels c.solved := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.syntAUT_car_lhs_member_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_Synt_conf?)):
--   member(car(c`unsolved)`lhs, img(Synt(c)`substitution))
theorem syntAUT_car_lhs_member_img : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), member (car c.unsolved.lhs) (img (Synt c.substitution)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.domain_Synt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_Synt_conf?)):
--   LET lbl = car(c`unsolved)`label IN
--     dom(Synt(c)`substitution) = union(singleton(lbl), dom(c`substitution))
theorem domain_Synt : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (lbl : {i : ℝ // i ≥ 0}) => dom (Synt c.substitution) = union (singleton lbl) (dom c.substitution) (car c.unsolved.label) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.match_Sol_conf?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: match_Sol?(c`unsolved)
axiom match_Sol_conf_p {pvs_τ74 : Type*} : pvs_τ74 → Prop

-- pvs2lean: antiunif.nice_sub_Solved_AUT
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (match_Sol_conf?)):
--   AUT_repeated_in?(car(c`unsolved), c`solved) IMPLIES
--    LET sol_AUT = car(c`unsolved),
--        rep_AUT = rep_AUT_in(cons(sol_AUT, c`solved))
--      IN
--      nice?(cons((sol
theorem nice_sub_Solved_AUT : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), AUT_repeated_in_p (car c.unsolved) c.solved → (let sol_AUT := car c.unsolved; let rep_AUT := rep_AUT_in (cons sol_AUT c.solved); nice_p (cons ((sol_AUT.label, variable rep_AUT.label)) c.substitution)) := by
  -- === TCCs for nice_sub_Solved_AUT ===
  -- TCC: nice_sub_Solved_AUT_TCC1
  -- Statement: FORALL (c: (match_Sol_conf?)): cons?[AUT](c`unsolved)
  have nice_sub_solved_aut_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nice_sub_Solved_AUT_TCC2
  -- Statement: FORALL (c: (match_Sol_conf?)):
  --   AUT_repeated_in?(car(c`unsolved), c`solved) IMPLIES
  --    FORALL (sol_AUT: AUT):
  --      sol_AUT = car(c`unsolved) IMPLIES
  --       first_AUT_repeated?(cons[AUT](sol_AUT, c`...
  have nice_sub_solved_aut_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.Solve
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF AUT_repeated_in?(car(c`unsolved), c`solved)
--   THEN LET sol_AUT = car(c`unsolved),
--            rep_AUT = rep_AUT_in(cons(sol_AUT, c`solved))
--          IN
--          c
--            WITH [unsolved := cdr(c`
axiom Solve {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ7 → pvs_τ8

-- pvs2lean: antiunif.antiunify
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF cons?(c`unsolved)
--   THEN IF match_DecF_conf?(c) THEN antiunify(DecF(c))
--        ELSIF match_DecP_conf?(c) THEN antiunify(DecP(c))
--        ELSIF match_Synt_conf?(c) THEN antiunify(Synt(c))
--        ELSE
axiom antiunify {pvs_τ15 : Type*} : pvs_τ15 → Prop

-- pvs2lean: antiunif.generalizer?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(append(rho, sigma))(eq`label) = eq`lhs AND
--  subs(append(delta, sigma))(eq`label) = eq`rhs
axiom generalizer_p {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} : pvs_τ46 → pvs_τ47 → pvs_τ48 → pvs_τ49 → Prop

-- pvs2lean: antiunif.build_subs_left
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(auts) THEN null
-- ELSE LET aut = car(auts) IN
--        cons((aut`label, aut`lhs), build_subs_left(cdr(auts)))
-- ENDIF
axiom build_subs_left {pvs_τ25 : Type*} : pvs_τ25 → Prop

-- pvs2lean: antiunif.super_domain_subs_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?)):
--   supset_dom(build_subs_left(auts)) = labels(auts)
theorem super_domain_subs_left : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), supset_dom (build_subs_left auts) = labels auts := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.domain_subs_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?)):
--   subset?(dom(build_subs_left(auts)), labels(auts))
theorem domain_subs_left : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), subset_p (dom (build_subs_left auts)) (labels auts) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.nice_subs_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?)): nice?(build_subs_left(auts))
theorem nice_subs_left : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), nice_p (build_subs_left auts) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.build_subs_right
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(auts) THEN null
-- ELSE LET aut = car(auts) IN
--        cons((aut`label, aut`rhs), build_subs_right(cdr(auts)))
-- ENDIF
axiom build_subs_right {pvs_τ26 : Type*} : pvs_τ26 → Prop

-- pvs2lean: antiunif.super_domain_subs_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?)):
--   supset_dom(build_subs_right(auts)) = labels(auts)
theorem super_domain_subs_right : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), supset_dom (build_subs_right auts) = labels auts := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.domain_subs_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?)):
--   subset?(dom(build_subs_right(auts)), labels(auts))
theorem domain_subs_right : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), subset_p (dom (build_subs_right auts)) (labels auts) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.nice_subs_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?)): nice?(build_subs_right(auts))
theorem nice_subs_right : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), nice_p (build_subs_right auts) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.images_of_build_subs_left_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (auts: (valid_AUTs?), eq: AUT):
--   member(eq, auts) IMPLIES
--    LET rho_l = build_subs_left(auts), rho_r = build_subs_right(auts) IN
--      subs(rho_l)(eq`label) = eq`lhs AND subs(rho_r)(eq`label) 
theorem images_of_build_subs_left_right : ∀ (auts : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))), member eq auts → (let rho_l := build_subs_left auts; let rho_r := build_subs_right auts; subs rho_l(eq.label) = eq.lhs ∧ subs rho_r(eq.label) = eq.rhs) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.normal_configuration?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null?(c`unsolved)
axiom normal_configuration_p {pvs_τ82 : Type*} : pvs_τ82 → Prop

-- pvs2lean: antiunif.antiunify_normality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (normal_configuration?)): antiunify(c) = c
theorem antiunify_normality : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), antiunify c = c := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_derivability
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   NOT normal_configuration?(c) =>
--    match_DecF_conf?(c) OR
--     match_DecP_conf?(c) OR match_Synt_conf?(c) OR match_Sol_conf?(c)
theorem antiunify_derivability : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ¬ normal_configuration_p c → match_DecF_conf_p c ∨
 match_DecP_conf_p c ∨ match_Synt_conf_p c ∨ match_Sol_conf_p c := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.match_DecP_classification
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   NOT normal_configuration?(c) IMPLIES
--    (match_DecP_conf?(c) IMPLIES NOT match_DecF_conf?(c))
theorem match_DecP_classification : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ¬ normal_configuration_p c → (match_DecP_conf_p c → ¬ match_DecF_conf_p c) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.match_Synt_classification
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   NOT normal_configuration?(c) IMPLIES
--    (match_Synt_conf?(c) IMPLIES
--      NOT (match_DecF_conf?(c) OR match_DecP_conf?(c)))
theorem match_Synt_classification : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ¬ normal_configuration_p c → (match_Synt_conf_p c →
  ¬ (match_DecF_conf_p c ∨ match_DecP_conf_p c)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.SolvedAUT_classification
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   NOT normal_configuration?(c) IMPLIES
--    (match_Sol_conf?(c) IMPLIES
--      NOT (match_DecF_conf?(c) OR
--            match_DecP_conf?(c) OR match_Synt_conf?(c)))
theorem SolvedAUT_classification : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ¬ normal_configuration_p c → (match_Sol_conf_p c →
  ¬ (match_DecF_conf_p c ∨ match_DecP_conf_p c ∨ match_Synt_conf_p c)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_monotony_Sol_AUTs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?), eq: AUT | member(eq, c`solved)):
--   member(eq, antiunify(c)`solved)
theorem antiunify_monotony_Sol_AUTs : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (eq : {eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) // member eq c.solved}), member eq (antiunify c.solved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_domain_disjoint_sol_labels
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   disjoint?(dom(antiunify(c)`substitution), labels(c`solved))
theorem antiunify_domain_disjoint_sol_labels : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), disjoint_p (dom (antiunify c.substitution)) (labels c.solved) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_sub_decomposition
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   EXISTS (theta: (nice?)):
--     antiunify(c)`substitution = append(theta, c`substitution)
theorem antiunify_sub_decomposition : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), ∃ (theta : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))), antiunify c.substitution = append theta c.substitution := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_sub_preserves_terms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?), t: Term):
--   (member(t, img(c`substitution)) AND
--     empty?(intersection(vars(t), labels(c`unsolved))))
--    IMPLIES subs(antiunify(c)`substitution)(t) = subs(c`substitu
theorem antiunify_sub_preserves_terms : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (t : first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))), (member t (img c.substitution) ∧
  empty_p (intersection (vars t) (labels c.unsolved))) → subs (antiunify c.substitution) (t) = subs c.substitution(t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_dom_sub_preserves_vars_unsolved
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   intersection(dom(antiunify(c)`substitution), vars(c`unsolved)) = emptyset
theorem antiunify_dom_sub_preserves_vars_unsolved : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), intersection (dom (antiunify c.substitution)) (vars c.unsolved) = emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_lbls_preserves_vars_unsolved
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?), t: Term):
--   member(t, img(c`substitution)) AND
--    disjoint?(vars(t), labels(append(c`unsolved, c`solved)))
--    IMPLIES disjoint?(vars(t), labels(antiunify(c)`solved))
theorem antiunify_lbls_preserves_vars_unsolved : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (t : first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))), member t (img c.substitution) ∧
 disjoint_p (vars t) (labels (append c.unsolved c.solved)) → disjoint_p (vars t) (labels (antiunify c.solved)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_solved_labels_preserve_vars_unsolved
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?)):
--   intersection(labels(antiunify(c)`solved), vars(c`unsolved)) = emptyset
theorem antiunify_solved_labels_preserve_vars_unsolved : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), intersection (labels (antiunify c.solved)) (vars c.unsolved) = emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_solved_substitution
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: (validConfiguration?), eq: AUT):
--   member(eq, c`solved) IMPLIES
--    LET rho_l = build_subs_left(antiunify(c)`solved),
--        rho_r = build_subs_right(antiunify(c)`solved)
--      IN subs(rho_l)
theorem antiunify_solved_substitution : ∀ (c : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) (eq : {i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))), member eq c.solved → (let rho_l := build_subs_left (antiunify c.solved); let rho_r := build_subs_right (antiunify c.solved); subs rho_l(eq.label) = eq.lhs ∧ subs rho_r(eq.label) = eq.rhs) := by
  -- === TCCs for antiunify_solved_substitution ===
  -- TCC: antiunify_solved_substitution_TCC1
  -- Statement: FORALL (c: (validConfiguration?), eq: AUT):
  --   member(eq, c`solved) IMPLIES valid_AUTs?(antiunify(c)`solved)
  have antiunify_solved_substitution_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: antiunif.antiunify_is_sound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ci: (validConfiguration?)):
--   LET cf = antiunify(ci),
--       sigma = cf`substitution,
--       rho_l = build_subs_left(cf`solved),
--       rho_r = build_subs_right(cf`solved)
--     IN generalizer?(ci`
theorem antiunify_is_sound : ∀ (ci : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))), fun (cf : (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) × (List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))))) => fun (sigma : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (rho_l : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (rho_r : List ({i : ℝ // i ≥ 0} × (first_order_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : number // i ≥ 0} // s < List.length} → character))))) => generalizer_p ci.unsolved sigma rho_l rho_r (build_subs_right cf.solved) (build_subs_left cf.solved) cf.substitution (antiunify ci) := by
  -- === TCCs for antiunify_is_sound ===
  -- TCC: antiunify_is_sound_TCC1
  -- Statement: FORALL (ci: (validConfiguration?), cf: (validConfiguration?),
  --         sigma: (nice?)):
  --   cf = antiunify(ci) AND sigma = cf`substitution IMPLIES
  --    valid_AUTs?(cf`solved)
  have antiunify_is_sound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: antiunify_is_sound_TCC2
  -- Statement: FORALL (ci: (validConfiguration?), cf: (validConfiguration?),
  --         sigma: (nice?), rho_l: sub, rho_r: sub):
  --   cf = antiunify(ci) AND
  --    sigma = cf`substitution AND
  --     rho_l = build_subs_left(cf...
  have antiunify_is_sound_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: antiunify_is_sound_TCC3
  -- Statement: FORALL (ci: (validConfiguration?), cf: (validConfiguration?),
  --         sigma: (nice?), rho_l: sub, rho_r: sub):
  --   cf = antiunify(ci) AND
  --    sigma = cf`substitution AND
  --     rho_l = build_subs_left(cf...
  have antiunify_is_sound_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: antiunify_is_sound_TCC4
  -- Statement: FORALL (ci: (validConfiguration?), cf: (validConfiguration?),
  --         sigma: (nice?), rho_l: sub, rho_r: sub):
  --   cf = antiunify(ci) AND
  --    sigma = cf`substitution AND
  --     rho_l = build_subs_left(cf...
  have antiunify_is_sound_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.antiunif