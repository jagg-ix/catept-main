-- Auto-generated by pvs2lean/translator.py
-- PVS theory: robinsonunification
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: robinsonunification

Auto-generated stubs from PVSLib theory `robinsonunification`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.robinsonunification

noncomputable section

variable {pvs_τ1 : Type*} (Card : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Dom : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Ran : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} (Sub_p : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (VRan : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (Vars : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (add_first : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (app : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (arity : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (child : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (comp : pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (difference : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (ext : pvs_τ26 → pvs_τ27 → pvs_τ28)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (is_countably_infinite : pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (is_finite : pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (length : pvs_τ35 → pvs_τ36)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (member : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} (mgu : pvs_τ41 → pvs_τ42 → pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} (min : pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} (nonempty_p : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (positionsOF : pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (singleton : pvs_τ51 → pvs_τ52)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (subset_p : pvs_τ53 → pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} (subtermOF : pvs_τ55 → pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} (unifiable : pvs_τ57 → pvs_τ58 → Prop)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} (union : pvs_τ59 → pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (vars : pvs_τ61 → pvs_τ62)
variable {pvs_τ63 : Type*} (vars_p : pvs_τ63 → Prop)

-- pvs2lean: robinsonunification.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.var_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(V)
theorem var_nonempty : nonempty_p V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.symbol_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?({f: symbol | arity(f) = 1})
theorem symbol_nonempty : nonempty_p (fun (f : symbol) => arity f = 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.xx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def xx : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: robinsonunification.ff
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def ff : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: robinsonunification.fail
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: id WITH [xx := app(ff,  #(xx))]
noncomputable def fail : sorry := by
  sorry  -- pvs2lean: definition stub for id WITH [xx := app(ff,  #(xx))]

-- pvs2lean: robinsonunification.first_diff
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (CASES s
--    OF vars(s): empty_seq,
--       app(f, st):
--         IF length(st) = 0 THEN empty_seq
--         ELSE (CASES t
--                 OF vars(t): empty_seq,
--                    app(fp, stp):
--             
axiom first_diff {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ29 → pvs_τ30 → Prop

-- pvs2lean: robinsonunification.commutative_first_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t, p: position):
--   p = first_diff(s, t) IMPLIES p = first_diff(t, s)
theorem commutative_first_diff : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), p = first_diff s t → p = first_diff t s := by
  -- === TCCs for commutative_first_diff ===
  -- TCC: commutative_first_diff_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         p: position[variable, symbol, arity]):
  --   p = first_diff(s, t) IMPLIES t /= s
  have commutative_first_diff_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.position_s_first_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t, p: position):
--   p = first_diff(s, t) IMPLIES positionsOF(s)(p)
theorem position_s_first_diff : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), p = first_diff s t → positionsOF s(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.position_t_first_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t, p: position):
--   p = first_diff(s, t) IMPLIES positionsOF(t)(p)
theorem position_t_first_diff : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), p = first_diff s t → positionsOF t(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.first_diff_has_diff_argument
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t, p: position):
--   p = first_diff(s, t) IMPLIES subtermOF(s, p) /= subtermOF(t, p)
theorem first_diff_has_diff_argument : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), p = first_diff s t → subtermOF s p ≠ subtermOF t p := by
  -- === TCCs for first_diff_has_diff_argument ===
  -- TCC: first_diff_has_diff_argument_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         p: position[variable, symbol, arity]):
  --   p = first_diff(s, t) IMPLIES positionsOF[variable, symb...
  have first_diff_has_diff_argument_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_diff_has_diff_argument_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         p: position[variable, symbol, arity]):
  --   p = first_diff(s, t) IMPLIES positionsOF[variable, symb...
  have first_diff_has_diff_argument_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.first_diff_unifiable_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t, p: position):
--   p = first_diff(s, t) AND unifiable(s, t) IMPLIES
--    vars?(subtermOF(s, p)) OR vars?(subtermOF(t, p))
theorem first_diff_unifiable_vars : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), p = first_diff s t ∧ unifiable s t → vars_p (subtermOF s p) ∨ vars_p (subtermOF t p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.fd_equal_symbol
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET fd = first_diff(s, t) IN
--     (FORALL (p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--        child(fd, p) => f(subtermOF(s, p)) = f(subtermOF(t, p)))
theorem fd_equal_symbol : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (fd : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => ∀ (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), child fd p → f (subtermOF s p) = f (subtermOF t p) (first_diff s t) := by
  -- === TCCs for fd_equal_symbol ===
  -- TCC: fd_equal_symbol_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         fd: position[variable, symbol, arity]):
  --   fd = first_diff(s, t) IMPLIES
  --    FORALL (p: position[v...
  have fd_equal_symbol_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fd_equal_symbol_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         fd: position[variable, symbol, arity]):
  --   fd = first_diff(s, t) IMPLIES
  --    FORALL (p: position[v...
  have fd_equal_symbol_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.link_of_frst_diff
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET k: position = first_diff(s, t) IN
--   LET sp = subtermOF(s, k), tp = subtermOF(t, k) IN
--     IF vars?(sp)
--       THEN IF NOT member(sp, Vars(tp))
--              THEN (LAMBDA (x: (V)): IF x = sp THEN tp 
axiom link_of_frst_diff {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ37 → pvs_τ38 → Prop

-- pvs2lean: robinsonunification.dom_link_of_frst_diff_is
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     NOT sig = fail AND p = first_diff(s, t) IMPLIES
--      IF vars?(subtermOF(s, p))
--        THEN Dom(sig) =
--              restri
theorem dom_link_of_frst_diff_is : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail ∧ p = first_diff s t → if vars_p (subtermOF s p)
  then Dom sig =
        restrict[term[variable, symbol, arity], (V[variable, symbol, arity]),
             bool]
            (singleton (subtermOF s p))
else Dom sig =
      restrict[term[variable, symbol, arity], (V[variable, symbol, arity]),
           bool]
          (singleton (subtermOF t p)) (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.dom_ran_link_disjoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     NOT sig = fail IMPLIES
--      FORALL (x: (V) | member(x, Dom(sig)),
--              r: term[variable, symbol, arity] | member(
theorem dom_ran_link_disjoint : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → ∀ (x : (V) | member x (Dom sig)) (r : term[variable, symbol, arity] | member r (Ran sig)), ¬ member x (Vars r) (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.link_remove_x
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     (NOT sig = fail AND Dom(sig)(x)) IMPLIES
--      (NOT member(x, Vars(ext(sig)(s)))) AND
--       (NOT member(x, Vars(ext(sig)(t
theorem link_remove_x : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => (¬ sig = fail ∧ Dom sig(x)) → (¬ member x (Vars (ext sig(s)))) ∧ (¬ member x (Vars (ext sig(t)))) (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.link_of_frst_diff_s_is_subset_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     NOT sig = fail IMPLIES
--      subset?(Vars(ext(sig)(s)), union(Vars(s), Vars(t)))
theorem link_of_frst_diff_s_is_subset_union : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → subset_p (Vars (ext sig(s))) (union (Vars s) (Vars t)) (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.commutative_link_fd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET p = first_diff(s, t) IN
--     NOT vars?(subtermOF(s, p)) IMPLIES
--      link_of_frst_diff(s, t) = link_of_frst_diff(t, s)
theorem commutative_link_fd : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => ¬ vars_p (subtermOF s p) → link_of_frst_diff s t = link_of_frst_diff t s (first_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.link_of_frst_diff_t_is_subset_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     NOT sig = fail IMPLIES
--      subset?(Vars(ext(sig)(t)), union(Vars(s), Vars(t)))
theorem link_of_frst_diff_t_is_subset_union : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → subset_p (Vars (ext sig(t))) (union (Vars s) (Vars t)) (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.union_vars_ext_link
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     NOT sig = fail IMPLIES
--      union(Vars(ext(sig)(s)), Vars(ext(sig)(t))) =
--       difference(union(Vars(s), Vars(t)), Dom(s
theorem union_vars_ext_link : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → union (Vars (ext sig(s))) (Vars (ext sig(t))) =
 difference (union (Vars s) (Vars t)) (Dom sig) (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.termination_lemma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     NOT sig = fail IMPLIES
--      Card(union(Vars(ext(sig)(s)), Vars(ext(sig)(t)))) <
--       Card(union(Vars(s), Vars(t)))
theorem termination_lemma : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ¬ sig = fail → Card (union (Vars (ext sig(s))) (Vars (ext sig(t)))) <
 Card (union (Vars s) (Vars t)) (link_of_frst_diff s t) := by
  -- === TCCs for termination_lemma ===
  -- TCC: termination_lemma_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         sig: Sub[variable, symbol, arity]):
  --   sig = link_of_frst_diff(s, t) AND NOT sig = fail IMPLIES
  --  ...
  have termination_lemma_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: termination_lemma_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | s /= t,
  --         sig: Sub[variable, symbol, arity]):
  --   sig = link_of_frst_diff(s, t) AND NOT sig = fail IMPLIES
  --  ...
  have termination_lemma_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.unifiable_implies_not_fail
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     unifiable(s, t) IMPLIES NOT sig = fail
theorem unifiable_implies_not_fail : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => unifiable s t → ¬ sig = fail (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.preserving_generality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   member(rho, U(s, t)) IMPLIES
--    LET sig = link_of_frst_diff(s, t) IN EXISTS theta: rho = comp(theta, sig)
theorem preserving_generality : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), member rho (U s t) → (let sig := link_of_frst_diff s t; ∃ theta,  rho = comp theta sig) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.unifiable_preserves_unifiability
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig = link_of_frst_diff(s, t) IN
--     unifiable(s, t) IMPLIES unifiable(ext(sig)(s), ext(sig)(t))
theorem unifiable_preserves_unifiability : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => unifiable s t → unifiable (ext sig(s)) (ext sig(t)) (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.robinson_unification_algorithm
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF s = t THEN identity
-- ELSE LET sig = link_of_frst_diff(s, t) IN
--        IF sig = fail THEN fail
--        ELSE LET sigma =
--                   robinson_unification_algorithm(ext(sig)(s), ext(sig)(t))
--      
axiom robinson_unification_algorithm {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ49 → pvs_τ50 → Prop

-- pvs2lean: robinsonunification.var_ext_term_exists_var_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(x, Vars(ext(sigma)(r))) IMPLIES
--  EXISTS y: member(y, Vars(r)) AND member(x, Vars(sigma(y)))
theorem var_ext_term_exists_var_term : member x (Vars (ext sigma(r))) → ∃ y,  member y (Vars r) ∧ member x (Vars (sigma y)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: robinsonunification.rob_uni_alg_dom_subset_union_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifiable(s, t) IMPLIES
--  LET sigma = robinson_unification_algorithm(s, t) IN
--    subset?(Dom(sigma), union(Vars(s), Vars(t)))
theorem rob_uni_alg_dom_subset_union_vars : unifiable s t → (let sigma := robinson_unification_algorithm s t; subset_p (Dom sigma) (union (Vars s) (Vars t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.rob_uni_alg_vran_subset_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | s /= t):
--   LET sig1 = link_of_frst_diff(s, t) IN
--     LET sig2 = robinson_unification_algorithm(ext(sig1)(s), ext(sig1)(t))
--       IN
--       unifiable(s, t) IMPLIES
--        subs
theorem rob_uni_alg_vran_subset_union : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // s ≠ t}), fun (sig1 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => fun (sig2 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => unifiable s t → subset_p (VRan (comp sig2 sig1)) (union (VRan sig2) (difference (VRan sig1) (Dom sig2))) (robinson_unification_algorithm (ext sig1(s)) (ext sig1(t))) (link_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.rob_uni_alg_dom_ran_disjoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifiable(s, t) IMPLIES
--  LET sigma = robinson_unification_algorithm(s, t) IN
--    subset?(VRan(sigma), difference(union(Vars(s), Vars(t)), Dom(sigma)))
theorem rob_uni_alg_dom_ran_disjoint : unifiable s t → (let sigma := robinson_unification_algorithm s t; subset_p (VRan sigma) (difference (union (Vars s) (Vars t)) (Dom sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.robinson_unification_algorithm_fails_iff_non_unifiable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT unifiable(s, t) IFF robinson_unification_algorithm(s, t) = fail
theorem robinson_unification_algorithm_fails_iff_non_unifiable : ¬ unifiable s t ↔ robinson_unification_algorithm s t = fail := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.robinson_unification_algorithm_gives_unifier
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifiable(s, t) IFF member(robinson_unification_algorithm(s, t), U(s, t))
theorem robinson_unification_algorithm_gives_unifier : unifiable s t ↔ member (robinson_unification_algorithm s t) (U s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.robinson_unification_algorithm_gives_mg_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(rho, U(s, t)) IMPLIES robinson_unification_algorithm(s, t) <= rho
theorem robinson_unification_algorithm_gives_mg_subs : member rho (U s t) → robinson_unification_algorithm s t ≤ rho := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: robinsonunification.completeness_robinson_unification_algorithm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem completeness_robinson_unification_algorithm : True := by
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.robinsonunification