-- Auto-generated by pvs2lean/translator.py
-- PVS theory: critical_pairs
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: critical_pairs

Auto-generated stubs from PVSLib theory `critical_pairs`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.critical_pairs

noncomputable section

variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Pos_var : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (RSigma : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (RTC : pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (Vars : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (comp : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (comp_cont_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (disjoint_p : pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (ext : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (is_countably_infinite : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (joinable_p : pvs_τ26 → pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} (length : pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (lhs : pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} (locally_confluent_p : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (member : pvs_τ34 → pvs_τ35 → pvs_τ36)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (mgu : pvs_τ37 → pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (positionsOF : pvs_τ40 → pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} (positions_p : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} (reduction_p : pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (replaceTerm : pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} (replace_pos : pvs_τ48 → pvs_τ49 → pvs_τ50 → pvs_τ51)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (rhs : pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} (set2seq : pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} (subtermOF : pvs_τ55 → pvs_τ56 → pvs_τ57)
variable {pvs_τ58 : Type*} (vars_p : pvs_τ58 → Prop)

-- pvs2lean: critical_pairs.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: critical_pairs.CP?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (sigma, rho,
--         (e1: rewrite_rule[variable, symbol, arity] | member(e1, E)),
--         (e2p: rewrite_rule[variable, symbol, arity] | member(e2p, E)),
--         (p: positions?(lhs(e1)))):
--   LET
axiom CP_p {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop

-- pvs2lean: critical_pairs.CP_lemma_aux1a
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL E,
--        (p: position),
--        (e1: rewrite_rule[variable, symbol, arity] | member(e1, E)),
--        (e2: rewrite_rule[variable, symbol, arity] | member(e2, E)):
--   (positionsOF(lhs(e1))(p) &
--    
theorem CP_lemma_aux1a : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) (e1 : {e1 : term pvs_variable symbol NIL × term pvs_variable symbol NIL // member e1 E}) (e2 : {e2 : term pvs_variable symbol NIL × term pvs_variable symbol NIL // member e2 E}), (positionsOF (lhs e1) (p) &
  ¬ vars_p (subtermOF (lhs e1) p) &
   ext sg1(subtermOF (lhs e1) p) = ext sg2(lhs e2)) → ∃ alpha rho, 
  disjoint_p (Vars (lhs e1)) (Vars (ext rho(lhs e2))) &
   ext sg1(subtermOF (lhs e1) p) = ext (comp alpha rho) (lhs e2) := by
  -- === TCCs for CP_lemma_aux1a ===
  -- TCC: CP_lemma_aux1a_TCC1
  -- Statement: FORALL (sg1, sg2: Sub[variable, symbol, arity],
  --         E: set[rewrite_rule[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         e1: rewrite_rule[variable, symbol, arity...
  have cp_lemma_aux1a_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: critical_pairs.CP_lemma_aux1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL E,
--        (p: position),
--        (e1: rewrite_rule[variable, symbol, arity] | member(e1, E)),
--        (e2: rewrite_rule[variable, symbol, arity] | member(e2, E)):
--   (positionsOF(lhs(e1))(p) &
--    
theorem CP_lemma_aux1 : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) (e1 : {e1 : term pvs_variable symbol NIL × term pvs_variable symbol NIL // member e1 E}) (e2 : {e2 : term pvs_variable symbol NIL × term pvs_variable symbol NIL // member e2 E}), (positionsOF (lhs e1) (p) &
  ¬ vars_p (subtermOF (lhs e1) p) &
   ext sg1(subtermOF (lhs e1) p) = ext sg2(lhs e2)) → ∃ t1 t2 delta, 
  CP_p E(t1, t2) &
   ext delta(t1) = ext sg1(rhs e1) &
    ext delta(t2) = replaceTerm (ext sg1(lhs e1)) (ext sg2(rhs e2)) p := by
  -- === TCCs for CP_lemma_aux1 ===
  -- TCC: CP_lemma_aux1_TCC1
  -- Statement: FORALL (sg1, sg2: Sub[variable, symbol, arity],
  --         E: set[rewrite_rule[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         e1: rewrite_rule[variable, symbol, arity...
  have cp_lemma_aux1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: critical_pairs.CP_lemma_aux2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem CP_lemma_aux2 : True := by
  -- === TCCs for CP_lemma_aux2 ===
  -- TCC: CP_lemma_aux2_TCC1
  -- Statement: FORALL (t, x, sg1, sg2, Posv: positions[variable, symbol, arity]):
  --   Posv = Pos_var(t, x) IMPLIES
  --    is_finite[position[variable, symbol, arity]](Posv)
  have cp_lemma_aux2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CP_lemma_aux2_TCC2
  -- Statement: FORALL (R, t, x, sg1, sg2, Posv: positions[variable, symbol, arity],
  --         seqv: finite_sequence[position[variable, symbol, arity]]):
  --   Posv = Pos_var(t, x) AND
  --    seqv = set2seq(Posv) AND comp_c...
  have cp_lemma_aux2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: critical_pairs.CP_Theorem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL E:
--   LET RRE = reduction?(E) IN
--     locally_confluent?(RRE) <=>
--      (FORALL t1, t2: CP?(E)(t1, t2) => joinable?(RRE)(t1, t2))
theorem CP_Theorem : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool), fun (RRE : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) => locally_confluent_p RRE <=> (∀ t1 t2,  CP_p E(t1, t2) → joinable_p RRE(t1, t2)) (reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.critical_pairs