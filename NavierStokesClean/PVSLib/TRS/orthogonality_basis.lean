-- Auto-generated by pvs2lean/translator.py
-- PVS theory: orthogonality_basis
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: orthogonality_basis

Auto-generated stubs from PVSLib theory `orthogonality_basis`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.orthogonality_basis

noncomputable section

variable {pvs_τ11 : Type*} (PP_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (Pos_var : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (RC : pvs_τ15 → pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (RTC : pvs_τ18 → pvs_τ19)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (SPP : pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (SPP_p : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (SP_p : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (Vars : pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} (Vars_p : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (add_first : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} (app_p : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (choose : pvs_τ34 → pvs_τ35)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} (complement_pos_set : pvs_τ41 → pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45)
variable {pvs_τ46 : Type*} (confluent_p : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (delete : pvs_τ47 → pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (disjoint_p : pvs_τ50 → pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} (dominates_p : pvs_τ52 → pvs_τ53 → pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} (ext : pvs_τ55 → pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} (finseq_appl : pvs_τ57 → pvs_τ58)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} (is_countably_infinite : pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (is_finite : pvs_τ61 → pvs_τ62)
variable {pvs_τ63 : Type*} (length : pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (lhs : pvs_τ64 → pvs_τ65)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (member : pvs_τ67 → pvs_τ68 → Prop)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} (mgu : pvs_τ69 → pvs_τ70 → pvs_τ71 → Prop)
variable {pvs_τ72 : Type*} (nonempty_p : pvs_τ72 → Prop)
variable {pvs_τ83 : Type*} {pvs_τ84 : Type*} (positionsOF : pvs_τ83 → pvs_τ84 → Prop)
variable {pvs_τ85 : Type*} (positions_p : pvs_τ85 → Prop)
variable {pvs_τ86 : Type*} {pvs_τ87 : Type*} {pvs_τ88 : Type*} {pvs_τ89 : Type*} (reduction_fix_p : pvs_τ86 → pvs_τ87 → pvs_τ88 → pvs_τ89 → Prop)
variable {pvs_τ90 : Type*} (reduction_p : pvs_τ90 → Prop)
variable {pvs_τ91 : Type*} (reflexive_p : pvs_τ91 → Prop)
variable {pvs_τ92 : Type*} {pvs_τ93 : Type*} {pvs_τ94 : Type*} (replaceTerm : pvs_τ92 → pvs_τ93 → pvs_τ94 → Prop)
variable {pvs_τ98 : Type*} (rest : pvs_τ98 → Prop)
variable {pvs_τ99 : Type*} {pvs_τ100 : Type*} (rhs : pvs_τ99 → pvs_τ100)
variable {pvs_τ101 : Type*} {pvs_τ102 : Type*} (seq : pvs_τ101 → pvs_τ102)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} (seq2set : pvs_τ103 → pvs_τ104 → Prop)
variable {pvs_τ109 : Type*} (sub_commutative_p : pvs_τ109 → Prop)
variable {pvs_τ110 : Type*} {pvs_τ111 : Type*} (subset_p : pvs_τ110 → pvs_τ111 → Prop)
variable {pvs_τ112 : Type*} {pvs_τ113 : Type*} (subtermOF : pvs_τ112 → pvs_τ113 → Prop)
variable {pvs_τ116 : Type*} (vars_p : pvs_τ116 → Prop)

-- pvs2lean: orthogonality_basis.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.complement_pos
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(fsp) = 0 THEN empty_seq
-- ELSIF nonempty?(complement_pos_set(<=, o)(p, fsp`seq(0)))
--   THEN add_first(choose(complement_pos_set(<=, o)(p, fsp`seq(0))),
--                  complement_pos(p, rest(f
axiom complement_pos {pvs_τ39 : Type*} {pvs_τ40 : Type*} : pvs_τ39 → pvs_τ40 → Prop

-- pvs2lean: orthogonality_basis.comp_pos
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := fsp`length,
--    seq
--      := (LAMBDA (i: below[fsp`length]):
--            p o finseq_appl[position[variable, symbol, arity]](fsp)(i)) #)
axiom comp_pos {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ36 → pvs_τ37 → pvs_τ38

-- pvs2lean: orthogonality_basis.C
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subtermOF(s, p) = ext(sigma)(lhs(e)) AND
--  subtermOF(t, p) = ext(sigma)(rhs(e))
axiom C {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ2 → pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7

-- pvs2lean: orthogonality_basis.D
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(e, E) AND EXISTS sigma: C(e, sigma, s, t, fsp`seq(i))
noncomputable def D : sorry := by
  sorry  -- pvs2lean: definition stub for member(e, E) AND EXISTS sigma: C(e, sigma, s, t, fsp`seq(i))

-- pvs2lean: orthogonality_basis.G
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (e: rewrite_rule[variable, symbol, arity] | member(e, E)):
--   C(e, sigma, s, t, fsp`seq(i))
noncomputable def G : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS (e: rewrite_rule[variable, symbol, arity] | member(e, E)):   C(e, sigma, 

-- pvs2lean: orthogonality_basis.ntCP?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (sigma, rho,
--         (e1: rewrite_rule[variable, symbol, arity] | member(e1, E)),
--         (e2p: rewrite_rule[variable, symbol, arity] | member(e2p, E)),
--         (p: positions?(lhs(e1)))):
--   NOT
axiom ntCP_p {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} : pvs_τ73 → pvs_τ74 → pvs_τ75 → Prop

-- pvs2lean: orthogonality_basis.Ambiguous?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (t1, t2): ntCP?(E)(t1, t2)
axiom Ambiguous_p {pvs_τ1 : Type*} : pvs_τ1 → Prop

-- pvs2lean: orthogonality_basis.linear?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: (V) | member(x, Vars(t))):
--   Card[position[variable, symbol, arity]](Pos_var(t, x)) = 1
axiom linear_p {pvs_τ66 : Type*} : pvs_τ66 → Prop

-- pvs2lean: orthogonality_basis.Right_Linear?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e1: rewrite_rule[variable, symbol, arity] | member(e1, E)):
--   linear?(rhs(e1))
axiom Right_Linear_p {pvs_τ20 : Type*} : pvs_τ20 → Prop

-- pvs2lean: orthogonality_basis.Left_Linear?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e1: rewrite_rule[variable, symbol, arity] | member(e1, E)):
--   linear?(lhs(e1))
axiom Left_Linear_p {pvs_τ8 : Type*} : pvs_τ8 → Prop

-- pvs2lean: orthogonality_basis.Linear?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Left_Linear?(E) AND Right_Linear?(E)
axiom Linear_p {pvs_τ9 : Type*} : pvs_τ9 → Prop

-- pvs2lean: orthogonality_basis.Orthogonal?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Left_Linear?(E) AND NOT Ambiguous?(E)
axiom Orthogonal_p {pvs_τ10 : Type*} : pvs_τ10 → Prop

-- pvs2lean: orthogonality_basis.replace_par_pos
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(fsp) = 0 THEN s
-- ELSE replace_par_pos(replaceTerm(s,
--                                  finseq_appl[term[variable, symbol, arity]]
--                                      (fst)(0),
--                
axiom replace_par_pos {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} : pvs_τ95 → pvs_τ96 → pvs_τ97 → Prop

-- pvs2lean: orthogonality_basis.sigma_rhs
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF fss`length = 0 THEN empty_seq[term[variable, symbol, arity]]
-- ELSE (# length := fss`length,
--         seq
--           := (LAMBDA (i: below[fss`length]):
--                 ext(finseq_appl[Sub[variable, sym
axiom sigma_rhs {pvs_τ107 : Type*} {pvs_τ108 : Type*} : pvs_τ107 → pvs_τ108 → Prop

-- pvs2lean: orthogonality_basis.sigma_lhs
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF fss`length = 0 THEN empty_seq[term[variable, symbol, arity]]
-- ELSE (# length := fss`length,
--         seq
--           := (LAMBDA (i: below[fss`length]):
--                 ext(finseq_appl[Sub[variable, sym
axiom sigma_lhs {pvs_τ105 : Type*} {pvs_τ106 : Type*} : pvs_τ105 → pvs_τ106 → Prop

-- pvs2lean: orthogonality_basis.subtermsOF
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := fsp`length,
--    seq
--      := (LAMBDA (i: below[fsp`length]):
--            subtermOF(t,
--                      finseq_appl[position[variable, symbol, arity]]
--                          (fsp)(i))) 
axiom subtermsOF {pvs_τ114 : Type*} {pvs_τ115 : Type*} : pvs_τ114 → pvs_τ115 → Prop

-- pvs2lean: orthogonality_basis.parallel_reduction_fix?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS ((fse: finseq[rewrite_rule[variable, symbol, arity]]
--              | subset?(fse, E)),
--         fss):
--   fsp`length = fse`length AND
--    fsp`length = fss`length AND
--     subtermsOF(s, fsp) = sigma_l
axiom parallel_reduction_fix_p {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} {pvs_τ79 : Type*} : pvs_τ76 → pvs_τ77 → pvs_τ78 → pvs_τ79 → Prop

-- pvs2lean: orthogonality_basis.parallel_reduction?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (fsp: SPP(s)): parallel_reduction_fix?(E)(s, t, fsp)
axiom parallel_reduction_p {pvs_τ80 : Type*} {pvs_τ81 : Type*} {pvs_τ82 : Type*} : pvs_τ80 → pvs_τ81 → pvs_τ82 → Prop

-- pvs2lean: orthogonality_basis.complement_pos_character
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: seq2set(complement_pos(p, fsp))(p1) IMPLIES
--  EXISTS (i: below[fsp`length]): fsp`seq(i) = p o p1
theorem complement_pos_character : seq2set (complement_pos p fsp) (p1) → ∃ (i : Fin fsp.List.length), fsp.seq i = p ∘ p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.complement_pos_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) IMPLIES PP?(complement_pos(p, fsp))
theorem complement_pos_is_PP : PP_p fsp → PP_p (complement_pos p fsp) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.complement_pos_empty
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(add_first(p, fsp)) IMPLIES complement_pos(p, fsp)`length = 0
theorem complement_pos_empty : PP_p (add_first p fsp) → complement_pos p fsp.List.length = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.comp_pos_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: comp_pos(p, rest(fsp)) = rest(comp_pos(p, fsp))
theorem comp_pos_rest : comp_pos p (rest fsp) = rest (comp_pos p fsp) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.comp_pos_preservs_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: comp_pos(p, fsp)`length = fsp`length
theorem comp_pos_preservs_length : comp_pos p fsp.List.length = fsp.List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.comp_pos_character
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below[fsp`length]): comp_pos(p, fsp)`seq(i) = p o fsp`seq(i)
theorem comp_pos_character : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < fsp.List.length}), comp_pos p fsp.seq i = p ∘ fsp.seq i := by
  -- === TCCs for comp_pos_character ===
  -- TCC: comp_pos_character_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity], i: below[fsp`length]):
  --   i < comp_pos(p, fsp)`length
  have comp_pos_character_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.comp_pos_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) IMPLIES PP?(comp_pos(p, fsp))
theorem comp_pos_is_PP : PP_p fsp → PP_p (comp_pos p fsp) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.comp_pos_in_PP_preservs_PP
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) AND PP?(fsp1) AND fsp`length > 0 IMPLIES
--  PP?(comp_pos(fsp`seq(0), fsp1) o rest(fsp))
theorem comp_pos_in_PP_preservs_PP : PP_p fsp ∧ PP_p fsp1 ∧ fsp.List.length > 0 → PP_p (comp_pos (fsp.seq 0) fsp1 ∘ rest fsp) := by
  -- === TCCs for comp_pos_in_PP_preservs_PP ===
  -- TCC: comp_pos_in_PP_preservs_PP_TCC1
  -- Statement: FORALL (fsp, fsp1: finseq[position[variable, symbol, arity]]):
  --   fsp`length > 0 AND PP?(fsp1) AND PP?(fsp) IMPLIES 0 < fsp`length
  have comp_pos_in_pp_preservs_pp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.sigma_rhs_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fse`length = fss`length IMPLIES
--  rest(sigma_rhs(fss, fse)) = sigma_rhs(rest(fss), rest(fse))
theorem sigma_rhs_rest : fse.List.length = fss.List.length → rest (sigma_rhs fss fse) = sigma_rhs (rest fss) (rest fse) := by
  -- === TCCs for sigma_rhs_rest ===
  -- TCC: sigma_rhs_rest_TCC1
  -- Statement: FORALL (fse: finseq[rewrite_rule[variable, symbol, arity]],
  --         fss: finseq[Sub[variable, symbol, arity]]):
  --   fse`length = fss`length IMPLIES
  --    rest[rewrite_rule[variable, symbol, arity]](fse)...
  have sigma_rhs_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.sigma_rhs_o
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fse1`length = fss1`length AND fse2`length = fss2`length IMPLIES
--  sigma_rhs(fss1 o fss2, fse1 o fse2) =
--   sigma_rhs(fss1, fse1) o sigma_rhs(fss2, fse2)
theorem sigma_rhs_o : fse1.List.length = fss1.List.length ∧ fse2.List.length = fss2.List.length → sigma_rhs (fss1 ∘ fss2) (fse1 ∘ fse2) =
 sigma_rhs fss1 fse1 ∘ sigma_rhs fss2 fse2 := by
  -- === TCCs for sigma_rhs_o ===
  -- TCC: sigma_rhs_o_TCC1
  -- Statement: FORALL (fse1, fse2: finseq[rewrite_rule[variable, symbol, arity]],
  --         fss1, fss2: finseq[Sub[variable, symbol, arity]]):
  --   fse2`length = fss2`length AND fse1`length = fss1`length IMPLIES
  --    (o...
  have sigma_rhs_o_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.subtermsOF_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SP?(t)(fsp) IMPLIES rest(subtermsOF(t, fsp)) = subtermsOF(t, rest(fsp))
theorem subtermsOF_rest : SP_p t(fsp) → rest (subtermsOF t fsp) = subtermsOF t (rest fsp) := by
  -- === TCCs for subtermsOF_rest ===
  -- TCC: subtermsOF_rest_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   SP?(t)(fsp) IMPLIES
  --    SP?[variable, symbol, arity]
  --        (t)(rest[position[variable, symbol, a...
  have subtermsof_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.ntCP_lemma_aux1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL E,
--        (p: position),
--        (e1: rewrite_rule[variable, symbol, arity] | member(e1, E)),
--        (e2: rewrite_rule[variable, symbol, arity] | member(e2, E)):
--   ((positionsOF(lhs(e1))(p) AND
-- 
theorem ntCP_lemma_aux1 : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) (e1 : {e1 : term pvs_variable symbol NIL × term pvs_variable symbol NIL // member e1 E}) (e2 : {e2 : term pvs_variable symbol NIL × term pvs_variable symbol NIL // member e2 E}), ((positionsOF (lhs e1) (p) ∧
   ¬ vars_p (subtermOF (lhs e1) p) ∧
    ext sg1(subtermOF (lhs e1) p) = ext sg2(lhs e2))
  ∧ ¬ (e1 = e2 ∧ p = empty_seq)) → ∃ t1 t2 delta, 
  ntCP_p E(t1, t2) ∧
   ext delta(t1) = ext sg1(rhs e1) ∧
    ext delta(t2) = replaceTerm (ext sg1(lhs e1)) (ext sg2(rhs e2)) p := by
  -- === TCCs for ntCP_lemma_aux1 ===
  -- TCC: ntCP_lemma_aux1_TCC1
  -- Statement: FORALL (sg1, sg2: Sub[variable, symbol, arity],
  --         E: set[rewrite_rule[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         e1: rewrite_rule[variable, symbol, arity...
  have ntcp_lemma_aux1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.Orth_lemma_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem Orth_lemma_aux : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: orthogonality_basis.Linear_and_Non_ambiguous_implies_sub_comm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (E):
--   (Linear?(E) AND NOT Ambiguous?(E)) IMPLIES sub_commutative?(reduction?(E))
theorem Linear_and_Non_ambiguous_implies_sub_comm : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool), (Linear_p E ∧ ¬ Ambiguous_p E) → sub_commutative_p (reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.Linear_and_Non_ambiguous_implies_confluent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (E):
--   ((Linear?(E) AND NOT Ambiguous?(E)) IMPLIES confluent?(reduction?(E)))
theorem Linear_and_Non_ambiguous_implies_confluent : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool), (Linear_p E ∧ ¬ Ambiguous_p E) → confluent_p (reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.parallel_reduction_inclusion
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(reduction?(E), parallel_reduction?(E)) AND
--  subset?(parallel_reduction?(E), RTC(reduction?(E)))
theorem parallel_reduction_inclusion : subset_p (reduction_p E) (parallel_reduction_p E) ∧ subset_p (parallel_reduction_p E) (RTC (reduction_p E)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.parallel_reduction_RTC
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: RTC(reduction?(E)) = RTC(parallel_reduction?(E))
theorem parallel_reduction_RTC : RTC (reduction_p E) = RTC (parallel_reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_preservs_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(add_first(p, fsp)) AND fst`length = fsp`length IMPLIES
--  positionsOF(replace_par_pos(t, fsp, fst))(p)
theorem replace_par_pos_preservs_pos : SPP_p t(add_first p fsp) ∧ fst.List.length = fsp.List.length → positionsOF (replace_par_pos t fsp fst) (p) := by
  -- === TCCs for replace_par_pos_preservs_pos ===
  -- TCC: replace_par_pos_preservs_pos_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t: term[variable, symbol, arity]):
  -- ...
  have replace_par_pos_preservs_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_preservs_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp) AND fst`length = fsp`length IMPLIES
--  (FORALL (i: below[fsp`length]):
--     positionsOF(replace_par_pos(t, fsp, fst))
--                (finseq_appl[position[variable, symbol, arity]](fsp)(i)))
theorem replace_par_pos_preservs_PP : SPP_p t(fsp) ∧ fst.List.length = fsp.List.length → (∀ (i : Fin fsp.List.length), positionsOF (replace_par_pos t fsp fst)
              (finseq_appl fsp(i))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_preservs_PP_o_PP
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp o fsq) AND fst`length = fsp`length IMPLIES
--  (FORALL (i: below[fsq`length]):
--     positionsOF(replace_par_pos(t, fsp, fst))
--                (finseq_appl[position[variable, symbol, arity]](fsq
theorem replace_par_pos_preservs_PP_o_PP : SPP_p t(fsp ∘ fsq) ∧ fst.List.length = fsp.List.length → (∀ (i : Fin fsq.List.length), positionsOF (replace_par_pos t fsp fst)
              (finseq_appl fsq(i))) := by
  -- === TCCs for replace_par_pos_preservs_PP_o_PP ===
  -- TCC: replace_par_pos_preservs_PP_o_PP_TCC1
  -- Statement: FORALL (fsp, fsq: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND SPP?(t)(fsp...
  have replace_par_pos_preservs_pp_o_pp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_equivalence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(s)(fsp) AND fsp`length /= 0 AND fst`length = fsp`length IMPLIES
--  replace_par_pos(replaceTerm(s,
--                              finseq_appl[term[variable, symbol, arity]]
--                            
theorem replace_par_pos_equivalence : SPP_p s(fsp) ∧ fsp.List.length ≠ 0 ∧ fst.List.length = fsp.List.length → replace_par_pos (replaceTerm s (finseq_appl[term[variable, symbol, arity]]
                                (fst) (0)) (finseq_appl[position[variable, symbol, arity]]
                                (fsp) (0))) (rest fsp) (rest fst)
 =
 replaceTerm (replace_par_pos s (rest fsp) (rest fst)) (finseq_appl fst(0)) (finseq_appl fsp(0)) := by
  -- === TCCs for replace_par_pos_equivalence ===
  -- TCC: replace_par_pos_equivalence_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND fsp`length /= 0 ...
  have replace_par_pos_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_equivalence_TCC2
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND fsp`length /= 0 ...
  have replace_par_pos_equivalence_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_equivalence_TCC3
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND fsp`length /= 0 ...
  have replace_par_pos_equivalence_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_equivalence_TCC4
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND fsp`length /= 0 ...
  have replace_par_pos_equivalence_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_equivalence_TCC5
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND fsp`length /= 0 ...
  have replace_par_pos_equivalence_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_equivalence_TCC6
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND fsp`length /= 0 ...
  have replace_par_pos_equivalence_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_equivalence1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(s)(fsp) AND
--  fsp`length /= 0 AND fst`length = fsp`length AND n < fsp`length
--  IMPLIES
--  replace_par_pos(replaceTerm(s,
--                              finseq_appl[term[variable, symbol, arity]]
--       
theorem replace_par_pos_equivalence1 : SPP_p s(fsp) ∧
 fsp.List.length ≠ 0 ∧ fst.List.length = fsp.List.length ∧ n < fsp.List.length → replace_par_pos (replaceTerm s (finseq_appl[term[variable, symbol, arity]]
                                (fst) (n)) (finseq_appl[position[variable, symbol, arity]]
                                (fsp) (n))) (delete fsp n) (delete fst n)
 = replace_par_pos s fsp fst := by
  -- === TCCs for replace_par_pos_equivalence1 ===
  -- TCC: replace_par_pos_equivalence1_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]], n: nat,
  --         s: term[variable, symbol, arity]):
  --   n < fsp`length AND
  --    fst`length = f...
  have replace_par_pos_equivalence1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_equivalence1_TCC2
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]], n: nat,
  --         s: term[variable, symbol, arity]):
  --   n < fsp`length AND
  --    fst`length = f...
  have replace_par_pos_equivalence1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_equivalence1_TCC3
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]], n: nat,
  --         s: term[variable, symbol, arity]):
  --   n < fsp`length AND
  --    fst`length = f...
  have replace_par_pos_equivalence1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_equivalence1_TCC4
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]], n: nat,
  --         s: term[variable, symbol, arity]):
  --   n < fsp`length AND
  --    fst`length = f...
  have replace_par_pos_equivalence1_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_preservs_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(add_first(p, fsp)) AND fst`length = fsp`length IMPLIES
--  subtermOF(replace_par_pos(t, fsp, fst), p) = subtermOF(t, p)
theorem replace_par_pos_preservs_subterm : SPP_p t(add_first p fsp) ∧ fst.List.length = fsp.List.length → subtermOF (replace_par_pos t fsp fst) p = subtermOF t p := by
  -- === TCCs for replace_par_pos_preservs_subterm ===
  -- TCC: replace_par_pos_preservs_subterm_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t: term[variable, symbol, arity]):
  -- ...
  have replace_par_pos_preservs_subterm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_preservs_subterm_TCC2
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t: term[variable, symbol, arity]):
  -- ...
  have replace_par_pos_preservs_subterm_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp) AND fst`length = fsp`length IMPLIES
--  (FORALL (i: below[fsp`length]):
--     subtermOF(replace_par_pos(t, fsp, fst),
--               finseq_appl[position[variable, symbol, arity]](fsp)(i))
--     
theorem replace_par_pos_subterm : SPP_p t(fsp) ∧ fst.List.length = fsp.List.length → (∀ (i : Fin fsp.List.length), subtermOF (replace_par_pos t fsp fst) (finseq_appl fsp(i))
    = finseq_appl fst(i)) := by
  -- === TCCs for replace_par_pos_subterm ===
  -- TCC: replace_par_pos_subterm_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND SPP?(t)(fsp) IMP...
  have replace_par_pos_subterm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_subterm_TCC2
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst`length = fsp`length AND SPP?(t)(fsp) IMP...
  have replace_par_pos_subterm_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_comp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp1 o fsp2) AND
--  fst1`length = fsp1`length AND fst2`length = fsp2`length
--  IMPLIES
--  replace_par_pos(t, fsp1 o fsp2, fst1 o fst2) =
--   replace_par_pos(replace_par_pos(t, fsp1, fst1), fsp2, fst2)
theorem replace_par_pos_comp : SPP_p t(fsp1 ∘ fsp2) ∧
 fst1.List.length = fsp1.List.length ∧ fst2.List.length = fsp2.List.length → replace_par_pos t (fsp1 ∘ fsp2) (fst1 ∘ fst2) =
 replace_par_pos (replace_par_pos t fsp1 fst1) fsp2 fst2 := by
  -- === TCCs for replace_par_pos_comp ===
  -- TCC: replace_par_pos_comp_TCC1
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         fst1, fst2: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst2`length = fsp2`length AND
  -- ...
  have replace_par_pos_comp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_comp_TCC2
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         fst1, fst2: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst2`length = fsp2`length AND
  -- ...
  have replace_par_pos_comp_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_comp_TCC3
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         fst1, fst2: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst2`length = fsp2`length AND
  -- ...
  have replace_par_pos_comp_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_comp_commute
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp1 o fsp2) AND
--  fst1`length = fsp1`length AND fst2`length = fsp2`length
--  IMPLIES
--  replace_par_pos(t, fsp1 o fsp2, fst1 o fst2) =
--   replace_par_pos(t, fsp2 o fsp1, fst2 o fst1)
theorem replace_par_pos_comp_commute : SPP_p t(fsp1 ∘ fsp2) ∧
 fst1.List.length = fsp1.List.length ∧ fst2.List.length = fsp2.List.length → replace_par_pos t (fsp1 ∘ fsp2) (fst1 ∘ fst2) =
 replace_par_pos t (fsp2 ∘ fsp1) (fst2 ∘ fst1) := by
  -- === TCCs for replace_par_pos_comp_commute ===
  -- TCC: replace_par_pos_comp_commute_TCC1
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         fst1, fst2: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst2`length = fsp2`length AND
  -- ...
  have replace_par_pos_comp_commute_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_comp_commute_TCC2
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         fst1, fst2: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst2`length = fsp2`length AND
  -- ...
  have replace_par_pos_comp_commute_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_replace_par_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(q) AND SPP?(s)(fsp) AND fst`length = fsp`length IMPLIES
--  replaceTerm(t, replace_par_pos(s, fsp, fst), q) =
--   replace_par_pos(replaceTerm(t, s, q), comp_pos(q, fsp), fst)
theorem replace_replace_par_pos : positionsOF t(q) ∧ SPP_p s(fsp) ∧ fst.List.length = fsp.List.length → replaceTerm t (replace_par_pos s fsp fst) q =
 replace_par_pos (replaceTerm t s q) (comp_pos q fsp) fst := by
  -- === TCCs for replace_replace_par_pos ===
  -- TCC: replace_replace_par_pos_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         q: position[variable, symbol, arity],
  --         s, t: term[variable, symbol, arity]...
  have replace_replace_par_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_replace_par_pos_TCC2
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         q: position[variable, symbol, arity],
  --         s, t: term[variable, symbol, arity]...
  have replace_replace_par_pos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.comp_pos_in_SP_preservs_SP_in_replace
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp) AND SP?(s)(fsp1) AND fsp`length > 0 IMPLIES
--  SP?(replaceTerm(t, s, fsp`seq(0)))(comp_pos(fsp`seq(0), fsp1) o rest(fsp))
theorem comp_pos_in_SP_preservs_SP_in_replace : SPP_p t(fsp) ∧ SP_p s(fsp1) ∧ fsp.List.length > 0 → SP_p (replaceTerm t s (fsp.seq 0)) (comp_pos (fsp.seq 0) fsp1 ∘ rest fsp) := by
  -- === TCCs for comp_pos_in_SP_preservs_SP_in_replace ===
  -- TCC: comp_pos_in_SP_preservs_SP_in_replace_TCC1
  -- Statement: FORALL (fsp, fsp1: finseq[position[variable, symbol, arity]],
  --         s, t: term[variable, symbol, arity]):
  --   fsp`length > 0 AND SP?(s)(fsp1) AND SPP?(t)(fsp) IMPLIES 0 < fsp`length
  have comp_pos_in_sp_preservs_sp_in_replace_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: comp_pos_in_SP_preservs_SP_in_replace_TCC2
  -- Statement: FORALL (fsp, fsp1: finseq[position[variable, symbol, arity]],
  --         s, t: term[variable, symbol, arity]):
  --   fsp`length > 0 AND SP?(s)(fsp1) AND SPP?(t)(fsp) IMPLIES
  --    positionsOF[variable, symbo...
  have comp_pos_in_sp_preservs_sp_in_replace_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.comp_pos_in_SPP_preservs_SPP_in_replace
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp) AND SPP?(s)(fsp1) AND fsp`length > 0 IMPLIES
--  SPP?(replaceTerm(t, s, fsp`seq(0)))(comp_pos(fsp`seq(0), fsp1) o rest(fsp))
theorem comp_pos_in_SPP_preservs_SPP_in_replace : SPP_p t(fsp) ∧ SPP_p s(fsp1) ∧ fsp.List.length > 0 → SPP_p (replaceTerm t s (fsp.seq 0)) (comp_pos (fsp.seq 0) fsp1 ∘ rest fsp) := by
  -- === TCCs for comp_pos_in_SPP_preservs_SPP_in_replace ===
  -- TCC: comp_pos_in_SPP_preservs_SPP_in_replace_TCC1
  -- Statement: FORALL (fsp, fsp1: finseq[position[variable, symbol, arity]],
  --         s, t: term[variable, symbol, arity]):
  --   fsp`length > 0 AND SPP?(s)(fsp1) AND SPP?(t)(fsp) IMPLIES 0 < fsp`length
  have comp_pos_in_spp_preservs_spp_in_replace_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: comp_pos_in_SPP_preservs_SPP_in_replace_TCC2
  -- Statement: FORALL (fsp, fsp1: finseq[position[variable, symbol, arity]],
  --         s, t: term[variable, symbol, arity]):
  --   fsp`length > 0 AND SPP?(s)(fsp1) AND SPP?(t)(fsp) IMPLIES
  --    positionsOF[variable, symb...
  have comp_pos_in_spp_preservs_spp_in_replace_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.comp_pos_in_SPP_preservs_SPP_in_replace_par_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      SPP?(t)(fsp) AND (fsp`length = fst`length) AND (fsp`length /= 0)
--  AND SPP?(fst`seq(0))(fsp1) AND SPP?(replace_par_pos(t, fsp, fst))(fsp2)
--  AND PP?(add_first(fsp`seq(0), fsp2))
--  IMPLIES
--  SPP?(repl
theorem comp_pos_in_SPP_preservs_SPP_in_replace_par_pos : SPP_p t(fsp) ∧ (fsp.List.length = fst.List.length) ∧ (fsp.List.length ≠ 0)
 ∧ SPP_p (fst.seq 0) (fsp1) ∧ SPP_p (replace_par_pos t fsp fst) (fsp2)
 ∧ PP_p (add_first (fsp.seq 0) fsp2) → SPP_p (replace_par_pos t fsp fst) (comp_pos (fsp.seq 0) fsp1 ∘ fsp2) := by
  -- === TCCs for comp_pos_in_SPP_preservs_SPP_in_replace_par_pos ===
  -- TCC: comp_pos_in_SPP_preservs_SPP_in_replace_par_pos_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fsp`length /= 0 AND fsp`length = fst`length ...
  have comp_pos_in_spp_preservs_spp_in_replace_par_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: comp_pos_in_SPP_preservs_SPP_in_replace_par_pos_TCC2
  -- Statement: FORALL (fsp, fsp1: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   SPP?(fst`seq(0))(fsp1) AND
  --    fsp`leng...
  have comp_pos_in_spp_preservs_spp_in_replace_par_pos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.parallel_reduction_reflexive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: reflexive?(parallel_reduction?(E))
theorem parallel_reduction_reflexive : reflexive_p (parallel_reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.non_ambiguous_implies_same_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT Ambiguous?(E) AND
--  positionsOF(s)(p) AND
--   reduction_fix?(E)(s, t1, p) AND reduction_fix?(E)(s, t2, p)
--  IMPLIES t1 = t2
theorem non_ambiguous_implies_same_term : ¬ Ambiguous_p E ∧
 positionsOF s(p) ∧
  reduction_fix_p E(s, t1, p) ∧ reduction_fix_p E(s, t2, p) → t1 = t2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.parallel_reduction_context_aux
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp) AND
--  fst1`length = fsp`length AND
--   fst2`length = fsp`length AND
--    (FORALL (i: below[fsp`length]):
--       parallel_reduction?(E)
--                          (finseq_appl[term[variable, symbo
theorem parallel_reduction_context_aux : SPP_p t(fsp) ∧
 fst1.List.length = fsp.List.length ∧
  fst2.List.length = fsp.List.length ∧
   (∀ (i : Fin fsp.List.length), parallel_reduction_p E
                         (finseq_appl[term[variable, symbol, arity]]
                              (fst1) (i),
                          finseq_appl[term[variable, symbol, arity]]
                              (fst2) (i))) → ∃ (fsq : SPP (replace_par_pos t fsp fst1)), parallel_reduction_fix_p E
                         (replace_par_pos t fsp fst1,
                          replace_par_pos t fsp fst2, fsq)
   ∧
   (∀ (p : positions_p t), PP_p (add_first p fsp) → PP_p (add_first p fsq)) := by
  -- === TCCs for parallel_reduction_context_aux ===
  -- TCC: parallel_reduction_context_aux_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst1, fst2: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   fst2`length = fsp`length AND fst1`len...
  have parallel_reduction_context_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.parallel_reduction_context
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem parallel_reduction_context : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: orthogonality_basis.replace_par_pos_of_app_is_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: app?(t) AND SPP?(t)(fsp) AND fst`length = fsp`length IMPLIES
--  fsp =  #(empty_seq) OR app?(replace_par_pos(t, fsp, fst))
theorem replace_par_pos_of_app_is_app : app_p t ∧ SPP_p t(fsp) ∧ fst.List.length = fsp.List.length → fsp =  #(empty_seq) ∨ app_p (replace_par_pos t fsp fst) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_preservs_f
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: app?(t) AND SPP?(t)(fsp) AND fst`length = fsp`length IMPLIES
--  fsp =  #(empty_seq) OR f(t) = f(replace_par_pos(t, fsp, fst))
theorem replace_par_pos_preservs_f : app_p t ∧ SPP_p t(fsp) ∧ fst.List.length = fsp.List.length → fsp =  #(empty_seq) ∨ f t = f (replace_par_pos t fsp fst) := by
  -- === TCCs for replace_par_pos_preservs_f ===
  -- TCC: replace_par_pos_preservs_f_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         t: term[variable, symbol, arity]):
  --   NOT fsp =  #(empty_seq) AND
  --    fst`length = ...
  have replace_par_pos_preservs_f_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.parallel_reduction_variables_context
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (x: Vars?(t)):
--    parallel_reduction?(E)(ext(sigma)(x), ext(theta)(x)))
--  IMPLIES parallel_reduction?(E)(ext(sigma)(t), ext(theta)(t))
theorem parallel_reduction_variables_context : (∀ (x : Vars_p t), parallel_reduction_p E(ext sigma(x), ext theta(x))) → parallel_reduction_p E(ext sigma(t), ext theta(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.parallel_reduction_result
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Orthogonal?(E) AND
--  E(e) AND
--   SPP?(ext(sigma)(lhs(e)))(fsp) AND
--    parallel_reduction_fix?(E)(ext(sigma)(lhs(e)), t, fsp)
--  IMPLIES fsp =  #(empty_seq) OR EXISTS (theta): t = ext(theta)(lhs(e))
theorem parallel_reduction_result : Orthogonal_p E ∧
 E e ∧
  SPP_p (ext sigma(lhs e)) (fsp) ∧
   parallel_reduction_fix_p E(ext sigma(lhs e), t, fsp) → fsp =  #(empty_seq) ∨ ∃ (theta : _), t = ext theta(lhs e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_subterm2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      SPP?(s)(fsp) AND seq2set(fsp)(p1) AND (p <= p1) AND (p /= p1)
--  AND (fsp`length = fst`length)
--  IMPLIES
--  EXISTS fst1:
--    (complement_pos(p, fsp)`length = fst1`length AND
--      subtermOF(replace_par_
theorem replace_par_pos_subterm2 : SPP_p s(fsp) ∧ seq2set fsp(p1) ∧ (p ≤ p1) ∧ (p ≠ p1)
 ∧ (fsp.List.length = fst.List.length) → ∃ fst1, 
  (complement_pos p fsp.List.length = fst1.List.length ∧
    subtermOF (replace_par_pos s fsp fst) p =
     replace_par_pos (subtermOF s p) (complement_pos p fsp) fst1) := by
  -- === TCCs for replace_par_pos_subterm2 ===
  -- TCC: replace_par_pos_subterm2_TCC1
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         p, p1: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity...
  have replace_par_pos_subterm2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_subterm2_TCC2
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         p, p1: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity...
  have replace_par_pos_subterm2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_subterm2_TCC3
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         p, p1: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity...
  have replace_par_pos_subterm2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_subterm2_TCC4
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         p, p1: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity...
  have replace_par_pos_subterm2_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_subterm2_TCC5
  -- Statement: FORALL (fsp: finseq[position[variable, symbol, arity]],
  --         fst: finseq[term[variable, symbol, arity]],
  --         p, p1: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity...
  have replace_par_pos_subterm2_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.parallel_reduction_variables
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (NOT Ambiguous?(E)) AND E(e) AND SPP?(ext(sigma)(lhs(e)))(fsp)
--  AND parallel_reduction_fix?(E)(ext(sigma)(lhs(e)), ext(theta)(lhs(e)), fsp)
--  AND NOT fsp =  #(empty_seq)
--  IMPLIES
--  FORALL (x: Vars?
theorem parallel_reduction_variables : (¬ Ambiguous_p E) ∧ E e ∧ SPP_p (ext sigma(lhs e)) (fsp)
 ∧ parallel_reduction_fix_p E(ext sigma(lhs e), ext theta(lhs e), fsp)
 ∧ ¬ fsp =  #(empty_seq) → ∀ (x : Vars_p (lhs e)), parallel_reduction_p E(ext sigma(x), ext theta(x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.replace_par_pos_dominance
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp) AND
--  SPP?(t)(fsp1) AND
--   parallel_reduction_fix?(E)(t, s, fsp1) AND
--    dominates?(<=)(seq2set(fsp), seq2set(fsp1))
--  IMPLIES s = replace_par_pos(t, fsp, subtermsOF(s, fsp))
theorem replace_par_pos_dominance : SPP_p t(fsp) ∧
 SPP_p t(fsp1) ∧
  parallel_reduction_fix_p E(t, s, fsp1) ∧
   dominates_p ≤(seq2set fsp, seq2set fsp1) → s = replace_par_pos t fsp (subtermsOF s fsp) := by
  -- === TCCs for replace_par_pos_dominance ===
  -- TCC: replace_par_pos_dominance_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1: finseq[position[variable, symbol, arity]],
  --         s, t: term[variable, symbol, arity]):
  --   dominates?(<=)(seq2set(fsp), seq...
  have replace_par_pos_dominance_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_par_pos_dominance_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1: finseq[position[variable, symbol, arity]],
  --         s, t: term[variable, symbol, arity]):
  --   dominates?(<=)(seq2set(fsp), seq...
  have replace_par_pos_dominance_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_basis.Parallel_Moves_Lemma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Orthogonal?(E) AND
--  member(e, E) AND parallel_reduction?(E)(ext(sigma)(lhs(e)), t)
--  IMPLIES
--  EXISTS s:
--    parallel_reduction?(E)(ext(sigma)(rhs(e)), s) AND
--     parallel_reduction?(E)(t, s)
theorem Parallel_Moves_Lemma : Orthogonal_p E ∧
 member e E ∧ parallel_reduction_p E(ext sigma(lhs e), t) → ∃ s, 
  parallel_reduction_p E(ext sigma(rhs e), s) ∧
   parallel_reduction_p E(t, s) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.orthogonality_basis