-- Auto-generated by pvs2lean/translator.py
-- PVS theory: substitution
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: substitution

Auto-generated stubs from PVSLib theory `substitution`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.substitution

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (pvs_1 : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (pvs_2 : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (Card : pvs_τ5 → pvs_τ6)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (IUnion : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (Pos_var : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (SPP : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (SPP_p : pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (Vars : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} (add_first : pvs_τ42 → pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (app : pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} (app_p : pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} (arity : pvs_τ48 → pvs_τ49)
variable {pvs_τ54 : Type*} (comp_cont_p : pvs_τ54 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} (disjoint_p : pvs_τ57 → pvs_τ58 → Prop)
variable {pvs_τ59 : Type*} (empty_p : pvs_τ59 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} (finseq_appl : pvs_τ62 → pvs_τ63 → pvs_τ64)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} (intersection : pvs_τ71 → pvs_τ72 → pvs_τ73)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} (is_countably_infinite : pvs_τ74 → pvs_τ75)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} (is_finite : pvs_τ76 → pvs_τ77)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} (length : pvs_τ78 → pvs_τ79)
variable {pvs_τ80 : Type*} {pvs_τ81 : Type*} (member : pvs_τ80 → pvs_τ81 → Prop)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} {pvs_τ84 : Type*} (parallel : pvs_τ82 → pvs_τ83 → pvs_τ84)
variable {pvs_τ85 : Type*} {pvs_τ86 : Type*} (positionsOF : pvs_τ85 → pvs_τ86 → Prop)
variable {pvs_τ87 : Type*} (positions_p : pvs_τ87 → Prop)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} {pvs_τ90 : Type*} {pvs_τ91 : Type*} (replaceTerm : pvs_τ88 → pvs_τ89 → pvs_τ90 → pvs_τ91)
variable {pvs_τ92 : Type*} {pvs_τ93 : Type*} (rest : pvs_τ92 → pvs_τ93)
variable {pvs_τ97 : Type*} {pvs_τ98 : Type*} (rho : pvs_τ97 → pvs_τ98)
variable {pvs_τ99 : Type*} {pvs_τ100 : Type*} (seq : pvs_τ99 → pvs_τ100)
variable {pvs_τ101 : Type*} {pvs_τ102 : Type*} (sg1 : pvs_τ101 → pvs_τ102)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} (sg2 : pvs_τ103 → pvs_τ104)
variable {pvs_τ105 : Type*} {pvs_τ106 : Type*} (sig : pvs_τ105 → pvs_τ106)
variable {pvs_τ107 : Type*} {pvs_τ108 : Type*} (singleton : pvs_τ107 → pvs_τ108)
variable {pvs_τ109 : Type*} {pvs_τ110 : Type*} (subset_p : pvs_τ109 → pvs_τ110 → Prop)
variable {pvs_τ111 : Type*} {pvs_τ112 : Type*} (subtermOF : pvs_τ111 → pvs_τ112 → Prop)
variable {pvs_τ113 : Type*} {pvs_τ114 : Type*} (tau : pvs_τ113 → pvs_τ114)
variable {pvs_τ115 : Type*} {pvs_τ116 : Type*} {pvs_τ117 : Type*} (union : pvs_τ115 → pvs_τ116 → pvs_τ117)
variable {pvs_τ121 : Type*} (vars : pvs_τ121 → Prop)
variable {pvs_τ122 : Type*} (vars_p : pvs_τ122 → Prop)

-- pvs2lean: substitution.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.Dom
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {x: (V) | sig(x) /= x}
axiom Dom {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: substitution.Ran
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {y: term | EXISTS (x: (V)): member(x, Dom(sig)) & y = sig(x)}
axiom Ran {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: substitution.VRan
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IUnion(LAMBDA (x: (V) | Dom(sig)(x)): Vars(sig(x)))
axiom VRan {pvs_τ38 : Type*} {pvs_τ39 : Type*} : pvs_τ38 → pvs_τ39

-- pvs2lean: substitution.Sub?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(Dom(sig))
axiom Sub_p {pvs_τ37 : Type*} : pvs_τ37 → Prop

-- pvs2lean: substitution.Ren?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Ran(sigma), V) &
--  (bijective?[(Dom(sigma)), (Ran(sigma))])
--      (restrict
--           [(V[variable, symbol, arity]), (Dom(sigma)),
--            term[variable, symbol, arity]]
--           (sigma))
axiom Ren_p {pvs_τ23 : Type*} : pvs_τ23 → Prop

-- pvs2lean: substitution.range_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(Ran(rho))
theorem range_finite : is_finite (Ran rho) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.exists_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS z: NOT member(z, union(V1, V2))
theorem exists_var : ∃ (z : term pvs_variable symbol NIL), ¬(member z (union V1 V2)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.restriction
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF member(x, Vs) THEN sigma(x) ELSE x ENDIF
axiom restriction {pvs_τ94 : Type*} {pvs_τ95 : Type*} {pvs_τ96 : Type*} : pvs_τ94 → pvs_τ95 → pvs_τ96

-- pvs2lean: substitution.restriction_Dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Dom(restriction(sigma)(Vs)), Dom(sigma))
theorem restriction_Dom : subset_p (Dom (restriction sigma(Vs))) (Dom sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.restriction_Dom_fin
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(Dom(restriction(sigma)(Vs)))
theorem restriction_Dom_fin : is_finite (Dom (restriction sigma Vs)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.restriction_Subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Sub?(restriction(sigma)(Vs))
theorem restriction_Subs : Sub_p (restriction sigma Vs) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.dom_restriction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Dom(restriction(sigma)(Vs)), Vs)
theorem dom_restriction : subset_p (Dom (restriction sigma(Vs))) Vs := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.disjoint_D?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(Dom(sigma), Dom(tau))
axiom disjoint_D_p {pvs_τ55 : Type*} {pvs_τ56 : Type*} : pvs_τ55 → pvs_τ56 → Prop

-- pvs2lean: substitution.union_subs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF member(x, Dom(sgi`1)) THEN sgi`1(x)
-- ELSE IF member(x, Dom(sgi`2)) THEN sgi`2(x) ELSE x ENDIF
-- ENDIF
axiom union_subs {pvs_τ118 : Type*} {pvs_τ119 : Type*} {pvs_τ120 : Type*} : pvs_τ118 → pvs_τ119 → pvs_τ120

-- pvs2lean: substitution.Dom_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Dom(union_subs(sgi)) = union(Dom(sgi`1), Dom(sgi`2))
theorem Dom_union : Dom (union_subs sgi) = union (Dom sgi.1) (Dom sgi.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.union_is_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Sub?(union_subs(sgi))
theorem union_is_sub : Sub_p (union_subs sgi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.union_commute
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(Dom(sigma), Dom(tau)) =>
--  union_subs(sigma, tau) = union_subs(tau, sigma)
theorem union_commute : disjoint_p (Dom sigma) (Dom tau) → union_subs sigma tau = union_subs tau sigma := by
  -- === TCCs for union_commute ===
  -- TCC: union_commute_TCC1
  -- Statement: FORALL (sigma, tau: Sub):
  --   disjoint?(Dom(sigma), Dom(tau)) IMPLIES disjoint_D?(sigma, tau)
  have union_commute_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: union_commute_TCC2
  -- Statement: FORALL (sigma, tau: Sub):
  --   disjoint?(Dom(sigma), Dom(tau)) IMPLIES disjoint_D?(tau, sigma)
  have union_commute_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.ext
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES t
--   OF vars(t): sigma(t),
--      app(f, st):
--        IF length(st) = 0 THEN t
--        ELSE LET sst =
--                   (# length := st`length,
--                      seq
--                        := (LAM
axiom ext {pvs_τ60 : Type*} {pvs_τ61 : Type*} : pvs_τ60 → pvs_τ61 → Prop

-- pvs2lean: substitution.iden_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Sub?(identity)
theorem iden_subs : Sub_p identity := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.iden_rename
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Ren?(identity)
theorem iden_rename : Ren_p identity := by
  -- === TCCs for iden_rename ===
  -- TCC: iden_rename_TCC1
  -- Statement: Sub?(identity[(V)])
  have iden_rename_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.ext_iden
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ext(identity)(t) = t
theorem ext_iden : ext identity(t) = t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.restriction_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Vars(t), Vs) => ext(sigma)(t) = ext(restriction(sigma)(Vs))(t)
theorem restriction_term : subset_p (Vars t) Vs → ext sigma(t) = ext (restriction sigma(Vs)) (t) := by
  -- === TCCs for restriction_term ===
  -- TCC: restriction_term_TCC1
  -- Statement: FORALL (Vs: set[(V)], sigma: Sub, t: term[variable, symbol, arity]):
  --   subset?(Vars(t), Vs) IMPLIES Sub?(restriction(sigma)(Vs))
  have restriction_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.restriction_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(Dom(sg1), Dom(sg2)) & disjoint?(Vars(t), Dom(sg2)) =>
--  ext(union_subs(sg1, sg2))(t) = ext(sg1)(t)
theorem restriction_union : disjoint_p (Dom sg1) (Dom sg2) & disjoint_p (Vars t) (Dom sg2) → ext (union_subs sg1 sg2) (t) = ext sg1(t) := by
  -- === TCCs for restriction_union ===
  -- TCC: restriction_union_TCC1
  -- Statement: FORALL (sg1, sg2: Sub, t: term[variable, symbol, arity]):
  --   disjoint?(Vars(t), Dom(sg2)) AND disjoint?(Dom(sg1), Dom(sg2)) IMPLIES
  --    Sub?(union_subs(sg1, sg2))
  have restriction_union_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.ext_preserv_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) => positionsOF(ext(sigma)(s))(p)
theorem ext_preserv_pos : positionsOF s(p) → positionsOF (ext sigma(s)) (p) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.subterm_ext_commute
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) =>
--  subtermOF(ext(sigma)(s), p) = ext(sigma)(subtermOF(s, p))
theorem subterm_ext_commute : positionsOF s(p) → subtermOF (ext sigma(s)) p = ext sigma(subtermOF s p) := by
  -- === TCCs for subterm_ext_commute ===
  -- TCC: subterm_ext_commute_TCC1
  -- Statement: FORALL (p: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], sigma: Sub):
  --   positionsOF(s)(p) IMPLIES
  --    positionsOF[variable, symbol, arity](ext(sigma)(s))(p)
  have subterm_ext_commute_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.positions_of_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(ext(sigma)(t)) =
--  union({p | positionsOF(t)(p) & (NOT vars?(subtermOF(t, p)))},
--        {q |
--           EXISTS p1, p2:
--             q = p1 o p2 AND
--              positionsOF(t)(p1) AND
--        
theorem positions_of_ext : positionsOF (ext sigma(t)) = union ({p | positionsOF t(p) & (¬ vars_p (subtermOF t p))}) ({q |
         ∃ p1 p2, 
           q = p1 ∘ p2 ∧
            positionsOF t(p1) ∧
             vars_p (subtermOF t p1) ∧
              positionsOF (ext sigma(subtermOF t p1)) (p2)}) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.comp
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ext(sigma)(tau(x))
axiom comp {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} : pvs_τ50 → pvs_τ51 → pvs_τ52 → pvs_τ53

-- pvs2lean: substitution.dom_o
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Dom(comp(sigma, tau)), union(Dom(sigma), Dom(tau)))
theorem dom_o : subset_p (Dom (comp sigma tau)) (union (Dom sigma) (Dom tau)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.dom_o_fin
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(Dom(comp(sigma, tau)))
theorem dom_o_fin : is_finite (Dom (comp sigma tau)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.subs_o
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Sub?(comp(sigma, tau))
theorem subs_o : Sub_p (comp sigma tau) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.ext_o
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ext(comp(sigma, tau)) = ext(sigma) o ext(tau)
theorem ext_o : ext (comp sigma tau) = ext sigma ∘ ext tau := by
  -- === TCCs for ext_o ===
  -- TCC: ext_o_TCC1
  -- Statement: FORALL (sigma, tau: Sub): Sub?(comp(sigma, tau))
  have ext_o_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.subs_o_identity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: comp(sigma, identity) = sigma
theorem subs_o_identity : comp sigma identity = sigma := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.ext_subs_o_identity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ext(comp(sigma, identity)) = ext(sigma)
theorem ext_subs_o_identity : ext (comp sigma identity) = ext sigma := by
  -- === TCCs for ext_subs_o_identity ===
  -- TCC: ext_subs_o_identity_TCC1
  -- Statement: FORALL (sigma: Sub): Sub?(comp(sigma, identity[(V)]))
  have ext_subs_o_identity_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.o_ass
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: comp(comp(sigma, delta), tau) = comp(sigma, comp(delta, tau))
theorem o_ass : comp (comp sigma delta) tau = comp sigma (comp delta tau) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.inverse_renaming
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS rho1: FORALL x: Dom(rho)(x) => comp(rho1, rho)(x) = x
theorem inverse_renaming : ∃ (rho1 : term pvs_variable symbol NIL → term pvs_variable symbol NIL), ∀ (x : term pvs_variable symbol NIL), Dom rho(x) → comp rho1 rho(x) = x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.inverse_rename_identity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Vars(t), Dom(rho)) &
--  (FORALL x: Dom(rho)(x) => comp(rho1, rho)(x) = x)
--  => ext(comp(rho1, rho))(t) = t
theorem inverse_rename_identity : subset_p (Vars t) (Dom rho) &
 (∀ x,  Dom rho(x) → comp rho1 rho(x) = x) → ext (comp rho1 rho) (t) = t := by
  -- === TCCs for inverse_rename_identity ===
  -- TCC: inverse_rename_identity_TCC1
  -- Statement: FORALL (rho, rho1: Ren, t: term[variable, symbol, arity]):
  --   (FORALL (x: (V)): Dom(rho)(x) => comp(rho1, rho)(x) = x) AND
  --    subset?(Vars(t), Dom(rho))
  --    IMPLIES Sub?(comp(rho1, rho))
  have inverse_rename_identity_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.idempotent_sub?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: comp(sigma, sigma) = sigma
axiom idempotent_sub_p {pvs_τ70 : Type*} : pvs_τ70 → Prop

-- pvs2lean: substitution.idemp_sub_term_empty_inter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t: term, sigma: Sub):
--   ext(sigma)(t) = t IFF empty?(intersection(Dom(sigma), Vars(t)))
theorem idemp_sub_term_empty_inter : ∀ (t : term pvs_variable symbol NIL) (sigma : term pvs_variable symbol NIL → term pvs_variable symbol NIL), ext sigma(t) = t ↔ empty_p (intersection (Dom sigma) (Vars t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.idemp_sub_iff_empty_intersection
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent_sub?(sigma) IFF empty?(intersection(Dom(sigma), VRan(sigma)))
theorem idemp_sub_iff_empty_intersection : idempotent_sub_p sigma ↔ empty_p (intersection (Dom sigma) (VRan sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.DSigma
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF y = x THEN s ELSE y ENDIF
axiom DSigma {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9

-- pvs2lean: substitution.Dom_DSigma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Dom(DSigma(s, x)),
--         restrict
--             [term[variable, symbol, arity], (V[variable, symbol, arity]),
--              bool]
--             (union(singleton(s), singleton(x))))
theorem Dom_DSigma : subset_p (Dom (DSigma s x)) (restrict[term[variable, symbol, arity], (V[variable, symbol, arity]), bool]
     (union (singleton s) (singleton x))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.Dom_DSigma_is_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(Dom(DSigma(s, x)))
theorem Dom_DSigma_is_finite : is_finite (Dom (DSigma s x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.DSigma_is_Sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Sub?(DSigma(s, x))
theorem DSigma_is_Sub : Sub_p (DSigma s x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.DSigma_do_nothing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, s, x: empty?(Pos_var(s, x)) IMPLIES ext(DSigma(t, x))(s) = s
theorem DSigma_do_nothing : ∀ (t : term pvs_variable symbol NIL) (s : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL), empty_p (Pos_var s x) → ext (DSigma t x) (s) = s := by
  -- === TCCs for DSigma_do_nothing ===
  -- TCC: DSigma_do_nothing_TCC1
  -- Statement: FORALL (t, s, x): empty?(Pos_var(s, x)) IMPLIES Sub?(DSigma(t, x))
  have dsigma_do_nothing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.RSigma
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (y: (V)): IF y /= x THEN sg1(y) = sg2(y) ELSE R(sg1(x), sg2(x)) ENDIF
axiom RSigma {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: substitution.SigmaP
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF y /= x THEN sigma(y) ELSE replaceTerm(sigma(x), t, p) ENDIF
axiom SigmaP {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36

-- pvs2lean: substitution.gamma
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF y /= z THEN sg1(z) ELSE sg2(x) ENDIF
axiom gamma {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} : pvs_τ65 → pvs_τ66 → pvs_τ67 → pvs_τ68 → pvs_τ69

-- pvs2lean: substitution.Rhop
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x /= z THEN rho(z) ELSE y ENDIF
axiom Rhop {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27

-- pvs2lean: substitution.SigmaP_Dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(sigma(x))(p) =>
--  subset?(Dom(SigmaP(sigma, t, x, p)), union(Dom(sigma), singleton(x)))
theorem SigmaP_Dom : positionsOF (sigma x) (p) → subset_p (Dom (SigmaP sigma t x p)) (union (Dom sigma) (singleton x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.SigmaP_Dom_fin
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(sigma(x))(p) => is_finite(Dom(SigmaP(sigma, t, x, p)))
theorem SigmaP_Dom_fin : positionsOF (sigma x) (p) → is_finite (Dom (SigmaP sigma t x p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.SigmaP_Subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(sigma(x))(p) => Sub?(SigmaP(sigma, t, x, p))
theorem SigmaP_Subs : positionsOF (sigma x) (p) → Sub_p (SigmaP sigma t x p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.SigmaP_is_RSigma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (positionsOF(sigma(x))(p) & comp_cont?(R) & R(subtermOF(sigma(x), p), t))
--  => RSigma(R, sigma, SigmaP(sigma, t, x, p), x)
theorem SigmaP_is_RSigma : (positionsOF (sigma x) (p) & comp_cont_p R & R (subtermOF (sigma x) p) t) → RSigma R sigma (SigmaP sigma t x p) x := by
  -- === TCCs for SigmaP_is_RSigma ===
  -- TCC: SigmaP_is_RSigma_TCC1
  -- Statement: FORALL (R:
  --           pred[[term[variable, symbol, arity],
  --                 term[variable, symbol, arity]]],
  --         p: position[variable, symbol, arity], sigma: Sub,
  --         t: term[variable, symbol...
  have sigmap_is_rsigma_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.SigmaP_equivalence
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (NOT member(x, Vars(s)) AND positionsOF(ext(sigma)(x))(p2)) IMPLIES
--  ext(SigmaP(sigma, t, x, p2))(s) = ext(sigma)(s)
theorem SigmaP_equivalence : (¬ member x (Vars s) ∧ positionsOF (ext sigma(x)) (p2)) → ext (SigmaP sigma t x p2) (s) = ext sigma(s) := by
  -- === TCCs for SigmaP_equivalence ===
  -- TCC: SigmaP_equivalence_TCC1
  -- Statement: FORALL (p2: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], sigma: Sub, x: (V)):
  --   positionsOF(ext(sigma)(x))(p2) AND NOT member(x, Vars(s)) IMPLIES
  --    positionsOF[vari...
  have sigmap_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: SigmaP_equivalence_TCC2
  -- Statement: FORALL (p2: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], sigma: Sub,
  --         t: term[variable, symbol, arity], x: (V)):
  --   positionsOF(ext(sigma)(x))(p2) AND NOT memb...
  have sigmap_equivalence_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.SigmaP_vs_replaceTerm_linearR
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      vars?(x) AND positionsOF(s)(p1) AND positionsOF(ext(sigma)(x))(p2)
--  AND (x = subtermOF(s, p1)) AND (Card(Pos_var(s, x)) = 1)
--  IMPLIES
--  ext(SigmaP(sigma, t, x, p2))(s) = replaceTerm(ext(sigma)(s),
theorem SigmaP_vs_replaceTerm_linearR : vars_p x ∧ positionsOF s(p1) ∧ positionsOF (ext sigma(x)) (p2)
 ∧ (x = subtermOF s p1) ∧ (Card (Pos_var s x) = 1) → ext (SigmaP sigma t x p2) (s) = replaceTerm (ext sigma(s)) t (p1 ∘ p2) := by
  -- === TCCs for SigmaP_vs_replaceTerm_linearR ===
  -- TCC: SigmaP_vs_replaceTerm_linearR_TCC1
  -- Statement: FORALL (p1, p2: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], sigma: Sub, x: (V)):
  --   x = subtermOF(s, p1) AND
  --    positionsOF(ext(sigma)(x))(p2) AND positionsOF(s)(p1)...
  have sigmap_vs_replaceterm_linearr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: SigmaP_vs_replaceTerm_linearR_TCC2
  -- Statement: FORALL (p1, p2: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], sigma: Sub, x: (V)):
  --        (Card(Pos_var(s, x)) = 1) AND (x = subtermOF(s, p1))
  --    AND positionsOF(ext(...
  have sigmap_vs_replaceterm_linearr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: SigmaP_vs_replaceTerm_linearR_TCC3
  -- Statement: FORALL (p1, p2: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], sigma: Sub,
  --         t: term[variable, symbol, arity], x: (V)):
  --        (Card(Pos_var(s, x)) = 1) AND (x =...
  have sigmap_vs_replaceterm_linearr_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: SigmaP_vs_replaceTerm_linearR_TCC4
  -- Statement: FORALL (p1, p2: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], sigma: Sub, x: (V)):
  --        (Card(Pos_var(s, x)) = 1) AND (x = subtermOF(s, p1))
  --    AND positionsOF(ext(...
  have sigmap_vs_replaceterm_linearr_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.gamma_Dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sg1Usg2 = union(Dom(sg1), Dom(sg2)) IN
--   subset?(Dom(gamma(sg1, sg2, x, y)), union(sg1Usg2, singleton(y)))
theorem gamma_Dom : fun (sg1Usg2 : term pvs_variable symbol NIL → Bool) => subset_p (Dom (gamma sg1 sg2 x y)) (union sg1Usg2 (singleton y)) (union (Dom sg1) (Dom sg2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.gamma_Dom_fin
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(Dom(gamma(sg1, sg2, x, y)))
theorem gamma_Dom_fin : is_finite (Dom (gamma sg1 sg2 x y)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.gamma_Subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Sub?(gamma(sg1, sg2, x, y))
theorem gamma_Subs : Sub_p (gamma sg1 sg2 x y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.gamma_is_RSigma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: RSigma(R, sg1, sg2, z) & z /= y =>
--  RSigma(R, gamma(sg1, sg2, x, y), gamma(sg2, sg2, x, y), z)
theorem gamma_is_RSigma : RSigma R sg1 sg2 z & z ≠ y → RSigma R (gamma sg1 sg2 x y) (gamma sg2 sg2 x y) z := by
  -- === TCCs for gamma_is_RSigma ===
  -- TCC: gamma_is_RSigma_TCC1
  -- Statement: FORALL (R:
  --           pred[[term[variable, symbol, arity],
  --                 term[variable, symbol, arity]]],
  --         sg1, sg2: Sub, x, y, z: (V)):
  --   z /= y AND RSigma(R, sg1, sg2, z) IMPLIES Sub?(gam...
  have gamma_is_rsigma_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: gamma_is_RSigma_TCC2
  -- Statement: FORALL (R:
  --           pred[[term[variable, symbol, arity],
  --                 term[variable, symbol, arity]]],
  --         sg1, sg2: Sub, x, y, z: (V)):
  --   z /= y AND RSigma(R, sg1, sg2, z) IMPLIES Sub?(gam...
  have gamma_is_rsigma_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.gamma_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(y, Vars(t)) =>
--  ext(gamma(sg1, sg2, x, y))(t) = ext(sg1)(t) &
--   ext(gamma(sg2, sg2, x, y))(t) = ext(sg2)(t)
theorem gamma_term : ¬ member y (Vars t) → ext (gamma sg1 sg2 x y) (t) = ext sg1(t) &
 ext (gamma sg2 sg2 x y) (t) = ext sg2(t) := by
  -- === TCCs for gamma_term ===
  -- TCC: gamma_term_TCC1
  -- Statement: FORALL (sg1, sg2: Sub, t: term[variable, symbol, arity], x, y: (V)):
  --   NOT member(y, Vars(t)) IMPLIES Sub?(gamma(sg1, sg2, x, y))
  have gamma_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: gamma_term_TCC2
  -- Statement: FORALL (sg1, sg2: Sub, t: term[variable, symbol, arity], x, y: (V)):
  --   ext(gamma(sg1, sg2, x, y))(t) = ext(sg1)(t) AND NOT member(y, Vars(t))
  --    IMPLIES Sub?(gamma(sg2, sg2, x, y))
  have gamma_term_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.Rhop_Dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= y & NOT Dom(rho)(x) =>
--  Dom(Rhop(rho, x, y)) = union(Dom(rho), singleton(x))
theorem Rhop_Dom : x ≠ y & ¬ Dom rho(x) → Dom (Rhop rho x y) = union (Dom rho) (singleton x) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.Rhop_Ran
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= y & NOT Dom(rho)(x) & NOT Ran(rho)(y) =>
--  Ran(Rhop(rho, x, y)) = union(Ran(rho), singleton(y))
theorem Rhop_Ran : x ≠ y & ¬ Dom rho(x) & ¬ Ran rho(y) → Ran (Rhop rho x y) = union (Ran rho) (singleton y) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.Rhop_Dom_fin
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= y & NOT Dom(rho)(x) => is_finite(Dom(Rhop(rho, x, y)))
theorem Rhop_Dom_fin : x ≠ y & ¬ Dom rho(x) → is_finite (Dom (Rhop rho x y)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.Rhop_Subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= y & NOT Dom(rho)(x) => Sub?(Rhop(rho, x, y))
theorem Rhop_Subs : x ≠ y & ¬ Dom rho(x) → Sub_p (Rhop rho x y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.Rhop_Ren
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= y & NOT Dom(rho)(x) & NOT Ran(rho)(y) => Ren?(Rhop(rho, x, y))
theorem Rhop_Ren : x ≠ y & ¬ Dom rho(x) & ¬ Ran rho(y) → Ren_p (Rhop rho x y) := by
  -- === TCCs for Rhop_Ren ===
  -- TCC: Rhop_Ren_TCC1
  -- Statement: FORALL (rho: Ren, x, y: (V)):
  --   NOT Ran(rho)(y) AND NOT Dom(rho)(x) AND x /= y IMPLIES
  --    Sub?(Rhop(rho, x, y))
  have rhop_ren_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.ext_replace_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) =>
--  ext(sigma)(replaceTerm(s, t, p)) =
--   replaceTerm(ext(sigma)(s), ext(sigma)(t), p)
theorem ext_replace_ext : positionsOF s(p) → ext sigma(replaceTerm s t p) =
 replaceTerm (ext sigma(s)) (ext sigma(t)) p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.rename_preserv_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(ext(rho)(t)) = positionsOF(t)
theorem rename_preserv_pos : positionsOF (ext rho(t)) = positionsOF t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.subterm_of_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(p) =>
--  (IF vars?(subtermOF(t, p))
--     THEN (positionsOF(ext(sigma)(subtermOF(t, p)))(p1) =>
--            subtermOF(ext(sigma)(t), p o p1) =
--             subtermOF(ext(sigma)(subtermOF(t, p
theorem subterm_of_ext : positionsOF t(p) → (if vars_p (subtermOF t p)
   then (positionsOF (ext sigma(subtermOF t p)) (p1) →
          subtermOF (ext sigma(t)) (p ∘ p1) =
           subtermOF (ext sigma(subtermOF t p)) p1)
 else subtermOF (ext sigma(t)) p = ext sigma(subtermOF t p)
 ) := by
  -- === TCCs for subterm_of_ext ===
  -- TCC: subterm_of_ext_TCC1
  -- Statement: FORALL (p, p1: position[variable, symbol, arity], sigma: Sub,
  --         t: term[variable, symbol, arity]):
  --   positionsOF(ext(sigma)(subtermOF(t, p)))(p1) AND
  --    vars?(subtermOF(t, p)) AND positionsOF...
  have subterm_of_ext_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.rename_preserv_inclusion
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(Vars(t), Vars(s)) => subset?(Vars(ext(rho)(t)), Vars(ext(rho)(s)))
theorem rename_preserv_inclusion : subset_p (Vars t) (Vars s) → subset_p (Vars (ext rho(t))) (Vars (ext rho(s))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.same_substitution
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ext(sg1)(t) = ext(sg2)(t) =>
--  (FORALL x: member(x, Vars(t)) => sg1(x) = sg2(x))
theorem same_substitution : ext sg1(t) = ext sg2(t) → (∀ x,  member x (Vars t) → sg1 x = sg2 x) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.same_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL x: member(x, Vars(t)) => sg1(x) = sg2(x)) =>
--  ext(sg1)(t) = ext(sg2)(t)
theorem same_term : (∀ x,  member x (Vars t) → sg1 x = sg2 x) → ext sg1(t) = ext sg2(t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.term_is_a_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ext(rho)(t) = x => vars?(t)
theorem term_is_a_var : ext rho(t) = x → vars_p t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.vars_term_rename
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Dom(rho) = Vars(t) =>
--  subset?(Vars(ext(rho)(t)),
--          restrict
--              [term[variable, symbol, arity], (V[variable, symbol, arity]),
--               bool]
--              (Ran(rho)))
theorem vars_term_rename : Dom rho = Vars t → subset_p (Vars (ext rho(t))) (restrict[term[variable, symbol, arity], (V[variable, symbol, arity]),
             bool]
            (Ran rho)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.exists_renaming
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS rho:
--   Dom(rho) = V1 &
--    disjoint?(V2,
--              restrict
--                  [term[variable, symbol, arity],
--                   (V[variable, symbol, arity]), bool]
--                  (Ran(rho)))
theorem exists_renaming : ∃ (rho : term pvs_variable symbol NIL → term pvs_variable symbol NIL), Dom rho = V1 & disjoint_p V2 (restrict[term[variable, symbol, arity], (V[variable, symbol, arity]),
               bool]
              (Ran rho)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.add_parallel_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, sigma, (fst: SPP(t)), (p: positions?(t)):
--   (FORALL (i: below[length(fst)]):
--      parallel(finseq_appl[position[variable, symbol, arity]](fst)(i), p))
--    => SPP?(ext(sigma)(t))(add_first(p, 
theorem add_parallel_pos : ∀ (t : term pvs_variable symbol NIL) (sigma : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (fst : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), (∀ (i : Fin List.length fst), parallel (finseq_appl fst(i)) p) → SPP_p (ext sigma(t)) (add_first p fst) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.ext_parallel_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, sigma, (fst: SPP(t)): SPP?(ext(sigma)(t))(fst)
theorem ext_parallel_pos : ∀ (t : term pvs_variable symbol NIL) (sigma : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (fst : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))), SPP_p (ext sigma t) fst := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.rest_parallel_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, (fst: SPP(t)):
--   fst`length /= 0 =>
--    FORALL (i: below[length(rest(fst))]):
--      parallel(finseq_appl(rest(fst))(i), fst`seq(0))
theorem rest_parallel_first : ∀ (t : term pvs_variable symbol NIL) (fst : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))), fst.List.length ≠ 0 → ∀ (i : Fin List.length (rest fst)), parallel (finseq_appl (rest fst) (i)) (fst.seq 0) := by
  -- === TCCs for rest_parallel_first ===
  -- TCC: rest_parallel_first_TCC1
  -- Statement: FORALL (t, fst: SPP[variable, symbol, arity](t)):
  --   fst`length /= 0 IMPLIES
  --    FORALL (i: below[length(rest[position[variable, symbol, arity]](fst))]):
  --      0 < fst`length
  have rest_parallel_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.rest_parallel_pos_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, (fst: SPP(t)), (p: positions?(t)):
--   (FORALL (i: below[length(fst)]):
--      parallel(finseq_appl[position[variable, symbol, arity]](fst)(i), p))
--    =>
--    FORALL (i: below[length(rest(fst))]):
theorem rest_parallel_pos_parallel : ∀ (t : term pvs_variable symbol NIL) (fst : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), (∀ (i : Fin List.length fst), parallel (finseq_appl fst(i)) p) → ∀ (i : Fin List.length (rest fst)), parallel (finseq_appl (rest fst) (i)) p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: substitution.vars_subst_not_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, sigma, x:
--   Dom(sigma)(x) AND
--    (FORALL r: Ran(sigma)(r) IMPLIES NOT member(x, Vars(r)))
--    IMPLIES NOT member(x, Vars(ext(sigma)(t)))
theorem vars_subst_not_in : ∀ (t : term pvs_variable symbol NIL) (sigma : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL), Dom sigma(x) ∧ (∀ r,  Ran sigma(r) → ¬ member x (Vars r)) → ¬ member x (Vars (ext sigma(t))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: substitution.ext_preserve_symbol
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, sig: Sub, p: position | positionsOF(s)(p)):
--   app?(subtermOF(s, p)) IMPLIES
--    f(subtermOF(s, p)) = f(subtermOF(ext(sig)(s), p))
theorem ext_preserve_symbol : ∀ (s : term pvs_variable symbol NIL) (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), app_p (subtermOF s p) → f (subtermOF s p) = f (subtermOF (ext sig(s)) p) := by
  -- === TCCs for ext_preserve_symbol ===
  -- TCC: ext_preserve_symbol_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], sig: Sub,
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p)):
  --   app?(subtermOF(s, p)) IMPLIES
  --    positi...
  have ext_preserve_symbol_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ext_preserve_symbol_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], sig: Sub,
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p)):
  --   app?(subtermOF(s, p)) IMPLIES
  --    app?[v...
  have ext_preserve_symbol_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.substitution