-- Auto-generated by pvs2lean/translator.py
-- PVS theory: innermost_reduction
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: innermost_reduction

Auto-generated stubs from PVSLib theory `innermost_reduction`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.innermost_reduction

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (RTC : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Vars : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} (app_p : pvs_τ6 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (args : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} (comp_op_p : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (ext : pvs_τ15 → pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (finseq_appl : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} (first : pvs_τ20 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (is_countably_infinite : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (is_normal_form_p : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} (length : pvs_τ30 → Prop)
variable {pvs_τ32 : Type*} (non_root_reduction_p : pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (normal_sub_p : pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (parallel : pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (positionsOF : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} (positions_p : pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (reducible_p : pvs_τ40 → pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} (reduction_fix_p : pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} (reduction_p : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} (rest_p : pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} (restriction : pvs_τ51 → pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (subset_p : pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} (subtermOF : pvs_τ56 → pvs_τ57 → pvs_τ58)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} (terminating_p : pvs_τ59 → pvs_τ60 → Prop)

-- pvs2lean: innermost_reduction.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.nr_normal_form_subterms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_normal_form?(non_root_reduction?(E))(t) IFF
--  FORALL (q: positions?(t)):
--    NOT (q = empty_seq) IMPLIES
--     is_normal_form?(reduction?(E))(subtermOF(t, q))
theorem nr_normal_form_subterms : is_normal_form_p (non_root_reduction_p E) (t) ↔ ∀ (q : positions_p t), ¬ (q = empty_seq) →
   is_normal_form_p (reduction_p E) (subtermOF t q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.innermost_reduction_fix?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_normal_form?(non_root_reduction?(E))(subtermOF(s, p)) AND
--  reduction_fix?(E)(s, t, p)
axiom innermost_reduction_fix_p {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24 → Prop

-- pvs2lean: innermost_reduction.innermost_reduction?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (p: positions?(s)): innermost_reduction_fix?(E)(s, t, p)
axiom innermost_reduction_p {pvs_τ25 : Type*} : pvs_τ25 → Prop

-- pvs2lean: innermost_reduction.innermost_subset_reduction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(innermost_reduction?(E), reduction?(E))
theorem innermost_subset_reduction : subset_p (innermost_reduction_p E) (reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.terminating_is_inn_terminating
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: terminating?(reduction?(E))(t) IMPLIES
--  terminating?(innermost_reduction?(E))(t)
theorem terminating_is_inn_terminating : terminating_p (reduction_p E) (t) → terminating_p (innermost_reduction_p E) (t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.inn_reduction_is_comp_op
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: comp_op?(innermost_reduction?(E))
theorem inn_reduction_is_comp_op : comp_op_p (innermost_reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.innermost_terminating_all_subterms
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (E: set[rewrite_rule[variable, symbol, arity]]):
--   FORALL (t: term):
--     terminating?(innermost_reduction?(E))(t) IFF
--      FORALL (p: positions?(t)):
--        terminating?(innermost_reduction?(E)
theorem innermost_terminating_all_subterms : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool), ∀ (t : term pvs_variable symbol NIL), terminating_p (innermost_reduction_p E) (t) ↔ ∀ (p : positions_p t), terminating_p (innermost_reduction_p E) (subtermOF t p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.normal_subst
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term | app?(s), sigma: Sub):
--   is_normal_form?(non_root_reduction?(E))(ext(sigma)(s)) IMPLIES
--    normal_sub?(E)(restriction(sigma)(Vars(s)))
theorem normal_subst : ∀ (s : {s : term pvs_variable symbol NIL // app_p s}) (sigma : term pvs_variable symbol NIL → term pvs_variable symbol NIL), is_normal_form_p (non_root_reduction_p E) (ext sigma(s)) → normal_sub_p E(restriction sigma(Vars s)) := by
  -- === TCCs for normal_subst ===
  -- TCC: normal_subst_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         s: term[variable, symbol, arity]
  --             | app?[variable, symbol, arity](s),
  --         sigma: Sub[variable, symbol, arity]):
  --   is_no...
  have normal_subst_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.inn_nf_subterms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT reducible?(innermost_reduction?(E))(t) IFF
--  FORALL (p: positions?(t)):
--    NOT reducible?(innermost_reduction?(E))(subtermOF(t, p))
theorem inn_nf_subterms : ¬ reducible_p (innermost_reduction_p E) (t) ↔ ∀ (p : positions_p t), ¬ reducible_p (innermost_reduction_p E) (subtermOF t p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.innf_iff_nf
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_normal_form?(reduction?(E))(t) IFF
--  is_normal_form?(innermost_reduction?(E))(t)
theorem innf_iff_nf : is_normal_form_p (reduction_p E) (t) ↔ is_normal_form_p (innermost_reduction_p E) (t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.non_nf_has_reducible_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (E) (t: term | NOT is_normal_form?(reduction?(E))(t)):
--   EXISTS (p: positions?(t)):
--     reducible?(innermost_reduction?(E))(subtermOF(t, p))
theorem non_nf_has_reducible_subterm : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool), ∀ (t : {t : term pvs_variable symbol NIL // ¬ is_normal_form_p (reduction_p E) (t)}), ∃ (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), reducible_p (innermost_reduction_p E) (subtermOF t p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.non_root_innermost_reduction?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (p: positions?(s) | NOT (p = empty_seq)):
--   is_normal_form?(non_root_reduction?(E))(subtermOF(s, p)) AND
--    reduction_fix?(E)(s, t, p)
axiom non_root_innermost_reduction_p {pvs_τ31 : Type*} : pvs_τ31 → Prop

-- pvs2lean: innermost_reduction.non_root_inn_subset_inn_reduction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(non_root_innermost_reduction?(E), innermost_reduction?(E))
theorem non_root_inn_subset_inn_reduction : subset_p (non_root_innermost_reduction_p E) (innermost_reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.non_root_ir_preserves_root_symbol
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term | app?(s), t: term):
--   RTC(non_root_innermost_reduction?(E))(s, t) IMPLIES
--    (app?(t) AND f(s) = f(t))
theorem non_root_ir_preserves_root_symbol : ∀ (s : {s : term pvs_variable symbol NIL // app_p s}) (t : term pvs_variable symbol NIL), RTC (non_root_innermost_reduction_p E) (s, t) → (app_p t ∧ f s = f t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.non_root_ir_preserves_pos_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term | app?(s), k: posnat | positionsOF(s)( #(k)), t: term):
--   RTC(non_root_innermost_reduction?(E))(s, t) IMPLIES positionsOF(t)( #(k))
theorem non_root_ir_preserves_pos_args : ∀ (s : {s : term pvs_variable symbol NIL // app_p s}) (k : {k : {i : {i : ℝ // i ≥ 0} // i > 0} // positionsOF s( #(k))}) (t : term pvs_variable symbol NIL), RTC (non_root_innermost_reduction_p E) (s, t) → positionsOF t( #(k)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.arg_preservation_in_finite_reduction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem arg_preservation_in_finite_reduction : True := by
  -- === TCCs for arg_preservation_in_finite_reduction ===
  -- TCC: arg_preservation_in_finite_reduction_TCC1
  -- Statement: FORALL (seqt: finseq[term[variable, symbol, arity]]):
  --   NOT seqt = empty_seq IMPLIES 0 < length(seqt)
  have arg_preservation_in_finite_reduction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC2
  -- Statement: FORALL (seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[variable, symbol, arity]
  --                    (finseq_app...
  have arg_preservation_in_finite_reduction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC3
  -- Statement: FORALL (seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[variable, symbol, arity]
  --                    (finseq_app...
  have arg_preservation_in_finite_reduction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC4
  -- Statement: FORALL (seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[variable, symbol, arity]
  --                    (finseq_app...
  have arg_preservation_in_finite_reduction_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC5
  -- Statement: FORALL (seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[variable, symbol, arity]
  --                    (finseq_app...
  have arg_preservation_in_finite_reduction_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC6
  -- Statement: FORALL (seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[variable, symbol, arity]
  --                    (finseq_app...
  have arg_preservation_in_finite_reduction_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC7
  -- Statement: FORALL (seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[variable, symbol, arity]
  --                    (finseq_app...
  have arg_preservation_in_finite_reduction_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC8
  -- Statement: FORALL (seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[variable, symbol, arity]
  --                    (finseq_app...
  have arg_preservation_in_finite_reduction_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC9
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[...
  have arg_preservation_in_finite_reduction_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC10
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[...
  have arg_preservation_in_finite_reduction_tcc10 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arg_preservation_in_finite_reduction_TCC11
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         seqt: finseq[term[variable, symbol, arity]]
  --             | NOT seqt = empty_seq[term[variable, symbol, arity]] AND
  --                app?[...
  have arg_preservation_in_finite_reduction_tcc11 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: innermost_reduction.non_root_rtc_reduction_of_argument
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term | app?(s)):
--   RTC(non_root_innermost_reduction?(E))(s, t) IMPLIES
--    FORALL (k: posnat | k <= length(args(s))):
--      RTC(innermost_reduction?(E))(subtermOF(s,  #(k)), subtermOF(t,  #(k
theorem non_root_rtc_reduction_of_argument : ∀ (s : {s : term pvs_variable symbol NIL // app_p s}), RTC (non_root_innermost_reduction_p E) (s, t) → ∀ (k : posnat | k ≤ List.length (args s)), RTC (innermost_reduction_p E) (subtermOF s #(k), subtermOF t #(k)) := by
  -- === TCCs for non_root_rtc_reduction_of_argument ===
  -- TCC: non_root_rtc_reduction_of_argument_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         t: term[variable, symbol, arity],
  --         s: term[variable, symbol, arity]
  --             | app?[variable, symbol, arity](s)):
  --   RTC(non_...
  have non_root_rtc_reduction_of_argument_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: non_root_rtc_reduction_of_argument_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         t: term[variable, symbol, arity],
  --         s: term[variable, symbol, arity]
  --             | app?[variable, symbol, arity](s)):
  --   RTC(non_...
  have non_root_rtc_reduction_of_argument_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.arg_rest?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: rest?(non_root_innermost_reduction?(E), s)(u, v) AND
--  EXISTS (p: positions?(u) | NOT (p = empty_seq)):
--    first(p) = k AND innermost_reduction_fix?(E)(u, v, p)
axiom arg_rest_p {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: innermost_reduction.nr_rest_is_union_of_arg_rests
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term | app?(s)):
--   LET k_rest =
--         {a: [term, term] |
--            app?(a`1) AND EXISTS (k: posnat): arg_rest?(E)(s)(k)(a`1, a`2)}
--     IN rest?(non_root_innermost_reduction?(E), s) = k_r
theorem nr_rest_is_union_of_arg_rests : ∀ (s : {s : term pvs_variable symbol NIL // app_p s}), fun (k_rest : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) => rest_p (non_root_innermost_reduction_p E) s = k_rest (fun (a : term pvs_variable symbol NIL × term pvs_variable symbol NIL) => app_p a.1 ∧ ∃ (k : {n : ℕ // 0 < n}), arg_rest_p E(s) (k) (a.1, a.2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: innermost_reduction.innermost_reduction_permutates
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t1, t: term, p, q: positions?(s)):
--   parallel(p, q) AND
--    innermost_reduction_fix?(E)(s, t1, p) AND
--     innermost_reduction_fix?(E)(t1, t, q)
--    IMPLIES
--    EXISTS (t2: term):
--      innermos
theorem innermost_reduction_permutates : ∀ (s : term pvs_variable symbol NIL) (t1 : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), parallel p q ∧
 innermost_reduction_fix_p E(s, t1, p) ∧
  innermost_reduction_fix_p E(t1, t, q) → ∃ (t2 : term), innermost_reduction_fix_p E(s, t2, q) ∧
   innermost_reduction_fix_p E(t2, t, p) := by
  -- === TCCs for innermost_reduction_permutates ===
  -- TCC: innermost_reduction_permutates_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         s, t1: term[variable, symbol, arity],
  --         p, q: positions?[variable, symbol, arity](s)):
  --   parallel(p, q) AND innermost_reduction_...
  have innermost_reduction_permutates_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: innermost_reduction_permutates_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         s, t1, t: term[variable, symbol, arity],
  --         p, q: positions?[variable, symbol, arity](s)):
  --   parallel(p, q) AND
  --    innermost_redu...
  have innermost_reduction_permutates_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.innermost_reduction