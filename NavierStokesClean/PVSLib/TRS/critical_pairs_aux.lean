-- Auto-generated by pvs2lean/translator.py
-- PVS theory: critical_pairs_aux
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: critical_pairs_aux

Auto-generated stubs from PVSLib theory `critical_pairs_aux`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.critical_pairs_aux

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Pos_var : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (RSigma : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (SPP : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (SPP_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (add_first : pvs_τ10 → pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} (comp_cont_p : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (ext : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (is_countably_infinite : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (iterate : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (length : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (parallel : pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} (positions_p : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (replace_pos : pvs_τ29 → pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} (set2seq : pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (subtermOF : pvs_τ33 → pvs_τ34 → Prop)

-- pvs2lean: critical_pairs_aux.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: critical_pairs_aux.CP_lemma_aux2b
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL R, t, x, sg1, sg2, (fstp: SPP(t)):
--   LET Mtn = replace_pos(ext(sg1)(t), ext(sg2)(x), fstp) IN
--     (FORALL (i: below[length(fstp)]):
--        subtermOF(t,
--                  finseq_appl[position[var
theorem CP_lemma_aux2b : ∀ (R : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) (t : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL) (sg1 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (sg2 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (fstp : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))), fun (Mtn : term pvs_variable symbol NIL) => (∀ (i : Fin List.length fstp), subtermOF t (finseq_appl fstp(i)) =
    x)
 & comp_cont_p R & RSigma R sg1 sg2 x → iterate R (List.length fstp) (ext sg1(t), Mtn) (replace_pos (ext sg1(t)) (ext sg2(x)) fstp) := by
  -- === TCCs for CP_lemma_aux2b ===
  -- TCC: CP_lemma_aux2b_TCC1
  -- Statement: FORALL (t, x, sg1, sg2, fstp: SPP[variable, symbol, arity](t)):
  --   SPP?[variable, symbol, arity](ext[variable, symbol, arity](sg1)(t))(fstp)
  have cp_lemma_aux2b_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CP_lemma_aux2b_TCC2
  -- Statement: FORALL (t, x, sg1, sg2, fstp: SPP[variable, symbol, arity](t),
  --         Mtn: term[variable, symbol, arity]):
  --   Mtn = replace_pos(ext(sg1)(t), ext(sg2)(x), fstp) IMPLIES
  --    FORALL (i: below[length(fs...
  have cp_lemma_aux2b_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: critical_pairs_aux.CP_lemma_aux2c1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL R, t, x, sg1, sg2, (fst: SPP(t)), (q: positions?(t)):
--   (FORALL (i: below[length(fst)]):
--      parallel(finseq_appl[position[variable, symbol, arity]](fst)(i), q) &
--       subtermOF(t, finseq_app
theorem CP_lemma_aux2c1 : ∀ (R : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) (t : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL) (sg1 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (sg2 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (fst : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), (∀ (i : Fin List.length fst), parallel (finseq_appl fst(i)) q &
    subtermOF t (finseq_appl fst(i)) =
     x)
 & subtermOF t q = x & comp_cont_p R & RSigma R sg1 sg2 x → iterate R (List.length fst)
       (replace_pos (ext sg1(t)) (ext sg2(x)) #(q),
        replace_pos (ext sg1(t)) (ext sg2(x)) (add_first q fst)) := by
  -- === TCCs for CP_lemma_aux2c1 ===
  -- TCC: CP_lemma_aux2c1_TCC1
  -- Statement: FORALL (t, x, sg1, sg2, fst: SPP[variable, symbol, arity](t),
  --         q: positions?[variable, symbol, arity](t), i: below[length(fst)]):
  --   parallel(finseq_appl[position[variable, symbol, arity]](fs...
  have cp_lemma_aux2c1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CP_lemma_aux2c1_TCC2
  -- Statement: FORALL (R, t, x, sg1, sg2, fst: SPP[variable, symbol, arity](t),
  --         q: positions?[variable, symbol, arity](t)):
  --   (FORALL (i: below[length(fst)]):
  --      parallel(finseq_appl[position[variable, ...
  have cp_lemma_aux2c1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CP_lemma_aux2c1_TCC3
  -- Statement: FORALL (R, t, x, sg1, sg2, fst: SPP[variable, symbol, arity](t),
  --         q: positions?[variable, symbol, arity](t)):
  --   (FORALL (i: below[length(fst)]):
  --      parallel(finseq_appl[position[variable, ...
  have cp_lemma_aux2c1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: critical_pairs_aux.CP_lemma_aux2c
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL R, t, x, sg1, sg2, (fstp: SPP(t)):
--   fstp`length /= 0 &
--    (FORALL (i: below[length(fstp)]):
--       subtermOF(t, finseq_appl[position[variable, symbol, arity]](fstp)(i))
--        = x)
--     & comp_c
theorem CP_lemma_aux2c : ∀ (R : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) (t : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL) (sg1 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (sg2 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (fstp : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))), fstp.List.length ≠ 0 &
 (∀ (i : Fin List.length fstp), subtermOF t (finseq_appl fstp(i)) =
     x)
  & comp_cont_p R & RSigma R sg1 sg2 x → (∀ (i : Fin List.length fstp), iterate R (List.length fstp - 1)
          (replace_pos (ext sg1(t)) (ext sg2(x)) (#(finseq_appl[position[variable, symbol, arity]]
                              (fstp) (i))),
           replace_pos (ext sg1(t)) (ext sg2(x)) fstp)) := by
  -- === TCCs for CP_lemma_aux2c ===
  -- TCC: CP_lemma_aux2c_TCC1
  -- Statement: FORALL (t, x, sg1, sg2, fstp: SPP[variable, symbol, arity](t)):
  --   fstp`length /= 0 IMPLIES
  --    FORALL (i: below[length(fstp)]):
  --      positionsOF[variable, symbol, arity]
  --          (t)(finseq_appl[pos...
  have cp_lemma_aux2c_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CP_lemma_aux2c_TCC2
  -- Statement: FORALL (R, t, x, sg1, sg2, fstp: SPP[variable, symbol, arity](t)):
  --   fstp`length /= 0 AND
  --    (FORALL (i: below[length(fstp)]):
  --       subtermOF(t, finseq_appl[position[variable, symbol, arity]](fstp...
  have cp_lemma_aux2c_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CP_lemma_aux2c_TCC3
  -- Statement: FORALL (R, t, x, sg1, sg2, fstp: SPP[variable, symbol, arity](t)):
  --   fstp`length /= 0 AND
  --    (FORALL (i: below[length(fstp)]):
  --       subtermOF(t, finseq_appl[position[variable, symbol, arity]](fstp...
  have cp_lemma_aux2c_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: critical_pairs_aux.CP_lemma_aux2d
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL R, t, x, sg1, sg2:
--   LET Posv = Pos_var(t, x), seqv = set2seq(Posv) IN
--     comp_cont?(R) & RSigma(R, sg1, sg2, x) =>
--      replace_pos(ext(sg1)(t), ext(sg2)(x), seqv) = ext(sg2)(t)
theorem CP_lemma_aux2d : ∀ (R : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) (t : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL) (sg1 : term pvs_variable symbol NIL → term pvs_variable symbol NIL) (sg2 : term pvs_variable symbol NIL → term pvs_variable symbol NIL), fun (Posv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) → Bool) => fun (seqv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) => comp_cont_p R & RSigma R sg1 sg2 x → replace_pos (ext sg1(t)) (ext sg2(x)) seqv = ext sg2(t) (set2seq Posv) (Pos_var t x) := by
  -- === TCCs for CP_lemma_aux2d ===
  -- TCC: CP_lemma_aux2d_TCC1
  -- Statement: FORALL (t, x, sg1, sg2, Posv: positions[variable, symbol, arity]):
  --   Posv = Pos_var(t, x) IMPLIES
  --    is_finite[position[variable, symbol, arity]](Posv)
  have cp_lemma_aux2d_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CP_lemma_aux2d_TCC2
  -- Statement: FORALL (R, t, x, sg1, sg2, Posv: positions[variable, symbol, arity],
  --         seqv: finite_sequence[position[variable, symbol, arity]]):
  --   Posv = Pos_var(t, x) AND
  --    seqv = set2seq(Posv) AND comp_c...
  have cp_lemma_aux2d_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.critical_pairs_aux