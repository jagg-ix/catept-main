-- Auto-generated by pvs2lean/translator.py
-- PVS theory: newman_yokouchi
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: newman_yokouchi

Auto-generated stubs from PVSLib theory `newman_yokouchi`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.newman_yokouchi

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (RTC : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (confluent_p : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} (diamond_property_p : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} (locally_confluent_p : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (noetherian_p : pvs_τ8 → Prop)

-- pvs2lean: newman_yokouchi.Newman_lemma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: noetherian?(R) => (confluent?(R) <=> locally_confluent?(R))
theorem Newman_lemma : noetherian_p R → (confluent_p R ≤> locally_confluent_p R) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: newman_yokouchi.Yokouchi_lemma_ax1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (noetherian?(R) &
--   confluent?(R) &
--    (FORALL x, y, z:
--       (S(x, y) & R(x, z)) =>
--        (EXISTS (u: T): RTC(R)(y, u) & (RTC(R) o S o RTC(R))(z, u))))
--  =>
--  (FORALL x, y, z:
--     (S(x, y) & RTC(R)(x,
theorem Yokouchi_lemma_ax1 : (noetherian_p R &
  confluent_p R &
   (∀ x y z, 
      (S x y & R x z) →
       (∃ (u : T), RTC R(y, u) & (RTC R ∘ S ∘ RTC R) (z, u)))) → (∀ x y z, 
   (S x y & RTC R(x, z)) →
    (∃ (w : T), RTC R(y, w) & (RTC R ∘ S ∘ RTC R) (z, w))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: newman_yokouchi.Yokouchi_lemma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (noetherian?(R) &
--   confluent?(R) &
--    diamond_property?(S) &
--     (FORALL x, y, z:
--        (S(x, y) & R(x, z)) =>
--         (EXISTS (u: T): RTC(R)(y, u) & (RTC(R) o S o RTC(R))(z, u))))
--  => diamond_prope
theorem Yokouchi_lemma : (noetherian_p R &
  confluent_p R &
   diamond_property_p S &
    (∀ x y z, 
       (S x y & R x z) →
        (∃ (u : T), RTC R(y, u) & (RTC R ∘ S ∘ RTC R) (z, u)))) → diamond_property_p (RTC R ∘ S ∘ RTC R) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.newman_yokouchi