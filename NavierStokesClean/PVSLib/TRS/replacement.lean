-- Auto-generated by pvs2lean/translator.py
-- PVS theory: replacement
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: replacement

Auto-generated stubs from PVSLib theory `replacement`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.replacement

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Pos_var : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (app : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (args : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (arity : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (difference : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} (first : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} (length : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (parallel : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (positionsOF : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (positions_p : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (remove : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (replace : pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ30 : Type*} (rest : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (singleton : pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (subset_p : pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (subtermOF : pvs_τ35 → pvs_τ36 → pvs_τ37)

-- pvs2lean: replacement.replaceTerm
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (IF length(p) = 0 THEN t
--  ELSE LET st = args(s),
--           i = first(p),
--           q = rest(p),
--           rst =
--             replace(replaceTerm(finseq_appl[term[variable, symbol, arity]]
--              
axiom replaceTerm {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ27 → pvs_τ28 → pvs_τ29 → Prop

-- pvs2lean: replacement.replace_preserv_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) => positionsOF(replaceTerm(s, t, p))(p)
theorem replace_preserv_pos : positionsOF s(p) → positionsOF (replaceTerm s t p) (p) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_compose_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(q) & positionsOF(s)(p) =>
--  positionsOF(replaceTerm(s, t, p))(p o q)
theorem replace_compose_pos : positionsOF t(q) & positionsOF s(p) → positionsOF (replaceTerm s t p) (p ∘ q) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_preserv_parallel_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(q) & positionsOF(s)(p) & parallel(p, q) =>
--  positionsOF(replaceTerm(s, t, p))(q)
theorem replace_preserv_parallel_pos : positionsOF s(q) & positionsOF s(p) & parallel p q → positionsOF (replaceTerm s t p) (q) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.preserv_unchanged_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) & positionsOF(replaceTerm(s, t, p))(q) & parallel(p, q)
--  => positionsOF(s)(q)
theorem preserv_unchanged_pos : positionsOF s(p) & positionsOF (replaceTerm s t p) (q) & parallel p q → positionsOF s(q) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.subterm_of_replace
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) => subtermOF(replaceTerm(s, t, p), p) = t
theorem subterm_of_replace : positionsOF s(p) → subtermOF (replaceTerm s t p) p = t := by
  -- === TCCs for subterm_of_replace ===
  -- TCC: subterm_of_replace_TCC1
  -- Statement: FORALL (p: position[variable, symbol, arity],
  --         s, t: term[variable, symbol, arity]):
  --   positionsOF(s)(p) IMPLIES
  --    positionsOF[variable, symbol, arity](replaceTerm(s, t, p))(p)
  have subterm_of_replace_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.equality_replaceTerm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) IMPLIES
--  (t1 = t2 IFF replaceTerm(s, t1, p) = replaceTerm(s, t2, p))
theorem equality_replaceTerm : positionsOF s(p) → (t1 = t2 ↔ replaceTerm s t1 p = replaceTerm s t2 p) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_subterm_of_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) => replaceTerm(s, subtermOF(s, p), p) = s
theorem replace_subterm_of_term : positionsOF s(p) → replaceTerm s (subtermOF s p) p = s := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_embedding
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) & positionsOF(t)(q) =>
--  subtermOF(replaceTerm(s, t, p), p o q) = subtermOF(t, q)
theorem replace_embedding : positionsOF s(p) & positionsOF t(q) → subtermOF (replaceTerm s t p) (p ∘ q) = subtermOF t q := by
  -- === TCCs for replace_embedding ===
  -- TCC: replace_embedding_TCC1
  -- Statement: FORALL (p, q: position[variable, symbol, arity],
  --         s, t: term[variable, symbol, arity]):
  --   positionsOF(t)(q) AND positionsOF(s)(p) IMPLIES
  --    positionsOF[variable, symbol, arity]
  --        (repl...
  have replace_embedding_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_associativity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) & positionsOF(t)(q) =>
--  replaceTerm(replaceTerm(s, t, p), r, p o q) =
--   replaceTerm(s, replaceTerm(t, r, q), p)
theorem replace_associativity : positionsOF s(p) & positionsOF t(q) → replaceTerm (replaceTerm s t p) r (p ∘ q) =
 replaceTerm s (replaceTerm t r q) p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_distributivity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p o q) =>
--  subtermOF(replaceTerm(s, t, p o q), p) = replaceTerm(subtermOF(s, p), t, q)
theorem replace_distributivity : positionsOF s(p ∘ q) → subtermOF (replaceTerm s t (p ∘ q)) p = replaceTerm (subtermOF s p) t q := by
  -- === TCCs for replace_distributivity ===
  -- TCC: replace_distributivity_TCC1
  -- Statement: FORALL (p, q: position[variable, symbol, arity],
  --         s, t: term[variable, symbol, arity]):
  --   positionsOF(s)(p o q) IMPLIES
  --    positionsOF[variable, symbol, arity]
  --        (replaceTerm(s, t, o[po...
  have replace_distributivity_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_distributivity_TCC2
  -- Statement: FORALL (p, q: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity]):
  --   positionsOF(s)(p o q) IMPLIES positionsOF[variable, symbol, arity](s)(p)
  have replace_distributivity_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: replace_distributivity_TCC3
  -- Statement: FORALL (p, q: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity]):
  --   positionsOF(s)(p o q) IMPLIES
  --    positionsOF[variable, symbol, arity]
  --        (subtermOF[variable, symbo...
  have replace_distributivity_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_dominance
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p o q) =>
--  replaceTerm(replaceTerm(s, t, p o q), r, p) = replaceTerm(s, r, p)
theorem replace_dominance : positionsOF s(p ∘ q) → replaceTerm (replaceTerm s t (p ∘ q)) r p = replaceTerm s r p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_persistence
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) & positionsOF(s)(q) & parallel(p, q) =>
--  subtermOF(replaceTerm(s, t, p), q) = subtermOF(s, q)
theorem replace_persistence : positionsOF s(p) & positionsOF s(q) & parallel p q → subtermOF (replaceTerm s t p) q = subtermOF s q := by
  -- === TCCs for replace_persistence ===
  -- TCC: replace_persistence_TCC1
  -- Statement: FORALL (p, q: position[variable, symbol, arity],
  --         s, t: term[variable, symbol, arity]):
  --   parallel(p, q) AND positionsOF(s)(q) AND positionsOF(s)(p) IMPLIES
  --    positionsOF[variable, symbol, ...
  have replace_persistence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.replace_commutativity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) & positionsOF(s)(q) & parallel(p, q) =>
--  replaceTerm(replaceTerm(s, t, p), r, q) =
--   replaceTerm(replaceTerm(s, r, q), t, p)
theorem replace_commutativity : positionsOF s(p) & positionsOF s(q) & parallel p q → replaceTerm (replaceTerm s t p) r q =
 replaceTerm (replaceTerm s r q) t p := by
  -- === TCCs for replace_commutativity ===
  -- TCC: replace_commutativity_TCC1
  -- Statement: FORALL (p, q: position[variable, symbol, arity],
  --         r, s: term[variable, symbol, arity]):
  --   parallel(p, q) AND positionsOF(s)(q) AND positionsOF(s)(p) IMPLIES
  --    positionsOF[variable, symbol, ...
  have replace_commutativity_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: replacement.pos_replaces_is_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, x, (p: positions?(t)):
--   subset?(positionsOF(replaceTerm(t, x, p)), positionsOF(t))
theorem pos_replaces_is_subset : ∀ (t : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), subset_p (positionsOF (replaceTerm t x p)) (positionsOF t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replacement.pos_occ_var_replace
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, y, x, (p: positions?(t)):
--   Pos_var(t, x)(p) & y /= x =>
--    Pos_var(replaceTerm(t, y, p), x) = remove(p, Pos_var(t, x))
theorem pos_occ_var_replace : ∀ (t : term pvs_variable symbol NIL) (y : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), Pos_var t x(p) & y ≠ x → Pos_var (replaceTerm t y p) x = remove p (Pos_var t x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: replacement.pos_occ_var_replace_as_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, y, x, (p: positions?(t)):
--   Pos_var(t, x)(p) & y /= x =>
--    Pos_var(replaceTerm(t, y, p), x) =
--     difference(Pos_var(t, x), singleton(p))
theorem pos_occ_var_replace_as_diff : ∀ (t : term pvs_variable symbol NIL) (y : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), Pos_var t x(p) & y ≠ x → Pos_var (replaceTerm t y p) x = difference (Pos_var t x) (singleton p) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.replacement