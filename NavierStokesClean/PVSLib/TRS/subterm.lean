-- Auto-generated by pvs2lean/translator.py
-- PVS theory: subterm
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: subterm

Auto-generated stubs from PVSLib theory `subterm`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.subterm

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (pvs_op_01abfc : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Card : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (IUnion : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (PP_p : pvs_τ7 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (Posv : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (SP_p : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (add_first : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (app : pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} (app_p : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (args : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (arity : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} (card : pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (child : pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (delete : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (disjoint_p : pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (finseq_appl : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} (first : pvs_τ38 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (is_finite : pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (left_without_children : pvs_τ44 → pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} (length : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (member : pvs_τ47 → pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (parallel : pvs_τ50 → pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (positionsOF : pvs_τ52 → pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} (positions_p : pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} (rest : pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (set2seq : pvs_τ56 → pvs_τ57)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (singleton : pvs_τ61 → pvs_τ62)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} (subset_p : pvs_τ63 → pvs_τ64 → Prop)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (subterm : pvs_τ65 → pvs_τ66 → Prop)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (vars : pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} (vars_p : pvs_τ71 → Prop)

-- pvs2lean: subterm.subtermOF
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (IF length(p) = 0 THEN t
--  ELSE LET st = args(t), i = first(p), q = rest(p) IN
--         subtermOF(finseq_appl[term[variable, symbol, arity]](st)(i - 1), q)
--  ENDIF)
axiom subtermOF {pvs_τ67 : Type*} {pvs_τ68 : Type*} : pvs_τ67 → pvs_τ68 → Prop

-- pvs2lean: subterm.Vars
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {x: (V) | EXISTS (p: positions?(t)): subtermOF(t, p) = x}
axiom Vars {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ14 → pvs_τ15

-- pvs2lean: subterm.ground
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Vars(t) = emptyset
noncomputable def ground : sorry := by
  sorry  -- pvs2lean: definition stub for Vars(t) = emptyset

-- pvs2lean: subterm.Pos_var
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: extend
--     [position[variable, symbol, arity],
--      positions?[variable, symbol, arity](t), bool, FALSE]
--     ({p: positions?(t) | subtermOF(t, p) = x})
axiom Pos_var {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: subterm.pos_vars_subset_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET Posv = Pos_var(t, x) IN subset?(Posv, positionsOF(t))
theorem pos_vars_subset_pos : fun (Posv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) → Bool) => subset_p Posv (positionsOF t) (Pos_var t x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.Pos_var_is_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET Posv = Pos_var(t, x) IN is_finite(Posv)
theorem Pos_var_is_finite : fun (Posv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) → Bool) => is_finite Posv (Pos_var t x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.Vars_is_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vars?(t) => Vars(t) = {y: (V) | y = t}
theorem Vars_is_var : vars_p t → Vars t = {y: (V) | y = t} := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.vars_term_is_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (CASES t
--    OF vars(t):
--         Vars(vars(t)) =
--          restrict
--              [term[variable, symbol, arity], (V[variable, symbol, arity]),
--               bool]
--              (singleton(vars(t))),
--     
theorem vars_term_is_union : (CASES t
   OF vars t:
        Vars (vars t) =
         restrict[term[variable, symbol, arity], (V[variable, symbol, arity]),
              bool]
             (singleton (vars t)),
      app f st:
        Vars t =
         IUnion (fun (i : Fin List.length st)=> Vars (finseq_appl st(i)))
   ) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.vars_of_term_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(Vars(t))
theorem vars_of_term_finite : is_finite (Vars t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.pos_subterm_ax
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(p o q) => positionsOF(subtermOF(t, p))(q)
theorem pos_subterm_ax : positionsOF t(p ∘ q) → positionsOF (subtermOF t p) (q) := by
  -- === TCCs for pos_subterm_ax ===
  -- TCC: pos_subterm_ax_TCC1
  -- Statement: FORALL (p, q: position[variable, symbol, arity],
  --         t: term[variable, symbol, arity]):
  --   positionsOF(t)(p o q) IMPLIES positionsOF[variable, symbol, arity](t)(p)
  have pos_subterm_ax_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: subterm.pos_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(p o q) => subtermOF(t, p o q) = subtermOF(subtermOF(t, p), q)
theorem pos_subterm : positionsOF t(p ∘ q) → subtermOF t (p ∘ q) = subtermOF (subtermOF t p) q := by
  -- === TCCs for pos_subterm ===
  -- TCC: pos_subterm_TCC1
  -- Statement: FORALL (p, q: position[variable, symbol, arity],
  --         t: term[variable, symbol, arity]):
  --   positionsOF(t)(p o q) IMPLIES
  --    positionsOF[variable, symbol, arity](subtermOF(t, p))(q)
  have pos_subterm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: subterm.pos_o_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(p) & positionsOF(subtermOF(t, p))(q) => positionsOF(t)(p o q)
theorem pos_o_term : positionsOF t(p) & positionsOF (subtermOF t p) (q) → positionsOF t(p ∘ q) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: subterm.subterm_is_app
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) AND length(p) /= 0 =>
--  app?(subtermOF(s, delete(p, length(p) - 1)))
theorem subterm_is_app : positionsOF s(p) ∧ List.length p ≠ 0 → app_p (subtermOF s (delete p (List.length p - 1))) := by
  -- === TCCs for subterm_is_app ===
  -- TCC: subterm_is_app_TCC1
  -- Statement: FORALL (p: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity]):
  --   length(p) /= 0 AND positionsOF(s)(p) IMPLIES
  --    length(p) - 1 >= 0 AND length(p) - 1 < length(p)
  have subterm_is_app_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_is_app_TCC2
  -- Statement: FORALL (p: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity]):
  --   length(p) /= 0 AND positionsOF(s)(p) IMPLIES
  --    positionsOF[variable, symbol, arity](s)(delete[posnat](p, ...
  have subterm_is_app_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.vars_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(p) => subset?(Vars(subtermOF(t, p)), Vars(t))
theorem vars_subterm : positionsOF t(p) → subset_p (Vars (subtermOF t p)) (Vars t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.disjoint_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(p) =>
--  (disjoint?(Vars(t), Vars(s)) => disjoint?(Vars(subtermOF(t, p)), Vars(s)))
theorem disjoint_subterm : positionsOF t(p) → (disjoint_p (Vars t) (Vars s) → disjoint_p (Vars (subtermOF t p)) (Vars s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.variable_positions_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: positions?(t)):
--   subtermOF(t, p) = x & subtermOF(t, q) = y & x /= y => parallel(p, q)
theorem variable_positions_parallel : ∀ (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), subtermOF t p = x & subtermOF t q = y & x ≠ y → parallel p q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.variable_positions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: positions?(t)):
--   LET Posv = Pos_var(t, x) IN
--     member(p, Posv) & member(q, Posv) => p = q OR parallel(p, q)
theorem variable_positions : ∀ (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), fun (Posv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) → Bool) => member p Posv & member q Posv → p = q ∨ parallel p q (Pos_var t x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.pos_occ_var_HAStypePP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET Posv = Pos_var(t, x), seqv = set2seq(Posv) IN PP?(seqv)
theorem pos_occ_var_HAStypePP : fun (Posv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) → Bool) => fun (seqv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) => PP_p seqv (set2seq Posv) (Pos_var t x) := by
  -- === TCCs for pos_occ_var_HAStypePP ===
  -- TCC: pos_occ_var_HAStypePP_TCC1
  -- Statement: FORALL (t: term[variable, symbol, arity], x: (V),
  --         Posv: positions[variable, symbol, arity]):
  --   Posv = Pos_var(t, x) IMPLIES
  --    is_finite[position[variable, symbol, arity]](Posv)
  have pos_occ_var_hastypepp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.pos_occ_var_HAStypeSP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET Posv = Pos_var(t, x), seqv = set2seq(Posv) IN SP?(t)(seqv)
theorem pos_occ_var_HAStypeSP : fun (Posv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) → Bool) => fun (seqv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) => SP_p t seqv (set2seq Posv) (Pos_var t x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.no_empty_set_positions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, x, (p: positions?(t)):
--   LET Posv = Pos_var(t, x) IN subtermOF(t, p) = x => Posv(p)
theorem no_empty_set_positions : ∀ (t : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), fun (Posv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) → Bool) => subtermOF t p = x → Posv p (Pos_var t x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.length_seq_var_g0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t, x:
--   LET Posv = Pos_var(t, x), seqv = set2seq(Posv) IN
--     (EXISTS (p: positions?(t)): subtermOF(t, p) = x) => length(seqv) > 0
theorem length_seq_var_g0 : ∀ (t : term pvs_variable symbol NIL) (x : term pvs_variable symbol NIL), fun (Posv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) → Bool) => fun (seqv : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) => (∃ (p : positions_p t), subtermOF t p = x) → List.length seqv > 0 (set2seq Posv) (Pos_var t x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.Var_occurs_only_once
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Card(Pos_var(t, x)) = 1 IMPLIES
--  EXISTS (p1): (positionsOF(t)(p1) AND subtermOF(t, p1) = x)
theorem Var_occurs_only_once : Card (Pos_var t x) = 1 → ∃ (p1 : _), (positionsOF t(p1) ∧ subtermOF t p1 = x) := by
  -- === TCCs for Var_occurs_only_once ===
  -- TCC: Var_occurs_only_once_TCC1
  -- Statement: FORALL (t: term[variable, symbol, arity], x: (V)):
  --   is_finite[position[variable, symbol, arity]](Pos_var(t, x))
  have var_occurs_only_once_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.Var_occurs_only_once_also_in_subterms
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      vars?(x) AND positionsOF(s)(p) AND (p`length /= 0)
--  AND (x = subtermOF(s, p)) AND (card(Pos_var(s, x)) = 1)
--  IMPLIES card(Pos_var(subtermOF(s,  #(first(p))), x)) = 1
theorem Var_occurs_only_once_also_in_subterms : vars_p x ∧ positionsOF s(p) ∧ (p.List.length ≠ 0)
 ∧ (x = subtermOF s p) ∧ (card (Pos_var s x) = 1) → card (Pos_var (subtermOF s (#(first p))) x) = 1 := by
  -- === TCCs for Var_occurs_only_once_also_in_subterms ===
  -- TCC: Var_occurs_only_once_also_in_subterms_TCC1
  -- Statement: FORALL (p: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], x: (V)):
  --        (card(Pos_var(s, x)) = 1) AND (x = subtermOF(s, p))
  --    AND (p`length /= 0) AND positionsOF(s)...
  have var_occurs_only_once_also_in_subterms_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Var_occurs_only_once_also_in_subterms_TCC2
  -- Statement: FORALL (p: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity], x: (V)):
  --        (card(Pos_var(s, x)) = 1) AND (x = subtermOF(s, p))
  --    AND (p`length /= 0) AND positionsOF(s)...
  have var_occurs_only_once_also_in_subterms_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.subterm_empty_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subtermOF(s, empty_seq) = s
theorem subterm_empty_seq : subtermOF s empty_seq = s := by
  -- === TCCs for subterm_empty_seq ===
  -- TCC: subterm_empty_seq_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity]):
  --   positionsOF[variable, symbol, arity](s)(empty_seq[posnat])
  have subterm_empty_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.equal_subterms_equal_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem equal_subterms_equal_term : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: subterm.subt_of_subt_is_subt_of_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, (k: posnat | positionsOF(s)( #(k))),
--         (p: position | positionsOF(subtermOF(s,  #(k)))(p)),
--         (x: position | positionsOF(s)(x))):
--   x = add_first(k, p) IMPLIES
--    subtermO
theorem subt_of_subt_is_subt_of_term : ∀ (s : term pvs_variable symbol NIL) (k : {k : {i : {i : ℝ // i ≥ 0} // i > 0} // positionsOF s( #(k))}) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF (subtermOF s #(k)) (p)}) (x : {x : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(x)}), x = add_first k p → subtermOF s x = subtermOF (subtermOF s #(k)) p := by
  -- === TCCs for subt_of_subt_is_subt_of_term ===
  -- TCC: subt_of_subt_is_subt_of_term_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         (k: posnat
  --              | positionsOF[variable, symbol, arity](s)( #[posnat](k)))):
  --   positionsOF[variable, symbol, arity](s)( #[posnat](k))
  have subt_of_subt_is_subt_of_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.subterm_to_subtermOF
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term):
--   subterm(s, t) IMPLIES
--    (EXISTS (p: position | positionsOF(t)(p)): subtermOF(t, p) = s)
theorem subterm_to_subtermOF : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL), subterm s t → (∃ (p : position | positionsOF t(p)), subtermOF t p = s) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: subterm.subtermOF_to_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term, (p: position | positionsOF(t)(p))):
--   subtermOF(t, p) = s IMPLIES subterm(s, t)
theorem subtermOF_to_subterm : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), subtermOF t p = s → subterm s t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: subterm.subterm_to_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term, r: term):
--   (subterm(s, r) AND subterm(r, t)) IMPLIES subterm(s, t)
theorem subterm_to_subterm : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (r : term pvs_variable symbol NIL), (subterm s r ∧ subterm r t) → subterm s t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: subterm.comp_of_pos
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN empty_seq ELSE p o comp_of_pos(p, n - 1) ENDIF
axiom comp_of_pos {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ28 → pvs_τ29 → Prop

-- pvs2lean: subterm.set_of_seq_pos
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IUnion(LAMBDA (n: nat): singleton(comp_of_pos(p, n)))
axiom set_of_seq_pos {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} : pvs_τ58 → pvs_τ59 → pvs_τ60

-- pvs2lean: subterm.n_for_a_pstart
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: position, q: (set_of_seq_pos(p))):
--   p /= empty_seq IMPLIES EXISTS (n: nat): q = comp_of_pos(p, n)
theorem n_for_a_pstart : ∀ (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) (q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), p ≠ empty_seq → ∃ (n : ℕ), q = comp_of_pos p n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.length_comp_p
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(comp_of_pos(p, n)) = n * length(p)
theorem length_comp_p : List.length (comp_of_pos p n) = n * List.length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.m_neq_n
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: p /= empty_seq AND m /= n IMPLIES comp_of_pos(p, n) /= comp_of_pos(p, m)
theorem m_neq_n : p ≠ empty_seq ∧ m ≠ n → comp_of_pos p n ≠ comp_of_pos p m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.function_inj
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: comp_of_pos(p, n)
axiom function_inj {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41

-- pvs2lean: subterm.is_injective_function_inj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: p /= empty_seq IMPLIES injective?[nat, (set_of_seq_pos(p))](function_inj(p))
theorem is_injective_function_inj : p ≠ empty_seq → injective_p (function_inj p) := by
  -- === TCCs for is_injective_function_inj ===
  -- TCC: is_injective_function_inj_TCC1
  -- Statement: FORALL (p: position[variable, symbol, arity]):
  --   p /= empty_seq IMPLIES
  --    FORALL (x1: nat): set_of_seq_pos(p)(function_inj(p)(x1))
  have is_injective_function_inj_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.infinite_set_of_seq_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: p /= empty_seq IMPLIES NOT is_finite(set_of_seq_pos(p))
theorem infinite_set_of_seq_pos : p ≠ empty_seq → ¬ is_finite (set_of_seq_pos p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.comp_of_pos_is_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat):
--   positionsOF(s)(p) AND subtermOF(s, p) = s IMPLIES
--    positionsOF(s)(comp_of_pos(p, n))
theorem comp_of_pos_is_pos : ∀ (n : {i : ℝ // i ≥ 0}), positionsOF s(p) ∧ subtermOF s p = s → positionsOF s(comp_of_pos p n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.subset_of_seq_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) AND subtermOF(s, p) = s IMPLIES
--  subset?(set_of_seq_pos(p), positionsOF(s))
theorem subset_of_seq_pos : positionsOF s(p) ∧ subtermOF s p = s → subset_p (set_of_seq_pos p) (positionsOF s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.term_eq_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) AND subtermOF(s, p) = s IMPLIES p = empty_seq
theorem term_eq_subterm : positionsOF s(p) ∧ subtermOF s p = s → p = empty_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.app_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (positionsOF(s)(p) AND positionsOF(s)(q) AND child(p, q)) IMPLIES
--  app?(subtermOF(s, q))
theorem app_term : (positionsOF s(p) ∧ positionsOF s(q) ∧ child p q) → app_p (subtermOF s q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.positions_of_a_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: position | positionsOF(t)(p)):
--   positionsOF(t)(q) IMPLIES
--    (left_without_children(p, q) OR
--      left_without_children(q, p) OR
--       child(p, q) OR
--        (EXISTS (q1: position):
--        
theorem positions_of_a_term : ∀ (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF t(p)}), positionsOF t(q) → (left_without_children p q ∨
  left_without_children q p ∨
   child p q ∨
    (∃ (q1 : position), positionsOF (subtermOF t p) (q1) ∧ q = p ∘ q1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.equal_app_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (app?(s) AND app?(t) AND f(s) = f(t) AND args(s) = args(t)) IMPLIES s = t
theorem equal_app_term : (app_p s ∧ app_p t ∧ f s = f t ∧ args s = args t) → s = t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.equal_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position | positionsOF(s)(p) AND positionsOF(t)(p)):
--   ((FORALL (q: position | positionsOF(s)(q) AND positionsOF(t)(q)):
--       left_without_children(p, q) => subte
theorem equal_term : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) ∧ positionsOF t(p)}), ((∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children p q → subtermOF s q = subtermOF t q)
  ∧
  (∀ (q : position | positionsOF s(q) ∧ positionsOF t(q)), left_without_children q p → subtermOF s q = subtermOF t q)
   ∧
   (∀ (p1 : position | positionsOF s(p1) ∧ positionsOF t(p1)), child p p1 → f (subtermOF s p1) = f (subtermOF t p1))
    ∧ subtermOF s p = subtermOF t p ∧ ¬ p = empty_seq) → s = t := by
  -- === TCCs for equal_term ===
  -- TCC: equal_term_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have equal_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: equal_term_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity], t: term[variable, symbol, arity],
  --         p: position[variable, symbol, arity]
  --             | positionsOF[variable, symbol, arity](s)(p) AND
  --                ...
  have equal_term_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: subterm.args_subterm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t: term | app?(t) AND length(args(t)) > 0):
--   FORALL (i: below[length(args(t))]):
--     finseq_appl(args(t))(i) = subtermOF(t,  #(i + 1))
theorem args_subterm : ∀ (t : {t : term pvs_variable symbol NIL // app_p t ∧ List.length (args t) > 0}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length (args t)}), finseq_appl (args t) (i) = subtermOF t (#(i + 1)) := by
  -- === TCCs for args_subterm ===
  -- TCC: args_subterm_TCC1
  -- Statement: FORALL (t: term[variable, symbol, arity]
  --             | app?[variable, symbol, arity](t) AND
  --                length(args[variable, symbol, arity](t)) > 0,
  --         i: below[length(args[variable, symbo...
  have args_subterm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.subterm