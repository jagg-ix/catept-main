-- Auto-generated by pvs2lean/translator.py
-- PVS theory: positions
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: positions

Auto-generated stubs from PVSLib theory `positions`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.positions

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (pvs_op_01abfc : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (IUnion : pvs_τ3 → pvs_τ4)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (SP : pvs_τ6 → pvs_τ7)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (add_first : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (add_last : pvs_τ15 → pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (app : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (app_p : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (args : pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} (arity : pvs_τ23 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (delete : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (finseq_appl : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} (first : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (is_finite : pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (last : pvs_τ38 → pvs_τ39)
variable {pvs_τ44 : Type*} (length : pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (member : pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ51 : Type*} (positions_p : pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (rest : pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} (union : pvs_τ54 → pvs_τ55 → pvs_τ56)
variable {pvs_τ57 : Type*} (upto_p : pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (vars : pvs_τ58 → pvs_τ59)

-- pvs2lean: positions.only_empty_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {x: position | x = empty_seq}
noncomputable def only_empty_seq : sorry := by
  sorry  -- pvs2lean: definition stub for {x: position | x = empty_seq}

-- pvs2lean: positions.catenate
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {seq: position |
--    EXISTS (x: position): (member(x, s) AND seq = add_first(i, x))}
axiom catenate {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26

-- pvs2lean: positions.positionsOF
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (CASES t
--    OF vars(t): only_empty_seq,
--       app(f, st):
--         IF length(st) = 0 THEN only_empty_seq
--         ELSE union(only_empty_seq,
--                    IUnion((LAMBDA (i: upto?(length(st))):
--    
axiom positionsOF {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ49 → pvs_τ50 → Prop

-- pvs2lean: positions.positions
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {p: position | positionsOF(t)(p)}
noncomputable def positions : sorry := by
  sorry  -- pvs2lean: definition stub for {p: position | positionsOF(t)(p)}

-- pvs2lean: positions.<=
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (p1: position): q = p o p1)
noncomputable def pvs_op_a7eeae : sorry := by
  sorry  -- pvs2lean: definition stub for (EXISTS (p1: position): q = p o p1)

-- pvs2lean: positions.parallel
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (NOT p <= q) & (NOT q <= p)
axiom parallel {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ47 → pvs_τ48 → Prop

-- pvs2lean: positions.child
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (p1: position): (p1 /= empty_seq AND p = q o p1))
axiom child {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ27 → pvs_τ28 → Prop

-- pvs2lean: positions.left_without_children
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (r, p1, q1: position):
--    (q = r o q1 AND
--      p = r o p1 AND
--       q1 /= empty_seq AND p1 /= empty_seq AND first(p1) < first(q1)))
axiom left_without_children {pvs_τ42 : Type*} {pvs_τ43 : Type*} : pvs_τ42 → pvs_τ43 → Prop

-- pvs2lean: positions.left_pos
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (r, p1, q1: position):
--    (q = r AND p1 /= empty_seq AND p = r o p1) OR
--     (q = r o q1 AND
--       p = r o p1 AND
--        q1 /= empty_seq AND p1 /= empty_seq AND first(p1) < first(q1)))
axiom left_pos {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ40 → pvs_τ41 → Prop

-- pvs2lean: positions.PP?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF fsp`length < 2 THEN TRUE
-- ELSE FORALL (i, j: below[length(fsp)]):
--        i /= j =>
--         parallel(finseq_appl[position](fsp)(i),
--                  finseq_appl[position](fsp)(j))
-- ENDIF
axiom PP_p {pvs_τ5 : Type*} : pvs_τ5 → Prop

-- pvs2lean: positions.SP?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below[length(fsp)]):
--   positionsOF(t)(finseq_appl[position](fsp)(i))
axiom SP_p {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: positions.SPP?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) & SP?(t)(fsp)
axiom SPP_p {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: positions.positions_of_terms_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(positionsOF(t))
theorem positions_of_terms_finite : is_finite (positionsOF t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.empty_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: q = empty_seq & p <= q => p = empty_seq
theorem empty_pos : q = empty_seq & p ≤ q → p = empty_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.empty_seq_is_position
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(empty_seq)
theorem empty_seq_is_position : positionsOF t empty_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.closed_positions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (positionsOF(t)(q) & p <= q) => positionsOF(t)(p)
theorem closed_positions : (positionsOF t(q) & p ≤ q) → positionsOF t(p) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.equal_symbol_equal_length_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t: term, fs, ft: symbol,
--         ss:
--           {args: finite_sequence[term[variable, symbol, arity]] |
--              args`length = arity(fs)},
--         st:
--           {argt: finite_sequence[te
theorem equal_symbol_equal_length_arg : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (fs : symbol) (ft : symbol) (ss : {args : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → term pvs_variable symbol NIL) // args.List.length = arity fs}) (st : {argt : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → term pvs_variable symbol NIL) // argt.List.length = arity ft}), (s = app fs ss ∧ t = app ft st ∧ fs = ft) → ss.List.length = st.List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.not_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (positionsOF(t)(p) & p = add_first(i, q)) => app?(t)
theorem not_var : (positionsOF t(p) & p = add_first i q) → app_p t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.not_var1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (positionsOF(t)(p) & p = add_last(q, i)) => app?(t)
theorem not_var1 : (positionsOF t(p) & p = add_last q i) → app_p t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.pos_ax
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(t)(p o q) => positionsOF(t)(p)
theorem pos_ax : positionsOF t(p ∘ q) → positionsOF t(p) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.rest_of_positions
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (positionsOF(s)(p) & length(p) /= 0) =>
--  positionsOF(finseq_appl(args(s))(first(p) - 1))(rest(p))
theorem rest_of_positions : (positionsOF s(p) & List.length p ≠ 0) → positionsOF (finseq_appl (args s) (first p - 1)) (rest p) := by
  -- === TCCs for rest_of_positions ===
  -- TCC: rest_of_positions_TCC1
  -- Statement: FORALL (p: position, s: term[variable, symbol, arity]):
  --   length(p) /= 0 AND positionsOF(s)(p) IMPLIES
  --    first[posnat](p) - 1 >= 0 AND
  --     first[posnat](p) - 1 < length(args[variable, symbol, arit...
  have rest_of_positions_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: rest_of_positions_TCC2
  -- Statement: FORALL (p: position, s: term[variable, symbol, arity]):
  --   length(p) /= 0 AND positionsOF(s)(p) IMPLIES
  --    app?[variable, symbol, arity](s)
  have rest_of_positions_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.delete_is_position
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)(p) & length(p) /= 0 =>
--  positionsOF(s)(delete(p, length(p) - 1))
theorem delete_is_position : positionsOF s(p) & List.length p ≠ 0 → positionsOF s(delete p (List.length p - 1)) := by
  -- === TCCs for delete_is_position ===
  -- TCC: delete_is_position_TCC1
  -- Statement: FORALL (p: position, s: term[variable, symbol, arity]):
  --   length(p) /= 0 AND positionsOF(s)(p) IMPLIES
  --    length(p) - 1 >= 0 AND length(p) - 1 < length(p)
  have delete_is_position_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.parallel_comm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: parallel(p, q) => parallel(q, p)
theorem parallel_comm : parallel p q → parallel q p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.rest_parallel
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(p) /= 0 & length(q) /= 0 & first(p) = first(q) & parallel(p, q) =>
--  parallel(rest(p), rest(q))
theorem rest_parallel : List.length p ≠ 0 & List.length q ≠ 0 & first p = first q & parallel p q → parallel (rest p) (rest q) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.parallel_pos_same_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: parallel(p1, p2) & p1 = p o q1 & p2 = p o q2 IMPLIES parallel(q1, q2)
theorem parallel_pos_same_prefix : parallel p1 p2 & p1 = p ∘ q1 & p2 = p ∘ q2 → parallel q1 q2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.comp_preservs_parallel_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: parallel(p1, p2) IMPLIES parallel(p o p1, p o p2)
theorem comp_preservs_parallel_pos : parallel p1 p2 → parallel (p ∘ p1) (p ∘ p2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.comp_preservs_parallel_pos2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: parallel(p1, p2) IMPLIES parallel(p1 o q1, p2)
theorem comp_preservs_parallel_pos2 : parallel p1 p2 → parallel (p1 ∘ q1) p2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.comp_preservs_parallel_pos3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: parallel(p1, p2) IMPLIES parallel(p1 o q1, p2 o q2)
theorem comp_preservs_parallel_pos3 : parallel p1 p2 → parallel (p1 ∘ q1) (p2 ∘ q2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: positions.rest_of_PP_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fss: PP): PP?(rest(fss))
theorem rest_of_PP_is_PP : ∀ (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))), PP_p (rest fss) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.rest_of_SP_is_SP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL s, (fss: SP(s)): SP?(s)(rest(fss))
theorem rest_of_SP_is_SP : ∀ (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))), SP_p s (rest fss) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.delete_of_PP_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fss: PP), (i: below[length(fss)]): PP?(delete(fss, i))
theorem delete_of_PP_is_PP : ∀ (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length fss}), PP_p (delete fss i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.delete_of_SP_is_SP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL s, (fss: SP(s)), (i: below[length(fss)]): SP?(s)(delete(fss, i))
theorem delete_of_SP_is_SP : ∀ (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length fss}), SP_p s (delete fss i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.add_first_parallel_pos_to_PP_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fss: PP), (q: position):
--   (FORALL (i: below[length(fss)]):
--      parallel(finseq_appl[position](fss)(i), q))
--    => PP?(add_first(q, fss))
theorem add_first_parallel_pos_to_PP_is_PP : ∀ (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), (∀ (i : Fin List.length fss), parallel (finseq_appl fss(i)) q) → PP_p (add_first q fss) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.add_first_parallel_pos_to_SP_is_SP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL s, (fss: SP(s)), (q: positions?(s)): SP?(s)(add_first(q, fss))
theorem add_first_parallel_pos_to_SP_is_SP : ∀ (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), SP_p s (add_first q fss) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.add_last_parallel_pos_to_PP_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fss: PP), (q: position):
--   (FORALL (i: below[length(fss)]):
--      parallel(finseq_appl[position](fss)(i), q))
--    => PP?(add_last(fss, q))
theorem add_last_parallel_pos_to_PP_is_PP : ∀ (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), (∀ (i : Fin List.length fss), parallel (finseq_appl fss(i)) q) → PP_p (add_last fss q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.add_last_parallel_pos_to_SP_is_SP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL s, (fss: SP(s)), (q: positions?(s)): SP?(s)(add_last(fss, q))
theorem add_last_parallel_pos_to_SP_is_SP : ∀ (s : term pvs_variable symbol NIL) (fss : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}))) (q : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p)}), SP_p s (add_last fss q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.parallel_pos_are_dif
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp) IMPLIES
--  FORALL (i, j: below[fsp`length]):
--    i /= j IMPLIES
--     finseq_appl[position](fsp)(i) /= finseq_appl[position](fsp)(j)
theorem parallel_pos_are_dif : PP_p fsp → ∀ (i : _) (j : Fin fsp.List.length), i ≠ j →
   finseq_appl fsp(i) ≠ finseq_appl fsp(j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.comp_SPP_commute
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(s)(fsp o fsq) IMPLIES SPP?(s)(fsq o fsp)
theorem comp_SPP_commute : SPP_p s(fsp ∘ fsq) → SPP_p s(fsq ∘ fsp) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.positions_of_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((s: term | app?(s)), k: below[length(args(s))]):
--   positionsOF(s)( #[posnat](k + 1))
theorem positions_of_arg : ∀ (s : {s : term pvs_variable symbol NIL // app_p s}) (k : {s : {i : ℝ // i ≥ 0} // s < List.length (args s)}), positionsOF s (pvs_op_01abfc (k + 1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.left_pos_transitive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q1: position):
--   left_pos(p, q1) AND left_pos(q1, q) IMPLIES left_pos(p, q)
theorem left_pos_transitive : ∀ (q1 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), left_pos p q1 ∧ left_pos q1 q → left_pos p q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.lwc_is_left_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: left_without_children(p, q) IMPLIES left_pos(p, q)
theorem lwc_is_left_pos : left_without_children p q → left_pos p q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.lwc_transitive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q1: position):
--   left_without_children(p, q1) AND left_without_children(q1, q) IMPLIES
--    left_without_children(p, q)
theorem lwc_transitive : ∀ (q1 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), left_without_children p q1 ∧ left_without_children q1 q → left_without_children p q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.subterm_if_le_arity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (app?(s) AND arity(f(s)) >= i) IMPLIES positionsOF(s)( #(i))
theorem subterm_if_le_arity : (app_p s ∧ arity (f s) ≥ i) → positionsOF s( #(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.subterms_acc_arity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionsOF(s)( #(i)) AND j <= i IMPLIES positionsOF(s)( #(j))
theorem subterms_acc_arity : positionsOF s( #(i)) ∧ j ≤ i → positionsOF s( #(j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.lwc_add_last_delete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q1: position):
--   (p /= empty_seq AND left_without_children(p, q1)) IMPLIES
--    LET q = add_last(delete(p, length(p) - 1), 1 + last(p)) IN
--      left_without_children(q, q1) OR child(q1, q) OR q1
theorem lwc_add_last_delete : ∀ (q1 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), (p ≠ empty_seq ∧ left_without_children p q1) → (let q := add_last (delete p (List.length p - 1)) (1 + last p); left_without_children q q1 ∨ child q1 q ∨ q1 = q) := by
  -- === TCCs for lwc_add_last_delete ===
  -- TCC: lwc_add_last_delete_TCC1
  -- Statement: FORALL (p, q1: position):
  --   p /= empty_seq AND left_without_children(p, q1) IMPLIES
  --    length(p) - 1 >= 0 AND length(p) - 1 < length(p)
  have lwc_add_last_delete_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lwc_add_last_delete_TCC2
  -- Statement: FORALL (p, q1: position):
  --   p /= empty_seq AND left_without_children(p, q1) IMPLIES length(p) /= 0
  have lwc_add_last_delete_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.lwc_add_last_delete1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q1: position, p: position | p /= empty_seq):
--   LET q = add_last(delete(p, length(p) - 1), 1 + last(p)) IN
--     left_without_children(q1, q) IMPLIES
--      left_without_children(q1, p) OR child(q1
theorem lwc_add_last_delete1 : ∀ (q1 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // p ≠ empty_seq}), fun (q : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})) => left_without_children q1 q → left_without_children q1 p ∨ child q1 p ∨ q1 = p (add_last (delete p (List.length p - 1)) (1 + last p)) := by
  -- === TCCs for lwc_add_last_delete1 ===
  -- TCC: lwc_add_last_delete1_TCC1
  -- Statement: FORALL (p: position | p /= empty_seq[posnat]):
  --   length(p) - 1 >= 0 AND length(p) - 1 < length(p)
  have lwc_add_last_delete1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lwc_add_last_delete1_TCC2
  -- Statement: FORALL (p: position | p /= empty_seq[posnat]): length(p) /= 0
  have lwc_add_last_delete1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: positions.leftmost_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (NOT positionsOF(s)( #(i + 1)) AND
--   positionsOF(s)( #(i)) AND positionsOF(s)(q))
--  IMPLIES (q = empty_seq OR left_pos(q,  #(i)) OR q =  #(i))
theorem leftmost_pos : (¬ positionsOF s( #(i + 1)) ∧
  positionsOF s( #(i)) ∧ positionsOF s(q)) → (q = empty_seq ∨ left_pos q #(i) ∨ q =  #(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.positions