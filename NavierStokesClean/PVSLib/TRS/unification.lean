-- Auto-generated by pvs2lean/translator.py
-- PVS theory: unification
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: unification

Auto-generated stubs from PVSLib theory `unification`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.unification

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Card : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Dom : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (Ran : pvs_τ5 → pvs_τ6)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (Vars : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (add_first : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (app : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} (app_p : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (arity : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (comp : pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (difference : pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (ext : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (first : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} (idempotent_sub_p : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (is_countably_infinite : pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (is_finite : pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} (length : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (member : pvs_τ36 → pvs_τ37 → Prop)
variable {pvs_τ42 : Type*} (min : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (positionsOF : pvs_τ43 → pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} (preorder_p : pvs_τ45 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} (rest : pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (singleton : pvs_τ50 → pvs_τ51)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (subset_p : pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (subtermOF : pvs_τ56 → pvs_τ57 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} (union : pvs_τ66 → pvs_τ67 → pvs_τ68)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (vars : pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} (vars_p : pvs_τ71 → Prop)

-- pvs2lean: unification.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.instance
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS sigma: ext(sigma)(s) = t
noncomputable def pvs_instance : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS sigma: ext(sigma)(s) = t

-- pvs2lean: unification.<=
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS tau: sigma = comp(tau, theta)
noncomputable def pvs_op_a7eeae : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS tau: sigma = comp(tau, theta)

-- pvs2lean: unification.mg_po
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: preorder?(<=)
theorem mg_po : preorder_p pvs_op_a7eeae := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.unifier
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ext(sigma)(s) = ext(sigma)(t)
axiom unifier {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} : pvs_τ62 → pvs_τ63 → pvs_τ64 → pvs_τ65

-- pvs2lean: unification.unifiable
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS sigma: unifier(sigma)(s, t)
axiom unifiable {pvs_τ58 : Type*} {pvs_τ59 : Type*} : pvs_τ58 → pvs_τ59 → Prop

-- pvs2lean: unification.U
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {sigma: Sub | unifier(sigma)(s, t)}
axiom U {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9

-- pvs2lean: unification.mgu
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(theta, U(s, t)) &
--  FORALL sigma: member(sigma, U(s, t)) IMPLIES theta <= sigma
axiom mgu {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ38 → pvs_τ39 → pvs_τ40 → pvs_τ41

-- pvs2lean: unification.uni_diff_equal_length_arg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, (t: term | unifiable(s, t) & s /= t), f: symbol,
--         st:
--           {args: finite_sequence[term[variable, symbol, arity]] |
--              args`length = arity(f)}):
--   NOT st`length =
theorem uni_diff_equal_length_arg : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}) (f : symbol) (st : {args : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → term pvs_variable symbol NIL) // args.List.length = arity f}), ¬ st.List.length = 0 ∧ s = app f st → (∀ (fp : symbol) (stp : {x : finite_sequence[term[variable, symbol, arity]] // x.List.List.length = arity fp}), t = app fp stp → (f = fp & st.List.length = stp.List.length)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.resolving_diff
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (CASES s
--    OF vars(s): empty_seq,
--       app(f, st):
--         IF length(st) = 0 THEN empty_seq
--         ELSE (CASES t
--                 OF vars(t): empty_seq,
--                    app(fp, stp):
--             
axiom resolving_diff {pvs_τ46 : Type*} {pvs_τ47 : Type*} : pvs_τ46 → pvs_τ47 → Prop

-- pvs2lean: unification.resol_diff_nonempty_implies_funct_terms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, (t: term | unifiable(s, t) & s /= t)):
--   resolving_diff(s, t) /= empty_seq IMPLIES (app?(s) AND app?(t))
theorem resol_diff_nonempty_implies_funct_terms : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), resolving_diff s t ≠ empty_seq → (app_p s ∧ app_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.resol_diff_to_rest_resol_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem resol_diff_to_rest_resol_diff : True := by
  -- === TCCs for resol_diff_to_rest_resol_diff ===
  -- TCC: resol_diff_to_rest_resol_diff_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         (t: term[variable, symbol, arity] | unifiable(s, t) & s /= t),
  --         rd: position[variable, symbol, arity]):
  --   rd = resolving_diff(s, t) AND rd /...
  have resol_diff_to_rest_resol_diff_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: resol_diff_to_rest_resol_diff_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         (t: term[variable, symbol, arity] | unifiable(s, t) & s /= t),
  --         rd: position[variable, symbol, arity]):
  --   rd = resolving_diff(s, t) AND rd /...
  have resol_diff_to_rest_resol_diff_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: resol_diff_to_rest_resol_diff_TCC3
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         (t: term[variable, symbol, arity] | unifiable(s, t) & s /= t),
  --         rd: position[variable, symbol, arity]):
  --   rd = resolving_diff(s, t) AND rd /...
  have resol_diff_to_rest_resol_diff_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: resol_diff_to_rest_resol_diff_TCC4
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         (t: term[variable, symbol, arity] | unifiable(s, t) & s /= t),
  --         rd: position[variable, symbol, arity]):
  --   rd = resolving_diff(s, t) AND rd /...
  have resol_diff_to_rest_resol_diff_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: unification.position_s_resolving_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t, p: position):
--   p = resolving_diff(s, t) IMPLIES positionsOF(s)(p)
theorem position_s_resolving_diff : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), p = resolving_diff s t → positionsOF s(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.position_t_resolving_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t, p: position):
--   p = resolving_diff(s, t) IMPLIES positionsOF(t)(p)
theorem position_t_resolving_diff : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}) (p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0})), p = resolving_diff s t → positionsOF t(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.resolving_diff_has_diff_argument
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t,
--         p: position | positionsOF(s)(p) & positionsOF(t)(p)):
--   p = resolving_diff(s, t) IMPLIES subtermOF(s, p) /= subtermOF(t, p)
theorem resolving_diff_has_diff_argument : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) & positionsOF t(p)}), p = resolving_diff s t → subtermOF s p ≠ subtermOF t p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.resolving_diff_has_unifiable_argument
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t,
--         p: position | positionsOF(s)(p) & positionsOF(t)(p)):
--   p = resolving_diff(s, t) IMPLIES
--    unifiable(subtermOF(s, p), subtermOF(t, p))
theorem resolving_diff_has_unifiable_argument : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) & positionsOF t(p)}), p = resolving_diff s t → unifiable (subtermOF s p) (subtermOF t p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.resolving_diff_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t,
--         p: position | positionsOF(s)(p) & positionsOF(t)(p)):
--   p = resolving_diff(s, t) IMPLIES
--    vars?(subtermOF(s, p)) OR vars?(subtermOF(t, p)
theorem resolving_diff_vars : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) & positionsOF t(p)}), p = resolving_diff s t → vars_p (subtermOF s p) ∨ vars_p (subtermOF t p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.unifier_o
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(sig, U(ext(theta)(s), ext(theta)(t))) IMPLIES
--  member(comp(sig, theta), U(s, t))
theorem unifier_o : member sig (U (ext theta(s)) (ext theta(t))) → member (comp sig theta) (U s t) := by
  -- === TCCs for unifier_o ===
  -- TCC: unifier_o_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         sig: Sub[variable, symbol, arity],
  --         t: term[variable, symbol, arity],
  --         theta: Sub[variable, symbol, arity]):
  --   member(sig, U(ext(thet...
  have unifier_o_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: unification.mgu_o
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sig <= rho IMPLIES comp(sig, theta) <= comp(rho, theta)
theorem mgu_o : sig ≤ rho → comp sig theta ≤ comp rho theta := by
  -- === TCCs for mgu_o ===
  -- TCC: mgu_o_TCC1
  -- Statement: FORALL (rho, sig, theta: Sub[variable, symbol, arity]):
  --   sig <= rho IMPLIES
  --    Sub?[variable, symbol, arity](comp[variable, symbol, arity](sig, theta))
  have mgu_o_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mgu_o_TCC2
  -- Statement: FORALL (rho, sig, theta: Sub[variable, symbol, arity]):
  --   sig <= rho IMPLIES
  --    Sub?[variable, symbol, arity](comp[variable, symbol, arity](rho, theta))
  have mgu_o_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: unification.unifier_and_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(theta, U(s, t)) IMPLIES
--  (FORALL (sig: Sub): member(comp(sig, theta), U(s, t)))
theorem unifier_and_subs : member theta (U s t) → (∀ (sig : Sub), member (comp sig theta) (U s t)) := by
  -- === TCCs for unifier_and_subs ===
  -- TCC: unifier_and_subs_TCC1
  -- Statement: FORALL (s, t: term[variable, symbol, arity],
  --         theta: Sub[variable, symbol, arity]):
  --   member(theta, U(s, t)) IMPLIES
  --    FORALL (sig: Sub[variable, symbol, arity]):
  --      Sub?[variable, symbol...
  have unifier_and_subs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: unification.idemp_mgu_iff_all_unifier
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (theta: Sub | member(theta, U(s, t))):
--   mgu(theta)(s, t) & idempotent_sub?(theta) IFF
--    (FORALL (sig: Sub | member(sig, U(s, t))): sig = comp(sig, theta))
theorem idemp_mgu_iff_all_unifier : ∀ (theta : {theta : term pvs_variable symbol NIL → term pvs_variable symbol NIL // member theta (U s t)}), mgu theta(s, t) & idempotent_sub_p theta ↔ (∀ (sig : Sub | member sig (U s t)), sig = comp sig theta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.unifiable_terms_unifiable_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term,
--         p: position | positionsOF(s)(p) & positionsOF(t)(p)):
--   member(sig, U(s, t)) IMPLIES
--    member(sig, U(subtermOF(s, p), subtermOF(t, p)))
theorem unifiable_terms_unifiable_args : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL) (p : {p : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : {i : ℝ // i ≥ 0} // i > 0}) // positionsOF s(p) & positionsOF t(p)}), member sig (U s t) → member sig (U (subtermOF s p) (subtermOF t p)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: unification.var_term_unifiable_not_var_in_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term):
--   vars?(s) & unifiable(s, t) & s /= t IMPLIES NOT member(s, Vars(t))
theorem var_term_unifiable_not_var_in_term : ∀ (s : term pvs_variable symbol NIL) (t : term pvs_variable symbol NIL), vars_p s & unifiable s t & s ≠ t → ¬ member s (Vars t) := by
  -- === TCCs for var_term_unifiable_not_var_in_term ===
  -- TCC: var_term_unifiable_not_var_in_term_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity]):
  --   vars?(s) AND unifiable(s, t) AND s /= t IMPLIES
  --    V[variable, symbol, arity](s)
  have var_term_unifiable_not_var_in_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.sub_of_frst_diff
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET k: position = resolving_diff(s, t) IN
--   LET sp = subtermOF(s, k), tp = subtermOF(t, k) IN
--     IF vars?(sp) THEN (LAMBDA (x: (V)): IF x = sp THEN tp ELSE x ENDIF)
--     ELSE (LAMBDA (x: (V)): IF x = 
axiom sub_of_frst_diff {pvs_τ52 : Type*} {pvs_τ53 : Type*} : pvs_τ52 → pvs_τ53 → Prop

-- pvs2lean: unification.dom_sub_of_frst_diff_is
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t, sig: Sub):
--   sig = sub_of_frst_diff(s, t) AND p = resolving_diff(s, t) IMPLIES
--    IF vars?(subtermOF(s, p))
--      THEN Dom(sig) =
--            restric
theorem dom_sub_of_frst_diff_is : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}) (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL), sig = sub_of_frst_diff s t ∧ p = resolving_diff s t → if vars_p (subtermOF s p)
  then Dom sig =
        restrict[term[variable, symbol, arity], (V[variable, symbol, arity]),
             bool]
            (singleton (subtermOF s p))
else Dom sig =
      restrict[term[variable, symbol, arity], (V[variable, symbol, arity]),
           bool]
          (singleton (subtermOF t p)) := by
  -- === TCCs for dom_sub_of_frst_diff_is ===
  -- TCC: dom_sub_of_frst_diff_is_TCC1
  -- Statement: FORALL (p: position[variable, symbol, arity],
  --         s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | unifiable(s, t) & s /= t,
  --         sig: Sub[variable, symbol, arity...
  have dom_sub_of_frst_diff_is_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.var_sub_1stdiff_not_member_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t):
--   LET sig = sub_of_frst_diff(s, t) IN
--     FORALL (x: (V) | member(x, Dom(sig)),
--             r: term[variable, symbol, arity] | member(r, Ran(sig))
theorem var_sub_1stdiff_not_member_term : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => ∀ (x : {x : term pvs_variable symbol NIL // member x (Dom sig)}) (r : {r : term pvs_variable symbol NIL // member r (Ran sig)}), ¬(member x (Vars r)) (sub_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.sub_of_frst_diff_unifier_o
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t):
--   member(rho, U(s, t)) IMPLIES
--    LET sig = sub_of_frst_diff(s, t) IN EXISTS theta: rho = comp(theta, sig)
theorem sub_of_frst_diff_unifier_o : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), member rho (U s t) → (let sig := sub_of_frst_diff s t; ∃ theta,  rho = comp theta sig) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.ext_sub_of_frst_diff_unifiable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t):
--   LET sig = sub_of_frst_diff(s, t) IN unifiable(ext(sig)(s), (ext(sig)(t)))
theorem ext_sub_of_frst_diff_unifiable : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => unifiable (ext sig(s)) ((ext sig(t))) (sub_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.sub_of_frst_diff_remove_x
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t):
--   LET sig = sub_of_frst_diff(s, t) IN
--     Dom(sig)(x) IMPLIES
--      (NOT member(x, Vars(ext(sig)(s)))) AND
--       (NOT member(x, Vars(ext(sig)(t))))
theorem sub_of_frst_diff_remove_x : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => Dom sig(x) → (¬ member x (Vars (ext sig(s)))) ∧ (¬ member x (Vars (ext sig(t)))) (sub_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.vars_sub_of_frst_diff_s_is_subset_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t):
--   LET sig = sub_of_frst_diff(s, t) IN
--     subset?(Vars(ext(sig)(s)), union(Vars(s), Vars(t)))
theorem vars_sub_of_frst_diff_s_is_subset_union : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => subset_p (Vars (ext sig(s))) (union (Vars s) (Vars t)) (sub_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.vars_sub_of_frst_diff_t_is_subset_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t):
--   LET sig = sub_of_frst_diff(s, t) IN
--     subset?(Vars(ext(sig)(t)), union(Vars(s), Vars(t)))
theorem vars_sub_of_frst_diff_t_is_subset_union : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => subset_p (Vars (ext sig(t))) (union (Vars s) (Vars t)) (sub_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.union_vars_ext_sub_of_frst_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t):
--   LET sig = sub_of_frst_diff(s, t) IN
--     union(Vars(ext(sig)(s)), Vars(ext(sig)(t))) =
--      difference(union(Vars(s), Vars(t)), Dom(sig))
theorem union_vars_ext_sub_of_frst_diff : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => union (Vars (ext sig(s))) (Vars (ext sig(t))) = difference (union (Vars s) (Vars t)) (Dom sig) (sub_of_frst_diff s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.vars_ext_sub_of_frst_diff_decrease
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: term, t: term | unifiable(s, t) & s /= t):
--   LET sig = sub_of_frst_diff(s, t) IN
--     Card(union(Vars(ext(sig)(s)), Vars(ext(sig)(t)))) <
--      Card(union(Vars(s), Vars(t)))
theorem vars_ext_sub_of_frst_diff_decrease : ∀ (s : term pvs_variable symbol NIL) (t : {t : term pvs_variable symbol NIL // unifiable s t & s ≠ t}), fun (sig : term pvs_variable symbol NIL → term pvs_variable symbol NIL) => Card (union (Vars (ext sig(s))) (Vars (ext sig(t)))) < Card (union (Vars s) (Vars t)) (sub_of_frst_diff s t) := by
  -- === TCCs for vars_ext_sub_of_frst_diff_decrease ===
  -- TCC: vars_ext_sub_of_frst_diff_decrease_TCC1
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | unifiable(s, t) & s /= t,
  --         sig: Sub[variable, symbol, arity]):
  --   sig = sub_of_frst_diff(s, t) IMPLIES
  --    ...
  have vars_ext_sub_of_frst_diff_decrease_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: vars_ext_sub_of_frst_diff_decrease_TCC2
  -- Statement: FORALL (s: term[variable, symbol, arity],
  --         t: term[variable, symbol, arity] | unifiable(s, t) & s /= t,
  --         sig: Sub[variable, symbol, arity]):
  --   sig = sub_of_frst_diff(s, t) IMPLIES
  --    ...
  have vars_ext_sub_of_frst_diff_decrease_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.unification_algorithm
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF s = t THEN identity
-- ELSE LET sig = sub_of_frst_diff(s, t) IN
--        comp(unification_algorithm(ext(sig)(s), ext(sig)(t)), sig)
-- ENDIF
axiom unification_algorithm {pvs_τ60 : Type*} {pvs_τ61 : Type*} : pvs_τ60 → pvs_τ61 → Prop

-- pvs2lean: unification.unification_algorithm_gives_unifier
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifiable(s, t) IMPLIES member(unification_algorithm(s, t), U(s, t))
theorem unification_algorithm_gives_unifier : unifiable s t → member (unification_algorithm s t) (U s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.unification_algorithm_gives_mg_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(rho, U(s, t)) IMPLIES unification_algorithm(s, t) <= rho
theorem unification_algorithm_gives_mg_subs : member rho (U s t) → unification_algorithm s t ≤ rho := by
  -- === TCCs for unification_algorithm_gives_mg_subs ===
  -- TCC: unification_algorithm_gives_mg_subs_TCC1
  -- Statement: FORALL (rho: Sub[variable, symbol, arity],
  --         s, t: term[variable, symbol, arity]):
  --   member(rho, U(s, t)) IMPLIES unifiable(s, t)
  have unification_algorithm_gives_mg_subs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: unification.unification
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifiable(s, t) => EXISTS theta: mgu(theta)(s, t)
theorem unification : unifiable s t → ∃ theta,  mgu theta(s, t) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.unification