-- Auto-generated by pvs2lean/translator.py
-- PVS theory: orthogonality_sets
-- PVSLib library: TRS
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: orthogonality_sets

Auto-generated stubs from PVSLib theory `orthogonality_sets`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.orthogonality_sets

noncomputable section

variable {pvs_τ1 : Type*} (Orthogonal_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} (PP_p : pvs_τ2 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (SPP_p : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (add_first : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (complement_pos : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} (confluent_p : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} (diamond_property_p : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (dominates_p : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (ext : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (intersection : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (is_countably_infinite : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (length : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (lhs : pvs_τ32 → pvs_τ33)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (member : pvs_τ34 → pvs_τ35 → pvs_τ36)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (parallel_reduction_fix_p : pvs_τ37 → pvs_τ38 → pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (parallel_reduction_p : pvs_τ41 → pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (positionsOF : pvs_τ44 → pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} (reduction_p : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (rhs : pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} (seq : pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (seq2set : pvs_τ50 → pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} (set2seq : pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} (set_of_prop_related_b : pvs_τ53 → pvs_τ54 → pvs_τ55 → pvs_τ56)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} (set_of_prop_related_nb : pvs_τ57 → pvs_τ58 → pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (subtermOF : pvs_τ61 → pvs_τ62 → Prop)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} (union : pvs_τ63 → pvs_τ64 → pvs_τ65)

-- pvs2lean: orthogonality_sets.var_countable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_countably_infinite(V)
theorem var_countable : is_countably_infinite V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.Pos_Over
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: set_of_prop_related_nb(<=)(fsp1, fsp2)
axiom Pos_Over {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ6 → pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: orthogonality_sets.Pos_Under
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: set_of_prop_related_b(<=)(fsp1, fsp2)
axiom Pos_Under {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11

-- pvs2lean: orthogonality_sets.Pos_Equal
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: intersection(fsp1, fsp2)
axiom Pos_Equal {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5

-- pvs2lean: orthogonality_sets.Pos_Over_character
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp2) AND Pos_Over(fsp1, fsp2)(p) IMPLIES
--  PP?(add_first(p, fsp2)) OR
--   EXISTS (i: below[fsp2`length]): p <= fsp2`seq(i) AND p /= fsp2`seq(i)
theorem Pos_Over_character : PP_p fsp2 ∧ Pos_Over fsp1 fsp2(p) → PP_p (add_first p fsp2) ∨
 ∃ (i : Fin fsp2.List.length), p ≤ fsp2.seq i ∧ p ≠ fsp2.seq i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.pos_up_in_Pos_Over
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp2) AND
--  p2`length /= 0 AND seq2set(fsp1)(p1) AND seq2set(fsp2)(p1 o p2)
--  IMPLIES Pos_Over(fsp1, fsp2)(p1)
theorem pos_up_in_Pos_Over : PP_p fsp2 ∧
 p2.List.length ≠ 0 ∧ seq2set fsp1(p1) ∧ seq2set fsp2(p1 ∘ p2) → Pos_Over fsp1 fsp2(p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.union_positions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: seq2set(fsp1) =
--  union(Pos_Over(fsp1, fsp2),
--        union(Pos_Equal(fsp1, fsp2), Pos_Under(fsp1, fsp2)))
theorem union_positions : seq2set fsp1 = union (Pos_Over fsp1 fsp2) (union (Pos_Equal fsp1 fsp2) (Pos_Under fsp1 fsp2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.Pos_Over_and_Pos_Equal_is_PP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) AND PP?(fsp2) IMPLIES
--  PP?(set2seq(union(Pos_Over(fsp1, fsp2),
--                    union(Pos_Over(fsp2, fsp1), Pos_Equal(fsp1, fsp2)))))
theorem Pos_Over_and_Pos_Equal_is_PP : PP_p fsp1 ∧ PP_p fsp2 → PP_p (set2seq (union (Pos_Over fsp1 fsp2) (union (Pos_Over fsp2 fsp1) (Pos_Equal fsp1 fsp2)))) := by
  -- === TCCs for Pos_Over_and_Pos_Equal_is_PP ===
  -- TCC: Pos_Over_and_Pos_Equal_is_PP_TCC1
  -- Statement: FORALL (fsp1, fsp2: finseq[position[variable, symbol, arity]]):
  --   PP?(fsp2) AND PP?(fsp1) IMPLIES
  --    is_finite[position[variable, symbol, arity]]
  --        (union[position[variable, symbol, arity]]
  --   ...
  have pos_over_and_pos_equal_is_pp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.Pos_Over_and_Pos_Equal_dominance
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: PP?(fsp1) AND PP?(fsp2) IMPLIES
--  dominates?(<=)
--            (union(Pos_Over(fsp1, fsp2),
--                   union(Pos_Over(fsp2, fsp1), Pos_Equal(fsp1, fsp2))),
--             seq2set(fsp1))
theorem Pos_Over_and_Pos_Equal_dominance : PP_p fsp1 ∧ PP_p fsp2 → dominates_p ≤
          (union (Pos_Over fsp1 fsp2) (union (Pos_Over fsp2 fsp1) (Pos_Equal fsp1 fsp2)),
           seq2set fsp1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.fork_in_Pos_Over_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SPP?(t)(fsp2) AND
--  parallel_reduction_fix?(E)(t, t2, fsp2) AND
--   positionsOF(t)(p) AND Pos_Over( #(p), fsp2)(p)
--  IMPLIES
--  parallel_reduction_fix?(E)
--                         (subtermOF(t, p), subtermOF
theorem fork_in_Pos_Over_aux : SPP_p t(fsp2) ∧
 parallel_reduction_fix_p E(t, t2, fsp2) ∧
  positionsOF t(p) ∧ Pos_Over #(p) fsp2(p) → parallel_reduction_fix_p E
                       (subtermOF t p, subtermOF t2 p,
                        complement_pos p fsp2) := by
  -- === TCCs for fork_in_Pos_Over_aux ===
  -- TCC: fork_in_Pos_Over_aux_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t2: term[variable, symbol, a...
  have fork_in_pos_over_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fork_in_Pos_Over_aux_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t2: term[variable, symbol, a...
  have fork_in_pos_over_aux_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.fork_in_Pos_Over
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      SPP?(t)(fsp1) AND SPP?(t)(fsp2)
--  AND parallel_reduction_fix?(E)(t, t1, fsp1)
--  AND parallel_reduction_fix?(E)(t, t2, fsp2) AND Pos_Over(fsp1, fsp2)(p)
--  IMPLIES
--  (EXISTS ((e: rewrite_rule[variable,
theorem fork_in_Pos_Over : SPP_p t(fsp1) ∧ SPP_p t(fsp2)
 ∧ parallel_reduction_fix_p E(t, t1, fsp1)
 ∧ parallel_reduction_fix_p E(t, t2, fsp2) ∧ Pos_Over fsp1 fsp2(p) → (∃ ((e : rewrite_rule[variable, symbol, arity] | member e E), sigma), subtermOF t p = ext sigma(lhs e) ∧
    subtermOF t1 p = ext sigma(rhs e) ∧
     parallel_reduction_p E(subtermOF t p, subtermOF t2 p)) := by
  -- === TCCs for fork_in_Pos_Over ===
  -- TCC: fork_in_Pos_Over_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t1, t2: term[variable,...
  have fork_in_pos_over_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fork_in_Pos_Over_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t1, t2: term[variable,...
  have fork_in_pos_over_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fork_in_Pos_Over_TCC3
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         p: position[variable, symbol, arity],
  --         t, t1, t2: term[variable,...
  have fork_in_pos_over_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.subterm_joinability
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem subterm_joinability : True := by
  -- === TCCs for subterm_joinability ===
  -- TCC: subterm_joinability_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        (fsp =
  --          ...
  have subterm_joinability_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_joinability_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        (fsp =
  --          ...
  have subterm_joinability_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_joinability_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reduction_fi...
  have subterm_joinability_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_joinability_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        (fsp =
  --          ...
  have subterm_joinability_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterm_joinability_TCC3
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        (fsp =
  --          ...
  have subterm_joinability_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: orthogonality_sets.subterms_joinability
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem subterms_joinability : True := by
  -- === TCCs for subterms_joinability ===
  -- TCC: subterms_joinability_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reducti...
  have subterms_joinability_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterms_joinability_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reducti...
  have subterms_joinability_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterms_joinability_TCC1
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reducti...
  have subterms_joinability_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subterms_joinability_TCC2
  -- Statement: FORALL (E: set[rewrite_rule[variable, symbol, arity]],
  --         fsp, fsp1, fsp2: finseq[position[variable, symbol, arity]],
  --         t, t1, t2: term[variable, symbol, arity]):
  --        parallel_reducti...
  have subterms_joinability_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: orthogonality_sets.parallel_reduction_has_DP
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Orthogonal?(E) IMPLIES diamond_property?(parallel_reduction?(E))
theorem parallel_reduction_has_DP : Orthogonal_p E → diamond_property_p (parallel_reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: orthogonality_sets.Orthogonal_implies_confluent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (E: Orthogonal): LET RRE = reduction?(E) IN confluent?(RRE)
theorem Orthogonal_implies_confluent : ∀ (E : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool), fun (RRE : term pvs_variable symbol NIL × term pvs_variable symbol NIL → Bool) => confluent_p RRE (reduction_p E) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.orthogonality_sets