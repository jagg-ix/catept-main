-- Auto-generated by pvs2lean/translator.py
-- PVS theory: epsilon_lemmas
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: epsilon_lemmas

Auto-generated stubs from PVSLib theory `epsilon_lemmas`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.epsilon_lemmas

noncomputable section

-- pvs2lean: epsilon_lemmas.prod_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x1 - y1) < e1 AND abs(x2 - y2) < e2 IMPLIES
--  abs(x1 * x2 - y1 * y2) < e1 * e2 + abs(y1) * e2 + abs(y2) * e1
theorem prod_bound : |x1 - y1| < e1 ∧ |x2 - y2| < e2 → |x1 * x2 - y1 * y2| < e1 * e2 + |y1| * e2 + |y2| * e1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: epsilon_lemmas.prod_epsilon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS e1, e2: e1 * e2 + abs(y1) * e2 + abs(y2) * e1 < e
theorem prod_epsilon : ∃ (e1 : {x : {x : {x : {x : ℝ // _} // _} // x ≥ 0} // x > 0}) (e2 : {x : {x : {x : {x : ℝ // _} // _} // x ≥ 0} // x > 0}), e1 * e2 + |y1| * e2 + |y2| * e1 < e := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: epsilon_lemmas.inv_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x1 - y1) < e1 AND e1 < abs(y1) AND x1 /= 0 AND y1 /= 0 IMPLIES
--  abs(1 / x1 - 1 / y1) < e1 / (abs(y1) * (abs(y1) - e1))
theorem inv_bound : |x1 - y1| < e1 ∧ e1 < |y1| ∧ x1 ≠ 0 ∧ y1 ≠ 0 → |1 / x1 - 1 / y1| < e1 / (|y1| * (|y1| - e1)) := by
  -- === TCCs for inv_bound ===
  -- TCC: inv_bound_TCC1
  -- Statement: FORALL (e1: posreal, x1, y1: real):
  --   y1 /= 0 AND x1 /= 0 AND e1 < abs(y1) AND abs(x1 - y1) < e1 IMPLIES
  --    (abs(y1) * (abs(y1) - e1)) /= 0
  have inv_bound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: epsilon_lemmas.inv_epsilon1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: y1 /= 0 IMPLIES EXISTS e1: e1 < abs(y1) AND e1 < e * (abs(y1) - e1)
theorem inv_epsilon1 : y1 ≠ 0 → ∃ e1,  e1 < |y1| ∧ e1 < e * (|y1| - e1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: epsilon_lemmas.inv_epsilon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: y1 /= 0 IMPLIES
--  EXISTS e1: e1 < abs(y1) AND e1 / (abs(y1) * (abs(y1) - e1)) < e
theorem inv_epsilon : y1 ≠ 0 → ∃ e1,  e1 < |y1| ∧ e1 / (|y1| * (|y1| - e1)) < e := by
  -- === TCCs for inv_epsilon ===
  -- TCC: inv_epsilon_TCC1
  -- Statement: FORALL (y1: real):
  --   y1 /= 0 IMPLIES
  --    FORALL (e1: posreal):
  --      e1 < abs(y1) IMPLIES (abs(y1) * (abs(y1) - e1)) /= 0
  have inv_epsilon_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: epsilon_lemmas.varying_epsilon
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: x <= y <=> (FORALL epsilon: x <= y + epsilon)
theorem varying_epsilon : x ≤ y <=> (∀ epsilon,  x ≤ y + epsilon) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.epsilon_lemmas