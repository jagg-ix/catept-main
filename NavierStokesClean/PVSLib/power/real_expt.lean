-- Auto-generated by pvs2lean/translator.py
-- PVS theory: real_expt
-- PVSLib library: power
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: real_expt

Auto-generated stubs from PVSLib theory `real_expt`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.real_expt

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (nnreal_expt : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (odd_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (root : pvs_τ5 → pvs_τ6 → pvs_τ7)

-- pvs2lean: real_expt.^
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF a >= 0 THEN nnreal_expt(x, a) ELSE nnreal_expt(1 / x, -a) ENDIF
noncomputable def pvs_op_7e6a2a : sorry := by
  sorry  -- pvs2lean: definition stub for IF a >= 0 THEN nnreal_expt(x, a) ELSE nnreal_expt(1 / x, -a) ENDIF

-- pvs2lean: real_expt.real_expt_nnreal_rew
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ y = nnreal_expt(x, y)
theorem real_expt_nnreal_rew : x ^ y = nnreal_expt x y := by
  -- === TCCs for real_expt_nnreal_rew ===
  -- TCC: real_expt_nnreal_rew_TCC1
  -- Statement: FORALL (x, y: nnreal): y >= 0 OR x > 0
  have real_expt_nnreal_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: real_expt.real_expt_int_rew
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: {i | x /= 0 OR i >= 0}): x ^ i = exponentiation.^(x, i)
theorem real_expt_int_rew : ∀ (i : {i : ℝ // x ≠ 0 ∨ i ≥ 0}), x ^ i = exponentiation.^(x, i) := by
  -- === TCCs for real_expt_int_rew ===
  -- TCC: real_expt_int_rew_TCC1
  -- Statement: FORALL (x: nnreal, i: {i: int | x /= 0 OR i >= 0}): i >= 0 OR x > 0
  have real_expt_int_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_root_rew
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ (1 / pn) = root(x, pn)
theorem real_expt_root_rew : x ^ (1 / pn) = root x pn := by
  -- === TCCs for real_expt_root_rew ===
  -- TCC: real_expt_root_rew_TCC1
  -- Statement: FORALL (pn: posnat, x: nnreal): (1 / pn) >= 0 OR x > 0
  have real_expt_root_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: real_expt_root_rew_TCC2
  -- Statement: FORALL (pn: posnat, x: nnreal): x >= 0 OR odd?(pn)
  have real_expt_root_rew_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_x0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ 0 = 1
theorem real_expt_x0 : x ^ 0 = 1 := by
  -- === TCCs for real_expt_x0 ===
  -- TCC: real_expt_x0_TCC1
  -- Statement: FORALL (x: nnreal): 0 >= 0 OR x > 0
  have real_expt_x0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_1a
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 ^ a = 1
theorem real_expt_1a : 1 ^ a = 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_0x
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 ^ x = IF x = 0 THEN 1 ELSE 0 ENDIF
theorem real_expt_0x : 0 ^ x = if x = 0 then 1 else 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_x1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ 1 = x
theorem real_expt_x1 : x ^ 1 = x := by
  -- === TCCs for real_expt_x1 ===
  -- TCC: real_expt_x1_TCC1
  -- Statement: FORALL (x: nnreal): 1 >= 0 OR x > 0
  have real_expt_x1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: px ^ a > 0
theorem real_expt_pos : px ^ a > 0 := by
  -- === TCCs for real_expt_pos ===
  -- TCC: real_expt_pos_TCC1
  -- Statement: FORALL (a: real, px: posreal): a >= 0 OR px > 0
  have real_expt_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_is_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, a: {a | a >= 0 OR x > 0}): x ^ a = 0 IFF x = 0 AND a > 0
theorem real_expt_is_0 : ∀ (x : {x : ℝ // x ≥ 0}) (a : {a : ℝ // a ≥ 0 ∨ x > 0}), x ^ a = 0 ↔ x = 0 ∧ a > 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_gt1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, a: {a | a >= 0 OR x > 0}):
--   x ^ a > 1 IFF (x > 1 AND a > 0 OR x < 1 AND a < 0)
theorem real_expt_gt1 : ∀ (x : {x : ℝ // x ≥ 0}) (a : {a : ℝ // a ≥ 0 ∨ x > 0}), x ^ a > 1 ↔ (x > 1 ∧ a > 0 ∨ x < 1 ∧ a < 0) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_lt1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, a: {a | a >= 0 OR x > 0}):
--   x ^ a < 1 IFF (x < 1 AND a > 0 OR x > 1 AND a < 0)
theorem real_expt_lt1 : ∀ (x : {x : ℝ // x ≥ 0}) (a : {a : ℝ // a ≥ 0 ∨ x > 0}), x ^ a < 1 ↔ (x < 1 ∧ a > 0 ∨ x > 1 ∧ a < 0) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.inv_real_expt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (1 / px) ^ a = 1 / (px ^ a)
theorem inv_real_expt : (1 / px) ^ a = 1 / (px ^ a) := by
  -- === TCCs for inv_real_expt ===
  -- TCC: inv_real_expt_TCC1
  -- Statement: FORALL (a: real, px: posreal): a >= 0 OR (1 / px) > 0
  have inv_real_expt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inv_real_expt_TCC2
  -- Statement: FORALL (a: real, px: posreal): (px ^ a) /= 0
  have inv_real_expt_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.mult_real_expt
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: nnreal, a: {a | a >= 0 OR x > 0 AND y > 0}):
--   (x * y) ^ a = x ^ a * y ^ a
theorem mult_real_expt : ∀ (x : {x : ℝ // x ≥ 0}) (y : {x : ℝ // x ≥ 0}) (a : {a : ℝ // a ≥ 0 ∨ x > 0 ∧ y > 0}), (x * y) ^ a = x ^ a * y ^ a := by
  -- === TCCs for mult_real_expt ===
  -- TCC: mult_real_expt_TCC1
  -- Statement: FORALL (x, y: nnreal, a: {a: real | a >= 0 OR x > 0 AND y > 0}):
  --   a >= 0 OR (x * y) > 0
  have mult_real_expt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_real_expt_TCC2
  -- Statement: FORALL (x, y: nnreal, a: {a: real | a >= 0 OR x > 0 AND y > 0}):
  --   a >= 0 OR x > 0
  have mult_real_expt_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_real_expt_TCC3
  -- Statement: FORALL (x, y: nnreal, a: {a: real | a >= 0 OR x > 0 AND y > 0}):
  --   a >= 0 OR y > 0
  have mult_real_expt_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: real_expt.div_real_expt
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, py: posreal, a: {a | a >= 0 OR x > 0}):
--   (x / py) ^ a = x ^ a / py ^ a
theorem div_real_expt : ∀ (x : {x : ℝ // x ≥ 0}) (py : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {a : ℝ // a ≥ 0 ∨ x > 0}), (x / py) ^ a = x ^ a / py ^ a := by
  -- === TCCs for div_real_expt ===
  -- TCC: div_real_expt_TCC1
  -- Statement: FORALL (x: nnreal, py: posreal, a: {a | a >= 0 OR x > 0}):
  --   a >= 0 OR (x / py) > 0
  have div_real_expt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px ^ (-a) = (1 / px) ^ a
theorem real_expt_neg : px ^ (-a) = (1 / px) ^ a := by
  -- === TCCs for real_expt_neg ===
  -- TCC: real_expt_neg_TCC1
  -- Statement: FORALL (a: real, px: posreal): (-a) >= 0 OR px > 0
  have real_expt_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_plus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, a, b: {a | a >= 0 OR x > 0}): x ^ (a + b) = x ^ a * x ^ b
theorem real_expt_plus : ∀ (x : {x : ℝ // x ≥ 0}) (a : {a : ℝ // a ≥ 0 ∨ x > 0}) (b : {a : ℝ // a ≥ 0 ∨ x > 0}), x ^ (a + b) = x ^ a * x ^ b := by
  -- === TCCs for real_expt_plus ===
  -- TCC: real_expt_plus_TCC1
  -- Statement: FORALL (x: nnreal, a, b: {a | a >= 0 OR x > 0}): (a + b) >= 0 OR x > 0
  have real_expt_plus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: real_expt.real_expt_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px ^ (a - b) = px ^ a / px ^ b
theorem real_expt_minus : px ^ (a - b) = px ^ a / px ^ b := by
  -- === TCCs for real_expt_minus ===
  -- TCC: real_expt_minus_TCC1
  -- Statement: FORALL (a, b: real, px: posreal): (a - b) >= 0 OR px > 0
  have real_expt_minus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_times
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, a: {a | a >= 0 OR x > 0}, b: {b | b >= 0 OR x ^ a > 0}):
--   x ^ (a * b) = (x ^ a) ^ b
theorem real_expt_times : ∀ (x : {x : ℝ // x ≥ 0}) (a : {a : ℝ // a ≥ 0 ∨ x > 0}) (b : {b : ℝ // b ≥ 0 ∨ x ^ a > 0}), x ^ (a * b) = (x ^ a) ^ b := by
  -- === TCCs for real_expt_times ===
  -- TCC: real_expt_times_TCC1
  -- Statement: FORALL (x: nnreal, a: {a | a >= 0 OR x > 0}, b: {b | b >= 0 OR x ^ a > 0}):
  --   (a * b) >= 0 OR x > 0
  have real_expt_times_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_decreasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b => lt1x ^ a > lt1x ^ b
theorem real_expt_decreasing : a < b → lt1x ^ a > lt1x ^ b := by
  -- === TCCs for real_expt_decreasing ===
  -- TCC: real_expt_decreasing_TCC1
  -- Statement: FORALL (a, b: real, lt1x: {r | r < 1}): a < b IMPLIES a >= 0 OR lt1x > 0
  have real_expt_decreasing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: real_expt_decreasing_TCC2
  -- Statement: FORALL (a, b: real, lt1x: {r | r < 1}): a < b IMPLIES b >= 0 OR lt1x > 0
  have real_expt_decreasing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b => gt1x ^ a < gt1x ^ b
theorem real_expt_increasing : a < b → gt1x ^ a < gt1x ^ b := by
  -- === TCCs for real_expt_increasing ===
  -- TCC: real_expt_increasing_TCC1
  -- Statement: FORALL (a, b: real, gt1x: {r | r > 1}): a < b IMPLIES a >= 0 OR gt1x > 0
  have real_expt_increasing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: real_expt_increasing_TCC2
  -- Statement: FORALL (a, b: real, gt1x: {r | r > 1}): a < b IMPLIES b >= 0 OR gt1x > 0
  have real_expt_increasing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_strict_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x < y => x ^ pa < y ^ pa
theorem real_expt_strict_increasing : x < y → x ^ pa < y ^ pa := by
  -- === TCCs for real_expt_strict_increasing ===
  -- TCC: real_expt_strict_increasing_TCC1
  -- Statement: FORALL (pa: posreal, x, y: nnreal): x < y IMPLIES pa >= 0 OR x > 0
  have real_expt_strict_increasing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: real_expt_strict_increasing_TCC2
  -- Statement: FORALL (pa: posreal, x, y: nnreal): x < y IMPLIES pa >= 0 OR y > 0
  have real_expt_strict_increasing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.real_expt_strict_decreasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px < y => px ^ na > y ^ na
theorem real_expt_strict_decreasing : px < y → px ^ na > y ^ na := by
  -- === TCCs for real_expt_strict_decreasing ===
  -- TCC: real_expt_strict_decreasing_TCC1
  -- Statement: FORALL (na: negreal, px: posreal, y: nnreal):
  --   px < y IMPLIES na >= 0 OR px > 0
  have real_expt_strict_decreasing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: real_expt_strict_decreasing_TCC2
  -- Statement: FORALL (na: negreal, px: posreal, y: nnreal):
  --   px < y IMPLIES na >= 0 OR y > 0
  have real_expt_strict_decreasing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ pa < y ^ pa IFF x < y
theorem both_sides_real_expt_lt : x ^ pa < y ^ pa ↔ x < y := by
  -- === TCCs for both_sides_real_expt_lt ===
  -- TCC: both_sides_real_expt_lt_TCC1
  -- Statement: FORALL (pa: posreal, x: nnreal): pa >= 0 OR x > 0
  have both_sides_real_expt_lt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_le
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ pa <= y ^ pa IFF x <= y
theorem both_sides_real_expt_le : x ^ pa ≤ y ^ pa ↔ x ≤ y := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_gt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ pa > y ^ pa IFF x > y
theorem both_sides_real_expt_gt : x ^ pa > y ^ pa ↔ x > y := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ pa >= y ^ pa IFF x >= y
theorem both_sides_real_expt_ge : x ^ pa ≥ y ^ pa ↔ x ≥ y := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ pa = y ^ pa IFF x = y
theorem both_sides_real_expt : x ^ pa = y ^ pa ↔ x = y := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_lt1_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: lt1x ^ a < lt1x ^ b IFF b < a
theorem both_sides_real_expt_lt1_lt : lt1x ^ a < lt1x ^ b ↔ b < a := by
  -- === TCCs for both_sides_real_expt_lt1_lt ===
  -- TCC: both_sides_real_expt_lt1_lt_TCC1
  -- Statement: FORALL (a: real, lt1x: {r | r < 1}): a >= 0 OR lt1x > 0
  have both_sides_real_expt_lt1_lt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_lt1_le
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: lt1x ^ a <= lt1x ^ b IFF b <= a
theorem both_sides_real_expt_lt1_le : lt1x ^ a ≤ lt1x ^ b ↔ b ≤ a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_lt1_gt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: lt1x ^ a > lt1x ^ b IFF b > a
theorem both_sides_real_expt_lt1_gt : lt1x ^ a > lt1x ^ b ↔ b > a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_lt1_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: lt1x ^ a >= lt1x ^ b IFF b >= a
theorem both_sides_real_expt_lt1_ge : lt1x ^ a ≥ lt1x ^ b ↔ b ≥ a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_gt1_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: gt1x ^ a < gt1x ^ b IFF a < b
theorem both_sides_real_expt_gt1_lt : gt1x ^ a < gt1x ^ b ↔ a < b := by
  -- === TCCs for both_sides_real_expt_gt1_lt ===
  -- TCC: both_sides_real_expt_gt1_lt_TCC1
  -- Statement: FORALL (a: real, gt1x: {r | r > 1}): a >= 0 OR gt1x > 0
  have both_sides_real_expt_gt1_lt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_gt1_le
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: gt1x ^ a <= gt1x ^ b IFF a <= b
theorem both_sides_real_expt_gt1_le : gt1x ^ a ≤ gt1x ^ b ↔ a ≤ b := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_gt1_gt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: gt1x ^ a > gt1x ^ b IFF a > b
theorem both_sides_real_expt_gt1_gt : gt1x ^ a > gt1x ^ b ↔ a > b := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_gt1_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: gt1x ^ a >= gt1x ^ b IFF a >= b
theorem both_sides_real_expt_gt1_ge : gt1x ^ a ≥ gt1x ^ b ↔ a ≥ b := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: real_expt.both_sides_real_expt_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ pa = x ^ pb IFF x = 0 OR x = 1 OR pa = pb
theorem both_sides_real_expt_eq : x ^ pa = x ^ pb ↔ x = 0 ∨ x = 1 ∨ pa = pb := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.real_expt