-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nn_log
-- PVSLib library: power
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nn_log

Auto-generated stubs from PVSLib theory `nn_log`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nn_log

noncomputable section

variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (nnreal_expt : pvs_τ10 → pvs_τ11 → Prop)

-- pvs2lean: nn_log.log_gt1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inverse[nnreal, {r | r >= 1}](LAMBDA x: nnreal_expt(gt1x, x))
axiom log_gt1 {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3

-- pvs2lean: nn_log.log_lt1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inverse[nnreal, {r | r <= 1}](LAMBDA x: nnreal_expt(lt1x, x))
axiom log_lt1 {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6

-- pvs2lean: nn_log.nn_log
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF ne1x > 1 THEN log_gt1(ne1x)(y) ELSE log_lt1(ne1x)(y) ENDIF
axiom nn_log {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9

-- pvs2lean: nn_log.nn_log_expt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nn_log(ne1x)(nnreal_expt(ne1x, y)) = y
theorem nn_log_expt : nn_log ne1x(nnreal_expt ne1x y) = y := by
  -- === TCCs for nn_log_expt ===
  -- TCC: nn_log_expt_TCC1
  -- Statement: FORALL (ne1x: {r | r /= 1}, y: nnreal):
  --   nnreal_expt(ne1x, y) > 0 AND
  --    (ne1x > 1 AND nnreal_expt(ne1x, y) >= 1 OR
  --      ne1x < 1 AND nnreal_expt(ne1x, y) <= 1)
  have nn_log_expt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nn_log.nn_expt_log
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (y: {r | ne1x > 1 AND r >= 1 OR ne1x < 1 AND r <= 1}):
--   nnreal_expt(ne1x, nn_log(ne1x)(y)) = y
theorem nn_expt_log : ∀ (y : {r : {x : {x : ℝ // x ≥ 0} // x > 0} // ne1x > 1 ∧ r ≥ 1 ∨ ne1x < 1 ∧ r ≤ 1}), nnreal_expt ne1x (nn_log ne1x(y)) = y := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nn_log.nn_log_plus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: {r | ne1x > 1 AND r >= 1 OR ne1x < 1 AND r <= 1}):
--   nn_log(ne1x)(x * y) = nn_log(ne1x)(x) + nn_log(ne1x)(y)
theorem nn_log_plus : ∀ (x : {r : {x : {x : ℝ // x ≥ 0} // x > 0} // ne1x > 1 ∧ r ≥ 1 ∨ ne1x < 1 ∧ r ≤ 1}) (y : {r : {x : {x : ℝ // x ≥ 0} // x > 0} // ne1x > 1 ∧ r ≥ 1 ∨ ne1x < 1 ∧ r ≤ 1}), nn_log ne1x(x * y) = nn_log ne1x(x) + nn_log ne1x(y) := by
  -- === TCCs for nn_log_plus ===
  -- TCC: nn_log_plus_TCC1
  -- Statement: FORALL (ne1x: {r | r /= 1},
  --         x, y: {r: posreal | ne1x > 1 AND r >= 1 OR ne1x < 1 AND r <= 1}):
  --   ne1x > 1 AND x * y >= 1 OR ne1x < 1 AND x * y <= 1
  have nn_log_plus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nn_log.nn_log_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nn_log(ne1x)(1) = 0
theorem nn_log_1 : nn_log ne1x(1) = 0 := by
  -- === TCCs for nn_log_1 ===
  -- TCC: nn_log_1_TCC1
  -- Statement: FORALL (ne1x: {r | r /= 1}): ne1x > 1 AND 1 >= 1 OR ne1x < 1 AND 1 <= 1
  have nn_log_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nn_log.nn_log_ne1x
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nn_log(ne1x)(ne1x) = 1
theorem nn_log_ne1x : nn_log ne1x(ne1x) = 1 := by
  -- === TCCs for nn_log_ne1x ===
  -- TCC: nn_log_ne1x_TCC1
  -- Statement: FORALL (ne1x: {r | r /= 1}):
  --   ne1x > 1 AND ne1x >= 1 OR ne1x < 1 AND ne1x <= 1
  have nn_log_ne1x_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nn_log.nn_log_gt1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nn_log(gt1x)(gt1y) > 1 IFF gt1y > gt1x
theorem nn_log_gt1 : nn_log gt1x(gt1y) > 1 ↔ gt1y > gt1x := by
  -- === TCCs for nn_log_gt1 ===
  -- TCC: nn_log_gt1_TCC1
  -- Statement: FORALL (gt1x, gt1y: {r | r > 1}):
  --   gt1x > 1 AND gt1y >= 1 OR gt1x < 1 AND gt1y <= 1
  have nn_log_gt1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nn_log_gt1_TCC2
  -- Statement: FORALL (gt1x: {r | r > 1}): gt1x /= 1
  have nn_log_gt1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nn_log.nn_inv_base_log
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (y: {r | ne1x > 1 AND r <= 1 OR ne1x < 1 AND r >= 1}):
--   nn_log(1 / ne1x)(y) = nn_log(ne1x)(1 / y)
theorem nn_inv_base_log : ∀ (y : {r : {x : {x : ℝ // x ≥ 0} // x > 0} // ne1x > 1 ∧ r ≤ 1 ∨ ne1x < 1 ∧ r ≥ 1}), nn_log (1 / ne1x) (y) = nn_log ne1x(1 / y) := by
  -- === TCCs for nn_inv_base_log ===
  -- TCC: nn_inv_base_log_TCC1
  -- Statement: FORALL (ne1x: {r | r /= 1},
  --         y: {r: posreal | ne1x > 1 AND r <= 1 OR ne1x < 1 AND r >= 1}):
  --   1 / ne1x > 1 AND y >= 1 OR 1 / ne1x < 1 AND y <= 1
  have nn_inv_base_log_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nn_inv_base_log_TCC2
  -- Statement: FORALL (ne1x: {r | r /= 1},
  --         y: {r: posreal | ne1x > 1 AND r <= 1 OR ne1x < 1 AND r >= 1}):
  --   1 / ne1x /= 1
  have nn_inv_base_log_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nn_inv_base_log_TCC3
  -- Statement: FORALL (ne1x: {r | r /= 1},
  --         y: {r: posreal | ne1x > 1 AND r <= 1 OR ne1x < 1 AND r >= 1}):
  --   ne1x > 1 AND 1 / y >= 1 OR ne1x < 1 AND 1 / y <= 1
  have nn_inv_base_log_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nn_log.nn_log_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 < x AND x < y => nn_log(gt1x)(x) < nn_log(gt1x)(y)
theorem nn_log_increasing : 1 < x ∧ x < y → nn_log gt1x(x) < nn_log gt1x(y) := by
  -- === TCCs for nn_log_increasing ===
  -- TCC: nn_log_increasing_TCC1
  -- Statement: FORALL (gt1x: {r | r > 1}, x, y: nnreal):
  --   x < y AND 1 < x IMPLIES
  --    x > 0 AND (gt1x > 1 AND x >= 1 OR gt1x < 1 AND x <= 1)
  have nn_log_increasing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nn_log_increasing_TCC2
  -- Statement: FORALL (gt1x: {r | r > 1}, x, y: nnreal):
  --   x < y AND 1 < x IMPLIES
  --    y > 0 AND (gt1x > 1 AND y >= 1 OR gt1x < 1 AND y <= 1)
  have nn_log_increasing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.nn_log