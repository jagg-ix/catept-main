-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ln_exp_def
-- PVSLib library: power
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ln_exp_def

Auto-generated stubs from PVSLib theory `ln_exp_def`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ln_exp_def

noncomputable section

variable {pvs_τ1 : Type*} (continuous_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (exp : pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (ln : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (log : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (nn_log : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (nn_rational_expt : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (nnreal_expt : pvs_τ14 → pvs_τ15 → pvs_τ16)

-- pvs2lean: ln_exp_def.nn_rational_expt_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nn_rational_expt(y, q) = exp(q * ln(y))
theorem nn_rational_expt_def : nn_rational_expt y q = exp (q * ln y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_def.nnreal_expt_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nnreal_expt(y, z) = exp(z * ln(y))
theorem nnreal_expt_def : nnreal_expt y z = exp (z * ln y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_def.real_expt_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: y ^ x = exp(x * ln(y))
theorem real_expt_def : y ^ x = exp (x * ln y) := by
  -- === TCCs for real_expt_def ===
  -- TCC: real_expt_def_TCC1
  -- Statement: FORALL (x: real, y: posreal): x >= 0 OR y > 0
  have real_expt_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_def.nn_log_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: r > 1 AND y >= 1 OR r < 1 AND y <= 1 => nn_log(r)(y) = ln(y) / ln(r)
theorem nn_log_def : r > 1 ∧ y ≥ 1 ∨ r < 1 ∧ y ≤ 1 → nn_log r(y) = ln y / ln r := by
  -- === TCCs for nn_log_def ===
  -- TCC: nn_log_def_TCC1
  -- Statement: FORALL (r, y: posreal): r > 1 AND y >= 1 OR r < 1 AND y <= 1 IMPLIES r /= 1
  have nn_log_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nn_log_def_TCC2
  -- Statement: FORALL (r, y: posreal):
  --   r > 1 AND y >= 1 OR r < 1 AND y <= 1 IMPLIES ln(r) /= 0
  have nn_log_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_def.log_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: log(ne1x, y) = ln(y) / ln(ne1x)
theorem log_def : log ne1x y = ln y / ln ne1x := by
  -- === TCCs for log_def ===
  -- TCC: log_def_TCC1
  -- Statement: FORALL (ne1x: {r | r /= 1}): ln(ne1x) /= 0
  have log_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ln_exp_def.log_continuous
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: continuous?[posreal](log(ne1x))
theorem log_continuous : continuous_p (log ne1x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_def.log_derivable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[posreal](log(ne1x)) AND
--  deriv[posreal](log(ne1x)) = (LAMBDA y: 1 / (ln(ne1x) * y))
theorem log_derivable : derivable_p (log ne1x) ∧ deriv (log ne1x) = (fun y=>  1 / (ln ne1x * y)) := by
  -- === TCCs for log_derivable ===
  -- TCC: log_derivable_TCC1
  -- Statement: deriv_domain?[posreal]
  have log_derivable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: log_derivable_TCC2
  -- Statement: not_one_element?[posreal]
  have log_derivable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: log_derivable_TCC3
  -- Statement: FORALL (ne1x: {r | r /= 1}):
  --   derivable?[posreal](log(ne1x)) IMPLIES
  --    FORALL (y: posreal): (ln(ne1x) * y) /= 0
  have log_derivable_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_def.real_expt_continuous
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: continuous?[posreal](LAMBDA y: y ^ x)
theorem real_expt_continuous : continuous_p (fun (y : {x : {x : ℝ // x ≥ 0} // x > 0}) => y ^ x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ln_exp_def.real_expt_derivable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[posreal](LAMBDA y: y ^ x) AND
--  deriv[posreal](LAMBDA y: y ^ x) = (LAMBDA y: x * y ^ (x - 1))
theorem real_expt_derivable : derivable_p (fun y=>  y ^ x) ∧ deriv (fun y=>  y ^ x) = (fun y=>  x * y ^ (x - 1)) := by
  -- === TCCs for real_expt_derivable ===
  -- TCC: real_expt_derivable_TCC1
  -- Statement: FORALL (x: real):
  --   derivable?[posreal](LAMBDA (y: posreal): y ^ x) IMPLIES
  --    FORALL (y: posreal): (x - 1) >= 0 OR y > 0
  have real_expt_derivable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.ln_exp_def