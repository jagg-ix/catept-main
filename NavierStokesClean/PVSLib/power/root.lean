-- Auto-generated by pvs2lean/translator.py
-- PVS theory: root
-- PVSLib library: power
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: root

Auto-generated stubs from PVSLib theory `root`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.root

noncomputable section

variable {pvs_τ1 : Type*} (exp : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (expt : pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (ln : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (nn_root : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (odd_p : pvs_τ10 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (root_real : pvs_τ19 → pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (sign : pvs_τ22 → pvs_τ23)

-- pvs2lean: root.expt_exp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: expt(py, n) = exp(n * ln(py))
theorem expt_exp : expt py n = exp (n * ln py) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: root.nn_root_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nn_root(nx, pn) = IF nx = 0 THEN 0 ELSE exp(ln(nx) / pn) ENDIF
theorem nn_root_def : nn_root nx pn = if nx = 0 then 0 else exp (ln nx / pn) := by
  -- === TCCs for nn_root_def ===
  -- TCC: nn_root_def_TCC1
  -- Statement: FORALL (nx: nnreal): NOT nx = 0 IMPLIES nx > 0
  have nn_root_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_nn
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF nx = 0 THEN 0 ELSE exp(ln(nx) / pn) ENDIF
axiom root_nn {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ14 → pvs_τ15 → Prop

-- pvs2lean: root.root_nn_root_pos_rew
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: root_nn(nx, pn) = root_pos(nx, pn)
theorem root_nn_root_pos_rew : root_nn nx pn = root_pos nx pn := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF pn = 1 THEN x
-- ELSIF x >= 0 THEN root_nn(x, pn)
-- ELSE -root_nn(-x, pn)
-- ENDIF
axiom root {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13

-- pvs2lean: root.root_root_nn_rew
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: root(nx, pn) = root_nn(nx, pn)
theorem root_root_nn_rew : root nx pn = root_nn nx pn := by
  -- === TCCs for root_root_nn_rew ===
  -- TCC: root_root_nn_rew_TCC1
  -- Statement: FORALL (nx: nnreal, pn: posnat): nx >= 0 OR odd?(pn)
  have root_root_nn_rew_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_0n
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: root(0, pn) = 0
theorem root_0n : root 0 pn = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: root.root_1n
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: root(1, pn) = 1
theorem root_1n : root 1 pn = 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: root.root_x1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: root(x, 1) = x
theorem root_x1 : root x 1 = x := by
  -- === TCCs for root_x1 ===
  -- TCC: root_x1_TCC1
  -- Statement: FORALL (x: real): x >= 0 OR odd?(1)
  have root_x1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: root.root_nn_root_rew
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: root(nx, pn) = nn_root(nx, pn)
theorem root_nn_root_rew : root nx pn = nn_root nx pn := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_expt
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): root(x ^ pn, pn) = x
theorem root_expt : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), root (x ^ pn) pn = x := by
  -- === TCCs for root_expt ===
  -- TCC: root_expt_TCC1
  -- Statement: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): x /= 0 OR pn >= 0
  have root_expt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: root_expt_TCC2
  -- Statement: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): x ^ pn >= 0 OR odd?(pn)
  have root_expt_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.expt_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): root(x, pn) ^ pn = x
theorem expt_root : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), root x pn ^ pn = x := by
  -- === TCCs for expt_root ===
  -- TCC: expt_root_TCC1
  -- Statement: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): root(x, pn) /= 0 OR pn >= 0
  have expt_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_is_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): root(x, pn) = 0 IFF x = 0
theorem root_is_0 : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), root x pn = 0 ↔ x = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): root(x, pn) > 0 IFF x > 0
theorem root_pos : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), root x pn > 0 ↔ x > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): root(x, pn) < 0 IFF x < 0
theorem root_neg : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), root x pn < 0 ↔ x < 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_gt1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): root(x, pn) > 1 IFF x > 1
theorem root_gt1 : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), root x pn > 1 ↔ x > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_lt1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}): root(x, pn) < 1 IFF x < 1
theorem root_lt1 : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), root x pn < 1 ↔ x < 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.neg_root
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: odd?(pn) => root(-x, pn) = -root(x, pn)
theorem neg_root : odd_p pn → root -x pn = -root x pn := by
  -- === TCCs for neg_root ===
  -- TCC: neg_root_TCC1
  -- Statement: FORALL (pn: posnat, x: real): odd?(pn) IMPLIES -x >= 0 OR odd?(pn)
  have neg_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: neg_root_TCC2
  -- Statement: FORALL (pn: posnat, x: real): odd?(pn) IMPLIES x >= 0 OR odd?(pn)
  have neg_root_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.mult_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: real, pn: {pm | x >= 0 AND y >= 0 OR odd?(pm)}):
--   root(x * y, pn) = root(x, pn) * root(y, pn)
theorem mult_root : ∀ (x : ℝ) (y : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∧ y ≥ 0 ∨ odd_p pm}), root (x * y) pn = root x pn * root y pn := by
  -- === TCCs for mult_root ===
  -- TCC: mult_root_TCC1
  -- Statement: FORALL (x, y: real, pn: {pm: posnat | x >= 0 AND y >= 0 OR odd?(pm)}):
  --   x * y >= 0 OR odd?(pn)
  have mult_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_root_TCC2
  -- Statement: FORALL (x, y: real, pn: {pm: posnat | x >= 0 AND y >= 0 OR odd?(pm)}):
  --   x >= 0 OR odd?(pn)
  have mult_root_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_root_TCC3
  -- Statement: FORALL (x, y: real, pn: {pm: posnat | x >= 0 AND y >= 0 OR odd?(pm)}):
  --   y >= 0 OR odd?(pn)
  have mult_root_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.inv_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n0x: nzreal, pn: {pm | n0x >= 0 OR odd?(pm)}):
--   root(1 / n0x, pn) = 1 / root(n0x, pn)
theorem inv_root : ∀ (n0x : {r : ℝ // r ≠ 0}) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // n0x ≥ 0 ∨ odd_p pm}), root (1 / n0x) pn = 1 / root n0x pn := by
  -- === TCCs for inv_root ===
  -- TCC: inv_root_TCC1
  -- Statement: FORALL (n0x: nzreal, pn: {pm | n0x >= 0 OR odd?(pm)}):
  --   1 / n0x >= 0 OR odd?(pn)
  have inv_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inv_root_TCC2
  -- Statement: FORALL (n0x: nzreal, pn: {pm | n0x >= 0 OR odd?(pm)}): root(n0x, pn) /= 0
  have inv_root_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.div_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, n0y: nzreal, pn: {pm | x >= 0 AND n0y >= 0 OR odd?(pm)}):
--   root(x / n0y, pn) = root(x, pn) / root(n0y, pn)
theorem div_root : ∀ (x : ℝ) (n0y : {r : ℝ // r ≠ 0}) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∧ n0y ≥ 0 ∨ odd_p pm}), root (x / n0y) pn = root x pn / root n0y pn := by
  -- === TCCs for div_root ===
  -- TCC: div_root_TCC1
  -- Statement: FORALL (x: real, n0y: nzreal, pn: {pm | x >= 0 AND n0y >= 0 OR odd?(pm)}):
  --   x / n0y >= 0 OR odd?(pn)
  have div_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: div_root_TCC2
  -- Statement: FORALL (x: real, n0y: nzreal, pn: {pm | x >= 0 AND n0y >= 0 OR odd?(pm)}):
  --   x >= 0 OR odd?(pn)
  have div_root_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: div_root_TCC3
  -- Statement: FORALL (x: real, n0y: nzreal, pn: {pm | x >= 0 AND n0y >= 0 OR odd?(pm)}):
  --   n0y >= 0 OR odd?(pn)
  have div_root_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: div_root_TCC4
  -- Statement: FORALL (x: real, n0y: nzreal, pn: {pm | x >= 0 AND n0y >= 0 OR odd?(pm)}):
  --   root(n0y, pn) /= 0
  have div_root_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_mult
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pm, pn: {i: posnat | x >= 0 OR odd?(i)}):
--   root(x, pn * pm) = root(root(x, pn), pm)
theorem root_mult : ∀ (x : ℝ) (pm : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p i}) (pn : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p i}), root x (pn * pm) = root (root x pn) pm := by
  -- === TCCs for root_mult ===
  -- TCC: root_mult_TCC1
  -- Statement: FORALL (x: real, pm, pn: {i: posnat | x >= 0 OR odd?(i)}):
  --   x >= 0 OR odd?(pn * pm)
  have root_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: root_mult_TCC2
  -- Statement: FORALL (x: real, pm, pn: {i: posnat | x >= 0 OR odd?(i)}):
  --   root(x, pn) >= 0 OR odd?(pm)
  have root_mult_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_increasing
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: ((0 < x AND x < 1) OR (odd?(pn) AND odd?(pm) AND x < -1)) AND pn < pm =>
--  root(x, pn) < root(x, pm)
theorem root_increasing : ((0 < x ∧ x < 1) ∨ (odd_p pn ∧ odd_p pm ∧ x < -1)) ∧ pn < pm → root x pn < root x pm := by
  -- === TCCs for root_increasing ===
  -- TCC: root_increasing_TCC1
  -- Statement: FORALL (pm, pn: posnat, x: real):
  --   pn < pm AND ((0 < x AND x < 1) OR (odd?(pn) AND odd?(pm) AND x < -1))
  --    IMPLIES x >= 0 OR odd?(pn)
  have root_increasing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: root_increasing_TCC2
  -- Statement: FORALL (pm, pn: posnat, x: real):
  --   pn < pm AND ((0 < x AND x < 1) OR (odd?(pn) AND odd?(pm) AND x < -1))
  --    IMPLIES x >= 0 OR odd?(pm)
  have root_increasing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_decreasing
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (1 < x OR (odd?(pn) AND odd?(pm) AND -1 < x AND x < 0)) AND pn < pm =>
--  root(x, pn) > root(x, pm)
theorem root_decreasing : (1 < x ∨ (odd_p pn ∧ odd_p pm ∧ -1 < x ∧ x < 0)) ∧ pn < pm → root x pn > root x pm := by
  -- === TCCs for root_decreasing ===
  -- TCC: root_decreasing_TCC1
  -- Statement: FORALL (pm, pn: posnat, x: real):
  --   pn < pm AND (1 < x OR (odd?(pn) AND odd?(pm) AND -1 < x AND x < 0))
  --    IMPLIES x >= 0 OR odd?(pn)
  have root_decreasing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: root_decreasing_TCC2
  -- Statement: FORALL (pm, pn: posnat, x: real):
  --   pn < pm AND (1 < x OR (odd?(pn) AND odd?(pm) AND -1 < x AND x < 0))
  --    IMPLIES x >= 0 OR odd?(pm)
  have root_decreasing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.continuous_alt_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, epsilon: posreal,
--         pn: {pm | x >= 0 AND y >= 0 OR odd?(pm)}):
--   EXISTS delta:
--     FORALL (y: {z | z >= 0 OR odd?(pn)}):
--       abs(x - y) < delta => abs(root(x, pn) - root(y, pn
theorem continuous_alt_root : ∀ (x : ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∧ y ≥ 0 ∨ odd_p pm}), ∃ (delta : {x : {x : ℝ // x ≥ 0} // x > 0}), ∀ (y : {z : ℝ // z ≥ 0 ∨ odd_p pn}), |x - y| < delta → |root x pn - root y pn| < epsilon := by
  -- === TCCs for continuous_alt_root ===
  -- TCC: continuous_alt_root_TCC1
  -- Statement: FORALL (y1, x: real, pn: {pm: posnat | x >= 0 AND y1 >= 0 OR odd?(pm)},
  --         delta: posreal, y: {z: real | z >= 0 OR odd?(pn)}):
  --   abs(x - y) < delta IMPLIES x >= 0 OR odd?(pn)
  have continuous_alt_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: continuous_alt_root_TCC2
  -- Statement: FORALL (y1, x: real, pn: {pm: posnat | x >= 0 AND y1 >= 0 OR odd?(pm)},
  --         delta: posreal, y: {z: real | z >= 0 OR odd?(pn)}):
  --   abs(x - y) < delta IMPLIES y >= 0 OR odd?(pn)
  have continuous_alt_root_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_root_pos_rew
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: root(nx, pn) = root_pos(nx, pn)
theorem root_root_pos_rew : root nx pn = root_pos nx pn := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_root_real_rew
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}):
--   root(x, pn) = root_real(x)(pn)
theorem root_root_real_rew : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), root x pn = root_real x(pn) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: root.root_type
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)}):
--   LET y = root(x, pn) IN
--          (y = 0 IFF x = 0) AND (y > 0 IFF x > 0) AND (y < 0 IFF x < 0)
--      AND (y >= 0 IFF x >= 0) AND (y <= 0 IFF x <= 0) AN
theorem root_type : ∀ (x : ℝ) (pn : {pm : {i : {i : ℝ // i ≥ 0} // i > 0} // x ≥ 0 ∨ odd_p pm}), fun (y : {y : ℝ // y = nn_root |x| pn * sign x}) => (y = 0 ↔ x = 0) ∧ (y > 0 ↔ x > 0) ∧ (y < 0 ↔ x < 0) ∧ (y ≥ 0 ↔ x ≥ 0)
 ∧ (y ≤ 0 ↔ x ≤ 0) ∧ (y ^ pn = x) (root x pn) := by
  -- === TCCs for root_type ===
  -- TCC: root_type_TCC1
  -- Statement: FORALL (x: real, pn: {pm | x >= 0 OR odd?(pm)},
  --         y: {y: real | y = nn_root(abs(x), pn) * sign(x)}):
  --   (     (y = root(x, pn)) AND (y = 0 IMPLIES x = 0)
  --     AND (x = 0 IMPLIES y = 0) AND (y >...
  have root_type_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.root