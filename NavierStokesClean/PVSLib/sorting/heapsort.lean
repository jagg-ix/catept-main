-- Auto-generated by pvs2lean/translator.py
-- PVS theory: heapsort
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: heapsort

Auto-generated stubs from PVSLib theory `heapsort`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.heapsort

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (both_childs : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (finseq_appl : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (floor : pvs_τ7 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (ind_gc : pvs_τ16 → pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (ind_lc : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (ind_parent : pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (ind_rc : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (is_heap : pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (is_p_heap : pvs_τ27 → pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (is_parent : pvs_τ30 → pvs_τ31 → pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} (is_sorted_p : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} (length : pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (leq : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (one_child : pvs_τ38 → pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (permutations : pvs_τ41 → pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} (psorted : pvs_τ43 → pvs_τ44 → pvs_τ45 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (swap : pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52)

-- pvs2lean: heapsort.sink
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i > floor((n + 1) / 2) - 1 THEN h
-- ELSE LET k = ind_gc(h)(n, i) IN
--        IF gt(finseq_appl[T](h)(k), finseq_appl[T](h)(i))
--          THEN sink(swap(h)(i, k))(k, n)
--        ELSE h
--        ENDIF
-- ENDIF
axiom sink {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ46 → pvs_τ47 → pvs_τ48 → Prop

-- pvs2lean: heapsort.sink_permutes
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: nat), (n: nat | n < length(h) AND n >= i)):
--   permutations(h, sink(h)(i, n))
theorem sink_permutes : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : ℝ // i ≥ 0}) (n : {n : {i : ℝ // i ≥ 0} // n < List.length h ∧ n ≥ i}), permutations h (sink h(i, n)) := by
  -- === TCCs for sink_permutes ===
  -- TCC: sink_permutes_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (i: nat),
  --         (n: nat | n < length(h) AND n >= i)):
  --   i < length(h)
  have sink_permutes_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: heapsort.sink_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: nat), (n: nat | n < length(h) AND n >= i)):
--   length(sink(h)(i, n)) = length(h)
theorem sink_length : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : ℝ // i ≥ 0}) (n : {n : {i : ℝ // i ≥ 0} // n < List.length h ∧ n ≥ i}), List.length (sink h(i, n)) = List.length h := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: heapsort.sink_stable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: nat), (n: nat | n < length(h) AND n >= i),
--         (k: below[length(h)] | k < i OR k > n)):
--   finseq_appl[T](h)(k) = finseq_appl((sink(h)(i, n)))(k)
theorem sink_stable : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : ℝ // i ≥ 0}) (n : {n : {i : ℝ // i ≥ 0} // n < List.length h ∧ n ≥ i}) (k : {k : {s : {i : ℝ // i ≥ 0} // s < List.length h} // k < i ∨ k > n}), finseq_appl h(k) = finseq_appl ((sink h(i, n))) (k) := by
  -- === TCCs for sink_stable ===
  -- TCC: sink_stable_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (i: nat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k < i OR k > n)):
  --   k < length((sink(h)(i, n)))
  have sink_stable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: heapsort.sink_in_stable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: nat), (n: nat | n < length(h) AND n >= i),
--         (k: below[length(h)] | k >= i AND k <= n)):
--   EXISTS (l: below[length(h)] | l >= i AND l <= n):
--     finseq_appl[T](h)(l) = finseq_appl
theorem sink_in_stable : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : ℝ // i ≥ 0}) (n : {n : {i : ℝ // i ≥ 0} // n < List.length h ∧ n ≥ i}) (k : {k : {s : {i : ℝ // i ≥ 0} // s < List.length h} // k ≥ i ∧ k ≤ n}), ∃ (l : {l : {s : {i : ℝ // i ≥ 0} // s < List.length h} // l ≥ i ∧ l ≤ n}), finseq_appl h(l) = finseq_appl ((sink h(i, n))) (k) := by
  -- === TCCs for sink_in_stable ===
  -- TCC: sink_in_stable_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (i: nat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k >= i AND k <= n),
  --         l: below[length(h)] | l >= i AND l <= n):
  --   k < leng...
  have sink_in_stable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: heapsort.sink_interval_children_stable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: posnat), (n: nat | n < length(h) AND n >= i),
--         (k: below[length(h)] | k < i AND k > ind_parent(i))):
--   LET sh = sink(h)(i, n) IN
--     NOT is_parent(h)(n, k) OR
--      (one_child(h)(
theorem sink_interval_children_stable : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {n : {i : ℝ // i ≥ 0} // n < List.length h ∧ n ≥ i}) (k : {k : {s : {i : ℝ // i ≥ 0} // s < List.length h} // k < i ∧ k > ind_parent i}), fun (sh : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) => ¬ is_parent h(n, k) ∨ (one_child h(n, k) ∧
  finseq_appl h(ind_lc k) = finseq_appl sh(ind_lc k))
 ∨
 (both_childs h(n, k) ∧
   finseq_appl h(ind_lc k) = finseq_appl sh(ind_lc k) ∧
    finseq_appl h(ind_rc k) = finseq_appl sh(ind_rc k)) (sink h i n) := by
  -- === TCCs for sink_interval_children_stable ===
  -- TCC: sink_interval_children_stable_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (i: posnat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k < i AND k > ind_parent[T, leq](i)),
  --         sh: finite_sequence[T]):
  --   sh =...
  have sink_interval_children_stable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sink_interval_children_stable_TCC2
  -- Statement: FORALL (h: finite_sequence[T], (i: posnat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k < i AND k > ind_parent[T, leq](i)),
  --         sh: finite_sequence[T]):
  --   sh =...
  have sink_interval_children_stable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sink_interval_children_stable_TCC3
  -- Statement: FORALL (h: finite_sequence[T], (i: posnat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k < i AND k > ind_parent[T, leq](i)),
  --         sh: finite_sequence[T]):
  --   sh =...
  have sink_interval_children_stable_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sink_interval_children_stable_TCC4
  -- Statement: FORALL (h: finite_sequence[T], (i: posnat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k < i AND k > ind_parent[T, leq](i)),
  --         sh: finite_sequence[T]):
  --   sh =...
  have sink_interval_children_stable_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sink_interval_children_stable_TCC5
  -- Statement: FORALL (h: finite_sequence[T], (i: posnat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k < i AND k > ind_parent[T, leq](i)),
  --         sh: finite_sequence[T]):
  --   sh =...
  have sink_interval_children_stable_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sink_interval_children_stable_TCC6
  -- Statement: FORALL (h: finite_sequence[T], (i: posnat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k < i AND k > ind_parent[T, leq](i)),
  --         sh: finite_sequence[T]):
  --       ...
  have sink_interval_children_stable_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sink_interval_children_stable_TCC7
  -- Statement: FORALL (h: finite_sequence[T], (i: posnat),
  --         (n: nat | n < length(h) AND n >= i),
  --         (k: below[length(h)] | k < i AND k > ind_parent[T, leq](i)),
  --         sh: finite_sequence[T]):
  --       ...
  have sink_interval_children_stable_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: heapsort.is_heap_from_to
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: nat), (j: below[length(h)] | i < j)):
--   is_p_heap(h)(i + 1, j) => is_p_heap(sink(h)(i, j))(i, j)
theorem is_heap_from_to : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : ℝ // i ≥ 0}) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length h} // i < j}), is_p_heap h(i + 1, j) → is_p_heap (sink h(i, j)) (i, j) := by
  -- === TCCs for is_heap_from_to ===
  -- TCC: is_heap_from_to_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (i: nat), (j: below[length(h)] | i < j)):
  --   i + 1 < length(h)
  have is_heap_from_to_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: is_heap_from_to_TCC2
  -- Statement: FORALL (h: finite_sequence[T], (i: nat), (j: below[length(h)] | i < j)):
  --   1 + i <= j
  have is_heap_from_to_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: is_heap_from_to_TCC3
  -- Statement: FORALL (h: finite_sequence[T], (i: nat), (j: below[length(h)] | i < j)):
  --   is_p_heap(h)(i + 1, j) IMPLIES i < length(sink(h)(i, j))
  have is_heap_from_to_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: is_heap_from_to_TCC4
  -- Statement: FORALL (h: finite_sequence[T], (i: nat), (j: below[length(h)] | i < j)):
  --   is_p_heap(h)(i + 1, j) IMPLIES j < length(sink(h)(i, j)) AND i <= j
  have is_heap_from_to_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: is_heap_from_to_TCC5
  -- Statement: FORALL (h: finite_sequence[T], (i: nat), (j: below[length(h)] | i < j)):
  --   is_p_heap(h)(i + 1, j) IMPLIES i < length(h)
  have is_heap_from_to_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: is_heap_from_to_TCC6
  -- Statement: FORALL (h: finite_sequence[T], (i: nat), (j: below[length(h)] | i < j)):
  --   is_p_heap(h)(i + 1, j) IMPLIES j >= i
  have is_heap_from_to_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: heapsort.heapify
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i > 0 THEN heapify(sink(h)(i, length(h) - 1))(i - 1)
-- ELSE sink(h)(i, length(h) - 1)
-- ENDIF
axiom heapify {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10

-- pvs2lean: heapsort.heapify_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: below[floor(length(h) / 2)])):
--   length(heapify(h)(i)) = length(h)
theorem heapify_length : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {s : {i : ℝ // i ≥ 0} // s < floor (List.length h / 2)}), List.length (heapify h(i)) = List.length h := by
  -- === TCCs for heapify_length ===
  -- TCC: heapify_length_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (i: below[floor(length(h) / 2)])):
  --   i <= floor(length(h) / 2) - 1
  have heapify_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: heapsort.heapify_permutes
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: below[floor(length(h) / 2)])): permutations(h, heapify(h)(i))
theorem heapify_permutes : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {s : {i : ℝ // i ≥ 0} // s < floor (List.length h / 2)}), permutations h (heapify h(i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: heapsort.heapify_heapifies
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h: finite_sequence[T]
--             | length(h) > 0 AND floor(length(h) / 2) - 1 >= 0):
--   is_heap(heapify(h)(floor(length(h) / 2) - 1))
theorem heapify_heapifies : ∀ (h : {h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length h > 0 ∧ floor (List.length h / 2) - 1 ≥ 0}), is_heap (heapify h (floor (List.length h / 2) - 1)) := by
  -- === TCCs for heapify_heapifies ===
  -- TCC: heapify_heapifies_TCC1
  -- Statement: FORALL (h: finite_sequence[T]
  --             | length(h) > 0 AND floor(length(h) / 2) - 1 >= 0):
  --   floor(length(h) / 2) - 1 >= 0 AND
  --    floor(length(h) / 2) - 1 <= floor(length(h) / 2) - 1
  have heapify_heapifies_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: heapsort.heapsort_aux
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN h
-- ELSIF n = 1 THEN swap(h)(0, 1)
-- ELSE heapsort_aux(sink(swap(h)(0, n))(0, n - 1))(n - 1)
-- ENDIF
axiom heapsort_aux {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15

-- pvs2lean: heapsort.heapsort_aux_permutes
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (n: below[length(h)])): permutations(h, heapsort_aux(h)(n))
theorem heapsort_aux_permutes : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (n : {s : {i : ℝ // i ≥ 0} // s < List.length h}), permutations h (heapsort_aux h(n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: heapsort.heapsort_aux_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (n: below[length(h)])): length(heapsort_aux(h)(n)) = length(h)
theorem heapsort_aux_length : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (n : {s : {i : ℝ // i ≥ 0} // s < List.length h}), List.length (heapsort_aux h(n)) = List.length h := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: heapsort.heapsort_aux_psorts
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (n: nat | n < length(h) - 1)):
--   (psorted(h)(n + 1, length(h) - 1) AND
--     is_p_heap(h)(0, n) AND
--      FORALL (i: below[n + 1]):
--        leq(finseq_appl[T](h)(i), finseq_appl[T](h)(n + 1)))
-- 
theorem heapsort_aux_psorts : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (n : {n : {i : ℝ // i ≥ 0} // n < List.length h - 1}), (psorted h(n + 1, List.length h - 1) ∧
  is_p_heap h(0, n) ∧
   ∀ (i : Fin n + 1), leq (finseq_appl h(i)) (finseq_appl h(n + 1))) → is_sorted_p (heapsort_aux h(n)) := by
  -- === TCCs for heapsort_aux_psorts ===
  -- TCC: heapsort_aux_psorts_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (n: nat | n < length(h) - 1)):
  --   n + 1 < length(h)
  have heapsort_aux_psorts_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: heapsort_aux_psorts_TCC2
  -- Statement: FORALL (h: finite_sequence[T], (n: nat | n < length(h) - 1)):
  --   length(h) - 1 >= 0 AND
  --    length(h) - 1 < length(h) AND 1 + n <= length(h) - 1
  have heapsort_aux_psorts_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: heapsort_aux_psorts_TCC3
  -- Statement: FORALL (h: finite_sequence[T], (n: nat | n < length(h) - 1)):
  --   psorted(h)(n + 1, length(h) - 1) IMPLIES 0 < length(h)
  have heapsort_aux_psorts_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: heapsort_aux_psorts_TCC4
  -- Statement: FORALL (h: finite_sequence[T], (n: nat | n < length(h) - 1)):
  --   psorted(h)(n + 1, length(h) - 1) IMPLIES n < length(h) AND 0 <= n
  have heapsort_aux_psorts_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: heapsort_aux_psorts_TCC5
  -- Statement: FORALL (h: finite_sequence[T], (n: nat | n < length(h) - 1)):
  --   psorted(h)(n + 1, length(h) - 1) AND is_p_heap(h)(0, n) IMPLIES
  --    FORALL (i: below[1 + n]): i < length(h)
  have heapsort_aux_psorts_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: heapsort_aux_psorts_TCC6
  -- Statement: FORALL (h: finite_sequence[T], (n: nat | n < length(h) - 1)):
  --   (psorted(h)(n + 1, length(h) - 1) AND
  --     is_p_heap(h)(0, n) AND
  --      FORALL (i: below[1 + n]):
  --        leq(finseq_appl[T](h)(i), fins...
  have heapsort_aux_psorts_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: heapsort.heapsort
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(h) > 1
--   THEN heapsort_aux(heapify(h)(floor(length(h) / 2) - 1))(length(h) - 1)
-- ELSE h
-- ENDIF
axiom heapsort {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12

-- pvs2lean: heapsort.heapsort_works
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h): is_sorted?(heapsort(h)) AND permutations(h, heapsort(h))
theorem heapsort_works : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), is_sorted_p (heapsort h) ∧ permutations h (heapsort h) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.heapsort