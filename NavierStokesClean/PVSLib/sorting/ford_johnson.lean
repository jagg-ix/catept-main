-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ford_johnson
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ford_johnson

Auto-generated stubs from PVSLib theory `ford_johnson`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ford_johnson

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (ELSIF : pvs_τ1 → pvs_τ2)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (above : pvs_τ8 → pvs_τ9)
variable {pvs_τ23 : Type*} (car : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (cdr : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (consFjnode : pvs_τ26 → pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} (cons_p : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (finseq_appl : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} (floor : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (insert_p : pvs_τ34 → pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} (integer_pred : pvs_τ37 → Prop)
variable {pvs_τ40 : Type*} (is_sorted_p : pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} (length : pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (leq : pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (logb : pvs_τ44 → pvs_τ45 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} (min : pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (nstruct_p : pvs_τ53 → pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} (nth : pvs_τ55 → pvs_τ56 → pvs_τ57)
variable {pvs_τ58 : Type*} (null_p : pvs_τ58 → Prop)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} (occurrences : pvs_τ59 → pvs_τ60 → pvs_τ61)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (permutation : pvs_τ64 → pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} (plain_finseqfj : pvs_τ66 → Prop)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} (rest : pvs_τ70 → pvs_τ71)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} (seq : pvs_τ72 → pvs_τ73)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} (seqfjBottleneck : pvs_τ74 → pvs_τ75)
variable {pvs_τ79 : Type*} {pvs_τ80 : Type*} (smallerones : pvs_τ79 → pvs_τ80)
variable {pvs_τ84 : Type*} (val : pvs_τ84 → Prop)

-- pvs2lean: ford_johnson.t
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (2 ^ (k + 1) + (-1) ^ (k)) / 3 - 1
noncomputable def t : sorry := by
  sorry  -- pvs2lean: definition stub for (2 ^ (k + 1) + (-1) ^ (k)) / 3 - 1

-- pvs2lean: ford_johnson.t_is_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: posnat): i < j IFF t(i) < t(j)
theorem t_is_increasing : ∀ (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (j : {i : {i : ℝ // i ≥ 0} // i > 0}), i < j ↔ t i < t j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.t_is_unbounded
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat): (EXISTS (j: nat): t(j) > i)
theorem t_is_unbounded : ∀ (i : {i : ℝ // i ≥ 0}), ∃ (j : {i : ℝ // i ≥ 0}), t j > i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ford_johnson.t_qinversible
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: posnat): EXISTS (k: posnat): t(k - 1) < l AND t(k) >= l
theorem t_qinversible : ∀ (l : {i : {i : ℝ // i ≥ 0} // i > 0}), ∃ (k : {i : {i : ℝ // i ≥ 0} // i > 0}), t (k - 1) < l ∧ t k ≥ l := by
  -- === TCCs for t_qinversible ===
  -- TCC: t_qinversible_TCC1
  -- Statement: FORALL (k: posnat): k - 1 >= 0
  have t_qinversible_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ford_johnson.maxk
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom maxk {pvs_τ46 : Type*} : pvs_τ46 → Prop

-- pvs2lean: ford_johnson.maxk_correction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: posnat, k: posnat): t(k) < l <=> k < maxk(l)
theorem maxk_correction : ∀ (l : {i : {i : ℝ // i ≥ 0} // i > 0}) (k : {i : {i : ℝ // i ≥ 0} // i > 0}), t k < l <=> k < maxk l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ford_johnson.is_indx?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_sorted?(indx) AND
--  (FORALL (i: below[length(indx)]):
--     finseq_appl[nat](indx)(i) < length(s) AND
--      FORALL (j: below[length(indx)] | i /= j):
--        finseq_appl[nat](indx)(i) /= finseq_appl[nat
axiom is_indx_p {pvs_τ38 : Type*} {pvs_τ39 : Type*} : pvs_τ38 → pvs_τ39 → Prop

-- pvs2lean: ford_johnson.removeSon
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := s`length,
--    seq
--      := (LAMBDA (i: below[s`length]):
--            IF i = indx
--              THEN consFjnode(val(finseq_appl[fjnode[T]](s)(i)),
--                              IF cons?(smaller
axiom removeSon {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} : pvs_τ67 → pvs_τ68 → pvs_τ69

-- pvs2lean: ford_johnson.upToOne
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := indx`length,
--    seq
--      := (LAMBDA (i: below[indx`length]):
--            IF finseq_appl[nat](indx)(i) < n THEN finseq_appl[nat](indx)(i)
--            ELSE finseq_appl[nat](indx)(i) + 1
--      
axiom upToOne {pvs_τ81 : Type*} {pvs_τ82 : Type*} {pvs_τ83 : Type*} : pvs_τ81 → pvs_τ82 → pvs_τ83

-- pvs2lean: ford_johnson.upToOneIsCorrectAll
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]], indx: finseq[nat])
--        (k: below[length(indx)], x: fjnode, i: below[length(s)]):
--   finseq_appl[nat](indx)(k) < length(s) IMPLIES
--    finseq_appl[fjnode[T]](s)(finseq_ap
theorem upToOneIsCorrectAll : ∀ (s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) (indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0})), ∀ (k : {s : {i : ℝ // i ≥ 0} // s < List.length indx}) (x : fjnode T) (i : {s : {i : ℝ // i ≥ 0} // s < List.length s}), finseq_appl indx(k) < List.length s → finseq_appl s(finseq_appl indx(k)) =
 finseq_appl (insert_p x s i) (finseq_appl (upToOne indx i) (k)) := by
  -- === TCCs for upToOneIsCorrectAll ===
  -- TCC: upToOneIsCorrectAll_TCC1
  -- Statement: FORALL (s: finseq[fjnode[T]], indx: finseq[nat], k: below[length(indx)],
  --         x: fjnode[T], i: below[length(s)]):
  --   finseq_appl[nat](indx)(k) < length(s) IMPLIES k < length(upToOne(indx, i))
  have uptooneiscorrectall_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: upToOneIsCorrectAll_TCC2
  -- Statement: FORALL (s: finseq[fjnode[T]], indx: finseq[nat], k: below[length(indx)],
  --         x: fjnode[T], i: below[length(s)]):
  --   finseq_appl[nat](indx)(k) < length(s) IMPLIES
  --    finseq_appl[nat](upToOne(indx...
  have uptooneiscorrectall_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: upToOneIsCorrectAll_TCC3
  -- Statement: FORALL (s: finseq[fjnode[T]], indx: finseq[nat], k: below[length(indx)],
  --         x: fjnode[T], i: below[length(s)]):
  --   finseq_appl[nat](indx)(k) < length(s) IMPLIES i <= length(s)
  have uptooneiscorrectall_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.upToOnePreservesIndx
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]] | length(s) > 0,
--         indx: finseq[nat] | length(indx) > 0, x: fjnode,
--         i: upto[length(s)]):
--   is_indx?(s, indx) IMPLIES is_indx?(insert?(x, s, i), upToOne(indx,
theorem upToOnePreservesIndx : ∀ (s : {s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T) // List.length s > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0}) (x : fjnode T) (i : {s : {i : ℝ // i ≥ 0} // s ≤ List.length s}), is_indx_p s indx → is_indx_p (insert_p x s i) (upToOne indx i) := by
  -- === TCCs for upToOnePreservesIndx ===
  -- TCC: upToOnePreservesIndx_TCC1
  -- Statement: FORALL (s: finseq[fjnode[T]] | length(s) > 0,
  --         indx: finseq[nat] | length(indx) > 0, x: fjnode[T],
  --         i: upto[length(s)]):
  --   is_indx?(s, indx) IMPLIES length(insert?[fjnode[T]](x, s, i)...
  have uptoonepreservesindx_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: upToOnePreservesIndx_TCC2
  -- Statement: FORALL (s: finseq[fjnode[T]] | length(s) > 0,
  --         indx: finseq[nat] | length(indx) > 0, x: fjnode[T],
  --         i: upto[length(s)]):
  --   is_indx?(s, indx) IMPLIES length(upToOne(indx, i)) > 0
  have uptoonepreservesindx_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.bin_search
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = j
--   THEN IF leq(val(k), val(finseq_appl[fjnode[T]](s)(i))) THEN i
--        ELSE i + 1
--        ENDIF
-- ELSE LET l = floor((i + j) / 2) IN
--        IF val(k) = val(finseq_appl[fjnode[T]](s)(l)) THEN l
--  
axiom bin_search {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: ford_johnson.b_search_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]]) (k: fjnode[T])
--        (i: below[length(s)], (j: below[length(s)] | i <= j)):
--   bin_search(s)(k)(i, j) >= i AND bin_search(s)(k)(i, j) <= j + 1
theorem b_search_bound : ∀ (s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)), ∀ (k : fjnode T), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length s}) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length s} // i ≤ j}), bin_search s(k) (i, j) ≥ i ∧ bin_search s(k) (i, j) ≤ j + 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.b_search_preserves_sorting
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]]) (k: fjnode[T])
--        (i: below[length(s)], (j: below[length(s)] | i <= j)):
--   (is_sorted?(s) AND
--     (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
--   
theorem b_search_preserves_sorting : ∀ (s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)), ∀ (k : fjnode T), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length s}) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length s} // i ≤ j}), (is_sorted_p s ∧
  (i = 0 ∨ leq (val (finseq_appl s(i - 1))) (val k)) ∧
   (j = s.List.length - 1 ∨ leq (val k) (val (finseq_appl s(j + 1))))) → is_sorted_p (insert_p k s (bin_search s(k) (i, j))) := by
  -- === TCCs for b_search_preserves_sorting ===
  -- TCC: b_search_preserves_sorting_TCC1
  -- Statement: FORALL (v: finite_sequence[T], k: T, i: below[length(v)],
  --         (j: below[length(v)] | i <= j)):
  --   is_sorted?(v) AND NOT i = 0 IMPLIES i - 1 >= 0 AND i - 1 < length(v)
  have b_search_preserves_sorting_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC2
  -- Statement: FORALL (v: finite_sequence[T], k: T, i: below[length(v)],
  --         (j: below[length(v)] | i <= j)):
  --   is_sorted?(v) AND
  --    (i = 0 OR geq(k, finseq_appl[T](v)(i - 1))) AND NOT j = v`length - 1
  --    IMP...
  have b_search_preserves_sorting_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC3
  -- Statement: FORALL (v: finite_sequence[T], k: T, i: below[length(v)],
  --         (j: below[length(v)] | i <= j)):
  --   is_sorted?(v) AND
  --    (i = 0 OR geq(k, finseq_appl[T](v)(i - 1))) AND
  --     (j = v`length - 1 OR le...
  have b_search_preserves_sorting_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC1
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   is_sorted?(s) AND NOT i = 0 IMPLIES i - 1 >= 0 AND i - 1 < length(s)
  have b_search_preserves_sorting_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC2
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   is_sorted?(s) AND
  --    (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
  --   ...
  have b_search_preserves_sorting_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC3
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   is_sorted?(s) AND
  --    (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
  --   ...
  have b_search_preserves_sorting_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.b_search_removeSon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]]) (k: fjnode[T]) (indx: nat)
--        (i: below[length(s)], (j: below[length(s)] | i <= j)):
--   (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
--    (j = s`len
theorem b_search_removeSon : ∀ (s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)), ∀ (k : fjnode T), ∀ (indx : {i : ℝ // i ≥ 0}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length s}) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length s} // i ≤ j}), (i = 0 ∨ leq (val (finseq_appl s(i - 1))) (val k)) ∧
 (j = s.List.length - 1 ∨ leq (val k) (val (finseq_appl s(j + 1)))) → bin_search s(k) (i, j) = bin_search (removeSon s indx) (k) (i, j) := by
  -- === TCCs for b_search_removeSon ===
  -- TCC: b_search_removeSon_TCC1
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   NOT i = 0 IMPLIES i - 1 >= 0 AND i - 1 < length(s)
  have b_search_removeson_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_removeSon_TCC2
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
  --    NOT j = s`length - 1...
  have b_search_removeson_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_removeSon_TCC3
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], indx: nat, i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
  --    (j = s`le...
  have b_search_removeson_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_removeSon_TCC4
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], indx: nat, i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
  --    (j = s`le...
  have b_search_removeson_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.PartialERforK?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mIdx = IF t(k) < length(indx) THEN t(k) ELSE length(indx) - 1 ENDIF IN
--   (FORALL (j: below[length(s`seqfj)]):
--      nstruct?(finseq_appl(s`seqfj)(j))(n - 1) OR
--       (nstruct?(finseq_appl(s`seqfj)(
axiom PartialERforK_p {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7 → Prop

-- pvs2lean: ford_johnson.oddNstructPreservation?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: below[length(s`oddList)]):
--    EXISTS (m: below[n]): nstruct?(nth(s`oddList, i))(m))
--  AND
--  (FORALL (i, j: below[length(s`oddList)]):
--     i > j =>
--      length(smallerones(nth(s`oddList, i)))
axiom oddNstructPreservation_p {pvs_τ62 : Type*} {pvs_τ63 : Type*} : pvs_τ62 → pvs_τ63 → Prop

-- pvs2lean: ford_johnson.oddNstructDeconstruction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0, n: posnat):
--   oddNstructPreservation?(s, n) IMPLIES
--    IF NOT null?(s`oddList) AND
--        length(smallerones(nth(s`oddList, 0))) = n - 1
--      THEN oddNstruct
theorem oddNstructDeconstruction : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}), oddNstructPreservation_p s n → if ¬ null_p s.oddList ∧ List.length (smallerones (nth s.oddList 0)) = n - 1
  then oddNstructPreservation_p ((# seqfj := s.seqfj,
                                  oddList := cdr s.oddList #)) (n - 1)
else oddNstructPreservation_p s (n - 1) := by
  -- === TCCs for oddNstructDeconstruction ===
  -- TCC: oddNstructDeconstruction_TCC1
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat):
  --   oddNstructPreservation?(s, n) AND NOT null?(s`oddList) IMPLIES
  --    0 < length[fjnode[T]](s`oddList)
  have oddnstructdeconstruction_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: oddNstructDeconstruction_TCC2
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat):
  --   oddNstructPreservation?(s, n) AND
  --    NOT null?(s`oddList) AND length(smallerones(nth(s`oddList, 0))) = n - 1
  --    IMPLIES cons?[fjnode...
  have oddnstructdeconstruction_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: oddNstructDeconstruction_TCC3
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat):
  --   oddNstructPreservation?(s, n) AND
  --    NOT null?(s`oddList) AND length(smallerones(nth(s`oddList, 0))) = n - 1
  --    IMPLIES length(s`seq...
  have oddnstructdeconstruction_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.nstructER_basic?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: below[s`seqfj`length]): nstruct?(s`seqfj`seq(i))(n)) AND
--  oddNstructPreservation?(s, n) AND is_sorted?(s`seqfj)
axiom nstructER_basic_p {pvs_τ51 : Type*} {pvs_τ52 : Type*} : pvs_τ51 → pvs_τ52 → Prop

-- pvs2lean: ford_johnson.nstructER_vs_nstructER_basic
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | plain_finseqfj(s) AND length(s`seqfj) /= 0):
--   LET n = logb(s`seqfj`length, 2) IN nstructER_basic?(seqfjBottleneck(s), n)
theorem nstructER_vs_nstructER_basic : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // plain_finseqfj s ∧ List.length s.seqfj ≠ 0}), fun (n : {i : ℝ // i ≥ 0}) => nstructER_basic_p (seqfjBottleneck s) n (logb s.seqfj.List.length 2) := by
  -- === TCCs for nstructER_vs_nstructER_basic ===
  -- TCC: nstructER_vs_nstructER_basic_TCC1
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | plain_finseqfj[T, leq](s) AND length(s`seqfj) /= 0):
  --   s`seqfj`length >= 1
  have nstructer_vs_nstructer_basic_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nstructER_vs_nstructER_basic_TCC2
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | plain_finseqfj[T, leq](s) AND length(s`seqfj) /= 0,
  --         n: nat):
  --   n = logb(s`seqfj`length, 2) IMPLIES
  --    length(seqfjBottleneck[T, leq](s)`seqfj) > 0
  have nstructer_vs_nstructer_basic_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.occ_in_rest
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]] | length(s) > 0, x: T):
--   occurrences(rest(s))(x) = occurrences(s)(x) - occurrences(x)(s`seq(0))
theorem occ_in_rest : ∀ (s : {s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T) // List.length s > 0}) (x : T), occurrences (rest s) (x) = occurrences s(x) - occurrences x(s.seq 0) := by
  -- === TCCs for occ_in_rest ===
  -- TCC: occ_in_rest_TCC1
  -- Statement: FORALL (s: finseq[fjnode[T]] | length(s) > 0, x: T): 0 < s`length
  have occ_in_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ford_johnson.occ_in_removeson
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]], i: below[length(s)], x: T):
--   cons?(smallerones(s`seq(i))) =>
--    occurrences(removeSon(s, i))(x) =
--     occurrences(s)(x) - occurrences(x)(car(smallerones(s`seq(i))))
theorem occ_in_removeson : ∀ (s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length s}) (x : T), cons_p (smallerones (s.seq i)) → occurrences (removeSon s i) (x) =
 occurrences s(x) - occurrences x(car (smallerones (s.seq i))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.occ_in_insert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]], node: fjnode[T], i: upto[length(s)], x: T):
--   occurrences(insert?(node, s, i))(x) =
--    occurrences(s)(x) + occurrences(x)(node)
theorem occ_in_insert : ∀ (s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) (node : fjnode T) (i : {s : {i : ℝ // i ≥ 0} // s ≤ List.length s}) (x : T), occurrences (insert_p node s i) (x) = occurrences s(x) + occurrences x(node) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.insert_removeson_permutation
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0,
--         i, searchIndx: below[length(s`seqfj)]):
--   cons?(smallerones(s`seqfj`seq(i))) =>
--    permutation(s`seqfj,
--                insert?(car(smallerones(s`seqf
theorem insert_removeson_permutation : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length s.seqfj}) (searchIndx : {s : {i : ℝ // i ≥ 0} // s < List.length s.seqfj}), cons_p (smallerones (s.seqfj.seq i)) → permutation s.seqfj (insert_p (car (smallerones (s.seqfj.seq i))) (removeSon s.seqfj i) searchIndx) := by
  -- === TCCs for insert_removeson_permutation ===
  -- TCC: insert_removeson_permutation_TCC1
  -- Statement: <unparse-error: SIMPLE-ERROR>
  have insert_removeson_permutation_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.insertSonPreservesPartialERforK
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0, k: posnat,
--         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
--         n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
theorem insertSonPreservesPartialERforK : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (pos : {pos : {i : {i : ℝ // i ≥ 0} // i > 0} // (k = 1 ∨ t (k - 1) < pos) ∧ pos ≤ t k}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧
 is_indx_p s.seqfj indx ∧
  pos < List.length indx ∧ PartialERforK_p s indx k pos n}), fun (b : fjnode T) => fun (bsearch : {s_1 : {i : ℝ // i ≥ 0} // s_1 ≤ indx.seq pos}) => fun (sk : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T))) => PartialERforK_p sk (upToOne indx bsearch) k (pos - 1) n (# seqfj := insert_p b (removeSon s.seqfj (indx.seq pos)) bsearch) (oddList := s.oddList #) (bin_search s.seqfj b 0 (indx.seq pos - 1)) (car (smallerones (s.seqfj.seq (indx.seq pos)))) := by
  -- === TCCs for insertSonPreservesPartialERforK ===
  -- TCC: insertSonPreservesPartialERforK_TCC1
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         indx: finseq[nat]):
  --   length(indx) > 0 AND is_indx?(s`seqfj,...
  have insertsonpreservespartialerfork_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC2
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         indx: finseq[nat]):
  --   length(indx) > 0 AND is_indx?(s`seqfj,...
  have insertsonpreservespartialerfork_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC3
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC4
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC5
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC6
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC7
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC8
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC9
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC10
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc10 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertSonPreservesPartialERforK_TCC11
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: posnat,
  --         (pos: posnat | (k = 1 OR t(k - 1) < pos) AND pos <= t(k)),
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have insertsonpreservespartialerfork_tcc11 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.insertCarPreservesPartialERforK
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0 AND length(s`oddList) > 0,
--         n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--                is_indx?(s`seqfj, indx) AND
--         
theorem insertCarPreservesPartialERforK : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0 ∧ List.length s.oddList > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧
 is_indx_p s.seqfj indx ∧
  PartialERforK_p s indx (maxk indx.List.length) (min (t (maxk indx.List.length)) (indx.List.length - 1)) n}), fun (bsearch : {s_1 : {i : ℝ // i ≥ 0} // s_1 ≤ List.length s.seqfj}) => fun (sk : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T))) => fun (k : {k : {s : ℝ // s > 1} // t (k - 1) < indx.List.length ∧ t k ≥ indx.List.length}) => nstruct_p (car s.oddList) (n - 1) → PartialERforK_p sk (upToOne indx bsearch) k (List.length indx - 1) n (maxk indx.List.length) (# seqfj := insert_p (car s.oddList) s.seqfj bsearch) (oddList := cdr s.oddList #) (bin_search s.seqfj (car s.oddList) 0 (List.length s.seqfj - 1)) := by
  -- === TCCs for insertCarPreservesPartialERforK ===
  -- TCC: insertCarPreservesPartialERforK_TCC1
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         indx: finseq[nat]):
  --   length(indx) > 0 AND is_indx?(s`seqfj, indx) IMPLIES
  --    length(indx...
  have insertcarpreservespartialerfork_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertCarPreservesPartialERforK_TCC2
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         indx: finseq[nat]):
  --   length(indx) > 0 AND is_indx?(s`seqfj, indx) IMPLIES
  --    min(t(maxk(...
  have insertcarpreservespartialerfork_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertCarPreservesPartialERforK_TCC3
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                is...
  have insertcarpreservespartialerfork_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertCarPreservesPartialERforK_TCC4
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                is...
  have insertcarpreservespartialerfork_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertCarPreservesPartialERforK_TCC5
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                is...
  have insertcarpreservespartialerfork_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertCarPreservesPartialERforK_TCC6
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                is...
  have insertcarpreservespartialerfork_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertCarPreservesPartialERforK_TCC7
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                is...
  have insertcarpreservespartialerfork_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertCarPreservesPartialERforK_TCC8
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                is...
  have insertcarpreservespartialerfork_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: insertCarPreservesPartialERforK_TCC9
  -- Statement: FORALL (s: finseqfj[T, leq]
  --             | length(s`seqfj) > 0 AND length[fjnode[T]](s`oddList) > 0,
  --         n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                is...
  have insertcarpreservespartialerfork_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.binsert_tk_interval
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF k = 1
--   THEN LET b = car(smallerones(s`seqfj`seq(finseq_appl[nat](indx)(0)))) IN
--          ((# seqfj
--                := insert?(b, removeSon(s`seqfj, finseq_appl[nat](indx)(0)),
--                      
axiom binsert_tk_interval {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: ford_johnson.binsert_tk_interval_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat, (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)))
--        (s: finseqfj | length(s`seqfj) > 0, n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--          
theorem binsert_tk_interval_length : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (pos : {pos : {i : ℝ // i ≥ 0} // (k = 1 ∨ t (k - 1) ≤ pos) ∧ pos ≤ t k}), ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧
 is_indx_p s.seqfj indx ∧
  pos < List.length indx ∧ PartialERforK_p s indx k pos n}), List.length (binsert_tk_interval s k pos n indx.1.seqfj) ≠ 0 ∧ List.length (binsert_tk_interval s k pos n indx.2) = List.length indx := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.binsert_tk_interval_preserves_indx
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat, (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)))
--        (s: finseqfj | length(s`seqfj) > 0, n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--          
theorem binsert_tk_interval_preserves_indx : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (pos : {pos : {i : ℝ // i ≥ 0} // (k = 1 ∨ t (k - 1) ≤ pos) ∧ pos ≤ t k}), ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧
 is_indx_p s.seqfj indx ∧
  pos < List.length indx ∧ PartialERforK_p s indx k pos n}), is_indx_p (binsert_tk_interval s k pos n indx.1.seqfj) (binsert_tk_interval s k pos n indx.2) := by
  -- === TCCs for binsert_tk_interval_preserves_indx ===
  -- TCC: binsert_tk_interval_preserves_indx_TCC1
  -- Statement: FORALL (k: posnat, (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)),
  --         s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AN...
  have binsert_tk_interval_preserves_indx_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: binsert_tk_interval_preserves_indx_TCC2
  -- Statement: FORALL (k: posnat, (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)),
  --         s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AN...
  have binsert_tk_interval_preserves_indx_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.up_partialER
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: above[1],
--         (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)))
--        (s: finseqfj | length(s`seqfj) > 0, n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
theorem up_partialER : ∀ (k : {s : ℝ // s > 1}) (pos : {pos : {i : ℝ // i ≥ 0} // (k = 1 ∨ t (k - 1) ≤ pos) ∧ pos ≤ t k}), ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧
 is_indx_p s.seqfj indx ∧
  pos < List.length indx ∧ PartialERforK_p s indx k pos n}), fun (sk : (List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T))) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}))) => PartialERforK_p sk.1 sk.2 k (t (k - 1)) n (binsert_tk_interval s k pos n indx) := by
  -- === TCCs for up_partialER ===
  -- TCC: up_partialER_TCC1
  -- Statement: FORALL (k: above[1],
  --         (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)),
  --         s: finseqfj[T, leq] | length(s`seqfj) > 0, indx: finseq[nat]):
  --   length(indx) > 0 AND is_indx?(s`seqfj,...
  have up_partialer_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: up_partialER_TCC2
  -- Statement: FORALL (k: above[1],
  --         (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)),
  --         s: finseqfj[T, leq] | length(s`seqfj) > 0, indx: finseq[nat]):
  --   length(indx) > 0 AND is_indx?(s`seqfj,...
  have up_partialer_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: up_partialER_TCC3
  -- Statement: FORALL (k: above[1],
  --         (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)),
  --         s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have up_partialer_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: up_partialER_TCC4
  -- Statement: FORALL (k: above[1],
  --         (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)),
  --         s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have up_partialer_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: up_partialER_TCC5
  -- Statement: FORALL (k: above[1],
  --         (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)),
  --         s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(in...
  have up_partialer_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.binsert_tk_interval_preserves_oddList
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat, (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)))
--        (s: finseqfj | length(s`seqfj) > 0, n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--          
theorem binsert_tk_interval_preserves_oddList : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (pos : {pos : {i : ℝ // i ≥ 0} // (k = 1 ∨ t (k - 1) ≤ pos) ∧ pos ≤ t k}), ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧
 is_indx_p s.seqfj indx ∧
  pos < List.length indx ∧ PartialERforK_p s indx k pos n}), binsert_tk_interval s k pos n indx.1.oddList = s.oddList := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.binsert_tk_interval_gives_permutation
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat, (pos: nat | (k = 1 OR t(k - 1) <= pos) AND pos <= t(k)))
--        (s: finseqfj | length(s`seqfj) > 0, n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--          
theorem binsert_tk_interval_gives_permutation : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (pos : {pos : {i : ℝ // i ≥ 0} // (k = 1 ∨ t (k - 1) ≤ pos) ∧ pos ≤ t k}), ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧
 is_indx_p s.seqfj indx ∧
  pos < List.length indx ∧ PartialERforK_p s indx k pos n}), permutation s (binsert_tk_interval s k pos n indx.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.PartialERforK_equiv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0, k: above[1], n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--                (is_indx?(s`seqfj, indx) AND k + 1 <= maxk(indx`length)))
theorem PartialERforK_equiv : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (k : {s : ℝ // s > 1}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧
 (is_indx_p s.seqfj indx ∧ k + 1 ≤ maxk indx.List.length)}), PartialERforK_p s indx k (t (k - 1)) n → PartialERforK_p s indx (1 + k) (min (t (1 + k)) (List.length indx - 1)) n := by
  -- === TCCs for PartialERforK_equiv ===
  -- TCC: PartialERforK_equiv_TCC1
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: above[1],
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                (is_indx?(s`seqfj, indx) AND 1 + k <= maxk(indx`length)))...
  have partialerfork_equiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: PartialERforK_equiv_TCC2
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, k: above[1], n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                (is_indx?(s`seqfj, indx) AND 1 + k <= maxk(ind...
  have partialerfork_equiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.basis_PartialERforK_equiv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0, n: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--                (is_indx?(s`seqfj, indx)) AND indx`seq(0) = 0):
--   PartialERforK?(s, in
theorem basis_PartialERforK_equiv : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧ (is_indx_p s.seqfj indx) ∧ indx.seq 0 = 0}), PartialERforK_p s indx 1 (t 1) n → (let sk := binsert_tk_interval s 1 (t 1) n indx; PartialERforK_p sk.1 sk.2 2 (min (t 2) (List.length sk.2 - 1)) n) := by
  -- === TCCs for basis_PartialERforK_equiv ===
  -- TCC: basis_PartialERforK_equiv_TCC1
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, indx: finseq[nat]):
  --   length(indx) > 0 AND (is_indx?(s`seqfj, indx)) IMPLIES 0 < indx`length
  have basis_partialerfork_equiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: basis_PartialERforK_equiv_TCC2
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                (is_indx?(s`seqfj, indx)) AND indx`seq(0) = 0):
  --   t(1) < length(indx) ...
  have basis_partialerfork_equiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: basis_PartialERforK_equiv_TCC3
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                (is_indx?(s`seqfj, indx)) AND indx`seq(0) = 0):
  --   PartialER...
  have basis_partialerfork_equiv_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: basis_PartialERforK_equiv_TCC4
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                (is_indx?(s`seqfj, indx)) AND indx`seq(0) = 0):
  --   PartialER...
  have basis_partialerfork_equiv_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: basis_PartialERforK_equiv_TCC5
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                (is_indx?(s`seqfj, indx)) AND indx`seq(0) = 0):
  --   PartialER...
  have basis_partialerfork_equiv_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: basis_PartialERforK_equiv_TCC6
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                (is_indx?(s`seqfj, indx)) AND indx`seq(0) = 0):
  --   PartialER...
  have basis_partialerfork_equiv_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: basis_PartialERforK_equiv_TCC7
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                (is_indx?(s`seqfj, indx)) AND indx`seq(0) = 0):
  --   PartialER...
  have basis_partialerfork_equiv_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.binsert_one_level
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF t(k - 1) >= length(indx) OR length(s`seqfj) = 0 THEN s
-- ELSE IF t(k) < length(indx)
--        THEN LET sk = binsert_tk_interval(s, k, t(k), ncycles, indx) IN
--               binsert_one_level(sk`1, k + 1
axiom binsert_one_level {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16 → pvs_τ17 → Prop

-- pvs2lean: ford_johnson.binsert_one_level_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0))
--        (s: finseqfj | length(s`seqfj) > 0,
--        
theorem binsert_one_level_length : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧ k ≤ maxk indx.List.length ∧ (k = 1 → indx.seq 0 = 0)}), ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {n : {i : {i : ℝ // i ≥ 0} // i > 0} // (is_indx_p s.seqfj indx ∧
  PartialERforK_p s indx k (min (t k) (List.length indx - 1)) n ∧
   oddNstructPreservation_p s n)}), List.length (binsert_one_level s k n indx.seqfj) ≠ 0 := by
  -- === TCCs for binsert_one_level_length ===
  -- TCC: binsert_one_level_length_TCC1
  -- Statement: FORALL (k: posnat, indx: finseq[nat]):
  --   length(indx) > 0 AND k <= maxk(indx`length) AND k = 1 IMPLIES
  --    0 < indx`length
  have binsert_one_level_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: binsert_one_level_length_TCC2
  -- Statement: FORALL (k: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0),
  --         s: finseqfj[T, leq] | length(s`seqfj) >...
  have binsert_one_level_length_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: binsert_one_level_length_TCC3
  -- Statement: FORALL (k: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0),
  --         s: finseqfj[T, leq] | length(s`seqfj) >...
  have binsert_one_level_length_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: binsert_one_level_length_TCC4
  -- Statement: FORALL (k: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0),
  --         s: finseqfj[T, leq] | length(s`seqfj) >...
  have binsert_one_level_length_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: binsert_one_level_length_TCC5
  -- Statement: FORALL (k: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0),
  --         s: finseqfj[T, leq] | length(s`seqfj) >...
  have binsert_one_level_length_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.binsert_one_level_gives_permutation
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0))
--        (s: finseqfj | length(s`seqfj) > 0,
--        
theorem binsert_one_level_gives_permutation : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧ k ≤ maxk indx.List.length ∧ (k = 1 → indx.seq 0 = 0)}), ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {n : {i : {i : ℝ // i ≥ 0} // i > 0} // (is_indx_p s.seqfj indx ∧
  PartialERforK_p s indx k (min (t k) (List.length indx - 1)) n ∧
   oddNstructPreservation_p s n)}), permutation s (binsert_one_level s k n indx) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.binsert_one_level_go_to_nstruct
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat,
--         indx: finseq[nat]
--             | length(indx) > 0 AND
--                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0))
--        (s: finseqfj | length(s`seqfj) > 0,
--        
theorem binsert_one_level_go_to_nstruct : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (indx : {indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {i : ℝ // i ≥ 0}) // List.length indx > 0 ∧ k ≤ maxk indx.List.length ∧ (k = 1 → indx.seq 0 = 0)}), ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {n : {i : {i : ℝ // i ≥ 0} // i > 0} // (is_indx_p s.seqfj indx ∧
  PartialERforK_p s indx k (min (t k) (List.length indx - 1)) n ∧
   oddNstructPreservation_p s n)}), fun (sk : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T))) => nstructER_basic_p sk (n - 1) (binsert_one_level s k n indx) := by
  -- === TCCs for binsert_one_level_go_to_nstruct ===
  -- TCC: binsert_one_level_go_to_nstruct_TCC1
  -- Statement: FORALL (k: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0),
  --         s: finseqfj[T, leq] | length(s`seqfj) >...
  have binsert_one_level_go_to_nstruct_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: binsert_one_level_go_to_nstruct_TCC2
  -- Statement: FORALL (k: posnat,
  --         indx: finseq[nat]
  --             | length(indx) > 0 AND
  --                k <= maxk(indx`length) AND (k = 1 => indx`seq(0) = 0),
  --         s: finseqfj[T, leq] | length(s`seqfj) >...
  have binsert_one_level_go_to_nstruct_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.nstructER_basic_equiv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0, n: posnat):
--   LET indx =
--         (# length := length(s`seqfj),
--            seq := LAMBDA (i: below[length(s`seqfj)]): i #)
--     IN
--     nstructER_basic?(s, n) =
theorem nstructER_basic_equiv : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}), fun (indx : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length s.seqfj} → {s : {i : ℝ // i ≥ 0} // s < List.length s.seqfj})) => nstructER_basic_p s n → (PartialERforK_p s indx 1 (min (t 1) (List.length s.seqfj - 1)) n ∧
  oddNstructPreservation_p s n) (# List.length := List.length s.seqfj) (seq := fun (i : Fin List.length s.seqfj)=> i #) := by
  -- === TCCs for nstructER_basic_equiv ===
  -- TCC: nstructER_basic_equiv_TCC1
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx:
  --           [# length: nat,
  --              seq: [below[length(s`seqfj)] -> below[length(s`seqfj)]] #]):
  --   indx =
  --    (# length...
  have nstructer_basic_equiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nstructER_basic_equiv_TCC2
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0, n: posnat,
  --         indx:
  --           [# length: nat,
  --              seq: [below[length(s`seqfj)] -> below[length(s`seqfj)]] #]):
  --   indx =
  --    (# length...
  have nstructer_basic_equiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.seqfjFatten
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN s
-- ELSE seqfjFatten(binsert_one_level(s, 1, n,
--                                    (# length := length(s`seqfj),
--                                       seq
--                                  
axiom seqfjFatten {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} : pvs_τ76 → pvs_τ77 → pvs_τ78

-- pvs2lean: ford_johnson.seqfjFatten_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0,
--         n: nat | nstructER_basic?(s, n)):
--   seqfjFatten(s, n)`seqfj`length /= 0
theorem seqfjFatten_length : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {n : {i : ℝ // i ≥ 0} // nstructER_basic_p s n}), seqfjFatten s n.seqfj.List.length ≠ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.seqfjFatten_gives_permutation
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0,
--         n: nat | nstructER_basic?(s, n)):
--   permutation(s, seqfjFatten(s, n))
theorem seqfjFatten_gives_permutation : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {n : {i : ℝ // i ≥ 0} // nstructER_basic_p s n}), permutation s (seqfjFatten s n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.seqfjFatten_gives_down_nstructER_basic
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseqfj | length(s`seqfj) > 0,
--         n: nat | nstructER_basic?(s, n)):
--   nstructER_basic?(seqfjFatten(s, n), 0)
theorem seqfjFatten_gives_down_nstructER_basic : ∀ (s : {s : List (fjnode T) × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T)) // List.length s.seqfj > 0}) (n : {n : {i : ℝ // i ≥ 0} // nstructER_basic_p s n}), nstructER_basic_p (seqfjFatten s n) 0 := by
  -- === TCCs for seqfjFatten_gives_down_nstructER_basic ===
  -- TCC: seqfjFatten_gives_down_nstructER_basic_TCC1
  -- Statement: FORALL (s: finseqfj[T, leq] | length(s`seqfj) > 0,
  --         n: nat | nstructER_basic?(s, n)):
  --   length(seqfjFatten(s, n)`seqfj) > 0
  have seqfjfatten_gives_down_nstructer_basic_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ford_johnson.mergeInsertion
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sc: finseqfj = (# seqfj := s, oddList := null #) IN
--   LET h = logb(length(s), 2) IN seqfjFatten(seqfjBottleneck(sc), h)`seqfj
axiom mergeInsertion {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ47 → pvs_τ48

-- pvs2lean: ford_johnson.mergeInsertion_is_ok
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: finseq[fjnode[T]]
--             | plain_finseqfj((# seqfj := s, oddList := null #)) AND
--                length(s) > 0):
--   permutation(s, mergeInsertion(s)) AND is_sorted?(mergeInsertion(s))
theorem mergeInsertion_is_ok : ∀ (s : {s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → fjnode T) // plain_finseqfj ((# seqfj := s, oddList := null #)) ∧ List.length s > 0}), permutation s (mergeInsertion s) ∧ is_sorted_p (mergeInsertion s) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.ford_johnson