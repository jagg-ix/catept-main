-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sorting_aux
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sorting_aux

Auto-generated stubs from PVSLib theory `sorting_aux`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sorting_aux

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (append : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (car : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (cdr : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (cons : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (cons_p : pvs_τ10 → Prop)
variable {pvs_τ14 : Type*} (length : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (member : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (nth : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} (null_p : pvs_τ20 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (reverse : pvs_τ28 → pvs_τ29)

-- pvs2lean: sorting_aux.elim_list
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x = car(l) THEN cdr(l) ELSE cons(car(l), elim_list(cdr(l), x)) ENDIF
axiom elim_list {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13

-- pvs2lean: sorting_aux.elim_list_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: T, l: list[T]):
--   member(n, l) IMPLIES length(elim_list(l, n)) = length(l) - 1
theorem elim_list_length : ∀ (n : T) (l : List T), member n l → List.length (elim_list l n) = List.length l - 1 := by
  -- === TCCs for elim_list_length ===
  -- TCC: elim_list_length_TCC1
  -- Statement: FORALL (n: T, l: list[T]): member(n, l) IMPLIES NOT null?[T](l)
  have elim_list_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.preservation_contents
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T] | NOT null?(l), i: T):
--   (FORALL (k: below[length(l)]): NOT nth(l, k) = i) =>
--    (FORALL (k: below[length(cdr(l))]): NOT nth(cdr(l), k) = i)
theorem preservation_contents : ∀ (l : {l : List T // ¬ null_p l}) (i : T), (∀ (k : Fin List.length l), ¬ nth l k = i) → (∀ (k : Fin List.length (cdr l)), ¬ nth (cdr l) k = i) := by
  -- === TCCs for preservation_contents ===
  -- TCC: preservation_contents_TCC1
  -- Statement: FORALL (l: list[T] | NOT null?[T](l), i: T):
  --   (FORALL (k: below[length(l)]): NOT nth(l, k) = i) IMPLIES cons?[T](l)
  have preservation_contents_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.nth_implies_member
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], i: T, k: below[length(l)]):
--   nth(l, k) = i => member(i, l)
theorem nth_implies_member : ∀ (l : List T) (i : T) (k : {s : {i : ℝ // i ≥ 0} // s < List.length l}), nth l k = i → member i l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.member_implies_exists_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T] | NOT null?(l), i: T):
--   member(i, l) => EXISTS (k: below[length(l)]): nth(l, k) = i
theorem member_implies_exists_nth : ∀ (l : {l : List T // ¬ null_p l}) (i : T), member i l → ∃ (k : Fin List.length l), nth l k = i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.nth_of_list_is_nth_append_list
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], k: below[length(l1)]):
--   nth(l1, k) = nth(append(l1, l2), k)
theorem nth_of_list_is_nth_append_list : ∀ (l1 : List T) (l2 : List T) (k : {s : {i : ℝ // i ≥ 0} // s < List.length l1}), nth l1 k = nth (append l1 l2) k := by
  -- === TCCs for nth_of_list_is_nth_append_list ===
  -- TCC: nth_of_list_is_nth_append_list_TCC1
  -- Statement: FORALL (l1, l2: list[T], k: below[length[T](l1)]):
  --   k < length[T](append[T](l1, l2))
  have nth_of_list_is_nth_append_list_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.nth_of_cons
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], k: T, x: below[length(l) + 1]):
--   IF x = 0 THEN nth(cons(k, l), x) = k
--   ELSE nth(cons(k, l), x) = nth(l, x - 1)
--   ENDIF
theorem nth_of_cons : ∀ (l : List T) (k : T) (x : {s : {i : ℝ // i ≥ 0} // s < 1 + List.length l}), IF (x = 0) (nth (cons k l) x = k) (nth (cons k l) x = nth l (x - 1)) := by
  -- === TCCs for nth_of_cons ===
  -- TCC: nth_of_cons_TCC1
  -- Statement: FORALL (l: list[T], k: T, x: below[1 + length[T](l)]):
  --   x = 0 IMPLIES x < length[T](cons[T](k, l))
  have nth_of_cons_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_of_cons_TCC2
  -- Statement: FORALL (l: list[T], k: T, x: below[1 + length[T](l)]):
  --   NOT x = 0 IMPLIES x < length[T](cons[T](k, l))
  have nth_of_cons_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_of_cons_TCC3
  -- Statement: FORALL (l: list[T], k: T, x: below[1 + length[T](l)]):
  --   NOT x = 0 IMPLIES x - 1 >= 0 AND x - 1 < length[T](l)
  have nth_of_cons_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.nth_of_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], (i: below[length(l1) + length(l2)])):
--   nth(append(l1, l2), i) =
--    (IF i < length(l1) THEN nth(l1, i) ELSE nth(l2, i - length(l1)) ENDIF)
theorem nth_of_append : ∀ (l1 : List T) (l2 : List T) (i : {s : {i : ℝ // i ≥ 0} // s < List.length l1 + List.length l2}), nth (append l1 l2) i = (if i < List.length l1 then nth l1 i else nth l2 (i - List.length l1) ) := by
  -- === TCCs for nth_of_append ===
  -- TCC: nth_of_append_TCC1
  -- Statement: FORALL (l1, l2: list[T], (i: below[length[T](l1) + length[T](l2)])):
  --   i < length[T](append[T](l1, l2))
  have nth_of_append_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_of_append_TCC2
  -- Statement: FORALL (l1, l2: list[T], (i: below[length[T](l1) + length[T](l2)])):
  --   NOT i < length(l1) IMPLIES
  --    i - length[T](l1) >= 0 AND i - length[T](l1) < length[T](l2)
  have nth_of_append_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.null_rev_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): null?(l) => null?(reverse(l))
theorem null_rev_null : ∀ (l : List T), null_p l → null_p (reverse l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.nth_member
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, (l: list[T] | m < length(l)), k: T):
--   nth(l, m) = k => member(k, l)
theorem nth_member : ∀ (m : {i : ℝ // i ≥ 0}) (l : {l : List T // m < List.length l}) (k : T), nth l m = k → member k l := by
  -- === TCCs for nth_member ===
  -- TCC: nth_member_TCC1
  -- Statement: FORALL (m: nat, (l: list[T] | m < length[T](l)), k: T): m < length[T](l)
  have nth_member_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.car_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((l: list[T] | length(l) > 1)):
--   car(reverse(cdr(l))) = car(reverse(l))
theorem car_rev : ∀ (l : {l : List T // List.length l > 1}), car (reverse (cdr l)) = car (reverse l) := by
  -- === TCCs for car_rev ===
  -- TCC: car_rev_TCC1
  -- Statement: FORALL ((l: list[T] | length[T](l) > 1)): cons?[T](l)
  have car_rev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: car_rev_TCC2
  -- Statement: FORALL ((l: list[T] | length[T](l) > 1)): cons?[T](reverse[T](cdr[T](l)))
  have car_rev_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: car_rev_TCC3
  -- Statement: FORALL ((l: list[T] | length[T](l) > 1)): cons?[T](reverse[T](l))
  have car_rev_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.nth_rev_zero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((l: list[T] | NOT null?(l))):
--   nth(l, 0) = nth(reverse(l), length(l) - 1)
theorem nth_rev_zero : ∀ (l : {l : List T // ¬ null_p l}), nth l 0 = nth (reverse l) (List.length l - 1) := by
  -- === TCCs for nth_rev_zero ===
  -- TCC: nth_rev_zero_TCC1
  -- Statement: FORALL ((l: list[T] | NOT null?[T](l))): 0 < length[T](l)
  have nth_rev_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_rev_zero_TCC2
  -- Statement: FORALL ((l: list[T] | NOT null?[T](l))):
  --   length[T](l) - 1 >= 0 AND length[T](l) - 1 < length[T](reverse[T](l))
  have nth_rev_zero_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.nth_rev_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((l: list[T] | NOT null?(l))):
--   nth(l, length(l) - 1) = nth(reverse(l), 0)
theorem nth_rev_last : ∀ (l : {l : List T // ¬ null_p l}), nth l (List.length l - 1) = nth (reverse l) 0 := by
  -- === TCCs for nth_rev_last ===
  -- TCC: nth_rev_last_TCC1
  -- Statement: FORALL ((l: list[T] | NOT null?[T](l))):
  --   length[T](l) - 1 >= 0 AND length[T](l) - 1 < length[T](l)
  have nth_rev_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_rev_last_TCC2
  -- Statement: FORALL ((l: list[T] | NOT null?[T](l))): 0 < length[T](reverse[T](l))
  have nth_rev_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.nth_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, (l: list[T] | NOT null?(l) AND m < length(l)), p: list[T]):
--   nth(append(l, p), m) = nth(l, m)
theorem nth_append : ∀ (m : {i : ℝ // i ≥ 0}) (l : {l : List T // ¬ null_p l ∧ m < List.length l}) (p : List T), nth (append l p) m = nth l m := by
  -- === TCCs for nth_append ===
  -- TCC: nth_append_TCC1
  -- Statement: FORALL (m: nat, (l: list[T] | NOT null?[T](l) AND m < length[T](l)),
  --         p: list[T]):
  --   m < length[T](append[T](l, p))
  have nth_append_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nth_append_TCC2
  -- Statement: FORALL (m: nat, (l: list[T] | NOT null?[T](l) AND m < length[T](l))):
  --   m < length[T](l)
  have nth_append_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.nth_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat):
--   FORALL (l: list[T] | NOT null?(l) AND m < length(l)):
--     nth(l, m) = nth(reverse(l), length(l) - 1 - m)
theorem nth_rev : ∀ (m : {i : ℝ // i ≥ 0}), ∀ (l : {l : List T // ¬ null_p l ∧ m < List.length l}), nth l m = nth (reverse l) (List.length l - 1 - m) := by
  -- === TCCs for nth_rev ===
  -- TCC: nth_rev_TCC1
  -- Statement: FORALL (m: nat, l: list[T] | NOT null?[T](l) AND m < length[T](l)):
  --   length[T](l) - 1 - m >= 0 AND
  --    length[T](l) - 1 - m < length[T](reverse[T](l))
  have nth_rev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.occurrences
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(l) THEN 0
-- ELSIF car(l) = x THEN 1 + occurrences(cdr(l))(x)
-- ELSE occurrences(cdr(l))(x)
-- ENDIF
axiom occurrences {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: sorting_aux.permutations
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: T): occurrences(l1)(x) = occurrences(l2)(x)
axiom permutations {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ23 → pvs_τ24 → Prop

-- pvs2lean: sorting_aux.occurrences_of_nth_element
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], i: below[length(l)]):
--   NOT null?(l) IMPLIES occurrences(l)(nth(l, i)) > 0
theorem occurrences_of_nth_element : ∀ (l : List T) (i : {s : {i : ℝ // i ≥ 0} // s < List.length l}), ¬ null_p l → occurrences l(nth l i) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.occurrences_of_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], x: T):
--   occurrences(append(l1, l2))(x) = occurrences(l1)(x) + occurrences(l2)(x)
theorem occurrences_of_app : ∀ (l1 : List T) (l2 : List T) (x : T), occurrences (append l1 l2) (x) = occurrences l1(x) + occurrences l2(x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.cons_of_perm_is_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], x: T):
--   permutations(l1, l2) IMPLIES permutations(cons(x, l1), cons(x, l2))
theorem cons_of_perm_is_permutation : ∀ (l1 : List T) (l2 : List T) (x : T), permutations l1 l2 → permutations (cons x l1) (cons x l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.ocurrence_list_has_element
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], x: T):
--   occurrences(l)(x) /= 0 IMPLIES EXISTS (i: below[length(l)]): nth(l, i) = x
theorem ocurrence_list_has_element : ∀ (l : List T) (x : T), occurrences l(x) ≠ 0 → ∃ (i : Fin List.length l), nth l i = x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.no_occurrence_implies
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], x: T):
--   occurrences(l)(x) = 0 IMPLIES FORALL (i: below[length(l)]): nth(l, i) /= x
theorem no_occurrence_implies : ∀ (l : List T) (x : T), occurrences l(x) = 0 → ∀ (i : Fin List.length l), nth l i ≠ x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.member_implies_pos_occurrences
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], x: T): member(x, l) IMPLIES occurrences(l)(x) > 0
theorem member_implies_pos_occurrences : ∀ (l : List T) (x : T), member x l → occurrences l(x) > 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.occurrences_implies_member
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], x: T): occurrences(l)(x) > 0 IMPLIES member(x, l)
theorem occurrences_implies_member : ∀ (l : List T) (x : T), occurrences l(x) > 0 → member x l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.permutations_preserve_contents
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], i: below[length(l1)]):
--   permutations(l1, l2) IMPLIES
--    EXISTS (j: below[length(l2)]): nth(l2, j) = nth(l1, i)
theorem permutations_preserve_contents : ∀ (l1 : List T) (l2 : List T) (i : {s : {i : ℝ // i ≥ 0} // s < List.length l1}), permutations l1 l2 → ∃ (j : Fin List.length l2), nth l2 j = nth l1 i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.permutations_is_commutative
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: permutations(l1, l2) IFF permutations(l2, l1)
theorem permutations_is_commutative : permutations l1 l2 ↔ permutations l2 l1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.permutations_is_transitive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: permutations(l, l1) AND permutations(l1, l2) IMPLIES permutations(l, l2)
theorem permutations_is_transitive : permutations l l1 ∧ permutations l1 l2 → permutations l l2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.cons_elim_is_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(x, l) IMPLIES permutations(l, cons(x, elim_list(l, x)))
theorem cons_elim_is_permutation : member x l → permutations l (cons x (elim_list l x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.same_occurrences_same_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l, l1: list[T]):
--   (FORALL (x: T): occurrences(l)(x) = occurrences(l1)(x)) IMPLIES
--    length(l) = length(l1)
theorem same_occurrences_same_length : ∀ (l : List T) (l1 : List T), (∀ (x : T), occurrences l(x) = occurrences l1(x)) → List.length l = List.length l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.permutations_is_ext_reflexive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], l1: list[T] | length(l1) = length(l)):
--   (FORALL (i: below[length(l)]): nth(l, i) = nth(l1, i)) =>
--    permutations(l, l1)
theorem permutations_is_ext_reflexive : ∀ (l : List T) (l1 : {l1 : List T // List.length l1 = List.length l}), (∀ (i : Fin List.length l), nth l i = nth l1 i) → permutations l l1 := by
  -- === TCCs for permutations_is_ext_reflexive ===
  -- TCC: permutations_is_ext_reflexive_TCC1
  -- Statement: FORALL (l: list[T], l1: list[T] | length[T](l1) = length[T](l),
  --         i: below[length[T](l)]):
  --   i < length[T](l1)
  have permutations_is_ext_reflexive_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.suffix
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN l ELSE suffix(cdr(l), n - 1) ENDIF
axiom suffix {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} : pvs_τ30 → pvs_τ31 → pvs_τ32

-- pvs2lean: sorting_aux.prefix
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN null ELSE cons(car(l), prefix(cdr(l), n - 1)) ENDIF
axiom pvs_prefix {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ25 → pvs_τ26 → pvs_τ27

-- pvs2lean: sorting_aux.app_prefix_suffix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l) + 1]):
--   l = append(prefix(l, n), suffix(l, n))
theorem app_prefix_suffix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < 1 + List.length l}), l = append (prefix l n) (suffix l n) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.length_suffix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l) + 1]):
--   length(suffix(l, n)) = length(l) - n
theorem length_suffix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < 1 + List.length l}), List.length (suffix l n) = List.length l - n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.length_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l) + 1]): length(prefix(l, n)) = n
theorem length_prefix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < 1 + List.length l}), List.length (prefix l n) = n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.contents_suffix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l) + 1], i: below[length(l) - n]):
--   nth(suffix(l, n), i) = nth(l, i + n)
theorem contents_suffix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < 1 + List.length l}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length l - n}), nth (suffix l n) i = nth l (i + n) := by
  -- === TCCs for contents_suffix ===
  -- TCC: contents_suffix_TCC1
  -- Statement: FORALL (l: list[T], n: below[1 + length[T](l)]): length[T](l) - n >= 0
  have contents_suffix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: contents_suffix_TCC2
  -- Statement: FORALL (l: list[T], n: below[1 + length[T](l)],
  --         i: below[length[T](l) - n]):
  --   i < length[T](suffix(l, n))
  have contents_suffix_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: contents_suffix_TCC3
  -- Statement: FORALL (l: list[T], n: below[1 + length[T](l)],
  --         i: below[length[T](l) - n]):
  --   i + n < length[T](l)
  have contents_suffix_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.contents_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l) + 1], i: below[n]):
--   nth(prefix(l, n), i) = nth(l, i)
theorem contents_prefix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < 1 + List.length l}) (i : {s : {i : ℝ // i ≥ 0} // s < n}), nth (prefix l n) i = nth l i := by
  -- === TCCs for contents_prefix ===
  -- TCC: contents_prefix_TCC1
  -- Statement: FORALL (l: list[T], n: below[1 + length[T](l)], i: below[n]):
  --   i < length[T](prefix(l, n))
  have contents_prefix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: contents_prefix_TCC2
  -- Statement: FORALL (l: list[T], n: below[1 + length[T](l)], i: below[n]):
  --   i < length[T](l)
  have contents_prefix_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_aux.permutations_of_appends
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l, l1, l2, l3: list[T]):
--   permutations(l, l1) AND permutations(l2, l3) IMPLIES
--    permutations(append(l, l2), append(l1, l3))
theorem permutations_of_appends : ∀ (l : List T) (l1 : List T) (l2 : List T) (l3 : List T), permutations l l1 ∧ permutations l2 l3 → permutations (append l l2) (append l1 l3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.permutations_of_app_suff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l, l1, l2, l3: list[T]):
--   permutations(l, l1) AND permutations(append(l, l2), append(l1, l3))
--    IMPLIES permutations(l2, l3)
theorem permutations_of_app_suff : ∀ (l : List T) (l1 : List T) (l2 : List T) (l3 : List T), permutations l l1 ∧ permutations (append l l2) (append l1 l3) → permutations l2 l3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_aux.permutations_of_app_pref
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l, l1, l2, l3: list[T]):
--   permutations(l2, l3) AND permutations(append(l, l2), append(l1, l3))
--    IMPLIES permutations(l, l1)
theorem permutations_of_app_pref : ∀ (l : List T) (l1 : List T) (l2 : List T) (l3 : List T), permutations l2 l3 ∧ permutations (append l l2) (append l1 l3) → permutations l l1 := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.sorting_aux