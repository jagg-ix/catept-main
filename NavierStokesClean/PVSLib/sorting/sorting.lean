-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sorting
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sorting

Auto-generated stubs from PVSLib theory `sorting`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sorting

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (append : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (car : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (cdr : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cons : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (converse : pvs_τ11 → pvs_τ12)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (length : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (leq : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (member : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (nth : pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} (null_p : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (reverse : pvs_τ24 → pvs_τ25)

-- pvs2lean: sorting.lt
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT leq(y, x)
noncomputable def lt : sorry := by
  sorry  -- pvs2lean: definition stub for NOT leq(y, x)

-- pvs2lean: sorting.lt_le
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: T): lt(x, y) IMPLIES leq(x, y)
theorem lt_le : ∀ (x : T) (y : T), lt x y → leq x y := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting.gt
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: converse(lt)
noncomputable def gt : sorry := by
  sorry  -- pvs2lean: definition stub for converse(lt)

-- pvs2lean: sorting.gt_not_le
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: T): gt(x, y) IFF NOT leq(x, y)
theorem gt_not_le : ∀ (x : T) (y : T), gt x y ↔ ¬ leq x y := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting.geq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: converse(leq)
noncomputable def geq : sorry := by
  sorry  -- pvs2lean: definition stub for converse(leq)

-- pvs2lean: sorting.search
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(l) THEN length(l)
-- ELSIF car(l) = i THEN 0
-- ELSE 1 + search(i, cdr(l))
-- ENDIF
axiom search {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ26 → pvs_τ27 → Prop

-- pvs2lean: sorting.not_in_l_gives_lenght_l
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], i: T):
--   (FORALL (k: below[length(l)]): NOT nth(l, k) = i) =>
--    search(i, l) = length(l)
theorem not_in_l_gives_lenght_l : ∀ (l : List T) (i : T), (∀ (k : Fin List.length l), ¬ nth l k = i) → search i l = List.length l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting.not_member_gives_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], i: T): NOT member(i, l) => search(i, l) = length(l)
theorem not_member_gives_length : ∀ (l : List T) (i : T), ¬ member i l → search i l = List.length l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting.search_works
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], k: T): member(k, l) => nth(l, search(k, l)) = k
theorem search_works : ∀ (l : List T) (k : T), member k l → nth l (search k l) = k := by
  -- === TCCs for search_works ===
  -- TCC: search_works_TCC1
  -- Statement: FORALL (l: list[T], k: T): member(k, l) IMPLIES search(k, l) < length[T](l)
  have search_works_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting.search_min_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], k: T):
--   FORALL (m: below[length(l)]): nth(l, m) = k => search(k, l) <= m
theorem search_min_index : ∀ (l : List T) (k : T), ∀ (m : {s : {i : ℝ // i ≥ 0} // s < List.length l}), nth l m = k → search k l ≤ m := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting.search_in_rev_max_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], k: T):
--   FORALL (m: below[length(l)]):
--     nth(l, m) = k => (length(l) - 1) - search(k, reverse(l)) >= m
theorem search_in_rev_max_index : ∀ (l : List T) (k : T), ∀ (m : {s : {i : ℝ // i ≥ 0} // s < List.length l}), nth l m = k → (List.length l - 1) - search k (reverse l) ≥ m := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting.search_in_append_of_lists_key_in_the_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], k: T):
--   member(k, l1) => search(k, append(l1, l2)) = search(k, l1)
theorem search_in_append_of_lists_key_in_the_first : ∀ (l1 : List T) (l2 : List T) (k : T), member k l1 → search k (append l1 l2) = search k l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting.search_in_append_of_lists_key_not_member_of_the_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], k: T):
--   NOT member(k, l1) =>
--    search(k, append(l1, l2)) = length(l1) + search(k, l2)
theorem search_in_append_of_lists_key_not_member_of_the_first : ∀ (l1 : List T) (l2 : List T) (k : T), ¬ member k l1 → search k (append l1 l2) = List.length l1 + search k l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting.search_in_append_of_lists_with_searched_key
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], k: T):
--   search(k, append(l1, cons(k, l2))) <= length(l1)
theorem search_in_append_of_lists_with_searched_key : ∀ (l1 : List T) (l2 : List T) (k : T), search k (append l1 (cons k l2)) ≤ List.length l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting.is_sorted?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: below[length(l)]):
--   0 <= k AND k <= length(l) - 2 => leq(nth(l, k), nth(l, k + 1))
axiom is_sorted_p {pvs_τ13 : Type*} : pvs_τ13 → Prop

-- pvs2lean: sorting.is_sorted_implies_monotone
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_sorted?(l) IMPLIES
--  FORALL (j: below[length(l)], i: below[j]): leq(nth(l, i), nth(l, j))
theorem is_sorted_implies_monotone : is_sorted_p l → ∀ (j : Fin List.length l) (i : Fin j), leq (nth l i) (nth l j) := by
  -- === TCCs for is_sorted_implies_monotone ===
  -- TCC: is_sorted_implies_monotone_TCC1
  -- Statement: FORALL (l: list[T]):
  --   is_sorted?(l) IMPLIES
  --    FORALL (j: below[length[T](l)], i: below[j]): i < length[T](l)
  have is_sorted_implies_monotone_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting.cdr_of_sorted_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]):
--   NOT null?(l) AND is_sorted?(l) IMPLIES is_sorted?(cdr(l))
theorem cdr_of_sorted_is_sorted : ∀ (l : List T), ¬ null_p l ∧ is_sorted_p l → is_sorted_p (cdr l) := by
  -- === TCCs for cdr_of_sorted_is_sorted ===
  -- TCC: cdr_of_sorted_is_sorted_TCC1
  -- Statement: FORALL (l: list[T]): NOT null?(l) AND is_sorted?(l) IMPLIES cons?[T](l)
  have cdr_of_sorted_is_sorted_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.sorting