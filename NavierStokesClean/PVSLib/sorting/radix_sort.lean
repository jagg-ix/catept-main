-- Auto-generated by pvs2lean/translator.py
-- PVS theory: radix_sort
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: radix_sort

Auto-generated stubs from PVSLib theory `radix_sort`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.radix_sort

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (is_sorted_p : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (leq : pvs_τ4 → pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (merge_sort : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (permutations : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ20 : Type*} (total_preorder_p : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (upto : pvs_τ21 → pvs_τ22)

-- pvs2lean: radix_sort.radixsort_aux
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = N THEN l ELSE radixsort_aux(merge_sort[T, leq(i)](l), i + 1) ENDIF
axiom radixsort_aux {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19

-- pvs2lean: radix_sort.radixsort
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: radixsort_aux(l, 0)
axiom radixsort {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ15 → pvs_τ16

-- pvs2lean: radix_sort.radixsort_permutes
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): permutations[T](radixsort(l), l)
theorem radixsort_permutes : ∀ (l : List T), permutations (radixsort l) l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: radix_sort.lex
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: i = 0 OR
--  (i > 0 AND
--    ((leq(i - 1)(x, y) AND NOT leq(i - 1)(y, x)) OR
--      (leq(i - 1)(x, y) AND leq(i - 1)(y, x) AND lex(i - 1)(x, y))))
axiom lex {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: radix_sort.lex_weak_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: T, P: [upto(N) -> boolean]):
--   (FORALL (i: upto(N)):
--      i = 0 OR
--       (i > 0 AND
--         ((leq(i - 1)(x, y) AND NOT leq(i - 1)(y, x)) OR
--           (leq(i - 1)(x, y) AND leq(i - 1)(y, 
theorem lex_weak_induction : ∀ (x : T) (y : T) (P : {s : {i : ℝ // i ≥ 0} // s ≤ N} → Bool), ∀ (i : {k : ℕ // k ≤ N}), i = 0 ∨
   (i > 0 ∧
     ((leq (i - 1) (x, y) ∧ ¬ leq (i - 1) (y, x)) ∨
       (leq (i - 1) (x, y) ∧ leq (i - 1) (y, x) ∧ P (i - 1))))
   → P i → ∀ (i : {k : ℕ // k ≤ N}), lex i(x, y) → P i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: radix_sort.lex_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: T, P: [upto(N) -> boolean]):
--   (FORALL (i: upto(N)):
--      i = 0 OR
--       (i > 0 AND
--         ((leq(i - 1)(x, y) AND NOT leq(i - 1)(y, x)) OR
--           (leq(i - 1)(x, y) AND
--             le
theorem lex_induction : ∀ (x : T) (y : T) (P : {s : {i : ℝ // i ≥ 0} // s ≤ N} → Bool), ∀ (i : {k : ℕ // k ≤ N}), i = 0 ∨
   (i > 0 ∧
     ((leq (i - 1) (x, y) ∧ ¬ leq (i - 1) (y, x)) ∨
       (leq (i - 1) (x, y) ∧
         leq (i - 1) (y, x) ∧ lex (i - 1) (x, y) ∧ P (i - 1))))
   → P i → ∀ (i : {k : ℕ // k ≤ N}), lex i(x, y) → P i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: radix_sort.lex_total_preorder
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: upto[N]): total_preorder?[T](lex(i))
theorem lex_total_preorder : ∀ (i : {s : {i : ℝ // i ≥ 0} // s ≤ N}), total_preorder_p (lex i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: radix_sort.merge_sort_lex_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], i: below(N) | is_sorted?[T, lex(i)](l)):
--   is_sorted?[T, lex(1 + i)](merge_sort[T, leq(i)](l))
theorem merge_sort_lex_sorted : ∀ (l : List T) (i : {i : {s : {i : ℝ // i ≥ 0} // s < N} // is_sorted_p l}), is_sorted_p (merge_sort l) := by
  -- === TCCs for merge_sort_lex_sorted ===
  -- TCC: merge_sort_lex_sorted_TCC1
  -- Statement: FORALL (i: below(N)): i <= N
  have merge_sort_lex_sorted_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_sort_lex_sorted_TCC2
  -- Statement: FORALL (i: below(N)): total_preorder?[T](lex(i))
  have merge_sort_lex_sorted_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_sort_lex_sorted_TCC3
  -- Statement: FORALL (l: list[T], i: below(N) | is_sorted?[T, lex(i)](l)): 1 + i <= N
  have merge_sort_lex_sorted_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: merge_sort_lex_sorted_TCC4
  -- Statement: FORALL (l: list[T], i: below(N) | is_sorted?[T, lex(i)](l)):
  --   total_preorder?[T](lex(1 + i))
  have merge_sort_lex_sorted_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: radix_sort.radix_sort_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], i: upto[N] | is_sorted?[T, lex(i)](l)):
--   is_sorted?[T, lex(N)](radixsort_aux(l, i))
theorem radix_sort_is_sorted : ∀ (l : List T) (i : {i : {s : {i : ℝ // i ≥ 0} // s ≤ N} // is_sorted_p l}), is_sorted_p (radixsort_aux l i) := by
  -- === TCCs for radix_sort_is_sorted ===
  -- TCC: radix_sort_is_sorted_TCC1
  -- Statement: FORALL (i: upto[N]): total_preorder?[T](lex(i))
  have radix_sort_is_sorted_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: radix_sort_is_sorted_TCC2
  -- Statement: N <= N
  have radix_sort_is_sorted_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: radix_sort_is_sorted_TCC3
  -- Statement: total_preorder?[T](lex(N))
  have radix_sort_is_sorted_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: radix_sort_is_sorted_TCC4
  -- Statement: FORALL (l: list[T], i: upto[N] | is_sorted?[T, lex(i)](l),
  --         v:
  --           [[l1: list[T], {i: upto[N] | is_sorted?[T, lex(i)](l1)}] ->
  --              (is_sorted?[T, lex(N)])]):
  --   (v =
  --     LAMBDA...
  have radix_sort_is_sorted_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: radix_sort_is_sorted_TCC5
  -- Statement: FORALL (l: list[T], i: upto[N] | is_sorted?[T, lex(i)](l),
  --         v:
  --           [[l1: list[T], {i: upto[N] | is_sorted?[T, lex(i)](l1)}] ->
  --              (is_sorted?[T, lex(N)])]):
  --   (v =
  --     LAMBDA...
  have radix_sort_is_sorted_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: radix_sort.radixsort_sorts
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): is_sorted?[T, lex(N)](radixsort(l))
theorem radixsort_sorts : ∀ (l : List T), is_sorted_p (radixsort l) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.radix_sort