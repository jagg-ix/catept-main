-- Auto-generated by pvs2lean/translator.py
-- PVS theory: mergesort
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: mergesort

Auto-generated stubs from PVSLib theory `mergesort`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.mergesort

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (append : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (car : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (cdr : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cons : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (floor : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (is_sorted_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (length : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (leq : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (nth : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} (null_p : pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (occurrences : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (permutations : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (pvs_prefix : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} (suffix : pvs_τ33 → pvs_τ34 → pvs_τ35)

-- pvs2lean: mergesort.merge
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(l1) OR null?(l2) THEN append(l1, l2)
-- ELSIF leq(car(l1), car(l2)) THEN cons(car(l1), merge(cdr(l1), l2))
-- ELSE cons(car(l2), merge(l1, cdr(l2)))
-- ENDIF
axiom merge {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19

-- pvs2lean: mergesort.merge_size
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T]): length(merge(l1, l2)) = length(l1) + length(l2)
theorem merge_size : ∀ (l1 : List T) (l2 : List T), List.length (merge l1 l2) = List.length l1 + List.length l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_occurrence
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T], x: T):
--   occurrences(merge(l1, l2))(x) = occurrences(l1)(x) + occurrences(l2)(x)
theorem merge_occurrence : ∀ (l1 : List T) (l2 : List T) (x : T), occurrences (merge l1 l2) (x) = occurrences l1(x) + occurrences l2(x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_is_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T]): permutations(merge(l1, l2), append(l1, l2))
theorem merge_is_permutation : ∀ (l1 : List T) (l2 : List T), permutations (merge l1 l2) (append l1 l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_of_sorted_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T]):
--   is_sorted?(l1) AND is_sorted?(l2) IMPLIES is_sorted?(merge(l1, l2))
theorem merge_of_sorted_is_sorted : ∀ (l1 : List T) (l2 : List T), is_sorted_p l1 ∧ is_sorted_p l2 → is_sorted_p (merge l1 l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_sort
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(l) <= 1 THEN l
-- ELSE merge(merge_sort(prefix(l, floor(length(l) / 2))),
--            merge_sort(suffix(l, floor(length(l) / 2))))
-- ENDIF
axiom merge_sort {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ20 → pvs_τ21

-- pvs2lean: mergesort.merge_sort_size
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): length(merge_sort(l)) = length(l)
theorem merge_sort_size : ∀ (l : List T), List.length (merge_sort l) = List.length l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_sort_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): is_sorted?(merge_sort(l))
theorem merge_sort_is_sorted : ∀ (l : List T), is_sorted_p (merge_sort l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_sort_is_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): permutations(merge_sort(l), l)
theorem merge_sort_is_permutation : ∀ (l : List T), permutations (merge_sort l) l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_sort_works
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]):
--   is_sorted?(merge_sort(l)) AND permutations(merge_sort(l), l)
theorem merge_sort_works : ∀ (l : List T), is_sorted_p (merge_sort l) ∧ permutations (merge_sort l) l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_is_conservative
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (is_sorted?), m, n: below[length(merge(l1, l2))]):
--   (m <= n AND
--     leq(nth(merge(l1, l2), m), nth(merge(l1, l2), n)) AND
--      leq(nth(merge(l1, l2), n), nth(merge(l1, l2), m)))
--    IM
theorem merge_is_conservative : ∀ (l1 : List T) (l2 : List T) (m : {s : {i : ℝ // i ≥ 0} // s < List.length (merge l1 l2)}) (n : {s : {i : ℝ // i ≥ 0} // s < List.length (merge l1 l2)}), (m ≤ n ∧
  leq (nth (merge l1 l2) m) (nth (merge l1 l2) n) ∧
   leq (nth (merge l1 l2) n) (nth (merge l1 l2) m)) → ∃ (i : _) (j : Fin List.length (append l1 l2)), i ≤ j ∧
   nth (merge l1 l2) m = nth (append l1 l2) i ∧
    nth (merge l1 l2) n = nth (append l1 l2) j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mergesort.merge_sort_is_conservative
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], m, n: below[length(merge_sort(l))]):
--   (m <= n AND
--     leq(nth(merge_sort(l), m), nth(merge_sort(l), n)) AND
--      leq(nth(merge_sort(l), n), nth(merge_sort(l), m)))
--    IMPLIES
--    E
theorem merge_sort_is_conservative : ∀ (l : List T) (m : {s : {i : ℝ // i ≥ 0} // s < List.length (merge_sort l)}) (n : {s : {i : ℝ // i ≥ 0} // s < List.length (merge_sort l)}), (m ≤ n ∧
  leq (nth (merge_sort l) m) (nth (merge_sort l) n) ∧
   leq (nth (merge_sort l) n) (nth (merge_sort l) m)) → ∃ (i : _) (j : Fin List.length l), i ≤ j ∧
   nth (merge_sort l) m = nth l i ∧ nth (merge_sort l) n = nth l j := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.mergesort