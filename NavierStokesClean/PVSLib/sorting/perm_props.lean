-- Auto-generated by pvs2lean/translator.py
-- PVS theory: perm_props
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: perm_props

Auto-generated stubs from PVSLib theory `perm_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.perm_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (append : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (below : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (car : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (cdr : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (cons_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (length : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (nth : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (occurrences : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (permutations : pvs_τ17 → pvs_τ18 → Prop)

-- pvs2lean: perm_props.remove
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: append(IF i = 0 THEN null[T] ELSE l ^ (0, i - 1) ENDIF,
--        l ^ (i + 1, length(l) - 1))
axiom remove {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21

-- pvs2lean: perm_props.remove_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?[T]), i: below(length(l))):
--   length(remove(l, i)) = length(l) - 1
theorem remove_length : ∀ (l : List T) (i : {s : {i : ℝ // i ≥ 0} // s < List.length l}), List.length (remove l i) = List.length l - 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perm_props.remove_cdr
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((l: list[T] | length(l) >= 2), (i: below(length(l)) | i > 0)):
--   cdr(remove(l, i)) = remove(cdr(l), i - 1)
theorem remove_cdr : ∀ (l : {l : List T // List.length l ≥ 2}) (i : {i : {s : {i : ℝ // i ≥ 0} // s < List.length l} // i > 0}), cdr (remove l i) = remove (cdr l) (i - 1) := by
  -- === TCCs for remove_cdr ===
  -- TCC: remove_cdr_TCC1
  -- Statement: FORALL ((l: list[T] | length[T](l) >= 2), (i: below(length(l)) | i > 0)):
  --   cons?[T](l)
  have remove_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_cdr_TCC2
  -- Statement: FORALL ((l: list[T] | length[T](l) >= 2), (i: below(length(l)) | i > 0)):
  --   cons?[T](remove(l, i))
  have remove_cdr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_cdr_TCC3
  -- Statement: FORALL ((l: list[T] | length[T](l) >= 2), (i: below(length(l)) | i > 0)):
  --   cons?[T](cdr[T](l))
  have remove_cdr_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remove_cdr_TCC4
  -- Statement: FORALL ((l: list[T] | length[T](l) >= 2), (i: below(length(l)) | i > 0)):
  --   i - 1 >= 0 AND i - 1 < length[T](cdr[T](l))
  have remove_cdr_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: perm_props.list_splitting_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?[T]), i: below(length(l))):
--   l =
--    append(append(IF i = 0 THEN null[T] ELSE l ^ (0, i - 1) ENDIF,
--                  (: nth(l, i) :)),
--           l ^ (i + 1, length(l) - 1))
theorem list_splitting_nth : ∀ (l : List T) (i : {s : {i : ℝ // i ≥ 0} // s < List.length l}), l = append (append (if i = 0 then null else l ^ (0, i - 1)) ((: nth l i :))) (l ^ (i + 1, List.length l - 1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perm_props.occurrences_remove_not
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?[T]), i: below(length(l)), x: T):
--   x /= nth(l, i) IMPLIES occurrences(l)(x) = occurrences(remove(l, i))(x)
theorem occurrences_remove_not : ∀ (l : List T) (i : {s : {i : ℝ // i ≥ 0} // s < List.length l}) (x : T), x ≠ nth l i → occurrences l(x) = occurrences (remove l i) (x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perm_props.occurrences_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?[T]), i: below(length(l))):
--   occurrences(l)(nth(l, i)) = occurrences(remove(l, i))(nth(l, i)) + 1
theorem occurrences_remove : ∀ (l : List T) (i : {s : {i : ℝ // i ≥ 0} // s < List.length l}), occurrences l(nth l i) = occurrences (remove l i) (nth l i) + 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perm_props.remove_perm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: (cons?[T])):
--   permutations(l1, l2) IMPLIES
--    EXISTS (i: below(length(l2))):
--      (car(l1) = nth(l2, i) AND permutations(cdr(l1), remove(l2, i)))
theorem remove_perm : ∀ (l1 : List T) (l2 : List T), permutations l1 l2 → ∃ (i : Finset.range List.length l2), (car l1 = nth l2 i ∧ permutations (cdr l1) (remove l2 i)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.perm_props