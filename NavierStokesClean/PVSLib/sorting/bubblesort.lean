-- Auto-generated by pvs2lean/translator.py
-- PVS theory: bubblesort
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: bubblesort

Auto-generated stubs from PVSLib theory `bubblesort`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.bubblesort

noncomputable section

variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (car : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cdr : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (cons : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} (is_sorted_p : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (length : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (leq : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (nth : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} (null_p : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (permutations : pvs_τ22 → pvs_τ23 → Prop)

-- pvs2lean: bubblesort.bubbling
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN l
-- ELSIF gt(car(l), car(cdr(l)))
--   THEN cons(car(cdr(l)), bubbling(cons(car(l), cdr(cdr(l))), n - 1))
-- ELSE cons(car(l), bubbling(cdr(l), n - 1))
-- ENDIF
axiom bubbling {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: bubblesort.bubbling_preserves_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]): length(bubbling(l, n)) = length(l)
theorem bubbling_preserves_length : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), List.length (bubbling l n) = List.length l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bubblesort.bubbling_preserves_suffix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]):
--   LET l1 = bubbling(l, n) IN
--     FORALL (i: below[length(l)]): i > n IMPLIES nth(l1, i) = nth(l, i)
theorem bubbling_preserves_suffix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), fun (l1 : List T) => ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length l}), i > n → nth l1 i = nth l i (bubbling l n) := by
  -- === TCCs for bubbling_preserves_suffix ===
  -- TCC: bubbling_preserves_suffix_TCC1
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], l1: list[T]):
  --   l1 = bubbling(l, n) IMPLIES
  --    FORALL (i: below[length[T](l)]): i > n IMPLIES i < length[T](l1)
  have bubbling_preserves_suffix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort_aux
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN l ELSE bubblesort_aux(bubbling(l, n), n - 1) ENDIF
axiom bubblesort_aux {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: bubblesort.bubblesort_aux_preserves_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]):
--   length(bubblesort_aux(l, n)) = length(l)
theorem bubblesort_aux_preserves_length : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), List.length (bubblesort_aux l n) = List.length l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(l) THEN l ELSE bubblesort_aux(l, length(l) - 1) ENDIF
axiom bubblesort {pvs_τ1 : Type*} {pvs_τ2 : Type*} : pvs_τ1 → pvs_τ2

-- pvs2lean: bubblesort.bubbling_preseves_contents
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]): permutations(l, bubbling(l, n))
theorem bubbling_preseves_contents : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), permutations l (bubbling l n) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bubblesort.bubbling_preserves_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]):
--   FORALL (i: below[n + 1]):
--     EXISTS (j: below[n + 1]): nth(l, i) = nth(bubbling(l, n), j)
theorem bubbling_preserves_prefix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < 1 + n}), ∃ (j : {s : {i : ℝ // i ≥ 0} // s < 1 + n}), nth l i = nth (bubbling l n) j := by
  -- === TCCs for bubbling_preserves_prefix ===
  -- TCC: bubbling_preserves_prefix_TCC1
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], i: below[1 + n],
  --         j: below[1 + n]):
  --   i < length[T](l)
  have bubbling_preserves_prefix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: bubbling_preserves_prefix_TCC2
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], i: below[1 + n],
  --         j: below[1 + n]):
  --   j < length[T](bubbling(l, n))
  have bubbling_preserves_prefix_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bubblesort.bubbling_bubbles
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]):
--   LET l1 = bubbling(l, n) IN
--     FORALL (i: nat): i <= n IMPLIES leq(nth(l1, i), nth(l1, n))
theorem bubbling_bubbles : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), fun (l1 : List T) => ∀ (i : {i : ℝ // i ≥ 0}), i ≤ n → leq (nth l1 i) (nth l1 n) (bubbling l n) := by
  -- === TCCs for bubbling_bubbles ===
  -- TCC: bubbling_bubbles_TCC1
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], l1: list[T]):
  --   l1 = bubbling(l, n) IMPLIES
  --    FORALL (i: nat): i <= n IMPLIES i < length[T](l1)
  have bubbling_bubbles_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: bubbling_bubbles_TCC2
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], l1: list[T]):
  --   l1 = bubbling(l, n) IMPLIES
  --    FORALL (i: nat): i <= n IMPLIES n < length[T](l1)
  have bubbling_bubbles_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort_aux_preserves_suffix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]):
--   LET l1 = bubblesort_aux(l, n) IN
--     FORALL (i: below[length(l)]): i > n IMPLIES nth(l1, i) = nth(l, i)
theorem bubblesort_aux_preserves_suffix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), fun (l1 : List T) => ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length l}), i > n → nth l1 i = nth l i (bubblesort_aux l n) := by
  -- === TCCs for bubblesort_aux_preserves_suffix ===
  -- TCC: bubblesort_aux_preserves_suffix_TCC1
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], l1: list[T]):
  --   l1 = bubblesort_aux(l, n) IMPLIES
  --    FORALL (i: below[length[T](l)]): i > n IMPLIES i < length[T](l1)
  have bubblesort_aux_preserves_suffix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort_aux_is_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]):
--   permutations(l, bubblesort_aux(l, n))
theorem bubblesort_aux_is_permutation : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), permutations l (bubblesort_aux l n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort_aux_preserves_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)]):
--   LET l1 = bubblesort_aux(l, n) IN
--     FORALL (i: below[n + 1]):
--       EXISTS (j: below[n + 1]): nth(l1, i) = nth(l, j)
theorem bubblesort_aux_preserves_prefix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}), fun (l1 : List T) => ∀ (i : {s : {i : ℝ // i ≥ 0} // s < 1 + n}), ∃ (j : {s : {i : ℝ // i ≥ 0} // s < 1 + n}), nth l1 i = nth l j (bubblesort_aux l n) := by
  -- === TCCs for bubblesort_aux_preserves_prefix ===
  -- TCC: bubblesort_aux_preserves_prefix_TCC1
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], l1: list[T]):
  --   l1 = bubblesort_aux(l, n) IMPLIES
  --    FORALL (i: below[1 + n], j: below[1 + n]): i < length[T](l1)
  have bubblesort_aux_preserves_prefix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: bubblesort_aux_preserves_prefix_TCC2
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], l1: list[T]):
  --   l1 = bubblesort_aux(l, n) IMPLIES
  --    FORALL (i: below[1 + n], j: below[1 + n]): j < length[T](l)
  have bubblesort_aux_preserves_prefix_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort_is_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): permutations(l, bubblesort(l))
theorem bubblesort_is_permutation : ∀ (l : List T), permutations l (bubblesort l) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort_aux_sorts_prefix
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], n: below[length(l)], i: below[n]):
--   leq(nth(bubblesort_aux(l, n), i), nth(bubblesort_aux(l, n), i + 1))
theorem bubblesort_aux_sorts_prefix : ∀ (l : List T) (n : {s : {i : ℝ // i ≥ 0} // s < List.length l}) (i : {s : {i : ℝ // i ≥ 0} // s < n}), leq (nth (bubblesort_aux l n) i) (nth (bubblesort_aux l n) (i + 1)) := by
  -- === TCCs for bubblesort_aux_sorts_prefix ===
  -- TCC: bubblesort_aux_sorts_prefix_TCC1
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], i: below[n]):
  --   i < length[T](bubblesort_aux(l, n))
  have bubblesort_aux_sorts_prefix_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: bubblesort_aux_sorts_prefix_TCC2
  -- Statement: FORALL (l: list[T], n: below[length[T](l)], i: below[n]):
  --   i + 1 < length[T](bubblesort_aux(l, n))
  have bubblesort_aux_sorts_prefix_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): is_sorted?(bubblesort(l))
theorem bubblesort_is_sorted : ∀ (l : List T), is_sorted_p (bubblesort l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: bubblesort.bubblesort_works
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]):
--   is_sorted?(bubblesort(l)) AND permutations(l, bubblesort(l))
theorem bubblesort_works : ∀ (l : List T), is_sorted_p (bubblesort l) ∧ permutations l (bubblesort l) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.bubblesort