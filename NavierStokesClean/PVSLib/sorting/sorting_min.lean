-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sorting_min
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sorting_min

Auto-generated stubs from PVSLib theory `sorting_min`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sorting_min

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (car : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (cdr : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (cons : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (geq : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (is_sorted_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (length : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (leq : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (list2finseq : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (member : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (nth : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} (null_p : pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (permutations : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} (reverse : pvs_τ27 → Prop)

-- pvs2lean: sorting_min.switching_min
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF list2finseq(l)`length < 2 THEN l
-- ELSIF lt(car(l), car(cdr(l)))
--   THEN cons(car(cdr(l)), switching_min(cons(car(l), cdr(cdr(l)))))
-- ELSE cons(car(l), switching_min(cdr(l)))
-- ENDIF
axiom switching_min {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ30 → pvs_τ31

-- pvs2lean: sorting_min.reverse_is_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): permutations(reverse(l), l)
theorem reverse_is_permutation : ∀ (l : List T), permutations (reverse l) l := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_min.swch_min_preserves_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): length(l) = length(switching_min(l))
theorem swch_min_preserves_length : ∀ (l : List T), List.length l = List.length (switching_min l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_min.swch_min_preserves_contents
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], i: T): member(i, l) IMPLIES member(i, switching_min(l))
theorem swch_min_preserves_contents : ∀ (l : List T) (i : T), member i l → member i (switching_min l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_min.swch_min_is_permutation
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): permutations(l, switching_min(l))
theorem swch_min_is_permutation : ∀ (l : List T), permutations l (switching_min l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_min.last_swch_min_element_is_least
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], k: below[list2finseq(l)`length]):
--   leq(nth(switching_min(l), length(l) - 1), nth(l, k))
theorem last_swch_min_element_is_least : ∀ (l : List T) (k : {s : {i : ℝ // i ≥ 0} // s < list2finseq l.List.length}), leq (nth (switching_min l) (List.length l - 1)) (nth l k) := by
  -- === TCCs for last_swch_min_element_is_least ===
  -- TCC: last_swch_min_element_is_least_TCC1
  -- Statement: FORALL (l: list[T], k: below[list2finseq[T](l)`length]):
  --   length[T](l) - 1 >= 0 AND length[T](l) - 1 < length[T](switching_min(l))
  have last_swch_min_element_is_least_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: last_swch_min_element_is_least_TCC2
  -- Statement: FORALL (l: list[T], k: below[list2finseq[T](l)`length]): k < length[T](l)
  have last_swch_min_element_is_least_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_min.sorting_min
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF list2finseq(l)`length < 2 THEN l
-- ELSE LET rev_sw_min = reverse(switching_min(l)) IN
--        cons(car(rev_sw_min), sorting_min(cdr(rev_sw_min)))
-- ENDIF
axiom sorting_min {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ28 → pvs_τ29

-- pvs2lean: sorting_min.sorting_min_preserves_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): length(l) = length(sorting_min(l))
theorem sorting_min_preserves_length : ∀ (l : List T), List.length l = List.length (sorting_min l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_min.sorting_min_is_permutations
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): permutations(l, sorting_min(l))
theorem sorting_min_is_permutations : ∀ (l : List T), permutations l (sorting_min l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_min.swch_min_moves_min_to_end
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]):
--   NOT null?(l) IMPLIES
--    FORALL (k: below[length(l)]):
--      geq(nth(switching_min(l), k), nth(switching_min(l), length(l) - 1))
theorem swch_min_moves_min_to_end : ∀ (l : List T), ¬ null_p l → ∀ (k : Fin List.length l), geq (nth (switching_min l) k) (nth (switching_min l) (List.length l - 1)) := by
  -- === TCCs for swch_min_moves_min_to_end ===
  -- TCC: swch_min_moves_min_to_end_TCC1
  -- Statement: FORALL (l: list[T]):
  --   NOT null?(l) IMPLIES
  --    FORALL (k: below[length[T](l)]): k < length[T](switching_min(l))
  have swch_min_moves_min_to_end_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: swch_min_moves_min_to_end_TCC2
  -- Statement: FORALL (l: list[T]):
  --   NOT null?(l) IMPLIES
  --    FORALL (k: below[length[T](l)]):
  --      length[T](l) - 1 >= 0 AND
  --       length[T](l) - 1 < length[T](switching_min(l))
  have swch_min_moves_min_to_end_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_min.sorting_min_moves_min_to_begining
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]):
--   NOT null?(l) IMPLIES
--    FORALL (k: below[list2finseq(l)`length]):
--      leq(nth(sorting_min(l), 0), nth(sorting_min(l), k))
theorem sorting_min_moves_min_to_begining : ∀ (l : List T), ¬ null_p l → ∀ (k : Fin list2finseq l.List.length), leq (nth (sorting_min l) 0) (nth (sorting_min l) k) := by
  -- === TCCs for sorting_min_moves_min_to_begining ===
  -- TCC: sorting_min_moves_min_to_begining_TCC1
  -- Statement: FORALL (l: list[T]):
  --   NOT null?(l) IMPLIES
  --    FORALL (k: below[list2finseq[T](l)`length]):
  --      0 < length[T](sorting_min(l))
  have sorting_min_moves_min_to_begining_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sorting_min_moves_min_to_begining_TCC2
  -- Statement: FORALL (l: list[T]):
  --   NOT null?(l) IMPLIES
  --    FORALL (k: below[list2finseq[T](l)`length]):
  --      k < length[T](sorting_min(l))
  have sorting_min_moves_min_to_begining_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_min.sorting_min_work
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]):
--   is_sorted?(sorting_min(l)) AND permutations(l, sorting_min(l))
theorem sorting_min_work : ∀ (l : List T), is_sorted_p (sorting_min l) ∧ permutations l (sorting_min l) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.sorting_min