-- Auto-generated by pvs2lean/translator.py
-- PVS theory: maxsort
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: maxsort

Auto-generated stubs from PVSLib theory `maxsort`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.maxsort

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (finseq_appl : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (geq : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (is_sorted_p : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (length : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (leq : pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (permutations : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (psorted : pvs_τ23 → pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (swap : pvs_τ26 → pvs_τ27 → pvs_τ28 → pvs_τ29)

-- pvs2lean: maxsort.move_max
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = j
--   THEN IF gt(finseq_appl[T](v)(i - 1), finseq_appl[T](v)(i))
--          THEN swap(v)(i - 1, i)
--        ELSE v
--        ENDIF
-- ELSIF gt(finseq_appl[T](v)(i - 1), finseq_appl[T](v)(i))
--   THEN move_ma
axiom move_max {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20

-- pvs2lean: maxsort.move_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (i: posnat), (j: nat | j < length(v) AND j >= i)):
--   length(move_max(v)(j, i)) = length(v)
theorem move_max_length : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (j : {j : {i : ℝ // i ≥ 0} // j < List.length v ∧ j ≥ i}), List.length (move_max v(j, i)) = List.length v := by
  -- === TCCs for move_max_length ===
  -- TCC: move_max_length_TCC1
  -- Statement: FORALL (v: finite_sequence[T], (i: posnat),
  --         (j: nat | j < length(v) AND j >= i)):
  --   j >= i
  have move_max_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.move_max_preserves_out
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (i: posnat), (j: nat | j < length(v) AND j >= i),
--         (k: nat | k < length(v) AND (k < i - 1 OR k > j))):
--   finseq_appl[T](v)(k) = finseq_appl(move_max(v)(j, i))(k)
theorem move_max_preserves_out : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (j : {j : {i : ℝ // i ≥ 0} // j < List.length v ∧ j ≥ i}) (k : {k : {i : ℝ // i ≥ 0} // k < List.length v ∧ (k < i - 1 ∨ k > j)}), finseq_appl v(k) = finseq_appl (move_max v(j, i)) (k) := by
  -- === TCCs for move_max_preserves_out ===
  -- TCC: move_max_preserves_out_TCC1
  -- Statement: FORALL (v: finite_sequence[T], (i: posnat),
  --         (j: nat | j < length(v) AND j >= i),
  --         (k: nat | k < length(v) AND (k < i - 1 OR k > j))):
  --   k < length(move_max(v)(j, i))
  have move_max_preserves_out_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.move_max_preserves
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (i: posnat), (j: nat | j < length(v) AND j >= i),
--         (k: nat | k < length(v) AND (k >= i - 1 AND k <= j))):
--   EXISTS (l: nat | l >= i - 1 AND l <= j):
--     finseq_appl[T](v)(k) = finseq
theorem move_max_preserves : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (j : {j : {i : ℝ // i ≥ 0} // j < List.length v ∧ j ≥ i}) (k : {k : {i : ℝ // i ≥ 0} // k < List.length v ∧ (k ≥ i - 1 ∧ k ≤ j)}), ∃ (l : {l : {i : ℝ // i ≥ 0} // l ≥ i - 1 ∧ l ≤ j}), finseq_appl v(k) = finseq_appl (move_max v(j, i)) (l) := by
  -- === TCCs for move_max_preserves ===
  -- TCC: move_max_preserves_TCC1
  -- Statement: FORALL (v: finite_sequence[T], (i: posnat),
  --         (j: nat | j < length(v) AND j >= i),
  --         (k: nat | k < length(v) AND (k >= i - 1 AND k <= j)),
  --         l: nat | l >= i - 1 AND l <= j):
  --   l <...
  have move_max_preserves_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.move_max_preserves2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (i: posnat), (j: nat | j < length(v) AND j >= i),
--         (k: nat | k < length(v) AND (k >= i - 1 AND k <= j))):
--   EXISTS (l: nat | l >= i - 1 AND l <= j):
--     finseq_appl[T](v)(l) = finseq
theorem move_max_preserves2 : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (j : {j : {i : ℝ // i ≥ 0} // j < List.length v ∧ j ≥ i}) (k : {k : {i : ℝ // i ≥ 0} // k < List.length v ∧ (k ≥ i - 1 ∧ k ≤ j)}), ∃ (l : {l : {i : ℝ // i ≥ 0} // l ≥ i - 1 ∧ l ≤ j}), finseq_appl v(l) = finseq_appl (move_max v(j, i)) (k) := by
  -- === TCCs for move_max_preserves2 ===
  -- TCC: move_max_preserves2_TCC1
  -- Statement: FORALL (v: finite_sequence[T], (i: posnat),
  --         (j: nat | j < length(v) AND j >= i),
  --         (k: nat | k < length(v) AND (k >= i - 1 AND k <= j)),
  --         l: nat | l >= i - 1 AND l <= j):
  --   l <...
  have move_max_preserves2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: move_max_preserves2_TCC2
  -- Statement: FORALL (v: finite_sequence[T], (i: posnat),
  --         (j: nat | j < length(v) AND j >= i),
  --         (k: nat | k < length(v) AND (k >= i - 1 AND k <= j)),
  --         l: nat | l >= i - 1 AND l <= j):
  --   k <...
  have move_max_preserves2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.move_max_works
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (i: posnat), (j: nat | j < length(v) AND j >= i),
--         (k: nat | k >= i - 1 AND k <= j)):
--   geq(finseq_appl(move_max(v)(j, i))(j), finseq_appl(move_max(v)(j, i))(k))
theorem move_max_works : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (j : {j : {i : ℝ // i ≥ 0} // j < List.length v ∧ j ≥ i}) (k : {k : {i : ℝ // i ≥ 0} // k ≥ i - 1 ∧ k ≤ j}), geq (finseq_appl (move_max v(j, i)) (j)) (finseq_appl (move_max v(j, i)) (k)) := by
  -- === TCCs for move_max_works ===
  -- TCC: move_max_works_TCC1
  -- Statement: FORALL (v: finite_sequence[T], (i: posnat),
  --         (j: nat | j < length(v) AND j >= i),
  --         (k: nat | k >= i - 1 AND k <= j)):
  --   j < length(move_max(v)(j, i))
  have move_max_works_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: move_max_works_TCC2
  -- Statement: FORALL (v: finite_sequence[T], (i: posnat),
  --         (j: nat | j < length(v) AND j >= i),
  --         (k: nat | k >= i - 1 AND k <= j)):
  --   k < length(move_max(v)(j, i))
  have move_max_works_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.move_max_permutes
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (i: posnat), (j: nat | j < length(v) AND j >= i)):
--   permutations(v, move_max(v)(j, i))
theorem move_max_permutes : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (j : {j : {i : ℝ // i ≥ 0} // j < List.length v ∧ j ≥ i}), permutations v (move_max v(j, i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.maxsort_aux
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF j = 0 THEN v ELSE maxsort_aux(move_max(v)(j, 1))(j - 1) ENDIF
axiom maxsort_aux {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16

-- pvs2lean: maxsort.maxsort_aux_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (j: below[length(v)])): length(maxsort_aux(v)(j)) = length(v)
theorem maxsort_aux_length : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (j : {s : {i : ℝ // i ≥ 0} // s < List.length v}), List.length (maxsort_aux v(j)) = List.length v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.maxsort_aux_psorts
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (j: nat | j < length(v) - 1)):
--   (psorted(v)(j + 1, length(v) - 1) AND
--     FORALL (i: below[j + 1]):
--       leq(finseq_appl[T](v)(i), finseq_appl[T](v)(j + 1)))
--    => is_sorted?(maxsort_aux(
theorem maxsort_aux_psorts : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (j : {j : {i : ℝ // i ≥ 0} // j < List.length v - 1}), (psorted v(j + 1, List.length v - 1) ∧
  ∀ (i : Fin j + 1), leq (finseq_appl v(i)) (finseq_appl v(j + 1))) → is_sorted_p (maxsort_aux v(j)) := by
  -- === TCCs for maxsort_aux_psorts ===
  -- TCC: maxsort_aux_psorts_TCC1
  -- Statement: FORALL (v: finite_sequence[T], (j: nat | j < length(v) - 1)):
  --   j + 1 < length(v)
  have maxsort_aux_psorts_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: maxsort_aux_psorts_TCC2
  -- Statement: FORALL (v: finite_sequence[T], (j: nat | j < length(v) - 1)):
  --   length(v) - 1 >= 0 AND
  --    length(v) - 1 < length(v) AND 1 + j <= length(v) - 1
  have maxsort_aux_psorts_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: maxsort_aux_psorts_TCC3
  -- Statement: FORALL (v: finite_sequence[T], (j: nat | j < length(v) - 1)):
  --   psorted(v)(j + 1, length(v) - 1) IMPLIES
  --    FORALL (i: below[1 + j]): i < length(v)
  have maxsort_aux_psorts_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: maxsort_aux_psorts_TCC4
  -- Statement: FORALL (v: finite_sequence[T], (j: nat | j < length(v) - 1)):
  --   (psorted(v)(j + 1, length(v) - 1) AND
  --     FORALL (i: below[1 + j]):
  --       leq(finseq_appl[T](v)(i), finseq_appl[T](v)(j + 1)))
  --    IMP...
  have maxsort_aux_psorts_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.maxsort_aux_permutes
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, (j: nat | j < length(v) - 1)): permutations(v, maxsort_aux(v)(j))
theorem maxsort_aux_permutes : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (j : {j : {i : ℝ // i ≥ 0} // j < List.length v - 1}), permutations v (maxsort_aux v(j)) := by
  -- === TCCs for maxsort_aux_permutes ===
  -- TCC: maxsort_aux_permutes_TCC1
  -- Statement: FORALL (v: finite_sequence[T], (j: nat | j < length(v) - 1)): j < length(v)
  have maxsort_aux_permutes_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.maxsort
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(v) < 2 THEN v ELSE maxsort_aux(v)(length(v) - 1) ENDIF
axiom maxsort {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ12 → pvs_τ13

-- pvs2lean: maxsort.maxsort_sorts
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v): is_sorted?(maxsort(v))
theorem maxsort_sorts : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), is_sorted_p (maxsort v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.maxsort_permutes
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v): permutations(v, maxsort(v))
theorem maxsort_permutes : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), permutations v (maxsort v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: maxsort.maxsort_works
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v): is_sorted?(maxsort(v)) AND permutations(v, maxsort(v))
theorem maxsort_works : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), is_sorted_p (maxsort v) ∧ permutations v (maxsort v) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.maxsort