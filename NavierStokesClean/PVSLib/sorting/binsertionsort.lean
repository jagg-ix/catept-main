-- Auto-generated by pvs2lean/translator.py
-- PVS theory: binsertionsort
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: binsertionsort

Auto-generated stubs from PVSLib theory `binsertionsort`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.binsertionsort

noncomputable section

variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (delete : pvs_τ10 → pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} (floor : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (geq : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (insert_p : pvs_τ16 → pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} (is_sorted_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (length : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (leq : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (permutations : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (v1 : pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28 → Prop)

-- pvs2lean: binsertionsort.bin_search
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = j THEN IF leq(k, finseq_appl[T](v)(i)) THEN i ELSE i + 1 ENDIF
-- ELSE LET l = floor((i + j) / 2) IN
--        IF k = finseq_appl[T](v)(l) THEN l
--        ELSIF lt(k, finseq_appl[T](v)(l))
--          THE
axiom bin_search {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: binsertionsort.binsertionsort_aux
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF j = 0 THEN v
-- ELSE LET l = bin_search(v)(finseq_appl[T](v)(j))(0, j - 1),
--          w = insert?(finseq_appl[T](v)(j), delete(v, j), l)
--        IN
--        IF j = length(v) - 1 THEN w ELSE binsertionsort
axiom binsertionsort_aux {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9

-- pvs2lean: binsertionsort.binsertionsort
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(v) <= 1 THEN v ELSE binsertionsort_aux(v)(1) ENDIF
axiom binsertionsort {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6

-- pvs2lean: binsertionsort.b_search_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v) (k) (i: below[length(v)], (j: below[length(v)] | i <= j)):
--   bin_search(v)(k)(i, j) >= i AND bin_search(v)(k)(i, j) <= j + 1
theorem b_search_bound : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), ∀ (k : T), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length v}) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length v} // i ≤ j}), bin_search v(k) (i, j) ≥ i ∧ bin_search v(k) (i, j) ≤ j + 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: binsertionsort.b_search_preserves_sorting
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v) (k) (i: below[length(v)], (j: below[length(v)] | i <= j)):
--   is_sorted?(v) AND
--    (i = 0 OR geq(k, finseq_appl[T](v)(i - 1))) AND
--     (j = v`length - 1 OR leq(k, finseq_appl[T](v)(j + 1)))
-- 
theorem b_search_preserves_sorting : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), ∀ (k : T), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length v}) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length v} // i ≤ j}), is_sorted_p v ∧
 (i = 0 ∨ geq k (finseq_appl v(i - 1))) ∧
  (j = v.List.length - 1 ∨ leq k (finseq_appl v(j + 1))) → is_sorted_p (insert_p k v (bin_search v(k) (i, j))) := by
  -- === TCCs for b_search_preserves_sorting ===
  -- TCC: b_search_preserves_sorting_TCC1
  -- Statement: FORALL (v: finite_sequence[T], k: T, i: below[length(v)],
  --         (j: below[length(v)] | i <= j)):
  --   is_sorted?(v) AND NOT i = 0 IMPLIES i - 1 >= 0 AND i - 1 < length(v)
  have b_search_preserves_sorting_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC2
  -- Statement: FORALL (v: finite_sequence[T], k: T, i: below[length(v)],
  --         (j: below[length(v)] | i <= j)):
  --   is_sorted?(v) AND
  --    (i = 0 OR geq(k, finseq_appl[T](v)(i - 1))) AND NOT j = v`length - 1
  --    IMP...
  have b_search_preserves_sorting_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC3
  -- Statement: FORALL (v: finite_sequence[T], k: T, i: below[length(v)],
  --         (j: below[length(v)] | i <= j)):
  --   is_sorted?(v) AND
  --    (i = 0 OR geq(k, finseq_appl[T](v)(i - 1))) AND
  --     (j = v`length - 1 OR le...
  have b_search_preserves_sorting_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC1
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   is_sorted?(s) AND NOT i = 0 IMPLIES i - 1 >= 0 AND i - 1 < length(s)
  have b_search_preserves_sorting_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC2
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   is_sorted?(s) AND
  --    (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
  --   ...
  have b_search_preserves_sorting_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_preserves_sorting_TCC3
  -- Statement: FORALL (s: finseq[fjnode[T]], k: fjnode[T], i: below[length(s)],
  --         (j: below[length(s)] | i <= j)):
  --   is_sorted?(s) AND
  --    (i = 0 OR leq(val(finseq_appl[fjnode[T]](s)(i - 1)), val(k))) AND
  --   ...
  have b_search_preserves_sorting_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: binsertionsort.b_search_translation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: finite_sequence[T] | length(v) >= 2) (k)
--        (j: below[length(v) - 1], i: upto[j]):
--   bin_search(v)(k)(i, j) = bin_search(v ^ (0, j))(k)(i, j)
theorem b_search_translation : ∀ (v : {v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length v ≥ 2}), ∀ (k : T), ∀ (j : {s : {i : ℝ // i ≥ 0} // s < List.length v - 1}) (i : {s : {i : ℝ // i ≥ 0} // s ≤ j}), bin_search v(k) (i, j) = bin_search (v ^ (0, j)) (k) (i, j) := by
  -- === TCCs for b_search_translation ===
  -- TCC: b_search_translation_TCC1
  -- Statement: FORALL (v: finite_sequence[T] | length(v) >= 2, k: T): length(v) - 1 >= 0
  have b_search_translation_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_translation_TCC2
  -- Statement: FORALL (v: finite_sequence[T] | length(v) >= 2, k: T,
  --         j: below[length(v) - 1], i: upto[j]):
  --   i < length(v)
  have b_search_translation_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_translation_TCC3
  -- Statement: FORALL (v: finite_sequence[T] | length(v) >= 2, k: T,
  --         j: below[length(v) - 1], i: upto[j]):
  --   j < length(v) AND i <= j
  have b_search_translation_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_translation_TCC4
  -- Statement: FORALL (v: finite_sequence[T] | length(v) >= 2, k: T,
  --         j: below[length(v) - 1], i: upto[j]):
  --   i < length(^[T](v, (0, j)))
  have b_search_translation_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_search_translation_TCC5
  -- Statement: FORALL (v: finite_sequence[T] | length(v) >= 2, k: T,
  --         j: below[length(v) - 1], i: upto[j]):
  --   j < length(^[T](v, (0, j))) AND i <= j
  have b_search_translation_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: binsertionsort.bs_aux_perm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((v: finite_sequence[T] | length(v) >= 2), (j: below[length(v)])):
--   permutations(v, binsertionsort_aux(v)(j))
theorem bs_aux_perm : ∀ (v : {v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length v ≥ 2}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length v}), permutations v (binsertionsort_aux v(j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: binsertionsort.bs_aux_sorts
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: finite_sequence[T] | length(v) >= 2,
--         j: below[length(v)] | j > 0):
--   is_sorted?(v ^ (0, j - 1)) => is_sorted?(binsertionsort_aux(v)(j))
theorem bs_aux_sorts : ∀ (v : {v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length v ≥ 2}) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length v} // j > 0}), is_sorted_p (v ^ (0, j - 1)) → is_sorted_p (binsertionsort_aux v(j)) := by
  -- === TCCs for bs_aux_sorts ===
  -- TCC: bs_aux_sorts_TCC1
  -- Statement: FORALL (v: finite_sequence[T] | length(v) >= 2,
  --         j: below[length(v)] | j > 0):
  --   j - 1 >= 0
  have bs_aux_sorts_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: binsertionsort.binsertion_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v): permutations(v, binsertionsort(v))
theorem binsertion_permutation : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), permutations v (binsertionsort v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: binsertionsort.binsertion_sorts
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v): is_sorted?(binsertionsort(v))
theorem binsertion_sorts : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), is_sorted_p (binsertionsort v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: binsertionsort.binsertionsort_works
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v):
--   is_sorted?(binsertionsort(v)) AND permutations(v, binsertionsort(v))
theorem binsertionsort_works : ∀ (v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), is_sorted_p (binsertionsort v) ∧ permutations v (binsertionsort v) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.binsertionsort