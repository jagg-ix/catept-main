-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sorting_seq
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sorting_seq

Auto-generated stubs from PVSLib theory `sorting_seq`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sorting_seq

noncomputable section

variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (cdr : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} (cons_p : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (delete : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (finseq2list : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (finseq2list_rec : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (finseq_appl : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} (floor : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (geq : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (insert_p : pvs_τ22 → pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ36 : Type*} (length : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (leq : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ48 : Type*} (reflexive_p : pvs_τ48 → Prop)
variable {pvs_τ56 : Type*} (symmetric_p : pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} (transitive_p : pvs_τ57 → Prop)

-- pvs2lean: sorting_seq.occurrences
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(h) = 0 THEN 0
-- ELSIF finseq_appl[T](h)(0) = x
--   THEN 1 + occurrences(h ^ (1, length(h) - 1))(x)
-- ELSE occurrences(h ^ (1, length(h) - 1))(x)
-- ENDIF
axiom occurrences {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41

-- pvs2lean: sorting_seq.finseq2list_rec_equivalence
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, k: below[length(h)]):
--   i <= length(h) - k IMPLIES
--    finseq2list_rec(h ^ (k, h`length - 1), i) = finseq2list_rec(h, i)
theorem finseq2list_rec_equivalence : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length h}) (k : {s : {i : ℝ // i ≥ 0} // s < List.length h}), i ≤ List.length h - k → finseq2list_rec (h ^ (k, h.List.length - 1)) i = finseq2list_rec h i := by
  -- === TCCs for finseq2list_rec_equivalence ===
  -- TCC: finseq2list_rec_equivalence_TCC1
  -- Statement: FORALL (h: finite_sequence[T], i, k: below[length(h)]):
  --   i <= length(h) - k IMPLIES h`length - 1 >= 0
  have finseq2list_rec_equivalence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: finseq2list_rec_equivalence_TCC2
  -- Statement: FORALL (h: finite_sequence[T], i, k: below[length(h)]):
  --   i <= length(h) - k IMPLIES i <= length(^[T](h, (k, h`length - 1)))
  have finseq2list_rec_equivalence_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: finseq2list_rec_equivalence_TCC3
  -- Statement: FORALL (h: finite_sequence[T], i, k: below[length(h)]):
  --   i <= length(h) - k IMPLIES i <= length(h)
  have finseq2list_rec_equivalence_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.finseq2lists_for_suffixes
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((h: finite_sequence[T] | length(h) > 0), i: below[length(h)]):
--   finseq2list_rec(h ^ (i, length(h) - 1), length(h) - i) =
--    finseq2list_rec(h, length(h) - i)
theorem finseq2lists_for_suffixes : ∀ (h : {h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length h > 0}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length h}), finseq2list_rec (h ^ (i, List.length h - 1)) (List.length h - i) = finseq2list_rec h (List.length h - i) := by
  -- === TCCs for finseq2lists_for_suffixes ===
  -- TCC: finseq2lists_for_suffixes_TCC1
  -- Statement: FORALL ((h: finite_sequence[T] | length(h) > 0), i: below[length(h)]):
  --   length(h) - 1 >= 0
  have finseq2lists_for_suffixes_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: finseq2lists_for_suffixes_TCC2
  -- Statement: FORALL ((h: finite_sequence[T] | length(h) > 0), i: below[length(h)]):
  --   length(h) - i >= 0 AND
  --    length(h) - i <= length(^[T](h, (i, length(h) - 1)))
  have finseq2lists_for_suffixes_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: finseq2lists_for_suffixes_TCC3
  -- Statement: FORALL ((h: finite_sequence[T] | length(h) > 0), i: below[length(h)]):
  --   length(h) - i >= 0 AND length(h) - i <= length(h)
  have finseq2lists_for_suffixes_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.subseq_is_cdr
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h: finite_sequence[T] | length(h) > 0):
--   finseq2list(h ^ (1, length(h) - 1)) = cdr(finseq2list(h))
theorem subseq_is_cdr : ∀ (h : {h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length h > 0}), finseq2list (h ^ (1, List.length h - 1)) = cdr (finseq2list h) := by
  -- === TCCs for subseq_is_cdr ===
  -- TCC: subseq_is_cdr_TCC1
  -- Statement: FORALL (h: finite_sequence[T] | length(h) > 0): length(h) - 1 >= 0
  have subseq_is_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subseq_is_cdr_TCC2
  -- Statement: FORALL (h: finite_sequence[T] | length(h) > 0): cons?[T](finseq2list[T](h))
  have subseq_is_cdr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.same_occ_fseq_list
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (x: T)): occurrences(h)(x) = occurrences(finseq2list(h))(x)
theorem same_occ_fseq_list : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (x : T), occurrences h(x) = occurrences (finseq2list h) (x) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sorting_seq.permutations
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: T): occurrences(h1)(x) = occurrences(h2)(x)
axiom permutations {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} : pvs_τ45 → pvs_τ46 → pvs_τ47

-- pvs2lean: sorting_seq.perm_fsq_iff_perm_list
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h1, h2: finite_sequence[T]):
--   permutations(h1, h2) <=> permutations(finseq2list(h1), finseq2list(h2))
theorem perm_fsq_iff_perm_list : ∀ (h1 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (h2 : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), permutations h1 h2 <=> permutations (finseq2list h1) (finseq2list h2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.permutations_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: reflexive?(permutations) AND
--  symmetric?(permutations) AND transitive?(permutations)
theorem permutations_equiv : reflexive_p permutations ∧ symmetric_p permutations ∧ transitive_p permutations := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.psorted
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(h) = 0 OR
--  (FORALL (k: below[length(h)]):
--     (i <= k AND k <= j - 1) =>
--      (k < i OR leq(finseq_appl[T](h)(k), finseq_appl[T](h)(k + 1))))
noncomputable def psorted : sorry := by
  sorry  -- pvs2lean: definition stub for length(h) = 0 OR  (FORALL (k: below[length(h)]):     (i <= k AND k <= j - 1) => 

-- pvs2lean: sorting_seq.is_sorted?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: below[length(h)]):
--   0 <= k AND k <= length(h) - 2 =>
--    leq(finseq_appl[T](h)(k), finseq_appl[T](h)(k + 1))
axiom is_sorted_p {pvs_τ32 : Type*} : pvs_τ32 → Prop

-- pvs2lean: sorting_seq.is_sorted_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_sorted?(h) IFF
--  FORALL (i: below[length(h)], j: below[length(h)] | i <= j):
--    leq(finseq_appl[T](h)(i), finseq_appl[T](h)(j))
theorem is_sorted_equiv : is_sorted_p h ↔ ∀ (i : Fin List.length h) (j : below[List.length h] | i ≤ j), leq (finseq_appl h(i)) (finseq_appl h(j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.ind_lc
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 * i + 1
axiom ind_lc {pvs_τ20 : Type*} : pvs_τ20 → Prop

-- pvs2lean: sorting_seq.ind_rc
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 2 * i + 2
axiom ind_rc {pvs_τ21 : Type*} : pvs_τ21 → Prop

-- pvs2lean: sorting_seq.ind_parent
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: floor((k - 1) / 2)
noncomputable def ind_parent : sorry := by
  sorry  -- pvs2lean: definition stub for floor((k - 1) / 2)

-- pvs2lean: sorting_seq.both_childs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ind_rc(i) < n + 1
axiom both_childs {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop

-- pvs2lean: sorting_seq.one_child
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ind_rc(i) = n + 1
axiom one_child {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} : pvs_τ42 → pvs_τ43 → pvs_τ44 → Prop

-- pvs2lean: sorting_seq.is_parent
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: both_childs(h)(n, i) OR one_child(h)(n, i)
axiom is_parent {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: sorting_seq.left_child
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: finseq_appl[T](h)(ind_lc(i))
axiom left_child {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ33 → pvs_τ34 → pvs_τ35

-- pvs2lean: sorting_seq.right_child
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: finseq_appl[T](h)(ind_rc(i))
axiom right_child {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} : pvs_τ49 → pvs_τ50 → pvs_τ51

-- pvs2lean: sorting_seq.is_p_heap
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: below[length(h) - 1]):
--   k < i OR
--    (k <= j =>
--      (is_parent(h)(j, k) =>
--        ((both_childs(h)(j, k) AND
--           geq(finseq_appl[T](h)(k), left_child(h)(k)) AND
--            geq(finseq
axiom is_p_heap {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ26 → pvs_τ27 → pvs_τ28 → Prop

-- pvs2lean: sorting_seq.is_heap
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(h) = 0 OR is_p_heap(h)(0, length(h) - 1)
axiom is_heap {pvs_τ25 : Type*} : pvs_τ25 → Prop

-- pvs2lean: sorting_seq.root_is_greatest_in_p_heap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (n: below[length(h)]), (i: below[n + 1])):
--   is_p_heap(h)(0, n) => geq(finseq_appl[T](h)(0), finseq_appl[T](h)(i))
theorem root_is_greatest_in_p_heap : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (n : {s : {i : ℝ // i ≥ 0} // s < List.length h}) (i : {s : {i : ℝ // i ≥ 0} // s < 1 + n}), is_p_heap h(0, n) → geq (finseq_appl h(0)) (finseq_appl h(i)) := by
  -- === TCCs for root_is_greatest_in_p_heap ===
  -- TCC: root_is_greatest_in_p_heap_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (n: below[length(h)]), (i: below[1 + n])):
  --   0 < length(h)
  have root_is_greatest_in_p_heap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: root_is_greatest_in_p_heap_TCC2
  -- Statement: FORALL (h: finite_sequence[T], (n: below[length(h)]), (i: below[1 + n])):
  --   0 <= n
  have root_is_greatest_in_p_heap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: root_is_greatest_in_p_heap_TCC3
  -- Statement: FORALL (h: finite_sequence[T], (n: below[length(h)]), (i: below[1 + n])):
  --   is_p_heap(h)(0, n) IMPLIES i < length(h)
  have root_is_greatest_in_p_heap_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.root_is_greatest_in_heap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i: below[length(h)])):
--   is_heap(h) => geq(finseq_appl[T](h)(0), finseq_appl[T](h)(i))
theorem root_is_greatest_in_heap : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length h}), is_heap h → geq (finseq_appl h(0)) (finseq_appl h(i)) := by
  -- === TCCs for root_is_greatest_in_heap ===
  -- TCC: root_is_greatest_in_heap_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (i: below[length(h)])):
  --   is_heap(h) IMPLIES 0 < length(h)
  have root_is_greatest_in_heap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.parent
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: finseq_appl[T](h)(floor((k - 1) / 2))
noncomputable def parent : sorry := by
  sorry  -- pvs2lean: definition stub for finseq_appl[T](h)(floor((k - 1) / 2))

-- pvs2lean: sorting_seq.ind_gc
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (both_childs(h)(n, i) AND NOT leq(right_child(h)(i), left_child(h)(i)))
--   THEN ind_rc(i)
-- ELSE ind_lc(i)
-- ENDIF
noncomputable def ind_gc : sorry := by
  sorry  -- pvs2lean: definition stub for IF (both_childs(h)(n, i) AND NOT leq(right_child(h)(i), left_child(h)(i)))   THE

-- pvs2lean: sorting_seq.swap
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := length(h),
--    seq
--      := (LAMBDA (k: below[length(h)]):
--            IF k = i THEN finseq_appl[T](h)(j)
--            ELSIF k = j THEN finseq_appl[T](h)(i)
--            ELSE finseq_appl[T](h)(k
axiom swap {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} : pvs_τ52 → pvs_τ53 → pvs_τ54 → pvs_τ55

-- pvs2lean: sorting_seq.swap_of_p_heap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((h: finite_sequence[T] | length(h) > 0), (n: below[length(h)]),
--         (i: below[n + 1])):
--   is_p_heap(h)(0, n) =>
--    geq(finseq_appl(swap(h)(n, 0))(n), finseq_appl[T](h)(i))
theorem swap_of_p_heap : ∀ (h : {h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length h > 0}) (n : {s : {i : ℝ // i ≥ 0} // s < List.length h}) (i : {s : {i : ℝ // i ≥ 0} // s < 1 + n}), is_p_heap h(0, n) → geq (finseq_appl (swap h(n, 0)) (n)) (finseq_appl h(i)) := by
  -- === TCCs for swap_of_p_heap ===
  -- TCC: swap_of_p_heap_TCC1
  -- Statement: FORALL ((h: finite_sequence[T] | length(h) > 0), (n: below[length(h)]),
  --         (i: below[n + 1])):
  --   is_p_heap(h)(0, n) IMPLIES n < length(swap(h)(n, 0))
  have swap_of_p_heap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.swap_commutes
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i, j: below[length(h)])): swap(h)(i, j) = swap(h)(j, i)
theorem swap_commutes : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length h}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length h}), swap h(i, j) = swap h(j, i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.occ_replaces_at_j
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem occ_replaces_at_j : True := by
  -- === TCCs for occ_replaces_at_j ===
  -- TCC: occ_replaces_at_j_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (j: below[length(h)]), (x, y: T),
  --         hp: [# length: nat, seq: [below[length(h)] -> T] #]):
  --   hp =
  --    (# length := length(h),
  --       seq
  --         := LAMBDA (k: belo...
  have occ_replaces_at_j_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sorting_seq.swap_occ_at_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (j: below[length(h)] | j > 0), (x: T)):
--   occurrences(swap(h)(0, j) ^ (1, length(h) - 1))(x) =
--    IF finseq_appl[T](h)(j) = x THEN occurrences(h)(x) - 1
--    ELSE occurrences(h)(x)
--    ENDIF
theorem swap_occ_at_0 : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length h} // j > 0}) (x : T), occurrences (swap h(0, j) ^ (1, List.length h - 1)) (x) = if finseq_appl h(j) = x then occurrences h(x) - 1
else occurrences h(x) := by
  -- === TCCs for swap_occ_at_0 ===
  -- TCC: swap_occ_at_0_TCC1
  -- Statement: FORALL (h: finite_sequence[T], (j: below[length(h)] | j > 0), (x: T)):
  --   0 < length(h)
  have swap_occ_at_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: swap_occ_at_0_TCC2
  -- Statement: FORALL (h: finite_sequence[T], (j: below[length(h)] | j > 0), (x: T)):
  --   length(h) - 1 >= 0
  have swap_occ_at_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.swap_permutes
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (h, (i, j: below[length(h)])): permutations(h, swap(h)(i, j))
theorem swap_permutes : ∀ (h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length h}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length h}), permutations h (swap h(i, j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.swap_of_heap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((h: finite_sequence[T] | length(h) > 0), (i: below[length(h)])):
--   is_heap(h) =>
--    geq(finseq_appl(swap(h)(length(h) - 1, 0))(length(h) - 1),
--        finseq_appl[T](h)(i))
theorem swap_of_heap : ∀ (h : {h : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length h > 0}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length h}), is_heap h → geq (finseq_appl (swap h(List.length h - 1, 0)) (List.length h - 1)) (finseq_appl h(i)) := by
  -- === TCCs for swap_of_heap ===
  -- TCC: swap_of_heap_TCC1
  -- Statement: FORALL ((h: finite_sequence[T] | length(h) > 0), (i: below[length(h)])):
  --   is_heap(h) IMPLIES
  --    length(h) - 1 >= 0 AND length(h) - 1 < length(swap(h)(length(h) - 1, 0))
  have swap_of_heap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: swap_of_heap_TCC2
  -- Statement: FORALL ((h: finite_sequence[T] | length(h) > 0), (i: below[length(h)])):
  --   is_heap(h) IMPLIES length(h) - 1 >= 0 AND length(h) - 1 < length(h)
  have swap_of_heap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sorting_seq.occ_in_insert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem occ_in_insert : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sorting_seq.occ_in_delete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem occ_in_delete : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sorting_seq.delete_insert_same_permutes
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: finite_sequence[T] | length(v) > 0) (i, j: below[length(v)]):
--   permutations(v, insert?(finseq_appl[T](v)(i), delete(v, i), j))
theorem delete_insert_same_permutes : ∀ (v : {v : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length v > 0}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length v}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length v}), permutations v (insert_p (finseq_appl v(i)) (delete v i) j) := by
  -- === TCCs for delete_insert_same_permutes ===
  -- TCC: delete_insert_same_permutes_TCC1
  -- Statement: FORALL (v: finite_sequence[T] | length(v) > 0, i, j: below[length(v)]):
  --   j <= length(delete[T](v, i))
  have delete_insert_same_permutes_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.sorting_seq