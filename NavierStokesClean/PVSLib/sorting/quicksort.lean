-- Auto-generated by pvs2lean/translator.py
-- PVS theory: quicksort
-- PVSLib library: sorting
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: quicksort

Auto-generated stubs from PVSLib theory `quicksort`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.quicksort

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (append : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (car : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (cdr : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (cons : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ13 : Type*} (is_sorted_p : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (length : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (leq : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (member : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} (null_p : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (occurrences : pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (permutations : pvs_τ27 → pvs_τ28 → Prop)

-- pvs2lean: quicksort.leq_elements
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES l
--   OF null: null,
--      cons(x, r):
--        IF leq(x, p) THEN cons(x, leq_elements(r, p))
--        ELSE leq_elements(r, p)
--        ENDIF
--   ENDCASES
axiom leq_elements {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ18 → pvs_τ19 → pvs_τ20

-- pvs2lean: quicksort.g_elements
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES l
--   OF null: null,
--      cons(x, r):
--        IF gt(x, p) THEN cons(x, g_elements(r, p))
--        ELSE g_elements(r, p)
--        ENDIF
--   ENDCASES
axiom g_elements {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ10 → pvs_τ11 → pvs_τ12

-- pvs2lean: quicksort.leq_elements_size
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1: list[T], x: T): length(leq_elements(l1, x)) <= length(l1)
theorem leq_elements_size : ∀ (l1 : List T) (x : T), List.length (leq_elements l1 x) ≤ List.length l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.g_elements_size
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1: list[T], x: T): length(g_elements(l1, x)) <= length(l1)
theorem g_elements_size : ∀ (l1 : List T) (x : T), List.length (g_elements l1 x) ≤ List.length l1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.same_occurrence_leq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1: list[T], x, p: T):
--   leq(x, p) IMPLIES
--    ((occurrences(l1)(x) = occurrences(leq_elements(l1, p))(x)) AND
--      (occurrences(g_elements(l1, p))(x) = 0))
theorem same_occurrence_leq : ∀ (l1 : List T) (x : T) (p : T), leq x p → ((occurrences l1(x) = occurrences (leq_elements l1 p) (x)) ∧
  (occurrences (g_elements l1 p) (x) = 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.same_occurrence_g
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1: list[T], x, p: T):
--   gt(x, p) IMPLIES
--    ((occurrences(l1)(x) = occurrences(g_elements(l1, p))(x)) AND
--      (occurrences(leq_elements(l1, p))(x) = 0))
theorem same_occurrence_g : ∀ (l1 : List T) (x : T) (p : T), gt x p → ((occurrences l1(x) = occurrences (g_elements l1 p) (x)) ∧
  (occurrences (leq_elements l1 p) (x) = 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.quick_sort
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES l
--   OF null: null,
--      cons(x, r):
--        append(quick_sort(leq_elements(r, x)),
--               cons(x, quick_sort(g_elements(r, x))))
--   ENDCASES
axiom quick_sort {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ29 → pvs_τ30

-- pvs2lean: quicksort.g_leq_car_size
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1: list[T]):
--   NOT (l1 = null) IMPLIES
--    (length(g_elements(cdr(l1), car(l1))) +
--      length(leq_elements(cdr(l1), car(l1)))
--      + 1
--      = length(l1))
theorem g_leq_car_size : ∀ (l1 : List T), ¬ (l1 = null) → (List.length (g_elements (cdr l1) (car l1)) +
  List.length (leq_elements (cdr l1) (car l1))
  + 1
  = List.length l1) := by
  -- === TCCs for g_leq_car_size ===
  -- TCC: g_leq_car_size_TCC1
  -- Statement: FORALL (l1: list[T]): NOT (l1 = null) IMPLIES cons?[T](l1)
  have g_leq_car_size_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.quick_sort_size
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): length(quick_sort(l)) = length(l)
theorem quick_sort_size : ∀ (l : List T), List.length (quick_sort l) = List.length l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.leq_elements_works
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], x, i: T):
--   member(i, leq_elements(l, x)) IMPLIES leq(i, x)
theorem leq_elements_works : ∀ (l : List T) (x : T) (i : T), member i (leq_elements l x) → leq i x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.g_elements_works
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T], x, i: T): member(i, g_elements(l, x)) IMPLIES gt(i, x)
theorem g_elements_works : ∀ (l : List T) (x : T) (i : T), member i (g_elements l x) → gt i x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.quick_sort_is_permutation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): permutations(quick_sort(l), l)
theorem quick_sort_is_permutation : ∀ (l : List T), permutations (quick_sort l) l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.append_null_in_sorted_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[T]):
--   ((is_sorted?(l1) AND is_sorted?(l2)) AND
--     (null?(l1) OR list2set[list[T]](null)(l2)))
--    IMPLIES is_sorted?(append(l1, l2))
theorem append_null_in_sorted_is_sorted : ∀ (l1 : List T) (l2 : List T), ((is_sorted_p l1 ∧ is_sorted_p l2) ∧
  (null_p l1 ∨ list2set null(l2))) → is_sorted_p (append l1 l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.cons_in_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((l1: list[T] | NOT null?(l1)), x: T):
--   is_sorted?(l1) AND leq(x, car(l1)) IMPLIES is_sorted?(cons(x, l1))
theorem cons_in_sorted : ∀ (l1 : {l1 : List T // ¬ null_p l1}) (x : T), is_sorted_p l1 ∧ leq x (car l1) → is_sorted_p (cons x l1) := by
  -- === TCCs for cons_in_sorted ===
  -- TCC: cons_in_sorted_TCC1
  -- Statement: FORALL ((l1: list[T] | NOT null?[T](l1)), x: T):
  --   is_sorted?(l1) IMPLIES cons?[T](l1)
  have cons_in_sorted_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.quick_sort_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]): is_sorted?(quick_sort(l))
theorem quick_sort_is_sorted : ∀ (l : List T), is_sorted_p (quick_sort l) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: quicksort.quick_sort_works
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[T]):
--   is_sorted?(quick_sort(l)) AND permutations(quick_sort(l), l)
theorem quick_sort_works : ∀ (l : List T), is_sorted_p (quick_sort l) ∧ permutations (quick_sort l) l := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.quicksort