-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cauchy
-- PVSLib library: algebra
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cauchy

Auto-generated stubs from PVSLib theory `cauchy`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cauchy

noncomputable section

variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (below : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (bijective_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (card : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (divides : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (expt : pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (group_action_p : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} (group_p : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} (inv : pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (is_finite : pvs_τ32 → pvs_τ33)
variable {pvs_τ34 : Type*} (length : pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (member : pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (order : pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (period : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} (prime_p : pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} (rem : pvs_τ42 → pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (rest : pvs_τ45 → pvs_τ46)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} (seq : pvs_τ49 → pvs_τ50)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} (set_seq : pvs_τ51 → pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (subgroup : pvs_τ54 → pvs_τ55)

-- pvs2lean: cauchy.fullset_is_group
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: group?(fullset[T])
theorem fullset_is_group : group_p fullset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.fseq_product
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(fs) = 0 THEN one ELSE fs`seq(0) * fseq_product(rest(fs)) ENDIF
axiom fseq_product {pvs_τ27 : Type*} : pvs_τ27 → Prop

-- pvs2lean: cauchy.S
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {fs: finseq |
--    length(fs) = n AND
--     (FORALL (i: below[n]): member(fs`seq(i), G)) AND fseq_product(fs) = one}
axiom S {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: cauchy.same_element
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below[length(fs)]): fs`seq(i) = a
axiom same_element {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ47 → pvs_τ48 → Prop

-- pvs2lean: cauchy.SE
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: extend[finseq[T], (S(G)(n)), bool, FALSE]
--     ({fs: (S(G)(n)) | EXISTS (a: (G)): same_element(a, fs)})
axiom SE {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16

-- pvs2lean: cauchy.fseq_product_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: below[length(fs)]): member(fs`seq(i), G)) IMPLIES
--  member(fseq_product(fs), G)
theorem fseq_product_in : (∀ (i : Fin List.length fs), member (fs.seq i) G) → member (fseq_product fs) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.fseq_product_o
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: below[length(fs1)]): member(fs1`seq(i), G)) AND
--  (FORALL (j: below[length(fs2)]): member(fs2`seq(j), G))
--  IMPLIES fseq_product(fs1 o fs2) = fseq_product(fs1) * fseq_product(fs2)
theorem fseq_product_o : (∀ (i : Fin List.length fs1), member (fs1.seq i) G) ∧
 (∀ (j : Fin List.length fs2), member (fs2.seq j) G) → fseq_product (fs1 ∘ fs2) = fseq_product fs1 * fseq_product fs2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.fseq_product_one
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: below[length(fs)]): member(fs`seq(i), G)) IMPLIES
--  inv(fseq_product(fs)) * fseq_product(fs) = one
theorem fseq_product_one : (∀ (i : Fin List.length fs), member (fs.seq i) G) → inv (fseq_product fs) * fseq_product fs = one := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.fseq_product_power
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (g: (G)):
--   same_element(g, fs) IMPLIES fseq_product(fs) = g ^ length(fs)
theorem fseq_product_power : ∀ (g : T), same_element g fs → fseq_product fs = g ^ List.length fs := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.one_in_SE
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(fs) = n AND same_element(one, fs) IMPLIES member(fs, SE(G)(n))
theorem one_in_SE : List.length fs = n ∧ same_element one fs → member fs (SE G(n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.order_SE
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (g: (G)):
--   prime?(p) AND g /= one AND same_element(g, fs) AND member(fs, SE(G)(p))
--    IMPLIES period(G, g) = p
theorem order_SE : ∀ (g : T), prime_p p ∧ g ≠ one ∧ same_element g fs ∧ member fs (SE G(p)) → period G g = p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.S_bij_set_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET A = set_seq(G)(p - 1), B = S(G)(p) IN
--   EXISTS (g: [(A) -> (B)]): bijective?(g)
theorem S_bij_set_seq : fun (A : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => fun (B : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => ∃ (g : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), bijective_p g (S G p) (set_seq G (p - 1)) := by
  -- === TCCs for S_bij_set_seq ===
  -- TCC: S_bij_set_seq_TCC1
  -- Statement: FORALL (p: posnat): p - 1 >= 0
  have s_bij_set_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.S_is_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(S(G)(n))
theorem S_is_finite : is_finite (S G n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.S_card
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: card(S(G)(p)) = expt(card(G), p - 1)
theorem S_card : card (S G(p)) = expt (card G) (p - 1) := by
  -- === TCCs for S_card ===
  -- TCC: S_card_TCC1
  -- Statement: FORALL (G: finite_group[T, *, one], p: posnat):
  --   is_finite[finseq[T]](S(G)(p))
  have s_card_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cauchy.F
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := p, seq := (LAMBDA (i: below[p]): fs`seq(rem(p)(i + k))) #)
axiom F {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: cauchy.F_1
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := length(fs) - k,
--    seq
--      := (LAMBDA (i: below[length(fs) - k]):
--            fs`seq(rem(length(fs))(i + k))) #)
axiom F_1 {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7

-- pvs2lean: cauchy.F_2
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := k, seq := (LAMBDA (i: below[k]): fs`seq(i)) #)
axiom F_2 {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10

-- pvs2lean: cauchy.F_o_F12
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: (Zn[p]), fs: (S(G)(p))): F(p, G)(k, fs) = F_1(fs)(k) o F_2(fs)(k)
theorem F_o_F12 : ∀ (k : {s : {i : ℝ // i ≥ 0} // s < p}) (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), F p G(k, fs) = F_1 fs(k) ∘ F_2 fs(k) := by
  -- === TCCs for F_o_F12 ===
  -- TCC: F_o_F12_TCC1
  -- Statement: FORALL (G: finite_group[T, *, one], p: posnat, k: (Zn[p]), fs: (S(G)(p))):
  --   k < length(fs)
  have f_o_f12_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.fs_o_F21
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: (Zn[p]), fs: (S(G)(p))): fs = F_2(fs)(k) o F_1(fs)(k)
theorem fs_o_F21 : ∀ (k : {s : {i : ℝ // i ≥ 0} // s < p}) (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), fs = F_2 fs(k) ∘ F_1 fs(k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.F_in_S
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: (Zn[p]), fs: (S(G)(p))): member(F(p, G)(k, fs), S(G)(p))
theorem F_in_S : ∀ (k : {s : {i : ℝ // i ≥ 0} // s < p}) (fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), member (F p G(k, fs)) (S G(p)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cauchy.F_is_action
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: group_action?[below(p), ++, 0, finseq[T]](Zn[p], S(G)(p))(F(p, G))
theorem F_is_action : group_action_p Zn[p] (S G(p)) (F p G) := by
  -- === TCCs for F_is_action ===
  -- TCC: F_is_action_TCC1
  -- Statement: FORALL (G: finite_group[T, *, one], p: posnat, x1: [(Zn[p]), (S(G)(p))]):
  --   S(G)(p)(F(p, G)(x1))
  have f_is_action_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: F_is_action_TCC2
  -- Statement: FORALL (p: posnat): group?[below(p), ++, 0](Zn[p])
  have f_is_action_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: F_is_action_TCC3
  -- Statement: FORALL (p: posnat): 0 < p
  have f_is_action_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: F_is_action_TCC4
  -- Statement: FORALL (p: posnat): group?[below(p), ++, 0](fullset[below(p)])
  have f_is_action_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.Fixed_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET H = Zn[p], X = S(G)(p), f = F(p, G) IN
--   Fix[below(p), ++, 0, finseq[T]](H, X)(f) = SE(G)(p)
theorem Fixed_subset : fun (H : {s : {i : ℝ // i ≥ 0} // s < p} → Bool) => fun (X : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => fun (f : {s : {i : ℝ // i ≥ 0} // s < p} × ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)) => Fix H X(f) = SE G(p) (F p G) (S G p) Zn := by
  -- === TCCs for Fixed_subset ===
  -- TCC: Fixed_subset_TCC1
  -- Statement: FORALL (G: finite_group[T, *, one], p: posnat, H: set[below(p)],
  --         X: set[finseq[T]], f: [[(Zn[p]), (S(G)(p))] -> finseq[T]]):
  --   H = Zn[p] AND X = S(G)(p) AND f = F(p, G) IMPLIES
  --    ((FORALL ...
  have fixed_subset_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Fixed_subset_TCC2
  -- Statement: FORALL (G: finite_group[T, *, one], p: posnat, H: set[below(p)],
  --         X: set[finseq[T]], f: [[(Zn[p]), (S(G)(p))] -> finseq[T]]):
  --   H = Zn[p] AND X = S(G)(p) AND f = F(p, G) IMPLIES
  --    group?[be...
  have fixed_subset_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cauchy.cauchy
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: prime?(p) AND divides(p, order(G)) IMPLIES EXISTS (x: (G)): period(G, x) = p
theorem cauchy : prime_p p ∧ divides p (order G) → ∃ (x : (G)), period G x = p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy.cauchy_cor
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: prime?(p) AND divides(p, order(G)) IMPLIES
--  EXISTS (H: subgroup(G)): order(H) = p
theorem cauchy_cor : prime_p p ∧ divides p (order G) → ∃ (H : subgroup G), order H = p := by
  -- === TCCs for cauchy_cor ===
  -- TCC: cauchy_cor_TCC1
  -- Statement: FORALL (G: finite_group[T, *, one], p: posnat):
  --   divides(p, order(G)) AND prime?(p) IMPLIES
  --    FORALL (H: subgroup[T, *, one](G)): finite_monad?[T, *, one](H)
  have cauchy_cor_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cauchy