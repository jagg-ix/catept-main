-- Auto-generated by pvs2lean/translator.py
-- PVS theory: class_equation_scaf
-- PVSLib library: algebra
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: class_equation_scaf

Auto-generated stubs from PVSLib theory `class_equation_scaf`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.class_equation_scaf

noncomputable section

variable {pvs_τ1 : Type*} (card : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (choose : pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (divides : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} (integer_pred : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} (nonempty_p : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (rational_pred : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (rest : pvs_τ9 → pvs_τ10)

-- pvs2lean: class_equation_scaf.card_rest_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(FP) IMPLIES
--  card(Union(FP)) = card(choose(FP)) + card(Union(rest(FP)))
theorem card_rest_aux : nonempty_p FP → card (Union FP) = card (choose FP) + card (Union (rest FP)) := by
  -- === TCCs for card_rest_aux ===
  -- TCC: card_rest_aux_TCC1
  -- Statement: FORALL (FP: finite_partition[T]):
  --   nonempty?(FP) IMPLIES is_finite[T](Union[T](FP))
  have card_rest_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: card_rest_aux_TCC2
  -- Statement: FORALL (FP: finite_partition[T]):
  --   nonempty?(FP) IMPLIES is_finite[T](choose[setof[T]](FP))
  have card_rest_aux_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: card_rest_aux_TCC3
  -- Statement: FORALL (FP: finite_partition[T]):
  --   nonempty?(FP) IMPLIES is_finite[T](Union[T](rest[setof[T]](FP)))
  have card_rest_aux_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: class_equation_scaf.card_partition
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (nonempty?(FP) AND FORALL (A: (FP)): card(A) /= 0) IMPLIES
--  card(Union(FP)) =
--   sigma_set.sigma(restrict[setof[T], finite_set[T], bool](FP), card)
theorem card_partition : (nonempty_p FP ∧ ∀ (A : (FP)), card A ≠ 0) → card (Union FP) =
 sigma (restrict FP) card := by
  -- === TCCs for card_partition ===
  -- TCC: card_partition_TCC1
  -- Statement: FORALL (FP: finite_partition[T]):
  --   nonempty?(FP) IMPLIES FORALL (A: (FP)): is_finite[T](A)
  have card_partition_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: card_partition_TCC2
  -- Statement: FORALL (FP: finite_partition[T]):
  --   (FORALL (A: (FP)): card(A) /= 0) AND nonempty?(FP) IMPLIES
  --    convergent?[finite_set[T]]
  --        (restrict[setof[T], finite_set[T], bool](FP))(card[T])
  have card_partition_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: class_equation_scaf.divide_sigma
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (nonempty?(FP) AND FORALL (A: (FP)): card(A) /= 0 AND divides(n, card(A)))
--  IMPLIES
--  divides(n,
--          sigma_set.sigma(restrict[setof[T], finite_set[T], bool](FP), card))
theorem divide_sigma : (nonempty_p FP ∧ ∀ (A : (FP)), card A ≠ 0 ∧ divides n (card A)) → divides n (sigma (restrict FP) card) := by
  -- === TCCs for divide_sigma ===
  -- TCC: divide_sigma_TCC1
  -- Statement: FORALL (FP: finite_partition[T], n: int):
  --   (FORALL (A: (FP)): card(A) /= 0 AND divides(n, card(A))) AND
  --    nonempty?(FP)
  --    IMPLIES
  --    convergent?[finite_set[T]]
  --        (restrict[setof[T], finite_...
  have divide_sigma_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: divide_sigma_TCC2
  -- Statement: FORALL (FP: finite_partition[T], n: int):
  --   (FORALL (A: (FP)): card(A) /= 0 AND divides(n, card(A))) AND
  --    nonempty?(FP)
  --    IMPLIES
  --    rational_pred(sigma_set[finite_set[T]].sigma
  --                 ...
  have divide_sigma_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.class_equation_scaf