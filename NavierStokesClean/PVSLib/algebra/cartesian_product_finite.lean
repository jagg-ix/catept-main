-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cartesian_product_finite
-- PVSLib library: algebra
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cartesian_product_finite

Auto-generated stubs from PVSLib theory `cartesian_product_finite`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cartesian_product_finite

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (card : pvs_τ3 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (disjoint_p : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (empty_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (finite_partition_p : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} (is_finite : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} (length : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (member : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (rest : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (seq : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (seq1 : pvs_τ21 → pvs_τ22)

-- pvs2lean: cartesian_product_finite.cartesian_product_one
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {fsx | EXISTS fsy: member(fsy, B) AND fsx = add_first[T](x, fsy)}
axiom cartesian_product_one {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ6 → pvs_τ7 → Prop

-- pvs2lean: cartesian_product_finite.cartesian_product_set
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Union({C | EXISTS x: member(x, A) AND C = cartesian_product_one(x, B)})
axiom cartesian_product_set {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: cartesian_product_finite.cartesian_product_n
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(fsA) = 0 THEN emptyset
-- ELSE {fsz |
--         length(fsz) = length(fsA) AND
--          FORALL (i: below(length(fsz))):
--            member(finseq_appl[T](fsz)(i), finseq_appl[set[T]](fsA)(i))}
-- ENDI
axiom cartesian_product_n {pvs_τ5 : Type*} : pvs_τ5 → Prop

-- pvs2lean: cartesian_product_finite.card_fs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := length(fsNS),
--    seq
--      := (LAMBDA (i: below[length(fsNS)]):
--            card(finseq_appl[non_empty_finite_set[T]](fsNS)(i))) #)
axiom card_fs {pvs_τ4 : Type*} : pvs_τ4 → Prop

-- pvs2lean: cartesian_product_finite.cartesian_product_one_disjoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cartesian_product_one(a, B) = cartesian_product_one(x, B) OR
--  disjoint?(cartesian_product_one(a, B), cartesian_product_one(x, B))
theorem cartesian_product_one_disjoint : cartesian_product_one a B = cartesian_product_one x B ∨ disjoint_p (cartesian_product_one a B) (cartesian_product_one x B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_one_emptyset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty?(B) IMPLIES cartesian_product_one(x, B) = emptyset
theorem cartesian_product_one_emptyset : empty_p B → cartesian_product_one x B = emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_set_emptyset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty?(A) OR empty?(B) IMPLIES cartesian_product_set(A, B) = emptyset
theorem cartesian_product_set_emptyset : empty_p A ∨ empty_p B → cartesian_product_set A B = emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_n_emptyset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (i: below(length(fsA))): empty?(finseq_appl[set[T]](fsA)(i)))
--  IMPLIES cartesian_product_n(fsA) = emptyset
theorem cartesian_product_n_emptyset : (∃ (i : Finset.range List.length fsA), empty_p (finseq_appl fsA(i))) → cartesian_product_n fsA = emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_n_add_is_union
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(fsA) /= 0 IMPLIES
--  cartesian_product_n(add_first[set[T]](A, fsA)) =
--   cartesian_product_set(A, cartesian_product_n(fsA))
theorem cartesian_product_n_add_is_union : List.length fsA ≠ 0 → cartesian_product_n (add_first A fsA) =
 cartesian_product_set A (cartesian_product_n fsA) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.rest_card_fs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(fsNS) /= 0 IMPLIES rest(card_fs(fsNS)) = card_fs(rest(fsNS))
theorem rest_card_fs : List.length fsNS ≠ 0 → rest (card_fs fsNS) = card_fs (rest fsNS) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.add_card_fs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(fsNS) /= 0 AND is_finite(A) AND A /= emptyset IMPLIES
--  card_fs(add_first[non_empty_finite_set[T]](A, fsNS)) =
--   add_first[nat](card(A), card_fs(fsNS))
theorem add_card_fs : List.length fsNS ≠ 0 ∧ is_finite A ∧ A ≠ emptyset → card_fs (add_first A fsNS) =
 add_first (card A) (card_fs fsNS) := by
  -- === TCCs for add_card_fs ===
  -- TCC: add_card_fs_TCC1
  -- Statement: FORALL (A: set[T], fsNS: finseq[non_empty_finite_set[T]]):
  --   A /= emptyset AND is_finite(A) AND length(fsNS) /= 0 IMPLIES
  --    NOT empty?[T](A)
  have add_card_fs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_one_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(B) IMPLIES is_finite(cartesian_product_one(x, B))
theorem cartesian_product_one_finite : is_finite B → is_finite (cartesian_product_one x B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_one_card
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(B) IMPLIES card(cartesian_product_one(x, B)) = card(B)
theorem cartesian_product_one_card : is_finite B → card (cartesian_product_one x B) = card B := by
  -- === TCCs for cartesian_product_one_card ===
  -- TCC: cartesian_product_one_card_TCC1
  -- Statement: FORALL (B: set[finseq[T]], x: T):
  --   is_finite(B) IMPLIES is_finite[finseq[T]](cartesian_product_one(x, B))
  have cartesian_product_one_card_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_set_finite_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (is_finite(A) AND is_finite(B)) IMPLIES
--  is_finite({C | EXISTS x: member(x, A) AND C = cartesian_product_one(x, B)})
theorem cartesian_product_set_finite_aux : (is_finite A ∧ is_finite B) → is_finite ({C | ∃ x,  member x A ∧ C = cartesian_product_one x B}) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_set_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (is_finite(A) AND is_finite(B)) IMPLIES
--  is_finite(cartesian_product_set(A, B))
theorem cartesian_product_set_finite : (is_finite A ∧ is_finite B) → is_finite (cartesian_product_set A B) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_set_partition
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (is_finite(A) AND is_finite(B)) IMPLIES
--  finite_partition?({C |
--                       EXISTS x:
--                         member(x, A) AND C = cartesian_product_one(x, B)})
theorem cartesian_product_set_partition : (is_finite A ∧ is_finite B) → finite_partition_p ({C |
                     ∃ x, 
                       member x A ∧ C = cartesian_product_one x B}) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_set_card_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (is_finite(A) AND is_finite(B) AND B /= emptyset) IMPLIES
--  card({C | EXISTS x: member(x, A) AND C = cartesian_product_one(x, B)}) =
--   card(A)
theorem cartesian_product_set_card_aux : (is_finite A ∧ is_finite B ∧ B ≠ emptyset) → card ({C | ∃ x,  member x A ∧ C = cartesian_product_one x B}) =
 card A := by
  -- === TCCs for cartesian_product_set_card_aux ===
  -- TCC: cartesian_product_set_card_aux_TCC1
  -- Statement: FORALL (A: set[T], B: set[finseq[T]]):
  --   B /= emptyset AND is_finite(B) AND is_finite(A) IMPLIES
  --    is_finite[set[finseq[T]]]
  --        ({C: set[finseq[T]] |
  --            EXISTS (x: T):
  --              mem...
  have cartesian_product_set_card_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_set_card
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (is_finite(A) AND is_finite(B) AND B /= emptyset) IMPLIES
--  card(cartesian_product_set(A, B)) = card(A) * card(B)
theorem cartesian_product_set_card : (is_finite A ∧ is_finite B ∧ B ≠ emptyset) → card (cartesian_product_set A B) = card A * card B := by
  -- === TCCs for cartesian_product_set_card ===
  -- TCC: cartesian_product_set_card_TCC1
  -- Statement: FORALL (A: set[T], B: set[finseq[T]]):
  --   B /= emptyset AND is_finite(B) AND is_finite(A) IMPLIES
  --    is_finite[finseq[T]](cartesian_product_set(A, B))
  have cartesian_product_set_card_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_n_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite(cartesian_product_n(fsNS))
theorem cartesian_product_n_finite : is_finite (cartesian_product_n fsNS) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_n_degenerated
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(fsNS) = 1 IMPLIES
--  cartesian_product_n(fsNS) =
--   {fsx |
--      EXISTS x:
--        member(x, finseq_appl[non_empty_finite_set[T]](fsNS)(0)) AND
--         fsx = seq1(x)}
theorem cartesian_product_n_degenerated : List.length fsNS = 1 → cartesian_product_n fsNS =
 {fsx |
    ∃ x, 
      member x (finseq_appl fsNS(0)) ∧
       fsx = seq1 x} := by
  -- === TCCs for cartesian_product_n_degenerated ===
  -- TCC: cartesian_product_n_degenerated_TCC1
  -- Statement: FORALL (fsNS: finseq[non_empty_finite_set[T]]):
  --   length(fsNS) = 1 IMPLIES 0 < length(fsNS)
  have cartesian_product_n_degenerated_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_n_card_degenerated
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(fsNS) = 1 IMPLIES
--  card(cartesian_product_n(fsNS)) =
--   card(finseq_appl[non_empty_finite_set[T]](fsNS)(0))
theorem cartesian_product_n_card_degenerated : List.length fsNS = 1 → card (cartesian_product_n fsNS) =
 card (finseq_appl fsNS(0)) := by
  -- === TCCs for cartesian_product_n_card_degenerated ===
  -- TCC: cartesian_product_n_card_degenerated_TCC1
  -- Statement: FORALL (fsNS: finseq[non_empty_finite_set[T]]):
  --   length(fsNS) = 1 IMPLIES is_finite[finseq[T]](cartesian_product_n(fsNS))
  have cartesian_product_n_card_degenerated_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cartesian_product_finite.cartesian_product_n_card
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(fsNS) /= 0 IMPLIES
--  card(cartesian_product_n(fsNS)) = product(card_fs(fsNS))
theorem cartesian_product_n_card : List.length fsNS ≠ 0 → card (cartesian_product_n fsNS) = product (card_fs fsNS) := by
  -- === TCCs for cartesian_product_n_card ===
  -- TCC: cartesian_product_n_card_TCC1
  -- Statement: FORALL (fsNS: finseq[non_empty_finite_set[T]]):
  --   length(fsNS) /= 0 IMPLIES is_finite[finseq[T]](cartesian_product_n(fsNS))
  have cartesian_product_n_card_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cartesian_product_n_card_TCC2
  -- Statement: FORALL (fsNS: finseq[non_empty_finite_set[T]]):
  --   length(fsNS) /= 0 IMPLIES
  --    FORALL (x1: below[card_fs(fsNS)`length]): card_fs(fsNS)`seq(x1) > 0
  have cartesian_product_n_card_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cartesian_product_finite