-- Auto-generated by pvs2lean/translator.py
-- PVS theory: chinese_remainder_theorem_rings
-- PVSLib library: algebra
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: chinese_remainder_theorem_rings

Auto-generated stubs from PVSLib theory `chinese_remainder_theorem_rings`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.chinese_remainder_theorem_rings

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Intersection : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Pfs : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (R_isomorphic_p : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (Sfs : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (add : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (cartesian_product_n : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (coset_p : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (empty_p : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (fsI : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (fsICM : pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (fsQ : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} (fsRI_p : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (inv : pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} (length : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (member : pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (phi : pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} (ring_with_one_p : pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (seq2set : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} (surjective_p : pvs_τ42 → Prop)

-- pvs2lean: chinese_remainder_theorem_rings.fullset_is_ring_with_one
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ring_with_one?(fullset[T])
theorem fullset_is_ring_with_one : ring_with_one_p fullset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: chinese_remainder_theorem_rings.oneSet
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: extend[T, (finseq_appl[set[T]](fsA)(i)), bool, FALSE]
--     ({x: (finseq_appl[set[T]](fsA)(i)) |
--         member(one + inv(x), finseq_appl[set[T]](fsA)(j))})
axiom oneSet {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36

-- pvs2lean: chinese_remainder_theorem_rings.oneSet_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (R: ring_with_one, fsA: fsICM(R) | length(fsA) > 1,
--         i: below[length(fsA)], (j: below[length(fsA)] | j /= i)):
--   NOT empty?(oneSet(R, fsA)(i, j))
theorem oneSet_nonempty : ∀ (R : T → Bool) (fsA : {fsA : {fsA : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool) // fsICM_p R(fsA)} // List.length fsA > 1}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length fsA}) (j : {j : {s : {i : ℝ // i ≥ 0} // s < List.length fsA} // j ≠ i}), ¬(empty_p (oneSet R fsA i j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: chinese_remainder_theorem_rings.surjective_aux_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (R: ring_with_one, fsA: fsICM(R)):
--   EXISTS (s: finseq[(R)] | length(s) = length(fsA)):
--     FORALL (i: below[length(fsA)]):
--       finseq_appl[(R)](s)(i) + finseq_appl[set[T]](fsA)(i) =
--        o
theorem surjective_aux_1 : ∀ (R : T → Bool) (fsA : {fsA : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool) // fsICM_p R(fsA)}), ∃ (s : {s : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length s = List.length fsA}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length fsA}), finseq_appl[(R)](s) (i) + finseq_appl fsA(i) =
 one + finseq_appl fsA(i) ∧ (∀ (j : below[List.length fsA] | j ≠ i), member finseq_appl[(R)](s) (i) (finseq_appl fsA(j))) := by
  -- === TCCs for surjective_aux_1 ===
  -- TCC: surjective_aux_1_TCC1
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R),
  --         s: finseq[(R)] | length(s) = length(fsA), i: below[length(fsA)]):
  --   i < length(s)
  have surjective_aux_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: chinese_remainder_theorem_rings.surjective_aux_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (R: ring_with_one, fsA: fsICM(R)):
--   member(y, cartesian_product_n[set[T]](fsQ(R)(fsA))) IMPLIES
--    EXISTS (s: finseq[(R)] | length(s) = length(fsA),
--            r: finseq[(R)] | length(r) = len
theorem surjective_aux_2 : ∀ (R : T → Bool) (fsA : {fsA : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool) // fsICM_p R(fsA)}), member y (cartesian_product_n (fsQ R(fsA))) → ∃ (s : finseq[(R)] | List.length s = List.length fsA) (r : finseq[(R)] | List.length r = List.length fsA), ∀ (i : Fin List.length fsA), finseq_appl y(i) =
     finseq_appl[(R)](r) (i) + finseq_appl fsA(i)
     ∧
     R_sigma_below
         (0, List.length fsA - 1,
          (fun (i : Fin List.length fsA)=> finseq_appl[(R)](s) (i) * finseq_appl[(R)](r) (i)))
      + finseq_appl fsA(i)
      = finseq_appl[(R)](r) (i) + finseq_appl fsA(i) := by
  -- === TCCs for surjective_aux_2 ===
  -- TCC: surjective_aux_2_TCC1
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R)):
  --   fsRI?[T, +, *, zero](R)(fsA)
  have surjective_aux_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: surjective_aux_2_TCC2
  -- Statement: FORALL (y: finseq[set[T]], R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R)):
  --   member(y, cartesian_product_n[set[T]](fsQ(R)(fsA))) IMPLIES
  --    FORALL (s: finseq[(R)] ...
  have surjective_aux_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: surjective_aux_2_TCC3
  -- Statement: FORALL (y: finseq[set[T]], R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R)):
  --   member(y, cartesian_product_n[set[T]](fsQ(R)(fsA))) IMPLIES
  --    FORALL (s: finseq[(R)] ...
  have surjective_aux_2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: surjective_aux_2_TCC4
  -- Statement: FORALL (y: finseq[set[T]], R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R)):
  --   member(y, cartesian_product_n[set[T]](fsQ(R)(fsA))) IMPLIES
  --    FORALL (s: finseq[(R)] ...
  have surjective_aux_2_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: surjective_aux_2_TCC5
  -- Statement: FORALL (y: finseq[set[T]], R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R)):
  --   member(y, cartesian_product_n[set[T]](fsQ(R)(fsA))) IMPLIES
  --    FORALL (s: finseq[(R)] ...
  have surjective_aux_2_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: surjective_aux_2_TCC6
  -- Statement: FORALL (y: finseq[set[T]], R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R)):
  --   member(y, cartesian_product_n[set[T]](fsQ(R)(fsA))) IMPLIES
  --    FORALL (s: finseq[(R)] ...
  have surjective_aux_2_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: surjective_aux_2_TCC7
  -- Statement: FORALL (y: finseq[set[T]], R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R)):
  --   member(y, cartesian_product_n[set[T]](fsQ(R)(fsA))) IMPLIES
  --    FORALL (s: finseq[(R)] ...
  have surjective_aux_2_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: surjective_aux_2_TCC8
  -- Statement: FORALL (y: finseq[set[T]], R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R)):
  --   member(y, cartesian_product_n[set[T]](fsQ(R)(fsA))) IMPLIES
  --    FORALL (s: finseq[(R)] ...
  have surjective_aux_2_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: chinese_remainder_theorem_rings.CRT_aux_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (fsA: fsI(R) | length(fsA) /= 0):
--   LET phi =
--         LAMBDA (x: (R)):
--           (# length := length(fsA),
--              seq
--                := (LAMBDA (i: below[length(fsA)]):
--                   
theorem CRT_aux_1 : ∀ (fsA : {fsA : {fsA : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool) // fsRI_p R(fsA)} // List.length fsA ≠ 0}), fun (phi : T → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length fsA} → T → Bool)) => R_homomorphism_p
    (R, cartesian_product_n (fsQ R(fsA))) (phi) ∧ R_kernel
    (R, cartesian_product_n (fsQ R(fsA))) (phi)
 = Intersection (seq2set fsA) (fun (x : T) => (# List.length := List.length fsA,
   seq
     := (fun (i : Fin List.length fsA)=> x + finseq_appl fsA(i)) #)) := by
  -- === TCCs for CRT_aux_1 ===
  -- TCC: CRT_aux_1_TCC1
  -- Statement: FORALL (R: ring[T, +, *, zero],
  --         fsA: fsI[T, +, *, zero](R) | length(fsA) /= 0,
  --         phi:
  --           [(R) -> [# length: nat, seq: [below[length(fsA)] -> set[T]] #]]):
  --   (phi =
  --     LAMBDA (...
  have crt_aux_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CRT_aux_1_TCC2
  -- Statement: FORALL (R: ring[T, +, *, zero],
  --         fsA: fsI[T, +, *, zero](R) | length(fsA) /= 0,
  --         phi:
  --           [(R) -> [# length: nat, seq: [below[length(fsA)] -> set[T]] #]]):
  --   (phi =
  --     LAMBDA (...
  have crt_aux_1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CRT_aux_1_TCC3
  -- Statement: FORALL (R: ring[T, +, *, zero],
  --         fsA: fsI[T, +, *, zero](R) | length(fsA) /= 0,
  --         phi:
  --           [(R) -> [# length: nat, seq: [below[length(fsA)] -> set[T]] #]]):
  --   (phi =
  --     LAMBDA (...
  have crt_aux_1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CRT_aux_1_TCC4
  -- Statement: FORALL (R: ring[T, +, *, zero],
  --         fsA: fsI[T, +, *, zero](R) | length(fsA) /= 0,
  --         phi:
  --           [(R) -> [# length: nat, seq: [below[length(fsA)] -> set[T]] #]]):
  --   (phi =
  --     LAMBDA (...
  have crt_aux_1_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CRT_aux_1_TCC5
  -- Statement: FORALL (R: ring[T, +, *, zero],
  --         fsA: fsI[T, +, *, zero](R) | length(fsA) /= 0,
  --         phi:
  --           [(R) -> [# length: nat, seq: [below[length(fsA)] -> set[T]] #]]):
  --   (phi =
  --     LAMBDA (...
  have crt_aux_1_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: CRT_aux_1_TCC6
  -- Statement: FORALL (R: ring[T, +, *, zero],
  --         fsA: fsI[T, +, *, zero](R) | length(fsA) /= 0,
  --         phi:
  --           [(R) -> [# length: nat, seq: [below[length(fsA)] -> set[T]] #]]):
  --   (phi =
  --     LAMBDA (...
  have crt_aux_1_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: chinese_remainder_theorem_rings.CRT_aux_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (R: ring_with_one, fsA: fsICM(R) | length(fsA) /= 0):
--   LET phi =
--         LAMBDA (x: (R)):
--           (# length := length(fsA),
--              seq
--                := (LAMBDA (i: below[length(fsA)])
theorem CRT_aux_2 : ∀ (R : T → Bool) (fsA : {fsA : {fsA : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool) // fsICM_p R(fsA)} // List.length fsA ≠ 0}), fun (phi : T → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length fsA} → T → Bool)) => surjective_p phi (fun (x : T) => (# List.length := List.length fsA,
   seq
     := (fun (i : Fin List.length fsA)=> x + finseq_appl fsA(i)) #)) := by
  -- === TCCs for CRT_aux_2 ===
  -- TCC: CRT_aux_2_TCC1
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0,
  --         phi:
  --           [(R) -> [# length: nat, seq: [below[length(fsA)] -> set[T]] #]]):
  -- ...
  have crt_aux_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: chinese_remainder_theorem_rings.Chinese_Remainder_Theorem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (R: ring_with_one, fsA: fsICM(R) | length(fsA) /= 0):
--   R_isomorphic?
--       [coset[T, +](R, Intersection(seq2set(fsA))),
--        add(R, Intersection(seq2set(fsA))),
--        product(R, Intersectio
theorem Chinese_Remainder_Theorem : ∀ (R : T → Bool) (fsA : {fsA : {fsA : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T → Bool) // fsICM_p R(fsA)} // List.length fsA ≠ 0}), R_isomorphic_p (restrict[set[T], coset[T, +](R, Intersection[T](seq2set[set[T]](fsA))), bool]
     (R / Intersection (seq2set fsA))) (cartesian_product_n (fsQ R(fsA))) := by
  -- === TCCs for Chinese_Remainder_Theorem ===
  -- TCC: Chinese_Remainder_Theorem_TCC1
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   FORALL (x: finseq[set[T]]):
  --     cartesian_product_n[set[T]](fsQ[T, +, *, zero](R)(fsA)...
  have chinese_remainder_theorem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Chinese_Remainder_Theorem_TCC2
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   EXISTS (x: coset[T, +](R, Intersection[T](seq2set[set[T]](fsA)))): TRUE
  have chinese_remainder_theorem_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Chinese_Remainder_Theorem_TCC3
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   FORALL (x1:
  --             [coset[T, +](R, Intersection[T](seq2set[set[T]](fsA))),
  --       ...
  have chinese_remainder_theorem_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Chinese_Remainder_Theorem_TCC4
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   FORALL (x1:
  --             [coset[T, +](R, Intersection[T](seq2set[set[T]](fsA))),
  --       ...
  have chinese_remainder_theorem_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Chinese_Remainder_Theorem_TCC5
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   coset?[T, +]
  --       (R, Intersection[T](seq2set[set[T]](fsA)))
  --       (Intersection[T](s...
  have chinese_remainder_theorem_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Chinese_Remainder_Theorem_TCC6
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   EXISTS (x: (cartesian_product_n[set[T]](fsQ[T, +, *, zero](R)(fsA)))):
  --     TRUE
  have chinese_remainder_theorem_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Chinese_Remainder_Theorem_TCC7
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   FORALL (x1:
  --             [(cartesian_product_n[set[T]](fsQ[T, +, *, zero](R)(fsA))),
  --   ...
  have chinese_remainder_theorem_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Chinese_Remainder_Theorem_TCC8
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   FORALL (x1:
  --             [(cartesian_product_n[set[T]](fsQ[T, +, *, zero](R)(fsA))),
  --   ...
  have chinese_remainder_theorem_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Chinese_Remainder_Theorem_TCC9
  -- Statement: FORALL (R: ring_with_one[T, +, *, zero, one],
  --         fsA: fsICM[T, +, *, zero, one](R) | length(fsA) /= 0):
  --   cartesian_product_n[set[T]](fsQ[T, +, *, zero](R)(fsA))(fsA)
  have chinese_remainder_theorem_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.chinese_remainder_theorem_rings