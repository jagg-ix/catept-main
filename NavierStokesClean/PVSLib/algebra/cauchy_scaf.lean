-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cauchy_scaf
-- PVSLib library: algebra
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cauchy_scaf

Auto-generated stubs from PVSLib theory `cauchy_scaf`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cauchy_scaf

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (add_first : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ11 : Type*} (bijective_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (card : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (disjoint_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} (empty_p : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (is_finite : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (length : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (member : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} (nonempty_p : pvs_τ23 → Prop)
variable {pvs_τ26 : Type*} (singleton : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (union : pvs_τ27 → pvs_τ28 → pvs_τ29)

-- pvs2lean: cauchy_scaf.add_element
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {seq: finseq | EXISTS (s: (S)): seq = add_first(a, s)}
axiom add_element {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ4 → pvs_τ5 → Prop

-- pvs2lean: cauchy_scaf.add_set
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {C: set[finseq[T]] | EXISTS (a: (M)): C = add_element(S)(a)}
axiom add_set {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ9 → pvs_τ10 → Prop

-- pvs2lean: cauchy_scaf.set_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {seq: finseq |
--    length(seq) = n AND
--     FORALL (i: below[n]): member(finseq_appl[T](seq)(i), M)}
axiom set_seq {pvs_τ24 : Type*} {pvs_τ25 : Type*} : pvs_τ24 → pvs_τ25 → Prop

-- pvs2lean: cauchy_scaf.emptyset_gives_emptyset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty?(S) IMPLIES empty?(add_element(S)(a))
theorem emptyset_gives_emptyset : empty_p S → empty_p (add_element S(a)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.emptyset_gives_emptyset1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty?(M) IMPLIES empty?(add_set(S)(M))
theorem emptyset_gives_emptyset1 : empty_p M → empty_p (add_set S(M)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.set_seq_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: set_seq(M)(0) = singleton(empty_seq)
theorem set_seq_singleton : set_seq M(0) = singleton empty_seq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.set_seq_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: empty?(M) IMPLIES empty?(set_seq(M)(p))
theorem set_seq_empty : empty_p M → empty_p (set_seq M(p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.add_element_add_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(a, M) IMPLIES
--  LET B = add_set(S)(M), A = add_element(S)(a) IN
--    Union(add_set(S)(add(a, M))) = union(Union(B), A)
theorem add_element_add_set : ¬ member a M → (let B := add_set S(M); let A := add_element S(a); Union (add_set S(add a M)) = union (Union B) A) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.card_add_element_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET A = add_element(S)(a) IN EXISTS (g: [(A) -> (S)]): bijective?(g)
theorem card_add_element_aux : fun (A : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => ∃ (g : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T)), bijective_p g (add_element S a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.card_add_element
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET A = add_element(S)(a) IN card(A) = card(S)
theorem card_add_element : fun (A : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => card A = card S (add_element S a) := by
  -- === TCCs for card_add_element ===
  -- TCC: card_add_element_TCC1
  -- Statement: FORALL (S: finite_set[finseq[T]], a: T, A: set[finseq[T]]):
  --   A = add_element(S)(a) IMPLIES is_finite[finseq[T]](A)
  have card_add_element_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.disjoint_add_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET B = add_set(S)(M), A = add_element(S)(a) IN
--   NOT member(a, M) IMPLIES disjoint?(Union(B), A)
theorem disjoint_add_set : fun (B : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) → Bool) => fun (A : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => ¬ member a M → disjoint_p (Union B) A (add_element S a) (add_set S M) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.add_set_is_add_ele
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET B = add_set(S)(M), A = add_element(S)(a) IN
--   M = singleton(a) IMPLIES Union(B) = A
theorem add_set_is_add_ele : fun (B : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) → Bool) => fun (A : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => M = singleton a → Union B = A (add_element S a) (add_set S M) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.add_set_is_finite_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(S) IMPLIES
--  LET B = add_set(S)(M) IN EXISTS (g: [(M) -> (B)]): bijective?(g)
theorem add_set_is_finite_aux : nonempty_p S → (let B := add_set S(M); ∃ (g: [(M): bijective_p g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.add_set_is_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET B = add_set(S)(M) IN is_finite(B)
theorem add_set_is_finite : fun (B : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) → Bool) => is_finite B (add_set S M) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.card_add_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET B = add_set(S)(M) IN card(Union(B)) = card(S) * card(M)
theorem card_add_set : fun (B : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) → Bool) => card (Union B) = card S * card M (add_set S M) := by
  -- === TCCs for card_add_set ===
  -- TCC: card_add_set_TCC1
  -- Statement: FORALL (M: finite_set[T], S: finite_set[finseq[T]],
  --         B: setofsets[finseq[T]]):
  --   B = add_set(S)(M) IMPLIES is_finite[finseq[T]](Union[finseq[T]](B))
  have card_add_set_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.set_seq_is_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET C = set_seq(M)(n) IN is_finite(C)
theorem set_seq_is_finite : fun (C : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => is_finite C (set_seq M n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.set_seq_is_add_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET C = set_seq(M)(p), D = set_seq(M)(p - 1) IN C = Union(add_set(D)(M))
theorem set_seq_is_add_set : fun (C : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => fun (D : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => C = Union (add_set D(M)) (set_seq M (p - 1)) (set_seq M p) := by
  -- === TCCs for set_seq_is_add_set ===
  -- TCC: set_seq_is_add_set_TCC1
  -- Statement: FORALL (M: finite_set[T], p: posnat, C: set[finseq[T]]):
  --   C = set_seq(M)(p) IMPLIES p - 1 >= 0
  have set_seq_is_add_set_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: set_seq_is_add_set_TCC2
  -- Statement: FORALL (M: finite_set[T], p: posnat, C: set[finseq[T]], D: set[finseq[T]]):
  --   C = set_seq(M)(p) AND D = set_seq(M)(p - 1) IMPLIES
  --    is_finite[finseq[T]](D)
  have set_seq_is_add_set_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cauchy_scaf.card_set_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET C = set_seq(M)(n) IN card(C) = card(M) ^ n
theorem card_set_seq : fun (C : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) → Bool) => card C = card M ^ n (set_seq M n) := by
  -- === TCCs for card_set_seq ===
  -- TCC: card_set_seq_TCC1
  -- Statement: FORALL (M: finite_set[T], n: nat, C: set[finseq[T]]):
  --   C = set_seq(M)(n) IMPLIES is_finite[finseq[T]](C)
  have card_set_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: card_set_seq_TCC2
  -- Statement: FORALL (M: finite_set[T], n: nat, C: set[finseq[T]]):
  --   C = set_seq(M)(n) IMPLIES card[T](M) /= 0 OR n >= 0
  have card_set_seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cauchy_scaf