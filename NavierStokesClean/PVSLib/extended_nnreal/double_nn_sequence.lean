-- Auto-generated by pvs2lean/translator.py
-- PVS theory: double_nn_sequence
-- PVSLib library: extended_nnreal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: double_nn_sequence

Auto-generated stubs from PVSLib theory `double_nn_sequence`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.double_nn_sequence

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Im : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (bounded_above_p : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (convergence : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} (convergent_p : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (double_index_n : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (increasing_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (least_upper_bound_p : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (limit : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (lub : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} (nonempty_p : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (series : pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (single_index : pvs_τ21 → pvs_τ22 → pvs_τ23)

-- pvs2lean: double_nn_sequence.nn_series_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: increasing?(series(v))
theorem nn_series_increasing : increasing_p (series v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.nn_index_scaf1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: series(single_index(u))(double_index_n(0, n)) =
--  sigma(0, n, LAMBDA i: sigma(0, n - i, LAMBDA j: u(i, j)))
theorem nn_index_scaf1 : series (single_index u) (double_index_n 0 n) = Finset.sum (Finset.Icc 0 n) (fun i=>  sigma 0 (n - i) (fun j=>  u i j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.nn_double_index_incr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: double_index_n(i, j) = double_index_n(0, i + j) - i
theorem nn_double_index_incr : double_index_n i j = double_index_n 0 (i + j) - i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.nn_index_scaf2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, n, LAMBDA i: sigma(0, n, LAMBDA j: u(i, j))) <=
--  series(single_index(u))(double_index_n(0, 2 * n))
theorem nn_index_scaf2 : Finset.sum (Finset.Icc 0 n) (fun i=>  sigma 0 n (fun j=>  u i j)) ≤ series (single_index u) (double_index_n 0 (2 * n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.nn_index_scaf3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS n:
--   sigma(0, i, LAMBDA i: sigma(0, j, LAMBDA j: u(i, j))) <=
--    series(single_index(u))(n)
theorem nn_index_scaf3 : ∃ (n : {i : ℝ // i ≥ 0}), Finset.sum (Finset.Icc 0 i) (fun i=>  sigma 0 j (fun j=>  u i j)) ≤ series (single_index u) (n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.nn_index_scaf4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS i, j:
--   series(single_index(u))(n) <=
--    sigma(0, i, LAMBDA i: sigma(0, j, LAMBDA j: u(i, j)))
theorem nn_index_scaf4 : ∃ (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), series (single_index u) (n) ≤ Finset.sum (Finset.Icc 0 i) (fun i=>  sigma 0 j (fun j=>  u i j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.nn_convegent_bounded
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(v)) <=> bounded_above?(Im(series(v)))
theorem nn_convegent_bounded : convergent_p (series v) <=> bounded_above_p (Im (series v)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.nn_limit_lub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(v)) => limit(series(v)) = lub(Im(series(v)))
theorem nn_limit_lub : convergent_p (series v) → limit (series v) = lub (Im (series v)) := by
  -- === TCCs for nn_limit_lub ===
  -- TCC: nn_limit_lub_TCC1
  -- Statement: FORALL (v: [nat -> nnreal]):
  --   convergent?(series(v)) IMPLIES bounded_above?(Im[nat](series(v)))
  have nn_limit_lub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.nn_convergence_least_upper_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: increasing?(v) => (convergence(v, l) <=> least_upper_bound?(l, Im(v)))
theorem nn_convergence_least_upper_bound : increasing_p v → (convergence v l ≤> least_upper_bound_p l (Im v)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_series
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: series(LAMBDA i: series(LAMBDA j: u(i, j))(j))(i) =
--  series(LAMBDA j: series(LAMBDA i: u(i, j))(i))(j)
theorem double_series : series (fun i=>  series (fun j=>  u i j) (j)) (i) = series (fun j=>  series (fun i=>  u i j) (i)) (j) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_subseq_convergent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(single_index(u))) =>
--  convergent?(series(LAMBDA j: u(i, j)))
theorem double_subseq_convergent : convergent_p (series (single_index u)) → convergent_p (series (fun j=>  u i j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_subseq_bounded
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: bounded_above?(Im(series(single_index(u)))) =>
--  bounded_above?(Im(series(LAMBDA j: u(i, j))))
theorem double_subseq_bounded : bounded_above_p (Im (series (single_index u))) → bounded_above_p (Im (series (fun j=>  u i j))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.series_limit_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL i: convergent?(LAMBDA j: u(i, j))) =>
--  series(LAMBDA i: limit(LAMBDA j: u(i, j)))(i) =
--   limit(LAMBDA j: series(LAMBDA i: u(i, j))(i))
theorem series_limit_def : (∀ i,  convergent_p (fun j=>  u i j)) → series (fun i=>  limit (fun j=>  u i j)) (i) =
 limit (fun j=>  series (fun i=>  u i j) (i)) := by
  -- === TCCs for series_limit_def ===
  -- TCC: series_limit_def_TCC1
  -- Statement: FORALL (u: [[nat, nat] -> nnreal]):
  --   (FORALL (i: nat): convergent?(LAMBDA (j: nat): u(i, j))) IMPLIES
  --    FORALL (i: nat): convergent?(LAMBDA (j: nat): u(i, j))
  have series_limit_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: series_limit_def_TCC2
  -- Statement: FORALL (i1: nat, u: [[nat, nat] -> nnreal]):
  --   (FORALL (i: nat): convergent?(LAMBDA (j: nat): u(i, j))) IMPLIES
  --    convergent?(LAMBDA (j: nat): series(LAMBDA (i: nat): u(i, j))(i1))
  have series_limit_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_approx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL i: convergent?(series(LAMBDA j: u(i, j)))) =>
--  series(LAMBDA i: limit(series(LAMBDA j: u(i, j))))(i) =
--   limit(series(LAMBDA j: series(LAMBDA i: u(i, j))(i)))
theorem double_approx : (∀ i,  convergent_p (series (fun j=>  u i j))) → series (fun i=>  limit (series (fun j=>  u i j))) (i) =
 limit (series (fun j=>  series (fun i=>  u i j) (i))) := by
  -- === TCCs for double_approx ===
  -- TCC: double_approx_TCC1
  -- Statement: FORALL (u: [[nat, nat] -> nnreal]):
  --   (FORALL (i: nat): convergent?(series(LAMBDA (j: nat): u(i, j)))) IMPLIES
  --    FORALL (i: nat): convergent?(series(LAMBDA (j: nat): u(i, j)))
  have double_approx_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: double_approx_TCC2
  -- Statement: FORALL (i1: nat, u: [[nat, nat] -> nnreal]):
  --   (FORALL (i: nat): convergent?(series(LAMBDA (j: nat): u(i, j)))) IMPLIES
  --    convergent?(series(LAMBDA (j: nat):
  --                         series(LAMBDA ...
  have double_approx_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_approx1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(series(single_index(u)), l) <=>
--  least_upper_bound?(l,
--                     {z |
--                        EXISTS i, j:
--                          series(LAMBDA j: series(LAMBDA i: u(i, j))(i))(
theorem double_approx1 : convergence (series (single_index u)) l <=> least_upper_bound_p l ({z |
                      ∃ i j, 
                        series (fun j=>  series (fun i=>  u i j) (i)) (j) =
                         z}) := by
  -- === TCCs for double_approx1 ===
  -- TCC: double_approx1_TCC1
  -- Statement: FORALL (u: [[nat, nat] -> nnreal]):
  --   nonempty?[real]
  --       ({z: real |
  --           EXISTS (i: nat), (j: nat):
  --             series(LAMBDA (j: nat): series(LAMBDA (i: nat): u(i, j))(i))(j)
  --             ...
  have double_approx1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_approx2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL i: convergent?(series(LAMBDA j: u(i, j)))) =>
--  (convergence(series(LAMBDA i: limit(series(LAMBDA j: u(i, j)))), l) <=>
--    least_upper_bound?(l,
--                       {z |
--                      
theorem double_approx2 : (∀ i,  convergent_p (series (fun j=>  u i j))) → (convergence (series (fun i=>  limit (series (fun j=>  u i j)))) l ≤>
  least_upper_bound_p l ({z |
                        ∃ i j, 
                          series (fun j=>  series (fun i=>  u i j) (i)) (j)
                           = z})) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_left_convergence
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(series(single_index(u)), l) <=>
--  (FORALL i: convergent?(series(LAMBDA j: u(i, j)))) AND
--   convergence(series(LAMBDA i: limit(series(LAMBDA j: u(i, j)))), l)
theorem double_left_convergence : convergence (series (single_index u)) l <=> (∀ i,  convergent_p (series (fun j=>  u i j))) ∧
 convergence (series (fun i=>  limit (series (fun j=>  u i j)))) l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_left_convergent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(single_index(u))) <=>
--  (FORALL i: convergent?(series(LAMBDA j: u(i, j)))) AND
--   convergent?(series(LAMBDA i: limit(series(LAMBDA j: u(i, j)))))
theorem double_left_convergent : convergent_p (series (single_index u)) <=> (∀ i,  convergent_p (series (fun j=>  u i j))) ∧
 convergent_p (series (fun i=>  limit (series (fun j=>  u i j)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_left_limit
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(single_index(u))) =>
--  limit(series(single_index(u))) =
--   limit(series(LAMBDA i: limit(series(LAMBDA j: u(i, j)))))
theorem double_left_limit : convergent_p (series (single_index u)) → limit (series (single_index u)) =
 limit (series (fun i=>  limit (series (fun j=>  u i j)))) := by
  -- === TCCs for double_left_limit ===
  -- TCC: double_left_limit_TCC1
  -- Statement: FORALL (u: [[nat, nat] -> nnreal]):
  --   convergent?(series(single_index(u))) IMPLIES
  --    FORALL (i: nat): convergent?(series(LAMBDA (j: nat): u(i, j)))
  have double_left_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: double_left_limit_TCC2
  -- Statement: FORALL (u: [[nat, nat] -> nnreal]):
  --   convergent?(series(single_index(u))) IMPLIES
  --    convergent?(series(LAMBDA (i: nat):
  --                         limit(series(LAMBDA (j: nat): u(i, j)))))
  have double_left_limit_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_right_convergence
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(series(single_index(u)), l) <=>
--  convergence(series(single_index(LAMBDA j, i: u(i, j))), l)
theorem double_right_convergence : convergence (series (single_index u)) l <=> convergence (series (single_index (fun j i=>  u i j))) l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_right_convergent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(single_index(u))) <=>
--  convergent?(series(single_index(LAMBDA j, i: u(i, j))))
theorem double_right_convergent : convergent_p (series (single_index u)) <=> convergent_p (series (single_index (fun j i=>  u i j))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: double_nn_sequence.double_right_limit
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(single_index(u))) =>
--  limit(series(single_index(u))) =
--   limit(series(single_index(LAMBDA j, i: u(i, j))))
theorem double_right_limit : convergent_p (series (single_index u)) → limit (series (single_index u)) =
 limit (series (single_index (fun j i=>  u i j))) := by
  -- === TCCs for double_right_limit ===
  -- TCC: double_right_limit_TCC1
  -- Statement: FORALL (u: [[nat, nat] -> nnreal]):
  --   convergent?(series(single_index(u))) IMPLIES
  --    convergent?(series(single_index[nnreal]
  --                           (LAMBDA (j: nat), (i: nat): u(i, j))))
  have double_right_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.double_nn_sequence