-- Auto-generated by pvs2lean/translator.py
-- PVS theory: series
-- PVSLib library: series
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: series

Auto-generated stubs from PVSLib theory `series`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.series

noncomputable section

variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (convergence : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} (convergent_p : pvs_τ5 → Prop)
variable {pvs_τ10 : Type*} (limit : pvs_τ10 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (upfrom : pvs_τ13 → pvs_τ14)

-- pvs2lean: series.series
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA (n: nat): sigma(0, n, a))
axiom series {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12 → Prop

-- pvs2lean: series.conv_series?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a))
axiom conv_series_p {pvs_τ1 : Type*} {pvs_τ2 : Type*} : pvs_τ1 → pvs_τ2 → Prop

-- pvs2lean: series.inf_sum
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: limit(series(cs))
axiom inf_sum {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: series.abs
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA (n: nat): abs(a(n)))
noncomputable def abs : sorry := by
  sorry  -- pvs2lean: definition stub for (LAMBDA (n: nat): abs(a(n)))

-- pvs2lean: series.series_diff
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: series(a) - series(b) = series(a - b)
theorem series_diff : series a - series b = series (a - b) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.series_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: series(a) + series(b) = series(a + b)
theorem series_sum : series a + series b = series (a + b) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.series_m_diff
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: series(a, m) - series(b, m) = series(a - b, m)
theorem series_m_diff : series a m - series b m = series (a - b) m := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.series_m_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: series(a, m) + series(b, m) = series(a + b, m)
theorem series_m_sum : series a m + series b m = series (a + b) m := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.series_m_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: c * series(a, m) = series(c * a, m)
theorem series_m_scal : c * series a m = series (c * a) m := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.series_m_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL k: k >= m IMPLIES a(k) = b(k)) IMPLIES series(a, m) = series(b, m)
theorem series_m_eq : (∀ k,  k ≥ m → a k = b k) → series a m = series b m := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.series_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: c * series(a) = series(LAMBDA n: c * a(n))
theorem series_scal : c * series a = series (fun n=>  c * a n) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.series_neg
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: series(-a) = -series(a)
theorem series_neg : series -a = -series a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.conv_series_terms_to_0
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IMPLIES convergence(a, 0)
theorem conv_series_terms_to_0 : convergent_p (series a) → convergence a 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.series_limit_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IMPLIES limit(a) = 0
theorem series_limit_0 : convergent_p (series a) → limit a = 0 := by
  -- === TCCs for series_limit_0 ===
  -- TCC: series_limit_0_TCC1
  -- Statement: FORALL (a: sequence[real]): convergent?(series(a)) IMPLIES convergent?(a)
  have series_limit_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.convergent_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(abs(a))) IMPLIES convergent?(series(a))
theorem convergent_abs : convergent_p (series |a|) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.partial_sums
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IFF
--  (FORALL (epsilon: posreal):
--     (EXISTS (N: posint):
--        (FORALL n, m:
--           (n > m AND m >= N) IMPLIES abs(sigma(m + 1, n, a)) < epsilon)))
theorem partial_sums : convergent_p (series a) ↔ (∀ (epsilon : {x : ℝ // 0 < x}), (∃ (N : {n : ℤ // 0 < n}), (∀ n m, 
         (n > m ∧ m ≥ N) → |Finset.sum (Finset.Icc m + 1 n) (a)| < epsilon))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.zero_series_conv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(LAMBDA n: 0))
theorem zero_series_conv : convergent_p (series (fun (n : {i : ℝ // i ≥ 0}) => 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.zero_series_limit
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: limit(series(LAMBDA n: 0)) = 0
theorem zero_series_limit : limit (series (fun n=>  0)) = 0 := by
  -- === TCCs for zero_series_limit ===
  -- TCC: zero_series_limit_TCC1
  -- Statement: convergent?(series(LAMBDA n: 0))
  have zero_series_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.tail_series_conv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IMPLIES convergent?(series((LAMBDA n: a(n + N))))
theorem tail_series_conv : convergent_p (series a) → convergent_p (series ((fun n=>  a (n + N)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.tail_series_conv2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series((LAMBDA n: a(n + N)))) IMPLIES convergent?(series(a))
theorem tail_series_conv2 : convergent_p (series ((fun n=>  a (n + N)))) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.conv_series_shift
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IFF convergent?(series((LAMBDA n: a(n + N))))
theorem conv_series_shift : convergent_p (series a) ↔ convergent_p (series ((fun n=>  a (n + N)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.tail_conv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) AND
--  (EXISTS (N: nat): (FORALL (n: nat): n >= N IMPLIES a(n) = b(n)))
--  IMPLIES convergent?(series(b))
theorem tail_conv : convergent_p (series a) ∧
 (∃ (N : ℕ), (∀ (n : ℕ), n ≥ N → a n = b n)) → convergent_p (series b) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.end_series_conv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IFF convergent?(series(a, m))
theorem end_series_conv : convergent_p (series a) ↔ convergent_p (series a m) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.scal_series_conv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IMPLIES convergent?(c * series(a))
theorem scal_series_conv : convergent_p (series a) → convergent_p (c * series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.conv_series_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(cnz * series(a)) IFF convergent?(series(a))
theorem conv_series_scal : convergent_p (cnz * series a) ↔ convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.limit_series_shift
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IMPLIES
--  limit(series(a)) = sigma(0, pn - 1, a) + limit(series(LAMBDA n: a(n + pn)))
theorem limit_series_shift : convergent_p (series a) → limit (series a) = Finset.sum (Finset.Icc 0 pn - 1) (a) + limit (series (fun n=>  a (n + pn))) := by
  -- === TCCs for limit_series_shift ===
  -- TCC: limit_series_shift_TCC1
  -- Statement: FORALL (a: sequence[real], pn: posnat):
  --   convergent?(series(a)) IMPLIES
  --    convergent?(series(LAMBDA (n: nat): a(n + pn)))
  have limit_series_shift_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.limit_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL n: a(n) > 0) AND convergent?(a) IMPLIES limit(a) >= 0
theorem limit_pos : (∀ n,  a n > 0) ∧ convergent_p a → limit a ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.series_first
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) IMPLIES limit(series(a)) = a(0) + limit(series(a, 1))
theorem series_first : convergent_p (series a) → limit (series a) = a 0 + limit (series a 1) := by
  -- === TCCs for series_first ===
  -- TCC: series_first_TCC1
  -- Statement: FORALL (a: sequence[real]):
  --   convergent?(series(a)) IMPLIES convergent?(series(a, 1))
  have series_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.inf_sum_scal
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a, k)) IMPLIES c * inf_sum(k, a) = inf_sum(k, c * a)
theorem inf_sum_scal : convergent_p (series a k) → c * inf_sum k a = inf_sum k (c * a) := by
  -- === TCCs for inf_sum_scal ===
  -- TCC: inf_sum_scal_TCC1
  -- Statement: FORALL (a: sequence[real], k: nat):
  --   convergent?(series(a, k)) IMPLIES conv_series?(a, k)
  have inf_sum_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inf_sum_scal_TCC2
  -- Statement: FORALL (a: sequence[real], c: real, k: nat):
  --   convergent?(series(a, k)) IMPLIES conv_series?(*[nat](c, a), k)
  have inf_sum_scal_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.inf_sum_gt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: conv_series?(s) AND (FORALL (n: nat): s(n) > 0) IMPLIES inf_sum(s) > 0
theorem inf_sum_gt_0 : conv_series_p s ∧ (∀ (n : ℕ), s n > 0) → inf_sum s > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.infsum_by2s_prep
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) AND (FORALL k: b(k) = a(2 * k) + a(2 * k + 1))
--  IMPLIES convergent?(series(b))
theorem infsum_by2s_prep : convergent_p (series a) ∧ (∀ k,  b k = a (2 * k) + a (2 * k + 1)) → convergent_p (series b) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.infsum_by2s
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) AND (FORALL k: b(k) = a(2 * k) + a(2 * k + 1))
--  IMPLIES convergent?(series(b)) AND inf_sum(a) = inf_sum(b)
theorem infsum_by2s : convergent_p (series a) ∧ (∀ k,  b k = a (2 * k) + a (2 * k + 1)) → convergent_p (series b) ∧ inf_sum a = inf_sum b := by
  -- === TCCs for infsum_by2s ===
  -- TCC: infsum_by2s_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(b)) AND
  --    (FORALL (k: nat): b(k) = a(2 * k) + a(2 * k + 1)) AND
  --     convergent?(series(a))
  --    IMPLIES conv_series?(a)
  have infsum_by2s_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: infsum_by2s_TCC2
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(b)) AND
  --    (FORALL (k: nat): b(k) = a(2 * k) + a(2 * k + 1)) AND
  --     convergent?(series(a))
  --    IMPLIES conv_series?(b)
  have infsum_by2s_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.infsum_even_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) AND (FORALL k: b(k) = a(2 * k + 1) AND a(2 * k) = 0)
--  IMPLIES convergent?(series(b)) AND inf_sum(a) = inf_sum(b)
theorem infsum_even_0 : convergent_p (series a) ∧ (∀ k,  b k = a (2 * k + 1) ∧ a (2 * k) = 0) → convergent_p (series b) ∧ inf_sum a = inf_sum b := by
  -- === TCCs for infsum_even_0 ===
  -- TCC: infsum_even_0_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(b)) AND
  --    (FORALL (k: nat): b(k) = a(2 * k + 1) AND a(2 * k) = 0) AND
  --     convergent?(series(a))
  --    IMPLIES conv_series?(a)
  have infsum_even_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: infsum_even_0_TCC2
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(b)) AND
  --    (FORALL (k: nat): b(k) = a(2 * k + 1) AND a(2 * k) = 0) AND
  --     convergent?(series(a))
  --    IMPLIES conv_series?(b)
  have infsum_even_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.comparison_test
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(b)) AND (FORALL n: abs(a(n)) <= b(n)) IMPLIES
--  convergent?(series(a))
theorem comparison_test : convergent_p (series b) ∧ (∀ n,  |a n| ≤ b n) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.comparison_test_gen
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(b)) AND
--  (EXISTS (N: nat): (FORALL (n: nat): n >= N IMPLIES abs(a(n)) <= b(n)))
--  IMPLIES convergent?(series(a))
theorem comparison_test_gen : convergent_p (series b) ∧
 (∃ (N : ℕ), (∀ (n : ℕ), n ≥ N → |a n| ≤ b n)) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.inf_sum_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL k: k >= m IMPLIES a(k) = b(k)) AND
--  conv_series?(a, m) AND conv_series?(b, m)
--  IMPLIES inf_sum(m, a) = inf_sum(m, b)
theorem inf_sum_eq : (∀ k,  k ≥ m → a k = b k) ∧
 conv_series_p a m ∧ conv_series_p b m → inf_sum m a = inf_sum m b := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.inf_sum_le
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (n: upfrom(m)): abs(a(n)) <= b(n)) AND convergent?(series(b, m))
--  IMPLIES inf_sum(m, a) <= inf_sum(m, b)
theorem inf_sum_le : (∀ (n : upfrom m), |a n| ≤ b n) ∧ convergent_p (series b m) → inf_sum m a ≤ inf_sum m b := by
  -- === TCCs for inf_sum_le ===
  -- TCC: inf_sum_le_TCC1
  -- Statement: FORALL (a, b: sequence[real], m: nat):
  --   (convergent?(series(b, m)) AND FORALL (n: upfrom(m)): abs(a(n)) <= b(n))
  --    IMPLIES conv_series?(a, m)
  have inf_sum_le_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inf_sum_le_TCC2
  -- Statement: FORALL (a, b: sequence[real], m: nat):
  --   (convergent?(series(b, m)) AND FORALL (n: upfrom(m)): abs(a(n)) <= b(n))
  --    IMPLIES conv_series?(b, m)
  have inf_sum_le_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.inf_sum_le_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (n: upfrom(m)): abs(a(n)) <= b(n)) AND convergent?(series(b, m))
--  IMPLIES abs(inf_sum(m, a)) <= inf_sum(m, b)
theorem inf_sum_le_abs : (∀ (n : upfrom m), |a n| ≤ b n) ∧ convergent_p (series b m) → |inf_sum m a| ≤ inf_sum m b := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.inf_sum_triangle
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(abs(a))) IMPLIES abs(inf_sum(m, a)) <= inf_sum(m, abs(a))
theorem inf_sum_triangle : convergent_p (series |a|) → |inf_sum m a| ≤ inf_sum m |a| := by
  -- === TCCs for inf_sum_triangle ===
  -- TCC: inf_sum_triangle_TCC1
  -- Statement: FORALL (a: sequence[real], m: nat):
  --   convergent?(series(abs(a))) IMPLIES conv_series?(a, m)
  have inf_sum_triangle_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inf_sum_triangle_TCC2
  -- Statement: FORALL (a: sequence[real], m: nat):
  --   convergent?(series(abs(a))) IMPLIES conv_series?(abs(a), m)
  have inf_sum_triangle_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.series_sum_conv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) AND convergent?(series(b)) IMPLIES
--  convergent?(series(a + b))
theorem series_sum_conv : convergent_p (series a) ∧ convergent_p (series b) → convergent_p (series (a + b)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.series_sum_convergence
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(series(a), l1) AND convergence(series(b), l2) IMPLIES
--  convergence(series(a + b), l1 + l2)
theorem series_sum_convergence : convergence (series a) l1 ∧ convergence (series b) l2 → convergence (series (a + b)) (l1 + l2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.inf_sum_of_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(a)) AND convergent?(series(b)) IMPLIES
--  inf_sum(a + b) = inf_sum(a) + inf_sum(b)
theorem inf_sum_of_sum : convergent_p (series a) ∧ convergent_p (series b) → inf_sum (a + b) = inf_sum a + inf_sum b := by
  -- === TCCs for inf_sum_of_sum ===
  -- TCC: inf_sum_of_sum_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(b)) AND convergent?(series(a)) IMPLIES
  --    conv_series?((+[nat])(a, b))
  have inf_sum_of_sum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inf_sum_of_sum_TCC2
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(b)) AND convergent?(series(a)) IMPLIES conv_series?(a)
  have inf_sum_of_sum_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inf_sum_of_sum_TCC3
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(b)) AND convergent?(series(a)) IMPLIES conv_series?(b)
  have inf_sum_of_sum_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.abs_x_to_n_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES convergent?((LAMBDA (n: nat): abs(x) ^ n))
theorem abs_x_to_n_conv : |x| < 1 → convergent_p ((fun (n : ℕ)=> |x| ^ n)) := by
  -- === TCCs for abs_x_to_n_conv ===
  -- TCC: abs_x_to_n_conv_TCC1
  -- Statement: FORALL (x: real): abs(x) < 1 IMPLIES FORALL (n: nat): abs(x) /= 0 OR n >= 0
  have abs_x_to_n_conv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.cnv_seq_abs_x_to_n
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES limit(LAMBDA (n: nat): abs(x) ^ n) = 0
theorem cnv_seq_abs_x_to_n : |x| < 1 → limit (fun (n : ℕ)=> |x| ^ n) = 0 := by
  -- === TCCs for cnv_seq_abs_x_to_n ===
  -- TCC: cnv_seq_abs_x_to_n_TCC1
  -- Statement: FORALL (x: real):
  --   abs(x) < 1 IMPLIES convergent?(LAMBDA (n: nat): abs(x) ^ n)
  have cnv_seq_abs_x_to_n_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.x_to_n_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES convergent?((LAMBDA (n: nat): x ^ n))
theorem x_to_n_conv : |x| < 1 → convergent_p ((fun (n : ℕ)=> x ^ n)) := by
  -- === TCCs for x_to_n_conv ===
  -- TCC: x_to_n_conv_TCC1
  -- Statement: FORALL (x: real): abs(x) < 1 IMPLIES FORALL (n: nat): x /= 0 OR n >= 0
  have x_to_n_conv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.convergence_x_to_n
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES convergence((LAMBDA (n: nat): c * x ^ n), 0)
theorem convergence_x_to_n : |x| < 1 → convergence ((fun (n : ℕ)=> c * x ^ n)) 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.geometric
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA n: x ^ n)
axiom geometric {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ6 → pvs_τ7

-- pvs2lean: series.sigma_geometric_aux
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (1 - x) * sigma(0, n, geometric(x)) = (1 - x ^ (n + 1))
theorem sigma_geometric_aux : (1 - x) * Finset.sum (Finset.Icc 0 n) (geometric x) = (1 - x ^ (n + 1)) := by
  -- === TCCs for sigma_geometric_aux ===
  -- TCC: sigma_geometric_aux_TCC1
  -- Statement: FORALL (n: nat, x: real): x /= 0 OR (n + 1) >= 0
  have sigma_geometric_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.sigma_geometric
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= 1 IMPLIES sigma(0, n, geometric(x)) = (1 - x ^ (n + 1)) / (1 - x)
theorem sigma_geometric : x ≠ 1 → Finset.sum (Finset.Icc 0 n) (geometric x) = (1 - x ^ (n + 1)) / (1 - x) := by
  -- === TCCs for sigma_geometric ===
  -- TCC: sigma_geometric_TCC1
  -- Statement: FORALL (x: real): x /= 1 IMPLIES (1 - x) /= 0
  have sigma_geometric_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.geometric_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES convergence(series(geometric(x)), 1 / (1 - x))
theorem geometric_series : |x| < 1 → convergence (series (geometric x)) (1 / (1 - x)) := by
  -- === TCCs for geometric_series ===
  -- TCC: geometric_series_TCC1
  -- Statement: FORALL (x: real): abs(x) < 1 IMPLIES (1 - x) /= 0
  have geometric_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.geometric_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES convergent?(series(geometric(x)))
theorem geometric_conv : |x| < 1 → convergent_p (series (geometric x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.const_geometric_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES convergence(series(c * geometric(x)), c / (1 - x))
theorem const_geometric_series : |x| < 1 → convergence (series (c * geometric x)) (c / (1 - x)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.geometric_sum
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x) < 1 IMPLIES inf_sum(geometric(x)) = 1 / (1 - x)
theorem geometric_sum : |x| < 1 → inf_sum (geometric x) = 1 / (1 - x) := by
  -- === TCCs for geometric_sum ===
  -- TCC: geometric_sum_TCC1
  -- Statement: FORALL (x: real): abs(x) < 1 IMPLIES conv_series?(geometric(x))
  have geometric_sum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.scaf_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (n: nat): abs(a(n + 1)) <= rho * abs(a(n))) IMPLIES
--  abs(a(n)) <= abs(a(0)) * rho ^ n
theorem scaf_abs : (∀ (n : ℕ), |a (n + 1)| ≤ rho * |a n|) → |a n| ≤ |a 0| * rho ^ n := by
  -- === TCCs for scaf_abs ===
  -- TCC: scaf_abs_TCC1
  -- Statement: FORALL (a: sequence[real], n1: nat, rho: posreal):
  --   (FORALL (n: nat): abs(a(n + 1)) <= rho * abs(a(n))) IMPLIES
  --    rho /= 0 OR n1 >= 0
  have scaf_abs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series.ratio_test
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL n: a(n) /= 0) AND
--  (EXISTS (rho: posreal):
--     rho < 1 AND (FORALL (n: nat): abs(a(n + 1) / a(n)) <= rho))
--  IMPLIES convergent?(series(a))
theorem ratio_test : (∀ n,  a n ≠ 0) ∧
 (∃ (rho : {x : ℝ // 0 < x}), rho < 1 ∧ (∀ (n : ℕ), |a (n + 1) / a n| ≤ rho)) → convergent_p (series a) := by
  -- === TCCs for ratio_test ===
  -- TCC: ratio_test_TCC1
  -- Statement: FORALL (a: sequence[real]):
  --   (FORALL (n: nat): a(n) /= 0) IMPLIES
  --    FORALL (rho: posreal): rho < 1 IMPLIES FORALL (n: nat): a(n) /= 0
  have ratio_test_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.ratio_test_gen
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL n: a(n) /= 0) AND
--  (EXISTS (rho: posreal):
--     rho < 1 AND
--      (EXISTS (N: nat):
--         (FORALL (n: nat): n >= N IMPLIES abs(a(n + 1) / a(n)) <= rho)))
--  IMPLIES convergent?(series(a))
theorem ratio_test_gen : (∀ n,  a n ≠ 0) ∧
 (∃ (rho : {x : ℝ // 0 < x}), rho < 1 ∧
     (∃ (N : ℕ), (∀ (n : ℕ), n ≥ N → |a (n + 1) / a n| ≤ rho))) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.ratio_test_gt_N
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (N: nat):
--    (EXISTS (rho: posreal):
--       rho < 1 AND
--        (FORALL (n: nat):
--           n >= N IMPLIES a(n) /= 0 AND abs(a(n + 1) / a(n)) <= rho)))
--  IMPLIES convergent?(series(a))
theorem ratio_test_gt_N : (∃ (N : ℕ), (∃ (rho : {x : ℝ // 0 < x}), rho < 1 ∧
       (∀ (n : ℕ), n ≥ N → a n ≠ 0 ∧ |a (n + 1) / a n| ≤ rho))) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series.ratio_test_lim
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL n: a(n) /= 0) AND
--  convergent?(LAMBDA n: abs(a(n + 1) / a(n))) AND
--   limit(LAMBDA n: abs(a(n + 1) / a(n))) < 1
--  IMPLIES convergent?(series(a))
theorem ratio_test_lim : (∀ n,  a n ≠ 0) ∧
 convergent_p (fun n=>  |a (n + 1) / a n|) ∧
  limit (fun n=>  |a (n + 1) / a n|) < 1 → convergent_p (series a) := by
  -- === TCCs for ratio_test_lim ===
  -- TCC: ratio_test_lim_TCC1
  -- Statement: FORALL (a: sequence[real]):
  --   (FORALL (n: nat): a(n) /= 0) IMPLIES FORALL (n: nat): a(n) /= 0
  have ratio_test_lim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.series