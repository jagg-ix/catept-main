-- Auto-generated by pvs2lean/translator.py
-- PVS theory: Cauchy_product
-- PVSLib library: series
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: Cauchy_product

Auto-generated stubs from PVSLib theory `Cauchy_product`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.Cauchy_product

noncomputable section

variable {pvs_τ1 : Type*} (conv_series_p : pvs_τ1 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (convergence : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (inf_sum : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (series : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (sup : pvs_τ14 → pvs_τ15)

-- pvs2lean: Cauchy_product.sigma_reverse
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real]):
--   FORALL (n: nat):
--     sigma[below[n + 1]](0, n, LAMBDA (i: below[n + 1]): a(i)) =
--      sigma[below[n + 1]](0, n, LAMBDA (i: below[n + 1]): a(n - i))
theorem sigma_reverse : ∀ (a : {i : ℝ // i ≥ 0} → ℝ), ∀ (n : {i : ℝ // i ≥ 0}), Finset.sum (Finset.Icc 0 n) (fun (i : Fin n + 1)=> a i) = Finset.sum (Finset.Icc 0 n) (fun (i : Fin n + 1)=> a (n - i)) := by
  -- === TCCs for sigma_reverse ===
  -- TCC: sigma_reverse_TCC1
  -- Statement: FORALL (n: nat): 0 >= 0 AND 0 < 1 + n OR EXISTS (j: below[1 + n]): j <= 0
  have sigma_reverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_reverse_TCC2
  -- Statement: FORALL (n: nat): n >= 0 AND n < 1 + n OR EXISTS (j: below[1 + n]): n <= j
  have sigma_reverse_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_reverse_TCC3
  -- Statement: FORALL (n: nat):
  --   (FORALL (x, y: below[1 + n]), (z: integer):
  --      x <= z AND z <= y IMPLIES z >= 0 AND z < 1 + n)
  have sigma_reverse_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_reverse_TCC4
  -- Statement: FORALL (n: nat, i: below[n + 1]): n - i >= 0
  have sigma_reverse_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Cauchy_product.conv_sum
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, n,
--       LAMBDA (i: nat):
--         sigma[below[i + 1]]
--             (0, i, LAMBDA (j: below[i + 1]): a(j) * b(i - j)))
axiom conv_sum {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ2 → pvs_τ3 → pvs_τ4 → pvs_τ5

-- pvs2lean: Cauchy_product.partial_sum_aux
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real]):
--   FORALL (n: nat):
--     conv_sum(a, b, n) =
--      sigma[below[n + 1]]
--          (0, n, LAMBDA (i: below[n + 1]): a(n - i) * series(b)(i))
theorem partial_sum_aux : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ), ∀ (n : {i : ℝ // i ≥ 0}), conv_sum a b n = Finset.sum (Finset.Icc 0 n) (fun (i : Fin n + 1)=> a (n - i) * series b(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Cauchy_product.Cauchy_product_aux
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n: nat):
--   conv_series?(a) AND conv_series?(b) =>
--    conv_sum(a, b, n) =
--     series(a)(n) * inf_sum(b) +
--      sigma[below[n + 1]]
--          (0, n,
--           LAMBDA (i: bel
theorem Cauchy_product_aux : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n : {i : ℝ // i ≥ 0}), conv_series_p a ∧ conv_series_p b → conv_sum a b n =
 series a(n) * inf_sum b +
  sigma[below[n + 1]]
      (0, n,
       fun (i : Fin n + 1)=> a (n - i) * (series b(i) - inf_sum b)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Cauchy_product.Cauchy_prod_ineq_aux
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal): 0 <= x / (1 + x) AND x / (1 + x) < 1
theorem Cauchy_prod_ineq_aux : ∀ (x : {x : ℝ // x ≥ 0}), 0 ≤ x / (1 + x) ∧ x / (1 + x) < 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Cauchy_product.Cauchy_product_ineq1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], epsilon: posreal):
--   conv_series?(a) AND conv_series?(b) =>
--    EXISTS (N: nat):
--      FORALL (n: nat | n >= N):
--        abs(series(a)(n) - inf_sum(a)) * abs(inf_sum(b)) < e
theorem Cauchy_product_ineq1 : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}), conv_series_p a ∧ conv_series_p b → ∃ (N : ℕ), ∀ (n : nat | n ≥ N), |series a(n) - inf_sum a| * |inf_sum b| < epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Cauchy_product.Cauchy_product_ineq2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], epsilon: posreal):
--   conv_series?(abs(a)) AND conv_series?(b) =>
--    EXISTS (N: nat):
--      FORALL (n: nat | n >= N):
--        sigma[below[n + 1]]
--            (N, n,
--          
theorem Cauchy_product_ineq2 : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}), conv_series_p |a| ∧ conv_series_p b → ∃ (N : ℕ), ∀ (n : nat | n ≥ N), sigma[below[n + 1]]
        (N, n,
         fun (i : Fin n + 1)=> |a (n - i)| * |series b(i) - inf_sum b|)
     < epsilon := by
  -- === TCCs for Cauchy_product_ineq2 ===
  -- TCC: Cauchy_product_ineq2_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   conv_series?(abs(a)) AND conv_series?(b) IMPLIES
  --    FORALL (N: nat, n: nat | n >= N):
  --      N >= 0 AND N < 1 + n OR EXISTS (j: below[1 + n]): j <= N
  have cauchy_product_ineq2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Cauchy_product.conv_ineq_aux_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: sequence[real] | conv_series?(b)):
--   nonempty?[real]
--       ({x: real | EXISTS (i: nat): x = abs(series(b)(i) - inf_sum(b))})
--    AND
--    above_bounded[real]
--        ({x: real | EXISTS (i: nat)
theorem conv_ineq_aux_exists : ∀ (b : {b : {i : ℝ // i ≥ 0} → ℝ // conv_series_p b}), nonempty_p[real]
    ({x: real | ∃ (i : ℕ), x = |series b(i) - inf_sum b|}) ∧ above_bounded[real]
    ({x: real | ∃ (i : ℕ), x = |series b(i) - inf_sum b|}) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Cauchy_product.conv_ineq_aux
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sup({x: real | EXISTS (i: nat): x = abs(series(b)(i) - inf_sum(b))})
noncomputable def conv_ineq_aux : sorry := by
  sorry  -- pvs2lean: definition stub for sup({x: real | EXISTS (i: nat): x = abs(series(b)(i) - inf_sum(b))})

-- pvs2lean: Cauchy_product.Cauchy_product_ineq3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], epsilon: posreal, k: posnat):
--   conv_series?(a) AND conv_series?(b) =>
--    EXISTS (N: nat | N >= k):
--      FORALL (n: nat | n >= N):
--        sigma[below[n + 1]]
--            (
theorem Cauchy_product_ineq3 : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}) (k : {i : {i : ℝ // i ≥ 0} // i > 0}), conv_series_p a ∧ conv_series_p b → ∃ (N : nat | N ≥ k), ∀ (n : nat | n ≥ N), sigma[below[n + 1]]
        (0, k - 1,
         fun (i : Fin n + 1)=> |a (n - i)| * |series b(i) - inf_sum b|)
     < epsilon := by
  -- === TCCs for Cauchy_product_ineq3 ===
  -- TCC: Cauchy_product_ineq3_TCC1
  -- Statement: FORALL (a, b: sequence[real], k: posnat):
  --   conv_series?(a) AND conv_series?(b) IMPLIES
  --    FORALL (N: nat | N >= k, n: nat | n >= N):
  --      k - 1 >= 0 AND k - 1 < 1 + n OR EXISTS (j: below[1 + n]): ...
  have cauchy_product_ineq3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Cauchy_product.Cauchy_product_convergence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real]):
--   conv_series?(abs(a)) AND conv_series?(b) =>
--    convergence(series(LAMBDA (i: nat):
--                         sigma[below[i + 1]]
--                             (0, i,
--      
theorem Cauchy_product_convergence : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ), conv_series_p |a| ∧ conv_series_p b → convergence (series (fun (i : ℕ)=> sigma[below[i + 1]]
                         (0, i,
                          fun (j : Fin i + 1)=> a j * b (i - j)))) (inf_sum a * inf_sum b) := by
  -- === TCCs for Cauchy_product_convergence ===
  -- TCC: Cauchy_product_convergence_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   conv_series?(abs(a)) AND conv_series?(b) IMPLIES conv_series?(a)
  have cauchy_product_convergence_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Cauchy_product.Cauchy_product_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real]):
--   conv_series?(abs(a)) AND conv_series?(b) =>
--    conv_series?(LAMBDA (i: nat):
--                   sigma[below[i + 1]]
--                       (0, i, LAMBDA (j: below[i + 1]
theorem Cauchy_product_conv : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ), conv_series_p |a| ∧ conv_series_p b → conv_series_p (fun (i : ℕ)=> sigma[below[i + 1]]
                   (0, i, fun (j : Fin i + 1)=> a j * b (i - j))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Cauchy_product.Cauchy_product
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real]):
--   conv_series?(abs(a)) AND conv_series?(b) =>
--    inf_sum(a) * inf_sum(b) =
--     inf_sum(LAMBDA (i: nat):
--               sigma[below[i + 1]]
--                   (0, i, LAMBDA
theorem Cauchy_product : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ), conv_series_p |a| ∧ conv_series_p b → inf_sum a * inf_sum b =
 inf_sum (fun (i : ℕ)=> sigma[below[i + 1]]
               (0, i, fun (j : Fin i + 1)=> a j * b (i - j))) := by
  -- === TCCs for Cauchy_product ===
  -- TCC: Cauchy_product_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   conv_series?(abs(a)) AND conv_series?(b) IMPLIES
  --    conv_series?(LAMBDA (i: nat):
  --                   sigma[below[i + 1]]
  --                       (0, i, LAMBDA (j: belo...
  have cauchy_product_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.Cauchy_product