-- Auto-generated by pvs2lean/translator.py
-- PVS theory: series_extra
-- PVSLib library: series
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: series_extra

Auto-generated stubs from PVSLib theory `series_extra`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.series_extra

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Tri : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (bijective_p : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} (conv_series_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (convergence : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (convergent_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (inf_sum : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (limit : pvs_τ11 → pvs_τ12)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (max : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (phi : pvs_τ17 → pvs_τ18)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (series : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} (strict_decreasing_p : pvs_τ25 → Prop)

-- pvs2lean: series_extra.Tri_strict_increasing
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat): FORALL (k: posnat): Tri(n) < Tri(n + k)
theorem Tri_strict_increasing : ∀ (n : {i : ℝ // i ≥ 0}), ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}), Tri n < Tri (n + k) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_extra.Tri_lt
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n, k: nat): Tri(n) < Tri(k) => n < k
theorem Tri_lt : ∀ (n : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), Tri n < Tri k → n < k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.Tri_le
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n, k: nat): Tri(n) <= Tri(k) => n <= k
theorem Tri_le : ∀ (n : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), Tri n ≤ Tri k → n ≤ k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.lower_inv_triang
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max({n: nat | Tri(n) <= k})
axiom lower_inv_triang {pvs_τ13 : Type*} {pvs_τ14 : Type*} : pvs_τ13 → pvs_τ14

-- pvs2lean: series_extra.triang_num_id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, k: nat | k <= n): lower_inv_triang(Tri(n) + k) = n
theorem triang_num_id : ∀ (n : {i : ℝ // i ≥ 0}) (k : {k : {i : ℝ // i ≥ 0} // k ≤ n}), lower_inv_triang (Tri n + k) = n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.triang_decomp
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat): EXISTS (n: nat, k: nat | k <= n): i = Tri(n) + k
theorem triang_decomp : ∀ (i : {i : ℝ // i ≥ 0}), ∃ (n : {i : ℝ // i ≥ 0}) (k : {k : {i : ℝ // i ≥ 0} // k ≤ n}), i = Tri n + k := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_extra.s_prod_idx1
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i - Tri(lower_inv_triang(i))
axiom s_prod_idx1 {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20

-- pvs2lean: series_extra.s_prod_idx2
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: lower_inv_triang(i) - i + Tri(lower_inv_triang(i))
axiom s_prod_idx2 {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22

-- pvs2lean: series_extra.s_prod_idx1_id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, k: nat | k <= n): s_prod_idx1(Tri(n) + k) = k
theorem s_prod_idx1_id : ∀ (n : {i : ℝ // i ≥ 0}) (k : {k : {i : ℝ // i ≥ 0} // k ≤ n}), s_prod_idx1 (Tri n + k) = k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.s_prod_idx2_id
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, k: nat | k <= n): s_prod_idx2(Tri(n) + k) = n - k
theorem s_prod_idx2_id : ∀ (n : {i : ℝ // i ≥ 0}) (k : {k : {i : ℝ // i ≥ 0} // k ≤ n}), s_prod_idx2 (Tri n + k) = n - k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.s_prod_identity
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n: nat):
--   sigma[nat]
--       (Tri(n), Tri(n) + n,
--        LAMBDA (i: nat): a(s_prod_idx1(i)) * b(s_prod_idx2(i)))
--    = sigma[below[n + 1]](0, n, LAMBDA (i: below[n + 1]): a
theorem s_prod_identity : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n : {i : ℝ // i ≥ 0}), sigma[nat]
    (Tri n, Tri n + n,
     fun (i : ℕ)=> a (s_prod_idx1 i) * b (s_prod_idx2 i)) = Finset.sum (Finset.Icc 0 n) (fun (i : Fin n + 1)=> a i * b (n - i)) := by
  -- === TCCs for s_prod_identity ===
  -- TCC: s_prod_identity_TCC1
  -- Statement: FORALL (n: nat): 0 >= 0 AND 0 < 1 + n OR EXISTS (j: below[1 + n]): j <= 0
  have s_prod_identity_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: s_prod_identity_TCC2
  -- Statement: FORALL (n: nat): n >= 0 AND n < 1 + n OR EXISTS (j: below[1 + n]): n <= j
  have s_prod_identity_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: s_prod_identity_TCC3
  -- Statement: FORALL (n: nat, i: below[n + 1]): n - i >= 0
  have s_prod_identity_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: s_prod_identity_TCC4
  -- Statement: FORALL (n: nat):
  --   (FORALL (x, y: below[1 + n]), (z: integer):
  --      x <= z AND z <= y IMPLIES z >= 0 AND z < 1 + n)
  have s_prod_identity_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.s_prod_identity2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real]):
--   FORALL (n: nat):
--     sigma[nat]
--         (0, Tri(n) + n,
--          LAMBDA (i: nat): a(s_prod_idx1(i)) * b(s_prod_idx2(i)))
--      =
--      sigma(0, n,
--            LAMBDA (i: 
theorem s_prod_identity2 : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ), ∀ (n : {i : ℝ // i ≥ 0}), sigma[nat]
    (0, Tri n + n, fun (i : ℕ)=> a (s_prod_idx1 i) * b (s_prod_idx2 i)) = Finset.sum (Finset.Icc 0 n) (fun (i : ℕ)=> sigma[below[i + 1]]
            (0, i, fun (j : Fin i + 1)=> a j * b (i - j))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.Tri_ineq_aux
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, k: nat | k <= n, m: nat): Tri(m) <= Tri(n) + k => m <= n
theorem Tri_ineq_aux : ∀ (n : {i : ℝ // i ≥ 0}) (k : {k : {i : ℝ // i ≥ 0} // k ≤ n}) (m : {i : ℝ // i ≥ 0}), Tri m ≤ Tri n + k → m ≤ n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_extra.s_prod_convergence
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[nnreal]):
--   conv_series?(a) AND conv_series?(b) =>
--    convergence(series(LAMBDA (i: nat):
--                         a(s_prod_idx1(i)) * b(s_prod_idx2(i))),
--                inf_sum(
theorem s_prod_convergence : ∀ (a : {i : ℝ // i ≥ 0} → {x : ℝ // x ≥ 0}) (b : {i : ℝ // i ≥ 0} → {x : ℝ // x ≥ 0}), conv_series_p a ∧ conv_series_p b → convergence (series (fun (i : ℕ)=> a (s_prod_idx1 i) * b (s_prod_idx2 i))) (inf_sum a * inf_sum b) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.s_prod_conv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[nnreal]):
--   conv_series?(a) AND conv_series?(b) =>
--    conv_series?(LAMBDA (i: nat): a(s_prod_idx1(i)) * b(s_prod_idx2(i)))
theorem s_prod_conv : ∀ (a : {i : ℝ // i ≥ 0} → {x : ℝ // x ≥ 0}) (b : {i : ℝ // i ≥ 0} → {x : ℝ // x ≥ 0}), conv_series_p a ∧ conv_series_p b → conv_series_p (fun (i : ℕ)=> a (s_prod_idx1 i) * b (s_prod_idx2 i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.lim_series_product
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[nnreal]):
--   convergent?(series(a)) AND convergent?(series(b)) =>
--    limit(series(a)) * limit(series(b)) =
--     limit(series(LAMBDA (i: nat): a(s_prod_idx1(i)) * b(s_prod_idx2(i))
theorem lim_series_product : ∀ (a : {i : ℝ // i ≥ 0} → {x : ℝ // x ≥ 0}) (b : {i : ℝ // i ≥ 0} → {x : ℝ // x ≥ 0}), convergent_p (series a) ∧ convergent_p (series b) → limit (series a) * limit (series b) =
 limit (series (fun (i : ℕ)=> a (s_prod_idx1 i) * b (s_prod_idx2 i))) := by
  -- === TCCs for lim_series_product ===
  -- TCC: lim_series_product_TCC1
  -- Statement: FORALL (a, b: sequence[nnreal]):
  --   convergent?(series(a)) AND convergent?(series(b)) IMPLIES
  --    convergent?(series(LAMBDA (i: nat):
  --                         a(s_prod_idx1(i)) * b(s_prod_idx2(i))))
  have lim_series_product_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.convergent_seq_sort
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n: nat):
--   strict_decreasing?(a) AND
--    (FORALL (i: nat | i <= n): EXISTS (j: nat): a(i) = b(j))
--    =>
--    (EXISTS (phi: [nat -> nat] | bijective?(phi)):
--       FORALL (i: 
theorem convergent_seq_sort : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n : {i : ℝ // i ≥ 0}), strict_decreasing_p a ∧
 (∀ (i : nat | i ≤ n), ∃ (j : ℕ), a i = b j) → (∃ (phi : [nat -> nat] | bijective_p phi), ∀ (i : nat | i ≤ n), a i = b (phi i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_extra.sigma_series_ineq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[posreal], n: nat):
--   (strict_decreasing?(a) AND
--     convergent?(series(b)) AND
--      FORALL (i: nat | i <= n): EXISTS (j: nat): a(i) = b(j))
--    => sigma(0, n, a) < limit(series(b
theorem sigma_series_ineq : ∀ (a : {i : ℝ // i ≥ 0} → {x : {x : ℝ // x ≥ 0} // x > 0}) (b : {i : ℝ // i ≥ 0} → {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : ℝ // i ≥ 0}), (strict_decreasing_p a ∧
  convergent_p (series b) ∧
   ∀ (i : nat | i ≤ n), ∃ (j : ℕ), a i = b j) → Finset.sum (Finset.Icc 0 n) (a) < limit (series b) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.series_extra