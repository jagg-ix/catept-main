-- Auto-generated by pvs2lean/translator.py
-- PVS theory: series_aux
-- PVSLib library: series
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: series_aux

Auto-generated stubs from PVSLib theory `series_aux`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.series_aux

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (convergence : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} (convergent_p : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (limit : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (series : pvs_τ6 → pvs_τ7 → pvs_τ8)

-- pvs2lean: series_aux.zero_tail_series_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL m: n < m => a(m) = 0) => convergent?(series(a))
theorem zero_tail_series_conv : (∀ m,  n < m → a m = 0) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_aux.zero_tail_series_limit
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL m: n < m => a(m) = 0) => limit(series(a)) = series(a)(n)
theorem zero_tail_series_limit : (∀ m,  n < m → a m = 0) → limit (series a) = series a(n) := by
  -- === TCCs for zero_tail_series_limit ===
  -- TCC: zero_tail_series_limit_TCC1
  -- Statement: FORALL (a: sequence[real], n: nat):
  --   (FORALL (m: nat): n < m => a(m) = 0) IMPLIES convergent?(series(a))
  have zero_tail_series_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: zero_tail_series_limit_TCC1
  -- Statement: FORALL (a: sequence[real], n: nat):
  --   (FORALL (m: nat): n < m => a(m) = 0) IMPLIES convergent?(series(a))
  have zero_tail_series_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_aux.zero_tail_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL m: n < m => a(m) = 0) => convergence(series(a), series(a)(n))
theorem zero_tail_series : (∀ m,  n < m → a m = 0) → convergence (series a) (series a(n)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_aux.single_nz_series_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL m: n /= m => a(m) = 0) => convergent?(series(a))
theorem single_nz_series_conv : (∀ m,  n ≠ m → a m = 0) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_aux.single_nz_series_limit
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL m: n /= m => a(m) = 0) => limit(series(a)) = a(n)
theorem single_nz_series_limit : (∀ m,  n ≠ m → a m = 0) → limit (series a) = a n := by
  -- === TCCs for single_nz_series_limit ===
  -- TCC: single_nz_series_limit_TCC1
  -- Statement: FORALL (a: sequence[real], n: nat):
  --   (FORALL (m: nat): n /= m => a(m) = 0) IMPLIES convergent?(series(a))
  have single_nz_series_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: single_nz_series_limit_TCC1
  -- Statement: FORALL (a: sequence[real], n: nat):
  --   (FORALL (m: nat): n /= m => a(m) = 0) IMPLIES convergent?(series(a))
  have single_nz_series_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_aux.single_nz_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL m: n /= m => a(m) = 0) => convergence(series(a), a(n))
theorem single_nz_series : (∀ m,  n ≠ m → a m = 0) → convergence (series a) (a n) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_aux.limit_nonneg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(nna) => limit(nna) >= 0
theorem limit_nonneg : convergent_p nna → limit nna ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_aux.convergence_nonneg
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(series(nna), nnx) => FORALL n: series(nna)(n) <= nnx
theorem convergence_nonneg : convergence (series nna) nnx → ∀ n,  series nna(n) ≤ nnx := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_aux.nonneg_series_bij
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(series(nna), nnx) => convergence(series(nna o phi), nnx)
theorem nonneg_series_bij : convergence (series nna) nnx → convergence (series (nna ∘ phi)) nnx := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: series_aux.nonneg_series_bij_conv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(nna)) => convergent?(series(nna o phi))
theorem nonneg_series_bij_conv : convergent_p (series nna) → convergent_p (series (nna ∘ phi)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_aux.nonneg_series_bij_limit
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(nna)) => limit(series(nna)) = limit(series(nna o phi))
theorem nonneg_series_bij_limit : convergent_p (series nna) → limit (series nna) = limit (series (nna ∘ phi)) := by
  -- === TCCs for nonneg_series_bij_limit ===
  -- TCC: nonneg_series_bij_limit_TCC1
  -- Statement: FORALL (nna: sequence[nnreal], phi: (bijective?[nat, nat])):
  --   convergent?(series(nna)) IMPLIES
  --    convergent?(series(o[nat, nat, nnreal](nna, phi)))
  have nonneg_series_bij_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nonneg_series_bij_limit_TCC1
  -- Statement: FORALL (nna: sequence[nnreal], phi: (bijective?[nat, nat])):
  --   convergent?(series(nna)) IMPLIES
  --    convergent?(series(o[nat, nat, nnreal](nna, phi)))
  have nonneg_series_bij_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_aux.abs_series_bij
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(abs(a))) AND convergence(series(a), x) =>
--  convergence(series(a o phi), x)
theorem abs_series_bij : convergent_p (series |a|) ∧ convergence (series a) x → convergence (series (a ∘ phi)) x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_aux.abs_series_bij_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(abs(a))) => convergent?(series(a o phi))
theorem abs_series_bij_conv : convergent_p (series |a|) → convergent_p (series (a ∘ phi)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: series_aux.abs_series_bij_limit
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergent?(series(abs(a))) => limit(series(a)) = limit(series(a o phi))
theorem abs_series_bij_limit : convergent_p (series |a|) → limit (series a) = limit (series (a ∘ phi)) := by
  -- === TCCs for abs_series_bij_limit ===
  -- TCC: abs_series_bij_limit_TCC1
  -- Statement: FORALL (a: sequence[real]):
  --   convergent?(series(abs(a))) IMPLIES convergent?(series(a))
  have abs_series_bij_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: abs_series_bij_limit_TCC2
  -- Statement: FORALL (a: sequence[real], phi: (bijective?[nat, nat])):
  --   convergent?(series(abs(a))) IMPLIES
  --    convergent?(series(o[nat, nat, real](a, phi)))
  have abs_series_bij_limit_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: abs_series_bij_limit_TCC1
  -- Statement: FORALL (a: sequence[real]):
  --   convergent?(series(abs(a))) IMPLIES convergent?(series(a))
  have abs_series_bij_limit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: abs_series_bij_limit_TCC2
  -- Statement: FORALL (a: sequence[real], phi: (bijective?[nat, nat])):
  --   convergent?(series(abs(a))) IMPLIES
  --    convergent?(series(o[nat, nat, real](a, phi)))
  have abs_series_bij_limit_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.series_aux