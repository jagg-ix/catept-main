-- Auto-generated by pvs2lean/translator.py
-- PVS theory: integral_chg_var
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: integral_chg_var

Auto-generated stubs from PVSLib theory `integral_chg_var`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.integral_chg_var

noncomputable section

-- pvs2lean: integral_chg_var.T_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_chg_var.U_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def U_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_chg_var.connected_domain
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[T]
theorem connected_domain : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_chg_var.not_one_element
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[T]
theorem not_one_element : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_chg_var.con_dom_U
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[U]
theorem con_dom_U : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_chg_var.not_one_U
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[U]
theorem not_one_U : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_chg_var.deriv_domain_T
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv_domain?[T]
theorem deriv_domain_T : deriv_domain_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_chg_var.deriv_domain_U
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv_domain?[U]
theorem deriv_domain_U : deriv_domain_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_chg_var.int_chg_var_prep
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: continuous?(f) AND derivable?(phi) AND continuous?(deriv(phi)) IMPLIES
--  continuous?((f o phi) * deriv(phi)) AND
--   Integrable?[U](a, b, LAMBDA (u: U): f(phi(u)) * deriv[U](phi)(u))
theorem int_chg_var_prep : continuous_p f ∧ derivable_p phi ∧ continuous_p (deriv phi) → continuous_p ((f ∘ phi) * deriv phi) ∧
 Integrable_p (a, b, fun (u : U)=> f (phi u) * deriv[U](phi) (u)) := by
  -- === TCCs for int_chg_var_prep ===
  -- TCC: int_chg_var_prep_TCC1
  -- Statement: FORALL (f: [T -> real]): continuous?(f) IMPLIES deriv_domain?[U]
  have int_chg_var_prep_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: int_chg_var_prep_TCC2
  -- Statement: FORALL (f: [T -> real]): continuous?(f) IMPLIES not_one_element?[U]
  have int_chg_var_prep_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: int_chg_var_prep_TCC3
  -- Statement: FORALL (f: [T -> real], phi: [U -> T]):
  --   continuous?((f o phi) * deriv(phi)) AND
  --    continuous?(deriv(phi)) AND derivable?(phi) AND continuous?(f)
  --    IMPLIES connected?[U]
  have int_chg_var_prep_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_chg_var.Int_chg_var
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?(phi) AND continuous?(deriv(phi)) AND continuous?(f) IMPLIES
--  Integral(phi(a), phi(b), f) =
--   Integral(a, b, (LAMBDA (u: U): f(phi(u)) * deriv(phi)(u)))
theorem Int_chg_var : derivable_p phi ∧ continuous_p (deriv phi) ∧ continuous_p f → Integral (phi a) (phi b) f =
 Integral a b ((fun (u : U)=> f (phi u) * deriv phi(u))) := by
  -- === TCCs for Int_chg_var ===
  -- TCC: Int_chg_var_TCC1
  -- Statement: deriv_domain?[U]
  have int_chg_var_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Int_chg_var_TCC2
  -- Statement: not_one_element?[U]
  have int_chg_var_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Int_chg_var_TCC3
  -- Statement: FORALL (a, b: U, f: [T -> real], phi: [U -> T]):
  --   continuous?(f) AND continuous?(deriv(phi)) AND derivable?(phi) IMPLIES
  --    Integrable?[T](phi(a), phi(b), f)
  have int_chg_var_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Int_chg_var_TCC4
  -- Statement: FORALL (f: [T -> real], phi: [U -> T]):
  --   continuous?(f) AND continuous?(deriv(phi)) AND derivable?(phi) IMPLIES
  --    connected?[T]
  have int_chg_var_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Int_chg_var_TCC5
  -- Statement: FORALL (f: [T -> real], phi: [U -> T]):
  --   continuous?(f) AND continuous?(deriv(phi)) AND derivable?(phi) IMPLIES
  --    not_one_element?[T]
  have int_chg_var_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Int_chg_var_TCC6
  -- Statement: FORALL (a, b: U, f: [T -> real], phi: [U -> T]):
  --   continuous?(f) AND continuous?(deriv(phi)) AND derivable?(phi) IMPLIES
  --    Integrable?[U](a, b, LAMBDA (u: U): f(phi(u)) * deriv[U](phi)(u))
  have int_chg_var_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Int_chg_var_TCC7
  -- Statement: FORALL (f: [T -> real], phi: [U -> T]):
  --   continuous?(f) AND continuous?(deriv(phi)) AND derivable?(phi) IMPLIES
  --    connected?[U]
  have int_chg_var_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_chg_var.int_chg_var
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt:      derivable?(phi) AND continuous?(deriv(phi)) AND continuous?(f)
--  AND (a < b) AND (phi(a) < phi(b))
--  IMPLIES
--  integral(phi(a), phi(b), f) =
--   integral(a, b, (LAMBDA (u: U): f(phi(u)) * deriv(phi)(u
theorem int_chg_var : derivable_p phi ∧ continuous_p (deriv phi) ∧ continuous_p f
 ∧ (a < b) ∧ (phi a < phi b) → integral (phi a) (phi b) f =
 integral a b ((fun (u : U)=> f (phi u) * deriv phi(u))) := by
  -- === TCCs for int_chg_var ===
  -- TCC: int_chg_var_TCC1
  -- Statement: FORALL (a, b: U, f: [T -> real], phi: [U -> T]):
  --        (phi(a) < phi(b)) AND (a < b) AND continuous?(f)
  --    AND continuous?(deriv(phi)) AND derivable?(phi)
  --    IMPLIES integrable?[T](phi(a), phi(b), f)
  have int_chg_var_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: int_chg_var_TCC2
  -- Statement: FORALL (a, b: U, f: [T -> real], phi: [U -> T]):
  --        (phi(a) < phi(b)) AND (a < b) AND continuous?(f)
  --    AND continuous?(deriv(phi)) AND derivable?(phi)
  --    IMPLIES integrable?[U](a, b, LAMBDA (u...
  have int_chg_var_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.integral_chg_var