-- Auto-generated by pvs2lean/translator.py
-- PVS theory: foursquare
-- PVSLib library: numbers
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: foursquare

Auto-generated stubs from PVSLib theory `foursquare`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.foursquare

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (divides : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (mod : pvs_τ5 → pvs_τ6 → Prop)

-- pvs2lean: foursquare.mod_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: mod(e, pn) = mod(f, pn) IFF divides(pn, e - f)
theorem mod_eq : (e ↔ divides pn (e - f) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: foursquare.Euler_four_square
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i1 ^ 2 + i2 ^ 2 + i3 ^ 2 + i4 ^ 2) * (j1 ^ 2 + j2 ^ 2 + j3 ^ 2 + j4 ^ 2) =
--  (i1 * j1 + i2 * j2 + i3 * j3 + i4 * j4) ^ 2 +
--   (i1 * j2 - i2 * j1 + i3 * j4 - i4 * j3) ^ 2
--   + (i1 * j3 - i2 * j4 - i3 * j
theorem Euler_four_square : (i1 ^ 2 + i2 ^ 2 + i3 ^ 2 + i4 ^ 2) * (j1 ^ 2 + j2 ^ 2 + j3 ^ 2 + j4 ^ 2) = (i1 * j1 + i2 * j2 + i3 * j3 + i4 * j4) ^ 2 +
 (i1 * j2 - i2 * j1 + i3 * j4 - i4 * j3) ^ 2
 + (i1 * j3 - i2 * j4 - i3 * j1 + i4 * j2) ^ 2
 + (i1 * j4 + i2 * j3 - i3 * j2 - i4 * j1) ^ 2 := by
  -- === TCCs for Euler_four_square ===
  -- TCC: Euler_four_square_TCC1
  -- Statement: FORALL (i1: int): i1 /= 0 OR 2 >= 0
  have euler_four_square_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Euler_four_square_TCC2
  -- Statement: FORALL (i1, i2, i3, i4, j1, j2, j3, j4: int):
  --   (i1 * j1 + i2 * j2 + i3 * j3 + i4 * j4) /= 0 OR 2 >= 0
  have euler_four_square_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Euler_four_square_TCC3
  -- Statement: FORALL (i1, i2, i3, i4, j1, j2, j3, j4: int):
  --   NOT (i1 * j2 + i3 * j4 - i2 * j1 - i4 * j3) = 0 OR 2 >= 0
  have euler_four_square_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Euler_four_square_TCC4
  -- Statement: FORALL (i1, i2, i3, i4, j1, j2, j3, j4: int):
  --   NOT i1 * j3 + i4 * j2 - i2 * j4 - i3 * j1 = 0 OR 2 >= 0
  have euler_four_square_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Euler_four_square_TCC5
  -- Statement: FORALL (i1, i2, i3, i4, j1, j2, j3, j4: int):
  --   (i1 * j4 + i2 * j3 - i3 * j2 - i4 * j1) /= 0 OR 2 >= 0
  have euler_four_square_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: foursquare.sum_four_squares?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (a, b, c, d): n = a ^ 2 + b ^ 2 + c ^ 2 + d ^ 2
axiom sum_four_squares_p {pvs_τ8 : Type*} : pvs_τ8 → Prop

-- pvs2lean: foursquare.sum_four_squares_int?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (i1, i2, i3, i4): e = i1 ^ 2 + i2 ^ 2 + i3 ^ 2 + i4 ^ 2
axiom sum_four_squares_int_p {pvs_τ7 : Type*} : pvs_τ7 → Prop

-- pvs2lean: foursquare.sum_four_squares_int_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sum_four_squares?(n) IFF sum_four_squares_int?(n)
theorem sum_four_squares_int_def : sum_four_squares_p n ↔ sum_four_squares_int_p n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: foursquare.sum_four_squares_reduces
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL p: p > 2 IMPLIES sum_four_squares?(p)) IMPLIES
--  (FORALL n: sum_four_squares?(n))
theorem sum_four_squares_reduces : (∀ p,  p > 2 → sum_four_squares_p p) → (∀ n,  sum_four_squares_p n) := by
  -- === TCCs for sum_four_squares_reduces ===
  -- TCC: sum_four_squares_reduces_TCC1
  -- Statement: FORALL (p): p > 2 IMPLIES p >= 0
  have sum_four_squares_reduces_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: foursquare.square_residues_distinct
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a <= (p - 1) / 2 AND b <= (p - 1) / 2 AND a /= b IMPLIES
--  mod(a ^ 2, p) /= mod(b ^ 2, p)
theorem square_residues_distinct : a ≤ (p - 1) / 2 ∧ b ≤ (p - 1) / 2 ∧ a ≠ b → (a ^ 2 := by
  -- === TCCs for square_residues_distinct ===
  -- TCC: square_residues_distinct_TCC1
  -- Statement: FORALL (a, b: nat, p: (prime?)):
  --   a /= b AND b <= (p - 1) / 2 AND a <= (p - 1) / 2 IMPLIES p /= 0
  have square_residues_distinct_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: square_residues_distinct_TCC2
  -- Statement: FORALL (a, b: nat, p: (prime?)):
  --   a /= b AND b <= (p - 1) / 2 AND a <= (p - 1) / 2 IMPLIES b /= 0 OR 2 >= 0
  have square_residues_distinct_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: foursquare.sum_four_squares_pigeonhole
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: p > 2 IMPLIES
--  EXISTS (a, b):
--    a <= (p - 1) / 2 AND
--     b <= (p - 1) / 2 AND mod(a ^ 2, p) = mod(-b ^ 2 - 1, p)
theorem sum_four_squares_pigeonhole : p > 2 → ∃ (a : _) (b : _), a ≤ (p - 1) / 2 ∧
   b ≤ (p - 1) / 2 ∧ (a ^ 2 := by
  -- === TCCs for sum_four_squares_pigeonhole ===
  -- TCC: sum_four_squares_pigeonhole_TCC1
  -- Statement: FORALL (p: (prime?)):
  --   p > 2 IMPLIES
  --    FORALL (a: nat, b: nat):
  --      a <= (p - 1) / 2 AND b <= (p - 1) / 2 IMPLIES p /= 0
  have sum_four_squares_pigeonhole_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sum_four_squares_pigeonhole_TCC2
  -- Statement: FORALL (p: (prime?)):
  --   p > 2 IMPLIES
  --    FORALL (a: nat, b: nat):
  --      a <= (p - 1) / 2 AND b <= (p - 1) / 2 IMPLIES b /= 0 OR 2 >= 0
  have sum_four_squares_pigeonhole_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: foursquare.min_mult_sum_four_squares?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: pm < p AND
--  sum_four_squares?(pm * p) AND
--   FORALL (pn): pn < pm IMPLIES NOT sum_four_squares?(pn * p)
axiom min_mult_sum_four_squares_p {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: foursquare.mult_is_sum_four_squares
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (pm): min_mult_sum_four_squares?(p, pm)
theorem mult_is_sum_four_squares : ∃ (pm : {i : {i : ℝ // i ≥ 0} // i > 0}), min_mult_sum_four_squares_p p pm := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: foursquare.nat_is_sum_four_squares
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sum_four_squares?(n)
theorem nat_is_sum_four_squares : sum_four_squares_p n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: foursquare.sum_four_squares
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (a, b, c, d): n = a ^ 2 + b ^ 2 + c ^ 2 + d ^ 2
theorem sum_four_squares : ∃ (a : {i : ℝ // i ≥ 0}) (b : {i : ℝ // i ≥ 0}) (c : {i : ℝ // i ≥ 0}) (d : {i : ℝ // i ≥ 0}), n = a ^ 2 + b ^ 2 + c ^ 2 + d ^ 2 := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.foursquare