-- Auto-generated by pvs2lean/translator.py
-- PVS theory: primes_sum_squares
-- PVSLib library: numbers
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: primes_sum_squares

Auto-generated stubs from PVSLib theory `primes_sum_squares`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.primes_sum_squares

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (divides : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ9 : Type*} (integer_pred : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (invol : pvs_τ10 → pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (is_finite : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (mod : pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} (odd_p : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (prime_p : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (rel_prime : pvs_τ23 → pvs_τ24 → Prop)

-- pvs2lean: primes_sum_squares.sum_of_two_squares?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (a, b): n = a ^ 2 + b ^ 2)
axiom sum_of_two_squares_p {pvs_τ26 : Type*} : pvs_τ26 → Prop

-- pvs2lean: primes_sum_squares.sots_int?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (ai, bi): n = ai ^ 2 + bi ^ 2)
axiom sots_int_p {pvs_τ25 : Type*} : pvs_τ25 → Prop

-- pvs2lean: primes_sum_squares.sots_int_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sots_int?(n) IMPLIES sum_of_two_squares?(n)
theorem sots_int_def : sots_int_p n → sum_of_two_squares_p n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.prime_sum_of_two_squares?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: prime?(n) AND sum_of_two_squares?(n)
noncomputable def prime_sum_of_two_squares_p : sorry := by
  sorry  -- pvs2lean: definition stub for prime?(n) AND sum_of_two_squares?(n)

-- pvs2lean: primes_sum_squares.Brahmagupta_Fibonacci
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sum_of_two_squares?(ns * ms)
theorem Brahmagupta_Fibonacci : sum_of_two_squares_p (ns * ms) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.prime_divides
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: divides(p, ai * bi) IMPLIES (divides(p, ai) OR divides(p, bi))
theorem prime_divides : divides p (ai * bi) → (divides p ai ∨ divides p bi) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: primes_sum_squares.sots_div_prime_closed
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: divides(ps, ns) IMPLIES sum_of_two_squares?(ns / ps)
theorem sots_div_prime_closed : divides ps ns → sum_of_two_squares_p (ns / ps) := by
  -- === TCCs for sots_div_prime_closed ===
  -- TCC: sots_div_prime_closed_TCC1
  -- Statement: FORALL (ns: (sum_of_two_squares?), ps: (prime_sum_of_two_squares?)):
  --   divides(ps, ns) IMPLIES ps /= 0
  have sots_div_prime_closed_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sots_div_prime_closed_TCC2
  -- Statement: FORALL (ns: (sum_of_two_squares?), ps: (prime_sum_of_two_squares?)):
  --   divides(ps, ns) IMPLIES integer_pred(ns / ps) AND ns / ps >= 0
  have sots_div_prime_closed_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.sots_div_quot_factor
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sum_of_two_squares?(ns) AND divides(m, ns) AND (NOT sum_of_two_squares?(m))
--  IMPLIES (EXISTS (b): divides(b, ns / m) AND (NOT sum_of_two_squares?(b)))
theorem sots_div_quot_factor : sum_of_two_squares_p ns ∧ divides m ns ∧ (¬ sum_of_two_squares_p m) → (∃ (b : _), divides b (ns / m) ∧ (¬ sum_of_two_squares_p b)) := by
  -- === TCCs for sots_div_quot_factor ===
  -- TCC: sots_div_quot_factor_TCC1
  -- Statement: FORALL (m: nat, ns: (sum_of_two_squares?)):
  --   (NOT sum_of_two_squares?(m)) AND
  --    divides(m, ns) AND sum_of_two_squares?(ns)
  --    IMPLIES m /= 0
  have sots_div_quot_factor_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sots_div_quot_factor_TCC2
  -- Statement: FORALL (m: nat, ns: (sum_of_two_squares?)):
  --   (NOT sum_of_two_squares?(m)) AND
  --    divides(m, ns) AND sum_of_two_squares?(ns)
  --    IMPLIES integer_pred(ns / m)
  have sots_div_quot_factor_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.rel_prime_sos_factor
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: rel_prime(pa, pb) AND divides(n, pa ^ 2 + pb ^ 2) IMPLIES
--  sum_of_two_squares?(n)
theorem rel_prime_sos_factor : rel_prime pa pb ∧ divides n (pa ^ 2 + pb ^ 2) → sum_of_two_squares_p n := by
  -- === TCCs for rel_prime_sos_factor ===
  -- TCC: rel_prime_sos_factor_TCC1
  -- Statement: FORALL (pa, pb: posnat): pa = 0 => pb /= 0
  have rel_prime_sos_factor_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: rel_prime_sos_factor_TCC2
  -- Statement: FORALL (pa, pb: posnat): rel_prime(pa, pb) IMPLIES pa /= 0 OR 2 >= 0
  have rel_prime_sos_factor_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: rel_prime_sos_factor_TCC3
  -- Statement: FORALL (pa, pb: posnat): rel_prime(pa, pb) IMPLIES pb /= 0 OR 2 >= 0
  have rel_prime_sos_factor_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.fermat_prime_sos_set
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ 2 + 4 * y * z = p
axiom fermat_prime_sos_set {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: primes_sum_squares.fermat_prime_sos_finite_set
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: finite_sets[[nat, nat, nat]].is_finite(fermat_prime_sos_set(p))
theorem fermat_prime_sos_finite_set : is_finite (fermat_prime_sos_set p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.involution_odd_has_fixedpoint
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (N: posnat, invol: [below(N) -> below(N)]):
--   odd?(N) AND (FORALL (i: below(N)): invol(invol(i)) = i) IMPLIES
--    EXISTS (i: below(N)): invol(i) = i
theorem involution_odd_has_fixedpoint : ∀ (N : {i : {i : ℝ // i ≥ 0} // i > 0}) (invol : {s : {i : ℝ // i ≥ 0} // s < N} → {s : {i : ℝ // i ≥ 0} // s < N}), odd_p N ∧ (∀ (i : Finset.range N), invol (invol i) = i) → ∃ (i : Finset.range N), invol i = i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.involution_one_fixedpoint_odd
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (N: posnat, invol: [below(N) -> below(N)]):
--   (FORALL (i: below(N)): invol(invol(i)) = i) AND
--    (EXISTS (j: below(N)): FORALL (i: below(N)): invol(i) = i IFF i = j)
--    IMPLIES odd?(N)
theorem involution_one_fixedpoint_odd : ∀ (N : {i : {i : ℝ // i ≥ 0} // i > 0}) (invol : {s : {i : ℝ // i ≥ 0} // s < N} → {s : {i : ℝ // i ≥ 0} // s < N}), (∀ (i : Finset.range N), invol (invol i) = i) ∧
 (∃ (j : Finset.range N), ∀ (i : Finset.range N), invol i = i ↔ i = j) → odd_p N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.involution_fermat_set_one_fp_invol
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET invol: [[nat, nat, nat] -> [nat, nat, nat]] =
--       (LAMBDA (x, y, z: nat):
--          IF x < y - z THEN (x + 2 * z, z, y - x - z)
--          ELSIF y - z <= x AND x <= 2 * y THEN (2 * y - x, y, x - y 
theorem involution_fermat_set_one_fp_invol : fun (invol : {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0}) => ∀ (x : {i : ℝ // i ≥ 0}) (y : {i : ℝ // i ≥ 0}) (z : {i : ℝ // i ≥ 0}), fermat_prime_sos_set p(x, y, z) → invol (invol x y z) = (x, y, z) ∧
 fermat_prime_sos_set p(invol x y z) (fun (x : {i : ℝ // i ≥ 0}) (y : {i : ℝ // i ≥ 0}) (z : {i : ℝ // i ≥ 0}) => IF (x < y - z) ((x + 2 * z, z, y - x - z)) (if y - z ≤ x ∧ x ≤ 2 * y then (2 * y - x, y, x - y + z)
 else (x - 2 * y, x - y + z, y))) := by
  -- === TCCs for involution_fermat_set_one_fp_invol ===
  -- TCC: involution_fermat_set_one_fp_invol_TCC1
  -- Statement: FORALL (x, y, z: nat): x < y - z IMPLIES y - x - z >= 0
  have involution_fermat_set_one_fp_invol_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: involution_fermat_set_one_fp_invol_TCC2
  -- Statement: FORALL (x, y, z: nat):
  --   NOT x < y - z AND y - z <= x AND x <= 2 * y IMPLIES 2 * y - x >= 0
  have involution_fermat_set_one_fp_invol_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: involution_fermat_set_one_fp_invol_TCC3
  -- Statement: FORALL (x, y, z: nat):
  --   NOT x < y - z AND y - z <= x AND x <= 2 * y IMPLIES x - y + z >= 0
  have involution_fermat_set_one_fp_invol_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: involution_fermat_set_one_fp_invol_TCC4
  -- Statement: FORALL (x, y, z: nat):
  --   NOT x < y - z AND NOT (y - z <= x AND x <= 2 * y) IMPLIES x - 2 * y >= 0
  have involution_fermat_set_one_fp_invol_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: involution_fermat_set_one_fp_invol_TCC5
  -- Statement: FORALL (x, y, z: nat):
  --   NOT x < y - z AND NOT (y - z <= x AND x <= 2 * y) IMPLIES x - y + z >= 0
  have involution_fermat_set_one_fp_invol_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.invol_fermat
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (x, y, z) = xyz IN
--   IF x < y - z THEN (x + 2 * z, z, y - x - z)
--   ELSIF y - z <= x AND x <= 2 * y THEN (2 * y - x, y, x - y + z)
--   ELSE (x - 2 * y, x - y + z, y)
--   ENDIF
axiom invol_fermat {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15

-- pvs2lean: primes_sum_squares.invol_fermat_one_fp
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: p = 4 * k + 1 IMPLIES
--  FORALL (xyz: (fermat_prime_sos_set(p))):
--    invol_fermat(p)(xyz) = xyz IFF xyz = (1, 1, k)
theorem invol_fermat_one_fp : p = 4 * k + 1 → ∀ (xyz : (fermat_prime_sos_set p)), invol_fermat p(xyz) = xyz ↔ xyz = (1, 1, k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: primes_sum_squares.fermat_prime_mod_4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: mod(p, 4) = 1 IMPLIES sum_of_two_squares?(p)
theorem fermat_prime_mod_4 : (p → sum_of_two_squares_p p := by
  -- === TCCs for fermat_prime_mod_4 ===
  -- TCC: fermat_prime_mod_4_TCC1
  -- Statement: FORALL (p: (prime?)): mod(p, 4) = 1 IMPLIES p >= 0
  have fermat_prime_mod_4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.primes_sum_squares