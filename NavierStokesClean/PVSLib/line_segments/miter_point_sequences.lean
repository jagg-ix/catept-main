-- Auto-generated by pvs2lean/translator.py
-- PVS theory: miter_point_sequences
-- PVSLib library: line_segments
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: miter_point_sequences

Auto-generated stubs from PVSLib theory `miter_point_sequences`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.miter_point_sequences

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (between_rays_p : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (cross : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (dir_parallel_p : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (empty_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (four_point_miniseq_p : pvs_τ13 → pvs_τ14 → pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (four_point_offset_dist_p : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (half_angle_vcos : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (half_angle_vsin : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} (loop_back_intercept : pvs_τ25 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (min_segment_sep : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (miter_point : pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} (miter_point_hyp : pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (miter_point_prev : pvs_τ39 → pvs_τ40 → pvs_τ41 → pvs_τ42 → pvs_τ43)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (miter_seq_offset_dist : pvs_τ47 → pvs_τ48)
variable {pvs_τ52 : Type*} (norm : pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} (perp_left : pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (point_on_segment_p : pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (point_on_segment_seq_p : pvs_τ56 → pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} (rightward_segment_offset : pvs_τ58 → pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} (seq : pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (singleton_seq : pvs_τ62 → pvs_τ63)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} (three_point_miniseq_p : pvs_τ64 → pvs_τ65 → pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} (three_point_offset_dist_p : pvs_τ67 → pvs_τ68 → pvs_τ69 → pvs_τ70 → Prop)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} (vcos : pvs_τ71 → pvs_τ72 → Prop)

-- pvs2lean: miter_point_sequences.miter_sine_set
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: {x: posreal |
--    EXISTS (i: below(S`length - 1)):
--      ((i = 0 OR i = loop_back_intercept(S)) AND
--        S`seq(i) = S`seq(S`length - 1) AND
--         LET u = S`seq(S`length - 2),
--             v = S`seq(i
axiom miter_sine_set {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ49 → pvs_τ50

-- pvs2lean: miter_point_sequences.miter_sine_set_nonempty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq): NOT empty?(miter_sine_set(S))
theorem miter_sine_set_nonempty : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}), ¬(empty_p (miter_sine_set S)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.min_miter_sine
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min[posreal, restrict[[real, real], [posreal, posreal], bool](<=)]
--     (miter_sine_set(S))
axiom min_miter_sine {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ26 → pvs_τ27

-- pvs2lean: miter_point_sequences.min_miter_sine_le_wrapped_sine
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET u = S`seq(S`length - 2),
--          v = S`seq(lbi),
--          w = S`seq
theorem min_miter_sine_le_wrapped_sine : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let u := S.seq (S.List.length - 2); let v := S.seq lbi; let w := S.seq (1 + lbi); let uv := u - v; let wv := w - v; min_miter_sine S ≤ half_angle_vsin ^(uv) ^(wv)) (loop_back_intercept S) := by
  -- === TCCs for min_miter_sine_le_wrapped_sine ===
  -- TCC: min_miter_sine_le_wrapped_sine_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have min_miter_sine_le_wrapped_sine_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_wrapped_sine_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have min_miter_sine_le_wrapped_sine_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_wrapped_sine_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have min_miter_sine_le_wrapped_sine_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_wrapped_sine_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have min_miter_sine_le_wrapped_sine_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_wrapped_sine_TCC5
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have min_miter_sine_le_wrapped_sine_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.min_miter_sine_le_wrapped_sine_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET u = S`seq(S`length - 3),
--          v = S`seq(S`length - 2),
--          
theorem min_miter_sine_le_wrapped_sine_2 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let u := S.seq (S.List.length - 3); let v := S.seq (S.List.length - 2); let w := S.seq lbi; let uv := u - v; let wv := w - v; min_miter_sine S ≤ half_angle_vsin ^(uv) ^(wv)) (loop_back_intercept S) := by
  -- === TCCs for min_miter_sine_le_wrapped_sine_2 ===
  -- TCC: min_miter_sine_le_wrapped_sine_2_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have min_miter_sine_le_wrapped_sine_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_wrapped_sine_2_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have min_miter_sine_le_wrapped_sine_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_wrapped_sine_2_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have min_miter_sine_le_wrapped_sine_2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.min_miter_sine_le_each_sine
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
--   LET u = S`seq(i - 1),
--       v = S`seq(i),
--       w = S`seq(i + 1),
--       uv = u - v,
--       wv = w - v
--     IN min_miter_sine(
theorem min_miter_sine_le_each_sine : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : ℝ × ℝ) => fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => min_miter_sine S ≤ half_angle_vsin ^(uv) ^(wv) (w - v) (u - v) (S.seq (i + 1)) (S.seq i) (S.seq (i - 1)) := by
  -- === TCCs for min_miter_sine_le_each_sine ===
  -- TCC: min_miter_sine_le_each_sine_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
  --   i - 1 >= 0 AND i - 1 < S`length
  have min_miter_sine_le_each_sine_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_each_sine_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         u: point_2d):
  --   u = S`seq(i - 1) IMPLIES i < S`length
  have min_miter_sine_le_each_sine_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_each_sine_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         u: point_2d, v: point_2d):
  --   u = S`seq(i - 1) AND v = S`seq(i) IMPLIES i + 1 < S`length
  have min_miter_sine_le_each_sine_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_each_sine_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         u: point_2d, v: point_2d, w: point_2d, uv: Vector, wv: Vector):
  --        (u = S`seq(i - 1)) AND (v = S`seq(i)) AND (w...
  have min_miter_sine_le_each_sine_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_each_sine_TCC5
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         u: point_2d, v: point_2d, w: point_2d, uv: Vector, wv: Vector):
  --        (u = S`seq(i - 1)) AND (v = S`seq(i)) AND (w...
  have min_miter_sine_le_each_sine_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_half_angle_sine_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
--   LET u = S`seq(i - 1),
--       v = S`seq(i),
--       w = S`seq(i + 1),
--       uv = u - v,
--       wv = w - v
--     IN half_angle_vsin
theorem miter_half_angle_sine_pos : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : ℝ × ℝ) => fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => half_angle_vsin ^(uv) ^(wv) > 0 (w - v) (u - v) (S.seq (i + 1)) (S.seq i) (S.seq (i - 1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.wrapped_miter_half_angle_sine_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET u = S`seq(S`length - 2),
--          v = S`seq(lbi),
--          w = S`seq
theorem wrapped_miter_half_angle_sine_pos : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let u := S.seq (S.List.length - 2); let v := S.seq lbi; let w := S.seq (1 + lbi); let uv := u - v; let wv := w - v; half_angle_vsin ^(uv) ^(wv) > 0) (loop_back_intercept S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.wrapped_miter_half_angle_sine_pos_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET u = S`seq(S`length - 3),
--          v = S`seq(S`length - 2),
--          
theorem wrapped_miter_half_angle_sine_pos_2 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let u := S.seq (S.List.length - 3); let v := S.seq (S.List.length - 2); let w := S.seq lbi; let uv := u - v; let wv := w - v; half_angle_vsin ^(uv) ^(wv) > 0) (loop_back_intercept S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.min_miter_sine_le_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq): min_miter_sine(S) <= 1
theorem min_miter_sine_le_1 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}), min_miter_sine S ≤ 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_half_angle_cosine_lt_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
--   LET u = S`seq(i - 1),
--       v = S`seq(i),
--       w = S`seq(i + 1),
--       uv = u - v,
--       wv = w - v
--     IN half_angle_vcos
theorem miter_half_angle_cosine_lt_1 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : ℝ × ℝ) => fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => half_angle_vcos ^(uv) ^(wv) < 1 (w - v) (u - v) (S.seq (i + 1)) (S.seq i) (S.seq (i - 1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.max_miter_offset
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min_miter_sine(S) * min_segment_sep(S) / 6
noncomputable def max_miter_offset : sorry := by
  sorry  -- pvs2lean: definition stub for min_miter_sine(S) * min_segment_sep(S) / 6

-- pvs2lean: miter_point_sequences.miter_point_defns_equiv_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
--         d: miter_seq_offset_dist(S)):
--   miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) =
--    miter_point_hyp(S`seq(i), S`
theorem miter_point_defns_equiv_seq : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d = miter_point_hyp (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d := by
  -- === TCCs for miter_point_defns_equiv_seq ===
  -- TCC: miter_point_defns_equiv_seq_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S)):
  --   i < S`length
  have miter_point_defns_equiv_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_defns_equiv_seq_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(i - 1) /= S`seq(i)
  have miter_point_defns_equiv_seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_defns_equiv_seq_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S)):
  --   i + 1 < S`length
  have miter_point_defns_equiv_seq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_defns_equiv_seq_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(i + 1) /= S`seq(i) AND
  --    NOT dir_parallel?(S`seq(i - 1) - S`seq(i), S`seq(i ...
  have miter_point_defns_equiv_seq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.three_pt_miniseq_in_endpt_seq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
--         d: miter_seq_offset_dist(S)):
--   three_point_miniseq?(S`seq(i - 1), S`seq(i))(S`seq(i + 1)) AND
--    three_point_offset_d
theorem three_pt_miniseq_in_endpt_seq : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), three_point_miniseq_p (S.seq (i - 1)) (S.seq i) (S.seq (i + 1)) ∧ three_point_offset_dist_p (S.seq (i - 1)) (S.seq i) (S.seq (i + 1)) (d) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.four_pt_miniseq_in_endpt_seq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
--         d: miter_seq_offset_dist(S)):
--   four_point_miniseq?(S`seq(i - 1), S`seq(i), S`seq(i + 1))(S`seq(i + 2))
--    AND
--    four
theorem four_pt_miniseq_in_endpt_seq : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 2}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), four_point_miniseq_p (S.seq (i - 1)) (S.seq i) (S.seq (i + 1)) (S.seq (i + 2)) ∧ four_point_offset_dist_p (S.seq (i - 1)) (S.seq i) (S.seq (i + 1)) (S.seq (i + 2))
                       (d) := by
  -- === TCCs for four_pt_miniseq_in_endpt_seq ===
  -- TCC: four_pt_miniseq_in_endpt_seq_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   i + 2 < S`length
  have four_pt_miniseq_in_endpt_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: four_pt_miniseq_in_endpt_seq_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   i - 1 >= 0 AND i - 1 < S`length
  have four_pt_miniseq_in_endpt_seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: four_pt_miniseq_in_endpt_seq_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   i < S`length
  have four_pt_miniseq_in_endpt_seq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: four_pt_miniseq_in_endpt_seq_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   i + 1 < S`length
  have four_pt_miniseq_in_endpt_seq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: four_pt_miniseq_in_endpt_seq_TCC5
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   four_point_miniseq?(S`seq(i - 1), S`seq(i), S`seq(i + 1))(S`seq(i + 2))
  --    IMPLIES
  -- ...
  have four_pt_miniseq_in_endpt_seq_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.three_pt_miniseq_in_endpt_seq_wrap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      three_point_miniseq?(S`seq(S`length - 2), S
theorem three_pt_miniseq_in_endpt_seq_wrap : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → three_point_miniseq_p (S.seq (S.List.length - 2)) (S.seq lbi) (S.seq (1 + lbi)) ∧
 three_point_offset_dist_p (S.seq (S.List.length - 2)) (S.seq lbi) (S.seq (1 + lbi))
                         (d) (loop_back_intercept S) := by
  -- === TCCs for three_pt_miniseq_in_endpt_seq_wrap ===
  -- TCC: three_pt_miniseq_in_endpt_seq_wrap_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have three_pt_miniseq_in_endpt_seq_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.four_pt_miniseq_in_endpt_seq_wrap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      four_point_miniseq?(S`seq(S`length - 2), S`
theorem four_pt_miniseq_in_endpt_seq_wrap : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → four_point_miniseq_p (S.seq (S.List.length - 2)) (S.seq lbi) (S.seq (1 + lbi))
                   (S.seq (2 + lbi))
 ∧
 four_point_offset_dist_p (S.seq (S.List.length - 2)) (S.seq lbi) (S.seq (1 + lbi)) (S.seq (2 + lbi))
                        (d) (loop_back_intercept S) := by
  -- === TCCs for four_pt_miniseq_in_endpt_seq_wrap ===
  -- TCC: four_pt_miniseq_in_endpt_seq_wrap_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have four_pt_miniseq_in_endpt_seq_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.four_pt_miniseq_in_endpt_seq_wrap_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      four_point_miniseq?(S`seq(S`length - 3), S`
theorem four_pt_miniseq_in_endpt_seq_wrap_2 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → four_point_miniseq_p (S.seq (S.List.length - 3)) (S.seq (S.List.length - 2)) (S.seq lbi)
                   (S.seq (1 + lbi))
 ∧
 four_point_offset_dist_p (S.seq (S.List.length - 3)) (S.seq (S.List.length - 2)) (S.seq lbi) (S.seq (1 + lbi))
                        (d) (loop_back_intercept S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_point_neq_vtx
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal):
--   miter_point(v, u, w, d) /= v
theorem miter_point_neq_vtx : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), miter_point v u w d ≠ v := by
  -- === TCCs for miter_point_neq_vtx ===
  -- TCC: miter_point_neq_vtx_TCC1
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v))): u /= v
  have miter_point_neq_vtx_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_neq_vtx_TCC2
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v))):
  --   w /= v AND NOT dir_parallel?(u - v, w - v)
  have miter_point_neq_vtx_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_point_neq_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
--         d: posreal):
--   miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) /= S`seq(i)
theorem miter_point_neq_vertex : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d ≠ S.seq i := by
  -- === TCCs for miter_point_neq_vertex ===
  -- TCC: miter_point_neq_vertex_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
  --   i < S`length
  have miter_point_neq_vertex_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_neq_vertex_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
  --   S`seq(i - 1) /= S`seq(i)
  have miter_point_neq_vertex_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_neq_vertex_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
  --   i + 1 < S`length
  have miter_point_neq_vertex_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_neq_vertex_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
  --   S`seq(i + 1) /= S`seq(i) AND
  --    NOT dir_parallel?(S`seq(i - 1) - S`seq(i), S`seq(i + 1) - S`seq(i))
  have miter_point_neq_vertex_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.successive_miter_pair_neq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
--         d: (four_point_offset_dist?(u, v, w, x))):
--   miter_point(v, u, w, d) /= miter_point(w, v, x, d)
theorem successive_miter_pair_neq : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (x : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), miter_point v u w d ≠ miter_point w v x d := by
  -- === TCCs for successive_miter_pair_neq ===
  -- TCC: successive_miter_pair_neq_TCC1
  -- Statement: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
  --         d: (four_point_offset_dist?(u, v, w, x))):
  --   u /= v
  have successive_miter_pair_neq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: successive_miter_pair_neq_TCC2
  -- Statement: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
  --         d: (four_point_offset_dist?(u, v, w, x))):
  --   w /= v AND NOT dir_parallel?(u - v, w - v)
  have successive_miter_pair_neq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: successive_miter_pair_neq_TCC3
  -- Statement: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
  --         d: (four_point_offset_dist?(u, v, w, x))):
  --   v /= w
  have successive_miter_pair_neq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: successive_miter_pair_neq_TCC4
  -- Statement: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
  --         d: (four_point_offset_dist?(u, v, w, x))):
  --   x /= w AND NOT dir_parallel?(v - w, x - w)
  have successive_miter_pair_neq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.successive_miter_points_neq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
--         d: miter_seq_offset_dist(S)):
--   miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) /=
--    miter_point(S`seq(i + 1), S
theorem successive_miter_points_neq : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 2}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d ≠ miter_point (S.seq (i + 1)) (S.seq i) (S.seq (i + 2)) d := by
  -- === TCCs for successive_miter_points_neq ===
  -- TCC: successive_miter_points_neq_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(i - 1) /= S`seq(i)
  have successive_miter_points_neq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: successive_miter_points_neq_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(i + 1) /= S`seq(i) AND
  --    NOT dir_parallel?(S`seq(i - 1) - S`seq(i), S`seq(i ...
  have successive_miter_points_neq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: successive_miter_points_neq_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   NOT S`seq(i) = S`seq(1 + i)
  have successive_miter_points_neq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: successive_miter_points_neq_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S)):
  --   NOT S`seq(i + 2) = S`seq(1 + i) AND
  --    NOT dir_parallel?(S`seq(i) - S`seq(1 + i), S...
  have successive_miter_points_neq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.wrapped_miter_points_neq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      miter_point(S`seq(lbi), S`seq(S`length - 2)
theorem wrapped_miter_points_neq : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → miter_point (S.seq lbi) (S.seq (S.List.length - 2)) (S.seq (1 + lbi)) d ≠
 miter_point (S.seq (1 + lbi)) (S.seq lbi) (S.seq (2 + lbi)) d
 ∧
 miter_point (S.seq lbi) (S.seq (S.List.length - 2)) (S.seq (1 + lbi)) d ≠
  miter_point (S.seq (S.List.length - 2)) (S.seq (S.List.length - 3)) (S.seq lbi) d (loop_back_intercept S) := by
  -- === TCCs for wrapped_miter_points_neq ===
  -- TCC: wrapped_miter_points_neq_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have wrapped_miter_points_neq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_miter_points_neq_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have wrapped_miter_points_neq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_miter_points_neq_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have wrapped_miter_points_neq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_miter_points_neq_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have wrapped_miter_points_neq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_miter_points_neq_TCC5
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have wrapped_miter_points_neq_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_miter_points_neq_TCC6
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have wrapped_miter_points_neq_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_point_neq_right_offset_point
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_point_neq_right_offset_point : True := by
  -- === TCCs for miter_point_neq_right_offset_point ===
  -- TCC: miter_point_neq_right_offset_point_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(i + 1) /= S`seq(i)
  have miter_point_neq_right_offset_point_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_point_neq_right_offset_point_prev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_point_neq_right_offset_point_prev : True := by
  -- === TCCs for miter_point_neq_right_offset_point_prev ===
  -- TCC: miter_point_neq_right_offset_point_prev_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(i) /= S`seq(i - 1)
  have miter_point_neq_right_offset_point_prev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_point_from_prev_segment
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal):
--   miter_point(v, u, w, d) = miter_point_prev(v, u, w, d)
theorem miter_point_from_prev_segment : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), miter_point v u w d = miter_point_prev v u w d := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_dist_to_pivot_pt_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal):
--   LET m = miter_point(v, u, w, d), hs = half_angle_vsin(^(u - v), ^(w - v))
--     IN norm(m - v) = d / hs
theorem miter_pt_dist_to_pivot_pt_bound : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (m : ℝ × ℝ) => fun (hs : {x : ℝ // 0 ≤ x ∧ x ≤ 1}) => norm (m - v) = d / hs (half_angle_vsin (^(u - v)) (^(w - v))) (miter_point v u w d) := by
  -- === TCCs for miter_pt_dist_to_pivot_pt_bound ===
  -- TCC: miter_pt_dist_to_pivot_pt_bound_TCC1
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal,
  --         m: point_2d):
  --   m = miter_point(v, u, w, d) IMPLIES u - v /= zero
  have miter_pt_dist_to_pivot_pt_bound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_dist_to_pivot_pt_bound_TCC2
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal,
  --         m: point_2d):
  --   m = miter_point(v, u, w, d) IMPLIES w - v /= zero
  have miter_pt_dist_to_pivot_pt_bound_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_dist_to_pivot_pt_bound_TCC3
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal,
  --         m: point_2d, hs: {x: real | 0 <= x AND x <= 1}):
  --   m = miter_point(v, u, w, d) AND hs = half_angle_vsin(^(u - v), ^(w - ...
  have miter_pt_dist_to_pivot_pt_bound_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_dist_to_pivot_lower_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal):
--   d <= norm(miter_point(v, u, w, d) - v)
theorem miter_pt_dist_to_pivot_lower_bound : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), d ≤ norm (miter_point v u w d - v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_dist_to_edge_endpt_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
--         d: miter_seq_offset_dist(S)):
--   LET m = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) IN
--     norm(m - S`seq(i))
theorem miter_pt_dist_to_edge_endpt_bound : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (m : ℝ × ℝ) => norm (m - S.seq i) ≤ d / min_miter_sine S (miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.wrapped_miter_pt_dist_to_edge_endpt_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET m =
--            miter_point(S`seq(lbi), 
theorem wrapped_miter_pt_dist_to_edge_endpt_bound : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let m := miter_point (S.seq lbi) (S.seq (S.List.length - 2)) (S.seq (1 + lbi)) d; norm (m - S.seq lbi) ≤ d / min_miter_sine S) (loop_back_intercept S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.wrapped_miter_pt_dist_to_edge_endpt_bound_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET m =
--            miter_point(S`seq(S`leng
theorem wrapped_miter_pt_dist_to_edge_endpt_bound_2 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let m := miter_point (S.seq (S.List.length - 2)) (S.seq (S.List.length - 3)) (S.seq lbi) d; norm (m - S.seq (S.List.length - 2)) ≤ d / min_miter_sine S) (loop_back_intercept S) := by
  -- === TCCs for wrapped_miter_pt_dist_to_edge_endpt_bound_2 ===
  -- TCC: wrapped_miter_pt_dist_to_edge_endpt_bound_2_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have wrapped_miter_pt_dist_to_edge_endpt_bound_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_miter_pt_dist_to_edge_endpt_bound_2_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have wrapped_miter_pt_dist_to_edge_endpt_bound_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_to_vertex_dist_lt_edge_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
--         d: miter_seq_offset_dist(S)):
--   LET m = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) IN
--     norm(m - S`seq(i))
theorem miter_pt_to_vertex_dist_lt_edge_length : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (m : ℝ × ℝ) => norm (m - S.seq i) ≤ min_segment_sep S / 6 ∧ ∀ (j : Finset.range S.List.length - 1), norm (m - S.seq i) ≤ norm (S.seq (j + 1) - S.seq j) / 6 (miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d) := by
  -- === TCCs for miter_pt_to_vertex_dist_lt_edge_length ===
  -- TCC: miter_pt_to_vertex_dist_lt_edge_length_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S), m: point_2d):
  --   m = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) AND
  --    norm(m...
  have miter_pt_to_vertex_dist_lt_edge_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_to_vertex_dist_lt_edge_length_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S), m: point_2d):
  --   m = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) AND
  --    norm(m...
  have miter_pt_to_vertex_dist_lt_edge_length_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_to_vtx_dist_lt_edge_len_wrap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET m =
--            miter_point(S`seq(lbi), 
theorem miter_pt_to_vtx_dist_lt_edge_len_wrap : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let m := miter_point (S.seq lbi) (S.seq (S.List.length - 2)) (S.seq (lbi + 1)) d; norm (m - S.seq lbi) ≤ min_segment_sep S / 6 ∧
   ∀ (j : Finset.range S.List.length - 1), norm (m - S.seq lbi) ≤ norm (S.seq (j + 1) - S.seq j) / 6) (loop_back_intercept S) := by
  -- === TCCs for miter_pt_to_vtx_dist_lt_edge_len_wrap ===
  -- TCC: miter_pt_to_vtx_dist_lt_edge_len_wrap_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have miter_pt_to_vtx_dist_lt_edge_len_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_to_vtx_dist_lt_edge_len_wrap_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have miter_pt_to_vtx_dist_lt_edge_len_wrap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_to_vtx_dist_lt_edge_len_wrap_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have miter_pt_to_vtx_dist_lt_edge_len_wrap_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_to_vtx_dist_lt_edge_len_wrap_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have miter_pt_to_vtx_dist_lt_edge_len_wrap_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_to_vtx_dist_lt_edge_len_wrap_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET m =
--            miter_point(S`seq(S`leng
theorem miter_pt_to_vtx_dist_lt_edge_len_wrap_2 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let m := miter_point (S.seq (S.List.length - 2)) (S.seq (S.List.length - 3)) (S.seq lbi) d; norm (m - S.seq (S.List.length - 2)) ≤ min_segment_sep S / 6 ∧
   ∀ (j : Finset.range S.List.length - 1), norm (m - S.seq (S.List.length - 2)) ≤ norm (S.seq (j + 1) - S.seq j) / 6) (loop_back_intercept S) := by
  -- === TCCs for miter_pt_to_vtx_dist_lt_edge_len_wrap_2 ===
  -- TCC: miter_pt_to_vtx_dist_lt_edge_len_wrap_2_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have miter_pt_to_vtx_dist_lt_edge_len_wrap_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_to_vtx_dist_lt_edge_len_wrap_2_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have miter_pt_to_vtx_dist_lt_edge_len_wrap_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_segment_parallel_to_orig
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
--         d: (four_point_offset_dist?(u, v, w, x))):
--   LET m1 = miter_point(v, u, w, d), m2 = miter_point(w, v, x, d) IN
--     dir_parallel?(w
theorem miter_pt_segment_parallel_to_orig : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (x : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (m1 : ℝ × ℝ) => fun (m2 : ℝ × ℝ) => dir_parallel_p (w - v) (m2 - m1) (miter_point w v x d) (miter_point v u w d) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_segment_parallel_to_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
--         d: miter_seq_offset_dist(S)):
--   LET m1 = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d),
--       m2 = miter_point(
theorem miter_pt_segment_parallel_to_edge : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 2}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (m1 : ℝ × ℝ) => fun (m2 : ℝ × ℝ) => dir_parallel_p (S.seq (i + 1) - S.seq i) (m2 - m1) (miter_point (S.seq (i + 1)) (S.seq i) (S.seq (i + 2)) d) (miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_point_cross_gt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal):
--   LET mp = miter_point(v, u, w, d) IN
--     cross(u - v, mp - v) > 0 AND cross(mp - v, w - v) > 0
theorem miter_point_cross_gt_0 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (mp : ℝ × ℝ) => cross (u - v) (mp - v) > 0 ∧ cross (mp - v) (w - v) > 0 (miter_point v u w d) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_point_cross_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
--         d: posreal):
--   LET mp = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) IN
--     cross(S`seq(i - 1) - S`seq(i), mp 
theorem miter_point_cross_pos : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (mp : ℝ × ℝ) => cross (S.seq (i - 1) - S.seq i) (mp - S.seq i) > 0 ∧ cross (mp - S.seq i) (S.seq (i + 1) - S.seq i) > 0 (miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.norm_ratio_miter_pt_seg_to_seg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
--         d: miter_seq_offset_dist(S)):
--   LET m1 = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d),
--       m2 = miter_point(
theorem norm_ratio_miter_pt_seg_to_seg : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 2}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (m1 : ℝ × ℝ) => fun (m2 : ℝ × ℝ) => fun (k : ℝ) => 2 / 3 ≤ k ∧ k ≤ 4 / 3 (norm (m2 - m1) / norm (S.seq (i + 1) - S.seq i)) (miter_point (S.seq (i + 1)) (S.seq i) (S.seq (i + 2)) d) (miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d) := by
  -- === TCCs for norm_ratio_miter_pt_seg_to_seg ===
  -- TCC: norm_ratio_miter_pt_seg_to_seg_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S), m1: point_2d, m2: point_2d):
  --   m1 = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), ...
  have norm_ratio_miter_pt_seg_to_seg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.norm_ratio_miter_pt_seg_to_seg_wrap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET m1 =
--            miter_point(S`seq(S`len
theorem norm_ratio_miter_pt_seg_to_seg_wrap : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let m1 := miter_point (S.seq (S.List.length - 2)) (S.seq (S.List.length - 3)) (S.seq lbi) d; let m2 := miter_point (S.seq lbi) (S.seq (S.List.length - 2)) (S.seq (lbi + 1)) d; let k := norm (m2 - m1) / norm (S.seq lbi - S.seq (S.List.length - 2)); 2 / 3 ≤ k ∧ k ≤ 4 / 3) (loop_back_intercept S) := by
  -- === TCCs for norm_ratio_miter_pt_seg_to_seg_wrap ===
  -- TCC: norm_ratio_miter_pt_seg_to_seg_wrap_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have norm_ratio_miter_pt_seg_to_seg_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_offset_with_bounded_shift_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
--         d: miter_seq_offset_dist(S)):
--   LET m1 = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) IN
--     EXISTS (a: real):
theorem miter_pt_offset_with_bounded_shift_1 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (m1 : ℝ × ℝ) => ∃ (a : ℝ), m1 =
 S.seq i + a * ^(S.seq (i + 1) - S.seq i) +
  d * ^(perp_left (S.seq (i + 1) - S.seq i)) ∧ |a| ≤ norm (S.seq (i + 1) - S.seq i) / 6 (miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d) := by
  -- === TCCs for miter_pt_offset_with_bounded_shift_1 ===
  -- TCC: miter_pt_offset_with_bounded_shift_1_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S), m1: point_2d):
  --   m1 = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) IMPLIES
  --    ...
  have miter_pt_offset_with_bounded_shift_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_offset_with_bounded_shift_1_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S), m1: point_2d):
  --   m1 = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) IMPLIES
  --    ...
  have miter_pt_offset_with_bounded_shift_1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_offset_with_bounded_shift_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2),
--         d: miter_seq_offset_dist(S)):
--   LET m2 = miter_point(S`seq(i + 1), S`seq(i), S`seq(i + 2), d) IN
--     EXISTS (b: real):
--       m2 =
theorem miter_pt_offset_with_bounded_shift_2 : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (m2 : ℝ × ℝ) => ∃ (b : ℝ), m2 =
 S.seq (i + 1) + b * ^(S.seq (i + 1) - S.seq i) +
  d * ^(perp_left (S.seq (i + 1) - S.seq i)) ∧ |b| ≤ norm (S.seq (i + 1) - S.seq i) / 6 (miter_point (S.seq (i + 1)) (S.seq i) (S.seq (i + 2)) d) := by
  -- === TCCs for miter_pt_offset_with_bounded_shift_2 ===
  -- TCC: miter_pt_offset_with_bounded_shift_2_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2),
  --         d: miter_seq_offset_dist(S)):
  --   i + 1 < S`length
  have miter_pt_offset_with_bounded_shift_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_offset_with_bounded_shift_2_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2),
  --         d: miter_seq_offset_dist(S)):
  --   i < S`length
  have miter_pt_offset_with_bounded_shift_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_offset_with_bounded_shift_2_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2),
  --         d: miter_seq_offset_dist(S)):
  --   NOT S`seq(i) = S`seq(1 + i)
  have miter_pt_offset_with_bounded_shift_2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_offset_with_bounded_shift_2_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2),
  --         d: miter_seq_offset_dist(S)):
  --   i + 2 < S`length
  have miter_pt_offset_with_bounded_shift_2_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_offset_with_bounded_shift_2_TCC5
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2),
  --         d: miter_seq_offset_dist(S)):
  --   NOT S`seq(i + 2) = S`seq(1 + i) AND
  --    NOT dir_parallel?(S`seq(i) - S`seq(1 + i), S`seq(i + 2)...
  have miter_pt_offset_with_bounded_shift_2_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_offset_with_bounded_shift_2_TCC6
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2),
  --         d: miter_seq_offset_dist(S), m2: point_2d):
  --   m2 = miter_point(S`seq(i + 1), S`seq(i), S`seq(i + 2), d) IMPLIES
  --    S`seq(i + 1...
  have miter_pt_offset_with_bounded_shift_2_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_offset_with_bounded_shift_2_TCC7
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2),
  --         d: miter_seq_offset_dist(S), m2: point_2d):
  --   m2 = miter_point(S`seq(i + 1), S`seq(i), S`seq(i + 2), d) IMPLIES
  --    perp_left(S...
  have miter_pt_offset_with_bounded_shift_2_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_pt_offset_with_bounded_shift_wrap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      LET m2 =
--            miter_point(S`seq(lbi),
theorem miter_pt_offset_with_bounded_shift_wrap : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → (let m2 := miter_point (S.seq lbi) (S.seq (S.List.length - 2)) (S.seq (lbi + 1)) d; ∃ (b : ℝ), m2 =
     S.seq lbi + b * ^(S.seq lbi - S.seq (S.List.length - 2)) +
      d * ^(perp_left (S.seq lbi - S.seq (S.List.length - 2)))
     ∧ |b| ≤ norm (S.seq lbi - S.seq (S.List.length - 2)) / 6) (loop_back_intercept S) := by
  -- === TCCs for miter_pt_offset_with_bounded_shift_wrap ===
  -- TCC: miter_pt_offset_with_bounded_shift_wrap_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have miter_pt_offset_with_bounded_shift_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_pt_offset_with_bounded_shift_wrap_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have miter_pt_offset_with_bounded_shift_wrap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.right_seg_offset_on_miter_pt_seg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem right_seg_offset_on_miter_pt_seg : True := by
  -- === TCCs for right_seg_offset_on_miter_pt_seg ===
  -- TCC: right_seg_offset_on_miter_pt_seg_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S), m1: point_2d, m2: point_2d,
  --         seg: [# p1: point_2d, p2: point_2d #],
  --         mps...
  have right_seg_offset_on_miter_pt_seg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: right_seg_offset_on_miter_pt_seg_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         d: miter_seq_offset_dist(S), m1: point_2d, m2: point_2d,
  --         seg: [# p1: point_2d, p2: point_2d #],
  --         mps...
  have right_seg_offset_on_miter_pt_seg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.right_seg_offset_on_miter_pt_seg_wrap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem right_seg_offset_on_miter_pt_seg_wrap : True := by
  -- === TCCs for right_seg_offset_on_miter_pt_seg_wrap ===
  -- TCC: right_seg_offset_on_miter_pt_seg_wrap_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have right_seg_offset_on_miter_pt_seg_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: right_seg_offset_on_miter_pt_seg_wrap_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S...
  have right_seg_offset_on_miter_pt_seg_wrap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_point_not_on_either_seg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal):
--   LET mp = miter_point(v, u, w, d) IN
--     NOT point_on_segment?(mp, (# p1 := u, p2 := v #)) AND
--      NOT point_on_segment?(mp, (# 
theorem miter_point_not_on_either_seg : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (mp : ℝ × ℝ) => ¬ point_on_segment_p mp ((# p1 := u, p2 := v #)) ∧ ¬ point_on_segment_p mp ((# p1 := v, p2 := w #)) (miter_point v u w d) := by
  -- === TCCs for miter_point_not_on_either_seg ===
  -- TCC: miter_point_not_on_either_seg_TCC1
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal,
  --         mp: point_2d):
  --   mp = miter_point(v, u, w, d) IMPLIES v /= u
  have miter_point_not_on_either_seg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_not_on_either_seg_TCC2
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), d: posreal,
  --         mp: point_2d):
  --   mp = miter_point(v, u, w, d) AND
  --    NOT point_on_segment?(mp, (# p1 := u, p2 := v #))
  --    IMPLIES w /= v
  have miter_point_not_on_either_seg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_point_not_on_adj_segs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_point_not_on_adj_segs : True := by
  -- === TCCs for miter_point_not_on_adj_segs ===
  -- TCC: miter_point_not_on_adj_segs_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: posreal, mp: point_2d):
  --   mp = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) IMPLIES
  --    S`seq(i) /= S`seq...
  have miter_point_not_on_adj_segs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_point_not_on_adj_segs_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: posreal, mp: point_2d):
  --   mp = miter_point(S`seq(i), S`seq(i - 1), S`seq(i + 1), d) AND
  --    NOT point_on_segment?...
  have miter_point_not_on_adj_segs_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_seg_points_not_on_seg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
--         d: (four_point_offset_dist?(u, v, w, x))):
--   LET g = (# p1 := v, p2 := w #),
--       m1 = miter_point(v, u, w, d),
--       m2 = miter_
theorem miter_seg_points_not_on_seg : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (x : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (g : (ℝ × ℝ) × (ℝ × ℝ)) => fun (m1 : ℝ × ℝ) => fun (m2 : ℝ × ℝ) => fun (h : (ℝ × ℝ) × (ℝ × ℝ)) => ∀ (p : ℝ × ℝ), point_on_segment_p p h → ¬ point_on_segment_p p g (# p1 := m1) (p2 := m2 #) (miter_point w v x d) (miter_point v u w d) (# p1 := v) (p2 := w #) := by
  -- === TCCs for miter_seg_points_not_on_seg ===
  -- TCC: miter_seg_points_not_on_seg_TCC1
  -- Statement: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
  --         d: (four_point_offset_dist?(u, v, w, x)),
  --         g: [# p1: point_2d, p2: point_2d #], m1: point_2d, m2: point_2d,
  --         h: [...
  have miter_seg_points_not_on_seg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_points_not_on_seg_TCC2
  -- Statement: FORALL (u, v, w: point_2d, x: (four_point_miniseq?(u, v, w)),
  --         d: (four_point_offset_dist?(u, v, w, x)),
  --         g: [# p1: point_2d, p2: point_2d #], m1: point_2d, m2: point_2d,
  --         h: [...
  have miter_seg_points_not_on_seg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_seg_points_not_on_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_seg_points_not_on_edge : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_seg_pts_not_on_prev_seg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), c: posreal,
--         d: (three_point_offset_dist?(u, v, w))):
--   LET g = (# p1 := u, p2 := v #),
--       mp = miter_point(v, u, w, d),
--       q = mp
theorem miter_seg_pts_not_on_prev_seg : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (g : (ℝ × ℝ) × (ℝ × ℝ)) => fun (mp : ℝ × ℝ) => fun (q : ℝ × ℝ) => fun (h : (ℝ × ℝ) × (ℝ × ℝ)) => ∀ (p : ℝ × ℝ), point_on_segment_p p h → ¬ point_on_segment_p p g (# p1 := mp) (p2 := q #) (mp + c * (w - v)) (miter_point v u w d) (# p1 := u) (p2 := v #) := by
  -- === TCCs for miter_seg_pts_not_on_prev_seg ===
  -- TCC: miter_seg_pts_not_on_prev_seg_TCC1
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), c: posreal,
  --         d: (three_point_offset_dist?(u, v, w)),
  --         g: [# p1: point_2d, p2: point_2d #], mp: point_2d, q: Vector,
  --         h:...
  have miter_seg_pts_not_on_prev_seg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_prev_seg_TCC2
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), c: posreal,
  --         d: (three_point_offset_dist?(u, v, w)),
  --         g: [# p1: point_2d, p2: point_2d #], mp: point_2d, q: Vector,
  --         h:...
  have miter_seg_pts_not_on_prev_seg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_seg_pts_not_on_prev_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_seg_pts_not_on_prev_edge : True := by
  -- === TCCs for miter_seg_pts_not_on_prev_edge ===
  -- TCC: miter_seg_pts_not_on_prev_edge_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         c: posreal, d: miter_seq_offset_dist(S),
  --         g: [# p1: point_2d, p2: point_2d #], mp: point_2d, q: Vector,
  --     ...
  have miter_seg_pts_not_on_prev_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_prev_edge_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         c: posreal, d: miter_seq_offset_dist(S),
  --         g: [# p1: point_2d, p2: point_2d #], mp: point_2d, q: Vector,
  --     ...
  have miter_seg_pts_not_on_prev_edge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_seg_pts_not_on_next_seg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), c: posreal,
--         d: (three_point_offset_dist?(u, v, w))):
--   LET g = (# p1 := v, p2 := w #),
--       mp = miter_point(v, u, w, d),
--       q = mp
theorem miter_seg_pts_not_on_next_seg : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (g : (ℝ × ℝ) × (ℝ × ℝ)) => fun (mp : ℝ × ℝ) => fun (q : ℝ × ℝ) => fun (h : (ℝ × ℝ) × (ℝ × ℝ)) => ∀ (p : ℝ × ℝ), point_on_segment_p p h → ¬ point_on_segment_p p g (# p1 := mp) (p2 := q #) (mp + c * (u - v)) (miter_point v u w d) (# p1 := v) (p2 := w #) := by
  -- === TCCs for miter_seg_pts_not_on_next_seg ===
  -- TCC: miter_seg_pts_not_on_next_seg_TCC1
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), c: posreal,
  --         d: (three_point_offset_dist?(u, v, w)),
  --         g: [# p1: point_2d, p2: (three_point_miniseq?(u, v)) #],
  --         mp: poi...
  have miter_seg_pts_not_on_next_seg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_next_seg_TCC2
  -- Statement: FORALL (u, v: point_2d, w: (three_point_miniseq?(u, v)), c: posreal,
  --         d: (three_point_offset_dist?(u, v, w)),
  --         g: [# p1: point_2d, p2: (three_point_miniseq?(u, v)) #],
  --         mp: poi...
  have miter_seg_pts_not_on_next_seg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_seg_pts_not_on_next_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_seg_pts_not_on_next_edge : True := by
  -- === TCCs for miter_seg_pts_not_on_next_edge ===
  -- TCC: miter_seg_pts_not_on_next_edge_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         c: posreal, d: miter_seq_offset_dist(S),
  --         g: [# p1: point_2d, p2: point_2d #], mp: point_2d, q: Vector,
  --     ...
  have miter_seg_pts_not_on_next_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_next_edge_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         c: posreal, d: miter_seq_offset_dist(S),
  --         g: [# p1: point_2d, p2: point_2d #], mp: point_2d, q: Vector,
  --     ...
  have miter_seg_pts_not_on_next_edge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_seg_pts_not_on_nonadj_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_seg_pts_not_on_nonadj_edge : True := by
  -- === TCCs for miter_seg_pts_not_on_nonadj_edge ===
  -- TCC: miter_seg_pts_not_on_nonadj_edge_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         j: below(S`length - 1), d: miter_seq_offset_dist(S)):
  --   j /= i AND
  --    j /= i - 1 AND
  --     j /= i + 1 AND
  --      (LET l...
  have miter_seg_pts_not_on_nonadj_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_nonadj_edge_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         j: below(S`length - 1), d: miter_seq_offset_dist(S)):
  --   j /= i AND
  --    j /= i - 1 AND
  --     j /= i + 1 AND
  --      (LET l...
  have miter_seg_pts_not_on_nonadj_edge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_nonadj_edge_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         j: below(S`length - 1), d: miter_seq_offset_dist(S)):
  --   j /= i AND
  --    j /= i - 1 AND
  --     j /= i + 1 AND
  --      (LET l...
  have miter_seg_pts_not_on_nonadj_edge_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_nonadj_edge_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 2},
  --         j: below(S`length - 1), d: miter_seq_offset_dist(S)):
  --   j /= i AND
  --    j /= i - 1 AND
  --     j /= i + 1 AND
  --      (LET l...
  have miter_seg_pts_not_on_nonadj_edge_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_seg_pts_not_on_nonadj_next_wrap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_seg_pts_not_on_nonadj_next_wrap : True := by
  -- === TCCs for miter_seg_pts_not_on_nonadj_next_wrap ===
  -- TCC: miter_seg_pts_not_on_nonadj_next_wrap_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, j: below(S`length - 2),
  --         d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):...
  have miter_seg_pts_not_on_nonadj_next_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_nonadj_next_wrap_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, j: below(S`length - 2),
  --         d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):...
  have miter_seg_pts_not_on_nonadj_next_wrap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_seg_pts_not_on_nonadj_prev_wrap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_seg_pts_not_on_nonadj_prev_wrap : True := by
  -- === TCCs for miter_seg_pts_not_on_nonadj_prev_wrap ===
  -- TCC: miter_seg_pts_not_on_nonadj_prev_wrap_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, j: below(S`length - 3),
  --         d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):...
  have miter_seg_pts_not_on_nonadj_prev_wrap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_nonadj_prev_wrap_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, j: below(S`length - 3),
  --         d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):...
  have miter_seg_pts_not_on_nonadj_prev_wrap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_nonadj_prev_wrap_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, j: below(S`length - 3),
  --         d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):...
  have miter_seg_pts_not_on_nonadj_prev_wrap_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_not_on_nonadj_prev_wrap_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, j: below(S`length - 3),
  --         d: miter_seq_offset_dist(S),
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):...
  have miter_seg_pts_not_on_nonadj_prev_wrap_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_seg_pts_between_segments_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_seg_pts_between_segments_1 : True := by
  -- === TCCs for miter_seg_pts_between_segments_1 ===
  -- TCC: miter_seg_pts_between_segments_1_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S)):
  --   1 + i < S`length
  have miter_seg_pts_between_segments_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S)):
  --   i < S`length
  have miter_seg_pts_between_segments_1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(i) /= S`seq(1 + i)
  have miter_seg_pts_between_segments_1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S)):
  --   2 + i < S`length
  have miter_seg_pts_between_segments_1_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC5
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(2 + i) /= S`seq(1 + i) AND
  --    NOT dir_parallel?(S`seq(i) - S`seq(1 + i), S`seq(2 + i) - ...
  have miter_seg_pts_between_segments_1_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC6
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S), m1: point_2d):
  --   m1 = miter_point(S`seq(1 + i), S`seq(i), S`seq(2 + i), d) IMPLIES
  --    S`seq(1 + i...
  have miter_seg_pts_between_segments_1_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC7
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S), m1: point_2d):
  --   m1 = miter_point(S`seq(1 + i), S`seq(i), S`seq(2 + i), d) IMPLIES
  --    3 + i < S`l...
  have miter_seg_pts_between_segments_1_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC8
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S), m1: point_2d):
  --   m1 = miter_point(S`seq(1 + i), S`seq(i), S`seq(2 + i), d) IMPLIES
  --    S`seq(3 + i...
  have miter_seg_pts_between_segments_1_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC9
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S), m1: point_2d, m2: point_2d):
  --   m1 = miter_point(S`seq(1 + i), S`seq(i), S`seq(2 + i), d) AND
  --    m...
  have miter_seg_pts_between_segments_1_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC10
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, p: point_2d, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S), m1: point_2d, m2: point_2d):
  --   m1 = miter_point(S`seq(1 + i), S`seq(i), S`seq(2 + i)...
  have miter_seg_pts_between_segments_1_tcc10 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: miter_seg_pts_between_segments_1_TCC11
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, p: point_2d, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S), m1: point_2d, m2: point_2d):
  --   m1 = miter_point(S`seq(1 + i), S`seq(i), S`seq(2 + i)...
  have miter_seg_pts_between_segments_1_tcc11 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.miter_seg_pts_between_segments_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_seg_pts_between_segments_2 : True := by
  -- === TCCs for miter_seg_pts_between_segments_2 ===
  -- TCC: miter_seg_pts_between_segments_2_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, p: point_2d, i: below(S`length - 3),
  --         d: miter_seq_offset_dist(S), m2: point_2d, m3: point_2d):
  --   m2 = miter_point(S`seq(1 + i), S`seq(i), S`seq(2 + i)...
  have miter_seg_pts_between_segments_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.pt_on_seg_imp_pt_on_derived_seg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem pt_on_seg_imp_pt_on_derived_seg : True := by
  -- === TCCs for pt_on_seg_imp_pt_on_derived_seg ===
  -- TCC: pt_on_seg_imp_pt_on_derived_seg_TCC1
  -- Statement: FORALL (p: point_2d, s: segment_2d, d1, d2: real, v: Vector):
  --        (v = s`p2 - s`p1) AND (norm(p - s`p1) >= d1)
  --    AND (norm(s`p2 - p) >= d2) AND point_on_segment?(p, s)
  --    AND (d1 + d2 < norm(v)...
  have pt_on_seg_imp_pt_on_derived_seg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: pt_on_seg_imp_pt_on_derived_seg_TCC2
  -- Statement: FORALL (p: point_2d, s: segment_2d, d1, d2: real, v: Vector):
  --        (v = s`p2 - s`p1) AND (norm(p - s`p1) >= d1)
  --    AND (norm(s`p2 - p) >= d2) AND point_on_segment?(p, s)
  --    AND (d1 + d2 < norm(v)...
  have pt_on_seg_imp_pt_on_derived_seg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.pt_on_seg_imp_offset_pt_on_parallel_seg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d, v: vector_2d):
--   point_on_segment?(p, e) AND
--    point_on_segment?(e`p1 + v, s) AND point_on_segment?(e`p2 + v, s)
--    IMPLIES point_on_segment?(p + v, s)
theorem pt_on_seg_imp_offset_pt_on_parallel_seg : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), point_on_segment_p p e ∧
 point_on_segment_p (e.p1 + v) s ∧ point_on_segment_p (e.p2 + v) s → point_on_segment_p (p + v) s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.half_angle_vcos_eq_miter_pt_vcos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
--         d: miter_seq_offset_dist(S)):
--   LET u = S`seq(i - 1),
--       v = S`seq(i),
--       w = S`seq(i + 1),
--       uv = u - v,
--   
theorem half_angle_vcos_eq_miter_pt_vcos : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : ℝ × ℝ) => fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => fun (m : ℝ × ℝ) => half_angle_vcos ^(uv) ^(wv) = vcos ^(uv) (^(m - v)) ∧ half_angle_vcos ^(uv) ^(wv) = vcos ^(wv) (^(m - v)) (miter_point v u w d) (w - v) (u - v) (S.seq (i + 1)) (S.seq i) (S.seq (i - 1)) := by
  -- === TCCs for half_angle_vcos_eq_miter_pt_vcos ===
  -- TCC: half_angle_vcos_eq_miter_pt_vcos_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S), u: point_2d, v: point_2d, w: point_2d,
  --         uv: Vector, wv: Vector):
  --        (u = S`...
  have half_angle_vcos_eq_miter_pt_vcos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: half_angle_vcos_eq_miter_pt_vcos_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S), u: point_2d, v: point_2d, w: point_2d,
  --         uv: Vector, wv: Vector):
  --        (u = S`...
  have half_angle_vcos_eq_miter_pt_vcos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: half_angle_vcos_eq_miter_pt_vcos_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1},
  --         d: miter_seq_offset_dist(S), u: point_2d, v: point_2d, w: point_2d,
  --         uv: Vector, wv: Vector, m: point_2d):
  --  ...
  have half_angle_vcos_eq_miter_pt_vcos_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_seg_pts_max_min_dist
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, p: point_2d, i: below(S`length - 3),
--         d: miter_seq_offset_dist(S)):
--   LET m1 = miter_point(S`seq(1 + i), S`seq(i), S`seq(2 + i), d),
--       m2 = miter_poin
theorem miter_seg_pts_max_min_dist : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (p : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 3}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), fun (m1 : ℝ × ℝ) => fun (m2 : ℝ × ℝ) => point_on_segment_p p ((# p1 := m1, p2 := m2 #)) → ∃ (q : point_2d), point_on_segment_p q ((# p1 := S.seq (1 + i), p2 := S.seq (2 + i) #)) ∧
   norm (p - q) ≤ d / min_miter_sine S (miter_point (S.seq (2 + i)) (S.seq (1 + i)) (S.seq (3 + i)) d) (miter_point (S.seq (1 + i)) (S.seq i) (S.seq (2 + i)) d) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.miter_point_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := S`length - 2,
--    seq
--      := LAMBDA (i: below(S`length - 2)):
--           miter_point(S`seq(i + 1), S`seq(i), S`seq(i + 2), d) #)
axiom miter_point_seq {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} : pvs_τ44 → pvs_τ45 → pvs_τ46

-- pvs2lean: miter_point_sequences.no_right_side_loop_back?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET lbi = loop_back_intercept(S) IN
--   lbi > 0 IMPLIES
--    NOT between_rays?((# p1 := S`seq(lbi - 1), p2 := S`seq(lbi) #),
--                      (# p1 := S`seq(lbi), p2 := S`seq(lbi + 1) #))
--             
axiom no_right_side_loop_back_p {pvs_τ51 : Type*} : pvs_τ51 → Prop

-- pvs2lean: miter_point_sequences.no_intersection_with_skimming_segments
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, d: miter_seq_offset_dist(S)):
--   no_right_side_loop_back?(S) IMPLIES
--    FORALL (p: point_2d):
--      NOT (point_on_segment_seq?(p, S) AND
--            point_on_segmen
theorem no_intersection_with_skimming_segments : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), no_right_side_loop_back_p S → ∀ (p : point_2d), ¬ (point_on_segment_seq_p p S ∧
        point_on_segment_seq_p p (miter_point_seq S d)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.closed_miter_point_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET m0 = miter_point(S`seq(0), S`seq(S`length - 2), S`seq(1), d) IN
--   singleton_seq(m0) o miter_point_seq(S, d) o singleton_seq(m0)
axiom closed_miter_point_seq {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7

-- pvs2lean: miter_point_sequences.length_closed_miter_point_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S:
--           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
--         d: miter_seq_offset_dist(S)):
--   closed_miter_point_seq(S, d)`length = S`length
theorem length_closed_miter_point_seq : ∀ (S : {S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S} // S.seq 0 = S.seq (S.List.length - 1)}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), closed_miter_point_seq S d.List.length = S.List.length := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.closed_miter_point_seq_element
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S:
--           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
--         d: miter_seq_offset_dist(S), i: below(S`length)):
--   closed_miter_point_seq(S, d)`seq(i) =
--    IF 0 < i AND
theorem closed_miter_point_seq_element : ∀ (S : {S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S} // S.seq 0 = S.seq (S.List.length - 1)}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length}), closed_miter_point_seq S d.seq i = if 0 < i ∧ i < S.List.length - 1
  then miter_point (S.seq i) (S.seq (i - 1)) (S.seq (i + 1)) d
else miter_point (S.seq 0) (S.seq (S.List.length - 2)) (S.seq 1) d := by
  -- === TCCs for closed_miter_point_seq_element ===
  -- TCC: closed_miter_point_seq_element_TCC1
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), i: below(S`length)):
  --   i < closed_miter_point_seq(S, d)`length
  have closed_miter_point_seq_element_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: closed_miter_point_seq_element_TCC2
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), i: below(S`length)):
  --   0 < i AND i < S`length - 1 IMPLIES i - 1 >= 0 AND ...
  have closed_miter_point_seq_element_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: closed_miter_point_seq_element_TCC3
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), i: below(S`length)):
  --   0 < i AND i < S`length - 1 IMPLIES S`seq(i - 1) /=...
  have closed_miter_point_seq_element_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: closed_miter_point_seq_element_TCC4
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), i: below(S`length)):
  --   0 < i AND i < S`length - 1 IMPLIES i + 1 < S`length
  have closed_miter_point_seq_element_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: closed_miter_point_seq_element_TCC5
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), i: below(S`length)):
  --   0 < i AND i < S`length - 1 IMPLIES
  --    S`seq(i + 1)...
  have closed_miter_point_seq_element_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: miter_point_sequences.no_intersection_with_wrap_edges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem no_intersection_with_wrap_edges : True := by
  -- === TCCs for no_intersection_with_wrap_edges ===
  -- TCC: no_intersection_with_wrap_edges_TCC1
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S)):
  --   S`length - 3 >= 0 AND S`length - 3 < S`length
  have no_intersection_with_wrap_edges_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_intersection_with_wrap_edges_TCC2
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(S`length - 3) /= S`seq(S`length - 2)
  have no_intersection_with_wrap_edges_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_intersection_with_wrap_edges_TCC3
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S)):
  --   S`seq(0) /= S`seq(S`length - 2) AND
  --    NOT dir_parallel?(S`seq(S`lengt...
  have no_intersection_with_wrap_edges_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_intersection_with_wrap_edges_TCC4
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), m0: point_2d, m1: point_2d):
  --   m0 = miter_point(S`seq(S`length - 2), S`se...
  have no_intersection_with_wrap_edges_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_intersection_with_wrap_edges_TCC5
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), m0: point_2d, m1: point_2d):
  --   m0 = miter_point(S`seq(S`length - 2), S`se...
  have no_intersection_with_wrap_edges_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_intersection_with_wrap_edges_TCC6
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), m0: point_2d, m1: point_2d):
  --   m0 = miter_point(S`seq(S`length - 2), S`se...
  have no_intersection_with_wrap_edges_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_intersection_with_wrap_edges_TCC7
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), m0: point_2d, m1: point_2d,
  --         m2: point_2d):
  --   m0 = miter_point(S`s...
  have no_intersection_with_wrap_edges_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_intersection_with_wrap_edges_TCC8
  -- Statement: FORALL (S:
  --           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
  --         d: miter_seq_offset_dist(S), m0: point_2d, m1: point_2d,
  --         m2: point_2d):
  --   m0 = miter_point(S`s...
  have no_intersection_with_wrap_edges_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: miter_point_sequences.no_intersection_with_closed_skimming_segs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S:
--           {S: edge_parallel_endpoint_seq | S`seq(0) = S`seq(S`length - 1)},
--         d: miter_seq_offset_dist(S)):
--   FORALL (p: point_2d):
--     NOT (point_on_segment_seq?(p, S) AND
--           
theorem no_intersection_with_closed_skimming_segs : ∀ (S : {S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S} // S.seq 0 = S.seq (S.List.length - 1)}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}), ∀ (p : ℝ × ℝ), ¬(point_on_segment_seq_p p S ∧ point_on_segment_seq_p p (closed_miter_point_seq S d)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.miter_point_sequences