-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nearest_segment_points
-- PVSLib library: line_segments
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nearest_segment_points

Auto-generated stubs from PVSLib theory `nearest_segment_points`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nearest_segment_points

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (are_segments_intersecting_alt_p : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (collinear_p : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (extending_s_crosses_e : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (find_perp_ray_crossing : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (find_perp_ray_line_crossing : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (is_nearest_endpoint_pair_p : pvs_τ11 → pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (line_crossing : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (nearest_endpoints : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} (norm : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (overlapping_p : pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (parallel_p : pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (point_on_segment_p : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (reverse_segment : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} (segment_endpoint_p : pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} (val : pvs_τ33 → Prop)

-- pvs2lean: nearest_segment_points.dropped_perp_after_extension_no_intersect_subcase
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, q, lc, pxs: point_2d):
--   NOT (parallel?(s)(e) OR
--         collinear?(s, e) OR are_segments_intersecting_alt?(s, e))
--    IMPLIES
--    LET closest_s_endpt =
--          IF norm(val(li
theorem dropped_perp_after_extension_no_intersect_subcase : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (q : ℝ × ℝ) (lc : ℝ × ℝ) (pxs : ℝ × ℝ), ¬ (parallel_p s(e) ∨
      collinear_p s e ∨ are_segments_intersecting_alt_p s e) → (let closest_s_endpt := if norm (val (line_crossing s e) - s.p1) <
          norm (val (line_crossing s e) - s.p2)
        then s.p1
      else s.p2
      ; (let closest_e_endpt := if norm (closest_s_endpt - e.p1) < norm (closest_s_endpt - e.p2)
          then e.p1
        else e.p2
        ; (extending_s_crosses_e s e ∧
      point_on_segment_p q e ∧
       find_perp_ray_line_crossing s q = pxs ∧
        val (line_crossing s e) = lc ∧
         find_perp_ray_crossing e closest_s_endpt = None)
     →
     ∃ (c : ℝ), pxs = lc + c * (closest_s_endpt - lc) ∧ c < 1)) := by
  -- === TCCs for dropped_perp_after_extension_no_intersect_subcase ===
  -- TCC: dropped_perp_after_extension_no_intersect_subcase_TCC1
  -- Statement: FORALL (s, e: segment_2d):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND NOT are_segments_intersecting_alt?(s, e)
  --    IMPLIES some?[point_2d](line_crossing(s, e))
  have dropped_perp_after_extension_no_intersect_subcase_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.dropped_perp_after_extension_intersect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e) OR extending_s_crosses_e(e, s))
--    IMPLIES
--    LET closest_s_endpt =
--          IF norm(val(line_crossing(s, e)) - s`p1) <
--           
theorem dropped_perp_after_extension_intersect : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ (parallel_p s(e) ∨ collinear_p s e ∨ extending_s_crosses_e e s) → (let closest_s_endpt := if norm (val (line_crossing s e) - s.p1) <
          norm (val (line_crossing s e) - s.p2)
        then s.p1
      else s.p2
      ; (find_perp_ray_crossing e closest_s_endpt ≠ None) →
   ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (val (find_perp_ray_crossing e closest_s_endpt) -
           closest_s_endpt)
      ≤ norm (p - q)) := by
  -- === TCCs for dropped_perp_after_extension_intersect ===
  -- TCC: dropped_perp_after_extension_intersect_TCC1
  -- Statement: FORALL (s, e: segment_2d):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND NOT extending_s_crosses_e(e, s)
  --    IMPLIES some?[point_2d](line_crossing(s, e))
  have dropped_perp_after_extension_intersect_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dropped_perp_after_extension_intersect_TCC2
  -- Statement: FORALL (s, e: segment_2d):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND NOT extending_s_crosses_e(e, s)
  --    IMPLIES
  --    FORALL (closest_s_endpt: point_2d):
  --      closest_s_endpt =
  --       IF no...
  have dropped_perp_after_extension_intersect_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.dropped_perp_after_extension_no_intersect_endpts_closest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   NOT (parallel?(s)(e) OR
--         collinear?(s, e) OR are_segments_intersecting_alt?(s, e))
--    IMPLIES
--    LET closest_s_endpt =
--          IF norm(val(line_crossing(s, e)) - s
theorem dropped_perp_after_extension_no_intersect_endpts_closest : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ (parallel_p s(e) ∨
      collinear_p s e ∨ are_segments_intersecting_alt_p s e) → (let closest_s_endpt := if norm (val (line_crossing s e) - s.p1) <
          norm (val (line_crossing s e) - s.p2)
        then s.p1
      else s.p2
      ; (let closest_e_endpt := if norm (closest_s_endpt - e.p1) < norm (closest_s_endpt - e.p2)
          then e.p1
        else e.p2
        ; (extending_s_crosses_e s e ∧
      find_perp_ray_crossing e closest_s_endpt = None)
     →
     ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (closest_e_endpt - closest_s_endpt) ≤ norm (p - q))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.nonoverlapping_collinear_closest_at_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   collinear?(s, e) IMPLIES
--    are_segments_intersecting_alt?(s, e) OR
--     FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e))):
--       norm(nearest_endpoints(s, e)`2
theorem nonoverlapping_collinear_closest_at_endpoints : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), collinear_p s e → are_segments_intersecting_alt_p s e ∨
 ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (nearest_endpoints s e.2 - nearest_endpoints s e.1) ≤
    norm (p - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.dropped_perp_with_no_extension_no_intersect_base
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   NOT (parallel?(s)(e) OR
--         collinear?(s, e) OR
--          extending_s_crosses_e(s, e) OR extending_s_crosses_e(e, s))
--    AND
--    (s`p1, e`p1) = nearest_endpoints(s, e) A
theorem dropped_perp_with_no_extension_no_intersect_base : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ (parallel_p s(e) ∨
      collinear_p s e ∨
       extending_s_crosses_e s e ∨ extending_s_crosses_e e s)
 ∧
 (s.p1, e.p1) = nearest_endpoints s e ∧
  find_perp_ray_crossing e s.p1 = None ∧
   find_perp_ray_crossing s e.p1 = None → ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (e.p1 - s.p1) ≤ norm (p - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.dropped_perp_with_no_extension_no_intersect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   NOT (parallel?(s)(e) OR
--         collinear?(s, e) OR
--          extending_s_crosses_e(s, e) OR extending_s_crosses_e(e, s))
--    IMPLIES
--    LET xing_pt = val(line_crossing(s, e
theorem dropped_perp_with_no_extension_no_intersect : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ (parallel_p s(e) ∨
      collinear_p s e ∨
       extending_s_crosses_e s e ∨ extending_s_crosses_e e s) → (let xing_pt := val (line_crossing s e); (let (s_closest_endpt, e_closest_endpt) := nearest_endpoints s e; find_perp_ray_crossing e s_closest_endpt = None ∧
     find_perp_ray_crossing s e_closest_endpt = None
     →
     ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (e_closest_endpt - s_closest_endpt) ≤ norm (p - q))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.dropped_perp_from_e_with_no_extension_intersect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e) OR extending_s_crosses_e(s, e))
--    IMPLIES
--    LET xing_pt = val(line_crossing(s, e)) IN
--      LET e_closest_endpt =
--            IF n
theorem dropped_perp_from_e_with_no_extension_intersect : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ (parallel_p s(e) ∨ collinear_p s e ∨ extending_s_crosses_e s e) → (let xing_pt := val (line_crossing s e); (let e_closest_endpt := if norm (xing_pt - e.p1) < norm (xing_pt - e.p2) then e.p1
        else e.p2
        ; find_perp_ray_crossing s e_closest_endpt ≠ None →
     ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (val (find_perp_ray_crossing s e_closest_endpt) -
             e_closest_endpt)
        ≤ norm (p - q))) := by
  -- === TCCs for dropped_perp_from_e_with_no_extension_intersect ===
  -- TCC: dropped_perp_from_e_with_no_extension_intersect_TCC1
  -- Statement: FORALL (s, e: segment_2d):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND NOT extending_s_crosses_e(s, e)
  --    IMPLIES some?[point_2d](line_crossing(s, e))
  have dropped_perp_from_e_with_no_extension_intersect_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dropped_perp_from_e_with_no_extension_intersect_TCC2
  -- Statement: FORALL (s, e: segment_2d):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND NOT extending_s_crosses_e(s, e)
  --    IMPLIES
  --    FORALL (xing_pt: point_2d, e_closest_endpt: point_2d):
  --      xing_pt = ...
  have dropped_perp_from_e_with_no_extension_intersect_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.dropped_perp_from_s_with_no_extension_intersect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e) OR extending_s_crosses_e(e, s))
--    IMPLIES
--    LET xing_pt = val(line_crossing(s, e)) IN
--      LET s_closest_endpt =
--            IF n
theorem dropped_perp_from_s_with_no_extension_intersect : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ (parallel_p s(e) ∨ collinear_p s e ∨ extending_s_crosses_e e s) → (let xing_pt := val (line_crossing s e); (let s_closest_endpt := if norm (xing_pt - s.p1) < norm (xing_pt - s.p2) then s.p1
        else s.p2
        ; find_perp_ray_crossing e s_closest_endpt ≠ None →
     ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (val (find_perp_ray_crossing e s_closest_endpt) -
             s_closest_endpt)
        ≤ norm (p - q))) := by
  -- === TCCs for dropped_perp_from_s_with_no_extension_intersect ===
  -- TCC: dropped_perp_from_s_with_no_extension_intersect_TCC1
  -- Statement: FORALL (s, e: segment_2d):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND NOT extending_s_crosses_e(e, s)
  --    IMPLIES
  --    FORALL (xing_pt: point_2d, s_closest_endpt: point_2d):
  --      xing_pt = ...
  have dropped_perp_from_s_with_no_extension_intersect_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.parallel_nonoverlapping_segs_closest_at_endpts
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   NOT overlapping?(s, e) IMPLIES
--    FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e))):
--      norm(nearest_endpoints(s, e)`1 - nearest_endpoints(s,
theorem parallel_nonoverlapping_segs_closest_at_endpts : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ overlapping_p s e → ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (nearest_endpoints s e.1 - nearest_endpoints s e.2) ≤ norm (p - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.parallel_overlapping_segs_closest_at_perps
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s)), r: (point_on_segment?(s))):
--   find_perp_ray_crossing(e, r) /= None IMPLIES
--    FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e))):
--      norm(val(fin
theorem parallel_overlapping_segs_closest_at_perps : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (r : ℝ × ℝ), find_perp_ray_crossing e r ≠ None → ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), norm (val (find_perp_ray_crossing e r) - r) ≤ norm (p - q) := by
  -- === TCCs for parallel_overlapping_segs_closest_at_perps ===
  -- TCC: parallel_overlapping_segs_closest_at_perps_TCC1
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s)), r: (point_on_segment?(s))):
  --   find_perp_ray_crossing(e, r) /= None IMPLIES
  --    FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e))):
  --      some?[poi...
  have parallel_overlapping_segs_closest_at_perps_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.endpoint_nearest_point_btw_segs_subcase_base
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: (point_on_segment?(s)),
--         q: (point_on_segment?(e))):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET xing_pt = val(line_crossing(s, e)) IN
--      norm(s`p2
theorem endpoint_nearest_point_btw_segs_subcase_base : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let xing_pt := val (line_crossing s e); norm (s.p2 - e.p1) ≤ norm (s.p1 - e.p1) ∧
   norm (s.p1 - xing_pt) < norm (s.p2 - xing_pt) ∧
    norm (e.p1 - xing_pt) < norm (e.p2 - xing_pt)
   →
        (find_perp_ray_crossing e s.p2 = None)
    ∨ extending_s_crosses_e s e ∨ extending_s_crosses_e e s
    ∨ are_segments_intersecting_alt_p s e
    ∨ (∃ (v: (segment_endpoint_p s)), (r: point_2d):
          point_on_segment_p r e ∧ norm (r - v) ≤ norm (p - q))
    ∨ (∃ (v: (segment_endpoint_p e)), (r: point_2d):
          point_on_segment_p r s ∧ norm (r - v) ≤ norm (p - q))) := by
  -- === TCCs for endpoint_nearest_point_btw_segs_subcase_base ===
  -- TCC: endpoint_nearest_point_btw_segs_subcase_base_TCC1
  -- Statement: FORALL (s, e: segment_2d, p: (point_on_segment?(s)),
  --         q: (point_on_segment?(e))):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    some?[point_2d](line_crossing(s, e))
  have endpoint_nearest_point_btw_segs_subcase_base_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.endpoint_nearest_point_btw_segs_subcase_base_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: (point_on_segment?(s)),
--         q: (point_on_segment?(e))):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET xing_pt = val(line_crossing(s, e)) IN
--      is_neares
theorem endpoint_nearest_point_btw_segs_subcase_base_2 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let xing_pt := val (line_crossing s e); is_nearest_endpoint_pair_p ((s.p2, e.p2)) s e ∧
   norm (s.p1 - xing_pt) < norm (s.p2 - xing_pt) ∧
    norm (e.p1 - xing_pt) < norm (e.p2 - xing_pt)
   →
        (find_perp_ray_crossing e s.p2 = None)
    ∨ extending_s_crosses_e s e ∨ extending_s_crosses_e e s
    ∨ are_segments_intersecting_alt_p s e
    ∨ (∃ (v: (segment_endpoint_p s)), (r: point_2d):
          point_on_segment_p r e ∧ norm (r - v) ≤ norm (p - q))
    ∨ (∃ (v: (segment_endpoint_p e)), (r: point_2d):
          point_on_segment_p r s ∧ norm (r - v) ≤ norm (p - q))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.endpoint_nearest_point_between_segments_subcase_s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: (point_on_segment?(s)),
--         q: (point_on_segment?(e))):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET s_closest_endpt = nearest_endpoints(s, e)`1,
--        
theorem endpoint_nearest_point_between_segments_subcase_s : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let s_closest_endpt := nearest_endpoints s e.1; let xing_pt := val (line_crossing s e); (s_closest_endpt =
         if norm (xing_pt - s.p1) < norm (xing_pt - s.p2) then s.p1
         else s.p2
         )
   ∨ (find_perp_ray_crossing e s_closest_endpt = None)
   ∨ extending_s_crosses_e s e ∨ extending_s_crosses_e e s
   ∨ are_segments_intersecting_alt_p s e
   ∨ (∃ (v: (segment_endpoint_p s)), (r: point_2d):
         point_on_segment_p r e ∧ norm (r - v) ≤ norm (p - q))
   ∨ (∃ (v: (segment_endpoint_p e)), (r: point_2d):
         point_on_segment_p r s ∧ norm (r - v) ≤ norm (p - q))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.endpoint_nearest_point_between_segments_subcase_e
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: (point_on_segment?(s)),
--         q: (point_on_segment?(e))):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET e_closest_endpt = nearest_endpoints(s, e)`2,
--        
theorem endpoint_nearest_point_between_segments_subcase_e : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let e_closest_endpt := nearest_endpoints s e.2; let xing_pt := val (line_crossing s e); (e_closest_endpt =
         if norm (xing_pt - e.p1) < norm (xing_pt - e.p2) then e.p1
         else e.p2
         )
   ∨ (find_perp_ray_crossing s e_closest_endpt = None)
   ∨ extending_s_crosses_e s e ∨ extending_s_crosses_e e s
   ∨ are_segments_intersecting_alt_p s e
   ∨ (∃ (v: (segment_endpoint_p s)), (r: point_2d):
         point_on_segment_p r e ∧ norm (r - v) ≤ norm (p - q))
   ∨ (∃ (v: (segment_endpoint_p e)), (r: point_2d):
         point_on_segment_p r s ∧ norm (r - v) ≤ norm (p - q))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.nearest_endpoint_cond
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: point_on_segment?(p, s) AND point_on_segment?(q, e) IMPLIES
--  (EXISTS (u: point_2d):
--     point_on_segment?(u, s) AND point_on_segment?(u, e))
--   OR
--   (EXISTS (v: (segment_endpoint?(s))), (r: point_2d):
-- 
axiom nearest_endpoint_cond {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: nearest_segment_points.endpoint_nearest_point_between_segments
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p, q: point_2d): nearest_endpoint_cond(s, e, p, q)
theorem endpoint_nearest_point_between_segments : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), nearest_endpoint_cond s e p q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nearest_segment_points.endpoint_nearest_between_reverse_segments
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, ss, e, ee: segment_2d, p, q: point_2d):
--   (ss = s OR ss = reverse_segment(s)) AND
--    (ee = e OR ee = reverse_segment(e)) AND
--     nearest_endpoint_cond(ss, ee, p, q)
--    IMPLIES nearest_endpo
theorem endpoint_nearest_between_reverse_segments : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ee : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), (ss = s ∨ ss = reverse_segment s) ∧
 (ee = e ∨ ee = reverse_segment e) ∧ nearest_endpoint_cond ss ee p q → nearest_endpoint_cond s e p q := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.nearest_segment_points