-- Auto-generated by pvs2lean/translator.py
-- PVS theory: vectors_2d_trig
-- PVSLib library: line_segments
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: vectors_2d_trig

Auto-generated stubs from PVSLib theory `vectors_2d_trig`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.vectors_2d_trig

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (pvs_op_7e6a2a : pvs_τ1 → pvs_τ2)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (cross : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (dir_parallel_p : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ34 : Type*} (norm : pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (parallel_p : pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} (perp_left : pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} (perp_right : pvs_τ38 → Prop)
variable {pvs_τ42 : Type*} (sq : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (sqv : pvs_τ43 → pvs_τ44)

-- pvs2lean: vectors_2d_trig.vsin
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cross(u, v)
axiom vsin {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ47 → pvs_τ48 → Prop

-- pvs2lean: vectors_2d_trig.vcos
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: u * v
axiom vcos {pvs_τ45 : Type*} {pvs_τ46 : Type*} : pvs_τ45 → pvs_τ46 → Prop

-- pvs2lean: vectors_2d_trig.vsin_sq_plus_vcos_sq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: Normalized): sq(vsin(u, v)) + sq(vcos(u, v)) = 1
theorem vsin_sq_plus_vcos_sq : ∀ (u : {v : ℝ × ℝ // norm v = 1}) (v : {v : ℝ × ℝ // norm v = 1}), (vsin u v^2) + (vcos u v^2) = 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.vector_lengths_from_sin_cos_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d):
--        (u /= v) AND (v /= w) AND (w /= u) AND (cross(v - u, w - u) > 0)
--    AND ((u - v) * (w - v) = 0)
--    IMPLIES
--    norm(v - u) = norm(w - u) * vcos(^(v - u), ^(w - u)) AN
theorem vector_lengths_from_sin_cos_1 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), (u ≠ v) ∧ (v ≠ w) ∧ (w ≠ u) ∧ (cross (v - u) (w - u) > 0)
 ∧ ((u - v) * (w - v) = 0) → norm (v - u) = norm (w - u) * vcos (^(v - u)) (^(w - u)) ∧
 norm (w - v) = norm (w - u) * vsin (^(v - u)) (^(w - u)) := by
  -- === TCCs for vector_lengths_from_sin_cos_1 ===
  -- TCC: vector_lengths_from_sin_cos_1_TCC1
  -- Statement: FORALL (u, v, w: point_2d):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (cross(v - u, w - u) > 0)
  --    AND ((u - v) * (w - v) = 0)
  --    IMPLIES v - u /= zero
  have vector_lengths_from_sin_cos_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: vector_lengths_from_sin_cos_1_TCC2
  -- Statement: FORALL (u, v, w: point_2d):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (cross(v - u, w - u) > 0)
  --    AND ((u - v) * (w - v) = 0)
  --    IMPLIES w - u /= zero
  have vector_lengths_from_sin_cos_1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.vector_lengths_from_sin_cos_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d):
--        (u /= v) AND (v /= w) AND (w /= u) AND (cross(w - u, v - u) > 0)
--    AND ((u - v) * (w - v) = 0)
--    IMPLIES
--    norm(v - u) = norm(w - u) * vcos(^(w - u), ^(v - u)) AN
theorem vector_lengths_from_sin_cos_2 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), (u ≠ v) ∧ (v ≠ w) ∧ (w ≠ u) ∧ (cross (w - u) (v - u) > 0)
 ∧ ((u - v) * (w - v) = 0) → norm (v - u) = norm (w - u) * vcos (^(w - u)) (^(v - u)) ∧
 norm (w - v) = norm (w - u) * vsin (^(w - u)) (^(v - u)) := by
  -- === TCCs for vector_lengths_from_sin_cos_2 ===
  -- TCC: vector_lengths_from_sin_cos_2_TCC1
  -- Statement: FORALL (u, v, w: point_2d):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (cross(w - u, v - u) > 0)
  --    AND ((u - v) * (w - v) = 0)
  --    IMPLIES w - u /= zero
  have vector_lengths_from_sin_cos_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: vector_lengths_from_sin_cos_2_TCC2
  -- Statement: FORALL (u, v, w: point_2d):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (cross(w - u, v - u) > 0)
  --    AND ((u - v) * (w - v) = 0)
  --    IMPLIES v - u /= zero
  have vector_lengths_from_sin_cos_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.scaled_sin_cos_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: Nz_vector, a, b: posreal):
--   vsin(^(a * u), ^(b * v)) = vsin(^(u), ^(v)) AND
--    vcos(^(a * u), ^(b * v)) = vcos(^(u), ^(v))
theorem scaled_sin_cos_eq : ∀ (u : {v : ℝ × ℝ // v ≠ zero}) (v : {v : ℝ × ℝ // v ≠ zero}) (a : {x : {x : ℝ // x ≥ 0} // x > 0}) (b : {x : {x : ℝ // x ≥ 0} // x > 0}), vsin (^(a * u)) (^(b * v)) = vsin ^(u) ^(v) ∧ vcos (^(a * u)) (^(b * v)) = vcos ^(u) ^(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.triangle_vertex_imp_sin_cos_pos_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, p, q: point_2d, c, d: posreal):
--   u /= v AND q = u + d * (v - u) AND p = q + c * perp_right(v - u) IMPLIES
--    vcos(^(v - u), ^(p - u)) > 0 AND vsin(^(v - u), ^(p - u)) > 0
theorem triangle_vertex_imp_sin_cos_pos_1 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (p : ℝ × ℝ) (q : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), u ≠ v ∧ q = u + d * (v - u) ∧ p = q + c * perp_right (v - u) → vcos (^(v - u)) (^(p - u)) > 0 ∧ vsin (^(v - u)) (^(p - u)) > 0 := by
  -- === TCCs for triangle_vertex_imp_sin_cos_pos_1 ===
  -- TCC: triangle_vertex_imp_sin_cos_pos_1_TCC1
  -- Statement: FORALL (u, v, p, q: point_2d, c, d: posreal):
  --   u /= v AND q = u + d * (v - u) AND p = q + c * perp_right(v - u) IMPLIES
  --    v - u /= zero
  have triangle_vertex_imp_sin_cos_pos_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: triangle_vertex_imp_sin_cos_pos_1_TCC2
  -- Statement: FORALL (u, v, p, q: point_2d, c, d: posreal):
  --   u /= v AND q = u + d * (v - u) AND p = q + c * perp_right(v - u) IMPLIES
  --    p - u /= zero
  have triangle_vertex_imp_sin_cos_pos_1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.triangle_vertex_imp_sin_cos_pos_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, p, q: point_2d, c, d: posreal):
--   u /= v AND q = u + d * (v - u) AND p = q + c * perp_left(v - u) IMPLIES
--    vcos(^(p - u), ^(v - u)) > 0 AND vsin(^(p - u), ^(v - u)) > 0
theorem triangle_vertex_imp_sin_cos_pos_2 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (p : ℝ × ℝ) (q : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), u ≠ v ∧ q = u + d * (v - u) ∧ p = q + c * perp_left (v - u) → vcos (^(p - u)) (^(v - u)) > 0 ∧ vsin (^(p - u)) (^(v - u)) > 0 := by
  -- === TCCs for triangle_vertex_imp_sin_cos_pos_2 ===
  -- TCC: triangle_vertex_imp_sin_cos_pos_2_TCC1
  -- Statement: FORALL (u, v, p, q: point_2d, c, d: posreal):
  --   u /= v AND q = u + d * (v - u) AND p = q + c * perp_left(v - u) IMPLIES
  --    p - u /= zero
  have triangle_vertex_imp_sin_cos_pos_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: triangle_vertex_imp_sin_cos_pos_2_TCC2
  -- Statement: FORALL (u, v, p, q: point_2d, c, d: posreal):
  --   u /= v AND q = u + d * (v - u) AND p = q + c * perp_left(v - u) IMPLIES
  --    v - u /= zero
  have triangle_vertex_imp_sin_cos_pos_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.complementary_angle_vcos_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w, x: point_2d, k: posreal):
--   u /= v AND v /= w AND u - v = k * (x - w) IMPLIES
--    vcos(^(u - v), ^(w - v)) = -vcos(^(v - w), ^(x - w))
theorem complementary_angle_vcos_eq : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (x : ℝ × ℝ) (k : {x : {x : ℝ // x ≥ 0} // x > 0}), u ≠ v ∧ v ≠ w ∧ u - v = k * (x - w) → vcos (^(u - v)) (^(w - v)) = -vcos (^(v - w)) (^(x - w)) := by
  -- === TCCs for complementary_angle_vcos_eq ===
  -- TCC: complementary_angle_vcos_eq_TCC1
  -- Statement: FORALL (u, v, w, x: point_2d, k: posreal):
  --   u /= v AND v /= w AND u - v = k * (x - w) IMPLIES u - v /= zero
  have complementary_angle_vcos_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: complementary_angle_vcos_eq_TCC2
  -- Statement: FORALL (u, v, w, x: point_2d, k: posreal):
  --   u /= v AND v /= w AND u - v = k * (x - w) IMPLIES w - v /= zero
  have complementary_angle_vcos_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: complementary_angle_vcos_eq_TCC3
  -- Statement: FORALL (u, v, w, x: point_2d, k: posreal):
  --   u /= v AND v /= w AND u - v = k * (x - w) IMPLIES v - w /= zero
  have complementary_angle_vcos_eq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: complementary_angle_vcos_eq_TCC4
  -- Statement: FORALL (u, v, w, x: point_2d, k: posreal):
  --   u /= v AND v /= w AND u - v = k * (x - w) IMPLIES x - w /= zero
  have complementary_angle_vcos_eq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.perp_comps_cos_sin_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: Normalized): vcos(u, v) * v + vsin(u, v) * perp_left(v) = u
theorem perp_comps_cos_sin_left : ∀ (u : {v : ℝ × ℝ // norm v = 1}) (v : {v : ℝ × ℝ // norm v = 1}), vcos u v * v + vsin u v * perp_left v = u := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.perp_comps_cos_sin_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: Normalized): vcos(u, v) * u + vsin(u, v) * perp_right(u) = v
theorem perp_comps_cos_sin_right : ∀ (u : {v : ℝ × ℝ // norm v = 1}) (v : {v : ℝ × ℝ // norm v = 1}), vcos u v * u + vsin u v * perp_right u = v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.half_angle_vsin
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sqrt((1 - vcos(u, v)) / 2)
axiom half_angle_vsin {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ17 → pvs_τ18 → Prop

-- pvs2lean: vectors_2d_trig.half_angle_vcos
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET mag = sqrt((1 + vcos(u, v)) / 2) IN
--   IF cross(u, v) >= 0 THEN mag ELSE -mag ENDIF
axiom half_angle_vcos {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ15 → pvs_τ16 → Prop

-- pvs2lean: vectors_2d_trig.half_angle_vsin_vcos_same
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u: Normalized):
--   half_angle_vsin(u, u) = 0 AND
--    half_angle_vsin(u, -u) = 1 AND
--     half_angle_vcos(u, u) = 1 AND half_angle_vcos(u, -u) = 0
theorem half_angle_vsin_vcos_same : ∀ (u : {v : ℝ × ℝ // norm v = 1}), half_angle_vsin u u = 0 ∧ half_angle_vsin u -u = 1 ∧
 half_angle_vcos u u = 1 ∧ half_angle_vcos u -u = 0 := by
  -- === TCCs for half_angle_vsin_vcos_same ===
  -- TCC: half_angle_vsin_vcos_same_TCC1
  -- Statement: FORALL (u: Normalized): half_angle_vsin(u, u) = 0 IMPLIES norm(-u) = 1
  have half_angle_vsin_vcos_same_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.half_angle_sine_dir_nonparallel
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, u: {p: point_2d | p /= v},
--         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)}):
--   LET uv = u - v, wv = w - v IN
--     vcos(^(uv), ^(wv)) < 1 AND sqrt((1 - vcos(^(
theorem half_angle_sine_dir_nonparallel : ∀ (v : ℝ × ℝ) (u : {p : ℝ × ℝ // p ≠ v}) (w : {p : ℝ × ℝ // p ≠ v ∧ ¬ dir_parallel_p (u - v) (p - v)}), fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => vcos ^(uv) ^(wv) < 1 ∧ Real.sqrt ((1 - vcos ^(uv) ^(wv)) / 2) > 0 (w - v) (u - v) := by
  -- === TCCs for half_angle_sine_dir_nonparallel ===
  -- TCC: half_angle_sine_dir_nonparallel_TCC1
  -- Statement: FORALL (v: point_2d, u: {p: point_2d | p /= v},
  --         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)},
  --         uv: Vector, wv: Vector):
  --   uv = u - v AND wv = w - v IMPLIES uv /= zero
  have half_angle_sine_dir_nonparallel_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: half_angle_sine_dir_nonparallel_TCC2
  -- Statement: FORALL (v: point_2d, u: {p: point_2d | p /= v},
  --         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)},
  --         uv: Vector, wv: Vector):
  --   uv = u - v AND wv = w - v IMPLIES wv /= zero
  have half_angle_sine_dir_nonparallel_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: half_angle_sine_dir_nonparallel_TCC3
  -- Statement: FORALL (v: point_2d, u: {p: point_2d | p /= v},
  --         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)},
  --         uv: Vector, wv: Vector):
  --   uv = u - v AND wv = w - v AND vcos(^(uv), ^...
  have half_angle_sine_dir_nonparallel_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.half_angle_cosine_dir_nonparallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, u: {p: point_2d | p /= v},
--         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)}):
--   LET uv = u - v, wv = w - v IN half_angle_vcos(^(uv), ^(wv)) < 1
theorem half_angle_cosine_dir_nonparallel : ∀ (v : ℝ × ℝ) (u : {p : ℝ × ℝ // p ≠ v}) (w : {p : ℝ × ℝ // p ≠ v ∧ ¬ dir_parallel_p (u - v) (p - v)}), fun (uv : ℝ × ℝ) => fun (wv : ℝ × ℝ) => half_angle_vcos ^(uv) ^(wv) < 1 (w - v) (u - v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.alt_half_angle_vsin
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET uv = ^(u - v), wv = ^(w - v) IN
--   IF uv = -wv THEN 1 ELSE abs(cross(uv, wv)) / norm(uv + wv) ENDIF
axiom alt_half_angle_vsin {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10

-- pvs2lean: vectors_2d_trig.alt_half_angle_vcos
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET uv = ^(u - v), wv = ^(w - v) IN
--   IF uv = -wv THEN 0
--   ELSE LET mag = (1 + uv * wv) / norm(uv + wv) IN
--          IF cross(uv, wv) >= 0 THEN mag ELSE -mag ENDIF
--   ENDIF
axiom alt_half_angle_vcos {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6

-- pvs2lean: vectors_2d_trig.half_angle_sine_defns_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, u, w: {p: point_2d | p /= v}):
--   half_angle_vsin(^(u - v), ^(w - v)) = alt_half_angle_vsin(v, u, w)
theorem half_angle_sine_defns_equiv : ∀ (v : ℝ × ℝ) (u : {p : ℝ × ℝ // p ≠ v}) (w : {p : ℝ × ℝ // p ≠ v}), half_angle_vsin (^(u - v)) (^(w - v)) = alt_half_angle_vsin v u w := by
  -- === TCCs for half_angle_sine_defns_equiv ===
  -- TCC: half_angle_sine_defns_equiv_TCC1
  -- Statement: FORALL (v: point_2d, u, w: {p: point_2d | p /= v}): w - v /= zero
  have half_angle_sine_defns_equiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.half_angle_cosine_defns_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, u, w: {p: point_2d | p /= v}):
--   half_angle_vcos(^(u - v), ^(w - v)) = alt_half_angle_vcos(v, u, w)
theorem half_angle_cosine_defns_equiv : ∀ (v : ℝ × ℝ) (u : {p : ℝ × ℝ // p ≠ v}) (w : {p : ℝ × ℝ // p ≠ v}), half_angle_vcos (^(u - v)) (^(w - v)) = alt_half_angle_vcos v u w := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.law_of_cosines_sqv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d):
--   LET a = u - v, b = w - v, c = w - u IN
--     sqv(c) = sqv(a) + sqv(b) - 2 * (a * b)
theorem law_of_cosines_sqv : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), fun (a : ℝ × ℝ) => fun (b : ℝ × ℝ) => fun (c : ℝ × ℝ) => sqv c = sqv a + sqv b - 2 * (a * b) (w - u) (w - v) (u - v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.law_of_cosines_sq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d):
--   LET a = norm(u - v), b = norm(w - v), c = norm(w - u) IN
--     sq(c) = sq(a) + sq(b) - 2 * ((u - v) * (w - v))
theorem law_of_cosines_sq : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), fun (a : {x : ℝ // x ≥ 0}) => fun (b : {x : ℝ // x ≥ 0}) => fun (c : {x : ℝ // x ≥ 0}) => (c)^2 = (a)^2 + (b)^2 - 2 * ((u - v) * (w - v)) (norm (w - u)) (norm (w - v)) (norm (u - v)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.law_of_cosines_sq_vcos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d):
--   u /= v AND w /= v IMPLIES
--    LET a = norm(u - v), b = norm(w - v), c = norm(w - u) IN
--      sq(c) = sq(a) + sq(b) - 2 * a * b * vcos(^(u - v), ^(w - v))
theorem law_of_cosines_sq_vcos : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), u ≠ v ∧ w ≠ v → (let a := norm (u - v); let b := norm (w - v); let c := norm (w - u); (c)^2 = (a)^2 + (b)^2 - 2 * a * b * vcos (^(u - v)) (^(w - v))) := by
  -- === TCCs for law_of_cosines_sq_vcos ===
  -- TCC: law_of_cosines_sq_vcos_TCC1
  -- Statement: FORALL (u, v, w: point_2d):
  --   u /= v AND w /= v IMPLIES
  --    FORALL (a: nnreal, b: nnreal, c: nnreal):
  --      a = norm(u - v) AND b = norm(w - v) AND c = norm(w - u) IMPLIES
  --       u - v /= zero
  have law_of_cosines_sq_vcos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: law_of_cosines_sq_vcos_TCC2
  -- Statement: FORALL (u, v, w: point_2d):
  --   u /= v AND w /= v IMPLIES
  --    FORALL (a: nnreal, b: nnreal, c: nnreal):
  --      a = norm(u - v) AND b = norm(w - v) AND c = norm(w - u) IMPLIES
  --       w - v /= zero
  have law_of_cosines_sq_vcos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.dot_0_eqv_perp_vectors
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: Nz_vector):
--   u * v = 0 IFF (^(v) = ^(perp_left(u)) OR ^(v) = ^(perp_right(u)))
theorem dot_0_eqv_perp_vectors : ∀ (u : {v : ℝ × ℝ // v ≠ zero}) (v : {v : ℝ × ℝ // v ≠ zero}), u * v = 0 ↔ (^(v) = ^(perp_left u) ∨ ^(v) = ^(perp_right u)) := by
  -- === TCCs for dot_0_eqv_perp_vectors ===
  -- TCC: dot_0_eqv_perp_vectors_TCC1
  -- Statement: FORALL (u, v: Nz_vector): perp_left(u) /= zero
  have dot_0_eqv_perp_vectors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dot_0_eqv_perp_vectors_TCC2
  -- Statement: FORALL (u, v: Nz_vector):
  --   NOT ^(v) = ^(perp_left(u)) IMPLIES perp_right(u) /= zero
  have dot_0_eqv_perp_vectors_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.norm_perp_sum_le_eqv_norm_le
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: vector_2d):
--   u * w = 0 AND v * w = 0 IMPLIES
--    (norm(u + w) <= norm(v + w) IFF norm(u) <= norm(v))
theorem norm_perp_sum_le_eqv_norm_le : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), u * w = 0 ∧ v * w = 0 → (norm (u + w) ≤ norm (v + w) ↔ norm u ≤ norm v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.norm_perp_diff_le_eqv_norm_le
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: vector_2d):
--   u * w = 0 AND v * w = 0 IMPLIES
--    (norm(u - w) <= norm(v - w) IFF norm(u) <= norm(v))
theorem norm_perp_diff_le_eqv_norm_le : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), u * w = 0 ∧ v * w = 0 → (norm (u - w) ≤ norm (v - w) ↔ norm u ≤ norm v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.pythagorean_path_between_points
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized):
--   EXISTS (a, b: real): q = p + a * v + b * perp_right(v)
theorem pythagorean_path_between_points : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}), ∃ (a : ℝ) (b : ℝ), q = p + a * v + b * perp_right v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.perp_ray_line_xing_components
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, c: nzreal):
--   u /= v AND u /= w IMPLIES
--    LET dir_v = ^(v - u), dir_w = ^(w - u) IN
--      NOT parallel?(dir_w, dir_v) AND dir_w * dir_v /= 0 IMPLIES
--       EXISTS (a, b: nzre
theorem perp_ray_line_xing_components : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (c : {r : ℝ // r ≠ 0}), u ≠ v ∧ u ≠ w → (let dir_v := ^(v - u); let dir_w := ^(w - u); ¬ parallel_p dir_w dir_v ∧ dir_w * dir_v ≠ 0 →
   ∃ (a : _) (b : {x : ℝ // x ≠ 0}), a = c * vcos dir_w dir_v ∧
      b = c * vsin dir_w dir_v ∧
       (c)^2 = (a)^2 + (b)^2 ∧
        c * dir_v = a * dir_w + b * perp_right dir_w) := by
  -- === TCCs for perp_ray_line_xing_components ===
  -- TCC: perp_ray_line_xing_components_TCC1
  -- Statement: FORALL (u, v, w: point_2d): u /= v AND u /= w IMPLIES v - u /= zero
  have perp_ray_line_xing_components_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: perp_ray_line_xing_components_TCC2
  -- Statement: FORALL (u, v, w: point_2d):
  --   u /= v AND u /= w IMPLIES
  --    FORALL (dir_v: Normalized): dir_v = ^(v - u) IMPLIES w - u /= zero
  have perp_ray_line_xing_components_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.segment_bisect_vector
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET uv = u - v, wv = w - v, c = cross(uv, wv), a = ^(wv) + ^(uv) IN
--   IF c = 0 THEN perp_right(uv) ELSIF c > 0 THEN a ELSE -a ENDIF
axiom segment_bisect_vector {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41 → Prop

-- pvs2lean: vectors_2d_trig.miter_point_hyp
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET s = half_angle_vsin(^(u - v), ^(w - v)),
--     bv = segment_bisect_vector(v, u, w)
--   IN v + d / s * ^(bv)
axiom miter_point_hyp {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28

-- pvs2lean: vectors_2d_trig.miter_point
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET uv = u - v,
--     wv = w - v,
--     hsin = half_angle_vsin(^(uv), ^(wv)),
--     hcos = half_angle_vcos(^(uv), ^(wv))
--   IN v + d * (hcos / hsin) * ^(wv) + d * ^(perp_left(wv))
axiom miter_point {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23

-- pvs2lean: vectors_2d_trig.miter_point_prev
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET uv = u - v,
--     wv = w - v,
--     hsin = half_angle_vsin(^(uv), ^(wv)),
--     hcos = half_angle_vcos(^(uv), ^(wv))
--   IN v + d * (hcos / hsin) * ^(uv) + d * ^(perp_right(uv))
axiom miter_point_prev {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33

-- pvs2lean: vectors_2d_trig.bisect_vector_cross_dot_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, u: {p: point_2d | p /= v},
--         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)}):
--   LET bv = ^(segment_bisect_vector(v, u, w)) IN
--     cross(^(u - v), bv) = cross(
theorem bisect_vector_cross_dot_eq : ∀ (v : ℝ × ℝ) (u : {p : ℝ × ℝ // p ≠ v}) (w : {p : ℝ × ℝ // p ≠ v ∧ ¬ dir_parallel_p (u - v) (p - v)}), fun (bv : {v : ℝ × ℝ // norm v = 1}) => cross (^(u - v)) bv = cross bv (^(w - v)) ∧ ^(u - v) * bv = bv * ^(w - v) (pvs_op_7e6a2a (segment_bisect_vector v u w)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.miter_point_defns_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, u: {p: point_2d | p /= v},
--         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)},
--         d: posreal):
--   miter_point(v, u, w, d) = miter_point_hyp(v, u, w, d)
theorem miter_point_defns_equiv : ∀ (v : ℝ × ℝ) (u : {p : ℝ × ℝ // p ≠ v}) (w : {p : ℝ × ℝ // p ≠ v ∧ ¬ dir_parallel_p (u - v) (p - v)}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), miter_point v u w d = miter_point_hyp v u w d := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: vectors_2d_trig.miter_point_defns_equiv_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: point_2d, u: {p: point_2d | p /= v},
--         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)},
--         d: posreal):
--   miter_point_prev(v, u, w, d) = miter_point_hyp(v, u, w, d)
theorem miter_point_defns_equiv_2 : ∀ (v : ℝ × ℝ) (u : {p : ℝ × ℝ // p ≠ v}) (w : {p : ℝ × ℝ // p ≠ v ∧ ¬ dir_parallel_p (u - v) (p - v)}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), miter_point_prev v u w d = miter_point_hyp v u w d := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.vectors_2d_trig