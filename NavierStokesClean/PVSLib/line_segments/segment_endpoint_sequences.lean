-- Auto-generated by pvs2lean/translator.py
-- PVS theory: segment_endpoint_sequences
-- PVSLib library: line_segments
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: segment_endpoint_sequences

Auto-generated stubs from PVSLib theory `segment_endpoint_sequences`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.segment_endpoint_sequences

noncomputable section

variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (below : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (dir_parallel_p : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ9 : Type*} (empty_p : pvs_τ9 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (half_angle_vsin : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (length : pvs_τ16 → pvs_τ17)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (min : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ31 : Type*} (norm : pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (point_on_segment_p : pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} (rem : pvs_τ36 → pvs_τ37 → pvs_τ38)
variable {pvs_τ46 : Type*} (seq : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} (sqv : pvs_τ47 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (upto : pvs_τ50 → pvs_τ51)

-- pvs2lean: segment_endpoint_sequences.segment_endpoint_cond
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: S`length > 0 AND FORALL (i: below(S`length - 1)): S`seq(i) /= S`seq(i + 1)
axiom segment_endpoint_cond {pvs_τ41 : Type*} : pvs_τ41 → Prop

-- pvs2lean: segment_endpoint_sequences.point_on_segment_seq?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF S`length = 1 THEN p = S`seq(0)
-- ELSE EXISTS (i: below(S`length - 1)):
--        point_on_segment?(p, (# p1 := S`seq(i), p2 := S`seq(i + 1) #))
-- ENDIF
axiom point_on_segment_seq_p {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ34 → pvs_τ35 → Prop

-- pvs2lean: segment_endpoint_sequences.reverse_segment_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := S`length,
--    seq := LAMBDA (i: below(S`length)): S`seq(S`length - 1 - i) #)
axiom reverse_segment_seq {pvs_τ39 : Type*} {pvs_τ40 : Type*} : pvs_τ39 → pvs_τ40

-- pvs2lean: segment_endpoint_sequences.reverse_reverse_segment_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: segment_endpoint_seq):
--   reverse_segment_seq(reverse_segment_seq(S)) = S
theorem reverse_reverse_segment_seq : ∀ (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}), reverse_segment_seq (reverse_segment_seq S) = S := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.point_on_reverse_segment_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, S: segment_endpoint_seq):
--   point_on_segment_seq?(p, S) IFF
--    point_on_segment_seq?(p, reverse_segment_seq(S))
theorem point_on_reverse_segment_seq : ∀ (p : ℝ × ℝ) (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}), point_on_segment_seq_p p S ↔ point_on_segment_seq_p p (reverse_segment_seq S) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.singleton_endpoint_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := 1, seq := LAMBDA (i: below(1)): p #)
noncomputable def singleton_endpoint_seq : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := 1, seq := LAMBDA (i: below(1)): p #)

-- pvs2lean: segment_endpoint_sequences.two_point_endpt_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF p = q THEN (# length := 1, seq := LAMBDA (i: below(1)): q #)
-- ELSE (# length := 2,
--         seq := LAMBDA (i: below(2)): IF i = 0 THEN p ELSE q ENDIF #)
-- ENDIF
noncomputable def two_point_endpt_seq : sorry := by
  sorry  -- pvs2lean: definition stub for IF p = q THEN (# length := 1, seq := LAMBDA (i: below(1)): q #) ELSE (# length :

-- pvs2lean: segment_endpoint_sequences.alternative_endpt_seqs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF P THEN X ELSE Y ENDIF
noncomputable def alternative_endpt_seqs : sorry := by
  sorry  -- pvs2lean: definition stub for IF P THEN X ELSE Y ENDIF

-- pvs2lean: segment_endpoint_sequences.segment_endpoint_subseq?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: X`length <= m AND
--  m <= Y`length AND
--   EXISTS (i: below(m)):
--     FORALL (j: below(X`length)): X`seq(j) = Y`seq(rem(m)(i + j))
axiom segment_endpoint_subseq_p {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} : pvs_τ42 → pvs_τ43 → pvs_τ44 → Prop

-- pvs2lean: segment_endpoint_sequences.append_segment_seqs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := X`length + Y`length - 1,
--    seq := (X ^^ (0, X`length - 1) o Y)`seq #)
axiom append_segment_seqs {pvs_τ1 : Type*} {pvs_τ2 : Type*} : pvs_τ1 → pvs_τ2 → Prop

-- pvs2lean: segment_endpoint_sequences.initial_point_append_segment_seqs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: segment_endpoint_seq,
--         Y: {S: segment_endpoint_seq | X`seq(X`length - 1) = S`seq(0)}):
--   append_segment_seqs(X, Y)`seq(0) = X`seq(0)
theorem initial_point_append_segment_seqs : ∀ (X : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (Y : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // X.seq (X.List.length - 1) = S.seq 0}), append_segment_seqs X Y.seq 0 = X.seq 0 := by
  -- === TCCs for initial_point_append_segment_seqs ===
  -- TCC: initial_point_append_segment_seqs_TCC1
  -- Statement: FORALL (X: segment_endpoint_seq,
  --         Y: {S: segment_endpoint_seq | X`seq(X`length - 1) = S`seq(0)}):
  --   0 < append_segment_seqs(X, Y)`length
  have initial_point_append_segment_seqs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.final_point_append_segment_seqs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: segment_endpoint_seq,
--         Y: {S: segment_endpoint_seq | X`seq(X`length - 1) = S`seq(0)}):
--   LET Z = append_segment_seqs(X, Y) IN
--     Z`seq(Z`length - 1) = Y`seq(Y`length - 1)
theorem final_point_append_segment_seqs : ∀ (X : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (Y : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // X.seq (X.List.length - 1) = S.seq 0}), fun (Z : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) => Z.seq (Z.List.length - 1) = Y.seq (Y.List.length - 1) (append_segment_seqs X Y) := by
  -- === TCCs for final_point_append_segment_seqs ===
  -- TCC: final_point_append_segment_seqs_TCC1
  -- Statement: FORALL (X: segment_endpoint_seq,
  --         Y: {S: segment_endpoint_seq | X`seq(X`length - 1) = S`seq(0)},
  --         Z: segment_endpoint_seq):
  --   Z = append_segment_seqs(X, Y) IMPLIES
  --    Z`length - 1 >= ...
  have final_point_append_segment_seqs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: final_point_append_segment_seqs_TCC2
  -- Statement: FORALL (X: segment_endpoint_seq,
  --         Y: {S: segment_endpoint_seq | X`seq(X`length - 1) = S`seq(0)},
  --         Z: segment_endpoint_seq):
  --   Z = append_segment_seqs(X, Y) IMPLIES
  --    Y`length - 1 >= ...
  have final_point_append_segment_seqs_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.point_on_append_seg_seqs_on_either
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, X: segment_endpoint_seq,
--         Y: {S: segment_endpoint_seq | X`seq(X`length - 1) = S`seq(0)}):
--   point_on_segment_seq?(p, append_segment_seqs(X, Y)) IFF
--    (point_on_segment_seq
theorem point_on_append_seg_seqs_on_either : ∀ (p : ℝ × ℝ) (X : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (Y : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // X.seq (X.List.length - 1) = S.seq 0}), point_on_segment_seq_p p (append_segment_seqs X Y) ↔ (point_on_segment_seq_p p X ∨ point_on_segment_seq_p p Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.point_on_segment_subseq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, X, Y: segment_endpoint_seq, m: posnat):
--        (X`length <= m) AND (m = Y`length - 1)
--    AND (Y`seq(0) = Y`seq(Y`length - 1)) AND point_on_segment_seq?(p, X)
--    AND segment_endpoi
theorem point_on_segment_subseq : ∀ (p : ℝ × ℝ) (X : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (Y : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}), (X.List.length ≤ m) ∧ (m = Y.List.length - 1)
 ∧ (Y.seq 0 = Y.seq (Y.List.length - 1)) ∧ point_on_segment_seq_p p X
 ∧ segment_endpoint_subseq_p X Y m → point_on_segment_seq_p p Y := by
  -- === TCCs for point_on_segment_subseq ===
  -- TCC: point_on_segment_subseq_TCC1
  -- Statement: FORALL (X, Y: segment_endpoint_seq, m: posnat):
  --   X`length <= m AND m = Y`length - 1 IMPLIES 0 < Y`length
  have point_on_segment_subseq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_segment_subseq_TCC2
  -- Statement: FORALL (X, Y: segment_endpoint_seq, m: posnat):
  --   X`length <= m AND m = Y`length - 1 IMPLIES
  --    Y`length - 1 >= 0 AND Y`length - 1 < Y`length
  have point_on_segment_subseq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.point_on_segment_on_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_on_segment_on_seq : True := by
  -- === TCCs for point_on_segment_on_seq ===
  -- TCC: point_on_segment_on_seq_TCC1
  -- Statement: FORALL (S: segment_endpoint_seq, i: below(S`length - 1)): i < S`length
  have point_on_segment_on_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_segment_on_seq_TCC2
  -- Statement: FORALL (S: segment_endpoint_seq, i: below(S`length - 1)): i + 1 < S`length
  have point_on_segment_on_seq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_segment_on_seq_TCC3
  -- Statement: FORALL (S: segment_endpoint_seq, i: below(S`length - 1)):
  --   S`seq(i + 1) /= S`seq(i)
  have point_on_segment_on_seq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segment_endpoint_sequences.point_on_segment_seq_subrange_on_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, S: segment_endpoint_seq, j: below(S`length),
--         i: upto(j)):
--   point_on_segment_seq?(p, S ^ (i, j)) IMPLIES point_on_segment_seq?(p, S)
theorem point_on_segment_seq_subrange_on_seq : ∀ (p : ℝ × ℝ) (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (j : {s : {i : ℝ // i ≥ 0} // s < S.List.length}) (i : {s : {i : ℝ // i ≥ 0} // s ≤ j}), point_on_segment_seq_p p (S ^ (i, j)) → point_on_segment_seq_p p S := by
  -- === TCCs for point_on_segment_seq_subrange_on_seq ===
  -- TCC: point_on_segment_seq_subrange_on_seq_TCC1
  -- Statement: FORALL (S: segment_endpoint_seq, j: below(S`length), i: upto(j)):
  --   (^[point_2d](S, (i, j)))`length > 0 AND
  --    FORALL (i_1: below((^[point_2d](S, (i, j)))`length - 1)):
  --      NOT (^[point_2d](S, (i,...
  have point_on_segment_seq_subrange_on_seq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.three_point_miniseq?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: u /= v AND w /= v AND NOT dir_parallel?(u - v, w - v)
axiom three_point_miniseq_p {pvs_τ48 : Type*} {pvs_τ49 : Type*} : pvs_τ48 → pvs_τ49 → Prop

-- pvs2lean: segment_endpoint_sequences.four_point_miniseq?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (u /= v) AND (w /= v) AND (x /= w)
--  AND (NOT dir_parallel?(u - v, w - v)) AND NOT dir_parallel?(v - w, x - w)
axiom four_point_miniseq_p {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ10 → pvs_τ11 → pvs_τ12 → Prop

-- pvs2lean: segment_endpoint_sequences.three_point_offset_dist?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: d <= half_angle_vsin(^(u - v), ^(w - v)) * min(norm(u - v), norm(w - v)) / 3
noncomputable def three_point_offset_dist_p : sorry := by
  sorry  -- pvs2lean: definition stub for d <= half_angle_vsin(^(u - v), ^(w - v)) * min(norm(u - v), norm(w - v)) / 3

-- pvs2lean: segment_endpoint_sequences.four_point_offset_dist?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: d <=
--  min(half_angle_vsin(^(u - v), ^(w - v)),
--      half_angle_vsin(^(v - w), ^(x - w)))
--   * min(norm(u - v), min(norm(w - v), norm(x - w)))
--   / 3
noncomputable def four_point_offset_dist_p : sorry := by
  sorry  -- pvs2lean: definition stub for d <=  min(half_angle_vsin(^(u - v), ^(w - v)),      half_angle_vsin(^(v - w), ^(

-- pvs2lean: segment_endpoint_sequences.segments_do_not_cross?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: S`length < 3 OR
--  FORALL (i, j: below(S`length - 1)):
--    LET si = (# p1 := S`seq(i), p2 := S`seq(i + 1) #),
--        sj = (# p1 := S`seq(j), p2 := S`seq(j + 1) #)
--      IN
--      FORALL (q: point_2d):
--      
axiom segments_do_not_cross_p {pvs_τ45 : Type*} : pvs_τ45 → Prop

-- pvs2lean: segment_endpoint_sequences.subseq_segments_do_not_cross
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: segment_endpoint_seq, i, j: below(S`length)):
--   segments_do_not_cross?(S) AND i <= j IMPLIES
--    segments_do_not_cross?(S ^ (i, j))
theorem subseq_segments_do_not_cross : ∀ (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length}) (j : {s : {i : ℝ // i ≥ 0} // s < S.List.length}), segments_do_not_cross_p S ∧ i ≤ j → segments_do_not_cross_p (S ^ (i, j)) := by
  -- === TCCs for subseq_segments_do_not_cross ===
  -- TCC: subseq_segments_do_not_cross_TCC1
  -- Statement: FORALL (S: segment_endpoint_seq, i, j: below(S`length)):
  --   segments_do_not_cross?(S) AND i <= j IMPLIES
  --    (^[point_2d](S, (i, j)))`length > 0 AND
  --     FORALL (i_1: below((^[point_2d](S, (i, j)))`le...
  have subseq_segments_do_not_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.single_loop_back_seq?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: S`length > 2 AND
--  ((EXISTS (i: below(S`length - 3)): S`seq(i) = S`seq(S`length - 1)) IMPLIES
--    EXISTS (i: below(S`length - 3)):
--      S`seq(i) = S`seq(S`length - 1) AND
--       FORALL (j: below(S`length
noncomputable def single_loop_back_seq_p : sorry := by
  sorry  -- pvs2lean: definition stub for S`length > 2 AND  ((EXISTS (i: below(S`length - 3)): S`seq(i) = S`seq(S`length -

-- pvs2lean: segment_endpoint_sequences.loop_back_intercept_rec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN 0
-- ELSIF n < S`length - 3 AND S`seq(n) = S`seq(S`length - 1) THEN n
-- ELSE loop_back_intercept_rec(S, n - 1)
-- ENDIF
axiom loop_back_intercept_rec {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: segment_endpoint_sequences.loop_back_intercept_rec_prop
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: (single_loop_back_seq?), n: below(S`length - 2)):
--   S`length > 3 IMPLIES
--    LET lbi = loop_back_intercept_rec(S, n) IN
--      IF FORALL (j: nat): j <= n IMPLIES S`seq(j) /= S`seq(S`length - 1
theorem loop_back_intercept_rec_prop : ∀ (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (n : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2}), S.List.length > 3 → (let lbi := loop_back_intercept_rec S n; if ∀ (j : ℕ), j ≤ n → S.seq j ≠ S.seq (S.List.length - 1)
    then lbi = 0
  else lbi < S.List.length - 3 ∧ S.seq lbi = S.seq (S.List.length - 1)
  ) := by
  -- === TCCs for loop_back_intercept_rec_prop ===
  -- TCC: loop_back_intercept_rec_prop_TCC1
  -- Statement: FORALL (S: (single_loop_back_seq?), n: below(S`length - 2)):
  --   S`length > 3 IMPLIES
  --    FORALL (lbi: below(S`length - 2)):
  --      lbi = loop_back_intercept_rec(S, n) IMPLIES
  --       FORALL (j: nat): j <...
  have loop_back_intercept_rec_prop_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: loop_back_intercept_rec_prop_TCC2
  -- Statement: FORALL (S: (single_loop_back_seq?), n: below(S`length - 2)):
  --   S`length > 3 IMPLIES
  --    FORALL (lbi: below(S`length - 2)):
  --      lbi = loop_back_intercept_rec(S, n) IMPLIES
  --       FORALL (j: nat):
  --    ...
  have loop_back_intercept_rec_prop_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: loop_back_intercept_rec_prop_TCC3
  -- Statement: FORALL (S: (single_loop_back_seq?), n: below(S`length - 2)):
  --   S`length > 3 IMPLIES
  --    FORALL (lbi: below(S`length - 2)):
  --      lbi = loop_back_intercept_rec(S, n) AND
  --       (NOT FORALL (j: nat): j ...
  have loop_back_intercept_rec_prop_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.loop_back_intercept
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom loop_back_intercept {pvs_τ18 : Type*} : pvs_τ18 → Prop

-- pvs2lean: segment_endpoint_sequences.intercept_0_cond
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: (single_loop_back_seq?)):
--   loop_back_intercept(S) = 0 IMPLIES
--    S`length = 3 OR
--     S`seq(0) = S`seq(S`length - 1) OR
--      FORALL (k: below(S`length - 3)): S`seq(k) /= S`seq(S`length - 1)
theorem intercept_0_cond : ∀ (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}), loop_back_intercept S = 0 → S.List.length = 3 ∨
 S.seq 0 = S.seq (S.List.length - 1) ∨
  ∀ (k : Finset.range S.List.length - 3), S.seq k ≠ S.seq (S.List.length - 1) := by
  -- === TCCs for intercept_0_cond ===
  -- TCC: intercept_0_cond_TCC1
  -- Statement: FORALL (S: (single_loop_back_seq?)):
  --   loop_back_intercept(S) = 0 AND NOT S`length = 3 IMPLIES
  --    S`length - 1 >= 0 AND S`length - 1 < S`length
  have intercept_0_cond_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: intercept_0_cond_TCC2
  -- Statement: FORALL (S: (single_loop_back_seq?)):
  --   loop_back_intercept(S) = 0 AND
  --    NOT S`length = 3 AND NOT S`seq(0) = S`seq(S`length - 1)
  --    IMPLIES FORALL (k: below(S`length - 3)): k < S`length
  have intercept_0_cond_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.mostly_uniq_point_list?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: below(n)):
--   i = lbi AND j = n - 1 OR
--    j = lbi AND i = n - 1 OR i = j OR seq(i) /= seq(j)
noncomputable def mostly_uniq_point_list_p : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL (i, j: below(n)):   i = lbi AND j = n - 1 OR    j = lbi AND i = n - 1 OR 

-- pvs2lean: segment_endpoint_sequences.intercept_index_value
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, k: below(S`length - 2)):
--   S`seq(k) = S`seq(S`length - 1) IMPLIES
--    k < S`length - 3 AND loop_back_intercept(S) = k
theorem intercept_index_value : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (k : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2}), S.seq k = S.seq (S.List.length - 1) → k < S.List.length - 3 ∧ loop_back_intercept S = k := by
  -- === TCCs for intercept_index_value ===
  -- TCC: intercept_index_value_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, k: below(S`length - 2)): k < S`length
  have intercept_index_value_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: intercept_index_value_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, k: below(S`length - 2)):
  --   S`length - 1 >= 0 AND S`length - 1 < S`length
  have intercept_index_value_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.adj_edge_parallel_endpts_neq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 1)):
--   S`seq(i) /= S`seq(i + 1)
theorem adj_edge_parallel_endpts_neq : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 1}), S.seq i ≠ S.seq (i + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.edge_parallel_endpts_mostly_uniq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i, j: below(S`length)):
--   S`seq(i) = S`seq(j) IMPLIES
--    LET lbi = loop_back_intercept(S) IN
--      i = j OR i = lbi AND j = S`length - 1 OR j = lbi AND i = S`leng
theorem edge_parallel_endpts_mostly_uniq : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length}) (j : {s : {i : ℝ // i ≥ 0} // s < S.List.length}), S.seq i = S.seq j → (let lbi := loop_back_intercept S; i = j ∨ i = lbi ∧ j = S.List.length - 1 ∨ j = lbi ∧ i = S.List.length - 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.adj_segs_not_dir_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2)):
--   ^(S`seq(i) - S`seq(i + 1)) /= ^(S`seq(i + 2) - S`seq(i + 1))
theorem adj_segs_not_dir_parallel : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2}), ^(S.seq i - S.seq (i + 1)) ≠ ^(S.seq (i + 2) - S.seq (i + 1)) := by
  -- === TCCs for adj_segs_not_dir_parallel ===
  -- TCC: adj_segs_not_dir_parallel_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2)):
  --   i + 1 < S`length
  have adj_segs_not_dir_parallel_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: adj_segs_not_dir_parallel_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2)):
  --   S`seq(i) - S`seq(i + 1) /= zero
  have adj_segs_not_dir_parallel_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: adj_segs_not_dir_parallel_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2)):
  --   i + 2 < S`length
  have adj_segs_not_dir_parallel_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: adj_segs_not_dir_parallel_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 2)):
  --   S`seq(i + 2) - S`seq(i + 1) /= zero
  have adj_segs_not_dir_parallel_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.wrapped_adj_segs_not_dir_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      ^(S`seq(S`length - 2) - S`seq(lbi)) /= ^(S`seq(1 + lbi) - S`seq(lbi))
--   
theorem wrapped_adj_segs_not_dir_parallel : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → ^(S.seq (S.List.length - 2) - S.seq lbi) ≠ ^(S.seq (1 + lbi) - S.seq lbi) ∧
 ^(S.seq (S.List.length - 3) - S.seq (S.List.length - 2)) ≠
  ^(S.seq lbi - S.seq (S.List.length - 2)) (loop_back_intercept S) := by
  -- === TCCs for wrapped_adj_segs_not_dir_parallel ===
  -- TCC: wrapped_adj_segs_not_dir_parallel_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have wrapped_adj_segs_not_dir_parallel_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_adj_segs_not_dir_parallel_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have wrapped_adj_segs_not_dir_parallel_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_adj_segs_not_dir_parallel_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have wrapped_adj_segs_not_dir_parallel_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_adj_segs_not_dir_parallel_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have wrapped_adj_segs_not_dir_parallel_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_adj_segs_not_dir_parallel_TCC5
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have wrapped_adj_segs_not_dir_parallel_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_adj_segs_not_dir_parallel_TCC6
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have wrapped_adj_segs_not_dir_parallel_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: wrapped_adj_segs_not_dir_parallel_TCC7
  -- Statement: FORALL (S: edge_parallel_endpoint_seq,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)
  --           ...
  have wrapped_adj_segs_not_dir_parallel_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.edge_para_endpts_valid_miter_point_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
--   S`seq(i) /= S`seq(i - 1) AND
--    S`seq(i) /= S`seq(i + 1) AND
--     NOT dir_parallel?(S`seq(i - 1) - S`seq(i), S`seq(i + 1) - 
theorem edge_para_endpts_valid_miter_point_args : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {i : {i : {i : ℝ // i ≥ 0} // i > 0} // i < S.List.length - 1}), S.seq i ≠ S.seq (i - 1) ∧ S.seq i ≠ S.seq (i + 1) ∧
 ¬ dir_parallel_p (S.seq (i - 1) - S.seq i) (S.seq (i + 1) - S.seq i) := by
  -- === TCCs for edge_para_endpts_valid_miter_point_args ===
  -- TCC: edge_para_endpts_valid_miter_point_args_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: {i: posnat | i < S`length - 1}):
  --   i - 1 >= 0 AND i - 1 < S`length
  have edge_para_endpts_valid_miter_point_args_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.min_seg_length
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min[posreal, restrict[[real, real], [posreal, posreal], bool](<=)]
--     ({len: posreal |
--         EXISTS (i: below(S`length - 1)):
--           len = norm(S`seq(i + 1) - S`seq(i))})
noncomputable def min_seg_length : sorry := by
  sorry  -- pvs2lean: definition stub for min[posreal, restrict[[real, real], [posreal, posreal], bool](<=)]     ({len: po

-- pvs2lean: segment_endpoint_sequences.eligible_seg_sep_index?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET lbi = loop_back_intercept(S) IN
--   j /= i AND
--    j /= i - 1 AND
--     (S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--       (i /= lbi OR j < S`length - 2) AND
--        (i /= S`length - 1 OR j /= lbi AND j /=
axiom eligible_seg_sep_index_p {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: segment_endpoint_sequences.min_seg_sep_vertex
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET e = (# p1 := S`seq(j), p2 := S`seq(j + 1) #),
--     v = e`p2 - e`p1,
--     vtx = S`seq(i),
--     t_min = (vtx - e`p1) * v / sqv(v)
--   IN
--   IF t_min <= 0 THEN norm(vtx - e`p1)
--   ELSIF t_min >= 1 THEN norm
axiom min_seg_sep_vertex {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27

-- pvs2lean: segment_endpoint_sequences.min_seg_separation
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min[posreal, restrict[[real, real], [posreal, posreal], bool](<=)]
--     ({v_min: posreal |
--         EXISTS (i: below(S`length), j: (eligible_seg_sep_index?(S, i))):
--           v_min = min_seg_sep_vertex(
axiom min_seg_separation {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ28 → pvs_τ29

-- pvs2lean: segment_endpoint_sequences.min_segment_sep
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min_seg_separation(S)
axiom min_segment_sep {pvs_τ30 : Type*} : pvs_τ30 → Prop

-- pvs2lean: segment_endpoint_sequences.min_segment_sep_bounds_sep_dist
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, p: point_2d, i: below(S`length),
--         j: (eligible_seg_sep_index?(S, i))):
--   point_on_segment?(p, (# p1 := S`seq(j), p2 := S`seq(j + 1) #)) IMPLIES
--    min_seg
theorem min_segment_sep_bounds_sep_dist : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (p : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length}) (j : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 1}), point_on_segment_p p ((# p1 := S.seq j, p2 := S.seq (j + 1) #)) → min_segment_sep S ≤ norm (S.seq i - p) := by
  -- === TCCs for min_segment_sep_bounds_sep_dist ===
  -- TCC: min_segment_sep_bounds_sep_dist_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length),
  --         j: (eligible_seg_sep_index?(S, i))):
  --   S`seq(j + 1) /= S`seq(j)
  have min_segment_sep_bounds_sep_dist_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.min_seg_sep_all_points
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, p, q: point_2d, i, j: nat):
--   LET lbi = loop_back_intercept(S) IN
--     i < j - 1 AND
--      j < S`length - 1 AND
--       (S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--    
theorem min_seg_sep_all_points : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (p : ℝ × ℝ) (q : ℝ × ℝ) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => i < j - 1 ∧
 j < S.List.length - 1 ∧
  (S.seq lbi = S.seq (S.List.length - 1) →
    j < S.List.length - 2 ∨ i ≠ lbi ∧ i ≠ lbi - 1) → (let g := (# p1 := S.seq i, p2 := S.seq (i + 1) #); let h := (# p1 := S.seq j, p2 := S.seq (j + 1) #); point_on_segment_p p g ∧ point_on_segment_p q h →
   min_segment_sep S ≤ norm (p - q)) (loop_back_intercept S) := by
  -- === TCCs for min_seg_sep_all_points ===
  -- TCC: min_seg_sep_all_points_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i, j: nat,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)...
  have min_seg_sep_all_points_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_seg_sep_all_points_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i, j: nat,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)...
  have min_seg_sep_all_points_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_seg_sep_all_points_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i, j: nat,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)...
  have min_seg_sep_all_points_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_seg_sep_all_points_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i, j: nat,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)...
  have min_seg_sep_all_points_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_seg_sep_all_points_TCC5
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i, j: nat,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(S`length - 1)...
  have min_seg_sep_all_points_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_seg_sep_all_points_TCC6
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, p: point_2d, i, j: nat,
  --         lbi:
  --           {i: below(S`length - 2) |
  --              IF FORALL (j: below(S`length - 3)):
  --                   S`seq(j) /= S`seq(...
  have min_seg_sep_all_points_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.min_seg_sep_bounds_seg_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 1)):
--   LET e = (# p1 := S`seq(i), p2 := S`seq(i + 1) #) IN
--     min_segment_sep(S) <= length(e)
theorem min_seg_sep_bounds_seg_length : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 1}), fun (e : (ℝ × ℝ) × (ℝ × ℝ)) => min_segment_sep S ≤ List.length e (# p1 := S.seq i) (p2 := S.seq (i + 1) #) := by
  -- === TCCs for min_seg_sep_bounds_seg_length ===
  -- TCC: min_seg_sep_bounds_seg_length_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, i: below(S`length - 1),
  --         e: [# p1: point_2d, p2: point_2d #]):
  --   e = (# p1 := S`seq(i), p2 := S`seq(i + 1) #) IMPLIES e`p2 /= e`p1
  have min_seg_sep_bounds_seg_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segment_endpoint_sequences.min_seg_sep_bounds_seg_length_wrap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq):
--   LET lbi = loop_back_intercept(S) IN
--     S`seq(lbi) = S`seq(S`length - 1) IMPLIES
--      min_segment_sep(S) <= norm(S`seq(S`length - 2) - S`seq(lbi)) AND
--       m
theorem min_seg_sep_bounds_seg_length_wrap : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}), fun (lbi : {i : {s : {i : ℝ // i ≥ 0} // s < S.List.length - 2} // if ∀ (j : Finset.range S.List.length - 3), S.seq j ≠ S.seq (S.List.length - 1)
  then i = 0
else i < S.List.length - 3 ∧ S.seq i = S.seq (S.List.length - 1)}) => S.seq lbi = S.seq (S.List.length - 1) → min_segment_sep S ≤ norm (S.seq (S.List.length - 2) - S.seq lbi) ∧
 min_segment_sep S ≤ norm (S.seq (1 + lbi) - S.seq lbi) (loop_back_intercept S) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.segment_endpoint_sequences