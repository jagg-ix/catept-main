-- Auto-generated by pvs2lean/translator.py
-- PVS theory: segments_2d_props
-- PVSLib library: line_segments
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: segments_2d_props

Auto-generated stubs from PVSLib theory `segments_2d_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.segments_2d_props

noncomputable section

variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (are_vectors_collinear_p : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (below : pvs_τ5 → pvs_τ6)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cross : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (dir_parallel_p : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (min : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ22 : Type*} (norm : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (parallel_p : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (perp_left : pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} (perp_right : pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (point_on_segment_p : pvs_τ31 → pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} (reverse_segment : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (segment_endpoint_p : pvs_τ34 → pvs_τ35 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (segment_intersect_kernel : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (seq : pvs_τ42 → pvs_τ43)

-- pvs2lean: segments_2d_props.subsegment?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: point_on_segment?(s`p1, e) AND point_on_segment?(s`p2, e)
axiom subsegment_p {pvs_τ44 : Type*} {pvs_τ45 : Type*} : pvs_τ44 → pvs_τ45 → Prop

-- pvs2lean: segments_2d_props.dir_subsegment?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subsegment?(s, e) AND dir_parallel?(s`p2 - s`p1, e`p2 - e`p1)
noncomputable def dir_subsegment_p : sorry := by
  sorry  -- pvs2lean: definition stub for subsegment?(s, e) AND dir_parallel?(s`p2 - s`p1, e`p2 - e`p1)

-- pvs2lean: segments_2d_props.point_on_subsegment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   point_on_segment?(p, s) AND subsegment?(s, e) IMPLIES
--    point_on_segment?(p, e)
theorem point_on_subsegment : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p s ∧ subsegment_p s e → point_on_segment_p p e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.proper_subsegment_excludes_p1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   subsegment?(s, e) AND NOT segment_endpoint?(e)(s`p1) IMPLIES
--    EXISTS (d: posreal): s`p1 = e`p1 + d * (e`p2 - e`p1) AND d < 1
theorem proper_subsegment_excludes_p1 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p s e ∧ ¬ segment_endpoint_p e(s.p1) → ∃ (d : {x : ℝ // 0 < x}), s.p1 = e.p1 + d * (e.p2 - e.p1) ∧ d < 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.proper_subsegment_excludes_p2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   subsegment?(s, e) AND NOT segment_endpoint?(e)(s`p2) IMPLIES
--    EXISTS (d: posreal): s`p2 = e`p1 + d * (e`p2 - e`p1) AND d < 1
theorem proper_subsegment_excludes_p2 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p s e ∧ ¬ segment_endpoint_p e(s.p2) → ∃ (d : {x : ℝ // 0 < x}), s.p2 = e.p1 + d * (e.p2 - e.p1) ∧ d < 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.proper_subsegment_excludes_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   point_on_segment?(p, s) AND
--    subsegment?(s, e) AND
--     NOT segment_endpoint?(e)(s`p1) AND NOT segment_endpoint?(e)(s`p2)
--    IMPLIES NOT segment_endpoint?(e)
theorem proper_subsegment_excludes_endpoints : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p s ∧
 subsegment_p s e ∧
  ¬ segment_endpoint_p e(s.p1) ∧ ¬ segment_endpoint_p e(s.p2) → ¬ segment_endpoint_p e(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.segment_endpoint_subsegment_endpoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   point_on_segment?(p, s) AND subsegment?(s, e) AND segment_endpoint?(e)(p)
--    IMPLIES segment_endpoint?(s)(p)
theorem segment_endpoint_subsegment_endpoint : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p s ∧ subsegment_p s e ∧ segment_endpoint_p e(p) → segment_endpoint_p s(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.point_on_segment_split
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_on_segment_split : True := by
  -- === TCCs for point_on_segment_split ===
  -- TCC: point_on_segment_split_TCC1
  -- Statement: FORALL (p, q: point_2d, s: segment_2d):
  --        point_on_segment?(p, s) AND (p /= s`p1) AND (p /= s`p2)
  --    AND point_on_segment?(q, s)
  --    AND NOT point_on_segment?(q, (# p1 := s`p1, p2 := p #))
  --    I...
  have point_on_segment_split_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.point_on_initial_subsegment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_on_initial_subsegment : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.point_on_final_subsegment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_on_final_subsegment : True := by
  -- === TCCs for point_on_final_subsegment ===
  -- TCC: point_on_final_subsegment_TCC1
  -- Statement: FORALL (p, q: point_2d, s: segment_2d):
  --   point_on_segment?(p, s) AND
  --    point_on_segment?(q, s) AND
  --     norm(s`p2 - p) <= norm(s`p2 - q) AND q /= s`p2
  --    IMPLIES s`p2 /= q
  have point_on_final_subsegment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.subsegment_same_normalized_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   subsegment?(s, e) IMPLIES
--    ^(s`p2 - s`p1) = ^(e`p2 - e`p1) OR ^(s`p2 - s`p1) = -^(e`p2 - e`p1)
theorem subsegment_same_normalized_diff : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p s e → ^(s.p2 - s.p1) = ^(e.p2 - e.p1) ∨ ^(s.p2 - s.p1) = -^(e.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.reverse_segment_has_neg_norm_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   e = reverse_segment(s) IMPLIES ^(s`p2 - s`p1) = -^(e`p2 - e`p1)
theorem reverse_segment_has_neg_norm_diff : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), e = reverse_segment s → ^(s.p2 - s.p1) = -^(e.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.points_on_segment_heads
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem points_on_segment_heads : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.points_on_segment_tails
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem points_on_segment_tails : True := by
  -- === TCCs for points_on_segment_tails ===
  -- TCC: points_on_segment_tails_TCC1
  -- Statement: FORALL (p, q: point_2d, s: segment_2d):
  --   point_on_segment?(p, s) AND
  --    point_on_segment?(q, s) AND p /= s`p2 AND q /= s`p2
  --    IMPLIES s`p2 /= q
  have points_on_segment_tails_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: points_on_segment_tails_TCC2
  -- Statement: FORALL (p, q: point_2d, s: segment_2d):
  --        point_on_segment?(p, s) AND point_on_segment?(q, s) AND (p /= s`p2)
  --    AND (q /= s`p2) AND NOT point_on_segment?(p, (# p1 := q, p2 := s`p2 #))
  --    IMPL...
  have points_on_segment_tails_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.heads_on_aligned_segments_are_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   point_on_segment?(s`p1, e) AND
--    point_on_segment?(e`p1, s) AND ^(s`p2 - s`p1) = ^(e`p2 - e`p1)
--    IMPLIES s`p1 = e`p1
theorem heads_on_aligned_segments_are_eq : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p s.p1 e ∧
 point_on_segment_p e.p1 s ∧ ^(s.p2 - s.p1) = ^(e.p2 - e.p1) → s.p1 = e.p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.tails_on_aligned_segments_are_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   point_on_segment?(s`p2, e) AND
--    point_on_segment?(e`p2, s) AND ^(s`p2 - s`p1) = ^(e`p2 - e`p1)
--    IMPLIES s`p2 = e`p2
theorem tails_on_aligned_segments_are_eq : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p s.p2 e ∧
 point_on_segment_p e.p2 s ∧ ^(s.p2 - s.p1) = ^(e.p2 - e.p1) → s.p2 = e.p2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.on_parallel_segment_within_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, e: segment_2d, d: nonneg_real):
--   point_on_segment?(p + d * v, e) AND parallel?(e`p2 - e`p1, v) IMPLIES
--    EXISTS (a: real, b: nonneg_real):
--      a <= d AND
--       d
theorem on_parallel_segment_within_endpoints : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (d : {x : ℝ // x ≥ 0}), point_on_segment_p (p + d * v) e ∧ parallel_p (e.p2 - e.p1) v → ∃ (a : ℝ) (b : NNReal), a ≤ d ∧
   d ≤ b ∧
    (e.p1 = p + a * v ∧ e.p2 = p + b * v ∨
      e.p2 = p + a * v ∧ e.p1 = p + b * v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.points_on_same_segment_dir_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, e: segment_2d):
--   point_on_segment?(p, e) AND
--    point_on_segment?(q, e) AND norm(p - e`p1) < norm(q - e`p1)
--    IMPLIES dir_parallel?(q - p, e`p2 - e`p1)
theorem points_on_same_segment_dir_parallel : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p e ∧
 point_on_segment_p q e ∧ norm (p - e.p1) < norm (q - e.p1) → dir_parallel_p (q - p) (e.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.points_on_same_segment_norm_diffs_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, e: segment_2d):
--   point_on_segment?(p, e) AND
--    point_on_segment?(q, e) AND norm(p - e`p1) < norm(q - e`p1)
--    IMPLIES ^(q - p) = ^(e`p2 - e`p1)
theorem points_on_same_segment_norm_diffs_eq : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p e ∧
 point_on_segment_p q e ∧ norm (p - e.p1) < norm (q - e.p1) → ^(q - p) = ^(e.p2 - e.p1) := by
  -- === TCCs for points_on_same_segment_norm_diffs_eq ===
  -- TCC: points_on_same_segment_norm_diffs_eq_TCC1
  -- Statement: FORALL (p, q: point_2d, e: segment_2d):
  --   point_on_segment?(p, e) AND
  --    point_on_segment?(q, e) AND norm(p - e`p1) < norm(q - e`p1)
  --    IMPLIES q - p /= zero
  have points_on_same_segment_norm_diffs_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.point_on_segment_on_subsegment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_on_segment_on_subsegment : True := by
  -- === TCCs for point_on_segment_on_subsegment ===
  -- TCC: point_on_segment_on_subsegment_TCC1
  -- Statement: FORALL (S: finseq[point_2d], e: segment_2d, n: nat):
  --   n = S`length AND S`length > 1 IMPLIES 0 < S`length
  have point_on_segment_on_subsegment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_segment_on_subsegment_TCC2
  -- Statement: FORALL (S: finseq[point_2d], e: segment_2d, n: nat):
  --   n = S`length AND S`length > 1 IMPLIES
  --    S`length - 1 >= 0 AND S`length - 1 < S`length
  have point_on_segment_on_subsegment_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_segment_on_subsegment_TCC3
  -- Statement: FORALL (p: point_2d, S: finseq[point_2d], e: segment_2d, n: nat):
  --        (n = S`length) AND (S`length > 1)
  --    AND (e = (# p1 := S`seq(0), p2 := S`seq(S`length - 1) #))
  --    AND (FORALL (i: below(S`le...
  have point_on_segment_on_subsegment_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_segment_on_subsegment_TCC4
  -- Statement: FORALL (p: point_2d, S: finseq[point_2d], e: segment_2d, n: nat):
  --        (n = S`length) AND (S`length > 1)
  --    AND (e = (# p1 := S`seq(0), p2 := S`seq(S`length - 1) #))
  --    AND (FORALL (i: below(S`le...
  have point_on_segment_on_subsegment_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_on_segment_on_subsegment_TCC5
  -- Statement: FORALL (p: point_2d, S: finseq[point_2d], e: segment_2d, n: nat):
  --        (n = S`length) AND (S`length > 1)
  --    AND (e = (# p1 := S`seq(0), p2 := S`seq(S`length - 1) #))
  --    AND (FORALL (i: below(S`le...
  have point_on_segment_on_subsegment_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.horiz_segment_straddles_point
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d, d: posreal):
--   e`p1`y = e`p2`y AND
--    point_on_segment?(p + (# x := d, y := 0 #), e) AND
--     min(e`p1`x, e`p2`x) <= p`x
--    IMPLIES point_on_segment?(p, e)
theorem horiz_segment_straddles_point : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), e.p1.y = e.p2.y ∧
 point_on_segment_p (p + (# x := d, y := 0 #)) e ∧
  min e.p1.x e.p2.x ≤ p.x → point_on_segment_p p e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.endpoint_on_straddling_horiz_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem endpoint_on_straddling_horiz_segment : True := by
  -- === TCCs for endpoint_on_straddling_horiz_segment ===
  -- TCC: endpoint_on_straddling_horiz_segment_TCC1
  -- Statement: FORALL (p, q: point_2d, e: segment_2d):
  --        (e`p1`y = e`p2`y) AND (p`y = q`y) AND (e`p1`y = p`y)
  --    AND (p`x <= e`p1`x) AND (p`x < q`x) AND (e`p1`x < e`p2`x)
  --    AND point_on_segment?(q, e)
  --    IM...
  have endpoint_on_straddling_horiz_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.segment_rising?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s`p1`y < s`p2`y
noncomputable def segment_rising_p : sorry := by
  sorry  -- pvs2lean: definition stub for s`p1`y < s`p2`y

-- pvs2lean: segments_2d_props.segment_falling?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s`p2`y < s`p1`y
axiom segment_falling_p {pvs_τ36 : Type*} : pvs_τ36 → Prop

-- pvs2lean: segments_2d_props.off_right_side?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cross(p - s`p1, s`p2 - s`p1) > 0
noncomputable def off_right_side_p : sorry := by
  sorry  -- pvs2lean: definition stub for cross(p - s`p1, s`p2 - s`p1) > 0

-- pvs2lean: segments_2d_props.extended_segment
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# p1 := s`p1, p2 := (1 + k) * s`p2 - k * s`p1 #)
axiom extended_segment {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15

-- pvs2lean: segments_2d_props.endpoint_on_extended_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, k: nnreal):
--   point_on_segment?(s`p2, extended_segment(s, k))
theorem endpoint_on_extended_segment : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (k : {x : ℝ // x ≥ 0}), point_on_segment_p s.p2 (extended_segment s k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.are_segments_overlapping?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (p1, p2: point_2d):
--        (p1 /= p2) AND point_on_segment?(p1, s1)
--    AND point_on_segment?(p2, s1) AND point_on_segment?(p1, s2)
--    AND point_on_segment?(p2, s2)
axiom are_segments_overlapping_p {pvs_τ1 : Type*} {pvs_τ2 : Type*} : pvs_τ1 → pvs_τ2 → Prop

-- pvs2lean: segments_2d_props.are_segments_overlapping_refl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d): are_segments_overlapping?(s)(s)
theorem are_segments_overlapping_refl : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_overlapping_p s s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.are_segments_overlapping_symm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   are_segments_overlapping?(s1)(s2) IFF are_segments_overlapping?(s2)(s1)
theorem are_segments_overlapping_symm : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_overlapping_p s1(s2) ↔ are_segments_overlapping_p s2(s1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, p: point_2d):
--   point_on_segment?(p, s) AND p /= s`p1 IMPLIES
--    parallel?(s`p2 - s`p1, p - s`p1)
theorem subsegment_parallel : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), point_on_segment_p p s ∧ p ≠ s.p1 → parallel_p (s.p2 - s.p1) (p - s.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_parallel_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, p1, p2: point_2d):
--   point_on_segment?(p1, s) AND point_on_segment?(p2, s) AND p1 /= p2
--    IMPLIES parallel?(s`p2 - s`p1, p2 - p1)
theorem subsegment_parallel_2 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p1 : ℝ × ℝ) (p2 : ℝ × ℝ), point_on_segment_p p1 s ∧ point_on_segment_p p2 s ∧ p1 ≠ p2 → parallel_p (s.p2 - s.p1) (p2 - p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.segments_parallel_imp_subsegments_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ss, ee, s, e: segment_2d):
--   parallel?(ss`p2 - ss`p1, ee`p2 - ee`p1) AND
--    subsegment?(s, ss) AND subsegment?(e, ee)
--    IMPLIES parallel?(s`p2 - s`p1, e`p2 - e`p1)
theorem segments_parallel_imp_subsegments_parallel : ∀ (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ee : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), parallel_p (ss.p2 - ss.p1) (ee.p2 - ee.p1) ∧
 subsegment_p s ss ∧ subsegment_p e ee → parallel_p (s.p2 - s.p1) (e.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.overlapping_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   are_segments_overlapping?(s1)(s2) IMPLIES
--    parallel?(s1`p2 - s1`p1, s2`p2 - s2`p1)
theorem overlapping_parallel : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_overlapping_p s1(s2) → parallel_p (s1.p2 - s1.p1) (s2.p2 - s2.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.overlapping_normalized_directions
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   are_segments_overlapping?(s1)(s2) IMPLIES
--    ^(s1`p2 - s1`p1) = ^(s2`p2 - s2`p1) OR
--     ^(s1`p2 - s1`p1) = -^(s2`p2 - s2`p1)
theorem overlapping_normalized_directions : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_overlapping_p s1(s2) → ^(s1.p2 - s1.p1) = ^(s2.p2 - s2.p1) ∨ ^(s1.p2 - s1.p1) = -^(s2.p2 - s2.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.parallel_segments_equiv_collinear
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   parallel?(s1`p2 - s1`p1, s2`p2 - s2`p1) IFF
--    are_vectors_collinear?(s1`p2 - s1`p1, s2`p2 - s2`p1)
theorem parallel_segments_equiv_collinear : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), parallel_p (s1.p2 - s1.p1) (s2.p2 - s2.p1) ↔ are_vectors_collinear_p (s1.p2 - s1.p1) (s2.p2 - s2.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.non_overlapping_segments_intersect_once
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d, p1, p2: point_2d):
--        (NOT are_segments_overlapping?(s1)(s2)) AND point_on_segment?(p1, s1)
--    AND point_on_segment?(p2, s1) AND point_on_segment?(p1, s2)
--    AND point_
theorem non_overlapping_segments_intersect_once : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p1 : ℝ × ℝ) (p2 : ℝ × ℝ), (¬ are_segments_overlapping_p s1(s2)) ∧ point_on_segment_p p1 s1
 ∧ point_on_segment_p p2 s1 ∧ point_on_segment_p p1 s2
 ∧ point_on_segment_p p2 s2 → p1 = p2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.overlapping_subsegments_and_segments
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, ss, ee: segment_2d):
--   are_segments_overlapping?(s)(e) AND
--    subsegment?(s, ss) AND subsegment?(e, ee)
--    IMPLIES are_segments_overlapping?(ss)(ee)
theorem overlapping_subsegments_and_segments : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ee : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_overlapping_p s(e) ∧
 subsegment_p s ss ∧ subsegment_p e ee → are_segments_overlapping_p ss(ee) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.reverse_segment_overlapping
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   are_segments_overlapping?(s)(reverse_segment(e)) IFF
--    are_segments_overlapping?(s)(e)
theorem reverse_segment_overlapping : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_overlapping_p s(reverse_segment e) ↔ are_segments_overlapping_p s(e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.parallel_segments_interior_point_overlapping
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   parallel?(s`p2 - s`p1, e`p2 - e`p1) AND
--    p = e`p1 AND point_on_segment?(p, s) AND NOT segment_endpoint?(s)(p)
--    IMPLIES are_segments_overlapping?(s)(e)
theorem parallel_segments_interior_point_overlapping : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), parallel_p (s.p2 - s.p1) (e.p2 - e.p1) ∧
 p = e.p1 ∧ point_on_segment_p p s ∧ ¬ segment_endpoint_p s(p) → are_segments_overlapping_p s(e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.non_overlapping_parallel_segments_with_endpt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   parallel?(s`p2 - s`p1, e`p2 - e`p1) AND
--    point_on_segment?(p, s) AND
--     point_on_segment?(p, e) AND NOT are_segments_overlapping?(s)(e)
--    IMPLIES segment_
theorem non_overlapping_parallel_segments_with_endpt : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), parallel_p (s.p2 - s.p1) (e.p2 - e.p1) ∧
 point_on_segment_p p s ∧
  point_on_segment_p p e ∧ ¬ are_segments_overlapping_p s(e) → segment_endpoint_p s(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.non_overlapping_parallel_segments_share_endpoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   parallel?(s`p2 - s`p1, e`p2 - e`p1) AND
--    point_on_segment?(p, s) AND
--     point_on_segment?(p, e) AND NOT are_segments_overlapping?(s)(e)
--    IMPLIES segment_
theorem non_overlapping_parallel_segments_share_endpoint : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), parallel_p (s.p2 - s.p1) (e.p2 - e.p1) ∧
 point_on_segment_p p s ∧
  point_on_segment_p p e ∧ ¬ are_segments_overlapping_p s(e) → segment_endpoint_p s(p) ∧ segment_endpoint_p e(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.contained_overlap?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (p: point_2d):
--    point_on_segment?(p, s1) IMPLIES point_on_segment?(p, s2))
--  OR
--  (FORALL (p: point_2d):
--     point_on_segment?(p, s2) IMPLIES point_on_segment?(p, s1))
axiom contained_overlap_p {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: segments_2d_props.partial_overlap?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (p, q: point_2d):
--   point_on_segment?(p, s1) AND
--    NOT point_on_segment?(p, s2) AND
--     NOT point_on_segment?(q, s1) AND point_on_segment?(q, s2)
axiom partial_overlap_p {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: segments_2d_props.contained_or_partial_overlap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   contained_overlap?(s1, s2) OR partial_overlap?(s1, s2)
theorem contained_or_partial_overlap : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), contained_overlap_p s1 s2 ∨ partial_overlap_p s1 s2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.min_overlap_point
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF point_on_segment?(s1`p1, s2) THEN s1`p1
-- ELSIF ^(s1`p2 - s1`p1) = ^(s2`p2 - s2`p1) THEN s2`p1
-- ELSE s2`p2
-- ENDIF
axiom min_overlap_point {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ20 → pvs_τ21 → Prop

-- pvs2lean: segments_2d_props.max_overlap_point
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF point_on_segment?(s1`p2, s2) THEN s1`p2
-- ELSIF ^(s1`p2 - s1`p1) = ^(s2`p2 - s2`p1) THEN s2`p2
-- ELSE s2`p1
-- ENDIF
axiom max_overlap_point {pvs_τ16 : Type*} {pvs_τ17 : Type*} : pvs_τ16 → pvs_τ17 → Prop

-- pvs2lean: segments_2d_props.overlap_endpoints_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   point_on_segment?(s1`p1, s2) OR
--    point_on_segment?(s2`p1, s1) OR point_on_segment?(s2`p2, s1)
theorem overlap_endpoints_first : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p s1.p1 s2 ∨ point_on_segment_p s2.p1 s1 ∨ point_on_segment_p s2.p2 s1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.overlap_endpoints_second
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   point_on_segment?(s1`p2, s2) OR
--    point_on_segment?(s2`p1, s1) OR point_on_segment?(s2`p2, s1)
theorem overlap_endpoints_second : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p s1.p2 s2 ∨ point_on_segment_p s2.p1 s1 ∨ point_on_segment_p s2.p2 s1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.min_overlap_point_props
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   LET p = min_overlap_point(s1, s2) IN
--     point_on_segment?(p, s1) AND
--      point_on_segment?(p, s2) AND
--       FORALL (q: point_2d):
--     
theorem min_overlap_point_props : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), fun (p : ℝ × ℝ) => point_on_segment_p p s1 ∧ point_on_segment_p p s2 ∧
 ∀ (q : point_2d), point_on_segment_p q s1 ∧ point_on_segment_p q s2 →
    norm (p - s1.p1) ≤ norm (q - s1.p1) (min_overlap_point s1 s2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.max_overlap_point_props
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   LET p = max_overlap_point(s1, s2) IN
--     point_on_segment?(p, s1) AND
--      point_on_segment?(p, s2) AND
--       FORALL (q: point_2d):
--     
theorem max_overlap_point_props : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), fun (p : ℝ × ℝ) => point_on_segment_p p s1 ∧ point_on_segment_p p s2 ∧
 ∀ (q : point_2d), point_on_segment_p q s1 ∧ point_on_segment_p q s2 →
    norm (p - s1.p1) ≥ norm (q - s1.p1) (max_overlap_point s1 s2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.min_max_overlap_points_neq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   min_overlap_point(s1, s2) /= max_overlap_point(s1, s2)
theorem min_max_overlap_points_neq : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), min_overlap_point s1 s2 ≠ max_overlap_point s1 s2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.min_overlap_point_neq_p2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   min_overlap_point(s1, s2) /= s1`p2
theorem min_overlap_point_neq_p2 : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), min_overlap_point s1 s2 ≠ s1.p2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.max_overlap_point_neq_p1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   max_overlap_point(s1, s2) /= s1`p1
theorem max_overlap_point_neq_p1 : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), max_overlap_point s1 s2 ≠ s1.p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.min_overlap_point_offset_lt_len
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   norm(min_overlap_point(s1, s2) - s1`p1) < norm(s1`p2 - s1`p1)
theorem min_overlap_point_offset_lt_len : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), norm (min_overlap_point s1 s2 - s1.p1) < norm (s1.p2 - s1.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.max_overlap_point_offset_gt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   0 < norm(max_overlap_point(s1, s2) - s1`p1)
theorem max_overlap_point_offset_gt_0 : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), 0 < norm (max_overlap_point s1 s2 - s1.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_min_overlap_point_eq_p1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)), ss: segment_2d):
--   ^(s`p2 - s`p1) = ^(ss`p2 - ss`p1) AND
--    subsegment?(s, ss) AND min_overlap_point(s, e) = ss`p1
--    IMPLIES s`p1 = ss`p1
theorem subsegment_min_overlap_point_eq_p1 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ^(s.p2 - s.p1) = ^(ss.p2 - ss.p1) ∧
 subsegment_p s ss ∧ min_overlap_point s e = ss.p1 → s.p1 = ss.p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_min_overlap_points_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)), ss: segment_2d,
--         ee: (are_segments_overlapping?(ss))):
--   ^(s`p2 - s`p1) = ^(ss`p2 - ss`p1) AND
--    subsegment?(s, ss) AND
--     subsegment
theorem subsegment_min_overlap_points_eq : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ee : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ^(s.p2 - s.p1) = ^(ss.p2 - ss.p1) ∧
 subsegment_p s ss ∧
  subsegment_p e ee ∧ min_overlap_point s e = ss.p1 → s.p1 = min_overlap_point ss ee := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_max_overlap_point_eq_p2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)), ss: segment_2d):
--   ^(s`p2 - s`p1) = ^(ss`p2 - ss`p1) AND
--    subsegment?(s, ss) AND max_overlap_point(s, e) = ss`p2
--    IMPLIES s`p2 = ss`p2
theorem subsegment_max_overlap_point_eq_p2 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ^(s.p2 - s.p1) = ^(ss.p2 - ss.p1) ∧
 subsegment_p s ss ∧ max_overlap_point s e = ss.p2 → s.p2 = ss.p2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_max_overlap_points_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)), ss: segment_2d,
--         ee: (are_segments_overlapping?(ss))):
--   ^(s`p2 - s`p1) = ^(ss`p2 - ss`p1) AND
--    subsegment?(s, ss) AND
--     subsegment
theorem subsegment_max_overlap_points_eq : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ee : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ^(s.p2 - s.p1) = ^(ss.p2 - ss.p1) ∧
 subsegment_p s ss ∧
  subsegment_p e ee ∧ max_overlap_point s e = ss.p2 → s.p2 = max_overlap_point ss ee := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_min_overlap_point_neq_p2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)), ss: segment_2d):
--   subsegment?(s, ss) AND ^(s`p2 - s`p1) = ^(ss`p2 - ss`p1) IMPLIES
--    min_overlap_point(s, e) /= ss`p2
theorem subsegment_min_overlap_point_neq_p2 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p s ss ∧ ^(s.p2 - s.p1) = ^(ss.p2 - ss.p1) → min_overlap_point s e ≠ ss.p2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_max_overlap_point_neq_p1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)), ss: segment_2d):
--   subsegment?(s, ss) AND ^(s`p2 - s`p1) = ^(ss`p2 - ss`p1) IMPLIES
--    max_overlap_point(s, e) /= ss`p1
theorem subsegment_max_overlap_point_neq_p1 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p s ss ∧ ^(s.p2 - s.p1) = ^(ss.p2 - ss.p1) → max_overlap_point s e ≠ ss.p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.min_overlap_pt_in_initial_subsegment
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, a: segment_2d, b: (are_segments_overlapping?(a))):
--   point_on_segment?(p, a) AND point_on_segment?(p, b) AND p /= a`p1 IMPLIES
--    point_on_segment?(min_overlap_point(a, b), (# p1 
theorem min_overlap_pt_in_initial_subsegment : ∀ (p : ℝ × ℝ) (a : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (b : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p a ∧ point_on_segment_p p b ∧ p ≠ a.p1 → point_on_segment_p (min_overlap_point a b) ((# p1 := a.p1, p2 := p #)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.max_overlap_pt_in_final_subsegment
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, a: segment_2d, b: (are_segments_overlapping?(a))):
--   point_on_segment?(p, a) AND point_on_segment?(p, b) AND p /= a`p2 IMPLIES
--    point_on_segment?(max_overlap_point(a, b), (# p1 
theorem max_overlap_pt_in_final_subsegment : ∀ (p : ℝ × ℝ) (a : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (b : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p a ∧ point_on_segment_p p b ∧ p ≠ a.p2 → point_on_segment_p (max_overlap_point a b) ((# p1 := p, p2 := a.p2 #)) := by
  -- === TCCs for max_overlap_pt_in_final_subsegment ===
  -- TCC: max_overlap_pt_in_final_subsegment_TCC1
  -- Statement: FORALL (p: point_2d, a: segment_2d, b: (are_segments_overlapping?(a))):
  --   point_on_segment?(p, a) AND point_on_segment?(p, b) AND p /= a`p2 IMPLIES
  --    a`p2 /= p
  have max_overlap_pt_in_final_subsegment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.all_overlapping_points_bounded
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, s1: segment_2d,
--         s2: (are_segments_overlapping?(s1))):
--   point_on_segment?(p, s1) AND
--    point_on_segment?(p, s2) AND
--     point_on_segment?(q, s1) AND point_on_segment?(
theorem all_overlapping_points_bounded : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p s1 ∧
 point_on_segment_p p s2 ∧
  point_on_segment_p q s1 ∧ point_on_segment_p q s2 → (let overlap := (# p1 := min_overlap_point s1 s2,
         p2 := max_overlap_point s1 s2 #); point_on_segment_p p overlap ∧ point_on_segment_p q overlap) := by
  -- === TCCs for all_overlapping_points_bounded ===
  -- TCC: all_overlapping_points_bounded_TCC1
  -- Statement: FORALL (p, q: point_2d, s1: segment_2d,
  --         s2: (are_segments_overlapping?(s1))):
  --   point_on_segment?(p, s1) AND
  --    point_on_segment?(p, s2) AND
  --     point_on_segment?(q, s1) AND point_on_segmen...
  have all_overlapping_points_bounded_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.only_one_overlapping_segment
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   point_on_segment?(p,
--                     (# p1 := min_overlap_point(s1, s2),
--                        p2 := max_overlap_point
theorem only_one_overlapping_segment : ∀ (p : ℝ × ℝ) (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p ((# p1 := min_overlap_point s1 s2,
                     p2 := max_overlap_point s1 s2 #)) → point_on_segment_p p s1 ∧ point_on_segment_p p s2 := by
  -- === TCCs for only_one_overlapping_segment ===
  -- TCC: only_one_overlapping_segment_TCC1
  -- Statement: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
  --   max_overlap_point(s1, s2) /= min_overlap_point(s1, s2)
  have only_one_overlapping_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.min_overlap_point_symm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   min_overlap_point(s1, s2) =
--    IF ^(s1`p2 - s1`p1) = ^(s2`p2 - s2`p1) THEN min_overlap_point(s2, s1)
--    ELSE max_overlap_point(s2, s1)
--  
theorem min_overlap_point_symm : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), min_overlap_point s1 s2 = if ^(s1.p2 - s1.p1) = ^(s2.p2 - s2.p1) then min_overlap_point s2 s1
else max_overlap_point s2 s1 := by
  -- === TCCs for min_overlap_point_symm ===
  -- TCC: min_overlap_point_symm_TCC1
  -- Statement: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
  --   ^(s1`p2 - s1`p1) = ^(s2`p2 - s2`p1) IMPLIES
  --    are_segments_overlapping?(s2)(s1)
  have min_overlap_point_symm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_overlap_point_symm_TCC2
  -- Statement: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
  --   NOT ^(s1`p2 - s1`p1) = ^(s2`p2 - s2`p1) IMPLIES
  --    are_segments_overlapping?(s2)(s1)
  have min_overlap_point_symm_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.max_overlap_point_symm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: (are_segments_overlapping?(s1))):
--   max_overlap_point(s1, s2) =
--    IF ^(s1`p2 - s1`p1) = ^(s2`p2 - s2`p1) THEN max_overlap_point(s2, s1)
--    ELSE min_overlap_point(s2, s1)
--  
theorem max_overlap_point_symm : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), max_overlap_point s1 s2 = if ^(s1.p2 - s1.p1) = ^(s2.p2 - s2.p1) then max_overlap_point s2 s1
else min_overlap_point s2 s1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegments_overlap_min_max_subsegments
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)),
--         ss, ee: segment_2d):
--   subsegment?(s, ss) AND subsegment?(e, ee) IMPLIES
--    subsegment?((# p1 := min_overlap_point(s, e),
--             
theorem subsegments_overlap_min_max_subsegments : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ee : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p s ss ∧ subsegment_p e ee → subsegment_p ((# p1 := min_overlap_point s e,
               p2 := max_overlap_point s e #)) ((# p1 := min_overlap_point ss ee,
               p2 := max_overlap_point ss ee #)) := by
  -- === TCCs for subsegments_overlap_min_max_subsegments ===
  -- TCC: subsegments_overlap_min_max_subsegments_TCC1
  -- Statement: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)),
  --         ss, ee: segment_2d):
  --   subsegment?(s, ss) AND subsegment?(e, ee) IMPLIES
  --    are_segments_overlapping?(ss)(ee)
  have subsegments_overlap_min_max_subsegments_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subsegments_overlap_min_max_subsegments_TCC2
  -- Statement: FORALL (s: segment_2d, e: (are_segments_overlapping?(s)),
  --         ss, ee: segment_2d):
  --   subsegment?(s, ss) AND subsegment?(e, ee) IMPLIES
  --    max_overlap_point(ss, ee) /= min_overlap_point(ss, ee)
  have subsegments_overlap_min_max_subsegments_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.shorter_segment_not_overlapping
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q: point_2d, s1, s2, sq: segment_2d):
--   point_on_segment?(q, s1) AND
--    sq = (# p1 := q, p2 := s1`p2 #) AND
--     NOT are_segments_overlapping?(s1)(s2)
--    IMPLIES NOT are_segments_overlapping?(s
theorem shorter_segment_not_overlapping : ∀ (q : ℝ × ℝ) (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (sq : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p q s1 ∧
 sq = (# p1 := q, p2 := s1.p2 #) ∧ ¬ are_segments_overlapping_p s1(s2) → ¬ are_segments_overlapping_p sq(s2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.overlap_straddles_interior_point
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: segment_2d, b: (are_segments_overlapping?(a)), p: point_2d):
--   point_on_segment?(p, a) AND
--    point_on_segment?(p, b) AND
--     NOT segment_endpoint?(a)(p) AND NOT segment_endpoint?(b)(p)
--    
theorem overlap_straddles_interior_point : ∀ (a : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (b : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), point_on_segment_p p a ∧
 point_on_segment_p p b ∧
  ¬ segment_endpoint_p a(p) ∧ ¬ segment_endpoint_p b(p) → p ≠ min_overlap_point a b ∧ p ≠ max_overlap_point a b := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.overlap_semi_straddles_interior_point
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: segment_2d, b: (are_segments_overlapping?(a)), p: point_2d):
--   point_on_segment?(p, a) AND
--    point_on_segment?(p, b) AND NOT segment_endpoint?(a)(p)
--    IMPLIES p /= min_overlap_point(a, b)
theorem overlap_semi_straddles_interior_point : ∀ (a : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (b : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), point_on_segment_p p a ∧
 point_on_segment_p p b ∧ ¬ segment_endpoint_p a(p) → p ≠ min_overlap_point a b ∨ p ≠ max_overlap_point a b := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.are_segments_overlapping_trans_aligned
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c: segment_2d, p: point_2d):
--        are_segments_overlapping?(a)(b) AND are_segments_overlapping?(b)(c)
--    AND (^(a`p2 - a`p1) = ^(b`p2 - b`p1)) AND point_on_segment?(p, a)
--    AND point_
theorem are_segments_overlapping_trans_aligned : ∀ (a : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (b : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (c : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), are_segments_overlapping_p a(b) ∧ are_segments_overlapping_p b(c)
 ∧ (^(a.p2 - a.p1) = ^(b.p2 - b.p1)) ∧ point_on_segment_p p a
 ∧ point_on_segment_p p b ∧ point_on_segment_p p c
 ∧ (¬ segment_endpoint_p a(p)) ∧ ¬ segment_endpoint_p b(p) → are_segments_overlapping_p a(c) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.are_segments_overlapping_trans
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c: segment_2d, p: point_2d):
--        are_segments_overlapping?(a)(b) AND are_segments_overlapping?(b)(c)
--    AND point_on_segment?(p, a) AND point_on_segment?(p, b)
--    AND point_on_segment
theorem are_segments_overlapping_trans : ∀ (a : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (b : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (c : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), are_segments_overlapping_p a(b) ∧ are_segments_overlapping_p b(c)
 ∧ point_on_segment_p p a ∧ point_on_segment_p p b
 ∧ point_on_segment_p p c ∧ (¬ segment_endpoint_p a(p))
 ∧ ¬ segment_endpoint_p b(p) → are_segments_overlapping_p a(c) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.segments_overlapping_implies_kernel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   are_segments_overlapping?(s1)(s2) IMPLIES
--    segment_intersect_kernel(s1, s2)`1 = Collinear_Overlapping
theorem segments_overlapping_implies_kernel : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_overlapping_p s1(s2) → segment_intersect_kernel s1 s2.1 = Collinear_Overlapping := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.segments_share_an_endpoint
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (s1`p1 = s2`p1) OR (s1`p1 = s2`p2) OR (s1`p2 = s2`p1) OR (s1`p2 = s2`p2)
axiom segments_share_an_endpoint {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41

-- pvs2lean: segments_2d_props.kernel_eq_segments_overlapping_unless_shared_endpt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   (NOT segments_share_an_endpoint(s1, s2)) IMPLIES
--    (are_segments_overlapping?(s1)(s2) IFF
--      segment_intersect_kernel(s1, s2)`1 = Collinear_Overlapping)
theorem kernel_eq_segments_overlapping_unless_shared_endpt : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (¬ segments_share_an_endpoint s1 s2) → (are_segments_overlapping_p s1(s2) ↔
  segment_intersect_kernel s1 s2.1 = Collinear_Overlapping) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.perp_right_segment_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d): perp_right(s`p2 - s`p1) /= zero
theorem perp_right_segment_nonzero : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), perp_right (s.p2 - s.p1) ≠ zero := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.perp_left_segment_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d): perp_left(s`p2 - s`p1) /= zero
theorem perp_left_segment_nonzero : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), perp_left (s.p2 - s.p1) ≠ zero := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.endpoint_norm_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q: point_2d, e: segment_2d):
--   point_on_segment?(q, e) IMPLIES norm(q - e`p1) <= norm(e`p2 - e`p1)
theorem endpoint_norm_bound : ∀ (q : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p q e → norm (q - e.p1) ≤ norm (e.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.endpoint_norm_bound_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q: point_2d, e: segment_2d):
--   point_on_segment?(q, e) AND q /= e`p2 IMPLIES
--    norm(q - e`p1) < norm(e`p2 - e`p1)
theorem endpoint_norm_bound_lt : ∀ (q : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p q e ∧ q ≠ e.p2 → norm (q - e.p1) < norm (e.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.subsegment_p1_closest_to_segment_p1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   subsegment?(s, e) AND
--    point_on_segment?(p, s) AND ^(e`p2 - e`p1) = ^(s`p2 - s`p1)
--    IMPLIES norm(s`p1 - e`p1) <= norm(p - e`p1)
theorem subsegment_p1_closest_to_segment_p1 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), subsegment_p s e ∧
 point_on_segment_p p s ∧ ^(e.p2 - e.p1) = ^(s.p2 - s.p1) → norm (s.p1 - e.p1) ≤ norm (p - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.non_overlapping_collinear_segments
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, h: segment_2d):
--        subsegment?(s, h) AND subsegment?(e, h)
--    AND (^(h`p2 - h`p1) = ^(s`p2 - s`p1))
--    AND (^(h`p2 - h`p1) = ^(e`p2 - e`p1))
--    AND (norm(s`p2 - h`p1) <= norm(e`p1 - 
theorem non_overlapping_collinear_segments : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p s h ∧ subsegment_p e h
 ∧ (^(h.p2 - h.p1) = ^(s.p2 - s.p1)) ∧ (^(h.p2 - h.p1) = ^(e.p2 - e.p1))
 ∧ (norm (s.p2 - h.p1) ≤ norm (e.p1 - h.p1)) → ¬ are_segments_overlapping_p s(e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.between_points_on_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, e: segment_2d, d: nonneg_real):
--   point_on_segment?(p, e) AND
--    point_on_segment?(q, e) AND d < norm(p - q) AND p /= q
--    IMPLIES point_on_segment?(q + d * ^(p - q), e)
theorem between_points_on_segment : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (d : {x : ℝ // x ≥ 0}), point_on_segment_p p e ∧
 point_on_segment_p q e ∧ d < norm (p - q) ∧ p ≠ q → point_on_segment_p (q + d * ^(p - q)) e := by
  -- === TCCs for between_points_on_segment ===
  -- TCC: between_points_on_segment_TCC1
  -- Statement: FORALL (p, q: point_2d, e: segment_2d, d: nonneg_real):
  --   point_on_segment?(p, e) AND
  --    point_on_segment?(q, e) AND d < norm(p - q) AND p /= q
  --    IMPLIES p - q /= zero
  have between_points_on_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.between_points_on_subsegment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem between_points_on_subsegment : True := by
  -- === TCCs for between_points_on_subsegment ===
  -- TCC: between_points_on_subsegment_TCC1
  -- Statement: FORALL (p, q: point_2d, d: nonneg_real):
  --   p /= q AND d < norm(p - q) IMPLIES p - q /= zero
  have between_points_on_subsegment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.endpoint_not_on_subsegment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem endpoint_not_on_subsegment : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.between_points_not_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem between_points_not_endpoints : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.point_on_segment_cross_is_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d):
--   point_on_segment?(p, e) IMPLIES cross(p - e`p1, e`p2 - e`p1) = 0
theorem point_on_segment_cross_is_0 : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), point_on_segment_p p e → cross (p - e.p1) (e.p2 - e.p1) = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.cross_is_0_point_on_segments
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d):
--   cross(p - e`p1, e`p2 - e`p1) = 0 AND norm(p - e`p1) <= norm(e`p2 - e`p1)
--    IMPLIES
--    point_on_segment?(p, e) OR
--     point_on_segment?(p, (# p1 := e`p1, p2 := 2
theorem cross_is_0_point_on_segments : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), cross (p - e.p1) (e.p2 - e.p1) = 0 ∧ norm (p - e.p1) ≤ norm (e.p2 - e.p1) → point_on_segment_p p e ∨
 point_on_segment_p p ((# p1 := e.p1, p2 := 2 * e.p1 - e.p2 #)) := by
  -- === TCCs for cross_is_0_point_on_segments ===
  -- TCC: cross_is_0_point_on_segments_TCC1
  -- Statement: FORALL (p: point_2d, e: segment_2d):
  --   cross(p - e`p1, e`p2 - e`p1) = 0 AND
  --    norm(p - e`p1) <= norm(e`p2 - e`p1) AND NOT point_on_segment?(p, e)
  --    IMPLIES 2 * e`p1 - e`p2 /= e`p1
  have cross_is_0_point_on_segments_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.rev_cross_is_0_point_on_segments
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d):
--   cross(e`p2 - e`p1, p - e`p1) = 0 AND norm(p - e`p1) <= norm(e`p2 - e`p1)
--    IMPLIES
--    point_on_segment?(p, e) OR
--     point_on_segment?(p, (# p1 := e`p1, p2 := 2
theorem rev_cross_is_0_point_on_segments : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), cross (e.p2 - e.p1) (p - e.p1) = 0 ∧ norm (p - e.p1) ≤ norm (e.p2 - e.p1) → point_on_segment_p p e ∨
 point_on_segment_p p ((# p1 := e.p1, p2 := 2 * e.p1 - e.p2 #)) := by
  -- === TCCs for rev_cross_is_0_point_on_segments ===
  -- TCC: rev_cross_is_0_point_on_segments_TCC1
  -- Statement: FORALL (p: point_2d, e: segment_2d):
  --   cross(e`p2 - e`p1, p - e`p1) = 0 AND
  --    norm(p - e`p1) <= norm(e`p2 - e`p1) AND NOT point_on_segment?(p, e)
  --    IMPLIES 2 * e`p1 - e`p2 /= e`p1
  have rev_cross_is_0_point_on_segments_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.all_segment_points_have_pos_cross_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s: segment_2d, v: vector_2d):
--   point_on_segment?(p, s) AND p /= s`p1 IMPLIES
--    (cross(p - s`p1, v) > 0 IFF cross(s`p2 - s`p1, v) > 0)
theorem all_segment_points_have_pos_cross_1 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), point_on_segment_p p s ∧ p ≠ s.p1 → (cross (p - s.p1) v > 0 ↔ cross (s.p2 - s.p1) v > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.all_segment_points_have_pos_cross_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s: segment_2d, v: vector_2d):
--   point_on_segment?(p, s) AND p /= s`p1 IMPLIES
--    (cross(v, p - s`p1) > 0 IFF cross(v, s`p2 - s`p1) > 0)
theorem all_segment_points_have_pos_cross_2 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), point_on_segment_p p s ∧ p ≠ s.p1 → (cross v (p - s.p1) > 0 ↔ cross v (s.p2 - s.p1) > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.all_segment_points_have_pos_cross_3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s: segment_2d, v: point_2d):
--   point_on_segment?(p, s) AND p /= s`p1 IMPLIES
--    (cross(p - v, s`p1 - v) > 0 IFF cross(s`p2 - v, s`p1 - v) > 0)
theorem all_segment_points_have_pos_cross_3 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), point_on_segment_p p s ∧ p ≠ s.p1 → (cross (p - v) (s.p1 - v) > 0 ↔ cross (s.p2 - v) (s.p1 - v) > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.all_segment_points_have_pos_cross_4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s: segment_2d, v: point_2d):
--   point_on_segment?(p, s) AND p /= s`p2 IMPLIES
--    (cross(s`p2 - v, p - v) > 0 IFF cross(s`p2 - v, s`p1 - v) > 0)
theorem all_segment_points_have_pos_cross_4 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), point_on_segment_p p s ∧ p ≠ s.p2 → (cross (s.p2 - v) (p - v) > 0 ↔ cross (s.p2 - v) (s.p1 - v) > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.pos_cross_falling_segment_horiz_points
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, s: segment_2d):
--   cross(p - s`p1, s`p2 - s`p1) > 0 AND
--    segment_falling?(s) AND point_on_segment?(q, s) AND p`y = q`y
--    IMPLIES p`x < q`x
theorem pos_cross_falling_segment_horiz_points : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), cross (p - s.p1) (s.p2 - s.p1) > 0 ∧
 segment_falling_p s ∧ point_on_segment_p q s ∧ p.y = q.y → p.x < q.x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.falling_segment_horiz_points_pos_cross
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, s: segment_2d):
--   segment_falling?(s) AND
--    point_on_segment?(q, s) AND p`x < q`x AND p`y = q`y
--    IMPLIES cross(p - s`p1, s`p2 - s`p1) > 0
theorem falling_segment_horiz_points_pos_cross : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), segment_falling_p s ∧ point_on_segment_p q s ∧ p.x < q.x ∧ p.y = q.y → cross (p - s.p1) (s.p2 - s.p1) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.complementary_angle_cross_pos_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, u, v, w: point_2d):
--        (u /= w) AND (u /= p) AND (w /= p)
--    AND point_on_segment?(p, (# p1 := u, p2 := w #))
--    AND (cross(v - p, u - p) > 0)
--    IMPLIES cross(w - p, v - p) > 0
theorem complementary_angle_cross_pos_1 : ∀ (p : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), (u ≠ w) ∧ (u ≠ p) ∧ (w ≠ p)
 ∧ point_on_segment_p p ((# p1 := u, p2 := w #))
 ∧ (cross (v - p) (u - p) > 0) → cross (w - p) (v - p) > 0 := by
  -- === TCCs for complementary_angle_cross_pos_1 ===
  -- TCC: complementary_angle_cross_pos_1_TCC1
  -- Statement: FORALL (p, u, w: point_2d): u /= w AND u /= p AND w /= p IMPLIES w /= u
  have complementary_angle_cross_pos_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.complementary_angle_cross_pos_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, u, v, w: point_2d):
--        (u /= w) AND (u /= p) AND (w /= p)
--    AND point_on_segment?(p, (# p1 := u, p2 := w #))
--    AND (cross(w - p, v - p) > 0)
--    IMPLIES cross(v - p, u - p) > 0
theorem complementary_angle_cross_pos_2 : ∀ (p : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), (u ≠ w) ∧ (u ≠ p) ∧ (w ≠ p)
 ∧ point_on_segment_p p ((# p1 := u, p2 := w #))
 ∧ (cross (w - p) (v - p) > 0) → cross (v - p) (u - p) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.endpts_cross_pos_imp_pt_on_seg_cross_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v, w, p, q, r: point_2d):
--   cross(v - w, p - w) > 0 AND
--    cross(v - w, q - w) > 0 AND
--     p /= q AND point_on_segment?(r, (# p1 := p, p2 := q #))
--    IMPLIES cross(v - w, r - w) > 0
theorem endpts_cross_pos_imp_pt_on_seg_cross_pos : ∀ (v : ℝ × ℝ) (w : ℝ × ℝ) (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ), cross (v - w) (p - w) > 0 ∧
 cross (v - w) (q - w) > 0 ∧
  p ≠ q ∧ point_on_segment_p r ((# p1 := p, p2 := q #)) → cross (v - w) (r - w) > 0 := by
  -- === TCCs for endpts_cross_pos_imp_pt_on_seg_cross_pos ===
  -- TCC: endpts_cross_pos_imp_pt_on_seg_cross_pos_TCC1
  -- Statement: FORALL (v, w, p, q: point_2d):
  --   cross(v - w, p - w) > 0 AND cross(v - w, q - w) > 0 AND p /= q IMPLIES
  --    q /= p
  have endpts_cross_pos_imp_pt_on_seg_cross_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.triangle_split_angles_cross_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w, p, q, r: point_2d):
--        (cross(w - u, v - u) > 0) AND (p /= w) AND (q /= u) AND (p /= q)
--    AND (r /= p) AND (r /= q)
--    AND point_on_segment?(p, (# p1 := w, p2 := u #))
--    AND poi
theorem triangle_split_angles_cross_pos : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ), (cross (w - u) (v - u) > 0) ∧ (p ≠ w) ∧ (q ≠ u) ∧ (p ≠ q)
 ∧ (r ≠ p) ∧ (r ≠ q) ∧ point_on_segment_p p ((# p1 := w, p2 := u #))
 ∧ point_on_segment_p q ((# p1 := u, p2 := v #))
 ∧ point_on_segment_p r ((# p1 := p, p2 := q #)) → cross (r - w) (u - w) > 0 ∧ cross (v - w) (r - w) > 0 := by
  -- === TCCs for triangle_split_angles_cross_pos ===
  -- TCC: triangle_split_angles_cross_pos_TCC1
  -- Statement: FORALL (u, v, w, p, q, r: point_2d):
  --        (cross(w - u, v - u) > 0) AND (p /= w) AND (q /= u) AND (p /= q)
  --    AND (r /= p) AND (r /= q)
  --    IMPLIES u /= w
  have triangle_split_angles_cross_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: triangle_split_angles_cross_pos_TCC2
  -- Statement: FORALL (u, v, w, p, q, r: point_2d):
  --        (cross(w - u, v - u) > 0) AND (p /= w) AND (q /= u) AND (p /= q)
  --    AND (r /= p) AND (r /= q)
  --    AND point_on_segment?(p, (# p1 := w, p2 := u #))
  --    IMPL...
  have triangle_split_angles_cross_pos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: triangle_split_angles_cross_pos_TCC3
  -- Statement: FORALL (u, v, w, p, q, r: point_2d):
  --        (cross(w - u, v - u) > 0) AND (p /= w) AND (q /= u) AND (p /= q)
  --    AND (r /= p) AND (r /= q)
  --    AND point_on_segment?(p, (# p1 := w, p2 := u #))
  --    AND ...
  have triangle_split_angles_cross_pos_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.triangle_cross_pos_below
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: vector_2d):
--        (v`y < 0) AND (w`y < 0) AND (0 < u`y) AND (cross(u, w) > 0)
--    AND (cross(v, u) > 0)
--    IMPLIES cross(w, v) > 0
theorem triangle_cross_pos_below : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), (v.y < 0) ∧ (w.y < 0) ∧ (0 < u.y) ∧ (cross u w > 0)
 ∧ (cross v u > 0) → cross w v > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.triangle_cross_pos_split_y
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c, u: point_2d):
--        (b`y < u`y) AND (c`y < u`y) AND (u`y < a`y)
--    AND (cross(a - u, c - u) > 0) AND (cross(b - u, a - u) > 0)
--    IMPLIES cross(c - u, b - u) > 0
theorem triangle_cross_pos_split_y : ∀ (a : ℝ × ℝ) (b : ℝ × ℝ) (c : ℝ × ℝ) (u : ℝ × ℝ), (b.y < u.y) ∧ (c.y < u.y) ∧ (u.y < a.y)
 ∧ (cross (a - u) (c - u) > 0) ∧ (cross (b - u) (a - u) > 0) → cross (c - u) (b - u) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.triangle_cross_pos_above
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: vector_2d):
--        (v`y < 0) AND (0 < w`y) AND (0 < u`y) AND (cross(w, v) > 0)
--    AND (cross(v, u) > 0)
--    IMPLIES cross(u, w) > 0
theorem triangle_cross_pos_above : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), (v.y < 0) ∧ (0 < w.y) ∧ (0 < u.y) ∧ (cross w v > 0)
 ∧ (cross v u > 0) → cross u w > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.isosceles_dist_to_opp_side_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem isosceles_dist_to_opp_side_bound : True := by
  -- === TCCs for isosceles_dist_to_opp_side_bound ===
  -- TCC: isosceles_dist_to_opp_side_bound_TCC1
  -- Statement: FORALL (u, v, w: point_2d):
  --   u /= w AND norm(u - v) = norm(w - v) IMPLIES w /= u
  have isosceles_dist_to_opp_side_bound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: segments_2d_props.two_segments_w_same_st_pt_and_one_more_pt_same_normalized
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: {s: segment_2d | s`p1 = s1`p1}):
--   (EXISTS (q: {p: point_2d | p /= s1`p1}):
--      point_on_segment?(q, s1) AND point_on_segment?(q, s2))
--    IMPLIES ^(s1) = ^(s2)
theorem two_segments_w_same_st_pt_and_one_more_pt_same_normalized : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : {s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1}) // s.p1 = s1.p1}), (∃ (q : {x : point_2d // x ≠ s1.x1}), point_on_segment_p q s1 ∧ point_on_segment_p q s2) → ^(s1) = ^(s2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_props.two_segments_w_opp_end_pt_and_one_more_pt_same_antinormalized
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1: segment_2d, s2: {s: segment_2d | s`p2 = s1`p1}):
--   (EXISTS (q: {p: point_2d | p /= s1`p1}):
--      point_on_segment?(q, s1) AND point_on_segment?(q, s2))
--    IMPLIES ^(s1) = -^(s2)
theorem two_segments_w_opp_end_pt_and_one_more_pt_same_antinormalized : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : {s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1}) // s.p2 = s1.p1}), (∃ (q : {x : point_2d // x ≠ s1.x1}), point_on_segment_p q s1 ∧ point_on_segment_p q s2) → ^(s1) = -^(s2) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.segments_2d_props