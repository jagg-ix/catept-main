-- Auto-generated by pvs2lean/translator.py
-- PVS theory: perp_crossings
-- PVSLib library: line_segments
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: perp_crossings

Auto-generated stubs from PVSLib theory `perp_crossings`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.perp_crossings

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Some : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (pvs_op_7e6a2a : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (are_segments_intersecting_alt_p : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (are_segments_intersecting_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (are_segments_strictly_intersecting_p : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (below : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (collinear_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (cross : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (is_point_on_segment_exclusive_p : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} (length : pvs_τ29 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (min : pvs_τ38 → pvs_τ39 → pvs_τ40)
variable {pvs_τ44 : Type*} (norm : pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (on_line_p : pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (opposite_endpoint : pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} (orthogonal_p : pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (parallel_p : pvs_τ53 → pvs_τ54 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} (perp_pt : pvs_τ57 → pvs_τ58 → pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} (perp_right : pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (point_beyond_segment_p : pvs_τ62 → pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (point_on_segment_p : pvs_τ64 → pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} (reverse_segment : pvs_τ66 → pvs_τ67)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} (segment_2d_to_Line : pvs_τ68 → pvs_τ69)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} (segment_endpoint_p : pvs_τ70 → pvs_τ71 → Prop)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} (segment_intersect_kernel : pvs_τ72 → pvs_τ73 → pvs_τ74)
variable {pvs_τ75 : Type*} (singleton_seq : pvs_τ75 → Prop)
variable {pvs_τ76 : Type*} (uniq_vertex_seq_with_empty_p : pvs_τ76 → Prop)
variable {pvs_τ77 : Type*} (val : pvs_τ77 → Prop)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} (vcos : pvs_τ78 → pvs_τ79 → Prop)

-- pvs2lean: perp_crossings.line_crossing
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET denom =
--       (s`p1`x - s`p2`x) * (e`p1`y - e`p2`y) -
--        (s`p1`y - s`p2`y) * (e`p1`x - e`p2`x)
--   IN
--   IF denom = 0 THEN None
--   ELSE Some(LET x =
--                   ((s`p1`x * s`p2`y - s`p1`y * 
axiom line_crossing {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: perp_crossings.line_crossing_alt
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET ds = s`p1 - s`p2, de = e`p1 - e`p2, denom = cross(ds, de) IN
--   IF denom = 0 THEN None
--   ELSE Some((cross(s`p1, s`p2) / denom) * de -
--              (cross(e`p1, e`p2) / denom) * ds)
--   ENDIF
axiom line_crossing_alt {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34

-- pvs2lean: perp_crossings.line_crossing_eq_line_crossing_alt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d): line_crossing(s, e) = line_crossing_alt(s, e)
theorem line_crossing_eq_line_crossing_alt : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), line_crossing s e = line_crossing_alt s e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.line_crossing_alt2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET ds = s`p2 - s`p1, de = e`p2 - e`p1, denom = cross(ds, de) IN
--   IF denom = 0 THEN None
--   ELSE Some(s`p1 + (cross(e`p1 - s`p1, e`p2 - e`p1) / denom) * ds)
--   ENDIF
axiom line_crossing_alt2 {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} : pvs_τ35 → pvs_τ36 → pvs_τ37

-- pvs2lean: perp_crossings.line_crossing_alt_eq_line_crossing_alt2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   line_crossing_alt(s, e) = line_crossing_alt2(s, e)
theorem line_crossing_alt_eq_line_crossing_alt2 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), line_crossing_alt s e = line_crossing_alt2 s e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.no_line_crossing_eqv_parallel_or_collinear
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   line_crossing(s, e) = None IFF (parallel?(s)(e) OR collinear?(s, e))
theorem no_line_crossing_eqv_parallel_or_collinear : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), line_crossing s e = None ↔ (parallel_p s(e) ∨ collinear_p s e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.line_crossing_commutative
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d): line_crossing(s, e) = line_crossing(e, s)
theorem line_crossing_commutative : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), line_crossing s e = line_crossing e s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.line_crossing_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   line_crossing(s, e) = line_crossing(s, reverse_segment(e))
theorem line_crossing_reverse : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), line_crossing s e = line_crossing s (reverse_segment e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.line_crossing_on_line
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   LET line_xing: Maybe[point_2d] = line_crossing(s, e) IN
--     line_xing = None OR
--      EXISTS (k: real): val(line_xing) = s`p1 + k * (s`p2 - s`p1)
theorem line_crossing_on_line : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), fun (line_xing : Maybe (ℝ × ℝ)) => line_xing = None ∨ ∃ (k : ℝ), val line_xing = s.p1 + k * (s.p2 - s.p1) (line_crossing s e) := by
  -- === TCCs for line_crossing_on_line ===
  -- TCC: line_crossing_on_line_TCC1
  -- Statement: FORALL (s, e: segment_2d, line_xing: Maybe[point_2d]):
  --   line_xing = line_crossing(s, e) AND NOT line_xing = None IMPLIES
  --    some?[point_2d](line_xing)
  have line_crossing_on_line_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.extending_s_crosses_e
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET line_xing: Maybe[point_2d] = line_crossing(s, e) IN
--   IF line_xing = None THEN FALSE
--   ELSE point_on_segment?(val(line_xing), e)
--   ENDIF
axiom extending_s_crosses_e {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ18 → pvs_τ19 → Prop

-- pvs2lean: perp_crossings.collinear_imp_not_extending_crosses
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   collinear?(s, e) IMPLIES NOT extending_s_crosses_e(s, e)
theorem collinear_imp_not_extending_crosses : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), collinear_p s e → ¬ extending_s_crosses_e s e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.intersection_point_for_each_line
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET delta = e`p1 - s`p1,
--        denom = cross(s`p2 - s`p1, e`p2 - e`p1),
--        s_t = cross(delta, e`p2 - e`p1) / deno
theorem intersection_point_for_each_line : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let delta := e.p1 - s.p1; let denom := cross (s.p2 - s.p1) (e.p2 - e.p1); let s_t := cross delta (e.p2 - e.p1) / denom; let e_t := cross delta (s.p2 - s.p1) / denom; s.p1 + s_t * (s.p2 - s.p1) = e.p1 + e_t * (e.p2 - e.p1)) := by
  -- === TCCs for intersection_point_for_each_line ===
  -- TCC: intersection_point_for_each_line_TCC1
  -- Statement: FORALL (s, e: segment_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (delta: Vector, denom: real):
  --      delta = e`p1 - s`p1 AND denom = cross(s`p2 - s`p1, e`p2 - e`p1)
  --       ...
  have intersection_point_for_each_line_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.line_crossing_is_unique
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) AND
--    (EXISTS (k: real): p = s`p1 + k * (s`p2 - s`p1)) AND
--     (EXISTS (k: real): p = e`p1 + k * (e`p2 - e`p1))
--    
theorem line_crossing_is_unique : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) ∧
 (∃ (k : ℝ), p = s.p1 + k * (s.p2 - s.p1)) ∧
  (∃ (k : ℝ), p = e.p1 + k * (e.p2 - e.p1)) → p = val (line_crossing s e) := by
  -- === TCCs for line_crossing_is_unique ===
  -- TCC: line_crossing_is_unique_TCC1
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND
  --     (EXISTS (k: real): p = s`p1 + k * (s`p2 - s`p1)) AND
  --      (EXISTS (k: real): p = e`p1 + k * (e`p2 -...
  have line_crossing_is_unique_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.segment_crossing_is_unique
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) AND
--    point_on_segment?(p, s) AND point_on_segment?(p, e)
--    IMPLIES p = val(line_crossing(s, e))
theorem segment_crossing_is_unique : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) ∧
 point_on_segment_p p s ∧ point_on_segment_p p e → p = val (line_crossing s e) := by
  -- === TCCs for segment_crossing_is_unique ===
  -- TCC: segment_crossing_is_unique_TCC1
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND
  --     point_on_segment?(p, s) AND point_on_segment?(p, e)
  --    IMPLIES some?[point_2d](line_crossing(s, e))
  have segment_crossing_is_unique_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.collinear_segments_same_line_crossing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, h: segment_2d):
--   collinear?(e, h) IMPLIES line_crossing(s, e) = line_crossing(s, h)
theorem collinear_segments_same_line_crossing : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), collinear_p e h → line_crossing s e = line_crossing s h := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_or_extending_crosses
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--        parallel?(s)(e) OR collinear?(s, e) OR extending_s_crosses_e(s, e)
--    OR extending_s_crosses_e(e, s)
--    OR NOT are_segments_intersecting_alt?(s, e)
theorem parallel_or_extending_crosses : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), parallel_p s(e) ∨ collinear_p s e ∨
 extending_s_crosses_e s e ∨
  extending_s_crosses_e e s ∨ ¬ are_segments_intersecting_alt_p s e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.are_segments_intersecting?_defs_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   are_segments_intersecting?(s1)(s2) =
--    are_segments_intersecting_alt?(s1, s2)
theorem are_segments_intersecting_p_defs_same : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_intersecting_p s1(s2) = are_segments_intersecting_alt_p s1 s2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.are_segments_intersecting?_sym
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   are_segments_intersecting?(s1)(s2) = are_segments_intersecting?(s2)(s1)
theorem are_segments_intersecting_p_sym : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_intersecting_p s1(s2) = are_segments_intersecting_p s2(s1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_lines_cross
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem perp_lines_cross : True := by
  -- === TCCs for perp_lines_cross ===
  -- TCC: perp_lines_cross_TCC1
  -- Statement: FORALL (s, e: segment_2d, p: point_2d): p + perp_right(e`p2 - e`p1) /= p
  have perp_lines_cross_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: perp_crossings.collinear_perp_line_crossings_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem collinear_perp_line_crossings_same : True := by
  -- === TCCs for collinear_perp_line_crossings_same ===
  -- TCC: collinear_perp_line_crossings_same_TCC1
  -- Statement: FORALL (s, e: segment_2d, p: point_2d): p + perp_right(s`p2 - s`p1) /= p
  have collinear_perp_line_crossings_same_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: perp_crossings.collinear_imp_seg_line_crossings_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, g, h: segment_2d):
--   collinear?(s, g) AND collinear?(e, h) IMPLIES
--    line_crossing(s, e) = line_crossing(g, h)
theorem collinear_imp_seg_line_crossings_same : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (g : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), collinear_p s g ∧ collinear_p e h → line_crossing s e = line_crossing g h := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_line_crossing_on_other_line
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem perp_line_crossing_on_other_line : True := by
  -- === TCCs for perp_line_crossing_on_other_line ===
  -- TCC: perp_line_crossing_on_other_line_TCC1
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s)), q: point_2d, a, b: real,
  --         dir_s: Nz_vector, perp_s: [# p1: point_2d, p2: Vector #]):
  --   dir_s = s`p2 - s`p1 AND
  --    perp_s = (# p1 := q, p2 := q + per...
  have perp_line_crossing_on_other_line_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: perp_line_crossing_on_other_line_TCC2
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s)), q: point_2d, a, b: real,
  --         dir_s: Nz_vector, perp_s: [# p1: point_2d, p2: Vector #]):
  --   dir_s = s`p2 - s`p1 AND
  --    perp_s = (# p1 := q, p2 := q + per...
  have perp_line_crossing_on_other_line_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: perp_crossings.find_perp_ray_crossing
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET e_perp_rt: segment_2d =
--       (# p1 := p, p2 := p + perp_right(e`p2 - e`p1) #)
--   IN
--   LET line_xing = line_crossing(e_perp_rt, e) IN
--     IF point_on_segment?(val(line_xing), e) THEN line_xing ELSE
axiom find_perp_ray_crossing {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ20 → pvs_τ21 → Prop

-- pvs2lean: perp_crossings.find_perp_ray_line_crossing
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET e_perp_rt: segment_2d =
--       (# p1 := p, p2 := p + perp_right(e`p2 - e`p1) #)
--   IN val(line_crossing(e_perp_rt, e))
axiom find_perp_ray_line_crossing {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ22 → pvs_τ23 → Prop

-- pvs2lean: perp_crossings.perp_ray_crossing_is_perp_ray_line_crossing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, v: point_2d):
--   find_perp_ray_crossing(e, v) /= None IMPLIES
--    val(find_perp_ray_crossing(e, v)) = find_perp_ray_line_crossing(e, v)
theorem perp_ray_crossing_is_perp_ray_line_crossing : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), find_perp_ray_crossing e v ≠ None → val (find_perp_ray_crossing e v) = find_perp_ray_line_crossing e v := by
  -- === TCCs for perp_ray_crossing_is_perp_ray_line_crossing ===
  -- TCC: perp_ray_crossing_is_perp_ray_line_crossing_TCC1
  -- Statement: FORALL (e: segment_2d, v: point_2d):
  --   find_perp_ray_crossing(e, v) /= None IMPLIES
  --    some?[point_2d](find_perp_ray_crossing(e, v))
  have perp_ray_crossing_is_perp_ray_line_crossing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_ray_line_crossing_distance_exists
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, v: point_2d):
--   EXISTS (b: real):
--     find_perp_ray_line_crossing(e, v) = v + b * perp_right(e`p2 - e`p1)
theorem perp_ray_line_crossing_distance_exists : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), ∃ (b : ℝ), find_perp_ray_line_crossing e v = v + b * perp_right (e.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_ray_line_crossing_distance_norm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, v: point_2d):
--   EXISTS (b: real):
--     find_perp_ray_line_crossing(e, v) = v + b * perp_right(^(e`p2 - e`p1))
theorem perp_ray_line_crossing_distance_norm : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), ∃ (b : ℝ), find_perp_ray_line_crossing e v = v + b * perp_right (^(e.p2 - e.p1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_crossing_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem perp_crossing_reverse : True := by
  -- === TCCs for perp_crossing_reverse ===
  -- TCC: perp_crossing_reverse_TCC1
  -- Statement: FORALL (e: segment_2d, p: point_2d): p + perp_right(e`p1 - e`p2) /= p
  have perp_crossing_reverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: perp_crossings.perp_crossing_reverse_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem perp_crossing_reverse_2 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: perp_crossings.find_perp_ray_crossing_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, v: point_2d):
--   find_perp_ray_crossing(e, v) =
--    find_perp_ray_crossing(reverse_segment(e), v)
theorem find_perp_ray_crossing_reverse : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), find_perp_ray_crossing e v = find_perp_ray_crossing (reverse_segment e) v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_ray_line_crossing_swapped_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, ss, ee: segment_2d, p, q: point_2d):
--   (ss = s OR ss = reverse_segment(s)) AND
--    (ee = e OR ee = reverse_segment(e))
--    IMPLIES
--    find_perp_ray_line_crossing(e, p) = find_perp_ray_line
theorem perp_ray_line_crossing_swapped_endpoints : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ss : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (ee : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), (ss = s ∨ ss = reverse_segment s) ∧ (ee = e ∨ ee = reverse_segment e) → find_perp_ray_line_crossing e p = find_perp_ray_line_crossing ee p ∧
 find_perp_ray_line_crossing s q = find_perp_ray_line_crossing ss q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.find_perp_ray_crossing_subseq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e, h: segment_2d, v: point_2d):
--   find_perp_ray_crossing(e, v) /= None AND
--    point_on_segment?(e`p1, h) AND point_on_segment?(e`p2, h)
--    IMPLIES find_perp_ray_crossing(h, v) /= None
theorem find_perp_ray_crossing_subseq : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), find_perp_ray_crossing e v ≠ None ∧
 point_on_segment_p e.p1 h ∧ point_on_segment_p e.p2 h → find_perp_ray_crossing h v ≠ None := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_line_crossing_is_perp_pt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, p: point_2d):
--   LET Q: Vect2 = p,
--       P0: Vect2 = e`p1,
--       nzv: Nz_vect2 = e`p2 - e`p1,
--       tp: real = perp_pt(Q, P0, nzv)
--     IN find_perp_ray_line_crossing(e, p) = P0 +
theorem perp_line_crossing_is_perp_pt : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), fun (Q : ℝ × ℝ) => fun (P0 : ℝ × ℝ) => fun (nzv : {v : ℝ × ℝ // v ≠ zero}) => fun (tp : ℝ) => find_perp_ray_line_crossing e p = P0 + tp * nzv (perp_pt Q P0 nzv) (e.p2 - e.p1) e.p1 p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.find_perp_ray_crossing_is_perp_pt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, p: point_2d):
--   LET Q: Vect2 = p,
--       P0: Vect2 = e`p1,
--       nzv: Nz_vect2 = e`p2 - e`p1,
--       tp: real = perp_pt(Q, P0, nzv)
--     IN
--     find_perp_ray_crossing(e, p) /= None
theorem find_perp_ray_crossing_is_perp_pt : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), fun (Q : ℝ × ℝ) => fun (P0 : ℝ × ℝ) => fun (nzv : {v : ℝ × ℝ // v ≠ zero}) => fun (tp : ℝ) => find_perp_ray_crossing e p ≠ None → val (find_perp_ray_crossing e p) = P0 + tp * nzv (perp_pt Q P0 nzv) (e.p2 - e.p1) e.p1 p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_is_min
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, p: point_2d):
--   LET Q: Vect2 = p,
--       P0: Vect2 = e`p1,
--       nzv: Nz_vect2 = e`p2 - e`p1,
--       tp: real = perp_pt(Q, P0, nzv)
--     IN
--     FORALL (t: real):
--       LET d = (P0 
theorem perp_is_min : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), fun (Q : ℝ × ℝ) => fun (P0 : ℝ × ℝ) => fun (nzv : {v : ℝ × ℝ // v ≠ zero}) => fun (tp : ℝ) => ∀ (t : ℝ), fun (d : ℝ × ℝ) => fun (c : ℝ × ℝ) => norm d ≥ norm c ((P0 + tp * nzv) - Q) ((P0 + t * nzv) - Q) (perp_pt Q P0 nzv) (e.p2 - e.p1) e.p1 p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.t_exists_on_line_seg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, q: (point_on_segment?(e))):
--   LET P0: Vect2 = e`p1, nzv: Nz_vect2 = e`p2 - e`p1 IN
--     EXISTS (t: real): (P0 + t * nzv) = q
theorem t_exists_on_line_seg : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (q : ℝ × ℝ), fun (P0 : ℝ × ℝ) => fun (nzv : {v : ℝ × ℝ // v ≠ zero}) => ∃ (t : ℝ), (P0 + t * nzv) = q (e.p2 - e.p1) e.p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_is_shortest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, v: point_2d):
--   find_perp_ray_crossing(e, v) /= None IMPLIES
--    FORALL (q: (point_on_segment?(e))):
--      norm(v - val(find_perp_ray_crossing(e, v))) <= norm(v - q)
theorem perp_is_shortest : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), find_perp_ray_crossing e v ≠ None → ∀ (q : (point_on_segment_p e)), norm (v - val (find_perp_ray_crossing e v)) ≤ norm (v - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.exists_parallel_parameterization
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e))):
--     EXISTS (p0, q0, v: Vect2, t: real):
--       (p = p0 + t * v) AND (q = q0 + t * v)
theorem exists_parallel_parameterization : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ∀ (p : ℝ × ℝ) (q : ℝ × ℝ), ∃ (p0 : ℝ × ℝ) (q0 : ℝ × ℝ) (v : ℝ × ℝ) (t : ℝ), (p = p0 + t * v) ∧ (q = q0 + t * v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.exists_parallel_parameterization_generalization
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   FORALL (p: {pt: Vect2 | on_line?(pt, segment_2d_to_Line(s))},
--           q: {pt: Vect2 | on_line?(pt, segment_2d_to_Line(e))}):
--     EXISTS (p0, q0, v: Vect2
theorem exists_parallel_parameterization_generalization : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ∀ (p : {pt : ℝ × ℝ // on_line_p pt (segment_2d_to_Line s)}) (q : {pt : ℝ × ℝ // on_line_p pt (segment_2d_to_Line e)}), ∃ (p0 : ℝ × ℝ) (q0 : ℝ × ℝ) (v : ℝ × ℝ) (t : ℝ), (p = p0 + t * v) ∧ (q = q0 + t * v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_is_shortest_on_line
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, p, q: point_2d):
--   (EXISTS (k: real): q = e`p1 + k * (e`p2 - e`p1)) IMPLIES
--    norm(p - find_perp_ray_line_crossing(e, p)) <= norm(p - q)
theorem perp_is_shortest_on_line : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), (∃ (k : ℝ), q = e.p1 + k * (e.p2 - e.p1)) → norm (p - find_perp_ray_line_crossing e p) ≤ norm (p - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.not_perp_p1_closer_p2_imp_p1_closest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, p, q: point_2d):
--   norm(p - e`p1) <= norm(p - e`p2) AND
--    find_perp_ray_crossing(e, p) = None AND point_on_segment?(q, e)
--    IMPLIES norm(p - e`p1) <= norm(p - q)
theorem not_perp_p1_closer_p2_imp_p1_closest : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), norm (p - e.p1) ≤ norm (p - e.p2) ∧
 find_perp_ray_crossing e p = None ∧ point_on_segment_p q e → norm (p - e.p1) ≤ norm (p - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_line_orthogonal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, p: (point_on_segment?(s)), q: point_2d):
--   LET u = find_perp_ray_line_crossing(s, q) IN orthogonal?(u - p, q - u)
theorem perp_line_orthogonal : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), fun (u : ℝ × ℝ) => orthogonal_p (u - p) (q - u) (find_perp_ray_line_crossing s q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.cos_neg_inner_point_closer
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w, p: point_2d):
--        (u /= v) AND (w /= v) AND (p /= v) AND (vcos(^(u - v), ^(w - v)) < 0)
--    AND (EXISTS (k: nnreal): p = v + k * (u - v))
--    IMPLIES (norm(u - w) <= norm(p - w) IFF 
theorem cos_neg_inner_point_closer : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (p : ℝ × ℝ), (u ≠ v) ∧ (w ≠ v) ∧ (p ≠ v) ∧ (vcos (^(u - v)) (^(w - v)) < 0)
 ∧ (∃ (k : NNReal), p = v + k * (u - v)) → (norm (u - w) ≤ norm (p - w) ↔ norm (u - v) ≤ norm (p - v)) := by
  -- === TCCs for cos_neg_inner_point_closer ===
  -- TCC: cos_neg_inner_point_closer_TCC1
  -- Statement: FORALL (u, v, w, p: point_2d):
  --   u /= v AND w /= v AND p /= v IMPLIES u - v /= zero
  have cos_neg_inner_point_closer_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_neg_inner_point_closer_TCC2
  -- Statement: FORALL (u, v, w, p: point_2d):
  --   u /= v AND w /= v AND p /= v IMPLIES w - v /= zero
  have cos_neg_inner_point_closer_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.cos_neg_inner_point_closer_lt
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w, p: point_2d):
--        (u /= v) AND (w /= v) AND (p /= v) AND (vcos(^(u - v), ^(w - v)) < 0)
--    AND (EXISTS (k: nnreal): p = v + k * (u - v))
--    IMPLIES (norm(u - w) < norm(p - w) IFF n
theorem cos_neg_inner_point_closer_lt : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (p : ℝ × ℝ), (u ≠ v) ∧ (w ≠ v) ∧ (p ≠ v) ∧ (vcos (^(u - v)) (^(w - v)) < 0)
 ∧ (∃ (k : NNReal), p = v + k * (u - v)) → (norm (u - w) < norm (p - w) ↔ norm (u - v) < norm (p - v)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.cos_pos_len_le_imp_find_perp_ray_crossing
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d):
--   u /= v AND w /= v IMPLIES
--    LET wv_seg = (# p1 := v, p2 := w #), uvw_cos = vcos(^(u - v), ^(w - v))
--      IN
--      uvw_cos >= 0 IMPLIES
--       (uvw_cos * norm(u - v) <= nor
theorem cos_pos_len_le_imp_find_perp_ray_crossing : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ), u ≠ v ∧ w ≠ v → (let wv_seg := (# p1 := v, p2 := w #); let uvw_cos := vcos (^(u - v)) (^(w - v)); uvw_cos ≥ 0 →
   (uvw_cos * norm (u - v) ≤ norm (w - v) ↔
     find_perp_ray_crossing wv_seg u ≠ None)) := by
  -- === TCCs for cos_pos_len_le_imp_find_perp_ray_crossing ===
  -- TCC: cos_pos_len_le_imp_find_perp_ray_crossing_TCC1
  -- Statement: FORALL (u, v, w: point_2d):
  --   u /= v AND w /= v IMPLIES
  --    FORALL (wv_seg: [# p1: point_2d, p2: point_2d #]):
  --      wv_seg = (# p1 := v, p2 := w #) IMPLIES u - v /= zero
  have cos_pos_len_le_imp_find_perp_ray_crossing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_pos_len_le_imp_find_perp_ray_crossing_TCC2
  -- Statement: FORALL (u, v, w: point_2d):
  --   u /= v AND w /= v IMPLIES
  --    FORALL (wv_seg: [# p1: point_2d, p2: point_2d #]):
  --      wv_seg = (# p1 := v, p2 := w #) IMPLIES w - v /= zero
  have cos_pos_len_le_imp_find_perp_ray_crossing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_pos_len_le_imp_find_perp_ray_crossing_TCC3
  -- Statement: FORALL (u, v, w: point_2d):
  --   u /= v AND w /= v IMPLIES
  --    FORALL (wv_seg: [# p1: point_2d, p2: point_2d #],
  --            uvw_cos: {x: real | -1 <= x AND x <= 1}):
  --      wv_seg = (# p1 := v, p2 := w #...
  have cos_pos_len_le_imp_find_perp_ray_crossing_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_segment_imp_points_on_same_side
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q, r: point_2d, s: segment_2d):
--   parallel?(s`p2 - s`p1, q - p) AND
--    p /= q AND
--     cross(s`p2 - s`p1, r - s`p1) < 0 AND
--      point_on_segment?(r, (# p1 := p, p2 := q #))
--    IMPLIES
--    FO
theorem parallel_segment_imp_points_on_same_side : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), parallel_p (s.p2 - s.p1) (q - p) ∧
 p ≠ q ∧
  cross (s.p2 - s.p1) (r - s.p1) < 0 ∧
   point_on_segment_p r ((# p1 := p, p2 := q #)) → ∀ (u : point_2d), point_on_segment_p u ((# p1 := p, p2 := q #)) →
   cross (s.p2 - s.p1) (u - s.p1) < 0 := by
  -- === TCCs for parallel_segment_imp_points_on_same_side ===
  -- TCC: parallel_segment_imp_points_on_same_side_TCC1
  -- Statement: FORALL (p, q, r: point_2d, s: segment_2d):
  --   parallel?(s`p2 - s`p1, q - p) AND
  --    p /= q AND cross(s`p2 - s`p1, r - s`p1) < 0
  --    IMPLIES q /= p
  have parallel_segment_imp_points_on_same_side_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_line_crossing_points_same_vectors
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s)), p, q: point_2d):
--   (EXISTS (k: real): p = s`p1 + k * (s`p2 - s`p1)) AND
--    (EXISTS (k: real): q = s`p1 + k * (s`p2 - s`p1))
--    IMPLIES
--    LET u = find_perp_ra
theorem parallel_line_crossing_points_same_vectors : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), (∃ (k : ℝ), p = s.p1 + k * (s.p2 - s.p1)) ∧
 (∃ (k : ℝ), q = s.p1 + k * (s.p2 - s.p1)) → (let u := find_perp_ray_line_crossing e p; let v := find_perp_ray_line_crossing e q; q - p = v - u) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_lines_same_distance_at_all_points
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s)), p, q: point_2d):
--   (EXISTS (k: real): p = s`p1 + k * (s`p2 - s`p1)) AND
--    (EXISTS (k: real): q = s`p1 + k * (s`p2 - s`p1))
--    IMPLIES
--    norm(find_perp_ray_l
theorem parallel_lines_same_distance_at_all_points : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), (∃ (k : ℝ), p = s.p1 + k * (s.p2 - s.p1)) ∧
 (∃ (k : ℝ), q = s.p1 + k * (s.p2 - s.p1)) → norm (find_perp_ray_line_crossing e p - p) =
 norm (find_perp_ray_line_crossing e q - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.points_on_parallel_lines_rectangular
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s)), p, q: point_2d):
--   (EXISTS (k: real): p = s`p1 + k * (s`p2 - s`p1)) AND
--    (EXISTS (k: real): q = e`p1 + k * (e`p2 - e`p1))
--    IMPLIES
--    LET u = find_perp_ra
theorem points_on_parallel_lines_rectangular : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), (∃ (k : ℝ), p = s.p1 + k * (s.p2 - s.p1)) ∧
 (∃ (k : ℝ), q = e.p1 + k * (e.p2 - e.p1)) → (let u := find_perp_ray_line_crossing s q; let v := find_perp_ray_line_crossing e p; u - p = q - v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_segment_offset
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET xing_pt = find_perp_ray_line_crossing(s, e`p1),
--     dist = norm(xing_pt - s`p1)
--   IN
--   IF xing_pt = s`p1 THEN 0
--   ELSIF ^(xing_pt - s`p1) = ^(s`p2 - s`p1) THEN dist
--   ELSE -dist
--   ENDIF
axiom parallel_segment_offset {pvs_τ55 : Type*} {pvs_τ56 : Type*} : pvs_τ55 → pvs_τ56 → Prop

-- pvs2lean: perp_crossings.parallel_segment_line_crossings_rectangular
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   LET u = find_perp_ray_line_crossing(s, e`p1),
--       v = find_perp_ray_line_crossing(e, s`p1)
--     IN u - s`p1 = e`p1 - v
theorem parallel_segment_line_crossings_rectangular : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => u - s.p1 = e.p1 - v (find_perp_ray_line_crossing e s.p1) (find_perp_ray_line_crossing s e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_segment_offset_semi_commutative
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: segment_2d, s: (parallel?(e))):
--   parallel_segment_offset(e, s) =
--    IF ^(e`p2 - e`p1) = ^(s`p2 - s`p1) THEN -parallel_segment_offset(s, e)
--    ELSE parallel_segment_offset(s, e)
--    ENDIF
theorem parallel_segment_offset_semi_commutative : ∀ (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), parallel_segment_offset e s = if ^(e.p2 - e.p1) = ^(s.p2 - s.p1) then -parallel_segment_offset s e
else parallel_segment_offset s e := by
  -- === TCCs for parallel_segment_offset_semi_commutative ===
  -- TCC: parallel_segment_offset_semi_commutative_TCC1
  -- Statement: FORALL (e: segment_2d, s: (parallel?(e))):
  --   ^(e`p2 - e`p1) = ^(s`p2 - s`p1) IMPLIES parallel?(s)(e)
  have parallel_segment_offset_semi_commutative_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: parallel_segment_offset_semi_commutative_TCC2
  -- Statement: FORALL (e: segment_2d, s: (parallel?(e))):
  --   NOT ^(e`p2 - e`p1) = ^(s`p2 - s`p1) IMPLIES parallel?(s)(e)
  have parallel_segment_offset_semi_commutative_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_perp_crossing_on_segment
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s)), q: point_2d, d: nnreal):
--   LET dir_s = ^(s`p2 - s`p1), dir_e = ^(e`p2 - e`p1) IN
--     q = e`p1 + d * dir_e AND d <= length(e) IMPLIES
--      LET a = parallel_seg
theorem parallel_perp_crossing_on_segment : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (q : ℝ × ℝ) (d : {x : ℝ // x ≥ 0}), fun (dir_s : {v : ℝ × ℝ // norm v = 1}) => fun (dir_e : {v : ℝ × ℝ // norm v = 1}) => q = e.p1 + d * dir_e ∧ d ≤ List.length e → (let a := parallel_segment_offset s e; let shift := if dir_s = dir_e then a + d else a - d ; find_perp_ray_crossing s q ≠ None ↔ 0 ≤ shift ≤ List.length s) (pvs_op_7e6a2a (e.p2 - e.p1)) (pvs_op_7e6a2a (s.p2 - s.p1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_perp_crossing_is_p1_plus_offset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   find_perp_ray_line_crossing(s, e`p1) =
--    s`p1 + parallel_segment_offset(s, e) * ^(s`p2 - s`p1)
theorem parallel_perp_crossing_is_p1_plus_offset : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), find_perp_ray_line_crossing s e.p1 = s.p1 + parallel_segment_offset s e * ^(s.p2 - s.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_seg_with_no_xing_imp_other_seg_has_0_or_2_xing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   (find_perp_ray_crossing(e, s`p1) = None AND
--     find_perp_ray_crossing(e, s`p2) = None)
--    IMPLIES
--    ((find_perp_ray_crossing(s, e`p1) = None AND
--       fi
theorem parallel_seg_with_no_xing_imp_other_seg_has_0_or_2_xing : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (find_perp_ray_crossing e s.p1 = None ∧
  find_perp_ray_crossing e s.p2 = None) → ((find_perp_ray_crossing s e.p1 = None ∧
   find_perp_ray_crossing s e.p2 = None)
  ∨
  (find_perp_ray_crossing s e.p1 ≠ None ∧
    find_perp_ray_crossing s e.p2 ≠ None)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_segments_point_distance_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p, q: point_2d, a, b: real, v: vector_2d):
--        (v = ^(s`p2 - s`p1)) AND parallel?(v, ^(e`p2 - e`p1))
--    AND point_on_segment?(p, s) AND point_on_segment?(q, e)
--    AND (e`p
theorem parallel_segments_point_distance_bound : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ) (a : ℝ) (b : ℝ) (v : ℝ × ℝ), (v = ^(s.p2 - s.p1)) ∧ parallel_p v (^(e.p2 - e.p1))
 ∧ point_on_segment_p p s ∧ point_on_segment_p q e
 ∧ (e.p1 = s.p1 + a * v + b * perp_right v) → |b| ≤ norm (p - q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.projected_point_preserves_distance_ordering
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, p, q: (point_on_segment?(s)), r: point_2d):
--   LET u = find_perp_ray_line_crossing(s, r) IN
--     norm(u - p) >= norm(u - q) IFF norm(r - p) >= norm(r - q)
theorem projected_point_preserves_distance_ordering : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ), fun (u : ℝ × ℝ) => norm (u - p) ≥ norm (u - q) ↔ norm (r - p) ≥ norm (r - q) (find_perp_ray_line_crossing s r) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_line_crossing_point_val
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, v: point_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET xing_pt = val(line_crossing(s, e)), c = norm(v - xing_pt) IN
--      v /= xing_pt IMPLIES
--       LET dir_
theorem perp_line_crossing_point_val : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let xing_pt := val (line_crossing s e); let c := norm (v - xing_pt); v ≠ xing_pt →
   (let dir_s := ^(v - xing_pt); let dir_e := ^(e.p2 - e.p1); let a := c * vcos dir_e dir_s; find_perp_ray_line_crossing e v = xing_pt + a * dir_e)) := by
  -- === TCCs for perp_line_crossing_point_val ===
  -- TCC: perp_line_crossing_point_val_TCC1
  -- Statement: FORALL (s, e: segment_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    some?[point_2d](line_crossing(s, e))
  have perp_line_crossing_point_val_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: perp_line_crossing_point_val_TCC2
  -- Statement: FORALL (s, e: segment_2d, v: point_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (xing_pt: point_2d, c: nnreal):
  --      xing_pt = val(line_crossing(s, e)) AND
  --       c = norm(v...
  have perp_line_crossing_point_val_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_line_crossing_at_line_crossing_self
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET xing_pt = val(line_crossing(s, e)) IN
--      find_perp_ray_line_crossing(e, xing_pt) = xing_pt
theorem perp_line_crossing_at_line_crossing_self : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let xing_pt := val (line_crossing s e); find_perp_ray_line_crossing e xing_pt = xing_pt) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.closer_point_imp_closer_perp_crossing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, u, v: (point_on_segment?(s))):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET xing_pt = val(line_crossing(s, e)),
--        p = find_perp_ray_line_crossing(e, u),
--   
theorem closer_point_imp_closer_perp_crossing : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (u : ℝ × ℝ) (v : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let xing_pt := val (line_crossing s e); let p := find_perp_ray_line_crossing e u; let q := find_perp_ray_line_crossing e v; u ≠ v ∧ v ≠ xing_pt ∧ norm (xing_pt - u) > norm (xing_pt - v) →
   norm (p - u) ≥ norm (q - v) ∧ norm (xing_pt - p) ≥ norm (xing_pt - q)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.point_on_seg_farther_from_perp_xing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s)), v: (segment_endpoint?(e)),
--         u: point_2d, q: (point_on_segment?(e))):
--   find_perp_ray_crossing(e, u) = None IMPLIES
--    LET ov = opposite_endpoint(e, v),
theorem point_on_seg_farther_from_perp_xing : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ) (u : ℝ × ℝ) (q : ℝ × ℝ), find_perp_ray_crossing e u = None → (let ov := opposite_endpoint e v; let w := find_perp_ray_line_crossing e u; norm (v - u) ≤ norm (ov - u) → norm (w - v) ≤ norm (w - q)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.perp_crossing_at_endpoint_is_closest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: (point_on_segment?(s)),
--         q: (point_on_segment?(e))):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) AND
--    find_perp_ray_crossing(e, s`p1) /= None
--    IMPLIES
--    LET xi
theorem perp_crossing_at_endpoint_is_closest : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) ∧
 find_perp_ray_crossing e s.p1 ≠ None → (let xing_pt := val (line_crossing s e); let u := val (find_perp_ray_crossing e s.p1); norm (s.p1 - xing_pt) < norm (s.p2 - xing_pt) →
   norm (s.p1 - u) ≤ norm (p - q) ∨ extending_s_crosses_e e s) := by
  -- === TCCs for perp_crossing_at_endpoint_is_closest ===
  -- TCC: perp_crossing_at_endpoint_is_closest_TCC1
  -- Statement: FORALL (s, e: segment_2d, p: (point_on_segment?(s)),
  --         q: (point_on_segment?(e))):
  --   NOT parallel?(s)(e) AND
  --    NOT collinear?(s, e) AND find_perp_ray_crossing(e, s`p1) /= None
  --    IMPLIES
  --    ...
  have perp_crossing_at_endpoint_is_closest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.cos_pos_perp_crossing_cases
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET xing_pt = val(line_crossing(s, e)),
--        pre_seg = (# p1 := xing_pt, p2 := e`p1 #),
--        perp_xin
theorem cos_pos_perp_crossing_cases : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let xing_pt := val (line_crossing s e); let pre_seg := (# p1 := xing_pt, p2 := e.p1 #); let perp_xing := find_perp_ray_line_crossing e p; (s.p1 ≠ xing_pt) ∧ (¬ point_on_segment_p xing_pt e)
   ∧ (norm (e.p1 - xing_pt) ≤ norm (e.p2 - xing_pt))
   ∧ (∃ (k : {x : ℝ // 0 < x}), p = xing_pt + k * (s.p1 - xing_pt))
   ∧ (vcos (^(s.p1 - xing_pt)) (^(e.p1 - xing_pt)) ≥ 0)
   →
   point_on_segment_p perp_xing e ∨
    point_on_segment_p perp_xing pre_seg ∧
     norm (e.p1 - p) < norm (e.p2 - p)
     ∨
     point_beyond_segment_p perp_xing e ∧ norm (e.p2 - p) < norm (e.p1 - p)) := by
  -- === TCCs for cos_pos_perp_crossing_cases ===
  -- TCC: cos_pos_perp_crossing_cases_TCC1
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (xing_pt: point_2d, pre_seg: [# p1: point_2d, p2: point_2d #],
  --            perp_xing: point_2...
  have cos_pos_perp_crossing_cases_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_pos_perp_crossing_cases_TCC2
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (xing_pt: point_2d, pre_seg: [# p1: point_2d, p2: point_2d #],
  --            perp_xing: point_2...
  have cos_pos_perp_crossing_cases_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_pos_perp_crossing_cases_TCC3
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (xing_pt: point_2d, pre_seg: [# p1: point_2d, p2: point_2d #],
  --            perp_xing: point_2...
  have cos_pos_perp_crossing_cases_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.cos_pos_find_perp_crossing_cases
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   NOT (parallel?(s)(e) OR collinear?(s, e)) IMPLIES
--    LET xing_pt = val(line_crossing(s, e)),
--        pre_seg_e = (# p1 := xing_pt, p2 := e`p1 #),
--        full_s
theorem cos_pos_find_perp_crossing_cases : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), ¬ (parallel_p s(e) ∨ collinear_p s e) → (let xing_pt := val (line_crossing s e); let pre_seg_e := (# p1 := xing_pt, p2 := e.p1 #); let full_seg_e := (# p1 := xing_pt, p2 := e.p2 #); (s.p1 ≠ xing_pt) ∧ (s.p2 ≠ xing_pt)
   ∧ (¬ point_on_segment_p xing_pt e)
   ∧ (norm (e.p1 - xing_pt) ≤ norm (e.p2 - xing_pt))
   ∧ (∃ (k : {x : ℝ // 0 < x}), p = xing_pt + k * (s.p1 - xing_pt))
   ∧ (vcos (^(s.p1 - xing_pt)) (^(e.p1 - xing_pt)) ≥ 0)
   →
   find_perp_ray_crossing e p ≠ None ∨
    find_perp_ray_crossing pre_seg_e p ≠ None ∧
     norm (e.p1 - p) < norm (e.p2 - p)
     ∨
     find_perp_ray_crossing full_seg_e p = None ∧
      norm (e.p2 - p) < norm (e.p1 - p)) := by
  -- === TCCs for cos_pos_find_perp_crossing_cases ===
  -- TCC: cos_pos_find_perp_crossing_cases_TCC1
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (xing_pt: point_2d, pre_seg_e: [# p1: point_2d, p2: point_2d #],
  --            full_seg_e: [# p...
  have cos_pos_find_perp_crossing_cases_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_pos_find_perp_crossing_cases_TCC2
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (xing_pt: point_2d, pre_seg_e: [# p1: point_2d, p2: point_2d #],
  --            full_seg_e: [# p...
  have cos_pos_find_perp_crossing_cases_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_pos_find_perp_crossing_cases_TCC3
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (xing_pt: point_2d, pre_seg_e: [# p1: point_2d, p2: point_2d #],
  --            full_seg_e: [# p...
  have cos_pos_find_perp_crossing_cases_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_pos_find_perp_crossing_cases_TCC4
  -- Statement: FORALL (s, e: segment_2d, p: point_2d):
  --   NOT parallel?(s)(e) AND NOT collinear?(s, e) IMPLIES
  --    FORALL (xing_pt: point_2d, pre_seg_e: [# p1: point_2d, p2: point_2d #],
  --            full_seg_e: [# p...
  have cos_pos_find_perp_crossing_cases_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.nearest_endpoints
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET d11 = norm(s`p1 - e`p1),
--     d12 = norm(s`p1 - e`p2),
--     d21 = norm(s`p2 - e`p1),
--     d22 = norm(s`p2 - e`p2),
--     min_d = min(min(min(d11, d12), d21), d22)
--   IN
--   IF min_d = d11 THEN (s`p1, e`p1
axiom nearest_endpoints {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} : pvs_τ41 → pvs_τ42 → pvs_τ43

-- pvs2lean: perp_crossings.is_nearest_endpoint_pair?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (p, q) = pair IN
--   norm(p - q) <= norm(s`p1 - e`p1) AND
--    norm(p - q) <= norm(s`p1 - e`p2) AND
--     norm(p - q) <= norm(s`p2 - e`p1) AND norm(p - q) <= norm(s`p2 - e`p2)
axiom is_nearest_endpoint_pair_p {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: perp_crossings.nearest_endpoints_is_nearest_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d):
--   is_nearest_endpoint_pair?(nearest_endpoints(s, e), s, e)
theorem nearest_endpoints_is_nearest_pair : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), is_nearest_endpoint_pair_p (nearest_endpoints s e) s e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.nearest_endpoint_pair_swapped
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, pair: [point_2d, point_2d]):
--   is_nearest_endpoint_pair?(pair, s, e) IFF
--    is_nearest_endpoint_pair?((pair`2, pair`1), e, s)
theorem nearest_endpoint_pair_swapped : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (pair : (ℝ × ℝ) × (ℝ × ℝ)), is_nearest_endpoint_pair_p pair s e ↔ is_nearest_endpoint_pair_p ((pair.2, pair.1)) e s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.overlapping?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: find_perp_ray_crossing(e, s`p1) /= None OR
--  find_perp_ray_crossing(e, s`p2) /= None OR
--   find_perp_ray_crossing(s, e`p1) /= None OR
--    find_perp_ray_crossing(s, e`p2) /= None
axiom overlapping_p {pvs_τ51 : Type*} {pvs_τ52 : Type*} : pvs_τ51 → pvs_τ52 → Prop

-- pvs2lean: perp_crossings.overlapping_symmetric
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   overlapping?(s, e) IFF overlapping?(e, s)
theorem overlapping_symmetric : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), overlapping_p s e ↔ overlapping_p e s := by
  -- === TCCs for overlapping_symmetric ===
  -- TCC: overlapping_symmetric_TCC1
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s))): parallel?(e)(s)
  have overlapping_symmetric_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.overlapping_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   overlapping?(s, e) IFF overlapping?(s, reverse_segment(e))
theorem overlapping_reverse : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), overlapping_p s e ↔ overlapping_p s (reverse_segment e) := by
  -- === TCCs for overlapping_reverse ===
  -- TCC: overlapping_reverse_TCC1
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s))): parallel?(s)(reverse_segment(e))
  have overlapping_reverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.alt_overlapping?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET v = ^(s`p2 - s`p1) IN
--   EXISTS (a, b: real):
--     e`p1 = s`p1 + a * v + b * perp_right(v) AND
--      IF v = ^(e`p2 - e`p1) THEN -length(e) <= a <= length(s)
--      ELSE 0 <= a <= length(s) + length(e)
-- 
noncomputable def alt_overlapping_p : sorry := by
  sorry  -- pvs2lean: definition stub for LET v = ^(s`p2 - s`p1) IN   EXISTS (a, b: real):     e`p1 = s`p1 + a * v + b * p

-- pvs2lean: perp_crossings.overlapping_covered
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   (overlapping?(s, e) AND
--     find_perp_ray_crossing(e, s`p1) = None AND
--      find_perp_ray_crossing(e, s`p2) = None)
--    IMPLIES
--    (find_perp_ray_crossing(s
theorem overlapping_covered : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (overlapping_p s e ∧
  find_perp_ray_crossing e s.p1 = None ∧
   find_perp_ray_crossing e s.p2 = None) → (find_perp_ray_crossing s e.p1 ≠ None ∧
  find_perp_ray_crossing s e.p2 ≠ None) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_nonoverlapping_imp_no_point_crossings
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   NOT overlapping?(s, e) IMPLIES
--    FORALL (p: (point_on_segment?(s))): find_perp_ray_crossing(e, p) = None
theorem parallel_nonoverlapping_imp_no_point_crossings : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ overlapping_p s e → ∀ (p : (point_on_segment_p s)), find_perp_ray_crossing e p = None := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.large_parallel_seg_offset_imp_p2_closer
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   parallel_segment_offset(s, e) > length(s) IMPLIES
--    norm(s`p1 - e`p1) > norm(s`p2 - e`p1)
theorem large_parallel_seg_offset_imp_p2_closer : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), parallel_segment_offset s e > List.length s → norm (s.p1 - e.p1) > norm (s.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_nonoverlapping_dir_offset_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   NOT overlapping?(s, e) AND
--    norm(s`p1 - e`p1) <= norm(s`p1 - e`p2) AND
--     norm(s`p1 - e`p1) <= norm(s`p2 - e`p1) AND
--      norm(s`p1 - e`p1) <= norm(s`p2
theorem parallel_nonoverlapping_dir_offset_neg : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ overlapping_p s e ∧
 norm (s.p1 - e.p1) ≤ norm (s.p1 - e.p2) ∧
  norm (s.p1 - e.p1) ≤ norm (s.p2 - e.p1) ∧
   norm (s.p1 - e.p1) ≤ norm (s.p2 - e.p2) → ^(s.p2 - s.p1) = -^(e.p2 - e.p1) ∧ parallel_segment_offset s e < 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.parallel_nonoverlapping_xing_pt_offsets
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s: segment_2d, e: (parallel?(s))):
--   NOT overlapping?(s, e) IMPLIES
--    FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e))):
--      LET u = nearest_endpoints(s, e)`1,
--          v = neare
theorem parallel_nonoverlapping_xing_pt_offsets : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬ overlapping_p s e → ∀ (p : (point_on_segment_p s)) (q : (point_on_segment_p e)), (let u := nearest_endpoints s e.1; let v := nearest_endpoints s e.2; let ou := opposite_endpoint s u; let ov := opposite_endpoint e v; ^(ou - u) = -^(ov - v) ∧
     (∃ (k : negreal), find_perp_ray_line_crossing e p = v + k * (ov - v))
      ∧
      (∃ (k : negreal), find_perp_ray_line_crossing s q = u + k * (ou - u))) := by
  -- === TCCs for parallel_nonoverlapping_xing_pt_offsets ===
  -- TCC: parallel_nonoverlapping_xing_pt_offsets_TCC1
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s))):
  --   NOT overlapping?(s, e) IMPLIES
  --    FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e)),
  --            u: point_2d, v: point_2d):
  --      u = nearest_e...
  have parallel_nonoverlapping_xing_pt_offsets_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: parallel_nonoverlapping_xing_pt_offsets_TCC2
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s))):
  --   NOT overlapping?(s, e) IMPLIES
  --    FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e)),
  --            u: point_2d, v: point_2d, ou: (segment_endpoi...
  have parallel_nonoverlapping_xing_pt_offsets_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: parallel_nonoverlapping_xing_pt_offsets_TCC3
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s))):
  --   NOT overlapping?(s, e) IMPLIES
  --    FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e)),
  --            u: point_2d, v: point_2d, ou: (segment_endpoi...
  have parallel_nonoverlapping_xing_pt_offsets_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: parallel_nonoverlapping_xing_pt_offsets_TCC4
  -- Statement: FORALL (s: segment_2d, e: (parallel?(s))):
  --   NOT overlapping?(s, e) IMPLIES
  --    FORALL (p: (point_on_segment?(s)), q: (point_on_segment?(e)),
  --            u: point_2d, v: point_2d, ou: (segment_endpoi...
  have parallel_nonoverlapping_xing_pt_offsets_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: perp_crossings.points_where_segment_crosses_segment
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF are_segments_intersecting?(s)(cur_segment)
--   THEN IF are_segments_strictly_intersecting?(s)(cur_segment)
--          THEN singleton_seq(segment_intersect_kernel(s, cur_segment)`2)
--        ELSE IF is_po
noncomputable def points_where_segment_crosses_segment : sorry := by
  sorry  -- pvs2lean: definition stub for IF are_segments_intersecting?(s)(cur_segment)   THEN IF are_segments_strictly_in

end

end NavierStokesClean.PVSLib.perp_crossings