-- Auto-generated by pvs2lean/translator.py
-- PVS theory: segments_2d_trig
-- PVSLib library: line_segments
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: segments_2d_trig

Auto-generated stubs from PVSLib theory `segments_2d_trig`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.segments_2d_trig

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (norm : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (perp_left : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} (perp_right : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (point_on_segment_p : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (subsegment_p : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (vcos : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (vsin : pvs_τ11 → pvs_τ12 → Prop)

-- pvs2lean: segments_2d_trig.subsegment_preserves_sin_cos_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, h: segment_2d):
--   subsegment?(h, s) AND h`p1 = s`p1 IMPLIES
--    vsin(^(s`p2 - s`p1), ^(e`p2 - e`p1)) =
--     vsin(^(h`p2 - h`p1), ^(e`p2 - e`p1))
--     AND
--     vcos(^(s`p2 - s`p1), ^(e`p2 - e
theorem subsegment_preserves_sin_cos_1 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p h s ∧ h.p1 = s.p1 → vsin (^(s.p2 - s.p1)) (^(e.p2 - e.p1)) = vsin (^(h.p2 - h.p1)) (^(e.p2 - e.p1))
 ∧
 vcos (^(s.p2 - s.p1)) (^(e.p2 - e.p1)) = vcos (^(h.p2 - h.p1)) (^(e.p2 - e.p1)) := by
  -- === TCCs for subsegment_preserves_sin_cos_1 ===
  -- TCC: subsegment_preserves_sin_cos_1_TCC1
  -- Statement: FORALL (s, e, h: segment_2d):
  --   subsegment?(h, s) AND h`p1 = s`p1 IMPLIES s`p2 - s`p1 /= zero
  have subsegment_preserves_sin_cos_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subsegment_preserves_sin_cos_1_TCC2
  -- Statement: FORALL (s, e, h: segment_2d):
  --   subsegment?(h, s) AND h`p1 = s`p1 IMPLIES e`p2 - e`p1 /= zero
  have subsegment_preserves_sin_cos_1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subsegment_preserves_sin_cos_1_TCC3
  -- Statement: FORALL (s, e, h: segment_2d):
  --   subsegment?(h, s) AND h`p1 = s`p1 IMPLIES h`p2 - h`p1 /= zero
  have subsegment_preserves_sin_cos_1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_trig.subsegment_preserves_sin_cos_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, h: segment_2d):
--   subsegment?(h, e) AND h`p1 = e`p1 IMPLIES
--    vsin(^(s`p2 - s`p1), ^(e`p2 - e`p1)) =
--     vsin(^(s`p2 - s`p1), ^(h`p2 - h`p1))
--     AND
--     vcos(^(s`p2 - s`p1), ^(e`p2 - e
theorem subsegment_preserves_sin_cos_2 : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p h e ∧ h.p1 = e.p1 → vsin (^(s.p2 - s.p1)) (^(e.p2 - e.p1)) = vsin (^(s.p2 - s.p1)) (^(h.p2 - h.p1))
 ∧
 vcos (^(s.p2 - s.p1)) (^(e.p2 - e.p1)) = vcos (^(s.p2 - s.p1)) (^(h.p2 - h.p1)) := by
  -- === TCCs for subsegment_preserves_sin_cos_2 ===
  -- TCC: subsegment_preserves_sin_cos_2_TCC1
  -- Statement: FORALL (s, e, h: segment_2d):
  --   subsegment?(h, e) AND h`p1 = e`p1 IMPLIES s`p2 - s`p1 /= zero
  have subsegment_preserves_sin_cos_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subsegment_preserves_sin_cos_2_TCC2
  -- Statement: FORALL (s, e, h: segment_2d):
  --   subsegment?(h, e) AND h`p1 = e`p1 IMPLIES e`p2 - e`p1 /= zero
  have subsegment_preserves_sin_cos_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subsegment_preserves_sin_cos_2_TCC3
  -- Statement: FORALL (s, e, h: segment_2d):
  --   subsegment?(h, e) AND h`p1 = e`p1 IMPLIES h`p2 - h`p1 /= zero
  have subsegment_preserves_sin_cos_2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_trig.subsegment_preserves_sin_cos_both
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, g, h: segment_2d):
--   subsegment?(g, s) AND g`p1 = s`p1 AND subsegment?(h, e) AND h`p1 = e`p1
--    IMPLIES
--    vsin(^(s`p2 - s`p1), ^(e`p2 - e`p1)) =
--     vsin(^(g`p2 - g`p1), ^(h`p2 - h`p1))
theorem subsegment_preserves_sin_cos_both : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (g : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), subsegment_p g s ∧ g.p1 = s.p1 ∧ subsegment_p h e ∧ h.p1 = e.p1 → vsin (^(s.p2 - s.p1)) (^(e.p2 - e.p1)) = vsin (^(g.p2 - g.p1)) (^(h.p2 - h.p1))
 ∧
 vcos (^(s.p2 - s.p1)) (^(e.p2 - e.p1)) = vcos (^(g.p2 - g.p1)) (^(h.p2 - h.p1)) := by
  -- === TCCs for subsegment_preserves_sin_cos_both ===
  -- TCC: subsegment_preserves_sin_cos_both_TCC1
  -- Statement: FORALL (s, e, g, h: segment_2d):
  --   subsegment?(g, s) AND g`p1 = s`p1 AND subsegment?(h, e) AND h`p1 = e`p1
  --    IMPLIES g`p2 - g`p1 /= zero
  have subsegment_preserves_sin_cos_both_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_trig.no_perp_segment_intersection_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
--        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
--    AND point_on_segment?(q, (# p1 := u, p2 := v #))
--    IMPLIES
--    LET sin_a = vsin(^(v - u),
theorem no_perp_segment_intersection_1 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (q : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}), (u ≠ v) ∧ (v ≠ w) ∧ (w ≠ u) ∧ (q ≠ u)
 ∧ point_on_segment_p q ((# p1 := u, p2 := v #)) → (let sin_a := vsin (^(v - u)) (^(w - u)); let cos_a := vcos (^(v - u)) (^(w - u)); let z := ^(perp_right (v - u)); let p := q + c * z; sin_a > 0 ∧ cos_a > 0 ∧ c < norm (q - u) * sin_a / cos_a →
   ¬ point_on_segment_p p ((# p1 := w, p2 := u #))) := by
  -- === TCCs for no_perp_segment_intersection_1 ===
  -- TCC: no_perp_segment_intersection_1_TCC1
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d):
  --   u /= v AND v /= w AND w /= u AND q /= u IMPLIES v /= u
  have no_perp_segment_intersection_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_segment_intersection_1_TCC2
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES v - u /= zero
  have no_perp_segment_intersection_1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_segment_intersection_1_TCC3
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES w - u /= zero
  have no_perp_segment_intersection_1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_segment_intersection_1_TCC4
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES
  --    FORALL (sin_a: {x: real | -1 <= x A...
  have no_perp_segment_intersection_1_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_segment_intersection_1_TCC5
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES
  --    FORALL (sin_a: {x: real...
  have no_perp_segment_intersection_1_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_segment_intersection_1_TCC6
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES
  --    FORALL (sin_a: {x: real...
  have no_perp_segment_intersection_1_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_trig.no_perp_segment_intersection_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
--        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
--    AND point_on_segment?(q, (# p1 := u, p2 := v #))
--    IMPLIES
--    LET sin_a = vsin(^(w - u),
theorem no_perp_segment_intersection_2 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (q : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}), (u ≠ v) ∧ (v ≠ w) ∧ (w ≠ u) ∧ (q ≠ u)
 ∧ point_on_segment_p q ((# p1 := u, p2 := v #)) → (let sin_a := vsin (^(w - u)) (^(v - u)); let cos_a := vcos (^(w - u)) (^(v - u)); let z := ^(perp_left (v - u)); let p := q + c * z; sin_a > 0 ∧ cos_a > 0 ∧ c < norm (q - u) * sin_a / cos_a →
   ¬ point_on_segment_p p ((# p1 := w, p2 := u #))) := by
  -- === TCCs for no_perp_segment_intersection_2 ===
  -- TCC: no_perp_segment_intersection_2_TCC1
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES
  --    FORALL (sin_a: {x: real | -1 <= x A...
  have no_perp_segment_intersection_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_segment_intersection_2_TCC2
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES
  --    FORALL (sin_a: {x: real...
  have no_perp_segment_intersection_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_segment_intersection_2_TCC3
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES
  --    FORALL (sin_a: {x: real...
  have no_perp_segment_intersection_2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_trig.no_perp_segment_intersection_3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
--        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
--    AND point_on_segment?(q, (# p1 := u, p2 := v #))
--    IMPLIES
--    LET sin_a = vsin(^(v - u),
theorem no_perp_segment_intersection_3 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (q : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}), (u ≠ v) ∧ (v ≠ w) ∧ (w ≠ u) ∧ (q ≠ u)
 ∧ point_on_segment_p q ((# p1 := u, p2 := v #)) → (let sin_a := vsin (^(v - u)) (^(w - u)); let cos_a := vcos (^(v - u)) (^(w - u)); let z := ^(perp_right (v - u)); let p := q + c * z; (sin_a ≤ 0 ∨ cos_a ≤ 0) →
   ¬ point_on_segment_p p ((# p1 := w, p2 := u #))) := by
  -- === TCCs for no_perp_segment_intersection_3 ===
  -- TCC: no_perp_segment_intersection_3_TCC1
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES
  --    FORALL (sin_a: {x: real...
  have no_perp_segment_intersection_3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_trig.no_perp_segment_intersection_4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
--        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
--    AND point_on_segment?(q, (# p1 := u, p2 := v #))
--    IMPLIES
--    LET sin_a = vsin(^(w - u),
theorem no_perp_segment_intersection_4 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (q : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}), (u ≠ v) ∧ (v ≠ w) ∧ (w ≠ u) ∧ (q ≠ u)
 ∧ point_on_segment_p q ((# p1 := u, p2 := v #)) → (let sin_a := vsin (^(w - u)) (^(v - u)); let cos_a := vcos (^(w - u)) (^(v - u)); let z := ^(perp_left (v - u)); let p := q + c * z; (sin_a ≤ 0 ∨ cos_a ≤ 0) →
   ¬ point_on_segment_p p ((# p1 := w, p2 := u #))) := by
  -- === TCCs for no_perp_segment_intersection_4 ===
  -- TCC: no_perp_segment_intersection_4_TCC1
  -- Statement: FORALL (u, v, w: point_2d, q: point_2d, c: posreal):
  --        (u /= v) AND (v /= w) AND (w /= u) AND (q /= u)
  --    AND point_on_segment?(q, (# p1 := u, p2 := v #))
  --    IMPLIES
  --    FORALL (sin_a: {x: real...
  have no_perp_segment_intersection_4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_trig.no_perp_mid_segment_intersection_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, c, d: posreal):
--   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES
--    LET sin_a = vsin(^(v - u), ^(w - u)),
--        cos_a = vcos(^(v - u), ^(w - u)),
--        m = 1 / 2 * (u + v
theorem no_perp_mid_segment_intersection_1 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), u ≠ v ∧ v ≠ w ∧ w ≠ u ∧ d ≤ 1 → (let sin_a := vsin (^(v - u)) (^(w - u)); let cos_a := vcos (^(v - u)) (^(w - u)); let m := 1 / 2 * (u + v); let z := ^(perp_right (v - u)); let p := m + d * c * z; sin_a > 0 ∧ cos_a > 0 ∧ c < 1 / 2 * norm (v - u) * sin_a / cos_a
   → ¬ point_on_segment_p p ((# p1 := w, p2 := u #))) := by
  -- === TCCs for no_perp_mid_segment_intersection_1 ===
  -- TCC: no_perp_mid_segment_intersection_1_TCC1
  -- Statement: FORALL (u, v, w: point_2d, d: posreal):
  --   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES v - u /= zero
  have no_perp_mid_segment_intersection_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_mid_segment_intersection_1_TCC2
  -- Statement: FORALL (u, v, w: point_2d, d: posreal):
  --   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES w - u /= zero
  have no_perp_mid_segment_intersection_1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_mid_segment_intersection_1_TCC3
  -- Statement: FORALL (u, v, w: point_2d, d: posreal):
  --   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES
  --    FORALL (sin_a: {x: real | -1 <= x AND x <= 1},
  --            cos_a: {x: real | -1 <= x AND x <= 1}, m: Vect...
  have no_perp_mid_segment_intersection_1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_mid_segment_intersection_1_TCC4
  -- Statement: FORALL (u, v, w: point_2d, c, d: posreal):
  --   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES
  --    FORALL (sin_a: {x: real | -1 <= x AND x <= 1},
  --            cos_a: {x: real | -1 <= x AND x <= 1}, m: V...
  have no_perp_mid_segment_intersection_1_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_mid_segment_intersection_1_TCC5
  -- Statement: FORALL (u, v, w: point_2d, c, d: posreal):
  --   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES
  --    FORALL (sin_a: {x: real | -1 <= x AND x <= 1},
  --            cos_a: {x: real | -1 <= x AND x <= 1}, m: V...
  have no_perp_mid_segment_intersection_1_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: segments_2d_trig.no_perp_mid_segment_intersection_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v, w: point_2d, c, d: posreal):
--   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES
--    LET sin_a = vsin(^(w - u), ^(v - u)),
--        cos_a = vcos(^(w - u), ^(v - u)),
--        m = 1 / 2 * (u + v
theorem no_perp_mid_segment_intersection_2 : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (w : ℝ × ℝ) (c : {x : {x : ℝ // x ≥ 0} // x > 0}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), u ≠ v ∧ v ≠ w ∧ w ≠ u ∧ d ≤ 1 → (let sin_a := vsin (^(w - u)) (^(v - u)); let cos_a := vcos (^(w - u)) (^(v - u)); let m := 1 / 2 * (u + v); let z := ^(perp_left (v - u)); let p := m + d * c * z; sin_a > 0 ∧ cos_a > 0 ∧ c < 1 / 2 * norm (v - u) * sin_a / cos_a
   → ¬ point_on_segment_p p ((# p1 := w, p2 := u #))) := by
  -- === TCCs for no_perp_mid_segment_intersection_2 ===
  -- TCC: no_perp_mid_segment_intersection_2_TCC1
  -- Statement: FORALL (u, v, w: point_2d, d: posreal):
  --   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES
  --    FORALL (sin_a: {x: real | -1 <= x AND x <= 1},
  --            cos_a: {x: real | -1 <= x AND x <= 1}, m: Vect...
  have no_perp_mid_segment_intersection_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_mid_segment_intersection_2_TCC2
  -- Statement: FORALL (u, v, w: point_2d, c, d: posreal):
  --   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES
  --    FORALL (sin_a: {x: real | -1 <= x AND x <= 1},
  --            cos_a: {x: real | -1 <= x AND x <= 1}, m: V...
  have no_perp_mid_segment_intersection_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: no_perp_mid_segment_intersection_2_TCC3
  -- Statement: FORALL (u, v, w: point_2d, c, d: posreal):
  --   u /= v AND v /= w AND w /= u AND d <= 1 IMPLIES
  --    FORALL (sin_a: {x: real | -1 <= x AND x <= 1},
  --            cos_a: {x: real | -1 <= x AND x <= 1}, m: V...
  have no_perp_mid_segment_intersection_2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.segments_2d_trig