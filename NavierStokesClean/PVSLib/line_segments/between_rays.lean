-- Auto-generated by pvs2lean/translator.py
-- PVS theory: between_rays
-- PVSLib library: line_segments
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: between_rays

Auto-generated stubs from PVSLib theory `between_rays`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.between_rays

noncomputable section

variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (cross : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (dir_parallel_p : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (miter_point : pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} (norm : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (off_right_side_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (point_on_arb_ray_p : pvs_τ19 → pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (point_on_segment_p : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (reverse_segment : pvs_τ24 → pvs_τ25)

-- pvs2lean: between_rays.between_rays?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET u = s`p1, v = e`p1, w = e`p2, uv = u - v, pv = p - v, wv = w - v IN
--   IF cross(uv, wv) < 0 THEN cross(uv, pv) > 0 OR cross(wv, pv) < 0
--   ELSE cross(uv, pv) > 0 AND cross(wv, pv) < 0
--   ENDIF
axiom between_rays_p {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop

-- pvs2lean: between_rays.point_between_rays?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET u = s`p1, v = s`p2, w = e`p2, vu = v - u, pu = p - u, wu = w - u IN
--   IF cross(vu, wu) < 0 THEN cross(vu, pu) > 0 OR cross(wu, pu) < 0
--   ELSE cross(vu, pu) > 0 AND cross(wu, pu) < 0
--   ENDIF
axiom point_between_rays_p {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → Prop

-- pvs2lean: between_rays.leftward_point_not_on_ray_of_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d):
--   p`x < e`p1`x AND e`p1`x <= e`p2`x IMPLIES
--    NOT point_on_arb_ray?(p, e`p1, ^(e`p2 - e`p1))
theorem leftward_point_not_on_ray_of_edge : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), p.x < e.p1.x ∧ e.p1.x ≤ e.p2.x → ¬ point_on_arb_ray_p p e.p1 (^(e.p2 - e.p1)) := by
  -- === TCCs for leftward_point_not_on_ray_of_edge ===
  -- TCC: leftward_point_not_on_ray_of_edge_TCC1
  -- Statement: FORALL (p: point_2d, e: segment_2d):
  --   p`x < e`p1`x AND e`p1`x <= e`p2`x IMPLIES e`p2 - e`p1 /= zero
  have leftward_point_not_on_ray_of_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.same_line_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem same_line_between_rays : True := by
  -- === TCCs for same_line_between_rays ===
  -- TCC: same_line_between_rays_TCC1
  -- Statement: FORALL (p, q, r: point_2d): p /= q AND q /= r IMPLIES q /= p
  have same_line_between_rays_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: same_line_between_rays_TCC2
  -- Statement: FORALL (p, q, r: point_2d): p /= q AND q /= r IMPLIES r /= q
  have same_line_between_rays_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.collinear_segments_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem collinear_segments_between_rays : True := by
  -- === TCCs for collinear_segments_between_rays ===
  -- TCC: collinear_segments_between_rays_TCC1
  -- Statement: FORALL (s, e: segment_2d, r: point_2d):
  --   s`p2 = e`p1 AND between_rays?(s, e)(r) IMPLIES s`p2 - s`p1 /= zero
  have collinear_segments_between_rays_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: collinear_segments_between_rays_TCC2
  -- Statement: FORALL (s, e: segment_2d, p, r: point_2d, c: posreal):
  --   s`p2 = e`p1 AND between_rays?(s, e)(r) AND p = s`p2 - c * ^(s`p2 - s`p1)
  --    IMPLIES e`p2 - e`p1 /= zero
  have collinear_segments_between_rays_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: collinear_segments_between_rays_TCC3
  -- Statement: FORALL (s, e: segment_2d, p, q, r: point_2d, c, d: posreal):
  --   s`p2 = e`p1 AND
  --    between_rays?(s, e)(r) AND
  --     p = s`p2 - c * ^(s`p2 - s`p1) AND q = e`p1 + d * ^(e`p2 - e`p1)
  --    IMPLIES s`p2 /= p
  have collinear_segments_between_rays_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: collinear_segments_between_rays_TCC4
  -- Statement: FORALL (s, e: segment_2d, p, q, r: point_2d, c, d: posreal):
  --   s`p2 = e`p1 AND
  --    between_rays?(s, e)(r) AND
  --     p = s`p2 - c * ^(s`p2 - s`p1) AND q = e`p1 + d * ^(e`p2 - e`p1)
  --    IMPLIES q /= e`p1
  have collinear_segments_between_rays_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.scaled_vector_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, v: vector_2d, d: posreal):
--   between_rays?(s, e)(e`p1 + v) IMPLIES between_rays?(s, e)(e`p1 + d * v)
theorem scaled_vector_between_rays : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), between_rays_p s e(e.p1 + v) → between_rays_p s e(e.p1 + d * v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.scaled_vector_between_rays_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, v: vector_2d, d: negreal):
--   between_rays?(s, e)(e`p1 - v) IMPLIES between_rays?(s, e)(e`p1 + d * v)
theorem scaled_vector_between_rays_neg : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (v : ℝ × ℝ) (d : {x : {x : ℝ // x ≤ 0} // x < 0}), between_rays_p s e(e.p1 - v) → between_rays_p s e(e.p1 + d * v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.segment_interior_points_all_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p, q, v: point_2d):
--        (cross(s`p1 - v, e`p2 - v) >= 0) AND (v = s`p2) AND (v = e`p1)
--    AND between_rays?(s, e)(p) AND between_rays?(s, e)(q) AND (p /= q)
--    IMPLIES
--    
theorem segment_interior_points_all_between : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ) (q : ℝ × ℝ) (v : ℝ × ℝ), (cross (s.p1 - v) (e.p2 - v) ≥ 0) ∧ (v = s.p2) ∧ (v = e.p1)
 ∧ between_rays_p s e(p) ∧ between_rays_p s e(q) ∧ (p ≠ q) → ∀ (u : point_2d), point_on_segment_p u ((# p1 := p, p2 := q #)) →
   between_rays_p s e(u) := by
  -- === TCCs for segment_interior_points_all_between ===
  -- TCC: segment_interior_points_all_between_TCC1
  -- Statement: FORALL (s, e: segment_2d, p, q, v: point_2d):
  --        (cross(s`p1 - v, e`p2 - v) >= 0) AND (v = s`p2) AND (v = e`p1)
  --    AND between_rays?(s, e)(p) AND between_rays?(s, e)(q) AND (p /= q)
  --    IMPLIES ...
  have segment_interior_points_all_between_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.segment_interior_points_all_bounded
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem segment_interior_points_all_bounded : True := by
  -- === TCCs for segment_interior_points_all_bounded ===
  -- TCC: segment_interior_points_all_bounded_TCC1
  -- Statement: FORALL (p, q, v: point_2d, d: nnreal):
  --   norm(p - v) <= d AND norm(q - v) <= d AND p /= q IMPLIES q /= p
  have segment_interior_points_all_bounded_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.segment_interior_points_all_norms_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q, v: point_2d):
--   norm(p - v) > 0 AND norm(q - v) > 0 AND p /= q AND ^(p - v) /= ^(v - q)
--    IMPLIES
--    FORALL (u: point_2d):
--      point_on_segment?(u, (# p1 := p, p2 := q #)) IMPLIES norm
theorem segment_interior_points_all_norms_pos : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : ℝ × ℝ), norm (p - v) > 0 ∧ norm (q - v) > 0 ∧ p ≠ q ∧ ^(p - v) ≠ ^(v - q) → ∀ (u : point_2d), point_on_segment_p u ((# p1 := p, p2 := q #)) → norm (u - v) > 0 := by
  -- === TCCs for segment_interior_points_all_norms_pos ===
  -- TCC: segment_interior_points_all_norms_pos_TCC1
  -- Statement: FORALL (p, q, v: point_2d):
  --   norm(p - v) > 0 AND norm(q - v) > 0 AND p /= q IMPLIES p - v /= zero
  have segment_interior_points_all_norms_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: segment_interior_points_all_norms_pos_TCC2
  -- Statement: FORALL (p, q, v: point_2d):
  --   norm(p - v) > 0 AND norm(q - v) > 0 AND p /= q IMPLIES v - q /= zero
  have segment_interior_points_all_norms_pos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: segment_interior_points_all_norms_pos_TCC3
  -- Statement: FORALL (p, q, v: point_2d):
  --   norm(p - v) > 0 AND norm(q - v) > 0 AND p /= q AND ^(p - v) /= ^(v - q)
  --    IMPLIES q /= p
  have segment_interior_points_all_norms_pos_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.on_ray_right_side_imp_on_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem on_ray_right_side_imp_on_segment : True := by
  -- === TCCs for on_ray_right_side_imp_on_segment ===
  -- TCC: on_ray_right_side_imp_on_segment_TCC1
  -- Statement: FORALL (u, v: point_2d, e: segment_2d): u /= v IMPLIES v - u /= zero
  have on_ray_right_side_imp_on_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: on_ray_right_side_imp_on_segment_TCC2
  -- Statement: FORALL (u, v, w: point_2d, e: segment_2d):
  --        (u /= v) AND point_on_arb_ray?(w, u, ^(v - u))
  --    AND point_on_segment?(w, e) AND off_right_side?(u, e)
  --    AND off_right_side?(v, e)
  --    IMPLIES w /= u
  have on_ray_right_side_imp_on_segment_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.between_rays_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   s`p2 = e`p1 IMPLIES
--    (between_rays?(s, e)(p) IFF
--      point_between_rays?(reverse_segment(s), e)(p))
theorem between_rays_equiv : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p2 = e.p1 → (between_rays_p s e(p) ↔ point_between_rays_p (reverse_segment s) e(p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_between_rays_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   s`p1 = e`p1 IMPLIES
--    (point_between_rays?(s, e)(p) IFF
--      between_rays?(reverse_segment(s), e)(p))
theorem point_between_rays_equiv : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p1 = e.p1 → (point_between_rays_p s e(p) ↔ between_rays_p (reverse_segment s) e(p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_between_rays_irreflexive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d): NOT point_between_rays?(e, e)(p)
theorem point_between_rays_irreflexive : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬(point_between_rays_p e e p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_on_neg_ray_between_pos_rays_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d, d: negreal):
--   s`p1 = e`p1 AND
--    p = s`p1 + d * ^(s`p2 - s`p1) AND cross(s`p2 - s`p1, e`p2 - e`p1) > 0
--    IMPLIES point_between_rays?(e, s)(p)
theorem point_on_neg_ray_between_pos_rays_1 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (d : {x : {x : ℝ // x ≤ 0} // x < 0}), s.p1 = e.p1 ∧
 p = s.p1 + d * ^(s.p2 - s.p1) ∧ cross (s.p2 - s.p1) (e.p2 - e.p1) > 0 → point_between_rays_p e s(p) := by
  -- === TCCs for point_on_neg_ray_between_pos_rays_1 ===
  -- TCC: point_on_neg_ray_between_pos_rays_1_TCC1
  -- Statement: FORALL (s, e: segment_2d): s`p1 = e`p1 IMPLIES s`p2 - s`p1 /= zero
  have point_on_neg_ray_between_pos_rays_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_on_neg_ray_between_pos_rays_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d, d: negreal):
--   s`p1 = e`p1 AND
--    p = s`p1 + d * ^(s`p2 - s`p1) AND cross(s`p2 - s`p1, e`p2 - e`p1) < 0
--    IMPLIES point_between_rays?(s, e)(p)
theorem point_on_neg_ray_between_pos_rays_2 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (d : {x : {x : ℝ // x ≤ 0} // x < 0}), s.p1 = e.p1 ∧
 p = s.p1 + d * ^(s.p2 - s.p1) ∧ cross (s.p2 - s.p1) (e.p2 - e.p1) < 0 → point_between_rays_p s e(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_between_rays_antisymmetric
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   s`p1 = e`p1 AND point_between_rays?(s, e)(p) IMPLIES
--    NOT point_between_rays?(e, s)(p)
theorem point_between_rays_antisymmetric : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p1 = e.p1 ∧ point_between_rays_p s e(p) → ¬ point_between_rays_p e s(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_between_rays_antisymmetric_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   s`p1 = e`p1 IMPLIES
--         point_between_rays?(s, e)(p) OR point_between_rays?(e, s)(p)
--     OR point_on_arb_ray?(p, s`p1, ^(s`p2 - s`p1))
--     OR point_on_arb
theorem point_between_rays_antisymmetric_2 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p1 = e.p1 → point_between_rays_p s e(p) ∨ point_between_rays_p e s(p)
 ∨ point_on_arb_ray_p p s.p1 (^(s.p2 - s.p1))
 ∨ point_on_arb_ray_p p e.p1 (^(e.p2 - e.p1))
 ∨ point_on_arb_ray_p s.p2 e.p1 (^(e.p2 - e.p1)) := by
  -- === TCCs for point_between_rays_antisymmetric_2 ===
  -- TCC: point_between_rays_antisymmetric_2_TCC1
  -- Statement: FORALL (p: point_2d, s, e: segment_2d):
  --   s`p1 = e`p1 AND
  --    NOT point_between_rays?(s, e)(p) AND
  --     NOT point_between_rays?(e, s)(p) AND
  --      NOT point_on_arb_ray?(p, s`p1, ^(s`p2 - s`p1))
  --    IMP...
  have point_between_rays_antisymmetric_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.between_rays_antisymmetric_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   s`p2 = e`p1 IMPLIES
--         between_rays?(s, e)(p)
--     OR between_rays?(reverse_segment(e), reverse_segment(s))(p)
--     OR point_on_arb_ray?(p, s`p2, ^(s`p1 - 
theorem between_rays_antisymmetric_2 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p2 = e.p1 → between_rays_p s e(p)
 ∨ between_rays_p (reverse_segment e) (reverse_segment s) (p)
 ∨ point_on_arb_ray_p p s.p2 (^(s.p1 - s.p2))
 ∨ point_on_arb_ray_p p e.p1 (^(e.p2 - e.p1))
 ∨ point_on_arb_ray_p s.p1 e.p1 (^(e.p2 - e.p1)) := by
  -- === TCCs for between_rays_antisymmetric_2 ===
  -- TCC: between_rays_antisymmetric_2_TCC1
  -- Statement: FORALL (p: point_2d, s, e: segment_2d):
  --   s`p2 = e`p1 AND
  --    NOT between_rays?(s, e)(p) AND
  --     NOT between_rays?(reverse_segment(e), reverse_segment(s))(p)
  --    IMPLIES s`p1 - s`p2 /= zero
  have between_rays_antisymmetric_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: between_rays_antisymmetric_2_TCC2
  -- Statement: FORALL (p: point_2d, s, e: segment_2d):
  --   s`p2 = e`p1 AND
  --    NOT between_rays?(s, e)(p) AND
  --     NOT between_rays?(reverse_segment(e), reverse_segment(s))(p) AND
  --      NOT point_on_arb_ray?(p, s`p2, ...
  have between_rays_antisymmetric_2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.extend_between_rays_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e, h: segment_2d, u: point_2d):
--   s`p2 = e`p1 AND
--    s`p2 = h`p1 AND between_rays?(s, h)(u) AND between_rays?(s, e)(h`p2)
--    IMPLIES between_rays?(s, e)(u)
theorem extend_between_rays_left : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (u : ℝ × ℝ), s.p2 = e.p1 ∧
 s.p2 = h.p1 ∧ between_rays_p s h(u) ∧ between_rays_p s e(h.p2) → between_rays_p s e(u) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.on_segment_not_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, e: segment_2d, p: point_2d):
--   s`p2 = e`p1 AND (point_on_segment?(p, s) OR point_on_segment?(p, e))
--    IMPLIES NOT between_rays?(s, e)(p)
theorem on_segment_not_between_rays : ∀ (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (p : ℝ × ℝ), s.p2 = e.p1 ∧ (point_on_segment_p p s ∨ point_on_segment_p p e) → ¬ between_rays_p s e(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.on_segment_btw_rays_imp_all_btw
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, s, e, h: segment_2d):
--        (s`p2 = e`p1) AND (e`p1 = h`p1) AND (q /= h`p1)
--    AND point_on_segment?(p, h) AND point_on_segment?(q, h)
--    AND between_rays?(s, e)(p)
--    IMPLIES
theorem on_segment_btw_rays_imp_all_btw : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p2 = e.p1) ∧ (e.p1 = h.p1) ∧ (q ≠ h.p1)
 ∧ point_on_segment_p p h ∧ point_on_segment_p q h
 ∧ between_rays_p s e(p) → between_rays_p s e(q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.between_rays_rotate_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem between_rays_rotate_left : True := by
  -- === TCCs for between_rays_rotate_left ===
  -- TCC: between_rays_rotate_left_TCC1
  -- Statement: FORALL (p: point_2d, s, e: segment_2d, u: point_2d,
  --         v: {p: point_2d | p /= s`p1},
  --         vp: [# p1: {p: point_2d | p /= s`p1}, p2: point_2d #],
  --         re: segment_2d):
  --        (u = s`p1) A...
  have between_rays_rotate_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.between_rays_rotate_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem between_rays_rotate_right : True := by
  -- === TCCs for between_rays_rotate_right ===
  -- TCC: between_rays_rotate_right_TCC1
  -- Statement: FORALL (p: point_2d, s, e: segment_2d, w: {p: point_2d | p /= e`p1},
  --         v: {p: point_2d | p /= s`p1},
  --         pv: [# p1: point_2d, p2: {p: point_2d | p /= s`p1} #],
  --         rs: segment_2d):
  --   ...
  have between_rays_rotate_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.point_between_rays_rotate_left
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_between_rays_rotate_left : True := by
  -- === TCCs for point_between_rays_rotate_left ===
  -- TCC: point_between_rays_rotate_left_TCC1
  -- Statement: FORALL (p: point_2d, s, e: segment_2d, u: point_2d,
  --         v: {p: point_2d | p /= s`p1},
  --         up: [# p1: point_2d, p2: point_2d #]):
  --        (u = s`p1) AND (v = s`p2) AND (up = (# p1 := u, p2 :=...
  have point_between_rays_rotate_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.point_between_rays_rotate_right
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_between_rays_rotate_right : True := by
  -- === TCCs for point_between_rays_rotate_right ===
  -- TCC: point_between_rays_rotate_right_TCC1
  -- Statement: FORALL (p: point_2d, s, e: segment_2d, w: {p: point_2d | p /= e`p1},
  --         u: point_2d, up: [# p1: point_2d, p2: point_2d #]):
  --        (w = e`p2) AND (u = s`p1) AND (up = (# p1 := u, p2 := p #))
  --  ...
  have point_between_rays_rotate_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.point_between_rays_with_overlap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem point_between_rays_with_overlap : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.between_rays_both_angles_narrow
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   s`p1 = e`p1 AND
--    point_between_rays?(s, e)(p) AND cross(s`p2 - s`p1, e`p2 - e`p1) > 0
--    IMPLIES cross(s`p2 - s`p1, p - s`p1) > 0
theorem between_rays_both_angles_narrow : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p1 = e.p1 ∧
 point_between_rays_p s e(p) ∧ cross (s.p2 - s.p1) (e.p2 - e.p1) > 0 → cross (s.p2 - s.p1) (p - s.p1) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.between_rays_both_angles_wide
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   s`p1 = e`p1 AND
--    point_between_rays?(s, e)(p) AND cross(s`p2 - s`p1, p - e`p1) < 0
--    IMPLIES cross(s`p2 - s`p1, e`p2 - e`p1) < 0
theorem between_rays_both_angles_wide : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p1 = e.p1 ∧
 point_between_rays_p s e(p) ∧ cross (s.p2 - s.p1) (p - e.p1) < 0 → cross (s.p2 - s.p1) (e.p2 - e.p1) < 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_between_rays_not_on_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e: segment_2d):
--   s`p1 = e`p1 AND point_between_rays?(s, e)(p) IMPLIES
--    NOT (point_on_arb_ray?(p, s`p1, ^(s`p2 - s`p1)) OR
--          point_on_arb_ray?(p, e`p1, ^(e`p2 - e`p1))
theorem point_between_rays_not_on_rays : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p1 = e.p1 ∧ point_between_rays_p s e(p) → ¬ (point_on_arb_ray_p p s.p1 (^(s.p2 - s.p1)) ∨
      point_on_arb_ray_p p e.p1 (^(e.p2 - e.p1)) ∨
       point_on_arb_ray_p s.p2 e.p1 (^(e.p2 - e.p1))) := by
  -- === TCCs for point_between_rays_not_on_rays ===
  -- TCC: point_between_rays_not_on_rays_TCC1
  -- Statement: FORALL (p: point_2d, s, e: segment_2d):
  --   s`p1 = e`p1 AND
  --    point_between_rays?(s, e)(p) AND
  --     NOT point_on_arb_ray?(p, s`p1, ^(s`p2 - s`p1))
  --    IMPLIES e`p2 - e`p1 /= zero
  have point_between_rays_not_on_rays_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.on_ray_also_point_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e, h: segment_2d):
--   s`p1 = e`p1 AND
--    s`p1 = h`p1 AND
--     point_between_rays?(s, e)(p) AND
--      point_on_arb_ray?(h`p2, s`p1, ^(s`p2 - s`p1))
--    IMPLIES point_between_rays?(h
theorem on_ray_also_point_between_rays : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), s.p1 = e.p1 ∧
 s.p1 = h.p1 ∧
  point_between_rays_p s e(p) ∧
   point_on_arb_ray_p h.p2 s.p1 (^(s.p2 - s.p1)) → point_between_rays_p h e(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_between_imp_pos_crosses
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, a, b: segment_2d):
--   a`p1 = b`p1 AND point_between_rays?(a, b)(p) IMPLIES
--    cross(p - b`p1, b`p2 - b`p1) > 0 OR cross(b`p2 - b`p1, a`p2 - a`p1) > 0
theorem point_between_imp_pos_crosses : ∀ (p : ℝ × ℝ) (a : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (b : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), a.p1 = b.p1 ∧ point_between_rays_p a b(p) → cross (p - b.p1) (b.p2 - b.p1) > 0 ∨ cross (b.p2 - b.p1) (a.p2 - a.p1) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_between_imp_pos_crosses_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, a, b: segment_2d):
--   a`p1 = b`p1 AND point_between_rays?(a, b)(p) IMPLIES
--    cross(a`p2 - a`p1, p - a`p1) > 0 OR cross(b`p2 - b`p1, a`p2 - a`p1) > 0
theorem point_between_imp_pos_crosses_2 : ∀ (p : ℝ × ℝ) (a : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (b : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), a.p1 = b.p1 ∧ point_between_rays_p a b(p) → cross (a.p2 - a.p1) (p - a.p1) > 0 ∨ cross (b.p2 - b.p1) (a.p2 - a.p1) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.point_btw_rays_imp_opp_side_crossing
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, a, b, c: point_2d):
--   a /= b AND
--    a /= c AND
--     cross(c - a, b - a) > 0 AND
--      point_between_rays?((# p1 := a, p2 := c #), (# p1 := a, p2 := b #))(p)
--    IMPLIES
--    EXISTS (q:
theorem point_btw_rays_imp_opp_side_crossing : ∀ (p : ℝ × ℝ) (a : ℝ × ℝ) (b : ℝ × ℝ) (c : ℝ × ℝ), a ≠ b ∧
 a ≠ c ∧
  cross (c - a) (b - a) > 0 ∧
   point_between_rays_p ((# p1 := a, p2 := c #)) ((# p1 := a, p2 := b #)) (p) → ∃ (q : point_2d), point_on_segment_p q ((# p1 := b, p2 := c #)) ∧
   point_on_arb_ray_p q a (^(p - a)) := by
  -- === TCCs for point_btw_rays_imp_opp_side_crossing ===
  -- TCC: point_btw_rays_imp_opp_side_crossing_TCC1
  -- Statement: FORALL (a, b, c: point_2d):
  --   a /= b AND a /= c AND cross(c - a, b - a) > 0 IMPLIES c /= a
  have point_btw_rays_imp_opp_side_crossing_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_btw_rays_imp_opp_side_crossing_TCC2
  -- Statement: FORALL (a, b, c: point_2d):
  --   a /= b AND a /= c AND cross(c - a, b - a) > 0 IMPLIES b /= a
  have point_btw_rays_imp_opp_side_crossing_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_btw_rays_imp_opp_side_crossing_TCC3
  -- Statement: FORALL (p: point_2d, a, b, c: point_2d):
  --   a /= b AND
  --    a /= c AND
  --     cross(c - a, b - a) > 0 AND
  --      point_between_rays?((# p1 := a, p2 := c #), (# p1 := a, p2 := b #))(p)
  --    IMPLIES c /= b
  have point_btw_rays_imp_opp_side_crossing_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: point_btw_rays_imp_opp_side_crossing_TCC4
  -- Statement: FORALL (p: point_2d, a, b, c: point_2d):
  --   a /= b AND
  --    a /= c AND
  --     cross(c - a, b - a) > 0 AND
  --      point_between_rays?((# p1 := a, p2 := c #), (# p1 := a, p2 := b #))(p)
  --    IMPLIES
  --    FORALL ...
  have point_btw_rays_imp_opp_side_crossing_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.on_segment_or_between_rays
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, e: segment_2d):
--        (u = v) OR point_on_segment?(u, e)
--    OR (cross(e`p1 - u, e`p2 - u) >= 0)
--    OR (EXISTS (w: point_2d):
--          point_on_segment?(w, e) AND point_on_arb_
theorem on_segment_or_between_rays : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), u = v ∨ point_on_segment_p u e ∨
 cross (e.p1 - u) (e.p2 - u) ≥ 0 ∨
  (∃ (w : point_2d), point_on_segment_p w e ∧ point_on_arb_ray_p w u (^(v - u)))
   ∨
   point_between_rays_p ((# p1 := u, p2 := e.p1 #)) ((# p1 := u, p2 := e.p2 #))
                      (v) := by
  -- === TCCs for on_segment_or_between_rays ===
  -- TCC: on_segment_or_between_rays_TCC1
  -- Statement: FORALL (u, v: point_2d, e: segment_2d):
  --   (NOT u = v AND
  --     NOT point_on_segment?(u, e) AND
  --      NOT cross(e`p1 - u, e`p2 - u) >= 0 AND
  --       NOT EXISTS (w: point_2d):
  --             point_on_segment...
  have on_segment_or_between_rays_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: on_segment_or_between_rays_TCC2
  -- Statement: FORALL (u, v: point_2d, e: segment_2d):
  --   (NOT u = v AND
  --     NOT point_on_segment?(u, e) AND
  --      NOT cross(e`p1 - u, e`p2 - u) >= 0 AND
  --       NOT EXISTS (w: point_2d):
  --             point_on_segment...
  have on_segment_or_between_rays_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.interpolate_point_between_rays
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem interpolate_point_between_rays : True := by
  -- === TCCs for interpolate_point_between_rays ===
  -- TCC: interpolate_point_between_rays_TCC1
  -- Statement: FORALL (p: point_2d, s, e, h: segment_2d):
  --        (s`p1 = e`p1) AND (s`p1 = h`p1) AND (h`p1 /= p)
  --    AND point_between_rays?(s, e)(p) AND point_between_rays?(s, h)(e`p2)
  --    IMPLIES p /= h`p1
  have interpolate_point_between_rays_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.interpolate_point_between_rays_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem interpolate_point_between_rays_rev : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.interpolate_point_between_rays_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e, h: segment_2d):
--        (s`p1 = e`p1) AND (s`p1 = h`p1) AND point_between_rays?(s, h)(p)
--    AND (NOT point_between_rays?(s, e)(p))
--    AND NOT point_on_arb_ray?(p, e`p1, ^(e`p
theorem interpolate_point_between_rays_2 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p1 = e.p1) ∧ (s.p1 = h.p1) ∧ point_between_rays_p s h(p)
 ∧ (¬ point_between_rays_p s e(p))
 ∧ ¬ point_on_arb_ray_p p e.p1 (^(e.p2 - e.p1)) → point_between_rays_p e h(p) := by
  -- === TCCs for interpolate_point_between_rays_2 ===
  -- TCC: interpolate_point_between_rays_2_TCC1
  -- Statement: FORALL (p: point_2d, s, e, h: segment_2d):
  --   s`p1 = e`p1 AND
  --    s`p1 = h`p1 AND
  --     point_between_rays?(s, h)(p) AND NOT point_between_rays?(s, e)(p)
  --    IMPLIES e`p2 - e`p1 /= zero
  have interpolate_point_between_rays_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.interpolate_point_between_rays_3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e, h: segment_2d):
--        (s`p1 = e`p1) AND (s`p1 = h`p1) AND (h`p1 /= p)
--    AND point_between_rays?(s, e)(p) AND point_between_rays?(h, s)(p)
--    IMPLIES point_between_rays?(s,
theorem interpolate_point_between_rays_3 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p1 = e.p1) ∧ (s.p1 = h.p1) ∧ (h.p1 ≠ p)
 ∧ point_between_rays_p s e(p) ∧ point_between_rays_p h s(p) → point_between_rays_p s e(h.p2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.interpolate_point_between_rays_3_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e, h: segment_2d):
--        (s`p1 = e`p1) AND (s`p1 = h`p1) AND (h`p1 /= p)
--    AND point_between_rays?(s, e)(p) AND point_between_rays?(e, h)(p)
--    IMPLIES point_between_rays?(s,
theorem interpolate_point_between_rays_3_rev : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p1 = e.p1) ∧ (s.p1 = h.p1) ∧ (h.p1 ≠ p)
 ∧ point_between_rays_p s e(p) ∧ point_between_rays_p e h(p) → point_between_rays_p s e(h.p2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.interpolate_point_between_rays_4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem interpolate_point_between_rays_4 : True := by
  -- === TCCs for interpolate_point_between_rays_4 ===
  -- TCC: interpolate_point_between_rays_4_TCC1
  -- Statement: FORALL (p, q: point_2d, s, e: segment_2d):
  --        (s`p1 = e`p1) AND (s`p1 /= p) AND (s`p1 /= q)
  --    AND point_between_rays?(s, e)(p) AND point_between_rays?(e, s)(q)
  --    IMPLIES p /= s`p1
  have interpolate_point_between_rays_4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.interpolate_point_between_rays_5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, g, h: segment_2d):
--        (s`p1 = g`p1) AND (s`p1 = h`p1) AND (h`p1 /= p)
--    AND point_between_rays?(s, g)(p) AND point_between_rays?(g, h)(p)
--    IMPLIES point_between_rays?(s,
theorem interpolate_point_between_rays_5 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (g : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p1 = g.p1) ∧ (s.p1 = h.p1) ∧ (h.p1 ≠ p)
 ∧ point_between_rays_p s g(p) ∧ point_between_rays_p g h(p) → point_between_rays_p s h(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.interpolate_point_between_rays_6
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e, g, h: segment_2d):
--        (s`p1 = e`p1) AND (s`p1 = g`p1) AND (s`p1 = h`p1) AND (h`p1 /= p)
--    AND point_between_rays?(s, e)(p) AND point_between_rays?(g, h)(p)
--    AND point
theorem interpolate_point_between_rays_6 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (g : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p1 = e.p1) ∧ (s.p1 = g.p1) ∧ (s.p1 = h.p1) ∧ (h.p1 ≠ p)
 ∧ point_between_rays_p s e(p) ∧ point_between_rays_p g h(p)
 ∧ point_between_rays_p g s(p) ∧ point_between_rays_p e h(p) → point_between_rays_p s h(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.interpolate_point_between_rays_7
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, s, e, g, h: segment_2d):
--        (s`p1 = e`p1) AND (s`p1 = g`p1) AND (s`p1 = h`p1) AND (h`p1 /= p)
--    AND point_between_rays?(s, e)(p) AND point_between_rays?(g, h)(p)
--    AND point
theorem interpolate_point_between_rays_7 : ∀ (p : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (g : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (h : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p1 = e.p1) ∧ (s.p1 = g.p1) ∧ (s.p1 = h.p1) ∧ (h.p1 ≠ p)
 ∧ point_between_rays_p s e(p) ∧ point_between_rays_p g h(p)
 ∧ point_between_rays_p g s(p) → point_between_rays_p s h(p) ∨ point_between_rays_p h e(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: between_rays.interpolate_point_between_rays_8
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem interpolate_point_between_rays_8 : True := by
  -- === TCCs for interpolate_point_between_rays_8 ===
  -- TCC: interpolate_point_between_rays_8_TCC1
  -- Statement: FORALL (p: point_2d, s, e, g, h: segment_2d):
  --        (s`p1 = e`p1) AND (s`p1 = g`p1) AND (s`p1 = h`p1) AND (h`p1 /= p)
  --    AND point_between_rays?(s, g)(p) AND point_between_rays?(s, g)(h`p2)
  --    AND...
  have interpolate_point_between_rays_8_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: between_rays.miter_point_between_segments
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem miter_point_between_segments : True := by
  -- === TCCs for miter_point_between_segments ===
  -- TCC: miter_point_between_segments_TCC1
  -- Statement: FORALL (v: point_2d, u: {p: point_2d | p /= v},
  --         w: {p: point_2d | p /= v AND NOT dir_parallel?(u - v, p - v)}):
  --   v /= u
  have miter_point_between_segments_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.between_rays