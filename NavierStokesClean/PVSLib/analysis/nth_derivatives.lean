-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nth_derivatives
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nth_derivatives

Auto-generated stubs from PVSLib theory `nth_derivatives`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nth_derivatives

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (deriv : pvs_τ1 → pvs_τ2)
variable {pvs_τ5 : Type*} (derivable_p : pvs_τ5 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (nderiv_fun : pvs_τ8 → pvs_τ9)

-- pvs2lean: nth_derivatives.T_pred
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: nth_derivatives.deriv_domain
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem deriv_domain : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nth_derivatives.not_one_element
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem not_one_element : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nth_derivatives.derivable_n_times?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom derivable_n_times_p {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: nth_derivatives.derivable_n_times_lem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem derivable_n_times_lem : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nth_derivatives.derivable_n_times_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem derivable_n_times_def : True := by
  -- === TCCs for derivable_n_times_def ===
  -- TCC: derivable_n_times_def_TCC1
  -- Statement: FORALL (f: [T -> real], n: nat):
  --   derivable_n_times?(f, n + 1) IMPLIES derivable?[T](f)
  have derivable_n_times_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: derivable_n_times_def_TCC2
  -- Statement: FORALL (f: [T -> real], n: nat):
  --   derivable_n_times?(f, n + 1) IMPLIES deriv_domain?[T]
  have derivable_n_times_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: derivable_n_times_def_TCC3
  -- Statement: FORALL (f: [T -> real], n: nat):
  --   derivable_n_times?(f, n + 1) IMPLIES not_one_element?[T]
  have derivable_n_times_def_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nth_derivatives.nderiv
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
axiom nderiv {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ6 → pvs_τ7 → Prop

-- pvs2lean: nth_derivatives.nderiv_derivable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem nderiv_derivable : True := by
  -- === TCCs for nderiv_derivable ===
  -- TCC: nderiv_derivable_TCC1
  -- Statement: FORALL (f: [T -> real], m, n: nat):
  --   derivable_n_times?(f, 1 + n) AND m <= n IMPLIES derivable_n_times?(f, m)
  have nderiv_derivable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_derivable_TCC2
  -- Statement: FORALL (f: [T -> real], m, n: nat):
  --   derivable_n_times?(f, 1 + n) AND m <= n IMPLIES deriv_domain?[T]
  have nderiv_derivable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_derivable_TCC3
  -- Statement: FORALL (f: [T -> real], m, n: nat):
  --   derivable_n_times?(f, 1 + n) AND m <= n IMPLIES not_one_element?[T]
  have nderiv_derivable_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nth_derivatives.nderiv_derivable_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem nderiv_derivable_aux : True := by
  -- === TCCs for nderiv_derivable_aux ===
  -- TCC: nderiv_derivable_aux_TCC1
  -- Statement: FORALL (f: [T -> real], n: nat):
  --   derivable_n_times?(f, n + 1) IMPLIES derivable_n_times?(f, 1 + n)
  have nderiv_derivable_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_derivable_aux_TCC2
  -- Statement: FORALL (f: [T -> real], n: nat):
  --   derivable_n_times?(f, n + 1) IMPLIES derivable_n_times?(f, n)
  have nderiv_derivable_aux_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_derivable_aux_TCC3
  -- Statement: FORALL (f: [T -> real], n: nat):
  --   derivable_n_times?(f, n + 1) IMPLIES derivable?[T](nderiv(n, f))
  have nderiv_derivable_aux_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nth_derivatives.nderiv_derivable_eqv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem nderiv_derivable_eqv : True := by
  -- === TCCs for nderiv_derivable_eqv ===
  -- TCC: nderiv_derivable_eqv_TCC1
  -- Statement: FORALL (f: [T -> real], n: nat):
  --   derivable_n_times?(f, n) IMPLIES deriv_domain?[T]
  have nderiv_derivable_eqv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nderiv_derivable_eqv_TCC2
  -- Statement: FORALL (f: [T -> real], n: nat):
  --   derivable_n_times?(f, n) IMPLIES not_one_element?[T]
  have nderiv_derivable_eqv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nth_derivatives.derivable_n_times_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem derivable_n_times_neg : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nth_derivatives.nderiv_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem nderiv_neg : True := by
  -- === TCCs for nderiv_neg ===
  -- TCC: nderiv_neg_TCC1
  -- Statement: FORALL (n: nat, f: nderiv_fun(n)):
  --   derivable_n_times?(f, n + 1) IMPLIES derivable_n_times?(-f, n)
  have nderiv_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.nth_derivatives