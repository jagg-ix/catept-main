-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sigma_nat
-- PVSLib library: reals
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sigma_nat

Auto-generated stubs from PVSLib theory `sigma_nat`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sigma_nat

noncomputable section

variable {pvs_τ1 : Type*} (integer_pred : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (low2 : pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (mod : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (sig : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (subrange : pvs_τ8 → pvs_τ9 → pvs_τ10)

-- pvs2lean: sigma_nat.sigma_shift
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_shift : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_shift_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_shift_neg : True := by
  -- === TCCs for sigma_shift_neg ===
  -- TCC: sigma_shift_neg_TCC1
  -- Statement: FORALL (low, m: nat):
  --   low - m >= 0 IMPLIES low - m >= 0 OR EXISTS (j: nat): j <= low - m
  have sigma_shift_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_shift_neg_TCC2
  -- Statement: FORALL (low, m: nat):
  --   low - m >= 0 IMPLIES FORALL (n: nat): NOT n - m < 0 IMPLIES n - m >= 0
  have sigma_shift_neg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_shift_neg_TCC1
  -- Statement: FORALL (low, m: posnat):
  --   low - m > 0 IMPLIES
  --    low - m >= 0 AND low - m > 0 OR EXISTS (j: posnat): j <= low - m
  have sigma_shift_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_shift_neg_TCC2
  -- Statement: FORALL (low, m: posnat):
  --   low - m > 0 IMPLIES
  --    FORALL (n: posnat): NOT n - m <= 0 IMPLIES n - m >= 0 AND n - m > 0
  have sigma_shift_neg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_shift_ng2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_shift_ng2 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_shift_i
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_shift_i : True := by
  -- === TCCs for sigma_shift_i ===
  -- TCC: sigma_shift_i_TCC1
  -- Statement: FORALL (i: int, low: nat):
  --   0 <= low + i IMPLIES low + i >= 0 OR EXISTS (j: nat): j <= low + i
  have sigma_shift_i_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_shift_i_TCC2
  -- Statement: FORALL (i: int, low: nat):
  --   0 <= low + i IMPLIES FORALL (n: nat): NOT n < low IMPLIES n + i >= 0
  have sigma_shift_i_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_shift_to_zero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_shift_to_zero : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_first_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_first_ge : True := by
  -- === TCCs for sigma_first_ge ===
  -- TCC: sigma_first_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat): high >= low IMPLIES low < N
  have sigma_first_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_first_ge_TCC1
  -- Statement: FORALL (high, low: upto[N]):
  --   high >= low IMPLIES
  --    high >= 0 AND high <= N OR EXISTS (j: upto[N]): high <= j
  have sigma_first_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_split_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_split_ge : True := by
  -- === TCCs for sigma_split_ge ===
  -- TCC: sigma_split_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat, m: below[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    m >= 0 AND m < N OR EXISTS (j: below(N)): m <= j
  have sigma_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_split_ge_TCC1
  -- Statement: FORALL (high, i: int, low: nat):
  --   i <= high AND low - 1 <= i IMPLIES
  --    i + 1 >= 0 OR EXISTS (j: nat): j <= i + 1
  have sigma_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_split_ge_TCC1
  -- Statement: FORALL (high, low, m: upto[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    high >= 0 AND high <= N OR EXISTS (j: upto[N]): high <= j
  have sigma_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_split_ge_TCC2
  -- Statement: FORALL (high, low, m: upto[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    m >= 0 AND m <= N OR EXISTS (j: upto[N]): m <= j
  have sigma_split_ge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_reverse : True := by
  -- === TCCs for sigma_reverse ===
  -- TCC: sigma_reverse_TCC1
  -- Statement: FORALL (high: int, low, n: nat):
  --   NOT n > high + low IMPLIES high + low - n >= 0
  have sigma_reverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_product
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_product : True := by
  -- === TCCs for sigma_product ===
  -- TCC: sigma_product_TCC1
  -- Statement: FORALL (high2: int, low2, k: nat, i: nat):
  --   NOT i < k - high2 AND NOT i > k - low2 IMPLIES k - i >= 0
  have sigma_product_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_by2s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_by2s : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_tolambda
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_tolambda : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_bij
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_bij : True := by
  -- === TCCs for sigma_bij ===
  -- TCC: sigma_bij_TCC1
  -- Statement: FORALL (high: int, low: nat, i: subrange(low, high)): i >= 0
  have sigma_bij_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_bij_TCC2
  -- Statement: FORALL (high: int, low: nat, sig: [nat -> nat]):
  --   (FORALL (i: subrange(low, high)): low <= sig(i) AND sig(i) <= high)
  --    IMPLIES FORALL (i: subrange(low, high), j: subrange(low, high)): j >= 0
  have sigma_bij_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_inj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_inj : True := by
  -- === TCCs for sigma_inj ===
  -- TCC: sigma_inj_TCC1
  -- Statement: FORALL (high1, high2: int, low1, low2: nat, sig: [nat -> nat]):
  --   (FORALL (i: subrange(low1, high1)): low2 <= sig(i) AND sig(i) <= high2)
  --    IMPLIES FORALL (i, j: subrange(low1, high1)): i /= j IMP...
  have sigma_inj_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_inj_TCC2
  -- Statement: FORALL (high1, high2: int, low1, low2: nat, sig: [nat -> nat]):
  --   ((FORALL (i: subrange(low1, high1)): low2 <= sig(i) AND sig(i) <= high2)
  --     AND
  --     FORALL (i, j: subrange(low1, high1)): i /= j I...
  have sigma_inj_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_0_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_0_neg : True := by
  -- === TCCs for sigma_0_neg ===
  -- TCC: sigma_0_neg_TCC1
  -- Statement: FORALL (nn: negint): nn >= 0 AND nn < N OR EXISTS (j: below(N)): nn <= j
  have sigma_0_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_0_neg_TCC1
  -- Statement: FORALL (nn: negint): nn >= 0 AND nn <= N OR EXISTS (j: upto[N]): nn <= j
  have sigma_0_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_product2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_product2 : True := by
  -- === TCCs for sigma_product2 ===
  -- TCC: sigma_product2_TCC1
  -- Statement: FORALL (N: posnat, k: nat):
  --   integer_pred((k - mod(k, N)) / N) AND (k - mod(k, N)) / N >= 0
  have sigma_product2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_nat.sigma_geometric
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_geometric : True := by
  -- === TCCs for sigma_geometric ===
  -- TCC: sigma_geometric_TCC1
  -- Statement: FORALL (m, n: nat, r: real):
  --   r /= 1 AND n <= m IMPLIES FORALL (k: nat): r /= 0 OR k >= 0
  have sigma_geometric_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_geometric_TCC2
  -- Statement: FORALL (m, n: nat, r: real): r /= 1 AND n <= m IMPLIES r /= 0 OR n >= 0
  have sigma_geometric_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_geometric_TCC3
  -- Statement: FORALL (m, n: nat, r: real):
  --   r /= 1 AND n <= m IMPLIES r /= 0 OR (m + 1) >= 0
  have sigma_geometric_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_geometric_TCC4
  -- Statement: FORALL (m, n: nat, r: real): r /= 1 AND n <= m IMPLIES (1 - r) /= 0
  have sigma_geometric_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.sigma_nat