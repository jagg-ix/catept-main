-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sigma_props
-- PVSLib library: reals
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sigma_props

Auto-generated stubs from PVSLib theory `sigma_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sigma_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (subrange : pvs_τ1 → pvs_τ2 → pvs_τ3)

-- pvs2lean: sigma_props.sigma_unfolding_theory_instantiation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_unfolding_theory_instantiation : True := by
  -- === TCCs for sigma_unfolding_theory_instantiation ===
  -- TCC: sigma_unfolding_theory_instantiation_TCC1
  -- Statement: FORALL (low_th: nat, low: nat | low_th <= low, high: nat | low <= high,
  --         high_th: nat | high_th >= high):
  --   low_th <= low AND low <= 1 + high_th OR
  --    EXISTS (j: subrange(low_th, 1 + high_th...
  have sigma_unfolding_theory_instantiation_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_unfolding_theory_instantiation_TCC2
  -- Statement: FORALL (low_th: nat, low: nat | low_th <= low, high: nat | low <= high,
  --         high_th: nat | high_th >= high):
  --   low_th <= high AND high <= 1 + high_th OR
  --    EXISTS (j: subrange(low_th, 1 + high_...
  have sigma_unfolding_theory_instantiation_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_unfolding_theory_instantiation_TCC3
  -- Statement: FORALL (low_th: nat, low: nat | low_th <= low, high: nat | low <= high,
  --         high_th: nat | high_th >= high):
  --   (FORALL (x, y: subrange(low_th, 1 + high_th)), (z: integer):
  --      x <= z AND z <= ...
  have sigma_unfolding_theory_instantiation_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_unfolding_theory_instantiation_TCC4
  -- Statement: FORALL (low_th: nat, low: nat | low_th <= low, high: nat | low <= high,
  --         high_th: nat | high_th >= high):
  --   low_th <= low AND low <= high_th OR
  --    EXISTS (j: subrange(low_th, high_th)): j <=...
  have sigma_unfolding_theory_instantiation_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_unfolding_theory_instantiation_TCC5
  -- Statement: FORALL (low_th: nat, low: nat | low_th <= low, high: nat | low <= high,
  --         high_th: nat | high_th >= high):
  --   low_th <= high AND high <= high_th OR
  --    EXISTS (j: subrange(low_th, high_th)): hi...
  have sigma_unfolding_theory_instantiation_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_unfolding_theory_instantiation_TCC6
  -- Statement: FORALL (low_th: nat, low: nat | low_th <= low, high: nat | low <= high,
  --         high_th: nat | high_th >= high):
  --   (FORALL (x, y: subrange(low_th, high_th)), (z: integer):
  --      x <= z AND z <= y IM...
  have sigma_unfolding_theory_instantiation_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_props.sigma_constrains_theory
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_constrains_theory : True := by
  -- === TCCs for sigma_constrains_theory ===
  -- TCC: sigma_constrains_theory_TCC1
  -- Statement: FORALL (low: nat, high: nat | low <= high):
  --   low >= 0 OR EXISTS (j: nat): j <= low
  have sigma_constrains_theory_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_constrains_theory_TCC2
  -- Statement: FORALL (low: nat, high: nat | low <= high):
  --   high >= 0 OR EXISTS (j: nat): high <= j
  have sigma_constrains_theory_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_constrains_theory_TCC3
  -- Statement: FORALL (low: nat, high: nat | low <= high):
  --   (FORALL (x, y: nat), (z: integer): x <= z AND z <= y IMPLIES z >= 0)
  have sigma_constrains_theory_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_constrains_theory_TCC4
  -- Statement: FORALL (low: nat, high: nat | low <= high):
  --   low <= low AND low <= high OR EXISTS (j: subrange(low, high)): j <= low
  have sigma_constrains_theory_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_constrains_theory_TCC5
  -- Statement: FORALL (low: nat, high: nat | low <= high):
  --   low <= high AND high <= high OR EXISTS (j: subrange(low, high)): high <= j
  have sigma_constrains_theory_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_constrains_theory_TCC6
  -- Statement: FORALL (low: nat, high: nat | low <= high):
  --   (FORALL (x, y: subrange(low, high)), (z: integer):
  --      x <= z AND z <= y IMPLIES low <= z AND z <= high)
  have sigma_constrains_theory_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.sigma_props