-- Auto-generated by pvs2lean/translator.py
-- PVS theory: circles_and_lines
-- PVSLib library: reals
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: circles_and_lines

Auto-generated stubs from PVSLib theory `circles_and_lines`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.circles_and_lines

noncomputable section

-- pvs2lean: circles_and_lines.t1_lt_t2_lt_D
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t, t1, t2, D: real):
--        (vx * vx + vy * vy /= 0) AND (t1 < t2)
--    AND ((sx + vx * t1) * (sx + vx * t1) + (sy + vy * t1) * (sy + vy * t1) =
--          D)
--    AND ((sx + vx * t2) * (sx + vx * t
theorem t1_lt_t2_lt_D : ∀ (t : ℝ) (t1 : ℝ) (t2 : ℝ) (D : ℝ), (vx * vx + vy * vy ≠ 0) ∧ (t1 < t2)
 ∧ ((sx + vx * t1) * (sx + vx * t1) + (sy + vy * t1) * (sy + vy * t1) = D)
 ∧ ((sx + vx * t2) * (sx + vx * t2) + (sy + vy * t2) * (sy + vy * t2) = D)
 ∧ (t1 < t) ∧ (t < t2) → (sx + vx * t) * (sx + vx * t) + (sy + vy * t) * (sy + vy * t) < D := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circles_and_lines.lt_D_t1_lt_t2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t, t1, t2, D: real):
--        (vx * vx + vy * vy /= 0) AND (t1 < t2)
--    AND ((sx + vx * t1) * (sx + vx * t1) + (sy + vy * t1) * (sy + vy * t1) =
--          D)
--    AND ((sx + vx * t2) * (sx + vx * t
theorem lt_D_t1_lt_t2 : ∀ (t : ℝ) (t1 : ℝ) (t2 : ℝ) (D : ℝ), (vx * vx + vy * vy ≠ 0) ∧ (t1 < t2)
 ∧ ((sx + vx * t1) * (sx + vx * t1) + (sy + vy * t1) * (sy + vy * t1) = D)
 ∧ ((sx + vx * t2) * (sx + vx * t2) + (sy + vy * t2) * (sy + vy * t2) = D)
 ∧ ((sx + vx * t) * (sx + vx * t) + (sy + vy * t) * (sy + vy * t) < D) → t1 < t ∧ t < t2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: circles_and_lines.discr_le_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t, discrm, D: real):
--   discrm =
--    2 * sx * vx * sy * vy + D * (vx * vx + vy * vy) -
--     (sx * sx * vy * vy + sy * sy * vx * vx)
--    AND discrm <= 0 AND vx * vx + vy * vy /= 0
--    IMPLIES
--    (sx
theorem discr_le_0 : ∀ (t : ℝ) (discrm : ℝ) (D : ℝ), discrm =
 2 * sx * vx * sy * vy + D * (vx * vx + vy * vy) -
  (sx * sx * vy * vy + sy * sy * vx * vx)
 ∧ discrm ≤ 0 ∧ vx * vx + vy * vy ≠ 0 → (sx + vx * t) * (sx + vx * t) + (sy + vy * t) * (sy + vy * t) ≥ D := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.circles_and_lines