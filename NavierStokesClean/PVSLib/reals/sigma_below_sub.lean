-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sigma_below_sub
-- PVSLib library: reals
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sigma_below_sub

Auto-generated stubs from PVSLib theory `sigma_below_sub`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sigma_below_sub

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)

-- pvs2lean: sigma_below_sub.sigma_diff_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_diff_eq : True := by
  -- === TCCs for sigma_diff_eq ===
  -- TCC: sigma_diff_eq_TCC1
  -- Statement: FORALL (n, n0: nat):
  --   n0 <= n AND n < N2 AND n < N1 IMPLIES
  --    FORALL (i: nat): n0 <= i AND i <= n IMPLIES i < N1
  have sigma_diff_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_eq_TCC2
  -- Statement: FORALL (n, n0: nat):
  --   n0 <= n AND n < N2 AND n < N1 IMPLIES
  --    FORALL (i: nat): n0 <= i AND i <= n IMPLIES i < N2
  have sigma_diff_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_eq_TCC3
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i)) AND
  --    n0 <= n AND n < N2 AND n < N1
  --    IMPLIES n0 >= 0 AND n0 <...
  have sigma_diff_eq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_eq_TCC4
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i)) AND
  --    n0 <= n AND n < N2 AND n < N1
  --    IMPLIES n >= 0 AND n < N...
  have sigma_diff_eq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_eq_TCC5
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i)) AND
  --    n0 <= n AND n < N2 AND n < N1
  --    IMPLIES
  --    (FORALL (x, y...
  have sigma_diff_eq_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_eq_TCC6
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i)) AND
  --    n0 <= n AND n < N2 AND n < N1
  --    IMPLIES n0 >= 0 AND n0 <...
  have sigma_diff_eq_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_eq_TCC7
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i)) AND
  --    n0 <= n AND n < N2 AND n < N1
  --    IMPLIES n >= 0 AND n < N...
  have sigma_diff_eq_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_eq_TCC8
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i)) AND
  --    n0 <= n AND n < N2 AND n < N1
  --    IMPLIES
  --    (FORALL (x, y...
  have sigma_diff_eq_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_below_sub.sigma_diff_shift
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_diff_shift : True := by
  -- === TCCs for sigma_diff_shift ===
  -- TCC: sigma_diff_shift_TCC1
  -- Statement: FORALL (m, n, n0: nat):
  --   n0 <= n AND n + m < N2 AND n < N1 IMPLIES
  --    FORALL (i: nat): n0 <= i AND i <= n IMPLIES i < N1
  have sigma_diff_shift_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_shift_TCC2
  -- Statement: FORALL (m, n, n0: nat):
  --   n0 <= n AND n + m < N2 AND n < N1 IMPLIES
  --    FORALL (i: nat): n0 <= i AND i <= n IMPLIES i + m < N2
  have sigma_diff_shift_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_shift_TCC3
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], m, n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i + m)) AND
  --    n0 <= n AND n + m < N2 AND n < N1
  --    IMPLIES n0 >=...
  have sigma_diff_shift_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_shift_TCC4
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], m, n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i + m)) AND
  --    n0 <= n AND n + m < N2 AND n < N1
  --    IMPLIES n >= ...
  have sigma_diff_shift_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_shift_TCC5
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], m, n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i + m)) AND
  --    n0 <= n AND n + m < N2 AND n < N1
  --    IMPLIES
  --    (F...
  have sigma_diff_shift_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_shift_TCC6
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], m, n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i + m)) AND
  --    n0 <= n AND n + m < N2 AND n < N1
  --    IMPLIES n0 + ...
  have sigma_diff_shift_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_shift_TCC7
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], m, n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i + m)) AND
  --    n0 <= n AND n + m < N2 AND n < N1
  --    IMPLIES n + m...
  have sigma_diff_shift_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_shift_TCC8
  -- Statement: FORALL (f1: [below(N1) -> real], f2: [below(N2) -> real], m, n, n0: nat):
  --   (FORALL (i: nat): n0 <= i AND i <= n IMPLIES f1(i) = f2(i + m)) AND
  --    n0 <= n AND n + m < N2 AND n < N1
  --    IMPLIES
  --    (F...
  have sigma_diff_shift_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.sigma_below_sub