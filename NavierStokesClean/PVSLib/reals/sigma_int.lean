-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sigma_int
-- PVSLib library: reals
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sigma_int

Auto-generated stubs from PVSLib theory `sigma_int`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sigma_int

noncomputable section

-- pvs2lean: sigma_int.sigma_shift
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_shift : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_int.sigma_split_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_split_ge : True := by
  -- === TCCs for sigma_split_ge ===
  -- TCC: sigma_split_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat, m: below[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    m >= 0 AND m < N OR EXISTS (j: below(N)): m <= j
  have sigma_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_split_ge_TCC1
  -- Statement: FORALL (high, i: int, low: nat):
  --   i <= high AND low - 1 <= i IMPLIES
  --    i + 1 >= 0 OR EXISTS (j: nat): j <= i + 1
  have sigma_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_split_ge_TCC1
  -- Statement: FORALL (high, low, m: upto[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    high >= 0 AND high <= N OR EXISTS (j: upto[N]): high <= j
  have sigma_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_split_ge_TCC2
  -- Statement: FORALL (high, low, m: upto[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    m >= 0 AND m <= N OR EXISTS (j: upto[N]): m <= j
  have sigma_split_ge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_int.sigma_first_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_first_ge : True := by
  -- === TCCs for sigma_first_ge ===
  -- TCC: sigma_first_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat): high >= low IMPLIES low < N
  have sigma_first_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_first_ge_TCC1
  -- Statement: FORALL (high, low: upto[N]):
  --   high >= low IMPLIES
  --    high >= 0 AND high <= N OR EXISTS (j: upto[N]): high <= j
  have sigma_first_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_int.sigma_mid_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_mid_ge : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: sigma_int.sigma_last_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_last_ge : True := by
  -- === TCCs for sigma_last_ge ===
  -- TCC: sigma_last_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat):
  --   high >= low IMPLIES
  --    high - 1 >= 0 AND high - 1 < N OR EXISTS (j: below(N)): high - 1 <= j
  have sigma_last_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_last_ge_TCC2
  -- Statement: FORALL (high: int_below, low: nat):
  --   high >= low IMPLIES high >= 0 AND high < N
  have sigma_last_ge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_last_ge_TCC1
  -- Statement: FORALL (high, low: upto[N]):
  --   high >= low IMPLIES
  --    high - 1 >= 0 AND high - 1 <= N OR EXISTS (j: upto[N]): high - 1 <= j
  have sigma_last_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.sigma_int