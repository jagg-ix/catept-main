-- Auto-generated by pvs2lean/translator.py
-- PVS theory: affine
-- PVSLib library: affine_arith
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: affine

Auto-generated stubs from PVSLib theory `affine`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.affine

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (Mult : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (Proper_p : pvs_τ9 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (append : pvs_τ28 → pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (append_ol : pvs_τ31 → pvs_τ32 → pvs_τ33)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (appendable_ol_p : pvs_τ34 → pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (below : pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} (car : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} (car_ol : pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (cdr : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} (cdr_ol : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (cons : pvs_τ43 → pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (cons_ol : pvs_τ45 → pvs_τ46 → pvs_τ47)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (cv : pvs_τ52 → pvs_τ53)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} (first : pvs_τ66 → pvs_τ67 → pvs_τ68)
variable {pvs_τ82 : Type*} (last_idx : pvs_τ82 → Prop)
variable {pvs_τ85 : Type*} (last_ol : pvs_τ85 → Prop)
variable {pvs_τ86 : Type*} {pvs_τ87 : Type*} (lb : pvs_τ86 → pvs_τ87)
variable {pvs_τ88 : Type*} (length : pvs_τ88 → Prop)
variable {pvs_τ93 : Type*} {pvs_τ94 : Type*} {pvs_τ95 : Type*} (max : pvs_τ93 → pvs_τ94 → pvs_τ95)
variable {pvs_τ96 : Type*} {pvs_τ97 : Type*} (member : pvs_τ96 → pvs_τ97 → Prop)
variable {pvs_τ108 : Type*} {pvs_τ109 : Type*} (midpoint : pvs_τ108 → pvs_τ109)
variable {pvs_τ110 : Type*} {pvs_τ111 : Type*} (min : pvs_τ110 → pvs_τ111 → Prop)
variable {pvs_τ138 : Type*} {pvs_τ139 : Type*} {pvs_τ140 : Type*} (nth : pvs_τ138 → pvs_τ139 → pvs_τ140)
variable {pvs_τ141 : Type*} (null_ol_p : pvs_τ141 → Prop)
variable {pvs_τ142 : Type*} (null_p : pvs_τ142 → Prop)
variable {pvs_τ143 : Type*} {pvs_τ144 : Type*} (op : pvs_τ143 → pvs_τ144 → Prop)
variable {pvs_τ145 : Type*} (ordered_list_p : pvs_τ145 → Prop)
variable {pvs_τ146 : Type*} {pvs_τ147 : Type*} (pd : pvs_τ146 → pvs_τ147)
variable {pvs_τ148 : Type*} {pvs_τ149 : Type*} (restrict : pvs_τ148 → pvs_τ149)
variable {pvs_τ155 : Type*} (strict_order_p : pvs_τ155 → Prop)
variable {pvs_τ163 : Type*} {pvs_τ164 : Type*} (ub : pvs_τ163 → pvs_τ164)
variable {pvs_τ165 : Type*} {pvs_τ166 : Type*} (upd : pvs_τ165 → pvs_τ166)
variable {pvs_τ167 : Type*} {pvs_τ168 : Type*} (upd_fun : pvs_τ167 → pvs_τ168)
variable {pvs_τ187 : Type*} {pvs_τ188 : Type*} (upfrom : pvs_τ187 → pvs_τ188)

-- pvs2lean: affine.empty_ErrorTerms
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null:: list[[nat, nzBaseType]]
noncomputable def empty_ErrorTerms : sorry := by
  sorry  -- pvs2lean: definition stub for null:: list[[nat, nzBaseType]]

-- pvs2lean: affine.non_empty?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(pds)
noncomputable def non_empty_p : sorry := by
  sorry  -- pvs2lean: definition stub for NOT null?(pds)

-- pvs2lean: affine.idxs_pd
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(pds) THEN null
-- ELSE cons(car_ol(pds)`1, idxs_pd(cdr_ol(pds)))
-- ENDIF
axiom idxs_pd {pvs_τ80 : Type*} {pvs_τ81 : Type*} : pvs_τ80 → pvs_τ81

-- pvs2lean: affine.idxs_pd_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (pds2: {pd: ErrorTerms | appendable_ol?(pds1, pd)}):
--   idxs_pd(append_ol(pds1, pds2)) = append(idxs_pd(pds1), idxs_pd(pds2))
theorem idxs_pd_append : ∀ (pds2 : {pd : {l : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0}) // ordered_list_p l} // appendable_ol_p pds1 pd}), idxs_pd (append_ol pds1 pds2) = append (idxs_pd pds1) (idxs_pd pds2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.add_ET
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x = 0 THEN pds
-- ELSIF null_ol?(pds) THEN cons_ol((n, x), empty_ErrorTerms)
-- ELSIF n < car_ol(pds)`1 THEN cons_ol((n, x), pds)
-- ELSE pds
-- ENDIF
axiom add_ET {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14

-- pvs2lean: affine.last_idx_append
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (pds2: {pd: ErrorTerms | appendable_ol?(pds1, pd)}):
--   last_idx(append_ol(pds1, pds2)) = max(last_idx(pds1), last_idx(pds2))
theorem last_idx_append : ∀ (pds2 : {pd : {l : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0}) // ordered_list_p l} // appendable_ol_p pds1 pd}), last_idx (append_ol pds1 pds2) = max (last_idx pds1) (last_idx pds2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.last_error
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nth(pds, length(pds) - 1)`2
noncomputable def last_error : sorry := by
  sorry  -- pvs2lean: definition stub for nth(pds, length(pds) - 1)`2

-- pvs2lean: affine.represents_var?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(pd(Xa)) <= 1
noncomputable def represents_var_p : sorry := by
  sorry  -- pvs2lean: definition stub for length(pd(Xa)) <= 1

-- pvs2lean: affine.{||}
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# cv := x, pd := pds #)
noncomputable def pvs_op_8991e5 : sorry := by
  sorry  -- pvs2lean: definition stub for (# cv := x, pd := pds #)

-- pvs2lean: affine.ac_proj_cv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cv({|x, pds|}) = x
theorem ac_proj_cv : cv ({|x, pds|}) = x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: affine.ac_proj_pd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pd({|x, pds|}) = pds
theorem ac_proj_pd : pd ({|x, pds|}) = pds := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: affine.idxs_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idxs_pd(pd(Xa))
axiom idxs_ac {pvs_τ76 : Type*} {pvs_τ77 : Type*} : pvs_τ76 → pvs_τ77

-- pvs2lean: affine.last_idx_acs
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES l
--   OF null: -1,
--      cons(h, t):
--        LET last_h = last_idx(pd(h)), last_t = last_idx_acs(t) IN
--          max(last_h, last_t)
--   ENDCASES
axiom last_idx_acs {pvs_τ83 : Type*} {pvs_τ84 : Type*} : pvs_τ83 → pvs_τ84

-- pvs2lean: affine.search_error_term_by_idx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES pds
--   OF null: 0,
--      cons(h, t):
--        IF h`1 = i THEN h`2
--        ELSIF h`1 > i THEN 0
--        ELSE search_error_term_by_idx(t, i)
--        ENDIF
--   ENDCASES
axiom search_error_term_by_idx {pvs_τ153 : Type*} {pvs_τ154 : Type*} : pvs_τ153 → pvs_τ154 → Prop

-- pvs2lean: affine.setbi_nnull_member
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: search_error_term_by_idx(pds, i) /= 0 IMPLIES member(i, idxs_pd(pds))
theorem setbi_nnull_member : search_error_term_by_idx pds i ≠ 0 → member i (idxs_pd pds) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.error_term
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: search_error_term_by_idx(pd(Xa), i)
noncomputable def error_term : sorry := by
  sorry  -- pvs2lean: definition stub for search_error_term_by_idx(pd(Xa), i)

-- pvs2lean: affine.next_idx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: last_idx(pds) + 1
axiom next_idx {pvs_τ134 : Type*} {pvs_τ135 : Type*} : pvs_τ134 → pvs_τ135

-- pvs2lean: affine.next_idx_2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max(next_idx(pds1), next_idx(pds2))
axiom next_idx_2 {pvs_τ136 : Type*} {pvs_τ137 : Type*} : pvs_τ136 → pvs_τ137 → Prop

-- pvs2lean: affine.beyond_last_idx_notmember
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n > last_idx(pds) IMPLIES NOT member(n, idxs_pd(pds))
theorem beyond_last_idx_notmember : n > last_idx pds → ¬ member n (idxs_pd pds) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.idxs_noise
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(N) THEN null ELSE cons(car_ol(N)`1, idxs_noise(cdr_ol(N))) ENDIF
axiom idxs_noise {pvs_τ78 : Type*} {pvs_τ79 : Type*} : pvs_τ78 → pvs_τ79

-- pvs2lean: affine.empty_noise
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null:: list[[naturalnumbers.nat, nzEpsilon]]
noncomputable def empty_noise : sorry := by
  sorry  -- pvs2lean: definition stub for null:: list[[naturalnumbers.nat, nzEpsilon]]

-- pvs2lean: affine.empty_noise_is_ol
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ordered_list?[[nat, nzBaseType], lt_idx[nzBaseType]](empty_noise)
theorem empty_noise_is_ol : ordered_list_p empty_noise := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.single_n
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cons_ol((n, nze), null)
noncomputable def single_n : sorry := by
  sorry  -- pvs2lean: definition stub for cons_ol((n, nze), null)

-- pvs2lean: affine.add_N
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF e = 0 THEN N
-- ELSIF null_ol?(N) THEN cons_ol((n, e), empty_noise)
-- ELSIF n < car_ol(N)`1 THEN cons_ol((n, e), N)
-- ELSE N
-- ENDIF
axiom add_N {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: affine.extend_N
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: add_N(n, e, N)
axiom extend_N {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} : pvs_τ62 → pvs_τ63 → pvs_τ64 → pvs_τ65

-- pvs2lean: affine.extend_N_prop1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (null_ol?(N) OR m < car_ol(N)`1) AND n < m IMPLIES
--  (null_ol?(extend_N(m, e, N)) OR n < car_ol(extend_N(m, e, N))`1)
theorem extend_N_prop1 : (null_ol_p N ∨ m < car_ol N.1) ∧ n < m → (null_ol_p (extend_N m e N) ∨ n < car_ol (extend_N m e N).1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.get_noise_by_idx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(N) THEN 0
-- ELSE LET h = car_ol(N), t = cdr_ol(N) IN
--        IF h`1 = n THEN h`2
--        ELSIF h`1 > n THEN 0
--        ELSE get_noise_by_idx(n, t)
--        ENDIF
-- ENDIF
axiom get_noise_by_idx {pvs_τ74 : Type*} {pvs_τ75 : Type*} : pvs_τ74 → pvs_τ75 → Prop

-- pvs2lean: affine.upd_noise
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(N) THEN add_N(n, e, empty_noise)
-- ELSE LET hN = car_ol(N), tN = cdr_ol(N) IN
--        IF hN`1 > n THEN IF e /= 0 THEN cons_ol((n, e), N) ELSE N ENDIF
--        ELSIF hN`1 = n THEN IF e /= 0 THEN
axiom upd_noise {pvs_τ169 : Type*} {pvs_τ170 : Type*} {pvs_τ171 : Type*} {pvs_τ172 : Type*} : pvs_τ169 → pvs_τ170 → pvs_τ171 → pvs_τ172

-- pvs2lean: affine.upd_noise_range
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: upd_noise(IF i = 0 THEN N
--           ELSE upd_noise_range(N, starting_idx, i - 1, upd)
--           ENDIF,
--           starting_idx + i, upd(i))
axiom upd_noise_range {pvs_τ182 : Type*} {pvs_τ183 : Type*} {pvs_τ184 : Type*} {pvs_τ185 : Type*} {pvs_τ186 : Type*} : pvs_τ182 → pvs_τ183 → pvs_τ184 → pvs_τ185 → pvs_τ186

-- pvs2lean: affine.upd_noise_burst_
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES nwidxs
--   OF null: N,
--      cons(h, t): upd_noise(upd_noise_burst_(N, t, upd, n + 1), h, upd(n))
--   ENDCASES
axiom upd_noise_burst_ {pvs_τ177 : Type*} {pvs_τ178 : Type*} {pvs_τ179 : Type*} {pvs_τ180 : Type*} {pvs_τ181 : Type*} : pvs_τ177 → pvs_τ178 → pvs_τ179 → pvs_τ180 → pvs_τ181

-- pvs2lean: affine.upd_noise_burst
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: upd_noise_burst_(N, nwidxs, upd, 0)
axiom upd_noise_burst {pvs_τ173 : Type*} {pvs_τ174 : Type*} {pvs_τ175 : Type*} {pvs_τ176 : Type*} : pvs_τ173 → pvs_τ174 → pvs_τ175 → pvs_τ176

-- pvs2lean: affine.gnbi_upd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: get_noise_by_idx(n, upd_noise(N, n, e)) = e
theorem gnbi_upd : get_noise_by_idx n (upd_noise N n e) = e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_upd_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i /= n IMPLIES
--  get_noise_by_idx(i, upd_noise(N, n, e)) = get_noise_by_idx(i, N)
theorem gnbi_upd_2 : i ≠ n → get_noise_by_idx i (upd_noise N n e) = get_noise_by_idx i N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_updr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (upd_fun: [nat -> Epsilon], start: nat):
--   (start <= i AND i <= start + n) IMPLIES
--    get_noise_by_idx(i, upd_noise_range(N, start, n, upd_fun)) =
--     upd_fun(i - start)
theorem gnbi_updr : ∀ (upd_fun : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (start : {i : ℝ // i ≥ 0}), (start ≤ i ∧ i ≤ start + n) → get_noise_by_idx i (upd_noise_range N start n upd_fun) =
 upd_fun (i - start) := by
  -- === TCCs for gnbi_updr ===
  -- TCC: gnbi_updr_TCC1
  -- Statement: FORALL (i, n: nat, upd_fun: [nat -> Epsilon], start: nat):
  --   start <= i AND i <= start + n IMPLIES i - start >= 0
  have gnbi_updr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_updr_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (upd_fun: [nat -> Epsilon], start: nat):
--   (i < start OR i > start + n) IMPLIES
--    get_noise_by_idx(i, upd_noise_range(N, start, n, upd_fun)) =
--     get_noise_by_idx(i, N)
theorem gnbi_updr_2 : ∀ (upd_fun : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (start : {i : ℝ // i ≥ 0}), (i < start ∨ i > start + n) → get_noise_by_idx i (upd_noise_range N start n upd_fun) =
 get_noise_by_idx i N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_updb_
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (upd_fun: [nat -> Epsilon], N: Noise):
--   FORALL (k: nat,
--           nwidxs:
--             ordered_list
--                 [nat, restrict[[real, real], [nat, nat], bool](<)]):
--     FORALL (n: nat):
--    
theorem gnbi_updb_ : ∀ (upd_fun : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (N : {l : List ({i : ℝ // i ≥ 0} × {ep : {x : ℝ // -1 ≤ x ∧ x ≤ 1} // ep ≠ 0}) // ordered_list_p l}), ∀ (k : {i : ℝ // i ≥ 0}) (nwidxs : {l : List ℝ // ordered_list_p l}), ∀ (n : {i : ℝ // i ≥ 0}), n < List.length nwidxs → get_noise_by_idx (nth nwidxs n) (upd_noise_burst_ N nwidxs upd_fun k) =
 upd_fun (n + k) := by
  -- === TCCs for gnbi_updb_ ===
  -- TCC: gnbi_updb__TCC1
  -- Statement: FORALL (upd_fun: [nat -> Epsilon], N: Noise):
  --   strict_order?[nat](restrict[[real, real], [nat, nat], bool](<))
  have gnbi_updb__tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_updb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (upd_fun: [nat -> Epsilon],
--         nwidxs:
--           ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)],
--         n: below(length(nwidxs))):
--   nth(nwidxs, n) = i IMPLIES
--    get_nois
theorem gnbi_updb : ∀ (upd_fun : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (nwidxs : {l : List ℝ // ordered_list_p l}) (n : {s : {i : ℝ // i ≥ 0} // s < List.length nwidxs}), nth nwidxs n = i → get_noise_by_idx i (upd_noise_burst N nwidxs upd_fun) = upd_fun n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_updb_2_
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (upd_fun: [nat -> Epsilon], nwidxs: list[nat], k: nat):
--   NOT member(i, nwidxs) IMPLIES
--    get_noise_by_idx(i, upd_noise_burst_(N, nwidxs, upd_fun, k)) =
--     get_noise_by_idx(i, N)
theorem gnbi_updb_2_ : ∀ (upd_fun : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (nwidxs : List ℝ) (k : {i : ℝ // i ≥ 0}), ¬ member i nwidxs → get_noise_by_idx i (upd_noise_burst_ N nwidxs upd_fun k) =
 get_noise_by_idx i N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_updb_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (upd_fun: [nat -> Epsilon], nwidxs: list[nat]):
--   NOT member(i, nwidxs) IMPLIES
--    get_noise_by_idx(i, upd_noise_burst(N, nwidxs, upd_fun)) =
--     get_noise_by_idx(i, N)
theorem gnbi_updb_2 : ∀ (upd_fun : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (nwidxs : List ℝ), ¬ member i nwidxs → get_noise_by_idx i (upd_noise_burst N nwidxs upd_fun) =
 get_noise_by_idx i N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.restrict_by_idxs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(N) THEN null_ol
-- ELSIF member(car_ol(N)`1, idxs)
--   THEN cons_ol(car_ol(N), restrict_by_idxs(cdr_ol(N), idxs))
-- ELSE restrict_by_idxs(cdr_ol(N), idxs)
-- ENDIF
axiom restrict_by_idxs {pvs_τ150 : Type*} {pvs_τ151 : Type*} {pvs_τ152 : Type*} : pvs_τ150 → pvs_τ151 → pvs_τ152

-- pvs2lean: affine.restrict_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: restrict_by_idxs(N, idxs_ac(Xa))
noncomputable def restrict_ac : sorry := by
  sorry  -- pvs2lean: definition stub for restrict_by_idxs(N, idxs_ac(Xa))

-- pvs2lean: affine.eval_pd_noise
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(noise) THEN 0
-- ELSE IF null_ol?(pds) THEN 0
--      ELSE LET h = car_ol(pds), t = cdr_ol(pds) IN
--             IF h`1 = car_ol(noise)`1
--               THEN h`2 * car_ol(noise)`2 + eval_pd_noise(t
axiom eval_pd_noise {pvs_τ60 : Type*} {pvs_τ61 : Type*} : pvs_τ60 → pvs_τ61 → Prop

-- pvs2lean: affine.epn_pds_monotony
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds) AND NOT null_ol?(N) AND car_ol(pds)`1 < car_ol(N)`1
--  IMPLIES eval_pd_noise(cdr_ol(pds), N) = eval_pd_noise(pds, N)
theorem epn_pds_monotony : ¬ null_ol_p pds ∧ ¬ null_ol_p N ∧ car_ol pds.1 < car_ol N.1 → eval_pd_noise (cdr_ol pds) N = eval_pd_noise pds N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_noise_monotony
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds) AND NOT null_ol?(N) AND car_ol(N)`1 < car_ol(pds)`1
--  IMPLIES eval_pd_noise(pds, N) = eval_pd_noise(pds, cdr_ol(N))
theorem epn_noise_monotony : ¬ null_ol_p pds ∧ ¬ null_ol_p N ∧ car_ol N.1 < car_ol pds.1 → eval_pd_noise pds N = eval_pd_noise pds (cdr_ol N) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_pd_noise_rec_pds_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(pds, N) =
--  IF null_ol?(pds) THEN 0
--  ELSE get_noise_by_idx(car_ol(pds)`1, N) * car_ol(pds)`2 +
--        eval_pd_noise(cdr_ol(pds), N)
--  ENDIF
theorem eval_pd_noise_rec_pds_def : eval_pd_noise pds N = if null_ol_p pds then 0
else get_noise_by_idx (car_ol pds.1) N * car_ol pds.2 +
      eval_pd_noise (cdr_ol pds) N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_pd_noise_rec_N_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(pds, N) =
--  IF null_ol?(N) THEN 0
--  ELSE car_ol(N)`2 * search_error_term_by_idx(pds, car_ol(N)`1) +
--        eval_pd_noise(pds, cdr_ol(N))
--  ENDIF
theorem eval_pd_noise_rec_N_def : eval_pd_noise pds N = if null_ol_p N then 0
else car_ol N.2 * search_error_term_by_idx pds (car_ol N.1) +
      eval_pd_noise pds (cdr_ol N) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.appendable_Noises_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: appendable_ol?(N1, N2) AND NOT null_ol?(N1) AND NOT null_ol?(N2) IMPLIES
--  car_ol(N1)`1 < car_ol(N2)`1
theorem appendable_Noises_1 : appendable_ol_p N1 N2 ∧ ¬ null_ol_p N1 ∧ ¬ null_ol_p N2 → car_ol N1.1 < car_ol N2.1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_pd_noise_noise_append_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n = length(pds) + length(N1) IMPLIES
--  (appendable_ol?(N1, N2) IMPLIES
--    eval_pd_noise(pds, append_ol(N1, N2)) =
--     eval_pd_noise(pds, N1) + eval_pd_noise(pds, N2))
theorem eval_pd_noise_noise_append_2 : n = List.length pds + List.length N1 → (appendable_ol_p N1 N2 →
  eval_pd_noise pds (append_ol N1 N2) =
   eval_pd_noise pds N1 + eval_pd_noise pds N2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_pd_noise_noise_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: appendable_ol?(N1, N2) IMPLIES
--  eval_pd_noise(pds, append_ol(N1, N2)) =
--   eval_pd_noise(pds, N1) + eval_pd_noise(pds, N2)
theorem eval_pd_noise_noise_append : appendable_ol_p N1 N2 → eval_pd_noise pds (append_ol N1 N2) =
 eval_pd_noise pds N1 + eval_pd_noise pds N2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_pd_noise_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: appendable_ol?(pds1, pds2) IMPLIES
--  eval_pd_noise(append_ol(pds1, pds2), N) =
--   eval_pd_noise(pds1, N) + eval_pd_noise(pds2, N)
theorem eval_pd_noise_append : appendable_ol_p pds1 pds2 → eval_pd_noise (append_ol pds1 pds2) N =
 eval_pd_noise pds1 N + eval_pd_noise pds2 N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_ac_noise
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cv(Xa) + eval_pd_noise(pd(Xa), noise)
axiom eval_ac_noise {pvs_τ54 : Type*} {pvs_τ55 : Type*} : pvs_τ54 → pvs_τ55 → Prop

-- pvs2lean: affine.eval_ac_partial_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: nat):
--    member(i, idxs_ac(Xa)) IMPLIES
--     get_noise_by_idx(i, N1) = get_noise_by_idx(i, N2))
--  IMPLIES eval_ac_noise(Xa, N1) = eval_ac_noise(Xa, N2)
theorem eval_ac_partial_ext : (∀ (i : ℕ), member i (idxs_ac Xa) →
    get_noise_by_idx i N1 = get_noise_by_idx i N2) → eval_ac_noise Xa N1 = eval_ac_noise Xa N2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_upd_no_idxs_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(n, idxs_ac(Xa)) IMPLIES
--  eval_ac_noise(Xa, upd_noise(N, n, e)) = eval_ac_noise(Xa, N)
theorem eval_upd_no_idxs_ac : ¬ member n (idxs_ac Xa) → eval_ac_noise Xa (upd_noise N n e) = eval_ac_noise Xa N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_updb_no_idxs_
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nwidxs: list[nat], upd: [nat -> Epsilon], k: nat):
--   (FORALL (i: nat): member(i, nwidxs) IMPLIES NOT member(i, idxs_ac(Xa)))
--    IMPLIES
--    eval_ac_noise(Xa, upd_noise_burst_(N, nwidxs, upd, k)
theorem eval_updb_no_idxs_ : ∀ (nwidxs : List ℝ) (upd : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (k : {i : ℝ // i ≥ 0}), (∀ (i : ℕ), member i nwidxs → ¬ member i (idxs_ac Xa)) → eval_ac_noise Xa (upd_noise_burst_ N nwidxs upd k) =
 eval_ac_noise Xa N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_updb_no_idxs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nwidxs: list[nat], upd: [nat -> Epsilon]):
--   (FORALL (i: nat): member(i, nwidxs) IMPLIES NOT member(i, idxs_ac(Xa)))
--    IMPLIES
--    eval_ac_noise(Xa, upd_noise_burst(N, nwidxs, upd)) = eval_ac_
theorem eval_updb_no_idxs : ∀ (nwidxs : List ℝ) (upd : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}), (∀ (i : ℕ), member i nwidxs → ¬ member i (idxs_ac Xa)) → eval_ac_noise Xa (upd_noise_burst N nwidxs upd) = eval_ac_noise Xa N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mergeable?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null_ol?(N1) OR
--  null_ol?(N2) OR
--   LET h1 = car_ol(N1), t1 = cdr_ol(N1), h2 = car_ol(N2), t2 = cdr_ol(N2) IN
--     (h1`1 < h2`1 AND mergeable?(t1)(N2)) OR
--      (h1`1 > h2`1 AND mergeable?(N1)(t2)) OR
--   
axiom mergeable_p {pvs_τ100 : Type*} {pvs_τ101 : Type*} : pvs_τ100 → pvs_τ101 → Prop

-- pvs2lean: affine.mergeable_wrt_pd?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: null_ol?(pds) OR
--  LET pdsh = car_ol(pds),
--      pdst = cdr_ol(pds),
--      noise_N1 = get_noise_by_idx(pdsh`1, N1),
--      noise_N2 = get_noise_by_idx(pdsh`1, N2)
--    IN
--    ((noise_N2 = 0 OR (noise_N1 = noi
axiom mergeable_wrt_pd_p {pvs_τ105 : Type*} {pvs_τ106 : Type*} {pvs_τ107 : Type*} : pvs_τ105 → pvs_τ106 → pvs_τ107 → Prop

-- pvs2lean: affine.mergeable_wrt_pd?_nonrec_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: mergeable_wrt_pd?(pds, N1)(N2) =
--  FORALL (i: nat):
--    member(i, idxs_pd(pds)) IMPLIES
--     LET noise_N1 = get_noise_by_idx(i, N1),
--         noise_N2 = get_noise_by_idx(i, N2)
--       IN (noise_N2 = 0 OR (
theorem mergeable_wrt_pd_p_nonrec_def : mergeable_wrt_pd_p pds N1(N2) = ∀ (i : ℕ), member i (idxs_pd pds) →
   (let noise_N1 := get_noise_by_idx i N1; let noise_N2 := get_noise_by_idx i N2; (noise_N2 = 0 ∨ (noise_N1 = noise_N2))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mergeable_wrt_ac?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: mergeable_wrt_pd?(pd(Xa), N1)(N2)
axiom mergeable_wrt_ac_p {pvs_τ102 : Type*} {pvs_τ103 : Type*} {pvs_τ104 : Type*} : pvs_τ102 → pvs_τ103 → pvs_τ104 → Prop

-- pvs2lean: affine.merge
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(N1) THEN N2
-- ELSIF null_ol?(N2) THEN N1
-- ELSE LET h1 = car_ol(N1), t1 = cdr_ol(N1), h2 = car_ol(N2), t2 = cdr_ol(N2)
--        IN
--        IF h2`1 < h1`1 THEN cons_ol(h2, merge(N1, t2))
--        EL
axiom merge {pvs_τ98 : Type*} {pvs_τ99 : Type*} : pvs_τ98 → pvs_τ99 → Prop

-- pvs2lean: affine.merge_prop1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat):
--   get_noise_by_idx(i, N1) = get_noise_by_idx(i, N2) IMPLIES
--    get_noise_by_idx(i, merge(N1, N2)) = get_noise_by_idx(i, N1)
theorem merge_prop1 : ∀ (i : {i : ℝ // i ≥ 0}), get_noise_by_idx i N1 = get_noise_by_idx i N2 → get_noise_by_idx i (merge N1 N2) = get_noise_by_idx i N1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mergeable_wrt_null_all_idxs_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: mergeable_wrt_pd?(pds, null)(N) IMPLIES
--  FORALL (i: nat): member(i, idxs_pd(pds)) IMPLIES get_noise_by_idx(i, N) = 0
theorem mergeable_wrt_null_all_idxs_zero : mergeable_wrt_pd_p pds null(N) → ∀ (i : ℕ), member i (idxs_pd pds) → get_noise_by_idx i N = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mergeable_wrt_null_eval_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: mergeable_wrt_pd?(pds, null)(N) IMPLIES eval_pd_noise(pds, N) = 0
theorem mergeable_wrt_null_eval_zero : mergeable_wrt_pd_p pds null(N) → eval_pd_noise pds N = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mergeable_noises_respect_eval
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: mergeable_wrt_ac?(Xa, N1)(N2) IMPLIES
--  eval_ac_noise(Xa, merge(N1, N2)) = eval_ac_noise(Xa, N1)
theorem mergeable_noises_respect_eval : mergeable_wrt_ac_p Xa N1(N2) → eval_ac_noise Xa (merge N1 N2) = eval_ac_noise Xa N1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mergeable_suff_conditions
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: nat):
--    member(i, idxs_ac(Xa)) IMPLIES
--     (get_noise_by_idx(i, N1) = get_noise_by_idx(i, N2) OR
--       get_noise_by_idx(i, N2) = 0))
--  IMPLIES mergeable_wrt_ac?(Xa, N1)(N2)
theorem mergeable_suff_conditions : (∀ (i : ℕ), member i (idxs_ac Xa) →
    (get_noise_by_idx i N1 = get_noise_by_idx i N2 ∨
      get_noise_by_idx i N2 = 0)) → mergeable_wrt_ac_p Xa N1(N2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.merge_commutativity
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: nat):
--    get_noise_by_idx(i, N1) = 0 OR
--     get_noise_by_idx(i, N2) = 0 OR
--      get_noise_by_idx(i, N1) = get_noise_by_idx(i, N2))
--  IMPLIES merge(N1, N2) = merge(N2, N1)
theorem merge_commutativity : (∀ (i : ℕ), get_noise_by_idx i N1 = 0 ∨
    get_noise_by_idx i N2 = 0 ∨
     get_noise_by_idx i N1 = get_noise_by_idx i N2) → merge N1 N2 = merge N2 N1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.map_error
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(pds1)
--   THEN IF null_ol?(pds2) THEN empty_ErrorTerms
--        ELSE LET h2 = car_ol(pds2), t2 = cdr_ol(pds2) IN
--               LET r = op(0, h2`2) IN
--                 add_ET(h2`1, r, map_error(
axiom map_error {pvs_τ89 : Type*} {pvs_τ90 : Type*} {pvs_τ91 : Type*} {pvs_τ92 : Type*} : pvs_τ89 → pvs_τ90 → pvs_τ91 → pvs_τ92

-- pvs2lean: affine.nol_mp_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: null_ol?(map_error(null_ol, pds2, op)) AND
--  (FORALL (x: BaseType): op(0, x) = 0 OR op(x, 0) = 0 IMPLIES x = 0)
--  IMPLIES null_ol?(pds2)
theorem nol_mp_1 : null_ol_p (map_error null_ol pds2 op) ∧
 (∀ (x : BaseType), op 0 x = 0 ∨ op x 0 = 0 → x = 0) → null_ol_p pds2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.me_ordered_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds2) AND NOT null_ol?(map_error(null, cdr_ol(pds2), op))
--  IMPLIES car_ol(pds2)`1 < car_ol(map_error(null, cdr_ol(pds2), op))`1
theorem me_ordered_1 : ¬ null_ol_p pds2 ∧ ¬ null_ol_p (map_error null (cdr_ol pds2) op) → car_ol pds2.1 < car_ol (map_error null (cdr_ol pds2) op).1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.me_ordered_1b
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds1) AND NOT null_ol?(map_error(cdr_ol(pds1), null_ol, op))
--  IMPLIES car_ol(pds1)`1 < car_ol(map_error(cdr_ol(pds1), null_ol, op))`1
theorem me_ordered_1b : ¬ null_ol_p pds1 ∧ ¬ null_ol_p (map_error (cdr_ol pds1) null_ol op) → car_ol pds1.1 < car_ol (map_error (cdr_ol pds1) null_ol op).1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.me_ordered_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds1) AND
--  NOT null_ol?(pds2) AND
--   NOT null_ol?(map_error(cdr_ol(pds1), cdr_ol(pds2), op))
--  IMPLIES
--  min(car_ol(pds1)`1, car_ol(pds2)`1) <
--   car_ol(map_error(cdr_ol(pds1), cdr_ol(pds2), 
theorem me_ordered_2 : ¬ null_ol_p pds1 ∧
 ¬ null_ol_p pds2 ∧
  ¬ null_ol_p (map_error (cdr_ol pds1) (cdr_ol pds2) op) → min (car_ol pds1.1) (car_ol pds2.1) <
 car_ol (map_error (cdr_ol pds1) (cdr_ol pds2) op).1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.me_ordered_3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds1) AND
--  NOT null_ol?(pds2) AND
--   car_ol(pds1)`1 < car_ol(pds2)`1 AND
--    NOT null_ol?(map_error(cdr_ol(pds1), pds2, op))
--  IMPLIES car_ol(pds1)`1 < car_ol(map_error(cdr_ol(pds1), pds2, o
theorem me_ordered_3 : ¬ null_ol_p pds1 ∧
 ¬ null_ol_p pds2 ∧
  car_ol pds1.1 < car_ol pds2.1 ∧
   ¬ null_ol_p (map_error (cdr_ol pds1) pds2 op) → car_ol pds1.1 < car_ol (map_error (cdr_ol pds1) pds2 op).1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.me_ordered_4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds1) AND
--  NOT null_ol?(pds2) AND
--   car_ol(pds2)`1 < car_ol(pds1)`1 AND
--    NOT null_ol?(map_error(pds1, cdr_ol(pds2), op))
--  IMPLIES car_ol(pds2)`1 < car_ol(map_error(pds1, cdr_ol(pds2), o
theorem me_ordered_4 : ¬ null_ol_p pds1 ∧
 ¬ null_ol_p pds2 ∧
  car_ol pds2.1 < car_ol pds1.1 ∧
   ¬ null_ol_p (map_error pds1 (cdr_ol pds2) op) → car_ol pds2.1 < car_ol (map_error pds1 (cdr_ol pds2) op).1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_me_null_1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (op: [[BaseType, BaseType] -> BaseType]
--             | op(0, 0) = 0 AND
--                (FORALL (x: BaseType):
--                   op(0, x) = 0 OR op(x, 0) = 0 IMPLIES x = 0)
--                 AND
--   
theorem epn_me_null_1 : ∀ (op : {op : ℝ × ℝ → ℝ // op 0 0 = 0 ∧
 (∀ (x : BaseType), op 0 x = 0 ∨ op x 0 = 0 → x = 0) ∧
  (∀ (a : _) (b : _) (c : BaseType), c * op a b = op (c * a) (c * b)) ∧
   (∀ (a : _) (b : _) (c : _) (d : BaseType), op (a + b) (c + d) = op a c + op b d)}), null_p (map_error pds1 pds2 op) → op (eval_pd_noise pds1 N) (eval_pd_noise pds2 N) = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.const_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {|x, empty_ErrorTerms|}
axiom const_ac {pvs_τ48 : Type*} {pvs_τ49 : Type*} : pvs_τ48 → pvs_τ49

-- pvs2lean: affine.next_idx_const_ac
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: next_idx(pd(const_ac(x))) = 0
theorem next_idx_const_ac : next_idx (pd (const_ac x)) = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.var_ac
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: {|(lb(Xi) + ub(Xi)) / 2,
--   IF (ub(Xi) - lb(Xi)) / 2 /= 0
--     THEN cons_ol[[nat, nzBaseType], lt_idx[nzBaseType]]
--              ((n, (ub(Xi) - lb(Xi)) / 2), empty_ErrorTerms)
--   ELSE empty_ErrorTerms
--   E
axiom var_ac {pvs_τ189 : Type*} {pvs_τ190 : Type*} {pvs_τ191 : Type*} : pvs_τ189 → pvs_τ190 → pvs_τ191

-- pvs2lean: affine.eval_var_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_ac_noise(var_ac(Xi, n), N) =
--  (lb(Xi) + ub(Xi)) / 2 + get_noise_by_idx(n, N) * ((ub(Xi) - lb(Xi)) / 2)
theorem eval_var_ac : eval_ac_noise (var_ac Xi n) N = (lb Xi + ub Xi) / 2 + get_noise_by_idx n N * ((ub Xi - lb Xi) / 2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_pd_i
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(pds) THEN [|0|]
-- ELSE LET h = car_ol(pds), t = cdr_ol(pds) IN
--        Add(Mult([|h`2|], [|-1, 1|]), eval_pd_i(t))
-- ENDIF
axiom eval_pd_i {pvs_τ58 : Type*} {pvs_τ59 : Type*} : pvs_τ58 → pvs_τ59

-- pvs2lean: affine.eval_by_intervals
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Add([|cv(Xa)|], eval_pd_i(pd(Xa)))
axiom eval_by_intervals {pvs_τ56 : Type*} {pvs_τ57 : Type*} : pvs_τ56 → pvs_τ57

-- pvs2lean: affine.AC_leq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_by_intervals(Xa1) << eval_by_intervals(Xa2)
noncomputable def AC_leq : sorry := by
  sorry  -- pvs2lean: definition stub for eval_by_intervals(Xa1) << eval_by_intervals(Xa2)

-- pvs2lean: affine.ebi_var_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_by_intervals(var_ac(pXi, n)) = pXi
theorem ebi_var_ac : eval_by_intervals (var_ac pXi n) = pXi := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.idxs_ac_var_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat): member(i, idxs_ac(var_ac(Xi, n))) IMPLIES i = n
theorem idxs_ac_var_ac : ∀ (i : {i : ℝ // i ≥ 0}), member i (idxs_ac (var_ac Xi n)) → i = n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.updated_noise_on_varac1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat):
--   i /= n IMPLIES
--    eval_ac_noise(var_ac(Xi, i), upd_noise(N, n, e)) =
--     eval_ac_noise(var_ac(Xi, i), N)
theorem updated_noise_on_varac1 : ∀ (i : {i : ℝ // i ≥ 0}), i ≠ n → eval_ac_noise (var_ac Xi i) (upd_noise N n e) =
 eval_ac_noise (var_ac Xi i) N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.updated_noise_on_varac2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_ac_noise(var_ac(Xi, n), upd_noise(N, n, e)) =
--  eval_ac_noise(var_ac(Xi, n), extend_N(n, e, empty_noise))
theorem updated_noise_on_varac2 : eval_ac_noise (var_ac Xi n) (upd_noise N n e) = eval_ac_noise (var_ac Xi n) (extend_N n e empty_noise) := by
  -- === TCCs for updated_noise_on_varac2 ===
  -- TCC: updated_noise_on_varac2_TCC1
  -- Statement: FORALL (n: nat):
  --   null_ol?[[nat, nzEpsilon], lt_idx[nzEpsilon]](empty_noise) OR
  --    n < car_ol[[nat, nzEpsilon], lt_idx[nzEpsilon]](empty_noise)`1
  have updated_noise_on_varac2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.updated_noise_range_on_v1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (upd_fun: [nat -> Epsilon], start: nat):
--   (i < start OR i > start + n) IMPLIES
--    eval_ac_noise(var_ac(Xi, i), upd_noise_range(N, start, n, upd_fun)) =
--     eval_ac_noise(var_ac(Xi, i), N)
theorem updated_noise_range_on_v1 : ∀ (upd_fun : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (start : {i : ℝ // i ≥ 0}), (i < start ∨ i > start + n) → eval_ac_noise (var_ac Xi i) (upd_noise_range N start n upd_fun) =
 eval_ac_noise (var_ac Xi i) N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.updated_noise_range_on_v2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (upd_fun: [nat -> Epsilon], start: nat):
--   (start <= i AND i <= start + n) IMPLIES
--    eval_ac_noise(var_ac(Xi, i), upd_noise_range(N, start, n, upd_fun)) =
--     eval_ac_noise(var_ac(Xi, i),
--     
theorem updated_noise_range_on_v2 : ∀ (upd_fun : {i : ℝ // i ≥ 0} → {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (start : {i : ℝ // i ≥ 0}), (start ≤ i ∧ i ≤ start + n) → eval_ac_noise (var_ac Xi i) (upd_noise_range N start n upd_fun) =
 eval_ac_noise (var_ac Xi i) (extend_N i (upd_fun (i - start)) empty_noise) := by
  -- === TCCs for updated_noise_range_on_v2 ===
  -- TCC: updated_noise_range_on_v2_TCC1
  -- Statement: FORALL (i, n: nat, upd_fun: [nat -> Epsilon], start: nat):
  --   start <= i AND i <= start + n IMPLIES
  --    null_ol?[[nat, nzEpsilon], lt_idx[nzEpsilon]](empty_noise) OR
  --     i < car_ol[[nat, nzEpsilon], ...
  have updated_noise_range_on_v2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.ub_aff
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ub(eval_by_intervals(Xa))
noncomputable def ub_aff : sorry := by
  sorry  -- pvs2lean: definition stub for ub(eval_by_intervals(Xa))

-- pvs2lean: affine.lb_aff
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: lb(eval_by_intervals(Xa))
noncomputable def lb_aff : sorry := by
  sorry  -- pvs2lean: definition stub for lb(eval_by_intervals(Xa))

-- pvs2lean: affine.containment
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS noise: eval_ac_noise(Xa, noise) = x
axiom containment {pvs_τ50 : Type*} {pvs_τ51 : Type*} : pvs_τ50 → pvs_τ51 → Prop

-- pvs2lean: affine.AC_Includes?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: containment(x, Xa)
noncomputable def AC_Includes_p : sorry := by
  sorry  -- pvs2lean: definition stub for containment(x, Xa)

-- pvs2lean: affine.containment_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ## Xi IMPLIES
--  EXISTS e: eval_ac_noise(var_ac(Xi, n), extend_N(n, e, empty_noise)) = x
theorem containment_var : x ∈ Xi → ∃ e,  eval_ac_noise (var_ac Xi n) (extend_N n e empty_noise) = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.SingInterval?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: lb(Xi) = ub(Xi)
axiom SingInterval_p {pvs_τ10 : Type*} : pvs_τ10 → Prop

-- pvs2lean: affine.containment_var_single_interval
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: SingInterval?(Xi) AND x ## Xi IMPLIES
--  eval_ac_noise(var_ac(Xi, n), empty_noise) = x
theorem containment_var_single_interval : SingInterval_p Xi ∧ x ∈ Xi → eval_ac_noise (var_ac Xi n) empty_noise = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.containment_var_simple
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ## Xi IMPLIES containment(x, var_ac(Xi, n))
theorem containment_var_simple : x ∈ Xi → containment x (var_ac Xi n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.containment_cnt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_ac_noise(const_ac(x), N) = x
theorem containment_cnt : eval_ac_noise (const_ac x) N = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.containment_pd_interval
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(pds, noise) ## eval_pd_i(pds)
theorem containment_pd_interval : eval_pd_noise pds noise ## eval_pd_i pds := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.containment_interval
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: containment(x, Xa1) IMPLIES x ## eval_by_intervals(Xa1)
theorem containment_interval : containment x Xa1 → x ∈ eval_by_intervals Xa1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.ean_in_ebi
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_ac_noise(Xa1, N) ## eval_by_intervals(Xa1)
theorem ean_in_ebi : eval_ac_noise Xa1 N ## eval_by_intervals Xa1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.add_pd_pd
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map_error(pds1, pds2,
--           restrict[[numfield, numfield], [BaseType, BaseType], numfield](+))
axiom add_pd_pd {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ22 → pvs_τ23 → pvs_τ24

-- pvs2lean: affine.epn_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(add_pd_pd(pds1, pds2), N) =
--  eval_pd_noise(pds1, N) + eval_pd_noise(pds2, N)
theorem epn_app : eval_pd_noise (add_pd_pd pds1 pds2) N = eval_pd_noise pds1 N + eval_pd_noise pds2 N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.add_ac_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {|cv(Xa1) + cv(Xa2), add_pd_pd(pd(Xa1), pd(Xa2))|}
axiom add_ac_ac {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21

-- pvs2lean: affine.containment_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL N:
--   eval_ac_noise(Xa1, N) = x1 AND eval_ac_noise(Xa2, N) = x2 IMPLIES
--    eval_ac_noise(add_ac_ac(Xa1, Xa2), N) = x1 + x2
theorem containment_add : ∀ (N : {l : List ({i : ℝ // i ≥ 0} × {ep : {x : ℝ // -1 ≤ x ∧ x ≤ 1} // ep ≠ 0}) // ordered_list_p l}), eval_ac_noise Xa1 N = x1 ∧ eval_ac_noise Xa2 N = x2 → eval_ac_noise (add_ac_ac Xa1 Xa2) N = x1 + x2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.next_idx_add_ac_ac
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: next_idx(pd(add_ac_ac(Xa1, Xa2))) <=
--  max(next_idx(pd(Xa1)), next_idx(pd(Xa2)))
theorem next_idx_add_ac_ac : next_idx (pd (add_ac_ac Xa1 Xa2)) ≤ max (next_idx (pd Xa1)) (next_idx (pd Xa2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.idxs_ac_add_ac_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat):
--   member(i, idxs_ac(add_ac_ac(Xa1, Xa2))) IMPLIES
--    member(i, idxs_ac(Xa1)) OR member(i, idxs_ac(Xa2))
theorem idxs_ac_add_ac_ac : ∀ (i : {i : ℝ // i ≥ 0}), member i (idxs_ac (add_ac_ac Xa1 Xa2)) → member i (idxs_ac Xa1) ∨ member i (idxs_ac Xa2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.sub_pd_pd
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map_error(pds1, pds2,
--           restrict[[numfield, numfield], [BaseType, BaseType], numfield](-))
axiom sub_pd_pd {pvs_τ159 : Type*} {pvs_τ160 : Type*} {pvs_τ161 : Type*} : pvs_τ159 → pvs_τ160 → pvs_τ161

-- pvs2lean: affine.epn_spp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(sub_pd_pd(pds1, pds2), N) =
--  eval_pd_noise(pds1, N) - eval_pd_noise(pds2, N)
theorem epn_spp : eval_pd_noise (sub_pd_pd pds1 pds2) N = eval_pd_noise pds1 N - eval_pd_noise pds2 N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.sub_ac_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {|cv(Xa1) - cv(Xa2), sub_pd_pd(pd(Xa1), pd(Xa2))|}
axiom sub_ac_ac {pvs_τ156 : Type*} {pvs_τ157 : Type*} {pvs_τ158 : Type*} : pvs_τ156 → pvs_τ157 → pvs_τ158

-- pvs2lean: affine.containment_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL N:
--   eval_ac_noise(Xa1, N) = x1 AND eval_ac_noise(Xa2, N) = x2 IMPLIES
--    eval_ac_noise(sub_ac_ac(Xa1, Xa2), N) = x1 - x2
theorem containment_sub : ∀ (N : {l : List ({i : ℝ // i ≥ 0} × {ep : {x : ℝ // -1 ≤ x ∧ x ≤ 1} // ep ≠ 0}) // ordered_list_p l}), eval_ac_noise Xa1 N = x1 ∧ eval_ac_noise Xa2 N = x2 → eval_ac_noise (sub_ac_ac Xa1 Xa2) N = x1 - x2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.next_idx_sub_ac_ac
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: next_idx(pd(sub_ac_ac(Xa1, Xa2))) <=
--  max(next_idx(pd(Xa1)), next_idx(pd(Xa2)))
theorem next_idx_sub_ac_ac : next_idx (pd (sub_ac_ac Xa1 Xa2)) ≤ max (next_idx (pd Xa1)) (next_idx (pd Xa2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.idxs_ac_sub_ac_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat):
--   member(i, idxs_ac(sub_ac_ac(Xa1, Xa2))) IMPLIES
--    member(i, idxs_ac(Xa1)) OR member(i, idxs_ac(Xa2))
theorem idxs_ac_sub_ac_ac : ∀ (i : {i : ℝ // i ≥ 0}), member i (idxs_ac (sub_ac_ac Xa1 Xa2)) → member i (idxs_ac Xa1) ∨ member i (idxs_ac Xa2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mul_s_pd
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(pds) THEN empty_ErrorTerms
-- ELSE LET h = car_ol(pds), t = cdr_ol(pds) IN
--        add_ET(h`1, h`2 * x, mul_s_pd(x, t))
-- ENDIF
axiom mul_s_pd {pvs_τ118 : Type*} {pvs_τ119 : Type*} {pvs_τ120 : Type*} : pvs_τ118 → pvs_τ119 → pvs_τ120

-- pvs2lean: affine.mul_s_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {|x * cv(Xa1), mul_s_pd(x, pd(Xa1))|}
axiom mul_s_ac {pvs_τ115 : Type*} {pvs_τ116 : Type*} {pvs_τ117 : Type*} : pvs_τ115 → pvs_τ116 → pvs_τ117

-- pvs2lean: affine.car_mul_s_ac
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x2 /= 0 AND NOT null_ol?(pds) IMPLIES car(mul_s_pd(x2, pds))`1 = car(pds)`1
theorem car_mul_s_ac : x2 ≠ 0 ∧ ¬ null_ol_p pds → car (mul_s_pd x2 pds).1 = car pds.1 := by
  -- === TCCs for car_mul_s_ac ===
  -- TCC: car_mul_s_ac_TCC1
  -- Statement: FORALL (pds: ErrorTerms, x2: BaseType):
  --   NOT null_ol?(pds) AND x2 /= 0 IMPLIES
  --    cons?[[nat, nzBaseType]](mul_s_pd(x2, pds))
  have car_mul_s_ac_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: car_mul_s_ac_TCC2
  -- Statement: FORALL (pds: ErrorTerms, x2: BaseType):
  --   NOT null_ol?(pds) AND x2 /= 0 IMPLIES cons?[[nat, nzBaseType]](pds)
  have car_mul_s_ac_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.containment_mul_s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL N:
--   eval_ac_noise(Xa, N) = x1 IMPLIES
--    eval_ac_noise(mul_s_ac(x2, Xa), N) = x2 * x1
theorem containment_mul_s : ∀ (N : {l : List ({i : ℝ // i ≥ 0} × {ep : {x : ℝ // -1 ≤ x ∧ x ≤ 1} // ep ≠ 0}) // ordered_list_p l}), eval_ac_noise Xa N = x1 → eval_ac_noise (mul_s_ac x2 Xa) N = x2 * x1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.add_s_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {|x + cv(Xa1), pd(Xa1)|}
axiom add_s_ac {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ25 → pvs_τ26 → pvs_τ27

-- pvs2lean: affine.containment_add_s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL N:
--   eval_ac_noise(Xa, N) = x1 IMPLIES
--    eval_ac_noise(add_s_ac(x2, Xa), N) = x2 + x1
theorem containment_add_s : ∀ (N : {l : List ({i : ℝ // i ≥ 0} × {ep : {x : ℝ // -1 ≤ x ∧ x ≤ 1} // ep ≠ 0}) // ordered_list_p l}), eval_ac_noise Xa N = x1 → eval_ac_noise (add_s_ac x2 Xa) N = x2 + x1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.neg_pd
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(pds) THEN empty_ErrorTerms
-- ELSE LET h = car_ol(pds), t = cdr_ol(pds) IN
--        cons_ol((h`1, -h`2), neg_pd(t))
-- ENDIF
axiom neg_pd {pvs_τ132 : Type*} {pvs_τ133 : Type*} : pvs_τ132 → pvs_τ133

-- pvs2lean: affine.neg_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {|-cv(Xa), neg_pd(pd(Xa))|}
axiom neg_ac {pvs_τ130 : Type*} {pvs_τ131 : Type*} : pvs_τ130 → pvs_τ131

-- pvs2lean: affine.containment_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL N:
--   eval_ac_noise(Xa, N) = x1 IMPLIES eval_ac_noise(neg_ac(Xa), N) = -x1
theorem containment_neg : ∀ (N : {l : List ({i : ℝ // i ≥ 0} × {ep : {x : ℝ // -1 ≤ x ∧ x ≤ 1} // ep ≠ 0}) // ordered_list_p l}), eval_ac_noise Xa N = x1 → eval_ac_noise (neg_ac Xa) N = -x1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.next_idx_neg_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: next_idx(pd(neg_ac(Xa))) = next_idx(pd(Xa))
theorem next_idx_neg_ac : next_idx (pd (neg_ac Xa)) = next_idx (pd Xa) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.idxs_ac_neg_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat):
--   member(i, idxs_ac(neg_ac(Xa))) IMPLIES member(i, idxs_ac(Xa))
theorem idxs_ac_neg_ac : ∀ (i : {i : ℝ // i ≥ 0}), member i (idxs_ac (neg_ac Xa)) → member i (idxs_ac Xa) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mult_pd_pd
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(pds1)
--   THEN IF null_ol?(pds2) THEN null_ol
--        ELSE LET hd2 = car_ol(pds2), tl2 = cdr_ol(pds2) IN
--               add_ET(hd2`1, hd2`2 * cv1, mult_pd_pd(cv1, cv2, pds1, tl2))
--        ENDIF
axiom mult_pd_pd {pvs_τ125 : Type*} {pvs_τ126 : Type*} {pvs_τ127 : Type*} {pvs_τ128 : Type*} {pvs_τ129 : Type*} : pvs_τ125 → pvs_τ126 → pvs_τ127 → pvs_τ128 → pvs_τ129

-- pvs2lean: affine.mult_pd_pd_preserves_order_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds) IMPLIES
--  null_ol?(mult_pd_pd(cv1, cv2, cdr_ol(pds), null)) OR
--   car_ol(pds)`1 < car(mult_pd_pd(cv1, cv2, cdr_ol(pds), null))`1
theorem mult_pd_pd_preserves_order_1 : ¬ null_ol_p pds → null_ol_p (mult_pd_pd cv1 cv2 (cdr_ol pds) null) ∨
 car_ol pds.1 < car (mult_pd_pd cv1 cv2 (cdr_ol pds) null).1 := by
  -- === TCCs for mult_pd_pd_preserves_order_1 ===
  -- TCC: mult_pd_pd_preserves_order_1_TCC1
  -- Statement: FORALL (cv1, cv2: BaseType, pds: ErrorTerms):
  --   NOT null_ol?(mult_pd_pd(cv1, cv2, cdr_ol(pds), null)) AND
  --    NOT null_ol?(pds)
  --    IMPLIES
  --    cons?[[nat, nzBaseType]]
  --        (mult_pd_pd(cv1, cv2,
  --   ...
  have mult_pd_pd_preserves_order_1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mult_pd_pd_preserves_order_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds2) IMPLIES
--  null_ol?(mult_pd_pd(cv1, cv2, null, cdr_ol(pds2))) OR
--   car_ol(pds2)`1 < car(mult_pd_pd(cv1, cv2, null, cdr_ol(pds2)))`1
theorem mult_pd_pd_preserves_order_2 : ¬ null_ol_p pds2 → null_ol_p (mult_pd_pd cv1 cv2 null (cdr_ol pds2)) ∨
 car_ol pds2.1 < car (mult_pd_pd cv1 cv2 null (cdr_ol pds2)).1 := by
  -- === TCCs for mult_pd_pd_preserves_order_2 ===
  -- TCC: mult_pd_pd_preserves_order_2_TCC1
  -- Statement: FORALL (cv1, cv2: BaseType, pds2: ErrorTerms):
  --   NOT null_ol?(mult_pd_pd(cv1, cv2, null, cdr_ol(pds2))) AND
  --    NOT null_ol?(pds2)
  --    IMPLIES
  --    cons?[[nat, nzBaseType]]
  --        (mult_pd_pd(cv1, cv2,...
  have mult_pd_pd_preserves_order_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mult_pd_pd_preserves_order_3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds1) AND NOT null_ol?(pds2) IMPLIES
--  null_ol?(mult_pd_pd(cv1, cv2, cdr_ol(pds1), cdr_ol(pds2))) OR
--   min(car_ol(pds1)`1, car_ol(pds2)`1) <
--    car(mult_pd_pd(cv1, cv2, cdr_ol(pds1), cdr_o
theorem mult_pd_pd_preserves_order_3 : ¬ null_ol_p pds1 ∧ ¬ null_ol_p pds2 → null_ol_p (mult_pd_pd cv1 cv2 (cdr_ol pds1) (cdr_ol pds2)) ∨
 min (car_ol pds1.1) (car_ol pds2.1) <
  car (mult_pd_pd cv1 cv2 (cdr_ol pds1) (cdr_ol pds2)).1 := by
  -- === TCCs for mult_pd_pd_preserves_order_3 ===
  -- TCC: mult_pd_pd_preserves_order_3_TCC1
  -- Statement: FORALL (cv1, cv2: BaseType, pds1, pds2: ErrorTerms):
  --   NOT null_ol?(mult_pd_pd(cv1, cv2, cdr_ol(pds1), cdr_ol(pds2))) AND
  --    NOT null_ol?(pds2) AND NOT null_ol?(pds1)
  --    IMPLIES
  --    cons?[[nat, nzBa...
  have mult_pd_pd_preserves_order_3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mult_pd_pd_last_bound__null_nnull
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds) AND
--  NOT null_ol?(mult_pd_pd(x1, x2, empty_ErrorTerms, pds))
--  IMPLIES
--  last_ol(mult_pd_pd(x1, x2, empty_ErrorTerms, pds))`1 <= last_ol(pds)`1
theorem mult_pd_pd_last_bound__null_nnull : ¬ null_ol_p pds ∧
 ¬ null_ol_p (mult_pd_pd x1 x2 empty_ErrorTerms pds) → last_ol (mult_pd_pd x1 x2 empty_ErrorTerms pds).1 ≤ last_ol pds.1 := by
  -- === TCCs for mult_pd_pd_last_bound__null_nnull ===
  -- TCC: mult_pd_pd_last_bound__null_nnull_TCC1
  -- Statement: FORALL (pds: ErrorTerms, x1, x2: BaseType):
  --   NOT null_ol?(mult_pd_pd(x1, x2, empty_ErrorTerms, pds)) AND
  --    NOT null_ol?(pds)
  --    IMPLIES
  --    NOT null?[[nat, nzBaseType]](mult_pd_pd(x1, x2, empty_Er...
  have mult_pd_pd_last_bound__null_nnull_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_pd_pd_last_bound__null_nnull_TCC2
  -- Statement: FORALL (pds: ErrorTerms, x1, x2: BaseType):
  --   NOT null_ol?(mult_pd_pd(x1, x2, empty_ErrorTerms, pds)) AND
  --    NOT null_ol?(pds)
  --    IMPLIES NOT null?[[nat, nzBaseType]](pds)
  have mult_pd_pd_last_bound__null_nnull_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mult_pd_pd_last_bound__nnull_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds) AND
--  NOT null_ol?(mult_pd_pd(x1, x2, pds, empty_ErrorTerms))
--  IMPLIES
--  last_ol(mult_pd_pd(x1, x2, pds, empty_ErrorTerms))`1 <= last_ol(pds)`1
theorem mult_pd_pd_last_bound__nnull_null : ¬ null_ol_p pds ∧
 ¬ null_ol_p (mult_pd_pd x1 x2 pds empty_ErrorTerms) → last_ol (mult_pd_pd x1 x2 pds empty_ErrorTerms).1 ≤ last_ol pds.1 := by
  -- === TCCs for mult_pd_pd_last_bound__nnull_null ===
  -- TCC: mult_pd_pd_last_bound__nnull_null_TCC1
  -- Statement: FORALL (pds: ErrorTerms, x1, x2: BaseType):
  --   NOT null_ol?(mult_pd_pd(x1, x2, pds, empty_ErrorTerms)) AND
  --    NOT null_ol?(pds)
  --    IMPLIES
  --    NOT null?[[nat, nzBaseType]](mult_pd_pd(x1, x2, pds, emp...
  have mult_pd_pd_last_bound__nnull_null_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_pd_pd_last_bound__nnull_null_TCC2
  -- Statement: FORALL (pds: ErrorTerms, x1, x2: BaseType):
  --   NOT null_ol?(mult_pd_pd(x1, x2, pds, empty_ErrorTerms)) AND
  --    NOT null_ol?(pds)
  --    IMPLIES NOT null?[[nat, nzBaseType]](pds)
  have mult_pd_pd_last_bound__nnull_null_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mult_pd_pd_last_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds1) AND
--  NOT null_ol?(pds2) AND NOT null_ol?(mult_pd_pd(x1, x2, pds1, pds2))
--  IMPLIES
--  last_ol(mult_pd_pd(x1, x2, pds1, pds2))`1 <=
--   max(last_ol(pds1)`1, last_ol(pds2)`1)
theorem mult_pd_pd_last_bound : ¬ null_ol_p pds1 ∧
 ¬ null_ol_p pds2 ∧ ¬ null_ol_p (mult_pd_pd x1 x2 pds1 pds2) → last_ol (mult_pd_pd x1 x2 pds1 pds2).1 ≤
 max (last_ol pds1.1) (last_ol pds2.1) := by
  -- === TCCs for mult_pd_pd_last_bound ===
  -- TCC: mult_pd_pd_last_bound_TCC1
  -- Statement: FORALL (pds1, pds2: ErrorTerms, x1, x2: BaseType):
  --   NOT null_ol?(mult_pd_pd(x1, x2, pds1, pds2)) AND
  --    NOT null_ol?(pds2) AND NOT null_ol?(pds1)
  --    IMPLIES NOT null?[[nat, nzBaseType]](mult_pd_pd...
  have mult_pd_pd_last_bound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_pd_pd_last_bound_TCC2
  -- Statement: FORALL (pds1, pds2: ErrorTerms, x1, x2: BaseType):
  --   NOT null_ol?(mult_pd_pd(x1, x2, pds1, pds2)) AND
  --    NOT null_ol?(pds2) AND NOT null_ol?(pds1)
  --    IMPLIES NOT null?[[nat, nzBaseType]](pds1)
  have mult_pd_pd_last_bound_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mult_pd_pd_last_bound_TCC3
  -- Statement: FORALL (pds1, pds2: ErrorTerms, x1, x2: BaseType):
  --   NOT null_ol?(mult_pd_pd(x1, x2, pds1, pds2)) AND
  --    NOT null_ol?(pds2) AND NOT null_ol?(pds1)
  --    IMPLIES NOT null?[[nat, nzBaseType]](pds2)
  have mult_pd_pd_last_bound_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.search_ET_on_add_ET
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null_ol?(pds) OR m < car(pds)`1 IMPLIES
--  search_error_term_by_idx(add_ET(m, x, pds), n) =
--   IF n = m THEN x ELSE search_error_term_by_idx(pds, n) ENDIF
theorem search_ET_on_add_ET : null_ol_p pds ∨ m < car pds.1 → search_error_term_by_idx (add_ET m x pds) n =
 if n = m then x else search_error_term_by_idx pds n := by
  -- === TCCs for search_ET_on_add_ET ===
  -- TCC: search_ET_on_add_ET_TCC1
  -- Statement: FORALL (pds: ErrorTerms):
  --   NOT null_ol?(pds) IMPLIES cons?[[nat, nzBaseType]](pds)
  have search_et_on_add_et_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.search_ET_car
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds) IMPLIES
--  search_error_term_by_idx(pds, car(pds)`1) = car(pds)`2
theorem search_ET_car : ¬ null_ol_p pds → search_error_term_by_idx pds (car pds.1) = car pds.2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.search_ET_not_car
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null_ol?(pds) AND n /= car(pds)`1 IMPLIES
--  search_error_term_by_idx(pds, n) = search_error_term_by_idx(cdr(pds), n)
theorem search_ET_not_car : ¬ null_ol_p pds ∧ n ≠ car pds.1 → search_error_term_by_idx pds n = search_error_term_by_idx (cdr pds) n := by
  -- === TCCs for search_ET_not_car ===
  -- TCC: search_ET_not_car_TCC1
  -- Statement: FORALL (n: nat, pds: ErrorTerms):
  --   n /= car(pds)`1 AND NOT null_ol?(pds) IMPLIES
  --    ordered_list?[[nat, nzBaseType], lt_idx[nzBaseType]]
  --        (cdr[[nat, nzBaseType]](pds))
  have search_et_not_car_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.search_ET_in_mult_pd_pd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: search_error_term_by_idx(mult_pd_pd(cv1, cv2, pds1, pds2), n) =
--  cv1 * search_error_term_by_idx(pds2, n) +
--   cv2 * search_error_term_by_idx(pds1, n)
theorem search_ET_in_mult_pd_pd : search_error_term_by_idx (mult_pd_pd cv1 cv2 pds1 pds2) n = cv1 * search_error_term_by_idx pds2 n +
 cv2 * search_error_term_by_idx pds1 n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.sum_of_modules
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES l OF null: 0, cons(hd, tl): abs(hd`2) + sum_of_modules(tl) ENDCASES
axiom sum_of_modules {pvs_τ162 : Type*} : pvs_τ162 → Prop

-- pvs2lean: affine.sum_of_modules_append
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[[nat, nzBaseType]]):
--   sum_of_modules(append(l1, l2)) = sum_of_modules(l1) + sum_of_modules(l2)
theorem sum_of_modules_append : ∀ (l1 : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0})) (l2 : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0})), sum_of_modules (append l1 l2) = sum_of_modules l1 + sum_of_modules l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.first_noises
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: first[nzEpsilon](N, n)
axiom first_noises {pvs_τ72 : Type*} {pvs_τ73 : Type*} : pvs_τ72 → pvs_τ73 → Prop

-- pvs2lean: affine.first_noises_upd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: first_noises(upd_noise(N, n, e), n) = first_noises(N, n)
theorem first_noises_upd : first_noises (upd_noise N n e) n = first_noises N n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.first_ET
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: first[nzBaseType](pds, n)
axiom first_ET {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} : pvs_τ69 → pvs_τ70 → pvs_τ71

-- pvs2lean: affine.last_first_ET
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(first_ET(pds, n)) IMPLIES last_ol(first_ET(pds, n))`1 < n
theorem last_first_ET : ¬ null_p (first_ET pds n) → last_ol (first_ET pds n).1 < n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.last_first_noises
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(first_noises(N, n)) IMPLIES last_ol(first_noises(N, n))`1 < n
theorem last_first_noises : ¬ null_p (first_noises N n) → last_ol (first_noises N n).1 < n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_lt_first_idem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n < m IMPLIES
--  get_noise_by_idx(n, first_noises(N, m)) = get_noise_by_idx(n, N)
theorem gnbi_lt_first_idem : n < m → get_noise_by_idx n (first_noises N m) = get_noise_by_idx n N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.first_noises_zero_prop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: first_noises(N, 0) = empty_noise
theorem first_noises_zero_prop : first_noises N 0 = empty_noise := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.first_error_terms_recursive_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: first_ET(pds, n + 1) =
--  append_ol(first_ET(pds, n),
--            IF search_error_term_by_idx(pds, n) = 0 THEN empty_ErrorTerms
--            ELSE cons_ol((n, search_error_term_by_idx(pds, n)),
--             
theorem first_error_terms_recursive_def : first_ET pds (n + 1) = append_ol (first_ET pds n) (if search_error_term_by_idx pds n = 0 then empty_ErrorTerms
          else cons_ol ((n, search_error_term_by_idx pds n)) empty_ErrorTerms) := by
  -- === TCCs for first_error_terms_recursive_def ===
  -- TCC: first_error_terms_recursive_def_TCC1
  -- Statement: FORALL (n: nat, pds: ErrorTerms):
  --   search_error_term_by_idx(pds, n) = 0 IMPLIES
  --    appendable_ol?[[nat, nzBaseType], lt_idx[nzBaseType]]
  --        (first_ET(pds, n), empty_ErrorTerms)
  have first_error_terms_recursive_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_error_terms_recursive_def_TCC2
  -- Statement: FORALL (n: nat, pds: ErrorTerms):
  --   NOT search_error_term_by_idx(pds, n) = 0 IMPLIES
  --    search_error_term_by_idx(pds, n) /= 0
  have first_error_terms_recursive_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_error_terms_recursive_def_TCC3
  -- Statement: FORALL (n: nat, pds: ErrorTerms):
  --   NOT search_error_term_by_idx(pds, n) = 0 IMPLIES
  --    null?[[nat, nzBaseType]](empty_ErrorTerms) OR
  --     lt_idx[nzBaseType]
  --         ((n, search_error_term_by_idx(pd...
  have first_error_terms_recursive_def_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_error_terms_recursive_def_TCC4
  -- Statement: FORALL (n: nat, pds: ErrorTerms):
  --   NOT search_error_term_by_idx(pds, n) = 0 IMPLIES
  --    appendable_ol?[[nat, nzBaseType], lt_idx[nzBaseType]]
  --        (first_ET(pds, n),
  --         cons_ol[[nat, nzBaseT...
  have first_error_terms_recursive_def_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.first_noises_recursive_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: first_noises(N, n + 1) =
--  append_ol(first_noises(N, n),
--            IF get_noise_by_idx(n, N) = 0 THEN empty_noise
--            ELSE cons_ol((n, get_noise_by_idx(n, N)), empty_noise)
--            ENDIF)
theorem first_noises_recursive_def : first_noises N (n + 1) = append_ol (first_noises N n) (if get_noise_by_idx n N = 0 then empty_noise
          else cons_ol ((n, get_noise_by_idx n N)) empty_noise) := by
  -- === TCCs for first_noises_recursive_def ===
  -- TCC: first_noises_recursive_def_TCC1
  -- Statement: FORALL (N: Noise, n: nat):
  --   get_noise_by_idx(n, N) = 0 IMPLIES
  --    appendable_ol?[[nat, nzEpsilon], lt_idx[nzEpsilon]]
  --        (first_noises(N, n), empty_noise)
  have first_noises_recursive_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_noises_recursive_def_TCC2
  -- Statement: FORALL (N: Noise, n: nat):
  --   NOT get_noise_by_idx(n, N) = 0 IMPLIES get_noise_by_idx(n, N) /= 0
  have first_noises_recursive_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_noises_recursive_def_TCC3
  -- Statement: FORALL (N: Noise, n: nat):
  --   NOT get_noise_by_idx(n, N) = 0 IMPLIES
  --    null?[[nat, nzEpsilon]](empty_noise) OR
  --     lt_idx[nzEpsilon]
  --         ((n, get_noise_by_idx(n, N)), car[[nat, nzEpsilon]](empt...
  have first_noises_recursive_def_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: first_noises_recursive_def_TCC4
  -- Statement: FORALL (N: Noise, n: nat):
  --   NOT get_noise_by_idx(n, N) = 0 IMPLIES
  --    appendable_ol?[[nat, nzEpsilon], lt_idx[nzEpsilon]]
  --        (first_noises(N, n),
  --         cons_ol[[nat, nzEpsilon], lt_idx[nzEps...
  have first_noises_recursive_def_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_pd_noise_incremental
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(pds1, first_noises(N, 1 + n)) =
--  eval_pd_noise(pds1, first_noises(N, n)) +
--   (search_error_term_by_idx(pds1, n) * get_noise_by_idx(n, N))
theorem eval_pd_noise_incremental : eval_pd_noise pds1 (first_noises N (1 + n)) = eval_pd_noise pds1 (first_noises N n) +
 (search_error_term_by_idx pds1 n * get_noise_by_idx n N) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.partial_eval_pd_noise_upper_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(eval_pd_noise(pds, first_noises(N, n))) <= sum_of_modules(first(pds, n))
theorem partial_eval_pd_noise_upper_bound : |eval_pd_noise pds (first_noises N n)| ≤ sum_of_modules (first pds n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mult_ac_ac
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: {|cv(Xa1) * cv(Xa2),
--   append_ol(mult_pd_pd(cv(Xa1), cv(Xa2), pd(Xa1), pd(Xa2)),
--             add_ET(newidx,
--                    sum_of_modules(pd(Xa1)) * sum_of_modules(pd(Xa2)),
--                    emp
axiom mult_ac_ac {pvs_τ121 : Type*} {pvs_τ122 : Type*} {pvs_τ123 : Type*} {pvs_τ124 : Type*} : pvs_τ121 → pvs_τ122 → pvs_τ123 → pvs_τ124

-- pvs2lean: affine.mul_noise_value
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET x1 = eval_ac_noise(Xa1, N), x2 = eval_ac_noise(Xa2, N) IN
--   LET som_x_som = sum_of_modules(pd(Xa1)) * sum_of_modules(pd(Xa2)) IN
--     IF som_x_som = 0 THEN 0
--     ELSE (x1 * x2 - cv(Xa1) * cv(Xa2) -
axiom mul_noise_value {pvs_τ112 : Type*} {pvs_τ113 : Type*} {pvs_τ114 : Type*} : pvs_τ112 → pvs_τ113 → pvs_τ114 → Prop

-- pvs2lean: affine.som_inc
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sum_of_modules(first(pds, n + 1)) =
--  sum_of_modules(first(pds, n)) + abs(search_error_term_by_idx(pds, n))
theorem som_inc : sum_of_modules (first pds (n + 1)) = sum_of_modules (first pds n) + |search_error_term_by_idx pds n| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_pd_noise_inc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(mult_pd_pd(cv(Xa1), cv(Xa2), pd(Xa1), pd(Xa2)),
--               first(N, n + 1))
--  =
--  eval_pd_noise(mult_pd_pd(cv(Xa1), cv(Xa2), pd(Xa1), pd(Xa2)), first(N, n))
--   +
--   (cv(Xa1) * get_noise_b
theorem eval_pd_noise_inc : eval_pd_noise (mult_pd_pd (cv Xa1) (cv Xa2) (pd Xa1) (pd Xa2)) (first N (n + 1)) = eval_pd_noise (mult_pd_pd (cv Xa1) (cv Xa2) (pd Xa1) (pd Xa2)) (first N n)
 +
 (cv Xa1 * get_noise_by_idx n N * search_error_term_by_idx (pd Xa2) n +
   cv Xa2 * get_noise_by_idx n N * search_error_term_by_idx (pd Xa1) n) := by
  -- === TCCs for eval_pd_noise_inc ===
  -- TCC: eval_pd_noise_inc_TCC1
  -- Statement: FORALL (N: Noise, n: nat):
  --   ordered_list?[[nat, nzEpsilon], lt_idx[nzEpsilon]]
  --       (first[nzEpsilon](N, n + 1))
  have eval_pd_noise_inc_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_ac_noise_inc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_ac_noise(Xa1, first_noises(N, 1 + n)) =
--  eval_ac_noise(Xa1, first_noises(N, n)) +
--   (search_error_term_by_idx(pd(Xa1), n) * get_noise_by_idx(n, N))
theorem eval_ac_noise_inc : eval_ac_noise Xa1 (first_noises N (1 + n)) = eval_ac_noise Xa1 (first_noises N n) +
 (search_error_term_by_idx (pd Xa1) n * get_noise_by_idx n N) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.eval_ac_noise_mult_inc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_ac_noise(Xa1, first_noises(N, 1 + n)) *
--  eval_ac_noise(Xa2, first_noises(N, 1 + n))
--  =
--  eval_ac_noise(Xa1, first_noises(N, n)) *
--   eval_ac_noise(Xa2, first_noises(N, n))
--   +
--   eval_ac_noise(Xa1, 
theorem eval_ac_noise_mult_inc : eval_ac_noise Xa1 (first_noises N (1 + n)) *
 eval_ac_noise Xa2 (first_noises N (1 + n)) = eval_ac_noise Xa1 (first_noises N n) *
 eval_ac_noise Xa2 (first_noises N n)
 +
 eval_ac_noise Xa1 (first_noises N n) *
  search_error_term_by_idx (pd Xa2) n
  * get_noise_by_idx n N
 +
 eval_ac_noise Xa2 (first_noises N n) *
  search_error_term_by_idx (pd Xa1) n
  * get_noise_by_idx n N
 +
 search_error_term_by_idx (pd Xa1) n * get_noise_by_idx n N *
  search_error_term_by_idx (pd Xa2) n
  * get_noise_by_idx n N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.sum_of_modules_decomp
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sum_of_modules(first(pds1, n + 1)) * sum_of_modules(first(pds2, n + 1)) =
--  sum_of_modules(first(pds1, n)) * sum_of_modules(first(pds2, n)) +
--   sum_of_modules(first(pds1, n)) * abs(search_error_term_by
theorem sum_of_modules_decomp : sum_of_modules (first pds1 (n + 1)) * sum_of_modules (first pds2 (n + 1)) = sum_of_modules (first pds1 n) * sum_of_modules (first pds2 n) +
 sum_of_modules (first pds1 n) * |search_error_term_by_idx pds2 n|
 + sum_of_modules (first pds2 n) * |search_error_term_by_idx pds1 n|
 +
 |search_error_term_by_idx pds1 n| *
  |search_error_term_by_idx pds2 n| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mul_ac_ac_noise_incremental_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(eval_ac_noise(Xa1, first_noises(N, n)) *
--      eval_ac_noise(Xa2, first_noises(N, n))
--      - cv(Xa1) * cv(Xa2)
--      -
--      eval_pd_noise(mult_pd_pd(cv(Xa1), cv(Xa2), pd(Xa1), pd(Xa2)),
--             
theorem mul_ac_ac_noise_incremental_bound : |eval_ac_noise Xa1 (first_noises N n) *
     eval_ac_noise Xa2 (first_noises N n)
     - cv Xa1 * cv Xa2
     -
     eval_pd_noise (mult_pd_pd (cv Xa1) (cv Xa2) (pd Xa1) (pd Xa2)) (first_noises N n)| ≤ sum_of_modules (first (pd Xa1) n) * sum_of_modules (first (pd Xa2) n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.som_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sum_of_modules(pds) = 0 IMPLIES null_ol?(pds)
theorem som_zero : sum_of_modules pds = 0 → null_ol_p pds := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_aET__nnull_pds
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(add_ET(n, x, pds), N) =
--  IF (null_ol?(pds) OR n < car(pds)`1) AND x /= 0
--    THEN eval_pd_noise(cons_ol((n, x), empty_ErrorTerms), N)
--  ELSE 0
--  ENDIF
--   + eval_pd_noise(pds, N)
theorem epn_aET__nnull_pds : eval_pd_noise (add_ET n x pds) N = if (null_ol_p pds ∨ n < car pds.1) ∧ x ≠ 0
  then eval_pd_noise (cons_ol ((n, x)) empty_ErrorTerms) N
else 0

 + eval_pd_noise pds N := by
  -- === TCCs for epn_aET__nnull_pds ===
  -- TCC: epn_aET__nnull_pds_TCC1
  -- Statement: FORALL (n: nat, pds: ErrorTerms, x: BaseType):
  --   x /= 0 AND (null_ol?(pds) OR n < car(pds)`1) IMPLIES
  --    null?[[nat, nzBaseType]](empty_ErrorTerms) OR
  --     lt_idx[nzBaseType]((n, x), car[[nat, nzBas...
  have epn_aet__nnull_pds_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_mpp_null_pds1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(mult_pd_pd(x1, x2, null, pds), N) = x1 * eval_pd_noise(pds, N)
theorem epn_mpp_null_pds1 : eval_pd_noise (mult_pd_pd x1 x2 null pds) N = x1 * eval_pd_noise pds N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_mpp_null_pds2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(mult_pd_pd(x1, x2, pds, null), N) = x2 * eval_pd_noise(pds, N)
theorem epn_mpp_null_pds2 : eval_pd_noise (mult_pd_pd x1 x2 pds null) N = x2 * eval_pd_noise pds N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_over_last_is_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: last_idx(pds) < n IMPLIES
--  eval_pd_noise(pds, cons_ol((n, nze), empty_noise)) = 0
theorem epn_over_last_is_zero : last_idx pds < n → eval_pd_noise pds (cons_ol ((n, nze)) empty_noise) = 0 := by
  -- === TCCs for epn_over_last_is_zero ===
  -- TCC: epn_over_last_is_zero_TCC1
  -- Statement: FORALL (n: nat, nze: nzEpsilon, pds: ErrorTerms):
  --   last_idx(pds) < n IMPLIES
  --    null?[[nat, nzEpsilon]](empty_noise) OR
  --     lt_idx[nzEpsilon]((n, nze), car[[nat, nzEpsilon]](empty_noise))
  have epn_over_last_is_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_up_to_last_idx_pd_is_zero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: eval_pd_noise(pds, first_noises(N, 1 + last_idx(pds))) =
--  eval_pd_noise(pds, N)
theorem epn_up_to_last_idx_pd_is_zero : eval_pd_noise pds (first_noises N (1 + last_idx pds)) = eval_pd_noise pds N := by
  -- === TCCs for epn_up_to_last_idx_pd_is_zero ===
  -- TCC: epn_up_to_last_idx_pd_is_zero_TCC1
  -- Statement: FORALL (pds: ErrorTerms): 1 + last_idx[nzBaseType](pds) >= 0
  have epn_up_to_last_idx_pd_is_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_trim_noise
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: last_idx(pds) < n IMPLIES
--  eval_pd_noise(pds, first_noises(N, n)) = eval_pd_noise(pds, N)
theorem epn_trim_noise : last_idx pds < n → eval_pd_noise pds (first_noises N n) = eval_pd_noise pds N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.epn_mult_first
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: last_idx(pds1) < n AND last_idx(pds2) < n IMPLIES
--  eval_pd_noise(mult_pd_pd(x1, x2, pds1, pds2), first_noises(N, n)) =
--   eval_pd_noise(mult_pd_pd(x1, x2, pds1, pds2), N)
theorem epn_mult_first : last_idx pds1 < n ∧ last_idx pds2 < n → eval_pd_noise (mult_pd_pd x1 x2 pds1 pds2) (first_noises N n) =
 eval_pd_noise (mult_pd_pd x1 x2 pds1 pds2) N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.mpp_first_N
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= next_idx_2(pd(Xa1), pd(Xa2)) IMPLIES
--  mul_noise_value(Xa1, Xa2, first_noises(N, n)) =
--   mul_noise_value(Xa1, Xa2, N)
theorem mpp_first_N : n ≥ next_idx_2 (pd Xa1) (pd Xa2) → mul_noise_value Xa1 Xa2 (first_noises N n) = mul_noise_value Xa1 Xa2 N := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.containment_mul
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= next_idx_2(pd(Xa1), pd(Xa2)) AND
--  eval_ac_noise(Xa1, N) = x1 AND
--   eval_ac_noise(Xa2, N) = x2 AND
--    get_noise_by_idx(n, N) = mul_noise_value(Xa1, Xa2, first_noises(N, n))
--  IMPLIES eval_ac_noise(
theorem containment_mul : n ≥ next_idx_2 (pd Xa1) (pd Xa2) ∧
 eval_ac_noise Xa1 N = x1 ∧
  eval_ac_noise Xa2 N = x2 ∧
   get_noise_by_idx n N = mul_noise_value Xa1 Xa2 (first_noises N n) → eval_ac_noise (mult_ac_ac Xa1 Xa2 n) N = x1 * x2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.next_idx_mult_ac_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: upfrom(next_idx_2(Xa1`pd, Xa2`pd))):
--   next_idx(pd(mult_ac_ac(Xa1, Xa2, n))) <= 1 + n
theorem next_idx_mult_ac_ac : ∀ (n : {s : ℝ // s ≥ next_idx_2 Xa1.pd Xa2.pd}), next_idx (pd (mult_ac_ac Xa1 Xa2 n)) ≤ 1 + n := by
  -- === TCCs for next_idx_mult_ac_ac ===
  -- TCC: next_idx_mult_ac_ac_TCC1
  -- Statement: FORALL (Xa1, Xa2: AffineCombination,
  --         n: upfrom(next_idx_2(Xa1`pd, Xa2`pd))):
  --   n >= next_idx_2(pd(Xa1), pd(Xa2))
  have next_idx_mult_ac_ac_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.idxs_ac_mult_ac_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: upfrom(next_idx_2(Xa1`pd, Xa2`pd)), i: nat):
--   member(i, idxs_ac(mult_ac_ac(Xa1, Xa2, n))) IMPLIES
--    member(i, idxs_ac(Xa1)) OR member(i, idxs_ac(Xa2)) OR i = n
theorem idxs_ac_mult_ac_ac : ∀ (n : {s : ℝ // s ≥ next_idx_2 Xa1.pd Xa2.pd}) (i : {i : ℝ // i ≥ 0}), member i (idxs_ac (mult_ac_ac Xa1 Xa2 n)) → member i (idxs_ac Xa1) ∨ member i (idxs_ac Xa2) ∨ i = n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.null_ol?_empty_noise
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null_ol?(empty_noise)
theorem null_ol_p_empty_noise : null_ol_p empty_noise := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.var_ac_noise_unf3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (null_ol?(N) OR m < car_ol(N)`1) AND
--  ((m = n AND eval_ac_noise(var_ac(Xi, n), extend_N(m, e, empty_noise)) = x)
--    OR (m /= n AND eval_ac_noise(var_ac(Xi, n), N) = x))
--  IMPLIES eval_ac_noise(var_ac(X
theorem var_ac_noise_unf3 : (null_ol_p N ∨ m < car_ol N.1) ∧
 ((m = n ∧ eval_ac_noise (var_ac Xi n) (extend_N m e empty_noise) = x)
   ∨ (m ≠ n ∧ eval_ac_noise (var_ac Xi n) N = x)) → eval_ac_noise (var_ac Xi n) (extend_N m e N) = x := by
  -- === TCCs for var_ac_noise_unf3 ===
  -- TCC: var_ac_noise_unf3_TCC1
  -- Statement: FORALL (N: Noise, m, n: nat):
  --   m = n AND (null_ol?(N) OR m < car_ol(N)`1) IMPLIES
  --    null_ol?[[nat, nzEpsilon], lt_idx[nzEpsilon]](empty_noise) OR
  --     m < car_ol[[nat, nzEpsilon], lt_idx[nzEpsilon...
  have var_ac_noise_unf3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_extend_N_unf
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (null_ol?(N) OR m < car_ol(N)`1) AND
--  ((m = n AND get_noise_by_idx(n, extend_N(m, e, empty_noise)) = e2) OR
--    (m /= n AND get_noise_by_idx(n, N) = e2))
--  IMPLIES get_noise_by_idx(n, extend_N(m, e, N))
theorem gnbi_extend_N_unf : (null_ol_p N ∨ m < car_ol N.1) ∧
 ((m = n ∧ get_noise_by_idx n (extend_N m e empty_noise) = e2) ∨
   (m ≠ n ∧ get_noise_by_idx n N = e2)) → get_noise_by_idx n (extend_N m e N) = e2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.gnbi_extend_N_trivial
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: get_noise_by_idx(n, extend_N(n, e, empty_noise)) = e
theorem gnbi_extend_N_trivial : get_noise_by_idx n (extend_N n e empty_noise) = e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.first__extend_N
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (null_ol?(N) OR m < car_ol(N)`1) IMPLIES
--  first_noises(extend_N(m, e, N), n) =
--   IF n <= m THEN empty_noise ELSE extend_N(m, e, first_noises(N, n)) ENDIF
theorem first__extend_N : (null_ol_p N ∨ m < car_ol N.1) → first_noises (extend_N m e N) n =
 if n ≤ m then empty_noise else extend_N m e (first_noises N n) := by
  -- === TCCs for first__extend_N ===
  -- TCC: first__extend_N_TCC1
  -- Statement: FORALL (N: Noise, m, n: nat):
  --   NOT n <= m AND (null_ol?(N) OR m < car_ol(N)`1) IMPLIES
  --    null_ol?[[nat, nzEpsilon], lt_idx[nzEpsilon]](first_noises(N, n)) OR
  --     m < car_ol[[nat, nzEpsilon], lt_i...
  have first__extend_n_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine.Half_pd
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null_ol?(pds) THEN empty_ErrorTerms
-- ELSE LET head = car_ol(pds), tail = cdr_ol(pds) IN
--        cons_ol((head`1, head`2 / 2), Half_pd(tail))
-- ENDIF
axiom Half_pd {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ4 → pvs_τ5

-- pvs2lean: affine.AC_HalfLeft
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET Xi = eval_by_intervals(Xa) IN
--   {|(midpoint(Xi) + lb(Xi)) / 2, Half_pd(pd(Xa))|}
noncomputable def AC_HalfLeft : sorry := by
  sorry  -- pvs2lean: definition stub for LET Xi = eval_by_intervals(Xa) IN   {|(midpoint(Xi) + lb(Xi)) / 2, Half_pd(pd(Xa

-- pvs2lean: affine.AC_HalfRight
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET Xi = eval_by_intervals(Xa) IN
--   {|(midpoint(Xi) + ub(Xi)) / 2, Half_pd(pd(Xa))|}
noncomputable def AC_HalfRight : sorry := by
  sorry  -- pvs2lean: definition stub for LET Xi = eval_by_intervals(Xa) IN   {|(midpoint(Xi) + ub(Xi)) / 2, Half_pd(pd(Xa

end

end NavierStokesClean.PVSLib.affine