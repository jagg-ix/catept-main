-- Auto-generated by pvs2lean/translator.py
-- PVS theory: affine_pow
-- PVSLib library: affine_arith
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: affine_pow

Auto-generated stubs from PVSLib theory `affine_pow`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.affine_pow

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (above : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (append : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (below : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (car : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (cons_ol : pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (cons_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (cv : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (eval_ac_noise : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (get_noise_by_idx : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (idxs_ac : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} (length : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (member : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (min : pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (next_idx : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (nth : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} (null_p : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (pd : pvs_τ36 → pvs_τ37)
variable {pvs_τ52 : Type*} (represents_var_p : pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (restrict : pvs_τ53 → pvs_τ54)
variable {pvs_τ55 : Type*} (strict_order_p : pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} (subrange : pvs_τ56 → pvs_τ57 → pvs_τ58)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} (take : pvs_τ59 → pvs_τ60 → pvs_τ61)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} (upd_noise_burst : pvs_τ62 → pvs_τ63 → pvs_τ64 → pvs_τ65)

-- pvs2lean: affine_pow.power_Epsilon_is_Epsilon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: Epsilon, n: nat): -1 <= e ^ n AND e ^ n <= 1
theorem power_Epsilon_is_Epsilon : ∀ (e : {x : ℝ // -1 ≤ x ∧ x ≤ 1}) (n : {i : ℝ // i ≥ 0}), -1 ≤ e ^ n ∧ e ^ n ≤ 1 := by
  -- === TCCs for power_Epsilon_is_Epsilon ===
  -- TCC: power_Epsilon_is_Epsilon_TCC1
  -- Statement: FORALL (e: Epsilon, n: nat): e /= 0 OR n >= 0
  have power_epsilon_is_epsilon_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: affine_pow.pow_var_pd
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF k = n THEN cons_ol((car(idxs), pds_bt ^ k), empty_ErrorTerms)
-- ELSE cons_ol((car(idxs), C(n, k) * cv ^ (n - k) * pds_bt ^ k),
--              pow_var_pd(cv, pds_bt, n, k + 1,
--                         cd
axiom pow_var_pd {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} : pvs_τ46 → pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → pvs_τ51

-- pvs2lean: affine_pow.pow_var_ac
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF power = 0 THEN {|1, empty_ErrorTerms|}
-- ELSE {|cv(Xa) ^ power,
--        IF length(pd(Xa)) = 0 THEN empty_ErrorTerms
--        ELSIF cv(Xa) = 0
--          THEN IF power = 1 THEN pd(Xa)
--               ELSE co
axiom pow_var_ac {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ38 → pvs_τ39 → pvs_τ40 → pvs_τ41

-- pvs2lean: affine_pow.nats
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF s = e THEN (: e :)
-- ELSE cons_ol[nat, restrict[[real, real], [nat, nat], bool](<)]
--          (s, nats(s + 1, e))
-- ENDIF
axiom nats {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ27 → pvs_τ28 → pvs_τ29

-- pvs2lean: affine_pow.member_nats
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat, s: nat, e: nat | s <= e):
--   member(i, nats(s, e)) IMPLIES s <= i AND i <= e
theorem member_nats : ∀ (i : {i : ℝ // i ≥ 0}) (s : {i : ℝ // i ≥ 0}) (e : {e : {i : ℝ // i ≥ 0} // s ≤ e}), member i (nats s e) → s ≤ i ∧ i ≤ e := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: affine_pow.take_nats
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: above(0), s: nat, e: nat | s <= e):
--   take(nats(s, e), i) = nats(s, min(e, s + i - 1))
theorem take_nats : ∀ (i : {s : ℝ // s > 0}) (s : {i : ℝ // i ≥ 0}) (e : {e : {i : ℝ // i ≥ 0} // s ≤ e}), take (nats s e) i = nats s (min e (s + i - 1)) := by
  -- === TCCs for take_nats ===
  -- TCC: take_nats_TCC1
  -- Statement: FORALL (i: above(0), s: nat, e: nat | s <= e):
  --   min(e, s + i - 1) >= 0 AND s <= min(e, s + i - 1)
  have take_nats_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: affine_pow.next_idx_pow_var_ac
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Xa: (represents_var?), power: nat,
--         (idxs:
--            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
--              | (length(idxs) > power - 2 AND
--                  (NOT n
theorem next_idx_pow_var_ac : ∀ (Xa : ℝ × ({l : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0}) // ordered_list_p l})) (power : {i : ℝ // i ≥ 0}) (idxs : {idxs : {l : List ℝ // ordered_list_p l} // (List.length idxs > power - 2 ∧
  (¬ null_p idxs → car idxs ≥ next_idx (pd Xa))
  ∨ List.length (pd Xa) = 0)}), next_idx (pd (pow_var_ac Xa power idxs)) = if power = 0 ∨ List.length (pd Xa) = 0 then 0
ELSIF power = 1 then next_idx (pd Xa)
else nth idxs (power - 2) + 1 := by
  -- === TCCs for next_idx_pow_var_ac ===
  -- TCC: next_idx_pow_var_ac_TCC1
  -- Statement: FORALL (Xa: (represents_var?), power: nat,
  --         (idxs:
  --            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
  --              | (length[nat](idxs) > power - 2 AND
  --                ...
  have next_idx_pow_var_ac_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine_pow.idxs_ac_pow_var_ac
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Xa: (represents_var?), power: nat,
--         (idxs:
--            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
--              | (length(idxs) > power - 2 AND
--                  (NOT n
theorem idxs_ac_pow_var_ac : ∀ (Xa : ℝ × ({l : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0}) // ordered_list_p l})) (power : {i : ℝ // i ≥ 0}) (idxs : {idxs : {l : List ℝ // ordered_list_p l} // (List.length idxs > power - 2 ∧
  (¬ null_p idxs → car idxs ≥ next_idx (pd Xa)))
 ∨ List.length (pd Xa) = 0}), member i (idxs_ac (pow_var_ac Xa power idxs)) → member i (idxs_ac Xa) ∨
 (¬ List.length (pd Xa) = 0 ∧
   ¬ null_p idxs ∧
    2 ≤ power ∧ member i (take idxs (power - 2 + 1))) := by
  -- === TCCs for idxs_ac_pow_var_ac ===
  -- TCC: idxs_ac_pow_var_ac_TCC1
  -- Statement: FORALL (i: nat, Xa: (represents_var?), power: nat,
  --         (idxs:
  --            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
  --              | (length[nat](idxs) > power - 2 AND
  --        ...
  have idxs_ac_pow_var_ac_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine_pow.idxs_ac_pow_var_ac_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Xa: (represents_var?), power: nat,
--         (idxs:
--            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
--              | (length(idxs) > power - 2 AND
--                  (NOT n
theorem idxs_ac_pow_var_ac_2 : ∀ (Xa : ℝ × ({l : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0}) // ordered_list_p l})) (power : {i : ℝ // i ≥ 0}) (idxs : {idxs : {l : List ℝ // ordered_list_p l} // (List.length idxs > power - 2 ∧
  (¬ null_p idxs → car idxs ≥ next_idx (pd Xa)))
 ∨ List.length (pd Xa) = 0}), (¬ List.length (pd Xa) = 0) ∧ (¬ null_p idxs) ∧ (2 ≤ power)
 ∧ member i (take idxs (power - 2 + 1)) ∧ (cv Xa ≠ 0) → member i (idxs_ac (pow_var_ac Xa power idxs)) := by
  -- === TCCs for idxs_ac_pow_var_ac_2 ===
  -- TCC: idxs_ac_pow_var_ac_2_TCC1
  -- Statement: FORALL (Xa: (represents_var?), power: nat,
  --         (idxs:
  --            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
  --              | (length[nat](idxs) > power - 2 AND
  --                ...
  have idxs_ac_pow_var_ac_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine_pow.pow_var_compatible?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (length(pd(Xa)) > 0 IMPLIES
--   FORALL (i: below(power - 1)):
--     get_noise_by_idx(nth(nwidxs, i), N) =
--      get_noise_by_idx(nth(pd(Xa), 0)`1, N) ^ (i + 2))
axiom pow_var_compatible_p {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} : pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45 → Prop

-- pvs2lean: affine_pow.containment_pow_var_ac
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Xa: (represents_var?), power: nat,
--         (nwidxs:
--            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
--              | (length(nwidxs) > power - 2 AND
--                  (N
theorem containment_pow_var_ac : ∀ (Xa : ℝ × ({l : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0}) // ordered_list_p l})) (power : {i : ℝ // i ≥ 0}) (nwidxs : {nwidxs : {l : List ℝ // ordered_list_p l} // (List.length nwidxs > power - 2 ∧
  (¬ null_p nwidxs → car nwidxs ≥ next_idx (pd Xa)))
 ∨ List.length (pd Xa) = 0}), eval_ac_noise Xa N = x ∧
 (¬ null_p (pd Xa) → ¬ member (car (pd Xa).1) nwidxs) ∧
  pow_var_compatible_p Xa power nwidxs(N) → eval_ac_noise (pow_var_ac Xa power nwidxs) N = x ^ power := by
  -- === TCCs for containment_pow_var_ac ===
  -- TCC: containment_pow_var_ac_TCC1
  -- Statement: FORALL (N: Noise, x: BaseType, Xa: (represents_var?), power: nat,
  --         (nwidxs:
  --            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
  --              | (length[nat](nwidxs) > po...
  have containment_pow_var_ac_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: containment_pow_var_ac_TCC2
  -- Statement: FORALL (N: Noise, x: BaseType, Xa: (represents_var?), power: nat,
  --         (nwidxs:
  --            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
  --              | (length[nat](nwidxs) > po...
  have containment_pow_var_ac_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine_pow.notmember_ol_nat
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ol: ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)],
--         n: nat):
--   NOT null?(ol) AND n < car(ol) IMPLIES NOT member(n, ol)
theorem notmember_ol_nat : ∀ (ol : {l : List ℝ // ordered_list_p l}) (n : {i : ℝ // i ≥ 0}), ¬ null_p ol ∧ n < car ol → ¬ member n ol := by
  -- === TCCs for notmember_ol_nat ===
  -- TCC: notmember_ol_nat_TCC1
  -- Statement: FORALL (ol:
  --           ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]):
  --   NOT null?(ol) IMPLIES cons?[nat](ol)
  have notmember_ol_nat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine_pow.pow_var_compatible_noise
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((Xa: AffineCombination
--              | represents_var?(Xa) AND length(pd(Xa)) > 0),
--         power: nat,
--         (nwidxs:
--            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)
theorem pow_var_compatible_noise : ∀ (Xa : {Xa : ℝ × ({l : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0}) // ordered_list_p l}) // represents_var_p Xa ∧ List.length (pd Xa) > 0}) (power : {i : ℝ // i ≥ 0}) (nwidxs : {nwidxs : {l : List ℝ // ordered_list_p l} // (List.length nwidxs > power - 2 ∧
  (¬ null_p nwidxs → car nwidxs ≥ next_idx (pd Xa))
  ∨ List.length (pd Xa) = 0)}), pow_var_compatible_p Xa power nwidxs (upd_noise_burst N nwidxs (fun (i : ℕ)=> (get_noise_by_idx (car (pd Xa).1) N ^ (2 + i)))) := by
  -- === TCCs for pow_var_compatible_noise ===
  -- TCC: pow_var_compatible_noise_TCC1
  -- Statement: FORALL ((Xa: AffineCombination
  --              | represents_var?(Xa) AND
  --                 length[[nat, nzBaseType]](pd(Xa)) > 0),
  --         power: nat,
  --         (nwidxs:
  --            ordered_list[nat, restr...
  have pow_var_compatible_noise_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: pow_var_compatible_noise_TCC2
  -- Statement: FORALL (N: Noise,
  --         (Xa: AffineCombination
  --              | represents_var?(Xa) AND
  --                 length[[nat, nzBaseType]](pd(Xa)) > 0),
  --         power: nat,
  --         (nwidxs:
  --            order...
  have pow_var_compatible_noise_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: affine_pow.powvar_comp_noise_extension
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (power1: above(1), power2: above(power1),
--         (varac: (represents_var?) | NOT null?(pd(varac))),
--         (idxs:
--            ordered_list[nat, restrict[[real, real], [nat, nat], bool](<)]
--    
theorem powvar_comp_noise_extension : ∀ (power1 : {s : ℝ // s > 1}) (power2 : {s : ℝ // s > power1}) (varac : {varac : ℝ × ({l : List ({i : ℝ // i ≥ 0} × {r : ℝ // r ≠ 0}) // ordered_list_p l}) // ¬ null_p (pd varac)}) (idxs : {idxs : {l : List ℝ // ordered_list_p l} // (List.length idxs > power1 - 2 ∧
  (¬ null_p idxs → car idxs ≥ next_idx (pd varac)))
 ∨ List.length (pd varac) = 0}) (nextfreeidx : {s : ℝ // s >
 last_ol[nat, restrict[[real, real], [nat, nat], bool](<)]
     (take idxs (power1 - 1))}), fun (newoff : ℝ) => pow_var_compatible_p varac power1 idxs(N) → pow_var_compatible_p varac power2 (append (take idxs (power1 - 1)) (nats nextfreeidx (newoff - 1)))
                   (upd_noise_burst N (nats nextfreeidx (newoff - 1)) (fun (i : ℕ)=> (get_noise_by_idx (car (pd varac).1) N
                                        ^ (1 + i + power1)))) (nextfreeidx - power1 + power2) := by
  -- === TCCs for powvar_comp_noise_extension ===
  -- TCC: powvar_comp_noise_extension_TCC1
  -- Statement: FORALL (N: Noise, power1: above(1), power2: above(power1),
  --         (varac: (represents_var?)
  --              | NOT null?[[nat, nzBaseType]](pd(varac))),
  --         (idxs:
  --            ordered_list[nat, res...
  have powvar_comp_noise_extension_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: powvar_comp_noise_extension_TCC2
  -- Statement: FORALL (N: Noise, power1: above(1), power2: above(power1),
  --         (varac: (represents_var?)
  --              | NOT null?[[nat, nzBaseType]](pd(varac))),
  --         (idxs:
  --            ordered_list[nat, res...
  have powvar_comp_noise_extension_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: powvar_comp_noise_extension_TCC3
  -- Statement: FORALL (N: Noise, power1: above(1), power2: above(power1),
  --         (varac: (represents_var?)
  --              | NOT null?[[nat, nzBaseType]](pd(varac))),
  --         (idxs:
  --            ordered_list[nat, res...
  have powvar_comp_noise_extension_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: powvar_comp_noise_extension_TCC4
  -- Statement: FORALL (N: Noise, power1: above(1), power2: above(power1),
  --         (varac: (represents_var?)
  --              | NOT null?[[nat, nzBaseType]](pd(varac))),
  --         (idxs:
  --            ordered_list[nat, res...
  have powvar_comp_noise_extension_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: powvar_comp_noise_extension_TCC5
  -- Statement: FORALL (N: Noise, power1: above(1), power2: above(power1),
  --         (varac: (represents_var?)
  --              | NOT null?[[nat, nzBaseType]](pd(varac))),
  --         (idxs:
  --            ordered_list[nat, res...
  have powvar_comp_noise_extension_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.affine_pow