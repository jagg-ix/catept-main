-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ordered_finite_sequences
-- PVSLib library: fault_tolerance
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ordered_finite_sequences

Auto-generated stubs from PVSLib theory `ordered_finite_sequences`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ordered_finite_sequences

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (card : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (in_p : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (is_finite : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (length : pvs_τ8 → Prop)
variable {pvs_τ17 : Type*} (max : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (min : pvs_τ18 → Prop)
variable {pvs_τ21 : Type*} (seq : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (sort : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (sort_map : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (subset_p : pvs_τ26 → pvs_τ27 → Prop)

-- pvs2lean: ordered_finite_sequences.extract_one
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= j AND i < s`length AND j < s`length AND l <= j - i IMPLIES
--  (s ^ (i, j))`seq(l) = s`seq(i + l)
theorem extract_one : i ≤ j ∧ i < s.List.length ∧ j < s.List.length ∧ l ≤ j - i → (s ^ (i, j)).seq l = s.seq (i + l) := by
  -- === TCCs for extract_one ===
  -- TCC: extract_one_TCC1
  -- Statement: FORALL (i, j, l: nat, s: ne_seqs[T]):
  --   l <= j - i AND j < s`length AND i < s`length AND i <= j IMPLIES
  --    l < (^[T](s, (i, j)))`length
  have extract_one_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: extract_one_TCC2
  -- Statement: FORALL (i, j, l: nat, s: ne_seqs[T]):
  --   l <= j - i AND j < s`length AND i < s`length AND i <= j IMPLIES
  --    i + l < s`length
  have extract_one_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.extract_upper
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= j AND i < s`length AND j < s`length IMPLIES
--  (s ^ (i, j))`seq(length(s ^ (i, j)) - 1) = s`seq(j)
theorem extract_upper : i ≤ j ∧ i < s.List.length ∧ j < s.List.length → (s ^ (i, j)).seq (List.length (s ^ (i, j)) - 1) = s.seq j := by
  -- === TCCs for extract_upper ===
  -- TCC: extract_upper_TCC1
  -- Statement: FORALL (i, j: nat, s: ne_seqs[T]):
  --   j < s`length AND i < s`length AND i <= j IMPLIES
  --    length(^[T](s, (i, j))) - 1 >= 0 AND
  --     length(^[T](s, (i, j))) - 1 < (^[T](s, (i, j)))`length
  have extract_upper_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.min_extract
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= j AND i < s`length IMPLIES min(sort(s) ^ (i, j)) = sort(s)`seq(i)
theorem min_extract : i ≤ j ∧ i < s.List.length → min (sort s ^ (i, j)) = sort s.seq i := by
  -- === TCCs for min_extract ===
  -- TCC: min_extract_TCC1
  -- Statement: FORALL (i, j: nat, s: ne_seqs[T]):
  --   i < s`length AND i <= j IMPLIES length(^[T](sort[T, <=](s), (i, j))) > 0
  have min_extract_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_extract_TCC2
  -- Statement: FORALL (i, j: nat, s: ne_seqs[T]):
  --   i < s`length AND i <= j IMPLIES i < sort[T, <=](s)`length
  have min_extract_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.max_extract
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= j AND i < s`length AND j < s`length IMPLIES
--  max(sort(s) ^ (i, j)) = sort(s)`seq(j)
theorem max_extract : i ≤ j ∧ i < s.List.length ∧ j < s.List.length → max (sort s ^ (i, j)) = sort s.seq j := by
  -- === TCCs for max_extract ===
  -- TCC: max_extract_TCC1
  -- Statement: FORALL (i, j: nat, s: ne_seqs[T]):
  --   j < s`length AND i < s`length AND i <= j IMPLIES j < sort[T, <=](s)`length
  have max_extract_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.minmax
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: list2finseq[T]((: min(s), max(s) :))
axiom minmax {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20

-- pvs2lean: ordered_finite_sequences.min_le_max
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min(s) <= max(s)
theorem min_le_max : min s ≤ max s := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.min_minmax
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min(minmax(s)) = min(s)
theorem min_minmax : min (minmax s) = min s := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.max_minmax
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max(minmax(s)) = max(s)
theorem max_minmax : max (minmax s) = max s := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.lower
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {j: below(k) | s`length = k AND s`seq(j) <= sort(s)`seq(i)}
axiom lower {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12

-- pvs2lean: ordered_finite_sequences.upper
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {j: below(k) | s`length = k AND sort(s)`seq(i) <= s`seq(j)}
axiom upper {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ28 → pvs_τ29 → pvs_τ30 → pvs_τ31

-- pvs2lean: ordered_finite_sequences.map_set
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {i: below(k) | s`length = k AND a(sort_map(s)(i))}
axiom map_set {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15 → pvs_τ16

-- pvs2lean: ordered_finite_sequences.map_subset_lower
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < k AND s`length = k IMPLIES
--  subset?(map_set(s, k, {m: below(k) | m <= i}), lower(s, k, i))
theorem map_subset_lower : i < k ∧ s.List.length = k → subset_p (map_set s k ({m: Finset.range k | m ≤ i})) (lower s k i) := by
  -- === TCCs for map_subset_lower ===
  -- TCC: map_subset_lower_TCC1
  -- Statement: FORALL (i: nat, k: posnat, s: ne_seqs[T]):
  --   s`length = k AND i < k IMPLIES is_finite[below(k)]({m: below(k) | m <= i})
  have map_subset_lower_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.map_subset_upper
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < k AND s`length = k IMPLIES
--  subset?(map_set(s, k, {m: below(k) | i <= m}), upper(s, k, i))
theorem map_subset_upper : i < k ∧ s.List.length = k → subset_p (map_set s k ({m: Finset.range k | i ≤ m})) (upper s k i) := by
  -- === TCCs for map_subset_upper ===
  -- TCC: map_subset_upper_TCC1
  -- Statement: FORALL (i: nat, k: posnat, s: ne_seqs[T]):
  --   s`length = k AND i < k IMPLIES is_finite[below(k)]({m: below(k) | i <= m})
  have map_subset_upper_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.mapper
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sort_map(s)(i)
noncomputable def mapper : sorry := by
  sorry  -- pvs2lean: definition stub for sort_map(s)(i)

-- pvs2lean: ordered_finite_sequences.map_card_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: finite_set[below(k)]):
--   s`length = k IMPLIES card(map_set(s, k, a)) = card(a)
theorem map_card_eq : ∀ (a : {s : {i : ℝ // i ≥ 0} // s < k} → Bool), s.List.length = k → card (map_set s k a) = card a := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.card_below_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < k IMPLIES i + 1 <= card({m: below(k) | m <= i})
theorem card_below_set : i < k → i + 1 ≤ card ({m: Finset.range k | m ≤ i}) := by
  -- === TCCs for card_below_set ===
  -- TCC: card_below_set_TCC1
  -- Statement: FORALL (i: nat, k: posnat):
  --   i < k IMPLIES is_finite[below(k)]({m: below(k) | m <= i})
  have card_below_set_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.card_above_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < k IMPLIES k - i <= card({m: below(k) | i <= m})
theorem card_above_set : i < k → k - i ≤ card ({m: Finset.range k | i ≤ m}) := by
  -- === TCCs for card_above_set ===
  -- TCC: card_above_set_TCC1
  -- Statement: FORALL (i: nat, k: posnat):
  --   i < k IMPLIES is_finite[below(k)]({m: below(k) | i <= m})
  have card_above_set_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.card_lower
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < k AND s`length = k IMPLIES i + 1 <= card[below(k)](lower(s, k, i))
theorem card_lower : i < k ∧ s.List.length = k → i + 1 ≤ card (lower s k i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.card_upper
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < k AND s`length = k IMPLIES k - i <= card[below(k)](upper(s, k, i))
theorem card_upper : i < k ∧ s.List.length = k → k - i ≤ card (upper s k i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.sort_overlap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s1`length = k AND s2`length = k IMPLIES
--  FORALL (i: below(k)):
--    EXISTS (j: below(k)):
--      s2`seq(j) <= sort(s2)`seq(i) AND sort(s1)`seq(i) <= s1`seq(j)
theorem sort_overlap : s1.List.length = k ∧ s2.List.length = k → ∀ (i : Finset.range k), ∃ (j : Finset.range k), s2.seq j ≤ sort s2.seq i ∧ sort s1.seq i ≤ s1.seq j := by
  -- === TCCs for sort_overlap ===
  -- TCC: sort_overlap_TCC1
  -- Statement: FORALL (k: posnat, s1, s2: ne_seqs[T]):
  --   s2`length = k AND s1`length = k IMPLIES
  --    FORALL (i: below(k), j: below(k)): j < s2`length
  have sort_overlap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sort_overlap_TCC2
  -- Statement: FORALL (k: posnat, s1, s2: ne_seqs[T]):
  --   s2`length = k AND s1`length = k IMPLIES
  --    FORALL (i: below(k), j: below(k)): i < sort[T, <=](s2)`length
  have sort_overlap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ordered_finite_sequences.leq_validity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s`length = k IMPLIES
--  FORALL (i: below(k)): EXISTS (j: below(k)): s`seq(j) <= sort(s)`seq(i)
theorem leq_validity : s.List.length = k → ∀ (i : Finset.range k), ∃ (j : Finset.range k), s.seq j ≤ sort s.seq i := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.ordered_finite_sequences