-- Auto-generated by pvs2lean/translator.py
-- PVS theory: reduce_properties
-- PVSLib library: fault_tolerance
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: reduce_properties

Auto-generated stubs from PVSLib theory `reduce_properties`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.reduce_properties

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (card : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (cf : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (eligible : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (enabled : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (enabled_symmetric_p : pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ16 : Type*} (length : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (lower : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ25 : Type*} (max : pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (min : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} (nodes : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (quorum_p : pvs_τ29 → pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ40 : Type*} (seq : pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} (sort : pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} (tau : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} (upper : pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46)
variable {pvs_τ47 : Type*} (v1 : pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} (v2 : pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (vec2seq : pvs_τ50 → pvs_τ51 → Prop)

-- pvs2lean: reduce_properties.lowset
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {j: below(N) | enabled(j) AND v(j) <= sort(vec2seq(v, enabled))`seq(i)}
axiom lowset {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24

-- pvs2lean: reduce_properties.card_lower_leq_lowset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(card(enabled))):
--   card[below(card(enabled))](lower(vec2seq(v, enabled), card(enabled), i))
--    <= card[below(N)](lowset(v, enabled, i))
theorem card_lower_leq_lowset : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < card enabled}), card (lower (vec2seq v enabled) (card enabled) i) ≤ card (lowset v enabled i) := by
  -- === TCCs for card_lower_leq_lowset ===
  -- TCC: card_lower_leq_lowset_TCC1
  -- Statement: FORALL (enabled: non_empty_finite_set[below(N)],
  --         i: below(card[below(N)](enabled))):
  --   card[below(N)](enabled) > 0
  have card_lower_leq_lowset_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.card_lowset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(card(enabled))): i + 1 <= card(lowset(v, enabled, i))
theorem card_lowset : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < card enabled}), i + 1 ≤ card (lowset v enabled i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.highset
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {j: below(N) | enabled(j) AND sort(vec2seq(v, enabled))`seq(i) <= v(j)}
axiom highset {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ12 → pvs_τ13 → pvs_τ14 → pvs_τ15

-- pvs2lean: reduce_properties.card_upper_leq_highset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(card(enabled))):
--   card[below(card(enabled))](upper(vec2seq(v, enabled), card(enabled), i))
--    <= card[below(N)](highset(v, enabled, i))
theorem card_upper_leq_highset : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < card enabled}), card (upper (vec2seq v enabled) (card enabled) i) ≤ card (highset v enabled i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.card_highset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(card(enabled))):
--   card(enabled) - i <= card(highset(v, enabled, i))
theorem card_highset : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < card enabled}), card enabled - i ≤ card (highset v enabled i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.reduce
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET s = vec2seq(v, enabled) IN
--   LET t = tau(s`length) IN sort(s) ^ (t, s`length - (t + 1))
axiom reduce {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop

-- pvs2lean: reduce_properties.min_reduce
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min(reduce(tau)(v, enabled)) =
--  sort(vec2seq(v, enabled))`seq(tau(card(enabled)))
theorem min_reduce : min (reduce tau(v, enabled)) = sort (vec2seq v enabled).seq (tau (card enabled)) := by
  -- === TCCs for min_reduce ===
  -- TCC: min_reduce_TCC1
  -- Statement: FORALL (enabled: non_empty_finite_set[below(N)]):
  --   card[below(N)](enabled) > 0
  have min_reduce_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_reduce_TCC2
  -- Statement: FORALL (enabled: non_empty_finite_set[below(N)], tau: tau_type,
  --         v: vec[N, T]):
  --   tau(card[below(N)](enabled)) <
  --    sort[T, <=](vec2seq[N, T](v, enabled))`length
  have min_reduce_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.max_reduce
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max(reduce(tau)(v, enabled)) =
--  sort(vec2seq(v, enabled))`seq(card(enabled) - 1 - tau(card(enabled)))
theorem max_reduce : max (reduce tau(v, enabled)) = sort (vec2seq v enabled).seq (card enabled - 1 - tau (card enabled)) := by
  -- === TCCs for max_reduce ===
  -- TCC: max_reduce_TCC1
  -- Statement: FORALL (enabled: non_empty_finite_set[below(N)], tau: tau_type,
  --         v: vec[N, T]):
  --   card[below(N)](enabled) - 1 - tau(card[below(N)](enabled)) >= 0 AND
  --    card[below(N)](enabled) - 1 - tau(car...
  have max_reduce_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.reduce_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: reduce(tau)(v, enabled)`length = M(enabled, tau)
theorem reduce_length : reduce tau(v, enabled).List.length = M enabled tau := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.reduce_rewrite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < M(enabled, tau) IMPLIES
--  reduce(tau)(v, enabled)`seq(i) =
--   sort(vec2seq(v, enabled))`seq(tau(card(enabled)) + i)
theorem reduce_rewrite : i < M enabled tau → reduce tau(v, enabled).seq i =
 sort (vec2seq v enabled).seq (tau (card enabled) + i) := by
  -- === TCCs for reduce_rewrite ===
  -- TCC: reduce_rewrite_TCC1
  -- Statement: FORALL (enabled: non_empty_finite_set[below(N)], i: nat, tau: tau_type,
  --         v: vec[N, T]):
  --   i < M(enabled, tau) IMPLIES i < reduce(tau)(v, enabled)`length
  have reduce_rewrite_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: reduce_rewrite_TCC2
  -- Statement: FORALL (enabled: non_empty_finite_set[below(N)], i: nat, tau: tau_type,
  --         v: vec[N, T]):
  --   i < M(enabled, tau) IMPLIES
  --    tau(card[below(N)](enabled)) + i <
  --     sort[T, <=](vec2seq[N, T](v, e...
  have reduce_rewrite_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.reduce_overlap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < M(enabled, tau) IMPLIES
--  EXISTS n:
--    enabled(n) AND
--     v1(n) <= reduce(tau)(v1, enabled)`seq(i) AND
--      reduce(tau)(v2, enabled)`seq(i) <= v2(n)
theorem reduce_overlap : i < M enabled tau → ∃ n, 
  enabled n ∧
   v1 n ≤ reduce tau(v1, enabled).seq i ∧
    reduce tau(v2, enabled).seq i ≤ v2 n := by
  -- === TCCs for reduce_overlap ===
  -- TCC: reduce_overlap_TCC1
  -- Statement: FORALL (enabled: non_empty_finite_set[below(N)], i: nat, tau: tau_type,
  --         v1, v2: vec[N, T]):
  --   i < M(enabled, tau) IMPLIES
  --    FORALL (n: below(N)):
  --      enabled(n) AND v1(n) <= reduce(tau)(v...
  have reduce_overlap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: reduce_properties.reduce_agreement
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: enabled_symmetric?(enabled, v1, v2) IMPLIES
--  reduce(tau)(v1, enabled) = reduce(tau)(v2, enabled)
theorem reduce_agreement : enabled_symmetric_p enabled v1 v2 → reduce tau(v1, enabled) = reduce tau(v2, enabled) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: reduce_properties.min_validity
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: quorum?(enabled, tau)(nodes) IMPLIES
--  EXISTS n: nodes(n) AND enabled(n) AND v(n) <= min(reduce(tau)(v, enabled))
theorem min_validity : quorum_p enabled tau(nodes) → ∃ n,  nodes n ∧ enabled n ∧ v n ≤ min (reduce tau(v, enabled)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: reduce_properties.max_validity
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: quorum?(enabled, tau)(nodes) IMPLIES
--  EXISTS n: nodes(n) AND enabled(n) AND max(reduce(tau)(v, enabled)) <= v(n)
theorem max_validity : quorum_p enabled tau(nodes) → ∃ n,  nodes n ∧ enabled n ∧ max (reduce tau(v, enabled)) ≤ v n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: reduce_properties.reduce_choice
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?[below(N)](eligible) THEN default
-- ELSE cf(reduce(tau)(v, eligible))
-- ENDIF
axiom reduce_choice {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} : pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39 → Prop

-- pvs2lean: reduce_properties.reduce_choice2
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?[below(N)](eligible) THEN default
-- ELSE cf(reduce(tau)(v, eligible))
-- ENDIF
noncomputable def reduce_choice2 : sorry := by
  sorry  -- pvs2lean: definition stub for IF empty?[below(N)](eligible) THEN default ELSE cf(reduce(tau)(v, eligible)) END

-- pvs2lean: reduce_properties.choice_lower_validity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: quorum?(eligible, tau)(nodes) IMPLIES
--  EXISTS n:
--    nodes(n) AND
--     eligible(n) AND v(n) <= reduce_choice(cf, eligible, v, tau, default)
theorem choice_lower_validity : quorum_p eligible tau(nodes) → ∃ n, 
  nodes n ∧
   eligible n ∧ v n ≤ reduce_choice cf eligible v tau default := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: reduce_properties.choice_upper_validity
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: quorum?(eligible, tau)(nodes) IMPLIES
--  EXISTS n:
--    nodes(n) AND
--     eligible(n) AND reduce_choice(cf, eligible, v, tau, default) <= v(n)
theorem choice_upper_validity : quorum_p eligible tau(nodes) → ∃ n, 
  nodes n ∧
   eligible n ∧ reduce_choice cf eligible v tau default ≤ v n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: reduce_properties.choice_agreement_generation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: enabled_symmetric?(eligible, v1, v2) IMPLIES
--  reduce_choice(cf, eligible, v1, tau, default) =
--   reduce_choice(cf, eligible, v2, tau, default)
theorem choice_agreement_generation : enabled_symmetric_p eligible v1 v2 → reduce_choice cf eligible v1 tau default =
 reduce_choice cf eligible v2 tau default := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.reduce_properties