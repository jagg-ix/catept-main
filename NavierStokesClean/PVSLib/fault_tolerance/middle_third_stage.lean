-- Auto-generated by pvs2lean/translator.py
-- PVS theory: middle_third_stage
-- PVSLib library: fault_tolerance
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: middle_third_stage

Auto-generated stubs from PVSLib theory `middle_third_stage`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.middle_third_stage

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (byzantine_intersection_majority_p : pvs_τ6 → pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (cf1 : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (cf2 : pvs_τ10 → pvs_τ11)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (diameter_p : pvs_τ16 → pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (enabled_within_p : pvs_τ19 → pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (max : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (max_length : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} (mean : pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} (midpoint : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} (min : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (overlap_imprecision_p : pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} (quorum_imprecision_p : pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (reduce_choice : pvs_τ39 → pvs_τ40 → pvs_τ41 → pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (single_imprecision_p : pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} (v_max : pvs_τ48 → pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (v_min : pvs_τ50 → pvs_τ51 → pvs_τ52)

-- pvs2lean: middle_third_stage.byz_overlap_imprecision?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (src_set: finite_set[below(S)]):
--   byzantine_intersection_majority?(rcvd, check)(src_set) AND
--    single_imprecision?(rcvd, check, epsilon)(src_set)
axiom byz_overlap_imprecision_p {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5 → Prop

-- pvs2lean: middle_third_stage.byzantine_overlap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: byz_overlap_imprecision?(rcvd, check, epsilon) IMPLIES
--  overlap_imprecision?(rcvd, check, byz, epsilon)
theorem byzantine_overlap : byz_overlap_imprecision_p rcvd check epsilon → overlap_imprecision_p rcvd check byz epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.middle_third_convergence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      quorum_imprecision?(sent, rcvd, check, byz, epsilon)
--  AND byz_overlap_imprecision?(rcvd, check, epsilon)
--  AND convergent?(cf, max_length(rcvd, check, byz), X)
--  AND enabled_within?(rcvd, check)(sr
theorem middle_third_convergence : quorum_imprecision_p sent rcvd check byz epsilon
 ∧ byz_overlap_imprecision_p rcvd check epsilon
 ∧ convergent_p cf (max_length rcvd check byz) X
 ∧ enabled_within_p rcvd check(src_set)
 ∧ (v_max sent src_set - v_min sent src_set ≤ delta) → v_max (reduce_choice byz cf(rcvd, check)) dst_set -
 v_min (reduce_choice byz cf(rcvd, check)) dst_set
 ≤ delta * ((X - 1) / X) + epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.convergent?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL s1, s2, delta, eps:
--   s1`length <= i AND
--    s2`length <= i AND
--     max(s1) - min(s2) <= delta + eps AND min(s1) - max(s2) <= eps
--    IMPLIES cf1(s1) - cf2(s2) <= eps + (delta * (k - 1)) / k
axiom convergent_p {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ12 → pvs_τ13 → pvs_τ14 → pvs_τ15 → Prop

-- pvs2lean: middle_third_stage.reduce_convergence2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      quorum_imprecision?(sent, rcvd, check, tau, epsilon)
--  AND diameter?(sent, delta)(src_set)
--  AND enabled_within?(rcvd, check)(src_set)
--  AND overlap_imprecision?(rcvd, check, tau, epsilon)
--  AND conv
theorem reduce_convergence2 : quorum_imprecision_p sent rcvd check tau epsilon
 ∧ diameter_p sent delta(src_set)
 ∧ enabled_within_p rcvd check(src_set)
 ∧ overlap_imprecision_p rcvd check tau epsilon
 ∧ convergent_p cf1 cf2 (max_length rcvd check tau) k → reduce_choice tau cf1(rcvd, check) (d1) -
 reduce_choice tau cf2(rcvd, check) (d2)
 ≤ delta * ((k - 1) / k) + epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.convergence2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      quorum_imprecision?(sent, rcvd, check, tau, epsilon)
--  AND overlap_imprecision?(rcvd, check, tau, epsilon)
--  AND convergent?(cf1, cf2, max_length(rcvd, check, tau), k)
--  AND enabled_within?(rcvd, ch
theorem convergence2 : quorum_imprecision_p sent rcvd check tau epsilon
 ∧ overlap_imprecision_p rcvd check tau epsilon
 ∧ convergent_p cf1 cf2 (max_length rcvd check tau) k
 ∧ enabled_within_p rcvd check(src_set)
 ∧ (v_max sent src_set - v_min sent src_set ≤ delta) → v_max (reduce_choice tau cf1(rcvd, check)) dst_set -
 v_min (reduce_choice tau cf2(rcvd, check)) dst_set
 ≤ delta * ((k - 1) / k) + epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.middle_third_convergence_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      quorum_imprecision?(sent, rcvd, check, byz, epsilon)
--  AND byz_overlap_imprecision?(rcvd, check, epsilon)
--  AND convergent?(cf1, cf2, max_length(rcvd, check, byz), X)
--  AND enabled_within?(rcvd, che
theorem middle_third_convergence_2 : quorum_imprecision_p sent rcvd check byz epsilon
 ∧ byz_overlap_imprecision_p rcvd check epsilon
 ∧ convergent_p cf1 cf2 (max_length rcvd check byz) X
 ∧ enabled_within_p rcvd check(src_set)
 ∧ (v_max sent src_set - v_min sent src_set ≤ delta) → v_max (reduce_choice byz cf1(rcvd, check)) dst_set -
 v_min (reduce_choice byz cf2(rcvd, check)) dst_set
 ≤ delta * ((X - 1) / X) + epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.mean_convergence_midpoint
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (2 <= k) AND (s1`length <= k) AND (s2`length <= k)
--  AND (max(s1) - min(s2) <= delta + eps) AND (min(s1) - max(s2) <= eps)
--  IMPLIES mean(s1) - midpoint(s2) <= delta * (k - 1) / k + eps
theorem mean_convergence_midpoint : (2 ≤ k) ∧ (s1.List.length ≤ k) ∧ (s2.List.length ≤ k)
 ∧ (max s1 - min s2 ≤ delta + eps) ∧ (min s1 - max s2 ≤ eps) → mean s1 - midpoint s2 ≤ delta * (k - 1) / k + eps := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: middle_third_stage.mean_convergence_midpoint_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: s1`length <= k AND
--  s2`length <= k AND
--   max(s1) - min(s2) <= delta + eps AND min(s1) - max(s2) <= eps
--  IMPLIES mean(s1) - midpoint(s2) <= delta * (k - 1) / k + eps
theorem mean_convergence_midpoint_0 : s1.List.length ≤ k ∧
 s2.List.length ≤ k ∧
  max s1 - min s2 ≤ delta + eps ∧ min s1 - max s2 ≤ eps → mean s1 - midpoint s2 ≤ delta * (k - 1) / k + eps := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: middle_third_stage.midpoint_convergence_mean
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (2 <= k) AND (s1`length <= k) AND (s2`length <= k)
--  AND (max(s1) - min(s2) <= delta + eps) AND (min(s1) - max(s2) <= eps)
--  IMPLIES midpoint(s1) - mean(s2) <= delta * (k - 1) / k + eps
theorem midpoint_convergence_mean : (2 ≤ k) ∧ (s1.List.length ≤ k) ∧ (s2.List.length ≤ k)
 ∧ (max s1 - min s2 ≤ delta + eps) ∧ (min s1 - max s2 ≤ eps) → midpoint s1 - mean s2 ≤ delta * (k - 1) / k + eps := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: middle_third_stage.convergent_mean_midpoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: k >= 2 IMPLIES convergent?(mean, midpoint, k, k + i)
theorem convergent_mean_midpoint : k ≥ 2 → convergent_p mean midpoint k (k + i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.convergent_midpoint_mean
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: k >= 2 IMPLIES convergent?(midpoint, mean, k, k + i)
theorem convergent_midpoint_mean : k ≥ 2 → convergent_p midpoint mean k (k + i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.middle_third_convergence_mean_midpoint
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (X >= 2) AND (X = max_length(rcvd, check, byz))
--  AND quorum_imprecision?(sent, rcvd, check, byz, epsilon)
--  AND byz_overlap_imprecision?(rcvd, check, epsilon)
--  AND enabled_within?(rcvd, check)(src
theorem middle_third_convergence_mean_midpoint : (X ≥ 2) ∧ (X = max_length rcvd check byz)
 ∧ quorum_imprecision_p sent rcvd check byz epsilon
 ∧ byz_overlap_imprecision_p rcvd check epsilon
 ∧ enabled_within_p rcvd check(src_set)
 ∧ (v_max sent src_set - v_min sent src_set ≤ delta) → v_max (reduce_choice byz mean(rcvd, check)) dst_set -
 v_min (reduce_choice byz midpoint(rcvd, check)) dst_set
 ≤ delta * ((X - 1) / X) + epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.middle_third_convergence_midpoint_mean
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (X >= 2) AND (X = max_length(rcvd, check, byz))
--  AND quorum_imprecision?(sent, rcvd, check, byz, epsilon)
--  AND byz_overlap_imprecision?(rcvd, check, epsilon)
--  AND enabled_within?(rcvd, check)(src
theorem middle_third_convergence_midpoint_mean : (X ≥ 2) ∧ (X = max_length rcvd check byz)
 ∧ quorum_imprecision_p sent rcvd check byz epsilon
 ∧ byz_overlap_imprecision_p rcvd check epsilon
 ∧ enabled_within_p rcvd check(src_set)
 ∧ (v_max sent src_set - v_min sent src_set ≤ delta) → v_max (reduce_choice byz midpoint(rcvd, check)) dst_set -
 v_min (reduce_choice byz mean(rcvd, check)) dst_set
 ≤ delta * ((X - 1) / X) + epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.mean_convergence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (X = max_length(rcvd, check, byz))
--  AND quorum_imprecision?(sent, rcvd, check, byz, epsilon)
--  AND byz_overlap_imprecision?(rcvd, check, epsilon)
--  AND enabled_within?(rcvd, check)(src_set)
--  AND (v
theorem mean_convergence : (X = max_length rcvd check byz)
 ∧ quorum_imprecision_p sent rcvd check byz epsilon
 ∧ byz_overlap_imprecision_p rcvd check epsilon
 ∧ enabled_within_p rcvd check(src_set)
 ∧ (v_max sent src_set - v_min sent src_set ≤ delta) → v_max (reduce_choice byz mean(rcvd, check)) dst_set -
 v_min (reduce_choice byz mean(rcvd, check)) dst_set
 ≤ delta * ((X - 1) / X) + epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: middle_third_stage.midpoint_convergence
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: quorum_imprecision?(sent, rcvd, check, byz, epsilon) AND
--  byz_overlap_imprecision?(rcvd, check, epsilon) AND
--   enabled_within?(rcvd, check)(src_set) AND
--    v_max(sent, src_set) - v_min(sent, src_set) 
theorem midpoint_convergence : quorum_imprecision_p sent rcvd check byz epsilon ∧
 byz_overlap_imprecision_p rcvd check epsilon ∧
  enabled_within_p rcvd check(src_set) ∧
   v_max sent src_set - v_min sent src_set ≤ delta → v_max (reduce_choice byz midpoint(rcvd, check)) dst_set -
 v_min (reduce_choice byz midpoint(rcvd, check)) dst_set
 ≤ delta / 2 + epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.middle_third_stage