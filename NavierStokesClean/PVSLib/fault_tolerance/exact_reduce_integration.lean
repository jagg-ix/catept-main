-- Auto-generated by pvs2lean/translator.py
-- PVS theory: exact_reduce_integration
-- PVSLib library: fault_tolerance
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: exact_reduce_integration

Auto-generated stubs from PVSLib theory `exact_reduce_integration`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.exact_reduce_integration

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (conforms : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (exists_all_symmetric_p : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (protocol : pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (quorum_correct_p : pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (reduce_choice : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (v_max : pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (v_min : pvs_τ27 → pvs_τ28 → pvs_τ29 → Prop)

-- pvs2lean: exact_reduce_integration.lower_validity
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: check = conforms(msg_check, common_check) AND
--  protocol(sent, check, reduce_choice(tau, cf), status, j, j + k) AND
--   quorum_correct?(sent, check, status, tau, j, j + k)
--  IMPLIES v_min(sent, common_che
theorem lower_validity : check = conforms msg_check common_check ∧
 protocol sent check (reduce_choice tau cf) status j (j + k) ∧
  quorum_correct_p sent check status tau j (j + k) → v_min sent common_check(j) ≤ v_min sent common_check(j + k) := by
  -- === TCCs for lower_validity ===
  -- TCC: lower_validity_TCC1
  -- Statement: FORALL (cf: k_consensus_function[N, real, <=, error],
  --         check: check_function[N, real], eps_l: [nat -> nonneg_real],
  --         j, k: nat, node_set: [i: nat -> non_empty_finite_set[below(N(i))]]...
  have lower_validity_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: exact_reduce_integration.upper_validity
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: check = conforms(msg_check, common_check) AND
--  protocol(sent, check, reduce_choice(tau, cf), status, j, j + k) AND
--   quorum_correct?(sent, check, status, tau, j, j + k)
--  IMPLIES v_max(sent, common_che
theorem upper_validity : check = conforms msg_check common_check ∧
 protocol sent check (reduce_choice tau cf) status j (j + k) ∧
  quorum_correct_p sent check status tau j (j + k) → v_max sent common_check(j + k) ≤ v_max sent common_check(j) := by
  -- === TCCs for upper_validity ===
  -- TCC: upper_validity_TCC1
  -- Statement: FORALL (cf: k_consensus_function[N, real, <=, error],
  --         check: check_function[N, real], eps_u: [nat -> nonneg_real],
  --         j, k: nat, node_set: [i: nat -> non_empty_finite_set[below(N(i))]]...
  have upper_validity_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: exact_reduce_integration.agreement_generation
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: check = conforms(msg_check, common_check) AND
--  protocol(sent, check, reduce_choice(tau, cf), status, j, j + k) AND
--   exists_all_symmetric?(sent, check, status, tau, j, j + k)
--  IMPLIES v_max(sent, comm
theorem agreement_generation : check = conforms msg_check common_check ∧
 protocol sent check (reduce_choice tau cf) status j (j + k) ∧
  exists_all_symmetric_p sent check status tau j (j + k) → v_max sent common_check(j + k) = v_min sent common_check(j + k) := by
  -- === TCCs for agreement_generation ===
  -- TCC: agreement_generation_TCC1
  -- Statement: FORALL (cf: k_consensus_function[N, real, <=, error],
  --         check: check_function[N, real], epsilon: [nat -> nonneg_real],
  --         j, k: nat, node_set: [i: nat -> non_empty_finite_set[below(N(i))...
  have agreement_generation_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.exact_reduce_integration