-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nominal_AC_ac_step
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nominal_AC_ac_step

Auto-generated stubs from PVSLib theory `nominal_AC_ac_step`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nominal_AC_ac_step

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (DS : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (abs_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (abstr : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (ac_app_p : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (ac_prb_p : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (ac_sym : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (app_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (append : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (apply_sub : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (arg : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (at : pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} (atom_p : pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (body : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (car : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} (cdr : pvs_τ32 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (cons : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (contained_p : pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (decompose : pvs_τ40 → pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} (difference : pvs_τ42 → pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (disjoint_p : pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (dom : pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (equal_p : pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} (ext : pvs_τ53 → pvs_τ54 → pvs_τ55)
variable {pvs_τ56 : Type*} (f_sym : pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} (fc : pvs_τ57 → pvs_τ58 → pvs_τ59)
variable {pvs_τ60 : Type*} (first_proj : pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (fresh_p : pvs_τ61 → pvs_τ62 → Prop)
variable {pvs_τ63 : Type*} (idempotent_p : pvs_τ63 → Prop)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} (inst_step : pvs_τ71 → pvs_τ72 → pvs_τ73 → Prop)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} (is_ac_sym_p : pvs_τ74 → pvs_τ75 → Prop)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} {pvs_τ81 : Type*} (is_sol_p : pvs_τ76 → pvs_τ77 → pvs_τ78 → pvs_τ79 → pvs_τ80 → pvs_τ81 → Prop)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} (match_problem_p : pvs_τ82 → pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} {pvs_τ86 : Type*} (max : pvs_τ84 → pvs_τ85 → pvs_τ86)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (member : pvs_τ88 → pvs_τ89 → Prop)
variable {pvs_τ95 : Type*} (no_pair_p : pvs_τ95 → Prop)
variable {pvs_τ96 : Type*} {pvs_τ97 : Type*} (nominal_AC_fresh_subs_p : pvs_τ96 → pvs_τ97 → Prop)
variable {pvs_τ98 : Type*} (null_p : pvs_τ98 → Prop)
variable {pvs_τ102 : Type*} {pvs_τ103 : Type*} (reverse : pvs_τ102 → pvs_τ103)
variable {pvs_τ104 : Type*} {pvs_τ105 : Type*} (rhs : pvs_τ104 → pvs_τ105)
variable {pvs_τ106 : Type*} {pvs_τ107 : Type*} (same_func_p : pvs_τ106 → pvs_τ107 → Prop)
variable {pvs_τ108 : Type*} {pvs_τ109 : Type*} (sec_proj : pvs_τ108 → pvs_τ109)
variable {pvs_τ110 : Type*} {pvs_τ111 : Type*} (size : pvs_τ110 → pvs_τ111 → Prop)
variable {pvs_τ112 : Type*} {pvs_τ113 : Type*} {pvs_τ114 : Type*} {pvs_τ115 : Type*} {pvs_τ116 : Type*} {pvs_τ117 : Type*} {pvs_τ118 : Type*} (solve_ac : pvs_τ112 → pvs_τ113 → pvs_τ114 → pvs_τ115 → pvs_τ116 → pvs_τ117 → pvs_τ118)
variable {pvs_τ119 : Type*} {pvs_τ120 : Type*} (subset_p : pvs_τ119 → pvs_τ120 → Prop)
variable {pvs_τ121 : Type*} {pvs_τ122 : Type*} (subterms : pvs_τ121 → pvs_τ122)
variable {pvs_τ123 : Type*} {pvs_τ124 : Type*} {pvs_τ125 : Type*} (unifies_p : pvs_τ123 → pvs_τ124 → pvs_τ125 → Prop)
variable {pvs_τ126 : Type*} {pvs_τ127 : Type*} {pvs_τ128 : Type*} (union : pvs_τ126 → pvs_τ127 → pvs_τ128)
variable {pvs_τ129 : Type*} (unit_p : pvs_τ129 → Prop)
variable {pvs_τ130 : Type*} (var_p : pvs_τ130 → Prop)
variable {pvs_τ131 : Type*} {pvs_τ132 : Type*} (vars : pvs_τ131 → pvs_τ132 → Prop)
variable {pvs_τ133 : Type*} {pvs_τ134 : Type*} (well_formed_p : pvs_τ133 → pvs_τ134 → Prop)

-- pvs2lean: nominal_AC_ac_step.nice_input?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      idempotent?(sigma) AND disjoint?(vars(P), dom(sigma))
--  AND contained?(sigma, V) AND subset?(vars(P), V) AND subset?(vars(G), V)
--  AND subset?(PV, V) AND well_formed?(P) AND well_formed?(sigma)
--  AN
axiom nice_input_p {pvs_τ90 : Type*} {pvs_τ91 : Type*} {pvs_τ92 : Type*} {pvs_τ93 : Type*} {pvs_τ94 : Type*} : pvs_τ90 → pvs_τ91 → pvs_τ92 → pvs_τ93 → pvs_τ94 → Prop

-- pvs2lean: nominal_AC_ac_step.max_size
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(P) THEN 0 ELSE max(size(car(P)), max_size(cdr(P))) ENDIF
axiom max_size {pvs_τ87 : Type*} : pvs_τ87 → Prop

-- pvs2lean: nominal_AC_ac_step.num_size
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(P) THEN 0
-- ELSE IF size(car(P)) = n THEN 1 + num_size(cdr(P), n)
--      ELSE num_size(cdr(P), n)
--      ENDIF
-- ENDIF
axiom num_size {pvs_τ100 : Type*} {pvs_τ101 : Type*} : pvs_τ100 → pvs_τ101 → Prop

-- pvs2lean: nominal_AC_ac_step.num_max_size
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_size(P, max_size(P))
axiom num_max_size {pvs_τ99 : Type*} : pvs_τ99 → Prop

-- pvs2lean: nominal_AC_ac_step.choose_eq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(P) THEN ((unit, unit), null)
-- ELSE IF size(car(P)) = max_size(P) THEN (car(P), cdr(P))
--      ELSE LET (up, P1) = choose_eq(cdr(P)) IN (up, cons(car(P), P1))
--      ENDIF
-- ENDIF
axiom choose_eq {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ33 → pvs_τ34

-- pvs2lean: nominal_AC_ac_step.input_apply_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(ac_results_subs) THEN null
-- ELSE LET (G1, P1, sigma1) = car(ac_results_subs),
--          new_P = append(P1, apply_sub(sigma1, P)),
--          new_sigma = append(sigma1, sigma),
--          input = (G
axiom input_apply_ac {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} : pvs_τ64 → pvs_τ65 → pvs_τ66 → pvs_τ67 → pvs_τ68 → pvs_τ69 → pvs_τ70

-- pvs2lean: nominal_AC_ac_step.nice_input_subterms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice_input?(G, P, sigma, V, PV) AND
--  subset?(subterms(P1), subterms(P)) AND
--   subset?(vars(rhs(P1)), vars(rhs(P))) AND no_pair?(P1)
--  IMPLIES nice_input?(G, P1, sigma, V, PV)
theorem nice_input_subterms : nice_input_p G P sigma V PV ∧
 subset_p (subterms P1) (subterms P) ∧
  subset_p (vars (rhs P1)) (vars (rhs P)) ∧ no_pair_p P1 → nice_input_p G P1 sigma V PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice_input?(G, P, sigma, V, PV) AND NOT null?(P) IMPLIES
--  nice_input?(G, cdr(P), sigma, V, PV)
theorem nice_input_cdr : nice_input_p G P sigma V PV ∧ ¬ null_p P → nice_input_p G (cdr P) sigma V PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice_input?(G, cons(up, P), sigma, V, PV) IFF
--  (nice_input?(G, cons(up, null), sigma, V, PV) AND
--    nice_input?(G, P, sigma, V, PV))
theorem nice_input_cons : nice_input_p G (cons up P) sigma V PV ↔ (nice_input_p G (cons up null) sigma V PV ∧
  nice_input_p G P sigma V PV) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_apply_sub
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sigma1 = cons((X, ext(reverse(pi))(s)), null),
--     G1 = nominal_AC_fresh_subs?(sigma1, G)`1
--   IN
--        nice_input?(G, P, sigma, V, PV) AND member(pi * X, P)
--    AND member(s, rhs(P)) AND (NOT memb
theorem nice_input_apply_sub : fun (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) => nice_input_p G P sigma V PV ∧ member (pi * X) P
 ∧ member s (rhs P) ∧ (¬ member X (vars s)) ∧ subset_p P1 P
 ∧ ¬ null_p P → nice_input_p (append G1 G) (apply_sub sigma1 P1) (append sigma1 sigma) V PV (nominal_AC_fresh_subs_p sigma1 G.1) (cons ((X, ext (reverse pi) (s))) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_decompose
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      nice_input?(G, P, sigma, V, PV) AND member(t, P) AND member(s, rhs(P))
--  AND (NOT null?(P)) AND app?(t) AND app?(s) AND same_func?(t, s)
--  AND subset?(P1, P)
--  IMPLIES
--  nice_input?(G, append(decompo
theorem nice_input_decompose : nice_input_p G P sigma V PV ∧ member t P ∧ member s (rhs P)
 ∧ (¬ null_p P) ∧ app_p t ∧ app_p s ∧ same_func_p t s
 ∧ subset_p P1 P → nice_input_p G (append (decompose (arg t) (arg s).1) P1) sigma V PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_dec_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      nice_input?(G, P, sigma, V, PV) AND member(t, P) AND member(s, rhs(P))
--  AND (NOT null?(P)) AND abs?(t) AND abs?(s) AND subset?(P1, P)
--  IMPLIES
--  nice_input?(G, append(decompose(body(t), ext(pi)(bo
theorem nice_input_dec_abs : nice_input_p G P sigma V PV ∧ member t P ∧ member s (rhs P)
 ∧ (¬ null_p P) ∧ abs_p t ∧ abs_p s ∧ subset_p P1 P → nice_input_p G (append (decompose (body t) (ext pi(body s)).1) P1) sigma V PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_dec_abs2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      nice_input?(G, P, sigma, V, PV) AND member(t, P) AND member(s, rhs(P))
--  AND (NOT null?(P)) AND abs?(t) AND abs?(s) AND subset?(P1, P)
--  IMPLIES
--  (LET G1 = fresh?(abstr(t), body(s))`1 IN
--     nice_i
theorem nice_input_dec_abs2 : nice_input_p G P sigma V PV ∧ member t P ∧ member s (rhs P)
 ∧ (¬ null_p P) ∧ abs_p t ∧ abs_p s ∧ subset_p P1 P → ((let G1 := fresh_p (abstr t) (body s).1; nice_input_p (append G1 G) (append (decompose (body t) (ext pi(body s)).1) P1) sigma V PV)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_initial_problem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(t, s), V) AND well_formed?(t, s) IMPLIES
--  nice_input?(null, cons((t, s), null), null, V, vars(s))
theorem nice_input_initial_problem : subset_p (vars t s) V ∧ well_formed_p t s → nice_input_p null (cons ((t, s)) null) null V (vars s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_initial_problem2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(t, s), V) AND well_formed?(t, s) IMPLIES
--  nice_input?(null, cons((t, s), null), null, V, vars(t, s))
theorem nice_input_initial_problem2 : subset_p (vars t s) V ∧ well_formed_p t s → nice_input_p null (cons ((t, s)) null) null V (vars t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.nice_input_fc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice_input?(G, P, sigma, V, PV) AND subset?(P1, P) AND member(X, vars(P))
--  IMPLIES nice_input?(append(fc(DS(pi, pi1), X), G), P1, sigma, V, PV)
theorem nice_input_fc : nice_input_p G P sigma V PV ∧ subset_p P1 P ∧ member X (vars P) → nice_input_p (append (fc (DS pi pi1) X) G) P1 sigma V PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.input_apply_ac_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(input1_, input_apply_ac(ac_results_subs, G, P, sigma, V, PV)) IFF
--  EXISTS G1, P1, sigma1:
--         member((G1, P1, sigma1), ac_results_subs) AND (input1_`1 = G1)
--     AND (input1_`2 = append(P1, 
theorem input_apply_ac_mem : member input1_ (input_apply_ac ac_results_subs G P sigma V PV) ↔ ∃ G1 P1 sigma1, 
       member ((G1, P1, sigma1)) ac_results_subs ∧ (input1_.1 = G1)
   ∧ (input1_.2 = append P1 (apply_sub sigma1 P))
   ∧ (input1_.3 = append sigma1 sigma) ∧ (input1_.4 = V)
   ∧ (input1_.5 = PV) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.max_size_unif_pair
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(P) IMPLIES EXISTS up: member(up, P) AND size(up) = max_size(P)
theorem max_size_unif_pair : ¬ null_p P → ∃ up,  member up P ∧ size up = max_size P := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.max_size_append
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max_size(append(P, P1)) = max(max_size(P), max_size(P1))
theorem max_size_append : max_size (append P P1) = max (max_size P) (max_size P1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.max_size_correct
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(up, P) IMPLIES size(up) <= max_size(P)
theorem max_size_correct : member up P → size up ≤ max_size P := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.max_size_decompose
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max_size(decompose(t, s)`1) <= size(t, s)
theorem max_size_decompose : max_size (decompose t s.1) ≤ size t s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.max_size_decompose_ext
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max_size(decompose(t, s)`1) = max_size(decompose(t, ext(pi)(s))`1)
theorem max_size_decompose_ext : max_size (decompose t s.1) = max_size (decompose t (ext pi(s)).1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.num_size_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_size(append(P, P1), n) = num_size(P, n) + num_size(P1, n)
theorem num_size_append : num_size (append P P1) n = num_size P n + num_size P1 n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.num_size0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: n > max_size(P) IMPLIES num_size(P, n) = 0
theorem num_size0 : n > max_size P → num_size P n = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, choose_eq(P)`2) IMPLIES member(t, P)
theorem choose_eq_mem : member t (choose_eq P.2) → member t P := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_mem1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(P) IMPLIES member(choose_eq(P)`1, P)
theorem choose_eq_mem1 : ¬ null_p P → member (choose_eq P.1) P := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_subterms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(P) IMPLIES subset?(subterms(choose_eq(P)`2), subterms(P))
theorem choose_eq_subterms : ¬ null_p P → subset_p (subterms (choose_eq P.2)) (subterms P) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq2_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(choose_eq(P)`2, P)
theorem choose_eq2_subset : subset_p (choose_eq P.2) P := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_vars_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(rhs(choose_eq(P)`2)), vars(rhs(P)))
theorem choose_eq_vars_rhs : subset_p (vars (rhs (choose_eq P.2))) (vars (rhs P)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_vars_rhs2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: match_problem?(P, PV) IMPLIES subset?(vars(choose_eq(P)`1`2), PV)
theorem choose_eq_vars_rhs2 : match_problem_p P PV → subset_p (vars (choose_eq P.1.2)) PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(choose_eq(P)`2), vars(P)) AND
--  subset?(vars(choose_eq(P)`1), vars(P))
theorem choose_eq_vars : subset_p (vars (choose_eq P.2)) (vars P) ∧ subset_p (vars (choose_eq P.1)) (vars P) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_no_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(P) IMPLIES no_pair?(choose_eq(P)`2)
theorem choose_eq_no_pair : well_formed_p P → no_pair_p (choose_eq P.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_well_formed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(P) IMPLIES
--  (well_formed?(choose_eq(P)`1) AND well_formed?(choose_eq(P)`2))
theorem choose_eq_well_formed : well_formed_p P → (well_formed_p (choose_eq P.1) ∧ well_formed_p (choose_eq P.2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_max_size_aux
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P1 = choose_eq(P)`2 IN
--   NOT null?(P) IMPLIES
--    max_size(P) = size(choose_eq(P)`1) AND
--     (max_size(P) > max_size(P1) OR
--       (max_size(P) = max_size(P1) AND
--         num_size(P, max_size(P)) = 
theorem choose_eq_max_size_aux : fun (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => ¬ null_p P → max_size P = size (choose_eq P.1) ∧
 (max_size P > max_size P1 ∨
   (max_size P = max_size P1 ∧
     num_size P (max_size P) = num_size P1 (max_size P1) + 1)) (choose_eq P.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_max_size
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P1 = choose_eq(P)`2 IN
--   NOT null?(P) IMPLIES
--    max_size(P) = size(choose_eq(P)`1) AND
--     (max_size(P) > max_size(P1) OR
--       (max_size(P) = max_size(P1) AND
--         num_max_size(P) = num_max_s
theorem choose_eq_max_size : fun (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => ¬ null_p P → max_size P = size (choose_eq P.1) ∧
 (max_size P > max_size P1 ∨
   (max_size P = max_size P1 ∧ num_max_size P = num_max_size P1 + 1)) (choose_eq P.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_unifies
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(P) IMPLIES
--  unifies?(Delta, delta, P) =
--   (unifies?(Delta, delta, choose_eq(P)`1) AND
--     unifies?(Delta, delta, choose_eq(P)`2))
theorem choose_eq_unifies : ¬ null_p P → unifies_p Delta delta P =
 (unifies_p Delta delta (choose_eq P.1) ∧
   unifies_p Delta delta (choose_eq P.2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_nice_input
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice_input?(G, P, sigma, V, PV) AND NOT null?(P) IMPLIES
--  nice_input?(G, choose_eq(P)`2, sigma, V, PV)
theorem choose_eq_nice_input : nice_input_p G P sigma V PV ∧ ¬ null_p P → nice_input_p G (choose_eq P.2) sigma V PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_nice_input2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input, (up, P1) = choose_eq(P), (t, s) = up IN
--   NOT null?(P) IMPLIES nice_input?(G, cons((t, s), P1), sigma, V, PV)
theorem choose_eq_nice_input2 : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => ¬ null_p P → nice_input_p G (cons ((t, s)) P1) sigma V PV up (choose_eq P) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_not_unifies_atom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P) IN
--        unifies?(G, delta, up) AND match_problem?(P, PV) AND atom?(up`1)
--    AND disjoint?(dom(delta), PV) AND NOT null?(P)
--    IMPLIES up`2 = at(a(up`1))
theorem choose_eq_not_unifies_atom : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => unifies_p G delta up ∧ match_problem_p P PV ∧ atom_p up.1
 ∧ disjoint_p (dom delta) PV ∧ ¬ null_p P → up.2 = at (a up.1) (choose_eq P) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_not_unifies_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        unifies?(G, delta, up) AND match_problem?(P, PV) AND var?(t)
--    AND disjoint?(dom(delta), PV) AND NOT null?(P)
--    IMPLIES
--    ((NOT member(V(t), vars(
theorem choose_eq_not_unifies_var : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p G delta up ∧ match_problem_p P PV ∧ var_p t
 ∧ disjoint_p (dom delta) PV ∧ ¬ null_p P → ((¬ member (V t) (vars s)) ∧ (¬ member (V t) PV)) ∨
 (var_p s ∧ V t = V s) up (choose_eq P) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_not_unifies_unit
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P) IN
--        unifies?(G, delta, up) AND match_problem?(P, PV) AND unit?(up`1)
--    AND disjoint?(dom(delta), PV) AND NOT null?(P)
--    IMPLIES up`2 = unit
theorem choose_eq_not_unifies_unit : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => unifies_p G delta up ∧ match_problem_p P PV ∧ unit_p up.1
 ∧ disjoint_p (dom delta) PV ∧ ¬ null_p P → up.2 = unit (choose_eq P) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_not_unifies_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        unifies?(G, delta, up) AND match_problem?(P, PV) AND app?(t)
--    AND (NOT null?(P)) AND disjoint?(dom(delta), PV) AND well_formed?(delta)
--    IMPLIES a
theorem choose_eq_not_unifies_app : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p G delta up ∧ match_problem_p P PV ∧ app_p t
 ∧ (¬ null_p P) ∧ disjoint_p (dom delta) PV ∧ well_formed_p delta → app_p s ∧ f_sym t = f_sym s ∧ decompose (arg t) (arg s).2 up (choose_eq P) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_not_unifies_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        unifies?(G, delta, up) AND match_problem?(P, PV) AND abs?(t)
--    AND (NOT null?(P)) AND disjoint?(dom(delta), PV) AND well_formed?(delta)
--    IMPLIES a
theorem choose_eq_not_unifies_abs : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p G delta up ∧ match_problem_p P PV ∧ abs_p t
 ∧ (¬ null_p P) ∧ disjoint_p (dom delta) PV ∧ well_formed_p delta → abs_p s ∧ decompose (body t) (body s).2 up (choose_eq P) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.choose_eq_not_unifies_ac_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        unifies?(G, delta, up) AND match_problem?(P, PV) AND ac_app?(t)
--    AND (NOT null?(P)) AND disjoint?(dom(delta), PV)
--    IMPLIES is_ac_sym?(s, ac_sym(t
theorem choose_eq_not_unifies_ac_app : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p G delta up ∧ match_problem_p P PV ∧ ac_app_p t
 ∧ (¬ null_p P) ∧ disjoint_p (dom delta) PV → is_ac_sym_p s (ac_sym t) up (choose_eq P) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input IN
--   IF null?(P) OR NOT ac_prb?(car(P)) THEN null
--   ELSE LET (t, s) = car(P), P1 = cdr(P) IN
--          IF equal?(G)(t, s) THEN cons((G, P1, sigma, V, PV), null)
--        
axiom apply_ac {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20

-- pvs2lean: nominal_AC_ac_step.apply_ac_is_nice_input
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member((G1, P1, sigma1, V1, PV1), apply_ac(input)) IMPLIES
--  nice_input?(G1, P1, sigma1, V1, PV1)
theorem apply_ac_is_nice_input : member ((G1, P1, sigma1, V1, PV1)) (apply_ac input) → nice_input_p G1 P1 sigma1 V1 PV1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_mem5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(input1, apply_ac(input)) IMPLIES input1`5 = input`5
theorem apply_ac_mem5 : member input1 (apply_ac input) → input1.5 = input.5 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P = input`2, P1 = input1`2 IN
--   member(input1, apply_ac(input)) IMPLIES subset?(vars(P1), vars(P))
theorem apply_ac_vars : fun (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => member input1 (apply_ac input) → subset_p (vars P1) (vars P) input1.2 input.2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_vars2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET V = input`4, V1 = input1`4 IN
--   member(input1, apply_ac(input)) IMPLIES subset?(V, V1)
theorem apply_ac_vars2 : fun (V : {i : ℝ // i ≥ 0} → Bool) => fun (V1 : {i : ℝ // i ≥ 0} → Bool) => member input1 (apply_ac input) → subset_p V V1 input1.4 input.4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_vars_ctx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input, (G1, P1, sigma1, V1, PV1) = input1 IN
--   member(input1, apply_ac(input)) IMPLIES
--    subset?(vars(G1), union(vars(P), vars(G)))
theorem apply_ac_vars_ctx : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V1 : {i : ℝ // i ≥ 0} → Bool) (PV1 : {i : ℝ // i ≥ 0} → Bool) => member input1 (apply_ac input) → subset_p (vars G1) (union (vars P) (vars G)) input1 input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_max_size_le
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input IN
--   (NOT null?(P) AND
--     ac_prb?(car(P)) AND
--      NOT equal?(G)(car(P)`1, car(P)`2) AND
--       LET (t, s) = car(P),
--           P1 = cdr(P),
--           lst_V = finset2li
theorem apply_ac_max_size_le : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => (¬ null_p P ∧
  ac_prb_p (car P) ∧
   ¬ equal_p G(car P.1, car P.2) ∧
    (let (t, s) := car P; let P1 := cdr P; let lst_V := finset2list V; let ac_results := first_proj (solve_ac G s t lst_V (ac_sym t) PV); let ac_results_subs := inst_step PV G ac_results; member ((G1, PI, sigma1)) ac_results_subs ∧
       vars (append PI (apply_sub sigma1 P1)) = vars P)) → max_size PI < size (car P.1) + size (car P.2) input := by
  -- === TCCs for apply_ac_max_size_le ===
  -- TCC: apply_ac_max_size_le_TCC1
  -- Statement: FORALL (input: nice_input, G: fresh_context, P: unif_prb, sigma: sub,
  --         V: finite_set[variable], PV: finite_set[variable]):
  --        (G = input`1) AND (P = input`2) AND (sigma = input`3)
  --    AND...
  have apply_ac_max_size_le_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_apply_sub_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input IN
--   (NOT null?(P) AND
--     ac_prb?(car(P)) AND
--      NOT equal?(G)(car(P)`1, car(P)`2) AND
--       LET (t, s) = car(P),
--           P1 = cdr(P),
--           lst_V = finset2li
theorem apply_ac_apply_sub_no_effect : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => (¬ null_p P ∧
  ac_prb_p (car P) ∧
   ¬ equal_p G(car P.1, car P.2) ∧
    (let (t, s) := car P; let P1 := cdr P; let lst_V := finset2list V; let ac_results := first_proj (solve_ac G s t lst_V (ac_sym t) PV); let ac_results_subs := inst_step PV G ac_results; member ((G1, PI, sigma1)) ac_results_subs ∧
       vars (append PI (apply_sub sigma1 P1)) = vars P)) → apply_sub sigma1 (cdr P) = cdr P input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_sound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input IN
--   (NOT null?(P) AND
--     LET (up, P1) = choose_eq(P), (t, s) = up IN
--       ac_prb?(t, s) AND
--        member(input1, apply_ac(G, cons((t, s), P1), sigma, V, PV)) AND
--  
theorem apply_ac_sound : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => (¬ null_p P ∧
  (let (up, P1) := choose_eq P, (t, s) = up; ac_prb_p t s ∧
     member input1 (apply_ac G (cons ((t, s)) P1) sigma V PV) ∧
      is_sol_p Delta delta(input1.1, input1.2, input1.3, input1.5))) → is_sol_p Delta delta(G, P, sigma, PV) input := by
  -- === TCCs for apply_ac_sound ===
  -- TCC: apply_ac_sound_TCC1
  -- Statement: FORALL (input: nice_input, G: fresh_context, P: unif_prb, sigma: sub,
  --         V: finite_set[variable], PV: finite_set[variable]):
  --        (G = input`1) AND (P = input`2) AND (sigma = input`3)
  --    AND...
  have apply_ac_sound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input, (up, P1) = choose_eq(P), (t, s) = up IN
--        ac_prb?(t, s) AND (NOT null?(P))
--    AND is_sol?(Delta, delta)(G, P, sigma, PV) AND contained?(delta, V)
--    AND subset?(
theorem apply_ac_complete : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => ac_prb_p t s ∧ (¬ null_p P)
 ∧ is_sol_p Delta delta(G, P, sigma, PV) ∧ contained_p delta V
 ∧ subset_p (vars Delta) V → ∃ gamma input1, 
       member input1 (apply_ac G (cons ((t, s)) P1) sigma V PV)
   ∧ is_sol_p Delta (append gamma delta)
              (input1.1, input1.2, input1.3, input1.5)
   ∧ well_formed_p gamma PV
   ∧ subset_p (dom gamma) (difference input1.4 V)
   ∧ contained_p gamma input1.4 up (choose_eq P) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_step.apply_ac_complete2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input, (up, P1) = choose_eq(P), (t, s) = up IN
--        ac_prb?(t, s) AND (NOT null?(P))
--    AND is_sol?(Delta, delta)(G, P, sigma, PV) AND contained?(delta, V)
--    AND subset?(
theorem apply_ac_complete2 : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => ac_prb_p t s ∧ (¬ null_p P)
 ∧ is_sol_p Delta delta(G, P, sigma, PV) ∧ contained_p delta V
 ∧ subset_p (vars Delta) V → ∃ gamma input1, 
       member input1 (apply_ac G (cons ((t, s)) P1) sigma V PV)
   ∧ is_sol_p Delta (append gamma delta)
              (input1.1, input1.2, input1.3, input1.5)
   ∧ contained_p (append gamma delta) input1.4
   ∧ subset_p (vars Delta) input1.4
   ∧ equal_p Delta(delta, append gamma delta, V) up (choose_eq P) input := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.nominal_AC_ac_step