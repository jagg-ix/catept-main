-- Auto-generated by pvs2lean/translator.py
-- PVS theory: first_order_AC_substitution
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: first_order_AC_substitution

Auto-generated stubs from PVSLib theory `first_order_AC_substitution`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.first_order_AC_substitution

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (ac_app : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} (ac_app_p : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (ac_sym : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (add : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (alm_well_formed_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (app : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (app_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (append : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} (car : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (cdr : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (cons : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (const : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} (const_p : pvs_τ22 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (count : pvs_τ25 → pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (count_coef : pvs_τ28 → pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (count_elem : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (count_elem2 : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} (count_vec : pvs_τ39 → pvs_τ40 → pvs_τ41 → pvs_τ42)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} (delete : pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (dif_elem_p : pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} (disjoint_p : pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (dot_product : pvs_τ52 → pvs_τ53 → Prop)
variable {pvs_τ60 : Type*} (equivalence_p : pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (finset2list : pvs_τ61 → pvs_τ62)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} (flatten : pvs_τ63 → pvs_τ64)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (flatten_pair : pvs_τ65 → pvs_τ66)
variable {pvs_τ67 : Type*} (func_p : pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} (get_ac_from_args : pvs_τ68 → pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} (get_args : pvs_τ71 → pvs_τ72 → Prop)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} (get_lst_mult : pvs_τ73 → pvs_τ74 → pvs_τ75)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} (get_repeat_lst : pvs_τ76 → pvs_τ77 → pvs_τ78)
variable {pvs_τ79 : Type*} {pvs_τ80 : Type*} (get_term_from_args : pvs_τ79 → pvs_τ80)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} (im_under_p : pvs_τ82 → pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} {pvs_τ86 : Type*} (image : pvs_τ84 → pvs_τ85 → pvs_τ86)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (is_ac_sym_p : pvs_τ88 → pvs_τ89 → Prop)
variable {pvs_τ90 : Type*} {pvs_τ91 : Type*} (is_finite : pvs_τ90 → pvs_τ91)
variable {pvs_τ92 : Type*} (length : pvs_τ92 → Prop)
variable {pvs_τ93 : Type*} {pvs_τ94 : Type*} {pvs_τ95 : Type*} (map : pvs_τ93 → pvs_τ94 → pvs_τ95)
variable {pvs_τ96 : Type*} {pvs_τ97 : Type*} (member : pvs_τ96 → pvs_τ97 → Prop)
variable {pvs_τ101 : Type*} {pvs_τ102 : Type*} (nice_ind_p : pvs_τ101 → pvs_τ102 → Prop)
variable {pvs_τ104 : Type*} (no_pair_p : pvs_τ104 → Prop)
variable {pvs_τ105 : Type*} {pvs_τ106 : Type*} (nth : pvs_τ105 → pvs_τ106 → Prop)
variable {pvs_τ107 : Type*} (null_p : pvs_τ107 → Prop)
variable {pvs_τ108 : Type*} {pvs_τ109 : Type*} (num_arg : pvs_τ108 → pvs_τ109 → Prop)
variable {pvs_τ110 : Type*} (ord : pvs_τ110 → Prop)
variable {pvs_τ111 : Type*} {pvs_τ112 : Type*} (pair : pvs_τ111 → pvs_τ112 → Prop)
variable {pvs_τ113 : Type*} (pair_p : pvs_τ113 → Prop)
variable {pvs_τ114 : Type*} {pvs_τ115 : Type*} (perm_p : pvs_τ114 → pvs_τ115 → Prop)
variable {pvs_τ116 : Type*} {pvs_τ117 : Type*} {pvs_τ118 : Type*} (pos_p : pvs_τ116 → pvs_τ117 → pvs_τ118 → Prop)
variable {pvs_τ119 : Type*} {pvs_τ120 : Type*} {pvs_τ121 : Type*} {pvs_τ122 : Type*} (remove : pvs_τ119 → pvs_τ120 → pvs_τ121 → pvs_τ122)
variable {pvs_τ123 : Type*} {pvs_τ124 : Type*} {pvs_τ125 : Type*} (remove_common : pvs_τ123 → pvs_τ124 → pvs_τ125 → Prop)
variable {pvs_τ127 : Type*} {pvs_τ128 : Type*} {pvs_τ129 : Type*} (same_count_p : pvs_τ127 → pvs_τ128 → pvs_τ129 → Prop)
variable {pvs_τ130 : Type*} {pvs_τ131 : Type*} (same_func_p : pvs_τ130 → pvs_τ131 → Prop)
variable {pvs_τ132 : Type*} {pvs_τ133 : Type*} (sec_proj : pvs_τ132 → pvs_τ133)
variable {pvs_τ134 : Type*} {pvs_τ135 : Type*} {pvs_τ136 : Type*} (select : pvs_τ134 → pvs_τ135 → pvs_τ136 → Prop)
variable {pvs_τ141 : Type*} (singleton : pvs_τ141 → Prop)
variable {pvs_τ142 : Type*} (size2 : pvs_τ142 → Prop)
variable {pvs_τ145 : Type*} {pvs_τ146 : Type*} (subset_lst_p : pvs_τ145 → pvs_τ146 → Prop)
variable {pvs_τ147 : Type*} {pvs_τ148 : Type*} (subset_p : pvs_τ147 → pvs_τ148 → Prop)
variable {pvs_τ149 : Type*} {pvs_τ150 : Type*} (subterms : pvs_τ149 → pvs_τ150)
variable {pvs_τ151 : Type*} {pvs_τ152 : Type*} (sum : pvs_τ151 → pvs_τ152)
variable {pvs_τ155 : Type*} {pvs_τ156 : Type*} {pvs_τ157 : Type*} (union : pvs_τ155 → pvs_τ156 → pvs_τ157)
variable {pvs_τ158 : Type*} (var_p : pvs_τ158 → Prop)
variable {pvs_τ159 : Type*} {pvs_τ160 : Type*} (pvs_variable : pvs_τ159 → pvs_τ160)
variable {pvs_τ161 : Type*} (vars : pvs_τ161 → Prop)

-- pvs2lean: first_order_AC_substitution.subs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET X = sigma_basic`1, s = sigma_basic`2 IN
--   CASES t
--     OF const(a): const(a),
--        variable(Y): IF X = Y THEN s ELSE variable(Y) ENDIF,
--        unit: unit,
--        pair(t1, t2): pair(subs(sigma_bas
axiom subs {pvs_τ143 : Type*} {pvs_τ144 : Type*} : pvs_τ143 → pvs_τ144 → Prop

-- pvs2lean: first_order_AC_substitution.equal?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL X: equal?(subs(sigma)(X), subs(delta)(X))
axiom equal_p {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} : pvs_τ54 → pvs_τ55 → pvs_τ56 → Prop

-- pvs2lean: first_order_AC_substitution.equal_syn?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL X: member(X, set_vars) IMPLIES subs(sigma)(X) = subs(delta)(X)
axiom equal_syn_p {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} : pvs_τ57 → pvs_τ58 → pvs_τ59 → Prop

-- pvs2lean: first_order_AC_substitution.more_general?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS delta2: equal?(sigma, append(delta2, delta))
axiom more_general_p {pvs_τ98 : Type*} {pvs_τ99 : Type*} {pvs_τ100 : Type*} : pvs_τ98 → pvs_τ99 → pvs_τ100 → Prop

-- pvs2lean: first_order_AC_substitution.supset_dom
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(sigma) THEN emptyset
-- ELSE LET X = car(sigma)`1 IN add(X, supset_dom(cdr(sigma)))
-- ENDIF
axiom supset_dom {pvs_τ153 : Type*} {pvs_τ154 : Type*} : pvs_τ153 → pvs_τ154

-- pvs2lean: first_order_AC_substitution.supset_dom_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(X) /= variable(X) IMPLIES member(X, supset_dom(sigma))
theorem supset_dom_correct : subs sigma(X) ≠ variable X → member X (supset_dom sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.dom
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {X | subs(sigma)(X) /= variable(X)}
axiom dom {pvs_τ51 : Type*} : pvs_τ51 → Prop

-- pvs2lean: first_order_AC_substitution.supset_dom_correct2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(dom(sigma), supset_dom(sigma))
theorem supset_dom_correct2 : subset_p (dom sigma) (supset_dom sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.img
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {t | EXISTS X: member(X, dom(sigma)) AND subs(sigma)(X) = t}
axiom img {pvs_τ87 : Type*} : pvs_τ87 → Prop

-- pvs2lean: first_order_AC_substitution.idempotent?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL X: subs(sigma)(subs(sigma)(X)) = subs(sigma)(X)
axiom idempotent_p {pvs_τ81 : Type*} : pvs_τ81 → Prop

-- pvs2lean: first_order_AC_substitution.nice?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(sigma) THEN TRUE
-- ELSE LET (X, t) = car(sigma) IN
--        (NOT member(X, vars(t)) AND
--          NOT member(X, supset_dom(cdr(sigma))) AND
--           disjoint?(vars(t), supset_dom(cdr(sigma))) AND
axiom nice_p {pvs_τ103 : Type*} : pvs_τ103 → Prop

-- pvs2lean: first_order_AC_substitution.well_formed?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    NOT pair?(subs(delta)(X)) AND
--     FORALL t:
--       member(t, subterms(subs(delta)(X))) AND ac_app?(t) IMPLIES
--        num_arg(ac_sym(t), t) > 1)
axiom well_formed_p {pvs_τ162 : Type*} : pvs_τ162 → Prop

-- pvs2lean: first_order_AC_substitution.contained?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(dom(delta), set_vars) AND subset?(vars(img(delta)), set_vars)
axiom contained_p {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ23 → pvs_τ24 → Prop

-- pvs2lean: first_order_AC_substitution.shift
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN cons((start, variable(start + inc)), null)
-- ELSE cons((start, variable(start + inc)), shift(start + 1, n - 1, inc))
-- ENDIF
axiom shift {pvs_τ137 : Type*} {pvs_τ138 : Type*} {pvs_τ139 : Type*} {pvs_τ140 : Type*} : pvs_τ137 → pvs_τ138 → pvs_τ139 → pvs_τ140

-- pvs2lean: first_order_AC_substitution.renaming?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL t: member(t, img(sigma)) IMPLIES var?(t)) AND
--  (FORALL X, Y:
--     member(X, dom(sigma)) AND
--      member(Y, dom(sigma)) AND subs(sigma)(X) = subs(sigma)(Y)
--      IMPLIES X = Y)
axiom renaming_p {pvs_τ126 : Type*} : pvs_τ126 → Prop

-- pvs2lean: first_order_AC_substitution.subs_const
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(const(a)) = const(a)
theorem subs_const : subs sigma(const a) = const a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_unit
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(unit) = unit
theorem subs_unit : subs sigma(unit) = unit := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(pair(t, s)) = pair(subs(sigma)(t), subs(sigma)(s))
theorem subs_pair : subs sigma(pair t s) = pair (subs sigma(t)) (subs sigma(s)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(app(f, arg)) = app(f, subs(sigma)(arg))
theorem subs_app : subs sigma(app f arg) = app f (subs sigma(arg)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(ac_app(f, arg)) = ac_app(f, subs(sigma)(arg))
theorem subs_ac : subs sigma(ac_app f arg) = ac_app f (subs sigma(arg)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(subs(sigma)(t)) IMPLIES var?(t)
theorem subs_var : var_p (subs sigma(t)) → var_p t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_const2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: const?(subs(sigma)(t)) IMPLIES (const?(t) OR var?(t))
theorem subs_const2 : const_p (subs sigma(t)) → (const_p t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_pair2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pair?(subs(sigma)(t)) IMPLIES (pair?(t) OR var?(t))
theorem subs_pair2 : pair_p (subs sigma(t)) → (pair_p t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_ac2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ac_app?(subs(sigma)(t)) IMPLIES (same_func?(subs(sigma)(t), t) OR var?(t))
theorem subs_ac2 : ac_app_p (subs sigma(t)) → (same_func_p (subs sigma(t)) t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_ac3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_ac_sym?(subs(sigma)(t), f) IMPLIES (is_ac_sym?(t, f) OR var?(t))
theorem subs_ac3 : is_ac_sym_p (subs sigma(t)) f → (is_ac_sym_p t f ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_func
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: func?(subs(sigma)(t)) IMPLIES (same_func?(subs(sigma)(t), t) OR var?(t))
theorem subs_func : func_p (subs sigma(t)) → (same_func_p (subs sigma(t)) t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_ord
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: t = subs(sigma)(s) IMPLIES
--  (ord(t) = ord(s) OR (var?(s) AND member(V(s), dom(sigma))))
theorem subs_ord : t = subs sigma(s) → (ord t = ord s ∨ (var_p s ∧ member (V s) (dom sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_num_arg_ge1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_arg(f, t) > 1 IMPLIES num_arg(f, subs(sigma)(t)) > 1
theorem subs_num_arg_ge1 : num_arg f t > 1 → num_arg f (subs sigma(t)) > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_select
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS i:
--    nice_ind?(i, num_arg(f, t)) AND
--     select(f, subs(sigma)(t))(n) = subs(sigma)(select(f, t)(i)))
--  OR member(select(f, subs(sigma)(t))(n), subterms(img(sigma)))
theorem subs_select : (∃ i, 
   nice_ind_p i (num_arg f t) ∧
    select f (subs sigma(t)) (n) = subs sigma(select f t(i))) ∨ member (select f (subs sigma(t)) (n)) (subterms (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subs(sigma)(args)) IFF
--  EXISTS s: member(s, args) AND t = subs(sigma)(s)
theorem subs_mem : member t (subs sigma(args)) ↔ ∃ s,  member s args ∧ t = subs sigma(s) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_len
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(subs(sigma)(args)) = length(args)
theorem subs_len : List.length (subs sigma(args)) = List.length args := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(append(args1, args2)) =
--  append(subs(sigma)(args1), subs(sigma)(args2))
theorem subs_append : subs sigma(append args1 args2) = append (subs sigma(args1)) (subs sigma(args2)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_append2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(append(sigma, delta))(t) = subs(sigma)(subs(delta)(t))
theorem subs_append2 : subs (append sigma delta) (t) = subs sigma(subs delta(t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_map
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(subs(sigma))(args) = subs(sigma)(args)
theorem subs_map : map (subs sigma) (args) = subs sigma(args) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_append_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(append(sigma, delta))(args) = subs(sigma)(subs(delta)(args))
theorem subs_append_args : subs (append sigma delta) (args) = subs sigma(subs delta(args)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_map_append_lst_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(subs(append(sigma, delta)))(lst_args) =
--  map(subs(sigma))(map(subs(delta))(lst_args))
theorem subs_map_append_lst_args : map (subs (append sigma delta)) (lst_args) = map (subs sigma) (map (subs delta) (lst_args)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(cons((X, t), delta))(args1) =
--  subs(cons((X, t), null))(subs(delta)(args1))
theorem subs_cons : subs (cons ((X, t)) delta) (args1) = subs (cons ((X, t)) null) (subs delta(args1)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_get_term_from_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(get_term_from_args(args1)) =
--  get_term_from_args(subs(sigma)(args1))
theorem subs_get_term_from_args : subs sigma(get_term_from_args args1) = get_term_from_args (subs sigma(args1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_get_ac_from_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(get_ac_from_args(f)(args)) =
--  get_ac_from_args(f)(subs(sigma)(args))
theorem subs_get_ac_from_args : subs sigma(get_ac_from_args f(args)) = get_ac_from_args f(subs sigma(args)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_args_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(args) IMPLIES
--  nth(subs(sigma)(args), i) = subs(sigma)(nth(args, i))
theorem subs_args_nth : i < List.length args → nth (subs sigma(args)) i = subs sigma(nth args i) := by
  -- === TCCs for subs_args_nth ===
  -- TCC: subs_args_nth_TCC1
  -- Statement: FORALL (args: args, i: nat, sigma: sub):
  --   i < length(args) IMPLIES
  --    i <
  --     length[first_order_AC_term[constant, variable, f_symbol, ac_symbol]]
  --         (subs(sigma)(args))
  have subs_args_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subs_args_nth_TCC1
  -- Statement: FORALL (args: args, i: nat, sigma: sub):
  --   i < length(args) IMPLIES
  --    i <
  --     length[nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]]
  --         (subs(sigma)(args))
  have subs_args_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: remove(subs(sigma)(args), i) = subs(sigma)(remove(args, i))
theorem subs_remove : remove (subs sigma(args)) i = subs sigma(remove args i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.args_subs_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: get_args(f)(subs(sigma)(t)) =
--  flatten(map(get_args(f))(map(subs(sigma))(get_args(f)(t))))
theorem args_subs_ac : get_args f(subs sigma(t)) = flatten (map (get_args f) (map (subs sigma) (get_args f(t)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.mem_get_args_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, get_args(f)(subs(sigma)(t))) IMPLIES
--  (EXISTS ti: member(ti, get_args(f)(t)) AND s = subs(sigma)(ti)) OR
--   member(s, subterms(img(sigma)))
theorem mem_get_args_subs : member s (get_args f(subs sigma(t))) → (∃ ti,  member ti (get_args f(t)) ∧ s = subs sigma(ti)) ∨
 member s (subterms (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_get_args1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_arg(f, t) = 1 IMPLIES
--  get_args(f)(subs(sigma)(t)) = get_args(f)(subs(sigma)(select(f, t)(i)))
theorem subs_get_args1 : num_arg f t = 1 → get_args f(subs sigma(t)) = get_args f(subs sigma(select f t(i))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_perm_get_args_ge1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_arg(f, t) > 1 IMPLIES
--  perm?(get_args(f)(subs(sigma)(t)),
--        append(get_args(f)(subs(sigma)(select(f, t)(i))),
--               get_args(f)(subs(sigma)(delete(f, t)(i)))))
theorem subs_perm_get_args_ge1 : num_arg f t > 1 → perm_p (get_args f(subs sigma(t))) (append (get_args f(subs sigma(select f t(i)))) (get_args f(subs sigma(delete f t(i))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.mem_get_args_subs4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(ti, get_args(f)(t)) AND member(s, get_args(f)(subs(sigma)(ti)))
--  IMPLIES member(s, get_args(f)(subs(sigma)(t)))
theorem mem_get_args_subs4 : member ti (get_args f(t)) ∧ member s (get_args f(subs sigma(ti))) → member s (get_args f(subs sigma(t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_equal?
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(t, s) IMPLIES equal?(subs(sigma)(t), subs(sigma)(s))
theorem subs_equal_p : equal_p t s → equal_p (subs sigma(t)) (subs sigma(s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_perm?
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: perm?(args1, args2) => perm?(subs(sigma)(args1), subs(sigma)(args2))
theorem subs_perm_p : perm_p args1 args2 → perm_p (subs sigma(args1)) (subs sigma(args2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_perm?_get_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: perm?(args1, args2) IMPLIES
--  perm?(flatten(map(get_args(f))(map(subs(delta))(args1))),
--        flatten(map(get_args(f))(map(subs(delta))(args2))))
theorem subs_perm_p_get_args : perm_p args1 args2 → perm_p (flatten (map (get_args f) (map (subs delta) (args1)))) (flatten (map (get_args f) (map (subs delta) (args2)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_same_count
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: same_count?(args1, args2, equal?) =>
--  same_count?(subs(sigma)(args1), subs(sigma)(args2), equal?)
theorem subs_same_count : same_count_p args1 args2 equal_p → same_count_p (subs sigma(args1)) (subs sigma(args2)) equal_p := by
  -- === TCCs for subs_same_count ===
  -- TCC: subs_same_count_TCC1
  -- Statement: equivalence?[first_order_AC_term[constant, variable, f_symbol, ac_symbol]]
  --     (equal?)
  have subs_same_count_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subs_same_count_TCC1
  -- Statement: FORALL (G: fresh_context):
  --   equivalence?[nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]]
  --       (equal?(G))
  have subs_same_count_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subs_same_count_TCC2
  -- Statement: FORALL (G, G1: fresh_context, args1, args2: args, sigma: sub):
  --   nominal_AC_fresh_subs(G1)(sigma, G) AND
  --    same_count?(args1, args2, equal?(G))
  --    IMPLIES
  --    equivalence?[nominal_AC_term[atom, per...
  have subs_same_count_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_get_repeat_lst
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(get_repeat_lst(t, i)) = get_repeat_lst(subs(sigma)(t), i)
theorem subs_get_repeat_lst : subs sigma(get_repeat_lst t i) = get_repeat_lst (subs sigma(t)) i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_get_repeat_lst_map
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(subs(sigma))(get_repeat_lst(args, i)) =
--  get_repeat_lst(subs(sigma)(args), i)
theorem subs_get_repeat_lst_map : map (subs sigma) (get_repeat_lst args i) = get_repeat_lst (subs sigma(args)) i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_get_lst_mult
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(get_lst_mult(args, coef)) =
--  get_lst_mult(subs(sigma)(args), coef)
theorem subs_get_lst_mult : subs sigma(get_lst_mult args coef) = get_lst_mult (subs sigma(args)) coef := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_get_lst_mult2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(subs(sigma))(get_lst_mult(lst_args, coef)) =
--  get_lst_mult(map(subs(sigma))(lst_args), coef)
theorem subs_get_lst_mult2 : map (subs sigma) (get_lst_mult lst_args coef) = get_lst_mult (map (subs sigma) (lst_args)) coef := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.perm?_get_args_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: perm?(subs(sigma)(get_args(f)(t)), subs(sigma)(get_args(f)(s))) IMPLIES
--  perm?(get_args(f)(subs(sigma)(t)), get_args(f)(subs(sigma)(s)))
theorem perm_p_get_args_subs : perm_p (subs sigma(get_args f(t))) (subs sigma(get_args f(s))) → perm_p (get_args f(subs sigma(t))) (get_args f(subs sigma(s))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_flatten
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(flatten(lst_args)) = flatten(map(subs(sigma))(lst_args))
theorem subs_flatten : subs sigma(flatten lst_args) = flatten (map (subs sigma) (lst_args)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.vars_subs_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(subs(sigma)(t)), union(vars(img(sigma)), vars(t)))
theorem vars_subs_t : subset_p (vars (subs sigma(t))) (union (vars (img sigma)) (vars t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.basic_sub_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, vars(t)) IMPLIES subs((X, s), t) = t
theorem basic_sub_no_effect : ¬ member X (vars t) → subs ((X, s)) t = t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.basic_sub_elim_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, vars(s)) IMPLIES NOT member(X, vars(subs((X, s), t)))
theorem basic_sub_elim_var : ¬ member X (vars s) → ¬ member X (vars (subs ((X, s)) t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.basic_sub_not_elim_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: X /= Y AND NOT member(Y, vars(s)) AND member(Y, vars(subs((X, s), t)))
--  IMPLIES member(Y, vars(t))
theorem basic_sub_not_elim_var : X ≠ Y ∧ ¬ member Y (vars s) ∧ member Y (vars (subs ((X, s)) t)) → member Y (vars t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.dom_basic_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dom(cons((X, t), null)) = singleton(X) OR member(X, vars(t))
theorem dom_basic_sub : dom (cons ((X, t)) null) = singleton X ∨ member X (vars t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.dom_basic_sub2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dom(cons((X, t), null)) = singleton(X) OR dom(cons((X, t), null)) = emptyset
theorem dom_basic_sub2 : dom (cons ((X, t)) null) = singleton X ∨ dom (cons ((X, t)) null) = emptyset := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.dom_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dom(null) = emptyset
theorem dom_null : dom null = emptyset := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.dom_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(dom(append(delta, sigma)), union(dom(delta), dom(sigma)))
theorem dom_append : subset_p (dom (append delta sigma)) (union (dom delta) (dom sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.img_basic_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: img(cons((X, t), null)) = singleton(t) OR member(X, vars(t))
theorem img_basic_sub : img (cons ((X, t)) null) = singleton t ∨ member X (vars t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.img_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: img(null) = emptyset
theorem img_null : img null = emptyset := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_no_effect_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(t)) IMPLIES subs(sigma)(t) = t
theorem subs_no_effect_t : disjoint_p (dom sigma) (vars t) → subs sigma(t) = t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.dom_append2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(vars(img(sigma)), dom(delta)) AND
--  disjoint?(dom(delta), dom(sigma))
--  IMPLIES dom(append(delta, sigma)) = union(dom(delta), dom(sigma))
theorem dom_append2 : disjoint_p (vars (img sigma)) (dom delta) ∧
 disjoint_p (dom delta) (dom sigma) → dom (append delta sigma) = union (dom delta) (dom sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_no_effect_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(args)) IMPLIES subs(sigma)(args) = args
theorem subs_no_effect_args : disjoint_p (dom sigma) (vars args) → subs sigma(args) = args := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.apply_sub_elim_var_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, dom(sigma)) AND NOT member(X, vars(img(sigma))) IMPLIES
--  NOT member(X, vars(subs(sigma)(t)))
theorem apply_sub_elim_var_t : member X (dom sigma) ∧ ¬ member X (vars (img sigma)) → ¬ member X (vars (subs sigma(t))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.vars_img_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(img(append(delta, sigma))),
--         union(vars(img(delta)), vars(img(sigma))))
theorem vars_img_append : subset_p (vars (img (append delta sigma))) (union (vars (img delta)) (vars (img sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.idempotent_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(null[basic_sub])
theorem idempotent_null : idempotent_p null := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.var_dom_sub_dif_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(t)) AND member(X, dom(sigma)) IMPLIES subs(sigma)(t) /= t
theorem var_dom_sub_dif_term : member X (vars t) ∧ member X (dom sigma) → subs sigma(t) ≠ t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.idempotent_disjoint_dom_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) IMPLIES disjoint?(dom(sigma), vars(img(sigma)))
theorem idempotent_disjoint_dom_img : idempotent_p sigma → disjoint_p (dom sigma) (vars (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.idempotent_disjoint_dom_img2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(img(sigma))) IMPLIES idempotent?(sigma)
theorem idempotent_disjoint_dom_img2 : disjoint_p (dom sigma) (vars (img sigma)) → idempotent_p sigma := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.apply_sub_elim_var_t2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, dom(sigma)) AND idempotent?(sigma) IMPLIES
--  NOT member(X, vars(subs(sigma)(t)))
theorem apply_sub_elim_var_t2 : member X (dom sigma) ∧ idempotent_p sigma → ¬ member X (vars (subs sigma(t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(singleton(t)) = singleton(subs(sigma)(t))
theorem subs_singleton : subs sigma(singleton t) = singleton (subs sigma(t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.img_append_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(img(append(sigma, delta)),
--         union(image(subs(sigma))(img(delta)), img(sigma)))
theorem img_append_subset : subset_p (img (append sigma delta)) (union (image (subs sigma) (img delta)) (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.idempotent_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) AND
--  idempotent?(delta) AND disjoint?(dom(delta), vars(img(sigma)))
--  IMPLIES idempotent?(append(sigma, delta))
theorem idempotent_append : idempotent_p sigma ∧
 idempotent_p delta ∧ disjoint_p (dom delta) (vars (img sigma)) → idempotent_p (append sigma delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.idempotent_subs_subs_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) IMPLIES subs(sigma)(subs(sigma)(t)) = subs(sigma)(t)
theorem idempotent_subs_subs_t : idempotent_p sigma → subs sigma(subs sigma(t)) = subs sigma(t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_divide_not_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT var?(subs(sigma)(X)) IMPLIES
--  EXISTS delta, delta2, sigma_basic, Y:
--    sigma = append(delta, cons(sigma_basic, delta2)) AND
--     subs(delta2)(X) = variable(Y) AND
--      sigma_basic`1 = Y AND NOT var
theorem subs_divide_not_var : ¬ var_p (subs sigma(X)) → ∃ delta delta2 sigma_basic Y, 
  sigma = append delta (cons sigma_basic delta2) ∧
   subs delta2(X) = variable Y ∧
    sigma_basic.1 = Y ∧ ¬ var_p sigma_basic.2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.basic_sub_info
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sigma = cons((X, t), null) IN
--   NOT member(X, vars(t)) IMPLIES
--    dom(sigma) = singleton(X) AND
--     img(sigma) = singleton(t) AND idempotent?(sigma)
theorem basic_sub_info : fun (sigma : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => ¬ member X (vars t) → dom sigma = singleton X ∧
 img sigma = singleton t ∧ idempotent_p sigma (cons ((X, t)) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.basic_sub_well_formed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sigma = cons((X, t), null) IN
--   well_formed?(t) IMPLIES well_formed?(sigma)
theorem basic_sub_well_formed : fun (sigma : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => well_formed_p t → well_formed_p sigma (cons ((X, t)) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subterms_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(subterms(subs(sigma)(t)),
--         union(image(subs(sigma))(subterms(t)), subterms(img(sigma))))
theorem subterms_subs : subset_p (subterms (subs sigma(t))) (union (image (subs sigma) (subterms t)) (subterms (img sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_no_effect_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(t) = t AND member(s, subterms(t)) IMPLIES subs(sigma)(s) = s
theorem subs_no_effect_subterm : subs sigma(t) = t ∧ member s (subterms t) → subs sigma(s) = s := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subterms_img_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(subterms(img(append(sigma, delta))),
--         union(image(subs(sigma))(subterms(img(delta))),
--               subterms(img(sigma))))
theorem subterms_img_append : subset_p (subterms (img (append sigma delta))) (union (image (subs sigma) (subterms (img delta))) (subterms (img sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.same_func_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: func?(t) IMPLIES same_func?(t, subs(sigma)(t))
theorem same_func_subs : func_p t → same_func_p t (subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.func_subterms_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: func?(s) AND member(s, subterms(subs(sigma)(t))) IMPLIES
--  ((EXISTS ti:
--      same_func?(ti, s) AND subs(sigma)(ti) = s AND member(ti, subterms(t)))
--    OR member(s, subterms(img(sigma))))
theorem func_subterms_subs : func_p s ∧ member s (subterms (subs sigma(t))) → ((∃ ti, 
    same_func_p ti s ∧ subs sigma(ti) = s ∧ member ti (subterms t))
  ∨ member s (subterms (img sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.flatten_pair_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, flatten_pair(subs(sigma)(t))) AND no_pair?(img(sigma)) IMPLIES
--  EXISTS ti: member(ti, flatten_pair(t)) AND s = subs(sigma)(ti)
theorem flatten_pair_subs : member s (flatten_pair (subs sigma(t))) ∧ no_pair_p (img sigma) → ∃ ti,  member ti (flatten_pair t) ∧ s = subs sigma(ti) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.mem_get_args_subs2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, get_args(f)(subs(sigma)(t))) IMPLIES
--  (EXISTS ti:
--     member(ti, get_args(f)(t)) AND
--      LET ti_sigma = subs(sigma)(ti) IN
--        (s = ti_sigma OR
--          (var?(ti) AND
--            member(V
theorem mem_get_args_subs2 : member s (get_args f(subs sigma(t))) → (∃ ti, 
   member ti (get_args f(t)) ∧
    (let ti_sigma := subs sigma(ti); (s = ti_sigma ∨
        (var_p ti ∧
          member (V ti) (dom sigma) ∧ member s (get_args f(ti_sigma)))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.mem_get_args_subs3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, get_args(f)(subs(sigma)(t))) IMPLIES
--  (EXISTS ti:
--     member(ti, get_args(f)(t)) AND
--      LET ti_sigma = subs(sigma)(ti) IN
--        ((s = ti_sigma AND ord(s) = ord(ti)) OR
--          (var?(ti) 
theorem mem_get_args_subs3 : member s (get_args f(subs sigma(t))) → (∃ ti, 
   member ti (get_args f(t)) ∧
    (let ti_sigma := subs sigma(ti); ((s = ti_sigma ∧ ord s = ord ti) ∨
        (var_p ti ∧
          member (V ti) (dom sigma) ∧ member s (get_args f(ti_sigma)))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.im_under_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: im_under?(Y, subs(sigma)(t)) AND func?(t) AND no_pair?(img(sigma)) IMPLIES
--  ((EXISTS X: im_under?(X, t) AND subs(sigma)(X) = variable(Y)) OR
--    im_under?(Y, img(sigma)))
theorem im_under_subs : im_under_p Y (subs sigma(t)) ∧ func_p t ∧ no_pair_p (img sigma) → ((∃ X,  im_under_p X t ∧ subs sigma(X) = variable Y) ∨
  im_under_p Y (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.im_under_subs2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: im_under?(Y, subs(sigma)(t)) AND
--  func?(t) AND no_pair?(img(sigma)) AND idempotent?(sigma)
--  IMPLIES
--  (EXISTS X:
--     subs(sigma)(X) = variable(Y) AND
--      (im_under?(X, t) OR
--        EXISTS s:
--          
theorem im_under_subs2 : im_under_p Y (subs sigma(t)) ∧
 func_p t ∧ no_pair_p (img sigma) ∧ idempotent_p sigma → (∃ X, 
   subs sigma(X) = variable Y ∧
    (im_under_p X t ∨
      ∃ s, 
        member s (img sigma) ∧ im_under_p X s ∧ same_func_p t s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.im_under_subs3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: im_under?(Y, subs(sigma)(t)) AND
--  no_pair?(img(sigma)) AND idempotent?(sigma)
--  IMPLIES
--  (EXISTS X:
--     subs(sigma)(X) = variable(Y) AND
--      (im_under?(X, t) OR
--        EXISTS s:
--          member(s, img
theorem im_under_subs3 : im_under_p Y (subs sigma(t)) ∧ no_pair_p (img sigma) ∧ idempotent_p sigma → (∃ X, 
   subs sigma(X) = variable Y ∧
    (im_under_p X t ∨
      ∃ s, 
        member s (img sigma) ∧
         im_under_p X s ∧ same_func_p (subs sigma(t)) s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.supset_dom_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, supset_dom(delta)) IFF EXISTS t: member((X, t), delta)
theorem supset_dom_equiv : member X (supset_dom delta) ↔ ∃ t,  member ((X, t)) delta := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.supset_dom_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: supset_dom(append(sigma, delta)) =
--  union(supset_dom(sigma), supset_dom(delta))
theorem supset_dom_append : supset_dom (append sigma delta) = union (supset_dom sigma) (supset_dom delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.nice_disjoint_dom_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(sigma) IMPLIES disjoint?(dom(sigma), vars(img(sigma)))
theorem nice_disjoint_dom_img : nice_p sigma → disjoint_p (dom sigma) (vars (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.nice_idempotent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(sigma) IMPLIES idempotent?(sigma)
theorem nice_idempotent : nice_p sigma → idempotent_p sigma := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.nice_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(append(sigma, delta)) IMPLIES nice?(sigma) AND nice?(delta)
theorem nice_append : nice_p (append sigma delta) → nice_p sigma ∧ nice_p delta := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.nice_append2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(delta) AND
--  nice?(sigma) AND
--   (FORALL X, t:
--      member((X, t), sigma) IMPLIES
--       NOT member(X, supset_dom(delta)) AND
--        disjoint?(vars(t), supset_dom(delta)))
--  IMPLIES nice?(append(sig
theorem nice_append2 : nice_p delta ∧
 nice_p sigma ∧
  (∀ X t, 
     member ((X, t)) sigma →
      ¬ member X (supset_dom delta) ∧
       disjoint_p (vars t) (supset_dom delta)) → nice_p (append sigma delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.nice_append_supset_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(append(sigma, delta)) AND member(X, supset_dom(sigma)) IMPLIES
--  NOT member(X, supset_dom(delta))
theorem nice_append_supset_dom : nice_p (append sigma delta) ∧ member X (supset_dom sigma) → ¬ member X (supset_dom delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.vars_img_sec_proj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(img(sigma)), vars(sec_proj(sigma)))
theorem vars_img_sec_proj : subset_p (vars (img sigma)) (vars (sec_proj sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.well_formed_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(null)
theorem well_formed_null : well_formed_p null := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.well_formed_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(sigma) AND well_formed?(delta) IMPLIES
--  well_formed?(append(sigma, delta))
theorem well_formed_append : well_formed_p sigma ∧ well_formed_p delta → well_formed_p (append sigma delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.well_formed_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(sigma) AND well_formed?(t) IMPLIES well_formed?(subs(sigma)(t))
theorem well_formed_subs : well_formed_p sigma ∧ well_formed_p t → well_formed_p (subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.well_formed_num_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(sigma) IMPLIES num_arg(subs(sigma)(t)) = num_arg(t)
theorem well_formed_num_arg : well_formed_p sigma → num_arg (subs sigma(t)) = num_arg t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.alm_well_formed_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(sigma) AND alm_well_formed?(t) IMPLIES
--  alm_well_formed?(subs(sigma)(t))
theorem alm_well_formed_subs : well_formed_p sigma ∧ alm_well_formed_p t → alm_well_formed_p (subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(delta, sigma) IMPLIES equal?(subs(delta)(t), subs(sigma)(t))
theorem equal_sub_equiv : equal_p delta sigma → equal_p (subs delta(t)) (subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_equiv_alt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(delta, sigma, set_vars) AND subset?(vars(t), set_vars) IMPLIES
--  equal?(subs(delta)(t), subs(sigma)(t))
theorem equal_sub_equiv_alt : equal_p delta sigma set_vars ∧ subset_p (vars t) set_vars → equal_p (subs delta(t)) (subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_change_eq_terms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(delta, sigma) IMPLIES
--  (equal?(subs(delta)(t), subs(delta)(s)) IFF
--    equal?(subs(sigma)(t), subs(sigma)(s)))
theorem equal_sub_change_eq_terms : equal_p delta sigma → (equal_p (subs delta(t)) (subs delta(s)) ↔
  equal_p (subs sigma(t)) (subs sigma(s))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_ref
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(sigma, sigma)
theorem equal_sub_ref : equal_p sigma sigma := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_sym
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(delta, gamma) = equal?(gamma, delta)
theorem equal_sub_sym : equal_p delta gamma = equal_p gamma delta := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_trans
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(delta, gamma) AND equal?(gamma, sigma) IMPLIES equal?(delta, sigma)
theorem equal_sub_trans : equal_p delta gamma ∧ equal_p gamma sigma → equal_p delta sigma := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(sigma, sigma1) IMPLIES
--  equal?(append(sigma, delta), append(sigma1, delta))
theorem equal_sub_append : equal_p sigma sigma1 → equal_p (append sigma delta) (append sigma1 delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(sigma, sigma1, set_vars) AND subset?(set_vars1, set_vars) IMPLIES
--  equal?(sigma, sigma1, set_vars1)
theorem equal_sub_subset : equal_p sigma sigma1 set_vars ∧ subset_p set_vars1 set_vars → equal_p sigma sigma1 set_vars1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_res_trans
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(delta, gamma, set_vars) AND equal?(gamma, sigma, set_vars) IMPLIES
--  equal?(delta, sigma, set_vars)
theorem equal_sub_res_trans : equal_p delta gamma set_vars ∧ equal_p gamma sigma set_vars → equal_p delta sigma set_vars := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_sub_res_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(sigma, sigma1, set_vars) IMPLIES
--  equal?(append(delta, sigma), append(delta, sigma1), set_vars)
theorem equal_sub_res_append : equal_p sigma sigma1 set_vars → equal_p (append delta sigma) (append delta sigma1) set_vars := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.equal_syn_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal_syn?(sigma, sigma1, vars(t)) IMPLIES subs(sigma)(t) = subs(sigma1)(t)
theorem equal_syn_t : equal_syn_p sigma sigma1 (vars t) → subs sigma(t) = subs sigma1(t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.idempotent_append_cond
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) AND
--  NOT member(X, vars(t)) AND
--   NOT member(X, dom(sigma)) AND disjoint?(vars(t), dom(sigma))
--  IMPLIES idempotent?(append(cons((X, t), null), sigma))
theorem idempotent_append_cond : idempotent_p sigma ∧
 ¬ member X (vars t) ∧
  ¬ member X (dom sigma) ∧ disjoint_p (vars t) (dom sigma) → idempotent_p (append (cons ((X, t)) null) sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_size2_map_get_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, args) AND length(args) > 1 IMPLIES
--  size2(subs(delta)(t)) <
--   sum(map(size2)(flatten(map(get_args(f))(map(subs(delta))(args)))))
theorem subs_size2_map_get_args : member t args ∧ List.length args > 1 → size2 (subs delta(t)) <
 sum (map size2(flatten (map (get_args f) (map (subs delta) (args))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_not_same_size2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(s)) AND
--  NOT s = variable(X) AND
--   (FORALL t:
--      member(t, subterms(s)) AND ac_app?(t) IMPLIES
--       num_arg(ac_sym(t), t) > 1)
--  IMPLIES size2(subs(delta)(X)) < size2(subs(delta)(s))
theorem subs_not_same_size2 : member X (vars s) ∧
 ¬ s = variable X ∧
  (∀ t, 
     member t (subterms s) ∧ ac_app_p t →
      num_arg (ac_sym t) t > 1) → size2 (subs delta(X)) < size2 (subs delta(s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_equal_impossible
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(s)) AND
--  NOT s = variable(X) AND
--   (FORALL t:
--      member(t, subterms(s)) AND ac_app?(t) IMPLIES
--       num_arg(ac_sym(t), t) > 1)
--  IMPLIES NOT equal?(subs(delta)(s), subs(delta)(variabl
theorem subs_equal_impossible : member X (vars s) ∧
 ¬ s = variable X ∧
  (∀ t, 
     member t (subterms s) ∧ ac_app_p t →
      num_arg (ac_sym t) t > 1) → ¬ equal_p (subs delta(s)) (subs delta(variable X)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_count_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL t1:
--    count(t1, get_args(f)(s), equal?) >= count(t1, get_args(f)(t), equal?))
--  IMPLIES
--  (FORALL t2:
--     count(t2, get_args(f)(subs(sigma)(s)), equal?) >=
--      count(t2, get_args(f)(subs(sigma
theorem subs_count_ge : (∀ t1, 
   count t1 (get_args f(s)) equal_p ≥ count t1 (get_args f(t)) equal_p) → (∀ t2, 
   count t2 (get_args f(subs sigma(s))) equal_p ≥
    count t2 (get_args f(subs sigma(t))) equal_p) := by
  -- === TCCs for subs_count_ge ===
  -- TCC: subs_count_ge_TCC1
  -- Statement: FORALL (G, G1: fresh_context, f: string,
  --         s: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --         sigma: sub,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_sym...
  have subs_count_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.count_subs_get_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: count(s, subs(sigma)(t), f) =
--  sum(count_vec(subs(sigma)(get_args(f)(t)), f)(s))
theorem count_subs_get_args : count s (subs sigma(t)) f = sum (count_vec (subs sigma(get_args f(t))) f(s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.count_subs_count_elem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (no_rep_args, coef) = count_elem(null, null, get_args(f)(t), equal?) IN
--   count(s, subs(sigma)(t), f) =
--    dot_product(count_vec(subs(sigma)(no_rep_args), f)(s), coef)
theorem count_subs_count_elem : fun (no_rep_args : {x : List (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) // every (fun (x : first_order_AC_term ℝ ℝ f_symbol ac_symbol)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x
                 ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (fun (x : ℝ)=> number_field_pred x ∧ real_pred x
                 ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (fun (x : f_symbol)=> True) (fun (x : ac_symbol)=> True)
             (x))
     (x)}) (coef : List ℝ) => count s (subs sigma(t)) f = dot_product (count_vec (subs sigma(no_rep_args)) f(s)) coef (count_elem null null (get_args f(t)) equal_p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.count_subs_subset_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (no_rep_args, coef) = count_elem(null, null, args1, equal?),
--     (no_rep_args1, coef1) = count_coef(args2, args1, equal?)
--   IN
--   subset_lst?(args1, args2) AND dif_elem?(args2, equal?) IMPLIES
--    d
theorem count_subs_subset_aux : fun (no_rep_args : {x : List (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) // every (fun (x : first_order_AC_term ℝ ℝ f_symbol ac_symbol)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x
                 ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (fun (x : ℝ)=> number_field_pred x ∧ real_pred x
                 ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (fun (x : f_symbol)=> True) (fun (x : ac_symbol)=> True)
             (x))
     (x)}) (coef : List ℝ) => fun (no_rep_args1 : {x : List (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) // every (fun (x : first_order_AC_term ℝ ℝ f_symbol ac_symbol)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x
                 ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (fun (x : ℝ)=> number_field_pred x ∧ real_pred x
                 ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (fun (x : f_symbol)=> True) (fun (x : ac_symbol)=> True)
             (x))
     (x)}) (coef1 : List ℝ) => subset_lst_p args1 args2 ∧ dif_elem_p args2 equal_p → dot_product (count_vec (subs sigma(no_rep_args)) f(s)) coef =
 dot_product (count_vec (subs sigma(no_rep_args1)) f(s)) coef1 (count_coef args2 args1 equal_p) (count_elem null null args1 equal_p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.count_subs_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (no_rep_args, coef) = count_coef(args1, get_args(f)(t), equal?) IN
--   subset_lst?(get_args(f)(t), args1) AND dif_elem?(args1, equal?) IMPLIES
--    count(s, subs(sigma)(t), f) =
--     dot_product(count_
theorem count_subs_subset : fun (no_rep_args : {x : List (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) // every (fun (x : first_order_AC_term ℝ ℝ f_symbol ac_symbol)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x
                 ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (fun (x : ℝ)=> number_field_pred x ∧ real_pred x
                 ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) (fun (x : f_symbol)=> True) (fun (x : ac_symbol)=> True)
             (x))
     (x)}) (coef : List ℝ) => subset_lst_p (get_args f(t)) args1 ∧ dif_elem_p args1 equal_p → count s (subs sigma(t)) f =
 dot_product (count_vec (subs sigma(no_rep_args)) f(s)) coef (count_coef args1 (get_args f(t)) equal_p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.subs_inst
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = cons((X, t), null) IN
--   equal?(subs(sigma)(variable(X)), subs(sigma)(t)) IMPLIES
--    equal?(sigma, append(sigma, delta))
theorem subs_inst : fun (delta : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => equal_p (subs sigma(variable X)) (subs sigma(t)) → equal_p sigma (append sigma delta) (cons ((X, t)) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.contained_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: contained?(delta, set_vars) AND contained?(sigma, set_vars) IMPLIES
--  contained?(append(delta, sigma), set_vars)
theorem contained_append : contained_p delta set_vars ∧ contained_p sigma set_vars → contained_p (append delta sigma) set_vars := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.contained_supset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: contained?(delta, set_vars) AND subset?(set_vars, set_vars1) IMPLIES
--  contained?(delta, set_vars1)
theorem contained_supset : contained_p delta set_vars ∧ subset_p set_vars set_vars1 → contained_p delta set_vars1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.more_general_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(sigma, sigma1) IMPLIES
--  more_general?(delta, sigma) = more_general?(delta, sigma1)
theorem more_general_equal : equal_p sigma sigma1 → more_general_p delta sigma = more_general_p delta sigma1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.more_general_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: more_general?(sigma, sigma1) IMPLIES
--  more_general?(append(sigma, delta), append(sigma1, delta))
theorem more_general_append : more_general_p sigma sigma1 → more_general_p (append sigma delta) (append sigma1 delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.more_general_supset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: more_general?(sigma, append(delta1, delta), set_vars1) AND
--  subset?(set_vars, set_vars1) AND
--   disjoint?(dom(delta1), set_vars) AND contained?(delta, set_vars)
--  IMPLIES more_general?(sigma, delta, set
theorem more_general_supset : more_general_p sigma (append delta1 delta) set_vars1 ∧
 subset_p set_vars set_vars1 ∧
  disjoint_p (dom delta1) set_vars ∧ contained_p delta set_vars → more_general_p sigma delta set_vars := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_ord
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ord(subs(rho)(t)) = ord(t)
theorem renaming_ord : ord (subs rho(t)) = ord t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_is_ac_sym
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_ac_sym?(subs(rho)(t), f) = is_ac_sym?(t, f)
theorem renaming_is_ac_sym : is_ac_sym_p (subs rho(t)) f = is_ac_sym_p t f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_ord2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (const?(t) IFF const?(subs(rho)(t)))
--  AND (var?(t) IFF var?(subs(rho)(t))) AND (t = unit IFF subs(rho)(t) = unit)
--  AND (pair?(t) IFF pair?(subs(rho)(t))) AND (app?(t) IFF app?(subs(rho)(t)))
--  AND
theorem renaming_ord2 : (const_p t ↔ const_p (subs rho(t))) ∧ (var_p t ↔ var_p (subs rho(t))) ∧ (t = unit ↔ subs rho(t) = unit)
 ∧ (pair_p t ↔ pair_p (subs rho(t))) ∧ (app_p t ↔ app_p (subs rho(t)))
 ∧ (ac_app_p t ↔ ac_app_p (subs rho(t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(subs(rho)(X))
theorem renaming_var : var_p (subs rho X) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_num_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_arg(f, subs(rho)(t)) = num_arg(f, t)
theorem renaming_num_arg : num_arg f (subs rho(t)) = num_arg f t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_select
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: select(f, subs(rho)(t))(i) = subs(rho)(select(f, t)(i))
theorem renaming_select : select f (subs rho(t)) (i) = subs rho(select f t(i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_delete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: delete(f, subs(rho)(t))(i) = subs(rho)(delete(f, t)(i))
theorem renaming_delete : delete f (subs rho(t)) (i) = subs rho(delete f t(i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND
--  subset?(vars(t), V1) AND
--   subset?(vars(s), V1) AND equal?(subs(rho)(t), subs(rho)(s))
--  IMPLIES
theorem renaming_equal : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧
 subset_p (vars t) V1 ∧
  subset_p (vars s) V1 ∧ equal_p (subs rho(t)) (subs rho(s)) → equal_p t s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_get_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: get_args(f)(subs(rho)(t)) = subs(rho)(get_args(f)(t))
theorem renaming_get_args : get_args f(subs rho(t)) = subs rho(get_args f(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_var2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND var?(s) AND member(V(s), V1) AND subset?(vars(t), V1)
--  IMPLIES
--  (member(V(subs(rho)(s)), vars(su
theorem renaming_var2 : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ var_p s ∧ member (V s) V1 ∧ subset_p (vars t) V1 → (member (V (subs rho(s))) (vars (subs rho(t))) ↔ member (V s) (vars t)) := by
  -- === TCCs for renaming_var2 ===
  -- TCC: renaming_var2_TCC1
  -- Statement: FORALL (V1: finite_set[variable], rho: renaming,
  --         s,
  --         t: first_order_AC_term[constant, variable, f_symbol, ac_symbol]):
  --   (subset?(vars(t), V1) AND
  --     member(V(s), V1) AND
  --      var?(...
  have renaming_var2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_var2_TCC1
  -- Statement: FORALL (V1: finite_set[variable], rho: renaming,
  --         s, t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   (subset?(vars(t), V1) AND
  --     member(V(s), V1) AND
  --      var?(s) AND
  --    ...
  have renaming_var2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND subset?(vars(t), V1) AND subset?(vars(args), V1)
--  IMPLIES pos?(t, args, equal?) = pos?(subs(rho)
theorem renaming_pos : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ subset_p (vars t) V1 ∧ subset_p (vars args) V1 → pos_p t args equal_p = pos_p (subs rho(t)) (subs rho(args)) equal_p := by
  -- === TCCs for renaming_pos ===
  -- TCC: renaming_pos_TCC1
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args: args,
  --         rho: renaming,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   (subset?(vars(G), V1) AND
  --     subset...
  have renaming_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_pos_TCC2
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args: args,
  --         rho: renaming,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   (subset?(vars(G), V1) AND
  --     subset...
  have renaming_pos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_count
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND subset?(vars(t), V1) AND subset?(vars(args), V1)
--  IMPLIES
--  count(subs(rho)(t), subs(rho)(args), 
theorem renaming_count : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ subset_p (vars t) V1 ∧ subset_p (vars args) V1 → count (subs rho(t)) (subs rho(args)) equal_p = count t args equal_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND subset?(vars(t), V1) AND subset?(vars(args), V1)
--  IMPLIES
--  subs(rho)(remove(args, t, equal?)) =
-- 
theorem renaming_remove : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ subset_p (vars t) V1 ∧ subset_p (vars args) V1 → subs rho(remove args t equal_p) =
 remove (subs rho(args)) (subs rho(t)) equal_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_remove_common
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND subset?(vars(args1), V1) AND subset?(vars(args2), V1)
--  IMPLIES
--  remove_common(subs(rho)(args1), 
theorem renaming_remove_common : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ subset_p (vars args1) V1 ∧ subset_p (vars args2) V1 → remove_common (subs rho(args1)) (subs rho(args2)) equal_p =
 subs rho(remove_common args1 args2 equal_p) := by
  -- === TCCs for renaming_remove_common ===
  -- TCC: renaming_remove_common_TCC1
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args1, args2: args,
  --         rho: renaming):
  --   (subset?(vars(G), V1) AND
  --     subset?(vars(args2), V1) AND
  --      subset?(vars(args1), V1) AND
  --       ...
  have renaming_remove_common_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_remove_common_TCC2
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args1, args2: args,
  --         rho: renaming):
  --   (subset?(vars(G), V1) AND
  --     subset?(vars(args2), V1) AND
  --      subset?(vars(args1), V1) AND
  --       ...
  have renaming_remove_common_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_count_elem2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND subset?(vars(args), V1)
--  IMPLIES
--  count_elem2(subs(rho)(args), equal?)`1 =
--   subs(rho)(count_ele
theorem renaming_count_elem2 : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ subset_p (vars args) V1 → count_elem2 (subs rho(args)) equal_p.1 =
 subs rho(count_elem2 args equal_p.1)
 ∧ count_elem2 (subs rho(args)) equal_p.2 = count_elem2 args equal_p.2 := by
  -- === TCCs for renaming_count_elem2 ===
  -- TCC: renaming_count_elem2_TCC1
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args: args,
  --         rho: renaming):
  --   (subset?(vars(G), V1) AND
  --     subset?(vars(args), V1) AND
  --      FORALL (X: variable):
  --        member(X, vars(...
  have renaming_count_elem2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_count_elem2_TCC2
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args: args,
  --         rho: renaming):
  --   (subset?(vars(G), V1) AND
  --     subset?(vars(args), V1) AND
  --      FORALL (X: variable):
  --        member(X, vars(...
  have renaming_count_elem2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.renaming_equal_syn
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sig1 = cons((Y, t), null),
--     sig1_ = cons((V(subs(rho)(Y)), subs(rho)(t)), null)
--   IN
--   (FORALL X:
--      member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--       NOT member(X, V1))
--    
theorem renaming_equal_syn : fun (sig1 : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (sig1_ : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ member Y V1 → equal_syn_p (append sig1_ rho) (append rho sig1) V1 (cons ((V (subs rho(Y)), subs rho(t))) null) (cons ((Y, t)) null) := by
  -- === TCCs for renaming_equal_syn ===
  -- TCC: renaming_equal_syn_TCC1
  -- Statement: FORALL (Y: variable, rho: renaming,
  --         t: first_order_AC_term[constant, variable, f_symbol, ac_symbol],
  --         sig1:
  --           (cons?[[variable,
  --                   first_order_AC_term
  --         ...
  have renaming_equal_syn_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_equal_syn_TCC1
  -- Statement: FORALL (Y: variable, rho: renaming,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --         sig1:
  --           (cons?[[variable,
  --                   nominal_AC_term
  --               ...
  have renaming_equal_syn_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.shift_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem shift_cor : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: first_order_AC_substitution.shift_append_equal_syn
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET rho1 = shift(V1, V1_, n) IN
--   (subset?(dom(rho), V1) AND subset?(vars(img(rho)), V1_)) IMPLIES
--    equal_syn?(rho, append(rho, rho1), V1)
theorem shift_append_equal_syn : fun (rho1 : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => (subset_p (dom rho) V1 ∧ subset_p (vars (img rho)) V1_) → equal_syn_p rho (append rho rho1) V1 (shift V1 V1_ n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.shift_dom
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(dom(shift(start, n, inc)),
--         list2set[nat](from_min2max[nat](start, start + n)))
theorem shift_dom : subset_p (dom (shift start n inc)) (list2set (from_min2max[nat](start, start + n))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.shift_dom2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (inc > 0 IMPLIES
--   dom(shift(start, n, inc)) =
--    list2set[nat](from_min2max[nat](start, start + n)))
--  AND (inc = 0 IMPLIES dom(shift(start, n, inc)) = emptyset)
theorem shift_dom2 : (inc > 0 →
  dom (shift start n inc) =
   list2set (from_min2max[nat](start, start + n))) ∧ (inc = 0 → dom (shift start n inc) = emptyset) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.shift_img
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(img(shift(start, n, inc))),
--         list2set[nat](from_min2max[nat](start + inc, start + inc + n)))
theorem shift_img : subset_p (vars (img (shift start n inc))) (list2set (from_min2max[nat](start + inc, start + inc + n))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.shift_img2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (inc > 0 IMPLIES
--   vars(img(shift(start, n, inc))) =
--    list2set[nat](from_min2max[nat](start + inc, start + n + inc)))
--  AND (inc = 0 IMPLIES img(shift(start, n, inc)) = emptyset)
theorem shift_img2 : (inc > 0 →
  vars (img (shift start n inc)) =
   list2set (from_min2max[nat](start + inc, start + n + inc))) ∧ (inc = 0 → img (shift start n inc) = emptyset) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.shift_renaming
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: renaming?(shift(start, n, inc))
theorem shift_renaming : renaming_p (shift start n inc) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_substitution.shift_append_renaming
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET rho1 = shift(V1, V1_, n) IN
--   (subset?(dom(rho), V1) AND subset?(vars(img(rho)), V1_)) IMPLIES
--    renaming?(append(rho, rho1))
theorem shift_append_renaming : fun (rho1 : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => (subset_p (dom rho) V1 ∧ subset_p (vars (img rho)) V1_) → renaming_p (append rho rho1) (shift V1 V1_ n) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.first_order_AC_substitution