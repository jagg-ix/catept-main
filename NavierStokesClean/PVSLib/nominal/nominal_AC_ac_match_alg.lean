-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nominal_AC_ac_match_alg
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nominal_AC_ac_match_alg

Auto-generated stubs from PVSLib theory `nominal_AC_ac_match_alg`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nominal_AC_ac_match_alg

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (DS : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (abs_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} (abstr : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (ac_app : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (ac_app_p : pvs_τ8 → Prop)
variable {pvs_τ15 : Type*} (ac_sym : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (app : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (app_p : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (append : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (apply_ac : pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (apply_sub : pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (arg : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} (at : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (body : pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (choose_eq : pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (cons : pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (contained_p : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (decompose : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (equal_syn_p : pvs_τ41 → pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} (ext : pvs_τ44 → pvs_τ45 → pvs_τ46)
variable {pvs_τ47 : Type*} (f_sym : pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} (fc : pvs_τ48 → pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (flatten : pvs_τ50 → pvs_τ51)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (fresh_p : pvs_τ52 → pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} (is_instance_p : pvs_τ54 → pvs_τ55 → pvs_τ56 → pvs_τ57 → pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} (is_sol_p : pvs_τ60 → pvs_τ61 → pvs_τ62 → pvs_τ63 → pvs_τ64 → pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} (lex3 : pvs_τ66 → pvs_τ67 → pvs_τ68 → Prop)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} (map : pvs_τ69 → pvs_τ70 → pvs_τ71)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} (max_size : pvs_τ72 → pvs_τ73)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} (member : pvs_τ74 → pvs_τ75 → Prop)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} (nice_input_p : pvs_τ76 → pvs_τ77 → pvs_τ78 → pvs_τ79 → pvs_τ80 → Prop)
variable {pvs_τ81 : Type*} {pvs_τ82 : Type*} (nominal_AC_fresh_subs_p : pvs_τ81 → pvs_τ82 → Prop)
variable {pvs_τ83 : Type*} (null_p : pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} (num_max_size : pvs_τ84 → pvs_τ85)
variable {pvs_τ86 : Type*} {pvs_τ87 : Type*} (reverse : pvs_τ86 → pvs_τ87)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (same_func_p : pvs_τ88 → pvs_τ89 → Prop)
variable {pvs_τ90 : Type*} {pvs_τ91 : Type*} {pvs_τ92 : Type*} {pvs_τ93 : Type*} {pvs_τ94 : Type*} {pvs_τ95 : Type*} {pvs_τ96 : Type*} (solves_p : pvs_τ90 → pvs_τ91 → pvs_τ92 → pvs_τ93 → pvs_τ94 → pvs_τ95 → pvs_τ96 → Prop)
variable {pvs_τ97 : Type*} {pvs_τ98 : Type*} (subset_p : pvs_τ97 → pvs_τ98 → Prop)
variable {pvs_τ99 : Type*} {pvs_τ100 : Type*} {pvs_τ101 : Type*} (union : pvs_τ99 → pvs_τ100 → pvs_τ101)
variable {pvs_τ102 : Type*} {pvs_τ103 : Type*} {pvs_τ104 : Type*} (var_inputs_p : pvs_τ102 → pvs_τ103 → pvs_τ104 → Prop)
variable {pvs_τ105 : Type*} (var_p : pvs_τ105 → Prop)
variable {pvs_τ106 : Type*} {pvs_τ107 : Type*} {pvs_τ108 : Type*} (vars : pvs_τ106 → pvs_τ107 → pvs_τ108)
variable {pvs_τ109 : Type*} {pvs_τ110 : Type*} (well_formed_p : pvs_τ109 → pvs_τ110 → Prop)

-- pvs2lean: nominal_AC_ac_match_alg.ac_match
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input IN
--   IF null?(P) THEN cons((G, sigma), null)
--   ELSE LET (up, P1) = choose_eq(P), (t, s) = up IN
--          CASES t
--            OF at(a):
--                 IF s = at(a) THEN
axiom ac_match {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_sound_inst
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input IN
--   (NOT null?(P) AND
--     LET (up, P1) = choose_eq(P), (t, s) = up IN
--       var?(t) AND
--        NOT member(V(t), vars(s)) AND
--         LET sigma1 = cons((V(t), ext(reve
theorem ac_match_sound_inst : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => (¬ null_p P ∧
  (let (up, P1) := choose_eq P, (t, s) = up; var_p t ∧
     ¬ member (V t) (vars s) ∧
      (let sigma1 := cons ((V t, ext (reverse (p t)) (s))) null,
          (G1, bool1) = nominal_AC_fresh_subs_p sigma1 G; let new_G := append G1 G; let new_sigma := append sigma1 sigma; let new_P := apply_sub sigma1 P1; is_sol_p Delta delta(new_G, new_P, new_sigma, PV)))) → is_sol_p Delta delta(G, P, sigma, PV) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_sound_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input IN
--   (NOT null?(P) AND
--     LET (up, P1) = choose_eq(P), (t, s) = up IN
--            abs?(t) AND abs?(s) AND decompose(body(t), body(s))`2
--        AND (abstr(t) /= abstr(s
theorem ac_match_sound_abs : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => (¬ null_p P ∧
  (let (up, P1) := choose_eq P, (t, s) = up; abs_p t ∧ abs_p s ∧ decompose (body t) (body s).2
     ∧ (abstr t ≠ abstr s)
     ∧ (let (G1, bool1) := fresh_p (abstr t) (body s); let new_G := append G1 G; let pi := cons ((abstr t, abstr s)) null; let dec_P := decompose (body t) (ext pi(body s)).1; let new_P := append dec_P P1; bool1 ∧ is_sol_p Delta delta(new_G, new_P, sigma, PV)))) → is_sol_p Delta delta(G, P, sigma, PV) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_sound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (Gamma, P, sigma, V, PV) = input IN
--   is_sol?(Delta, delta)(Gamma1, null, sigma1, PV) AND
--    member((Gamma1, sigma1), ac_match(input))
--    IMPLIES is_sol?(Delta, delta)(Gamma, P, sigma, PV)
theorem ac_match_sound : fun (Gamma : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => is_sol_p Delta delta(Gamma1, null, sigma1, PV) ∧
 member ((Gamma1, sigma1)) (ac_match input) → is_sol_p Delta delta(Gamma, P, sigma, PV) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_sound_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_sol?(Delta, delta)(Gamma, null, sigma, vars(s)) AND
--  well_formed?(t, s) AND
--   member((Gamma, sigma),
--          ac_match(null, cons((t, s), null), null, vars(t, s), vars(s)))
--  IMPLIES is_sol?(Delta, 
theorem ac_match_sound_cor : is_sol_p Delta delta(Gamma, null, sigma, vars s) ∧
 well_formed_p t s ∧
  member ((Gamma, sigma)) (ac_match null (cons ((t, s)) null) null (vars t s) (vars s)) → is_sol_p Delta delta(null, cons ((t, s)) null, null, vars s) := by
  -- === TCCs for ac_match_sound_cor ===
  -- TCC: ac_match_sound_cor_TCC1
  -- Statement: FORALL (Delta, Gamma: fresh_context, delta: sub,
  --         s: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --         sigma: sub,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol...
  have ac_match_sound_cor_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_sound_equality_check
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_sol?(Delta, delta)(Gamma, null, sigma, vars(t, s)) AND
--  well_formed?(t, s) AND
--   member((Gamma, sigma),
--          ac_match(null, cons((t, s), null), null, vars(t, s), vars(t, s)))
--  IMPLIES is_sol?(D
theorem ac_match_sound_equality_check : is_sol_p Delta delta(Gamma, null, sigma, vars t s) ∧
 well_formed_p t s ∧
  member ((Gamma, sigma)) (ac_match null (cons ((t, s)) null) null (vars t s) (vars t s)) → is_sol_p Delta delta(null, cons ((t, s)) null, null, vars t s) := by
  -- === TCCs for ac_match_sound_equality_check ===
  -- TCC: ac_match_sound_equality_check_TCC1
  -- Statement: FORALL (Delta, Gamma: fresh_context, delta: sub,
  --         s: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --         sigma: sub,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol...
  have ac_match_sound_equality_check_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_vars_ctx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input IN
--   member((G1, sigma1), ac_match(input)) IMPLIES
--    subset?(vars(G1), union(vars(G), vars(P)))
theorem ac_match_vars_ctx : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => member ((G1, sigma1)) (ac_match input) → subset_p (vars G1) (union (vars G) (vars P)) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness_inst
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input, (up, P1) = choose_eq(P), (t, s) = up IN
--   is_sol?(Delta, delta)(G, P, sigma, PV) AND
--    NOT null?(P) AND var?(t) AND NOT member(V(t), vars(s))
--    IMPLIES
--    (LET sigm
theorem ac_match_completeness_inst : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => is_sol_p Delta delta(G, P, sigma, PV) ∧
 ¬ null_p P ∧ var_p t ∧ ¬ member (V t) (vars s) → ((let sigma1 := cons ((V t, ext (reverse (p t)) (s))) null,
     (G1, bool1) = nominal_AC_fresh_subs_p sigma1 G; let new_G := append G1 G; let new_sigma := append sigma1 sigma; let new_P := apply_sub sigma1 P1; bool1 → is_sol_p Delta delta(new_G, new_P, new_sigma, PV))) up (choose_eq P) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness_inst2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input, (up, P1) = choose_eq(P), (t, s) = up IN
--   is_sol?(Delta, delta)(G, P, sigma, PV) AND
--    NOT null?(P) AND var?(t) AND NOT member(V(t), vars(s))
--    IMPLIES
--    (LET sigm
theorem ac_match_completeness_inst2 : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => is_sol_p Delta delta(G, P, sigma, PV) ∧
 ¬ null_p P ∧ var_p t ∧ ¬ member (V t) (vars s) → ((let sigma1 := cons ((V t, ext (reverse (p t)) (s))) null,
     (G1, bool1) = nominal_AC_fresh_subs_p sigma1 G; let new_G := append G1 G; let new_sigma := append sigma1 sigma; let new_P := apply_sub sigma1 P1; bool1)) up (choose_eq P) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness_inst3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input, (up, P1) = choose_eq(P), (t, s) = up IN
--        is_sol?(Delta, delta)(G, P, sigma, PV) AND (NOT null?(P)) AND var?(t)
--    AND var?(s) AND (V(t) = V(s))
--    IMPLIES
--    (L
theorem ac_match_completeness_inst3 : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => is_sol_p Delta delta(G, P, sigma, PV) ∧ (¬ null_p P) ∧ var_p t
 ∧ var_p s ∧ (V t = V s) → ((let new_G := append (fc (DS (p t) (p s)) (V t)) G; is_sol_p Delta delta(new_G, P1, sigma, PV))) up (choose_eq P) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma, V, PV) = input, (up, P1) = choose_eq(P), (t, s) = up IN
--        is_sol?(Delta, delta)(G, P, sigma, PV) AND (NOT null?(P)) AND abs?(t)
--    AND abs?(s) AND (abstr(t) /= abstr(s)) AND dec
theorem ac_match_completeness_abs : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => is_sol_p Delta delta(G, P, sigma, PV) ∧ (¬ null_p P) ∧ abs_p t
 ∧ abs_p s ∧ (abstr t ≠ abstr s) ∧ decompose (body t) (body s).2 → ((let (G1, bool1) := fresh_p (abstr t) (body s); let new_G := append G1 G; let pi := cons ((abstr t, abstr s)) null; let dec_P := decompose (body t) (ext pi(body s)).1; let new_P := append dec_P P1; bool1 ∧ is_sol_p Delta delta(new_G, new_P, sigma, PV))) up (choose_eq P) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (Gamma, P, sigma, V, PV) = input IN
--   is_sol?(Delta, delta)(Gamma, P, sigma, PV) AND
--    contained?(delta, V) AND subset?(vars(Delta), V)
--    IMPLIES
--    EXISTS Gamma1, sigma1:
--      member((Gamma1, s
theorem ac_match_completeness : fun (Gamma : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => is_sol_p Delta delta(Gamma, P, sigma, PV) ∧
 contained_p delta V ∧ subset_p (vars Delta) V → ∃ Gamma1 sigma1, 
  member ((Gamma1, sigma1)) (ac_match input) ∧
   solves_p Delta delta(Gamma1, null, sigma1, V, PV) input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness_match_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      is_sol?(Delta, delta)(null, cons((t, s), null), null, vars(s))
--  AND contained?(delta, V) AND subset?(vars(Delta), V)
--  AND subset?(vars(t, s), V) AND well_formed?(t, s)
--  IMPLIES
--  EXISTS Gamma1, si
theorem ac_match_completeness_match_aux : is_sol_p Delta delta(null, cons ((t, s)) null, null, vars s)
 ∧ contained_p delta V ∧ subset_p (vars Delta) V
 ∧ subset_p (vars t s) V ∧ well_formed_p t s → ∃ Gamma1 sigma1, 
  member ((Gamma1, sigma1)) (ac_match null (cons ((t, s)) null) null V (vars s))
   ∧ is_instance_p Delta delta(Gamma1, sigma1, V, vars s) := by
  -- === TCCs for ac_match_completeness_match_aux ===
  -- TCC: ac_match_completeness_match_aux_TCC1
  -- Statement: FORALL (Delta: fresh_context, V: finite_set[variable], delta: sub,
  --         s, t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --        well_formed?(t, s) AND subset?(vars(t, s), V)
  --   ...
  have ac_match_completeness_match_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness_eq_check_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      is_sol?(Delta, delta)(null, cons((t, s), null), null, vars(t, s))
--  AND contained?(delta, V) AND subset?(vars(Delta), V)
--  AND subset?(vars(t, s), V) AND well_formed?(t, s)
--  IMPLIES
--  EXISTS Gamma1,
theorem ac_match_completeness_eq_check_aux : is_sol_p Delta delta(null, cons ((t, s)) null, null, vars t s)
 ∧ contained_p delta V ∧ subset_p (vars Delta) V
 ∧ subset_p (vars t s) V ∧ well_formed_p t s → ∃ Gamma1 sigma1, 
  member ((Gamma1, sigma1)) (ac_match null (cons ((t, s)) null) null V (vars t s))
   ∧ is_instance_p Delta delta(Gamma1, sigma1, V, vars t s) := by
  -- === TCCs for ac_match_completeness_eq_check_aux ===
  -- TCC: ac_match_completeness_eq_check_aux_TCC1
  -- Statement: FORALL (Delta: fresh_context, V: finite_set[variable], delta: sub,
  --         s, t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --        well_formed?(t, s) AND subset?(vars(t, s), V)
  --   ...
  have ac_match_completeness_eq_check_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_var_inp_choose_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P) IN
--   nice_input?(G, P, sigma_, V_, PV) AND
--    nice_input?(G, P, sigma, V, PV) AND
--     var_inputs?((G, P, sigma, V, PV), (G, P, sigma_, V_, PV), FV) AND
--      NOT null?(P)
--   
theorem ac_match_var_inp_choose_eq : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => nice_input_p G P sigma_ V_ PV ∧
 nice_input_p G P sigma V PV ∧
  var_inputs_p ((G, P, sigma, V, PV)) ((G, P, sigma_, V_, PV)) FV ∧
   ¬ null_p P → var_inputs_p ((G, P1, sigma, V, PV)) ((G, P1, sigma_, V_, PV)) FV (choose_eq P) := by
  -- === TCCs for ac_match_var_inp_choose_eq ===
  -- TCC: ac_match_var_inp_choose_eq_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V_: finite_set[variable], sigma, sigma_: sub, up: unif_pair,
  --         P1: unif_prb):
  --        (up = choose_eq(P)`1) AND ...
  have ac_match_var_inp_choose_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ac_match_var_inp_choose_eq_TCC2
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V_: finite_set[variable], sigma, sigma_: sub, up: unif_pair,
  --         P1: unif_prb):
  --        (up = choose_eq(P)`1) AND ...
  have ac_match_var_inp_choose_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_var_inp_var
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        nice_input?(G, P, sigma_, V_, PV) AND nice_input?(G, P, sigma, V, PV)
--    AND var_inputs?((G, P, sigma, V, PV), (G, P, sigma_, V_, PV), FV)
--    AND (NO
theorem ac_match_var_inp_var : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => nice_input_p G P sigma_ V_ PV ∧ nice_input_p G P sigma V PV
 ∧ var_inputs_p ((G, P, sigma, V, PV)) ((G, P, sigma_, V_, PV)) FV
 ∧ (¬ null_p P) ∧ var_p t ∧ ¬ member (V t) (vars s) → ((let sigma1 := cons ((V t, ext (reverse (p t)) (s))) null,
     (G1, bool1) = nominal_AC_fresh_subs_p sigma1 G; let new_G := append G1 G; let new_P := apply_sub sigma1 P1; var_inputs_p ((new_G, new_P, append sigma1 sigma, V, PV)) ((new_G, new_P, append sigma1 sigma_, V_, PV)) FV)) up (choose_eq P) := by
  -- === TCCs for ac_match_var_inp_var ===
  -- TCC: ac_match_var_inp_var_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V1, V_: finite_set[variable], sigma, sigma_: sub,
  --         up: unif_pair, P1: unif_prb):
  --   up = choose_eq(P)`1 AND P1 = c...
  have ac_match_var_inp_var_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ac_match_var_inp_var_TCC2
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V1, V_: finite_set[variable], sigma, sigma_: sub,
  --         up: unif_pair, P1: unif_prb):
  --   up = choose_eq(P)`1 AND P1 = c...
  have ac_match_var_inp_var_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_var_inp_var2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        nice_input?(G, P, sigma_, V_, PV) AND nice_input?(G, P, sigma, V, PV)
--    AND var_inputs?((G, P, sigma, V, PV), (G, P, sigma_, V_, PV), FV)
--    AND (NO
theorem ac_match_var_inp_var2 : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => nice_input_p G P sigma_ V_ PV ∧ nice_input_p G P sigma V PV
 ∧ var_inputs_p ((G, P, sigma, V, PV)) ((G, P, sigma_, V_, PV)) FV
 ∧ (¬ null_p P) ∧ var_p t ∧ var_p s → ((let G2 := fc (DS (p t) (p s)) (V t); let new_G := append G2 G; var_inputs_p ((new_G, P1, sigma, V, PV)) ((new_G, P1, sigma_, V_, PV)) FV)) up (choose_eq P) := by
  -- === TCCs for ac_match_var_inp_var2 ===
  -- TCC: ac_match_var_inp_var2_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V1, V_: finite_set[variable], sigma, sigma_: sub,
  --         up: unif_pair, P1: unif_prb):
  --   up = choose_eq(P)`1 AND P1 = c...
  have ac_match_var_inp_var2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ac_match_var_inp_var2_TCC2
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V1, V_: finite_set[variable], sigma, sigma_: sub,
  --         up: unif_pair, P1: unif_prb):
  --   up = choose_eq(P)`1 AND P1 = c...
  have ac_match_var_inp_var2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_var_inp_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        nice_input?(G, P, sigma_, V_, PV) AND nice_input?(G, P, sigma, V, PV)
--    AND var_inputs?((G, P, sigma, V, PV), (G, P, sigma_, V_, PV), FV)
--    AND (NO
theorem ac_match_var_inp_app : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => nice_input_p G P sigma_ V_ PV ∧ nice_input_p G P sigma V PV
 ∧ var_inputs_p ((G, P, sigma, V, PV)) ((G, P, sigma_, V_, PV)) FV
 ∧ (¬ null_p P) ∧ app_p t ∧ app_p s ∧ same_func_p t s → ((let dec_P := decompose (arg t) (arg s).1; let new_P := append dec_P P1; var_inputs_p ((G, new_P, sigma, V, PV)) ((G, new_P, sigma_, V_, PV)) FV)) up (choose_eq P) := by
  -- === TCCs for ac_match_var_inp_app ===
  -- TCC: ac_match_var_inp_app_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V_: finite_set[variable], sigma, sigma_: sub, up: unif_pair,
  --         P1: unif_prb):
  --   up = choose_eq(P)`1 AND P1 = ch...
  have ac_match_var_inp_app_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ac_match_var_inp_app_TCC2
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V_: finite_set[variable], sigma, sigma_: sub, up: unif_pair,
  --         P1: unif_prb):
  --   up = choose_eq(P)`1 AND P1 = ch...
  have ac_match_var_inp_app_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_var_inp_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        nice_input?(G, P, sigma_, V_, PV) AND nice_input?(G, P, sigma, V, PV)
--    AND var_inputs?((G, P, sigma, V, PV), (G, P, sigma_, V_, PV), FV)
--    AND (NO
theorem ac_match_var_inp_abs : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => nice_input_p G P sigma_ V_ PV ∧ nice_input_p G P sigma V PV
 ∧ var_inputs_p ((G, P, sigma, V, PV)) ((G, P, sigma_, V_, PV)) FV
 ∧ (¬ null_p P) ∧ abs_p t ∧ abs_p s → ((let dec_P := decompose (body t) (body s).1; let new_P := append dec_P P1; var_inputs_p ((G, new_P, sigma, V, PV)) ((G, new_P, sigma_, V_, PV)) FV)) up (choose_eq P) := by
  -- === TCCs for ac_match_var_inp_abs ===
  -- TCC: ac_match_var_inp_abs_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V_: finite_set[variable], sigma, sigma_: sub, up: unif_pair,
  --         P1: unif_prb):
  --   up = choose_eq(P)`1 AND P1 = ch...
  have ac_match_var_inp_abs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ac_match_var_inp_abs_TCC2
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V_: finite_set[variable], sigma, sigma_: sub, up: unif_pair,
  --         P1: unif_prb):
  --   up = choose_eq(P)`1 AND P1 = ch...
  have ac_match_var_inp_abs_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_var_inp_abs2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (up, P1) = choose_eq(P), (t, s) = up IN
--        nice_input?(G, P, sigma_, V_, PV) AND nice_input?(G, P, sigma, V, PV)
--    AND var_inputs?((G, P, sigma, V, PV), (G, P, sigma_, V_, PV), FV)
--    AND (NO
theorem ac_match_var_inp_abs2 : fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => nice_input_p G P sigma_ V_ PV ∧ nice_input_p G P sigma V PV
 ∧ var_inputs_p ((G, P, sigma, V, PV)) ((G, P, sigma_, V_, PV)) FV
 ∧ (¬ null_p P) ∧ abs_p t ∧ abs_p s → ((let G1 := fresh_p (abstr t) (body s).1; let new_G := append G1 G; let dec_P := decompose (body t) (ext pi(body s)).1; let new_P := append dec_P P1; var_inputs_p ((new_G, new_P, sigma, V, PV)) ((new_G, new_P, sigma_, V_, PV)) FV)) up (choose_eq P) := by
  -- === TCCs for ac_match_var_inp_abs2 ===
  -- TCC: ac_match_var_inp_abs2_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V_: finite_set[variable], pi: perm, sigma, sigma_: sub,
  --         up: unif_pair, P1: unif_prb):
  --   up = choose_eq(P)`1 A...
  have ac_match_var_inp_abs2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ac_match_var_inp_abs2_TCC2
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V_: finite_set[variable], pi: perm, sigma, sigma_: sub,
  --         up: unif_pair, P1: unif_prb):
  --   up = choose_eq(P)`1 A...
  have ac_match_var_inp_abs2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_var_inputs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var_inputs?(input, input_, FV) AND
--  member((Gamma1, sigma1_), ac_match(input_))
--  IMPLIES
--  EXISTS sigma1:
--    member((Gamma1, sigma1), ac_match(input)) AND
--     equal_syn?(sigma1_, sigma1, FV)
theorem ac_match_var_inputs : var_inputs_p input input_ FV ∧
 member ((Gamma1, sigma1_)) (ac_match input_) → ∃ sigma1, 
  member ((Gamma1, sigma1)) (ac_match input) ∧
   equal_syn_p sigma1_ sigma1 FV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness_match
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_sol?(Delta, delta)(null, cons((t, s), null), null, vars(s)) AND
--  well_formed?(t, s)
--  IMPLIES
--  EXISTS Gamma1, sigma1:
--    member((Gamma1, sigma1),
--           ac_match(null, cons((t, s), null), null, v
theorem ac_match_completeness_match : is_sol_p Delta delta(null, cons ((t, s)) null, null, vars s) ∧
 well_formed_p t s → ∃ Gamma1 sigma1, 
  member ((Gamma1, sigma1)) (ac_match null (cons ((t, s)) null) null (vars t s) (vars s))
   ∧ is_instance_p Delta delta(Gamma1, sigma1, vars t s, vars s) := by
  -- === TCCs for ac_match_completeness_match ===
  -- TCC: ac_match_completeness_match_TCC1
  -- Statement: FORALL (Delta: fresh_context, delta: sub,
  --         s, t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   well_formed?(t, s) AND
  --    is_sol?(Delta, delta)(null, cons((t, s), null), null...
  have ac_match_completeness_match_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_ac_match_alg.ac_match_completeness_equality
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_sol?(Delta, delta)(null, cons((t, s), null), null, vars(t, s)) AND
--  well_formed?(t, s)
--  IMPLIES
--  EXISTS Gamma1, sigma1:
--    member((Gamma1, sigma1),
--           ac_match(null, cons((t, s), null), null
theorem ac_match_completeness_equality : is_sol_p Delta delta(null, cons ((t, s)) null, null, vars t s) ∧
 well_formed_p t s → ∃ Gamma1 sigma1, 
  member ((Gamma1, sigma1)) (ac_match null (cons ((t, s)) null) null (vars t s) (vars t s))
   ∧ is_instance_p Delta delta(Gamma1, sigma1, vars t s, vars t s) := by
  -- === TCCs for ac_match_completeness_equality ===
  -- TCC: ac_match_completeness_equality_TCC1
  -- Statement: FORALL (Delta: fresh_context, delta: sub,
  --         s, t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   well_formed?(t, s) AND
  --    is_sol?(Delta, delta)(null, cons((t, s), null), null...
  have ac_match_completeness_equality_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.nominal_AC_ac_match_alg