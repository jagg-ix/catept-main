-- Auto-generated by pvs2lean/translator.py
-- PVS theory: first_order_AC_unification
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: first_order_AC_unification

Auto-generated stubs from PVSLib theory `first_order_AC_unification`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.first_order_AC_unification

noncomputable section

variable {pvs_τ1 : Type*} (ac_app_p : pvs_τ1 → Prop)
variable {pvs_τ3 : Type*} (ac_sym : pvs_τ3 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (app : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (append : pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (are_ac_p : pvs_τ14 → pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} (car : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (cdr : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (cons : pvs_τ20 → pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (count : pvs_τ23 → pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (disjoint_p : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (dom : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (equal_p : pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (equal_syn_p : pvs_τ35 → pvs_τ36 → pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} (equivalence_p : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} (first_proj : pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (flatten : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (get_args : pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (img : pvs_τ44 → pvs_τ45)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} (is_finite : pvs_τ46 → pvs_τ47)
variable {pvs_τ48 : Type*} (length : pvs_τ48 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (list2set : pvs_τ51 → pvs_τ52)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} (no_common_args_p : pvs_τ57 → pvs_τ58 → Prop)
variable {pvs_τ61 : Type*} (null_p : pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (num_arg : pvs_τ62 → pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} (ord : pvs_τ64 → Prop)
variable {pvs_τ65 : Type*} (pair_p : pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} (pareate : pvs_τ66 → pvs_τ67 → pvs_τ68)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} (remove_common : pvs_τ69 → pvs_τ70 → pvs_τ71 → Prop)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} {pvs_τ76 : Type*} (same_count_p : pvs_τ74 → pvs_τ75 → pvs_τ76 → Prop)
variable {pvs_τ77 : Type*} {pvs_τ78 : Type*} (sec_proj : pvs_τ77 → pvs_τ78)
variable {pvs_τ83 : Type*} {pvs_τ84 : Type*} {pvs_τ85 : Type*} (subs : pvs_τ83 → pvs_τ84 → pvs_τ85)
variable {pvs_τ90 : Type*} {pvs_τ91 : Type*} (term1 : pvs_τ90 → pvs_τ91)
variable {pvs_τ92 : Type*} {pvs_τ93 : Type*} (term2 : pvs_τ92 → pvs_τ93)
variable {pvs_τ96 : Type*} {pvs_τ97 : Type*} {pvs_τ98 : Type*} (union : pvs_τ96 → pvs_τ97 → pvs_τ98)
variable {pvs_τ99 : Type*} (var_p : pvs_τ99 → Prop)
variable {pvs_τ100 : Type*} {pvs_τ101 : Type*} (pvs_variable : pvs_τ100 → pvs_τ101)

-- pvs2lean: first_order_AC_unification.member
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (t = unif_pair`1 OR t = unif_pair`2)
axiom member {pvs_τ55 : Type*} {pvs_τ56 : Type*} : pvs_τ55 → pvs_τ56 → Prop

-- pvs2lean: first_order_AC_unification.lhs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: first_proj(unif_prb)
axiom lhs {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ49 → pvs_τ50

-- pvs2lean: first_order_AC_unification.rhs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sec_proj(unif_prb)
axiom rhs {pvs_τ72 : Type*} {pvs_τ73 : Type*} : pvs_τ72 → pvs_τ73

-- pvs2lean: first_order_AC_unification.no_var?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t: member(t, unif_prb) IMPLIES NOT var?(t)
noncomputable def no_var_p : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL t: member(t, unif_prb) IMPLIES NOT var?(t)

-- pvs2lean: first_order_AC_unification.no_var_pair?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t: member(t, unif_prb) IMPLIES NOT var?(t) AND NOT pair?(t)
axiom no_var_pair_p {pvs_τ60 : Type*} : pvs_τ60 → Prop

-- pvs2lean: first_order_AC_unification.no_pair?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t: member(t, unif_prb) IMPLIES NOT pair?(t)
axiom no_pair_p {pvs_τ59 : Type*} : pvs_τ59 → Prop

-- pvs2lean: first_order_AC_unification.vars
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN union(vars(t), vars(s))
axiom vars {pvs_τ102 : Type*} {pvs_τ103 : Type*} {pvs_τ104 : Type*} : pvs_τ102 → pvs_τ103 → pvs_τ104

-- pvs2lean: first_order_AC_unification.subterms
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN union(subterms(t), subterms(s))
axiom subterms {pvs_τ88 : Type*} {pvs_τ89 : Type*} : pvs_τ88 → pvs_τ89 → Prop

-- pvs2lean: first_order_AC_unification.unifies?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN equal?(subs(sigma)(t), subs(sigma)(s))
axiom unifies_p {pvs_τ94 : Type*} {pvs_τ95 : Type*} : pvs_τ94 → pvs_τ95 → Prop

-- pvs2lean: first_order_AC_unification.unifiable?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS sigma: unifies?(sigma, (t, s))
noncomputable def unifiable_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS sigma: unifies?(sigma, (t, s))

-- pvs2lean: first_order_AC_unification.apply_sub
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN (subs(sigma)(t), subs(sigma)(s))
axiom apply_sub {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: first_order_AC_unification.size
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN size(t) + size(s)
axiom size {pvs_τ81 : Type*} {pvs_τ82 : Type*} : pvs_τ81 → pvs_τ82 → Prop

-- pvs2lean: first_order_AC_unification.lst_vars
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN append(lst_vars(t), lst_vars(s))
axiom lst_vars {pvs_τ53 : Type*} {pvs_τ54 : Type*} : pvs_τ53 → pvs_τ54

-- pvs2lean: first_order_AC_unification.ac_prb?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN
--   ac_app?(t) AND ac_app?(s) AND ac_sym(t) = ac_sym(s)
axiom ac_prb_p {pvs_τ2 : Type*} : pvs_τ2 → Prop

-- pvs2lean: first_order_AC_unification.all_ac_prb?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL unif_pair: member(unif_pair, unif_prb) IMPLIES ac_prb?(unif_pair)
axiom all_ac_prb_p {pvs_τ4 : Type*} : pvs_τ4 → Prop

-- pvs2lean: first_order_AC_unification.temp?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(unif_pair, unif_prb) AND
--  EXISTS X:
--    member(variable(X), unif_pair) AND
--     count(variable(X), append(lhs(unif_prb), rhs(unif_prb)), =) = 1
noncomputable def temp_p : sorry := by
  sorry  -- pvs2lean: definition stub for member(unif_pair, unif_prb) AND  EXISTS X:    member(variable(X), unif_pair) AND

-- pvs2lean: first_order_AC_unification.select_greater
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF size(t) >= size(s) THEN t ELSE s ENDIF
axiom select_greater {pvs_τ79 : Type*} {pvs_τ80 : Type*} : pvs_τ79 → pvs_τ80 → Prop

-- pvs2lean: first_order_AC_unification.well_formed?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(unif_pair`1) AND well_formed?(unif_pair`2)
axiom well_formed_p {pvs_τ105 : Type*} {pvs_τ106 : Type*} : pvs_τ105 → pvs_τ106 → Prop

-- pvs2lean: first_order_AC_unification.alm_well_formed?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t: member(t, unif_prb) IMPLIES alm_well_formed?(t)
axiom alm_well_formed_p {pvs_τ5 : Type*} : pvs_τ5 → Prop

-- pvs2lean: first_order_AC_unification.subset?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL unif_pair:
--   member(unif_pair, unif_prb) IMPLIES member(unif_pair, unif_prb1)
axiom subset_p {pvs_τ86 : Type*} {pvs_τ87 : Type*} : pvs_τ86 → pvs_τ87 → Prop

-- pvs2lean: first_order_AC_unification.decompose
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF pair?(t) AND pair?(s)
--   THEN LET (unif_prb1, bool1) = decompose(term1(t), term1(s)),
--            (unif_prb2, bool2) = decompose(term2(t), term2(s))
--          IN (append(unif_prb1, unif_prb2), bool1 A
axiom decompose {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ26 → pvs_τ27 → Prop

-- pvs2lean: first_order_AC_unification.first_proj_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(args1) = length(args2) IMPLIES
--  first_proj(pareate(args1, args2)) = args1
theorem first_proj_unif_prb : List.length args1 = List.length args2 → first_proj (pareate args1 args2) = args1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.member_unif_prb_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, unif_prb) IFF
--  EXISTS unif_pair:
--    (t = unif_pair`1 OR t = unif_pair`2) AND member(unif_pair, unif_prb)
theorem member_unif_prb_equiv : member t unif_prb ↔ ∃ unif_pair, 
  (t = unif_pair.1 ∨ t = unif_pair.2) ∧ member unif_pair unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.member_unif_prb_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, append(unif_prb1, unif_prb2)) IFF
--  (member(t, unif_prb1) OR member(t, unif_prb2))
theorem member_unif_prb_append : member t (append unif_prb1 unif_prb2) ↔ (member t unif_prb1 ∨ member t unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.member_lst_unif_prb_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, lst_unif_prb) IFF
--  EXISTS unif_prb: member(t, unif_prb) AND member(unif_prb, lst_unif_prb)
theorem member_lst_unif_prb_equiv : member t lst_unif_prb ↔ ∃ unif_prb,  member t unif_prb ∧ member unif_prb lst_unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.is_finite_mem_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite({t | member(t, unif_prb)})
theorem is_finite_mem_unif_prb : is_finite (fun (t : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => member t unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.member_lhs_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, unif_prb) IFF
--  (member(t, lhs(unif_prb)) OR member(t, rhs(unif_prb)))
theorem member_lhs_rhs : member t unif_prb ↔ (member t (lhs unif_prb) ∨ member t (rhs unif_prb)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.member_lhs_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, lhs(pareate(args1, args2))) IMPLIES member(t, args1)
theorem member_lhs_pareate : member t (lhs (pareate args1 args2)) → member t args1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_unification.member_rhs_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, rhs(pareate(args1, args2))) IMPLIES member(t, args2)
theorem member_rhs_pareate : member t (rhs (pareate args1 args2)) → member t args2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_unification.vars_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(unif_prb)) IFF
--  EXISTS t: member(t, unif_prb) AND member(X, vars(t))
theorem vars_unif_prb : member X (vars unif_prb) ↔ ∃ t,  member t unif_prb ∧ member X (vars t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.vars_lst_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(lst_unif_prb)) IFF
--  EXISTS t: member(t, lst_unif_prb) AND member(X, vars(t))
theorem vars_lst_unif_prb : member X (vars lst_unif_prb) ↔ ∃ t,  member t lst_unif_prb ∧ member X (vars t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.lst_vars_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: lst_vars(append(unif_prb, unif_prb1)) =
--  append(lst_vars(unif_prb), lst_vars(unif_prb1))
theorem lst_vars_append : lst_vars (append unif_prb unif_prb1) = append (lst_vars unif_prb) (lst_vars unif_prb1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.vars_lst_vars_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vars(unif_prb) = list2set(lst_vars(unif_prb))
theorem vars_lst_vars_unif_prb : vars unif_prb = list2set (lst_vars unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.vars_lst_vars_flatten_lst_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vars(lst_unif_prb) = list2set(lst_vars(flatten(lst_unif_prb)))
theorem vars_lst_vars_flatten_lst_unif_prb : vars lst_unif_prb = list2set (lst_vars (flatten lst_unif_prb)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.no_var_pair_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_var_pair?(append(unif_prb, unif_prb1)) =
--  (no_var_pair?(unif_prb) AND no_var_pair?(unif_prb1))
theorem no_var_pair_append : no_var_pair_p (append unif_prb unif_prb1) = (no_var_pair_p unif_prb ∧ no_var_pair_p unif_prb1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.no_pair_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_pair?(append(unif_prb, unif_prb1)) =
--  (no_pair?(unif_prb) AND no_pair?(unif_prb1))
theorem no_pair_append : no_pair_p (append unif_prb unif_prb1) = (no_pair_p unif_prb ∧ no_pair_p unif_prb1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.all_ac_prb_no_var_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: all_ac_prb?(unif_prb) IMPLIES no_var_pair?(unif_prb)
theorem all_ac_prb_no_var_pair : all_ac_prb_p unif_prb → no_var_pair_p unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.all_ac_prb_is_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: all_ac_prb?(unif_prb) AND member(t, unif_prb) IMPLIES ac_app?(t)
theorem all_ac_prb_is_ac : all_ac_prb_p unif_prb ∧ member t unif_prb → ac_app_p t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.size_app_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: size(append(unif_prb1, unif_prb2)) = size(unif_prb1) + size(unif_prb2)
theorem size_app_unif_prb : size (append unif_prb1 unif_prb2) = size unif_prb1 + size unif_prb2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.subterms_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(unif_prb)) IFF
--  EXISTS unif_pair:
--    member(t, subterms(unif_pair)) AND member(unif_pair, unif_prb)
theorem subterms_mem : member t (subterms unif_prb) ↔ ∃ unif_pair, 
  member t (subterms unif_pair) ∧ member unif_pair unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.subterms_mem_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, subterms(unif_prb)) IFF
--  EXISTS t: member(s, subterms(t)) AND member(t, unif_prb)
theorem subterms_mem_unif_prb : member s (subterms unif_prb) ↔ ∃ t,  member s (subterms t) ∧ member t unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.subterms_mem_lst_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, subterms(lst_unif_prb)) IFF
--  EXISTS t: member(s, subterms(t)) AND member(t, lst_unif_prb)
theorem subterms_mem_lst_unif_prb : member s (subterms lst_unif_prb) ↔ ∃ t,  member s (subterms t) ∧ member t lst_unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.subterms_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(append(unif_prb1, unif_prb2))) IFF
--  (member(t, subterms(unif_prb1)) OR member(t, subterms(unif_prb2)))
theorem subterms_append : member t (subterms (append unif_prb1 unif_prb2)) ↔ (member t (subterms unif_prb1) ∨ member t (subterms unif_prb2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.subterms_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(pareate(args1, args2))) IMPLIES
--  (member(t, subterms(args1)) OR member(t, subterms(args2)))
theorem subterms_pareate : member t (subterms (pareate args1 args2)) → (member t (subterms args1) ∨ member t (subterms args2)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_unification.subset_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(unif_prb1, unif_prb2) IMPLIES
--  subset?(vars(unif_prb1), vars(unif_prb2))
theorem subset_vars : subset_p unif_prb1 unif_prb2 → subset_p (vars unif_prb1) (vars unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.subset_unif_prb_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (subset?(unif_prb, unif_prb1) IMPLIES
--   subset?(unif_prb, append(unif_prb1, unif_prb2)))
--  AND
--  (subset?(unif_prb, unif_prb2) IMPLIES
--    subset?(unif_prb, append(unif_prb1, unif_prb2)))
theorem subset_unif_prb_append : (subset_p unif_prb unif_prb1 →
  subset_p unif_prb (append unif_prb1 unif_prb2)) ∧ (subset_p unif_prb unif_prb2 →
  subset_p unif_prb (append unif_prb1 unif_prb2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(sigma, append(unif_prb1, unif_prb2)) =
--  (unifies?(sigma, unif_prb1) AND unifies?(sigma, unif_prb2))
theorem unifies_p_append : unifies_p sigma (append unif_prb1 unif_prb2) = (unifies_p sigma unif_prb1 ∧ unifies_p sigma unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(args1) = length(args2) AND unifies?(sigma, pareate(args1, args2))
--  IMPLIES equal?(subs(sigma)(args1), subs(sigma)(args2))
theorem unifies_p_pareate : List.length args1 = List.length args2 ∧ unifies_p sigma (pareate args1 args2) → equal_p (subs sigma(args1)) (subs sigma(args2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(sigma, unif_prb) IFF
--  FORALL unif_pair:
--    member(unif_pair, unif_prb) IMPLIES unifies?(sigma, unif_pair)
theorem unifies_p_equiv : unifies_p sigma unif_prb ↔ ∀ unif_pair, 
  member unif_pair unif_prb → unifies_p sigma unif_pair := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(sigma, sigma1) AND unifies?(sigma, unif_prb) IMPLIES
--  unifies?(sigma1, unif_prb)
theorem unifies_p_equal : equal_p sigma sigma1 ∧ unifies_p sigma unif_prb → unifies_p sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_equal_alt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(sigma, sigma1, set_vars) AND
--  unifies?(sigma, unif_prb) AND subset?(vars(unif_prb), set_vars)
--  IMPLIES unifies?(sigma1, unif_prb)
theorem unifies_p_equal_alt : equal_p sigma sigma1 set_vars ∧
 unifies_p sigma unif_prb ∧ subset_p (vars unif_prb) set_vars → unifies_p sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(unif_prb, unif_prb1) AND unifies?(sigma, unif_prb1) IMPLIES
--  unifies?(sigma, unif_prb)
theorem unifies_p_subset : subset_p unif_prb unif_prb1 ∧ unifies_p sigma unif_prb1 → unifies_p sigma unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_append_disjoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(append(sigma1, sigma), unif_prb) AND
--  disjoint?(dom(sigma), vars(unif_prb))
--  IMPLIES unifies?(sigma1, unif_prb)
theorem unifies_p_append_disjoint : unifies_p (append sigma1 sigma) unif_prb ∧
 disjoint_p (dom sigma) (vars unif_prb) → unifies_p sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_append_disjoint2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(sigma1, unif_prb) AND disjoint?(dom(sigma), vars(unif_prb))
--  IMPLIES unifies?(append(sigma1, sigma), unif_prb)
theorem unifies_p_append_disjoint2 : unifies_p sigma1 unif_prb ∧ disjoint_p (dom sigma) (vars unif_prb) → unifies_p (append sigma1 sigma) unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_append_disjoint3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(unif_prb)) IMPLIES
--  unifies?(append(sigma1, sigma), unif_prb) = unifies?(sigma1, unif_prb)
theorem unifies_p_append_disjoint3 : disjoint_p (dom sigma) (vars unif_prb) → unifies_p (append sigma1 sigma) unif_prb = unifies_p sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_append_disjoint4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(sigma, unif_prb) IMPLIES unifies?(append(sigma1, sigma), unif_prb)
theorem unifies_p_append_disjoint4 : unifies_p sigma unif_prb → unifies_p (append sigma1 sigma) unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, apply_sub(sigma, unif_prb)) IFF
--  EXISTS s: member(s, unif_prb) AND t = subs(sigma)(s)
theorem apply_sub_mem : member t (apply_sub sigma unif_prb) ↔ ∃ s,  member s unif_prb ∧ t = subs sigma(s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_unif_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(unif_pair, apply_sub(sigma, unif_prb)) IFF
--  EXISTS unif_pair1:
--    member(unif_pair1, unif_prb) AND unif_pair = apply_sub(sigma, unif_pair1)
theorem apply_sub_unif_pair : member unif_pair (apply_sub sigma unif_prb) ↔ ∃ unif_pair1, 
  member unif_pair1 unif_prb ∧ unif_pair = apply_sub sigma unif_pair1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_subterms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(apply_sub(sigma, unif_prb))) IMPLIES
--  ((EXISTS s: member(s, subterms(unif_prb)) AND t = subs(sigma)(s)) OR
--    member(t, subterms(img(sigma))))
theorem apply_sub_subterms : member t (subterms (apply_sub sigma unif_prb)) → ((∃ s,  member s (subterms unif_prb) ∧ t = subs sigma(s)) ∨
  member t (subterms (img sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: apply_sub(null, unif_prb) = unif_prb
theorem apply_sub_null : apply_sub null unif_prb = unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: apply_sub(sigma, append(unif_prb1, unif_prb2)) =
--  append(apply_sub(sigma, unif_prb1), apply_sub(sigma, unif_prb2))
theorem apply_sub_append : apply_sub sigma (append unif_prb1 unif_prb2) = append (apply_sub sigma unif_prb1) (apply_sub sigma unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_sub_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: apply_sub(sigma, apply_sub(sigma1, unif_prb)) =
--  apply_sub(append(sigma, sigma1), unif_prb)
theorem apply_sub_sub_append : apply_sub sigma (apply_sub sigma1 unif_prb) = apply_sub (append sigma sigma1) unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_len
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(apply_sub(sigma, unif_prb)) = length(unif_prb)
theorem apply_sub_len : List.length (apply_sub sigma unif_prb) = List.length unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_first_proj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(first_proj(unif_prb)) = first_proj(apply_sub(sigma, unif_prb))
theorem apply_sub_first_proj : subs sigma(first_proj unif_prb) = first_proj (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_sec_proj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(sec_proj(unif_prb)) = sec_proj(apply_sub(sigma, unif_prb))
theorem apply_sub_sec_proj : subs sigma(sec_proj unif_prb) = sec_proj (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_subset_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(unif_prb, apply_sub(sigma, unif_prb1)) IMPLIES
--  EXISTS unif_prb2:
--    subset?(unif_prb2, unif_prb1) AND unif_prb = apply_sub(sigma, unif_prb2)
theorem apply_sub_subset_unif_prb : subset_p unif_prb (apply_sub sigma unif_prb1) → ∃ unif_prb2, 
  subset_p unif_prb2 unif_prb1 ∧ unif_prb = apply_sub sigma unif_prb2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pareate(subs(sigma)(args1), subs(sigma)(args2)) =
--  apply_sub(sigma, pareate(args1, args2))
theorem apply_sub_pareate : pareate (subs sigma(args1)) (subs sigma(args2)) = apply_sub sigma (pareate args1 args2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_elim_var_unif_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, dom(sigma)) AND NOT member(X, vars(img(sigma))) IMPLIES
--  NOT member(X, vars(apply_sub(sigma, unif_pair)))
theorem apply_sub_elim_var_unif_pair : member X (dom sigma) ∧ ¬ member X (vars (img sigma)) → ¬ member X (vars (apply_sub sigma unif_pair)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_elim_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, dom(sigma)) AND NOT member(X, vars(img(sigma))) IMPLIES
--  NOT member(X, vars(apply_sub(sigma, unif_prb)))
theorem apply_sub_elim_var : member X (dom sigma) ∧ ¬ member X (vars (img sigma)) → ¬ member X (vars (apply_sub sigma unif_prb)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_no_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_pair?(unif_prb) AND no_pair?(img(sigma)) IMPLIES
--  no_pair?(apply_sub(sigma, unif_prb))
theorem apply_sub_no_pair : no_pair_p unif_prb ∧ no_pair_p (img sigma) → no_pair_p (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_no_var_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_var_pair?(unif_prb) IMPLIES no_var_pair?(apply_sub(sigma, unif_prb))
theorem apply_sub_no_var_pair : no_var_pair_p unif_prb → no_var_pair_p (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_all_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: all_ac_prb?(unif_prb) IMPLIES all_ac_prb?(apply_sub(sigma, unif_prb))
theorem apply_sub_all_ac : all_ac_prb_p unif_prb → all_ac_prb_p (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.ac_prb_renaming
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ac_prb?(unif_pair) IFF ac_prb?(apply_sub(rho, unif_pair))
theorem ac_prb_renaming : ac_prb_p unif_pair ↔ ac_prb_p (apply_sub rho unif_pair) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.all_ac_prb_renaming
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: all_ac_prb?(unif_prb) IFF all_ac_prb?(apply_sub(rho, unif_prb))
theorem all_ac_prb_renaming : all_ac_prb_p unif_prb ↔ all_ac_prb_p (apply_sub rho unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_var_not_dom_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(variable(X), unif_pair) AND NOT member(X, dom(sigma)) IMPLIES
--  member(variable(X), apply_sub(sigma, unif_pair))
theorem apply_sub_var_not_dom_mem : member (variable X) unif_pair ∧ ¬ member X (dom sigma) → member (variable X) (apply_sub sigma unif_pair) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_var_not_dom_lhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, dom(sigma)) AND NOT member(variable(X), img(sigma)) IMPLIES
--  count(variable(X), lhs(unif_prb), =) =
--   count(variable(X), lhs(apply_sub(sigma, unif_prb)), =)
theorem apply_sub_var_not_dom_lhs : ¬ member X (dom sigma) ∧ ¬ member (variable X) (img sigma) → count (variable X) (lhs unif_prb) = =
 count (variable X) (lhs (apply_sub sigma unif_prb)) = := by
  -- === TCCs for apply_sub_var_not_dom_lhs ===
  -- TCC: apply_sub_var_not_dom_lhs_TCC1
  -- Statement: FORALL (X: variable, sigma: sub):
  --   NOT member(variable(X), img(sigma)) AND NOT member(X, dom(sigma)) IMPLIES
  --    equivalence?
  --        [first_order_AC_term[constant, variable, f_symbol, ac_symbol]]
  --  ...
  have apply_sub_var_not_dom_lhs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_var_not_dom_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, dom(sigma)) AND NOT member(variable(X), img(sigma)) IMPLIES
--  count(variable(X), rhs(unif_prb), =) =
--   count(variable(X), rhs(apply_sub(sigma, unif_prb)), =)
theorem apply_sub_var_not_dom_rhs : ¬ member X (dom sigma) ∧ ¬ member (variable X) (img sigma) → count (variable X) (rhs unif_prb) = =
 count (variable X) (rhs (apply_sub sigma unif_prb)) = := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_var_not_dom_count
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, dom(sigma)) AND NOT member(variable(X), img(sigma)) IMPLIES
--  count(variable(X), append(lhs(unif_prb), rhs(unif_prb)), =) =
--   count(variable(X),
--         append(lhs(apply_sub(sigma, unif_p
theorem apply_sub_var_not_dom_count : ¬ member X (dom sigma) ∧ ¬ member (variable X) (img sigma) → count (variable X) (append (lhs unif_prb) (rhs unif_prb)) = =
 count (variable X) (append (lhs (apply_sub sigma unif_prb)) (rhs (apply_sub sigma unif_prb))) = := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_unifies
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(sigma, apply_sub(sigma1, unif_prb)) IFF
--  unifies?(append(sigma, sigma1), unif_prb)
theorem apply_sub_unifies : unifies_p sigma (apply_sub sigma1 unif_prb) ↔ unifies_p (append sigma sigma1) unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal_syn?(sigma, sigma1, vars(unif_prb)) IMPLIES
--  apply_sub(sigma, unif_prb) = apply_sub(sigma1, unif_prb)
theorem apply_sub_eq : equal_syn_p sigma sigma1 (vars unif_prb) → apply_sub sigma unif_prb = apply_sub sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.apply_sub_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(unif_prb)) IMPLIES
--  apply_sub(sigma, unif_prb) = unif_prb
theorem apply_sub_no_effect : disjoint_p (dom sigma) (vars unif_prb) → apply_sub sigma unif_prb = unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.select_greater_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(unif_prb) IMPLIES
--  member(select_greater(unif_prb), unif_prb) AND
--   FORALL t:
--     member(t, unif_prb) IMPLIES size(t) <= size(select_greater(unif_prb))
theorem select_greater_cor : ¬ null_p unif_prb → member (select_greater unif_prb) unif_prb ∧
 ∀ t, 
   member t unif_prb → size t ≤ size (select_greater unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.well_formed_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(unif_prb) IFF
--  (FORALL t: member(t, unif_prb) IMPLIES well_formed?(t))
theorem well_formed_unif_prb : well_formed_p unif_prb ↔ (∀ t,  member t unif_prb → well_formed_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.well_formed_apply_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(unif_prb) AND well_formed?(sigma) IMPLIES
--  well_formed?(apply_sub(sigma, unif_prb))
theorem well_formed_apply_sub : well_formed_p unif_prb ∧ well_formed_p sigma → well_formed_p (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.well_formed_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(unif_prb, unif_prb1) AND well_formed?(unif_prb1) IMPLIES
--  well_formed?(unif_prb)
theorem well_formed_subset : subset_p unif_prb unif_prb1 ∧ well_formed_p unif_prb1 → well_formed_p unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_impossible
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL t1:
--    count(t1, get_args(f)(s), equal?) >= count(t1, get_args(f)(t), equal?))
--  AND
--  are_ac?(t, s, f) AND
--   NOT same_count?(get_args(f)(t), get_args(f)(s), equal?)
--  IMPLIES NOT unifies?(delta,
theorem unifies_p_impossible : (∀ t1, 
   count t1 (get_args f(s)) equal_p ≥ count t1 (get_args f(t)) equal_p)
 ∧
 are_ac_p t s f ∧
  ¬ same_count_p (get_args f(t)) (get_args f(s)) equal_p → ¬ unifies_p delta ((t, s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.unifies?_impossible2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(sigma, (t, s)) AND NOT var?(t) AND well_formed?(t) IMPLIES
--  ord(t) = ord(s) OR (var?(s) AND NOT (member(V(s), vars(t))))
theorem unifies_p_impossible2 : unifies_p sigma ((t, s)) ∧ ¬ var_p t ∧ well_formed_p t → ord t = ord s ∨ (var_p s ∧ ¬ (member (V s) (vars t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.remove_common_not_null_count
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS t1: count(t1, args1, equal?) > count(t1, args2, equal?)) IMPLIES
--  NOT null?(remove_common(args1, args2, equal?))
theorem remove_common_not_null_count : (∃ t1,  count t1 args1 equal_p > count t1 args2 equal_p) → ¬ null_p (remove_common args1 args2 equal_p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.remove_common_not_null_unifies
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(delta, (t, s)) AND NOT equal?(t, s) AND are_ac?(t, s, f) IMPLIES
--  (NOT null?(remove_common(get_args(f)(t), get_args(f)(s), equal?)) AND
--    NOT null?(remove_common(get_args(f)(s), get_args(f)(
theorem remove_common_not_null_unifies : unifies_p delta ((t, s)) ∧ ¬ equal_p t s ∧ are_ac_p t s f → (¬ null_p (remove_common (get_args f(t)) (get_args f(s)) equal_p) ∧
  ¬ null_p (remove_common (get_args f(s)) (get_args f(t)) equal_p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.remove_common_args_cor
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f) IN
--   unifies?(delta, (t, s)) AND NOT equal?(t, s) AND are_ac?(t, s, f) IMPLIES
--    remove_common(get_args(f)(t), get_args(f)(s), equal?) =
--     remove_common(get_ar
theorem remove_common_args_cor : fun (t1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p delta ((t, s)) ∧ ¬ equal_p t s ∧ are_ac_p t s f → remove_common (get_args f(t)) (get_args f(s)) equal_p =
 remove_common (get_args f(t1)) (get_args f(s1)) equal_p (remove_common t s f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.get_args_remove_common
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f) IN
--   unifies?(delta, (t, s)) AND NOT equal?(t, s) AND are_ac?(t, s, f) IMPLIES
--    get_args(f)(t1) = remove_common(get_args(f)(t), get_args(f)(s), equal?)
theorem get_args_remove_common : fun (t1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p delta ((t, s)) ∧ ¬ equal_p t s ∧ are_ac_p t s f → get_args f(t1) = remove_common (get_args f(t)) (get_args f(s)) equal_p (remove_common t s f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.remove_common_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f) IN
--   NOT equal?(t, s) AND are_ac?(t, s, f) AND unifies?(delta, (t, s)) IMPLIES
--    equal?(subs(delta)(t1), subs(delta)(s1))
theorem remove_common_eq : fun (t1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => ¬ equal_p t s ∧ are_ac_p t s f ∧ unifies_p delta ((t, s)) → equal_p (subs delta(t1)) (subs delta(s1)) (remove_common t s f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.remove_common_no_common_args
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f) IN
--   unifies?(delta, (t, s)) AND NOT equal?(t, s) AND are_ac?(t, s, f) IMPLIES
--    no_common_args?(get_args(f)(t1), get_args(f)(s1))
theorem remove_common_no_common_args : fun (t1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p delta ((t, s)) ∧ ¬ equal_p t s ∧ are_ac_p t s f → no_common_args_p (get_args f(t1)) (get_args f(s1)) (remove_common t s f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.remove_common_vars
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f) IN subset?(vars(t1, s1), vars(t, s))
theorem remove_common_vars : fun (t1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => subset_p (vars t1 s1) (vars t s) (remove_common t s f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.remove_common_alm_well_formed?
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f) IN
--   unifies?(delta, (t, s)) AND NOT equal?(t, s) AND are_ac?(t, s, f) IMPLIES
--    well_formed?(cons((t, s), null)) IMPLIES
--     alm_well_formed?(cons((t1, s1), nul
theorem remove_common_alm_well_formed_p : fun (t1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p delta ((t, s)) ∧ ¬ equal_p t s ∧ are_ac_p t s f → well_formed_p (cons ((t, s)) null) →
 alm_well_formed_p (cons ((t1, s1)) null) (remove_common t s f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_num_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 IMPLIES num_arg(t) = num_arg(s)
theorem decompose_num_arg : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 → num_arg t = num_arg s (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 IMPLIES subterms(unif_prb) = subterms(t, s)
theorem decompose_subterm : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 → subterms unif_prb = subterms t s (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_subterm2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   subset?(subterms(unif_prb), subterms(t, s))
theorem decompose_subterm2 : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => subset_p (subterms unif_prb) (subterms t s) (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 IMPLIES unifies?(sigma, (t, s)) = unifies?(sigma, unif_prb)
theorem decompose_correct : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 → unifies_p sigma ((t, s)) = unifies_p sigma unif_prb (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_correct_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 AND well_formed?(sigma) IMPLIES
--    unifies?(sigma, (app(f, t), app(f, s))) = unifies?(sigma, unif_prb)
theorem decompose_correct_app : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 ∧ well_formed_p sigma → unifies_p sigma ((app f t, app f s)) = unifies_p sigma unif_prb (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_no_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_pair?(decompose(t, s)`1)
theorem decompose_no_pair : no_pair_p (decompose t s.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_not_fail
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   well_formed?(sigma) AND unifies?(sigma, (t, s)) IMPLIES bool1
theorem decompose_not_fail : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => well_formed_p sigma ∧ unifies_p sigma ((t, s)) → bool1 (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_not_fail2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   well_formed?(sigma) AND unifies?(sigma, (app(f, t), app(f, s))) IMPLIES
--    bool1
theorem decompose_not_fail2 : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => well_formed_p sigma ∧ unifies_p sigma ((app f t, app f s)) → bool1 (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_app_well_formed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   well_formed?(sigma) AND well_formed?(app(f, t), app(f, s)) AND bool1
--    IMPLIES well_formed?(unif_prb)
theorem decompose_app_well_formed : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => well_formed_p sigma ∧ well_formed_p (app f t) (app f s) ∧ bool1 → well_formed_p unif_prb (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_size
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN size(unif_prb) <= size(t, s)
theorem decompose_size : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => size unif_prb ≤ size t s (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_ren
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: decompose(t, s)`2 IFF decompose(subs(rho)(t), subs(rho)(s))`2
theorem decompose_ren : decompose t s.2 ↔ decompose (subs rho(t)) (subs rho(s)).2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: first_order_AC_unification.decompose_ren1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: decompose(subs(rho)(t), subs(rho)(s))`1 = apply_sub(rho, decompose(t, s)`1)
theorem decompose_ren1 : decompose (subs rho(t)) (subs rho(s)).1 = apply_sub rho (decompose t s.1) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.first_order_AC_unification