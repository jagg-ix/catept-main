-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nominal_AC_inst_step
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nominal_AC_inst_step

Auto-generated stubs from PVSLib theory `nominal_AC_inst_step`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nominal_AC_inst_step

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (DS : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (append : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (apply_sub : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (car : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cdr : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (cons : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (contained_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (disjoint_p : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (dom : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (equal_p : pvs_τ19 → pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (ext : pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (fc : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} (idempotent_p : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} (img : pvs_τ29 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (inst_ptc_p : pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (is_sol_p : pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} (length : pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (match_condition_p : pvs_τ50 → pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (match_problem_p : pvs_τ52 → pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (member : pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} (more_general_p : pvs_τ56 → pvs_τ57 → pvs_τ58 → Prop)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} (nominal_AC_fresh_subs : pvs_τ59 → pvs_τ60 → pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (nominal_AC_fresh_subs_p : pvs_τ62 → pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (nth : pvs_τ64 → pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} (null_p : pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (reverse : pvs_τ67 → pvs_τ68)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (rhs : pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} (singleton : pvs_τ71 → Prop)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} (subs : pvs_τ76 → pvs_τ77 → Prop)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} (subset_p : pvs_τ78 → pvs_τ79 → Prop)
variable {pvs_τ80 : Type*} {pvs_τ81 : Type*} (supset_dom : pvs_τ80 → pvs_τ81)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} {pvs_τ84 : Type*} (unifies_p : pvs_τ82 → pvs_τ83 → pvs_τ84 → Prop)
variable {pvs_τ85 : Type*} {pvs_τ86 : Type*} {pvs_τ87 : Type*} (union : pvs_τ85 → pvs_τ86 → pvs_τ87)
variable {pvs_τ88 : Type*} (var_p : pvs_τ88 → Prop)
variable {pvs_τ89 : Type*} {pvs_τ90 : Type*} (vars : pvs_τ89 → pvs_τ90 → Prop)
variable {pvs_τ91 : Type*} {pvs_τ92 : Type*} (well_formed_p : pvs_τ91 → pvs_τ92 → Prop)

-- pvs2lean: nominal_AC_inst_step.inst_step
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF var?(s)
--   THEN IF (NOT member(V(s), vars(t))) AND (NOT member(V(s), PV))
--          THEN LET sigma = cons((V(s), ext(reverse(p(s)))(t)), null),
--                   (G1, bool1) = nominal_AC_fresh_subs?(
axiom inst_step {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → Prop

-- pvs2lean: nominal_AC_inst_step.skip_index?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(P) THEN FALSE
-- ELSE LET (t, s) = car(P), (G1, delta, skip, fail) = inst_step(PV, G, t, s)
--        IN
--        IF i = 0 THEN skip
--        ELSIF skip THEN skip_index?(i - 1, PV, G, cdr(P))
--        EL
axiom skip_index_p {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} : pvs_τ72 → pvs_τ73 → pvs_τ74 → pvs_τ75 → Prop

-- pvs2lean: nominal_AC_inst_step.inst_step_ctx
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF var?(s)
--   THEN IF (NOT member(V(s), vars(t))) AND (NOT member(V(s), PV))
--          THEN LET sigma = cons((V(s), ext(reverse(p(s)))(t)), null),
--                   (G1, bool1) = nominal_AC_fresh_subs?(
axiom inst_step_ctx {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41 → pvs_τ42 → Prop

-- pvs2lean: nominal_AC_inst_step.inst_left?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(t) AND NOT var?(s)
axiom inst_left_p {pvs_τ30 : Type*} {pvs_τ31 : Type*} : pvs_τ30 → pvs_τ31 → Prop

-- pvs2lean: nominal_AC_inst_step.inst_step_skip
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET skip = inst_step(PV, G, t, s)`3 IN
--   skip IMPLIES
--    ((NOT var?(t) OR member(V(t), PV)) AND (NOT var?(s) OR member(V(s), PV)))
theorem inst_step_skip : fun (skip : Bool) => skip → ((¬ var_p t ∨ member (V t) PV) ∧ (¬ var_p s ∨ member (V s) PV)) (inst_step PV G t s.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_supset_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2 IN
--   subset?(supset_dom(delta), vars(t, s))
theorem inst_step_supset_dom : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => subset_p (supset_dom delta) (vars t s) (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2 IN subset?(dom(delta), vars(t, s))
theorem inst_step_dom : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => subset_p (dom delta) (vars t s) (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_dom_PV_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2 IN disjoint?(dom(delta), PV)
theorem inst_step_dom_PV_aux : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => disjoint_p (dom delta) PV (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_ctx_correct_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, t, s)`1 = append(inst_step_ctx(PV, G, t, s), G)
theorem inst_step_ctx_correct_aux : inst_step PV G t s.1 = append (inst_step_ctx PV G t s) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_sound_aux
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   NOT skip AND
--    NOT fail AND
--     disjoint?(dom(sigma), vars(t, s)) AND
--      nominal_AC_fresh_subs(G2)(append(append(sigma1, delta), sigma), G1
theorem inst_step_sound_aux : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => ¬ skip ∧
 ¬ fail ∧
  disjoint_p (dom sigma) (vars t s) ∧
   nominal_AC_fresh_subs G2(append (append sigma1 delta) sigma, G1) → unifies_p G2 (append (append sigma1 delta) sigma) ((t, s)) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(img(inst_step(PV, G, t, s)`2)), vars(t, s))
theorem inst_step_img : subset_p (vars (img (inst_step PV G t s.2))) (vars t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_dom_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   (dom(delta) = emptyset AND img(delta) = emptyset) OR
--    (var?(s) AND
--      dom(delta) = singleton(V(s)) AND vars(img(delta)) = vars(t))
--     OR
-- 
theorem inst_step_dom_img : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => (dom delta = emptyset ∧ img delta = emptyset) ∨ (var_p s ∧ dom delta = singleton (V s) ∧ vars (img delta) = vars t)
 ∨
 (var_p t ∧ dom delta = singleton (V t) ∧ vars (img delta) = vars s) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_chooses_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   NOT skip AND NOT fail AND var?(s) AND NOT member(V(s), PV) IMPLIES
--    (dom(delta) = emptyset AND img(delta) = emptyset) OR
--     (dom(delta) = s
theorem inst_step_chooses_rhs : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => ¬ skip ∧ ¬ fail ∧ var_p s ∧ ¬ member (V s) PV → (dom delta = emptyset ∧ img delta = emptyset) ∨
 (dom delta = singleton (V s) ∧ vars (img delta) = vars t) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_vars_context_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(inst_step(PV, G, t, s)`1), union(vars(G), vars(t, s)))
theorem inst_step_vars_context_aux : subset_p (vars (inst_step PV G t s.1)) (union (vars G) (vars t s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_well_formed_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(t) AND well_formed?(s) IMPLIES
--  well_formed?(inst_step(PV, G, t, s)`2)
theorem inst_step_well_formed_aux : well_formed_p t ∧ well_formed_p s → well_formed_p (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_idempotent2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(inst_step(PV, G, t, s)`2)
theorem inst_step_idempotent2 : idempotent_p (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_inst_left_not_var_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   inst_left?(t, s) AND NOT fail IMPLIES NOT var?(subs(delta)(t))
theorem inst_step_inst_left_not_var_aux : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => inst_left_p t s ∧ ¬ fail → ¬ var_p (subs delta(t)) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_inst_ptc_vars_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2 IN
--   inst_ptc?((t, s), PV) IMPLIES subset?(vars(img(delta)), vars(t))
theorem inst_step_inst_ptc_vars_img : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => inst_ptc_p ((t, s)) PV → subset_p (vars (img delta)) (vars t) (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_inst_ptc_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   inst_ptc?((t, s), PV) AND NOT fail AND NOT member(V(s), PV) IMPLIES
--    dom(delta) = singleton(V(s))
theorem inst_step_inst_ptc_dom : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => inst_ptc_p ((t, s)) PV ∧ ¬ fail ∧ ¬ member (V s) PV → dom delta = singleton (V s) (inst_step PV G t s) := by
  -- === TCCs for inst_step_inst_ptc_dom ===
  -- TCC: inst_step_inst_ptc_dom_TCC1
  -- Statement: FORALL (G: fresh_context, PV: finite_set[variable],
  --         s, t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --         G1: fresh_context, delta: sub, skip: bool, fail: bool):
  --        ...
  have inst_step_inst_ptc_dom_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_inst_ptc_vars_img2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2 IN
--   inst_ptc?((t, s), PV) IMPLIES subset?(vars(img(delta)), PV)
theorem inst_step_inst_ptc_vars_img2 : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => inst_ptc_p ((t, s)) PV → subset_p (vars (img delta)) PV (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_inst_ptc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   inst_ptc?((t, s), PV) AND var?(s) AND NOT fail AND NOT member(V(s), PV)
--    IMPLIES subset?(vars(subs(delta)(V(s))), PV)
theorem inst_step_inst_ptc : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => inst_ptc_p ((t, s)) PV ∧ var_p s ∧ ¬ fail ∧ ¬ member (V s) PV → subset_p (vars (subs delta(V s))) PV (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_cond_not_skip
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   member(X, vars(s)) AND
--    match_condition?(cons((t, s), P), PV) AND NOT member(X, PV)
--    IMPLIES NOT skip
theorem inst_step_match_cond_not_skip : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => member X (vars s) ∧
 match_condition_p (cons ((t, s)) P) PV ∧ ¬ member X PV → ¬ skip (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_cond_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   member(X, vars(s)) AND
--    match_condition?(cons((t, s), P), PV) AND NOT member(X, PV) AND NOT fail
--    IMPLIES member(X, dom(delta))
theorem inst_step_match_cond_dom : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => member X (vars s) ∧
 match_condition_p (cons ((t, s)) P) PV ∧ ¬ member X PV ∧ ¬ fail → member X (dom delta) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_cond_dom2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   member(X, dom(delta)) AND
--    match_condition?(cons((t, s), P), PV) AND
--     member(X, vars(rhs(P))) AND NOT member(X, PV)
--    IMPLIES member(X, 
theorem inst_step_match_cond_dom2 : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => member X (dom delta) ∧
 match_condition_p (cons ((t, s)) P) PV ∧
  member X (vars (rhs P)) ∧ ¬ member X PV → member X (vars s) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_cond_not_in_ctx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   member(X, vars(rhs(cons((t, s), P)))) AND
--    match_condition?(cons((t, s), P), PV) AND
--     NOT member(X, PV) AND NOT member(X, vars(G))
--    IMP
theorem inst_step_match_cond_not_in_ctx : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => member X (vars (rhs (cons ((t, s)) P))) ∧
 match_condition_p (cons ((t, s)) P) PV ∧
  ¬ member X PV ∧ ¬ member X (vars G) → ¬ member X (vars G1) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_cond_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   member(X, vars(s)) AND
--    match_condition?(cons((t, s), P), PV) AND NOT member(X, PV) AND NOT fail
--    IMPLIES subs(delta)(X) = subs(delta)(t)
theorem inst_step_match_cond_subs : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => member X (vars s) ∧
 match_condition_p (cons ((t, s)) P) PV ∧ ¬ member X PV ∧ ¬ fail → subs delta(X) = subs delta(t) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   match_condition?(cons((t, s), P), PV) IMPLIES
--    subset?(vars(img(delta)), PV)
theorem inst_step_match_img : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => match_condition_p (cons ((t, s)) P) PV → subset_p (vars (img delta)) PV (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_inst_ptc_match2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2 IN
--   inst_ptc?((t, s), PV) AND match_condition?(P, PV) IMPLIES
--    match_condition?(apply_sub(delta, P), PV)
theorem inst_step_inst_ptc_match2 : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => inst_ptc_p ((t, s)) PV ∧ match_condition_p P PV → match_condition_p (apply_sub delta P) PV (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_condition_apply_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s) IN
--   NOT skip AND NOT fail AND match_condition?(cons((t, s), P), PV) IMPLIES
--    match_condition?(apply_sub(delta, P), PV)
theorem inst_step_match_condition_apply_sub : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => ¬ skip ∧ ¬ fail ∧ match_condition_p (cons ((t, s)) P) PV → match_condition_p (apply_sub delta P) PV (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_condition_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET skip = inst_step(PV, G, t, s)`3 IN
--   skip AND match_condition?(cons((t, s), P), PV) IMPLIES
--    match_condition?(P, PV)
theorem inst_step_match_condition_cdr : fun (skip : Bool) => skip ∧ match_condition_p (cons ((t, s)) P) PV → match_condition_p P PV (inst_step PV G t s.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_unifies_not_fail_aux
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, sigma, skip, fail) = inst_step(PV, G, t, s) IN
--   unifies?(Del, del, (t, s)) AND
--    nominal_AC_fresh_subs(Del)(del, G) AND
--     disjoint?(dom(del), PV) AND well_formed?(t, s)
--    IMPLIES NOT fai
theorem inst_step_unifies_not_fail_aux : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => unifies_p Del del ((t, s)) ∧
 nominal_AC_fresh_subs Del(del, G) ∧
  disjoint_p (dom del) PV ∧ well_formed_p t s → ¬ fail (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_unifies_equal_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, sigma, skip, fail) = inst_step(PV, G, t, s) IN
--   unifies?(Del, del, (t, s)) IMPLIES equal?(Del)(del, append(del, sigma))
theorem inst_step_unifies_equal_sub : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => unifies_p Del del ((t, s)) → equal_p Del(del, append del sigma) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_nominal_AC_fresh_subs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, sigma, skip, fail) = inst_step(PV, G, t, s) IN
--   nominal_AC_fresh_subs(Del)(del, G) AND
--    unifies?(Del, del, (t, s)) AND
--     match_condition?(cons((t, s), P), PV) AND disjoint?(dom(del), PV)
theorem inst_step_nominal_AC_fresh_subs : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => nominal_AC_fresh_subs Del(del, G) ∧
 unifies_p Del del ((t, s)) ∧
  match_condition_p (cons ((t, s)) P) PV ∧ disjoint_p (dom del) PV → nominal_AC_fresh_subs Del(del, G1) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_fail
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fail = inst_step(PV, G, P, Ps, sigma)`4 IN
--   fail IMPLIES inst_step(PV, G, P, Ps, sigma)`2 = null
theorem inst_step_fail : fun (fail : Bool) => fail → inst_step PV G P Ps sigma.2 = null (inst_step PV G P Ps sigma.4) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step1_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`1 = inst_step(PV, G, P, P2, delta)`1
theorem inst_step1_no_effect : inst_step PV G P P1 sigma.1 = inst_step PV G P P2 delta.1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step1_no_effect_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`1 = inst_step(PV, G, P, null, sigma)`1
theorem inst_step1_no_effect_cor : inst_step PV G P P1 sigma.1 = inst_step PV G P null sigma.1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step1_no_effect_cor2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`1 = inst_step(PV, G, P, P1, null)`1
theorem inst_step1_no_effect_cor2 : inst_step PV G P P1 sigma.1 = inst_step PV G P P1 null.1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step3_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`3 = inst_step(PV, G, P, P2, sigma)`3
theorem inst_step3_no_effect : inst_step PV G P P1 sigma.3 = inst_step PV G P P2 sigma.3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step3_no_effect_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`3 = inst_step(PV, G, P, null, sigma)`3
theorem inst_step3_no_effect_cor : inst_step PV G P P1 sigma.3 = inst_step PV G P null sigma.3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_sub_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, Ps, sigma)`3,
--     delta1 = inst_step(PV, G, P, Ps, null)`3
--   IN delta = append(delta1, sigma)
theorem inst_step_sub_append : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (delta1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => delta = append delta1 sigma (inst_step PV G P Ps null.3) (inst_step PV G P Ps sigma.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_sub_append_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, null, sigma)`3 =
--  append(inst_step(PV, G, P, null, null)`3, sigma)
theorem inst_step_sub_append_cor : inst_step PV G P null sigma.3 = append (inst_step PV G P null null.3) sigma := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step4_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`4 = inst_step(PV, G, P, P2, sigma)`4
theorem inst_step4_no_effect : inst_step PV G P P1 sigma.4 = inst_step PV G P P2 sigma.4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step4_no_effect_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`4 = inst_step(PV, G, P, null, sigma)`4
theorem inst_step4_no_effect_cor : inst_step PV G P P1 sigma.4 = inst_step PV G P null sigma.4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step4_no_effect2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`4 = inst_step(PV, G, P, P1, sigma2)`4
theorem inst_step4_no_effect2 : inst_step PV G P P1 sigma.4 = inst_step PV G P P1 sigma2.4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step4_no_effect2_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, P1, sigma)`4 = inst_step(PV, G, P, P1, null)`4
theorem inst_step4_no_effect2_cor : inst_step PV G P P1 sigma.4 = inst_step PV G P P1 null.4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step2_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, Ps, sigma1)`2 = inst_step(PV, G, P, Ps, sigma2)`2
theorem inst_step2_no_effect : inst_step PV G P Ps sigma1.2 = inst_step PV G P Ps sigma2.2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step2_no_effect_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: inst_step(PV, G, P, Ps, sigma)`2 = inst_step(PV, G, P, Ps, null)`2
theorem inst_step2_no_effect_cor : inst_step PV G P Ps sigma.2 = inst_step PV G P Ps null.2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step2_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   NOT fail IMPLIES
--    inst_step(PV, G, P, Ps, null)`2 =
--     append(inst_step(PV, G, P, null, null)`2, apply_sub(delta, Ps))
theorem inst_step2_append : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => ¬ fail → inst_step PV G P Ps null.2 =
 append (inst_step PV G P null null.2) (apply_sub delta Ps) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step2_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, null, null)`3 IN
--   member(t, inst_step(PV, G, P, null, null)`2) IMPLIES
--    EXISTS s: member(s, P) AND t = subs(delta)(s)
theorem inst_step2_mem : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => member t (inst_step PV G P null null.2) → ∃ s,  member s P ∧ t = subs delta(s) (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step2_mem_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   member(t, rhs(P1)) IMPLIES
--    EXISTS s: member(s, rhs(P)) AND t = subs(delta)(s)
theorem inst_step2_mem_rhs : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => member t (rhs P1) → ∃ s,  member s (rhs P) ∧ t = subs delta(s) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_vars_supset_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, null, null)`3 IN
--   subset?(supset_dom(delta), vars(P))
theorem inst_step_vars_supset_dom : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => subset_p (supset_dom delta) (vars P) (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_vars_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, null, null)`3 IN
--   subset?(vars(img(delta)), vars(P))
theorem inst_step_vars_img : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => subset_p (vars (img delta)) (vars P) (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_idempotent3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, null, null)`3 IN idempotent?(delta)
theorem inst_step_idempotent3 : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => idempotent_p delta (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_vars_unif
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   disjoint?(vars(P1), dom(delta))
theorem inst_step_vars_unif : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => disjoint_p (vars P1) (dom delta) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_vars_unif2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   subset?(vars(P1), vars(P))
theorem inst_step_vars_unif2 : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => subset_p (vars P1) (vars P) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_dom_PV
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, null, null)`3 IN disjoint?(dom(delta), PV)
theorem inst_step_dom_PV : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => disjoint_p (dom delta) PV (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_cont
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, null, null)`3 IN contained?(delta, vars(P))
theorem inst_step_cont : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => contained_p delta (vars P) (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_complete_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sigma = inst_step(PV, G, P, null, null)`3 IN
--   unifies?(Del, del, P) IMPLIES equal?(Del)(del, append(del, sigma))
theorem inst_step_complete_equal : fun (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => unifies_p Del del P → equal_p Del(del, append del sigma) (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_unifies_not_fail
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET fail = inst_step(PV, G, P, null, null)`4 IN
--        unifies?(Del, del, P) AND well_formed?(P) AND disjoint?(dom(del), PV)
--    AND nominal_AC_fresh_subs(Del)(del, G) AND match_condition?(P, PV)
--    IM
theorem inst_step_unifies_not_fail : fun (fail : Bool) => unifies_p Del del P ∧ well_formed_p P ∧ disjoint_p (dom del) PV
 ∧ nominal_AC_fresh_subs Del(del, G) ∧ match_condition_p P PV → ¬ fail (inst_step PV G P null null.4) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_complete_nominal_AC_fresh_subs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET G1 = inst_step(PV, G, P, null, null)`1 IN
--        unifies?(Del, del, P) AND well_formed?(P) AND disjoint?(dom(del), PV)
--    AND nominal_AC_fresh_subs(Del)(del, G) AND match_condition?(P, PV)
--    IMPL
theorem inst_step_complete_nominal_AC_fresh_subs : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) => unifies_p Del del P ∧ well_formed_p P ∧ disjoint_p (dom del) PV
 ∧ nominal_AC_fresh_subs Del(del, G) ∧ match_condition_p P PV → nominal_AC_fresh_subs Del(del, G1) (inst_step PV G P null null.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_complete_unifies
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null) IN
--        unifies?(Del, del, P) AND well_formed?(P) AND disjoint?(dom(del), PV)
--    AND nominal_AC_fresh_subs(Del)(del, G) AND match_conditi
theorem inst_step_complete_unifies : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => unifies_p Del del P ∧ well_formed_p P ∧ disjoint_p (dom del) PV
 ∧ nominal_AC_fresh_subs Del(del, G) ∧ match_condition_p P PV → unifies_p Del del P1 (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_complete_more_general
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null) IN
--   more_general?(Del)(sigma, delta) AND
--    disjoint?(dom(sigma), vars(P)) AND unifies?(Del, delta, P)
--    IMPLIES more_general?(Del)(appen
theorem inst_step_complete_more_general : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => more_general_p Del(sigma, delta) ∧
 disjoint_p (dom sigma) (vars P) ∧ unifies_p Del delta P → more_general_p Del(append sigma1 sigma, delta) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null) IN
--   is_sol?(Del, del)(G, P, sigma, PV) AND
--    match_condition?(P, PV) AND
--     well_formed?(P) AND disjoint?(dom(sigma), vars(P))
--    IMPLIE
theorem inst_step_complete : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => is_sol_p Del del(G, P, sigma, PV) ∧
 match_condition_p P PV ∧
  well_formed_p P ∧ disjoint_p (dom sigma) (vars P) → is_sol_p Del del(G1, P1, append sigma1 sigma, PV) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_well_formed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, null, null)`3 IN
--   well_formed?(P) IMPLIES well_formed?(delta)
theorem inst_step_well_formed : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => well_formed_p P → well_formed_p delta (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_well_formed2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P1 = inst_step(PV, G, P, null, null)`2 IN
--   well_formed?(P) IMPLIES well_formed?(P1)
theorem inst_step_well_formed2 : fun (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => well_formed_p P → well_formed_p P1 (inst_step PV G P null null.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_vars_context
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET G1 = inst_step(PV, G, P, null, null)`1 IN
--   subset?(vars(G1), union(vars(G), vars(P)))
theorem inst_step_vars_context : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) => subset_p (vars G1) (union (vars G) (vars P)) (inst_step PV G P null null.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_ctx_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET G1 = inst_step(PV, G, P, null, null)`1 IN
--   G1 = append(inst_step_ctx(PV, G, P), G)
theorem inst_step_ctx_correct : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) => G1 = append (inst_step_ctx PV G P) G (inst_step PV G P null null.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_img2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, P, null, null)`3 IN
--   match_condition?(P, PV) IMPLIES subset?(vars(img(delta)), PV)
theorem inst_step_match_img2 : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => match_condition_p P PV → subset_p (vars (img delta)) PV (inst_step PV G P null null.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   match_condition?(P, PV) AND
--    member(Y, vars(rhs(P))) AND NOT member(Y, PV) AND NOT fail
--    IMPLIES subset?(vars(subs(delta)(Y)), PV)
theorem inst_step_match_aux : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => match_condition_p P PV ∧
 member Y (vars (rhs P)) ∧ ¬ member Y PV ∧ ¬ fail → subset_p (vars (subs delta(Y))) PV (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   NOT fail AND match_condition?(P, PV) IMPLIES match_problem?(P1, PV)
theorem inst_step_match : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => ¬ fail ∧ match_condition_p P PV → match_problem_p P1 PV (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   NOT fail AND
--    match_condition?(P, PV) AND
--     member(X, vars(rhs(P))) AND NOT member(X, PV)
--    IMPLIES member(X, dom(delta))
theorem inst_step_match_dom : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => ¬ fail ∧
 match_condition_p P PV ∧ member X (vars (rhs P)) ∧ ¬ member X PV → member X (dom delta) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_not_in_context
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   match_condition?(P, PV) AND
--    member(X, vars(rhs(P))) AND NOT member(X, PV) AND NOT member(X, vars(G))
--    IMPLIES NOT member(X, vars(G
theorem inst_step_match_not_in_context : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => match_condition_p P PV ∧
 member X (vars (rhs P)) ∧ ¬ member X PV ∧ ¬ member X (vars G) → ¬ member X (vars G1) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_skip_ind_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   member(up1, P1) IMPLIES
--    EXISTS i:
--      i < length(P) AND
--       up1 = apply_sub(delta, nth(P, i)) AND skip_index?(i, PV, G, P)
theorem inst_step_skip_ind_mem : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => member up1 P1 → ∃ i, 
  i < List.length P ∧
   up1 = apply_sub delta (nth P i) ∧ skip_index_p i PV G P (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_inst_left_not_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   member((t1, s1), P) AND inst_left?(t1, s1) AND NOT fail IMPLIES
--    NOT var?(subs(delta)(V(t1)))
theorem inst_step_inst_left_not_var : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => member ((t1, s1)) P ∧ inst_left_p t1 s1 ∧ ¬ fail → ¬ var_p (subs delta(V t1)) (inst_step PV G P null null) := by
  -- === TCCs for inst_step_inst_left_not_var ===
  -- TCC: inst_step_inst_left_not_var_TCC1
  -- Statement: FORALL (G: fresh_context, P: unif_prb, PV: finite_set[variable],
  --         s1, t1: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --         G1: fresh_context, P1: unif_prb, delta: sub, fai...
  have inst_step_inst_left_not_var_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_inst_left_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   member((t1, s1), P) AND inst_left?(t1, s1) AND NOT fail IMPLIES
--    member(V(t1), dom(delta))
theorem inst_step_inst_left_dom : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => member ((t1, s1)) P ∧ inst_left_p t1 s1 ∧ ¬ fail → member (V t1) (dom delta) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_match_skip_ind
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, delta, fail) = inst_step(PV, G, P, null, null) IN
--   member(Y, vars(rhs(P))) AND
--    NOT member(Y, PV) AND match_condition?(P, PV) AND NOT fail
--    IMPLIES
--    EXISTS j:
--      j < length(P) AN
theorem inst_step_match_skip_ind : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => member Y (vars (rhs P)) ∧
 ¬ member Y PV ∧ match_condition_p P PV ∧ ¬ fail → ∃ j, 
  j < List.length P ∧
   nth P j.2 = null * Y ∧
    subs delta(Y) = subs delta(nth P j.1) ∧
     ¬ skip_index_p j PV G P (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_sound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, lambda1, fail) = inst_step(PV, G, P, null, null) IN
--   unifies?(Delta, append(delta, append(lambda1, sigma)), P1) AND
--    disjoint?(dom(sigma), vars(P)) AND
--     nominal_AC_fresh_subs(Delta)
theorem inst_step_sound : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (lambda1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => unifies_p Delta (append delta (append lambda1 sigma)) P1 ∧
 disjoint_p (dom sigma) (vars P) ∧
  nominal_AC_fresh_subs Delta(append delta (append lambda1 sigma), G1)
   ∧ ¬ fail → unifies_p Delta (append delta (append lambda1 sigma)) P (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member((G1, P, sigma), inst_step(PV, G, lst_P)) IMPLIES
--  EXISTS P1:
--         member(P1, lst_P) AND (G1 = inst_step(PV, G, P1, null, null)`1)
--     AND (P = inst_step(PV, G, P1, null, null)`2)
--     AND (si
theorem inst_step_mem : member ((G1, P, sigma)) (inst_step PV G lst_P) → ∃ P1, 
       member P1 lst_P ∧ (G1 = inst_step PV G P1 null null.1)
   ∧ (P = inst_step PV G P1 null null.2)
   ∧ (sigma = inst_step PV G P1 null null.3)
   ∧ ¬ inst_step PV G P1 null null.4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_inst_step.inst_step_mem2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS P1:
--         member(P1, lst_P) AND (G1 = inst_step(PV, G, P1, null, null)`1)
--     AND (P = inst_step(PV, G, P1, null, null)`2)
--     AND (sigma = inst_step(PV, G, P1, null, null)`3)
--     AND NOT in
theorem inst_step_mem2 : (∃ P1, 
        member P1 lst_P ∧ (G1 = inst_step PV G P1 null null.1)
    ∧ (P = inst_step PV G P1 null null.2)
    ∧ (sigma = inst_step PV G P1 null null.3)
    ∧ ¬ inst_step PV G P1 null null.4) → member ((G1, P, sigma)) (inst_step PV G lst_P) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.nominal_AC_inst_step