-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nominal_AC_variant_inputs
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nominal_AC_variant_inputs

Auto-generated stubs from PVSLib theory `nominal_AC_variant_inputs`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nominal_AC_variant_inputs

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (ac_prb_p : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (append : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (apply_ac : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (apply_sub : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (choose_eq : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (cons : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (disjoint_p : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (dom : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (equal_syn_p : pvs_τ22 → pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (ext : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (img : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (inst_ptc_p : pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (inst_step : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (lhs : pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (match_condition_p : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (member : pvs_τ41 → pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (nice_input_p : pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} (null_p : pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} (reverse : pvs_τ49 → pvs_τ50)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (subs : pvs_τ51 → pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (subset_p : pvs_τ53 → pvs_τ54 → Prop)
variable {pvs_τ67 : Type*} (var_p : pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} (vars : pvs_τ68 → pvs_τ69)

-- pvs2lean: nominal_AC_variant_inputs.var_pair?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: G_ = G AND equal_syn?(sig_, sig, FV)
noncomputable def var_pair_p : sorry := by
  sorry  -- pvs2lean: definition stub for G_ = G AND equal_syn?(sig_, sig, FV)

-- pvs2lean: nominal_AC_variant_inputs.var_inputs?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sig, V, PV) = input, (G_, P_, sig_, V_, PV_) = input_ IN
--        (G = G_) AND (P = P_) AND (PV = PV_) AND equal_syn?(sig_, sig, FV)
--    AND subset?(FV, V) AND subset?(FV, V_)
--    AND (select_g
axiom var_inputs_p {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} : pvs_τ64 → pvs_τ65 → pvs_τ66 → Prop

-- pvs2lean: nominal_AC_variant_inputs.var_inp_cond?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      match_condition?(P, PV) AND subset?(PV, FV) AND subset?(FV, V)
--  AND subset?(vars(G), FV) AND disjoint?(dom(rho), V)
--  AND subset?(vars(lhs(P)), FV) AND equal_syn?(sig_, sig, FV)
--  AND subset?(vars(
axiom var_inp_cond_p {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} : pvs_τ55 → pvs_τ56 → pvs_τ57 → pvs_τ58 → pvs_τ59 → pvs_τ60 → pvs_τ61 → pvs_τ62 → pvs_τ63 → Prop

-- pvs2lean: nominal_AC_variant_inputs.var_inp_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET skip = inst_step(PV, G, t, s)`3 IN
--   var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) AND skip
--    IMPLIES var_inp_cond?(G, P, sig, V, PV, sig_, FV, rho, V1)
theorem var_inp_cdr : fun (skip : Bool) => var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 ∧ skip → var_inp_cond_p G P sig V PV sig_ FV rho V1 (inst_step PV G t s.3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_no_effect_lhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) IMPLIES
--  subs(rho)(t) = t
theorem var_inp_no_effect_lhs : var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → subs rho(t) = t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_no_effect_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) AND
--  subset?(vars(s), FV)
--  IMPLIES subs(rho)(s) = s
theorem var_inp_no_effect_rhs : var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 ∧
 subset_p (vars s) FV → subs rho(s) = s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_disjoint_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) IMPLIES
--  disjoint?(vars(img(rho)), V)
theorem var_inp_disjoint_img : var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → disjoint_p (vars (img rho)) V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_option_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) IMPLIES
--  (subset?(vars(s), FV) OR
--    (var?(s) AND NOT member(V(s), PV) AND inst_ptc?((t, s), PV)))
theorem var_inp_option_rhs : var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → (subset_p (vars s) FV ∨
  (var_p s ∧ ¬ member (V s) PV ∧ inst_ptc_p ((t, s)) PV)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_option_rhs_ren
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) IMPLIES
--  subset?(vars(s), FV) OR
--   (var?(subs(rho)(s)) AND
--     NOT member(V(subs(rho)(s)), PV) AND
--      inst_ptc?((subs(rho)(t), subs(r
theorem var_inp_option_rhs_ren : var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → subset_p (vars s) FV ∨
 (var_p (subs rho(s)) ∧
   ¬ member (V (subs rho(s))) PV ∧
    inst_ptc_p ((subs rho(t), subs rho(s))) PV) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) AND
--  (NOT subset?(vars(s), FV))
--  IMPLIES
--  inst_step(PV, G, t, s) =
--   (append(G, G), cons((V(s), ext(reverse(p(s)))(t)), null), FALSE, F
theorem var_inp_inst_step_aux : var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 ∧
 (¬ subset_p (vars s) FV) → inst_step PV G t s =
 (append G G, cons ((V s, ext (reverse (p s)) (t))) null, False, False) := by
  -- === TCCs for var_inp_inst_step_aux ===
  -- TCC: var_inp_inst_step_aux_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V1: finite_set[variable], rho: renaming,
  --         s: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --      ...
  have var_inp_inst_step_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_aux_ren
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) AND
--  (NOT subset?(vars(s), FV))
--  IMPLIES
--  inst_step(PV, G, subs(rho)(t), subs(rho)(s)) =
--   (append(G, G),
--    cons((V(subs(rho)(s)), ext
theorem var_inp_inst_step_aux_ren : var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 ∧
 (¬ subset_p (vars s) FV) → inst_step PV G (subs rho(t)) (subs rho(s)) =
 (append G G,
  cons ((V (subs rho(s)), ext (reverse (p (subs rho(s)))) (subs rho(t)))) null,
  False, False) := by
  -- === TCCs for var_inp_inst_step_aux_ren ===
  -- TCC: var_inp_inst_step_aux_ren_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V, V1: finite_set[variable], rho: renaming,
  --         s: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --      ...
  have var_inp_inst_step_aux_ren_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_disjoint_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2,
--     delta_ = inst_step(PV, G, subs(rho)(t), subs(rho)(s))`2
--   IN
--   var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) AND
--    (NOT subset?(vars(s), F
theorem var_inp_inst_step_disjoint_dom : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (delta_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 ∧
 (¬ subset_p (vars s) FV) → (disjoint_p (dom delta) FV ∧ disjoint_p (dom delta_) FV) (inst_step PV G (subs rho(t)) (subs rho(s)).2) (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_equal_syn_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(s) AND
--  var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1)
--  IMPLIES
--  LET sig1 = cons((V(s), ext(reverse(p(s)))(t)), null),
--      sig1_ =
--        cons((V(subs(rho)(s)), ext(reverse(p(s
theorem var_inp_equal_syn_aux : var_p s ∧ var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → (let sig1 := cons ((V s, ext (reverse (p s)) (t))) null; let sig1_ := cons ((V (subs rho(s)), ext (reverse (p (subs rho(s)))) (subs rho(t)))) null; equal_syn_p (append sig1_ rho) (append rho sig1) V1) := by
  -- === TCCs for var_inp_equal_syn_aux ===
  -- TCC: var_inp_equal_syn_aux_TCC1
  -- Statement: FORALL (FV: finite_set[variable], G: fresh_context, P: unif_prb,
  --         PV, V2, V1: finite_set[variable], rho: renaming,
  --         s: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --     ...
  have var_inp_equal_syn_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_equal_syn
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2,
--     delta_ = inst_step(PV, G, subs(rho)(t), subs(rho)(s))`2
--   IN
--   var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) IMPLIES
--    equal_syn?(append(d
theorem var_inp_equal_syn : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (delta_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → equal_syn_p (append delta_ rho) (append rho delta) V1 (inst_step PV G (subs rho(t)) (subs rho(s)).2) (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_equal_syn_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2,
--     delta_ = inst_step(PV, G, subs(rho)(t), subs(rho)(s))`2
--   IN
--   var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) IMPLIES
--    apply_sub(delta_, a
theorem var_inp_equal_syn_unif_prb : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (delta_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → apply_sub delta_ (apply_sub rho P) = apply_sub rho (apply_sub delta P) (inst_step PV G (subs rho(t)) (subs rho(s)).2) (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_equal_syn2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2,
--     delta_ = inst_step(PV, G, subs(rho)(t), subs(rho)(s))`2
--   IN
--   var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) IMPLIES
--    equal_syn?(append(d
theorem var_inp_equal_syn2 : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (delta_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → equal_syn_p (append delta_ sig_) (append delta sig) FV (inst_step PV G (subs rho(t)) (subs rho(s)).2) (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_equal_syn3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = inst_step(PV, G, t, s)`2,
--     delta_ = inst_step(PV, G, subs(rho)(t), subs(rho)(s))`2
--   IN
--   var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) IMPLIES
--    equal_syn?(delta_, 
theorem var_inp_equal_syn3 : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (delta_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 → equal_syn_p delta_ delta FV (inst_step PV G (subs rho(t)) (subs rho(s)).2) (inst_step PV G t s.2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_preservation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, delta, skip, fail) = inst_step(PV, G, t, s),
--     delta_ = inst_step(PV, G, subs(rho)(t), subs(rho)(s))`2
--   IN
--   var_inp_cond?(G, cons((t, s), P), sig, V, PV, sig_, FV, rho, V1) AND
--    NOT fai
theorem var_inp_inst_step_preservation : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (skip : Bool) (fail : Bool) => fun (delta_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => var_inp_cond_p G (cons ((t, s)) P) sig V PV sig_ FV rho V1 ∧
 ¬ fail ∧ ¬ skip → var_inp_cond_p G1 (apply_sub delta P) (append delta sig) V PV (append delta_ sig_) FV rho V1 (inst_step PV G (subs rho(t)) (subs rho(s)).2) (inst_step PV G t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_ctx
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null),
--     (G1_, P1_, sigma1_, fail_) =
--       inst_step(PV, G, apply_sub(rho, P), null, null)
--   IN var_inp_cond?(G, P, sig, V, PV, sig_, FV, rho,
theorem var_inp_inst_step_ctx : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => fun (G1_ : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1_ : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail_ : Bool) => var_inp_cond_p G P sig V PV sig_ FV rho V1 → G1 = G1_ (inst_step PV G (apply_sub rho P) null null) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_fail
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null),
--     (G1_, P1_, sigma1_, fail_) =
--       inst_step(PV, G, apply_sub(rho, P), null, null)
--   IN var_inp_cond?(G, P, sig, V, PV, sig_, FV, rho,
theorem var_inp_inst_step_fail : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => fun (G1_ : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1_ : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail_ : Bool) => var_inp_cond_p G P sig V PV sig_ FV rho V1 → fail = fail_ (inst_step PV G (apply_sub rho P) null null) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null),
--     (G1_, P1_, sigma1_, fail_) =
--       inst_step(PV, G, apply_sub(rho, P), null, null)
--   IN
--   var_inp_cond?(G, P, sig, V, PV, sig_, FV, rh
theorem var_inp_inst_step_sub : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => fun (G1_ : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1_ : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail_ : Bool) => var_inp_cond_p G P sig V PV sig_ FV rho V1 → equal_syn_p (append sigma1_ sig_) (append sigma1 sig) FV (inst_step PV G (apply_sub rho P) null null) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_sub2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null),
--     (G1_, P1_, sigma1_, fail_) =
--       inst_step(PV, G, apply_sub(rho, P), null, null)
--   IN
--   var_inp_cond?(G, P, sig, V, PV, sig_, FV, rh
theorem var_inp_inst_step_sub2 : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => fun (G1_ : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1_ : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail_ : Bool) => var_inp_cond_p G P sig V PV sig_ FV rho V1 → equal_syn_p sigma1_ sigma1 FV (inst_step PV G (apply_sub rho P) null null) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null),
--     (G1_, P1_, sigma1_, fail_) =
--       inst_step(PV, G, apply_sub(rho, P), null, null)
--   IN var_inp_cond?(G, P, sig, V, PV, sig_, FV, rho,
theorem var_inp_inst_step_unif_prb : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => fun (G1_ : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1_ : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail_ : Bool) => var_inp_cond_p G P sig V PV sig_ FV rho V1 → P1 = P1_ (inst_step PV G (apply_sub rho P) null null) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_inst_step
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G1, P1, sigma1, fail) = inst_step(PV, G, P, null, null),
--     (G1_, P1_, sigma1_, fail_) =
--       inst_step(PV, G, apply_sub(rho, P), null, null)
--   IN
--   var_inp_cond?(G, P, sig, V, PV, sig_, FV, rh
theorem var_inp_inst_step : fun (G1 : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail : Bool) => fun (G1_ : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P1_ : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma1_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (fail_ : Bool) => var_inp_cond_p G P sig V PV sig_ FV rho V1 → G1 = G1_ ∧
 P1 = P1_ ∧
  fail = fail_ ∧
   equal_syn_p (append sigma1 sig) (append sigma1_ sig_) FV (inst_step PV G (apply_sub rho P) null null) (inst_step PV G P null null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_variant_inputs.var_inp_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (G, P, sigma_, V_, PV) = input_, (up, P1) = choose_eq(P), (t, s) = up
--   IN
--   ac_prb?(t, s) AND
--    NOT null?(P) AND
--     var_inputs?(input, input_, FV) AND
--      member(input1_, apply_ac(G, cons((t, 
theorem var_inp_ac : fun (G : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (P : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (V_ : {i : ℝ // i ≥ 0} → Bool) (PV : {i : ℝ // i ≥ 0} → Bool) => fun (up : (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)))) (P1 : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => ac_prb_p t s ∧
 ¬ null_p P ∧
  var_inputs_p input input_ FV ∧
   member input1_ (apply_ac G (cons ((t, s)) P1) sigma_ V_ PV) → ∃ input1, 
  member input1 (apply_ac G (cons ((t, s)) P1) input.3 input.4 PV) ∧
   var_inputs_p input1 input1_ FV up (choose_eq P) input_ := by
  -- === TCCs for var_inp_ac ===
  -- TCC: var_inp_ac_TCC1
  -- Statement: FORALL (FV: finite_set[variable], input, input_: nice_input,
  --         G: fresh_context, P: unif_prb, sigma_: sub,
  --         V_: finite_set[variable], PV: finite_set[variable]):
  --        (G = input_`1) A...
  have var_inp_ac_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: var_inp_ac_TCC2
  -- Statement: FORALL (FV: finite_set[variable], input, input1_, input_: nice_input,
  --         G: fresh_context, P: unif_prb, sigma_: sub,
  --         V_: finite_set[variable], PV: finite_set[variable]):
  --        (G = in...
  have var_inp_ac_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.nominal_AC_variant_inputs