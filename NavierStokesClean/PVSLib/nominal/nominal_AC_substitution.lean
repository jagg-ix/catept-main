-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nominal_AC_substitution
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nominal_AC_substitution

Auto-generated stubs from PVSLib theory `nominal_AC_substitution`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nominal_AC_substitution

noncomputable section

variable {pvs_τ1 : Type*} (abs_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (ac_app : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} (ac_app_p : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (ac_sym : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (add : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (alm_well_formed_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (app : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} (app_p : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (append : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (are_ac_p : pvs_τ16 → pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (at : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} (atom_p : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (car : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (cdr : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (cons : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (count : pvs_τ29 → pvs_τ30 → pvs_τ31 → pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (count_elem2 : pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (count_vec : pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (delete : pvs_τ40 → pvs_τ41 → pvs_τ42 → pvs_τ43)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (ext : pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (finset2list : pvs_τ56 → pvs_τ57)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (flatten : pvs_τ58 → pvs_τ59)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (flatten_pair : pvs_τ60 → pvs_τ61)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} (fresh : pvs_τ62 → pvs_τ63 → pvs_τ64 → Prop)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (fresh_p : pvs_τ65 → pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} (func_p : pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} (get_ac_from_args : pvs_τ68 → pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} (get_args : pvs_τ71 → pvs_τ72 → Prop)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} (get_lst_mult : pvs_τ73 → pvs_τ74 → pvs_τ75)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} (get_repeat_lst : pvs_τ76 → pvs_τ77 → pvs_τ78)
variable {pvs_τ79 : Type*} {pvs_τ80 : Type*} (get_term_from_args : pvs_τ79 → pvs_τ80)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} {pvs_τ84 : Type*} (image : pvs_τ82 → pvs_τ83 → pvs_τ84)
variable {pvs_τ86 : Type*} {pvs_τ87 : Type*} (is_ac_sym_p : pvs_τ86 → pvs_τ87 → Prop)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (is_finite : pvs_τ88 → pvs_τ89)
variable {pvs_τ90 : Type*} (length : pvs_τ90 → Prop)
variable {pvs_τ91 : Type*} {pvs_τ92 : Type*} (map : pvs_τ91 → pvs_τ92 → Prop)
variable {pvs_τ93 : Type*} {pvs_τ94 : Type*} (member : pvs_τ93 → pvs_τ94 → Prop)
variable {pvs_τ99 : Type*} {pvs_τ100 : Type*} (nice_ind_p : pvs_τ99 → pvs_τ100 → Prop)
variable {pvs_τ102 : Type*} (no_pair_p : pvs_τ102 → Prop)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} (nth : pvs_τ103 → pvs_τ104 → Prop)
variable {pvs_τ105 : Type*} (null_p : pvs_τ105 → Prop)
variable {pvs_τ106 : Type*} {pvs_τ107 : Type*} (num_arg : pvs_τ106 → pvs_τ107 → Prop)
variable {pvs_τ108 : Type*} (ord : pvs_τ108 → Prop)
variable {pvs_τ109 : Type*} {pvs_τ110 : Type*} (pair : pvs_τ109 → pvs_τ110 → Prop)
variable {pvs_τ111 : Type*} (pair_p : pvs_τ111 → Prop)
variable {pvs_τ112 : Type*} {pvs_τ113 : Type*} {pvs_τ114 : Type*} (perm_p : pvs_τ112 → pvs_τ113 → pvs_τ114 → Prop)
variable {pvs_τ115 : Type*} {pvs_τ116 : Type*} {pvs_τ117 : Type*} (pos_p : pvs_τ115 → pvs_τ116 → pvs_τ117 → Prop)
variable {pvs_τ118 : Type*} {pvs_τ119 : Type*} {pvs_τ120 : Type*} {pvs_τ121 : Type*} (remove : pvs_τ118 → pvs_τ119 → pvs_τ120 → pvs_τ121)
variable {pvs_τ122 : Type*} {pvs_τ123 : Type*} {pvs_τ124 : Type*} (remove_common : pvs_τ122 → pvs_τ123 → pvs_τ124 → Prop)
variable {pvs_τ126 : Type*} {pvs_τ127 : Type*} (reverse : pvs_τ126 → pvs_τ127)
variable {pvs_τ128 : Type*} {pvs_τ129 : Type*} (same_func_p : pvs_τ128 → pvs_τ129 → Prop)
variable {pvs_τ130 : Type*} {pvs_τ131 : Type*} (sec_proj : pvs_τ130 → pvs_τ131)
variable {pvs_τ132 : Type*} {pvs_τ133 : Type*} {pvs_τ134 : Type*} {pvs_τ135 : Type*} (select : pvs_τ132 → pvs_τ133 → pvs_τ134 → pvs_τ135)
variable {pvs_τ140 : Type*} (singleton : pvs_τ140 → Prop)
variable {pvs_τ141 : Type*} (size2 : pvs_τ141 → Prop)
variable {pvs_τ144 : Type*} {pvs_τ145 : Type*} (subset_p : pvs_τ144 → pvs_τ145 → Prop)
variable {pvs_τ146 : Type*} {pvs_τ147 : Type*} (subterms : pvs_τ146 → pvs_τ147)
variable {pvs_τ148 : Type*} {pvs_τ149 : Type*} (sum : pvs_τ148 → pvs_τ149)
variable {pvs_τ152 : Type*} {pvs_τ153 : Type*} {pvs_τ154 : Type*} (union : pvs_τ152 → pvs_τ153 → pvs_τ154)
variable {pvs_τ155 : Type*} (var_p : pvs_τ155 → Prop)
variable {pvs_τ156 : Type*} (vars : pvs_τ156 → Prop)

-- pvs2lean: nominal_AC_substitution.subs
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET X = sigma_basic`1, s = sigma_basic`2 IN
--   CASES t
--     OF at(a): at(a),
--        *(pi, Y): IF X = Y THEN ext(pi)(s) ELSE pi * Y ENDIF,
--        unit: unit,
--        pair(t1, t2): pair(subs(sigma_basic, t
axiom subs {pvs_τ142 : Type*} {pvs_τ143 : Type*} : pvs_τ142 → pvs_τ143 → Prop

-- pvs2lean: nominal_AC_substitution.equal?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL X: equal?(G)(subs(sigma)(X), subs(delta)(X))
axiom equal_p {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → Prop

-- pvs2lean: nominal_AC_substitution.equal_syn?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL X: member(X, set_vars) IMPLIES subs(sigma)(X) = subs(delta)(X)
axiom equal_syn_p {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} : pvs_τ51 → pvs_τ52 → pvs_τ53 → Prop

-- pvs2lean: nominal_AC_substitution.more_general?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS delta2: equal?(G)(sigma, append(delta2, delta))
axiom more_general_p {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} {pvs_τ98 : Type*} : pvs_τ95 → pvs_τ96 → pvs_τ97 → pvs_τ98 → Prop

-- pvs2lean: nominal_AC_substitution.supset_dom
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(sigma) THEN emptyset
-- ELSE LET X = car(sigma)`1 IN add(X, supset_dom(cdr(sigma)))
-- ENDIF
axiom supset_dom {pvs_τ150 : Type*} {pvs_τ151 : Type*} : pvs_τ150 → pvs_τ151

-- pvs2lean: nominal_AC_substitution.supset_dom_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(X) /= *(null, X) IMPLIES member(X, supset_dom(sigma))
theorem supset_dom_correct : subs sigma(X) ≠ *(null, X) → member X (supset_dom sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.dom
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {X | subs(sigma)(X) /= *(null, X)}
axiom dom {pvs_τ46 : Type*} : pvs_τ46 → Prop

-- pvs2lean: nominal_AC_substitution.supset_dom_correct2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(dom(sigma), supset_dom(sigma))
theorem supset_dom_correct2 : subset_p (dom sigma) (supset_dom sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.img
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {t | EXISTS X: member(X, dom(sigma)) AND subs(sigma)(X) = t}
axiom img {pvs_τ85 : Type*} : pvs_τ85 → Prop

-- pvs2lean: nominal_AC_substitution.idempotent?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL X: subs(sigma)(subs(sigma)(X)) = subs(sigma)(X)
axiom idempotent_p {pvs_τ81 : Type*} : pvs_τ81 → Prop

-- pvs2lean: nominal_AC_substitution.nice?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(sigma) THEN TRUE
-- ELSE LET (X, t) = car(sigma) IN
--        (NOT member(X, vars(t)) AND
--          NOT member(X, supset_dom(cdr(sigma))) AND
--           disjoint?(vars(t), supset_dom(cdr(sigma))) AND
axiom nice_p {pvs_τ101 : Type*} : pvs_τ101 → Prop

-- pvs2lean: nominal_AC_substitution.well_formed?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    NOT pair?(subs(delta)(X)) AND
--     FORALL t:
--       member(t, subterms(subs(delta)(X))) AND ac_app?(t) IMPLIES
--        num_arg(ac_sym(t), t) > 1)
axiom well_formed_p {pvs_τ157 : Type*} : pvs_τ157 → Prop

-- pvs2lean: nominal_AC_substitution.contained?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(dom(delta), set_vars) AND subset?(vars(img(delta)), set_vars)
axiom contained_p {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ27 → pvs_τ28 → Prop

-- pvs2lean: nominal_AC_substitution.shift
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN cons((start, (null * (start + inc))), null)
-- ELSE cons((start, (null * (start + inc))), shift(start + 1, n - 1, inc))
-- ENDIF
axiom shift {pvs_τ136 : Type*} {pvs_τ137 : Type*} {pvs_τ138 : Type*} {pvs_τ139 : Type*} : pvs_τ136 → pvs_τ137 → pvs_τ138 → pvs_τ139

-- pvs2lean: nominal_AC_substitution.renaming?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL t: member(t, img(sigma)) IMPLIES var?(t) AND p(t) = null) AND
--  (FORALL X, Y:
--     member(X, dom(sigma)) AND
--      member(Y, dom(sigma)) AND subs(sigma)(X) = subs(sigma)(Y)
--      IMPLIES X = Y)
axiom renaming_p {pvs_τ125 : Type*} : pvs_τ125 → Prop

-- pvs2lean: nominal_AC_substitution.disjoint?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(union(dom(delta), vars(img(delta))),
--           union(dom(sigma), vars(img(sigma))))
axiom disjoint_p {pvs_τ44 : Type*} {pvs_τ45 : Type*} : pvs_τ44 → pvs_τ45 → Prop

-- pvs2lean: nominal_AC_substitution.subs_const
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(at(a)) = at(a)
theorem subs_const : subs sigma(at a) = at a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_unit
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(unit) = unit
theorem subs_unit : subs sigma(unit) = unit := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(pair(t, s)) = pair(subs(sigma)(t), subs(sigma)(s))
theorem subs_pair : subs sigma(pair t s) = pair (subs sigma(t)) (subs sigma(s)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(app(f, arg)) = app(f, subs(sigma)(arg))
theorem subs_app : subs sigma(app f arg) = app f (subs sigma(arg)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(ac_app(f, arg)) = ac_app(f, subs(sigma)(arg))
theorem subs_ac : subs sigma(ac_app f arg) = ac_app f (subs sigma(arg)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(abs(a, bd)) = abs(a, subs(sigma)(bd))
theorem subs_abs : subs sigma(|a, bd|) = |a, subs sigma(bd)| := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(subs(sigma)(t)) IMPLIES var?(t)
theorem subs_var : var_p (subs sigma(t)) → var_p t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_const2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atom?(subs(sigma)(t)) IMPLIES (atom?(t) OR var?(t))
theorem subs_const2 : atom_p (subs sigma(t)) → (atom_p t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_pair2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pair?(subs(sigma)(t)) IMPLIES (pair?(t) OR var?(t))
theorem subs_pair2 : pair_p (subs sigma(t)) → (pair_p t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_ac2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ac_app?(subs(sigma)(t)) IMPLIES (same_func?(subs(sigma)(t), t) OR var?(t))
theorem subs_ac2 : ac_app_p (subs sigma(t)) → (same_func_p (subs sigma(t)) t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_ac3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_ac_sym?(subs(sigma)(t), f) IMPLIES (is_ac_sym?(t, f) OR var?(t))
theorem subs_ac3 : is_ac_sym_p (subs sigma(t)) f → (is_ac_sym_p t f ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_is_ac_sym
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_ac_sym?(t, f) IMPLIES is_ac_sym?(subs(sigma)(t), f)
theorem subs_is_ac_sym : is_ac_sym_p t f → is_ac_sym_p (subs sigma(t)) f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_are_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: are_ac?(t, s, f) IMPLIES are_ac?(subs(sigma)(t), subs(sigma)(s), f)
theorem subs_are_ac : are_ac_p t s f → are_ac_p (subs sigma(t)) (subs sigma(s)) f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_func
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: func?(subs(sigma)(t)) IMPLIES (same_func?(subs(sigma)(t), t) OR var?(t))
theorem subs_func : func_p (subs sigma(t)) → (same_func_p (subs sigma(t)) t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_abs2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs?(subs(sigma)(t)) IMPLIES (abs?(t) OR var?(t))
theorem subs_abs2 : abs_p (subs sigma(t)) → (abs_p t ∨ var_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_basic_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma_basic, ext(pi)(t)) = ext(pi)(subs(sigma_basic, t))
theorem subs_basic_ext : subs sigma_basic (ext pi(t)) = ext pi(subs sigma_basic t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_ext_pi
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(pi * X) = ext(pi)(subs(sigma)(X))
theorem subs_ext_pi : subs sigma(pi * X) = ext pi(subs sigma(X)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_ext_pi2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(pi * X) = pi * X IMPLIES subs(sigma)(pi1 * X) = pi1 * X
theorem subs_ext_pi2 : subs sigma(pi * X) = pi * X → subs sigma(pi1 * X) = pi1 * X := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.basic_subs_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs((X, s), ext(pi)(t)) = ext(pi)(subs((X, s), t))
theorem basic_subs_ext : subs ((X, s)) (ext pi(t)) = ext pi(subs ((X, s)) t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(ext(pi)(t)) = ext(pi)(subs(sigma)(t))
theorem subs_ext : subs sigma(ext pi(t)) = ext pi(subs sigma(t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_fresh
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fresh?(a, subs(sigma)(t))`2 IMPLIES fresh?(a, t)`2
theorem subs_fresh : fresh_p a (subs sigma(t)).2 → fresh_p a t.2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_ord
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: t = subs(sigma)(s) IMPLIES
--  (ord(t) = ord(s) OR (var?(s) AND member(V(s), dom(sigma))))
theorem subs_ord : t = subs sigma(s) → (ord t = ord s ∨ (var_p s ∧ member (V s) (dom sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_var2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(t) IMPLIES (var?(subs(sigma)(t)) = var?(subs(sigma)(V(t))))
theorem subs_var2 : var_p t → (var_p (subs sigma(t)) = var_p (subs sigma(V t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_ac3_better
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_ac_sym?(subs(sigma)(t), f) IMPLIES
--  (is_ac_sym?(t, f) OR (var?(t) AND member(V(t), dom(sigma))))
theorem subs_ac3_better : is_ac_sym_p (subs sigma(t)) f → (is_ac_sym_p t f ∨ (var_p t ∧ member (V t) (dom sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_num_arg_ge1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_arg(f, t) > 1 IMPLIES num_arg(f, subs(sigma)(t)) > 1
theorem subs_num_arg_ge1 : num_arg f t > 1 → num_arg f (subs sigma(t)) > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_select_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS i:
--   nice_ind?(i, num_arg(f, t)) AND
--    LET ti = select(f, t)(i) IN
--      (select(f, subs(sigma)(t))(n) = subs(sigma)(ti) OR
--        (var?(ti) AND
--          member(select(f, subs(sigma)(t))(n),
--   
theorem subs_select_img : ∃ (i : {i : ℝ // i ≥ 0}), nice_ind_p i (num_arg f t) ∧ (let ti := select f t(i); (select f (subs sigma(t)) (n) = subs sigma(ti) ∨
    (var_p ti ∧
      member (select f (subs sigma(t)) (n)) (ext (p ti) (subterms (img sigma)))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subs(sigma)(args)) IFF
--  EXISTS s: member(s, args) AND t = subs(sigma)(s)
theorem subs_mem : member t (subs sigma(args)) ↔ ∃ s,  member s args ∧ t = subs sigma(s) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_len
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(subs(sigma)(args)) = length(args)
theorem subs_len : List.length (subs sigma(args)) = List.length args := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(append(args1, args2)) =
--  append(subs(sigma)(args1), subs(sigma)(args2))
theorem subs_append : subs sigma(append args1 args2) = append (subs sigma(args1)) (subs sigma(args2)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_append2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(append(sigma, delta))(t) = subs(sigma)(subs(delta)(t))
theorem subs_append2 : subs (append sigma delta) (t) = subs sigma(subs delta(t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_map
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(subs(sigma))(args) = subs(sigma)(args)
theorem subs_map : map (subs sigma) (args) = subs sigma(args) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_append_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(append(sigma, delta))(args) = subs(sigma)(subs(delta)(args))
theorem subs_append_args : subs (append sigma delta) (args) = subs sigma(subs delta(args)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_map_append_lst_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(subs(append(sigma, delta)))(lst_args) =
--  map(subs(sigma))(map(subs(delta))(lst_args))
theorem subs_map_append_lst_args : map (subs (append sigma delta)) (lst_args) = map (subs sigma) (map (subs delta) (lst_args)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(cons((X, t), delta))(args1) =
--  subs(cons((X, t), null))(subs(delta)(args1))
theorem subs_cons : subs (cons ((X, t)) delta) (args1) = subs (cons ((X, t)) null) (subs delta(args1)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_get_term_from_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(get_term_from_args(args1)) =
--  get_term_from_args(subs(sigma)(args1))
theorem subs_get_term_from_args : subs sigma(get_term_from_args args1) = get_term_from_args (subs sigma(args1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_get_ac_from_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(get_ac_from_args(f)(args)) =
--  get_ac_from_args(f)(subs(sigma)(args))
theorem subs_get_ac_from_args : subs sigma(get_ac_from_args f(args)) = get_ac_from_args f(subs sigma(args)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_args_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(args) IMPLIES
--  nth(subs(sigma)(args), i) = subs(sigma)(nth(args, i))
theorem subs_args_nth : i < List.length args → nth (subs sigma(args)) i = subs sigma(nth args i) := by
  -- === TCCs for subs_args_nth ===
  -- TCC: subs_args_nth_TCC1
  -- Statement: FORALL (args: args, i: nat, sigma: sub):
  --   i < length(args) IMPLIES
  --    i <
  --     length[first_order_AC_term[constant, variable, f_symbol, ac_symbol]]
  --         (subs(sigma)(args))
  have subs_args_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: subs_args_nth_TCC1
  -- Statement: FORALL (args: args, i: nat, sigma: sub):
  --   i < length(args) IMPLIES
  --    i <
  --     length[nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]]
  --         (subs(sigma)(args))
  have subs_args_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: remove(subs(sigma)(args), i) = subs(sigma)(remove(args, i))
theorem subs_remove : remove (subs sigma(args)) i = subs sigma(remove args i) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.args_subs_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: get_args(f)(subs(sigma)(t)) =
--  flatten(map(get_args(f))(map(subs(sigma))(get_args(f)(t))))
theorem args_subs_ac : get_args f(subs sigma(t)) = flatten (map (get_args f) (map (subs sigma) (get_args f(t)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.mem_get_args_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, get_args(f)(subs(sigma)(t))) IMPLIES
--  EXISTS ti:
--    (member(ti, get_args(f)(t)) AND
--      (s = subs(sigma)(ti) OR
--        var?(ti) AND member(s, ext(p(ti))(subterms(img(sigma))))))
theorem mem_get_args_subs : member s (get_args f(subs sigma(t))) → ∃ ti, 
  (member ti (get_args f(t)) ∧
    (s = subs sigma(ti) ∨
      var_p ti ∧ member s (ext (p ti) (subterms (img sigma))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_get_args1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_arg(f, t) = 1 IMPLIES
--  get_args(f)(subs(sigma)(t)) = get_args(f)(subs(sigma)(select(f, t)(i)))
theorem subs_get_args1 : num_arg f t = 1 → get_args f(subs sigma(t)) = get_args f(subs sigma(select f t(i))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_perm_get_args_ge1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_arg(f, t) > 1 IMPLIES
--  perm?(G)
--       (get_args(f)(subs(sigma)(t)),
--        append(get_args(f)(subs(sigma)(select(f, t)(i))),
--               get_args(f)(subs(sigma)(delete(f, t)(i)))))
theorem subs_perm_get_args_ge1 : num_arg f t > 1 → perm_p G
     (get_args f(subs sigma(t)),
      append (get_args f(subs sigma(select f t(i)))) (get_args f(subs sigma(delete f t(i))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.mem_get_args_subs4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(ti, get_args(f)(t)) AND member(s, get_args(f)(subs(sigma)(ti)))
--  IMPLIES member(s, get_args(f)(subs(sigma)(t)))
theorem mem_get_args_subs4 : member ti (get_args f(t)) ∧ member s (get_args f(subs sigma(ti))) → member s (get_args f(subs sigma(t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_get_repeat_lst
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(get_repeat_lst(t, i)) = get_repeat_lst(subs(sigma)(t), i)
theorem subs_get_repeat_lst : subs sigma(get_repeat_lst t i) = get_repeat_lst (subs sigma(t)) i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_get_repeat_lst_map
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(subs(sigma))(get_repeat_lst(args, i)) =
--  get_repeat_lst(subs(sigma)(args), i)
theorem subs_get_repeat_lst_map : map (subs sigma) (get_repeat_lst args i) = get_repeat_lst (subs sigma(args)) i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_get_lst_mult
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(get_lst_mult(args, coef)) =
--  get_lst_mult(subs(sigma)(args), coef)
theorem subs_get_lst_mult : subs sigma(get_lst_mult args coef) = get_lst_mult (subs sigma(args)) coef := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_get_lst_mult2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(subs(sigma))(get_lst_mult(lst_args, coef)) =
--  get_lst_mult(map(subs(sigma))(lst_args), coef)
theorem subs_get_lst_mult2 : map (subs sigma) (get_lst_mult lst_args coef) = get_lst_mult (map (subs sigma) (lst_args)) coef := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.perm?_get_args_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: perm?(G)(subs(sigma)(get_args(f)(t)), subs(sigma)(get_args(f)(s))) IMPLIES
--  perm?(G)(get_args(f)(subs(sigma)(t)), get_args(f)(subs(sigma)(s)))
theorem perm_p_get_args_subs : perm_p G(subs sigma(get_args f(t)), subs sigma(get_args f(s))) → perm_p G(get_args f(subs sigma(t)), get_args f(subs sigma(s))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_flatten
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(flatten(lst_args)) = flatten(map(subs(sigma))(lst_args))
theorem subs_flatten : subs sigma(flatten lst_args) = flatten (map (subs sigma) (lst_args)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.vars_subs_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(subs(sigma)(t)), union(vars(img(sigma)), vars(t)))
theorem vars_subs_t : subset_p (vars (subs sigma(t))) (union (vars (img sigma)) (vars t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.vars_subs_t2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(subs(sigma)(t))) IMPLIES
--  (EXISTS Y: member(Y, vars(t)) AND member(X, vars(subs(sigma)(Y))))
theorem vars_subs_t2 : member X (vars (subs sigma(t))) → (∃ Y,  member Y (vars t) ∧ member X (vars (subs sigma(Y)))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.basic_sub_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, vars(t)) IMPLIES subs((X, s), t) = t
theorem basic_sub_no_effect : ¬ member X (vars t) → subs ((X, s)) t = t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.basic_sub_elim_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, vars(s)) IMPLIES NOT member(X, vars(subs((X, s), t)))
theorem basic_sub_elim_var : ¬ member X (vars s) → ¬ member X (vars (subs ((X, s)) t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.basic_sub_not_elim_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: X /= Y AND NOT member(Y, vars(s)) AND member(Y, vars(subs((X, s), t)))
--  IMPLIES member(Y, vars(t))
theorem basic_sub_not_elim_var : X ≠ Y ∧ ¬ member Y (vars s) ∧ member Y (vars (subs ((X, s)) t)) → member Y (vars t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.dom_basic_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dom(cons((X, t), null)) = singleton(X) OR member(X, vars(t))
theorem dom_basic_sub : dom (cons ((X, t)) null) = singleton X ∨ member X (vars t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.dom_basic_sub2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dom(cons((X, t), null)) = singleton(X) OR dom(cons((X, t), null)) = emptyset
theorem dom_basic_sub2 : dom (cons ((X, t)) null) = singleton X ∨ dom (cons ((X, t)) null) = emptyset := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.dom_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dom(null) = emptyset
theorem dom_null : dom null = emptyset := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.dom_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(dom(append(delta, sigma)), union(dom(delta), dom(sigma)))
theorem dom_append : subset_p (dom (append delta sigma)) (union (dom delta) (dom sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.img_basic_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: img(cons((X, t), null)) = singleton(t) OR member(X, vars(t))
theorem img_basic_sub : img (cons ((X, t)) null) = singleton t ∨ member X (vars t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.img_basic_sub2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: img(cons((X, t), null)) = singleton(t) OR img(cons((X, t), null)) = emptyset
theorem img_basic_sub2 : img (cons ((X, t)) null) = singleton t ∨ img (cons ((X, t)) null) = emptyset := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.img_basic_sub_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(img(cons((X, t), null))), vars(t))
theorem img_basic_sub_var : subset_p (vars (img (cons ((X, t)) null))) (vars t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.img_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: img(null) = emptyset
theorem img_null : img null = emptyset := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.img_dom_emptyset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dom(sigma) = emptyset IMPLIES img(sigma) = emptyset
theorem img_dom_emptyset : dom sigma = emptyset → img sigma = emptyset := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.img_dom_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dom(sigma) = singleton(X) IMPLIES img(sigma) = singleton(subs(sigma)(X))
theorem img_dom_singleton : dom sigma = singleton X → img sigma = singleton (subs sigma(X)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_no_effect_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(t)) IMPLIES subs(sigma)(t) = t
theorem subs_no_effect_t : disjoint_p (dom sigma) (vars t) → subs sigma(t) = t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_no_effect_t2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(t) = t IMPLIES disjoint?(dom(sigma), vars(t))
theorem subs_no_effect_t2 : subs sigma(t) = t → disjoint_p (dom sigma) (vars t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.dom_append2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(vars(img(sigma)), dom(delta)) AND
--  disjoint?(dom(delta), dom(sigma))
--  IMPLIES dom(append(delta, sigma)) = union(dom(delta), dom(sigma))
theorem dom_append2 : disjoint_p (vars (img sigma)) (dom delta) ∧
 disjoint_p (dom delta) (dom sigma) → dom (append delta sigma) = union (dom delta) (dom sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_no_effect_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(args)) IMPLIES subs(sigma)(args) = args
theorem subs_no_effect_args : disjoint_p (dom sigma) (vars args) → subs sigma(args) = args := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_no_effect_lst_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(lst_args)) IMPLIES
--  map(subs(sigma))(lst_args) = lst_args
theorem subs_no_effect_lst_args : disjoint_p (dom sigma) (vars lst_args) → map (subs sigma) (lst_args) = lst_args := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.apply_sub_elim_var_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, dom(sigma)) AND NOT member(X, vars(img(sigma))) IMPLIES
--  NOT member(X, vars(subs(sigma)(t)))
theorem apply_sub_elim_var_t : member X (dom sigma) ∧ ¬ member X (vars (img sigma)) → ¬ member X (vars (subs sigma(t))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.apply_sub_not_elim_var_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(t)) AND NOT member(X, dom(sigma)) IMPLIES
--  member(X, vars(subs(sigma)(t)))
theorem apply_sub_not_elim_var_t : member X (vars t) ∧ ¬ member X (dom sigma) → member X (vars (subs sigma(t))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.vars_img_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(img(append(delta, sigma))),
--         union(vars(img(delta)), vars(img(sigma))))
theorem vars_img_append : subset_p (vars (img (append delta sigma))) (union (vars (img delta)) (vars (img sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.idempotent_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(null[basic_sub])
theorem idempotent_null : idempotent_p null := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.var_dom_sub_dif_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(t)) AND member(X, dom(sigma)) IMPLIES subs(sigma)(t) /= t
theorem var_dom_sub_dif_term : member X (vars t) ∧ member X (dom sigma) → subs sigma(t) ≠ t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.idempotent_disjoint_dom_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) IMPLIES disjoint?(dom(sigma), vars(img(sigma)))
theorem idempotent_disjoint_dom_img : idempotent_p sigma → disjoint_p (dom sigma) (vars (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.idempotent_disjoint_dom_img2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(img(sigma))) IMPLIES idempotent?(sigma)
theorem idempotent_disjoint_dom_img2 : disjoint_p (dom sigma) (vars (img sigma)) → idempotent_p sigma := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.idempotent_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, vars(t)) IMPLIES idempotent?(cons((X, ext(pi)(t)), null))
theorem idempotent_cons : ¬ member X (vars t) → idempotent_p (cons ((X, ext pi(t))) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.apply_sub_elim_var_t2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, dom(sigma)) AND idempotent?(sigma) IMPLIES
--  NOT member(X, vars(subs(sigma)(t)))
theorem apply_sub_elim_var_t2 : member X (dom sigma) ∧ idempotent_p sigma → ¬ member X (vars (subs sigma(t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(singleton(t)) = singleton(subs(sigma)(t))
theorem subs_singleton : subs sigma(singleton t) = singleton (subs sigma(t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.img_append_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(img(append(sigma, delta)),
--         union(image(subs(sigma))(img(delta)), img(sigma)))
theorem img_append_subset : subset_p (img (append sigma delta)) (union (image (subs sigma) (img delta)) (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.idempotent_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) AND
--  idempotent?(delta) AND disjoint?(dom(delta), vars(img(sigma)))
--  IMPLIES idempotent?(append(sigma, delta))
theorem idempotent_append : idempotent_p sigma ∧
 idempotent_p delta ∧ disjoint_p (dom delta) (vars (img sigma)) → idempotent_p (append sigma delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.idempotent_subs_subs_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) IMPLIES subs(sigma)(subs(sigma)(t)) = subs(sigma)(t)
theorem idempotent_subs_subs_t : idempotent_p sigma → subs sigma(subs sigma(t)) = subs sigma(t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_divide_not_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT var?(subs(sigma)(X)) IMPLIES
--  EXISTS delta, delta2, sigma_basic, pi, Y:
--    sigma = append(delta, cons(sigma_basic, delta2)) AND
--     subs(delta2)(X) = pi * Y AND
--      sigma_basic`1 = Y AND NOT var?
theorem subs_divide_not_var : ¬ var_p (subs sigma(X)) → ∃ delta delta2 sigma_basic pi Y, 
  sigma = append delta (cons sigma_basic delta2) ∧
   subs delta2(X) = pi * Y ∧
    sigma_basic.1 = Y ∧ ¬ var_p sigma_basic.2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.basic_sub_info
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sigma = cons((X, t), null) IN
--   NOT member(X, vars(t)) IMPLIES
--    dom(sigma) = singleton(X) AND
--     img(sigma) = singleton(t) AND idempotent?(sigma)
theorem basic_sub_info : fun (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => ¬ member X (vars t) → dom sigma = singleton X ∧
 img sigma = singleton t ∧ idempotent_p sigma (cons ((X, t)) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.basic_sub_well_formed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sigma = cons((X, t), null) IN
--   well_formed?(t) IMPLIES well_formed?(sigma)
theorem basic_sub_well_formed : fun (sigma : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => well_formed_p t → well_formed_p sigma (cons ((X, t)) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subterms_subs_x
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(subterms(subs(sigma)(X)), add(null * X, subterms(img(sigma))))
theorem subterms_subs_x : subset_p (subterms (subs sigma(X))) (add (null * X) (subterms (img sigma))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subterms_subs2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(subs(sigma)(s))) IMPLIES
--  EXISTS t1:
--    member(t1, subterms(s)) AND
--     (t = subs(sigma)(t1) OR
--       (var?(t1) AND member(t, ext(p(t1))(subterms(img(sigma))))))
theorem subterms_subs2 : member t (subterms (subs sigma(s))) → ∃ t1, 
  member t1 (subterms s) ∧
   (t = subs sigma(t1) ∨
     (var_p t1 ∧ member t (ext (p t1) (subterms (img sigma))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_no_effect_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(t) = t AND member(s, subterms(t)) IMPLIES subs(sigma)(s) = s
theorem subs_no_effect_subterm : subs sigma(t) = t ∧ member s (subterms t) → subs sigma(s) = s := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.same_func_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: func?(t) IMPLIES same_func?(t, subs(sigma)(t))
theorem same_func_subs : func_p t → same_func_p t (subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.flatten_pair_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, flatten_pair(subs(sigma)(t))) AND no_pair?(img(sigma)) IMPLIES
--  EXISTS ti: member(ti, flatten_pair(t)) AND s = subs(sigma)(ti)
theorem flatten_pair_subs : member s (flatten_pair (subs sigma(t))) ∧ no_pair_p (img sigma) → ∃ ti,  member ti (flatten_pair t) ∧ s = subs sigma(ti) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.mem_get_args_subs2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, get_args(f)(subs(sigma)(t))) IMPLIES
--  (EXISTS ti:
--     member(ti, get_args(f)(t)) AND
--      LET ti_sigma = subs(sigma)(ti) IN
--        (s = ti_sigma OR
--          (var?(ti) AND
--            member(V
theorem mem_get_args_subs2 : member s (get_args f(subs sigma(t))) → (∃ ti, 
   member ti (get_args f(t)) ∧
    (let ti_sigma := subs sigma(ti); (s = ti_sigma ∨
        (var_p ti ∧
          member (V ti) (dom sigma) ∧ member s (get_args f(ti_sigma)))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.mem_get_args_subs3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, get_args(f)(subs(sigma)(t))) IMPLIES
--  (EXISTS ti:
--     member(ti, get_args(f)(t)) AND
--      LET ti_sigma = subs(sigma)(ti) IN
--        ((s = ti_sigma AND ord(s) = ord(ti)) OR
--          (var?(ti) 
theorem mem_get_args_subs3 : member s (get_args f(subs sigma(t))) → (∃ ti, 
   member ti (get_args f(t)) ∧
    (let ti_sigma := subs sigma(ti); ((s = ti_sigma ∧ ord s = ord ti) ∨
        (var_p ti ∧
          member (V ti) (dom sigma) ∧ member s (get_args f(ti_sigma)))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.supset_dom_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, supset_dom(delta)) IFF EXISTS t: member((X, t), delta)
theorem supset_dom_equiv : member X (supset_dom delta) ↔ ∃ t,  member ((X, t)) delta := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.supset_dom_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: supset_dom(append(sigma, delta)) =
--  union(supset_dom(sigma), supset_dom(delta))
theorem supset_dom_append : supset_dom (append sigma delta) = union (supset_dom sigma) (supset_dom delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.nice_disjoint_dom_img
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(sigma) IMPLIES disjoint?(dom(sigma), vars(img(sigma)))
theorem nice_disjoint_dom_img : nice_p sigma → disjoint_p (dom sigma) (vars (img sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.nice_idempotent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(sigma) IMPLIES idempotent?(sigma)
theorem nice_idempotent : nice_p sigma → idempotent_p sigma := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.nice_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(append(sigma, delta)) IMPLIES nice?(sigma) AND nice?(delta)
theorem nice_append : nice_p (append sigma delta) → nice_p sigma ∧ nice_p delta := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.nice_append2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(delta) AND
--  nice?(sigma) AND
--   (FORALL X, t:
--      member((X, t), sigma) IMPLIES
--       NOT member(X, supset_dom(delta)) AND
--        disjoint?(vars(t), supset_dom(delta)))
--  IMPLIES nice?(append(sig
theorem nice_append2 : nice_p delta ∧
 nice_p sigma ∧
  (∀ X t, 
     member ((X, t)) sigma →
      ¬ member X (supset_dom delta) ∧
       disjoint_p (vars t) (supset_dom delta)) → nice_p (append sigma delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.nice_append_supset_dom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nice?(append(sigma, delta)) AND member(X, supset_dom(sigma)) IMPLIES
--  NOT member(X, supset_dom(delta))
theorem nice_append_supset_dom : nice_p (append sigma delta) ∧ member X (supset_dom sigma) → ¬ member X (supset_dom delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.vars_img_sec_proj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(img(sigma)), vars(sec_proj(sigma)))
theorem vars_img_sec_proj : subset_p (vars (img sigma)) (vars (sec_proj sigma)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.well_formed_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(null)
theorem well_formed_null : well_formed_p null := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.well_formed_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(sigma) AND well_formed?(delta) IMPLIES
--  well_formed?(append(sigma, delta))
theorem well_formed_append : well_formed_p sigma ∧ well_formed_p delta → well_formed_p (append sigma delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.well_formed_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(sigma) AND well_formed?(t) IMPLIES well_formed?(subs(sigma)(t))
theorem well_formed_subs : well_formed_p sigma ∧ well_formed_p t → well_formed_p (subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.well_formed_num_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(sigma) IMPLIES num_arg(subs(sigma)(t)) = num_arg(t)
theorem well_formed_num_arg : well_formed_p sigma → num_arg (subs sigma(t)) = num_arg t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.alm_well_formed_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(sigma) AND alm_well_formed?(t) IMPLIES
--  alm_well_formed?(subs(sigma)(t))
theorem alm_well_formed_subs : well_formed_p sigma ∧ alm_well_formed_p t → alm_well_formed_p (subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.well_formed_basic_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(t) IMPLIES well_formed?(cons((X, ext(pi)(t)), null))
theorem well_formed_basic_sub : well_formed_p t → well_formed_p (cons ((X, ext pi(t))) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(delta, sigma) IMPLIES equal?(G)(subs(delta)(t), subs(sigma)(t))
theorem equal_sub_equiv : equal_p G(delta, sigma) → equal_p G(subs delta(t), subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_equiv_alt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(delta, sigma, set_vars) AND subset?(vars(t), set_vars) IMPLIES
--  equal?(G)(subs(delta)(t), subs(sigma)(t))
theorem equal_sub_equiv_alt : equal_p G(delta, sigma, set_vars) ∧ subset_p (vars t) set_vars → equal_p G(subs delta(t), subs sigma(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_change_eq_terms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(delta, sigma) IMPLIES
--  (equal?(G)(subs(delta)(t), subs(delta)(s)) IFF
--    equal?(G)(subs(sigma)(t), subs(sigma)(s)))
theorem equal_sub_change_eq_terms : equal_p G(delta, sigma) → (equal_p G(subs delta(t), subs delta(s)) ↔
  equal_p G(subs sigma(t), subs sigma(s))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_ref
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(sigma, sigma)
theorem equal_sub_ref : equal_p G sigma sigma := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_sym
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(delta, gamma) = equal?(G)(gamma, delta)
theorem equal_sub_sym : equal_p G(delta, gamma) = equal_p G(gamma, delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_trans
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(delta, gamma) AND equal?(G)(gamma, sigma) IMPLIES
--  equal?(G)(delta, sigma)
theorem equal_sub_trans : equal_p G(delta, gamma) ∧ equal_p G(gamma, sigma) → equal_p G(delta, sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(sigma, sigma1) IMPLIES
--  equal?(G)(append(sigma, delta), append(sigma1, delta))
theorem equal_sub_append : equal_p G(sigma, sigma1) → equal_p G(append sigma delta, append sigma1 delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(sigma, sigma1, set_vars) AND subset?(set_vars1, set_vars) IMPLIES
--  equal?(G)(sigma, sigma1, set_vars1)
theorem equal_sub_subset : equal_p G(sigma, sigma1, set_vars) ∧ subset_p set_vars1 set_vars → equal_p G(sigma, sigma1, set_vars1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_res_sym
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(sigma, sigma1, set_vars) = equal?(G)(sigma1, sigma, set_vars)
theorem equal_sub_res_sym : equal_p G(sigma, sigma1, set_vars) = equal_p G(sigma1, sigma, set_vars) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_sub_res_trans
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(delta, gamma, set_vars) AND equal?(G)(gamma, sigma, set_vars)
--  IMPLIES equal?(G)(delta, sigma, set_vars)
theorem equal_sub_res_trans : equal_p G(delta, gamma, set_vars) ∧ equal_p G(gamma, sigma, set_vars) → equal_p G(delta, sigma, set_vars) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_syn_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal_syn?(sigma, sigma1, vars(t)) IMPLIES subs(sigma)(t) = subs(sigma1)(t)
theorem equal_syn_t : equal_syn_p sigma sigma1 (vars t) → subs sigma(t) = subs sigma1(t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_syn_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal_syn?(sigma, sigma1, V1) IMPLIES
--  equal_syn?(append(delta, sigma), append(delta, sigma1), V1)
theorem equal_syn_append : equal_syn_p sigma sigma1 V1 → equal_syn_p (append delta sigma) (append delta sigma1) V1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_syn_append2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal_syn?(delta, delta1, V1) AND subset?(vars(img(sigma)), V1) IMPLIES
--  equal_syn?(append(delta, sigma), append(delta1, sigma), V1)
theorem equal_syn_append2 : equal_syn_p delta delta1 V1 ∧ subset_p (vars (img sigma)) V1 → equal_syn_p (append delta sigma) (append delta1 sigma) V1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_syn_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal_syn?(sigma, sigma1, V1) AND subset?(V2, V1) IMPLIES
--  equal_syn?(sigma, sigma1, V2)
theorem equal_syn_subset : equal_syn_p sigma sigma1 V1 ∧ subset_p V2 V1 → equal_syn_p sigma sigma1 V2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.equal_syn_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal_syn?(sigma, sigma1, V1) IMPLIES equal?(G)(sigma, sigma1, V1)
theorem equal_syn_equal : equal_syn_p sigma sigma1 V1 → equal_p G(sigma, sigma1, V1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.idempotent_append_cond
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) AND
--  NOT member(X, vars(t)) AND
--   NOT member(X, dom(sigma)) AND disjoint?(vars(t), dom(sigma))
--  IMPLIES idempotent?(append(cons((X, t), null), sigma))
theorem idempotent_append_cond : idempotent_p sigma ∧
 ¬ member X (vars t) ∧
  ¬ member X (dom sigma) ∧ disjoint_p (vars t) (dom sigma) → idempotent_p (append (cons ((X, t)) null) sigma) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_size2_map_get_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, args) AND length(args) > 1 IMPLIES
--  size2(subs(delta)(t)) <
--   sum(map(size2)(flatten(map(get_args(f))(map(subs(delta))(args)))))
theorem subs_size2_map_get_args : member t args ∧ List.length args > 1 → size2 (subs delta(t)) <
 sum (map size2(flatten (map (get_args f) (map (subs delta) (args))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_not_same_size2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(s)) AND
--  NOT var?(s) AND
--   (FORALL t:
--      member(t, subterms(s)) AND ac_app?(t) IMPLIES
--       num_arg(ac_sym(t), t) > 1)
--  IMPLIES size2(subs(delta)(X)) < size2(subs(delta)(s))
theorem subs_not_same_size2 : member X (vars s) ∧
 ¬ var_p s ∧
  (∀ t, 
     member t (subterms s) ∧ ac_app_p t →
      num_arg (ac_sym t) t > 1) → size2 (subs delta(X)) < size2 (subs delta(s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_equal_impossible
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(s)) AND
--  NOT var?(s) AND
--   (FORALL t:
--      member(t, subterms(s)) AND ac_app?(t) IMPLIES
--       num_arg(ac_sym(t), t) > 1)
--  IMPLIES NOT equal?(G)(subs(delta)(s), subs(delta)(X))
theorem subs_equal_impossible : member X (vars s) ∧
 ¬ var_p s ∧
  (∀ t, 
     member t (subterms s) ∧ ac_app_p t →
      num_arg (ac_sym t) t > 1) → ¬ equal_p G(subs delta(s), subs delta(X)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_equal_impossible2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(s)) AND
--  NOT var?(s) AND
--   (FORALL t:
--      member(t, subterms(s)) AND ac_app?(t) IMPLIES
--       num_arg(ac_sym(t), t) > 1)
--  IMPLIES NOT equal?(G)(subs(delta)(s), subs(delta)(pi * X))
theorem subs_equal_impossible2 : member X (vars s) ∧
 ¬ var_p s ∧
  (∀ t, 
     member t (subterms s) ∧ ac_app_p t →
      num_arg (ac_sym t) t > 1) → ¬ equal_p G(subs delta(s), subs delta(pi * X)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.count_subs_get_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: count(s, subs(sigma)(t), f, G) =
--  sum(count_vec(subs(sigma)(get_args(f)(t)), f, G)(s))
theorem count_subs_get_args : count s (subs sigma(t)) f G = sum (count_vec (subs sigma(get_args f(t))) f G(s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_inst
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = cons((X, t), null) IN
--   equal?(G)(subs(sigma)(X), subs(sigma)(t)) IMPLIES
--    equal?(G)(sigma, append(sigma, delta))
theorem subs_inst : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => equal_p G(subs sigma(X), subs sigma(t)) → equal_p G(sigma, append sigma delta) (cons ((X, t)) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_inst_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = cons((X, ext(reverse(pi))(s)), null) IN
--   NOT member(X, vars(s)) IMPLIES
--    equal?(G)(subs(sigma)(subs(delta)(pi * X)), subs(sigma)(subs(delta)(s)))
theorem subs_inst_correct : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => ¬ member X (vars s) → equal_p G(subs sigma(subs delta(pi * X)), subs sigma(subs delta(s))) (cons ((X, ext (reverse pi) (s))) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_unify_alg_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(delta, append(delta1, append(sigma1, sigma))) AND
--  disjoint?(dom(sigma), union(vars(t), vars(s))) AND
--   idempotent?(sigma1) AND disjoint?(dom(sigma), vars(img(sigma1)))
--  IMPLIES
--  (equal?(G)(
theorem subs_unify_alg_aux : equal_p G(delta, append delta1 (append sigma1 sigma)) ∧
 disjoint_p (dom sigma) (union (vars t) (vars s)) ∧
  idempotent_p sigma1 ∧ disjoint_p (dom sigma) (vars (img sigma1)) → (equal_p G(subs delta(subs sigma1(t)), subs delta(subs sigma1(s))) ↔
  equal_p G(subs delta(t), subs delta(s))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_unify_alg_aux2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: more_general?(G)(append(sigma1, sigma), delta) AND
--  idempotent?(sigma1) AND
--   disjoint?(dom(sigma), V1) AND contained?(sigma1, V1)
--  IMPLIES equal?(G)(delta, append(delta, sigma1))
theorem subs_unify_alg_aux2 : more_general_p G(append sigma1 sigma, delta) ∧
 idempotent_p sigma1 ∧
  disjoint_p (dom sigma) V1 ∧ contained_p sigma1 V1 → equal_p G(delta, append delta sigma1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.contained_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: contained?(delta, set_vars) AND contained?(sigma, set_vars) IMPLIES
--  contained?(append(delta, sigma), set_vars)
theorem contained_append : contained_p delta set_vars ∧ contained_p sigma set_vars → contained_p (append delta sigma) set_vars := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.contained_supset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: contained?(delta, set_vars) AND subset?(set_vars, set_vars1) IMPLIES
--  contained?(delta, set_vars1)
theorem contained_supset : contained_p delta set_vars ∧ subset_p set_vars set_vars1 → contained_p delta set_vars1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.more_general_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(sigma, sigma1) IMPLIES
--  more_general?(G)(delta, sigma) = more_general?(G)(delta, sigma1)
theorem more_general_equal : equal_p G(sigma, sigma1) → more_general_p G(delta, sigma) = more_general_p G(delta, sigma1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.more_general_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: more_general?(G)(sigma, sigma1) IMPLIES
--  more_general?(G)(append(sigma, delta), append(sigma1, delta))
theorem more_general_append : more_general_p G(sigma, sigma1) → more_general_p G(append sigma delta, append sigma1 delta) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.more_general_supset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: more_general?(G)(sigma, append(delta1, delta), set_vars1) AND
--  subset?(set_vars, set_vars1) AND
--   disjoint?(dom(delta1), set_vars) AND contained?(delta, set_vars)
--  IMPLIES more_general?(G)(sigma, delt
theorem more_general_supset : more_general_p G(sigma, append delta1 delta, set_vars1) ∧
 subset_p set_vars set_vars1 ∧
  disjoint_p (dom delta1) set_vars ∧ contained_p delta set_vars → more_general_p G(sigma, delta, set_vars) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.more_general_is_more_general
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: more_general?(G)(sigma, sigma1) IMPLIES more_general?(G)(sigma, sigma1, V1)
theorem more_general_is_more_general : more_general_p G(sigma, sigma1) → more_general_p G(sigma, sigma1, V1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_ord
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ord(subs(rho)(t)) = ord(t)
theorem renaming_ord : ord (subs rho(t)) = ord t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_is_ac_sym
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_ac_sym?(subs(rho)(t), f) = is_ac_sym?(t, f)
theorem renaming_is_ac_sym : is_ac_sym_p (subs rho(t)) f = is_ac_sym_p t f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_ord2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (atom?(t) IFF atom?(subs(rho)(t))) AND (var?(t) IFF var?(subs(rho)(t)))
--  AND (t = unit IFF subs(rho)(t) = unit)
--  AND (pair?(t) IFF pair?(subs(rho)(t))) AND (app?(t) IFF app?(subs(rho)(t)))
--  AND (
theorem renaming_ord2 : (atom_p t ↔ atom_p (subs rho(t))) ∧ (var_p t ↔ var_p (subs rho(t))) ∧ (t = unit ↔ subs rho(t) = unit)
 ∧ (pair_p t ↔ pair_p (subs rho(t))) ∧ (app_p t ↔ app_p (subs rho(t)))
 ∧ (ac_app_p t ↔ ac_app_p (subs rho(t)))
 ∧ (abs_p t ↔ abs_p (subs rho(t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(subs(rho)(X))
theorem renaming_var : var_p (subs rho X) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_susp_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(subs(rho)(X)) AND subs(rho)(pi * X) = pi * V(subs(rho)(X))
theorem renaming_susp_var : var_p (subs rho(X)) ∧ subs rho(pi * X) = pi * V (subs rho(X)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_var3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(t) IMPLIES V(subs(rho)(t)) = V(subs(rho)(V(t)))
theorem renaming_var3 : var_p t → V (subs rho(t)) = V (subs rho(V t)) := by
  -- === TCCs for renaming_var3 ===
  -- TCC: renaming_var3_TCC1
  -- Statement: FORALL (rho: renaming,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   var?(t) IMPLIES
  --    var?[atom, perm, variable, f_symbol, ac_symbol](subs(rho)(t))
  have renaming_var3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_var3_TCC2
  -- Statement: FORALL (rho: renaming,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   var?(t) IMPLIES
  --    var?[atom, perm, variable, f_symbol, ac_symbol]
  --        (subs(rho)(V[atom, perm, v...
  have renaming_var3_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_susp_var2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(s) IMPLIES var?(subs(rho)(s)) AND p(subs(rho)(s)) = p(s)
theorem renaming_susp_var2 : var_p s → var_p (subs rho(s)) ∧ p (subs rho(s)) = p s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_susp_var_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: var?(s) IMPLIES
--  ext(reverse(p(subs(rho)(s))))(subs(rho)(t)) =
--   subs(rho)(ext(reverse(p(s)))(t))
theorem renaming_susp_var_ext : var_p s → ext (reverse (p (subs rho(s)))) (subs rho(t)) =
 subs rho(ext (reverse (p s)) (t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_num_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: num_arg(f, subs(rho)(t)) = num_arg(f, t)
theorem renaming_num_arg : num_arg f (subs rho(t)) = num_arg f t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_select
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: select(f, subs(rho)(t))(i) = subs(rho)(select(f, t)(i))
theorem renaming_select : select f (subs rho(t)) (i) = subs rho(select f t(i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_delete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: delete(f, subs(rho)(t))(i) = subs(rho)(delete(f, t)(i))
theorem renaming_delete : delete f (subs rho(t)) (i) = subs rho(delete f t(i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_context_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND
--  subset?(vars(G), V1) AND
--   member(X, V1) AND member((a, V(subs(rho)(X))), subs(rho, G))
--  IMPLIE
theorem subs_context_mem : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧
 subset_p (vars G) V1 ∧
  member X V1 ∧ member ((a, V (subs rho(X)))) (subs rho G) → member ((a, X)) G := by
  -- === TCCs for subs_context_mem ===
  -- TCC: subs_context_mem_TCC1
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], X1: variable,
  --         rho: renaming):
  --   (member(X1, V1) AND
  --     subset?(vars(G), V1) AND
  --      FORALL (X: variable):
  --        member(X, vars(img(rho...
  have subs_context_mem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.subs_context_mem2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member((a, X), G) IMPLIES member((a, V(subs(rho)(X))), subs(rho, G))
theorem subs_context_mem2 : member ((a, X)) G → member ((a, V (subs rho(X)))) (subs rho G) := by
  -- === TCCs for subs_context_mem2 ===
  -- TCC: subs_context_mem2_TCC1
  -- Statement: FORALL (G: fresh_context, X: variable, a: nat, rho: renaming):
  --   member((a, X), G) IMPLIES
  --    var?[atom, perm, variable, f_symbol, ac_symbol](subs(rho)(X))
  have subs_context_mem2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_fresh
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND
--  subset?(vars(t), V1) AND
--   subset?(vars(G), V1) AND fresh(subs(rho, G))(a, subs(rho)(t))
--  IMPLI
theorem renaming_fresh : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧
 subset_p (vars t) V1 ∧
  subset_p (vars G) V1 ∧ fresh (subs rho G) (a, subs rho(t)) → fresh G(a, t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_fresh2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fresh(G)(a, t) IMPLIES fresh(subs(rho, G))(a, subs(rho)(t))
theorem renaming_fresh2 : fresh G(a, t) → fresh (subs rho G) (a, subs rho(t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (FORALL X:
--         member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--          NOT member(X, V1))
--  AND subset?(vars(t), V1) AND subset?(vars(s), V1)
--  AND equal?(subs(rho, G))(subs(rho)
theorem renaming_equal : (∀ X, 
        member X (vars (img rho)) ∧ ¬ member X (dom rho) →
         ¬ member X V1)
 ∧ subset_p (vars t) V1 ∧ subset_p (vars s) V1
 ∧ equal_p (subs rho G) (subs rho(t), subs rho(s))
 ∧ subset_p (vars G) V1 → equal_p G(t, s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_equal2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(t, s) IMPLIES equal?(subs(rho, G))(subs(rho)(t), subs(rho)(s))
theorem renaming_equal2 : equal_p G(t, s) → equal_p (subs rho G) (subs rho(t), subs rho(s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_get_args
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: get_args(f)(subs(rho)(t)) = subs(rho)(get_args(f)(t))
theorem renaming_get_args : get_args f(subs rho(t)) = subs rho(get_args f(t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_var2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND var?(s) AND member(V(s), V1) AND subset?(vars(t), V1)
--  IMPLIES
--  (member(V(subs(rho)(s)), vars(su
theorem renaming_var2 : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ var_p s ∧ member (V s) V1 ∧ subset_p (vars t) V1 → (member (V (subs rho(s))) (vars (subs rho(t))) ↔ member (V s) (vars t)) := by
  -- === TCCs for renaming_var2 ===
  -- TCC: renaming_var2_TCC1
  -- Statement: FORALL (V1: finite_set[variable], rho: renaming,
  --         s,
  --         t: first_order_AC_term[constant, variable, f_symbol, ac_symbol]):
  --   (subset?(vars(t), V1) AND
  --     member(V(s), V1) AND
  --      var?(...
  have renaming_var2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_var2_TCC1
  -- Statement: FORALL (V1: finite_set[variable], rho: renaming,
  --         s, t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   (subset?(vars(t), V1) AND
  --     member(V(s), V1) AND
  --      var?(s) AND
  --    ...
  have renaming_var2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND
--  subset?(vars(t), V1) AND subset?(vars(args), V1) AND subset?(vars(G), V1)
--  IMPLIES
--  pos?(t, arg
theorem renaming_pos : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧
 subset_p (vars t) V1 ∧ subset_p (vars args) V1 ∧ subset_p (vars G) V1 → pos_p t args (equal_p G) =
 pos_p (subs rho(t)) (subs rho(args)) (equal_p (subs rho G)) := by
  -- === TCCs for renaming_pos ===
  -- TCC: renaming_pos_TCC1
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args: args,
  --         rho: renaming,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   (subset?(vars(G), V1) AND
  --     subset...
  have renaming_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_pos_TCC2
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args: args,
  --         rho: renaming,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol]):
  --   (subset?(vars(G), V1) AND
  --     subset...
  have renaming_pos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_count
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND
--  subset?(vars(t), V1) AND subset?(vars(args), V1) AND subset?(vars(G), V1)
--  IMPLIES
--  count(subs(
theorem renaming_count : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧
 subset_p (vars t) V1 ∧ subset_p (vars args) V1 ∧ subset_p (vars G) V1 → count (subs rho(t)) (subs rho(args)) (equal_p (subs rho G)) =
 count t args (equal_p G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND
--  subset?(vars(t), V1) AND subset?(vars(args), V1) AND subset?(vars(G), V1)
--  IMPLIES
--  subs(rho)(r
theorem renaming_remove : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧
 subset_p (vars t) V1 ∧ subset_p (vars args) V1 ∧ subset_p (vars G) V1 → subs rho(remove args t (equal_p G)) =
 remove (subs rho(args)) (subs rho(t)) (equal_p (subs rho G)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_remove_common
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND
--  subset?(vars(args1), V1) AND
--   subset?(vars(args2), V1) AND subset?(vars(G), V1)
--  IMPLIES
--  remo
theorem renaming_remove_common : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧
 subset_p (vars args1) V1 ∧
  subset_p (vars args2) V1 ∧ subset_p (vars G) V1 → remove_common (subs rho(args1)) (subs rho(args2)) (equal_p (subs rho G)) =
 subs rho(remove_common args1 args2 (equal_p G)) := by
  -- === TCCs for renaming_remove_common ===
  -- TCC: renaming_remove_common_TCC1
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args1, args2: args,
  --         rho: renaming):
  --   (subset?(vars(G), V1) AND
  --     subset?(vars(args2), V1) AND
  --      subset?(vars(args1), V1) AND
  --       ...
  have renaming_remove_common_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_remove_common_TCC2
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args1, args2: args,
  --         rho: renaming):
  --   (subset?(vars(G), V1) AND
  --     subset?(vars(args2), V1) AND
  --      subset?(vars(args1), V1) AND
  --       ...
  have renaming_remove_common_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_count_elem2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL X:
--    member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--     NOT member(X, V1))
--  AND subset?(vars(args), V1) AND subset?(vars(G), V1)
--  IMPLIES
--  count_elem2(subs(rho)(args), equal?(s
theorem renaming_count_elem2 : (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ subset_p (vars args) V1 ∧ subset_p (vars G) V1 → count_elem2 (subs rho(args)) (equal_p (subs rho G)).1 =
 subs rho(count_elem2 args (equal_p G).1)
 ∧
 count_elem2 (subs rho(args)) (equal_p (subs rho G)).2 =
  count_elem2 args (equal_p G).2 := by
  -- === TCCs for renaming_count_elem2 ===
  -- TCC: renaming_count_elem2_TCC1
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args: args,
  --         rho: renaming):
  --   (subset?(vars(G), V1) AND
  --     subset?(vars(args), V1) AND
  --      FORALL (X: variable):
  --        member(X, vars(...
  have renaming_count_elem2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_count_elem2_TCC2
  -- Statement: FORALL (G: fresh_context, V1: finite_set[variable], args: args,
  --         rho: renaming):
  --   (subset?(vars(G), V1) AND
  --     subset?(vars(args), V1) AND
  --      FORALL (X: variable):
  --        member(X, vars(...
  have renaming_count_elem2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_equal_syn
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sig1 = cons((Y, t), null),
--     sig1_ = cons((V(subs(rho)(Y)), subs(rho)(t)), null)
--   IN
--   (FORALL X:
--      member(X, vars(img(rho))) AND NOT member(X, dom(rho)) IMPLIES
--       NOT member(X, V1))
--    
theorem renaming_equal_syn : fun (sig1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (sig1_ : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => (∀ X, 
   member X (vars (img rho)) ∧ ¬ member X (dom rho) →
    ¬ member X V1)
 ∧ member Y V1 → equal_syn_p (append sig1_ rho) (append rho sig1) V1 (cons ((V (subs rho(Y)), subs rho(t))) null) (cons ((Y, t)) null) := by
  -- === TCCs for renaming_equal_syn ===
  -- TCC: renaming_equal_syn_TCC1
  -- Statement: FORALL (Y: variable, rho: renaming,
  --         t: first_order_AC_term[constant, variable, f_symbol, ac_symbol],
  --         sig1:
  --           (cons?[[variable,
  --                   first_order_AC_term
  --         ...
  have renaming_equal_syn_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: renaming_equal_syn_TCC1
  -- Statement: FORALL (Y: variable, rho: renaming,
  --         t: nominal_AC_term[atom, perm, variable, f_symbol, ac_symbol],
  --         sig1:
  --           (cons?[[variable,
  --                   nominal_AC_term
  --               ...
  have renaming_equal_syn_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.renaming_prot_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (var?(t) AND disjoint?(union(dom(rho), vars(img(rho))), PV)) IMPLIES
--  (member(V(subs(rho)(t)), PV) IFF member(V(t), PV))
theorem renaming_prot_vars : (var_p t ∧ disjoint_p (union (dom rho) (vars (img rho))) PV) → (member (V (subs rho(t))) PV ↔ member (V t) PV) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem shift_cor : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: nominal_AC_substitution.shift_append_equal_syn
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET rho1 = shift(V1, V1_, n) IN
--   (subset?(dom(rho), V1) AND subset?(vars(img(rho)), V1_)) IMPLIES
--    equal_syn?(rho, append(rho, rho1), V1)
theorem shift_append_equal_syn : fun (rho1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => (subset_p (dom rho) V1 ∧ subset_p (vars (img rho)) V1_) → equal_syn_p rho (append rho rho1) V1 (shift V1 V1_ n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_dom
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(dom(shift(start, n, inc)),
--         list2set[nat](from_min2max[nat](start, start + n)))
theorem shift_dom : subset_p (dom (shift start n inc)) (list2set (from_min2max[nat](start, start + n))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_dom2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (inc > 0 IMPLIES
--   dom(shift(start, n, inc)) =
--    list2set[nat](from_min2max[nat](start, start + n)))
--  AND (inc = 0 IMPLIES dom(shift(start, n, inc)) = emptyset)
theorem shift_dom2 : (inc > 0 →
  dom (shift start n inc) =
   list2set (from_min2max[nat](start, start + n))) ∧ (inc = 0 → dom (shift start n inc) = emptyset) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_dom_disjoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET rho = shift(lst_V, lst_V_, n) IN
--   disjoint?(dom(rho), list2set[variable](lst_V))
theorem shift_dom_disjoint : fun (rho : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => disjoint_p (dom rho) (list2set lst_V) (shift lst_V lst_V_ n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_img
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(img(shift(start, n, inc))),
--         list2set[nat](from_min2max[nat](start + inc, start + inc + n)))
theorem shift_img : subset_p (vars (img (shift start n inc))) (list2set (from_min2max[nat](start + inc, start + inc + n))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_img2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (inc > 0 IMPLIES
--   vars(img(shift(start, n, inc))) =
--    list2set[nat](from_min2max[nat](start + inc, start + n + inc)))
--  AND (inc = 0 IMPLIES img(shift(start, n, inc)) = emptyset)
theorem shift_img2 : (inc > 0 →
  vars (img (shift start n inc)) =
   list2set (from_min2max[nat](start + inc, start + n + inc))) ∧ (inc = 0 → img (shift start n inc) = emptyset) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_renaming
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: renaming?(shift(start, n, inc))
theorem shift_renaming : renaming_p (shift start n inc) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_renaming_cor
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: renaming?(shift(lst_V, lst_V_, n))
theorem shift_renaming_cor : renaming_p (shift lst_V lst_V_ n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_substitution.shift_append_renaming
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET rho1 = shift(V1, V1_, n) IN
--   (subset?(dom(rho), V1) AND subset?(vars(img(rho)), V1_)) IMPLIES
--    renaming?(append(rho, rho1))
theorem shift_append_renaming : fun (rho1 : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => (subset_p (dom rho) V1 ∧ subset_p (vars (img rho)) V1_) → renaming_p (append rho rho1) (shift V1 V1_ n) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.nominal_AC_substitution