-- Auto-generated by pvs2lean/translator.py
-- PVS theory: nominal_AC_unification
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: nominal_AC_unification

Auto-generated stubs from PVSLib theory `nominal_AC_unification`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.nominal_AC_unification

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (DS : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} (ac_app_p : pvs_τ4 → Prop)
variable {pvs_τ6 : Type*} (ac_sym : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (add : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (app : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (append : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (are_ac_p : pvs_τ21 → pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} (car : pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (cdr : pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (cons : pvs_τ27 → pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (contained_p : pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} (count : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (disjoint_p : pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (dom : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} (equal_p : pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (equal_syn_p : pvs_τ46 → pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} (equivalence_p : pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (ext : pvs_τ50 → pvs_τ51 → pvs_τ52)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} (fc : pvs_τ53 → pvs_τ54 → pvs_τ55)
variable {pvs_τ56 : Type*} (first_proj : pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} (flatten : pvs_τ57 → pvs_τ58)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} (fresh : pvs_τ59 → pvs_τ60 → pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (get_args : pvs_τ62 → pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} (idempotent_p : pvs_τ64 → Prop)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (img : pvs_τ65 → pvs_τ66)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (is_finite : pvs_τ67 → pvs_τ68)
variable {pvs_τ81 : Type*} (length : pvs_τ81 → Prop)
variable {pvs_τ83 : Type*} {pvs_τ84 : Type*} (list2set : pvs_τ83 → pvs_τ84)
variable {pvs_τ91 : Type*} {pvs_τ92 : Type*} {pvs_τ93 : Type*} {pvs_τ94 : Type*} (more_general_p : pvs_τ91 → pvs_τ92 → pvs_τ93 → pvs_τ94 → Prop)
variable {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} (no_common_args_p : pvs_τ95 → pvs_τ96 → pvs_τ97 → Prop)
variable {pvs_τ100 : Type*} {pvs_τ101 : Type*} {pvs_τ102 : Type*} (nominal_AC_fresh_subs : pvs_τ100 → pvs_τ101 → pvs_τ102 → Prop)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} (nth : pvs_τ103 → pvs_τ104 → Prop)
variable {pvs_τ105 : Type*} (null_p : pvs_τ105 → Prop)
variable {pvs_τ106 : Type*} {pvs_τ107 : Type*} (num_arg : pvs_τ106 → pvs_τ107 → Prop)
variable {pvs_τ108 : Type*} (ord : pvs_τ108 → Prop)
variable {pvs_τ109 : Type*} (pair_p : pvs_τ109 → Prop)
variable {pvs_τ110 : Type*} {pvs_τ111 : Type*} {pvs_τ112 : Type*} (pareate : pvs_τ110 → pvs_τ111 → pvs_τ112)
variable {pvs_τ113 : Type*} {pvs_τ114 : Type*} {pvs_τ115 : Type*} {pvs_τ116 : Type*} (remove_common : pvs_τ113 → pvs_τ114 → pvs_τ115 → pvs_τ116 → Prop)
variable {pvs_τ117 : Type*} {pvs_τ118 : Type*} (reverse : pvs_τ117 → pvs_τ118)
variable {pvs_τ120 : Type*} {pvs_τ121 : Type*} {pvs_τ122 : Type*} (same_count_p : pvs_τ120 → pvs_τ121 → pvs_τ122 → Prop)
variable {pvs_τ123 : Type*} {pvs_τ124 : Type*} (sec_proj : pvs_τ123 → pvs_τ124)
variable {pvs_τ136 : Type*} {pvs_τ137 : Type*} (subs : pvs_τ136 → pvs_τ137 → Prop)
variable {pvs_τ142 : Type*} {pvs_τ143 : Type*} (term1 : pvs_τ142 → pvs_τ143)
variable {pvs_τ144 : Type*} {pvs_τ145 : Type*} (term2 : pvs_τ144 → pvs_τ145)
variable {pvs_τ149 : Type*} {pvs_τ150 : Type*} {pvs_τ151 : Type*} (union : pvs_τ149 → pvs_τ150 → pvs_τ151)
variable {pvs_τ152 : Type*} (var_p : pvs_τ152 → Prop)

-- pvs2lean: nominal_AC_unification.member
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (t = unif_pair`1 OR t = unif_pair`2)
axiom member {pvs_τ89 : Type*} {pvs_τ90 : Type*} : pvs_τ89 → pvs_τ90 → Prop

-- pvs2lean: nominal_AC_unification.lhs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: first_proj(unif_prb)
axiom lhs {pvs_τ82 : Type*} : pvs_τ82 → Prop

-- pvs2lean: nominal_AC_unification.rhs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sec_proj(unif_prb)
axiom rhs {pvs_τ119 : Type*} : pvs_τ119 → Prop

-- pvs2lean: nominal_AC_unification.no_var?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t: member(t, unif_prb) IMPLIES NOT var?(t)
noncomputable def no_var_p : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL t: member(t, unif_prb) IMPLIES NOT var?(t)

-- pvs2lean: nominal_AC_unification.no_var_pair?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t: member(t, unif_prb) IMPLIES NOT var?(t) AND NOT pair?(t)
axiom no_var_pair_p {pvs_τ99 : Type*} : pvs_τ99 → Prop

-- pvs2lean: nominal_AC_unification.no_pair?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL t: member(t, unif_prb) IMPLIES NOT pair?(t)
axiom no_pair_p {pvs_τ98 : Type*} : pvs_τ98 → Prop

-- pvs2lean: nominal_AC_unification.vars
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN union(vars(t), vars(s))
axiom vars {pvs_τ153 : Type*} {pvs_τ154 : Type*} {pvs_τ155 : Type*} : pvs_τ153 → pvs_τ154 → pvs_τ155

-- pvs2lean: nominal_AC_unification.subterms
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN union(subterms(t), subterms(s))
axiom subterms {pvs_τ140 : Type*} {pvs_τ141 : Type*} : pvs_τ140 → pvs_τ141 → Prop

-- pvs2lean: nominal_AC_unification.unifies?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN equal?(G)(subs(sigma)(t), subs(sigma)(s))
axiom unifies_p {pvs_τ146 : Type*} {pvs_τ147 : Type*} {pvs_τ148 : Type*} : pvs_τ146 → pvs_τ147 → pvs_τ148 → Prop

-- pvs2lean: nominal_AC_unification.unifiable?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS sigma, G: unifies?(G, sigma, (t, s))
noncomputable def unifiable_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS sigma, G: unifies?(G, sigma, (t, s))

-- pvs2lean: nominal_AC_unification.apply_sub
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN (subs(sigma)(t), subs(sigma)(s))
axiom apply_sub {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: nominal_AC_unification.solves?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      nominal_AC_fresh_subs(Delta)(delta, Nabla)
--  AND unifies?(Delta, delta, P) AND more_general?(Delta)(sigma, delta, V1)
--  AND disjoint?(dom(delta), PV) AND well_formed?(delta)
axiom solves_p {pvs_τ129 : Type*} {pvs_τ130 : Type*} {pvs_τ131 : Type*} {pvs_τ132 : Type*} {pvs_τ133 : Type*} {pvs_τ134 : Type*} {pvs_τ135 : Type*} : pvs_τ129 → pvs_τ130 → pvs_τ131 → pvs_τ132 → pvs_τ133 → pvs_τ134 → pvs_τ135 → Prop

-- pvs2lean: nominal_AC_unification.is_sol?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      nominal_AC_fresh_subs(Delta)(delta, Nabla)
--  AND unifies?(Delta, delta, P) AND more_general?(Delta)(sigma, delta)
--  AND disjoint?(dom(delta), PV) AND well_formed?(delta)
axiom is_sol_p {pvs_τ75 : Type*} {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} : pvs_τ75 → pvs_τ76 → pvs_τ77 → pvs_τ78 → pvs_τ79 → pvs_τ80 → Prop

-- pvs2lean: nominal_AC_unification.is_instance?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_sol?(Delta, delta)(Nabla, null, sigma, PV)
axiom is_instance_p {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} : pvs_τ69 → pvs_τ70 → pvs_τ71 → pvs_τ72 → pvs_τ73 → pvs_τ74 → Prop

-- pvs2lean: nominal_AC_unification.size
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN size(t) + size(s)
axiom size {pvs_τ127 : Type*} {pvs_τ128 : Type*} : pvs_τ127 → pvs_τ128 → Prop

-- pvs2lean: nominal_AC_unification.lst_vars
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN append(lst_vars(t), lst_vars(s))
axiom lst_vars {pvs_τ85 : Type*} {pvs_τ86 : Type*} : pvs_τ85 → pvs_τ86

-- pvs2lean: nominal_AC_unification.ac_prb?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t, s) = unif_pair IN
--   ac_app?(t) AND ac_app?(s) AND ac_sym(t) = ac_sym(s)
axiom ac_prb_p {pvs_τ5 : Type*} : pvs_τ5 → Prop

-- pvs2lean: nominal_AC_unification.all_ac_prb?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL unif_pair: member(unif_pair, unif_prb) IMPLIES ac_prb?(unif_pair)
axiom all_ac_prb_p {pvs_τ10 : Type*} : pvs_τ10 → Prop

-- pvs2lean: nominal_AC_unification.exists_ac?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS up: member(up, P) AND ac_prb?(up)
noncomputable def exists_ac_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS up: member(up, P) AND ac_prb?(up)

-- pvs2lean: nominal_AC_unification.select_greater
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF size(t) >= size(s) THEN t ELSE s ENDIF
axiom select_greater {pvs_τ125 : Type*} {pvs_τ126 : Type*} : pvs_τ125 → pvs_τ126 → Prop

-- pvs2lean: nominal_AC_unification.well_formed?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(unif_pair`1) AND well_formed?(unif_pair`2)
axiom well_formed_p {pvs_τ156 : Type*} {pvs_τ157 : Type*} : pvs_τ156 → pvs_τ157 → Prop

-- pvs2lean: nominal_AC_unification.alm_well_formed?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: alm_well_formed?(unif_pair`1) AND alm_well_formed?(unif_pair`2)
axiom alm_well_formed_p {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12 → Prop

-- pvs2lean: nominal_AC_unification.subset?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL unif_pair:
--   member(unif_pair, unif_prb) IMPLIES member(unif_pair, unif_prb1)
axiom subset_p {pvs_τ138 : Type*} {pvs_τ139 : Type*} : pvs_τ138 → pvs_τ139 → Prop

-- pvs2lean: nominal_AC_unification.decompose
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF pair?(t) AND pair?(s)
--   THEN LET (unif_prb1, bool1) = decompose(term1(t), term1(s)),
--            (unif_prb2, bool2) = decompose(term2(t), term2(s))
--          IN (append(unif_prb1, unif_prb2), bool1 A
axiom decompose {pvs_τ36 : Type*} {pvs_τ37 : Type*} : pvs_τ36 → pvs_τ37 → Prop

-- pvs2lean: nominal_AC_unification.match_problem?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL X: member(X, vars(rhs(unif_prb))) IMPLIES member(X, PV)
axiom match_problem_p {pvs_τ87 : Type*} {pvs_τ88 : Type*} : pvs_τ87 → pvs_τ88 → Prop

-- pvs2lean: nominal_AC_unification.first_proj_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(args1) = length(args2) IMPLIES
--  first_proj(pareate(args1, args2)) = args1
theorem first_proj_unif_prb : List.length args1 = List.length args2 → first_proj (pareate args1 args2) = args1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_unif_prb_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, unif_prb) IFF
--  EXISTS unif_pair:
--    (t = unif_pair`1 OR t = unif_pair`2) AND member(unif_pair, unif_prb)
theorem member_unif_prb_equiv : member t unif_prb ↔ ∃ unif_pair, 
  (t = unif_pair.1 ∨ t = unif_pair.2) ∧ member unif_pair unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_unif_prb_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, append(unif_prb1, unif_prb2)) IFF
--  (member(t, unif_prb1) OR member(t, unif_prb2))
theorem member_unif_prb_append : member t (append unif_prb1 unif_prb2) ↔ (member t unif_prb1 ∨ member t unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_unif_prb_append2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(unif_pair, append(unif_prb1, unif_prb2)) IFF
--  (member(unif_pair, unif_prb1) OR member(unif_pair, unif_prb2))
theorem member_unif_prb_append2 : member unif_pair (append unif_prb1 unif_prb2) ↔ (member unif_pair unif_prb1 ∨ member unif_pair unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_lst_unif_prb_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, lst_unif_prb) IFF
--  EXISTS unif_prb: member(t, unif_prb) AND member(unif_prb, lst_unif_prb)
theorem member_lst_unif_prb_equiv : member t lst_unif_prb ↔ ∃ unif_prb,  member t unif_prb ∧ member unif_prb lst_unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.is_finite_mem_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_finite({t | member(t, unif_prb)})
theorem is_finite_mem_unif_prb : is_finite (fun (t : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => member t unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_lhs_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, unif_prb) IFF
--  (member(t, lhs(unif_prb)) OR member(t, rhs(unif_prb)))
theorem member_lhs_rhs : member t unif_prb ↔ (member t (lhs unif_prb) ∨ member t (rhs unif_prb)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.lhs_rhs_len
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(lhs(P)) = length(rhs(P))
theorem lhs_rhs_len : List.length (lhs P) = List.length (rhs P) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_lhs_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, lhs(pareate(args1, args2))) IMPLIES member(t, args1)
theorem member_lhs_pareate : member t (lhs (pareate args1 args2)) → member t args1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.lhs_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(args1) = length(args2) IMPLIES lhs(pareate(args1, args2)) = args1
theorem lhs_pareate : List.length args1 = List.length args2 → lhs (pareate args1 args2) = args1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.rhs_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(args1) = length(args2) IMPLIES rhs(pareate(args1, args2)) = args2
theorem rhs_pareate : List.length args1 = List.length args2 → rhs (pareate args1 args2) = args2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_rhs_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, rhs(pareate(args1, args2))) IMPLIES member(t, args2)
theorem member_rhs_pareate : member t (rhs (pareate args1 args2)) → member t args2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_rhs_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, rhs(append(unif_prb1, unif_prb2))) IFF
--  (member(t, rhs(unif_prb1)) OR member(t, rhs(unif_prb2)))
theorem member_rhs_append : member t (rhs (append unif_prb1 unif_prb2)) ↔ (member t (rhs unif_prb1) ∨ member t (rhs unif_prb2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.member_lhs_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, lhs(append(unif_prb1, unif_prb2))) IFF
--  (member(t, lhs(unif_prb1)) OR member(t, lhs(unif_prb2)))
theorem member_lhs_append : member t (lhs (append unif_prb1 unif_prb2)) ↔ (member t (lhs unif_prb1) ∨ member t (lhs unif_prb2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.vars_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(unif_prb)) IFF
--  EXISTS t: member(t, unif_prb) AND member(X, vars(t))
theorem vars_unif_prb : member X (vars unif_prb) ↔ ∃ t,  member t unif_prb ∧ member X (vars t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.vars_unif_prb_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(append(unif_prb1, unif_prb2))) IFF
--  (member(X, vars(unif_prb1)) OR member(X, vars(unif_prb2)))
theorem vars_unif_prb_append : member X (vars (append unif_prb1 unif_prb2)) ↔ (member X (vars unif_prb1) ∨ member X (vars unif_prb2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.vars_lst_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, vars(lst_unif_prb)) IFF
--  EXISTS t: member(t, lst_unif_prb) AND member(X, vars(t))
theorem vars_lst_unif_prb : member X (vars lst_unif_prb) ↔ ∃ t,  member t lst_unif_prb ∧ member X (vars t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.lst_vars_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: lst_vars(append(unif_prb, unif_prb1)) =
--  append(lst_vars(unif_prb), lst_vars(unif_prb1))
theorem lst_vars_append : lst_vars (append unif_prb unif_prb1) = append (lst_vars unif_prb) (lst_vars unif_prb1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.vars_lst_vars_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vars(unif_prb) = list2set(lst_vars(unif_prb))
theorem vars_lst_vars_unif_prb : vars unif_prb = list2set (lst_vars unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.vars_lst_vars_flatten_lst_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vars(lst_unif_prb) = list2set(lst_vars(flatten(lst_unif_prb)))
theorem vars_lst_vars_flatten_lst_unif_prb : vars lst_unif_prb = list2set (lst_vars (flatten lst_unif_prb)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.no_var_pair_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_var_pair?(append(unif_prb, unif_prb1)) =
--  (no_var_pair?(unif_prb) AND no_var_pair?(unif_prb1))
theorem no_var_pair_append : no_var_pair_p (append unif_prb unif_prb1) = (no_var_pair_p unif_prb ∧ no_var_pair_p unif_prb1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.no_pair_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_pair?(append(unif_prb, unif_prb1)) =
--  (no_pair?(unif_prb) AND no_pair?(unif_prb1))
theorem no_pair_append : no_pair_p (append unif_prb unif_prb1) = (no_pair_p unif_prb ∧ no_pair_p unif_prb1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.all_ac_prb_no_var_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: all_ac_prb?(unif_prb) IMPLIES no_var_pair?(unif_prb)
theorem all_ac_prb_no_var_pair : all_ac_prb_p unif_prb → no_var_pair_p unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.all_ac_prb_is_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: all_ac_prb?(unif_prb) AND member(t, unif_prb) IMPLIES ac_app?(t)
theorem all_ac_prb_is_ac : all_ac_prb_p unif_prb ∧ member t unif_prb → ac_app_p t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.size_app_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: size(append(unif_prb1, unif_prb2)) = size(unif_prb1) + size(unif_prb2)
theorem size_app_unif_prb : size (append unif_prb1 unif_prb2) = size unif_prb1 + size unif_prb2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.size_lhs_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, lhs(P)) IMPLIES size(t) <= size(lhs(P))
theorem size_lhs_mem : member t (lhs P) → size t ≤ size (lhs P) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.size_lhs_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(P) AND j < length(P) AND NOT i = j IMPLIES
--  size(nth(P, i)`1) + size(nth(P, j)`1) <= size(lhs(P))
theorem size_lhs_nth : i < List.length P ∧ j < List.length P ∧ ¬ i = j → size (nth P i.1) + size (nth P j.1) ≤ size (lhs P) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.subterms_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(unif_prb)) IFF
--  EXISTS unif_pair:
--    member(t, subterms(unif_pair)) AND member(unif_pair, unif_prb)
theorem subterms_mem : member t (subterms unif_prb) ↔ ∃ unif_pair, 
  member t (subterms unif_pair) ∧ member unif_pair unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.subterms_mem_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, subterms(unif_prb)) IFF
--  EXISTS t: member(s, subterms(t)) AND member(t, unif_prb)
theorem subterms_mem_unif_prb : member s (subterms unif_prb) ↔ ∃ t,  member s (subterms t) ∧ member t unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.subterms_mem_lst_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(s, subterms(lst_unif_prb)) IFF
--  EXISTS t: member(s, subterms(t)) AND member(t, lst_unif_prb)
theorem subterms_mem_lst_unif_prb : member s (subterms lst_unif_prb) ↔ ∃ t,  member s (subterms t) ∧ member t lst_unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.subterms_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(append(unif_prb1, unif_prb2))) IFF
--  (member(t, subterms(unif_prb1)) OR member(t, subterms(unif_prb2)))
theorem subterms_append : member t (subterms (append unif_prb1 unif_prb2)) ↔ (member t (subterms unif_prb1) ∨ member t (subterms unif_prb2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.subterms_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(pareate(args1, args2))) IMPLIES
--  (member(t, subterms(args1)) OR member(t, subterms(args2)))
theorem subterms_pareate : member t (subterms (pareate args1 args2)) → (member t (subterms args1) ∨ member t (subterms args2)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.subset_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(unif_prb1, unif_prb2) IMPLIES
--  subset?(vars(unif_prb1), vars(unif_prb2))
theorem subset_vars : subset_p unif_prb1 unif_prb2 → subset_p (vars unif_prb1) (vars unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.subset_unif_prb_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (subset?(unif_prb, unif_prb1) IMPLIES
--   subset?(unif_prb, append(unif_prb1, unif_prb2)))
--  AND
--  (subset?(unif_prb, unif_prb2) IMPLIES
--    subset?(unif_prb, append(unif_prb1, unif_prb2)))
theorem subset_unif_prb_append : (subset_p unif_prb unif_prb1 →
  subset_p unif_prb (append unif_prb1 unif_prb2)) ∧ (subset_p unif_prb unif_prb2 →
  subset_p unif_prb (append unif_prb1 unif_prb2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, sigma, append(unif_prb1, unif_prb2)) =
--  (unifies?(G, sigma, unif_prb1) AND unifies?(G, sigma, unif_prb2))
theorem unifies_p_append : unifies_p G sigma (append unif_prb1 unif_prb2) = (unifies_p G sigma unif_prb1 ∧ unifies_p G sigma unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(args1) = length(args2) AND unifies?(G, sigma, pareate(args1, args2))
--  IMPLIES equal?(G)(subs(sigma)(args1), subs(sigma)(args2))
theorem unifies_p_pareate : List.length args1 = List.length args2 ∧ unifies_p G sigma (pareate args1 args2) → equal_p G(subs sigma(args1), subs sigma(args2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_lhs_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, sigma, P) IMPLIES
--  equal?(G)(subs(sigma)(lhs(P)), subs(sigma)(rhs(P)))
theorem unifies_p_lhs_rhs : unifies_p G sigma P → equal_p G(subs sigma(lhs P), subs sigma(rhs P)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, sigma, unif_prb) IFF
--  FORALL unif_pair:
--    member(unif_pair, unif_prb) IMPLIES unifies?(G, sigma, unif_pair)
theorem unifies_p_equiv : unifies_p G sigma unif_prb ↔ ∀ unif_pair, 
  member unif_pair unif_prb → unifies_p G sigma unif_pair := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(sigma, sigma1) AND unifies?(G, sigma, unif_prb) IMPLIES
--  unifies?(G, sigma1, unif_prb)
theorem unifies_p_equal : equal_p G(sigma, sigma1) ∧ unifies_p G sigma unif_prb → unifies_p G sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_equal_alt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal?(G)(sigma, sigma1, set_vars) AND
--  unifies?(G, sigma, unif_prb) AND subset?(vars(unif_prb), set_vars)
--  IMPLIES unifies?(G, sigma1, unif_prb)
theorem unifies_p_equal_alt : equal_p G(sigma, sigma1, set_vars) ∧
 unifies_p G sigma unif_prb ∧ subset_p (vars unif_prb) set_vars → unifies_p G sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(unif_prb, unif_prb1) AND unifies?(G, sigma, unif_prb1) IMPLIES
--  unifies?(G, sigma, unif_prb)
theorem unifies_p_subset : subset_p unif_prb unif_prb1 ∧ unifies_p G sigma unif_prb1 → unifies_p G sigma unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_append_disjoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, append(sigma1, sigma), unif_prb) AND
--  disjoint?(dom(sigma), vars(unif_prb))
--  IMPLIES unifies?(G, sigma1, unif_prb)
theorem unifies_p_append_disjoint : unifies_p G (append sigma1 sigma) unif_prb ∧
 disjoint_p (dom sigma) (vars unif_prb) → unifies_p G sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_append_disjoint2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, sigma1, unif_prb) AND disjoint?(dom(sigma), vars(unif_prb))
--  IMPLIES unifies?(G, append(sigma1, sigma), unif_prb)
theorem unifies_p_append_disjoint2 : unifies_p G sigma1 unif_prb ∧ disjoint_p (dom sigma) (vars unif_prb) → unifies_p G (append sigma1 sigma) unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_append_disjoint3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(unif_prb)) IMPLIES
--  unifies?(G, append(sigma1, sigma), unif_prb) =
--   unifies?(G, sigma1, unif_prb)
theorem unifies_p_append_disjoint3 : disjoint_p (dom sigma) (vars unif_prb) → unifies_p G (append sigma1 sigma) unif_prb = unifies_p G sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_mem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, apply_sub(sigma, unif_prb)) IFF
--  EXISTS s: member(s, unif_prb) AND t = subs(sigma)(s)
theorem apply_sub_mem : member t (apply_sub sigma unif_prb) ↔ ∃ s,  member s unif_prb ∧ t = subs sigma(s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_unif_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(unif_pair, apply_sub(sigma, unif_prb)) IFF
--  EXISTS unif_pair1:
--    member(unif_pair1, unif_prb) AND unif_pair = apply_sub(sigma, unif_pair1)
theorem apply_sub_unif_pair : member unif_pair (apply_sub sigma unif_prb) ↔ ∃ unif_pair1, 
  member unif_pair1 unif_prb ∧ unif_pair = apply_sub sigma unif_pair1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_subterms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, subterms(apply_sub(sigma, unif_prb))) IMPLIES
--  EXISTS s:
--    (member(s, subterms(unif_prb)) AND
--      (t = subs(sigma)(s) OR
--        (var?(s) AND member(t, ext(p(s))(subterms(img(sigma)))))))
theorem apply_sub_subterms : member t (subterms (apply_sub sigma unif_prb)) → ∃ s, 
  (member s (subterms unif_prb) ∧
    (t = subs sigma(s) ∨
      (var_p s ∧ member t (ext (p s) (subterms (img sigma)))))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(t, rhs(apply_sub(sigma, unif_prb))) IFF
--  EXISTS s: (member(s, rhs(unif_prb)) AND t = subs(sigma)(s))
theorem apply_sub_rhs : member t (rhs (apply_sub sigma unif_prb)) ↔ ∃ s,  (member s (rhs unif_prb) ∧ t = subs sigma(s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: apply_sub(null, unif_prb) = unif_prb
theorem apply_sub_null : apply_sub null unif_prb = unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: apply_sub(sigma, append(unif_prb1, unif_prb2)) =
--  append(apply_sub(sigma, unif_prb1), apply_sub(sigma, unif_prb2))
theorem apply_sub_append : apply_sub sigma (append unif_prb1 unif_prb2) = append (apply_sub sigma unif_prb1) (apply_sub sigma unif_prb2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_sub_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: apply_sub(sigma, apply_sub(sigma1, unif_prb)) =
--  apply_sub(append(sigma, sigma1), unif_prb)
theorem apply_sub_sub_append : apply_sub sigma (apply_sub sigma1 unif_prb) = apply_sub (append sigma sigma1) unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_len
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(apply_sub(sigma, unif_prb)) = length(unif_prb)
theorem apply_sub_len : List.length (apply_sub sigma unif_prb) = List.length unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(unif_prb) IMPLIES
--  nth(apply_sub(sigma, unif_prb), i) = apply_sub(sigma, nth(unif_prb, i))
theorem apply_sub_nth : i < List.length unif_prb → nth (apply_sub sigma unif_prb) i = apply_sub sigma (nth unif_prb i) := by
  -- === TCCs for apply_sub_nth ===
  -- TCC: apply_sub_nth_TCC1
  -- Statement: FORALL (i: nat, sigma: sub, unif_prb: unif_prb):
  --   i < length(unif_prb) IMPLIES
  --    i < length[unif_pair](apply_sub(sigma, unif_prb))
  have apply_sub_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_first_proj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(first_proj(unif_prb)) = first_proj(apply_sub(sigma, unif_prb))
theorem apply_sub_first_proj : subs sigma(first_proj unif_prb) = first_proj (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_sec_proj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subs(sigma)(sec_proj(unif_prb)) = sec_proj(apply_sub(sigma, unif_prb))
theorem apply_sub_sec_proj : subs sigma(sec_proj unif_prb) = sec_proj (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_subset_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(unif_prb, apply_sub(sigma, unif_prb1)) IMPLIES
--  EXISTS unif_prb2:
--    subset?(unif_prb2, unif_prb1) AND unif_prb = apply_sub(sigma, unif_prb2)
theorem apply_sub_subset_unif_prb : subset_p unif_prb (apply_sub sigma unif_prb1) → ∃ unif_prb2, 
  subset_p unif_prb2 unif_prb1 ∧ unif_prb = apply_sub sigma unif_prb2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_pareate
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pareate(subs(sigma)(args1), subs(sigma)(args2)) =
--  apply_sub(sigma, pareate(args1, args2))
theorem apply_sub_pareate : pareate (subs sigma(args1)) (subs sigma(args2)) = apply_sub sigma (pareate args1 args2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_elim_var_unif_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, dom(sigma)) AND NOT member(X, vars(img(sigma))) IMPLIES
--  NOT member(X, vars(apply_sub(sigma, unif_pair)))
theorem apply_sub_elim_var_unif_pair : member X (dom sigma) ∧ ¬ member X (vars (img sigma)) → ¬ member X (vars (apply_sub sigma unif_pair)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_elim_var
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member(X, dom(sigma)) AND NOT member(X, vars(img(sigma))) IMPLIES
--  NOT member(X, vars(apply_sub(sigma, unif_prb)))
theorem apply_sub_elim_var : member X (dom sigma) ∧ ¬ member X (vars (img sigma)) → ¬ member X (vars (apply_sub sigma unif_prb)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_elim_var2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: idempotent?(sigma) IMPLIES
--  disjoint?(dom(sigma), vars(apply_sub(sigma, unif_prb)))
theorem apply_sub_elim_var2 : idempotent_p sigma → disjoint_p (dom sigma) (vars (apply_sub sigma unif_prb)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_elim_var3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT member(X, vars(t)) IMPLIES
--  NOT member(X, vars(apply_sub(cons((X, ext(pi)(t)), null), P)))
theorem apply_sub_elim_var3 : ¬ member X (vars t) → ¬ member X (vars (apply_sub (cons ((X, ext pi(t))) null) P)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(apply_sub(sigma, unif_prb)),
--         union(vars(img(sigma)), vars(unif_prb)))
theorem apply_sub_vars : subset_p (vars (apply_sub sigma unif_prb)) (union (vars (img sigma)) (vars unif_prb)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_no_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_pair?(unif_prb) AND no_pair?(img(sigma)) IMPLIES
--  no_pair?(apply_sub(sigma, unif_prb))
theorem apply_sub_no_pair : no_pair_p unif_prb ∧ no_pair_p (img sigma) → no_pair_p (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_no_var_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_var_pair?(unif_prb) IMPLIES no_var_pair?(apply_sub(sigma, unif_prb))
theorem apply_sub_no_var_pair : no_var_pair_p unif_prb → no_var_pair_p (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_all_ac
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: all_ac_prb?(unif_prb) IMPLIES all_ac_prb?(apply_sub(sigma, unif_prb))
theorem apply_sub_all_ac : all_ac_prb_p unif_prb → all_ac_prb_p (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.ac_prb_renaming
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ac_prb?(unif_pair) IFF ac_prb?(apply_sub(rho, unif_pair))
theorem ac_prb_renaming : ac_prb_p unif_pair ↔ ac_prb_p (apply_sub rho unif_pair) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.all_ac_prb_renaming
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: all_ac_prb?(unif_prb) IFF all_ac_prb?(apply_sub(rho, unif_prb))
theorem all_ac_prb_renaming : all_ac_prb_p unif_prb ↔ all_ac_prb_p (apply_sub rho unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_unifies
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, sigma, apply_sub(sigma1, unif_prb)) IFF
--  unifies?(G, append(sigma, sigma1), unif_prb)
theorem apply_sub_unifies : unifies_p G sigma (apply_sub sigma1 unif_prb) ↔ unifies_p G (append sigma sigma1) unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_unifies2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, sigma, apply_sub(sigma1, unif_pair)) IFF
--  unifies?(G, append(sigma, sigma1), unif_pair)
theorem apply_sub_unifies2 : unifies_p G sigma (apply_sub sigma1 unif_pair) ↔ unifies_p G (append sigma sigma1) unif_pair := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: equal_syn?(sigma, sigma1, vars(unif_prb)) IMPLIES
--  apply_sub(sigma, unif_prb) = apply_sub(sigma1, unif_prb)
theorem apply_sub_eq : equal_syn_p sigma sigma1 (vars unif_prb) → apply_sub sigma unif_prb = apply_sub sigma1 unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.apply_sub_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: disjoint?(dom(sigma), vars(unif_prb)) IMPLIES
--  apply_sub(sigma, unif_prb) = unif_prb
theorem apply_sub_no_effect : disjoint_p (dom sigma) (vars unif_prb) → apply_sub sigma unif_prb = unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.select_greater_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT null?(unif_prb) IMPLIES
--  member(select_greater(unif_prb), unif_prb) AND
--   FORALL t:
--     member(t, unif_prb) IMPLIES size(t) <= size(select_greater(unif_prb))
theorem select_greater_cor : ¬ null_p unif_prb → member (select_greater unif_prb) unif_prb ∧
 ∀ t, 
   member t unif_prb → size t ≤ size (select_greater unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.well_formed_unif_prb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(unif_prb) IFF
--  (FORALL t: member(t, unif_prb) IMPLIES well_formed?(t))
theorem well_formed_unif_prb : well_formed_p unif_prb ↔ (∀ t,  member t unif_prb → well_formed_p t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.well_formed_apply_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(unif_prb) AND well_formed?(sigma) IMPLIES
--  well_formed?(apply_sub(sigma, unif_prb))
theorem well_formed_apply_sub : well_formed_p unif_prb ∧ well_formed_p sigma → well_formed_p (apply_sub sigma unif_prb) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.well_formed_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(unif_prb, unif_prb1) AND well_formed?(unif_prb1) IMPLIES
--  well_formed?(unif_prb)
theorem well_formed_subset : subset_p unif_prb unif_prb1 ∧ well_formed_p unif_prb1 → well_formed_p unif_prb := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.well_formed_unif_prb_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(append(unif_prb, unif_prb1)) IFF
--  (well_formed?(unif_prb) AND well_formed?(unif_prb1))
theorem well_formed_unif_prb_append : well_formed_p (append unif_prb unif_prb1) ↔ (well_formed_p unif_prb ∧ well_formed_p unif_prb1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_impossible
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL t1:
--    count(t1, get_args(f)(s), equal?(G)) >=
--     count(t1, get_args(f)(t), equal?(G)))
--  AND
--  are_ac?(t, s, f) AND
--   NOT same_count?(get_args(f)(t), get_args(f)(s), equal?(G)) AND
--    nominal_
theorem unifies_p_impossible : (∀ t1, 
   count t1 (get_args f(s)) (equal_p G) ≥
    count t1 (get_args f(t)) (equal_p G))
 ∧
 are_ac_p t s f ∧
  ¬ same_count_p (get_args f(t)) (get_args f(s)) (equal_p G) ∧
   nominal_AC_fresh_subs G1(delta, G) → ¬ unifies_p G1 delta ((t, s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_impossible2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, sigma, (t, s)) AND NOT var?(t) AND well_formed?(t) IMPLIES
--  ord(t) = ord(s) OR (var?(s) AND NOT (member(V(s), vars(t))))
theorem unifies_p_impossible2 : unifies_p G sigma ((t, s)) ∧ ¬ var_p t ∧ well_formed_p t → ord t = ord s ∨ (var_p s ∧ ¬ (member (V s) (vars t))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_impossible3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, sigma, (t, s)) AND
--  disjoint?(dom(sigma), PV) AND var?(s) AND well_formed?(t)
--  IMPLIES
--  (var?(t) AND V(s) = V(t)) OR
--   (var?(t) AND (NOT member(V(t), vars(s))) AND (NOT member(V(t), PV))) 
theorem unifies_p_impossible3 : unifies_p G sigma ((t, s)) ∧
 disjoint_p (dom sigma) PV ∧ var_p s ∧ well_formed_p t → (var_p t ∧ V s = V t) ∨
 (var_p t ∧ (¬ member (V t) (vars s)) ∧ (¬ member (V t) PV)) ∨
  ((¬ member (V s) (vars t)) ∧ (¬ member (V s) PV)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies?_impossible4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      unifies?(G, sigma, (t, s)) AND var?(t) AND (NOT var?(s))
--  AND disjoint?(dom(sigma), PV) AND well_formed?(s)
--  IMPLIES ((NOT member(V(t), PV)) AND (NOT member(V(t), vars(s))))
theorem unifies_p_impossible4 : unifies_p G sigma ((t, s)) ∧ var_p t ∧ (¬ var_p s)
 ∧ disjoint_p (dom sigma) PV ∧ well_formed_p s → ((¬ member (V t) PV) ∧ (¬ member (V t) (vars s))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.remove_common_not_null_count
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS t1: count(t1, args1, equal?(G)) > count(t1, args2, equal?(G)))
--  IMPLIES NOT null?(remove_common(args1, args2, equal?(G)))
theorem remove_common_not_null_count : (∃ t1,  count t1 args1 (equal_p G) > count t1 args2 (equal_p G)) → ¬ null_p (remove_common args1 args2 (equal_p G)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.remove_common_not_null_unifies
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G1, delta, (t, s)) AND
--  NOT equal?(G)(t, s) AND
--   are_ac?(t, s, f) AND nominal_AC_fresh_subs(G1)(delta, G)
--  IMPLIES
--  (NOT null?(remove_common(get_args(f)(t), get_args(f)(s), equal?(G))) AND
--  
theorem remove_common_not_null_unifies : unifies_p G1 delta ((t, s)) ∧
 ¬ equal_p G(t, s) ∧
  are_ac_p t s f ∧ nominal_AC_fresh_subs G1(delta, G) → (¬ null_p (remove_common (get_args f(t)) (get_args f(s)) (equal_p G)) ∧
  ¬ null_p (remove_common (get_args f(s)) (get_args f(t)) (equal_p G))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.remove_common_args_cor
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f, G) IN
--   unifies?(G1, delta, (t, s)) AND
--    NOT equal?(G)(t, s) AND
--     are_ac?(t, s, f) AND nominal_AC_fresh_subs(G1)(delta, G)
--    IMPLIES
--    remove_common(get_ar
theorem remove_common_args_cor : fun (t1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p G1 delta ((t, s)) ∧
 ¬ equal_p G(t, s) ∧
  are_ac_p t s f ∧ nominal_AC_fresh_subs G1(delta, G) → remove_common (get_args f(t)) (get_args f(s)) (equal_p G) =
 remove_common (get_args f(t1)) (get_args f(s1)) (equal_p G) (remove_common t s f G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.get_args_remove_common
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f, G) IN
--   unifies?(G1, delta, (t, s)) AND
--    NOT equal?(G)(t, s) AND
--     are_ac?(t, s, f) AND nominal_AC_fresh_subs(G1)(delta, G)
--    IMPLIES
--    get_args(f)(t1) =
--   
theorem get_args_remove_common : fun (t1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p G1 delta ((t, s)) ∧
 ¬ equal_p G(t, s) ∧
  are_ac_p t s f ∧ nominal_AC_fresh_subs G1(delta, G) → get_args f(t1) = remove_common (get_args f(t)) (get_args f(s)) (equal_p G) (remove_common t s f G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.remove_common_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f, G) IN
--   NOT equal?(G)(t, s) AND
--    are_ac?(t, s, f) AND
--     unifies?(G1, delta, (t, s)) AND nominal_AC_fresh_subs(G1)(delta, G)
--    IMPLIES equal?(G1)(subs(delta)(
theorem remove_common_eq : fun (t1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => ¬ equal_p G(t, s) ∧
 are_ac_p t s f ∧
  unifies_p G1 delta ((t, s)) ∧ nominal_AC_fresh_subs G1(delta, G) → equal_p G1(subs delta(t1), subs delta(s1)) (remove_common t s f G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.remove_common_no_common_args
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f, G) IN
--   unifies?(G1, delta, (t, s)) AND
--    NOT equal?(G)(t, s) AND
--     are_ac?(t, s, f) AND nominal_AC_fresh_subs(G1)(delta, G)
--    IMPLIES no_common_args?(get_arg
theorem remove_common_no_common_args : fun (t1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p G1 delta ((t, s)) ∧
 ¬ equal_p G(t, s) ∧
  are_ac_p t s f ∧ nominal_AC_fresh_subs G1(delta, G) → no_common_args_p (get_args f(t1)) (get_args f(s1)) G (remove_common t s f G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.remove_common_eq2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f, G) IN
--        (NOT null?(remove_common(get_args(f)(t), get_args(f)(s), equal?(G))))
--    AND (NOT null?(remove_common(get_args(f)(s), get_args(f)(t), equal?(G))))
--   
theorem remove_common_eq2 : fun (t1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => (¬ null_p (remove_common (get_args f(t)) (get_args f(s)) (equal_p G)))
 ∧ (¬ null_p (remove_common (get_args f(s)) (get_args f(t)) (equal_p G)))
 ∧ are_ac_p t s f ∧ unifies_p G1 delta ((t1, s1))
 ∧ nominal_AC_fresh_subs G1(delta, G) → equal_p G1(subs delta(t), subs delta(s)) (remove_common t s f G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.remove_common_vars
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f, G) IN
--   subset?(vars(t1, s1), vars(t, s))
theorem remove_common_vars : fun (t1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => subset_p (vars t1 s1) (vars t s) (remove_common t s f G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.remove_common_alm_well_formed?
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (t1, s1) = remove_common(t, s, f, G) IN
--        unifies?(G1, delta, (t, s)) AND (NOT equal?(G)(t, s))
--    AND are_ac?(t, s, f) AND nominal_AC_fresh_subs(G1)(delta, G)
--    AND well_formed?(t, s)
--    IM
theorem remove_common_alm_well_formed_p : fun (t1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) (s1 : nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) => unifies_p G1 delta ((t, s)) ∧ (¬ equal_p G(t, s))
 ∧ are_ac_p t s f ∧ nominal_AC_fresh_subs G1(delta, G)
 ∧ well_formed_p t s → alm_well_formed_p t1 s1 (remove_common t s f G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_num_arg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 IMPLIES num_arg(t) = num_arg(s)
theorem decompose_num_arg : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 → num_arg t = num_arg s (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_subterm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 IMPLIES subterms(unif_prb) = subterms(t, s)
theorem decompose_subterm : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 → subterms unif_prb = subterms t s (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_subterm2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   subset?(subterms(unif_prb), subterms(t, s))
theorem decompose_subterm2 : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => subset_p (subterms unif_prb) (subterms t s) (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 IMPLIES unifies?(G, sigma, (t, s)) = unifies?(G, sigma, unif_prb)
theorem decompose_correct : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 → unifies_p G sigma ((t, s)) = unifies_p G sigma unif_prb (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_correct_app
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 AND well_formed?(sigma) IMPLIES
--    unifies?(G, sigma, (app(f, t), app(f, s))) = unifies?(G, sigma, unif_prb)
theorem decompose_correct_app : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 ∧ well_formed_p sigma → unifies_p G sigma ((app f t, app f s)) = unifies_p G sigma unif_prb (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_correct_abs1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   bool1 AND well_formed?(sigma) IMPLIES
--    unifies?(G, sigma, (abs(a, t), abs(a, s))) = unifies?(G, sigma, unif_prb)
theorem decompose_correct_abs1 : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 ∧ well_formed_p sigma → unifies_p G sigma ((|a, t|, |a, s|)) = unifies_p G sigma unif_prb (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_correct_abs2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET pi = cons((a, b), null), (unif_prb, bool1) = decompose(t, ext(pi)(s))
--   IN
--   bool1 AND well_formed?(sigma) AND a /= b AND fresh(G)(a, subs(sigma)(s))
--    IMPLIES
--    unifies?(G, sigma, (abs(a, t), a
theorem decompose_correct_abs2 : fun (pi : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) => fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => bool1 ∧ well_formed_p sigma ∧ a ≠ b ∧ fresh G(a, subs sigma(s)) → unifies_p G sigma ((|a, t|, |b, s|)) = unifies_p G sigma unif_prb (decompose t (ext pi(s))) (cons ((a, b)) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_no_pair
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: no_pair?(decompose(t, s)`1)
theorem decompose_no_pair : no_pair_p (decompose t s.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_not_fail
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   well_formed?(sigma) AND unifies?(G, sigma, (t, s)) IMPLIES bool1
theorem decompose_not_fail : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => well_formed_p sigma ∧ unifies_p G sigma ((t, s)) → bool1 (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_not_fail2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   well_formed?(sigma) AND unifies?(G, sigma, (app(f, t), app(f, s)))
--    IMPLIES bool1
theorem decompose_not_fail2 : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => well_formed_p sigma ∧ unifies_p G sigma ((app f t, app f s)) → bool1 (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_app_well_formed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   well_formed?(sigma) AND well_formed?(app(f, t), app(f, s)) AND bool1
--    IMPLIES well_formed?(unif_prb)
theorem decompose_app_well_formed : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => well_formed_p sigma ∧ well_formed_p (app f t) (app f s) ∧ bool1 → well_formed_p unif_prb (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_well_formed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET unif_prb = decompose(t, s)`1 IN
--   well_formed?(app(f, t), app(f, s)) IMPLIES well_formed?(unif_prb)
theorem decompose_well_formed : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => well_formed_p (app f t) (app f s) → well_formed_p unif_prb (decompose t s.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_well_formed_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET unif_prb = decompose(t, s)`1 IN
--   well_formed?(abs(a, t), abs(b, s)) IMPLIES well_formed?(unif_prb)
theorem decompose_well_formed_abs : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => well_formed_p |a t| |b s| → well_formed_p unif_prb (decompose t s.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_size
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN size(unif_prb) <= size(t, s)
theorem decompose_size : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => size unif_prb ≤ size t s (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_ren
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: decompose(t, s)`2 IFF decompose(subs(rho)(t), subs(rho)(s))`2
theorem decompose_ren : decompose t s.2 ↔ decompose (subs rho(t)) (subs rho(s)).2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_ren1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: decompose(subs(rho)(t), subs(rho)(s))`1 = apply_sub(rho, decompose(t, s)`1)
theorem decompose_ren1 : decompose (subs rho(t)) (subs rho(s)).1 = apply_sub rho (decompose t s.1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_ext
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: decompose(t, s)`2 = decompose(t, ext(pi)(s))`2
theorem decompose_ext : decompose t s.2 = decompose t (ext pi(s)).2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_not_fail3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, bool1) = decompose(t, s) IN
--   well_formed?(sigma) AND unifies?(G, sigma, (abs(a, t), abs(b, s)))
--    IMPLIES bool1
theorem decompose_not_fail3 : fun (unif_prb : List ((nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (bool1 : Bool) => well_formed_p sigma ∧ unifies_p G sigma ((|a, t|, |b, s|)) → bool1 (decompose t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_vars
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(decompose(t, s)`1), vars(t, s))
theorem decompose_vars : subset_p (vars (decompose t s.1)) (vars t s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.decompose_vars_rhs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(vars(rhs(decompose(t, s)`1)), vars(s))
theorem decompose_vars_rhs : subset_p (vars (rhs (decompose t s.1))) (vars s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.match_prb_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: match_problem?(append(P1, P2), PV) =
--  (match_problem?(P1, PV) AND match_problem?(P2, PV))
theorem match_prb_append : match_problem_p (append P1 P2) PV = (match_problem_p P1 PV ∧ match_problem_p P2 PV) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.match_prb_apply_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: match_problem?(P, PV) AND subset?(vars(img(sigma)), PV) IMPLIES
--  match_problem?(apply_sub(sigma, P), PV)
theorem match_prb_apply_sub : match_problem_p P PV ∧ subset_p (vars (img sigma)) PV → match_problem_p (apply_sub sigma P) PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.match_prb_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(P1, P) AND match_problem?(P, PV) IMPLIES match_problem?(P1, PV)
theorem match_prb_subset : subset_p P1 P ∧ match_problem_p P PV → match_problem_p P1 PV := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies_more_general_sub_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      unifies?(Delta, delta, apply_sub(sigma1, P))
--  AND more_general?(Delta)(append(sigma1, sigma), delta)
--  AND disjoint?(dom(sigma), vars(P)) AND idempotent?(sigma1)
--  AND disjoint?(dom(sigma), vars(im
theorem unifies_more_general_sub_no_effect : unifies_p Delta delta (apply_sub sigma1 P)
 ∧ more_general_p Delta(append sigma1 sigma, delta)
 ∧ disjoint_p (dom sigma) (vars P) ∧ idempotent_p sigma1
 ∧ disjoint_p (dom sigma) (vars (img sigma1)) → unifies_p Delta delta P := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies_is_more_general_basic_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      more_general?(G)(sigma, delta) AND unifies?(G, delta, (t, s))
--  AND var?(t) AND (NOT member(V(t), vars(s)))
--  AND disjoint?(dom(sigma), vars(t, s))
--  IMPLIES
--  (LET sigma1 = cons((V(t), ext(reverse(p
theorem unifies_is_more_general_basic_sub : more_general_p G(sigma, delta) ∧ unifies_p G delta ((t, s))
 ∧ var_p t ∧ (¬ member (V t) (vars s))
 ∧ disjoint_p (dom sigma) (vars t s) → ((let sigma1 := cons ((V t, ext (reverse (p t)) (s))) null; more_general_p G(append sigma1 sigma, delta))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies_is_equal_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(G, delta, (t, s)) AND var?(t) AND NOT member(V(t), vars(s))
--  IMPLIES
--  (LET sigma1 = cons((V(t), ext(reverse(p(t)))(s)), null) IN
--     equal?(G)(delta, append(delta, sigma1)))
theorem unifies_is_equal_sub : unifies_p G delta ((t, s)) ∧ var_p t ∧ ¬ member (V t) (vars s) → ((let sigma1 := cons ((V t, ext (reverse (p t)) (s))) null; equal_p G(delta, append delta sigma1))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies_sub_no_effect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unifies?(Delta, append(delta, append(sigma1, sigma)),
--          apply_sub(sigma1, (t, s)))
--  AND
--  disjoint?(dom(sigma), vars(img(sigma1))) AND
--   idempotent?(sigma1) AND disjoint?(dom(sigma), vars(t, s))
theorem unifies_sub_no_effect : unifies_p Delta (append delta (append sigma1 sigma)) (apply_sub sigma1 ((t, s)))
 ∧
 disjoint_p (dom sigma) (vars (img sigma1)) ∧
  idempotent_p sigma1 ∧ disjoint_p (dom sigma) (vars t s) → unifies_p Delta (append delta (append sigma1 sigma)) ((t, s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies_basic_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET delta = cons((X, ext(reverse(pi))(s)), null) IN
--   disjoint?(dom(sigma), add(X, vars(s))) AND NOT member(X, vars(s)) IMPLIES
--    unifies?(Delta, append(append(sigma1, delta), sigma), (pi * X, s))
theorem unifies_basic_sub : fun (delta : List ({i : ℝ // i ≥ 0} × (nominal_AC_term ℝ (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => disjoint_p (dom sigma) (add X (vars s)) ∧ ¬ member X (vars s) → unifies_p Delta (append (append sigma1 delta) sigma) ((pi * X, s)) (cons ((X, ext (reverse pi) (s))) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.unifies_nominal_AC_fresh_subs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      unifies?(Delta, delta, (t, s)) AND var?(t) AND var?(s)
--  AND (V(t) = V(s)) AND disjoint?(dom(delta), vars(s))
--  IMPLIES nominal_AC_fresh_subs(Delta)(delta, fc(DS(p(t), p(s)), V(t)))
theorem unifies_nominal_AC_fresh_subs : unifies_p Delta delta ((t, s)) ∧ var_p t ∧ var_p s
 ∧ (V t = V s) ∧ disjoint_p (dom delta) (vars s) → nominal_AC_fresh_subs Delta(delta, fc (DS (p t) (p s)) (V t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.solves_elim_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      equal?(Delta)(delta, append(gamma, delta), V1)
--  AND subset?(vars(Nabla), V1) AND subset?(V1, V2)
--  AND solves?(Delta, append(gamma, delta))(Nabla, null, sigma, V2, PV2)
--  AND disjoint?(dom(delta), 
theorem solves_elim_append : equal_p Delta(delta, append gamma delta, V1)
 ∧ subset_p (vars Nabla) V1 ∧ subset_p V1 V2
 ∧ solves_p Delta (append gamma delta) (Nabla, null, sigma, V2, PV2)
 ∧ disjoint_p (dom delta) PV ∧ well_formed_p delta
 ∧ contained_p delta V1 → solves_p Delta delta(Nabla, null, sigma, V1, PV) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: nominal_AC_unification.is_instance_equal_syn
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_instance?(Delta, delta)(Nabla, sigma_, V1, PV) AND
--  equal_syn?(sigma, sigma_, V2) AND subset?(V2, V1)
--  IMPLIES is_instance?(Delta, delta)(Nabla, sigma, V2, PV)
theorem is_instance_equal_syn : is_instance_p Delta delta(Nabla, sigma_, V1, PV) ∧
 equal_syn_p sigma sigma_ V2 ∧ subset_p V2 V1 → is_instance_p Delta delta(Nabla, sigma, V2, PV) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.nominal_AC_unification