-- Auto-generated by pvs2lean/translator.py
-- PVS theory: syntactic_freshness
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: syntactic_freshness

Auto-generated stubs from PVSLib theory `syntactic_freshness`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.syntactic_freshness

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (act : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (app : pvs_τ3 → pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (append : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (at : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (cons : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (ext : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (member : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (pair : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (reverse : pvs_τ27 → pvs_τ28)

-- pvs2lean: syntactic_freshness.fresh
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES t
--   OF at(b): a /= b,
--      *(pm, v): member((act(reverse(pm))(a), v), Delta),
--      unit: TRUE,
--      pair(t1, t2): fresh(Delta)(a, t1) AND fresh(Delta)(a, t2),
--      abs(b, bd): IF a = b THEN TRUE
axiom fresh {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ18 → pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: syntactic_freshness.ext_preserves_fresh
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fresh(Delta)(act(pi)(a), ext(pi)(t)) = fresh(Delta)(a, t)
theorem ext_preserves_fresh : fresh Delta(act pi(a), ext pi(t)) = fresh Delta(a, t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: syntactic_freshness.ds_fresh_invariance
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: act(pi1) = act(pi2) IMPLIES
--  fresh(Delta)(a, ext(pi1)(t)) = fresh(Delta)(a, ext(pi2)(t))
theorem ds_fresh_invariance : act pi1 = act pi2 → fresh Delta(a, ext pi1(t)) = fresh Delta(a, ext pi2(t)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: syntactic_freshness.ext_reverse_fresh
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fresh(Delta)(a, ext(pi)(t)) = fresh(Delta)(act(reverse(pi))(a), t)
theorem ext_reverse_fresh : fresh Delta(a, ext pi(t)) = fresh Delta(act (reverse pi) (a), t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: syntactic_freshness.fresh_weakening
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (ax: [atom, variable]): member(ax, Delta) => member(ax, Nabla)) AND
--  fresh(Delta)(a, t)
--  => fresh(Nabla)(a, t)
theorem fresh_weakening : (∀ (ax : atom × variable), member ax Delta → member ax Nabla) ∧
 fresh Delta(a, t) → fresh Nabla(a, t) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: syntactic_freshness.fc
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES l OF null: null, cons(head, tail): cons((head, X), fc(tail, X))
--   ENDCASES
axiom fc {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} : pvs_τ15 → pvs_τ16 → pvs_τ17

-- pvs2lean: syntactic_freshness.fc_member
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: member((a, Y), fc(l, X)) = (member(a, l) AND Y = X)
theorem fc_member : member ((a, Y)) (fc l X) = (member a l ∧ Y = X) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: syntactic_freshness.x
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF Delta1`2 AND Delta2`2 THEN (append(Delta1`1, Delta2`1), TRUE)
-- ELSE ((: :), FALSE)
-- ENDIF
noncomputable def x : sorry := by
  sorry  -- pvs2lean: definition stub for IF Delta1`2 AND Delta2`2 THEN (append(Delta1`1, Delta2`1), TRUE) ELSE ((: :), FA

-- pvs2lean: syntactic_freshness.fresh?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES t
--   OF at(b): ((: :), a /= b),
--      *(pm, X): ((: (act(reverse(pm))(a), X) :), TRUE),
--      unit: ((: :), TRUE),
--      pair(t1, t2): x(fresh?(a, t1), fresh?(a, t2)),
--      abs(b, bd): IF a = b THEN
axiom fresh_p {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: syntactic_freshness.fresh?_is_fresh
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fresh?(a, t) = (Delta, TRUE) => fresh(Delta)(a, t)
theorem fresh_p_is_fresh : fresh_p a t = (Delta, True) → fresh Delta(a, t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: syntactic_freshness.ext_reverse_fresh?
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fresh?(a, ext(pi)(t)) = fresh?(act(reverse(pi))(a), t)
theorem ext_reverse_fresh_p : fresh_p a (ext pi(t)) = fresh_p (act (reverse pi) (a)) t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: syntactic_freshness.not_fresh_is_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT fresh?(a, t)`2 IMPLIES fresh?(a, t)`1 = (: :)
theorem not_fresh_is_null : ¬ fresh_p a t.2 → fresh_p a t.1 = (: :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: syntactic_freshness.fresh?_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (Nabla, b) = fresh?(a, t) IN
--   fresh(Delta)(a, t) IMPLIES
--    b AND (FORALL (b, X): member((b, X), Nabla) => member((b, X), Delta))
theorem fresh_p_complete : fun (Nabla : List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0})) (b : Bool) => fresh Delta(a, t) → b ∧ (∀ (b : _) (X : _), member ((b, X)) Nabla → member ((b, X)) Delta) (fresh_p a t) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.syntactic_freshness