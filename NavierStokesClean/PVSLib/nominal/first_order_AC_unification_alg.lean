-- Auto-generated by pvs2lean/translator.py
-- PVS theory: first_order_AC_unification_alg
-- PVSLib library: nominal
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: first_order_AC_unification_alg

Auto-generated stubs from PVSLib theory `first_order_AC_unification_alg`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.first_order_AC_unification_alg

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (ac_app : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} (ac_app_p : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} (ac_sym : pvs_τ4 → Prop)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (admissible_subterms : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (app : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (app_p : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (append : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (apply_sub : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (arg : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (choose_unif_pair : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (cons : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (const : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (contained_p : pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (decompose : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (equal_syn_p : pvs_τ25 → pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} (f_sym : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (first_order_AC_apply_ac_step : pvs_τ29 → pvs_τ30 → pvs_τ31 → pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (flatten : pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} (lex4 : pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} (map : pvs_τ39 → pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (member : pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} (more_general_p : pvs_τ44 → pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (nice_input_p : pvs_τ47 → pvs_τ48 → pvs_τ49 → Prop)
variable {pvs_τ50 : Type*} (null_p : pvs_τ50 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} (renamed_inputs_p : pvs_τ51 → pvs_τ52 → pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (size : pvs_τ54 → pvs_τ55)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (subset_p : pvs_τ56 → pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (unifies_p : pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ64 : Type*} (var_p : pvs_τ64 → Prop)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (pvs_variable : pvs_τ65 → pvs_τ66)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} (vars : pvs_τ67 → pvs_τ68 → pvs_τ69)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} (vars_not_ac : pvs_τ70 → pvs_τ71)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} (vars_under2more : pvs_τ72 → pvs_τ73)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} (well_formed_p : pvs_τ74 → pvs_τ75 → Prop)

-- pvs2lean: first_order_AC_unification_alg.unify
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, sigma, vars2avoid) = input IN
--   IF null?(unif_prb) THEN cons(sigma, null)
--   ELSE LET (unif_pair, unif_prb1) = choose_unif_pair(unif_prb),
--            (t, s) = unif_pair
--          IN
--      
axiom unify {pvs_τ60 : Type*} {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} : pvs_τ60 → pvs_τ61 → pvs_τ62 → pvs_τ63

-- pvs2lean: first_order_AC_unification_alg.unify_subs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, sigma, vars2avoid) = input IN
--   member(delta, unify(unif_prb, sigma, vars2avoid)) IMPLIES
--    EXISTS sigma1: delta = append(sigma1, sigma)
theorem unify_subs : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (vars2avoid : {i : ℝ // i ≥ 0} → Bool) => member delta (unify unif_prb sigma vars2avoid) → ∃ sigma1,  delta = append sigma1 sigma input := by
  -- === TCCs for unify_subs ===
  -- TCC: unify_subs_TCC1
  -- Statement: FORALL (input: nice_input, unif_prb: unif_prb, sigma: sub,
  --         vars2avoid: finite_set[variable]):
  --   unif_prb = input`1 AND sigma = input`2 AND vars2avoid = input`3 IMPLIES
  --    nice_input?(unif_p...
  have unify_subs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.unify_well_formed
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, sigma, vars2avoid) = input IN
--   member(delta, unify(unif_prb, sigma, vars2avoid)) IMPLIES
--    well_formed?(delta)
theorem unify_well_formed : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (vars2avoid : {i : ℝ // i ≥ 0} → Bool) => member delta (unify unif_prb sigma vars2avoid) → well_formed_p delta input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.renamed_inputs_unify_alg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: renamed_inputs?(input, input_, fixed_vars) AND
--  member(gamma_, unify(input_))
--  IMPLIES
--  EXISTS rho, gamma:
--    member(gamma, unify(input)) AND
--     equal_syn?(gamma_, append(rho, gamma), fixed_vars)
theorem renamed_inputs_unify_alg : renamed_inputs_p input input_ fixed_vars ∧ member gamma_ (unify input_) → ∃ rho gamma, 
  member gamma (unify input) ∧
   equal_syn_p gamma_ (append rho gamma) fixed_vars := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.unify_alg_correct_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sigma1 = cons((X, t), null),
--     new_sigma = append(sigma1, sigma),
--     new_unif_prb = apply_sub(sigma1, unif_prb1)
--   IN
--        (NOT member(X, vars(t))) AND member(variable(X), unif_prb)
--    AND me
theorem unify_alg_correct_sub : fun (sigma1 : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (new_sigma : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (new_unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => (¬ member X (vars t)) ∧ member (variable X) unif_prb
 ∧ member t unif_prb ∧ nice_input_p unif_prb sigma vars2avoid
 ∧ subset_p unif_prb1 unif_prb ∧ unifies_p delta new_unif_prb
 ∧ member delta (unify new_unif_prb new_sigma vars2avoid) → unifies_p delta ((variable X, t)) ∧ unifies_p delta unif_prb1 (apply_sub sigma1 unif_prb1) (append sigma1 sigma) (cons ((X, t)) null) := by
  -- === TCCs for unify_alg_correct_sub ===
  -- TCC: unify_alg_correct_sub_TCC1
  -- Statement: FORALL (X: variable, delta, sigma: sub,
  --         t: first_order_AC_term[constant, variable, f_symbol, ac_symbol],
  --         unif_prb, unif_prb1: unif_prb, vars2avoid: finite_set[variable],
  --         sig...
  have unify_alg_correct_sub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.unify_alg_correct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, sigma, vars2avoid) = input IN
--   member(delta, unify(unif_prb, sigma, vars2avoid)) IMPLIES
--    unifies?(delta, unif_prb)
theorem unify_alg_correct : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (vars2avoid : {i : ℝ // i ≥ 0} → Bool) => member delta (unify unif_prb sigma vars2avoid) → unifies_p delta unif_prb input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.unify_alg_correct_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(t, s) AND
--  member(delta, unify(cons((t, s), null), null, vars(t, s)))
--  IMPLIES unifies?(delta, cons((t, s), null))
theorem unify_alg_correct_cor : well_formed_p t s ∧
 member delta (unify (cons ((t, s)) null) null (vars t s)) → unifies_p delta (cons ((t, s)) null) := by
  -- === TCCs for unify_alg_correct_cor ===
  -- TCC: unify_alg_correct_cor_TCC1
  -- Statement: FORALL (s,
  --         t: first_order_AC_term[constant, variable, f_symbol, ac_symbol]):
  --   well_formed?(t, s) IMPLIES
  --    nice_input?(cons[[first_order_AC_term
  --                          [constant, variab...
  have unify_alg_correct_cor_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.unify_alg_complete_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET sigma1 = cons((X, t), null),
--     new_sigma = append(sigma1, sigma),
--     new_unif_prb = apply_sub(sigma1, unif_prb1)
--   IN
--        (NOT member(X, vars(t))) AND member(variable(X), unif_pair)
--    AND m
theorem unify_alg_complete_sub : fun (sigma1 : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (new_sigma : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => fun (new_unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) => (¬ member X (vars t)) ∧ member (variable X) unif_pair
 ∧ member t unif_pair ∧ member unif_pair unif_prb
 ∧ nice_input_p unif_prb sigma vars2avoid
 ∧ subset_p unif_prb1 unif_prb ∧ unifies_p delta unif_prb
 ∧ more_general_p sigma delta → more_general_p new_sigma delta ∧
 unifies_p delta (apply_sub sigma1 unif_prb1) (apply_sub sigma1 unif_prb1) (append sigma1 sigma) (cons ((X, t)) null) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.unify_alg_complete
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET (unif_prb, sigma, vars2avoid) = input IN
--   unifies?(delta, unif_prb) AND
--    more_general?(sigma, delta) AND
--     contained?(delta, vars2avoid) AND well_formed?(delta)
--    IMPLIES
--    EXISTS gamma:
--   
theorem unify_alg_complete : fun (unif_prb : List ((first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))) × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (sigma : List ({i : ℝ // i ≥ 0} × (first_order_AC_term ℝ ℝ ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character)) ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → character))))) (vars2avoid : {i : ℝ // i ≥ 0} → Bool) => unifies_p delta unif_prb ∧
 more_general_p sigma delta ∧
  contained_p delta vars2avoid ∧ well_formed_p delta → ∃ gamma, 
  member gamma (unify unif_prb sigma vars2avoid) ∧
   more_general_p gamma delta vars2avoid input := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.unify_alg_complete_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      well_formed?(t, s) AND unifies?(delta, (t, s))
--  AND contained?(delta, vars2avoid) AND subset?(vars(t, s), vars2avoid)
--  AND well_formed?(delta)
--  IMPLIES
--  EXISTS gamma:
--    member(gamma, unify(cons(
theorem unify_alg_complete_cor : well_formed_p t s ∧ unifies_p delta ((t, s))
 ∧ contained_p delta vars2avoid ∧ subset_p (vars t s) vars2avoid
 ∧ well_formed_p delta → ∃ gamma, 
  member gamma (unify (cons ((t, s)) null) null vars2avoid) ∧
   more_general_p gamma delta vars2avoid := by
  -- === TCCs for unify_alg_complete_cor ===
  -- TCC: unify_alg_complete_cor_TCC1
  -- Statement: FORALL (delta: sub,
  --         s, t: first_order_AC_term[constant, variable, f_symbol, ac_symbol],
  --         vars2avoid: finite_set[variable]):
  --        well_formed?(delta) AND subset?(vars(t, s), vars2av...
  have unify_alg_complete_cor_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: first_order_AC_unification_alg.unify_alg_complete_cor2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: well_formed?(t, s) AND unifies?(delta, (t, s)) AND well_formed?(delta)
--  IMPLIES
--  EXISTS gamma:
--    member(gamma, unify(cons((t, s), null), null, vars(t, s))) AND
--     more_general?(gamma, delta, vars(t,
theorem unify_alg_complete_cor2 : well_formed_p t s ∧ unifies_p delta ((t, s)) ∧ well_formed_p delta → ∃ gamma, 
  member gamma (unify (cons ((t, s)) null) null (vars t s)) ∧
   more_general_p gamma delta (vars t s) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.first_order_AC_unification_alg