-- Auto-generated by pvs2lean/translator.py
-- PVS theory: Plan
-- PVSLib library: aviation
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: Plan

Auto-generated stubs from PVSLib theory `Plan`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.Plan

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (ELSIF : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Range : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (alt : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (angle_between : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (below : pvs_τ10 → pvs_τ11)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (distanceH : pvs_τ19 → pvs_τ20 → pvs_τ21)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (final_course : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} (initialVelocity : pvs_τ73 → pvs_τ74 → pvs_τ75)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} (initial_course : pvs_τ76 → pvs_τ77 → pvs_τ78)
variable {pvs_τ89 : Type*} (length : pvs_τ89 → Prop)
variable {pvs_τ90 : Type*} {pvs_τ91 : Type*} {pvs_τ92 : Type*} {pvs_τ93 : Type*} {pvs_τ94 : Type*} (linearDist2D : pvs_τ90 → pvs_τ91 → pvs_τ92 → pvs_τ93 → pvs_τ94)
variable {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} {pvs_τ98 : Type*} (mkTrkGsVs : pvs_τ95 → pvs_τ96 → pvs_τ97 → pvs_τ98)
variable {pvs_τ99 : Type*} {pvs_τ100 : Type*} {pvs_τ101 : Type*} (mkZ : pvs_τ99 → pvs_τ100 → pvs_τ101)
variable {pvs_τ102 : Type*} {pvs_τ103 : Type*} {pvs_τ104 : Type*} (nth : pvs_τ102 → pvs_τ103 → pvs_τ104)
variable {pvs_τ134 : Type*} {pvs_τ135 : Type*} (pred : pvs_τ134 → pvs_τ135 → Prop)
variable {pvs_τ156 : Type*} {pvs_τ157 : Type*} (printstr : pvs_τ156 → pvs_τ157)
variable {pvs_τ160 : Type*} {pvs_τ161 : Type*} (real2str : pvs_τ160 → pvs_τ161)
variable {pvs_τ162 : Type*} (sign : pvs_τ162 → Prop)
variable {pvs_τ166 : Type*} {pvs_τ167 : Type*} {pvs_τ168 : Type*} (strconcat : pvs_τ166 → pvs_τ167 → pvs_τ168)
variable {pvs_τ169 : Type*} {pvs_τ170 : Type*} {pvs_τ171 : Type*} (track : pvs_τ169 → pvs_τ170 → pvs_τ171)
variable {pvs_τ172 : Type*} {pvs_τ173 : Type*} (trk : pvs_τ172 → pvs_τ173)
variable {pvs_τ189 : Type*} {pvs_τ190 : Type*} {pvs_τ191 : Type*} {pvs_τ192 : Type*} {pvs_τ193 : Type*} {pvs_τ194 : Type*} (turnByDist2D : pvs_τ189 → pvs_τ190 → pvs_τ191 → pvs_τ192 → pvs_τ193 → pvs_τ194)
variable {pvs_τ201 : Type*} {pvs_τ202 : Type*} (vect2 : pvs_τ201 → pvs_τ202)

-- pvs2lean: Plan.point
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i >= length(p) THEN ZERO_LL ELSE nth(p, i)`np ENDIF
axiom point {pvs_τ119 : Type*} {pvs_τ120 : Type*} {pvs_τ121 : Type*} : pvs_τ119 → pvs_τ120 → pvs_τ121

-- pvs2lean: Plan.data
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i >= length(p) THEN ZERO ELSE nth(p, i)`tcp ENDIF
axiom data {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: Plan.time
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i >= 0 AND i < length(p) THEN nth(p, i)`np`time ELSE -1 ENDIF
noncomputable def time : sorry := by
  sorry  -- pvs2lean: definition stub for IF i >= 0 AND i < length(p) THEN nth(p, i)`np`time ELSE -1 ENDIF

-- pvs2lean: Plan.ordered?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: int):
--   0 <= i AND i < length(p) - 1 IMPLIES
--    IF i >= 0 AND i < length(p) THEN nth(p, i)`np`time ELSE -1 ENDIF <
--     IF i + 1 >= 0 AND i + 1 < length(p) THEN nth(p, i + 1)`np`time
--     ELS
axiom ordered_p {pvs_τ105 : Type*} : pvs_τ105 → Prop

-- pvs2lean: Plan.size
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(p)
noncomputable def size : sorry := by
  sorry  -- pvs2lean: definition stub for length(p)

-- pvs2lean: Plan.ordered_lem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: list[PlanPoint], i, j: below[length(p)]):
--   ordered?(p) AND i < j IMPLIES
--    IF i >= 0 AND i < length(p) THEN nth(p, i)`np`time ELSE -1 ENDIF <
--     IF j >= 0 AND j < length(p) THEN nth(p, j
theorem ordered_lem : ∀ (p : List (((Bool × Bool × (ℝ × ℝ × ℝ) × (ℝ × ℝ × ℝ)) × {x : ℝ // x ≥ 0}) × (ℝ × ℝ × (Bool × Bool × (ℝ × ℝ × ℝ) × (ℝ × ℝ × ℝ)) × ℝ × ℝ × Gs_TCPType × Trk_TCPType × Vs_TCPType × WayType))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length p}), ordered_p p ∧ i < j → if i ≥ 0 ∧ i < List.length p then nth p i.np.time else -1  <
 if j ≥ 0 ∧ j < List.length p then nth p j.np.time else -1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Plan.getFirstTime
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF 0 >= 0 AND 0 < length(p) THEN nth(p, 0)`np`time ELSE -1 ENDIF
noncomputable def getFirstTime : sorry := by
  sorry  -- pvs2lean: definition stub for IF 0 >= 0 AND 0 < length(p) THEN nth(p, 0)`np`time ELSE -1 ENDIF

-- pvs2lean: Plan.getLastTime
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(p) - 1 >= 0 AND length(p) - 1 < length(p)
--   THEN nth(p, length(p) - 1)`np`time
-- ELSE -1
-- ENDIF
noncomputable def getLastTime : sorry := by
  sorry  -- pvs2lean: definition stub for IF length(p) - 1 >= 0 AND length(p) - 1 < length(p)   THEN nth(p, length(p) - 1)

-- pvs2lean: Plan.isLatLon
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: point(p, 0)`position`latlon
axiom isLatLon {pvs_τ84 : Type*} {pvs_τ85 : Type*} : pvs_τ84 → pvs_τ85

-- pvs2lean: Plan.range
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: {t: real |
--    length(p) > 0 IMPLIES
--     IF 0 >= 0 AND 0 < length(p) THEN nth(p, 0)`np`time ELSE -1 ENDIF <= t
--      AND
--      t <=
--       IF length(p) - 1 >= 0 AND length(p) - 1 < length(p)
--         THEN 
axiom range {pvs_τ158 : Type*} {pvs_τ159 : Type*} : pvs_τ158 → pvs_τ159 → Prop

-- pvs2lean: Plan.getIndexInRange_rec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (n <= 1) THEN 0
-- ELSIF IF n >= 0 AND n < length(p) THEN nth(p, n)`np`time ELSE -1 ENDIF = t
--   THEN n
-- ELSIF IF n - 1 >= 0 AND n - 1 < length(p) THEN nth(p, n - 1)`np`time
--       ELSE -1
--       ENDIF
--   
axiom getIndexInRange_rec {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} : pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45

-- pvs2lean: Plan.getIndexInRange
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(p) = 0 THEN -1 ELSE getIndexInRange_rec(p, t, length(p) - 1) ENDIF
axiom getIndexInRange {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41

-- pvs2lean: Plan.getIndex
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(p) = 0 OR
--     t < IF 0 >= 0 AND 0 < length(p) THEN nth(p, 0)`np`time ELSE -1 ENDIF
--   THEN -1
-- ELSIF t >
--        IF length(p) - 1 >= 0 AND length(p) - 1 < length(p)
--          THEN nth(p, length(
axiom getIndex {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ36 → pvs_τ37 → pvs_τ38

-- pvs2lean: Plan.getSegment
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET i = getIndex(p, time) IN
--   IF (i = -1) THEN -1
--   ELSIF (i >= 0) THEN i
--   ELSIF (-i - 2 = length(p) - 1) THEN -1
--   ELSE -i - 2
--   ENDIF
axiom getSegment {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ46 → pvs_τ47 → pvs_τ48

-- pvs2lean: Plan.getTcpData
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)
noncomputable def getTcpData : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)

-- pvs2lean: Plan.isTCP
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_trk /= NONE OR
--  data(p, i)`tcp_gs /= NONE OR data(p, i)`tcp_vs /= NONE
axiom isTCP {pvs_τ86 : Type*} {pvs_τ87 : Type*} {pvs_τ88 : Type*} : pvs_τ86 → pvs_τ87 → pvs_τ88

-- pvs2lean: Plan.isTrkTCP
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_trk /= NONE
noncomputable def isTrkTCP : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_trk /= NONE

-- pvs2lean: Plan.isBOT
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_trk = BOT OR data(p, i)`tcp_trk = EOTBOT
noncomputable def isBOT : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_trk = BOT OR data(p, i)`tcp_trk = EOTBOT

-- pvs2lean: Plan.isEOT
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_trk = EOT OR data(p, i)`tcp_trk = EOTBOT
noncomputable def isEOT : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_trk = EOT OR data(p, i)`tcp_trk = EOTBOT

-- pvs2lean: Plan.isGsTCP
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_gs /= NONE
noncomputable def isGsTCP : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_gs /= NONE

-- pvs2lean: Plan.isBGS
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_gs = BGS OR data(p, i)`tcp_gs = EGSBGS
noncomputable def isBGS : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_gs = BGS OR data(p, i)`tcp_gs = EGSBGS

-- pvs2lean: Plan.isEGS
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_gs = EGS OR data(p, i)`tcp_gs = EGSBGS
noncomputable def isEGS : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_gs = EGS OR data(p, i)`tcp_gs = EGSBGS

-- pvs2lean: Plan.isVsTCP
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_vs /= NONE
noncomputable def isVsTCP : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_vs /= NONE

-- pvs2lean: Plan.isBVS
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_vs = BVS OR data(p, i)`tcp_vs = EVSBVS
noncomputable def isBVS : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_vs = BVS OR data(p, i)`tcp_vs = EVSBVS

-- pvs2lean: Plan.isEVS
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: data(p, i)`tcp_vs = EVS OR data(p, i)`tcp_vs = EVSBVS
noncomputable def isEVS : sorry := by
  sorry  -- pvs2lean: definition stub for data(p, i)`tcp_vs = EVS OR data(p, i)`tcp_vs = EVSBVS

-- pvs2lean: Plan.isLinear
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(length(p))): NOT isTCP(p, i)
noncomputable def isLinear : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL (i: below(length(p))): NOT isTCP(p, i)

-- pvs2lean: Plan.makeNavPoint
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# np := npn, tcp := pp`tcp #)
noncomputable def makeNavPoint : sorry := by
  sorry  -- pvs2lean: definition stub for (# np := npn, tcp := pp`tcp #)

-- pvs2lean: Plan.makeTcpData
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# np := pp`np, tcp := tcpd #)
noncomputable def makeTcpData : sorry := by
  sorry  -- pvs2lean: definition stub for (# np := pp`np, tcp := tcpd #)

-- pvs2lean: Plan.findPrev
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF current < 0 THEN -1
-- ELSIF pred(p, current) THEN current
-- ELSE findPrev(p, current - 1, pred)
-- ENDIF
axiom findPrev {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35

-- pvs2lean: Plan.prevBOT
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF current < 0 OR current > length(p) THEN -1
-- ELSE findPrev(p, current - 1, isBOT)
-- ENDIF
axiom prevBOT {pvs_τ139 : Type*} {pvs_τ140 : Type*} {pvs_τ141 : Type*} : pvs_τ139 → pvs_τ140 → pvs_τ141

-- pvs2lean: Plan.prevEOT
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF current < 0 OR current > length(p) THEN -1
-- ELSE findPrev(p, current - 1, isEOT)
-- ENDIF
axiom prevEOT {pvs_τ148 : Type*} {pvs_τ149 : Type*} {pvs_τ150 : Type*} : pvs_τ148 → pvs_τ149 → pvs_τ150

-- pvs2lean: Plan.prevBGS
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF current < 0 OR current > length(p) THEN -1
-- ELSE findPrev(p, current - 1, isBGS)
-- ENDIF
axiom prevBGS {pvs_τ136 : Type*} {pvs_τ137 : Type*} {pvs_τ138 : Type*} : pvs_τ136 → pvs_τ137 → pvs_τ138

-- pvs2lean: Plan.prevEGS
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF current < 0 OR current > length(p) THEN -1
-- ELSE findPrev(p, current - 1, isEGS)
-- ENDIF
axiom prevEGS {pvs_τ145 : Type*} {pvs_τ146 : Type*} {pvs_τ147 : Type*} : pvs_τ145 → pvs_τ146 → pvs_τ147

-- pvs2lean: Plan.prevBVS
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF current < 0 OR current > length(p) THEN -1
-- ELSE findPrev(p, current - 1, isBVS)
-- ENDIF
axiom prevBVS {pvs_τ142 : Type*} {pvs_τ143 : Type*} {pvs_τ144 : Type*} : pvs_τ142 → pvs_τ143 → pvs_τ144

-- pvs2lean: Plan.prevEVS
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF current < 0 OR current > length(p) THEN -1
-- ELSE findPrev(p, current - 1, isEVS)
-- ENDIF
axiom prevEVS {pvs_τ151 : Type*} {pvs_τ152 : Type*} {pvs_τ153 : Type*} : pvs_τ151 → pvs_τ152 → pvs_τ153

-- pvs2lean: Plan.inTrkChange
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET i = getSegment(p, t), j1 = prevBOT(p, i + 1), j2 = prevEOT(p, i + 1) IN
--   j1 >= 0 AND j1 >= j2
axiom inTrkChange {pvs_τ69 : Type*} {pvs_τ70 : Type*} : pvs_τ69 → pvs_τ70 → Prop

-- pvs2lean: Plan.inGsChange
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET i = getSegment(p, t), j1 = prevBGS(p, i + 1), j2 = prevEGS(p, i + 1) IN
--   j1 >= 0 AND j1 >= j2
axiom inGsChange {pvs_τ67 : Type*} {pvs_τ68 : Type*} : pvs_τ67 → pvs_τ68 → Prop

-- pvs2lean: Plan.inVsChange
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET i = getSegment(p, t), j1 = prevBVS(p, i + 1), j2 = prevEVS(p, i + 1) IN
--   j1 >= 0 AND j1 >= j2
axiom inVsChange {pvs_τ71 : Type*} {pvs_τ72 : Type*} : pvs_τ71 → pvs_τ72 → Prop

-- pvs2lean: Plan.signedRadius
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i >= length(p) THEN 0 ELSE data(p, i)`radiusSigned ENDIF
axiom signedRadius {pvs_τ163 : Type*} {pvs_τ164 : Type*} {pvs_τ165 : Type*} : pvs_τ163 → pvs_τ164 → pvs_τ165

-- pvs2lean: Plan.turnDir
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sign(data(p, i)`radiusSigned)
axiom turnDir {pvs_τ198 : Type*} {pvs_τ199 : Type*} {pvs_τ200 : Type*} : pvs_τ198 → pvs_τ199 → pvs_τ200

-- pvs2lean: Plan.turnCenter
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i >= length(p) THEN INVALID ELSE data(p, i)`centerTurn ENDIF
axiom turnCenter {pvs_τ195 : Type*} {pvs_τ196 : Type*} {pvs_τ197 : Type*} : pvs_τ195 → pvs_τ196 → pvs_τ197

-- pvs2lean: Plan.gsAccel
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i >= length(p) THEN -1 ELSE data(p, i)`accelGs ENDIF
axiom gsAccel {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} : pvs_τ49 → pvs_τ50 → pvs_τ51

-- pvs2lean: Plan.vsAccel
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i >= length(p) THEN -1 ELSE data(p, i)`accelVs ENDIF
axiom vsAccel {pvs_τ203 : Type*} {pvs_τ204 : Type*} {pvs_τ205 : Type*} : pvs_τ203 → pvs_τ204 → pvs_τ205

-- pvs2lean: Plan.pathDistance
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i + 1 >= length(p) THEN 0
-- ELSE LET p1 = point(p, i), tt = p1`time IN
--        IF NOT linear AND inTrkChange(p, tt)
--          THEN LET p2 = point(p, i + 1),
--                   ixBOT = prevBOT(p
axiom pathDistance {pvs_τ106 : Type*} {pvs_τ107 : Type*} {pvs_τ108 : Type*} {pvs_τ109 : Type*} : pvs_τ106 → pvs_τ107 → pvs_τ108 → pvs_τ109

-- pvs2lean: Plan.pathDistanceNat
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (length(p) = 0) THEN 0
-- ELSIF j >= length(p) THEN pathDistanceNat(p, i, length(p) - 1, linear)
-- ELSIF i >= j THEN 0
-- ELSE pathDistanceNat(p, i, j - 1, linear) + pathDistance(p, j - 1, linear)
-- ENDIF
axiom pathDistanceNat {pvs_τ114 : Type*} {pvs_τ115 : Type*} {pvs_τ116 : Type*} {pvs_τ117 : Type*} {pvs_τ118 : Type*} : pvs_τ114 → pvs_τ115 → pvs_τ116 → pvs_τ117 → pvs_τ118

-- pvs2lean: Plan.pathDistance4
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 THEN pathDistanceNat(p, 0, j, linear)
-- ELSE pathDistanceNat(p, i, j, linear)
-- ENDIF
axiom pathDistance4 {pvs_τ110 : Type*} {pvs_τ111 : Type*} {pvs_τ112 : Type*} {pvs_τ113 : Type*} : pvs_τ110 → pvs_τ111 → pvs_τ112 → pvs_τ113 → Prop

-- pvs2lean: Plan.pathDistanceFull
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pathDistance4(p, 0, length(p), FALSE)
noncomputable def pathDistanceFull : sorry := by
  sorry  -- pvs2lean: definition stub for pathDistance4(p, 0, length(p), FALSE)

-- pvs2lean: Plan.trkInTurn
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF pos`latlon
--   THEN LET final_course = GreatCircle.final_course(center`ll, pos`ll) IN
--          final_course + dir * pi / 2
-- ELSE LET trkFromCenter = Velocity.track(center`s3, pos`s3),
--          nTrk = 
axiom trkInTurn {pvs_τ181 : Type*} {pvs_τ182 : Type*} {pvs_τ183 : Type*} {pvs_τ184 : Type*} {pvs_τ185 : Type*} : pvs_τ181 → pvs_τ182 → pvs_τ183 → pvs_τ184 → pvs_τ185

-- pvs2lean: Plan.trkFinal
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF seg < 0 OR seg >= length(p) - 1 THEN -1
-- ELSE IF inTrkChange(p,
--                     IF seg >= 0 AND seg < length(p)
--                       THEN nth(p, seg)`np`time
--                     ELSE -1
--        
axiom trkFinal {pvs_τ174 : Type*} {pvs_τ175 : Type*} {pvs_τ176 : Type*} {pvs_τ177 : Type*} : pvs_τ174 → pvs_τ175 → pvs_τ176 → pvs_τ177

-- pvs2lean: Plan.trkOut
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (seg = length(p) - 1) THEN trkFinal(p, seg - 1, linear)
-- ELSE IF inTrkChange(p,
--                     IF seg >= 0 AND seg < length(p)
--                       THEN nth(p, seg)`np`time
--                    
axiom trkOut {pvs_τ186 : Type*} {pvs_τ187 : Type*} {pvs_τ188 : Type*} : pvs_τ186 → pvs_τ187 → pvs_τ188 → Prop

-- pvs2lean: Plan.trkIn
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: trkFinal(p, seg - 1, FALSE)
axiom trkIn {pvs_τ178 : Type*} {pvs_τ179 : Type*} {pvs_τ180 : Type*} : pvs_τ178 → pvs_τ179 → pvs_τ180

-- pvs2lean: Plan.gsFinal
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i > length(p) - 1 THEN -1
-- ELSE LET dist = pathDistance4(p, i, i + 1, linear),
--          dt =
--            IF i + 1 >= 0 AND i + 1 < length(p) THEN nth(p, i + 1)`np`time
--            ELSE -1
--    
axiom gsFinal {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} : pvs_τ58 → pvs_τ59 → pvs_τ60 → pvs_τ61

-- pvs2lean: Plan.gsOut
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i > length(p) - 1 THEN -1
-- ELSIF i = length(p) - 1 THEN gsFinal(p, i - 1)
-- ELSE LET j = i + 1,
--          dist = pathDistance4(p, i, j, linear),
--          dt =
--            IF j >= 0 AND j < leng
axiom gsOut {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} : pvs_τ64 → pvs_τ65 → pvs_τ66 → Prop

-- pvs2lean: Plan.gsIn
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: gsFinal(p, i - 1, FALSE)
axiom gsIn {pvs_τ62 : Type*} {pvs_τ63 : Type*} : pvs_τ62 → pvs_τ63 → Prop

-- pvs2lean: Plan.gsAtTime
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF inGsChange(p, t) AND NOT linear
--   THEN LET dt =
--              t -
--               IF seg >= 0 AND seg < length(p) THEN nth(p, seg)`np`time
--               ELSE -1
--               ENDIF,
--            gsAccel
axiom gsAtTime {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} : pvs_τ52 → pvs_τ53 → pvs_τ54 → pvs_τ55 → pvs_τ56 → pvs_τ57

-- pvs2lean: Plan.vsFinal
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i > length(p) - 1 THEN -1
-- ELSE LET j = i + 1,
--          dist = alt(point(p, j)) - alt(point(p, i)),
--          dt =
--            IF j >= 0 AND j < length(p) THEN nth(p, j)`np`time ELSE -1 ENDIF
axiom vsFinal {pvs_τ212 : Type*} {pvs_τ213 : Type*} {pvs_τ214 : Type*} {pvs_τ215 : Type*} : pvs_τ212 → pvs_τ213 → pvs_τ214 → pvs_τ215

-- pvs2lean: Plan.vsOut
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 OR i >= length(p) - 1 THEN -1
-- ELSIF i = length(p) - 1 THEN vsFinal(p, i - 1)
-- ELSE LET dist = alt(point(p, i + 1)) - alt(point(p, i)),
--          dt =
--            IF i + 1 >= 0 AND i + 1 < length
axiom vsOut {pvs_τ218 : Type*} {pvs_τ219 : Type*} {pvs_τ220 : Type*} {pvs_τ221 : Type*} : pvs_τ218 → pvs_τ219 → pvs_τ220 → pvs_τ221

-- pvs2lean: Plan.vsIn
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vsFinal(p, i - 1, FALSE)
axiom vsIn {pvs_τ216 : Type*} {pvs_τ217 : Type*} : pvs_τ216 → pvs_τ217 → Prop

-- pvs2lean: Plan.vsAtTime
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF inVsChange(p, t) AND NOT linear
--   THEN LET dt =
--              t -
--               IF seg >= 0 AND seg < length(p) THEN nth(p, seg)`np`time
--               ELSE -1
--               ENDIF
--          IN vsAtSeg
axiom vsAtTime {pvs_τ206 : Type*} {pvs_τ207 : Type*} {pvs_τ208 : Type*} {pvs_τ209 : Type*} {pvs_τ210 : Type*} {pvs_τ211 : Type*} : pvs_τ206 → pvs_τ207 → pvs_τ208 → pvs_τ209 → pvs_τ210 → pvs_τ211

-- pvs2lean: Plan.print_i
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: printstr(strconcat(strconcat(" finalVelocity3: i = ", real2str(i)),
--                    newline))
axiom print_i {pvs_τ154 : Type*} {pvs_τ155 : Type*} : pvs_τ154 → pvs_τ155

-- pvs2lean: Plan.finalVelocity3
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET pr = print_i(i) IN
--   IF i >= length(p) THEN INVALID
--   ELSIF i = length(p) - 1 THEN vectors_3D.zero
--   ELSE Velocity.mkTrkGsVs
--            (trkFinal(p, i, linear), gsFinal(p, i, linear),
--             
axiom finalVelocity3 {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28

-- pvs2lean: Plan.finalVelocity
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: finalVelocity3(p, i, FALSE)
axiom finalVelocity {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ22 → pvs_τ23 → pvs_τ24

-- pvs2lean: Plan.distFromPointToTime
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET gs0 = gsOut(p, seg, linear),
--     dt =
--       t -
--        IF seg >= 0 AND seg < length(p) THEN nth(p, seg)`np`time
--        ELSE -1
--        ENDIF
--   IN
--   IF inGsChange(p, t) AND NOT linear
--     THEN LET a
axiom distFromPointToTime {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: Plan.interpolateAltVs
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET tSeg =
--       IF seg >= 0 AND seg < length(p) THEN nth(p, seg)`np`time
--       ELSE -1
--       ENDIF,
--     vsAccel_d =
--       IF (NOT linear AND inVsChange(p, tSeg))
--         THEN vsAccel(p, prevBVS(p, se
axiom interpolateAltVs {pvs_τ79 : Type*} {pvs_τ80 : Type*} {pvs_τ81 : Type*} {pvs_τ82 : Type*} {pvs_τ83 : Type*} : pvs_τ79 → pvs_τ80 → pvs_τ81 → pvs_τ82 → pvs_τ83

-- pvs2lean: Plan.posVelWithinSeg2D
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET np1 = point(p, seg), so = np1`position IN
--   IF NOT linear AND inTrkChange(p, t)
--     THEN LET ixPrevBOT = prevBOT(p, seg + 1),
--              center = turnCenter(p, ixPrevBOT),
--              dir = tur
axiom posVelWithinSeg2D {pvs_τ122 : Type*} {pvs_τ123 : Type*} {pvs_τ124 : Type*} {pvs_τ125 : Type*} {pvs_τ126 : Type*} {pvs_τ127 : Type*} : pvs_τ122 → pvs_τ123 → pvs_τ124 → pvs_τ125 → pvs_τ126 → pvs_τ127

-- pvs2lean: Plan.print_adv
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: printstr(strconcat(strconcat(" positionVelocity: gs0 = ", real2str(x)),
--                    newline))
noncomputable def print_adv : sorry := by
  sorry  -- pvs2lean: definition stub for printstr(strconcat(strconcat(" positionVelocity: gs0 = ", real2str(x)),         

-- pvs2lean: Plan.positionVelocity
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (t < IF 0 >= 0 AND 0 < length(p) THEN nth(p, 0)`np`time ELSE -1 ENDIF)
--   THEN (INVALID, vectors_3D.zero)
-- ELSE LET seg = getSegment(p, t), np1 = point(p, seg) IN
--        IF (seg + 1 > length(p) - 1)
-- 
axiom positionVelocity {pvs_τ130 : Type*} {pvs_τ131 : Type*} {pvs_τ132 : Type*} {pvs_τ133 : Type*} : pvs_τ130 → pvs_τ131 → pvs_τ132 → pvs_τ133

-- pvs2lean: Plan.position
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionVelocity(p, t, linear)`1
axiom position {pvs_τ128 : Type*} {pvs_τ129 : Type*} : pvs_τ128 → pvs_τ129

-- pvs2lean: Plan.velocity
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: positionVelocity(p, t, linear)`2
noncomputable def velocity : sorry := by
  sorry  -- pvs2lean: definition stub for positionVelocity(p, t, linear)`2

-- pvs2lean: Plan.mk_PlanPoint
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# np := np0, tcp := ZERO #)
noncomputable def mk_PlanPoint : sorry := by
  sorry  -- pvs2lean: definition stub for (# np := np0, tcp := ZERO #)

-- pvs2lean: Plan.isVelocityContinuous
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: trkIn(p, i) = trkOut(p, i) AND
--  gsIn(p, i) = gsOut(p, i) AND vsIn(p, i) = vsOut(p, i)
noncomputable def isVelocityContinuous : sorry := by
  sorry  -- pvs2lean: definition stub for trkIn(p, i) = trkOut(p, i) AND  gsIn(p, i) = gsOut(p, i) AND vsIn(p, i) = vsOut(

end

end NavierStokesClean.PVSLib.Plan