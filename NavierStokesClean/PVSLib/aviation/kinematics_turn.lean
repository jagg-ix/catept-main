-- Auto-generated by pvs2lean/translator.py
-- PVS theory: kinematics_turn
-- PVSLib library: aviation
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: kinematics_turn

Auto-generated stubs from PVSLib theory `kinematics_turn`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.kinematics_turn

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (ELSIF : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (Tan_p : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (add_track : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} (atan : pvs_τ6 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (cos : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (dist : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} (groundSpeed : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} (ground_speed : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (linear_pos : pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (norm : pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (sign : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (sin : pvs_τ32 → pvs_τ33)
variable {pvs_τ36 : Type*} (sq : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (tan : pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (track : pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} (trk : pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} (trkgs2vect : pvs_τ42 → pvs_τ43 → pvs_τ44)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} {pvs_τ72 : Type*} (util_clockwise : pvs_τ70 → pvs_τ71 → pvs_τ72)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} (util_turnDelta : pvs_τ73 → pvs_τ74 → pvs_τ75 → Prop)
variable {pvs_τ76 : Type*} (vect2 : pvs_τ76 → Prop)

-- pvs2lean: kinematics_turn.linear_traj
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# pos := linear_pos(sv0, t), vel := sv0`vel #)
axiom linear_traj {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ25 → pvs_τ26 → pvs_τ27

-- pvs2lean: kinematics_turn.linear_preserves_gs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2: posreal, sv0: svpair):
--   ground_speed(linear_traj(sv0, t1)`vel) =
--    ground_speed(linear_traj(sv0, t2)`vel)
theorem linear_preserves_gs : ∀ (t1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (t2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (sv0 : (ℝ × ℝ × ℝ) × (ℝ × ℝ × ℝ)), ground_speed (linear_traj sv0 t1.vel) = ground_speed (linear_traj sv0 t2.vel) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.linear_preserves_vs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2: posreal, sv0: svpair):
--   linear_traj(sv0, t1)`vel`z = linear_traj(sv0, t2)`vel`z
theorem linear_preserves_vs : ∀ (t1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (t2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (sv0 : (ℝ × ℝ × ℝ) × (ℝ × ℝ × ℝ)), linear_traj sv0 t1.vel.z = linear_traj sv0 t2.vel.z := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.linear_preserves_track
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2: posreal, sv0: nzh_svpair):
--   track(linear_traj(sv0, t1)`vel) = track(linear_traj(sv0, t2)`vel)
theorem linear_preserves_track : ∀ (t1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (t2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (sv0 : (ℝ × ℝ × ℝ) × ({v : ℝ × ℝ × ℝ // vect2 v ≠ zero})), track (linear_traj sv0 t1.vel) = track (linear_traj sv0 t2.vel) := by
  -- === TCCs for linear_preserves_track ===
  -- TCC: linear_preserves_track_TCC1
  -- Statement: FORALL (t1: posreal, sv0: nzh_svpair):
  --   vect2(linear_traj(sv0, t1)`vel) /= zero
  have linear_preserves_track_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnRadius
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (abs(bank) >= pi / 2) THEN NaN
-- ELSE sq(speed) / (g * tan(abs(bank)))
-- ENDIF
axiom turnRadius {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} : pvs_τ55 → pvs_τ56 → pvs_τ57 → pvs_τ58

-- pvs2lean: kinematics_turn.speedOfTurn
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (abs(bank) >= pi / 2) THEN NaN ELSE sqrt(gn * tan(abs(bank)) * R) ENDIF
axiom speedOfTurn {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ34 → pvs_τ35 → Prop

-- pvs2lean: kinematics_turn.turnRate
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (bank = 0) THEN 0
-- ELSE sign(bank) * speed / turnRadius(speed, bank, g)
-- ENDIF
axiom turnRate {pvs_τ59 : Type*} {pvs_τ60 : Type*} : pvs_τ59 → pvs_τ60 → Prop

-- pvs2lean: kinematics_turn.bankAngle
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(sq(speed) / (R * gn))
axiom bankAngle {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9

-- pvs2lean: kinematics_turn.signedBankAngle
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(turnRate * speed / gn)
noncomputable def signedBankAngle : sorry := by
  sorry  -- pvs2lean: definition stub for atan(turnRate * speed / gn)

-- pvs2lean: kinematics_turn.bank_radius
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(bank) < pi / 2 IMPLIES
--  LET R = turnRadius(speed, bank, gn) IN
--    bankAngle(R, speed) = atan(sq(speed) / (R * gn))
theorem bank_radius : |bank| < pi / 2 → (let R := turnRadius speed bank gn; bankAngle R speed = atan ((speed)^2 / (R * gn))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: kinematics_turn.bank_speed
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(bank) < pi / 2 IMPLIES
--  LET s = speedOfTurn(radius, bank) IN
--    bankAngle(radius, s) = atan(sq(s) / (radius * gn))
theorem bank_speed : |bank| < pi / 2 → (let s := speedOfTurn radius bank; bankAngle radius s = atan ((s)^2 / (radius * gn))) := by
  -- === TCCs for bank_speed ===
  -- TCC: bank_speed_TCC1
  -- Statement: FORALL (bank, radius: posreal):
  --   abs(bank) < pi / 2 IMPLIES
  --    FORALL (s: nnreal): s = speedOfTurn(radius, bank) IMPLIES s > 0
  have bank_speed_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: kinematics_turn.turnDone
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (currentTrk = targetTrk) THEN TRUE
-- ELSIF (turnRight) THEN NOT util_clockwise(currentTrk, targetTrk)
-- ELSE util_clockwise(currentTrk, targetTrk)
-- ENDIF
noncomputable def turnDone : sorry := by
  sorry  -- pvs2lean: definition stub for IF (currentTrk = targetTrk) THEN TRUE ELSIF (turnRight) THEN NOT util_clockwise(

-- pvs2lean: kinematics_turn.delta_clockwise
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (alpha < beta AND alpha + pi > beta) IMPLIES
--  util_turnDelta(alpha, beta) = util_turnDelta(alpha, beta, TURN_RIGHT)
theorem delta_clockwise : (alpha < beta ∧ alpha + pi > beta) → util_turnDelta alpha beta = util_turnDelta alpha beta TURN_RIGHT := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnTime
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET omega = turnRate(ground_speed(v0), bankAng) IN
--   LET deltaAng = util_turnDelta(track(v0), goalTrack, turnRight) IN
--     IF (omega = 0) THEN NaN ELSE abs(deltaAng / omega) ENDIF
axiom turnTime {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} : pvs_τ61 → pvs_τ62 → pvs_τ63 → pvs_τ64

-- pvs2lean: kinematics_turn.turnTimeAlt
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET omega = turnRate(gs, bankAng),
--     deltaAng = util_turnDelta(origTrack, goalTrack)
--   IN IF omega = 0 THEN NaN ELSE abs(deltaAng / omega) ENDIF
axiom turnTimeAlt {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} : pvs_τ65 → pvs_τ66 → pvs_τ67 → pvs_τ68 → pvs_τ69

-- pvs2lean: kinematics_turn.turnTimeAlt_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v0: nzhVect3, goalTrack: real, bankAng: real):
--   turnTime(v0, goalTrack, bankAng) =
--    turnTimeAlt(ground_speed(v0), track(v0), goalTrack, bankAng)
theorem turnTimeAlt_def : ∀ (v0 : {v : ℝ × ℝ × ℝ // vect2 v ≠ zero}) (goalTrack : ℝ) (bankAng : ℝ), turnTime v0 goalTrack bankAng = turnTimeAlt (ground_speed v0) (track v0) goalTrack bankAng := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnRight
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: util_clockwise(track(v0), goalTrack)
noncomputable def turnRight : sorry := by
  sorry  -- pvs2lean: definition stub for util_clockwise(track(v0), goalTrack)

-- pvs2lean: kinematics_turn.turnDir
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (turnRight) THEN 1 ELSE -1 ENDIF
noncomputable def turnDir : sorry := by
  sorry  -- pvs2lean: definition stub for IF (turnRight) THEN 1 ELSE -1 ENDIF

-- pvs2lean: kinematics_turn.turnOmega
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF omega = 0 THEN (s0 + t * v0, v0)
-- ELSE LET v = ground_speed(v0) IN
--        LET theta = trk(vect2(v0)) IN
--          LET trk = theta + omega * t IN
--            ((# x
--                  := s0`x +
--           
axiom turnOmega {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} : pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48 → pvs_τ49

-- pvs2lean: kinematics_turn.turnOmega_start
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s0: Vect3, (v0: Vect3 | vect2(v0) /= zero), omega: real):
--   turnOmega(s0, v0, 0, omega) = (s0, v0)
theorem turnOmega_start : ∀ (s0 : ℝ × ℝ × ℝ) (v0 : {v0 : ℝ × ℝ × ℝ // vect2 v0 ≠ zero}) (omega : ℝ), turnOmega s0 v0 0 omega = (s0, v0) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: kinematics_turn.turnOmegaAT
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF omega = 0 THEN (s0 + t * v0, v0)
-- ELSE LET nv = add_track(vect2(v0), omega * t) IN
--        ((# x := s0`x + (v0`y - nv`y) / omega,
--            y := s0`y + (-v0`x + nv`x) / omega,
--            z := s0`z +
axiom turnOmegaAT {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} : pvs_τ50 → pvs_τ51 → pvs_τ52 → pvs_τ53 → pvs_τ54

-- pvs2lean: kinematics_turn.turnOmega_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s0: Vect3, v0: nzhVect3, t: real, omega: real):
--   turnOmega(s0, v0, t, omega) = turnOmegaAT(s0, v0, t, omega)
theorem turnOmega_equal : ∀ (s0 : ℝ × ℝ × ℝ) (v0 : {v : ℝ × ℝ × ℝ // vect2 v ≠ zero}) (t : ℝ) (omega : ℝ), turnOmega s0 v0 t omega = turnOmegaAT s0 v0 t omega := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: kinematics_turn.turn
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET dir = IF turnRight THEN 1 ELSE -1 ENDIF,
--     omega = dir * ground_speed(v0) / R
--   IN turnOmega(s0, v0, t, omega)
noncomputable def turn : sorry := by
  sorry  -- pvs2lean: definition stub for LET dir = IF turnRight THEN 1 ELSE -1 ENDIF,     omega = dir * ground_speed(v0) 

-- pvs2lean: kinematics_turn.center
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET epsi: Sign = IF turnRight THEN 1 ELSE -1 ENDIF IN
--   (# x := s0`x + epsi * R * cos(theta), y := s0`y - epsi * R * sin(theta) #)
axiom center {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} : pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14

-- pvs2lean: kinematics_turn.center_test_3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: center(zero, pi / 4, 1, TRUE) = (# x := 1 / sqrt(2), y := -1 / sqrt(2) #)
theorem center_test_3 : center zero (pi / 4) 1 True = (# x := 1 / Real.sqrt (2), y := -1 / Real.sqrt (2) #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.center_test_4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: center(zero, pi / 4, 1, FALSE) = (# x := -1 / sqrt(2), y := 1 / sqrt(2) #)
theorem center_test_4 : center zero (pi / 4) 1 False = (# x := -1 / Real.sqrt (2), y := 1 / Real.sqrt (2) #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.center_test
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: center((# x := 0, y := 0, z := 0 #), (# x := 0, y := 1, z := 0 #), pi) =
--  (# x := 1 / pi, y := 0 #)
theorem center_test : center ((# x := 0, y := 0, z := 0 #)) ((# x := 0, y := 1, z := 0 #)) pi = (# x := 1 / pi, y := 0 #) := by
  -- === TCCs for center_test ===
  -- TCC: center_test_TCC1
  -- Statement: vect2((# x := 0, y := 1, z := 0 #)) /= zero
  have center_test_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.center_test2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: center((# x := 0, y := 0, z := 0 #), (# x := 0, y := 1, z := 0 #), -pi) =
--  (# x := -1 / pi, y := 0 #)
theorem center_test2 : center ((# x := 0, y := 0, z := 0 #)) ((# x := 0, y := 1, z := 0 #)) -pi = (# x := -1 / pi, y := 0 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnOmega_thm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t: real, omega: nzreal):
--   dist(vect2(turnOmega(s0, v0, t, omega)`1), center(s0, v0, omega)) =
--    abs(ground_speed(v0) / omega)
theorem turnOmega_thm : ∀ (t : ℝ) (omega : {r : ℝ // r ≠ 0}), dist (vect2 (turnOmega s0 v0 t omega.1)) (center s0 v0 omega) = |ground_speed v0 / omega| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnOmega_vel
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t: real, omega: nzreal):
--   ground_speed(turnOmega(s0, v0, t, omega)`2) = ground_speed(v0) AND
--    (vect2(turnOmega(s0, v0, t, omega)`1) - center(s0, v0, omega)) *
--     vect2(turnOmega(s0, v0, t,
theorem turnOmega_vel : ∀ (t : ℝ) (omega : {r : ℝ // r ≠ 0}), ground_speed (turnOmega s0 v0 t omega.2) = ground_speed v0 ∧ (vect2 (turnOmega s0 v0 t omega.1) - center s0 v0 omega) *
 vect2 (turnOmega s0 v0 t omega.2)
 = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnOmega_test
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: turnOmega((# x := 0, y := 0 #), (# x := 0, y := 1 #), 1, pi)`1 =
--  (# x := 2 / pi, y := 0 #)
theorem turnOmega_test : turnOmega ((# x := 0, y := 0 #)) ((# x := 0, y := 1 #)) 1 pi.1 = (# x := 2 / pi, y := 0 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnOmega_test2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: turnOmega((# x := 0, y := 0 #), (# x := 0, y := 1 #), 1, -pi)`1 =
--  (# x := -2 / pi, y := 0 #)
theorem turnOmega_test2 : turnOmega ((# x := 0, y := 0 #)) ((# x := 0, y := 1 #)) 1 -pi.1 = (# x := -2 / pi, y := 0 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnOmega_test3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: turnOmega((# x := 0, y := 0 #), (# x := 0, y := 1 #), 1 / 2, pi)`1 =
--  (# x := 1 / pi, y := 1 / pi #)
theorem turnOmega_test3 : turnOmega ((# x := 0, y := 0 #)) ((# x := 0, y := 1 #)) (1 / 2) pi.1 = (# x := 1 / pi, y := 1 / pi #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: kinematics_turn.turnOmega_test4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: turnOmega((# x := 0, y := 0 #), (# x := 0, y := 1 #), 1 / 2, -pi)`1 =
--  (# x := -1 / pi, y := 1 / pi #)
theorem turnOmega_test4 : turnOmega ((# x := 0, y := 0 #)) ((# x := 0, y := 1 #)) (1 / 2) -pi.1 = (# x := -1 / pi, y := 1 / pi #) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.kinematics_turn