-- Auto-generated by pvs2lean/translator.py
-- PVS theory: interm_value_thm
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: interm_value_thm

Auto-generated stubs from PVSLib theory `interm_value_thm`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.interm_value_thm

noncomputable section

-- pvs2lean: interm_value_thm.interm_value1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (f: [closed_interval(a, b) -> real]):
--    continuous?(f) AND f(a) < x AND x < f(b) IMPLIES
--     EXISTS (c: closed_interval(a, b)): f(c) = x
theorem interm_value1 : a < b → ∀ (f : closed_interval a b → ℝ), continuous_p f ∧ f a < x ∧ x < f b →
   ∃ (c : closed_interval a b), f c = x := by
  -- === TCCs for interm_value1 ===
  -- TCC: interm_value1_TCC1
  -- Statement: FORALL (a, b: real):
  --   a < b IMPLIES
  --    FORALL (f: [closed_interval[real](a, b) -> real]):
  --      continuous?(f) IMPLIES a <= a AND a <= b
  have interm_value1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: interm_value1_TCC2
  -- Statement: FORALL (a, b, x: real):
  --   a < b IMPLIES
  --    FORALL (f: [closed_interval[real](a, b) -> real]):
  --      continuous?(f) AND f(a) < x IMPLIES a <= b AND b <= b
  have interm_value1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.interm_value2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (f: [closed_interval(a, b) -> real]):
--    continuous?(f) AND f(a) <= x AND x <= f(b) IMPLIES
--     EXISTS (c: closed_interval(a, b)): f(c) = x
theorem interm_value2 : a < b → ∀ (f : closed_interval a b → ℝ), continuous_p f ∧ f a ≤ x ∧ x ≤ f b →
   ∃ (c : closed_interval a b), f c = x := by
  -- === TCCs for interm_value2 ===
  -- TCC: interm_value2_TCC1
  -- Statement: FORALL (a, b, x: real):
  --   a < b IMPLIES
  --    FORALL (f: [closed_interval[real](a, b) -> real]):
  --      continuous?(f) AND f(a) <= x IMPLIES a <= b AND b <= b
  have interm_value2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.interm_value3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (f: [closed_interval(a, b) -> real]):
--    continuous?(f) AND f(b) < x AND x < f(a) IMPLIES
--     EXISTS (c: closed_interval(a, b)): f(c) = x
theorem interm_value3 : a < b → ∀ (f : closed_interval a b → ℝ), continuous_p f ∧ f b < x ∧ x < f a →
   ∃ (c : closed_interval a b), f c = x := by
  -- === TCCs for interm_value3 ===
  -- TCC: interm_value3_TCC1
  -- Statement: FORALL (a, b: real):
  --   a < b IMPLIES
  --    FORALL (f: [closed_interval[real](a, b) -> real]):
  --      continuous?(f) IMPLIES a <= b AND b <= b
  have interm_value3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.interm_value4
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (f: [closed_interval(a, b) -> real]):
--    continuous?(f) AND f(b) <= x AND x <= f(a) IMPLIES
--     EXISTS (c: closed_interval(a, b)): f(c) = x
theorem interm_value4 : a < b → ∀ (f : closed_interval a b → ℝ), continuous_p f ∧ f b ≤ x ∧ x ≤ f a →
   ∃ (c : closed_interval a b), f c = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.interm_value5
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (f: [closed_interval(a, b) -> real], p, q: closed_interval(a, b)):
--    continuous?(f) AND f(p) <= x AND x <= f(q) IMPLIES
--     EXISTS (c: closed_interval(a, b)):
--       ((p <= c AND
theorem interm_value5 : a < b → ∀ (f : closed_interval a b → ℝ) (p : _) (q : closed_interval a b), continuous_p f ∧ f p ≤ x ∧ x ≤ f q →
   ∃ (c : closed_interval a b), ((p ≤ c ∧ c ≤ q) ∨ (q ≤ c ∧ c ≤ p)) ∧ f c = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.cont_intv
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND continuous?(f) IMPLIES
--  continuous?(LAMBDA (x: closed_interval(a, b)): f(x))
theorem cont_intv : a < b ∧ continuous_p f → continuous_p (fun (x : closed_interval a b)=> f x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.cont_intv1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND
--  continuous?[closed_interval[real](a, b)]
--      (restrict[real, closed_interval[real](a, b), real](f))
--  IMPLIES continuous?(LAMBDA (x: closed_interval(a, b)): f(x))
theorem cont_intv1 : a < b ∧
 continuous_p[closed_interval[real](a, b)]
     (restrict (f)) → continuous_p (fun (x : closed_interval a b)=> f x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.interm_val1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  continuous?[closed_interval[real](a, b)]
--      (restrict[real, closed_interval[real](a, b), real](f))
--   AND f(a) < x AND x < f(b)
--   IMPLIES EXISTS (c: closed_interval(a, b)): f(c) = x
theorem interm_val1 : a < b → continuous_p[closed_interval[real](a, b)]
    (restrict (f))
 ∧ f a < x ∧ x < f b
 → ∃ (c : closed_interval a b), f c = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.interm_val2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (f: [real -> real]):
--    continuous?[closed_interval[real](a, b)]
--        (restrict[real, closed_interval[real](a, b), real](f))
--     AND f(a) <= x AND x <= f(b)
--     IMPLIES EXISTS 
theorem interm_val2 : a < b → ∀ (f : ℝ → ℝ), continuous_p[closed_interval[real](a, b)]
      (restrict (f))
   ∧ f a ≤ x ∧ x ≤ f b
   → ∃ (c : closed_interval a b), f c = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.interm_val3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (f: [real -> real]):
--    continuous?[closed_interval[real](a, b)]
--        (restrict[real, closed_interval[real](a, b), real](f))
--     AND f(b) < x AND x < f(a)
--     IMPLIES EXISTS (c
theorem interm_val3 : a < b → ∀ (f : ℝ → ℝ), continuous_p[closed_interval[real](a, b)]
      (restrict (f))
   ∧ f b < x ∧ x < f a
   → ∃ (c : closed_interval a b), f c = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.interm_val4
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (f: [real -> real]):
--    continuous?[closed_interval[real](a, b)]
--        (restrict[real, closed_interval[real](a, b), real](f))
--     AND f(b) <= x AND x <= f(a)
--     IMPLIES EXISTS 
theorem interm_val4 : a < b → ∀ (f : ℝ → ℝ), continuous_p[closed_interval[real](a, b)]
      (restrict (f))
   ∧ f b ≤ x ∧ x ≤ f a
   → ∃ (c : closed_interval a b), f c = x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.zeros_interm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: continuous?(f) AND
--  f(xl) = 0 AND f(xu) = 0 AND (FORALL (x: open_interval(xl, xu)): f(x) /= 0)
--  IMPLIES
--  (FORALL (x: open_interval(xl, xu)): f(x) > 0) OR
--   (FORALL (x: open_interval(xl, xu)): f(x) < 0
theorem zeros_interm : continuous_p f ∧
 f xl = 0 ∧ f xu = 0 ∧ (∀ (x : open_interval xl xu), f x ≠ 0) → (∀ (x : open_interval xl xu), f x > 0) ∨
 (∀ (x : open_interval xl xu), f x < 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: interm_value_thm.IntermediateValue
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real]):
--   continuous?(f) AND (FORALL (c: (ab)): f(c) /= 0) IMPLIES
--    (FORALL (x, y: (ab)): f(x) * f(y) > 0)
theorem IntermediateValue : ∀ (f : {x : {x : ℝ // _} // _} → {x : {x : ℝ // _} // _}), continuous_p f ∧ (∀ (c : (ab)), f c ≠ 0) → (∀ (x : _) (y : (ab)), f x * f y > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.interm_value_thm