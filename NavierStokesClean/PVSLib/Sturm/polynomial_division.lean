-- Auto-generated by pvs2lean/translator.py
-- PVS theory: polynomial_division
-- PVSLib library: Sturm
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: polynomial_division

Auto-generated stubs from PVSLib theory `polynomial_division`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.polynomial_division

noncomputable section

variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (max : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (polynomial : pvs_τ14 → pvs_τ15 → pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (rem : pvs_τ18 → pvs_τ19)

-- pvs2lean: polynomial_division.make_divtype
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# quot := qu, qdeg := n, rem := rm, rdeg := m #)
axiom make_divtype {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4 → pvs_τ5

-- pvs2lean: polynomial_division.poly_divide
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m > n OR i > n - m THEN make_divtype(LAMBDA (j: nat): 0, 0, g, n)
-- ELSIF m = 0 THEN make_divtype((1 / h(m)) * g, n - m, LAMBDA (j: nat): 0, 0)
-- ELSE LET ldt =
--            IF i = n - m THEN make_divtyp
axiom poly_divide {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13

-- pvs2lean: polynomial_division.poly_divide_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: h(m) /= 0 IMPLIES
--  LET pd = poly_divide(g, n)(h, m)(i) IN
--    FORALL (x: real):
--      polynomial(g, n)(x) =
--       polynomial(pd`quot, pd`qdeg)(x) * polynomial(h, m)(x) +
--        polynomial(pd`rem, pd`rde
theorem poly_divide_def : h m ≠ 0 → (let pd := poly_divide g n(h, m) (i); ∀ (x : ℝ), polynomial g n(x) =
     polynomial pd.quot pd.qdeg(x) * polynomial h m(x) +
      polynomial pd.rem pd.rdeg(x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polynomial_division.poly_divide_struct
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: h(m) /= 0 IMPLIES
--  LET pd = poly_divide(g, n)(h, m)(0) IN
--    (m = 0 AND pd`rdeg = 0 AND pd`rem = (LAMBDA (i: nat): 0)) OR
--     (m > 0 AND pd`rdeg < m)
theorem poly_divide_struct : h m ≠ 0 → (let pd := poly_divide g n(h, m) (0); (m = 0 ∧ pd.rdeg = 0 ∧ pd.rem = (fun (i : ℕ)=> 0)) ∨
   (m > 0 ∧ pd.rdeg < m)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polynomial_division.poly_divide_lengths
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: h(m) /= 0 IMPLIES
--  LET pd = poly_divide(g, n)(h, m)(i) IN
--    pd`qdeg = (IF i <= n - m THEN n - m ELSE 0 ENDIF) AND
--     pd`rdeg = (IF i > n - m THEN n ELSIF m = 0 THEN 0 ELSE m + i - 1 ENDIF)
theorem poly_divide_lengths : h m ≠ 0 → (let pd := poly_divide g n(h, m) (i); pd.qdeg = (if i ≤ n - m then n - m else 0 ) ∧
   pd.rdeg = (if i > n - m then n ELSIF m = 0 then 0 else m + i - 1 )) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.polynomial_division