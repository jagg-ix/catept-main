-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sturm
-- PVSLib library: Sturm
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sturm

Auto-generated stubs from PVSLib theory `sturm`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sturm

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (bijective_p : pvs_τ3 → Prop)
variable {pvs_τ7 : Type*} (enum : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (max_linear_div_power_p : pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (nsc : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} (number_sign_changes : pvs_τ14 → pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (poly_deriv : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (poly_divide : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (polynomial : pvs_τ25 → pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} (sign_ext : pvs_τ28 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (upto : pvs_τ33 → pvs_τ34)

-- pvs2lean: sturm.constructed_sturm_sequence?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (FORALL (i: below(m)): p(i)(n(i)) /= 0)
--  AND (FORALL (i, j: below(m)): i < j IMPLIES n(i) > n(j))
--  AND (p(1) = poly_deriv(p(0))) AND (n(1) = n(0) - 1) AND (n(m) = 0)
--  AND (p(m)(0) = 0)
--  AND (FORA
axiom constructed_sturm_sequence_p {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: sturm.constructed_sturm_seq_repeated_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: constructed_sturm_sequence?(p, n, m) IMPLIES
--  FORALL (i: nat):
--    i + 1 <= m AND
--     polynomial(p(i), n(i))(x) = 0 AND polynomial(p(i + 1), n(i + 1))(x) = 0
--     IMPLIES (FORALL (j: upto(m)): polynomia
theorem constructed_sturm_seq_repeated_root : constructed_sturm_sequence_p p n m → ∀ (i : ℕ), i + 1 ≤ m ∧
   polynomial (p i) (n i) (x) = 0 ∧ polynomial (p (i + 1)) (n (i + 1)) (x) = 0
   → (∀ (j : {k : ℕ // k ≤ m}), polynomial (p j) (n j) (x) = 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.constructed_sturm_seq_repeated_root_mth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mm: posnat):
--   constructed_sturm_sequence?(p, n, m) AND
--    max_linear_div_power?(p(0), n(0), y)(mm) AND i < m AND mm > 1
--    IMPLIES
--    FORALL (u: upto(i)):
--      EXISTS (kp: posnat):
--        kp 
theorem constructed_sturm_seq_repeated_root_mth : ∀ (mm : {i : {i : ℝ // i ≥ 0} // i > 0}), constructed_sturm_sequence_p p n m ∧
 max_linear_div_power_p (p 0) (n 0) y(mm) ∧ i < m ∧ mm > 1 → ∀ (u : {k : ℕ // k ≤ i}), ∃ (kp : {n : ℕ // 0 < n}), kp ≥ mm - 1 ∧ max_linear_div_power_p (p u) (n u) y(kp) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.constructed_sturm_seq_repeated_root_struct
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: constructed_sturm_sequence?(p, n, m) IMPLIES
--  FORALL (i: nat):
--    i + 1 <= m AND
--     polynomial(p(i), n(i))(y) = 0 AND polynomial(p(i + 1), n(i + 1))(y) = 0
--     IMPLIES
--     EXISTS (mm: posnat):
--       
theorem constructed_sturm_seq_repeated_root_struct : constructed_sturm_sequence_p p n m → ∀ (i : ℕ), i + 1 ≤ m ∧
   polynomial (p i) (n i) (y) = 0 ∧ polynomial (p (i + 1)) (n (i + 1)) (y) = 0
   →
   ∃ (mm : {n : ℕ // 0 < n}), mm > 1 ∧
      max_linear_div_power_p (p 0) (n 0) y(mm) ∧
       (∀ (j : ℕ), (1 ≤ j ∧
            j < m - 1 ∧
             (¬ max_linear_div_power_p (p j) (n j) y(mm - 1)))
           →
           (1 < j ∧
             max_linear_div_power_p (p (j - 1)) (n (j - 1)) y(mm - 1) ∧
              max_linear_div_power_p (p (j + 1)) (n (j + 1)) y(mm - 1)))
        ∧ max_linear_div_power_p (p (m - 1)) (n (m - 1)) y(mm - 1) := by
  -- === TCCs for constructed_sturm_seq_repeated_root_struct ===
  -- TCC: constructed_sturm_seq_repeated_root_struct_TCC1
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], y: real):
  --   constructed_sturm_sequence?(p, n, m) IMPLIES
  --    FORALL (i: nat):
  --      i + 1 <= m AND
  --       polynomial(p(i), n(i))(y) = 0 AND
  -- ...
  have constructed_sturm_seq_repeated_root_struct_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: constructed_sturm_seq_repeated_root_struct_TCC2
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], y: real):
  --   constructed_sturm_sequence?(p, n, m) IMPLIES
  --    FORALL (i: nat):
  --      i + 1 <= m AND
  --       polynomial(p(i), n(i))(y) = 0 AND
  -- ...
  have constructed_sturm_seq_repeated_root_struct_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: constructed_sturm_seq_repeated_root_struct_TCC3
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], y: real):
  --   constructed_sturm_sequence?(p, n, m) IMPLIES
  --    FORALL (i: nat):
  --      i + 1 <= m AND
  --       polynomial(p(i), n(i))(y) = 0 AND
  -- ...
  have constructed_sturm_seq_repeated_root_struct_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: constructed_sturm_seq_repeated_root_struct_TCC4
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], y: real):
  --   constructed_sturm_sequence?(p, n, m) IMPLIES
  --    FORALL (i: nat):
  --      i + 1 <= m AND
  --       polynomial(p(i), n(i))(y) = 0 AND
  -- ...
  have constructed_sturm_seq_repeated_root_struct_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.sturm_sig
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: number_sign_changes(LAMBDA (i): polynomial(p(i), n(i))(x), m)`num
axiom sturm_sig {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31 → pvs_τ32 → Prop

-- pvs2lean: sturm.constructed_sturm_seq_first_signs_eq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (x < b) AND (b < y) AND (polynomial(p(0), n(0))(b) = 0)
--  AND (FORALL (c: real):
--         x <= c AND c <= y AND polynomial(p(0), n(0))(c) = 0 IMPLIES
--          (polynomial(p(1), n(1))(c) /= 0 AND c 
theorem constructed_sturm_seq_first_signs_eq : (x < b) ∧ (b < y) ∧ (polynomial (p 0) (n 0) (b) = 0)
 ∧ (∀ (c : ℝ), x ≤ c ∧ c ≤ y ∧ polynomial (p 0) (n 0) (c) = 0 →
         (polynomial (p 1) (n 1) (c) ≠ 0 ∧ c = b))
 ∧ constructed_sturm_sequence_p p n m → ((x ≠ b → polynomial (p 0) (n 0) (x) ≠ 0) ∧
  (y ≠ b → polynomial (p 0) (n 0) (y) ≠ 0) ∧
   (x ≠ b →
     sign_ext (polynomial (p 0) (n 0) (x)) =
      -sign_ext (polynomial (p 1) (n 1) (b)))
    ∧
    (y ≠ b →
      sign_ext (polynomial (p 0) (n 0) (y)) =
       sign_ext (polynomial (p 1) (n 1) (b)))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.sturm_lem_no_roots
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x < y AND
--  (FORALL (c: real, i: nat):
--     i <= m AND x <= c AND c <= y IMPLIES polynomial(p(i), n(i))(c) /= 0)
--  IMPLIES sturm_sig(p, n, m)(x) = sturm_sig(p, n, m)(y)
theorem sturm_lem_no_roots : x < y ∧
 (∀ (c : ℝ) (i : ℕ), i ≤ m ∧ x ≤ c ∧ c ≤ y → polynomial (p i) (n i) (c) ≠ 0) → sturm_sig p n m(x) = sturm_sig p n m(y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.constructed_sturm_lem_one_simple_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (x < y) AND (x < b) AND (b < y)
--  AND (FORALL (c: real, i: nat):
--         i <= m - 1 AND x <= c AND c <= y AND polynomial(p(i), n(i))(c) = 0
--          IMPLIES c = b)
--  AND (polynomial(p(0), n(0))(b) 
theorem constructed_sturm_lem_one_simple_root : (x < y) ∧ (x < b) ∧ (b < y)
 ∧ (∀ (c : ℝ) (i : ℕ), i ≤ m - 1 ∧ x ≤ c ∧ c ≤ y ∧ polynomial (p i) (n i) (c) = 0
         → c = b)
 ∧ (polynomial (p 0) (n 0) (b) = 0 → polynomial (p 1) (n 1) (b) ≠ 0)
 ∧ constructed_sturm_sequence_p p n m → (let nsc := fun (xyz : ℝ) (pj : ℕ)=> number_sign_changes (fun (i : _)=> polynomial (p i) (n i) (xyz)) pj; sign_ext (nsc x (m - 1).lastnz) = sign_ext (nsc y (m - 1).lastnz) ∧
   nsc x (m - 1).num =
    nsc y (m - 1).num +
     (if polynomial (p 0) (n 0) (b) = 0 then 1 else 0 )) := by
  -- === TCCs for constructed_sturm_lem_one_simple_root ===
  -- TCC: constructed_sturm_lem_one_simple_root_TCC1
  -- Statement: FORALL (b: real, m: nat, n: [nat -> nat], p: [nat -> [nat -> real]],
  --         x, y: real):
  --        constructed_sturm_sequence?(p, n, m)
  --    AND (polynomial(p(0), n(0))(b) = 0 IMPLIES
  --          polynomi...
  have constructed_sturm_lem_one_simple_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.constructed_sturm_lem_one_multi_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (x < y) AND (x < b) AND (b < y)
--  AND (FORALL (c: real, i: nat):
--         i <= m - 1 AND x <= c AND c <= y AND polynomial(p(i), n(i))(c) = 0
--          IMPLIES c = b)
--  AND (polynomial(p(0), n(0))(b) 
theorem constructed_sturm_lem_one_multi_root : (x < y) ∧ (x < b) ∧ (b < y)
 ∧ (∀ (c : ℝ) (i : ℕ), i ≤ m - 1 ∧ x ≤ c ∧ c ≤ y ∧ polynomial (p i) (n i) (c) = 0
         → c = b)
 ∧ (polynomial (p 0) (n 0) (b) = 0) ∧ (polynomial (p 1) (n 1) (b) = 0)
 ∧ constructed_sturm_sequence_p p n m → (let nsc := fun (xyz : ℝ) (pj : ℕ)=> number_sign_changes (fun (i : _)=> polynomial (p i) (n i) (xyz)) pj; nsc x (m - 1).num = nsc y (m - 1).num + 1) := by
  -- === TCCs for constructed_sturm_lem_one_multi_root ===
  -- TCC: constructed_sturm_lem_one_multi_root_TCC1
  -- Statement: FORALL (b: real, m: nat, n: [nat -> nat], p: [nat -> [nat -> real]],
  --         x, y: real):
  --        constructed_sturm_sequence?(p, n, m)
  --    AND (polynomial(p(1), n(1))(b) = 0) AND (polynomial(p(0), n(...
  have constructed_sturm_lem_one_multi_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.constructed_sturm_lem_edge_root
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (x < y) AND (x = b OR y = b)
--  AND (FORALL (c: real, i: nat):
--         i <= m - 1 AND x <= c AND c <= y AND polynomial(p(i), n(i))(c) = 0
--          IMPLIES c = b)
--  AND (polynomial(p(0), n(0))(b) = 0
theorem constructed_sturm_lem_edge_root : (x < y) ∧ (x = b ∨ y = b)
 ∧ (∀ (c : ℝ) (i : ℕ), i ≤ m - 1 ∧ x ≤ c ∧ c ≤ y ∧ polynomial (p i) (n i) (c) = 0
         → c = b)
 ∧ (polynomial (p 0) (n 0) (b) = 0 → polynomial (p 1) (n 1) (b) ≠ 0)
 ∧ constructed_sturm_sequence_p p n m → (let nsc := fun (xyz : ℝ) (pj : ℕ)=> number_sign_changes (fun (i : _)=> polynomial (p i) (n i) (xyz)) pj; sign_ext (nsc x (m - 1).lastnz) = sign_ext (nsc y (m - 1).lastnz) ∧
   nsc x (m - 1).num =
    nsc y (m - 1).num +
     (if b = y ∧ polynomial (p 0) (n 0) (b) = 0 then 1 else 0 )) := by
  -- === TCCs for constructed_sturm_lem_edge_root ===
  -- TCC: constructed_sturm_lem_edge_root_TCC1
  -- Statement: FORALL (b: real, m: nat, n: [nat -> nat], p: [nat -> [nat -> real]],
  --         x, y: real):
  --        constructed_sturm_sequence?(p, n, m)
  --    AND (polynomial(p(0), n(0))(b) = 0 IMPLIES
  --          polynomi...
  have constructed_sturm_lem_edge_root_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.constructed_sturm_roots_between_enum
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x < y AND constructed_sturm_sequence?(p, n, m) IMPLIES
--  EXISTS ((K: nat | K >= 2), enum: [below(K) -> real]):
--    (FORALL (i, j: below(K)): i < j IMPLIES enum(i) < enum(j)) AND
--     enum(0) = x AND
--     
theorem constructed_sturm_roots_between_enum : x < y ∧ constructed_sturm_sequence_p p n m → ∃ ((K: nat | K ≥ 2), enum: [Finset.range K:
  (∀ (i : _) (j : Finset.range K), i < j → enum i < enum j) ∧
   enum 0 = x ∧
    enum (K - 1) = y ∧
     (∀ (b : ℝ) (j : ℕ), j ≤ m - 1 ∧ x < b ∧ b ≤ y ∧ polynomial (p j) (n j) (b) = 0
         → ∃ (i : Finset.range K), b = enum i) := by
  -- === TCCs for constructed_sturm_roots_between_enum ===
  -- TCC: constructed_sturm_roots_between_enum_TCC1
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], x, y: real):
  --   constructed_sturm_sequence?(p, n, m) AND x < y IMPLIES
  --    FORALL ((K: nat | K >= 2), enum: [below(K) -> real]):
  --      (FORA...
  have constructed_sturm_roots_between_enum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: constructed_sturm_roots_between_enum_TCC2
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], x, y: real):
  --   constructed_sturm_sequence?(p, n, m) AND x < y IMPLIES
  --    FORALL ((K: nat | K >= 2), enum: [below(K) -> real]):
  --      (FORA...
  have constructed_sturm_roots_between_enum_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.constructed_sturm_lem_no_roots_full
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x < y AND
--  (FORALL (c: real, i: nat):
--     i <= m - 1 AND x < c AND c <= y IMPLIES polynomial(p(i), n(i))(c) /= 0)
--   AND
--   constructed_sturm_sequence?(p, n, m) AND
--    (polynomial(p(0), n(0))(x) = 0 IMP
theorem constructed_sturm_lem_no_roots_full : x < y ∧
 (∀ (c : ℝ) (i : ℕ), i ≤ m - 1 ∧ x < c ∧ c ≤ y → polynomial (p i) (n i) (c) ≠ 0)
  ∧
  constructed_sturm_sequence_p p n m ∧
   (polynomial (p 0) (n 0) (x) = 0 → polynomial (p 1) (n 1) (x) ≠ 0) → sturm_sig p n (m - 1) (x) = sturm_sig p n (m - 1) (y) := by
  -- === TCCs for constructed_sturm_lem_no_roots_full ===
  -- TCC: constructed_sturm_lem_no_roots_full_TCC1
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], x, y: real):
  --   (polynomial(p(0), n(0))(x) = 0 IMPLIES polynomial(p(1), n(1))(x) /= 0)
  --    AND
  --    constructed_sturm_sequence?(p, n, m) AND
  -- ...
  have constructed_sturm_lem_no_roots_full_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.sturm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x < y AND
--  constructed_sturm_sequence?(p, n, m) AND
--   (polynomial(p(1), n(1))(x) /= 0 OR polynomial(p(0), n(0))(x) /= 0) AND
--    (polynomial(p(1), n(1))(y) /= 0 OR polynomial(p(0), n(0))(y) /= 0)
--  IMPL
theorem sturm : x < y ∧
 constructed_sturm_sequence_p p n m ∧
  (polynomial (p 1) (n 1) (x) ≠ 0 ∨ polynomial (p 0) (n 0) (x) ≠ 0) ∧
   (polynomial (p 1) (n 1) (y) ≠ 0 ∨ polynomial (p 0) (n 0) (y) ≠ 0) → (let nsc := fun (xyz : ℝ)=> number_sign_changes (fun (i : _)=> polynomial (p i) (n i) (xyz)) (m - 1); let Nroots := nsc x.num - nsc y.num; Nroots ≥ 0 ∧
   ∃ (bij : [Finset.range Nroots ->
                {xr: real |
                   x < xr ∧
                    xr ≤ y ∧ polynomial (p 0) (n 0) (xr) = 0}]), bijective_p bij) := by
  -- === TCCs for sturm ===
  -- TCC: sturm_TCC1
  -- Statement: FORALL ((pl: Polylist | deg(pl) > 0), (I: RealInt | I`lb < I`ub)):
  --   deg(pl) > 0 AND list2array[rat](0)(pl)(deg(pl)) /= 0
  have sturm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sturm_TCC1
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], x, y: real):
  --   (polynomial(p(1), n(1))(y) /= 0 OR polynomial(p(0), n(0))(y) /= 0) AND
  --    (polynomial(p(1), n(1))(x) /= 0 OR polynomial(p(...
  have sturm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.sturm_unbounded_left
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: constructed_sturm_sequence?(p, n, m) AND
--  (polynomial(p(1), n(1))(y) /= 0 OR polynomial(p(0), n(0))(y) /= 0)
--  IMPLIES
--  LET nscy =
--        number_sign_changes(LAMBDA (i): polynomial(p(i), n(i))(y), m - 
theorem sturm_unbounded_left : constructed_sturm_sequence_p p n m ∧
 (polynomial (p 1) (n 1) (y) ≠ 0 ∨ polynomial (p 0) (n 0) (y) ≠ 0) → (let nscy := number_sign_changes (fun (i : _)=> polynomial (p i) (n i) (y)) (m - 1); let nscninf := number_sign_changes (fun (i : _)=> (-1) ^ (n i) * p i(n i)) (m - 1); let Nroots := nscninf.num - nscy.num; Nroots ≥ 0 ∧
   ∃ (bij : Finset.range Nroots → {x : ℝ // x ≤ y ∧ polynomial (p 0) (n 0) (x) = 0}), bijective_p bij) := by
  -- === TCCs for sturm_unbounded_left ===
  -- TCC: sturm_unbounded_left_TCC1
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]], y: real):
  --   (polynomial(p(1), n(1))(y) /= 0 OR polynomial(p(0), n(0))(y) /= 0) AND
  --    constructed_sturm_sequence?(p, n, m)
  --    IMPLIES m -...
  have sturm_unbounded_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.sturm_unbounded_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: constructed_sturm_sequence?(p, n, m) AND
--  (polynomial(p(1), n(1))(x) /= 0 OR polynomial(p(0), n(0))(x) /= 0)
--  IMPLIES
--  LET nscx =
--        number_sign_changes(LAMBDA (i): polynomial(p(i), n(i))(x), m - 
theorem sturm_unbounded_right : constructed_sturm_sequence_p p n m ∧
 (polynomial (p 1) (n 1) (x) ≠ 0 ∨ polynomial (p 0) (n 0) (x) ≠ 0) → (let nscx := number_sign_changes (fun (i : _)=> polynomial (p i) (n i) (x)) (m - 1); let nscinf := number_sign_changes (fun (i : _)=> p i(n i)) (m - 1); let Nroots := nscx.num - nscinf.num; Nroots ≥ 0 ∧
   ∃ (bij : Finset.range Nroots → {x : ℝ // x > x ∧ polynomial (p 0) (n 0) (x) = 0}), bijective_p bij) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sturm.sturm_unbounded
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: constructed_sturm_sequence?(p, n, m) IMPLIES
--  LET nschigh = number_sign_changes(LAMBDA (i): p(i)(n(i)), m - 1),
--      nsclow =
--        number_sign_changes(LAMBDA (i): (-1) ^ (n(i)) * p(i)(n(i)), m - 1),
theorem sturm_unbounded : constructed_sturm_sequence_p p n m → (let nschigh := number_sign_changes (fun (i : _)=> p i(n i)) (m - 1); let nsclow := number_sign_changes (fun (i : _)=> (-1) ^ (n i) * p i(n i)) (m - 1); let Nroots := nsclow.num - nschigh.num; Nroots ≥ 0 ∧
   ∃ (bij : Finset.range Nroots → {x : ℝ // polynomial (p 0) (n 0) (x) = 0}), bijective_p bij) := by
  -- === TCCs for sturm_unbounded ===
  -- TCC: sturm_unbounded_TCC1
  -- Statement: FORALL (m: nat, n: [nat -> nat], p: [nat -> [nat -> real]]):
  --   constructed_sturm_sequence?(p, n, m) IMPLIES m - 1 >= 0
  have sturm_unbounded_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.sturm