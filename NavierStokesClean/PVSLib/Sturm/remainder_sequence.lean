-- Auto-generated by pvs2lean/translator.py
-- PVS theory: remainder_sequence
-- PVSLib library: Sturm
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: remainder_sequence

Auto-generated stubs from PVSLib theory `remainder_sequence`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.remainder_sequence

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (adjusted_remainder : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (cons : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (integer_pred : pvs_τ15 → Prop)
variable {pvs_τ21 : Type*} (length : pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} (newh : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (nth : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} (null_p : pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} (plittle : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (poly_deriv : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} (rational_pred : pvs_τ29 → Prop)

-- pvs2lean: remainder_sequence.is_neg_remainder_list?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (FORALL (icurr, jcurr: nat):
--         icurr < jcurr AND jcurr < length(curr) - 1 IMPLIES
--          length(nth(curr, icurr)) < length(nth(curr, jcurr)))
--  AND (FORALL (icurr: nat):
--         icurr < le
axiom is_neg_remainder_list_p {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: remainder_sequence.compute_remainder_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET currlength = length(curr) IN
--   IF currlength = 0
--     THEN compute_remainder_seq(g, n, h, m)
--                               (cons[list[int]]
--                                    (array2list[int](m + 1
axiom compute_remainder_seq {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11

-- pvs2lean: remainder_sequence.remainder_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: compute_remainder_seq(g, n, h, m)(null[list[int]])
axiom remainder_seq {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34

-- pvs2lean: remainder_sequence.sturm_chain
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: remainder_seq(g, pn, poly_deriv(g), pn - 1)
noncomputable def sturm_chain : sorry := by
  sorry  -- pvs2lean: definition stub for remainder_seq(g, pn, poly_deriv(g), pn - 1)

-- pvs2lean: remainder_sequence.remainder_seq_test
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET g =
--       (LAMBDA (i: nat): IF i <= 3 THEN 2 ELSIF i <= 6 THEN -3 ELSE 0 ENDIF)
--   IN
--   remainder_seq(g, 6, poly_deriv(g), 6 - 1) =
--    (: (: :), (: -1 :), (: -579, -608 :), (: 2, 4, 55 :),
--       (:
theorem remainder_seq_test : fun (g : {i : ℝ // i ≥ 0} → ℝ) => remainder_seq g 6 (poly_deriv g) (6 - 1) = (: (: :), (: -1 :), (: -579, -608 :), (: 2, 4, 55 :),
   (: 126, 140, 105, 120 :), (: -70, -56, -42, -48, 21 :),
   (: 2, 4, 6, -12, -15, -18 :), (: 2, 2, 2, 2, -3, -3, -3 :) :) (fun (i : {i : ℝ // i ≥ 0}) => IF (i ≤ 3) 2 (if i ≤ 6 then -3 else 0)) := by
  -- === TCCs for remainder_seq_test ===
  -- TCC: remainder_seq_test_TCC1
  -- Statement: FORALL (g: [nat -> int]):
  --   g = (LAMBDA (i: nat): IF i <= 3 THEN 2 ELSIF i <= 6 THEN -3 ELSE 0 ENDIF)
  --    IMPLIES g(6) /= 0
  have remainder_seq_test_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: remainder_seq_test_TCC2
  -- Statement: FORALL (g: [nat -> int]):
  --   g = (LAMBDA (i: nat): IF i <= 3 THEN 2 ELSIF i <= 6 THEN -3 ELSE 0 ENDIF)
  --    IMPLIES
  --    FORALL (x1: nat):
  --      rational_pred(poly_deriv(g)(x1)) AND integer_pred(poly_der...
  have remainder_seq_test_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.remainder_sequence