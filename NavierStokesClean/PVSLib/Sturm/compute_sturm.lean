-- Auto-generated by pvs2lean/translator.py
-- PVS theory: compute_sturm
-- PVSLib library: Sturm
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: compute_sturm

Auto-generated stubs from PVSLib theory `compute_sturm`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.compute_sturm

noncomputable section

variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Knuth_poly_root_strict_bound : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (aa : pvs_τ8 → pvs_τ9)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (aq : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (below : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} (bijective_p : pvs_τ22 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (constructed_sturm_sequence_p : pvs_τ35 → pvs_τ36 → pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (contains_p : pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ42 : Type*} (integer_pred : pvs_τ42 → Prop)
variable {pvs_τ44 : Type*} (length : pvs_τ44 → Prop)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} (max : pvs_τ46 → pvs_τ47 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} (min : pvs_τ54 → pvs_τ55 → pvs_τ56)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} (min_poly_root_dist : pvs_τ57 → pvs_τ58 → pvs_τ59)
variable {pvs_τ66 : Type*} (newa : pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (nschighlow : pvs_τ67 → pvs_τ68)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (nscnorm : pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} (nth : pvs_τ71 → pvs_τ72 → pvs_τ73)
variable {pvs_τ79 : Type*} {pvs_τ80 : Type*} {pvs_τ81 : Type*} (number_sign_changes : pvs_τ79 → pvs_τ80 → pvs_τ81)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} (poly_deriv : pvs_τ82 → pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} {pvs_τ86 : Type*} {pvs_τ87 : Type*} {pvs_τ88 : Type*} (poly_rootless_width : pvs_τ84 → pvs_τ85 → pvs_τ86 → pvs_τ87 → pvs_τ88)
variable {pvs_τ89 : Type*} {pvs_τ90 : Type*} {pvs_τ91 : Type*} (polynomial : pvs_τ89 → pvs_τ90 → pvs_τ91 → Prop)
variable {pvs_τ92 : Type*} {pvs_τ93 : Type*} {pvs_τ94 : Type*} {pvs_τ95 : Type*} (rat_poly_to_int : pvs_τ92 → pvs_τ93 → pvs_τ94 → pvs_τ95)
variable {pvs_τ96 : Type*} (rational_pred : pvs_τ96 → Prop)
variable {pvs_τ97 : Type*} {pvs_τ98 : Type*} {pvs_τ99 : Type*} (real_ord_ep : pvs_τ97 → pvs_τ98 → pvs_τ99 → Prop)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} {pvs_τ105 : Type*} {pvs_τ106 : Type*} (remainder_seq : pvs_τ103 → pvs_τ104 → pvs_τ105 → pvs_τ106 → Prop)
variable {pvs_τ114 : Type*} {pvs_τ115 : Type*} (sign : pvs_τ114 → pvs_τ115)
variable {pvs_τ116 : Type*} (strict_well_founded_p : pvs_τ116 → Prop)
variable {pvs_τ119 : Type*} {pvs_τ120 : Type*} (upto : pvs_τ119 → pvs_τ120)
variable {pvs_τ121 : Type*} (well_founded_p : pvs_τ121 → Prop)

-- pvs2lean: compute_sturm.finite_bij_real_remove_one
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, A: set[real], bij: [below(m) -> (A)]):
--   bijective?(bij) AND A(x) IMPLIES
--    m - 1 >= 0 AND
--     EXISTS (bijec: [below(m - 1) -> {r: (A) | r /= x}]): bijective?(bijec)
theorem finite_bij_real_remove_one : ∀ (m : {i : ℝ // i ≥ 0}) (A : ℝ → Bool) (bij : {s : {i : ℝ // i ≥ 0} // s < m} → ℝ), bijective_p bij ∧ A x → m - 1 ≥ 0 ∧
 ∃ (bijec : Finset.range m - 1 → {x : (A) // x ≠ x}), bijective_p bijec := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.finite_bij_real_remove_two
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, A: set[real], bij: [below(m) -> (A)]):
--   bijective?(bij) AND A(x) AND A(y) AND x /= y IMPLIES
--    m - 2 >= 0 AND
--     EXISTS (bijec: [below(m - 2) -> {r: (A) | r /= x AND r /= y}]):
--     
theorem finite_bij_real_remove_two : ∀ (m : {i : ℝ // i ≥ 0}) (A : ℝ → Bool) (bij : {s : {i : ℝ // i ≥ 0} // s < m} → ℝ), bijective_p bij ∧ A x ∧ A y ∧ x ≠ y → m - 2 ≥ 0 ∧
 ∃ (bijec : Finset.range m - 2 → {x : (A) // x ≠ x ∧ x ≠ y}), bijective_p bijec := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.computed_sturm_spec
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND (FORALL (i: nat): i > n IMPLIES a(i) = 0) IMPLIES
--  LET sl = remainder_seq(a, n, poly_deriv(a), n - 1),
--      P: [nat -> [nat -> int]] =
--        (LAMBDA (j: nat):
--           IF j < length(sl
theorem computed_sturm_spec : a n ≠ 0 ∧ (∀ (i : ℕ), i > n → a i = 0) → (let sl := remainder_seq a n (poly_deriv a) (n - 1),
    P: [nat -> [nat -> int]] =
      (fun (j : ℕ)=> if j < List.length sl
           then list2array 0(nth sl (List.length sl - 1 - j))
         else (fun (i : ℕ)=> 0)
         ),
    N: [nat -> nat] =
      (fun (j : ℕ)=> if j < List.length sl
           then max (List.length (nth sl (List.length sl - 1 - j)) - 1) 0
         else 0
         ),
    M: nat = List.length sl - 1; constructed_sturm_sequence_p P N M ∧ P 0 = a ∧ N 0 = n) := by
  -- === TCCs for computed_sturm_spec ===
  -- TCC: computed_sturm_spec_TCC1
  -- Statement: FORALL (a: [nat -> int], n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND a(n) /= 0 IMPLIES
  --    FORALL (x1: nat):
  --      rational_pred(poly_deriv(a)(x1)) AND integer_pred(poly_deriv(a)(x1))
  have computed_sturm_spec_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: computed_sturm_spec_TCC2
  -- Statement: FORALL (a: [nat -> int], n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND a(n) /= 0 IMPLIES
  --    n - 1 >= 0 AND poly_deriv(a)(n - 1) /= 0
  have computed_sturm_spec_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: computed_sturm_spec_TCC3
  -- Statement: FORALL (a: [nat -> int], n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND a(n) /= 0 IMPLIES
  --    FORALL (sl: list[list[int]]):
  --      sl = remainder_seq(a, n, poly_deriv(a), n - 1) IMPLIES
  --  ...
  have computed_sturm_spec_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: computed_sturm_spec_TCC4
  -- Statement: FORALL (a: [nat -> int], n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND a(n) /= 0 IMPLIES
  --    FORALL (sl: list[list[int]], P: [nat -> [nat -> int]]):
  --      (sl = remainder_seq(a, n, poly...
  have computed_sturm_spec_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: computed_sturm_spec_TCC5
  -- Statement: FORALL (a: [nat -> int], n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND a(n) /= 0 IMPLIES
  --    FORALL (sl: list[list[int]], P: [nat -> [nat -> int]], N: [nat -> nat]):
  --      (sl = remaind...
  have computed_sturm_spec_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.Eq_computed_remainder?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sl =
--  remainder_seq(LAMBDA (i: nat): IF i <= n THEN a(i) ELSE 0 ENDIF, n,
--                poly_deriv(LAMBDA (i: nat):
--                             IF i <= n THEN a(i) ELSE 0 ENDIF),
--                n - 1
axiom Eq_computed_remainder_p {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop

-- pvs2lean: compute_sturm.roots_closed_int
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET newa = (LAMBDA (i: nat): IF i <= n THEN a(i) ELSE 0 ENDIF),
--     P: [nat -> [nat -> int]] =
--       (LAMBDA (j: nat):
--          IF j < length(sl)
--            THEN list2array[int](0)(nth(sl, length(sl) 
axiom roots_closed_int {pvs_τ107 : Type*} {pvs_τ108 : Type*} {pvs_τ109 : Type*} {pvs_τ110 : Type*} {pvs_τ111 : Type*} {pvs_τ112 : Type*} {pvs_τ113 : Type*} : pvs_τ107 → pvs_τ108 → pvs_τ109 → pvs_τ110 → pvs_τ111 → pvs_τ112 → pvs_τ113 → Prop

-- pvs2lean: compute_sturm.roots_closed_int_def_truetrue
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (aa: [nat -> int], (nn: posnat | aa(nn) /= 0), x, y: real,
--         sl: (Eq_computed_remainder?(aa, nn))):
--   (aa(nn) /= 0 AND x < y) IMPLIES
--    LET rootnum = roots_closed_int(aa, nn, x, y, TRUE,
theorem roots_closed_int_def_truetrue : ∀ (aa : {i : ℝ // i ≥ 0} → ℝ) (nn : {nn : {i : {i : ℝ // i ≥ 0} // i > 0} // aa nn ≠ 0}) (x : ℝ) (y : ℝ) (sl : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧
                 real_pred x ∧ rational_pred x ∧ integer_pred x)
             (x))
     (x)}), (aa nn ≠ 0 ∧ x < y) → (let rootnum := roots_closed_int aa nn x y True True sl; let Aset := {z: real | x ≤ z ∧ z ≤ y ∧ polynomial aa nn(z) = 0}; rootnum ≥ 0 ∧ ∃ (bij: [Finset.range rootnum: bijective_p bij) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.roots_closed_int_def_falsetrue
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (aa: [nat -> int], (nn: posnat | aa(nn) /= 0), x, y: real,
--         sl: (Eq_computed_remainder?(aa, nn))):
--   aa(nn) /= 0 AND x < y IMPLIES
--    LET rootnum = roots_closed_int(aa, nn, x, y, FALSE, 
theorem roots_closed_int_def_falsetrue : ∀ (aa : {i : ℝ // i ≥ 0} → ℝ) (nn : {nn : {i : {i : ℝ // i ≥ 0} // i > 0} // aa nn ≠ 0}) (x : ℝ) (y : ℝ) (sl : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧
                 real_pred x ∧ rational_pred x ∧ integer_pred x)
             (x))
     (x)}), aa nn ≠ 0 ∧ x < y → (let rootnum := roots_closed_int aa nn x y False True sl; let Aset := {z: real | z ≤ y ∧ polynomial aa nn(z) = 0}; rootnum ≥ 0 ∧ ∃ (bij: [Finset.range rootnum: bijective_p bij) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.roots_closed_int_def_truefalse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (aa: [nat -> int], (nn: posnat | aa(nn) /= 0), x, y: real,
--         sl: (Eq_computed_remainder?(aa, nn))):
--   aa(nn) /= 0 AND x < y IMPLIES
--    LET rootnum = roots_closed_int(aa, nn, x, y, TRUE, F
theorem roots_closed_int_def_truefalse : ∀ (aa : {i : ℝ // i ≥ 0} → ℝ) (nn : {nn : {i : {i : ℝ // i ≥ 0} // i > 0} // aa nn ≠ 0}) (x : ℝ) (y : ℝ) (sl : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧
                 real_pred x ∧ rational_pred x ∧ integer_pred x)
             (x))
     (x)}), aa nn ≠ 0 ∧ x < y → (let rootnum := roots_closed_int aa nn x y True False sl; let Aset := {z: real | x ≤ z ∧ polynomial aa nn(z) = 0}; rootnum ≥ 0 ∧ ∃ (bij: [Finset.range rootnum: bijective_p bij) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.roots_closed_int_def_falsefalse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (aa: [nat -> int], (nn: posnat | aa(nn) /= 0), x, y: real,
--         sl: (Eq_computed_remainder?(aa, nn))):
--   aa(nn) /= 0 AND x < y IMPLIES
--    LET rootnum = roots_closed_int(aa, nn, x, y, FALSE, 
theorem roots_closed_int_def_falsefalse : ∀ (aa : {i : ℝ // i ≥ 0} → ℝ) (nn : {nn : {i : {i : ℝ // i ≥ 0} // i > 0} // aa nn ≠ 0}) (x : ℝ) (y : ℝ) (sl : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧
                 real_pred x ∧ rational_pred x ∧ integer_pred x)
             (x))
     (x)}), aa nn ≠ 0 ∧ x < y → (let rootnum := roots_closed_int aa nn x y False False sl; let Aset := {z: real | polynomial aa nn(z) = 0}; rootnum ≥ 0 ∧ ∃ (bij: [Finset.range rootnum: bijective_p bij) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.roots_closed_int_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (aa: [nat -> int], (nn: posnat | aa(nn) /= 0), x, y: real,
--         sl: (Eq_computed_remainder?(aa, nn))):
--   aa(nn) /= 0 AND x < y IMPLIES
--    LET rootnum =
--          roots_closed_int(aa, nn, x, y
theorem roots_closed_int_def : ∀ (aa : {i : ℝ // i ≥ 0} → ℝ) (nn : {nn : {i : {i : ℝ // i ≥ 0} // i > 0} // aa nn ≠ 0}) (x : ℝ) (y : ℝ) (sl : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧
                 real_pred x ∧ rational_pred x ∧ integer_pred x)
             (x))
     (x)}), aa nn ≠ 0 ∧ x < y → (let rootnum := roots_closed_int aa nn x y lower_bound upper_bound sl; let Aset := {z: real |
         (lower_bound → x ≤ z) ∧
          (upper_bound → z ≤ y) ∧ polynomial aa nn(z) = 0}; rootnum ≥ 0 ∧ ∃ (bij: [Finset.range rootnum: bijective_p bij) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.number_roots_interval
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF I`lb >= I`ub THEN 0
-- ELSE LET rootstotal =
--            roots_closed_int(a, n, I`lb, I`ub, I`bounded_below,
--                             I`bounded_above, sl),
--          adjlow =
--            IF I`bounded_
axiom number_roots_interval {pvs_τ74 : Type*} {pvs_τ75 : Type*} {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} : pvs_τ74 → pvs_τ75 → pvs_τ76 → pvs_τ77 → pvs_τ78

-- pvs2lean: compute_sturm.number_roots_interval_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (aa: [nat -> int], (nn: posnat | aa(nn) /= 0), I: RealInt,
--         sl: (Eq_computed_remainder?(aa, nn))):
--   aa(nn) /= 0 AND I`lb < I`ub IMPLIES
--    LET rootnum = number_roots_interval(aa, nn, I,
theorem number_roots_interval_def : ∀ (aa : {i : ℝ // i ≥ 0} → ℝ) (nn : {nn : {i : {i : ℝ // i ≥ 0} // i > 0} // aa nn ≠ 0}) (I : Bool × Bool × Bool × Bool × ℝ × ℝ) (sl : {x : List (List ℝ) // every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧
                 real_pred x ∧ rational_pred x ∧ integer_pred x)
             (x))
     (x)}), aa nn ≠ 0 ∧ I.lb < I.ub → (let rootnum := number_roots_interval aa nn I sl; let Aset := {z: real | contains_p I(z) ∧ polynomial aa nn(z) = 0}; rootnum ≥ 0 ∧ ∃ (bij: [Finset.range rootnum: bijective_p bij) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.meas_fun_alw_nonneg
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: y - x
axiom meas_fun_alw_nonneg {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} : pvs_τ48 → pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52 → pvs_τ53

-- pvs2lean: compute_sturm.always_nonnegative_int
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x > y THEN TRUE
-- ELSIF x = y THEN polynomial(a, n)(x) >= 0
-- ELSIF roots_closed_int(a, n, x, y, TRUE, TRUE, sl) <= 1
--   THEN polynomial(a, n)(x) >= 0 AND polynomial(a, n)(y) >= 0
-- ELSE always_nonnegativ
axiom always_nonnegative_int {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15 → pvs_τ16 → pvs_τ17 → Prop

-- pvs2lean: compute_sturm.always_nonnegative
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET newa = (LAMBDA (i: nat): IF i <= n THEN a(i) ELSE 0 ENDIF),
--     sl = remainder_seq(newa, n, poly_deriv(newa), n - 1)
--   IN
--   IF I`bounded_below AND I`bounded_above
--     THEN always_nonnegative_int(a
axiom always_nonnegative {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ10 → pvs_τ11 → pvs_τ12 → Prop

-- pvs2lean: compute_sturm.always_nonnegative_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND I`lb < I`ub IMPLIES
--  (always_nonnegative(a, n, I) IFF
--    (FORALL (x: real): contains?(I)(x) IMPLIES polynomial(a, n)(x) >= 0))
theorem always_nonnegative_def : a n ≠ 0 ∧ I.lb < I.ub → (always_nonnegative a n I ↔
  (∀ (x : ℝ), contains_p I(x) → polynomial a n(x) ≥ 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.le_?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom le__p {pvs_τ43 : Type*} : pvs_τ43 → Prop

-- pvs2lean: compute_sturm.lt_?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom lt__p {pvs_τ45 : Type*} : pvs_τ45 → Prop

-- pvs2lean: compute_sturm.ge_?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom ge__p {pvs_τ40 : Type*} : pvs_τ40 → Prop

-- pvs2lean: compute_sturm.gt_?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom gt__p {pvs_τ41 : Type*} : pvs_τ41 → Prop

-- pvs2lean: compute_sturm.ne_?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom ne__p {pvs_τ65 : Type*} : pvs_τ65 → Prop

-- pvs2lean: compute_sturm.le_
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def le_ : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: compute_sturm.lt_
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def lt_ : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: compute_sturm.ge_
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def ge_ : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: compute_sturm.gt_
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def gt_ : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: compute_sturm.ne_
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def ne_ : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: compute_sturm.SturmRel_ord
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom SturmRel_ord {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ6 → pvs_τ7

-- pvs2lean: compute_sturm.SturmRel_ord_defaxiom
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (SturmRel_ord(le_) = 0) AND (SturmRel_ord(lt_) = 1)
--  AND (SturmRel_ord(ge_) = 2) AND (SturmRel_ord(gt_) = 3)
--  AND (SturmRel_ord(ne_) = 4)
theorem SturmRel_ord_defaxiom : SturmRel_ord le_ = 0 ∧ SturmRel_ord lt_ = 1 ∧
 SturmRel_ord ge_ = 2 ∧ SturmRel_ord gt_ = 3 ∧ SturmRel_ord ne_ = 4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.ord
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: CASES x OF le_: 0, lt_: 1, ge_: 2, gt_: 3, ne_: 4 ENDCASES
noncomputable def ord : sorry := by
  sorry  -- pvs2lean: definition stub for CASES x OF le_: 0, lt_: 1, ge_: 2, gt_: 3, ne_: 4 ENDCASES

-- pvs2lean: compute_sturm.SturmRel_inclusive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (SturmRel_var: SturmRel):
--        le_?(SturmRel_var) OR lt_?(SturmRel_var) OR ge_?(SturmRel_var)
--    OR gt_?(SturmRel_var) OR ne_?(SturmRel_var)
theorem SturmRel_inclusive : ∀ (SturmRel_var : SturmRel), le__p SturmRel_var ∨ lt__p SturmRel_var ∨
 ge__p SturmRel_var ∨ gt__p SturmRel_var ∨ ne__p SturmRel_var := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.SturmRel_induction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: [SturmRel -> boolean]):
--        p(le_) AND p(lt_) AND p(ge_) AND p(gt_) AND p(ne_) IMPLIES
--    FORALL (SturmRel_var: SturmRel): p(SturmRel_var)
theorem SturmRel_induction : ∀ (p : SturmRel → Bool), p le_ ∧ p lt_ ∧ p ge_ ∧ p gt_ ∧ p ne_ → ∀ (SturmRel_var : SturmRel), p SturmRel_var := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.subterm
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x = y
noncomputable def subterm : sorry := by
  sorry  -- pvs2lean: definition stub for x = y

-- pvs2lean: compute_sturm.<<
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (x: SturmRel, y: SturmRel): FALSE
noncomputable def pvs_op_9c1628 : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA (x: SturmRel, y: SturmRel): FALSE

-- pvs2lean: compute_sturm.SturmRel_well_founded
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_well_founded?[SturmRel](<<)
theorem SturmRel_well_founded : strict_well_founded_p pvs_op_9c1628 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.rel_disjunction
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (re = lt_) OR (re = le_) OR (re = gt_) OR (re = ge_) OR (re = ne_)
theorem rel_disjunction : re = lt_ ∨ re = le_ ∨ re = gt_ ∨ re = ge_ ∨ re = ne_ := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.swap
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF re = lt_ THEN gt_
-- ELSIF re = le_ THEN ge_
-- ELSIF re = gt_ THEN lt_
-- ELSIF re = ge_ THEN le_
-- ELSE ne_
-- ENDIF
axiom swap {pvs_τ117 : Type*} {pvs_τ118 : Type*} : pvs_τ117 → pvs_τ118

-- pvs2lean: compute_sturm.realord
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF re = lt_ THEN x < y
-- ELSIF re = le_ THEN x <= y
-- ELSIF re = gt_ THEN x > y
-- ELSIF re = ge_ THEN x >= y
-- ELSE x /= y
-- ENDIF
axiom realord {pvs_τ100 : Type*} {pvs_τ101 : Type*} {pvs_τ102 : Type*} : pvs_τ100 → pvs_τ101 → pvs_τ102 → Prop

-- pvs2lean: compute_sturm.real_order_scal_pos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: posreal, x, y: real):
--   realord(re)(p * x, p * y) = realord(re)(x, y)
theorem real_order_scal_pos : ∀ (p : {x : {x : ℝ // x ≥ 0} // x > 0}) (x : ℝ) (y : ℝ), realord re(p * x, p * y) = realord re(x, y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.real_order_scal_pos_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: posreal, x: real): realord(re)(p * x, 0) = realord(re)(x, 0)
theorem real_order_scal_pos_0 : ∀ (p : {x : {x : ℝ // x ≥ 0} // x > 0}) (x : ℝ), realord re(p * x, 0) = realord re(x, 0) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: compute_sturm.compute_poly_sat
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF re = ge_ THEN always_nonnegative(a, n, I)
-- ELSIF re = le_ THEN always_nonnegative(-a, n, I)
-- ELSE LET newa = (LAMBDA (i: nat): IF i <= n THEN a(i) ELSE 0 ENDIF),
--          sl = remainder_seq(newa, n, 
axiom compute_poly_sat {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ27 → pvs_τ28 → pvs_τ29 → pvs_τ30 → Prop

-- pvs2lean: compute_sturm.compute_poly_sat_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND I`lb < I`ub IMPLIES
--  (compute_poly_sat(a, n, I, re) IFF
--    (FORALL (x: real):
--       contains?(I)(x) IMPLIES realord(re)(polynomial(a, n)(x), 0)))
theorem compute_poly_sat_def : a n ≠ 0 ∧ I.lb < I.ub → (compute_poly_sat a n I re ↔
  (∀ (x : ℝ), contains_p I(x) → realord re(polynomial a n(x), 0))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.compute_poly_sat_rational
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: compute_poly_sat(rat_poly_to_int(aq, n), n, I, re)
axiom compute_poly_sat_rational {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop

-- pvs2lean: compute_sturm.compute_poly_sat_rational_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: aq(n) /= 0 AND I`lb < I`ub IMPLIES
--  (compute_poly_sat_rational(aq, n, I, re) IFF
--    (FORALL (x: real):
--       contains?(I)(x) IMPLIES realord(re)(polynomial(aq, n)(x), 0)))
theorem compute_poly_sat_rational_def : aq n ≠ 0 ∧ I.lb < I.ub → (compute_poly_sat_rational aq n I re ↔
  (∀ (x : ℝ), contains_p I(x) → realord re(polynomial aq n(x), 0))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.compute_poly_mono_basic
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 1 THEN realord(re)(sign(aq(1)) * I`lb, sign(aq(1)) * I`ub)
-- ELSIF re = ne_
--   THEN compute_poly_sat_rational(poly_deriv(aq), n - 1, I, ge_) OR
--         compute_poly_sat_rational(poly_deriv(aq), n 
axiom compute_poly_mono_basic {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: compute_sturm.compute_poly_mono_basic_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: aq(n) /= 0 AND I`lb < I`ub IMPLIES
--  ((FORALL (x, y: real):
--      x < y AND contains?(I)(x) AND contains?(I)(y) IMPLIES
--       realord(re)(polynomial(aq, n)(x), polynomial(aq, n)(y)))
--    IFF compute_poly
theorem compute_poly_mono_basic_def : aq n ≠ 0 ∧ I.lb < I.ub → ((∀ (x : _) (y : ℝ), x < y ∧ contains_p I(x) ∧ contains_p I(y) →
     realord re(polynomial aq n(x), polynomial aq n(y)))
  ↔ compute_poly_mono_basic aq n I re) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.mono
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF realord(ro)(1, 1) AND NOT realord(re)(1, 1) THEN FALSE
-- ELSIF ro = lt_ OR ro = le_ THEN compute_poly_mono_basic(aq, n, I, re)
-- ELSIF ro = gt_ OR ro = ge_ THEN compute_poly_mono_basic(aq, n, I, swap(r
axiom mono {pvs_τ60 : Type*} {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} : pvs_τ60 → pvs_τ61 → pvs_τ62 → pvs_τ63 → pvs_τ64 → Prop

-- pvs2lean: compute_sturm.poly_non_constant_real_int
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: aq(n) /= 0 AND I`lb < I`ub IMPLIES
--  EXISTS (x, y: real):
--    x < y AND
--     contains?(I)(x) AND
--      contains?(I)(y) AND polynomial(aq, n)(x) /= polynomial(aq, n)(y)
theorem poly_non_constant_real_int : aq n ≠ 0 ∧ I.lb < I.ub → ∃ (x : _) (y : ℝ), x < y ∧
   contains_p I(x) ∧
    contains_p I(y) ∧ polynomial aq n(x) ≠ polynomial aq n(y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm.mono_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: aq(n) /= 0 AND I`lb < I`ub IMPLIES
--  ((FORALL (x, y: real):
--      realord(ro)(x, y) AND contains?(I)(x) AND contains?(I)(y) IMPLIES
--       realord(re)(polynomial(aq, n)(x), polynomial(aq, n)(y)))
--    IFF 
theorem mono_def : aq n ≠ 0 ∧ I.lb < I.ub → ((∀ (x : _) (y : ℝ), realord ro(x, y) ∧ contains_p I(x) ∧ contains_p I(y) →
     realord re(polynomial aq n(x), polynomial aq n(y)))
  ↔ mono aq n I ro re) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.compute_sturm