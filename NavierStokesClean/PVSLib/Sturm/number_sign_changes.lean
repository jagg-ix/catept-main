-- Auto-generated by pvs2lean/translator.py
-- PVS theory: number_sign_changes
-- PVSLib library: Sturm
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: number_sign_changes

Auto-generated stubs from PVSLib theory `number_sign_changes`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.number_sign_changes

noncomputable section

variable {pvs_τ3 : Type*} (sigfun : pvs_τ3 → Prop)
variable {pvs_τ10 : Type*} (sign_ext : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} (subrange : pvs_τ11 → pvs_τ12 → pvs_τ13)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (upto : pvs_τ16 → pvs_τ17)

-- pvs2lean: number_sign_changes.number_sign_changes
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN (# num := 0, lastnz := a(0) #)
-- ELSE LET prev = number_sign_changes(a, i - 1) IN
--        IF a(i) /= 0 AND
--            prev`lastnz /= 0 AND sign_ext(a(i)) /= sign_ext(prev`lastnz)
--          T
axiom number_sign_changes {pvs_τ1 : Type*} {pvs_τ2 : Type*} : pvs_τ1 → pvs_τ2 → Prop

-- pvs2lean: number_sign_changes.sign_changes
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = n + 1 THEN num
-- ELSIF a(i) /= 0 AND lastnz /= 0 AND sign_ext(a(i)) /= sign_ext(lastnz)
--   THEN sign_changes(a, n, i + 1, num + 1, a(i))
-- ELSIF a(i) /= 0 THEN sign_changes(a, n, i + 1, num, a(i))
-- E
axiom sign_changes {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9

-- pvs2lean: number_sign_changes.num_sign_changes_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: number_sign_changes(a, i)`num = sign_changes(a, i, 1, 0, a(0))
theorem num_sign_changes_def : number_sign_changes a i.num = sign_changes a i 1 0 (a 0) := by
  -- === TCCs for num_sign_changes_def ===
  -- TCC: num_sign_changes_def_TCC1
  -- Statement: FORALL (i: nat): 1 <= 1 AND 1 <= 1 + i
  have num_sign_changes_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_sign_changes.number_sign_changes_lastnz_nonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: number_sign_changes(a, i)`lastnz = 0 IFF
--  (FORALL (j: nat): j <= i IMPLIES a(j) = 0)
theorem number_sign_changes_lastnz_nonzero : number_sign_changes a i.lastnz = 0 ↔ (∀ (j : ℕ), j ≤ i → a j = 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_sign_changes.number_sign_changes_lastnz
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(i) /= 0 IMPLIES number_sign_changes(a, i)`lastnz = a(i)
theorem number_sign_changes_lastnz : a i ≠ 0 → number_sign_changes a i.lastnz = a i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_sign_changes.number_sign_changes_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (j: upto(i)): sign_ext(a(j)) = sign_ext(b(j))) IMPLIES
--  LET nsca = number_sign_changes(a, i), nscb = number_sign_changes(b, i) IN
--    (nsca`num = nscb`num AND sign_ext(nsca`lastnz) = sign_ext(n
theorem number_sign_changes_eq : (∀ (j : {k : ℕ // k ≤ i}), sign_ext (a j) = sign_ext (b j)) → (let nsca := number_sign_changes a i; let nscb := number_sign_changes b i; (nsca.num = nscb.num ∧ sign_ext nsca.lastnz = sign_ext nscb.lastnz)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_sign_changes.testseqaa1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (-1) ^ i
axiom testseqaa1 {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ14 → pvs_τ15

-- pvs2lean: number_sign_changes.testseqaa2
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: testseqaa1 WITH [(6) := -1]
noncomputable def testseqaa2 : sorry := by
  sorry  -- pvs2lean: definition stub for testseqaa1 WITH [(6) := -1]

-- pvs2lean: number_sign_changes.testseqaa3
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: testseqaa2 WITH [(7) := 0]
noncomputable def testseqaa3 : sorry := by
  sorry  -- pvs2lean: definition stub for testseqaa2 WITH [(7) := 0]

-- pvs2lean: number_sign_changes.testseqaa4
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: testseqaa1 WITH [(4) := 0]
noncomputable def testseqaa4 : sorry := by
  sorry  -- pvs2lean: definition stub for testseqaa1 WITH [(4) := 0]

-- pvs2lean: number_sign_changes.testseqaa5
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 5 THEN 3
-- ELSIF i = 6 THEN 4
-- ELSIF i = 7 THEN 0
-- ELSIF i = 8 THEN -4
-- ELSE (-1) ^ i
-- ENDIF
noncomputable def testseqaa5 : sorry := by
  sorry  -- pvs2lean: definition stub for IF i = 5 THEN 3 ELSIF i = 6 THEN 4 ELSIF i = 7 THEN 0 ELSIF i = 8 THEN -4 ELSE (

-- pvs2lean: number_sign_changes.testseqaa6
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 OR i = 1 THEN 0 ELSE testseqaa1(i) ENDIF
noncomputable def testseqaa6 : sorry := by
  sorry  -- pvs2lean: definition stub for IF i = 0 OR i = 1 THEN 0 ELSE testseqaa1(i) ENDIF

-- pvs2lean: number_sign_changes.number_sign_changes_easy
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem number_sign_changes_easy : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: number_sign_changes.number_sign_changes_test1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem number_sign_changes_test1 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: number_sign_changes.number_sign_changes_test2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem number_sign_changes_test2 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: number_sign_changes.number_sign_changes_test3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem number_sign_changes_test3 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: number_sign_changes.number_sign_changes_test4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem number_sign_changes_test4 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: number_sign_changes.number_sign_changes_test5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem number_sign_changes_test5 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: number_sign_changes.number_sign_changes_test6
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem number_sign_changes_test6 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: number_sign_changes.number_sign_changes_test7
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem number_sign_changes_test7 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: number_sign_changes.nsc_regular_swap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: k <= d AND
--  (FORALL (j):
--     j <= k IMPLIES
--      (sign_ext(a(j)) = sign_ext(b(j)) OR
--        (sign_ext(a(j)) /= sign_ext(b(j)) AND
--          a(j + 1) /= 0 AND
--           (j > 0 IMPLIES sign_ext(a(j - 1))
theorem nsc_regular_swap : k ≤ d ∧
 (∀ (j : _), j ≤ k →
     (sign_ext (a j) = sign_ext (b j) ∨
       (sign_ext (a j) ≠ sign_ext (b j) ∧
         a (j + 1) ≠ 0 ∧
          (j > 0 → sign_ext (a (j - 1)) = -sign_ext (a (j + 1))) ∧
           (j > 0 → sign_ext (b (j - 1)) = -sign_ext (b (j + 1))) ∧
            (j = 0 → sign_ext (a 0) = -sign_ext (a 1)))))
  ∧
  sign_ext (a k) = sign_ext (b k) ∧
   (∀ (j : ℕ), j < k → a j ≠ 0 ∧ b j ≠ 0) → (let nsca := number_sign_changes a k; let nscb := number_sign_changes b k; ((k ≥ 1 → sign_ext nsca.lastnz = sign_ext nscb.lastnz) ∧
    nsca.num =
     nscb.num +
      (if k ≥ 1 ∧ sign_ext (a 0) = -sign_ext (b 0) then 1 else 0 ))) := by
  -- === TCCs for nsc_regular_swap ===
  -- TCC: nsc_regular_swap_TCC1
  -- Statement: FORALL (a, b: [nat -> real], d, k: nat):
  --   k <= d IMPLIES
  --    FORALL (j: nat):
  --           (j <= k) AND (NOT sign_ext(a(j)) = sign_ext(b(j)))
  --       AND (sign_ext(a(j)) /= sign_ext(b(j))) AND (a(j + 1)...
  have nsc_regular_swap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_sign_changes.nsc_edge_diff
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (sig = -1 OR sig = 1) AND (k <= d)
--  AND (FORALL (j):
--         j <= k IMPLIES (sign_ext(a(j)) = sign_ext(b(j)) OR b(j) = 0))
--  AND (FORALL (j): 0 < j AND j <= k AND b(j) = 0 IMPLIES b(j - 1) /= 0)
--  
theorem nsc_edge_diff : (sig = -1 ∨ sig = 1) ∧ (k ≤ d)
 ∧ (∀ (j : _), j ≤ k → (sign_ext (a j) = sign_ext (b j) ∨ b j = 0))
 ∧ (∀ (j : _), 0 < j ∧ j ≤ k ∧ b j = 0 → b (j - 1) ≠ 0)
 ∧ (sign_ext (a k) = sign_ext (b k))
 ∧ (∀ (j : ℕ), j < k → a j ≠ 0)
 ∧ (b 0 = 0 → sign_ext (a 0) = sig * sign_ext (a 1))
 ∧ (∀ (j : ℕ), 0 < j ∧ j < k ∧ b j = 0 →
         sign_ext (b (j - 1)) = -sign_ext (b (j + 1))) → (let nsca := number_sign_changes a k; let nscb := number_sign_changes b k; ((k ≥ 1 → sign_ext nsca.lastnz = sign_ext nscb.lastnz) ∧
    nsca.num =
     nscb.num + (if k ≥ 1 ∧ b 0 = 0 ∧ sig = -1 then 1 else 0 ))) := by
  -- === TCCs for nsc_edge_diff ===
  -- TCC: nsc_edge_diff_TCC1
  -- Statement: FORALL (a, b: [nat -> real], d, k: nat, sig: real):
  --   (FORALL (j: nat):
  --      j <= k IMPLIES (sign_ext(a(j)) = sign_ext(b(j)) OR b(j) = 0))
  --    AND k <= d AND (sig = -1 OR sig = 1)
  --    IMPLIES FORALL ...
  have nsc_edge_diff_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nsc_edge_diff_TCC2
  -- Statement: FORALL (a, b: [nat -> real], d, k: nat, sig: real):
  --        (b(0) = 0 IMPLIES sign_ext(a(0)) = sig * sign_ext(a(1)))
  --    AND (FORALL (j: nat): j < k IMPLIES a(j) /= 0)
  --    AND (sign_ext(a(k)) = sign_e...
  have nsc_edge_diff_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_sign_changes.nsc_multiroot
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (sig = -1 OR sig = 1) AND (k <= d)
--  AND (FORALL (j):
--         0 < j AND j <= k IMPLIES sign_ext(a(j)) = sig * sign_ext(b(j)))
--  AND (sign_ext(a(0)) = -sig * sign_ext(b(0)))
--  AND (sign_ext(a(0)) = -
theorem nsc_multiroot : (sig = -1 ∨ sig = 1) ∧ (k ≤ d)
 ∧ (∀ (j : _), 0 < j ∧ j ≤ k → sign_ext (a j) = sig * sign_ext (b j))
 ∧ (sign_ext (a 0) = -sig * sign_ext (b 0))
 ∧ (sign_ext (a 0) = -sign_ext (a 1))
 ∧ (sign_ext (b 0) = sign_ext (b 1))
 ∧ (∀ (j : _), j < k → (a j ≠ 0 ∧ b j ≠ 0)) → (let nsca := number_sign_changes a k.num; let nscb := number_sign_changes b k.num; nsca = nscb + (if k ≥ 1 then 1 else 0 )) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_sign_changes.nsc_multiroot_full
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (sigfun: [nat -> real]):
--        (sig = -1 OR sig = 1)
--    AND (FORALL (j): sigfun(j) = -1 OR sigfun(j) = 1) AND (k <= d)
--    AND (FORALL (j):
--           0 < j AND j <= k IMPLIES
--            sign_ex
theorem nsc_multiroot_full : ∀ (sigfun : {i : ℝ // i ≥ 0} → ℝ), (sig = -1 ∨ sig = 1) ∧ (∀ (j : _), sigfun j = -1 ∨ sigfun j = 1)
 ∧ (k ≤ d)
 ∧ (∀ (j : _), 0 < j ∧ j ≤ k →
         sign_ext (a j) = sigfun j * sign_ext (b j))
 ∧ (∀ (j : _), 0 < j ∧ j ≤ k ∧ sigfun j ≠ sig →
         (1 < j ∧
                (j < k) ∧ (sigfun (j - 1) = sig) ∧ (sigfun (j + 1) = sig)
            ∧ (sign_ext (a (j - 1)) = -sign_ext (a (j + 1)))
            ∧ (sign_ext (b (j - 1)) = -sign_ext (b (j + 1)))))
 ∧ (sign_ext (a 0) = -sig * sign_ext (b 0))
 ∧ (sign_ext (a 0) = -sign_ext (a 1))
 ∧ (sign_ext (b 0) = sign_ext (b 1))
 ∧ (∀ (j : _), j ≤ k → (a j ≠ 0 ∧ b j ≠ 0)) → (let nsca := number_sign_changes a k.num; let nscb := number_sign_changes b k.num; nsca = nscb + (if k ≥ 1 then 1 else 0 )) := by
  -- === TCCs for nsc_multiroot_full ===
  -- TCC: nsc_multiroot_full_TCC1
  -- Statement: FORALL (a, b: [nat -> real], d, k: nat, sig: real, sigfun: [nat -> real]):
  --   ((sig = -1 OR sig = 1) AND
  --     (FORALL (j): sigfun(j) = -1 OR sigfun(j) = 1) AND
  --      k <= d AND
  --       FORALL (j: nat):
  -- ...
  have nsc_multiroot_full_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.number_sign_changes