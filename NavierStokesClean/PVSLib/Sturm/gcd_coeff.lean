-- Auto-generated by pvs2lean/translator.py
-- PVS theory: gcd_coeff
-- PVSLib library: Sturm
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: gcd_coeff

Auto-generated stubs from PVSLib theory `gcd_coeff`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.gcd_coeff

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (car : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (compute_gcd : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (divides : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ13 : Type*} (integer_pred : pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (length : pvs_τ14 → pvs_τ15)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (nth : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (null_p : pvs_τ20 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (reverse : pvs_τ22 → pvs_τ23)

-- pvs2lean: gcd_coeff.gcd_coeff
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length[int](ll) = 0 THEN 0
-- ELSIF length[int](ll) = 1 THEN abs(nth[int](ll, 0))
-- ELSE LET oldgcd = gcd_coeff(cdr[int](ll)) IN
--        IF car[int](ll) = 0 THEN oldgcd
--        ELSIF oldgcd = 1 THEN 1
--    
axiom gcd_coeff {pvs_τ12 : Type*} : pvs_τ12 → Prop

-- pvs2lean: gcd_coeff.gcd_coeff_nonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (i: below(length[int](l))): nth[int](l, i) /= 0) IMPLIES
--  gcd_coeff(l) /= 0
theorem gcd_coeff_nonzero : (∃ (i : Finset.range List.length l), nth l i ≠ 0) → gcd_coeff l ≠ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd_coeff.gcd_coeff_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: nat): i < length[int](l) IMPLIES nth[int](l, i) = 0) IMPLIES
--  gcd_coeff(l) = 0
theorem gcd_coeff_zero : (∀ (i : ℕ), i < List.length l → nth l i = 0) → gcd_coeff l = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd_coeff.descalarize_list
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(ll) THEN ll
-- ELSE cons[int](car[int](ll) / k, descalarize_list(cdr[int](ll), k))
-- ENDIF
axiom descalarize_list {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9

-- pvs2lean: gcd_coeff.primitize_list
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET thisgcd = gcd_coeff(ll) IN
--   IF thisgcd = 0 OR thisgcd = 1 THEN ll
--   ELSE descalarize_list(ll, thisgcd)
--   ENDIF
axiom primitize_list {pvs_τ21 : Type*} : pvs_τ21 → Prop

-- pvs2lean: gcd_coeff.primitize_list_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (i: below(length[int](l))): nth[int](l, i) /= 0) IMPLIES
--  length[int](l) = length[int](primitize_list(l)) AND
--   LET thisgcd = gcd_coeff(l) IN
--     thisgcd > 0 AND
--      FORALL (k: below(length[i
theorem primitize_list_def : (∃ (i : Finset.range List.length l), nth l i ≠ 0) → List.length l = List.length (primitize_list l) ∧
 (let thisgcd := gcd_coeff l; thisgcd > 0 ∧
    ∀ (k : Finset.range List.length l), nth l k = thisgcd * nth (primitize_list l) k) := by
  -- === TCCs for primitize_list_def ===
  -- TCC: primitize_list_def_TCC1
  -- Statement: FORALL (l: list[int]):
  --   (EXISTS (i: below(length[int](l))): nth[int](l, i) /= 0) IMPLIES
  --    FORALL (thisgcd:
  --              {m: nat |
  --                 FORALL (ii: nat):
  --                   ii < length[...
  have primitize_list_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd_coeff.primitize_zero_list
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: nat): i < length[int](l) IMPLIES nth[int](l, i) = 0) IMPLIES
--  primitize_list(l) = array2list[int](length[int](l))(LAMBDA (i: nat): 0)
theorem primitize_zero_list : (∀ (i : ℕ), i < List.length l → nth l i = 0) → primitize_list l = array2list List.length[int](l) (fun (i : ℕ)=> 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd_coeff.primitize_list_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length[int](primitize_list(l)) = length[int](l)
theorem primitize_list_length : List.length (primitize_list l) = List.length l := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd_coeff.nonzero_version_rec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length[int](ll) = 0 OR car[int](ll) /= 0 THEN ll
-- ELSE nonzero_version_rec(cdr[int](ll))
-- ENDIF
axiom nonzero_version_rec {pvs_τ17 : Type*} : pvs_τ17 → Prop

-- pvs2lean: gcd_coeff.nonzero_version_rec_length_nonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: list[int]):
--   length[int](nonzero_version_rec(ll)) > 0 IFF
--    (length[int](ll) > 0 AND
--      EXISTS (i: below(length[int](ll))): nth(ll, i) /= 0)
theorem nonzero_version_rec_length_nonzero : ∀ (ll : List ℝ), List.length (nonzero_version_rec ll) > 0 ↔ (List.length ll > 0 ∧
  ∃ (i : Finset.range List.length ll), nth ll i ≠ 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd_coeff.nonzero_version_rec_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: list[int]):
--   LET L = nonzero_version_rec(ll) IN
--     length[int](L) > 0 IMPLIES
--      (car(L) /= 0 AND
--        FORALL (i: nat):
--          i < length[int](ll) - length[int](L) IMPLIES nth[int]
theorem nonzero_version_rec_def : ∀ (ll : List ℝ), fun (L : {nl : List ℝ // List.length nl ≤ List.length ll ∧
 (∀ (i : ℕ), i < List.length ll →
     nth ll i =
      (if i < List.length ll - List.length nl then 0
       else nth nl (i + List.length[int](nl) - List.length[int](ll))
       ))
  ∧
  ((List.length nl > 0 ∨
     ∃ (i : Finset.range List.length ll), nth ll i ≠ 0)
    → (List.length nl > 0 ∧ car nl ≠ 0))}) => List.length L > 0 → (car L ≠ 0 ∧
  ∀ (i : ℕ), i < List.length ll - List.length L → nth ll i = 0) (nonzero_version_rec ll) := by
  -- === TCCs for nonzero_version_rec_def ===
  -- TCC: nonzero_version_rec_def_TCC1
  -- Statement: FORALL (ll: list[int],
  --         L:
  --           {nl: list[int] |
  --              length[int](nl) <= length[int](ll) AND
  --               (FORALL (i: nat):
  --                  i < length[int](ll) IMPLIES
  --        ...
  have nonzero_version_rec_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nonzero_version_rec_def_TCC2
  -- Statement: FORALL (ll: list[int],
  --         L:
  --           {nl: list[int] |
  --              length[int](nl) <= length[int](ll) AND
  --               (FORALL (i: nat):
  --                  i < length[int](ll) IMPLIES
  --        ...
  have nonzero_version_rec_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd_coeff.nonzero_version
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: reverse[int](nonzero_version_rec(reverse(ll)))
axiom nonzero_version {pvs_τ16 : Type*} : pvs_τ16 → Prop

-- pvs2lean: gcd_coeff.nonzero_version_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: list[int]):
--   LET L = nonzero_version(ll) IN
--          (length[int](L) <= length[int](ll))
--      AND (FORALL (i: nat):
--             i < length[int](ll) IMPLIES
--              nth[int](ll, i) =
-- 
theorem nonzero_version_def : ∀ (ll : List ℝ), fun (L : List ℝ) => List.length L ≤ List.length ll ∧ (∀ (i : ℕ), i < List.length ll →
    nth ll i =
     (if i < List.length L then nth L i else 0 ))
 ∧
 ((∃ (i : Finset.range List.length ll), nth ll i ≠ 0) →
   (List.length L > 0 ∧ nth L (List.length L - 1) ≠ 0))
  ∧
  (List.length L > 0 ↔
    (List.length ll > 0 ∧
      ∃ (i : Finset.range List.length ll), nth ll i ≠ 0))
   ∧
   (List.length L > 0 →
     (nth L (List.length[int](L) - 1) ≠ 0 ∧
       ∀ (i : ℕ), i > List.length L - 1 ∧ i < List.length ll →
          nth ll i = 0)) (nonzero_version ll) := by
  -- === TCCs for nonzero_version_def ===
  -- TCC: nonzero_version_def_TCC1
  -- Statement: FORALL (ll: list[int], L: list[int]):
  --        (L = nonzero_version(ll)) AND (length[int](L) <= length[int](ll))
  --    AND (FORALL (i: nat):
  --           i < length[int](ll) IMPLIES
  --            nth[int](ll,...
  have nonzero_version_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nonzero_version_def_TCC2
  -- Statement: FORALL (ll: list[int], L: list[int]):
  --        (L = nonzero_version(ll)) AND (length[int](L) <= length[int](ll))
  --    AND (FORALL (i: nat):
  --           i < length[int](ll) IMPLIES
  --            nth[int](ll,...
  have nonzero_version_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd_coeff.nonzero_version_length_nz
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: list[int]):
--   length(ll) > 0 AND nth(ll, length(ll) - 1) /= 0 IMPLIES
--    nonzero_version(ll) = ll
theorem nonzero_version_length_nz : ∀ (ll : List ℝ), List.length ll > 0 ∧ nth ll (List.length ll - 1) ≠ 0 → nonzero_version ll = ll := by
  -- === TCCs for nonzero_version_length_nz ===
  -- TCC: nonzero_version_length_nz_TCC1
  -- Statement: FORALL (ll: list[int]):
  --   length(ll) > 0 IMPLIES
  --    length[int](ll) - 1 >= 0 AND length[int](ll) - 1 < length[int](ll)
  have nonzero_version_length_nz_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.gcd_coeff