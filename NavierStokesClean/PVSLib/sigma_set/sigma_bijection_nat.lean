-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sigma_bijection_nat
-- PVSLib library: sigma_set
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sigma_bijection_nat

Auto-generated stubs from PVSLib theory `sigma_bijection_nat`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sigma_bijection_nat

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (series : pvs_τ1 → pvs_τ2 → pvs_τ3)

-- pvs2lean: sigma_bijection_nat.series_bijection
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (phi: (bijective?[below[n + 1], below[n + 1]])):
--   series(a)(n) =
--    series(a o extend[nat, below[n + 1], below[n + 1], 0](phi))(n)
theorem series_bijection : ∀ (phi : {s : {i : ℝ // i ≥ 0} // s < 1 + n} → {s : {i : ℝ // i ≥ 0} // s < 1 + n}), series a(n) = series (a ∘ extend phi) (n) := by
  -- === TCCs for series_bijection ===
  -- TCC: series_bijection_TCC1
  -- Statement: FORALL (n: nat, phi: (bijective?[below[1 + n], below[1 + n]])): 0 < 1 + n
  have series_bijection_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.sigma_bijection_nat