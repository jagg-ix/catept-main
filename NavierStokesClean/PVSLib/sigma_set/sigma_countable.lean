-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sigma_countable
-- PVSLib library: sigma_set
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sigma_countable

Auto-generated stubs from PVSLib theory `sigma_countable`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sigma_countable

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (pvs_op_336d5e : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (add : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (card : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (choose : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (convergent_p : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (denumerable_enumeration : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (difference : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (disjoint_p : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (empty_p : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (extend : pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (finite_enumeration : pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (inf : pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (intersection : pvs_τ25 → pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} (is_countable : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} (is_finite : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (limit : pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (member : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} (nonempty_p : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} (remove : pvs_τ36 → pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (rest : pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (series : pvs_τ41 → pvs_τ42)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (singleton : pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (subset_p : pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (sup : pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} (union : pvs_τ49 → pvs_τ50 → pvs_τ51)

-- pvs2lean: sigma_countable.sigma
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF empty?(X) THEN 0
-- ELSIF is_finite(X)
--   THEN sigma[below[card(X)]](0, card(X) - 1, f o finite_enumeration(X))
-- ELSE limit(series(f o denumerable_enumeration(X)))
-- ENDIF
noncomputable def sigma : sorry := by
  sorry  -- pvs2lean: definition stub for IF empty?(X) THEN 0 ELSIF is_finite(X)   THEN sigma[below[card(X)]](0, card(X) -

-- pvs2lean: sigma_countable.sigma_empty
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(emptyset[T], g) = 0
theorem sigma_empty : sigma ((∅ : Set T)) g = 0 := by
  -- === TCCs for sigma_empty ===
  -- TCC: sigma_empty_TCC1
  -- Statement: FORALL (g: [T -> real]): convergent?[T](emptyset[T])(g)
  have sigma_empty_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_empty_TCC1
  -- Statement: FORALL (g: [T -> real]): convergent?[T](emptyset[T])(g)
  have sigma_empty_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET n = card(F) - 1 IN
--   sigma(F, g) = sigma[below[n + 1]](0, n, g o finite_enumeration(F))
theorem sigma_finite : fun (n : ℝ) => sigma F g = Finset.sum (Finset.Icc 0 n) (g ∘ finite_enumeration F) (card F - 1) := by
  -- === TCCs for sigma_finite ===
  -- TCC: sigma_finite_TCC1
  -- Statement: FORALL (F: non_empty_finite_set[T], g: [T -> real], n: int):
  --   n = card(F) - 1 IMPLIES convergent?[T](F)(g)
  have sigma_finite_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_finite_TCC2
  -- Statement: FORALL (F: non_empty_finite_set[T], n: int):
  --   n = card(F) - 1 IMPLIES
  --    0 >= 0 AND 0 < 1 + n OR EXISTS (j: below[1 + n]): j <= 0
  have sigma_finite_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_finite_TCC3
  -- Statement: FORALL (F: non_empty_finite_set[T], n: int):
  --   n = card(F) - 1 IMPLIES
  --    n >= 0 AND n < 1 + n OR EXISTS (j: below[1 + n]): n <= j
  have sigma_finite_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_finite_TCC4
  -- Statement: FORALL (F: non_empty_finite_set[T], n: int):
  --   n = card(F) - 1 IMPLIES FORALL (x: nat): x < card[T](F) IFF x < 1 + n
  have sigma_finite_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_finite_TCC5
  -- Statement: FORALL (F: non_empty_finite_set[T], n: int):
  --   n = card(F) - 1 IMPLIES
  --    FORALL (x, y: below[1 + n]), (z: integer):
  --      x <= z AND z <= y IMPLIES z >= 0 AND z < 1 + n
  have sigma_finite_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_finite_TCC1
  -- Statement: FORALL (F: non_empty_finite_set[T], g: [T -> real]): convergent?[T](F)(g)
  have sigma_finite_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_finite_TCC2
  -- Statement: FORALL (F: non_empty_finite_set[T], g: [T -> real]): convergent?[T](F)(g)
  have sigma_finite_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_singleton
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(singleton(t), g) = g(t)
theorem sigma_singleton : sigma (singleton t) g = g t := by
  -- === TCCs for sigma_singleton ===
  -- TCC: sigma_singleton_TCC1
  -- Statement: FORALL (g: [T -> real], t: T): convergent?[T](singleton[T](t))(g)
  have sigma_singleton_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_singleton_TCC1
  -- Statement: FORALL (g: [T -> real], t: T): convergent?[T](singleton[T](t))(g)
  have sigma_singleton_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_infinite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: countably_infinite_set, f: (convergent?(X))):
--   sigma(X, f) = limit(series(f o denumerable_enumeration(X)))
theorem sigma_infinite : ∀ (X : T → Bool) (f : T → ℝ), sigma X f = limit (series (f ∘ denumerable_enumeration X)) := by
  -- === TCCs for sigma_infinite ===
  -- TCC: sigma_infinite_TCC1
  -- Statement: FORALL (X: countably_infinite_set[T], f: (convergent?[T](X))):
  --   convergent?(series(o[nat, T, real](f, denumerable_enumeration[T](X))))
  have sigma_infinite_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.nonempty_countable?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?(X) AND is_countable(X)
noncomputable def nonempty_countable_p : sorry := by
  sorry  -- pvs2lean: definition stub for nonempty?(X) AND is_countable(X)

-- pvs2lean: sigma_countable.sigma_disjoint_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(union(X, Y)))):
--   disjoint?(X, Y) => sigma(union(X, Y), f) = sigma(X, f) + sigma(Y, f)
theorem sigma_disjoint_union : ∀ (f : T → ℝ), disjoint_p X Y → sigma (union X Y) f = sigma X f + sigma Y f := by
  -- === TCCs for sigma_disjoint_union ===
  -- TCC: sigma_disjoint_union_TCC1
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](union[T](X, Y)))):
  --   disjoint?(X, Y) IMPLIES convergent?[T](X)(f)
  have sigma_disjoint_union_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_disjoint_union_TCC2
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](union[T](X, Y)))):
  --   disjoint?(X, Y) IMPLIES convergent?[T](Y)(f)
  have sigma_disjoint_union_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_disjoint_union_TCC1
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](union[T](X, Y)))):
  --   disjoint?(X, Y) IMPLIES convergent?[T](X)(f)
  have sigma_disjoint_union_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_disjoint_union_TCC2
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](union[T](X, Y)))):
  --   disjoint?(X, Y) IMPLIES convergent?[T](Y)(f)
  have sigma_disjoint_union_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_union
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(union(X, Y)))):
--   sigma(union(X, Y), f) =
--    sigma(X, f) + sigma(Y, f) - sigma(intersection(X, Y), f)
theorem sigma_union : ∀ (f : T → ℝ), sigma (union X Y) f = sigma X f + sigma Y f - sigma (intersection X Y) f := by
  -- === TCCs for sigma_union ===
  -- TCC: sigma_union_TCC1
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](X)(f)
  have sigma_union_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_union_TCC2
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](Y)(f)
  have sigma_union_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_union_TCC3
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](intersection[T](X, Y))(f)
  have sigma_union_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_union_TCC1
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](X)(f)
  have sigma_union_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_union_TCC2
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](Y)(f)
  have sigma_union_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_union_TCC3
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](intersection[T](X, Y))(f)
  have sigma_union_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_intersection
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(union(X, Y)))):
--   sigma(intersection(X, Y), f) =
--    sigma(X, f) + sigma(Y, f) - sigma(union(X, Y), f)
theorem sigma_intersection : ∀ (f : T → ℝ), sigma (intersection X Y) f = sigma X f + sigma Y f - sigma (union X Y) f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_difference
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(union(X, Y)))):
--   sigma(difference(X, Y), f) =
--    sigma(X, f) - sigma(Y, f) + sigma(difference(Y, X), f)
theorem sigma_difference : ∀ (f : T → ℝ), sigma (difference X Y) f = sigma X f - sigma Y f + sigma (difference Y X) f := by
  -- === TCCs for sigma_difference ===
  -- TCC: sigma_difference_TCC1
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](difference[T](X, Y))(f)
  have sigma_difference_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_difference_TCC2
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](difference[T](Y, X))(f)
  have sigma_difference_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_difference_TCC1
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](difference[T](X, Y))(f)
  have sigma_difference_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_difference_TCC2
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](union[T](X, Y)))):
  --   convergent?[T](difference[T](Y, X))(f)
  have sigma_difference_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(Y))):
--   subset?(X, Y) => sigma(Y, f) = sigma(X, f) + sigma(difference(Y, X), f)
theorem sigma_subset : ∀ (f : T → ℝ), subset_p X Y → sigma Y f = sigma X f + sigma (difference Y X) f := by
  -- === TCCs for sigma_subset ===
  -- TCC: sigma_subset_TCC1
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](Y))):
  --   subset?(X, Y) IMPLIES convergent?[T](X)(f)
  have sigma_subset_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_subset_TCC2
  -- Statement: FORALL (X, Y: countable_set[T], f: (convergent?[T](Y))):
  --   subset?(X, Y) IMPLIES convergent?[T](difference[T](Y, X))(f)
  have sigma_subset_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_subset_TCC1
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](Y))):
  --   subset?(X, Y) IMPLIES convergent?[T](X)(f)
  have sigma_subset_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_subset_TCC2
  -- Statement: FORALL (X, Y: set[T], f: (convergent?[T](Y))):
  --   subset?(X, Y) IMPLIES convergent?[T](difference[T](Y, X))(f)
  have sigma_subset_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X))):
--   sigma(add(t, X), f) =
--    IF member(t, X) THEN sigma(X, f) ELSE f(t) + sigma(X, f) ENDIF
theorem sigma_add : ∀ (f : T → ℝ), sigma (add t X) f = if member t X then sigma X f else f t + sigma X f := by
  -- === TCCs for sigma_add ===
  -- TCC: sigma_add_TCC1
  -- Statement: FORALL (X: countable_set[T], t: T, f: (convergent?[T](X))):
  --   convergent?[T](add[T](t, X))(f)
  have sigma_add_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_add_TCC1
  -- Statement: FORALL (X: set[T], t: T, f: (convergent?[T](X))):
  --   convergent?[T](add[T](t, X))(f)
  have sigma_add_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X))):
--   sigma(remove(t, X), f) =
--    IF member(t, X) THEN sigma(X, f) - f(t) ELSE sigma(X, f) ENDIF
theorem sigma_remove : ∀ (f : T → ℝ), sigma (remove t X) f = if member t X then sigma X f - f t else sigma X f := by
  -- === TCCs for sigma_remove ===
  -- TCC: sigma_remove_TCC1
  -- Statement: FORALL (X: countable_set[T], t: T, f: (convergent?[T](X))):
  --   convergent?[T](remove[T](t, X))(f)
  have sigma_remove_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_remove_TCC1
  -- Statement: FORALL (X: set[T], t: T, f: (convergent?[T](X))):
  --   convergent?[T](remove[T](t, X))(f)
  have sigma_remove_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_choose_rest
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: nonempty_countable, f: (convergent?(X))):
--   sigma(X, f) = f(choose(X)) + sigma(rest(X), f)
theorem sigma_choose_rest : ∀ (X : T → Bool) (f : T → ℝ), sigma X f = f (choose X) + sigma (rest X) f := by
  -- === TCCs for sigma_choose_rest ===
  -- TCC: sigma_choose_rest_TCC1
  -- Statement: FORALL (X: nonempty_countable, f: (convergent?[T](X))):
  --   convergent?[T](rest[T](X))(f)
  have sigma_choose_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_choose_rest_TCC1
  -- Statement: FORALL (X: nonempty_set[T], f: (convergence_set[T].convergent?(X))):
  --   convergent?[T](rest[T](X))(f)
  have sigma_choose_rest_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(X, LAMBDA t: 0) = 0
theorem sigma_zero : sigma X (fun t=>  0) = 0 := by
  -- === TCCs for sigma_zero ===
  -- TCC: sigma_zero_TCC1
  -- Statement: FORALL (X: countable_set[T]): convergent?[T](X)(LAMBDA t: 0)
  have sigma_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_zero_TCC1
  -- Statement: FORALL (X: set[T]): convergent?[T](X)(LAMBDA t: 0)
  have sigma_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_scal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X))): sigma(X, a * f) = a * sigma(X, f)
theorem sigma_scal : ∀ (f : T → ℝ), sigma X (a * f) = a * sigma X f := by
  -- === TCCs for sigma_scal ===
  -- TCC: sigma_scal_TCC1
  -- Statement: FORALL (X: countable_set[T], a: real, f: (convergent?[T](X))):
  --   convergent?[T](X)(*[T](a, f))
  have sigma_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_scal_TCC1
  -- Statement: FORALL (X: set[T], a: real, f: (convergent?[T](X))):
  --   convergent?[T](X)(*[T](a, f))
  have sigma_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_opp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X))): sigma(X, -f) = -sigma(X, f)
theorem sigma_opp : ∀ (f : T → ℝ), sigma X -f = -sigma X f := by
  -- === TCCs for sigma_opp ===
  -- TCC: sigma_opp_TCC1
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X))): convergent?[T](X)(-f)
  have sigma_opp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_opp_TCC1
  -- Statement: FORALL (X: set[T], f: (convergent?[T](X))): convergent?[T](X)(-f)
  have sigma_opp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_plus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: (convergent?(X))): sigma(X, f + g) = sigma(X, f) + sigma(X, g)
theorem sigma_plus : ∀ (f : T → ℝ) (g : T → ℝ), sigma X (f + g) = sigma X f + sigma X g := by
  -- === TCCs for sigma_plus ===
  -- TCC: sigma_plus_TCC1
  -- Statement: FORALL (X: countable_set[T], f, g: (convergent?[T](X))):
  --   convergent?[T](X)((+[T])(f, g))
  have sigma_plus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_plus_TCC1
  -- Statement: FORALL (X: set[T], f, g: (convergent?[T](X))):
  --   convergent?[T](X)((+[T])(f, g))
  have sigma_plus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: (convergent?(X))): sigma(X, f - g) = sigma(X, f) - sigma(X, g)
theorem sigma_diff : ∀ (f : T → ℝ) (g : T → ℝ), sigma X (f - g) = sigma X f - sigma X g := by
  -- === TCCs for sigma_diff ===
  -- TCC: sigma_diff_TCC1
  -- Statement: FORALL (X: countable_set[T], f, g: (convergent?[T](X))):
  --   convergent?[T](X)((-[T])(f, g))
  have sigma_diff_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_diff_TCC1
  -- Statement: FORALL (X: set[T], f, g: (convergent?[T](X))):
  --   convergent?[T](X)((-[T])(f, g))
  have sigma_diff_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_ge_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X))):
--   (FORALL (t: (X)): f(t) >= 0) => sigma(X, f) >= 0
theorem sigma_ge_0 : ∀ (f : T → ℝ), (∀ (t : (X)), f t ≥ 0) → sigma X f ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_gt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: nonempty_countable, f: (convergent?(X))):
--   (FORALL (t: (X)): f(t) > 0) => sigma(X, f) > 0
theorem sigma_gt_0 : ∀ (X : T → Bool) (f : T → ℝ), (∀ (t : (X)), f t > 0) → sigma X f > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X))): abs(sigma(X, f)) <= sigma(X, abs(f))
theorem sigma_abs : ∀ (f : T → ℝ), |sigma X f| ≤ sigma X |f| := by
  -- === TCCs for sigma_abs ===
  -- TCC: sigma_abs_TCC1
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X))):
  --   convergent?[T](X)(abs[T](f))
  have sigma_abs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_abs_TCC1
  -- Statement: FORALL (X: set[T], f: (convergent?[T](X))): convergent?[T](X)(abs[T](f))
  have sigma_abs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: (convergent?(X))):
--   (FORALL (t: (X)): f(t) = g(t)) => sigma(X, f) = sigma(X, g)
theorem sigma_eq : ∀ (f : T → ℝ) (g : T → ℝ), (∀ (t : (X)), f t = g t) → sigma X f = sigma X g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_le
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: (convergent?(X))):
--   (FORALL (t: (X)): f(t) <= g(t)) => sigma(X, f) <= sigma(X, g)
theorem sigma_le : ∀ (f : T → ℝ) (g : T → ℝ), (∀ (t : (X)), f t ≤ g t) → sigma X f ≤ sigma X g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: nonempty_countable, f, g: (convergent?(X))):
--   (FORALL (t: (X)): f(t) < g(t)) => sigma(X, f) < sigma(X, g)
theorem sigma_lt : ∀ (X : T → Bool) (f : T → ℝ) (g : T → ℝ), (∀ (t : (X)), f t < g t) → sigma X f < sigma X g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_bij
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X)), phi: (bijective?[(X), (X)])):
--   sigma(X, extend[T, (X), real, 0](f o phi)) = sigma(X, f)
theorem sigma_bij : ∀ (f : T → ℝ) (phi : T → T), sigma X (extend (f ∘ phi)) = sigma X f := by
  -- === TCCs for sigma_bij ===
  -- TCC: sigma_bij_TCC1
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X)),
  --         phi: (bijective?[(X), (X)])):
  --   convergent?[T](X)(extend[T, (X), real, 0](o[(X), T, real](f, phi)))
  have sigma_bij_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_nn_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X))):
--   (FORALL t: X(t) => f(t) >= 0) =>
--    sigma(X, f) =
--     sup({nnx |
--            EXISTS (F: finite_set[T]): subset?(F, X) AND nnx = sigma(F, f)})
theorem sigma_nn_def : ∀ (f : T → ℝ), (∀ t,  X t → f t ≥ 0) → sigma X f =
 sup ({nnx | ∃ (F : finite_set T), subset_p F X ∧ nnx = sigma F f}) := by
  -- === TCCs for sigma_nn_def ===
  -- TCC: sigma_nn_def_TCC1
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X))):
  --   (FORALL (t: T): X(t) => f(t) >= 0) IMPLIES
  --    FORALL (F: finite_set[T]): subset?(F, X) IMPLIES convergent?[T](F)(f)
  have sigma_nn_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_nn_def_TCC2
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X))):
  --   (FORALL (t: T): X(t) => f(t) >= 0) IMPLIES
  --    nonempty?[nnreal]
  --        ({nnx: nnreal |
  --            EXISTS (F: finite_set[T]):
  --              sub...
  have sigma_nn_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sigma_countable.sigma_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (convergent?(X))):
--   sigma(X, f) =
--    sup({nnx |
--           EXISTS (F: finite_set[T]):
--             subset?(F, X) AND
--              (FORALL t: F(t) => f(t) >= 0) AND nnx = sigma(F, f)})
--     +
-- 
theorem sigma_def : ∀ (f : T → ℝ), sigma X f = sup ({nnx |
       ∃ (F : finite_set T), subset_p F X ∧
          (∀ t,  F t → f t ≥ 0) ∧ nnx = sigma F f})
 +
 inf ({npx |
        ∃ (F : finite_set T), subset_p F X ∧
           (∀ t,  F t → f t ≤ 0) ∧ npx = sigma F f}) := by
  -- === TCCs for sigma_def ===
  -- TCC: sigma_def_TCC1
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X)), F: finite_set[T]):
  --   (subset?(F, X) AND FORALL (t: T): F(t) => f(t) >= 0) IMPLIES
  --    convergent?[T](F)(f)
  have sigma_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_def_TCC2
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X))):
  --   nonempty?[nnreal]
  --       ({nnx: nnreal |
  --           EXISTS (F: finite_set[T]):
  --             subset?[T](F, X) AND
  --              (FORALL (t: T): F(...
  have sigma_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_def_TCC3
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X)), F: finite_set[T]):
  --   (subset?(F, X) AND FORALL (t: T): F(t) => f(t) <= 0) IMPLIES
  --    convergent?[T](F)(f)
  have sigma_def_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_def_TCC4
  -- Statement: FORALL (X: countable_set[T], f: (convergent?[T](X))):
  --   nonempty?[npreal]
  --       ({npx: npreal |
  --           EXISTS (F: finite_set[T]):
  --             subset?[T](F, X) AND
  --              (FORALL (t: T): F(...
  have sigma_def_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.sigma_countable