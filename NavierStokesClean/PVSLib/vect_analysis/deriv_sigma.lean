-- Auto-generated by pvs2lean/translator.py
-- PVS theory: deriv_sigma
-- PVSLib library: vect_analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: deriv_sigma

Auto-generated stubs from PVSLib theory `deriv_sigma`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.deriv_sigma

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (deriv : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (derivable_p : pvs_τ4 → pvs_τ5 → Prop)

-- pvs2lean: deriv_sigma.T_pred
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: deriv_sigma.deriv_domain
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem deriv_domain : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: deriv_sigma.not_one_element
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem not_one_element : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: deriv_sigma.sigma_derivable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem sigma_derivable : True := by
  -- === TCCs for sigma_derivable ===
  -- TCC: sigma_derivable_TCC1
  -- Statement: FORALL (m: posnat, y: T, h: [T -> [Index[m] -> real]]):
  --   (FORALL (i: below[m]): derivable?(LAMBDA (t: T): h(t)(i), y)) IMPLIES
  --    FORALL (x: T): 0 >= 0 AND 0 < m OR EXISTS (j: Index[m]): j <= 0
  have sigma_derivable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_derivable_TCC2
  -- Statement: FORALL (m: posnat, y: T, h: [T -> [Index[m] -> real]]):
  --   (FORALL (i: below[m]): derivable?(LAMBDA (t: T): h(t)(i), y)) IMPLIES
  --    FORALL (x: T):
  --      m - 1 >= 0 AND m - 1 < m OR EXISTS (j: Index[m...
  have sigma_derivable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_derivable_TCC3
  -- Statement: FORALL (m: posnat, y1: T, h: [T -> [Index[m] -> real]]):
  --   (FORALL (i: below[m]): derivable?(LAMBDA (t: T): h(t)(i), y1)) IMPLIES
  --    FORALL (x: T):
  --      FORALL (x, y: Index[m]), (z: integer):
  --      ...
  have sigma_derivable_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: deriv_sigma.deriv_sigma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem deriv_sigma : True := by
  -- === TCCs for deriv_sigma ===
  -- TCC: deriv_sigma_TCC1
  -- Statement: FORALL (n: nat, y: T):
  --   n > 0 IMPLIES
  --    FORALL (h: [T -> [Index[n] -> real]]):
  --      (FORALL (i: below[n]): derivable?(LAMBDA (t: T): h(t)(i), y)) IMPLIES
  --       FORALL (x: T): 0 >= 0 AND 0 < n OR ...
  have deriv_sigma_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_sigma_TCC2
  -- Statement: FORALL (n: nat, y: T):
  --   n > 0 IMPLIES
  --    FORALL (h: [T -> [Index[n] -> real]]):
  --      (FORALL (i: below[n]): derivable?(LAMBDA (t: T): h(t)(i), y)) IMPLIES
  --       FORALL (x: T):
  --         n - 1 >= 0 A...
  have deriv_sigma_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_sigma_TCC3
  -- Statement: FORALL (n: nat, y1: T):
  --   n > 0 IMPLIES
  --    FORALL (h: [T -> [Index[n] -> real]]):
  --      (FORALL (i: below[n]): derivable?(LAMBDA (t: T): h(t)(i), y1)) IMPLIES
  --       FORALL (x: T):
  --         FORALL (x,...
  have deriv_sigma_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_sigma_TCC4
  -- Statement: FORALL (n: nat, y: T):
  --   n > 0 IMPLIES
  --    FORALL (h: [T -> [Index[n] -> real]]):
  --      (FORALL (i: below[n]): derivable?(LAMBDA (t: T): h(t)(i), y)) IMPLIES
  --       derivable?[T]
  --           (LAMBDA (x:...
  have deriv_sigma_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_sigma_TCC5
  -- Statement: FORALL (n: nat, y: T):
  --   n > 0 IMPLIES
  --    FORALL (h: [T -> [Index[n] -> real]]):
  --      (FORALL (i: below[n]): derivable?(LAMBDA (t: T): h(t)(i), y)) IMPLIES
  --       0 >= 0 AND 0 < n OR EXISTS (j: Inde...
  have deriv_sigma_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_sigma_TCC6
  -- Statement: FORALL (n: nat, y: T):
  --   n > 0 IMPLIES
  --    FORALL (h: [T -> [Index[n] -> real]]):
  --      (FORALL (i: below[n]): derivable?(LAMBDA (t: T): h(t)(i), y)) IMPLIES
  --       n - 1 >= 0 AND n - 1 < n OR EXISTS ...
  have deriv_sigma_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_sigma_TCC7
  -- Statement: FORALL (n: nat, y: T):
  --   n > 0 IMPLIES
  --    FORALL (h: [T -> [Index[n] -> real]]):
  --      (FORALL (i: below[n]): derivable?(LAMBDA (t: T): h(t)(i), y)) IMPLIES
  --       FORALL (i: Index[n]): derivable?[T,...
  have deriv_sigma_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_sigma_TCC8
  -- Statement: FORALL (n: nat, y1: T):
  --   n > 0 IMPLIES
  --    FORALL (h: [T -> [Index[n] -> real]]):
  --      (FORALL (i: below[n]): derivable?(LAMBDA (t: T): h(t)(i), y1)) IMPLIES
  --       FORALL (x, y: Index[n]), (z: inte...
  have deriv_sigma_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

end

end NavierStokesClean.PVSLib.deriv_sigma