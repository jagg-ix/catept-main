-- Auto-generated by pvs2lean/translator.py
-- PVS theory: Riemann_integ_interval_approx_perf
-- PVSLib library: Riemann
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: Riemann_integ_interval_approx_perf

Auto-generated stubs from PVSLib theory `Riemann_integ_interval_approx_perf`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.Riemann_integ_interval_approx_perf

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (F_Bound_p : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (LocallyProper : pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (Mult : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} (Proper_p : pvs_τ16 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (RiemannSum_R2I : pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} (Sigma : pvs_τ58 → pvs_τ59 → pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} (StrictInterval_p : pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} (pvs_op_d6fc54 : pvs_τ62 → pvs_τ63 → pvs_τ64)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} (best_rational : pvs_τ73 → pvs_τ74 → pvs_τ75)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} (upfrom : pvs_τ76 → pvs_τ77)

-- pvs2lean: Riemann_integ_interval_approx_perf.f_bound_determines_locally_proper_results
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m],
--         n: nat, F: [nat -> [Interval -> Interval]], g: [real -> real]):
--   F_Bound?([|a + (i - 1) * (b - a) / (2 ^ m), a + i * (
theorem f_bound_determines_locally_proper_results : ∀ (a : ℝ) (b : {x : ℝ // a < x}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ m}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ) (g : ℝ → ℝ), F_Bound_p ([|a + (i - 1) * (b - a) / (2 ^ m), a + i * (b - a) / (2 ^ m)|]) g n F → Proper_p (F n
         ([|a + (i - 1) * (b - a) / (2 ^ m), a + i * (b - a) / (2 ^ m)|])) := by
  -- === TCCs for f_bound_determines_locally_proper_results ===
  -- TCC: f_bound_determines_locally_proper_results_TCC1
  -- Statement: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m]):
  --   StrictInterval?([|a + (i - 1) * (b - a) / (2 ^ m),
  --                     a + i * (b - a) / (2 ^ m)|])
  have f_bound_determines_locally_proper_results_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.locallyProper?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: subrange[1, 2 ^ m]):
--   Proper?(F(n)
--            ([|a + (i - 1) * (b - a) / (2 ^ m), a + i * (b - a) / (2 ^ m)|]))
noncomputable def locallyProper_p : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL (i: subrange[1, 2 ^ m]):   Proper?(F(n)            ([|a + (i - 1) * (b - 

-- pvs2lean: Riemann_integ_interval_approx_perf.r_proper_on_f_locally_proper
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: real, b: {x: real | a < x}, m: posnat, n: nat,
--         F: LocallyProper(a, b, m, n)):
--   FORALL (i: subrange[1, 2 ^ m]): Proper?(R(a, b, m, i, n, F))
theorem r_proper_on_f_locally_proper : ∀ (a : ℝ) (b : {x : ℝ // a < x}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ), ∀ (i : {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ m}), Proper_p (R a b m i n F) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.rienmann_sum_r2i_proper_if_every_participating_r_is_proper
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m],
--         n: nat, F: LocallyProper(a, b, m, n)):
--   (FORALL (j: subrange[1, i]): Proper?(R(a, b, m, j, n, F))) IMPLIES
--    Proper?(
theorem rienmann_sum_r2i_proper_if_every_participating_r_is_proper : ∀ (a : ℝ) (b : {x : ℝ // a < x}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ m}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ), (∀ (j : {n : ℤ // 1 ≤ n ∧ n ≤ i}), Proper_p (R a b m j n F)) → Proper_p (RiemannSum_R2I a b m i n F R) := by
  -- === TCCs for rienmann_sum_r2i_proper_if_every_participating_r_is_proper ===
  -- TCC: rienmann_sum_r2i_proper_if_every_participating_r_is_proper_TCC1
  -- Statement: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m],
  --         n: nat, F: LocallyProper(a, b, m, n), j: subrange[1, i]):
  --   1 <= j AND j <= 2 ^ m
  have rienmann_sum_r2i_proper_if_every_participating_r_is_proper_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.bestRationalInterval
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: [|best_rational(I`lb - epsilon, I`lb), best_rational(I`ub, I`ub + epsilon)|]
axiom bestRationalInterval {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} : pvs_τ65 → pvs_τ66 → pvs_τ67

-- pvs2lean: Riemann_integ_interval_approx_perf.best_rational_interval_overapproximates
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (I: Interval, epsilon: posreal):
--   I << bestRationalInterval(I, epsilon)
theorem best_rational_interval_overapproximates : ∀ (I : ℝ × ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}), I << bestRationalInterval I epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.R_
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Mult(bestRationalInterval(F(n)
--                            ([|a + (i - 1) * (b - a) / (2 ^ m),
--                               a + i * (b - a) / (2 ^ m)|]),
--                           epsilon),
--      Delta
axiom R_ {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24 → pvs_τ25

-- pvs2lean: Riemann_integ_interval_approx_perf.R__
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: R_(a, b, m, i, n, F, bestRationalInterval([|(b - a) / 2 ^ m|], epsilon),
--    epsilon)
axiom R__ {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ26 → pvs_τ27 → pvs_τ28 → pvs_τ29 → pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33

-- pvs2lean: Riemann_integ_interval_approx_perf.r___includes_r
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m],
--         n: nat, F: [nat -> [Interval -> Interval]], epsilon: posreal,
--         x: real):
--   Proper?(R(a, b, m, i, n, F)) IMPLIES
-- 
theorem r___includes_r : ∀ (a : ℝ) (b : {x : ℝ // a < x}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ m}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}) (x : ℝ), Proper_p (R a b m i n F) → R a b m i n F pvs_lt_ord R__ epsilon(a, b, m, i, n, F) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.RiemannSum_R2I_tr_
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 1 THEN accum
-- ELSE RiemannSum_R2I_tr_(a, b, m, i - 1, n, F, R, epsilon)
--                        (bestRationalInterval(Add(R__(epsilon)
--                                                     (a, b, m
axiom RiemannSum_R2I_tr_ {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} : pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52 → pvs_τ53 → pvs_τ54 → pvs_τ55 → pvs_τ56 → pvs_τ57

-- pvs2lean: Riemann_integ_interval_approx_perf.RiemannSum_R2I_tr
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: RiemannSum_R2I_tr_(a, b, m, 2 ^ m, n, F, R, epsilon)
--                   (bestRationalInterval(R__(epsilon)(a, b, m, 2 ^ m, n, F),
--                                         epsilon))
axiom RiemannSum_R2I_tr {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ41 → pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48

-- pvs2lean: Riemann_integ_interval_approx_perf.bestRationalAdd
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: bestRationalInterval(Add(X, Y), epsilon)
noncomputable def bestRationalAdd : sorry := by
  sorry  -- pvs2lean: definition stub for bestRationalInterval(Add(X, Y), epsilon)

-- pvs2lean: Riemann_integ_interval_approx_perf.bestRationalSigma
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF l > u THEN [|0|]
-- ELSE bestRationalInterval(Add(F(l),
--                               bestRationalSigma(epsilon)(l + 1, u, F)),
--                           epsilon)
-- ENDIF
axiom bestRationalSigma {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} {pvs_τ72 : Type*} : pvs_τ68 → pvs_τ69 → pvs_τ70 → pvs_τ71 → pvs_τ72

-- pvs2lean: Riemann_integ_interval_approx_perf.bestRationalSigma_one
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: nat, F: [nat -> Interval], epsilon: posreal):
--   bestRationalSigma(epsilon)(j, j, F) = bestRationalInterval(F(j), epsilon)
theorem bestRationalSigma_one : ∀ (j : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}), bestRationalSigma epsilon(j, j, F) = bestRationalInterval (F j) epsilon := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.Sigma_alternative_definition
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat, j: upfrom(i), F: [nat -> Interval]):
--   Sigma(i, j, F) = Add(F(i), Sigma(i + 1, j, F))
theorem Sigma_alternative_definition : ∀ (i : {i : ℝ // i ≥ 0}) (j : {s : ℝ // s ≥ i}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ), Sigma i j F = Add (F i) (Sigma (i + 1) j F) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.Sigma_fundamental
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, F, G: [nat -> Interval], epsilon: posreal):
--   (FORALL (i: nat): F(i) << G(i)) IMPLIES Sigma(1, n, F) << Sigma(1, n, G)
theorem Sigma_fundamental : ∀ (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ) (G : {i : ℝ // i ≥ 0} → ℝ × ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}), (∀ (i : ℕ), F i pvs_lt_ord G i) → Sigma 1 n F pvs_lt_ord Sigma 1 n G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.bestRationalSigma_includes_sigma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat, j: int, F: [nat -> Interval], epsilon: posreal):
--   Sigma(i, j, F) << bestRationalSigma(epsilon)(i, j, F)
theorem bestRationalSigma_includes_sigma : ∀ (i : {i : ℝ // i ≥ 0}) (j : ℝ) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}), Sigma i j F << bestRationalSigma epsilon(i, j, F) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.extendedR__
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF 1 <= i AND i <= 2 ^ m THEN R__(epsilon)(a, b, m, i, n, F)
-- ELSE [|0|]
-- ENDIF
noncomputable def extendedR__ : sorry := by
  sorry  -- pvs2lean: definition stub for IF 1 <= i AND i <= 2 ^ m THEN R__(epsilon)(a, b, m, i, n, F) ELSE [|0|] ENDIF

-- pvs2lean: Riemann_integ_interval_approx_perf.tr_Riemann_as_sum
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m],
--         n: nat, F: LocallyProper(a, b, m, n), epsilon: posreal):
--   RiemannSum_R2I_tr_(a, b, m, i, n, F, R__(epsilon), epsilon)
-- 
theorem tr_Riemann_as_sum : ∀ (a : ℝ) (b : {x : ℝ // a < x}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ m}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}), RiemannSum_R2I_tr_ a b m i n F (R__ epsilon) epsilon
                  (bestRationalSigma epsilon
                                    (i, 2 ^ m,
                                     (fun (k : ℕ)=> if 1 ≤ k ∧ k ≤ 2 ^ m
                                          then R__ epsilon
                                                  (a, b, m, k, n, F)
                                        else[|0|]
                                        ))) = bestRationalSigma epsilon
                 (1, 2 ^ m,
                  (fun (k : ℕ)=> if 1 ≤ k ∧ k ≤ 2 ^ m
                       then R__ epsilon(a, b, m, k, n, F)
                     else[|0|]
                     )) := by
  -- === TCCs for tr_Riemann_as_sum ===
  -- TCC: tr_Riemann_as_sum_TCC1
  -- Statement: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m],
  --         n: nat, F: LocallyProper(a, b, m, n), k: nat):
  --   1 <= k AND k <= 2 ^ m IMPLIES 1 <= k AND k <= 2 ^ m
  have tr_riemann_as_sum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.tr_Riemann_inclusion
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: real, b: {x: real | a < x}, m: posnat, n: nat,
--         F: LocallyProper(a, b, m, n), epsilon: posreal, x: real):
--   RiemannSum_R2I(a, b, m, 2 ^ m, n, F, R) <<
--    RiemannSum_R2I_tr(a, b, m, n
theorem tr_Riemann_inclusion : ∀ (a : ℝ) (b : {x : ℝ // a < x}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}) (x : ℝ), RiemannSum_R2I a b m (2 ^ m) n F R << RiemannSum_R2I_tr a b m n F (R__ epsilon) epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx_perf.tr_Riemann_correctness
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: real, b: {x: real | a < x}, m: posnat, n: nat,
--         F: LocallyProper(a, b, m, n), epsilon: posreal, x: real):
--   x ## RiemannSum_R2I(a, b, m, 2 ^ m, n, F, R) IMPLIES
--    x ## RiemannSum_R2
theorem tr_Riemann_correctness : ∀ (a : ℝ) (b : {x : ℝ // a < x}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}) (x : ℝ), x ∈ RiemannSum_R2I a b m (2 ^ m) n F R → x ∈ RiemannSum_R2I_tr a b m n F (R__ epsilon) epsilon := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.Riemann_integ_interval_approx_perf