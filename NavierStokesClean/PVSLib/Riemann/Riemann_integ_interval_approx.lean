-- Auto-generated by pvs2lean/translator.py
-- PVS theory: Riemann_integ_interval_approx
-- PVSLib library: Riemann
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: Riemann_integ_interval_approx

Auto-generated stubs from PVSLib theory `Riemann_integ_interval_approx`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.Riemann_integ_interval_approx

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (Eval : pvs_τ4 → pvs_τ5)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (Integrable_p : pvs_τ10 → pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (Mult : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (Sigma : pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ37 : Type*} (StrictInterval_p : pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (closed_interval : pvs_τ38 → pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (eq_partition : pvs_τ41 → pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} (fDom_p : pvs_τ44 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} (integrable_p : pvs_τ48 → pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} (integral : pvs_τ51 → pvs_τ52 → pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} (lb : pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} (length : pvs_τ55 → pvs_τ56)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} (subrange : pvs_τ57 → pvs_τ58 → pvs_τ59)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (ub : pvs_τ69 → pvs_τ70)

-- pvs2lean: Riemann_integ_interval_approx.R
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P = eq_partition(a, b, 2 ^ m + 1) IN
--   Mult(F(n)
--         ([|finseq_appl[closed_interval[real](a, b)](P)(i - 1),
--            finseq_appl[closed_interval[real](a, b)](P)(i)|]),
--        [|(b - a) / 2 ^
axiom R {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22

-- pvs2lean: Riemann_integ_interval_approx.RiemannSum_R2I
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 1 THEN R(a, b, m, 1, n, F)
-- ELSE Add(R(a, b, m, i, n, F), RiemannSum_R2I(a, b, m, i - 1, n, F, R))
-- ENDIF
axiom RiemannSum_R2I {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28 → pvs_τ29 → pvs_τ30

-- pvs2lean: Riemann_integ_interval_approx.SigmaSub
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Sigma(i, j, LAMBDA (n: nat): IF fDom?(n) THEN F(n) ELSE [|0|] ENDIF)
axiom SigmaSub {pvs_τ35 : Type*} {pvs_τ36 : Type*} : pvs_τ35 → pvs_τ36

-- pvs2lean: Riemann_integ_interval_approx.subrange?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= k AND k <= j
axiom subrange_p {pvs_τ60 : Type*} {pvs_τ61 : Type*} {pvs_τ62 : Type*} : pvs_τ60 → pvs_τ61 → pvs_τ62 → Prop

-- pvs2lean: Riemann_integ_interval_approx.riemann_sum_r2i_alternative_definition
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m],
--         n: nat, F: [nat -> [Interval -> Interval]], R: Riem_sec_r2i):
--   RiemannSum_R2I(a, b, m, i, n, F, R) =
--    SigmaSub(restr
theorem riemann_sum_r2i_alternative_definition : ∀ (a : ℝ) (b : {x : ℝ // a < x}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ m}) (n : {i : ℝ // i ≥ 0}) (F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ) (R : _ × {az : ℝ // aa < az} × _ × {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ mm} × {i : ℝ // i ≥ 0} × (_ → ℝ × ℝ → ℝ × ℝ) → ℝ × ℝ), RiemannSum_R2I a b m i n F R = SigmaSub (restrict (subrange_p 1 (2 ^ m)))
        (1, i, fun (k : (subrange_p 1 (2 ^ m)))=> R a b m k n F) := by
  -- === TCCs for riemann_sum_r2i_alternative_definition ===
  -- TCC: riemann_sum_r2i_alternative_definition_TCC1
  -- Statement: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m],
  --         k: (subrange?(1, 2 ^ m))):
  --   1 <= k AND k <= 2 ^ m
  have riemann_sum_r2i_alternative_definition_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: riemann_sum_r2i_alternative_definition_TCC2
  -- Statement: FORALL (a: real, b: {x: real | a < x}, m: posnat, i: subrange[1, 2 ^ m]):
  --   FORALL (x: int):
  --     subrange?(1, 2 ^ m)(x) IFF
  --      x >= 0 AND restrict[int, nat, bool](subrange?(1, 2 ^ m))(x)
  have riemann_sum_r2i_alternative_definition_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.F_integrable_cos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Integrable?(lb(X), ub(X), cos)
theorem F_integrable_cos : Integrable_p (lb X) (ub X) cos := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.F_integrable_cos2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: integrable?(lb(X), ub(X), cos)
theorem F_integrable_cos2 : integrable_p (lb X) (ub X) cos := by
  -- === TCCs for F_integrable_cos2 ===
  -- TCC: F_integrable_cos2_TCC1
  -- Statement: FORALL (X: StrictInterval): lb(X) < ub(X)
  have f_integrable_cos2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.eq_part_width
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P = eq_partition(lb(X), ub(X), 2 ^ m + 1) IN
--   FORALL (i: subrange(1, 2 ^ m)):
--     StrictInterval?([|finseq_appl[closed_interval[real](lb(X), ub(X))]
--                           (P)(i - 1),
--         
theorem eq_part_width : fun (P : {fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {x : ℝ // lb X ≤ x ∧ x ≤ ub X}) // List.length fs > 1 ∧
 seq fs(0) = lb X ∧
  seq fs(List.length fs - 1) = ub X ∧
   ∀ (ii : Finset.range List.length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) => ∀ (i : {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ m}), StrictInterval_p ([|finseq_appl[closed_interval[real](lb X, ub X)]
                      (P) (i - 1),
                  finseq_appl P(i)|]) → finseq_appl P(i) -
 finseq_appl P(i - 1)
 = (ub X - lb X) / 2 ^ m (eq_partition (lb X) (ub X) (2 ^ m + 1)) := by
  -- === TCCs for eq_part_width ===
  -- TCC: eq_part_width_TCC1
  -- Statement: FORALL (m: posnat): 2 ^ m + 1 > 1
  have eq_part_width_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eq_part_width_TCC2
  -- Statement: FORALL (X: StrictInterval, m: posnat, P: partition[real](lb(X), ub(X))):
  --   P = eq_partition(lb(X), ub(X), 2 ^ m + 1) IMPLIES
  --    FORALL (i: subrange(1, 2 ^ m)): i - 1 >= 0 AND i - 1 < length(P)
  have eq_part_width_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eq_part_width_TCC3
  -- Statement: FORALL (X: StrictInterval, m: posnat, P: partition[real](lb(X), ub(X))):
  --   P = eq_partition(lb(X), ub(X), 2 ^ m + 1) IMPLIES
  --    FORALL (i: subrange(1, 2 ^ m)): i < length(P)
  have eq_part_width_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.Mult_r2i_dist
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: [|lb(Xt) * l, ub(Xt) * l|] = Mult([|lb(Xt), ub(Xt)|], [|l|])
theorem Mult_r2i_dist : [|lb Xt * l, ub Xt * l|] = Mult ([|lb Xt, ub Xt|]) [|l|] := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.integ_r2i_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND
--  integrable?(a, b, f) AND
--   (FORALL (x: closed_interval(a, b)): m₀ <= f(x) AND f(x) <= M₀)
--  IMPLIES integral(a, b, f) ## [|m₀ * (b - a), M₀ * (b - a)|]
theorem integ_r2i_bound : a < b ∧
 integrable_p a b f ∧
  (∀ (x : closed_interval a b), m₀ ≤ f x ∧ f x ≤ M₀) → integral a b f ∈ [|m₀ * (b - a), M₀ * (b - a)|] := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.simple_one
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: closed_interval(lb(X), ub(X))): x ## [|lb(X), ub(X)|]
theorem simple_one : ∀ (x : {x : ℝ // lb X ≤ x ∧ x ≤ ub X}), x ## [|lb X, ub X|] := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.F_Bound?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: closed_interval(lb(X), ub(X))): f(x) ## F(n)(X)
axiom F_Bound_p {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: Riemann_integ_interval_approx.integ_inclus_fun?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: integrable?(lb(X), ub(X), f) AND
--  (EXISTS (F: [nat -> [Interval -> Interval]]):
--     FORALL (x: closed_interval(lb(X), ub(X))): f(x) ## F(n)(X))
axiom integ_inclus_fun_p {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} : pvs_τ45 → pvs_τ46 → pvs_τ47 → Prop

-- pvs2lean: Riemann_integ_interval_approx.integ_r2i_bound₂
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: integrable?(lb(X), ub(X), f) AND
--  (FORALL (x: closed_interval(lb(X), ub(X))): f(x) ## [|m₀, M₀|])
--  IMPLIES integral(lb(X), ub(X), f) ## Mult([|m₀, M₀|], [|ub(X) - lb(X)|])
theorem integ_r2i_bound_ : integrable_p (lb X) (ub X) f ∧
 (∀ (x : closed_interval (lb X) (ub X)), f x ∈ [|m₀, M₀|]) → integral (lb X) (ub X) f ∈ Mult ([|m₀, M₀|]) ([|ub X - lb X|]) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.Integ_inclus_f
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: integrable?(lb(X), ub(X), f) AND F_Bound?(X, f, n, F) IMPLIES
--  integral(lb(X), ub(X), f) ##
--   Mult([|lb(F(n)(X)), ub(F(n)(X))|], [|ub(X) - lb(X)|])
theorem Integ_inclus_f : integrable_p (lb X) (ub X) f ∧ F_Bound_p X f n F → integral (lb X) (ub X) f ∈ Mult ([|lb (F n(X)), ub (F n(X))|]) ([|ub X - lb X|]) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.Integ_inclusion_f
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: integrable?(lb(X), ub(X), f) AND F_Bound?(X, f, n, F) IMPLIES
--  integral(lb(X), ub(X), f) ## Mult(F(n)(X), [|ub(X) - lb(X)|])
theorem Integ_inclusion_f : integrable_p (lb X) (ub X) f ∧ F_Bound_p X f n F → integral (lb X) (ub X) f ∈ Mult (F n(X)) ([|ub X - lb X|]) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.Integ_inclusion₂_f
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem Integ_inclusion__f : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: Riemann_integ_interval_approx.simple_two
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P = eq_partition(lb(X), ub(X), 2 ^ m + 1) IN
--   finseq_appl[closed_interval[real](lb(X), ub(X))](P)(0) = lb(X) AND
--    finseq_appl[closed_interval[real](lb(X), ub(X))](P)(2 ^ m) = ub(X)
theorem simple_two : fun (P : {fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {x : ℝ // lb X ≤ x ∧ x ≤ ub X}) // List.length fs > 1 ∧
 seq fs(0) = lb X ∧
  seq fs(List.length fs - 1) = ub X ∧
   ∀ (ii : Finset.range List.length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) => finseq_appl P(0) = lb X ∧ finseq_appl P(2 ^ m) = ub X (eq_partition (lb X) (ub X) (2 ^ m + 1)) := by
  -- === TCCs for simple_two ===
  -- TCC: simple_two_TCC1
  -- Statement: FORALL (X: StrictInterval, m: posnat, P: partition[real](lb(X), ub(X))):
  --   P = eq_partition(lb(X), ub(X), 2 ^ m + 1) IMPLIES 0 < length(P)
  have simple_two_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: simple_two_TCC2
  -- Statement: FORALL (X: StrictInterval, m: posnat, P: partition[real](lb(X), ub(X))):
  --   P = eq_partition(lb(X), ub(X), 2 ^ m + 1) AND
  --    finseq_appl[closed_interval[real](lb(X), ub(X))](P)(0) = lb(X)
  --    IMPLIES...
  have simple_two_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.sub_integ
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P = eq_partition(a, b, 2 ^ m + 1) IN
--   integral(lb
--              ([|finseq_appl[closed_interval[real](a, b)](P)(i - 1),
--                 finseq_appl[closed_interval[real](a, b)](P)(i)|]),
--           
noncomputable def sub_integ : sorry := by
  sorry  -- pvs2lean: definition stub for LET P = eq_partition(a, b, 2 ^ m + 1) IN   integral(lb              ([|finseq_ap

-- pvs2lean: Riemann_integ_interval_approx.sum_n_split
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P = eq_partition(a, b, 2 ^ m + 1) IN
--   IF i = 1
--     THEN integral(lb
--                     ([|finseq_appl[closed_interval[real](a, b)](P)(0),
--                        finseq_appl[closed_interval[real]
axiom sum_n_split {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} : pvs_τ63 → pvs_τ64 → pvs_τ65 → pvs_τ66 → pvs_τ67 → pvs_τ68

-- pvs2lean: Riemann_integ_interval_approx.general_integ_split
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  LET P = eq_partition(a, b, 2 ^ m + 1) IN
--    FORALL (i: subrange(1, 2 ^ m)):
--      integral(finseq_appl[closed_interval[real](a, b)](P)(0),
--               finseq_appl[closed_interval[real]
theorem general_integ_split : a < b → (let P := eq_partition a b (2 ^ m + 1); ∀ (i : Set.Icc 1 2 ^ m), integral (finseq_appl P(0)) (finseq_appl P(i)) g
     = sum_n_split a b m i g) := by
  -- === TCCs for general_integ_split ===
  -- TCC: general_integ_split_TCC1
  -- Statement: FORALL (a, b: real, m: posnat):
  --   a < b IMPLIES
  --    FORALL (P: partition[real](a, b)):
  --      P = eq_partition(a, b, 2 ^ m + 1) IMPLIES
  --       FORALL (i: subrange(1, 2 ^ m)): 0 < length(P)
  have general_integ_split_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: general_integ_split_TCC2
  -- Statement: FORALL (a, b: real, m: posnat):
  --   a < b IMPLIES
  --    FORALL (P: partition[real](a, b)):
  --      P = eq_partition(a, b, 2 ^ m + 1) IMPLIES
  --       FORALL (i: subrange(1, 2 ^ m)): i < length(P)
  have general_integ_split_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: general_integ_split_TCC3
  -- Statement: FORALL (a, b: real, m: posnat):
  --   a < b IMPLIES
  --    FORALL (P: partition[real](a, b)):
  --      P = eq_partition(a, b, 2 ^ m + 1) IMPLIES
  --       FORALL (i: subrange(1, 2 ^ m)):
  --         finseq_appl[closed...
  have general_integ_split_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: general_integ_split_TCC4
  -- Statement: FORALL (a, b: real, g: Integ_Inclusion_fun, m: posnat):
  --   a < b IMPLIES
  --    FORALL (P: partition[real](a, b)):
  --      P = eq_partition(a, b, 2 ^ m + 1) IMPLIES
  --       FORALL (i: subrange(1, 2 ^ m)):
  --   ...
  have general_integ_split_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.general_integ_split₁
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: integral(lb(X), ub(X), g) = sum_n_split(lb(X), ub(X), m, 2 ^ m, g)
theorem general_integ_split_ : integral (lb X) (ub X) g = sum_n_split (lb X) (ub X) m (2 ^ m) g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.trivial
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (n, n2: nat): n < n2 => n + 1 <= n2)
theorem trivial : ∀ (n : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}), n < n2 → n + 1 ≤ n2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.Fundamental_Riemann_inclusion₁
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P = eq_partition(lb(X), ub(X), 2 ^ m + 1) IN
--   (FORALL (i: subrange(1, 2 ^ m)):
--      (FORALL (Eval:
--                 {F: [nat -> [Interval -> Interval]] |
--                    FORALL (i: subrange(1, 
theorem Fundamental_Riemann_inclusion_ : fun (P : {fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {x : ℝ // lb X ≤ x ∧ x ≤ ub X}) // List.length fs > 1 ∧
 seq fs(0) = lb X ∧
  seq fs(List.length fs - 1) = ub X ∧
   ∀ (ii : Finset.range List.length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) => ∀ (i : {k : ℝ // 1 ≤ k ∧ k ≤ 2 ^ m}), ∀ (Eval : {F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ // ∀ (i : Set.Icc 1 2 ^ m), F_Bound_p ([|finseq_appl P(i - 1),
             finseq_appl P(i)|]) g n F}), sum_n_split (lb X) (ub X) m i g ## RiemannSum_R2I (lb X) (ub X) m i n Eval R (eq_partition (lb X) (ub X) (2 ^ m + 1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: Riemann_integ_interval_approx.Simple_Riemann_Soundness
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (X: StrictInterval, g: Integ_Inclusion_fun, m: posnat, n: nat):
--   LET P = eq_partition(lb(X), ub(X), 2 ^ m + 1) IN
--     FORALL (Eval:
--               {F: [nat -> [Interval -> Interval]] |
--         
theorem Simple_Riemann_Soundness : ∀ (X : ℝ × ℝ) (g : {f : ℝ → ℝ // ∀ (X : StrictInterval) (n : ℕ), integ_inclus_fun_p X f n}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (n : {i : ℝ // i ≥ 0}), fun (P : {fs : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → {x : ℝ // lb X ≤ x ∧ x ≤ ub X}) // List.length fs > 1 ∧
 seq fs(0) = lb X ∧
  seq fs(List.length fs - 1) = ub X ∧
   ∀ (ii : Finset.range List.length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}) => ∀ (Eval : {F : {i : ℝ // i ≥ 0} → ℝ × ℝ → ℝ × ℝ // ∀ (i : Set.Icc 1 2 ^ m), F_Bound_p ([|finseq_appl P(i - 1),
             finseq_appl P(i)|]) g n F}), integral (lb X) (ub X) g ## RiemannSum_R2I (lb X) (ub X) m (2 ^ m) n Eval R (eq_partition (lb X) (ub X) (2 ^ m + 1)) := by
  -- === TCCs for Simple_Riemann_Soundness ===
  -- TCC: Simple_Riemann_Soundness_TCC1
  -- Statement: FORALL (X: StrictInterval, m: posnat, P: partition[real](lb(X), ub(X))):
  --   P = eq_partition(lb(X), ub(X), 2 ^ m + 1) IMPLIES
  --    FORALL (i: subrange(1, 2 ^ m)):
  --      StrictInterval?([|finseq_appl[cl...
  have simple_riemann_soundness_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.Riemann_integ_interval_approx