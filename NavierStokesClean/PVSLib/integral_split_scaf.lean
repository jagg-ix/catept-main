-- Auto-generated by pvs2lean/translator.py
-- PVS theory: integral_split_scaf
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: integral_split_scaf

Auto-generated stubs from PVSLib theory `integral_split_scaf`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.integral_split_scaf

noncomputable section

-- pvs2lean: integral_split_scaf.T_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_split_scaf.connected_domain
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[T]
theorem connected_domain : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.not_one_element
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[T]
theorem not_one_element : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.diff_step_is_step_on
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition[T](a, b)):
--    step_function_on?(a, b, f, P) AND step_function_on?(a, b, g, P) IMPLIES
--     step_function_on?(a, b, f - g, P)
theorem diff_step_is_step_on : a < b → ∀ (P : partition (a, b)), step_function_on_p a b f P ∧ step_function_on_p a b g P →
   step_function_on_p a b (f - g) P := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.se_not_on
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: partition[T](a, b)):
--   FORALL (ii, jj: below(length(P) - 1)):
--     FORALL (x: open_interval[T](seq(P)(ii), seq(P)(1 + ii))):
--       x /= seq(P)(jj)
theorem se_not_on : ∀ (P : {fs : { length : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0}, seq : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length} → {x : {x : {x : {x : ℝ // _} // _} // _} // a ≤ x ∧ x ≤ b} } // length fs > 1 ∧
 seq fs(0) = a ∧
  seq fs(length fs - 1) = b ∧
   ∀ (ii : Finset.range length (fs - 1)), seq fs(ii) < seq fs(1 + ii)}), ∀ (ii : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1}) (jj : {s : {i : {x : {x : {x : {x : ℝ // _} // _} // _} // _} // i ≥ 0} // s < length P - 1}), ∀ (x : {x : {x : {x : {x : ℝ // _} // _} // _} // seq P(ii) < x ∧ x < seq P(1 + ii)}), x ≠ seq P(jj) := by
  -- === TCCs for se_not_on ===
  -- TCC: se_not_on_TCC1
  -- Statement: FORALL (a, b: T, P: partition[T](a, b), ii, jj: below(length(P) - 1),
  --         x: open_interval[T](seq(P)(ii), seq(P)(1 + ii))):
  --   jj < P`length
  have se_not_on_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.find_P_sec
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def find_P_sec : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_split_scaf.find_P_sec_lem
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  (FORALL (xx: closed_interval(a, b)):
--     FORALL (P: partition[T](a, b)):
--       LET JJ = find_P_sec(a, b, P, xx) IN
--         seq(P)(JJ) <= xx AND xx <= seq(P)(JJ + 1))
theorem find_P_sec_lem : a < b → (∀ (xx : closed_interval a b), ∀ (P : partition (a, b)), (let JJ := find_P_sec a b P xx; seq P(JJ) ≤ xx ∧ xx ≤ seq P(JJ + 1))) := by
  -- === TCCs for find_P_sec_lem ===
  -- TCC: find_P_sec_lem_TCC1
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (xx: closed_interval[T](a, b), P: partition[T](a, b),
  --            JJ:
  --              {ii: below(length(P) - 1) |
  --                 seq(P)(ii) <= xx AND xx <= ...
  have find_p_sec_lem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: find_P_sec_lem_TCC2
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (xx: closed_interval[T](a, b), P: partition[T](a, b),
  --            JJ:
  --              {ii: below(length(P) - 1) |
  --                 seq(P)(ii) <= xx AND xx <= ...
  have find_p_sec_lem_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.find_P_sec_in
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition[T](a, b)):
--    FORALL (ii: below(length(P) - 1)):
--      FORALL (x: open_interval[T](seq(P)(ii), seq(P)(1 + ii))):
--        find_P_sec(a, b, P, x) = ii
theorem find_P_sec_in : a < b → ∀ (P : partition (a, b)), ∀ (ii : Finset.range length (P - 1)), ∀ (x : open_interval (seq P(ii), seq P(1 + ii))), find_P_sec a b P x = ii := by
  -- === TCCs for find_P_sec_in ===
  -- TCC: find_P_sec_in_TCC1
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b), ii: below(length(P) - 1),
  --            x: open_interval[T](seq(P)(ii), seq(P)(1 + ii))):
  --      a <= x AND x <= b
  have find_p_sec_in_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.gen3P
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := 3,
--    seq
--      := (LAMBDA (ii: below(3)):
--            IF ii = 0 THEN a ELSIF ii = 1 THEN b ELSE c ENDIF) #)
noncomputable def gen3P : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := 3,    seq      := (LAMBDA (ii: below(3)):            IF ii = 0 THEN

-- pvs2lean: integral_split_scaf.F1
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x < a OR x > b THEN 0
-- ELSE LET JJ = find_P_sec(a, b, P, x) IN
--        IF x = finseq_appl[closed_interval[T](a, b)](P)(JJ) OR
--            x = finseq_appl[closed_interval[T](a, b)](P)(JJ + 1)
--          
noncomputable def F1 : sorry := by
  sorry  -- pvs2lean: definition stub for IF x < a OR x > b THEN 0 ELSE LET JJ = find_P_sec(a, b, P, x) IN        IF x = f

-- pvs2lean: integral_split_scaf.F2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x < a OR x > b THEN 0
-- ELSE LET JJ = find_P_sec(a, b, P, x) IN
--        IF x = finseq_appl[closed_interval[T](a, b)](P)(JJ) OR
--            x = finseq_appl[closed_interval[T](a, b)](P)(JJ + 1)
--          
noncomputable def F2 : sorry := by
  sorry  -- pvs2lean: definition stub for IF x < a OR x > b THEN 0 ELSE LET JJ = find_P_sec(a, b, P, x) IN        IF x = f

-- pvs2lean: integral_split_scaf.F1_F2_lem
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND integrable?(a, b, f) IMPLIES
--  FORALL (P: partition[T](a, b)):
--    step_function_on?(a, b, F1(a, b, P, f), P) AND
--     step_function_on?(a, b, F2(a, b, P, f), P) AND
--      (FORALL (x: closed_int
theorem F1_F2_lem : a < b ∧ integrable_p a b f → ∀ (P : partition (a, b)), step_function_on_p a b (F1 a b P f) P ∧
   step_function_on_p a b (F2 a b P f) P ∧
    (∀ (x : closed_interval a b), F1 a b P f(x) ≤ f x ∧ f x ≤ F2 a b P f(x)) := by
  -- === TCCs for F1_F2_lem ===
  -- TCC: F1_F2_lem_TCC1
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b)): bnded_on?[T](a, b)(f)
  have f1_f2_lem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.epsilon_is_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL eps: abs(xv) < eps) IMPLIES xv = 0
theorem epsilon_is_0 : (∀ eps,  |xv| < eps) → xv = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: integral_split_scaf.F2_F1_step_function_on
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND integrable?(a, b, f) IMPLIES
--  FORALL (P: partition[T](a, b)):
--    LET N = length(P) - 1 IN
--      step_function_on?[T](a, b, F2(a, b, P, f) - F1(a, b, P, f), P)
theorem F2_F1_step_function_on : a < b ∧ integrable_p a b f → ∀ (P : partition (a, b)), (let N := length P - 1; step_function_on_p (a, b, F2 a b P f - F1 a b P f, P)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.integrable_F2_F1
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND integrable?(a, b, f) IMPLIES
--  FORALL (P: partition[T](a, b)):
--    LET N = length(P) - 1 IN
--      integrable?[T](a, b, F2(a, b, P, f) - F1(a, b, P, f))
theorem integrable_F2_F1 : a < b ∧ integrable_p a b f → ∀ (P : partition (a, b)), (let N := length P - 1; integrable_p (a, b, F2 a b P f - F1 a b P f)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.integral_F2_F1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b AND integrable?(a, b, f) IMPLIES
--  FORALL (P: partition[T](a, b)):
--    LET N = length(P) - 1 IN
--      integral(a, b, F2(a, b, P, f) - F1(a, b, P, f)) =
--       sigma(0, N - 1,
--             (LAMBDA (j:
theorem integral_F2_F1 : a < b ∧ integrable_p a b f → ∀ (P : partition (a, b)), (let N := length P - 1; integral a b (F2 a b P f - F1 a b P f) =
     Finset.sum (Finset.Icc 0 N - 1) ((fun (j : Finset.range N)=> (let DP := finseq_appl (P) (j + 1) -
                     finseq_appl (P) (j); let DELj := MAXj a b P j f - MINj a b P j f; DELj * DP)))) := by
  -- === TCCs for integral_F2_F1 ===
  -- TCC: integral_F2_F1_TCC1
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b), N: int):
  --      N = length(P) - 1 IMPLIES
  --       integrable?[T](a, b, (-[T])(F2(a, b, P, f)...
  have integral_f2_f1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_F2_F1_TCC2
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b), N: int):
  --      N = length(P) - 1 IMPLIES
  --       0 >= 0 AND 0 < N OR EXISTS (j: below(N)): ...
  have integral_f2_f1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_F2_F1_TCC3
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b), N: int):
  --      N = length(P) - 1 IMPLIES
  --       N - 1 >= 0 AND N - 1 < N OR EXISTS (j: bel...
  have integral_f2_f1_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_F2_F1_TCC4
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b), N: int):
  --      N = length(P) - 1 IMPLIES FORALL (j: below(N)): j + 1 < length(P)
  have integral_f2_f1_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_F2_F1_TCC5
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b), N: int):
  --      N = length(P) - 1 IMPLIES FORALL (j: below(N)): j < length(P)
  have integral_f2_f1_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_F2_F1_TCC6
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b), N: int, j: below(N), DP: real):
  --      N = length(P) - 1 AND
  --       DP =
  --        finseq_appl...
  have integral_f2_f1_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_F2_F1_TCC7
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b), N: int, j: below(N), DP: real):
  --      N = length(P) - 1 AND
  --       DP =
  --        finseq_appl...
  have integral_f2_f1_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: integral_F2_F1_TCC8
  -- Statement: FORALL (a, b: T, f: [T -> real]):
  --   integrable?(a, b, f) AND a < b IMPLIES
  --    FORALL (P: partition[T](a, b), N: int):
  --      N = length(P) - 1 IMPLIES
  --       FORALL (x, y: below(N)), (z: integer):
  --    ...
  have integral_f2_f1_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.se_prep
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition[T](a, b), f: (bounded_on_all?(a, b, P)),
--          j: below(length(P) - 1), nu: posreal):
--    (EXISTS (xp: closed_interval[T](P`seq(j), P`seq(j + 1))):
--       f(xp) < 
theorem se_prep : a < b → ∀ (P : partition (a, b)) (f : (bounded_on_all_p a b P)) (j : Finset.range length (P - 1)) (nu : {x : ℝ // 0 < x}), (∃ (xp : closed_interval (P.seq j, P.seq (j + 1))), f xp < MINj a b P j f + nu)
   ∧
   (∃ (xpp : closed_interval (P.seq j, P.seq (j + 1))), f xpp > MAXj a b P j f - nu) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.get_xp
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def get_xp : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_split_scaf.get_xpp
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def get_xpp : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_split_scaf.lt_via_epsi
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, a: real): (FORALL (epsi: posreal): x < a + epsi) IMPLIES x <= a
theorem lt_via_epsi : ∀ (x : {x : {x : ℝ // _} // _}) (a : {x : {x : ℝ // _} // _}), (∀ (epsi : {x : ℝ // 0 < x}), x < a + epsi) → x ≤ a := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.sigma_all_parts
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  FORALL (P: partition[T](a, b)):
--    sigma(0, length(P) - 2,
--          (LAMBDA (j: below(length(P) - 1)):
--             finseq_appl[closed_interval[T](a, b)](P)(j + 1) -
--              finseq_
theorem sigma_all_parts : a < b → ∀ (P : partition (a, b)), sigma 0 (length P - 2) ((fun (j : Finset.range length (P - 1))=> finseq_appl (P) (j + 1) -
            finseq_appl (P) (j)))
   = b - a := by
  -- === TCCs for sigma_all_parts ===
  -- TCC: sigma_all_parts_TCC1
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      0 >= 0 AND 0 < length(P) - 1 OR
  --       EXISTS (j: below(length(P) - 1)): j <= 0
  have sigma_all_parts_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_all_parts_TCC2
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      length(P) - 2 >= 0 AND length(P) - 2 < length(P) - 1 OR
  --       EXISTS (j: below(length(P) - 1)): length(P) - 2 <= j
  have sigma_all_parts_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_all_parts_TCC3
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b), j: below(length(P) - 1)):
  --      j + 1 < length(P)
  have sigma_all_parts_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_all_parts_TCC4
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b), j: below(length(P) - 1)): j < length(P)
  have sigma_all_parts_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sigma_all_parts_TCC5
  -- Statement: FORALL (a, b: T):
  --   a < b IMPLIES
  --    FORALL (P: partition[T](a, b)):
  --      FORALL (x, y: below(length(P) - 1)), (z: integer):
  --        x <= z AND z <= y IMPLIES z >= 0 AND z < length(P) - 1
  have sigma_all_parts_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.steps_exist
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES
--  (integrable?(a, b, f) IMPLIES
--    (FORALL eps:
--       EXISTS (f1, f2):
--              step_function?(a, b, f1) AND step_function?(a, b, f2)
--          AND (FORALL (x: closed_interval(a, b)):
-- 
theorem steps_exist : a < b → (integrable_p a b f →
  (∀ eps, 
     ∃ (f1 : _) (f2 : _), step_function_p a b f1 ∧ step_function_p a b f2
        ∧ (∀ (x : closed_interval a b), f1 x ≤ f x ∧ f x ≤ f2 x)
        ∧ integrable_p a b (f2 - f1) ∧ (integral a b (f2 - f1) < eps))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_scaf.partition_exists
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: a < b IMPLIES (EXISTS (P: partition[T](a, b)): width(a, b, P) < delta)
theorem partition_exists : a < b → (∃ (P : partition (a, b)), width a b P < delta) := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.integral_split_scaf