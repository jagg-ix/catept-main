-- Auto-generated by pvs2lean/translator.py
-- PVS theory: strat_util
-- PVSLib library: Bernstein
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: strat_util

Auto-generated stubs from PVSLib theory `strat_util`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.strat_util

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Counterexample : pvs_τ1 → pvs_τ2)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (a2l__multipolynomial_minmax : pvs_τ9 → pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14 → pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (bounded_points_exclusive_p : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (boxbetween_p : pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} (counterexample : pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (eps_localexit : pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (forall_X_poly_interval : pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39 → pvs_τ40 → pvs_τ41 → pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} (isfalse_p : pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} (istrue_p : pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (lt_below_p : pvs_τ46 → pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} (multipoly_eval : pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52 → pvs_τ53 → pvs_τ54)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} {pvs_τ79 : Type*} (multipolynomial_minmax : pvs_τ67 → pvs_τ68 → pvs_τ69 → pvs_τ70 → pvs_τ71 → pvs_τ72 → pvs_τ73 → pvs_τ74 → pvs_τ75 → pvs_τ76 → pvs_τ77 → pvs_τ78 → pvs_τ79 → Prop)
variable {pvs_τ80 : Type*} {pvs_τ81 : Type*} (rel : pvs_τ80 → pvs_τ81 → Prop)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} (rel_counterex : pvs_τ82 → pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} {pvs_τ86 : Type*} {pvs_τ87 : Type*} (rel_globalexit : pvs_τ84 → pvs_τ85 → pvs_τ86 → pvs_τ87)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (rel_localexit : pvs_τ88 → pvs_τ89 → Prop)
variable {pvs_τ90 : Type*} {pvs_τ91 : Type*} {pvs_τ92 : Type*} (represents : pvs_τ90 → pvs_τ91 → pvs_τ92)
variable {pvs_τ93 : Type*} {pvs_τ94 : Type*} {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} {pvs_τ98 : Type*} {pvs_τ99 : Type*} {pvs_τ100 : Type*} {pvs_τ101 : Type*} (sound_poly_fin_p : pvs_τ93 → pvs_τ94 → pvs_τ95 → pvs_τ96 → pvs_τ97 → pvs_τ98 → pvs_τ99 → pvs_τ100 → pvs_τ101 → Prop)
variable {pvs_τ102 : Type*} {pvs_τ103 : Type*} {pvs_τ104 : Type*} {pvs_τ105 : Type*} {pvs_τ106 : Type*} {pvs_τ107 : Type*} {pvs_τ108 : Type*} {pvs_τ109 : Type*} {pvs_τ110 : Type*} (sound_poly_p : pvs_τ102 → pvs_τ103 → pvs_τ104 → pvs_τ105 → pvs_τ106 → pvs_τ107 → pvs_τ108 → pvs_τ109 → pvs_τ110 → Prop)

-- pvs2lean: strat_util.multipoly_minmax
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: multipolynomial_minmax(mp`mpoly, mp`mdeg, mv`numvars, mp`terms, mp`mcoeff,
--                        mv`vars_lb, mv`vars_ub, depth,
--                        eps_localexit(precision), false_globalexit,
--     
axiom multipoly_minmax {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} : pvs_τ55 → pvs_τ56 → pvs_τ57 → pvs_τ58 → pvs_τ59 → pvs_τ60

-- pvs2lean: strat_util.multipoly_minmax_sound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mp: MPoly, mv: MVars, depth, varselect, precision):
--   lt_below?(mv`numvars)(mv`vars_lb, mv`vars_ub) IMPLIES
--    sound_poly_fin?(mp`mpoly, mp`mdeg, mv`numvars, mp`terms, mp`mcoeff,
--              
theorem multipoly_minmax_sound : ∀ (mp : ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × {i : {i : ℝ // i ≥ 0} // i > 0}) (mv : ({i : ℝ // i ≥ 0} → Bool × Bool) × ({i : ℝ // i ≥ 0} → Bool × Bool) × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → ℝ)) (depth : {i : ℝ // i ≥ 0}) (varselect : ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0}) (precision : {x : {x : ℝ // x ≥ 0} // x > 0}), lt_below_p mv.numvars(mv.vars_lb, mv.vars_ub) → sound_poly_fin_p mp.mpoly mp.mdeg mv.numvars mp.terms mp.mcoeff mv.vars_lb mv.vars_ub (fun (i : ℕ)=> (True, True)) mv.iepts
               (multipoly_minmax mp mv depth varselect precision) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: strat_util.multipoly_strategy
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET omm =
--       multipolynomial_minmax(mpr`mpoly, mpr`mdeg, mv`numvars, mpr`terms,
--                              mpr`mcoeff, mv`vars_lb, mv`vars_ub, depth,
--                              rel_localexit(mp
axiom multipoly_strategy {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} : pvs_τ61 → pvs_τ62 → pvs_τ63 → pvs_τ64 → pvs_τ65 → pvs_τ66

-- pvs2lean: strat_util.multipoly_strategy_true
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mpr: MPolyRel, mv: MVars, depth, varselect):
--   lt_below?(mv`numvars)(mv`vars_lb, mv`vars_ub) AND
--    bounded_points_exclusive?(mv`numvars)(mv`bdpts) AND
--     istrue?(multipoly_strategy(mpr, mv, 
theorem multipoly_strategy_true : ∀ (mpr : ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × (ℝ × ℝ → Bool) × {i : {i : ℝ // i ≥ 0} // i > 0}) (mv : ({i : ℝ // i ≥ 0} → Bool × Bool) × ({i : ℝ // i ≥ 0} → Bool × Bool) × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → ℝ)) (depth : {i : ℝ // i ≥ 0}) (varselect : ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0}), lt_below_p mv.numvars(mv.vars_lb, mv.vars_ub) ∧
 bounded_points_exclusive_p mv.numvars(mv.bdpts) ∧
  istrue_p (multipoly_strategy mpr mv depth varselect True) → forall_X_poly_interval mpr.mpoly mpr.mdeg mpr.mcoeff mv.numvars mpr.terms mv.vars_lb mv.vars_ub mv.iepts mv.bdpts mpr.rel 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: strat_util.multipoly_strategy_false
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mpr: MPolyRel, mv: MVars, depth, varselect):
--   lt_below?(mv`numvars)(mv`vars_lb, mv`vars_ub) AND
--    bounded_points_exclusive?(mv`numvars)(mv`bdpts) AND
--     isfalse?(multipoly_strategy(mpr, mv,
theorem multipoly_strategy_false : ∀ (mpr : ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × (ℝ × ℝ → Bool) × {i : {i : ℝ // i ≥ 0} // i > 0}) (mv : ({i : ℝ // i ≥ 0} → Bool × Bool) × ({i : ℝ // i ≥ 0} → Bool × Bool) × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → ℝ)) (depth : {i : ℝ // i ≥ 0}) (varselect : ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0}), lt_below_p mv.numvars(mv.vars_lb, mv.vars_ub) ∧
 bounded_points_exclusive_p mv.numvars(mv.bdpts) ∧
  isfalse_p (multipoly_strategy mpr mv depth varselect False) → ¬ forall_X_poly_interval mpr.mpoly mpr.mdeg mpr.mcoeff mv.numvars mpr.terms mv.vars_lb mv.vars_ub mv.iepts mv.bdpts mpr.rel 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: strat_util.multipoly_strategy_counterexample
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mpr: MPolyRel, mv: MVars, depth, varselect):
--   LET mpstrat = multipoly_strategy(mpr, mv, depth, varselect, FALSE) IN
--     lt_below?(mv`numvars)(mv`vars_lb, mv`vars_ub) AND
--      bounded_points_e
theorem multipoly_strategy_counterexample : ∀ (mpr : ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × (ℝ × ℝ → Bool) × {i : {i : ℝ // i ≥ 0} // i > 0}) (mv : ({i : ℝ // i ≥ 0} → Bool × Bool) × ({i : ℝ // i ≥ 0} → Bool × Bool) × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → ℝ)) (depth : {i : ℝ // i ≥ 0}) (varselect : ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0}), fun (mpstrat : Outcome) => lt_below_p mv.numvars(mv.vars_lb, mv.vars_ub) ∧
 bounded_points_exclusive_p mv.numvars(mv.bdpts) ∧ isfalse_p mpstrat → (¬ mpr.rel
         (multipoly_eval mpr.mpoly mpr.mdeg mpr.mcoeff mv.numvars mpr.terms
                        (list2array 0(counterexample mpstrat)),
          0))
 ∧
 boxbetween_p mv.numvars(mv.vars_lb, mv.vars_ub, mv.iepts, mv.bdpts)
            (list2array 0(counterexample mpstrat)) (multipoly_strategy mpr mv depth varselect False) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: strat_util.a2l__multipoly_strategy
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET omm =
--       a2l__multipolynomial_minmax(mpr`mpoly, mpr`mdeg, mv`numvars,
--                                   mpr`terms, mpr`mcoeff, mv`vars_lb,
--                                   mv`vars_ub, depth,
--  
axiom a2l__multipoly_strategy {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8

-- pvs2lean: strat_util.a2l__multipoly_strategy__equivalence
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mpr: MPolyRel, mv: MVars, varselect,
--         (a2l__varselect: a2l__VarSelector
--              | represents(a2l__varselect, varselect))):
--   a2l__multipoly_strategy(mpr, mv, depth, a2l__varselect,
theorem a2l__multipoly_strategy__equivalence : ∀ (mpr : ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × (ℝ × ℝ → Bool) × {i : {i : ℝ // i ≥ 0} // i > 0}) (mv : ({i : ℝ // i ≥ 0} → Bool × Bool) × ({i : ℝ // i ≥ 0} → Bool × Bool) × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → ℝ)) (varselect : ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0}) (a2l__varselect : {a2l__varselect : {x : List (List (List ℝ)) // every (fun (x : list (List ℝ))=> every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
                     (x))
             (x))
     (x)} × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0} // represents a2l__varselect varselect}), a2l__multipoly_strategy mpr mv depth a2l__varselect exitatdepth = multipoly_strategy mpr mv depth varselect exitatdepth := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: strat_util.a2l__multipoly_strategy_true
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mpr: MPolyRel, mv: MVars, depth, a2l__varselect):
--   lt_below?(mv`numvars)(mv`vars_lb, mv`vars_ub) AND
--    bounded_points_exclusive?(mv`numvars)(mv`bdpts) AND
--     istrue?(a2l__multipoly_strategy
theorem a2l__multipoly_strategy_true : ∀ (mpr : ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × (ℝ × ℝ → Bool) × {i : {i : ℝ // i ≥ 0} // i > 0}) (mv : ({i : ℝ // i ≥ 0} → Bool × Bool) × ({i : ℝ // i ≥ 0} → Bool × Bool) × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → ℝ)) (depth : {i : ℝ // i ≥ 0}) (a2l__varselect : {x : List (List (List ℝ)) // every (fun (x : list (List ℝ))=> every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
                     (x))
             (x))
     (x)} × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0}), lt_below_p mv.numvars(mv.vars_lb, mv.vars_ub) ∧
 bounded_points_exclusive_p mv.numvars(mv.bdpts) ∧
  istrue_p (a2l__multipoly_strategy mpr mv depth a2l__varselect True) → forall_X_poly_interval mpr.mpoly mpr.mdeg mpr.mcoeff mv.numvars mpr.terms mv.vars_lb mv.vars_ub mv.iepts mv.bdpts mpr.rel 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: strat_util.a2l__multipoly_strategy_false
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mpr: MPolyRel, mv: MVars, depth, varselect,
--         (a2l__varselect: a2l__VarSelector
--              | represents(a2l__varselect, varselect))):
--   lt_below?(mv`numvars)(mv`vars_lb, mv`vars_ub) AN
theorem a2l__multipoly_strategy_false : ∀ (mpr : ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × (ℝ × ℝ → Bool) × {i : {i : ℝ // i ≥ 0} // i > 0}) (mv : ({i : ℝ // i ≥ 0} → Bool × Bool) × ({i : ℝ // i ≥ 0} → Bool × Bool) × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → ℝ)) (depth : {i : ℝ // i ≥ 0}) (varselect : ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0}) (a2l__varselect : {a2l__varselect : {x : List (List (List ℝ)) // every (fun (x : list (List ℝ))=> every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
                     (x))
             (x))
     (x)} × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0} // represents a2l__varselect varselect}), lt_below_p mv.numvars(mv.vars_lb, mv.vars_ub) ∧
 bounded_points_exclusive_p mv.numvars(mv.bdpts) ∧
  isfalse_p (a2l__multipoly_strategy mpr mv depth a2l__varselect False) → ¬ forall_X_poly_interval mpr.mpoly mpr.mdeg mpr.mcoeff mv.numvars mpr.terms mv.vars_lb mv.vars_ub mv.iepts mv.bdpts mpr.rel 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: strat_util.a2l__multipoly_strategy_counterexample
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (mpr: MPolyRel, mv: MVars, depth, varselect,
--         (a2l__varselect: a2l__VarSelector
--              | represents(a2l__varselect, varselect))):
--   LET mpstrat =
--         a2l__multipoly_strategy(mp
theorem a2l__multipoly_strategy_counterexample : ∀ (mpr : ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × (ℝ × ℝ → Bool) × {i : {i : ℝ // i ≥ 0} // i > 0}) (mv : ({i : ℝ // i ≥ 0} → Bool × Bool) × ({i : ℝ // i ≥ 0} → Bool × Bool) × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → ℝ)) (depth : {i : ℝ // i ≥ 0}) (varselect : ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} → ℝ) × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0}) (a2l__varselect : {a2l__varselect : {x : List (List (List ℝ)) // every (fun (x : list (List ℝ))=> every (fun (x : List ℝ)=> every (fun (x : ℝ)=> number_field_pred x ∧ real_pred x)
                     (x))
             (x))
     (x)} × ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) × {i : {i : ℝ // i ≥ 0} // i > 0} × {i : {i : ℝ // i ≥ 0} // i > 0} × ({i : ℝ // i ≥ 0} → ℝ) × {i : ℝ // i ≥ 0} → Bool × {i : ℝ // i ≥ 0} // represents a2l__varselect varselect}), fun (mpstrat : Outcome) => lt_below_p mv.numvars(mv.vars_lb, mv.vars_ub) ∧
 bounded_points_exclusive_p mv.numvars(mv.bdpts) ∧ isfalse_p mpstrat → (¬ mpr.rel
         (multipoly_eval mpr.mpoly mpr.mdeg mpr.mcoeff mv.numvars mpr.terms
                        (list2array 0(counterexample mpstrat)),
          0))
 ∧
 boxbetween_p mv.numvars(mv.vars_lb, mv.vars_ub, mv.iepts, mv.bdpts)
            (list2array 0(counterexample mpstrat)) (a2l__multipoly_strategy mpr mv depth a2l__varselect False) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.strat_util