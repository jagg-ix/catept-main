-- Auto-generated by pvs2lean/translator.py
-- PVS theory: arithmetic_poly
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: arithmetic_poly

Auto-generated stubs from PVSLib theory `arithmetic_poly`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.arithmetic_poly

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (car : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (cdr : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (chop : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (cons : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (cons_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (eval : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (init_eval : pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ30 : Type*} (last : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} (length : pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (max : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} (null_p : pvs_τ35 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (standard_form : pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} (standard_form_p : pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (upfrom : pvs_τ45 → pvs_τ46)

-- pvs2lean: arithmetic_poly.init_polyplus
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(f) THEN g
-- ELSIF null?(g) THEN f
-- ELSE cons(car(f) + car(g), init_polyplus(cdr(f), cdr(g)))
-- ENDIF
axiom init_polyplus {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ22 → pvs_τ23 → Prop

-- pvs2lean: arithmetic_poly.init_polyscale
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(f) THEN f ELSE cons(r * car(f), init_polyscale(r, cdr(f))) ENDIF
axiom init_polyscale {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26

-- pvs2lean: arithmetic_poly.init_polymult
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(f) OR null?(g) THEN null
-- ELSIF length(f) = 1 THEN init_polyscale(car(f), g)
-- ELSE init_polyplus(init_polyscale(car(f), g),
--                    init_polymult(cdr(f), cons(0, g)))
-- ENDIF
axiom init_polymult {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21

-- pvs2lean: arithmetic_poly.init_polyexp
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(f) THEN null
-- ELSIF k = 0 THEN (: 1 :)
-- ELSE init_polymult(f, init_polyexp(f, k - 1))
-- ENDIF
axiom init_polyexp {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18

-- pvs2lean: arithmetic_poly.init_polysub
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: init_polyplus(f, init_polyscale(-1, g))
axiom init_polysub {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} : pvs_τ27 → pvs_τ28 → pvs_τ29

-- pvs2lean: arithmetic_poly.assoc_init_polyplus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g, h: Polynomial):
--   init_polyplus(init_polyplus(f, g), h) =
--    init_polyplus(f, init_polyplus(g, h))
theorem assoc_init_polyplus : ∀ (f : List ℝ) (g : List ℝ) (h : List ℝ), init_polyplus (init_polyplus f g) h = init_polyplus f (init_polyplus g h) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.poly_scale_assoc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r1, r2: real, f: Polynomial):
--   init_polyscale(r1, init_polyscale(r2, f)) = init_polyscale(r1 * r2, f)
theorem poly_scale_assoc : ∀ (r1 : ℝ) (r2 : ℝ) (f : List ℝ), init_polyscale r1 (init_polyscale r2 f) = init_polyscale (r1 * r2) f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.poly_scale_dist1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: real, f, g: Polynomial):
--   init_polyscale(r, init_polyplus(f, g)) =
--    init_polyplus(init_polyscale(r, f), init_polyscale(r, g))
theorem poly_scale_dist1 : ∀ (r : ℝ) (f : List ℝ) (g : List ℝ), init_polyscale r (init_polyplus f g) = init_polyplus (init_polyscale r f) (init_polyscale r g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.poly_scale_dist2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r1, r2: real, f: Polynomial):
--   init_polyscale(r1 + r2, f) =
--    init_polyplus(init_polyscale(r1, f), init_polyscale(r2, f))
theorem poly_scale_dist2 : ∀ (r1 : ℝ) (r2 : ℝ) (f : List ℝ), init_polyscale (r1 + r2) f = init_polyplus (init_polyscale r1 f) (init_polyscale r2 f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.poly_scale_mult1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: real, f, g: Polynomial):
--   init_polyscale(r, init_polymult(f, g)) =
--    init_polymult(init_polyscale(r, f), g)
theorem poly_scale_mult1 : ∀ (r : ℝ) (f : List ℝ) (g : List ℝ), init_polyscale r (init_polymult f g) = init_polymult (init_polyscale r f) g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.poly_scale_one
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: Polynomial): init_polyscale(1, f) = f
theorem poly_scale_one : ∀ (f : List ℝ), init_polyscale 1 f = f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.last_plus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (cons?), g: {gg: Polynomial | length(gg) = length(f)}):
--   last(init_polyplus(f, g)) = last(f) + last(g)
theorem last_plus : ∀ (f : List ℝ) (g : {gg : List ℝ // List.length gg = List.length f}), last (init_polyplus f g) = last f + last g := by
  -- === TCCs for last_plus ===
  -- TCC: last_plus_TCC1
  -- Statement: FORALL (f: (cons?[real]),
  --         g: {gg: Polynomial | length[real](gg) = length[real](f)}):
  --   cons?[real](init_polyplus(f, g))
  have last_plus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: last_plus_TCC2
  -- Statement: FORALL (f: (cons?[real]),
  --         g: {gg: Polynomial | length[real](gg) = length[real](f)}):
  --   cons?[real](g)
  have last_plus_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.last_plus1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (cons?), g: {gg: Polynomial | length(gg) > length(f)}):
--   last(init_polyplus(f, g)) = last(g)
theorem last_plus1 : ∀ (f : List ℝ) (g : {gg : List ℝ // List.length gg > List.length f}), last (init_polyplus f g) = last g := by
  -- === TCCs for last_plus1 ===
  -- TCC: last_plus1_TCC1
  -- Statement: FORALL (f: (cons?[real]),
  --         g: {gg: Polynomial | length[real](gg) > length[real](f)}):
  --   cons?[real](init_polyplus(f, g))
  have last_plus1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: last_plus1_TCC2
  -- Statement: FORALL (f: (cons?[real]),
  --         g: {gg: Polynomial | length[real](gg) > length[real](f)}):
  --   cons?[real](g)
  have last_plus1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.last_scale
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: real, g: {ff: Polynomial | cons?(ff)}):
--   c * last(g) = last(init_polyscale(c, g))
theorem last_scale : ∀ (c : ℝ) (g : {ff : List ℝ // cons_p ff}), c * last g = last (init_polyscale c g) := by
  -- === TCCs for last_scale ===
  -- TCC: last_scale_TCC1
  -- Statement: FORALL (c: real, g: {ff: Polynomial | cons?[real](ff)}):
  --   cons?[real](init_polyscale(c, g))
  have last_scale_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.last_mult
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: {ff: Polynomial | cons?(ff)}):
--   last(f) * last(g) = last(init_polymult(f, g))
theorem last_mult : ∀ (f : {ff : List ℝ // cons_p ff}) (g : {ff : List ℝ // cons_p ff}), last f * last g = last (init_polymult f g) := by
  -- === TCCs for last_mult ===
  -- TCC: last_mult_TCC1
  -- Statement: FORALL (f, g: {ff: Polynomial | cons?[real](ff)}):
  --   cons?[real](init_polymult(f, g))
  have last_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.polyplus
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: standard_form(init_polyplus(f, g))
axiom polyplus {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41

-- pvs2lean: arithmetic_poly.polyscale
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: standard_form(init_polyscale(r, f))
noncomputable def polyscale : sorry := by
  sorry  -- pvs2lean: definition stub for standard_form(init_polyscale(r, f))

-- pvs2lean: arithmetic_poly.polymult
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: standard_form(init_polymult(f, g))
axiom polymult {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ36 → pvs_τ37 → pvs_τ38

-- pvs2lean: arithmetic_poly.standard_polymult
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: (standard_form?)): standard_form?(init_polymult(f, g))
theorem standard_polymult : ∀ (f : List ℝ) (g : List ℝ), standard_form_p (init_polymult f g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.reflex_init
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial): init_polyplus(f, g) = init_polyplus(g, f)
theorem reflex_init : ∀ (f : List ℝ) (g : List ℝ), init_polyplus f g = init_polyplus g f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.reflex_init1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial): polyplus(f, g) = polyplus(g, f)
theorem reflex_init1 : ∀ (f : List ℝ) (g : List ℝ), polyplus f g = polyplus g f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.add_car
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: {ff: Polynomial | cons?(ff)}):
--   car(init_polyplus(f, g)) = car(f) + car(g)
theorem add_car : ∀ (f : {ff : List ℝ // cons_p ff}) (g : {ff : List ℝ // cons_p ff}), car (init_polyplus f g) = car f + car g := by
  -- === TCCs for add_car ===
  -- TCC: add_car_TCC1
  -- Statement: FORALL (f, g: {ff: Polynomial | cons?[real](ff)}):
  --   cons?[real](init_polyplus(f, g))
  have add_car_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.mult_car
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: {ff: Polynomial | cons?(ff)}):
--   car(init_polymult(f, g)) = car(f) * car(g)
theorem mult_car : ∀ (f : {ff : List ℝ // cons_p ff}) (g : {ff : List ℝ // cons_p ff}), car (init_polymult f g) = car f * car g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.zero_p
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN null ELSE cons(0, zero_p(n - 1)) ENDIF
axiom zero_p {pvs_τ47 : Type*} : pvs_τ47 → Prop

-- pvs2lean: arithmetic_poly.zero_eval
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, x: real, deg: upfrom(n - 1), acc: real):
--   init_eval(zero_p(n), deg, acc)(x) = acc
theorem zero_eval : ∀ (n : {i : ℝ // i ≥ 0}) (x : ℝ) (deg : {s : ℝ // s ≥ n - 1}) (acc : ℝ), init_eval (zero_p n) deg acc(x) = acc := by
  -- === TCCs for zero_eval ===
  -- TCC: zero_eval_TCC1
  -- Statement: FORALL (n: nat, deg: upfrom(n - 1)): deg >= length[real](zero_p(n)) - 1
  have zero_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.last_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat): last(zero_p(n)) = 0
theorem last_zero : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}), last (zero_p n) = 0 := by
  -- === TCCs for last_zero ===
  -- TCC: last_zero_TCC1
  -- Statement: FORALL (n: posnat): cons?[real](zero_p(n))
  have last_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.chop_zero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat): chop(zero_p(n)) = zero_p(n - 1)
theorem chop_zero : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}), chop (zero_p n) = zero_p (n - 1) := by
  -- === TCCs for chop_zero ===
  -- TCC: chop_zero_TCC1
  -- Statement: FORALL (n: posnat): n - 1 >= 0
  have chop_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_zero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat): standard_form(zero_p(n)) = null
theorem standard_zero : ∀ (n : {i : ℝ // i ≥ 0}), standard_form (zero_p n) = null := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.zero_p_props
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat): last(zero_p(n)) = 0
theorem zero_p_props : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}), last (zero_p n) = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.zero_p_props1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat): chop(zero_p(n)) = zero_p(n - 1)
theorem zero_p_props1 : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}), chop (zero_p n) = zero_p (n - 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.zero_p_init_polyplus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: Polynomial):
--   FORALL (i: nat): i <= length(f) IMPLIES init_polyplus(zero_p(i), f) = f
theorem zero_p_init_polyplus : ∀ (f : List ℝ), ∀ (i : {i : ℝ // i ≥ 0}), i ≤ List.length f → init_polyplus (zero_p i) f = f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.zero_p_scal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (g: Polynomial): init_polyscale(0, g) = zero_p(length(g))
theorem zero_p_scal : ∀ (g : List ℝ), init_polyscale 0 g = zero_p (List.length g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.zero_p_scal1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: real, i: nat): init_polyscale(r, zero_p(i)) = zero_p(i)
theorem zero_p_scal1 : ∀ (r : ℝ) (i : {i : ℝ // i ≥ 0}), init_polyscale r (zero_p i) = zero_p i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.zero_p_mult
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: posnat, g: {gg: Polynomial | cons?(gg)}):
--   init_polymult(zero_p(i), g) = zero_p(i + length(g) - 1)
theorem zero_p_mult : ∀ (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (g : {gg : List ℝ // cons_p gg}), init_polymult (zero_p i) g = zero_p (i + List.length g - 1) := by
  -- === TCCs for zero_p_mult ===
  -- TCC: zero_p_mult_TCC1
  -- Statement: FORALL (i: posnat, g: {gg: Polynomial | cons?[real](gg)}):
  --   i + length[real](g) - 1 >= 0
  have zero_p_mult_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.zero_p_multr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: posnat, g: {gg: Polynomial | cons?(gg)}):
--   init_polymult(g, zero_p(i)) = zero_p(i + length(g) - 1)
theorem zero_p_multr : ∀ (i : {i : {i : ℝ // i ≥ 0} // i > 0}) (g : {gg : List ℝ // cons_p gg}), init_polymult g (zero_p i) = zero_p (i + List.length g - 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.cons_init_poly_plus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: real, f, g: Polynomial):
--   cons(r, init_polyplus(f, g)) = init_polyplus(cons(r, f), cons(0, g))
theorem cons_init_poly_plus : ∀ (r : ℝ) (f : List ℝ) (g : List ℝ), cons r (init_polyplus f g) = init_polyplus (cons r f) (cons 0 g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.poly_mult_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: {gg: Polynomial | cons?(gg)}):
--   cons?(init_polymult(f, g)) IMPLIES
--    cons(0, init_polymult(f, g)) = init_polymult(cons(0, f), g)
theorem poly_mult_cons : ∀ (f : {gg : List ℝ // cons_p gg}) (g : {gg : List ℝ // cons_p gg}), cons_p (init_polymult f g) → cons 0 (init_polymult f g) = init_polymult (cons 0 f) g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.chop_last_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: {ff: Polynomial | cons?(ff)}):
--   (last(f) = last(g) AND chop(f) = chop(g)) IFF f = g
theorem chop_last_equiv : ∀ (f : {ff : List ℝ // cons_p ff}) (g : {ff : List ℝ // cons_p ff}), (last f = last g ∧ chop f = chop g) ↔ f = g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, f: {ff: Polynomial | length(ff) = n}):
--   null?(standard_form(f)) IFF f = zero_p(n)
theorem standard_null : ∀ (n : {i : ℝ // i ≥ 0}) (f : {ff : List ℝ // List.length ff = n}), null_p (standard_form f) ↔ f = zero_p n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_form_polyscale
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: real, f: Polynomial):
--   standard_form(init_polyscale(r, f)) =
--    standard_form(init_polyscale(r, standard_form(f)))
theorem standard_form_polyscale : ∀ (r : ℝ) (f : List ℝ), standard_form (init_polyscale r f) = standard_form (init_polyscale r (standard_form f)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.chop_plus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | NOT standard_form?(ff)}, g: Polynomial):
--   polyplus(f, g) = polyplus(chop(f), g)
theorem chop_plus : ∀ (f : {ff : List ℝ // ¬ standard_form_p ff}) (g : List ℝ), polyplus f g = polyplus (chop f) g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_eliminatel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial):
--   polyplus(f, g) = standard_form(init_polyplus(standard_form(f), g))
theorem standard_eliminatel : ∀ (f : List ℝ) (g : List ℝ), polyplus f g = standard_form (init_polyplus (standard_form f) g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_eliminater
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial):
--   polyplus(f, g) = standard_form(init_polyplus(f, standard_form(g)))
theorem standard_eliminater : ∀ (f : List ℝ) (g : List ℝ), polyplus f g = standard_form (init_polyplus f (standard_form g)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_eliminate1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial):
--   polyplus(f, g) =
--    standard_form(init_polyplus(standard_form(f), standard_form(g)))
theorem standard_eliminate1 : ∀ (f : List ℝ) (g : List ℝ), polyplus f g = standard_form (init_polyplus (standard_form f) (standard_form g)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_eliminate_multl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial):
--   polymult(f, g) = standard_form(init_polymult(standard_form(f), g))
theorem standard_eliminate_multl : ∀ (f : List ℝ) (g : List ℝ), polymult f g = standard_form (init_polymult (standard_form f) g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_eliminate_multr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial):
--   polymult(f, g) = standard_form(init_polymult(f, standard_form(g)))
theorem standard_eliminate_multr : ∀ (f : List ℝ) (g : List ℝ), polymult f g = standard_form (init_polymult f (standard_form g)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_eliminate_mult1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial):
--   polymult(f, g) =
--    standard_form(init_polymult(standard_form(f), standard_form(g)))
theorem standard_eliminate_mult1 : ∀ (f : List ℝ) (g : List ℝ), polymult f g = standard_form (init_polymult (standard_form f) (standard_form g)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.standard_polymult2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial):
--   standard_form(init_polymult(f, g)) =
--    init_polymult(standard_form(f), standard_form(g))
theorem standard_polymult2 : ∀ (f : List ℝ) (g : List ℝ), standard_form (init_polymult f g) = init_polymult (standard_form f) (standard_form g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.Poly_one
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: standard_form((: 1 :))
noncomputable def Poly_one : sorry := by
  sorry  -- pvs2lean: definition stub for standard_form((: 1 :))

-- pvs2lean: arithmetic_poly.Poly_zero
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null[real]
noncomputable def Poly_zero : sorry := by
  sorry  -- pvs2lean: definition stub for null[real]

-- pvs2lean: arithmetic_poly.assocpr_init
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3: Polynomial):
--   init_polyplus(init_polyplus(t1, t2), t3) =
--    init_polyplus(t1, init_polyplus(t2, t3))
theorem assocpr_init : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ), init_polyplus (init_polyplus t1 t2) t3 = init_polyplus t1 (init_polyplus t2 t3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.assocpr1_init
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3: Polynomial):
--   init_polyplus(init_polyplus(t1, t2), t3) =
--    init_polyplus(init_polyplus(t1, t3), t2)
theorem assocpr1_init : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ), init_polyplus (init_polyplus t1 t2) t3 = init_polyplus (init_polyplus t1 t3) t2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.assocpr2_init
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3: Polynomial):
--   init_polyplus(t1, init_polyplus(t2, t3)) =
--    init_polyplus(t2, init_polyplus(t1, t3))
theorem assocpr2_init : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ), init_polyplus t1 (init_polyplus t2 t3) = init_polyplus t2 (init_polyplus t1 t3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.assocpr3_init
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3, t4: Polynomial):
--   init_polyplus(init_polyplus(t1, t2), init_polyplus(t3, t4)) =
--    init_polyplus(init_polyplus(t1, t3), init_polyplus(t2, t4))
theorem assocpr3_init : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ) (t4 : List ℝ), init_polyplus (init_polyplus t1 t2) (init_polyplus t3 t4) = init_polyplus (init_polyplus t1 t3) (init_polyplus t2 t4) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.assocpr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3: (standard_form?)):
--   polyplus(polyplus(t1, t2), t3) = polyplus(t1, polyplus(t2, t3))
theorem assocpr : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ), polyplus (polyplus t1 t2) t3 = polyplus t1 (polyplus t2 t3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.poly_plus_mult1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g, h: Polynomial):
--   init_polyplus(init_polymult(f, h), init_polymult(g, h)) =
--    init_polymult(init_polyplus(f, g), h)
theorem poly_plus_mult1 : ∀ (f : List ℝ) (g : List ℝ) (h : List ℝ), init_polyplus (init_polymult f h) (init_polymult g h) = init_polymult (init_polyplus f g) h := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.poly_plus_mult2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g, h: Polynomial):
--   init_polyplus(init_polymult(f, g), init_polymult(f, h)) =
--    init_polymult(f, init_polyplus(g, h))
theorem poly_plus_mult2 : ∀ (f : List ℝ) (g : List ℝ) (h : List ℝ), init_polyplus (init_polymult f g) (init_polymult f h) = init_polymult f (init_polyplus g h) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.abelianpr_init
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2: Polynomial): init_polyplus(t1, t2) = init_polyplus(t2, t1)
theorem abelianpr_init : ∀ (t1 : List ℝ) (t2 : List ℝ), init_polyplus t1 t2 = init_polyplus t2 t1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.abelianpr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2: (standard_form?)): polyplus(t1, t2) = polyplus(t2, t1)
theorem abelianpr : ∀ (t1 : List ℝ) (t2 : List ℝ), polyplus t1 t2 = polyplus t2 t1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.assocmultpr_init
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3: Polynomial):
--   init_polymult(t1, init_polymult(t2, t3)) =
--    init_polymult(init_polymult(t1, t2), t3)
theorem assocmultpr_init : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ), init_polymult t1 (init_polymult t2 t3) = init_polymult (init_polymult t1 t2) t3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.assocmultpr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3: (standard_form?)):
--   polymult(polymult(t1, t2), t3) = polymult(t1, polymult(t2, t3))
theorem assocmultpr : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ), polymult (polymult t1 t2) t3 = polymult t1 (polymult t2 t3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.distl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3: (standard_form?)):
--   polymult(t1, polyplus(t2, t3)) =
--    polyplus(polymult(t1, t2), polymult(t1, t3))
theorem distl : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ), polymult t1 (polyplus t2 t3) = polyplus (polymult t1 t2) (polymult t1 t3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.distr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (t1, t2, t3: (standard_form?)):
--   polymult(polyplus(t1, t2), t3) =
--    polyplus(polymult(t1, t3), polymult(t2, t3))
theorem distr : ∀ (t1 : List ℝ) (t2 : List ℝ) (t3 : List ℝ), polymult (polyplus t1 t2) t3 = polyplus (polymult t1 t3) (polymult t2 t3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.eval_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial): eval(init_polyplus(f, g)) = eval(f) + eval(g)
theorem eval_add : ∀ (f : List ℝ) (g : List ℝ), eval (init_polyplus f g) = eval f + eval g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.init_eval_scal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, r: real, f: Polynomial, deg: upfrom(length(f) - 1), acc: real):
--   init_eval(init_polyscale(r, f), deg, r * acc)(x) =
--    init_eval(f, deg, acc)(x) * r
theorem init_eval_scal : ∀ (x : ℝ) (r : ℝ) (f : List ℝ) (deg : {s : ℝ // s ≥ List.length f - 1}) (acc : ℝ), init_eval (init_polyscale r f) deg (r * acc) (x) = init_eval f deg acc(x) * r := by
  -- === TCCs for init_eval_scal ===
  -- TCC: init_eval_scal_TCC1
  -- Statement: FORALL (r: real, f: Polynomial, deg: upfrom(length[real](f) - 1)):
  --   deg >= length[real](init_polyscale(r, f)) - 1
  have init_eval_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.eval_scale
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, r: real, f: Polynomial):
--   eval(init_polyscale(r, f))(x) = eval(f)(x) * r
theorem eval_scale : ∀ (x : ℝ) (r : ℝ) (f : List ℝ), eval (init_polyscale r f) (x) = eval f(x) * r := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.eval_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial): eval(init_polysub(f, g)) = eval(f) - eval(g)
theorem eval_sub : ∀ (f : List ℝ) (g : List ℝ), eval (init_polysub f g) = eval f - eval g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.sub_zerop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: Polynomial, g: {gg: Polynomial | length(gg) = length(f)}):
--   init_polysub(f, g) = zero_p(length(f)) IFF f = g
theorem sub_zerop : ∀ (f : List ℝ) (g : {gg : List ℝ // List.length gg = List.length f}), init_polysub f g = zero_p (List.length f) ↔ f = g := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.eval_prod
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial, x: real):
--   eval(init_polymult(f, g))(x) = eval(f)(x) * eval(g)(x)
theorem eval_prod : ∀ (f : List ℝ) (g : List ℝ) (x : ℝ), eval (init_polymult f g) (x) = eval f(x) * eval g(x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.eval_zero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: Polynomial):
--   f = zero_p(length(f)) IFF (eval(f) = LAMBDA (x: real): 0)
theorem eval_zero : ∀ (f : List ℝ), f = zero_p (List.length f) ↔ (eval f = fun (x : ℝ)=> 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.eval_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: Polynomial, g: {gg: Polynomial | length(gg) = length(f)}):
--   f = g IFF eval(f) = eval(g)
theorem eval_eq : ∀ (f : List ℝ) (g : {gg : List ℝ // List.length gg = List.length f}), f = g ↔ eval f = eval g := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: arithmetic_poly.mult_commute
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f, g: Polynomial): init_polymult(f, g) = init_polymult(g, f)
theorem mult_commute : ∀ (f : List ℝ) (g : List ℝ), init_polymult f g = init_polymult g f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arithmetic_poly.even_exp
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: Polynomial, n: nat):
--   FORALL (x: real): eval(init_polyexp(f, 2 * n))(x) >= 0
theorem even_exp : ∀ (f : List ℝ) (n : {i : ℝ // i ≥ 0}), ∀ (x : ℝ), eval (init_polyexp f (2 * n)) (x) ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.arithmetic_poly