-- Auto-generated by pvs2lean/translator.py
-- PVS theory: standard_form_extras
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: standard_form_extras

Auto-generated stubs from PVSLib theory `standard_form_extras`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.standard_form_extras

noncomputable section

variable {pvs_τ1 : Type*} (Unif_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (add : pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (allnonzero : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (allnonzero_p : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (append : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (below : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (car : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (cdr : pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (cons : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (cons_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (cutting : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (eval : pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (every : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (full_eval : pvs_τ27 → pvs_τ28 → pvs_τ29)
variable {pvs_τ30 : Type*} (is_sorted_p : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} (length : pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (lft : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (map : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (max : pvs_τ38 → pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} (max_length : pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} (min_add : pvs_τ42 → pvs_τ43 → pvs_τ44)
variable {pvs_τ45 : Type*} (minlength_p : pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (mp_mult : pvs_τ46 → pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} (mult : pvs_τ49 → pvs_τ50 → pvs_τ51)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} (mv_standard_form : pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} (mv_standard_form_p : pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} (mv_standard_sans_cut_p : pvs_τ55 → pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} (nth : pvs_τ57 → pvs_τ58 → pvs_τ59)
variable {pvs_τ60 : Type*} (null_p : pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (permutations : pvs_τ61 → pvs_τ62 → Prop)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} (remove : pvs_τ63 → pvs_τ64 → pvs_τ65)
variable {pvs_τ66 : Type*} (simplified_p : pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (simplify : pvs_τ67 → pvs_τ68)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} (sort_add : pvs_τ69 → pvs_τ70 → pvs_τ71)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} (sorted_add : pvs_τ72 → pvs_τ73 → pvs_τ74)
variable {pvs_τ75 : Type*} {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} (sorted_sans_add : pvs_τ75 → pvs_τ76 → pvs_τ77 → pvs_τ78)
variable {pvs_τ79 : Type*} {pvs_τ80 : Type*} (sortedsimplify : pvs_τ79 → pvs_τ80)
variable {pvs_τ81 : Type*} (sum_C : pvs_τ81 → Prop)
variable {pvs_τ82 : Type*} (total_preorder_p : pvs_τ82 → Prop)

-- pvs2lean: standard_form_extras.cons_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, p: MultPoly): max_length(cons(m, p)) >= max_length(p)
theorem cons_max_length : ∀ (m : List ℝ × ℝ) (p : List (List ℝ × ℝ)), max_length (cons m p) ≥ max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.cutting_null
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial):
--   cutting(m`alpha) = null IFF every(LAMBDA (n: nat): n = 0)(m`alpha)
theorem cutting_null : ∀ (m : List ℝ × ℝ), cutting m.alpha = null ↔ every (fun (n : ℕ)=> n = 0) (m.alpha) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_extras.cutting_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?[nat])):
--   cons?(cutting(l)) IMPLIES cutting(l) = cons(car(l), cutting(cdr(l)))
theorem cutting_cons : ∀ (l : List ℝ), cons_p (cutting l) → cutting l = cons (car l) (cutting (cdr l)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.lft_ml_n
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, (n: nat | n >= max_length(p))):
--   cons?(p) IMPLIES max_length(lft(p)(n)) = n
theorem lft_ml_n : ∀ (p : List (List ℝ × ℝ)) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max_length p}), cons_p p → max_length (lft p(n)) = n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.lft_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): max_length(p) = max_length(lft(p)(max_length(p)))
theorem lft_max_length : ∀ (p : List (List ℝ × ℝ)), max_length p = max_length (lft p(max_length p)) := by
  -- === TCCs for lft_max_length ===
  -- TCC: lft_max_length_TCC1
  -- Statement: FORALL (p: MultPoly): max_length(p) >= max_length(p)
  have lft_max_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.permutations_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly):
--   permutations(p, q) IMPLIES max_length(p) = max_length(q)
theorem permutations_max_length : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), permutations p q → max_length p = max_length q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sortedsimplify_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   (is_sorted?(p) AND cons?(p)) IMPLIES cons?(sortedsimplify(p))
theorem sortedsimplify_cons : ∀ (p : List (List ℝ × ℝ)), (is_sorted_p p ∧ cons_p p) → cons_p (sortedsimplify p) := by
  -- === TCCs for sortedsimplify_cons ===
  -- TCC: sortedsimplify_cons_TCC1
  -- Statement: total_preorder?[monomial](leq)
  have sortedsimplify_cons_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.simplify_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): max_length(p) = max_length(simplify(p))
theorem simplify_max_length : ∀ (p : List (List ℝ × ℝ)), max_length p = max_length (simplify p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.mv_standard_form_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): max_length(p) >= max_length(mv_standard_form(p))
theorem mv_standard_form_max_length : ∀ (p : List (List ℝ × ℝ)), max_length p ≥ max_length (mv_standard_form p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.eval_vals_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem eval_vals_0 : True := by
  -- === TCCs for eval_vals_0 ===
  -- TCC: eval_vals_0_TCC1
  -- Statement: FORALL (vals: list[real]): length[real](vals) >= length[nat](null[nat])
  have eval_vals_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: standard_form_extras.eval_is_map
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, index: list[nat],
--         (vals: list[real] | length(vals) = length(index))):
--   eval(p, index)(vals) = map(LAMBDA (m: monomial): eval(m, index)(vals))(p)
theorem eval_is_map : ∀ (p : List (List ℝ × ℝ)) (index : List ℝ) (vals : {vals : List ℝ // List.length vals = List.length index}), eval p index(vals) = map (fun (m : monomial)=> eval m index(vals)) (p) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_extras.sum_C_remove
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, (p: MultPoly | length(p) = n), i: below(length(p))):
--   sum_C(p) = nth(p, i)`C + sum_C(remove(p, i))
theorem sum_C_remove : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (p : {p : List (List ℝ × ℝ) // List.length p = n}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}), sum_C p = nth p i.C + sum_C (remove p i) := by
  -- === TCCs for sum_C_remove ===
  -- TCC: sum_C_remove_TCC1
  -- Statement: FORALL (n: posnat, (p: MultPoly | length[monomial](p) = n),
  --         i: below(length[monomial](p))):
  --   cons?[monomial](p)
  have sum_c_remove_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sum_C_perm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly): permutations(p, q) IMPLIES sum_C(p) = sum_C(q)
theorem sum_C_perm : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), permutations p q → sum_C p = sum_C q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.mult_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: real, p: MultPoly): max_length(mult(r, p)) = max_length(p)
theorem mult_max_length : ∀ (r : ℝ) (p : List (List ℝ × ℝ)), max_length (mult r p) = max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_sans_add__max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, p: (mv_standard_sans_cut?(n)),
--         q: (mv_standard_sans_cut?(n))):
--   max_length(sorted_sans_add(n, p, q)) <= max(max_length(p), max_length(q))
theorem sorted_sans_add__max_length : ∀ (n : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), max_length (sorted_sans_add n p q) ≤ max (max_length p) (max_length q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_add_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: (mv_standard_form?)):
--   max_length(sorted_add(p, q)) <= max(max_length(p), max_length(q))
theorem sorted_add_max_length : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), max_length (sorted_add p q) ≤ max (max_length p) (max_length q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sort_add_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly):
--   max_length(sort_add(p, q)) <=
--    max(max_length(mv_standard_form(p)), max_length(mv_standard_form(q)))
theorem sort_add_max_length : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), max_length (sort_add p q) ≤ max (max_length (mv_standard_form p)) (max_length (mv_standard_form q)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.min_add_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly):
--   max_length(min_add(p, q)) <=
--    max(max_length(mv_standard_form(p)), max_length(mv_standard_form(q)))
theorem min_add_max_length : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), max_length (min_add p q) ≤ max (max_length (mv_standard_form p)) (max_length (mv_standard_form q)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.add_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly):
--   max_length(add(p, q)) <= max(max_length(p), max_length(q))
theorem add_max_length : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), max_length (add p q) ≤ max (max_length p) (max_length q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.mp_mult_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly):
--   max_length(mp_mult(p, q)) <= max(max_length(p), max_length(q))
theorem mp_mult_max_length : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), max_length (mp_mult p q) ≤ max (max_length p) (max_length q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.null_allnonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   null?(allnonzero(p)) IFF every(LAMBDA (m: monomial): m`C = 0)(p)
theorem null_allnonzero : ∀ (p : List (List ℝ × ℝ)), null_p (allnonzero p) ↔ every (fun (m : monomial)=> m.C = 0) (p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.lft_constants
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, i: below(length(p)), (n: nat | n >= max_length(p))):
--   nth(p, i)`C = nth(lft(p)(n), i)`C
theorem lft_constants : ∀ (p : List (List ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max_length p}), nth p i.C = nth (lft p(n)) i.C := by
  -- === TCCs for lft_constants ===
  -- TCC: lft_constants_TCC1
  -- Statement: FORALL (p: MultPoly, i: below(length[monomial](p)),
  --         (n: nat | n >= max_length(p))):
  --   i < length[monomial](lft(p)(n))
  have lft_constants_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.mult_length_monom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, r: real): length(mult(r, m)`alpha) = length(m`alpha)
theorem mult_length_monom : ∀ (m : List ℝ × ℝ) (r : ℝ), List.length (mult r m.alpha) = List.length m.alpha := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_extras.mult_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, r: real): length(mult(r, p)) = length(p)
theorem mult_length : ∀ (p : List (List ℝ × ℝ)) (r : ℝ), List.length (mult r p) = List.length p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_extras.scale_constants
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, i: below(length(p)), r: real):
--   nth(mult(r, p), i)`C = r * nth(p, i)`C
theorem scale_constants : ∀ (p : List (List ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}) (r : ℝ), nth (mult r p) i.C = r * nth p i.C := by
  -- === TCCs for scale_constants ===
  -- TCC: scale_constants_TCC1
  -- Statement: FORALL (p: MultPoly, i: below(length[monomial](p)), r: real):
  --   i < length[monomial](mult(r, p))
  have scale_constants_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_extras.mult_map
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem mult_map : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: standard_form_extras.mult_mv_sans
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, p: (mv_standard_sans_cut?(n)), r: nzreal):
--   mv_standard_sans_cut?(n)(mult(r, p))
theorem mult_mv_sans : ∀ (n : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (r : {r : ℝ // r ≠ 0}), mv_standard_sans_cut_p n (mult r p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.mult_mv_standard
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: (mv_standard_form?), r: nzreal): mv_standard_form?(mult(r, p))
theorem mult_mv_standard : ∀ (p : List (List ℝ × ℝ)) (r : {r : ℝ // r ≠ 0}), mv_standard_form_p (mult r p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.max_length_different_lft_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: (minlength?)):
--   length(p) = length(q) AND max_length(p) /= max_length(q) IMPLIES
--    EXISTS (i: below(length(p))):
--      nth(lft(p)(max(max_length(p), max_length(q))), i)`alpha /=
--       n
theorem max_length_different_lft_nth : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), List.length p = List.length q ∧ max_length p ≠ max_length q → ∃ (i : Finset.range List.length p), nth (lft p(max (max_length p) (max_length q))) i.alpha ≠
   nth (lft q(max (max_length p) (max_length q))) i.alpha := by
  -- === TCCs for max_length_different_lft_nth ===
  -- TCC: max_length_different_lft_nth_TCC1
  -- Statement: FORALL (p, q: (minlength?)):
  --   length(p) = length(q) AND max_length(p) /= max_length(q) IMPLIES
  --    FORALL (i: below(length[monomial](p))):
  --      max(max_length(p), max_length(q)) >= max_length(p)
  have max_length_different_lft_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_length_different_lft_nth_TCC2
  -- Statement: FORALL (p, q: (minlength?)):
  --   length(p) = length(q) AND max_length(p) /= max_length(q) IMPLIES
  --    FORALL (i: below(length[monomial](p))):
  --      i < length[monomial](lft(p)(max(max_length(p), max_le...
  have max_length_different_lft_nth_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_length_different_lft_nth_TCC3
  -- Statement: FORALL (p, q: (minlength?)):
  --   length(p) = length(q) AND max_length(p) /= max_length(q) IMPLIES
  --    FORALL (i: below(length[monomial](p))):
  --      max(max_length(p), max_length(q)) >= max_length(q)
  have max_length_different_lft_nth_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_length_different_lft_nth_TCC4
  -- Statement: FORALL (p, q: (minlength?)):
  --   length(p) = length(q) AND max_length(p) /= max_length(q) IMPLIES
  --    FORALL (i: below(length[monomial](p))):
  --      i < length[monomial](lft(q)(max(max_length(p), max_le...
  have max_length_different_lft_nth_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.lft_max_on_minlength
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: (minlength?)): lft(p)(max_length(p)) = p
theorem lft_max_on_minlength : ∀ (p : List (List ℝ × ℝ)), lft p(max_length p) = p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_sans_add_never_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nn: nat, p, q: MultPoly, n: nat):
--   (n = length(p) AND
--     mv_standard_sans_cut?(nn)(p) AND mv_standard_sans_cut?(nn)(q))
--    IMPLIES
--    (FORALL (i: below(length(sorted_sans_add(nn, p, q)))):
--  
theorem sorted_sans_add_never_zero : ∀ (nn : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)) (n : {i : ℝ // i ≥ 0}), (n = List.length p ∧
  mv_standard_sans_cut_p nn(p) ∧ mv_standard_sans_cut_p nn(q)) → (∀ (i : Finset.range List.length (sorted_sans_add nn p q)), nth (sorted_sans_add nn p q) i.C ≠ 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_sans_add_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (nn: nat, p, q: MultPoly, n: nat):
--   (n = length(p) AND
--     mv_standard_sans_cut?(nn)(p) AND mv_standard_sans_cut?(nn)(q))
--    IMPLIES
--    (every(LAMBDA (m: monomial): m`C = 0)(sorted_sans_add(nn
theorem sorted_sans_add_zero : ∀ (nn : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)) (n : {i : ℝ // i ≥ 0}), (n = List.length p ∧
  mv_standard_sans_cut_p nn(p) ∧ mv_standard_sans_cut_p nn(q)) → (every (fun (m : monomial)=> m.C = 0) (sorted_sans_add nn p q) ↔
  (List.length p = List.length q ∧
    ∀ (i : Finset.range List.length p), nth p i = mult -1 (nth q i))) := by
  -- === TCCs for sorted_sans_add_zero ===
  -- TCC: sorted_sans_add_zero_TCC1
  -- Statement: FORALL (nn: nat, p, q: MultPoly, n: nat):
  --   n = length(p) AND
  --    mv_standard_sans_cut?(nn)(p) AND
  --     mv_standard_sans_cut?(nn)(q) AND length(p) = length(q)
  --    IMPLIES FORALL (i: below(length[monom...
  have sorted_sans_add_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_sans_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, p: (mv_standard_sans_cut?(n)),
--         q: (mv_standard_sans_cut?(n))):
--   FORALL (i: below(length(sorted_sans_add(n, p, q)))):
--     (EXISTS (j: below(length(p))):
--        nth(sorted_sans_
theorem sorted_sans_nth : ∀ (n : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length (sorted_sans_add n p q)}), (∃ (j : Finset.range List.length p), nth (sorted_sans_add n p q) i.alpha = nth p j.alpha) ∨ (∃ (j : Finset.range List.length q), nth (sorted_sans_add n p q) i.alpha = nth q j.alpha) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_sans_add_Unif
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, p: (mv_standard_sans_cut?(n)),
--         q: (mv_standard_sans_cut?(n))):
--   Unif?(sorted_sans_add(n, p, q))
theorem sorted_sans_add_Unif : ∀ (n : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), Unif_p (sorted_sans_add n p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_sans_add_sort
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, p: (mv_standard_sans_cut?(n)),
--         q: (mv_standard_sans_cut?(n))):
--   is_sorted?(sorted_sans_add(n, p, q))
theorem sorted_sans_add_sort : ∀ (n : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), is_sorted_p (sorted_sans_add n p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_add_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p1: (mv_standard_form?), p2: (mv_standard_form?)):
--   is_sorted?(sorted_add(p1, p2))
theorem sorted_add_sorted : ∀ (p1 : List (List ℝ × ℝ)) (p2 : List (List ℝ × ℝ)), is_sorted_p (sorted_add p1 p2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.sorted_sans_add_simp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, p: (mv_standard_sans_cut?(n)),
--         q: (mv_standard_sans_cut?(n))):
--   simplified?(sorted_sans_add(n, p, q))
theorem sorted_sans_add_simp : ∀ (n : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), simplified_p (sorted_sans_add n p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.add_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly): allnonzero?(add(p, q))
theorem add_nonzero : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), allnonzero_p (add p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.add_simplified
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly): simplified?(add(p, q))
theorem add_simplified : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), simplified_p (add p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.add_minlength
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly): minlength?(add(p, q))
theorem add_minlength : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), minlength_p (add p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.add_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly): is_sorted?(add(p, q))
theorem add_sorted : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), is_sorted_p (add p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.add_standard_form
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly): mv_standard_form?(add(p, q))
theorem add_standard_form : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), mv_standard_form_p (add p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.mult_standard_form
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly): mv_standard_form?(mp_mult(p, q))
theorem mult_standard_form : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), mv_standard_form_p (mp_mult p q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.scal_full_eval_monom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: real, m: monomial,
--         vals: list[real] | length(vals) >= length(m`alpha)):
--   full_eval(mult(r, m))(vals) = r * full_eval(m)(vals)
theorem scal_full_eval_monom : ∀ (r : ℝ) (m : List ℝ × ℝ) (vals : {vals : List ℝ // List.length vals ≥ List.length m.alpha}), full_eval (mult r m) (vals) = r * full_eval m(vals) := by
  -- === TCCs for scal_full_eval_monom ===
  -- TCC: scal_full_eval_monom_TCC1
  -- Statement: FORALL (r: real, m: monomial,
  --         vals: list[real] | length[real](vals) >= length[nat](m`alpha)):
  --   length[real](vals) >= length[nat](mult(r, m)`alpha)
  have scal_full_eval_monom_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.scale_full_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: real, p: MultPoly,
--         (vals: list[real] | length(vals) >= max_length(p))):
--   full_eval(mult(r, p))(vals) = r * full_eval(p)(vals)
theorem scale_full_eval : ∀ (r : ℝ) (p : List (List ℝ × ℝ)) (vals : {vals : List ℝ // List.length vals ≥ max_length p}), full_eval (mult r p) (vals) = r * full_eval p(vals) := by
  -- === TCCs for scale_full_eval ===
  -- TCC: scale_full_eval_TCC1
  -- Statement: FORALL (r: real, p: MultPoly,
  --         (vals: list[real] | length[real](vals) >= max_length(p))):
  --   length[real](vals) >= max_length(mult(r, p))
  have scale_full_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.vals_pad
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: append(vals, (: 1 :) ^ n)
axiom vals_pad {pvs_τ83 : Type*} {pvs_τ84 : Type*} {pvs_τ85 : Type*} : pvs_τ83 → pvs_τ84 → pvs_τ85

-- pvs2lean: standard_form_extras.vals_pad_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (vals: list[real], n: nat):
--   cons?(vals) IMPLIES cdr(vals_pad(vals, n)) = vals_pad(cdr(vals), n)
theorem vals_pad_cdr : ∀ (vals : List ℝ) (n : {i : ℝ // i ≥ 0}), cons_p vals → cdr (vals_pad vals n) = vals_pad (cdr vals) n := by
  -- === TCCs for vals_pad_cdr ===
  -- TCC: vals_pad_cdr_TCC1
  -- Statement: FORALL (vals: list[real], n: nat):
  --   cons?(vals) IMPLIES cons?[real](vals_pad(vals, n))
  have vals_pad_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.vals_extend_monom
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, (vals: list[real] | length(vals) >= length(m`alpha)),
--         n: nat):
--   full_eval(m)(vals_pad(vals, n)) = full_eval(m)(vals)
theorem vals_extend_monom : ∀ (m : List ℝ × ℝ) (vals : {vals : List ℝ // List.length vals ≥ List.length m.alpha}) (n : {i : ℝ // i ≥ 0}), full_eval m(vals_pad vals n) = full_eval m(vals) := by
  -- === TCCs for vals_extend_monom ===
  -- TCC: vals_extend_monom_TCC1
  -- Statement: FORALL (m: monomial,
  --         (vals: list[real] | length[real](vals) >= length[nat](m`alpha)),
  --         n: nat):
  --   length[real](vals_pad(vals, n)) >= length[nat](m`alpha)
  have vals_extend_monom_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_extras.vals_extend
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, (vals: list[real] | length(vals) >= max_length(p)),
--         n: nat):
--   full_eval(p)(vals_pad(vals, n)) = full_eval(p)(vals)
theorem vals_extend : ∀ (p : List (List ℝ × ℝ)) (vals : {vals : List ℝ // List.length vals ≥ max_length p}) (n : {i : ℝ // i ≥ 0}), full_eval p(vals_pad vals n) = full_eval p(vals) := by
  -- === TCCs for vals_extend ===
  -- TCC: vals_extend_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         (vals: list[real] | length[real](vals) >= max_length(p)), n: nat):
  --   length[real](vals_pad(vals, n)) >= max_length(p)
  have vals_extend_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.standard_form_extras