-- Auto-generated by pvs2lean/translator.py
-- PVS theory: standard_form_poly
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: standard_form_poly

Auto-generated stubs from PVSLib theory `standard_form_poly`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.standard_form_poly

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (car : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (cdr : pvs_τ3 → pvs_τ4)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} (cons : pvs_τ7 → pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} (cons_p : pvs_τ10 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (length : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (null_p : pvs_τ15 → Prop)

-- pvs2lean: standard_form_poly.last
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(f) = 1 THEN car(f) ELSE last(cdr(f)) ENDIF
axiom last {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12

-- pvs2lean: standard_form_poly.last_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | length(ff) > 1}): last(f) = last(cdr(f))
theorem last_cdr : ∀ (f : {ff : List ℝ // List.length ff > 1}), last f = last (cdr f) := by
  -- === TCCs for last_cdr ===
  -- TCC: last_cdr_TCC1
  -- Statement: FORALL (f: {ff: Polynomial | length[real](ff) > 1}): cons?[real](f)
  have last_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: last_cdr_TCC2
  -- Statement: FORALL (f: {ff: Polynomial | length[real](ff) > 1}):
  --   cons?[real](cdr[real](f))
  have last_cdr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_poly.chop
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(f) <= 1 THEN null ELSE cons(car(f), chop(cdr(f))) ENDIF
axiom chop {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6

-- pvs2lean: standard_form_poly.first_chop
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | length(ff) > 1}): car(chop(f)) = car(f)
theorem first_chop : ∀ (f : {ff : List ℝ // List.length ff > 1}), car (chop f) = car f := by
  -- === TCCs for first_chop ===
  -- TCC: first_chop_TCC1
  -- Statement: FORALL (f: {ff: Polynomial | length[real](ff) > 1}): cons?[real](chop(f))
  have first_chop_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_poly.chop_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | cons?(ff) AND cons?(cdr(ff))}):
--   cdr(chop(f)) = chop(cdr(f))
theorem chop_cdr : ∀ (f : {ff : List ℝ // cons_p ff ∧ cons_p (cdr ff)}), cdr (chop f) = chop (cdr f) := by
  -- === TCCs for chop_cdr ===
  -- TCC: chop_cdr_TCC1
  -- Statement: FORALL (f:
  --           {ff: Polynomial |
  --              cons?[real](ff) AND cons?[real](cdr[real](ff))}):
  --   cons?[real](chop(f))
  have chop_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.chop_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | cons?(ff)}): length(f) = length(chop(f)) + 1
theorem chop_length : ∀ (f : {ff : List ℝ // cons_p ff}), List.length f = List.length (chop f) + 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.standard_form?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(f) THEN TRUE ELSIF last(f) = 0 THEN FALSE ELSE TRUE ENDIF
axiom standard_form_p {pvs_τ17 : Type*} : pvs_τ17 → Prop

-- pvs2lean: standard_form_poly.standard_form
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF standard_form?(f) = TRUE THEN f ELSE standard_form(chop(f)) ENDIF
axiom standard_form {pvs_τ16 : Type*} : pvs_τ16 → Prop

-- pvs2lean: standard_form_poly.standard_form_ex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: standard_form((: 0, 1, 2, 0, 0 :)) = (: 0, 1, 2 :)
theorem standard_form_ex : standard_form ((: 0, 1, 2, 0, 0 :)) = (: 0, 1, 2 :) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.standard_form_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: Polynomial):
--   cons?(f) AND standard_form?(f) IMPLIES standard_form?(cdr(f))
theorem standard_form_cdr : ∀ (f : List ℝ), cons_p f ∧ standard_form_p f → standard_form_p (cdr f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.standard_form_cdr1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | cons?(ff) AND cons?(cdr(ff))}):
--   standard_form?(cdr(f)) IMPLIES standard_form?(f)
theorem standard_form_cdr1 : ∀ (f : {ff : List ℝ // cons_p ff ∧ cons_p (cdr ff)}), standard_form_p (cdr f) → standard_form_p f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.standard_form_cdr2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | cons?(standard_form(ff))}):
--   (standard_form(f) = cons(car(f), standard_form(cdr(f))))
theorem standard_form_cdr2 : ∀ (f : {ff : List ℝ // cons_p (standard_form ff)}), standard_form f = cons (car f) (standard_form (cdr f)) := by
  -- === TCCs for standard_form_cdr2 ===
  -- TCC: standard_form_cdr2_TCC1
  -- Statement: FORALL (f: {ff: Polynomial | cons?[real](standard_form(ff))}):
  --   cons?[real](f)
  have standard_form_cdr2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.standard_form_cdr3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | cons?(ff) AND cons?(standard_form(cdr(ff)))}):
--   standard_form(f) = cons(car(f), standard_form(cdr(f)))
theorem standard_form_cdr3 : ∀ (f : {ff : List ℝ // cons_p ff ∧ cons_p (standard_form (cdr ff))}), standard_form f = cons (car f) (standard_form (cdr f)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.standard_form_car
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | cons?(ff) AND cons?(cdr(ff))}):
--   standard_form(cdr(f)) = null IMPLIES
--    standard_form(f) = standard_form((: car(f) :))
theorem standard_form_car : ∀ (f : {ff : List ℝ // cons_p ff ∧ cons_p (cdr ff)}), standard_form (cdr f) = null → standard_form f = standard_form ((: car f :)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.standard_null_decomp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | cons?(ff)}):
--   null?(standard_form(f)) IFF
--    null?(standard_form((: car(f) :))) AND null?(standard_form(cdr(f)))
theorem standard_null_decomp : ∀ (f : {ff : List ℝ // cons_p ff}), null_p (standard_form f) ↔ null_p (standard_form ((: car f :))) ∧ null_p (standard_form (cdr f)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_poly.standard_form_cons
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: {ff: Polynomial | cons?(ff)}):
--   car(f) /= 0 IMPLIES standard_form(f) = cons(car(f), standard_form(cdr(f)))
theorem standard_form_cons : ∀ (f : {ff : List ℝ // cons_p ff}), car f ≠ 0 → standard_form f = cons (car f) (standard_form (cdr f)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.standard_form_poly