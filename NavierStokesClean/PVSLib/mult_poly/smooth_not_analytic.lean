-- Auto-generated by pvs2lean/translator.py
-- PVS theory: smooth_not_analytic
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: smooth_not_analytic

Auto-generated stubs from PVSLib theory `smooth_not_analytic`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.smooth_not_analytic

noncomputable section

variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (analytic_p : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} (bounded_below_p : pvs_τ4 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cons : pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (continuous_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (convergence : pvs_τ12 → pvs_τ13 → Prop)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (convergent_p : pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (cos : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (deriv : pvs_τ18 → pvs_τ19 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (derivable_n_times_p : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (derivable_p : pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} (exp : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (glb : pvs_τ27 → pvs_τ28)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (lim : pvs_τ31 → pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (ln : pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (max : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (meet_max : pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} (mv_standard_form_p : pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (nderiv : pvs_τ41 → pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} (polynomial : pvs_τ44 → pvs_τ45 → pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} (polynomial_prod : pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → pvs_τ51)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} (power_linear : pvs_τ52 → pvs_τ53 → pvs_τ54 → pvs_τ55)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (restrict : pvs_τ56 → pvs_τ57)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} (semi_alg : pvs_τ58 → pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} (sin : pvs_τ61 → pvs_τ62)
variable {pvs_τ66 : Type*} (xn : pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} (yn : pvs_τ67 → Prop)

-- pvs2lean: smooth_not_analytic.Rolles_thrm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], a: real, b: {bb: real | bb > a}):
--   ((derivable?[open_interval[real](a, b)]
--         (restrict[real, open_interval[real](a, b), real](f))
--      AND
--      continuous?[closed_int
theorem Rolles_thrm : ∀ (f : ℝ → ℝ) (a : ℝ) (b : {bb : ℝ // bb > a}), ((derivable_p[open_interval[real](a, b)]
      (restrict f)
   ∧
   continuous_p[closed_interval[real](a, b)]
       (restrict f))
  ∧ f a = f b) → ∃ (c : ℝ), a < c ∧ c < b ∧ deriv f c = 0 := by
  -- === TCCs for Rolles_thrm ===
  -- TCC: Rolles_thrm_TCC1
  -- Statement: FORALL (a: real, b: {bb: real | bb > a}):
  --   deriv_domain?[open_interval[real](a, b)]
  have rolles_thrm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Rolles_thrm_TCC2
  -- Statement: FORALL (a: real, b: {bb: real | bb > a}):
  --   not_one_element?[open_interval[real](a, b)]
  have rolles_thrm_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Rolles_thrm_TCC3
  -- Statement: FORALL (f: [real -> real], a: real, b: {bb: real | bb > a}):
  --   derivable?[open_interval[real](a, b)]
  --       (restrict[real, open_interval[real](a, b), real](f))
  --    AND
  --    continuous?[closed_interval...
  have rolles_thrm_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.open_noe
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: real):
--   b > a IMPLIES not_one_element?[open_interval[real](a, b)]
theorem open_noe : ∀ (a : ℝ) (b : ℝ), b > a → not_one_element_p[open_interval[real](a, b)] := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.closed_cont
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: real, f: [real -> real]):
--   (continuous?(f) AND b > a) IMPLIES
--    continuous?[closed_interval[real](a, b)]
--        (LAMBDA (s: closed_interval[real](a, b)): f(s))
theorem closed_cont : ∀ (a : ℝ) (b : ℝ) (f : ℝ → ℝ), (continuous_p f ∧ b > a) → continuous_p[closed_interval[real](a, b)]
    (fun (s : closed_interval a b)=> f s) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.mean_value_gen
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], a: real, b: {bb: real | bb > a}):
--   (derivable?[open_interval[real](a, b)]
--        (restrict[real, open_interval[real](a, b), real](f))
--     AND
--     continuous?[closed_interva
theorem mean_value_gen : ∀ (f : ℝ → ℝ) (a : ℝ) (b : {bb : ℝ // bb > a}), (derivable_p[open_interval[real](a, b)]
     (restrict f)
  ∧
  continuous_p[closed_interval[real](a, b)]
      (restrict f)) → ∃ (c : ℝ), a < c ∧ c < b ∧ deriv f c * (b - a) = f b - f a := by
  -- === TCCs for mean_value_gen ===
  -- TCC: mean_value_gen_TCC1
  -- Statement: FORALL (f: [real -> real], a: real, b: {bb: real | bb > a}):
  --   derivable?[open_interval[real](a, b)]
  --       (restrict[real, open_interval[real](a, b), real](f))
  --    AND
  --    continuous?[closed_interval...
  have mean_value_gen_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_left_right_point
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, f: [real -> real]):
--   (((((derivable?[(LAMBDA (r: real): r < x)]
--            (restrict[real, (LAMBDA (r: real): r < x), real](f))
--         AND
--         derivable?[(LAMBDA (r: real): r > 
theorem deriv_left_right_point : ∀ (x : ℝ) (f : ℝ → ℝ), (((((derivable_p[(fun (r : ℝ)=> r < x)]
         (restrict f)
      ∧
      derivable_p[(fun (r : ℝ)=> r > x)]
          (restrict f))
     ∧
     (convergent_p[(fun (r : ℝ)=> r < x)]
          (deriv[(fun (r : ℝ)=> r < x)]
               (restrict f),
           x)
       ∧
       convergent_p[(fun (r : ℝ)=> r > x)]
           (deriv[(fun (r : ℝ)=> r > x)]
                (restrict f),
            x)))
    ∧
    lim (deriv[(fun (r : ℝ)=> r < x)]
            (restrict f)) x
     =
     lim (deriv[(fun (r : ℝ)=> r > x)]
             (restrict f)) x))
  ∧ continuous_p f) → derivable_p f x := by
  -- === TCCs for deriv_left_right_point ===
  -- TCC: deriv_left_right_point_TCC1
  -- Statement: FORALL (x: real): deriv_domain?[(LAMBDA (r: real): r < x)]
  have deriv_left_right_point_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_left_right_point_TCC2
  -- Statement: FORALL (x: real): not_one_element?[(LAMBDA (r: real): r < x)]
  have deriv_left_right_point_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_left_right_point_TCC3
  -- Statement: FORALL (x: real, f: [real -> real]):
  --   derivable?[(LAMBDA (r: real): r < x)]
  --       (restrict[real, (LAMBDA (r: real): r < x), real](f))
  --    IMPLIES deriv_domain?[(LAMBDA (r: real): r > x)]
  have deriv_left_right_point_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_left_right_point_TCC4
  -- Statement: FORALL (x: real, f: [real -> real]):
  --   derivable?[(LAMBDA (r: real): r < x)]
  --       (restrict[real, (LAMBDA (r: real): r < x), real](f))
  --    IMPLIES not_one_element?[(LAMBDA (r: real): r > x)]
  have deriv_left_right_point_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_left_right_point_deriv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, f: [real -> real]):
--   (((((derivable?[(LAMBDA (r: real): r < x)]
--            (restrict[real, (LAMBDA (r: real): r < x), real](f))
--         AND
--         derivable?[(LAMBDA (r: real): r > 
theorem deriv_left_right_point_deriv : ∀ (x : ℝ) (f : ℝ → ℝ), (((((derivable_p[(fun (r : ℝ)=> r < x)]
         (restrict f)
      ∧
      derivable_p[(fun (r : ℝ)=> r > x)]
          (restrict f))
     ∧
     (convergent_p[(fun (r : ℝ)=> r < x)]
          (deriv[(fun (r : ℝ)=> r < x)]
               (restrict f),
           x)
       ∧
       convergent_p[(fun (r : ℝ)=> r > x)]
           (deriv[(fun (r : ℝ)=> r > x)]
                (restrict f),
            x)))
    ∧
    lim (deriv[(fun (r : ℝ)=> r < x)]
            (restrict f)) x
     =
     lim (deriv[(fun (r : ℝ)=> r > x)]
             (restrict f)) x))
  ∧ continuous_p f) → deriv f x =
 lim (deriv[(fun (r : ℝ)=> r < x)]
         (restrict f)) x := by
  -- === TCCs for deriv_left_right_point_deriv ===
  -- TCC: deriv_left_right_point_deriv_TCC1
  -- Statement: FORALL (x: real, f: [real -> real]):
  --        derivable?[(LAMBDA (r: real): r < x)]
  --            (restrict[real, (LAMBDA (r: real): r < x), real](f))
  --    AND derivable?[(LAMBDA (r: real): r > x)]
  --       ...
  have deriv_left_right_point_deriv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.sm
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x <= 0 THEN 0 ELSE exp(-1 / x) * sin(1 / x) ENDIF
axiom sm {pvs_τ63 : Type*} {pvs_τ64 : Type*} : pvs_τ63 → pvs_τ64

-- pvs2lean: smooth_not_analytic.smooth?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat): derivable_n_times?(f, n)
axiom smooth_p {pvs_τ65 : Type*} : pvs_τ65 → Prop

-- pvs2lean: smooth_not_analytic.sm_derivable_init_le0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat):
--   (derivable_n_times?[(LAMBDA (r: real): r < 0)]
--        (restrict[real, (LAMBDA (r: real): r < 0), real](sm), n)
--     AND
--     nderiv[(LAMBDA (r: real): r < 0)]
--         (n, restrict[rea
theorem sm_derivable_init_le0 : ∀ (n : {i : ℝ // i ≥ 0}), derivable_n_times_p[(fun (r : ℝ)=> r < 0)]
    (restrict sm, n) ∧ nderiv[(fun (r : ℝ)=> r < 0)]
    (n, restrict sm)
 = (fun (x : real | x < 0)=> 0) := by
  -- === TCCs for sm_derivable_init_le0 ===
  -- TCC: sm_derivable_init_le0_TCC1
  -- Statement: deriv_domain?[(LAMBDA (r: real): r < 0)]
  have sm_derivable_init_le0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sm_derivable_init_le0_TCC2
  -- Statement: not_one_element?[(LAMBDA (r: real): r < 0)]
  have sm_derivable_init_le0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.chop
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (i: nat): IF i <= n THEN a(i) ELSE 0 ENDIF
axiom chop {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7

-- pvs2lean: smooth_not_analytic.chop_poly
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real], n, m: nat):
--   m >= n IMPLIES polynomial(a, n) = polynomial(chop(a, n), m)
theorem chop_poly : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}), m ≥ n → polynomial a n = polynomial (chop a n) m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.chop_poly_add
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real], b: sequence[real], n1, n2: nat):
--   polynomial(a, n1) + polynomial(b, n2) =
--    polynomial(chop(a, n1) + chop(b, n2), max(n1, n2))
theorem chop_poly_add : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}), polynomial a n1 + polynomial b n2 = polynomial (chop a n1 + chop b n2) (max n1 n2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.poly_restrict_derivable
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real], n: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (restrict[real, (LAMBDA (r: real): r > 0), real](polynomial(a, n)))
theorem poly_restrict_derivable : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (n : {i : ℝ // i ≥ 0}), derivable_p (restrict (polynomial a n)) := by
  -- === TCCs for poly_restrict_derivable ===
  -- TCC: poly_restrict_derivable_TCC1
  -- Statement: deriv_domain?[(LAMBDA (r: real): r > 0)]
  have poly_restrict_derivable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: poly_restrict_derivable_TCC2
  -- Statement: not_one_element?[(LAMBDA (r: real): r > 0)]
  have poly_restrict_derivable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_p
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN (LAMBDA (i: nat): 0)
-- ELSE (LAMBDA (i: nat): (i + 1) * a(i + 1))
-- ENDIF
axiom deriv_p {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ20 → pvs_τ21 → Prop

-- pvs2lean: smooth_not_analytic.deriv_p_old_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real], n: nat):
--   (IF n = 0 THEN (LAMBDA (x: real): 0)
--    ELSE polynomial((LAMBDA (i: nat): (i + 1) * a(i + 1)), n - 1)
--    ENDIF)
--    = polynomial(deriv_p(a, n), max(n - 1, 0))
theorem deriv_p_old_def : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (n : {i : ℝ // i ≥ 0}), (if n = 0 then (fun (x : ℝ)=> 0)
 else polynomial ((fun (i : ℕ)=> (i + 1) * a (i + 1))) (n - 1)
 ) = polynomial (deriv_p a n) (max (n - 1) 0) := by
  -- === TCCs for deriv_p_old_def ===
  -- TCC: deriv_p_old_def_TCC1
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES n - 1 >= 0
  have deriv_p_old_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_p_old_def_TCC2
  -- Statement: FORALL (n: nat): max(n - 1, 0) >= 0
  have deriv_p_old_def_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.poly_restrict_deriv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real], n: nat):
--   deriv[(LAMBDA (r: real): r > 0)]
--       (restrict[real, (LAMBDA (r: real): r > 0), real](polynomial(a, n)))
--    =
--    restrict[real, (LAMBDA (r: real): r > 0), real]
theorem poly_restrict_deriv : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (n : {i : ℝ // i ≥ 0}), deriv[(fun (r : ℝ)=> r > 0)]
    (restrict (polynomial a n)) = restrict[real, (fun (r : ℝ)=> r > 0), real]
    (polynomial (deriv_p a n) (max 0 (n - 1))) := by
  -- === TCCs for poly_restrict_deriv ===
  -- TCC: poly_restrict_deriv_TCC1
  -- Statement: FORALL (a: sequence[real], n: nat):
  --   derivable?[(LAMBDA (r: real): r > 0)]
  --       (restrict[real, (LAMBDA (r: real): r > 0), real](polynomial(a, n)))
  have poly_restrict_deriv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: poly_restrict_deriv_TCC2
  -- Statement: FORALL (n: nat): max(0, n - 1) >= 0
  have poly_restrict_deriv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.derivable_trig_exp_poly_div
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: nat, a, b: sequence[real], n1, n2: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (LAMBDA (x: real | x > 0):
--          (exp(-1 / x) *
--            (sin(1 / x) * polynomial(a, n1)(x) +
--    
theorem derivable_trig_exp_poly_div : ∀ (k : {i : ℝ // i ≥ 0}) (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}), derivable_p (fun (x : {x : ℝ // x > 0}) => (exp (-1 / x) *
  (sin (1 / x) * polynomial a n1(x) + cos (1 / x) * polynomial b n2(x))) / x ^ k) := by
  -- === TCCs for derivable_trig_exp_poly_div ===
  -- TCC: derivable_trig_exp_poly_div_TCC1
  -- Statement: FORALL (x: real | x > 0): x /= 0
  have derivable_trig_exp_poly_div_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: derivable_trig_exp_poly_div_TCC2
  -- Statement: FORALL (k: nat, x: real | x > 0): x ^ k /= 0
  have derivable_trig_exp_poly_div_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_sin1x
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[(LAMBDA (r: real): r > 0)]
--     (LAMBDA (x: real | x > 0): (sin(1 / x)))
--  AND
--  deriv[(LAMBDA (r: real): r > 0)](LAMBDA (x: real | x > 0): sin(1 / x)) =
--   LAMBDA (x: real | x > 0): -cos(1 / x
theorem deriv_sin1x : derivable_p[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> (sin (1 / x))) ∧ deriv[(fun (r : ℝ)=> r > 0)](fun (x : real | x > 0)=> sin (1 / x)) =
 fun (x : real | x > 0)=> -cos (1 / x) / x ^ 2 := by
  -- === TCCs for deriv_sin1x ===
  -- TCC: deriv_sin1x_TCC1
  -- Statement: derivable?[(LAMBDA (r: real): r > 0)]
  --     (LAMBDA (x: real | x > 0): (sin(1 / x)))
  --  IMPLIES FORALL (x: real | x > 0): x ^ 2 /= 0
  have deriv_sin1x_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_cos1x
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[(LAMBDA (r: real): r > 0)](LAMBDA (x: real | x > 0): cos(1 / x))
--  AND
--  deriv[(LAMBDA (r: real): r > 0)](LAMBDA (x: real | x > 0): cos(1 / x)) =
--   LAMBDA (x: real | x > 0): sin(1 / x) / x ^ 
theorem deriv_cos1x : derivable_p[(fun (r : ℝ)=> r > 0)](fun (x : real | x > 0)=> cos (1 / x)) ∧ deriv[(fun (r : ℝ)=> r > 0)](fun (x : real | x > 0)=> cos (1 / x)) =
 fun (x : real | x > 0)=> sin (1 / x) / x ^ 2 := by
  -- === TCCs for deriv_cos1x ===
  -- TCC: deriv_cos1x_TCC1
  -- Statement: derivable?[(LAMBDA (r: real): r > 0)](LAMBDA (x: real | x > 0): cos(1 / x))
  --  IMPLIES FORALL (x: real | x > 0): x ^ 2 /= 0
  have deriv_cos1x_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_sin1x_poly
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real], n1: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (LAMBDA (x: real | x > 0): (sin(1 / x)) * polynomial(a, n1)(x))
--    AND
--    deriv[(LAMBDA (r: real): r > 0)]
--        (LA
theorem deriv_sin1x_poly : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}), derivable_p[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> (sin (1 / x)) * polynomial a n1(x)) ∧ deriv[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> sin (1 / x) * polynomial a n1(x))
 =
 fun (x : real | x > 0)=> sin (1 / x) * polynomial (deriv_p a n1) (max 0 (n1 - 1)) (x) +
    cos (1 / x) * polynomial -a n1(x) / x ^ 2 := by
  -- === TCCs for deriv_sin1x_poly ===
  -- TCC: deriv_sin1x_poly_TCC1
  -- Statement: FORALL (a: sequence[real], n1: nat):
  --   derivable?[(LAMBDA (r: real): r > 0)]
  --       (LAMBDA (x: real | x > 0): (sin(1 / x)) * polynomial(a, n1)(x))
  --    IMPLIES FORALL (x: real | x > 0): x ^ 2 /= 0
  have deriv_sin1x_poly_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_cos1x_poly
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: sequence[real], n2: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (LAMBDA (x: real | x > 0): (cos(1 / x)) * polynomial(b, n2)(x))
--    AND
--    deriv[(LAMBDA (r: real): r > 0)]
--        (LA
theorem deriv_cos1x_poly : ∀ (b : {i : ℝ // i ≥ 0} → ℝ) (n2 : {i : ℝ // i ≥ 0}), derivable_p[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> (cos (1 / x)) * polynomial b n2(x)) ∧ deriv[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> cos (1 / x) * polynomial b n2(x))
 =
 fun (x : real | x > 0)=> cos (1 / x) * polynomial (deriv_p b n2) (max 0 (n2 - 1)) (x) +
    sin (1 / x) * polynomial b n2(x) / x ^ 2 := by
  -- === TCCs for deriv_cos1x_poly ===
  -- TCC: deriv_cos1x_poly_TCC1
  -- Statement: FORALL (b: sequence[real], n2: nat):
  --   derivable?[(LAMBDA (r: real): r > 0)]
  --       (LAMBDA (x: real | x > 0): (cos(1 / x)) * polynomial(b, n2)(x))
  --    IMPLIES FORALL (x: real | x > 0): x ^ 2 /= 0
  have deriv_cos1x_poly_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.poly_pull_div_xk
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real], n, k: nat):
--   (LAMBDA (x: real | x > 0): polynomial(a, n)(x)) =
--    (LAMBDA (x: real | x > 0):
--       polynomial(polynomial_prod(a, n, power_linear(0, 1, k), k), n + k)(x)
--    
theorem poly_pull_div_xk : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (n : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), (fun (x : real | x > 0)=> polynomial a n(x)) = (fun (x : real | x > 0)=> polynomial (polynomial_prod a n (power_linear 0 1 k) k) (n + k) (x) /
    x ^ k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_sin1x_polyx2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real], n1: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (LAMBDA (x: real | x > 0): (sin(1 / x)) * polynomial(a, n1)(x))
--    AND
--    deriv[(LAMBDA (r: real): r > 0)]
--        (LA
theorem deriv_sin1x_polyx2 : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}), derivable_p[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> (sin (1 / x)) * polynomial a n1(x)) ∧ deriv[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> sin (1 / x) * polynomial a n1(x))
 =
 fun (x : real | x > 0)=> (sin (1 / x) *
     polynomial (polynomial_prod (deriv_p a n1) (max 0 (n1 - 1)) (power_linear 0 1 2) 2) (max 0 (n1 - 1) + 2)
               (x)
     + cos (1 / x) * polynomial -a n1(x))
    / x ^ 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_cos1x_polyx2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: sequence[real], n2: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (LAMBDA (x: real | x > 0): (cos(1 / x)) * polynomial(b, n2)(x))
--    AND
--    deriv[(LAMBDA (r: real): r > 0)]
--        (LA
theorem deriv_cos1x_polyx2 : ∀ (b : {i : ℝ // i ≥ 0} → ℝ) (n2 : {i : ℝ // i ≥ 0}), derivable_p[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> (cos (1 / x)) * polynomial b n2(x)) ∧ deriv[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> cos (1 / x) * polynomial b n2(x))
 =
 fun (x : real | x > 0)=> (cos (1 / x) *
     polynomial (polynomial_prod (deriv_p b n2) (max 0 (n2 - 1)) (power_linear 0 1 2) 2) (max 0 (n2 - 1) + 2)
               (x)
     + sin (1 / x) * polynomial b n2(x))
    / x ^ 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_sin_cos1x
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n1, n2: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (LAMBDA (x: real | x > 0):
--          (sin(1 / x) * polynomial(a, n1)(x) +
--            cos(1 / x) * polynomial(b,
theorem deriv_sin_cos1x : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}), derivable_p[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> (sin (1 / x) * polynomial a n1(x) +
         cos (1 / x) * polynomial b n2(x))) ∧ (deriv[(fun (r : ℝ)=> r > 0)]
     (fun (x : real | x > 0)=> (sin (1 / x) * polynomial a n1(x) +
          cos (1 / x) * polynomial b n2(x)))
  =
  (fun (x : real | x > 0)=> (sin (1 / x) *
       polynomial (chop b n2 +
                   chop (polynomial_prod (deriv_p a n1) (max 0 (n1 - 1)) (power_linear 0 1 2) 2) (max 0 (n1 - 1) + 2)) (max n2 (max 0 (n1 - 1) + 2))
                 (x)
       +
       cos (1 / x) *
        polynomial (chop -a n1 +
                    chop (polynomial_prod (deriv_p b n2) (max 0 (n2 - 1)) (power_linear 0 1 2) 2) (max 0 (n2 - 1) + 2)) (max n1 (max 0 (n2 - 1) + 2))
                  (x))
      / x ^ 2)) := by
  -- === TCCs for deriv_sin_cos1x ===
  -- TCC: deriv_sin_cos1x_TCC1
  -- Statement: FORALL (a, b: sequence[real], n1, n2: nat):
  --   derivable?[(LAMBDA (r: real): r > 0)]
  --       (LAMBDA (x: real | x > 0):
  --          (sin(1 / x) * polynomial(a, n1)(x) +
  --            cos(1 / x) * polynomial...
  have deriv_sin_cos1x_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_sin_cos1x_TCC2
  -- Statement: FORALL (a, b: sequence[real], n1, n2: nat):
  --   derivable?[(LAMBDA (r: real): r > 0)]
  --       (LAMBDA (x: real | x > 0):
  --          (sin(1 / x) * polynomial(a, n1)(x) +
  --            cos(1 / x) * polynomial...
  have deriv_sin_cos1x_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_e1x
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[(LAMBDA (r: real): r > 0)]
--     (LAMBDA (x: real | x > 0): exp(-1 / x))
--  AND
--  deriv[(LAMBDA (r: real): r > 0)](LAMBDA (x: real | x > 0): exp(-1 / x)) =
--   LAMBDA (x: real | x > 0): exp(-1 / x
theorem deriv_e1x : derivable_p[(fun (r : ℝ)=> r > 0)](fun (x : real | x > 0)=> exp (-1 / x)) ∧ deriv[(fun (r : ℝ)=> r > 0)](fun (x : real | x > 0)=> exp (-1 / x)) =
 fun (x : real | x > 0)=> exp (-1 / x) / x ^ 2 := by
  -- === TCCs for deriv_e1x ===
  -- TCC: deriv_e1x_TCC1
  -- Statement: derivable?[(LAMBDA (r: real): r > 0)]
  --     (LAMBDA (x: real | x > 0): exp(-1 / x))
  --  IMPLIES FORALL (x: real | x > 0): x ^ 2 /= 0
  have deriv_e1x_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_e_sin_cos1x
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n1, n2: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (LAMBDA (x: real | x > 0):
--          (exp(-1 / x) *
--            (sin(1 / x) * polynomial(a, n1)(x) +
--            
theorem deriv_e_sin_cos1x : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}), derivable_p[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> (exp (-1 / x) *
         (sin (1 / x) * polynomial a n1(x) +
           cos (1 / x) * polynomial b n2(x)))) ∧ ∃ (c : _) (d : ℕ → ℝ) (n3 : _) (n4 : ℕ), deriv[(fun (r : ℝ)=> r > 0)]
      (fun (x : real | x > 0)=> (exp (-1 / x) *
           (sin (1 / x) * polynomial a n1(x) +
             cos (1 / x) * polynomial b n2(x))))
   =
   (fun (x : real | x > 0)=> (exp (-1 / x) *
        (sin (1 / x) * polynomial c n3(x) +
          cos (1 / x) * polynomial d n4(x)))
       / x ^ 2) := by
  -- === TCCs for deriv_e_sin_cos1x ===
  -- TCC: deriv_e_sin_cos1x_TCC1
  -- Statement: FORALL (a, b: sequence[real], n1, n2: nat):
  --   derivable?[(LAMBDA (r: real): r > 0)]
  --       (LAMBDA (x: real | x > 0):
  --          (exp(-1 / x) *
  --            (sin(1 / x) * polynomial(a, n1)(x) +
  --         ...
  have deriv_e_sin_cos1x_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_e_sin_cos1xk
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n1, n2, k: nat):
--   derivable?[(LAMBDA (r: real): r > 0)]
--       (LAMBDA (x: real | x > 0):
--          (exp(-1 / x) *
--            (sin(1 / x) * polynomial(a, n1)(x) +
--         
theorem deriv_e_sin_cos1xk : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), derivable_p[(fun (r : ℝ)=> r > 0)]
    (fun (x : real | x > 0)=> (exp (-1 / x) *
         (sin (1 / x) * polynomial a n1(x) +
           cos (1 / x) * polynomial b n2(x)))
        / x ^ k) ∧ ∃ (c : _) (d : ℕ → ℝ) (n3 : _) (n4 : ℕ), deriv[(fun (r : ℝ)=> r > 0)]
      (fun (x : real | x > 0)=> (exp (-1 / x) *
           (sin (1 / x) * polynomial a n1(x) +
             cos (1 / x) * polynomial b n2(x)))
          / x ^ k)
   =
   (fun (x : real | x > 0)=> (exp (-1 / x) *
        (sin (1 / x) * polynomial c n3(x) +
          cos (1 / x) * polynomial d n4(x)))
       / x ^ (k + 2)) := by
  -- === TCCs for deriv_e_sin_cos1xk ===
  -- TCC: deriv_e_sin_cos1xk_TCC1
  -- Statement: FORALL (a, b: sequence[real], n1, n2, k: nat):
  --   derivable?[(LAMBDA (r: real): r > 0)]
  --       (LAMBDA (x: real | x > 0):
  --          (exp(-1 / x) *
  --            (sin(1 / x) * polynomial(a, n1)(x) +
  --      ...
  have deriv_e_sin_cos1xk_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.init_deriv_n
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat):
--   (derivable_n_times?[(LAMBDA (r: real): r > 0)]
--        (restrict[real, (LAMBDA (r: real): r > 0), real](sm), n)
--     IMPLIES
--     (EXISTS (c, d: sequence[real], n3, n4, k: nat):
--       
theorem init_deriv_n : ∀ (n : {i : ℝ // i ≥ 0}), derivable_n_times_p[(fun (r : ℝ)=> r > 0)]
    (restrict sm, n) → (∃ (c : _) (d : ℕ → ℝ) (n3 : _) (n4 : _) (k : ℕ), nderiv[(fun (r : ℝ)=> r > 0)]
       (n, restrict sm)
    =
    (fun (x : real | x > 0)=> (exp (-1 / x) *
         (sin (1 / x) * polynomial c n3(x) +
           cos (1 / x) * polynomial d n4(x)))
        / x ^ (k))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.deriv_sm_n
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat):
--   (derivable_n_times?[(LAMBDA (r: real): r > 0)]
--        (restrict[real, (LAMBDA (r: real): r > 0), real](sm), n))
--    AND
--    (EXISTS (c, d: sequence[real], n3, n4, k: nat):
--       nderi
theorem deriv_sm_n : ∀ (n : {i : ℝ // i ≥ 0}), (derivable_n_times_p[(fun (r : ℝ)=> r > 0)]
     (restrict sm, n)) ∧ (∃ (c : _) (d : ℕ → ℝ) (n3 : _) (n4 : _) (k : ℕ), nderiv[(fun (r : ℝ)=> r > 0)]
       (n, restrict sm)
    =
    (fun (x : real | x > 0)=> (exp (-1 / x) *
         (sin (1 / x) * polynomial c n3(x) +
           cos (1 / x) * polynomial d n4(x)))
        / x ^ (k))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.abs_between
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c: real):
--   (a <= b AND b <= c) IMPLIES abs(b) <= max(abs(a), abs(c))
theorem abs_between : ∀ (a : ℝ) (b : ℝ) (c : ℝ), (a ≤ b ∧ b ≤ c) → |b| ≤ max |a| |c| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.bound_sin_p_cos_p
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n1, n2: nat):
--   EXISTS (C: posreal):
--     FORALL (x: real | x <= 1 AND 0 < x):
--       abs((sin(1 / x) * polynomial(a, n1)(x) +
--             cos(1 / x) * polynomial(b, n2)(x)
theorem bound_sin_p_cos_p : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}), ∃ (C : {x : {x : ℝ // x ≥ 0} // x > 0}), ∀ (x : {x : ℝ // x ≤ 1 ∧ 0 < x}), |(sin (1 / x) * polynomial a n1(x) + cos (1 / x) * polynomial b n2(x))| < C := by
  -- === TCCs for bound_sin_p_cos_p ===
  -- TCC: bound_sin_p_cos_p_TCC1
  -- Statement: FORALL (x: real | x <= 1 AND 0 < x): x /= 0
  have bound_sin_p_cos_p_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.lnx_derivable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: nat, c: posreal):
--   derivable?[(LAMBDA (r: real): r >= c)]
--       (LAMBDA (x: real | x >= c): -x + k * ln(x))
--    AND
--    deriv[(LAMBDA (r: real): r >= c)]
--        (LAMBDA (x: real | x >= c): -
theorem lnx_derivable : ∀ (k : {i : ℝ // i ≥ 0}) (c : {x : {x : ℝ // x ≥ 0} // x > 0}), derivable_p[(fun (r : ℝ)=> r ≥ c)]
    (fun (x : real | x ≥ c)=> -x + k * ln x) ∧ deriv[(fun (r : ℝ)=> r ≥ c)]
    (fun (x : real | x ≥ c)=> -x + k * ln x)
 = (fun (x : real | x ≥ c)=> -1 + k / x) := by
  -- === TCCs for lnx_derivable ===
  -- TCC: lnx_derivable_TCC1
  -- Statement: FORALL (c: posreal, x: real | x >= c): x >= 0 AND x > 0
  have lnx_derivable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lnx_derivable_TCC2
  -- Statement: FORALL (c: posreal): deriv_domain?[(LAMBDA (r: real): r >= c)]
  have lnx_derivable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lnx_derivable_TCC3
  -- Statement: FORALL (c: posreal): not_one_element?[(LAMBDA (r: real): r >= c)]
  have lnx_derivable_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lnx_derivable_TCC4
  -- Statement: FORALL (k: nat, c: posreal):
  --   derivable?[(LAMBDA (r: real): r >= c)]
  --       (LAMBDA (x: real | x >= c): -x + k * ln(x))
  --    IMPLIES FORALL (x: real | x >= c): x /= 0
  have lnx_derivable_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.lnx_le
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat):
--   FORALL (x: posreal):
--     x > (2 * k) IMPLIES
--      -x + k * ln(x) <= (-1 / 2) * x + -2 * k + k * ln(2 * k) + k
theorem lnx_le : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}), ∀ (x : {x : {x : ℝ // x ≥ 0} // x > 0}), x > (2 * k) → -x + k * ln x ≤ (-1 / 2) * x + -2 * k + k * ln (2 * k) + k := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: smooth_not_analytic.lnx_neg_large
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (M1: posreal, k: posnat):
--   EXISTS (M2: posreal):
--     FORALL (x: posreal): x >= M2 IMPLIES -x + k * ln(x) <= -M1
theorem lnx_neg_large : ∀ (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (k : {i : {i : ℝ // i ≥ 0} // i > 0}), ∃ (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}), ∀ (x : {x : {x : ℝ // x ≥ 0} // x > 0}), x ≥ M2 → -x + k * ln x ≤ -M1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: smooth_not_analytic.exp_xk_small
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: nat, epsilon: posreal):
--   EXISTS (M: posreal):
--     FORALL (x: real): x >= M IMPLIES exp(-x) * x ^ k < epsilon
theorem exp_xk_small : ∀ (k : {i : ℝ // i ≥ 0}) (epsilon : {x : {x : ℝ // x ≥ 0} // x > 0}), ∃ (M : {x : {x : ℝ // x ≥ 0} // x > 0}), ∀ (x : ℝ), x ≥ M → exp -x * x ^ k < epsilon := by
  -- === TCCs for exp_xk_small ===
  -- TCC: exp_xk_small_TCC1
  -- Statement: FORALL (k: nat, M: posreal, x: real): x >= M IMPLIES x /= 0 OR k >= 0
  have exp_xk_small_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: smooth_not_analytic.exp_conv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: nat): convergent?(LAMBDA (x: posreal): exp(-1 / x) / x ^ k, 0)
theorem exp_conv : ∀ (k : {i : ℝ // i ≥ 0}), convergent_p (fun (x : {x : ℝ // 0 < x})=> exp (-1 / x) / x ^ k) 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.exp_sin_p_cos_p_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n1, n2: nat, k: nat):
--   convergent?(LAMBDA (x: real | x > 0):
--                 exp(-1 / x) *
--                  (sin(1 / x) * polynomial(a, n1)(x) +
--                    cos(1
theorem exp_sin_p_cos_p_conv : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), convergent_p (fun (x : real | x > 0)=> exp (-1 / x) *
    (sin (1 / x) * polynomial a n1(x) + cos (1 / x) * polynomial b n2(x))
    / x ^ (k)) 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.exp_lim
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], n1, n2: nat, k: nat):
--   lim(LAMBDA (x: real | x > 0):
--         exp(-1 / x) *
--          (sin(1 / x) * polynomial(a, n1)(x) +
--            cos(1 / x) * polynomial(b, n2)(x))
--   
theorem exp_lim : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (n1 : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), lim (fun (x : real | x > 0)=> exp (-1 / x) *
       (sin (1 / x) * polynomial a n1(x) +
         cos (1 / x) * polynomial b n2(x))
       / x ^ (k)) 0 = 0 := by
  -- === TCCs for exp_lim ===
  -- TCC: exp_lim_TCC1
  -- Statement: FORALL (a, b: sequence[real], n1, n2: nat, k: nat):
  --   convergent?[{x: real | x > 0}]
  --       (LAMBDA (x: real | x > 0):
  --          (polynomial(a, n1)(x) * exp(-1 / x) * sin(1 / x) +
  --            polynomi...
  have exp_lim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.derivable_nth_split
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], n: posnat):
--   derivable_n_times?(f, n - 1) AND
--    derivable_n_times?[(LAMBDA (r: real): r < 0)]
--        (restrict[real, (LAMBDA (r: real): r < 0), real](f), n)
--     AND
--     de
theorem derivable_nth_split : ∀ (f : ℝ → ℝ) (n : {i : {i : ℝ // i ≥ 0} // i > 0}), derivable_n_times_p f (n - 1) ∧
 derivable_n_times_p[(fun (r : ℝ)=> r < 0)]
     (restrict f, n)
  ∧
  derivable_n_times_p[(fun (r : ℝ)=> r > 0)]
      (restrict f, n)
   ∧ derivable_p (nderiv (n - 1) f) 0 → derivable_n_times_p f n := by
  -- === TCCs for derivable_nth_split ===
  -- TCC: derivable_nth_split_TCC1
  -- Statement: FORALL (n: posnat): n - 1 >= 0
  have derivable_nth_split_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.sm_n_derivable
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat):
--   (derivable_n_times?(sm, n)) AND
--    (EXISTS (c, d: sequence[real], n3, n4, k: nat):
--       nderiv(n, sm) =
--        (LAMBDA (x: real):
--           IF x <= 0 THEN 0
--           ELSE (exp(-1 
theorem sm_n_derivable : ∀ (n : {i : ℝ // i ≥ 0}), (derivable_n_times_p sm n) ∧ (∃ (c : _) (d : ℕ → ℝ) (n3 : _) (n4 : _) (k : ℕ), nderiv n sm =
    (fun (x : ℝ)=> if x ≤ 0 then 0
       else (exp (-1 / x) *
              (sin (1 / x) * polynomial c n3(x) +
                cos (1 / x) * polynomial d n4(x)))
             / x ^ (k)
       )) := by
  -- === TCCs for sm_n_derivable ===
  -- TCC: sm_n_derivable_TCC1
  -- Statement: FORALL (n: nat):
  --   (derivable_n_times?(sm, n)) IMPLIES
  --    FORALL (k: nat, x: real): NOT x <= 0 IMPLIES x ^ (k) /= 0
  have sm_n_derivable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.smooth_not_analytic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: smooth?(sm) AND NOT analytic?(0)(sm)
theorem smooth_not_analytic : smooth_p sm ∧ ¬ analytic_p 0(sm) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.psmxn
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def psmxn : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: smooth_not_analytic.nsmxn
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def nsmxn : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: smooth_not_analytic.nsmxn_conv_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(nsmxn, 0)
theorem nsmxn_conv_0 : convergence nsmxn 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: smooth_not_analytic.psmxn_conv_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: convergence(psmxn, 0)
theorem psmxn_conv_0 : convergence psmxn 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: smooth_not_analytic.inf
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: glb(S)
axiom inf {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ29 → pvs_τ30

-- pvs2lean: smooth_not_analytic.p1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (: (# C := 1, alpha := (: 1 :) #) :)
noncomputable def p1 : sorry := by
  sorry  -- pvs2lean: definition stub for (: (# C := 1, alpha := (: 1 :) #) :)

-- pvs2lean: smooth_not_analytic.atom1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# poly := p1, ineq := <= #)
noncomputable def atom1 : sorry := by
  sorry  -- pvs2lean: definition stub for (# poly := p1, ineq := <= #)

-- pvs2lean: smooth_not_analytic.SA
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: semi_alg((: (: atom1 :) :))(1)
axiom SA {pvs_τ1 : Type*} : pvs_τ1 → Prop

-- pvs2lean: smooth_not_analytic.non_empt_bound_sm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?[real]({xx: real | NOT SA((: sm(xx) :))}) AND
--  bounded_below?({xx: real | NOT SA((: sm(xx) :))})
theorem non_empt_bound_sm : nonempty_p ({xx: real | ¬ SA ((: sm xx :))}) ∧ bounded_below_p ({xx: real | ¬ SA ((: sm xx :))}) := by
  -- === TCCs for non_empt_bound_sm ===
  -- TCC: non_empt_bound_sm_TCC1
  -- Statement: FORALL (xx: real): length[real]((: sm(xx) :)) = 1
  have non_empt_bound_sm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: smooth_not_analytic.inf_0_sm
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: inf({xx: real | NOT SA((: sm(xx) :))}) = 0
theorem inf_0_sm : inf ({xx: real | ¬ SA ((: sm xx :))}) = 0 := by
  -- === TCCs for inf_0_sm ===
  -- TCC: inf_0_sm_TCC1
  -- Statement: nonempty?[real]({xx: real | NOT SA((: sm(xx) :))}) AND
  --  bounded_below?({xx: real | NOT SA((: sm(xx) :))})
  have inf_0_sm_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: smooth_not_analytic.not_clean_break
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: inf({xx: real | NOT SA((: sm(xx) :))}) = 0 AND
--  EXISTS (xn, yn: sequence[posreal]):
--    convergence(xn, 0) AND
--     convergence(yn, 0) AND
--      FORALL (i: nat):
--        SA((: sm(xn(i)) :)) AND
--         xn
theorem not_clean_break : inf ({xx: real | ¬ SA ((: sm xx :))}) = 0 ∧ ∃ (xn : _) (yn : ℕ → {x : ℝ // 0 < x}), convergence xn 0 ∧
   convergence yn 0 ∧
    ∀ (i : ℕ), SA ((: sm (xn i) :)) ∧
       xn i > 0 ∧ ¬ SA ((: sm (yn i) :)) ∧ yn i > 0 := by
  -- === TCCs for not_clean_break ===
  -- TCC: not_clean_break_TCC1
  -- Statement: inf({xx: real | NOT SA((: sm(xx) :))}) = 0 IMPLIES
  --  FORALL (xn, yn: sequence[posreal]):
  --    convergence(xn, 0) AND convergence(yn, 0) IMPLIES
  --     FORALL (i: nat): length[real]((: sm(xn(i)) :)) = 1
  have not_clean_break_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: not_clean_break_TCC2
  -- Statement: inf({xx: real | NOT SA((: sm(xx) :))}) = 0 IMPLIES
  --  FORALL (xn, yn: sequence[posreal]):
  --    convergence(xn, 0) AND convergence(yn, 0) IMPLIES
  --     FORALL (i: nat):
  --       SA((: sm(xn(i)) :)) AND xn(i)...
  have not_clean_break_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.smooth_not_analytic