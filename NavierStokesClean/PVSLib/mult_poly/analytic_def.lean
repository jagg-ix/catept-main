-- Auto-generated by pvs2lean/translator.py
-- PVS theory: analytic_def
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: analytic_def

Auto-generated stubs from PVSLib theory `analytic_def`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.analytic_def

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (pvs_3 : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (VectorN : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (aa : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (absolutely_convergent_series_p : pvs_τ7 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (below : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} (continuous_p : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} (conv_series_p : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} (convergent_p : pvs_τ24 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (derivable_n_times_p : pvs_τ34 → pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (factorial : pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} (ff : pvs_τ38 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (inf_sum : pvs_τ41 → pvs_τ42)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} (limit : pvs_τ43 → pvs_τ44)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} (max : pvs_τ49 → pvs_τ50 → pvs_τ51)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (min : pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} (nonempty_p : pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} (nth : pvs_τ61 → pvs_τ62 → pvs_τ63)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} (powerseq : pvs_τ64 → pvs_τ65 → pvs_τ66 → pvs_τ67)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} (powerseries : pvs_τ68 → pvs_τ69 → pvs_τ70)
variable {pvs_τ80 : Type*} {pvs_τ81 : Type*} (restrict : pvs_τ80 → pvs_τ81)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} {pvs_τ84 : Type*} (series : pvs_τ82 → pvs_τ83 → pvs_τ84)

-- pvs2lean: analytic_def.con_conv_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha, x: real, a: sequence[real]):
--   convergent?(powerseries(a)(x - alpha)) IFF
--    conv_series?(powerseq(a, x - alpha))
theorem con_conv_series : ∀ (alpha : ℝ) (x : ℝ) (a : {i : ℝ // i ≥ 0} → ℝ), convergent_p (powerseries a(x - alpha)) ↔ conv_series_p (powerseq a (x - alpha)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.ball
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(x - alpha) < M
axiom ball {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19 → Prop

-- pvs2lean: analytic_def.ball_noe
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem ball_noe : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: analytic_def.ball_dd_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem ball_dd_0 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: analytic_def.ball_dd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem ball_dd : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: analytic_def.ball_open_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (M: posreal, x: (ball(0, M))):
--   EXISTS (eps: posreal):
--     FORALL (y: real): abs(x - y) < eps IMPLIES ball(0, M)(y)
theorem ball_open_0 : ∀ (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (x : ℝ), ∃ (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), ∀ (y : ℝ), |x - y| < eps → ball 0 M(y) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: analytic_def.ball_open
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (M: posreal, alpha: real, x: (ball(alpha, M))):
--   EXISTS (eps: posreal):
--     FORALL (y: real): abs(x - y) < eps IMPLIES ball(alpha, M)(y)
theorem ball_open : ∀ (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (alpha : ℝ) (x : ℝ), ∃ (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), ∀ (y : ℝ), |x - y| < eps → ball alpha M(y) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: analytic_def.ball_con_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem ball_con_0 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: analytic_def.ball_con
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem ball_con : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: analytic_def.conv_ball
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, a: sequence[real], M: posreal):
--   (FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(x - alpha)))
--    IFF FORALL (x: (ball(0, M))): convergent?(powerseries(a)(x))
theorem conv_ball : ∀ (alpha : ℝ) (a : {i : ℝ // i ≥ 0} → ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}), (∀ (x : (ball alpha M)), convergent_p (powerseries a(x - alpha))) ↔ ∀ (x : (ball 0 M)), convergent_p (powerseries a(x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.lim_ball
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, a: sequence[real], M: posreal):
--   (FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(x - alpha)))
--    IMPLIES
--    FORALL (x: (ball(alpha, M))):
--      Inf_sum[(ball(0, M))](a)(
theorem lim_ball : ∀ (alpha : ℝ) (a : {i : ℝ // i ≥ 0} → ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}), (∀ (x : (ball alpha M)), convergent_p (powerseries a(x - alpha))) → ∀ (x : (ball alpha M)), Inf_sum[(ball 0 M)](a) (x - alpha) = limit (powerseries a(x - alpha)) := by
  -- === TCCs for lim_ball ===
  -- TCC: lim_ball_TCC1
  -- Statement: FORALL (alpha: real, a: sequence[real], M: posreal):
  --   (FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(x - alpha)))
  --    IMPLIES FORALL (x: (ball(alpha, M))): ball(0, M)(x - alpha)
  have lim_ball_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lim_ball_TCC2
  -- Statement: FORALL (alpha: real, a: sequence[real], M: posreal):
  --   (FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(x - alpha)))
  --    IMPLIES FORALL (x: (ball(alpha, M))): conv_powerseries?[(ball(0, M))...
  have lim_ball_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lim_ball_TCC3
  -- Statement: FORALL (alpha: real, a: sequence[real], M: posreal):
  --   (FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(x - alpha)))
  --    IMPLIES FORALL (x: (ball(alpha, M))): not_one_element?[(ball(0, M))]
  have lim_ball_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lim_ball_TCC4
  -- Statement: FORALL (alpha: real, a: sequence[real], M: posreal):
  --   (FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(x - alpha)))
  --    IMPLIES FORALL (x: (ball(alpha, M))): deriv_domain?[(ball(0, M))]
  have lim_ball_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lim_ball_TCC5
  -- Statement: FORALL (alpha: real, a: sequence[real], M: posreal):
  --   (FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(x - alpha)))
  --    IMPLIES
  --    FORALL (x: (ball(alpha, M))):
  --      FORALL (x: (ball(0, M)...
  have lim_ball_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lim_ball_TCC6
  -- Statement: FORALL (alpha: real, a: sequence[real], M: posreal):
  --   (FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(x - alpha)))
  --    IMPLIES
  --    FORALL (x: (ball(alpha, M))): convergent?(powerseries(a)(...
  have lim_ball_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.derivable_shift
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, M: posreal, f: [real -> real]):
--   derivable?[(ball(0, M))](restrict[real, (ball(0, M)), real](f)) IFF
--    derivable?[(ball(alpha, M))](LAMBDA (x: (ball(alpha, M))): f(x - alpha))
theorem derivable_shift : ∀ (alpha : ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (f : ℝ → ℝ), derivable_p[(ball 0 M)](restrict f) ↔ derivable_p[(ball alpha M)](fun (x : (ball alpha M))=> f (x - alpha)) := by
  -- === TCCs for derivable_shift ===
  -- TCC: derivable_shift_TCC1
  -- Statement: FORALL (M: posreal): deriv_domain?[(ball(0, M))]
  have derivable_shift_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: derivable_shift_TCC2
  -- Statement: FORALL (M: posreal): not_one_element?[(ball(0, M))]
  have derivable_shift_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: derivable_shift_TCC3
  -- Statement: FORALL (alpha: real, M: posreal): deriv_domain?[(ball(alpha, M))]
  have derivable_shift_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: derivable_shift_TCC4
  -- Statement: FORALL (alpha: real, M: posreal): not_one_element?[(ball(alpha, M))]
  have derivable_shift_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.deriv_shift
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, M: posreal, f: [real -> real]):
--   derivable?[(ball(0, M))](restrict[real, (ball(0, M)), real](f)) IMPLIES
--    FORALL (x: (ball(0, M))):
--      deriv[(ball(0, M))](restrict[real, (bal
theorem deriv_shift : ∀ (alpha : ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (f : ℝ → ℝ), derivable_p[(ball 0 M)](restrict f) → ∀ (x : (ball 0 M)), deriv[(ball 0 M)](restrict f) (x) =
   deriv[(ball alpha M)]
       (fun (x : (ball alpha M))=> f (x - alpha)) (x + alpha) := by
  -- === TCCs for deriv_shift ===
  -- TCC: deriv_shift_TCC1
  -- Statement: FORALL (alpha: real, M: posreal, f: [real -> real]):
  --   derivable?[(ball(0, M))](restrict[real, (ball(0, M)), real](f)) IMPLIES
  --    FORALL (x: (ball(0, M))): ball(alpha, M)(x + alpha)
  have deriv_shift_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_shift_TCC2
  -- Statement: FORALL (alpha: real, M: posreal, f: [real -> real]):
  --   derivable?[(ball(0, M))](restrict[real, (ball(0, M)), real](f)) IMPLIES
  --    FORALL (x: (ball(0, M))):
  --      derivable?[(ball(alpha, M))]
  --        ...
  have deriv_shift_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.derivable_shift_n
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, n: nat, M: posreal, f: [real -> real]):
--   derivable_n_times?[(ball(0, M))]
--       (restrict[real, (ball(0, M)), real](f), n)
--    IFF
--    derivable_n_times?[(ball(alpha, M))]
--        (
theorem derivable_shift_n : ∀ (alpha : ℝ) (n : {i : ℝ // i ≥ 0}) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (f : ℝ → ℝ), derivable_n_times_p[(ball 0 M)](restrict f, n) ↔ derivable_n_times_p[(ball alpha M)]
    (fun (x : (ball alpha M))=> f (x - alpha), n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.deriv_shift_n
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, n: nat, M: posreal, f: [real -> real]):
--   derivable_n_times?[(ball(0, M))]
--       (restrict[real, (ball(0, M)), real](f), n)
--    IMPLIES
--    FORALL (x1: (ball(0, M))):
--      nderiv[(b
theorem deriv_shift_n : ∀ (alpha : ℝ) (n : {i : ℝ // i ≥ 0}) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (f : ℝ → ℝ), derivable_n_times_p[(ball 0 M)](restrict f, n) → ∀ (x1 : (ball 0 M)), nderiv[(ball 0 M)](n, fun (x : (ball 0 M))=> f x) (x1) =
   nderiv[(ball alpha M)]
       (n, fun (s : (ball alpha M))=> f (s - alpha)) (x1 + alpha) := by
  -- === TCCs for deriv_shift_n ===
  -- TCC: deriv_shift_n_TCC1
  -- Statement: FORALL (n: nat, M: posreal, f: [real -> real]):
  --   derivable_n_times?[(ball(0, M))]
  --       (restrict[real, (ball(0, M)), real](f), n)
  --    IMPLIES
  --    FORALL (x1: (ball(0, M))):
  --      derivable_n_times?[...
  have deriv_shift_n_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_shift_n_TCC2
  -- Statement: FORALL (alpha: real, n: nat, M: posreal, f: [real -> real]):
  --   derivable_n_times?[(ball(0, M))]
  --       (restrict[real, (ball(0, M)), real](f), n)
  --    IMPLIES FORALL (x1: (ball(0, M))): ball(alpha, M)...
  have deriv_shift_n_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_shift_n_TCC3
  -- Statement: FORALL (alpha: real, n: nat, M: posreal, f: [real -> real]):
  --   derivable_n_times?[(ball(0, M))]
  --       (restrict[real, (ball(0, M)), real](f), n)
  --    IMPLIES
  --    FORALL (x1: (ball(0, M))):
  --      deriva...
  have deriv_shift_n_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.derivable_n_subtype
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (M1, M2: posreal, alpha: real, f: [real -> real], n: nat):
--   M1 <= M2 IMPLIES
--    derivable_n_times?[(ball(alpha, M2))]
--        (restrict[real, (ball(alpha, M2)), real](f), n)
--     IMPLIES
--     der
theorem derivable_n_subtype : ∀ (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (alpha : ℝ) (f : ℝ → ℝ) (n : {i : ℝ // i ≥ 0}), M1 ≤ M2 → derivable_n_times_p[(ball alpha M2)]
    (restrict f, n)
 →
 derivable_n_times_p[(ball alpha M1)]
     (restrict f, n) := by
  -- === TCCs for derivable_n_subtype ===
  -- TCC: derivable_n_subtype_TCC1
  -- Statement: FORALL (M1, M2: posreal, alpha: real):
  --   M1 <= M2 IMPLIES deriv_domain?[(ball(alpha, M2))]
  have derivable_n_subtype_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: derivable_n_subtype_TCC2
  -- Statement: FORALL (M1, M2: posreal, alpha: real):
  --   M1 <= M2 IMPLIES not_one_element?[(ball(alpha, M2))]
  have derivable_n_subtype_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.deriv_n_subtype
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (M1, M2: posreal, alpha: real, n: nat, f: [real -> real]):
--   (M1 <= M2 AND
--     derivable_n_times?[(ball(alpha, M2))]
--         (restrict[real, (ball(alpha, M2)), real](f), n))
--    IMPLIES
--    FORAL
theorem deriv_n_subtype : ∀ (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (alpha : ℝ) (n : {i : ℝ // i ≥ 0}) (f : ℝ → ℝ), (M1 ≤ M2 ∧
  derivable_n_times_p[(ball alpha M2)]
      (restrict f, n)) → ∀ (x : (ball alpha M1)), nderiv[(ball alpha M2)]
      (n, restrict f) (x)
   =
   nderiv[(ball alpha M1)]
       (n, restrict f) (x) := by
  -- === TCCs for deriv_n_subtype ===
  -- TCC: deriv_n_subtype_TCC1
  -- Statement: FORALL (M1, M2: posreal, alpha: real, n: nat, f: [real -> real]):
  --   M1 <= M2 AND
  --    derivable_n_times?[(ball(alpha, M2))]
  --        (restrict[real, (ball(alpha, M2)), real](f), n)
  --    IMPLIES FORALL (x...
  have deriv_n_subtype_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: deriv_n_subtype_TCC2
  -- Statement: FORALL (M1, M2: posreal, alpha: real, n: nat, f: [real -> real]):
  --   M1 <= M2 AND
  --    derivable_n_times?[(ball(alpha, M2))]
  --        (restrict[real, (ball(alpha, M2)), real](f), n)
  --    IMPLIES
  --    FORALL...
  have deriv_n_subtype_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_parts?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: {xx: real | abs(xx - alpha) < M}):
--   convergent?(powerseries(a)(x - alpha)) AND
--    f(x) = inf_sum(powerseq(a, x - alpha))
axiom analytic_parts_p {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ10 → pvs_τ11 → pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: analytic_def.analytic_rad?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (a: sequence[real]):
--   FORALL (x: {xx: real | abs(xx - alpha) < M}):
--     convergent?(powerseries(a)(x - alpha)) AND
--      f(x) = limit(powerseries(a)(x - alpha))
axiom analytic_rad_p {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16 → Prop

-- pvs2lean: analytic_def.analytic?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (M: posreal, a: sequence[real]): analytic_parts?(alpha, f)(M, a)
axiom analytic_p {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: analytic_def.analytic_der
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real], M: (analytic_rad?(alpha, f)),
--         n: nat):
--   derivable_n_times?[(ball(alpha, M))]
--       (restrict[real, (ball(alpha, M)), real](f), n)
theorem analytic_der : ∀ (alpha : ℝ) (f : ℝ → ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : ℝ // i ≥ 0}), derivable_n_times_p (restrict f) n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_cont
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real], M: (analytic_rad?(alpha, f))):
--   continuous?[(ball(alpha, M))](restrict[real, (ball(alpha, M)), real](f))
theorem analytic_cont : ∀ (alpha : ℝ) (f : ℝ → ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}), continuous_p (restrict f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_term
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], alpha: real, M: posreal, a: sequence[real]):
--   analytic_parts?(alpha, f)(M, a) IMPLIES
--    (FORALL (n: nat):
--       factorial(n) * a(n) =
--        nderiv[(ball(alpha, M))]
--      
theorem analytic_term : ∀ (f : ℝ → ℝ) (alpha : ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {i : ℝ // i ≥ 0} → ℝ), analytic_parts_p alpha f(M, a) → (∀ (n : ℕ), factorial n * a n =
    nderiv[(ball alpha M)]
        (n, restrict f) (alpha)) := by
  -- === TCCs for analytic_term ===
  -- TCC: analytic_term_TCC1
  -- Statement: FORALL (f: [real -> real], alpha: real, M: posreal, a: sequence[real]):
  --   analytic_parts?(alpha, f)(M, a) IMPLIES ball(alpha, M)(alpha)
  have analytic_term_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: analytic_term_TCC2
  -- Statement: FORALL (f: [real -> real], alpha: real, M: posreal, a: sequence[real]):
  --   analytic_parts?(alpha, f)(M, a) IMPLIES
  --    FORALL (n: nat):
  --      derivable_n_times?[(ball(alpha, M))]
  --          (restrict[re...
  have analytic_term_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_term_unique
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], alpha: real, M1, M2: posreal,
--         a1, a2: sequence[real]):
--   (analytic_parts?(alpha, f)(M1, a1) AND analytic_parts?(alpha, f)(M2, a2))
--    IMPLIES a1 = a2
theorem analytic_term_unique : ∀ (f : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a1 : {i : ℝ // i ≥ 0} → ℝ) (a2 : {i : ℝ // i ≥ 0} → ℝ), (analytic_parts_p alpha f(M1, a1) ∧ analytic_parts_p alpha f(M2, a2)) → a1 = a2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.max_rad?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: analytic_rad?(alpha, f)(M) AND
--  FORALL (M1: posreal, a: sequence[real]):
--    analytic_parts?(alpha, f)(M1, a) IMPLIES M1 <= M
axiom max_rad_p {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} : pvs_τ55 → pvs_τ56 → pvs_τ57 → Prop

-- pvs2lean: analytic_def.inf_rad?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (a: sequence[real]):
--   FORALL (x: real):
--     convergent?(powerseries(a)(x - alpha)) AND
--      f(x) = inf_sum(powerseq(a, x - alpha))
axiom inf_rad_p {pvs_τ39 : Type*} {pvs_τ40 : Type*} : pvs_τ39 → pvs_τ40 → Prop

-- pvs2lean: analytic_def.max_rad_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: (analytic?(0))):
--   inf_rad?(0, f) OR EXISTS (M: posreal): max_rad?(0, f)(M)
theorem max_rad_0 : ∀ (f : ℝ → ℝ), inf_rad_p 0 f ∨ ∃ (M : {x : ℝ // 0 < x}), max_rad_p 0 f(M) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.max_rad
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: (analytic?(alpha))):
--   inf_rad?(alpha, f) OR EXISTS (M: posreal): max_rad?(alpha, f)(M)
theorem max_rad : ∀ (alpha : ℝ) (f : ℝ → ℝ), inf_rad_p alpha f ∨ ∃ (M : {x : ℝ // 0 < x}), max_rad_p alpha f(M) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_absolute_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], alpha: real, M: posreal, a: sequence[real]):
--   analytic_parts?(alpha, f)(M, a) IMPLIES
--    FORALL (x: (ball(alpha, M))):
--      absolutely_convergent_series?(powerseq(a, x - al
theorem analytic_absolute_conv : ∀ (f : ℝ → ℝ) (alpha : ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {i : ℝ // i ≥ 0} → ℝ), analytic_parts_p alpha f(M, a) → ∀ (x : (ball alpha M)), absolutely_convergent_series_p (powerseq a (x - alpha)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.pos_series_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], alpha: real, M: posreal,
--         a: {aa: sequence[real] | FORALL (i: nat): aa(i) >= 0}):
--   convergent?(series(a)) IMPLIES
--    FORALL (i: nat): sigma(0, i, a) <= inf_sum(a)
theorem pos_series_bound : ∀ (f : ℝ → ℝ) (alpha : ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), aa i ≥ 0}), convergent_p (series a) → ∀ (i : ℕ), Finset.sum (Finset.Icc 0 i) (a) ≤ inf_sum a := by
  -- === TCCs for pos_series_bound ===
  -- TCC: pos_series_bound_TCC1
  -- Statement: FORALL (a: {aa: sequence[real] | FORALL (i: nat): aa(i) >= 0}):
  --   convergent?(series(a)) IMPLIES conv_series?(a)
  have pos_series_bound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.inf_sum_nn
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], alpha: real, M: posreal,
--         a: {aa: sequence[real] | FORALL (i: nat): aa(i) >= 0}):
--   convergent?(series(a)) IMPLIES 0 <= inf_sum(a)
theorem inf_sum_nn : ∀ (f : ℝ → ℝ) (alpha : ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // ∀ (i : ℕ), aa i ≥ 0}), convergent_p (series a) → 0 ≤ inf_sum a := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.convlf
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i <= m THEN a(i) * b(m - i) ELSE 0 ENDIF
axiom convlf {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28

-- pvs2lean: analytic_def.convlfsh
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i + sh <= m THEN a(i + sh) * b(m - i - sh) ELSE 0 ENDIF
axiom convlfsh {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33 → Prop

-- pvs2lean: analytic_def.convl_convlfsh_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, a: sequence[real], b: sequence[real], sh: nat):
--   convlf(m, a, b) = convlfsh(m, a, b, 0)
theorem convl_convlfsh_0 : ∀ (m : {i : ℝ // i ≥ 0}) (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (sh : {i : ℝ // i ≥ 0}), convlf m a b = convlfsh m a b 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: analytic_def.convl_convlfsh_
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, a: sequence[real], b: sequence[real], i, sh1, sh2: nat):
--   (i + (sh1 - sh2) <= m AND i + (sh1 - sh2) >= 0) IMPLIES
--    convlfsh(m, a, b, sh1)(i) = convlfsh(m, a, b, sh2)(i + (sh1 - sh2)
theorem convl_convlfsh_ : ∀ (m : {i : ℝ // i ≥ 0}) (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (i : {i : ℝ // i ≥ 0}) (sh1 : {i : ℝ // i ≥ 0}) (sh2 : {i : ℝ // i ≥ 0}), (i + (sh1 - sh2) ≤ m ∧ i + (sh1 - sh2) ≥ 0) → convlfsh m a b sh1(i) = convlfsh m a b sh2(i + (sh1 - sh2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.sig_convl
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, m, convlf(m, a, b))
axiom sig_convl {pvs_τ85 : Type*} {pvs_τ86 : Type*} {pvs_τ87 : Type*} : pvs_τ85 → pvs_τ86 → pvs_τ87

-- pvs2lean: analytic_def.sig_convl_front
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real], m: nat, i: below(m)):
--   sigma(i, m, convlf(m, a, b)) =
--    a(i) * b(m - i) + sigma(i + 1, m, convlf(m, a, b))
theorem sig_convl_front : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (m : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < m}), Finset.sum (Finset.Icc i m) (convlf m a b) = a i * b (m - i) + Finset.sum (Finset.Icc i + 1 m) (convlf m a b) := by
  -- === TCCs for sig_convl_front ===
  -- TCC: sig_convl_front_TCC1
  -- Statement: FORALL (m: nat, i: below(m)): m - i >= 0
  have sig_convl_front_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.sig_sig_convl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, a: sequence[real], b: sequence[real], x: real, t: nat,
--         i: nat):
--   sigma(i, t,
--         LAMBDA (k: nat): sigma(i, k, convlf(k, a, b)) * (x - alpha) ^ k)
--    =
--    a(i) *
--     s
theorem sig_sig_convl : ∀ (alpha : ℝ) (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (x : ℝ) (t : {i : ℝ // i ≥ 0}) (i : {i : ℝ // i ≥ 0}), Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> Finset.sum (Finset.Icc i k) (convlf k a b) * (x - alpha) ^ k) = a i *
 Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> if i ≤ k then b (k - i) * (x - alpha) ^ k else 0 )
 +
 Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> Finset.sum (Finset.Icc i + 1 k) (convlf k a b) * (x - alpha) ^ k) := by
  -- === TCCs for sig_sig_convl ===
  -- TCC: sig_sig_convl_TCC1
  -- Statement: FORALL (alpha: real, x: real, k: nat): (x - alpha) /= 0 OR k >= 0
  have sig_sig_convl_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sig_sig_convl_TCC2
  -- Statement: FORALL (i: nat, k: nat): i <= k IMPLIES k - i >= 0
  have sig_sig_convl_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: analytic_def.sig_sig_convl_gen
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, a: sequence[real], b: sequence[real], x: real, t: nat,
--         i: nat, j: nat):
--   sigma(i, t,
--         LAMBDA (k: nat): sigma(j, k, convlf(k, a, b)) * (x - alpha) ^ k)
--    =
--    a(j)
theorem sig_sig_convl_gen : ∀ (alpha : ℝ) (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (x : ℝ) (t : {i : ℝ // i ≥ 0}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> Finset.sum (Finset.Icc j k) (convlf k a b) * (x - alpha) ^ k) = a j *
 Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> if j ≤ k then b (k - j) * (x - alpha) ^ k else 0 )
 +
 Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> Finset.sum (Finset.Icc j + 1 k) (convlf k a b) * (x - alpha) ^ k) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: analytic_def.sig_sig_ab_init
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, a: sequence[real], b: sequence[real], x: real, t: nat,
--         i: below(t + 1), j: below(t + 1)):
--   sigma(i, t,
--         LAMBDA (k: nat): sigma(j, k, convlf(k, a, b)) * (x - alpha)
theorem sig_sig_ab_init : ∀ (alpha : ℝ) (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (x : ℝ) (t : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < 1 + t}) (j : {s : {i : ℝ // i ≥ 0} // s < 1 + t}), Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> Finset.sum (Finset.Icc j k) (convlf k a b) * (x - alpha) ^ k) = Finset.sum (Finset.Icc j t) (fun (j : ℕ)=> a j *
         Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> if j ≤ k then b (k - j) * (x - alpha) ^ k else 0 )) := by
  -- === TCCs for sig_sig_ab_init ===
  -- TCC: sig_sig_ab_init_TCC1
  -- Statement: FORALL (t: nat, i: below(t + 1), j: below(t + 1), j1: nat, k: nat):
  --   j1 <= k IMPLIES k - j1 >= 0
  have sig_sig_ab_init_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: analytic_def.sig_a_pull_conv2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, a: sequence[real], b: sequence[real], x: real, j: nat,
--         i: below(j + 1)):
--   sigma(i, j,
--         LAMBDA (k: nat): sigma(i, k, convlf(k, a, b)) * (x - alpha) ^ k)
--    =
--    sig
theorem sig_a_pull_conv2 : ∀ (alpha : ℝ) (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (x : ℝ) (j : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < 1 + j}), Finset.sum (Finset.Icc i j) (fun (k : ℕ)=> Finset.sum (Finset.Icc i k) (convlf k a b) * (x - alpha) ^ k) = Finset.sum (Finset.Icc i j) (fun (k : ℕ)=> a k *
         Finset.sum (Finset.Icc i j) (fun (m : ℕ)=> if k ≤ m then b (m - k) * (x - alpha) ^ m else 0 )) := by
  -- === TCCs for sig_a_pull_conv2 ===
  -- TCC: sig_a_pull_conv2_TCC1
  -- Statement: FORALL (j: nat, i: below(j + 1), k: nat, m: nat): k <= m IMPLIES m - k >= 0
  have sig_a_pull_conv2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sig_a_pull_conv2_TCC2
  -- Statement: FORALL (alpha: real, x: real, j: nat, i: below(j + 1), k: nat, m: nat):
  --   k <= m IMPLIES (x - alpha) /= 0 OR m >= 0
  have sig_a_pull_conv2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: analytic_def.sig_a_pull_conv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c0: real, a, b: sequence[real], x: real, n: nat, i: below(n + 1)):
--   sigma(i, n, LAMBDA (k: nat): sigma(i, k, convlf(k, a, b)) * (x - c0) ^ k)
--    =
--    sigma(i, n,
--          LAMBDA (k: nat):
--    
theorem sig_a_pull_conv : ∀ (c0 : ℝ) (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ) (x : ℝ) (n : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < 1 + n}), Finset.sum (Finset.Icc i n) (fun (k : ℕ)=> Finset.sum (Finset.Icc i k) (convlf k a b) * (x - c0) ^ k) = Finset.sum (Finset.Icc i n) (fun (k : ℕ)=> a k *
         Finset.sum (Finset.Icc i n) (fun (m : ℕ)=> if k ≤ m then b (m - k) * (x - c0) ^ m else 0 )) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.b_flip
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: sequence[real], x, alpha: real, t: nat, j: below(t + 1),
--         i: below(j + 1)):
--   sigma(i, t,
--         LAMBDA (k: nat):
--           IF j <= k THEN b(k - j) * (x - alpha) ^ k ELSE 0 ENDIF)
--  
theorem b_flip : ∀ (b : {i : ℝ // i ≥ 0} → ℝ) (x : ℝ) (alpha : ℝ) (t : {i : ℝ // i ≥ 0}) (j : {s : {i : ℝ // i ≥ 0} // s < 1 + t}) (i : {s : {i : ℝ // i ≥ 0} // s < 1 + j}), Finset.sum (Finset.Icc i t) (fun (k : ℕ)=> if j ≤ k then b (k - j) * (x - alpha) ^ k else 0 ) = (x - alpha) ^ j * Finset.sum (Finset.Icc 0 t - j) (fun (k : ℕ)=> b k * (x - alpha) ^ k) := by
  -- === TCCs for b_flip ===
  -- TCC: b_flip_TCC1
  -- Statement: FORALL (t: nat, j: below(t + 1), i: below(j + 1), k: nat):
  --   j <= k IMPLIES k - j >= 0
  have b_flip_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: b_flip_TCC2
  -- Statement: FORALL (x, alpha: real, t: nat, j: below(t + 1), i: below(j + 1)):
  --   (x - alpha) /= 0 OR j >= 0
  have b_flip_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.prod_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: powerseries(sig_convl(a, b))(x - alpha)
axiom prod_seq {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} {pvs_τ79 : Type*} : pvs_τ71 → pvs_τ72 → pvs_τ73 → pvs_τ74 → pvs_τ75 → pvs_τ76 → pvs_τ77 → pvs_τ78 → pvs_τ79

-- pvs2lean: analytic_def.arrange_terms_a
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem arrange_terms_a : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), prod_seq f1 f2 alpha M1 M2 a b x = (fun (m : ℕ)=> Finset.sum (Finset.Icc 0 m) (fun (j : ℕ)=> if j ≤ m
             then a j * (x - alpha) ^ j *
                   series (powerseq b (x - alpha)) (m - j)
           else 0
           )) := by
  -- === TCCs for arrange_terms_a ===
  -- TCC: arrange_terms_a_TCC1
  -- Statement: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
  --         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
  --         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, b...
  have arrange_terms_a_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.arrange_terms_diff
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem arrange_terms_diff : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), prod_seq f1 f2 alpha M1 M2 a b x = (fun (m : ℕ)=> Finset.sum (Finset.Icc 0 m) (fun (j : ℕ)=> if j ≤ m
             then a j * (x - alpha) ^ j *
                   (f2 x -
                     limit (series (fun (n : ℕ)=> powerseq b (x - alpha)
                                            (n + m - j + 1))))
           else 0
           )) := by
  -- === TCCs for arrange_terms_diff ===
  -- TCC: arrange_terms_diff_TCC1
  -- Statement: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
  --         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
  --         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, b...
  have arrange_terms_diff_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: arrange_terms_diff_TCC2
  -- Statement: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
  --         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
  --         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, b...
  have arrange_terms_diff_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.arrange_terms_diff_split
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem arrange_terms_diff_split : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), prod_seq f1 f2 alpha M1 M2 a b x = (fun (m : ℕ)=> series (powerseq a (x - alpha)) (m) * f2 x) -
 (fun (m : ℕ)=> Finset.sum (Finset.Icc 0 m) (fun (j : ℕ)=> if j ≤ m
              then a j * (x - alpha) ^ j *
                    limit (series (fun (n : ℕ)=> powerseq b (x - alpha) (n + m - j + 1)))
            else 0
            )) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.conv_prod_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem conv_prod_first : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), convergent_p (fun (m : {i : ℝ // i ≥ 0}) => series (powerseq a (x - alpha)) (m) * f2 x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.lim_prod_first
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem lim_prod_first : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), limit (fun (m : ℕ)=> series (powerseq a (x - alpha)) (m) * f2 x) = f1 x * f2 x := by
  -- === TCCs for lim_prod_first ===
  -- TCC: lim_prod_first_TCC1
  -- Statement: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
  --         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
  --         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, b...
  have lim_prod_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.max_below
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = 0 THEN abs(f(0)) ELSE max(abs(f(m)), max_below(m - 1)(f)) ENDIF
axiom max_below {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} : pvs_τ52 → pvs_τ53 → pvs_τ54

-- pvs2lean: analytic_def.tail_b_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem tail_b_conv : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}) (j : {i : ℝ // i ≥ 0}), convergent_p (fun (m : {i : ℝ // i ≥ 0}) => IF (j ≤ m) (limit (series (fun (n : ℕ)=> powerseq b (x - alpha) (n + m - j + 1)))) 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.tail_b_lim
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem tail_b_lim : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}) (j : {i : ℝ // i ≥ 0}), limit (fun (m : ℕ)=> if j ≤ m
          then limit (series (fun (n : ℕ)=> powerseq b (x - alpha) (n + m - j + 1)))
        else 0) = 0 := by
  -- === TCCs for tail_b_lim ===
  -- TCC: tail_b_lim_TCC1
  -- Statement: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
  --         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
  --         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, b...
  have tail_b_lim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.conv_prod_second
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem conv_prod_second : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), convergent_p (fun (m : {i : ℝ // i ≥ 0}) => sigma 0 m (fun (j : ℕ)=> if j ≤ m
     then a j * (x - alpha) ^ j *
           limit (series (fun (n : ℕ)=> powerseq b (x - alpha) (n + m - j + 1)))
   else 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.lim_prod_second
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem lim_prod_second : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), limit (fun (m : ℕ)=> Finset.sum (Finset.Icc 0 m) (fun (j : ℕ)=> if j ≤ m
                  then a j * (x - alpha) ^ j *
                        limit (series (fun (n : ℕ)=> powerseq b (x - alpha)
                                               (n + m - j + 1)))
                else 0
                )) = 0 := by
  -- === TCCs for lim_prod_second ===
  -- TCC: lim_prod_second_TCC1
  -- Statement: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
  --         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
  --         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, b...
  have lim_prod_second_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.init_prod_analytic_conv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem init_prod_analytic_conv : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), convergent_p (powerseries (sig_convl a b) (x - alpha)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.init_prod_analytic_inf_sum
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem init_prod_analytic_inf_sum : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}) (x : {xx : ℝ // |xx - alpha| < min M1 M2}), (f1 * f2) (x) = inf_sum (powerseq (sig_convl a b) (x - alpha)) := by
  -- === TCCs for init_prod_analytic_inf_sum ===
  -- TCC: init_prod_analytic_inf_sum_TCC1
  -- Statement: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
  --         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
  --         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, b...
  have init_prod_analytic_inf_sum_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.prod_analytic
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem prod_analytic : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}), analytic_parts_p alpha (f1 * f2) (min M1 M2) (sig_convl a b) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.sum_analytic
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1, f2: [real -> real], alpha: real, M1, M2: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         b: {bb: sequence[real] | analytic_parts?(alpha, f2)(M2, bb)}
theorem sum_analytic : ∀ (f1 : ℝ → ℝ) (f2 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (M2 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (b : {bb : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f2(M2, bb)}), analytic_parts_p alpha (f1 + f2) (min M1 M2) (a + b) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.scal_analytic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f1: [real -> real], alpha: real, M1: posreal,
--         a: {aa: sequence[real] | analytic_parts?(alpha, f1)(M1, aa)},
--         c: real):
--   analytic_parts?(alpha, c * f1)(M1, c * a)
theorem scal_analytic : ∀ (f1 : ℝ → ℝ) (alpha : ℝ) (M1 : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {aa : {i : ℝ // i ≥ 0} → ℝ // analytic_parts_p alpha f1(M1, aa)}) (c : ℝ), analytic_parts_p alpha (c * f1) M1 (c * a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_prod_fun
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f, g: [real -> real]):
--   analytic?(alpha)(f) AND analytic?(alpha)(g) IMPLIES
--    analytic?(alpha)(f * g)
theorem analytic_prod_fun : ∀ (alpha : ℝ) (f : ℝ → ℝ) (g : ℝ → ℝ), analytic_p alpha(f) ∧ analytic_p alpha(g) → analytic_p alpha(f * g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_const
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha, c: real): analytic?(alpha)(LAMBDA (x: real): c)
theorem analytic_const : ∀ (alpha : ℝ) (c : ℝ), analytic_p alpha (fun (x : ℝ) => c) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_sum_fun
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f, g: [real -> real]):
--   analytic?(alpha)(f) AND analytic?(alpha)(g) IMPLIES
--    analytic?(alpha)(f + g)
theorem analytic_sum_fun : ∀ (alpha : ℝ) (f : ℝ → ℝ) (g : ℝ → ℝ), analytic_p alpha(f) ∧ analytic_p alpha(g) → analytic_p alpha(f + g) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_scal_fun
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real], c: real):
--   analytic?(alpha)(f) IMPLIES analytic?(alpha)(c * f)
theorem analytic_scal_fun : ∀ (alpha : ℝ) (f : ℝ → ℝ) (c : ℝ), analytic_p alpha(f) → analytic_p alpha(c * f) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_pow_fun
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real], n: nat):
--   analytic?(alpha)(f) IMPLIES analytic?(alpha)(f ^ n)
theorem analytic_pow_fun : ∀ (alpha : ℝ) (f : ℝ → ℝ) (n : {i : ℝ // i ≥ 0}), analytic_p alpha(f) → analytic_p alpha(f ^ n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_pow_fun_scal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real], n: nat, c: real):
--   analytic?(alpha)(f) IMPLIES analytic?(alpha)(c * f ^ n)
theorem analytic_pow_fun_scal : ∀ (alpha : ℝ) (f : ℝ → ℝ) (n : {i : ℝ // i ≥ 0}) (c : ℝ), analytic_p alpha(f) → analytic_p alpha(c * f ^ n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_pos_ball
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real]):
--   (analytic?(alpha)(f) AND f(alpha) > 0) IMPLIES
--    EXISTS (delta: posreal):
--      FORALL (x: real): abs(x - alpha) < delta IMPLIES f(x) > 0
theorem analytic_pos_ball : ∀ (alpha : ℝ) (f : ℝ → ℝ), (analytic_p alpha(f) ∧ f alpha > 0) → ∃ (delta : {x : ℝ // 0 < x}), ∀ (x : ℝ), |x - alpha| < delta → f x > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_neg_ball
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real]):
--   (analytic?(alpha)(f) AND f(alpha) < 0) IMPLIES
--    EXISTS (delta: posreal):
--      FORALL (x: real): abs(x - alpha) < delta IMPLIES f(x) < 0
theorem analytic_neg_ball : ∀ (alpha : ℝ) (f : ℝ → ℝ), (analytic_p alpha(f) ∧ f alpha < 0) → ∃ (delta : {x : ℝ // 0 < x}), ∀ (x : ℝ), |x - alpha| < delta → f x < 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.nz_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def nz_seq : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: analytic_def.lowest_nz_der
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real], M: (analytic_rad?(alpha, f))):
--   (NOT ((EXISTS (delta: posreal):
--            FORALL (x: real): abs(x - alpha) < delta IMPLIES f(x) = 0)))
--    IMPLIES
--    EXISTS (n
theorem lowest_nz_der : ∀ (alpha : ℝ) (f : ℝ → ℝ) (M : {x : {x : ℝ // x ≥ 0} // x > 0}), (¬ ((∃ (delta : {x : ℝ // 0 < x}), ∀ (x : ℝ), |x - alpha| < delta → f x = 0))) → ∃ (n : ℕ), nderiv[(ball alpha M)]
      (n, restrict f) (alpha)
   ≠ 0 := by
  -- === TCCs for lowest_nz_der ===
  -- TCC: lowest_nz_der_TCC1
  -- Statement: FORALL (alpha: real, f: [real -> real], M: (analytic_rad?(alpha, f))):
  --   (NOT ((EXISTS (delta: posreal):
  --            FORALL (x: real): abs(x - alpha) < delta IMPLIES f(x) = 0)))
  --    IMPLIES ball(alph...
  have lowest_nz_der_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lowest_nz_der_TCC2
  -- Statement: FORALL (alpha: real, f: [real -> real], M: (analytic_rad?(alpha, f))):
  --   (NOT ((EXISTS (delta: posreal):
  --            FORALL (x: real): abs(x - alpha) < delta IMPLIES f(x) = 0)))
  --    IMPLIES
  --    FORALL...
  have lowest_nz_der_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.lowdn
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min({n: nat |
--        nderiv[(ball(alpha, M))]
--            (n, restrict[real, (ball(alpha, M)), real](f))(alpha)
--         /= 0})
axiom lowdn {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48

-- pvs2lean: analytic_def.lowdn_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def lowdn_seq : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: analytic_def.analytic_zero_ball
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real]):
--   (analytic?(alpha)(f) AND f(alpha) = 0) IMPLIES
--    (EXISTS (delta: posreal):
--       FORALL (x: real):
--         (abs(x - alpha) < delta AND x /= alpha) IMPLIES f
theorem analytic_zero_ball : ∀ (alpha : ℝ) (f : ℝ → ℝ), (analytic_p alpha(f) ∧ f alpha = 0) → (∃ (delta : {x : ℝ // 0 < x}), ∀ (x : ℝ), (|x - alpha| < delta ∧ x ≠ alpha) → f x ≠ 0)
 ∨
 (∃ (delta : {x : ℝ // 0 < x}), ∀ (x : ℝ), |x - alpha| < delta → f x = 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: analytic_def.analytic_zero_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, f: [real -> real]):
--   (analytic?(alpha)(f) AND f(alpha) = 0) IMPLIES
--    (((EXISTS (delta: posreal):
--         FORALL (x: posreal): x < delta IMPLIES f(alpha + x) = 0)
--       OR
--      
theorem analytic_zero_right : ∀ (alpha : ℝ) (f : ℝ → ℝ), (analytic_p alpha(f) ∧ f alpha = 0) → (((∃ (delta : {x : ℝ // 0 < x}), ∀ (x : {x : ℝ // 0 < x}), x < delta → f (alpha + x) = 0)
   ∨
   (∃ (delta : {x : ℝ // 0 < x}), ∀ (x : {x : ℝ // 0 < x}), x < delta → f (alpha + x) > 0))
  ∨
  (∃ (delta : {x : ℝ // 0 < x}), ∀ (x : {x : ℝ // 0 < x}), x < delta → f (alpha + x) < 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.analytic_def