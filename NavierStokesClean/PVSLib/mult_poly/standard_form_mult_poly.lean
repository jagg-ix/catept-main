-- Auto-generated by pvs2lean/translator.py
-- PVS theory: standard_form_mult_poly
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: standard_form_mult_poly

Auto-generated stubs from PVSLib theory `standard_form_mult_poly`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.standard_form_mult_poly

noncomputable section

variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (append : pvs_τ8 → pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (below : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (bubblesort : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} (car : pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (cdr : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (cons : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} (cons_p : pvs_τ20 → Prop)
variable {pvs_τ24 : Type*} (dichotomous_p : pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (every : pvs_τ25 → pvs_τ26)
variable {pvs_τ28 : Type*} (integer_pred : pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} (is_sorted_p : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} (last : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} (length : pvs_τ31 → Prop)
variable {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} (map : pvs_τ43 → pvs_τ44 → pvs_τ45)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (max : pvs_τ46 → pvs_τ47 → pvs_τ48)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} (member : pvs_τ52 → pvs_τ53 → pvs_τ54)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (nth : pvs_τ65 → pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} (null_p : pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} (number_field_pred : pvs_τ68 → Prop)
variable {pvs_τ69 : Type*} (rational_pred : pvs_τ69 → Prop)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} (rdc : pvs_τ70 → pvs_τ71)
variable {pvs_τ72 : Type*} (real_pred : pvs_τ72 → Prop)
variable {pvs_τ73 : Type*} (reflexive_p : pvs_τ73 → Prop)
variable {pvs_τ82 : Type*} (total_preorder_p : pvs_τ82 → Prop)
variable {pvs_τ83 : Type*} (transitive_p : pvs_τ83 → Prop)

-- pvs2lean: standard_form_mult_poly.Unif?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: null?(p) OR
--  (FORALL (i, j: below(length(p))):
--     length(nth(p, i)`alpha) = length(nth(p, j)`alpha))
axiom Unif_p {pvs_τ1 : Type*} : pvs_τ1 → Prop

-- pvs2lean: standard_form_mult_poly.Unif_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, p: MultPoly | cons?(p) AND Unif?(p)):
--   length(m`alpha) = length(car(p)`alpha) IMPLIES Unif?(cons(m, p))
theorem Unif_cons : ∀ (m : List ℝ × ℝ) (p : {p : List (List ℝ × ℝ) // cons_p p ∧ Unif_p p}), List.length m.alpha = List.length (car p.alpha) → Unif_p (cons m p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.caretzero_every
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat):
--   every[number]
--       (LAMBDA (x: number):
--               number_field_pred(x) AND real_pred(x) AND rational_pred(x)
--           AND integer_pred(x) AND (x >= 0))
--       ((: 0 :) ^ n)
theorem caretzero_every : ∀ (n : {i : ℝ // i ≥ 0}), every (fun (x : ℝ) => number_field_pred x ∧ real_pred x ∧ rational_pred x ∧ integer_pred x ∧ (x ≥ 0)) ((: 0 :) ^ n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.sum
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(l) THEN 0 ELSE car(l) + sum(cdr(l)) ENDIF
axiom sum {pvs_τ79 : Type*} : pvs_τ79 → Prop

-- pvs2lean: standard_form_mult_poly.last_last_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]): cons?(l) IMPLIES (last(l) = nth(l, length(l) - 1))
theorem last_last_nth : ∀ (l : List ℝ), cons_p l → (last l = nth l (List.length l - 1)) := by
  -- === TCCs for last_last_nth ===
  -- TCC: last_last_nth_TCC1
  -- Statement: FORALL (l: list[nat]):
  --   cons?(l) IMPLIES
  --    length[nat](l) - 1 >= 0 AND length[nat](l) - 1 < length[nat](l)
  have last_last_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.sum_nat
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]):
--   rational_pred(sum(l)) AND integer_pred(sum(l)) AND sum(l) >= 0
theorem sum_nat : ∀ (l : List ℝ), rational_pred (sum l) ∧ integer_pred (sum l) ∧ sum l ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.sum_append
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[nat]): sum(append(l1, l2)) = sum(l1) + sum(l2)
theorem sum_append : ∀ (l1 : List ℝ) (l2 : List ℝ), sum (append l1 l2) = sum l1 + sum l2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.sum_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat): sum((: 0 :) ^ n) = 0
theorem sum_0 : ∀ (n : {i : ℝ // i ≥ 0}), sum ((: 0 :) ^ n) = 0 := by
  -- === TCCs for sum_0 ===
  -- TCC: sum_0_TCC1
  -- Statement: every[number]
  --     (LAMBDA (x: number):
  --             number_field_pred(x) AND real_pred(x) AND rational_pred(x)
  --         AND integer_pred(x) AND (x >= 0))
  --     ((: 0 :))
  have sum_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.sum_append_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat], n: nat): sum(append(l, (: 0 :) ^ n)) = sum(l)
theorem sum_append_0 : ∀ (l : List ℝ) (n : {i : ℝ // i ≥ 0}), sum (append l ((: 0 :) ^ n)) = sum l := by
  -- === TCCs for sum_append_0 ===
  -- TCC: sum_append_0_TCC1
  -- Statement: FORALL (n: nat):
  --   every[number]
  --       (LAMBDA (x: number):
  --               number_field_pred(x) AND real_pred(x) AND rational_pred(x)
  --           AND integer_pred(x) AND (x >= 0))
  --       (^[real]((: 0 ...
  have sum_append_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.sum_0_con
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]): sum(l) = 0 IMPLIES EXISTS (n: nat): l = (: 0 :) ^ n
theorem sum_0_con : ∀ (l : List ℝ), sum l = 0 → ∃ (n : ℕ), l = (: 0 :) ^ n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.add_mono
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# C := m1`C + m2`C, alpha := m1`alpha #)
axiom add_mono {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ2 → pvs_τ3 → pvs_τ4

-- pvs2lean: standard_form_mult_poly.add_mono_commutative
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1: monomial, m2: {mm: monomial | mm`alpha = m1`alpha}):
--   add_mono(m1, m2) = add_mono(m2, m1)
theorem add_mono_commutative : ∀ (m1 : List ℝ × ℝ) (m2 : {mm : List ℝ × ℝ // mm.alpha = m1.alpha}), add_mono m1 m2 = add_mono m2 m1 := by
  -- === TCCs for add_mono_commutative ===
  -- TCC: add_mono_commutative_TCC1
  -- Statement: FORALL (m1: monomial, m2: {mm: monomial | mm`alpha = m1`alpha}):
  --   m1`alpha = m2`alpha
  have add_mono_commutative_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.term_deg
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(p) < 1 THEN null
-- ELSE cons(sum(car(p)`alpha), term_deg(cdr(p)))
-- ENDIF
axiom term_deg {pvs_τ80 : Type*} {pvs_τ81 : Type*} : pvs_τ80 → pvs_τ81

-- pvs2lean: standard_form_mult_poly.term_deg_example
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem term_deg_example : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: standard_form_mult_poly.homogen?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF p = null THEN TRUE
-- ELSIF FORALL (j: below(length(term_deg(p)) - 1)):
--         nth(term_deg(p), j) = nth(term_deg(p), j + 1)
--   THEN TRUE
-- ELSE FALSE
-- ENDIF
noncomputable def homogen_p : sorry := by
  sorry  -- pvs2lean: definition stub for IF p = null THEN TRUE ELSIF FORALL (j: below(length(term_deg(p)) - 1)):         

-- pvs2lean: standard_form_mult_poly.max_deg
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(p) THEN 0 ELSE max(car(term_deg(p)), max_deg(cdr(p))) ENDIF
axiom max_deg {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ49 → pvs_τ50

-- pvs2lean: standard_form_mult_poly.max_length
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF p = null THEN 0 ELSE max(length(car(p)`alpha), max_length(cdr(p))) ENDIF
axiom max_length {pvs_τ51 : Type*} : pvs_τ51 → Prop

-- pvs2lean: standard_form_mult_poly.max_length_ex1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max_length((: (# C := 0, alpha := (: :) #) :)) = 0
theorem max_length_ex1 : max_length ((: (# C := 0, alpha := (: :) #) :)) = 0 := by
  -- === TCCs for max_length_ex1 ===
  -- TCC: max_length_ex1_TCC1
  -- Statement: every[[# alpha: list[real], C: real #]]
  --     (LAMBDA (t: [# alpha: list[real], C: real #]):
  --        every[number]
  --            (LAMBDA (x: number):
  --                    number_field_pred(x) AND real_pred...
  have max_length_ex1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.max_length_ex2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max_length((: (# C := 1, alpha := (: 0, 0 :) #),
--               (# C := 2, alpha := (: 3, 4 :) #),
--               (# C := -1, alpha := (: 2, 5 :) #) :))
--  = 2
theorem max_length_ex2 : max_length ((: (# C := 1, alpha := (: 0, 0 :) #),
              (# C := 2, alpha := (: 3, 4 :) #),
              (# C := -1, alpha := (: 2, 5 :) #) :)) = 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.max_length_is_max
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: {q: MultPoly | cons?(q)}, j: below(length(p))):
--   length(nth(p, j)`alpha) <= max_length(p)
theorem max_length_is_max : ∀ (p : {q : List (List ℝ × ℝ) // cons_p q}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length p}), List.length (nth p j.alpha) ≤ max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.max_length_is_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: {q: MultPoly | cons?(q)}):
--   EXISTS (j: below(length(p))): length(nth(p, j)`alpha) = max_length(p)
theorem max_length_is_nth : ∀ (p : {q : List (List ℝ × ℝ) // cons_p q}), ∃ (j : {s : {i : ℝ // i ≥ 0} // s < List.length p}), List.length (nth p j.alpha) = max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.max_length_Unif
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: {q: UnifInd | cons?(q)}, j: below(length(p))):
--   length(nth(p, j)`alpha) = max_length(p)
theorem max_length_Unif : ∀ (p : {q : {ll : List (List ℝ × ℝ) // ∀ (i : _) (j : Finset.range List.length ll), List.length (nth ll i.alpha) = List.length (nth ll j.alpha)} // cons_p q}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length p}), List.length (nth p j.alpha) = max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.max_length_Unif_cdr
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: {q: UnifInd | cons?(q) AND cons?(cdr(q))}):
--   max_length(cdr(p)) = max_length(p)
theorem max_length_Unif_cdr : ∀ (p : {q : {ll : List (List ℝ × ℝ) // ∀ (i : _) (j : Finset.range List.length ll), List.length (nth ll i.alpha) = List.length (nth ll j.alpha)} // cons_p q ∧ cons_p (cdr q)}), max_length (cdr p) = max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.length_a
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF p = null THEN 0 ELSE length(car(p)`alpha) + length_a(cdr(p)) ENDIF
axiom length_a {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ32 → pvs_τ33

-- pvs2lean: standard_form_mult_poly.max_length_a
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): max_length(p) * length(p) >= length_a(p)
theorem max_length_a : ∀ (p : List (List ℝ × ℝ)), max_length p * List.length p ≥ length_a p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(p) < 1 THEN null
-- ELSIF 0 <= length(car(p)`alpha) < n
--   THEN cons((# C := car(p)`C,
--                alpha
--                  := append(car(p)`alpha,
--                            (: 0 :) ^ (n - len
axiom lft {pvs_τ36 : Type*} {pvs_τ37 : Type*} : pvs_τ36 → pvs_τ37 → Prop

-- pvs2lean: standard_form_mult_poly.lft_one
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# C := m`C, alpha := append(m`alpha, (: 0 :) ^ (n - length(m`alpha))) #)
axiom lft_one {pvs_τ41 : Type*} {pvs_τ42 : Type*} : pvs_τ41 → pvs_τ42 → Prop

-- pvs2lean: standard_form_mult_poly.lft_one_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, n: nat | n >= length(m`alpha)):
--   length(lft_one(n)(m)`alpha) = n
theorem lft_one_length : ∀ (m : List ℝ × ℝ) (n : {n : {i : ℝ // i ≥ 0} // n ≥ List.length m.alpha}), List.length (lft_one n(m).alpha) = n := by
  -- === TCCs for lft_one_length ===
  -- TCC: lft_one_length_TCC1
  -- Statement: FORALL (m: monomial, n: nat | n >= length[nat](m`alpha)):
  --   n >= length[nat](m`alpha)
  have lft_one_length_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_alt
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(lft_one(n))(p)
axiom lft_alt {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} : pvs_τ38 → pvs_τ39 → pvs_τ40

-- pvs2lean: standard_form_mult_poly.lft_is_alt
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, (n: nat | n >= max_length(p))):
--   lft(p)(n) = lft_alt(p)(n)
theorem lft_is_alt : ∀ (p : List (List ℝ × ℝ)) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max_length p}), lft p(n) = lft_alt p(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_example1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem lft_example1 : True := by
  -- === TCCs for lft_example1 ===
  -- TCC: lft_example1_TCC1
  -- Statement: FORALL (mp: MultPoly):
  --   mp =
  --    (: (# C := 1, alpha := (: 0, 0 :) #), (# C := 2, alpha := (: 3, 4 :) #),
  --       (# C := -1, alpha := (: 2, 5 :) #) :)
  --    IMPLIES 3 >= max_length(mp)
  have lft_example1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: standard_form_mult_poly.Lft_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)}):
--   length(p) = length(lft(p)(n))
theorem Lft_length : ∀ (p : List (List ℝ × ℝ)) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), List.length p = List.length (lft p(n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.max_length_lft
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)}):
--   max_length(p) <= max_length(lft(p)(n))
theorem max_length_lft : ∀ (p : List (List ℝ × ℝ)) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), max_length p ≤ max_length (lft p(n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_length_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: {q: MultPoly | cons?(q)}, n: {nn: nat | nn >= max_length(p)}):
--   FORALL (j: below(length(p))): length(nth(lft(p)(n), j)`alpha) = n
theorem lft_length_nth : ∀ (p : {q : List (List ℝ × ℝ) // cons_p q}) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), ∀ (j : {s : {i : ℝ // i ≥ 0} // s < List.length p}), List.length (nth (lft p(n)) j.alpha) = n := by
  -- === TCCs for lft_length_nth ===
  -- TCC: lft_length_nth_TCC1
  -- Statement: FORALL (p: {q: MultPoly | cons?[monomial](q)},
  --         n: {nn: nat | nn >= max_length(p)}, j: below(length[monomial](p))):
  --   j < length[monomial](lft(p)(n))
  have lft_length_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_nth_C
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)},
--         i: below(length(p))):
--   nth(p, i)`C = nth(lft(p)(n), i)`C
theorem lft_nth_C : ∀ (p : List (List ℝ × ℝ)) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}), nth p i.C = nth (lft p(n)) i.C := by
  -- === TCCs for lft_nth_C ===
  -- TCC: lft_nth_C_TCC1
  -- Statement: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)},
  --         i: below(length[monomial](p))):
  --   i < length[monomial](lft(p)(n))
  have lft_nth_c_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_nth_alpha
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)},
--         i: below(length(p))):
--   nth(lft(p)(n), i)`alpha =
--    append(nth(p, i)`alpha, (: 0 :) ^ (n - length(nth(p, i)`alpha)))
theorem lft_nth_alpha : ∀ (p : List (List ℝ × ℝ)) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}), nth (lft p(n)) i.alpha = append (nth p i.alpha) ((: 0 :) ^ (n - List.length (nth p i.alpha))) := by
  -- === TCCs for lft_nth_alpha ===
  -- TCC: lft_nth_alpha_TCC1
  -- Statement: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)},
  --         i: below(length[monomial](p))):
  --   (n - length[nat](nth[monomial](p, i)`alpha)) >= 0
  have lft_nth_alpha_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_Unif
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): Unif?(lft(p)(max_length(p))) = TRUE
theorem lft_Unif : ∀ (p : List (List ℝ × ℝ)), Unif_p (lft p(max_length p)) = True := by
  -- === TCCs for lft_Unif ===
  -- TCC: lft_Unif_TCC1
  -- Statement: FORALL (p: MultPoly): max_length(p) >= max_length(p)
  have lft_unif_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_n_last
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)},
--         j: below(length(p))):
--   n /= 0 AND length(nth(p, j)`alpha) = n IMPLIES
--    last(nth(p, j)`alpha) = last(nth(lft(p)(n), j)`alpha)
theorem lft_n_last : ∀ (p : List (List ℝ × ℝ)) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length p}), n ≠ 0 ∧ List.length (nth p j.alpha) = n → last (nth p j.alpha) = last (nth (lft p(n)) j.alpha) := by
  -- === TCCs for lft_n_last ===
  -- TCC: lft_n_last_TCC1
  -- Statement: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)},
  --         j: below(length[monomial](p))):
  --   n /= 0 AND length(nth(p, j)`alpha) = n IMPLIES
  --    cons?[real](nth[monomial](p, j)`alpha)
  have lft_n_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lft_n_last_TCC2
  -- Statement: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)},
  --         j: below(length[monomial](p))):
  --   n /= 0 AND length(nth(p, j)`alpha) = n IMPLIES
  --    cons?[real](nth[monomial](lft(p)(n), j)`alpha)
  have lft_n_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_n_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: {nn: nat | nn >= max_length(p)}):
--   cons?(p) IMPLIES max_length(lft(p)(n)) = n
theorem lft_n_max_length : ∀ (p : List (List ℝ × ℝ)) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), cons_p p → max_length (lft p(n)) = n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (length(l1`alpha) < length(l2`alpha))
--  OR (null?(l1`alpha) AND null?(l2`alpha))
--  OR (length(l1`alpha) = length(l2`alpha) AND
--       (sum(l1`alpha) < sum(l2`alpha)))
--  OR (length(l1`alpha) = length(
axiom leq {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ34 → pvs_τ35 → Prop

-- pvs2lean: standard_form_mult_poly.leq_reflexive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: reflexive?[monomial](leq)
theorem leq_reflexive : reflexive_p leq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq_transitive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: transitive?[monomial](leq)
theorem leq_transitive : transitive_p leq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq_dichotomous
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dichotomous?[monomial](leq)
theorem leq_dichotomous : dichotomous_p leq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.almost_total_order
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2: monomial):
--   leq(m1, m2) AND leq(m2, m1) IMPLIES m1`alpha = m2`alpha
theorem almost_total_order : ∀ (m1 : List ℝ × ℝ) (m2 : List ℝ × ℝ), leq m1 m2 ∧ leq m2 m1 → m1.alpha = m2.alpha := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq_total_preorder
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: total_preorder?[monomial](leq)
theorem leq_total_preorder : total_preorder_p leq := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_sort
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(p) THEN null ELSE bubblesort(lft(p)(max_length(p))) ENDIF
axiom mv_sort {pvs_τ60 : Type*} {pvs_τ61 : Type*} : pvs_τ60 → pvs_τ61

-- pvs2lean: standard_form_mult_poly.mv_sort_ex1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem mv_sort_ex1 : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: standard_form_mult_poly.sort_of_unif
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): Unif?(mv_sort(p))
theorem sort_of_unif : ∀ (p : List (List ℝ × ℝ)), Unif_p (mv_sort p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_sort_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): max_length(mv_sort(p)) = max_length(p)
theorem mv_sort_max_length : ∀ (p : List (List ℝ × ℝ)), max_length (mv_sort p) = max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_is_sorted?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Unif?(p) AND is_sorted?(p)
axiom mv_is_sorted_p {pvs_τ59 : Type*} : pvs_τ59 → Prop

-- pvs2lean: standard_form_mult_poly.mv_sort_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): mv_is_sorted?(mv_sort(p)) = TRUE
theorem mv_sort_is_sorted : ∀ (p : List (List ℝ × ℝ)), mv_is_sorted_p (mv_sort p) = True := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cons_smaller_to_sorted_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, m: monomial):
--   cons?(p) AND is_sorted?(p) AND leq(m, car(p)) IMPLIES
--    is_sorted?(cons(m, p))
theorem cons_smaller_to_sorted_is_sorted : ∀ (p : List (List ℝ × ℝ)) (m : List ℝ × ℝ), cons_p p ∧ is_sorted_p p ∧ leq m (car p) → is_sorted_p (cons m p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.add_mono_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, m1: monomial,
--         m2: {mm: monomial | m1`alpha = mm`alpha}):
--   cons?(p) AND is_sorted?(p) AND leq(add_mono(m1, m2), car(p)) IMPLIES
--    is_sorted?(cons(add_mono(m1, m2), p))
theorem add_mono_sorted : ∀ (p : List (List ℝ × ℝ)) (m1 : List ℝ × ℝ) (m2 : {mm : List ℝ × ℝ // m1.alpha = mm.alpha}), cons_p p ∧ is_sorted_p p ∧ leq (add_mono m1 m2) (car p) → is_sorted_p (cons (add_mono m1 m2) p) := by
  -- === TCCs for add_mono_sorted ===
  -- TCC: add_mono_sorted_TCC1
  -- Statement: FORALL (p: MultPoly, m1: monomial,
  --         m2: {mm: monomial | m1`alpha = mm`alpha}):
  --   cons?(p) AND is_sorted?(p) IMPLIES m2`alpha = m1`alpha
  have add_mono_sorted_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.constant_doesnt_matter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2, m3: monomial):
--   (leq(m1, m3) AND m1`alpha = m2`alpha) IMPLIES leq(m2, m3)
theorem constant_doesnt_matter : ∀ (m1 : List ℝ × ℝ) (m2 : List ℝ × ℝ) (m3 : List ℝ × ℝ), (leq m1 m3 ∧ m1.alpha = m2.alpha) → leq m2 m3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.constant_doesnt_matter_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2, m3: monomial):
--   (leq(m1, m2) AND m2`alpha = m3`alpha) IMPLIES leq(m1, m3)
theorem constant_doesnt_matter_last : ∀ (m1 : List ℝ × ℝ) (m2 : List ℝ × ℝ) (m3 : List ℝ × ℝ), (leq m1 m2 ∧ m2.alpha = m3.alpha) → leq m1 m3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.append_leq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem append_leq : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: standard_form_mult_poly.simplified?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF p = null OR length(p) = 1 THEN TRUE
-- ELSIF FORALL (i, j: below(length(p))):
--         i /= j IMPLIES nth(p, i)`alpha /= nth(p, j)`alpha
--   THEN TRUE
-- ELSE FALSE
-- ENDIF
axiom simplified_p {pvs_τ74 : Type*} : pvs_τ74 → Prop

-- pvs2lean: standard_form_mult_poly.sortedsimplify
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF p = null THEN null
-- ELSIF cdr(p) = null THEN p
-- ELSIF car(p)`alpha = car(cdr(p))`alpha
--   THEN sortedsimplify(cons(add_mono(car(p), car(cdr(p))), cdr(cdr(p))))
-- ELSE cons(car(p), sortedsimplify(cdr(p))
axiom sortedsimplify {pvs_τ77 : Type*} {pvs_τ78 : Type*} : pvs_τ77 → pvs_τ78

-- pvs2lean: standard_form_mult_poly.simplify
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF is_sorted?(p) THEN sortedsimplify(p)
-- ELSE sortedsimplify(mv_sort(p))
-- ENDIF
axiom simplify {pvs_τ75 : Type*} {pvs_τ76 : Type*} : pvs_τ75 → pvs_τ76

-- pvs2lean: standard_form_mult_poly.simplify_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND simplified?(p)): simplified?(cdr(p))
theorem simplify_cdr : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ simplified_p p}), simplified_p (cdr p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.simplify_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | is_sorted?(p)):
--   cons?(p) IMPLIES cons?(sortedsimplify(p))
theorem simplify_cons : ∀ (p : {p : List (List ℝ × ℝ) // is_sorted_p p}), cons_p p → cons_p (sortedsimplify p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq_simplify
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND is_sorted?(p), m: monomial):
--   leq(m, car(p)) IMPLIES leq(m, car(sortedsimplify(p)))
theorem leq_simplify : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ is_sorted_p p}) (m : List ℝ × ℝ), leq m (car p) → leq m (car (sortedsimplify p)) := by
  -- === TCCs for leq_simplify ===
  -- TCC: leq_simplify_TCC1
  -- Statement: FORALL (p: MultPoly | cons?[monomial](p) AND is_sorted?[monomial, leq](p),
  --         m: monomial):
  --   leq(m, car(p)) IMPLIES cons?[monomial](sortedsimplify(p))
  have leq_simplify_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.sorted_simp_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: {q: MultPoly | is_sorted?(q)}): is_sorted?(sortedsimplify(p))
theorem sorted_simp_is_sorted : ∀ (p : {q : List (List ℝ × ℝ) // is_sorted_p q}), is_sorted_p (sortedsimplify p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cons_smaller_simplify
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND is_sorted?(p) AND simplified?(p),
--         m: monomial):
--   leq(m, car(p)) AND m`alpha /= car(p)`alpha IMPLIES simplified?(cons(m, p))
theorem cons_smaller_simplify : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ is_sorted_p p ∧ simplified_p p}) (m : List ℝ × ℝ), leq m (car p) ∧ m.alpha ≠ car p.alpha → simplified_p (cons m p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cons_simplify
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND simplified?(p), m: monomial):
--   (FORALL (j: below(length(p))): m`alpha /= nth(p, j)`alpha) IMPLIES
--    simplified?(cons(m, p))
theorem cons_simplify : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ simplified_p p}) (m : List ℝ × ℝ), (∀ (j : Finset.range List.length p), m.alpha ≠ nth p j.alpha) → simplified_p (cons m p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.car_equal_simplify
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND is_sorted?(p)):
--   car(p)`alpha = car(sortedsimplify(p))`alpha
theorem car_equal_simplify : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ is_sorted_p p}), car p.alpha = car (sortedsimplify p).alpha := by
  -- === TCCs for car_equal_simplify ===
  -- TCC: car_equal_simplify_TCC1
  -- Statement: FORALL (p: MultPoly | cons?[monomial](p) AND is_sorted?[monomial, leq](p)):
  --   cons?[monomial](sortedsimplify(p))
  have car_equal_simplify_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.length_sortedsimplify_car_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, m1, m2: {m: monomial | is_sorted?(cons(m, p))}):
--   m1`alpha = m2`alpha IMPLIES
--    length(sortedsimplify(cons(m1, p))) = length(sortedsimplify(cons(m2, p)))
theorem length_sortedsimplify_car_eq : ∀ (p : List (List ℝ × ℝ)) (m1 : {m : List ℝ × ℝ // is_sorted_p (cons m p)}) (m2 : {m : List ℝ × ℝ // is_sorted_p (cons m p)}), m1.alpha = m2.alpha → List.length (sortedsimplify (cons m1 p)) = List.length (sortedsimplify (cons m2 p)) := by
  -- === TCCs for length_sortedsimplify_car_eq ===
  -- TCC: length_sortedsimplify_car_eq_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         m1,
  --         m2:
  --           {m: monomial | is_sorted?[monomial, leq](cons[monomial](m, p))}):
  --   m1`alpha = m2`alpha IMPLIES
  --    is_sorted?[monomial, leq](cons[monomial](m1...
  have length_sortedsimplify_car_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: length_sortedsimplify_car_eq_TCC2
  -- Statement: FORALL (p: MultPoly,
  --         m1,
  --         m2:
  --           {m: monomial | is_sorted?[monomial, leq](cons[monomial](m, p))}):
  --   m1`alpha = m2`alpha IMPLIES
  --    is_sorted?[monomial, leq](cons[monomial](m2...
  have length_sortedsimplify_car_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.car_const_nth_sortedsimplify
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, m1, m2: {m: monomial | is_sorted?(cons(m, p))}):
--   m1`alpha = m2`alpha IMPLIES
--    FORALL (i: below(length(sortedsimplify(cons(m1, p))))):
--      nth(sortedsimplify(cons(m1, p)), i)`
theorem car_const_nth_sortedsimplify : ∀ (p : List (List ℝ × ℝ)) (m1 : {m : List ℝ × ℝ // is_sorted_p (cons m p)}) (m2 : {m : List ℝ × ℝ // is_sorted_p (cons m p)}), m1.alpha = m2.alpha → ∀ (i : Finset.range List.length (sortedsimplify (cons m1 p))), nth (sortedsimplify (cons m1 p)) i.alpha =
   nth (sortedsimplify (cons m2 p)) i.alpha := by
  -- === TCCs for car_const_nth_sortedsimplify ===
  -- TCC: car_const_nth_sortedsimplify_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         m1,
  --         m2:
  --           {m: monomial | is_sorted?[monomial, leq](cons[monomial](m, p))}):
  --   m1`alpha = m2`alpha IMPLIES
  --    FORALL (i:
  --              below(length[monomi...
  have car_const_nth_sortedsimplify_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.not_equal_simplify
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND is_sorted?(p), m: monomial):
--   m`alpha /= car(p)`alpha IMPLIES m`alpha /= car(sortedsimplify(p))`alpha
theorem not_equal_simplify : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ is_sorted_p p}) (m : List ℝ × ℝ), m.alpha ≠ car p.alpha → m.alpha ≠ car (sortedsimplify p).alpha := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.simplify_alpha
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND is_sorted?(p)):
--   FORALL (j: below(length(sortedsimplify(p)))):
--     EXISTS (i: below(length(p))):
--       nth(p, i)`alpha = nth(sortedsimplify(p), j)`alpha
theorem simplify_alpha : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ is_sorted_p p}), ∀ (j : {s : {i : ℝ // i ≥ 0} // s < List.length (sortedsimplify p)}), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}), nth p i.alpha = nth (sortedsimplify p) j.alpha := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.simplify_alpha_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND is_sorted?(p)):
--   FORALL (i: below(length(p))):
--     EXISTS (j: below(length(sortedsimplify(p)))):
--       nth(p, i)`alpha = nth(sortedsimplify(p), j)`alpha
theorem simplify_alpha_reverse : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ is_sorted_p p}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}), ∃ (j : {s : {i : ℝ // i ≥ 0} // s < List.length (sortedsimplify p)}), nth p i.alpha = nth (sortedsimplify p) j.alpha := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.simplified
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | is_sorted?(p)): simplified?(sortedsimplify(p)) = TRUE
theorem simplified : ∀ (p : {p : List (List ℝ × ℝ) // is_sorted_p p}), simplified_p (sortedsimplify p) = True := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.simplify_Unif
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | is_sorted?(p) AND Unif?(p)): Unif?(sortedsimplify(p))
theorem simplify_Unif : ∀ (p : {p : List (List ℝ × ℝ) // is_sorted_p p ∧ Unif_p p}), Unif_p (sortedsimplify p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_sorted_simp_is_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: {q: MultPoly | mv_is_sorted?(q)}):
--   mv_is_sorted?(sortedsimplify(p))
theorem mv_sorted_simp_is_sorted : ∀ (p : {q : List (List ℝ × ℝ) // mv_is_sorted_p q}), mv_is_sorted_p (sortedsimplify p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.max_length_sorted_simplify
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | is_sorted?(p)):
--   max_length(p) = max_length(sortedsimplify(p))
theorem max_length_sorted_simplify : ∀ (p : {p : List (List ℝ × ℝ) // is_sorted_p p}), max_length p = max_length (sortedsimplify p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.is_sorted_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   is_sorted?(p) IFF
--    (FORALL (i, j: below(length(p))):
--       i < j IMPLIES leq(nth(p, i), nth(p, j)))
theorem is_sorted_nth : ∀ (p : List (List ℝ × ℝ)), is_sorted_p p ↔ (∀ (i : _) (j : Finset.range List.length p), i < j → leq (nth p i) (nth p j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null = p THEN TRUE
-- ELSIF EXISTS (i: below(length(p))): nth(p, i)`C = 0 THEN FALSE
-- ELSE TRUE
-- ENDIF
axiom allnonzero_p {pvs_τ7 : Type*} : pvs_τ7 → Prop

-- pvs2lean: standard_form_mult_poly.allnonzero
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null = p THEN p
-- ELSIF car(p)`C = 0 THEN allnonzero(cdr(p))
-- ELSE cons(car(p), allnonzero(cdr(p)))
-- ENDIF
axiom allnonzero {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6

-- pvs2lean: standard_form_mult_poly.allnonzero_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   cons?(p) AND allnonzero?(p) IMPLIES allnonzero?(cdr(p))
theorem allnonzero_cdr : ∀ (p : List (List ℝ × ℝ)), cons_p p ∧ allnonzero_p p → allnonzero_p (cdr p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.all_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): allnonzero?(allnonzero(p))
theorem all_nonzero : ∀ (p : List (List ℝ × ℝ)), allnonzero_p (allnonzero p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero_alpha
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p)):
--   FORALL (j: below(length(p))):
--     nth(p, j)`C /= 0 IMPLIES
--      EXISTS (k: below(length(allnonzero(p)))):
--        k <= j AND nth(p, j)`alpha = nth(allnonzero(p), k)`a
theorem allnonzero_alpha : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p}), ∀ (j : {s : {i : ℝ // i ≥ 0} // s < List.length p}), nth p j.C ≠ 0 → ∃ (k : Finset.range List.length (allnonzero p)), k ≤ j ∧ nth p j.alpha = nth (allnonzero p) k.alpha := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero_alpha_reverse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p)):
--   cons?(allnonzero(p)) IMPLIES
--    FORALL (k: below(length(allnonzero(p)))):
--      EXISTS (j: below(length(p))):
--        nth(p, j)`C /= 0 AND nth(p, j)`alpha = nth(allnon
theorem allnonzero_alpha_reverse : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p}), cons_p (allnonzero p) → ∀ (k : Finset.range List.length (allnonzero p)), ∃ (j : Finset.range List.length p), nth p j.C ≠ 0 ∧ nth p j.alpha = nth (allnonzero p) k.alpha := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): length(allnonzero(p)) <= length(p)
theorem allnonzero_length : ∀ (p : List (List ℝ × ℝ)), List.length (allnonzero p) ≤ List.length p := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero_sorted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): mv_is_sorted?(p) IMPLIES mv_is_sorted?(allnonzero(p))
theorem allnonzero_sorted : ∀ (p : List (List ℝ × ℝ)), mv_is_sorted_p p → mv_is_sorted_p (allnonzero p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero_simp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): simplified?(p) IMPLIES simplified?(allnonzero(p))
theorem allnonzero_simp : ∀ (p : List (List ℝ × ℝ)), simplified_p p → simplified_p (allnonzero p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero_lft
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: nat):
--   allnonzero?(p) AND n >= max_length(p) IMPLIES allnonzero?(lft(p)(n))
theorem allnonzero_lft : ∀ (p : List (List ℝ × ℝ)) (n : {i : ℝ // i ≥ 0}), allnonzero_p p ∧ n ≥ max_length p → allnonzero_p (lft p(n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): max_length(allnonzero(p)) <= max_length(p)
theorem allnonzero_max_length : ∀ (p : List (List ℝ × ℝ)), max_length (allnonzero p) ≤ max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.allnonzero_id
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): allnonzero?(p) IMPLIES p = allnonzero(p)
theorem allnonzero_id : ∀ (p : List (List ℝ × ℝ)), allnonzero_p p → p = allnonzero p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.minlength?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(p) THEN TRUE
-- ELSIF FORALL (i: below(length(p))): null?(nth(p, i)`alpha) THEN TRUE
-- ELSIF Unif?(p) AND
--        (EXISTS (j: below(length(p))):
--           cons?(nth(p, j)`alpha) AND last(nth(p, j)`
axiom minlength_p {pvs_τ55 : Type*} : pvs_τ55 → Prop

-- pvs2lean: standard_form_mult_poly.minlength_unif
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): minlength?(p) IMPLIES Unif?(p)
theorem minlength_unif : ∀ (p : List (List ℝ × ℝ)), minlength_p p → Unif_p p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cutting
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF l = null THEN null ELSIF last(l) = 0 THEN cutting(rdc(l)) ELSE l ENDIF
axiom cutting {pvs_τ23 : Type*} : pvs_τ23 → Prop

-- pvs2lean: standard_form_mult_poly.mv_cut
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null = p THEN p
-- ELSIF cons?(car(p)`alpha) AND EXISTS (n: posnat): member(n, car(p)`alpha)
--   THEN cons((# C := car(p)`C, alpha := cutting(car(p)`alpha) #),
--             mv_cut(cdr(p)))
-- ELSE cons((# C
axiom mv_cut {pvs_τ56 : Type*} : pvs_τ56 → Prop

-- pvs2lean: standard_form_mult_poly.cut_monom
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# C := m`C, alpha := cutting(m`alpha) #)
axiom cut_monom {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22

-- pvs2lean: standard_form_mult_poly.last_is_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: (cons?[nat])): last(l) = nth(l, length(l) - 1)
theorem last_is_nth : ∀ (l : List ℝ), last l = nth l (List.length l - 1) := by
  -- === TCCs for last_is_nth ===
  -- TCC: last_is_nth_TCC1
  -- Statement: FORALL (l: (cons?[nat])):
  --   length[nat](l) - 1 >= 0 AND length[nat](l) - 1 < length[nat](l)
  have last_is_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cut_monom_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial):
--   cut_monom(m)`alpha = null IFF
--    NOT (cons?(m`alpha) AND EXISTS (n: posnat): member(n, m`alpha))
theorem cut_monom_null : ∀ (m : List ℝ × ℝ), cut_monom m.alpha = null ↔ ¬ (cons_p m.alpha ∧ ∃ (n : {n : ℕ // 0 < n}), member n m.alpha) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_cut_alt
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: map(cut_monom)(p)
axiom mv_cut_alt {pvs_τ57 : Type*} {pvs_τ58 : Type*} : pvs_τ57 → pvs_τ58

-- pvs2lean: standard_form_mult_poly.mv_cut_is_alt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): mv_cut(p) = mv_cut_alt(p)
theorem mv_cut_is_alt : ∀ (p : List (List ℝ × ℝ)), mv_cut p = mv_cut_alt p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cutting_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]): length(l) >= length(cutting(l))
theorem cutting_length : ∀ (l : List ℝ), List.length l ≥ List.length (cutting l) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cutting_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat], i: below(length(cutting(l)))):
--   nth(cutting(l), i) = nth(l, i)
theorem cutting_nth : ∀ (l : List ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < List.length (cutting l)}), nth (cutting l) i = nth l i := by
  -- === TCCs for cutting_nth ===
  -- TCC: cutting_nth_TCC1
  -- Statement: FORALL (l: list[nat], i: below(length[nat](cutting(l)))): i < length[nat](l)
  have cutting_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cutting_last
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]): cutting(l) = null OR last(cutting(l)) /= 0
theorem cutting_last : ∀ (l : List ℝ), cutting l = null ∨ last (cutting l) ≠ 0 := by
  -- === TCCs for cutting_last ===
  -- TCC: cutting_last_TCC1
  -- Statement: FORALL (l: list[nat]): NOT cutting(l) = null IMPLIES cons?[real](cutting(l))
  have cutting_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cutting_alt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]):
--   l = append(cutting(l), (: 0 :) ^ (length(l) - length(cutting(l))))
theorem cutting_alt : ∀ (l : List ℝ), l = append (cutting l) ((: 0 :) ^ (List.length l - List.length (cutting l))) := by
  -- === TCCs for cutting_alt ===
  -- TCC: cutting_alt_TCC1
  -- Statement: FORALL (l: list[nat]): (length[nat](l) - length[nat](cutting(l))) >= 0
  have cutting_alt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cutting_sum
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]): sum(l) = sum(cutting(l))
theorem cutting_sum : ∀ (l : List ℝ), sum l = sum (cutting l) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_cut_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): length(p) = length(mv_cut(p))
theorem mv_cut_length : ∀ (p : List (List ℝ × ℝ)), List.length p = List.length (mv_cut p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_cut_C
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, i: below(length(p))): nth(p, i)`C = nth(mv_cut(p), i)`C
theorem mv_cut_C : ∀ (p : List (List ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}), nth p i.C = nth (mv_cut p) i.C := by
  -- === TCCs for mv_cut_C ===
  -- TCC: mv_cut_C_TCC1
  -- Statement: FORALL (p: MultPoly, i: below(length[monomial](p))):
  --   i < length[monomial](mv_cut(p))
  have mv_cut_c_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_cut_alpha
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, i: below(length(p))):
--   nth(mv_cut(p), i)`alpha = cutting(nth(p, i)`alpha)
theorem mv_cut_alpha : ∀ (p : List (List ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}), nth (mv_cut p) i.alpha = cutting (nth p i.alpha) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.null_cut_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]):
--   cons?(l) AND null?(cutting(l)) IMPLIES null?(cutting(cdr(l)))
theorem null_cut_cdr : ∀ (l : List ℝ), cons_p l ∧ null_p (cutting l) → null_p (cutting (cdr l)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.zero_n_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, i: below(n)): nth((: 0 :) ^ n, i) = 0
theorem zero_n_nth : ∀ (n : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < n}), nth ((: 0 :) ^ n) i = 0 := by
  -- === TCCs for zero_n_nth ===
  -- TCC: zero_n_nth_TCC1
  -- Statement: FORALL (n: nat, i: below(n)): i < length[real](^[real]((: 0 :), n))
  have zero_n_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cutting_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1: list[nat]):
--   (cons?(cutting(l1)) AND cons?(l1)) IMPLIES
--    cutting(cdr(l1)) = cdr(cutting(l1))
theorem cutting_cdr : ∀ (l1 : List ℝ), (cons_p (cutting l1) ∧ cons_p l1) → cutting (cdr l1) = cdr (cutting l1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cutting_append_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[nat], n, m: nat):
--   (length(l1) = length(l2) AND
--     append(cutting(l1), (: 0 :) ^ n) = append(cutting(l2), (: 0 :) ^ m))
--    IMPLIES length(cutting(l1)) = length(cutting(l2))
theorem cutting_append_length : ∀ (l1 : List ℝ) (l2 : List ℝ) (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}), (List.length l1 = List.length l2 ∧
  append (cutting l1) ((: 0 :) ^ n) = append (cutting l2) ((: 0 :) ^ m)) → List.length (cutting l1) = List.length (cutting l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.max_length_cut
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): max_length(p) >= max_length(mv_cut(p))
theorem max_length_cut : ∀ (p : List (List ℝ × ℝ)), max_length p ≥ max_length (mv_cut p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_cut_last_nonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p)):
--   LET P = mv_cut(p) IN
--     FORALL (j: below(length(P))):
--       null?(nth(P, j)`alpha) OR last(nth(P, j)`alpha) /= 0
theorem mv_cut_last_nonzero : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p}), fun (P : List (List ℝ × ℝ)) => ∀ (j : {s : {i : ℝ // i ≥ 0} // s < List.length P}), null_p (nth P j.alpha) ∨ last (nth P j.alpha) ≠ 0 (mv_cut p) := by
  -- === TCCs for mv_cut_last_nonzero ===
  -- TCC: mv_cut_last_nonzero_TCC1
  -- Statement: FORALL (p: MultPoly | cons?[monomial](p), P: MultPoly):
  --   P = mv_cut(p) IMPLIES
  --    FORALL (j: below(length[monomial](P))):
  --      NOT null?(nth(P, j)`alpha) IMPLIES
  --       cons?[real](nth[monomial](P,...
  have mv_cut_last_nonzero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_max_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p)):
--   max_length(p) <= 0 IMPLIES
--    LET P = lft(p)(0) IN FORALL (j: below(length(P))): null?(nth(P, j)`alpha)
theorem lft_max_0 : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p}), max_length p ≤ 0 → (let P := lft p(0); ∀ (j : Finset.range List.length P), null_p (nth P j.alpha)) := by
  -- === TCCs for lft_max_0 ===
  -- TCC: lft_max_0_TCC1
  -- Statement: FORALL (p: MultPoly | cons?[monomial](p)):
  --   max_length(p) <= 0 IMPLIES 0 >= max_length(p)
  have lft_max_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_minlength
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p)):
--   LET P = lft(p)(max_length(p)) IN
--     (EXISTS (j: below(length(p))):
--        cons?(nth(p, j)`alpha) AND
--         length(nth(p, j)`alpha) = max_length(p) AND
--          la
theorem lft_minlength : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p}), fun (P : List (List ℝ × ℝ)) => (∃ (j : Finset.range List.length p), cons_p (nth p j.alpha) ∧
    List.length (nth p j.alpha) = max_length p ∧ last (nth p j.alpha) ≠ 0) → minlength_p P (lft p (max_length p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_all_null
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p)):
--   LET P = lft(p)(max_length(p)) IN
--     (FORALL (j: below(length(P))): null?(nth(p, j)`alpha)) IMPLIES
--      minlength?(P)
theorem lft_all_null : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p}), fun (P : List (List ℝ × ℝ)) => (∀ (j : Finset.range List.length P), null_p (nth p j.alpha)) → minlength_p P (lft p (max_length p)) := by
  -- === TCCs for lft_all_null ===
  -- TCC: lft_all_null_TCC1
  -- Statement: FORALL (p: MultPoly | cons?[monomial](p), P: MultPoly):
  --   P = lft(p)(max_length(p)) IMPLIES
  --    FORALL (j: below(length[monomial](P))): j < length[monomial](p)
  have lft_all_null_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_mv_cut
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p)):
--   LET P = lft(mv_cut(p))(max_length(mv_cut(p))) IN minlength?(P)
theorem lft_mv_cut : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p}), fun (P : List (List ℝ × ℝ)) => minlength_p P (lft (mv_cut p) (max_length (mv_cut p))) := by
  -- === TCCs for lft_mv_cut ===
  -- TCC: lft_mv_cut_TCC1
  -- Statement: FORALL (p: MultPoly | cons?[monomial](p)):
  --   max_length(mv_cut(p)) >= max_length(mv_cut(p))
  have lft_mv_cut_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.minlength_cdr
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   cons?(p) AND minlength?(p) IMPLIES
--    minlength?(lft(mv_cut(cdr(p)))(max_length(mv_cut(cdr(p)))))
theorem minlength_cdr : ∀ (p : List (List ℝ × ℝ)), cons_p p ∧ minlength_p p → minlength_p (lft (mv_cut (cdr p)) (max_length (mv_cut (cdr p)))) := by
  -- === TCCs for minlength_cdr ===
  -- TCC: minlength_cdr_TCC1
  -- Statement: FORALL (p: MultPoly):
  --   cons?(p) AND minlength?(p) IMPLIES
  --    max_length(mv_cut(cdr[monomial](p))) >=
  --     max_length(mv_cut(cdr[monomial](p)))
  have minlength_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.minlength_simplify
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   cons?(p) AND is_sorted?(p) AND minlength?(p) IMPLIES
--    minlength?(sortedsimplify(p))
theorem minlength_simplify : ∀ (p : List (List ℝ × ℝ)), cons_p p ∧ is_sorted_p p ∧ minlength_p p → minlength_p (sortedsimplify p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_sorted
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND Unif?(p) AND is_sorted?(p),
--         n: {nn: nat | nn >= max_length(p)}):
--   LET P = lft(p)(n) IN is_sorted?(P)
theorem lft_sorted : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ Unif_p p ∧ is_sorted_p p}) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), fun (P : List (List ℝ × ℝ)) => is_sorted_p P (lft p n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_allnonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | allnonzero?(p), n: {nn: nat | nn >= max_length(p)}):
--   allnonzero?(lft(p)(n))
theorem lft_allnonzero : ∀ (p : {p : List (List ℝ × ℝ) // allnonzero_p p}) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), allnonzero_p (lft p n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.cut_allnonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | allnonzero?(p)): allnonzero?(mv_cut(p))
theorem cut_allnonzero : ∀ (p : {p : List (List ℝ × ℝ) // allnonzero_p p}), allnonzero_p (mv_cut p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_cut_allnonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | allnonzero?(p),
--         n: {nn: nat | nn >= max_length(mv_cut(p))}):
--   allnonzero?(lft(mv_cut(p))(n))
theorem lft_cut_allnonzero : ∀ (p : {p : List (List ℝ × ℝ) // allnonzero_p p}) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length (mv_cut p)}), allnonzero_p (lft (mv_cut p) n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_cut_is_lft
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, n: nat):
--   n >= length(m`alpha) IMPLIES lft_one(n)(cut_monom(m)) = lft_one(n)(m)
theorem lft_cut_is_lft : ∀ (m : List ℝ × ℝ) (n : {i : ℝ // i ≥ 0}), n ≥ List.length m.alpha → lft_one n(cut_monom m) = lft_one n(m) := by
  -- === TCCs for lft_cut_is_lft ===
  -- TCC: lft_cut_is_lft_TCC1
  -- Statement: FORALL (m: monomial, n: nat):
  --   n >= length(m`alpha) IMPLIES n >= length[nat](cut_monom(m)`alpha)
  have lft_cut_is_lft_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_alpha
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p), n: {nn: nat | nn >= max_length(p)}):
--   FORALL (i: below(length(p))):
--     append(nth(p, i)`alpha, (: 0 :) ^ (n - length(nth(p, i)`alpha))) =
--      nth(lft(p)(n), i)`alpha
theorem lft_alpha : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p}) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}), append (nth p i.alpha) ((: 0 :) ^ (n - List.length (nth p i.alpha))) =
 nth (lft p(n)) i.alpha ∨ nth p i.alpha = nth (lft p(n)) i.alpha := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_simplify
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((p: MultPoly | simplified?(p) AND Unif?(p)),
--         n: {nn: nat | nn >= max_length(p)}):
--   simplified?(lft(p)(n))
theorem lft_simplify : ∀ (p : {p : List (List ℝ × ℝ) // simplified_p p ∧ Unif_p p}) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), simplified_p (lft p n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_cut_alpha
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((p: MultPoly | simplified?(p) AND Unif?(p)),
--         n: {nn: nat | nn >= max_length(p)}):
--   simplified?(lft(mv_cut(p))(n))
theorem lft_cut_alpha : ∀ (p : {p : List (List ℝ × ℝ) // simplified_p p ∧ Unif_p p}) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length p}), simplified_p (lft (mv_cut p) n) := by
  -- === TCCs for lft_cut_alpha ===
  -- TCC: lft_cut_alpha_TCC1
  -- Statement: FORALL ((p: MultPoly | simplified?(p) AND Unif?(p)),
  --         n: {nn: nat | nn >= max_length(p)}):
  --   n >= max_length(mv_cut(p))
  have lft_cut_alpha_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.lft_cut_alpha_mv_cut
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((p: MultPoly | simplified?(p) AND Unif?(p)),
--         n: {nn: nat | nn >= max_length(mv_cut(p))}):
--   simplified?(lft(mv_cut(p))(n))
theorem lft_cut_alpha_mv_cut : ∀ (p : {p : List (List ℝ × ℝ) // simplified_p p ∧ Unif_p p}) (n : {nn : {i : ℝ // i ≥ 0} // nn ≥ max_length (mv_cut p)}), simplified_p (lft (mv_cut p) n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.minlength_sorted
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly | cons?(p) AND minlength?(p)): minlength?(mv_sort(p))
theorem minlength_sorted : ∀ (p : {p : List (List ℝ × ℝ) // cons_p p ∧ minlength_p p}), minlength_p (mv_sort p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_cut_lft_alpha
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, i: below(length(p)),
--         n: nat | n >= max_length(mv_cut(p))):
--   nth(lft(mv_cut(p))(n), i)`alpha =
--    append(cutting(nth(p, i)`alpha),
--           (: 0 :) ^ (n - length(cutting(
theorem mv_cut_lft_alpha : ∀ (p : List (List ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max_length (mv_cut p)}), nth (lft (mv_cut p) (n)) i.alpha = append (cutting (nth p i.alpha)) ((: 0 :) ^ (n - List.length (cutting (nth p i.alpha)))) := by
  -- === TCCs for mv_cut_lft_alpha ===
  -- TCC: mv_cut_lft_alpha_TCC1
  -- Statement: FORALL (p: MultPoly, i: below(length[monomial](p)),
  --         n: nat | n >= max_length(mv_cut(p))):
  --   i < length[monomial](lft(mv_cut(p))(n))
  have mv_cut_lft_alpha_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mv_cut_lft_alpha_TCC2
  -- Statement: FORALL (p: MultPoly, i: below(length[monomial](p)),
  --         n: nat | n >= max_length(mv_cut(p))):
  --   (n - length[nat](cutting(nth[monomial](p, i)`alpha))) >= 0
  have mv_cut_lft_alpha_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq_Os
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n, m: nat, m1, m2: monomial):
--   (n <= m AND (m1`alpha = (: 0 :) ^ n AND m2`alpha = (: 0 :) ^ m)) IMPLIES
--    leq(m1, m2)
theorem leq_Os : ∀ (n : {i : ℝ // i ≥ 0}) (m : {i : ℝ // i ≥ 0}) (m1 : List ℝ × ℝ) (m2 : List ℝ × ℝ), (n ≤ m ∧ (m1.alpha = (: 0 :) ^ n ∧ m2.alpha = (: 0 :) ^ m)) → leq m1 m2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq_0_all
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2: monomial, n: nat):
--   (length(m1`alpha) = length(m2`alpha) AND m1`alpha = (: 0 :) ^ n) IMPLIES
--    leq(m1, m2)
theorem leq_0_all : ∀ (m1 : List ℝ × ℝ) (m2 : List ℝ × ℝ) (n : {i : ℝ // i ≥ 0}), (List.length m1.alpha = List.length m2.alpha ∧ m1.alpha = (: 0 :) ^ n) → leq m1 m2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq_lft_monom
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2: monomial,
--         n: nat
--             | n >=
--                max(length(cut_monom(m1)`alpha),
--                    length(cut_monom(m2)`alpha))):
--   (leq(m1, m2) AND length(m1`alpha) = leng
theorem leq_lft_monom : ∀ (m1 : List ℝ × ℝ) (m2 : List ℝ × ℝ) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max (List.length (cut_monom m1.alpha)) (List.length (cut_monom m2.alpha))}), (leq m1 m2 ∧ List.length m1.alpha = List.length m2.alpha) → leq (lft_one n(cut_monom m1)) (lft_one n(cut_monom m2)) := by
  -- === TCCs for leq_lft_monom ===
  -- TCC: leq_lft_monom_TCC1
  -- Statement: FORALL (m1, m2: monomial,
  --         n: nat
  --             | n >=
  --                max(length[nat](cut_monom(m1)`alpha),
  --                    length[nat](cut_monom(m2)`alpha))):
  --   leq(m1, m2) AND length(m1`a...
  have leq_lft_monom_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: leq_lft_monom_TCC2
  -- Statement: FORALL (m1, m2: monomial,
  --         n: nat
  --             | n >=
  --                max(length[nat](cut_monom(m1)`alpha),
  --                    length[nat](cut_monom(m2)`alpha))):
  --   leq(m1, m2) AND length(m1`a...
  have leq_lft_monom_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.leq_lft_mv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, i, j: below(length(p)),
--         n: nat | n >= max_length(mv_cut(p))):
--   (leq(nth(p, i), nth(p, j)) AND Unif?(p)) IMPLIES
--    leq(nth(lft(mv_cut(p))(n), i), nth(lft(mv_cut(p))(n), j
theorem leq_lft_mv : ∀ (p : List (List ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < List.length p}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length p}) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max_length (mv_cut p)}), (leq (nth p i) (nth p j) ∧ Unif_p p) → leq (nth (lft (mv_cut p) (n)) i) (nth (lft (mv_cut p) (n)) j) := by
  -- === TCCs for leq_lft_mv ===
  -- TCC: leq_lft_mv_TCC1
  -- Statement: FORALL (p: MultPoly, i, j: below(length[monomial](p)),
  --         n: nat | n >= max_length(mv_cut(p))):
  --   leq(nth(p, i), nth(p, j)) AND Unif?(p) IMPLIES
  --    j < length[monomial](lft(mv_cut(p))(n))
  have leq_lft_mv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.sorted_mv_lift
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: nat | n >= max_length(mv_cut(p))):
--   is_sorted?(p) AND Unif?(p) IMPLIES is_sorted?(lft(mv_cut(p))(n))
theorem sorted_mv_lift : ∀ (p : List (List ℝ × ℝ)) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max_length (mv_cut p)}), is_sorted_p p ∧ Unif_p p → is_sorted_p (lft (mv_cut p) (n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_standard_form?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(p) THEN TRUE
-- ELSIF minlength?(p) AND allnonzero?(p) AND is_sorted?(p) AND simplified?(p)
--   THEN TRUE
-- ELSE FALSE
-- ENDIF
axiom mv_standard_form_p {pvs_τ64 : Type*} : pvs_τ64 → Prop

-- pvs2lean: standard_form_mult_poly.false_counter
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF mv_standard_form?(p) = TRUE THEN 0
-- ELSIF mv_is_sorted?(p) = FALSE THEN 4
-- ELSIF simplified?(p) = FALSE THEN 3
-- ELSIF allnonzero?(p) = FALSE THEN 2
-- ELSE 1
-- ENDIF
axiom false_counter {pvs_τ27 : Type*} : pvs_τ27 → Prop

-- pvs2lean: standard_form_mult_poly.f_count_le4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): false_counter(p) <= 4
theorem f_count_le4 : ∀ (p : List (List ℝ × ℝ)), false_counter p ≤ 4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.f_count_mv_sort
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   NOT mv_standard_form?(p) = TRUE AND mv_is_sorted?(p) = FALSE IMPLIES
--    false_counter(mv_sort(p)) < false_counter(p)
theorem f_count_mv_sort : ∀ (p : List (List ℝ × ℝ)), ¬ mv_standard_form_p p = True ∧ mv_is_sorted_p p = False → false_counter (mv_sort p) < false_counter p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.f_count_simplify
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   NOT mv_standard_form?(p) = TRUE AND
--    NOT mv_is_sorted?(p) = FALSE AND simplified?(p) = FALSE
--    IMPLIES false_counter(simplify(p)) < false_counter(p)
theorem f_count_simplify : ∀ (p : List (List ℝ × ℝ)), ¬ mv_standard_form_p p = True ∧
 ¬ mv_is_sorted_p p = False ∧ simplified_p p = False → false_counter (simplify p) < false_counter p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.f_count_all_nz
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly):
--   NOT mv_standard_form?(p) = TRUE AND
--    NOT mv_is_sorted?(p) = FALSE AND
--     NOT simplified?(p) = FALSE AND allnonzero?(p) = FALSE
--    IMPLIES false_counter(allnonzero(p)) < fals
theorem f_count_all_nz : ∀ (p : List (List ℝ × ℝ)), ¬ mv_standard_form_p p = True ∧
 ¬ mv_is_sorted_p p = False ∧
  ¬ simplified_p p = False ∧ allnonzero_p p = False → false_counter (allnonzero p) < false_counter p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.mv_standard_form
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF mv_standard_form?(p) = TRUE THEN p
-- ELSIF mv_is_sorted?(p) = FALSE THEN mv_standard_form(mv_sort(p))
-- ELSIF simplified?(p) = FALSE THEN mv_standard_form(simplify(p))
-- ELSIF allnonzero?(p) = FALSE THEN
axiom mv_standard_form {pvs_τ62 : Type*} {pvs_τ63 : Type*} : pvs_τ62 → pvs_τ63

-- pvs2lean: standard_form_mult_poly.standard_form_is_standard
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): mv_standard_form?(mv_standard_form(p)) = TRUE
theorem standard_form_is_standard : ∀ (p : List (List ℝ × ℝ)), mv_standard_form_p (mv_standard_form p) = True := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_mult_poly.standard_form_max_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly): max_length(mv_standard_form(p)) <= max_length(p)
theorem standard_form_max_length : ∀ (p : List (List ℝ × ℝ)), max_length (mv_standard_form p) ≤ max_length p := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.standard_form_mult_poly