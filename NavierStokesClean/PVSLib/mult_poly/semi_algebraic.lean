-- Auto-generated by pvs2lean/translator.py
-- PVS theory: semi_algebraic
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: semi_algebraic

Auto-generated stubs from PVSLib theory `semi_algebraic`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.semi_algebraic

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (VectorN : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (append : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (below : pvs_τ13 → pvs_τ14)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (car : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (cdr : pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} (complement : pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (cons : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} (cons_p : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (difference : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (full_eval : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (ineq : pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (intersection : pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (length : pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (max : pvs_τ41 → pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (max_length : pvs_τ44 → pvs_τ45)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} (nth : pvs_τ55 → pvs_τ56 → pvs_τ57)
variable {pvs_τ58 : Type*} (null_p : pvs_τ58 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} (union : pvs_τ62 → pvs_τ63 → Prop)

-- pvs2lean: semi_algebraic.atom_eval
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atom`ineq(full_eval(atom`poly)(x), 0)
axiom atom_eval {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ9 → pvs_τ10 → Prop

-- pvs2lean: semi_algebraic.negative_atom
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF a`ineq = > THEN (# poly := a`poly, ineq := <= #)
-- ELSIF a`ineq = <= THEN (# poly := a`poly, ineq := > #)
-- ELSIF a`ineq = < THEN (# poly := a`poly, ineq := >= #)
-- ELSE (# poly := a`poly, ineq := < #)
-- E
axiom negative_atom {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ49 → pvs_τ50

-- pvs2lean: semi_algebraic.negative_atom_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: atomic_poly) (x: list[real] | length(x) >= max_length(a`poly)):
--   atom_eval(a)(x) = (NOT atom_eval(negative_atom(a))(x))
theorem negative_atom_eval : ∀ (a : ({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))), ∀ (x : {x : List ℝ // List.length x ≥ max_length a.poly}), atom_eval a(x) = (¬ atom_eval (negative_atom a) (x)) := by
  -- === TCCs for negative_atom_eval ===
  -- TCC: negative_atom_eval_TCC1
  -- Statement: FORALL (a: atomic_poly,
  --         x: list[real] | length[real](x) >= max_length(a`poly)):
  --   length[real](x) >= max_length(negative_atom(a)`poly)
  have negative_atom_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.atom_max
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = null THEN 0 ELSE max(max_length(car(m)`poly), atom_max(cdr(m))) ENDIF
axiom atom_max {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12

-- pvs2lean: semi_algebraic.atom_max_max
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: meeting, i: below(length(m))):
--   atom_max(m) >= max_length(nth(m, i)`poly)
theorem atom_max_max : ∀ (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length m}), atom_max m ≥ max_length (nth m i.poly) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.atom_max_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: meeting):
--   null?(m) OR
--    EXISTS (i: below(length(m))): atom_max(m) = max_length(nth(m, i)`poly)
theorem atom_max_nth : ∀ (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))), null_p m ∨ ∃ (i : Finset.range List.length m), atom_max m = max_length (nth m i.poly) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.max_atom
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2: meeting):
--   atom_max(append(m1, m2)) = max(atom_max(m1), atom_max(m2))
theorem max_atom : ∀ (m1 : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (m2 : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))), atom_max (append m1 m2) = max (atom_max m1) (atom_max m2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.meet
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = null THEN TRUE ELSE atom_eval(car(m))(x) AND meet(cdr(m))(x) ENDIF
axiom meet {pvs_τ46 : Type*} {pvs_τ47 : Type*} : pvs_τ46 → pvs_τ47 → Prop

-- pvs2lean: semi_algebraic.meet_meet
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2: meeting,
--         x: list[real] | length(x) >= max(atom_max(m1), atom_max(m2))):
--   meet(append(m1, m2))(x) = (meet(m1)(x) AND meet(m2)(x))
theorem meet_meet : ∀ (m1 : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (m2 : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (x : {x : List ℝ // List.length x ≥ max (atom_max m1) (atom_max m2)}), meet (append m1 m2) (x) = (meet m1(x) ∧ meet m2(x)) := by
  -- === TCCs for meet_meet ===
  -- TCC: meet_meet_TCC1
  -- Statement: FORALL (m1, m2: meeting,
  --         x: list[real]
  --             | length[real](x) >= max(atom_max(m1), atom_max(m2))):
  --   length[real](x) >= atom_max(append[atomic_poly](m1, m2))
  have meet_meet_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meet_meet_TCC2
  -- Statement: FORALL (m1, m2: meeting,
  --         x: list[real]
  --             | length[real](x) >= max(atom_max(m1), atom_max(m2))):
  --   length[real](x) >= atom_max(m1)
  have meet_meet_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meet_meet_TCC3
  -- Statement: FORALL (m1, m2: meeting,
  --         x: list[real]
  --             | length[real](x) >= max(atom_max(m1), atom_max(m2))):
  --   meet(m1)(x) IMPLIES length[real](x) >= atom_max(m2)
  have meet_meet_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.meet_dim
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: meeting, x1: {xx: list[real] | length(xx) >= atom_max(m)},
--         x2: {xx: list[real] | length(xx) > length(x1)}):
--   (FORALL (i: below(atom_max(m))): nth(x1, i) = nth(x2, i)) IMPLIES
--    me
theorem meet_dim : ∀ (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (x1 : {xx : List ℝ // List.length xx ≥ atom_max m}) (x2 : {xx : List ℝ // List.length xx > List.length x1}), (∀ (i : Finset.range atom_max m), nth x1 i = nth x2 i) → meet m(x1) = meet m(x2) := by
  -- === TCCs for meet_dim ===
  -- TCC: meet_dim_TCC1
  -- Statement: FORALL (m: meeting, x1: {xx: list[real] | length[real](xx) >= atom_max(m)},
  --         x2: {xx: list[real] | length[real](xx) > length[real](x1)},
  --         i: below(atom_max(m))):
  --   i < length[real](x1)
  have meet_dim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meet_dim_TCC2
  -- Statement: FORALL (m: meeting, x1: {xx: list[real] | length[real](xx) >= atom_max(m)},
  --         x2: {xx: list[real] | length[real](xx) > length[real](x1)},
  --         i: below(atom_max(m))):
  --   i < length[real](x2)
  have meet_dim_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meet_dim_TCC3
  -- Statement: FORALL (m: meeting, x1: {xx: list[real] | length[real](xx) >= atom_max(m)},
  --         x2: {xx: list[real] | length[real](xx) > length[real](x1)}):
  --   (FORALL (i: below(atom_max(m))): nth(x1, i) = nth(...
  have meet_dim_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.meet_max
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF j = null THEN 0 ELSE max(atom_max(car(j)), meet_max(cdr(j))) ENDIF
axiom meet_max {pvs_τ48 : Type*} : pvs_τ48 → Prop

-- pvs2lean: semi_algebraic.meet_max_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining, i: below(length(j))): meet_max(j) >= atom_max(nth(j, i))
theorem meet_max_nth : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length j}), meet_max j ≥ atom_max (nth j i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.meet_max_nth_e
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining):
--   null?(j) OR
--    EXISTS (i: below(length(j))): meet_max(j) = atom_max(nth(j, i))
theorem meet_max_nth_e : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), null_p j ∨ ∃ (i : Finset.range List.length j), meet_max j = atom_max (nth j i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.max_meet
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j1, j2: joining):
--   meet_max(append(j1, j2)) = max(meet_max(j1), meet_max(j2))
theorem max_meet : ∀ (j1 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (j2 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), meet_max (append j1 j2) = max (meet_max j1) (meet_max j2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.meet_max_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining):
--   meet_max(j) = 0 IMPLIES
--    j = null OR
--     (FORALL (i: below(length(j))):
--        nth(j, i) = null OR
--         FORALL (n: below(length(nth(j, i)))):
--           max_length(nth(nth(j
theorem meet_max_zero : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), meet_max j = 0 → j = null ∨
 (∀ (i : Finset.range List.length j), nth j i = null ∨
     ∀ (n : Finset.range List.length (nth j i)), max_length (nth (nth j i) n.poly) = 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.join
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF j = null THEN FALSE ELSE meet(car(j))(x) OR join(cdr(j))(x) ENDIF
axiom join {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ37 → pvs_τ38 → Prop

-- pvs2lean: semi_algebraic.join_composed_null
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining):
--   (FORALL (i: below(length(j))):
--      nth(j, i) = null OR
--       FORALL (n: below(length(nth(j, i)))):
--         max_length(nth(nth(j, i), n)`poly) = 0)
--    IMPLIES
--    (FORALL (x: lis
theorem join_composed_null : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), (∀ (i : Finset.range List.length j), nth j i = null ∨
    ∀ (n : Finset.range List.length (nth j i)), max_length (nth (nth j i) n.poly) = 0) → (∀ (x : list[real] | List.length x ≥ meet_max j), join j(x) = True) ∨
 (∀ (x : list[real] | List.length x ≥ meet_max j), join j(x) = False) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.join_dim
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining, x1: {xx: list[real] | length(xx) >= meet_max(j)},
--         x2: {xx: list[real] | length(xx) > length(x1)}):
--   (FORALL (i: below(meet_max(j))): nth(x1, i) = nth(x2, i)) IMPLIES
--    jo
theorem join_dim : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (x1 : {xx : List ℝ // List.length xx ≥ meet_max j}) (x2 : {xx : List ℝ // List.length xx > List.length x1}), (∀ (i : Finset.range meet_max j), nth x1 i = nth x2 i) → join j(x1) = join j(x2) := by
  -- === TCCs for join_dim ===
  -- TCC: join_dim_TCC1
  -- Statement: FORALL (j: joining, x1: {xx: list[real] | length[real](xx) >= meet_max(j)},
  --         x2: {xx: list[real] | length[real](xx) > length[real](x1)},
  --         i: below(meet_max(j))):
  --   i < length[real](x1)
  have join_dim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: join_dim_TCC2
  -- Statement: FORALL (j: joining, x1: {xx: list[real] | length[real](xx) >= meet_max(j)},
  --         x2: {xx: list[real] | length[real](xx) > length[real](x1)},
  --         i: below(meet_max(j))):
  --   i < length[real](x2)
  have join_dim_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: join_dim_TCC3
  -- Statement: FORALL (j: joining, x1: {xx: list[real] | length[real](xx) >= meet_max(j)},
  --         x2: {xx: list[real] | length[real](xx) > length[real](x1)}):
  --   (FORALL (i: below(meet_max(j))): nth(x1, i) = nth(...
  have join_dim_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.append_to_each
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF L = null THEN null
-- ELSE cons(append(m, car(L)), append_to_each(m, cdr(L)))
-- ENDIF
axiom append_to_each {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ6 → pvs_τ7 → pvs_τ8

-- pvs2lean: semi_algebraic.max_append_to_each
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: meeting, L: joining):
--   cons?(L) IMPLIES
--    meet_max(append_to_each(x, L)) = max(atom_max(x), meet_max(L))
theorem max_append_to_each : ∀ (x : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (L : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), cons_p L → meet_max (append_to_each x L) = max (atom_max x) (meet_max L) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.append_join
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: meeting, L: joining,
--         x: list[real] | length(x) >= max(atom_max(m), meet_max(L))):
--   join(append_to_each(m, L))(x) = (meet(m)(x) AND join(L)(x))
theorem append_join : ∀ (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (L : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (x : {x : List ℝ // List.length x ≥ max (atom_max m) (meet_max L)}), join (append_to_each m L) (x) = (meet m(x) ∧ join L(x)) := by
  -- === TCCs for append_join ===
  -- TCC: append_join_TCC1
  -- Statement: FORALL (m: meeting, L: joining,
  --         x: list[real] | length[real](x) >= max(atom_max(m), meet_max(L))):
  --   length[real](x) >= meet_max(append_to_each(m, L))
  have append_join_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: append_join_TCC2
  -- Statement: FORALL (m: meeting, L: joining,
  --         x: list[real] | length[real](x) >= max(atom_max(m), meet_max(L))):
  --   length[real](x) >= atom_max(m)
  have append_join_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: append_join_TCC3
  -- Statement: FORALL (m: meeting, L: joining,
  --         x: list[real] | length[real](x) >= max(atom_max(m), meet_max(L))):
  --   meet(m)(x) IMPLIES length[real](x) >= meet_max(L)
  have append_join_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.cap_join
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF j1 = null THEN null
-- ELSIF j2 = null THEN null
-- ELSE append(append_to_each(car(j1), j2), cap_join(cdr(j1), j2))
-- ENDIF
axiom cap_join {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} : pvs_τ15 → pvs_τ16 → pvs_τ17

-- pvs2lean: semi_algebraic.max_cap_join
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j1, j2: joining):
--   cons?(j1) AND cons?(j2) IMPLIES
--    meet_max(cap_join(j1, j2)) = max(meet_max(j1), meet_max(j2))
theorem max_cap_join : ∀ (j1 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (j2 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), cons_p j1 ∧ cons_p j2 → meet_max (cap_join j1 j2) = max (meet_max j1) (meet_max j2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.union_join
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j1, j2: joining,
--         x: list[real] | length(x) >= max(meet_max(j1), meet_max(j2))):
--   (join(j1)(x) OR join(j2)(x)) = join(append(j1, j2))(x)
theorem union_join : ∀ (j1 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (j2 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (x : {x : List ℝ // List.length x ≥ max (meet_max j1) (meet_max j2)}), (join j1(x) ∨ join j2(x)) = join (append j1 j2) (x) := by
  -- === TCCs for union_join ===
  -- TCC: union_join_TCC1
  -- Statement: FORALL (j1, j2: joining,
  --         x: list[real]
  --             | length[real](x) >= max(meet_max(j1), meet_max(j2))):
  --   length[real](x) >= meet_max(j1)
  have union_join_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: union_join_TCC2
  -- Statement: FORALL (j1, j2: joining,
  --         x: list[real]
  --             | length[real](x) >= max(meet_max(j1), meet_max(j2))):
  --   NOT join(j1)(x) IMPLIES length[real](x) >= meet_max(j2)
  have union_join_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: union_join_TCC3
  -- Statement: FORALL (j1, j2: joining,
  --         x: list[real]
  --             | length[real](x) >= max(meet_max(j1), meet_max(j2))):
  --   length[real](x) >= meet_max(append[meeting](j1, j2))
  have union_join_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.intersect_join
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j1, j2: joining,
--         x: list[real] | length(x) >= max(meet_max(j1), meet_max(j2))):
--   (join(j1)(x) AND join(j2)(x)) = join(cap_join(j1, j2))(x)
theorem intersect_join : ∀ (j1 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (j2 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (x : {x : List ℝ // List.length x ≥ max (meet_max j1) (meet_max j2)}), (join j1(x) ∧ join j2(x)) = join (cap_join j1 j2) (x) := by
  -- === TCCs for intersect_join ===
  -- TCC: intersect_join_TCC1
  -- Statement: FORALL (j1, j2: joining,
  --         x: list[real]
  --             | length[real](x) >= max(meet_max(j1), meet_max(j2))):
  --   join(j1)(x) IMPLIES length[real](x) >= meet_max(j2)
  have intersect_join_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: intersect_join_TCC2
  -- Statement: FORALL (j1, j2: joining,
  --         x: list[real]
  --             | length[real](x) >= max(meet_max(j1), meet_max(j2))):
  --   length[real](x) >= meet_max(cap_join(j1, j2))
  have intersect_join_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.negative_atom_meet
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m = null THEN null
-- ELSE cons((: negative_atom(car(m)) :), negative_atom_meet(cdr(m)))
-- ENDIF
axiom negative_atom_meet {pvs_τ51 : Type*} {pvs_τ52 : Type*} : pvs_τ51 → pvs_τ52

-- pvs2lean: semi_algebraic.not_join
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF j = null THEN (: (: :) :)
-- ELSE cap_join(negative_atom_meet(car(j)), not_join(cdr(j)))
-- ENDIF
axiom not_join {pvs_τ53 : Type*} {pvs_τ54 : Type*} : pvs_τ53 → pvs_τ54

-- pvs2lean: semi_algebraic.not_join_cons
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining | FORALL (i: below(length(j))): cons?(nth(j, i))):
--   cons?(not_join(j))
theorem not_join_cons : ∀ (j : {j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) // ∀ (i : Finset.range List.length j), cons_p (nth j i)}), cons_p (not_join j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.not_join_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining | EXISTS (i: below(length(j))): null?(nth(j, i))):
--   not_join(j) = null
theorem not_join_null : ∀ (j : {j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) // ∃ (i : Finset.range List.length j), null_p (nth j i)}), not_join j = null := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.max_not_meet
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: meeting): atom_max(m) = meet_max(negative_atom_meet(m))
theorem max_not_meet : ∀ (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))), atom_max m = meet_max (negative_atom_meet m) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.max_not_null
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining | FORALL (i: below(length(j))): cons?(nth(j, i))):
--   meet_max(j) = meet_max(not_join(j))
theorem max_not_null : ∀ (j : {j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) // ∀ (i : Finset.range List.length j), cons_p (nth j i)}), meet_max j = meet_max (not_join j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.max_not
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining):
--   IF (FORALL (i: below(length(j))): cons?(nth(j, i)))
--     THEN meet_max(j) = meet_max(not_join(j))
--   ELSE 0 = meet_max(not_join(j))
--   ENDIF
theorem max_not : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), IF ((∀ (i : Finset.range List.length j), cons_p (nth j i))) (meet_max j = meet_max (not_join j)) (0 = meet_max (not_join j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.not_meet
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: meeting, x: list[real] | length(x) >= atom_max(m)):
--   (NOT meet(m)(x)) = join(negative_atom_meet(m))(x)
theorem not_meet : ∀ (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (x : {x : List ℝ // List.length x ≥ atom_max m}), (¬ meet m(x)) = join (negative_atom_meet m) (x) := by
  -- === TCCs for not_meet ===
  -- TCC: not_meet_TCC1
  -- Statement: FORALL (m: meeting, x: list[real] | length[real](x) >= atom_max(m)):
  --   length[real](x) >= meet_max(negative_atom_meet(m))
  have not_meet_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.semi_alg
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {x: VectorN(n) | join(j)(x) = TRUE}
axiom semi_alg {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} : pvs_τ59 → pvs_τ60 → pvs_τ61

-- pvs2lean: semi_algebraic.all_true
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat): semi_alg((: (: :) :))(n) = {x: VectorN(n) | TRUE}
theorem all_true : ∀ (n : {i : ℝ // i ≥ 0}), semi_alg ((: (: :) :)) (n) = {x: VectorN n | True} := by
  -- === TCCs for all_true ===
  -- TCC: all_true_TCC1
  -- Statement: FORALL (n: nat): n >= meet_max((: (: :) :))
  have all_true_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.sa_union
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j1, j2: joining, n: nat | n >= max(meet_max(j1), meet_max(j2))):
--   EXISTS (j: joining):
--     n >= meet_max(j) IMPLIES
--      union(semi_alg(j1)(n), semi_alg(j2)(n)) = semi_alg(j)(n)
theorem sa_union : ∀ (j1 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (j2 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max (meet_max j1) (meet_max j2)}), ∃ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), n ≥ meet_max j → union (semi_alg j1(n)) (semi_alg j2(n)) = semi_alg j(n) := by
  -- === TCCs for sa_union ===
  -- TCC: sa_union_TCC1
  -- Statement: FORALL (j1, j2: joining, n: nat | n >= max(meet_max(j1), meet_max(j2)),
  --         j: joining):
  --   n >= meet_max(j) IMPLIES n >= meet_max(j1)
  have sa_union_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sa_union_TCC2
  -- Statement: FORALL (j1, j2: joining, n: nat | n >= max(meet_max(j1), meet_max(j2)),
  --         j: joining):
  --   n >= meet_max(j) IMPLIES n >= meet_max(j2)
  have sa_union_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.sa_intersection
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j1, j2: joining, n: nat | n >= max(meet_max(j1), meet_max(j2))):
--   EXISTS (j: joining):
--     n >= meet_max(j) IMPLIES
--      intersection(semi_alg(j1)(n), semi_alg(j2)(n)) = semi_alg(j)(n)
theorem sa_intersection : ∀ (j1 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (j2 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max (meet_max j1) (meet_max j2)}), ∃ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), n ≥ meet_max j → intersection (semi_alg j1(n)) (semi_alg j2(n)) = semi_alg j(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.sa_complement
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining, n: nat | n >= meet_max(j)):
--   EXISTS (j1: joining):
--     n >= meet_max(j1) IMPLIES complement(semi_alg(j)(n)) = semi_alg(j1)(n)
theorem sa_complement : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (n : {n : {i : ℝ // i ≥ 0} // n ≥ meet_max j}), ∃ (j1 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), n ≥ meet_max j1 → complement (semi_alg j(n)) = semi_alg j1(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: semi_algebraic.sa_difference
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j1, j2: joining, n: nat | n >= max(meet_max(j1), meet_max(j2))):
--   EXISTS (j: joining):
--     n >= meet_max(j) IMPLIES
--      difference(semi_alg(j1)(n), semi_alg(j2)(n)) = semi_alg(j)(n)
theorem sa_difference : ∀ (j1 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (j2 : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max (meet_max j1) (meet_max j2)}), ∃ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))), n ≥ meet_max j → difference (semi_alg j1(n)) (semi_alg j2(n)) = semi_alg j(n) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.semi_algebraic