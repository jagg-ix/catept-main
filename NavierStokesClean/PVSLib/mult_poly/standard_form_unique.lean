-- Auto-generated by pvs2lean/translator.py
-- PVS theory: standard_form_unique
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: standard_form_unique

Auto-generated stubs from PVSLib theory `standard_form_unique`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.standard_form_unique

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (add_mono : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (allnonzero : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (append : pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (cons_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (cutting : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (every : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (full_eval : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} (integer_pred : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (length : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (lft : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (max : pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} (max_length : pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (mp_mono_mult : pvs_τ28 → pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (mp_mult : pvs_τ31 → pvs_τ32 → pvs_τ33)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (mult : pvs_τ34 → pvs_τ35 → pvs_τ36)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (mv_cut : pvs_τ37 → pvs_τ38)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (mv_sort : pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} (mv_standard_form : pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} (mv_standard_sans_cut_p : pvs_τ42 → Prop)
variable {pvs_τ43 : Type*} (null_p : pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} (number_field_pred : pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} (rational_pred : pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} (real_pred : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (simplify : pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (sorted_sans_add : pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52)

-- pvs2lean: standard_form_unique.monom_cut_eval
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem monom_cut_eval : True := by
  -- === TCCs for monom_cut_eval ===
  -- TCC: monom_cut_eval_TCC1
  -- Statement: FORALL (n: nat, m: monomial,
  --         (vals: list[real] | length[real](vals) >= length[nat](m`alpha))):
  --   n = length(m`alpha) IMPLIES
  --    length[real](vals) >= length[nat](cutting(m`alpha))
  have monom_cut_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: standard_form_unique.mv_cut_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, vals: {l: list[real] | length(l) >= max_length(p)}):
--   full_eval(p)(vals) = full_eval(mv_cut(p))(vals)
theorem mv_cut_eval : ∀ (p : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p}), full_eval p(vals) = full_eval (mv_cut p) (vals) := by
  -- === TCCs for mv_cut_eval ===
  -- TCC: mv_cut_eval_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   length[real](vals) >= max_length(mv_cut(p))
  have mv_cut_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.monom_lft_eval
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem monom_lft_eval : True := by
  -- === TCCs for monom_lft_eval ===
  -- TCC: monom_lft_eval_TCC1
  -- Statement: FORALL (n: nat, (m: monomial | n >= length[nat](m`alpha)),
  --         (vals: list[real] | length[real](vals) >= n)):
  --   length[real](vals) >= length[nat](m`alpha)
  have monom_lft_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: monom_lft_eval_TCC2
  -- Statement: FORALL (n: nat, (m: monomial | n >= length[nat](m`alpha)),
  --         (vals: list[real] | length[real](vals) >= n)):
  --   length[real](vals) >=
  --    length[nat]
  --        (append[nat](m`alpha, ^[real]((: 0 :)...
  have monom_lft_eval_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: monom_lft_eval_TCC3
  -- Statement: FORALL (n: nat, (m: monomial | n >= length[nat](m`alpha)),
  --         (vals: list[real] | length[real](vals) >= n)):
  --   (n - length[nat](m`alpha)) >= 0
  have monom_lft_eval_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: monom_lft_eval_TCC4
  -- Statement: FORALL (n: nat, (m: monomial | n >= length[nat](m`alpha)),
  --         (vals: list[real] | length[real](vals) >= n)):
  --   every[number]
  --       (LAMBDA (x: number):
  --               number_field_pred(x) AND r...
  have monom_lft_eval_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: standard_form_unique.lft_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, vals: {l: list[real] | length(l) >= max_length(p)}):
--   full_eval(p)(vals) = full_eval(lft(p)(max_length(p)))(vals)
theorem lft_eval : ∀ (p : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p}), full_eval p(vals) = full_eval (lft p(max_length p)) (vals) := by
  -- === TCCs for lft_eval ===
  -- TCC: lft_eval_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   length[real](vals) >= max_length(lft(p)(max_length(p)))
  have lft_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lft_eval_TCC2
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   max_length(p) >= max_length(p)
  have lft_eval_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.lft_eval_gen
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, n: nat | n >= max_length(p),
--         vals: {l: list[real] | length(l) >= n}):
--   full_eval(p)(vals) = full_eval(lft(p)(n))(vals)
theorem lft_eval_gen : ∀ (p : List (List ℝ × ℝ)) (n : {n : {i : ℝ // i ≥ 0} // n ≥ max_length p}) (vals : {l : List ℝ // List.length l ≥ n}), full_eval p(vals) = full_eval (lft p(n)) (vals) := by
  -- === TCCs for lft_eval_gen ===
  -- TCC: lft_eval_gen_TCC1
  -- Statement: FORALL (p: MultPoly, n: nat | n >= max_length(p),
  --         vals: {l: list[real] | length[real](l) >= n}):
  --   length[real](vals) >= max_length(p)
  have lft_eval_gen_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: lft_eval_gen_TCC2
  -- Statement: FORALL (p: MultPoly, n: nat | n >= max_length(p),
  --         vals: {l: list[real] | length[real](l) >= n}):
  --   length[real](vals) >= max_length(lft(p)(n))
  have lft_eval_gen_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.allnonzero_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, vals: {l: list[real] | length(l) >= max_length(p)}):
--   full_eval(p)(vals) = full_eval(allnonzero(p))(vals)
theorem allnonzero_eval : ∀ (p : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p}), full_eval p(vals) = full_eval (allnonzero p) (vals) := by
  -- === TCCs for allnonzero_eval ===
  -- TCC: allnonzero_eval_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   length[real](vals) >= max_length(allnonzero(p))
  have allnonzero_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.sorting_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, vals: {l: list[real] | length(l) >= max_length(p)}):
--   full_eval(p)(vals) = full_eval(mv_sort(p))(vals)
theorem sorting_eval : ∀ (p : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p}), full_eval p(vals) = full_eval (mv_sort p) (vals) := by
  -- === TCCs for sorting_eval ===
  -- TCC: sorting_eval_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   length[real](vals) >= max_length(mv_sort(p))
  have sorting_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.simplify_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, vals: {l: list[real] | length(l) >= max_length(p)}):
--   full_eval(p)(vals) = full_eval(simplify(p))(vals)
theorem simplify_eval : ∀ (p : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p}), full_eval p(vals) = full_eval (simplify p) (vals) := by
  -- === TCCs for simplify_eval ===
  -- TCC: simplify_eval_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   length[real](vals) >= max_length(simplify(p))
  have simplify_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.standard_form_full_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, vals: {l: list[real] | length(l) >= max_length(p)}):
--   full_eval(mv_standard_form(p))(vals) = full_eval(p)(vals)
theorem standard_form_full_eval : ∀ (p : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p}), full_eval (mv_standard_form p) (vals) = full_eval p(vals) := by
  -- === TCCs for standard_form_full_eval ===
  -- TCC: standard_form_full_eval_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   length[real](vals) >= max_length(mv_standard_form(p))
  have standard_form_full_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.standard_impl_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly,
--         vals:
--           {l: list[real] |
--              length(l) >= max_length(p) AND length(l) >= max_length(q)}):
--   mv_standard_form(p) = mv_standard_form(q) IMPLIES
--    full_e
theorem standard_impl_eval : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p ∧ List.length l ≥ max_length q}), mv_standard_form p = mv_standard_form q → full_eval p(vals) = full_eval q(vals) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.add_mono_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2: monomial,
--         vals: list[real]
--             | length(vals) >= max(length(m1`alpha), length(m2`alpha))):
--   m1`alpha = m2`alpha IMPLIES
--    full_eval(add_mono(m1, m2))(vals) =
--     full
theorem add_mono_eval : ∀ (m1 : List ℝ × ℝ) (m2 : List ℝ × ℝ) (vals : {vals : List ℝ // List.length vals ≥ max (List.length m1.alpha) (List.length m2.alpha)}), m1.alpha = m2.alpha → full_eval (add_mono m1 m2) (vals) =
 full_eval m1(vals) + full_eval m2(vals) := by
  -- === TCCs for add_mono_eval ===
  -- TCC: add_mono_eval_TCC1
  -- Statement: FORALL (m1, m2: monomial,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m1`alpha), length[nat](m2`alpha))):
  --   m1`alpha = m2`alpha IMPLIES
  --    length[rea...
  have add_mono_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: add_mono_eval_TCC2
  -- Statement: FORALL (m1, m2: monomial,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m1`alpha), length[nat](m2`alpha))):
  --   m1`alpha = m2`alpha IMPLIES m2`alpha = m1...
  have add_mono_eval_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: add_mono_eval_TCC3
  -- Statement: FORALL (m1, m2: monomial,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m1`alpha), length[nat](m2`alpha))):
  --   m1`alpha = m2`alpha IMPLIES length[real](...
  have add_mono_eval_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: add_mono_eval_TCC4
  -- Statement: FORALL (m1, m2: monomial,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m1`alpha), length[nat](m2`alpha))):
  --   m1`alpha = m2`alpha IMPLIES length[real](...
  have add_mono_eval_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.sorted_sans_add_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, p: (mv_standard_sans_cut?(n)),
--         q: (mv_standard_sans_cut?(n)),
--         (vals: list[real]
--              | length(vals) >= max_length(p) AND
--                 length(vals) >= max_len
theorem sorted_sans_add_eval : ∀ (n : {i : ℝ // i ≥ 0}) (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)) (vals : {vals : List ℝ // List.length vals ≥ max_length p ∧ List.length vals ≥ max_length q}), full_eval (sorted_sans_add n p q) (vals) = full_eval p(vals) + full_eval q(vals) := by
  -- === TCCs for sorted_sans_add_eval ===
  -- TCC: sorted_sans_add_eval_TCC1
  -- Statement: FORALL (n: nat, p: (mv_standard_sans_cut?(n)),
  --         q: (mv_standard_sans_cut?(n)),
  --         (vals: list[real]
  --              | length[real](vals) >= max_length(p) AND
  --                 length[real](v...
  have sorted_sans_add_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.add_full_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly,
--         (vals: list[real]
--              | length(vals) >= max_length(p) AND
--                 length(vals) >= max_length(q))):
--   full_eval(add(p, q))(vals) = full_eval(p)(vals) + 
theorem add_full_eval : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)) (vals : {vals : List ℝ // List.length vals ≥ max_length p ∧ List.length vals ≥ max_length q}), full_eval (add p q) (vals) = full_eval p(vals) + full_eval q(vals) := by
  -- === TCCs for add_full_eval ===
  -- TCC: add_full_eval_TCC1
  -- Statement: FORALL (p, q: MultPoly,
  --         (vals: list[real]
  --              | length[real](vals) >= max_length(p) AND
  --                 length[real](vals) >= max_length(q))):
  --   length[real](vals) >= max_length(ad...
  have add_full_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.mp_mono_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, q: MultPoly,
--         vals: list[real]
--             | length(vals) >= max(length(m`alpha), max_length(q))):
--   full_eval(mp_mono_mult(m, q))(vals) =
--    full_eval(q)(vals) * full_eval
theorem mp_mono_eval : ∀ (m : List ℝ × ℝ) (q : List (List ℝ × ℝ)) (vals : {vals : List ℝ // List.length vals ≥ max (List.length m.alpha) (max_length q)}), full_eval (mp_mono_mult m q) (vals) = full_eval q(vals) * full_eval m(vals) := by
  -- === TCCs for mp_mono_eval ===
  -- TCC: mp_mono_eval_TCC1
  -- Statement: FORALL (m: monomial, q: MultPoly,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m`alpha), max_length(q))):
  --   length[real](vals) >= max_length(mp_mono_m...
  have mp_mono_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mp_mono_eval_TCC2
  -- Statement: FORALL (m: monomial, q: MultPoly,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m`alpha), max_length(q))):
  --   length[real](vals) >= max_length(q)
  have mp_mono_eval_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: mp_mono_eval_TCC3
  -- Statement: FORALL (m: monomial, q: MultPoly,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m`alpha), max_length(q))):
  --   length[real](vals) >= length[nat](m`alpha)
  have mp_mono_eval_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.mult_full_eval
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly,
--         (vals: list[real]
--              | length(vals) >= max_length(p) AND
--                 length(vals) >= max_length(q))):
--   full_eval(mp_mult(p, q))(vals) = full_eval(p)(vals
theorem mult_full_eval : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)) (vals : {vals : List ℝ // List.length vals ≥ max_length p ∧ List.length vals ≥ max_length q}), full_eval (mp_mult p q) (vals) = full_eval p(vals) * full_eval q(vals) := by
  -- === TCCs for mult_full_eval ===
  -- TCC: mult_full_eval_TCC1
  -- Statement: FORALL (p, q: MultPoly,
  --         (vals: list[real]
  --              | length[real](vals) >= max_length(p) AND
  --                 length[real](vals) >= max_length(q))):
  --   length[real](vals) >= max_length(mp...
  have mult_full_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.standard_form_adds_id
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly,
--         (vals: list[real]
--              | length(vals) >= max_length(p) AND
--                 length(vals) >= max_length(q))):
--   full_eval(add(p, q))(vals) = full_eval(mv_standard
theorem standard_form_adds_id : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)) (vals : {vals : List ℝ // List.length vals ≥ max_length p ∧ List.length vals ≥ max_length q}), full_eval (add p q) (vals) = full_eval (mv_standard_form (add p q)) (vals) := by
  -- === TCCs for standard_form_adds_id ===
  -- TCC: standard_form_adds_id_TCC1
  -- Statement: FORALL (p, q: MultPoly,
  --         (vals: list[real]
  --              | length[real](vals) >= max_length(p) AND
  --                 length[real](vals) >= max_length(q))):
  --   length[real](vals) >= max_length(mv...
  have standard_form_adds_id_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.equal_everywhere_add
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly,
--         vals:
--           {l: list[real] |
--              length(l) >= max_length(p) AND length(l) >= max_length(q)}):
--   full_eval(p)(vals) = full_eval(q)(vals) IMPLIES
--    full_eva
theorem equal_everywhere_add : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p ∧ List.length l ≥ max_length q}), full_eval p(vals) = full_eval q(vals) → full_eval (add p (mult -1 q)) (vals) = 0 := by
  -- === TCCs for equal_everywhere_add ===
  -- TCC: equal_everywhere_add_TCC1
  -- Statement: FORALL (p, q: MultPoly,
  --         vals:
  --           {l: list[real] |
  --              length[real](l) >= max_length(p) AND
  --               length[real](l) >= max_length(q)}):
  --   full_eval(p)(vals) = full_eval...
  have equal_everywhere_add_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.standard_form_nonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: (mv_standard_form?)):
--   cons?(p) IMPLIES
--    EXISTS ((vals: list[real] | length(vals) = max_length(p))):
--      full_eval(p)(vals) /= 0
theorem standard_form_nonzero : ∀ (p : List (List ℝ × ℝ)), cons_p p → ∃ ((vals : list[real] | List.length vals = max_length p)), full_eval p(vals) ≠ 0 := by
  -- === TCCs for standard_form_nonzero ===
  -- TCC: standard_form_nonzero_TCC1
  -- Statement: FORALL (p: (mv_standard_form?)):
  --   cons?(p) IMPLIES
  --    FORALL ((vals: list[real] | length[real](vals) = max_length(p))):
  --      length[real](vals) >= max_length(p)
  have standard_form_nonzero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.standard_form_add_null
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: (mv_standard_form?)): null?(add(p, mult(-1, q))) IMPLIES p = q
theorem standard_form_add_null : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), null_p (add p (mult -1 q)) → p = q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.eval_impl_standard
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly):
--   (FORALL (vals:
--              {l: list[real] |
--                 length(l) >= max_length(p) AND
--                  length(l) >= max_length(q)}):
--      full_eval(p)(vals) = full_eva
theorem eval_impl_standard : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), (∀ (vals : {x : list[real] // List.xength x ≥ max_xength p ∧ List.xength x ≥ max_xength q}), full_eval p(vals) = full_eval q(vals)) → mv_standard_form p = mv_standard_form q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: standard_form_unique.standard_form_is_unique
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: MultPoly):
--   mv_standard_form(p) = mv_standard_form(q) IFF
--    FORALL ((x: list[real]
--                 | length(x) >= max(max_length(p), max_length(q)))):
--      full_eval(p)(x) = full_eval(
theorem standard_form_is_unique : ∀ (p : List (List ℝ × ℝ)) (q : List (List ℝ × ℝ)), mv_standard_form p = mv_standard_form q ↔ ∀ ((x : list[real] | List.length x ≥ max (max_length p) (max_length q))), full_eval p(x) = full_eval q(x) := by
  -- === TCCs for standard_form_is_unique ===
  -- TCC: standard_form_is_unique_TCC1
  -- Statement: FORALL (p, q: MultPoly,
  --         (x: list[real]
  --              | length[real](x) >= max(max_length(p), max_length(q)))):
  --   length[real](x) >= max_length(p)
  have standard_form_is_unique_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: standard_form_is_unique_TCC2
  -- Statement: FORALL (p, q: MultPoly,
  --         (x: list[real]
  --              | length[real](x) >= max(max_length(p), max_length(q)))):
  --   length[real](x) >= max_length(q)
  have standard_form_is_unique_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.standard_form_unique