-- Auto-generated by pvs2lean/translator.py
-- PVS theory: poly_comp_analytic
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: poly_comp_analytic

Auto-generated stubs from PVSLib theory `poly_comp_analytic`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.poly_comp_analytic

noncomputable section

variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (VectorN : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (analytic_p : pvs_τ5 → pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (atom_max : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (below : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (bounded_below_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (bov_p : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (car : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (cdr : pvs_τ17 → pvs_τ18)
variable {pvs_τ19 : Type*} (cons_p : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (convergent_p : pvs_τ20 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (eval : pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (full_eval : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} (glb : pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (hat : pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34)
variable {pvs_τ38 : Type*} (length : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} (max_length : pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (meet_max : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (nth : pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (replace : pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} (semi_alg : pvs_τ48 → pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (series : pvs_τ51 → pvs_τ52)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (sub1 : pvs_τ53 → pvs_τ54)

-- pvs2lean: poly_comp_analytic.e_mon
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF m`alpha = null THEN m`C
-- ELSE car(vals) ^ car[nat](m`alpha) *
--       e_mon((# C := m`C, alpha := cdr(m`alpha) #))(cdr(vals))
-- ENDIF
axiom e_mon {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ21 → pvs_τ22 → pvs_τ23

-- pvs2lean: poly_comp_analytic.increasing?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(l) <= 1 THEN TRUE
-- ELSE car[nat](l) <= car[nat](cdr(l)) AND increasing?(cdr(l))
-- ENDIF
axiom increasing_p {pvs_τ35 : Type*} : pvs_τ35 → Prop

-- pvs2lean: poly_comp_analytic.increase_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[nat]):
--   increasing?(l) IMPLIES
--    (FORALL (i, j: below(length(l))): i <= j IMPLIES nth(l, i) <= nth(l, j))
theorem increase_nth : ∀ (l : List ℝ), increasing_p l → (∀ (i : _) (j : Finset.range List.length l), i ≤ j → nth l i ≤ nth l j) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.index_great_increase
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: {mm: monomial | cons?(mm`alpha)},
--         vals: {l: list[real] | cons?(l)},
--         index:
--           {l: list[nat] |
--              (length(l) = length(vals) AND increasing?(l)) AND
--          
theorem index_great_increase : ∀ (m : {mm : List ℝ × ℝ // cons_p mm.alpha}) (vals : {l : List ℝ // cons_p l}) (index : {l : List ℝ // (List.length l = List.length vals ∧ increasing_p l) ∧ bov_p 0(cdr l)}), car index ≥ List.length m.alpha → eval m index(vals).C = m.C := by
  -- === TCCs for index_great_increase ===
  -- TCC: index_great_increase_TCC1
  -- Statement: FORALL (m: {mm: monomial | cons?[real](mm`alpha)},
  --         vals: {l: list[real] | cons?[real](l)}, l: list[nat]):
  --   length(l) = length(vals) AND increasing?(l) IMPLIES cons?[nat](l)
  have index_great_increase_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: index_great_increase_TCC2
  -- Statement: FORALL (m: {mm: monomial | cons?[real](mm`alpha)},
  --         vals: {l: list[real] | cons?[real](l)},
  --         index:
  --           {l: list[nat] |
  --              (length[nat](l) = length[real](vals) AND inc...
  have index_great_increase_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: index_great_increase_TCC3
  -- Statement: FORALL (m: {mm: monomial | cons?[real](mm`alpha)},
  --         vals: {l: list[real] | cons?[real](l)},
  --         index:
  --           {l: list[nat] |
  --              (length[nat](l) = length[real](vals) AND inc...
  have index_great_increase_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.cdr_replace
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial | cons?(m`alpha), index: list[nat] | cons?(index)):
--   (car[nat](index) < length(m`alpha) AND NOT member[nat](0, index)) IMPLIES
--    cdr[nat](replace(m`alpha, car[nat](index))(0)) =
-- 
theorem cdr_replace : ∀ (m : {m : List ℝ × ℝ // cons_p m.alpha}) (index : {index : List ℝ // cons_p index}), (car index < List.length m.alpha ∧ ¬ member 0 index) → cdr (replace m.alpha car[nat](index) (0)) =
 replace (cdr m.alpha) (car (sub1 index)) (0) := by
  -- === TCCs for cdr_replace ===
  -- TCC: cdr_replace_TCC1
  -- Statement: FORALL (m: monomial | cons?[real](m`alpha),
  --         index: list[nat] | cons?[real](index)):
  --   car[nat](index) < length(m`alpha) AND NOT member[nat](0, index) IMPLIES
  --    cons?[nat](replace(m`alpha, ...
  have cdr_replace_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cdr_replace_TCC2
  -- Statement: FORALL (m: monomial | cons?[real](m`alpha),
  --         index: list[nat] | cons?[real](index)):
  --   car[nat](index) < length(m`alpha) AND NOT member[nat](0, index) IMPLIES
  --    cons?[nat](m`alpha)
  have cdr_replace_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cdr_replace_TCC3
  -- Statement: FORALL (m: monomial | cons?[real](m`alpha),
  --         index: list[nat] | cons?[real](index)):
  --   car[nat](index) < length(m`alpha) AND NOT member[nat](0, index) IMPLIES
  --    bov?(0)(index)
  have cdr_replace_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cdr_replace_TCC4
  -- Statement: FORALL (m: monomial | cons?[real](m`alpha),
  --         index: list[nat] | cons?[real](index)):
  --   car[nat](index) < length(m`alpha) AND NOT member[nat](0, index) IMPLIES
  --    cons?[nat](sub1(index))
  have cdr_replace_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cdr_replace_TCC5
  -- Statement: FORALL (m: monomial | cons?[real](m`alpha),
  --         index: list[nat] | cons?[real](index)):
  --   car[nat](index) < length(m`alpha) AND NOT member[nat](0, index) IMPLIES
  --    car[nat](sub1(index)) < leng...
  have cdr_replace_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.eval0_fconst
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, v: list[real], ind: list[nat]):
--   (length(m`alpha) = 0 AND length(v) = length(ind)) IMPLIES
--    eval(m, ind)(v)`C = m`C
theorem eval0_fconst : ∀ (m : List ℝ × ℝ) (v : List ℝ) (ind : List ℝ), (List.length m.alpha = 0 ∧ List.length v = List.length ind) → eval m ind(v).C = m.C := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: poly_comp_analytic.fulleval0_fconst
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, v: list[real]):
--   (length(m`alpha) = 0) IMPLIES full_eval(m)(v) = m`C
theorem fulleval0_fconst : ∀ (m : List ℝ × ℝ) (v : List ℝ), (List.length m.alpha = 0) → full_eval m(v) = m.C := by
  -- === TCCs for fulleval0_fconst ===
  -- TCC: fulleval0_fconst_TCC1
  -- Statement: FORALL (m: monomial, v: list[real]):
  --   (length(m`alpha) = 0) IMPLIES length[real](v) >= length[nat](m`alpha)
  have fulleval0_fconst_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.eval_hat_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, m: {mm: monomial | length(mm`alpha) = n},
--         f: [real -> VectorN(n)]):
--   (LAMBDA (x: real): full_eval(m)(f(x))) =
--    (LAMBDA (x: real):
--       m`C * car(f(x)) ^ car[nat](m`alpha
theorem eval_hat_equiv : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (m : {mm : List ℝ × ℝ // List.length mm.alpha = n}) (f : ℝ → {l : List ℝ // List.length l = n}), (fun (x : ℝ)=> full_eval m(f x)) = (fun (x : ℝ)=> m.C * car (f x) ^ car m.alpha * full_eval (hat m) (hat n(f) (x))) := by
  -- === TCCs for eval_hat_equiv ===
  -- TCC: eval_hat_equiv_TCC1
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n)], x: real):
  --   length[real](f(x)) >= length[nat](m`alpha)
  have eval_hat_equiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_hat_equiv_TCC2
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n)], x: real):
  --   cons?[real](f(x))
  have eval_hat_equiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_hat_equiv_TCC3
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n)]):
  --   cons?[nat](m`alpha)
  have eval_hat_equiv_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_hat_equiv_TCC4
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n)], x: real):
  --   car[real](f(x)) /= 0 OR car[nat](m`alpha) >= 0
  have eval_hat_equiv_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_hat_equiv_TCC5
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n)], x: real):
  --   length[real](hat(n)(f)(x)) >= length[nat](hat(m)`alpha)
  have eval_hat_equiv_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.eval_hat_equiv_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n, n2: posnat, m: {mm: monomial | length(mm`alpha) = n},
--         f: [real -> VectorN(n2)]):
--   n2 >= n IMPLIES
--    ((LAMBDA (x: real): full_eval(m)(f(x))) =
--      (LAMBDA (x: real):
--         m`C *
theorem eval_hat_equiv_ge : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (n2 : {i : {i : ℝ // i ≥ 0} // i > 0}) (m : {mm : List ℝ × ℝ // List.length mm.alpha = n}) (f : ℝ → {l : List ℝ // List.length l = n2}), n2 ≥ n → ((fun (x : ℝ)=> full_eval m(f x)) =
  (fun (x : ℝ)=> m.C * car (f x) ^ car m.alpha *
      full_eval (hat m) (hat n2(f) (x)))) := by
  -- === TCCs for eval_hat_equiv_ge ===
  -- TCC: eval_hat_equiv_ge_TCC1
  -- Statement: FORALL (n, n2: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n2)]):
  --   n2 >= n IMPLIES
  --    FORALL (x: real): length[real](f(x)) >= length[nat](m`alpha)
  have eval_hat_equiv_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_hat_equiv_ge_TCC2
  -- Statement: FORALL (n, n2: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n2)]):
  --   n2 >= n IMPLIES FORALL (x: real): cons?[real](f(x))
  have eval_hat_equiv_ge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_hat_equiv_ge_TCC3
  -- Statement: FORALL (n, n2: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n2)]):
  --   n2 >= n IMPLIES cons?[nat](m`alpha)
  have eval_hat_equiv_ge_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_hat_equiv_ge_TCC4
  -- Statement: FORALL (n, n2: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n2)]):
  --   n2 >= n IMPLIES
  --    FORALL (x: real): car[real](f(x)) /= 0 OR car[nat](m`alpha) >= 0
  have eval_hat_equiv_ge_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_hat_equiv_ge_TCC5
  -- Statement: FORALL (n, n2: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: [real -> VectorN(n2)]):
  --   n2 >= n IMPLIES
  --    FORALL (x: real):
  --      length[real](hat(n2)(f)(x)) >= length[nat](hat(m...
  have eval_hat_equiv_ge_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.analytic_hat
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: posnat, alpha: real, f: [real -> VectorN(m)]):
--   analytic?(m, alpha)(f) IMPLIES analytic?(m - 1, alpha)(hat(m)(f))
theorem analytic_hat : ∀ (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (alpha : ℝ) (f : ℝ → {l : List ℝ // List.length l = m}), analytic_p m alpha(f) → analytic_p (m - 1) alpha(hat m(f)) := by
  -- === TCCs for analytic_hat ===
  -- TCC: analytic_hat_TCC1
  -- Statement: FORALL (m: posnat, alpha: real, f: [real -> VectorN(m)]):
  --   analytic?(m, alpha)(f) IMPLIES m - 1 >= 0
  have analytic_hat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.mono_comp_analytic_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, m: {mm: monomial | length(mm`alpha) = 0},
--         f: (analytic?(0, alpha))):
--   analytic?(alpha)(LAMBDA (x: real): full_eval(m)(f(x)))
theorem mono_comp_analytic_0 : ∀ (alpha : ℝ) (m : {mm : List ℝ × ℝ // List.length mm.alpha = 0}) (f : ℝ → {l : List ℝ // List.length l = 0}), analytic_p alpha (fun (x : ℝ) => full_eval m (f x)) := by
  -- === TCCs for mono_comp_analytic_0 ===
  -- TCC: mono_comp_analytic_0_TCC1
  -- Statement: FORALL (alpha: real, m: {mm: monomial | length[nat](mm`alpha) = 0},
  --         f: (analytic?(0, alpha)), x: real):
  --   length[real](f(x)) >= length[nat](m`alpha)
  have mono_comp_analytic_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.mono_comp_analytic
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, n: nat, m: {mm: monomial | length(mm`alpha) = n},
--         f: (analytic?(n, alpha))):
--   analytic?(alpha)(LAMBDA (x: real): full_eval(m)(f(x)))
theorem mono_comp_analytic : ∀ (alpha : ℝ) (n : {i : ℝ // i ≥ 0}) (m : {mm : List ℝ × ℝ // List.length mm.alpha = n}) (f : ℝ → {l : List ℝ // List.length l = n}), analytic_p alpha (fun (x : ℝ) => full_eval m (f x)) := by
  -- === TCCs for mono_comp_analytic ===
  -- TCC: mono_comp_analytic_TCC1
  -- Statement: FORALL (alpha: real, n: nat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: (analytic?(n, alpha)), x: real):
  --   length[real](f(x)) >= length[nat](m`alpha)
  have mono_comp_analytic_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.mono_comp_analytic_ge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (alpha: real, n, n2: nat, m: {mm: monomial | length(mm`alpha) = n},
--         f: (analytic?(n2, alpha))):
--   n2 >= n IMPLIES analytic?(alpha)(LAMBDA (x: real): full_eval(m)(f(x)))
theorem mono_comp_analytic_ge : ∀ (alpha : ℝ) (n : {i : ℝ // i ≥ 0}) (n2 : {i : ℝ // i ≥ 0}) (m : {mm : List ℝ × ℝ // List.length mm.alpha = n}) (f : ℝ → {l : List ℝ // List.length l = n2}), n2 ≥ n → analytic_p alpha(fun (x : ℝ)=> full_eval m(f x)) := by
  -- === TCCs for mono_comp_analytic_ge ===
  -- TCC: mono_comp_analytic_ge_TCC1
  -- Statement: FORALL (alpha: real, n, n2: nat,
  --         m: {mm: monomial | length[nat](mm`alpha) = n},
  --         f: (analytic?(n2, alpha))):
  --   n2 >= n IMPLIES
  --    FORALL (x: real): length[real](f(x)) >= length[nat](...
  have mono_comp_analytic_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.poly_comp_analytic
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, alpha: real, p: MultPoly, f: (analytic?(n, alpha))):
--   n >= max_length(p) IMPLIES
--    analytic?(alpha)(LAMBDA (x: real): full_eval(p)(f(x)))
theorem poly_comp_analytic : ∀ (n : {i : ℝ // i ≥ 0}) (alpha : ℝ) (p : List (List ℝ × ℝ)) (f : ℝ → {l : List ℝ // List.length l = n}), n ≥ max_length p → analytic_p alpha(fun (x : ℝ)=> full_eval p(f x)) := by
  -- === TCCs for poly_comp_analytic ===
  -- TCC: poly_comp_analytic_TCC1
  -- Statement: FORALL (n: nat, alpha: real, p: MultPoly, f: (analytic?(n, alpha))):
  --   n >= max_length(p) IMPLIES
  --    FORALL (x: real): length[real](f(x)) >= max_length(p)
  have poly_comp_analytic_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.meeting_analytic
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x0: real, m: meeting, n: nat | n >= atom_max(m),
--         f: (analytic?(n, x0))):
--   (EXISTS (epsilon: posreal):
--      FORALL (t: posreal | t < epsilon): semi_alg((: m :))(n)(f(x0 + t)))
--    OR
--   
theorem meeting_analytic : ∀ (x0 : ℝ) (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (n : {n : {i : ℝ // i ≥ 0} // n ≥ atom_max m}) (f : ℝ → {l : List ℝ // List.length l = n}), (∃ (epsilon : {x : ℝ // 0 < x}), ∀ (t : posreal | t < epsilon), semi_alg ((: m :)) (n) (f (x0 + t))) ∨ ∃ (epsilon : {x : ℝ // 0 < x}), ∀ (t : posreal | t ≤ epsilon), ¬ semi_alg ((: m :)) (n) (f (x0 + t)) := by
  -- === TCCs for meeting_analytic ===
  -- TCC: meeting_analytic_TCC1
  -- Statement: FORALL (x0: real, m: meeting, n: nat | n >= atom_max(m),
  --         f: (analytic?(n, x0)), epsilon: posreal, t: posreal | t < epsilon):
  --   n >= meet_max((: m :))
  have meeting_analytic_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meeting_analytic_TCC2
  -- Statement: FORALL (x0: real, m: meeting, n: nat | n >= atom_max(m),
  --         f: (analytic?(n, x0))):
  --   NOT (EXISTS (epsilon: posreal):
  --          FORALL (t: posreal | t < epsilon):
  --            semi_alg((: m :))(n...
  have meeting_analytic_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.joining_analytic
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x0: real, j: joining, n: nat | n >= meet_max(j),
--         f: (analytic?(n, x0))):
--   (EXISTS (epsilon: posreal):
--      FORALL (t: posreal | t < epsilon): semi_alg(j)(n)(f(x0 + t)))
--    OR
--    EXIST
theorem joining_analytic : ∀ (x0 : ℝ) (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (n : {n : {i : ℝ // i ≥ 0} // n ≥ meet_max j}) (f : ℝ → {l : List ℝ // List.length l = n}), (∃ (epsilon : {x : ℝ // 0 < x}), ∀ (t : posreal | t < epsilon), semi_alg j(n) (f (x0 + t))) ∨ ∃ (epsilon : {x : ℝ // 0 < x}), ∀ (t : posreal | t ≤ epsilon), ¬ semi_alg j(n) (f (x0 + t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.atom_max_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: meeting, i: below(length(m))):
--   atom_max(m) >= max_length(nth[atomic_poly](m, i)`poly)
theorem atom_max_nth : ∀ (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (i : {s : {i : ℝ // i ≥ 0} // s < List.length m}), atom_max m ≥ max_length (nth m i.poly) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.min_eps
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: meeting, x0: real, n: nat | n >= atom_max(m),
--         f: (analytic?(n, x0))):
--   EXISTS (eps_min: posreal):
--     FORALL (i: below(length(m))):
--       (FORALL (t: real):
--          (x0 < t AND t 
theorem min_eps : ∀ (m : List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ)))) (x0 : ℝ) (n : {n : {i : ℝ // i ≥ 0} // n ≥ atom_max m}) (f : ℝ → {l : List ℝ // List.length l = n}), ∃ (eps_min : {x : {x : ℝ // x ≥ 0} // x > 0}), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length m}), (∀ (t : ℝ), (x0 < t ∧ t < x0 + eps_min) →
    full_eval (nth m i.poly) (f t) ≠ 0) ∨ (∀ (t : ℝ), (x0 < t ∧ t < x0 + eps_min) →
    full_eval (nth m i.poly) (f t) = 0) := by
  -- === TCCs for min_eps ===
  -- TCC: min_eps_TCC1
  -- Statement: FORALL (m: meeting, x0: real, n: nat | n >= atom_max(m),
  --         f: (analytic?(n, x0)), eps_min: posreal,
  --         i: below(length[atomic_poly](m)), t: real):
  --   x0 < t AND t < x0 + eps_min IMPLIES
  --  ...
  have min_eps_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_eps_TCC2
  -- Statement: FORALL (m: meeting, x0: real, n: nat | n >= atom_max(m),
  --         f: (analytic?(n, x0)), eps_min: posreal,
  --         i: below(length[atomic_poly](m))):
  --   (NOT FORALL (t: real):
  --          (x0 < t AND t ...
  have min_eps_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.inf
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: glb(S)
axiom inf {pvs_τ36 : Type*} {pvs_τ37 : Type*} : pvs_τ36 → pvs_τ37

-- pvs2lean: poly_comp_analytic.Analytic?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real | x >= x0): analytic?(n, x)(f)
axiom Analytic_p {pvs_τ1 : Type*} {pvs_τ2 : Type*} : pvs_τ1 → pvs_τ2 → Prop

-- pvs2lean: poly_comp_analytic.clean_exit
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining, x0: real, f: (Analytic?(meet_max(j), x0))):
--   semi_alg(j)(meet_max(j))(f(x0)) IMPLIES
--    ((FORALL (x: real): x >= x0 IMPLIES semi_alg(j)(meet_max(j))(f(x))) OR
--      (EXISTS (eps: p
theorem clean_exit : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (x0 : ℝ) (f : ℝ → {l : List ℝ // List.length l = meet_max j}), semi_alg j(meet_max j) (f x0) → ((∀ (x : ℝ), x ≥ x0 → semi_alg j(meet_max j) (f x)) ∨
  (∃ (eps : {x : ℝ // 0 < x}), ∀ (t : ℝ), inf ({xx: real | x0 ≤ xx ∧ ¬ semi_alg j(meet_max j) (f xx)}) <
        t
        ∧
        t <
         inf ({xx: real | x0 ≤ xx ∧ ¬ semi_alg j(meet_max j) (f xx)})
          + eps
        → semi_alg j(meet_max j) (f t))
   ∨
   (∃ (eps : {x : ℝ // 0 < x}), ∀ (t : ℝ), inf ({xx: real | x0 ≤ xx ∧ ¬ semi_alg j(meet_max j) (f xx)})
         < t
         ∧
         t <
          inf ({xx: real |
                 x0 ≤ xx ∧ ¬ semi_alg j(meet_max j) (f xx)})
           + eps
         → ¬ semi_alg j(meet_max j) (f t))) := by
  -- === TCCs for clean_exit ===
  -- TCC: clean_exit_TCC1
  -- Statement: FORALL (j: joining, x0: real, f: (Analytic?(meet_max(j), x0))):
  --   meet_max(j) >= meet_max(j)
  have clean_exit_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: clean_exit_TCC2
  -- Statement: FORALL (j: joining, x0: real, f: (Analytic?(meet_max(j), x0))):
  --   semi_alg(j)(meet_max(j))(f(x0)) AND
  --    NOT (FORALL (x: real): x >= x0 IMPLIES semi_alg(j)(meet_max(j))(f(x)))
  --    IMPLIES
  --    nonempt...
  have clean_exit_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.clean_enter
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: joining, x0: real, f: (Analytic?(meet_max(j), x0))):
--   NOT semi_alg(j)(meet_max(j))(f(x0)) IMPLIES
--    ((FORALL (x: real): x >= x0 IMPLIES NOT semi_alg(j)(meet_max(j))(f(x)))
--      OR
--      (E
theorem clean_enter : ∀ (j : List (List (({ff : ℝ × ℝ → Bool // (ff = ≤) ∨ (ff = ≥) ∨ (ff = <) ∨ (ff = >)}) × (List (List ℝ × ℝ))))) (x0 : ℝ) (f : ℝ → {l : List ℝ // List.length l = meet_max j}), ¬ semi_alg j(meet_max j) (f x0) → ((∀ (x : ℝ), x ≥ x0 → ¬ semi_alg j(meet_max j) (f x)) ∨
  (∃ (eps : {x : ℝ // 0 < x}), ∀ (t : ℝ), inf ({xx: real | x0 ≤ xx ∧ semi_alg j(meet_max j) (f xx)}) < t
        ∧
        t <
         inf ({xx: real | x0 ≤ xx ∧ semi_alg j(meet_max j) (f xx)}) +
          eps
        → semi_alg j(meet_max j) (f t))
   ∨
   (∃ (eps : {x : ℝ // 0 < x}), ∀ (t : ℝ), inf ({xx: real | x0 ≤ xx ∧ semi_alg j(meet_max j) (f xx)}) < t
         ∧
         t <
          inf ({xx: real | x0 ≤ xx ∧ semi_alg j(meet_max j) (f xx)}) +
           eps
         → ¬ semi_alg j(meet_max j) (f t))) := by
  -- === TCCs for clean_enter ===
  -- TCC: clean_enter_TCC1
  -- Statement: FORALL (j: joining, x0: real, f: (Analytic?(meet_max(j), x0))):
  --   NOT semi_alg(j)(meet_max(j))(f(x0)) AND
  --    NOT (FORALL (x: real):
  --           x >= x0 IMPLIES NOT semi_alg(j)(meet_max(j))(f(x)))
  --    ...
  have clean_enter_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.finite_support_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real]):
--   ((EXISTS (N: nat): FORALL (n: nat): n > N IMPLIES a(n) = 0)) IMPLIES
--    convergent?(series(a))
theorem finite_support_series : ∀ (a : {i : ℝ // i ≥ 0} → ℝ), ((∃ (N : ℕ), ∀ (n : ℕ), n > N → a n = 0)) → convergent_p (series a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_comp_analytic.analytic_mono
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, alpha: real, mon: {mm: monomial | length(mm`alpha) = m},
--         f: (analytic?(m, alpha))):
--   analytic?(alpha)(LAMBDA (t: real): full_eval(mon)(f(t)))
theorem analytic_mono : ∀ (m : {i : ℝ // i ≥ 0}) (alpha : ℝ) (mon : {mm : List ℝ × ℝ // List.length mm.alpha = m}) (f : ℝ → {l : List ℝ // List.length l = m}), analytic_p alpha (fun (t : ℝ) => full_eval mon (f t)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.poly_comp_analytic