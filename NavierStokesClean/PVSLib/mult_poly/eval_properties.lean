-- Auto-generated by pvs2lean/translator.py
-- PVS theory: eval_properties
-- PVSLib library: mult_poly
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: eval_properties

Auto-generated stubs from PVSLib theory `eval_properties`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.eval_properties

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (VectorN : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (below : pvs_τ3 → pvs_τ4)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (car : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cdr : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} (cons_p : pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (eval : pvs_τ12 → pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (full_eval : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ19 : Type*} (integer_pred : pvs_τ19 → Prop)
variable {pvs_τ20 : Type*} (length : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} (list_length_n : pvs_τ21 → pvs_τ22)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} (max : pvs_τ23 → pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (max_length : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (member : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (min : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} (nth : pvs_τ33 → pvs_τ34 → pvs_τ35)
variable {pvs_τ36 : Type*} (null_p : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} (number_field_pred : pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} (rational_pred : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} (real_pred : pvs_τ39 → Prop)

-- pvs2lean: eval_properties.eval_monom_const
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem eval_monom_const : True := by
  -- === TCCs for eval_monom_const ===
  -- TCC: eval_monom_const_TCC1
  -- Statement: FORALL (ind: list[nat], vals: list[real]):
  --   length(ind) = length(vals) IMPLIES length[real](vals) = length[nat](ind)
  have eval_monom_const_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: eval_properties.bov?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF alpha = null THEN TRUE
-- ELSE car[nat](alpha) > i AND bov?(i)(cdr(alpha))
-- ENDIF
axiom bov_p {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: eval_properties.bov_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: nat, alpha: list[nat]):
--   bov?(i)(alpha) IFF FORALL (k: below(length(alpha))): nth(alpha, k) > i
theorem bov_nth : ∀ (i : {i : ℝ // i ≥ 0}) (alpha : List ℝ), bov_p i(alpha) ↔ ∀ (k : Finset.range List.length alpha), nth alpha k > i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.sub1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF l = null THEN null
-- ELSE cons[nat](car[nat](l) - 1, sub1(cdr[nat](l)))
-- ENDIF
axiom sub1 {pvs_τ40 : Type*} : pvs_τ40 → Prop

-- pvs2lean: eval_properties.one_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, k: below(n)): nth(^[nat]((: 1 :), (n)), k) = 1
theorem one_nth : ∀ (n : {i : ℝ // i ≥ 0}) (k : {s : {i : ℝ // i ≥ 0} // s < n}), nth (^[nat]((: 1 :), (n))) k = 1 := by
  -- === TCCs for one_nth ===
  -- TCC: one_nth_TCC1
  -- Statement: FORALL (n: nat, k: below(n)): k < length[nat]((^[nat]((: 1 :), (n))))
  have one_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: eval_properties.alpha_null_eval
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, vals: list[real],
--         index: {l: list[nat] | length(l) = length(vals)}):
--   m`alpha = null IMPLIES eval(m, index)(vals)`C = m`C
theorem alpha_null_eval : ∀ (m : List ℝ × ℝ) (vals : List ℝ) (index : {l : List ℝ // List.length l = List.length vals}), m.alpha = null → eval m index(vals).C = m.C := by
  -- === TCCs for alpha_null_eval ===
  -- TCC: alpha_null_eval_TCC1
  -- Statement: FORALL (m: monomial, vals: list[real],
  --         index: {l: list[nat] | length[nat](l) = length[real](vals)}):
  --   m`alpha = null IMPLIES length[real](vals) = length[nat](index)
  have alpha_null_eval_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: eval_properties.add_lists_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[nat]):
--   length(add_lists[nat](l1, l2)) = max(length(l1), length(l2))
theorem add_lists_length : ∀ (l1 : List ℝ) (l2 : List ℝ), List.length (add_lists l1 l2) = max (List.length l1) (List.length l2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.add_lists_nth
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2: list[nat], i: below(min(length(l1), length(l2)))):
--   nth(add_lists[nat](l1, l2), i) = nth(l1, i) + nth(l2, i)
theorem add_lists_nth : ∀ (l1 : List ℝ) (l2 : List ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < min (List.length l1) (List.length l2)}), nth (add_lists l1 l2) i = nth l1 i + nth l2 i := by
  -- === TCCs for add_lists_nth ===
  -- TCC: add_lists_nth_TCC1
  -- Statement: FORALL (l1, l2: list[nat],
  --         i: below(min(length[nat](l1), length[nat](l2)))):
  --   i < length[nat](add_lists[nat](l1, l2))
  have add_lists_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: add_lists_nth_TCC2
  -- Statement: FORALL (l1, l2: list[nat],
  --         i: below(min(length[nat](l1), length[nat](l2)))):
  --   i < length[nat](l1)
  have add_lists_nth_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: add_lists_nth_TCC3
  -- Statement: FORALL (l1, l2: list[nat],
  --         i: below(min(length[nat](l1), length[nat](l2)))):
  --   i < length[nat](l2)
  have add_lists_nth_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.member_0_bov
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (index: list[nat]): NOT member(0, index) IMPLIES bov?(0)(index)
theorem member_0_bov : ∀ (index : List ℝ), ¬ member 0 index → bov_p 0(index) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.bov_0_lln
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat): bov?(0)(cdr[nat](list_length_n[nat](n)))
theorem bov_0_lln : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}), bov_p 0 (cdr (list_length_n n)) := by
  -- === TCCs for bov_0_lln ===
  -- TCC: bov_0_lln_TCC1
  -- Statement: FORALL (n: posnat): cons?[nat](list_length_n[nat](n))
  have bov_0_lln_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.sub1_add0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, l: list[nat]):
--   length(l) = n IMPLIES sub1(add_lists[nat]((: 1 :) ^ (n), l)) = l
theorem sub1_add0 : ∀ (n : {i : ℝ // i ≥ 0}) (l : List ℝ), List.length l = n → sub1 (add_lists ((: 1 :) ^ (n)) l) = l := by
  -- === TCCs for sub1_add0 ===
  -- TCC: sub1_add0_TCC1
  -- Statement: FORALL (n: nat, l: list[nat]):
  --   length(l) = n IMPLIES
  --    every[number]
  --        (LAMBDA (x: number):
  --                number_field_pred(x) AND real_pred(x) AND rational_pred(x)
  --            AND integer_...
  have sub1_add0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sub1_add0_TCC2
  -- Statement: FORALL (n: nat, l: list[nat]):
  --   length(l) = n IMPLIES bov?(0)(add_lists[nat](^[nat]((: 1 :), (n)), l))
  have sub1_add0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.list_length_n_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat):
--   sub1(cdr(list_length_n[nat](n))) = list_length_n[nat](n - 1)
theorem list_length_n_cdr : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}), sub1 (cdr (list_length_n n)) = list_length_n (n - 1) := by
  -- === TCCs for list_length_n_cdr ===
  -- TCC: list_length_n_cdr_TCC1
  -- Statement: FORALL (n: posnat): bov?(0)(cdr[nat](list_length_n[nat](n)))
  have list_length_n_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: list_length_n_cdr_TCC2
  -- Statement: FORALL (n: posnat): n - 1 >= 0
  have list_length_n_cdr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.eval_replace
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem eval_replace : True := by
  -- === TCCs for eval_replace ===
  -- TCC: eval_replace_TCC1
  -- Statement: FORALL (m: monomial, vals: list[real],
  --         index: {l: list[nat] | length[nat](l) = length[real](vals)}):
  --   NOT member[nat](0, index) AND NOT m`alpha = null IMPLIES
  --    length[real](vals) = lengt...
  have eval_replace_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_replace_TCC2
  -- Statement: FORALL (m: monomial, vals: list[real],
  --         index: {l: list[nat] | length[nat](l) = length[real](vals)}):
  --   NOT member[nat](0, index) AND NOT m`alpha = null IMPLIES
  --    length[real](vals) = lengt...
  have eval_replace_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_replace_TCC3
  -- Statement: FORALL (m: monomial, vals: list[real],
  --         index: {l: list[nat] | length[nat](l) = length[real](vals)}):
  --   NOT member[nat](0, index) AND NOT m`alpha = null IMPLIES
  --    cons?[nat](m`alpha)
  have eval_replace_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_replace_TCC4
  -- Statement: FORALL (m: monomial, vals: list[real],
  --         index: {l: list[nat] | length[nat](l) = length[real](vals)}):
  --   NOT member[nat](0, index) AND NOT m`alpha = null IMPLIES bov?(0)(index)
  have eval_replace_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: eval_properties.eval_cdr
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem eval_cdr : True := by
  -- === TCCs for eval_cdr ===
  -- TCC: eval_cdr_TCC1
  -- Statement: FORALL (m: {mm: monomial | cons?[real](mm`alpha)},
  --         vals: {l: list[real] | cons?[real](l)}):
  --   length[real](vals) = length[nat](list_length_n[nat](length[real](vals)))
  have eval_cdr_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_cdr_TCC2
  -- Statement: FORALL (m: {mm: monomial | cons?[real](mm`alpha)},
  --         vals: {l: list[real] | cons?[real](l)}):
  --   length[real](cdr[real](vals)) =
  --    length[nat](list_length_n[nat](length[real](cdr[real](vals))))
  have eval_cdr_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_cdr_TCC3
  -- Statement: FORALL (m: {mm: monomial | cons?[real](mm`alpha)},
  --         vals: {l: list[real] | cons?[real](l)}):
  --   cons?[nat](m`alpha)
  have eval_cdr_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_cdr_TCC4
  -- Statement: FORALL (m: {mm: monomial | cons?[real](mm`alpha)},
  --         vals: {l: list[real] | cons?[real](l)}):
  --   0 < length[nat](m`alpha)
  have eval_cdr_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_cdr_TCC5
  -- Statement: FORALL (m: {mm: monomial | cons?[real](mm`alpha)},
  --         vals: {l: list[real] | cons?[real](l)}):
  --   car[real](vals) /= 0 OR nth[nat](m`alpha, 0) >= 0
  have eval_cdr_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: eval_properties.hat
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# C := 1, alpha := cdr[nat](m`alpha) #)
axiom hat {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ17 → pvs_τ18

-- pvs2lean: eval_properties.first
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: m`C * x ^ car[nat](m`alpha)
noncomputable def first : sorry := by
  sorry  -- pvs2lean: definition stub for m`C * x ^ car[nat](m`alpha)

-- pvs2lean: eval_properties.eval_vals
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, m: {mm: monomial | length(mm`alpha) = n},
--         vals: list[real] | length(vals) >= length(m`alpha)):
--   full_eval(m)(vals) =
--    m`C * car(vals) ^ car[nat](m`alpha) * full_eval(hat(
theorem eval_vals : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (m : {mm : List ℝ × ℝ // List.length mm.alpha = n}) (vals : {vals : List ℝ // List.length vals ≥ List.length m.alpha}), full_eval m(vals) = m.C * car vals ^ car m.alpha * full_eval (hat m) (cdr vals) := by
  -- === TCCs for eval_vals ===
  -- TCC: eval_vals_TCC1
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         vals: list[real] | length[real](vals) >= length[nat](m`alpha)):
  --   cons?[real](vals)
  have eval_vals_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_vals_TCC2
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         vals: list[real] | length[real](vals) >= length[nat](m`alpha)):
  --   cons?[nat](m`alpha)
  have eval_vals_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_vals_TCC3
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         vals: list[real] | length[real](vals) >= length[nat](m`alpha)):
  --   car[real](vals) /= 0 OR car[nat](m`alpha) >= 0
  have eval_vals_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_vals_TCC4
  -- Statement: FORALL (n: posnat, m: {mm: monomial | length[nat](mm`alpha) = n},
  --         vals: list[real] | length[real](vals) >= length[nat](m`alpha)):
  --   length[real](cdr[real](vals)) >= length[nat](hat(m)`alpha)
  have eval_vals_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.full_eval_recurs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, vals: {l: list[real] | length(l) >= max_length(p)}):
--   null?(p) OR
--    full_eval(p)(vals) = full_eval(car(p))(vals) + full_eval(cdr(p))(vals)
theorem full_eval_recurs : ∀ (p : List (List ℝ × ℝ)) (vals : {l : List ℝ // List.length l ≥ max_length p}), null_p p ∨ full_eval p(vals) = full_eval (car p) (vals) + full_eval (cdr p) (vals) := by
  -- === TCCs for full_eval_recurs ===
  -- TCC: full_eval_recurs_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   NOT null?(p) IMPLIES
  --    length[real](vals) >= length[nat](car[monomial](p)`alpha)
  have full_eval_recurs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: full_eval_recurs_TCC2
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   NOT null?(p) IMPLIES cons?[monomial](p)
  have full_eval_recurs_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: full_eval_recurs_TCC3
  -- Statement: FORALL (p: MultPoly,
  --         vals: {l: list[real] | length[real](l) >= max_length(p)}):
  --   NOT null?(p) IMPLIES length[real](vals) >= max_length(cdr[monomial](p))
  have full_eval_recurs_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.eval_canc_car
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m1, m2: monomial,
--         vals: list[real]
--             | length(vals) >= max(length(m1`alpha), length(m2`alpha))):
--   (m1`C + m2`C = 0 AND m1`alpha = m2`alpha) IMPLIES
--    0 = full_eval(m1)(vals
theorem eval_canc_car : ∀ (m1 : List ℝ × ℝ) (m2 : List ℝ × ℝ) (vals : {vals : List ℝ // List.length vals ≥ max (List.length m1.alpha) (List.length m2.alpha)}), (m1.C + m2.C = 0 ∧ m1.alpha = m2.alpha) → 0 = full_eval m1(vals) + full_eval m2(vals) := by
  -- === TCCs for eval_canc_car ===
  -- TCC: eval_canc_car_TCC1
  -- Statement: FORALL (m1, m2: monomial,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m1`alpha), length[nat](m2`alpha))):
  --   m1`C + m2`C = 0 AND m1`alpha = m2`alpha I...
  have eval_canc_car_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_canc_car_TCC2
  -- Statement: FORALL (m1, m2: monomial,
  --         vals: list[real]
  --             | length[real](vals) >=
  --                max(length[nat](m1`alpha), length[nat](m2`alpha))):
  --   m1`C + m2`C = 0 AND m1`alpha = m2`alpha I...
  have eval_canc_car_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.eval_higher_dim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: monomial, val1: {v: list[real] | length(v) = length(m`alpha)},
--         val2: {v: list[real] | length(v) > length(val1)}):
--   (FORALL (i: below(length(m`alpha))): nth(val1, i) = nth(val2, i))
theorem eval_higher_dim : ∀ (m : List ℝ × ℝ) (val1 : {v : List ℝ // List.length v = List.length m.alpha}) (val2 : {v : List ℝ // List.length v > List.length val1}), (∀ (i : Finset.range List.length m.alpha), nth val1 i = nth val2 i) → full_eval m(val1) = full_eval m(val2) := by
  -- === TCCs for eval_higher_dim ===
  -- TCC: eval_higher_dim_TCC1
  -- Statement: FORALL (m: monomial,
  --         val1: {v: list[real] | length[real](v) = length[nat](m`alpha)},
  --         val2: {v: list[real] | length[real](v) > length[real](val1)},
  --         i: below(length[nat](m`alp...
  have eval_higher_dim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_higher_dim_TCC2
  -- Statement: FORALL (m: monomial,
  --         val1: {v: list[real] | length[real](v) = length[nat](m`alpha)},
  --         val2: {v: list[real] | length[real](v) > length[real](val1)},
  --         i: below(length[nat](m`alp...
  have eval_higher_dim_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_higher_dim_TCC3
  -- Statement: FORALL (m: monomial,
  --         val1: {v: list[real] | length[real](v) = length[nat](m`alpha)},
  --         val2: {v: list[real] | length[real](v) > length[real](val1)}):
  --   (FORALL (i: below(length(m`alph...
  have eval_higher_dim_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_higher_dim_TCC4
  -- Statement: FORALL (m: monomial,
  --         val1: {v: list[real] | length[real](v) = length[nat](m`alpha)},
  --         val2: {v: list[real] | length[real](v) > length[real](val1)}):
  --   (FORALL (i: below(length(m`alph...
  have eval_higher_dim_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: eval_properties.eval_higher_dim_poly
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: MultPoly, val1: {v: list[real] | length(v) >= max_length(p)},
--         val2: {v: list[real] | length(v) > length(val1)}):
--   (FORALL (i: below(max_length(p))): nth(val1, i) = nth(val2, i)) IM
theorem eval_higher_dim_poly : ∀ (p : List (List ℝ × ℝ)) (val1 : {v : List ℝ // List.length v ≥ max_length p}) (val2 : {v : List ℝ // List.length v > List.length val1}), (∀ (i : Finset.range max_length p), nth val1 i = nth val2 i) → full_eval p(val1) = full_eval p(val2) := by
  -- === TCCs for eval_higher_dim_poly ===
  -- TCC: eval_higher_dim_poly_TCC1
  -- Statement: FORALL (p: MultPoly,
  --         val1: {v: list[real] | length[real](v) >= max_length(p)},
  --         val2: {v: list[real] | length[real](v) > length[real](val1)},
  --         i: below(max_length(p))):
  --   i < ...
  have eval_higher_dim_poly_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_higher_dim_poly_TCC2
  -- Statement: FORALL (p: MultPoly,
  --         val1: {v: list[real] | length[real](v) >= max_length(p)},
  --         val2: {v: list[real] | length[real](v) > length[real](val1)},
  --         i: below(max_length(p))):
  --   i < ...
  have eval_higher_dim_poly_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: eval_higher_dim_poly_TCC3
  -- Statement: FORALL (p: MultPoly,
  --         val1: {v: list[real] | length[real](v) >= max_length(p)},
  --         val2: {v: list[real] | length[real](v) > length[real](val1)}):
  --   (FORALL (i: below(max_length(p))): nt...
  have eval_higher_dim_poly_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.eval_properties