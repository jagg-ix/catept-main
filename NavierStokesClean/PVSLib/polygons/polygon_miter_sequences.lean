-- Auto-generated by pvs2lean/translator.py
-- PVS theory: polygon_miter_sequences
-- PVSLib library: polygons
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: polygon_miter_sequences

Auto-generated stubs from PVSLib theory `polygon_miter_sequences`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.polygon_miter_sequences

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (append_segment_seqs : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (below : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (closed_miter_point_seq : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (cross : pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (dir_parallel_p : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (edges_of_polygon : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (find_index_any : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (four_point_miniseq_p : pvs_τ21 → pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} (four_point_offset_dist_p : pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (half_angle_vsin : pvs_τ28 → pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} (has_rightward_crossings_p : pvs_τ31 → pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (leftmost_intersection_point : pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (loop_back_intercept : pvs_τ35 → pvs_τ36)
variable {pvs_τ37 : Type*} (max_miter_offset : pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (min : pvs_τ38 → pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (min_edge_separation : pvs_τ41 → pvs_τ42)
variable {pvs_τ43 : Type*} (min_miter_sine : pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (min_seg_separation : pvs_τ44 → pvs_τ45)
variable {pvs_τ46 : Type*} (min_segment_sep : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} (miter_point : pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (miter_seq_offset_dist : pvs_τ51 → pvs_τ52)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (mostly_uniq_point_list_p : pvs_τ53 → pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} (next_index : pvs_τ55 → pvs_τ56 → pvs_τ57)
variable {pvs_τ58 : Type*} (norm : pvs_τ58 → Prop)
variable {pvs_τ59 : Type*} {pvs_τ60 : Type*} (off_right_side_p : pvs_τ59 → pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} (point_between_edges_p : pvs_τ61 → pvs_τ62 → pvs_τ63 → pvs_τ64 → Prop)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} (point_between_rays_p : pvs_τ65 → pvs_τ66 → pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} (point_on_polygon_perimeter_p : pvs_τ68 → pvs_τ69 → Prop)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} (point_on_segment_p : pvs_τ70 → pvs_τ71 → Prop)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} (point_on_segment_seq_p : pvs_τ72 → pvs_τ73 → Prop)
variable {pvs_τ75 : Type*} {pvs_τ76 : Type*} {pvs_τ77 : Type*} (prev_index : pvs_τ75 → pvs_τ76 → pvs_τ77)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} (rem : pvs_τ78 → pvs_τ79 → pvs_τ80)
variable {pvs_τ81 : Type*} {pvs_τ82 : Type*} {pvs_τ83 : Type*} (rev_polygon_index : pvs_τ81 → pvs_τ82 → pvs_τ83)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} (reverse_polygon : pvs_τ84 → pvs_τ85)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (segment_endpoint_p : pvs_τ88 → pvs_τ89 → Prop)
variable {pvs_τ90 : Type*} (segment_falling_p : pvs_τ90 → Prop)
variable {pvs_τ91 : Type*} (segment_rising_p : pvs_τ91 → Prop)
variable {pvs_τ92 : Type*} (segments_do_not_cross_p : pvs_τ92 → Prop)
variable {pvs_τ93 : Type*} (seq : pvs_τ93 → Prop)
variable {pvs_τ94 : Type*} (simple_polygon_2d_p : pvs_τ94 → Prop)
variable {pvs_τ95 : Type*} {pvs_τ96 : Type*} (single_loop_back_seq_p : pvs_τ95 → pvs_τ96 → Prop)
variable {pvs_τ97 : Type*} {pvs_τ98 : Type*} (singleton_endpoint_seq : pvs_τ97 → pvs_τ98)
variable {pvs_τ99 : Type*} {pvs_τ100 : Type*} (three_point_miniseq_p : pvs_τ99 → pvs_τ100 → Prop)
variable {pvs_τ101 : Type*} {pvs_τ102 : Type*} {pvs_τ103 : Type*} (three_point_offset_dist_p : pvs_τ101 → pvs_τ102 → pvs_τ103 → Prop)
variable {pvs_τ104 : Type*} {pvs_τ105 : Type*} (upto : pvs_τ104 → pvs_τ105)
variable {pvs_τ112 : Type*} (vertices : pvs_τ112 → Prop)

-- pvs2lean: polygon_miter_sequences.vertex_crossing_index
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET s = edges_of_polygon(G)(prev_index(G, i)),
--     e = edges_of_polygon(G)(i),
--     u = s`p1,
--     v = e`p1,
--     w = e`p2
--   IN
--   IF u`y = v`y THEN i
--   ELSIF v`y = w`y THEN prev_index(G, i)
--   ELSIF u`y <
axiom vertex_crossing_index {pvs_τ106 : Type*} {pvs_τ107 : Type*} {pvs_τ108 : Type*} : pvs_τ106 → pvs_τ107 → pvs_τ108

-- pvs2lean: polygon_miter_sequences.rightward_crossing_index
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET q = leftmost_intersection_point(G, p), i = find_index_any(G, q) IN
--   IF q = G`vertices(i) THEN vertex_crossing_index(G, i) ELSE i ENDIF
axiom rightward_crossing_index {pvs_τ86 : Type*} {pvs_τ87 : Type*} : pvs_τ86 → pvs_τ87 → Prop

-- pvs2lean: polygon_miter_sequences.rightward_crossing_index_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   rightward_crossing_index(reverse_polygon(G), p) =
--    prev_index(G, rev_polygon_index(G, rightward_crossing_index(G, p)))
theorem rightward_crossing_index_reverse : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), rightward_crossing_index (reverse_polygon G) p = prev_index G (rev_polygon_index G (rightward_crossing_index G p)) := by
  -- === TCCs for rightward_crossing_index_reverse ===
  -- TCC: rightward_crossing_index_reverse_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
  --   simple_polygon_2d?(reverse_polygon(G))
  have rightward_crossing_index_reverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: rightward_crossing_index_reverse_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
  --   has_rightward_crossings?(reverse_polygon(G))(p)
  have rightward_crossing_index_reverse_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.crossed_edge_rising_or_falling
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET e = edges_of_polygon(G)(rightward_crossing_index(G, p)) IN
--     segment_rising?(e) OR segment_falling?(e)
theorem crossed_edge_rising_or_falling : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => segment_rising_p e ∨ segment_falling_p e (edges_of_polygon G (rightward_crossing_index G p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.leftmost_intersection_on_crossed_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET e = edges_of_polygon(G)(rightward_crossing_index(G, p)),
--       q = leftmost_intersection_point(G, p)
--     IN point_on_segment?(q, 
theorem leftmost_intersection_on_crossed_edge : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (q : ℝ × ℝ) => point_on_segment_p q e (leftmost_intersection_point G p) (edges_of_polygon G (rightward_crossing_index G p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.crossing_within_falling_edge_y_range
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET e = edges_of_polygon(G)(rightward_crossing_index(G, p)) IN
--     segment_falling?(e) IMPLIES e`p2`y <= p`y AND p`y <= e`p1`y
theorem crossing_within_falling_edge_y_range : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => segment_falling_p e → e.p2.y ≤ p.y ∧ p.y ≤ e.p1.y (edges_of_polygon G (rightward_crossing_index G p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.cross_horiz_seq_with_falling_edge_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET e = edges_of_polygon(G)(rightward_crossing_index(G, p)) IN
--     segment_falling?(e) IMPLIES cross(p - e`p1, e`p2 - e`p1) > 0
theorem cross_horiz_seq_with_falling_edge_pos : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => segment_falling_p e → cross (p - e.p1) (e.p2 - e.p1) > 0 (edges_of_polygon G (rightward_crossing_index G p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.between_edges_at_falling_edge_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET i = rightward_crossing_index(G, p),
--       e = edges_of_polygon(G)(i),
--       q = leftmost_intersection_point(G, p)
--     IN
--     q = 
theorem between_edges_at_falling_edge_vertex : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (q : ℝ × ℝ) => q = G.vertices i ∧ segment_falling_p e → point_between_edges_p True G i(p) (leftmost_intersection_point G p) (edges_of_polygon G i) (rightward_crossing_index G p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.crossing_falling_off_right_side
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET i = rightward_crossing_index(G, p),
--       e = edges_of_polygon(G)(i),
--       q = leftmost_intersection_point(G, p)
--     IN segment_
theorem crossing_falling_off_right_side : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (q : ℝ × ℝ) => segment_falling_p e → off_right_side_p p e (leftmost_intersection_point G p) (edges_of_polygon G i) (rightward_crossing_index G p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.three_pt_miniseq_in_polygon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   three_point_miniseq?(G`vertices(prev_index(G, i)), G`vertices(i))
--                       (G`vertices(next_index(G, i)))
theorem three_pt_miniseq_in_polygon : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), three_point_miniseq_p (G.vertices (prev_index G i)) (G.vertices i) (G.vertices (next_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.four_pt_miniseq_in_polygon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   four_point_miniseq?(G`vertices(prev_index(G, i)), G`vertices(i),
--                       G`vertices(next_index(G, i)))
--                      (G`
theorem four_pt_miniseq_in_polygon : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), four_point_miniseq_p (G.vertices (prev_index G i)) (G.vertices i) (G.vertices (next_index G i)) (G.vertices (next_index G (next_index G i))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.polygon_vertex_seq_single_loop
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d):
--   single_loop_back_seq?((# length := G`num_vertices + 1,
--                            seq
--                              := LAMBDA (i: upto(G`num_vertices)):
--                
theorem polygon_vertex_seq_single_loop : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), single_loop_back_seq_p (# List.length := G.num_vertices + 1) (seq
     := fun (i : {k : ℕ // k ≤ G.num_vertices})=> G.vertices (rem G.num_vertices(i)) #) := by
  -- === TCCs for polygon_vertex_seq_single_loop ===
  -- TCC: polygon_vertex_seq_single_loop_TCC1
  -- Statement: FORALL (G: simple_polygon_2d):
  --   FORALL (x: nat): x <= G`num_vertices IFF x < 1 + G`num_vertices
  have polygon_vertex_seq_single_loop_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: polygon_vertex_seq_single_loop_TCC2
  -- Statement: FORALL (G: simple_polygon_2d):
  --   G`num_vertices + 1 > 0 AND
  --    FORALL (i_1: below(G`num_vertices)):
  --      NOT G`vertices(rem(G`num_vertices)(i_1)) =
  --           G`vertices(rem(G`num_vertices)(1 + i_1))
  have polygon_vertex_seq_single_loop_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.polygon_vertex_seq_intercept_eq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d):
--   0 =
--    loop_back_intercept((# length := G`num_vertices + 1,
--                           seq
--                             := LAMBDA (i: upto(G`num_vertices)):
--             
theorem polygon_vertex_seq_intercept_eq_0 : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), 0 = loop_back_intercept ((# List.length := G.num_vertices + 1,
                       seq
                         := fun (i : {k : ℕ // k ≤ G.num_vertices})=> G.vertices (rem G.num_vertices(i)) #)) := by
  -- === TCCs for polygon_vertex_seq_intercept_eq_0 ===
  -- TCC: polygon_vertex_seq_intercept_eq_0_TCC1
  -- Statement: FORALL (G: simple_polygon_2d):
  --   (G`num_vertices + 1 > 0 AND
  --     FORALL (i_1: below(G`num_vertices)):
  --       NOT G`vertices(rem(G`num_vertices)(i_1)) =
  --            G`vertices(rem(G`num_vertices)(1 + ...
  have polygon_vertex_seq_intercept_eq_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.polygon_vertex_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := G`num_vertices + 1,
--    seq
--      := LAMBDA (i: upto(G`num_vertices)):
--           G`vertices(rem(G`num_vertices)(i)) #)
axiom polygon_vertex_seq {pvs_τ74 : Type*} : pvs_τ74 → Prop

-- pvs2lean: polygon_miter_sequences.polygon_vertex_seq_intercept
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d):
--   loop_back_intercept(polygon_vertex_seq(G)) = 0
theorem polygon_vertex_seq_intercept : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), loop_back_intercept (polygon_vertex_seq G) = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.min_seg_separation_eq_min_edge_separation
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d):
--   min_seg_separation(polygon_vertex_seq(G)) = min_edge_separation(G)
theorem min_seg_separation_eq_min_edge_separation : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), min_seg_separation (polygon_vertex_seq G) = min_edge_separation G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.max_miter_offset_lt_min_edge_sep
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d):
--   max_miter_offset(polygon_vertex_seq(G)) < min_edge_separation(G)
theorem max_miter_offset_lt_min_edge_sep : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), max_miter_offset (polygon_vertex_seq G) < min_edge_separation G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.three_pt_offset_dist_in_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices),
--         d: miter_seq_offset_dist(polygon_vertex_seq(G))):
--   three_point_offset_dist?(G`vertices(prev_index(G, i)), G`vertices(i),
--               
theorem three_pt_offset_dist_in_polygon : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset (polygon_vertex_seq G)}), three_point_offset_dist_p (G.vertices (prev_index G i)) (G.vertices i) (G.vertices (next_index G i)) d := by
  -- === TCCs for three_pt_offset_dist_in_polygon ===
  -- TCC: three_pt_offset_dist_in_polygon_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices),
  --         d: miter_seq_offset_dist(polygon_vertex_seq(G))):
  --   three_point_miniseq?(G`vertices(prev_index(G, i)), G`vertices(i))
  --                ...
  have three_pt_offset_dist_in_polygon_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.four_pt_offset_dist_in_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices),
--         d: miter_seq_offset_dist(polygon_vertex_seq(G))):
--   four_point_offset_dist?(G`vertices(prev_index(G, i)), G`vertices(i),
--                
theorem four_pt_offset_dist_in_polygon : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset (polygon_vertex_seq G)}), four_point_offset_dist_p (G.vertices (prev_index G i)) (G.vertices i) (G.vertices (next_index G i)) (G.vertices (next_index G (next_index G i))) d := by
  -- === TCCs for four_pt_offset_dist_in_polygon ===
  -- TCC: four_pt_offset_dist_in_polygon_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices),
  --         d: miter_seq_offset_dist(polygon_vertex_seq(G))):
  --   four_point_miniseq?(G`vertices(prev_index(G, i)), G`vertices(i),
  --                 ...
  have four_pt_offset_dist_in_polygon_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.polygon_miter_point_seq_element
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d,
--         d: miter_seq_offset_dist(S), i: below(G`num_vertices)):
--   S = polygon_vertex_seq(G) IMPLIES
--    closed_miter_point_seq(S, d)`seq(i) 
theorem polygon_miter_point_seq_element : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {d : {x : {x : ℝ // x ≥ 0} // x > 0} // d ≤ max_miter_offset S}) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), S = polygon_vertex_seq G → closed_miter_point_seq S d.seq i =
 miter_point (G.vertices i) (G.vertices (prev_index G i)) (G.vertices (next_index G i)) d := by
  -- === TCCs for polygon_miter_point_seq_element ===
  -- TCC: polygon_miter_point_seq_element_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d,
  --         d: miter_seq_offset_dist(S), i: below(G`num_vertices)):
  --   S = polygon_vertex_seq(G) IMPLIES i < closed_miter_point_seq(S, d)`length
  have polygon_miter_point_seq_element_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: polygon_miter_point_seq_element_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d,
  --         d: miter_seq_offset_dist(S), i: below(G`num_vertices)):
  --   S = polygon_vertex_seq(G) IMPLIES S`seq(0) = S`seq(S`length - 1)
  have polygon_miter_point_seq_element_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: polygon_miter_point_seq_element_TCC3
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d,
  --         d: miter_seq_offset_dist(S), i: below(G`num_vertices)):
  --   S = polygon_vertex_seq(G) IMPLIES
  --    G`vertices(prev_index(G, i)) /= G...
  have polygon_miter_point_seq_element_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: polygon_miter_point_seq_element_TCC4
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d,
  --         d: miter_seq_offset_dist(S), i: below(G`num_vertices)):
  --   S = polygon_vertex_seq(G) IMPLIES
  --    G`vertices(next_index(G, i)) /= G...
  have polygon_miter_point_seq_element_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.point_on_perim_iff_on_vertex_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   point_on_polygon_perimeter?(G)(p) IFF
--    point_on_segment_seq?(p, polygon_vertex_seq(G))
theorem point_on_perim_iff_on_vertex_seq : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_polygon_perimeter_p G(p) ↔ point_on_segment_seq_p p (polygon_vertex_seq G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.min_seg_sep_le_edge_length
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d, e: segment_2d,
--         i: below(G`num_vertices)):
--   S = polygon_vertex_seq(G) AND e = edges_of_polygon(G)(i) IMPLIES
--    min_segment_sep(S) 
theorem min_seg_sep_le_edge_length : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), S = polygon_vertex_seq G ∧ e = edges_of_polygon G(i) → min_segment_sep S ≤ norm (e.p2 - e.p1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.min_miter_sine_le_vertex_sine
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d,
--         i: below(G`num_vertices)):
--   S = polygon_vertex_seq(G) IMPLIES
--    min_miter_sine(S) <=
--     half_angle_vsin(^(G`vertices(prev_index(
theorem min_miter_sine_le_vertex_sine : ∀ (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), S = polygon_vertex_seq G → min_miter_sine S ≤
 half_angle_vsin (^(G.vertices (prev_index G i) - G.vertices i)) (^(G.vertices (next_index G i) - G.vertices i)) := by
  -- === TCCs for min_miter_sine_le_vertex_sine ===
  -- TCC: min_miter_sine_le_vertex_sine_TCC1
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d,
  --         i: below(G`num_vertices)):
  --   S = polygon_vertex_seq(G) IMPLIES
  --    G`vertices(prev_index(G, i)) - G`vertices(i) /= zero
  have min_miter_sine_le_vertex_sine_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_miter_sine_le_vertex_sine_TCC2
  -- Statement: FORALL (S: edge_parallel_endpoint_seq, G: simple_polygon_2d,
  --         i: below(G`num_vertices)):
  --   S = polygon_vertex_seq(G) IMPLIES
  --    G`vertices(next_index(G, i)) - G`vertices(i) /= zero
  have min_miter_sine_le_vertex_sine_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.vertex_miter_point
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: miter_point(G`vertices(i), G`vertices(prev_index(G, i)),
--             G`vertices(next_index(G, i)), d)
axiom vertex_miter_point {pvs_τ109 : Type*} {pvs_τ110 : Type*} {pvs_τ111 : Type*} : pvs_τ109 → pvs_τ110 → pvs_τ111 → Prop

-- pvs2lean: polygon_miter_sequences.seg_from_miter_point_not_on_perim
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, q: point_2d, i: below(G`num_vertices),
--         d: posreal):
--   LET S = polygon_vertex_seq(G),
--       mp =
--         miter_point(G`vertices(i), G`vertices(prev_index(G, i)),
--  
theorem seg_from_miter_point_not_on_perim : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (q : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (mp : ℝ × ℝ) => d ≤ max_miter_offset S ∧ point_on_segment_p q (edges_of_polygon G(i)) → ∀ (r : point_2d), point_on_segment_p r ((# p1 := mp, p2 := q #)) ∧ r ≠ q →
   ¬ point_on_polygon_perimeter_p G(r) (miter_point (G.vertices i) (G.vertices (prev_index G i)) (G.vertices (next_index G i)) d) (polygon_vertex_seq G) := by
  -- === TCCs for seg_from_miter_point_not_on_perim ===
  -- TCC: seg_from_miter_point_not_on_perim_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, q: point_2d, i: below(G`num_vertices),
  --         d: posreal, S: edge_parallel_endpoint_seq, mp: point_2d):
  --   S = polygon_vertex_seq(G) AND
  --    mp =
  --     miter_point(G`vert...
  have seg_from_miter_point_not_on_perim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.miter_point_seqs_not_on_perim
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: point_2d, d: posreal):
--   LET S = polygon_vertex_seq(G) IN
--     d <= max_miter_offset(S) AND
--      point_on_segment_seq?(p, closed_miter_point_seq(S, d))
--      IMPLIES NOT
theorem miter_point_seqs_not_on_perim : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => d ≤ max_miter_offset S ∧
 point_on_segment_seq_p p (closed_miter_point_seq S d) → ¬ point_on_polygon_perimeter_p G(p) (polygon_vertex_seq G) := by
  -- === TCCs for miter_point_seqs_not_on_perim ===
  -- TCC: miter_point_seqs_not_on_perim_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, d: posreal, S: edge_parallel_endpoint_seq):
  --   S = polygon_vertex_seq(G) AND d <= max_miter_offset(S) IMPLIES
  --    S`seq(0) = S`seq(S`length - 1)
  have miter_point_seqs_not_on_perim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.miter_point_in_polygon_miter_seg_seq
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET S = polygon_vertex_seq(G),
--       mp =
--         miter_point(G`vertices(i), G`vertices(prev_index(G, i)),
--                     G`
theorem miter_point_in_polygon_miter_seg_seq : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (mp : ℝ × ℝ) => d ≤ max_miter_offset S → point_on_segment_seq_p mp (closed_miter_point_seq S d) (miter_point (G.vertices i) (G.vertices (prev_index G i)) (G.vertices (next_index G i)) d) (polygon_vertex_seq G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.miter_point_not_on_perim
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), mp: point_2d,
--         d: posreal):
--   d <= max_miter_offset(polygon_vertex_seq(G)) AND
--    mp = vertex_miter_point(G, i, d)
--    IMPLIES NOT point_o
theorem miter_point_not_on_perim : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (mp : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), d ≤ max_miter_offset (polygon_vertex_seq G) ∧
 mp = vertex_miter_point G i d → ¬ point_on_polygon_perimeter_p G(mp) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.miter_point_dist_to_vertex_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET H = polygon_vertex_seq(G), mp = vertex_miter_point(G, i, d) IN
--     d <= max_miter_offset(H) IMPLIES
--      norm(mp - G`vertices
theorem miter_point_dist_to_vertex_bound : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (H : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (mp : ℝ × ℝ) => d ≤ max_miter_offset H → norm (mp - G.vertices i) ≤ d / min_miter_sine H (vertex_miter_point G i d) (polygon_vertex_seq G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.contact_free_path?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: point_2d):
--   NOT (point_on_segment_seq?(r, S) AND point_on_polygon_perimeter?(G)(r))
axiom contact_free_path_p {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12 → Prop

-- pvs2lean: polygon_miter_sequences.contact_free_path_excl?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (r: point_2d):
--   r = q OR
--    NOT (point_on_segment_seq?(r, S) AND point_on_polygon_perimeter?(G)(r))
axiom contact_free_path_excl_p {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10 → Prop

-- pvs2lean: polygon_miter_sequences.singleton_contact_free_path
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: point_2d):
--   point_on_polygon_perimeter?(G)(p) OR
--    contact_free_path?(G)(singleton_endpoint_seq(p))
theorem singleton_contact_free_path : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), point_on_polygon_perimeter_p G(p) ∨ contact_free_path_p G(singleton_endpoint_seq p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.append_contact_free_paths
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, X, Y: segment_endpoint_seq):
--   X`seq(X`length - 1) = Y`seq(0) AND
--    contact_free_path?(G)(X) AND contact_free_path?(G)(Y)
--    IMPLIES contact_free_path?(G)(append_segment
theorem append_contact_free_paths : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (X : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (Y : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}), X.seq (X.List.length - 1) = Y.seq 0 ∧
 contact_free_path_p G(X) ∧ contact_free_path_p G(Y) → contact_free_path_p G(append_segment_seqs X Y) := by
  -- === TCCs for append_contact_free_paths ===
  -- TCC: append_contact_free_paths_TCC1
  -- Statement: FORALL (X, Y: segment_endpoint_seq):
  --   X`length - 1 >= 0 AND X`length - 1 < X`length
  have append_contact_free_paths_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: append_contact_free_paths_TCC2
  -- Statement: FORALL (X, Y: segment_endpoint_seq): 0 < Y`length
  have append_contact_free_paths_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.append_contact_free_paths_excl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, q: point_2d, X, Y: segment_endpoint_seq):
--   X`seq(X`length - 1) = Y`seq(0) AND
--    contact_free_path?(G)(X) AND contact_free_path_excl?(G, q)(Y)
--    IMPLIES contact_free_pa
theorem append_contact_free_paths_excl : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (q : ℝ × ℝ) (X : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (Y : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}), X.seq (X.List.length - 1) = Y.seq 0 ∧
 contact_free_path_p G(X) ∧ contact_free_path_excl_p G q(Y) → contact_free_path_excl_p G q(append_segment_seqs X Y) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.polygon_miter_pt_seq_contact_free
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, d: posreal):
--   LET S = polygon_vertex_seq(G) IN
--     d <= max_miter_offset(S) IMPLIES
--      contact_free_path?(G)(closed_miter_point_seq(S, d))
theorem polygon_miter_pt_seq_contact_free : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => d ≤ max_miter_offset S → contact_free_path_p G(closed_miter_point_seq S d) (polygon_vertex_seq G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.polygon_miter_pt_subseq_contact_free
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, j: upto(G`num_vertices), i: upto(j),
--         d: posreal):
--   LET S = polygon_vertex_seq(G) IN
--     d <= max_miter_offset(S) IMPLIES
--      contact_free_path?(G)(closed_miter_
theorem polygon_miter_pt_subseq_contact_free : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s ≤ G.num_vertices}) (i : {s : {i : ℝ // i ≥ 0} // s ≤ j}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => d ≤ max_miter_offset S → contact_free_path_p G(closed_miter_point_seq S d ^ (i, j)) (polygon_vertex_seq G) := by
  -- === TCCs for polygon_miter_pt_subseq_contact_free ===
  -- TCC: polygon_miter_pt_subseq_contact_free_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, j: upto(G`num_vertices), i: upto(j),
  --         d: posreal, S: edge_parallel_endpoint_seq):
  --   S = polygon_vertex_seq(G) AND d <= max_miter_offset(S) IMPLIES
  --    (^[point_2...
  have polygon_miter_pt_subseq_contact_free_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.polygon_miter_pt_subseq_pair_contact_free
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), j: below(i),
--         d: posreal):
--   LET S = polygon_vertex_seq(G) IN
--     d <= max_miter_offset(S) IMPLIES
--      LET M = closed_miter_point_seq(S,
theorem polygon_miter_pt_subseq_pair_contact_free : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < i}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => d ≤ max_miter_offset S → (let M := closed_miter_point_seq S d; let M1 := M ^ (i, G.num_vertices); let M2 := M ^ (0, j); contact_free_path_p G(append_segment_seqs M1 M2)) (polygon_vertex_seq G) := by
  -- === TCCs for polygon_miter_pt_subseq_pair_contact_free ===
  -- TCC: polygon_miter_pt_subseq_pair_contact_free_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), j: below(i),
  --         d: posreal, S: edge_parallel_endpoint_seq):
  --   S = polygon_vertex_seq(G) AND d <= max_miter_offset(S) IMPLIES
  --    FORALL (...
  have polygon_miter_pt_subseq_pair_contact_free_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: polygon_miter_pt_subseq_pair_contact_free_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), j: below(i),
  --         d: posreal, S: edge_parallel_endpoint_seq):
  --   S = polygon_vertex_seq(G) AND d <= max_miter_offset(S) IMPLIES
  --    FORALL (...
  have polygon_miter_pt_subseq_pair_contact_free_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.below_left_falling_seg_point_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, u, v: point_2d, s, e: segment_2d):
--        (u /= s`p1) AND (u /= s`p2) AND (e = (# p1 := u, p2 := s`p2 #))
--    AND (cross(u - s`p1, s`p2 - s`p1) > 0)
--    AND (cross(p - s`p1, s`p2 - s`p1) > 0)
theorem below_left_falling_seg_point_right : ∀ (p : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (u ≠ s.p1) ∧ (u ≠ s.p2) ∧ (e = (# p1 := u, p2 := s.p2 #))
 ∧ (cross (u - s.p1) (s.p2 - s.p1) > 0)
 ∧ (cross (p - s.p1) (s.p2 - s.p1) > 0)
 ∧ point_between_rays_p ((# p1 := u, p2 := s.p1 #)) e(p) ∧ (s.p2.y < p.y)
 ∧ (p.y < s.p1.y) ∧ (p.y < u.y) ∧ (p.y = v.y)
 ∧ point_on_segment_p v e → p.x < v.x := by
  -- === TCCs for below_left_falling_seg_point_right ===
  -- TCC: below_left_falling_seg_point_right_TCC1
  -- Statement: FORALL (p, u: point_2d, s, e: segment_2d):
  --        (u /= s`p1) AND (u /= s`p2) AND (e = (# p1 := u, p2 := s`p2 #))
  --    AND (cross(u - s`p1, s`p2 - s`p1) > 0)
  --    AND (cross(p - s`p1, s`p2 - s`p1) > 0)...
  have below_left_falling_seg_point_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.above_left_falling_seg_point_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, u, v: point_2d, s: segment_2d):
--        (u /= s`p1) AND (u /= s`p2) AND (cross(u - s`p1, s`p2 - s`p1) > 0)
--    AND (cross(p - s`p1, s`p2 - s`p1) > 0)
--    AND point_between_rays?((# p1 := u, p2
theorem above_left_falling_seg_point_right : ∀ (p : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (u ≠ s.p1) ∧ (u ≠ s.p2) ∧ (cross (u - s.p1) (s.p2 - s.p1) > 0)
 ∧ (cross (p - s.p1) (s.p2 - s.p1) > 0)
 ∧ point_between_rays_p ((# p1 := u, p2 := s.p1 #)) ((# p1 := u, p2 := s.p2 #))
                        (p)
 ∧ (s.p2.y < p.y) ∧ (p.y < s.p1.y) ∧ (p.y > u.y) ∧ (p.y = v.y)
 ∧ point_on_segment_p v ((# p1 := u, p2 := s.p1 #)) → p.x < v.x := by
  -- === TCCs for above_left_falling_seg_point_right ===
  -- TCC: above_left_falling_seg_point_right_TCC1
  -- Statement: FORALL (p, u: point_2d, s: segment_2d):
  --   u /= s`p1 AND
  --    u /= s`p2 AND
  --     cross(u - s`p1, s`p2 - s`p1) > 0 AND cross(p - s`p1, s`p2 - s`p1) > 0
  --    IMPLIES s`p1 /= u
  have above_left_falling_seg_point_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: above_left_falling_seg_point_right_TCC2
  -- Statement: FORALL (p, u: point_2d, s: segment_2d):
  --   u /= s`p1 AND
  --    u /= s`p2 AND
  --     cross(u - s`p1, s`p2 - s`p1) > 0 AND cross(p - s`p1, s`p2 - s`p1) > 0
  --    IMPLIES s`p2 /= u
  have above_left_falling_seg_point_right_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.on_seg_from_miter_pt_to_endpoint
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
--         d: posreal):
--   LET H = polygon_vertex_seq(G),
--       q = leftmost_intersection_point(G, p),
--       i = rightward_crossing_index(G,
theorem on_seg_from_miter_pt_to_endpoint : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (H : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (q : ℝ × ℝ) => fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ max_miter_offset H ∧ segment_falling_p e → (let mp := vertex_miter_point G i d; point_between_rays_p ((# p1 := mp, p2 := e.p1 #)) ((# p1 := mp, p2 := e.p2 #))
                     (p)
   ∧ q ≠ e.p1 ∧ q ≠ e.p2
   →
   ∃ (u : _) (v : point_2d), segment_endpoint_p e(u)
      ∧ point_on_segment_p v ((# p1 := p, p2 := q #))
      ∧ point_on_segment_p v ((# p1 := mp, p2 := u #)) ∧ (p.x < v.x)
      ∧ (v.x < q.x) ∧ (e.p2.y < v.y) ∧ (v.y < e.p1.y)) (edges_of_polygon G i) (rightward_crossing_index G p) (leftmost_intersection_point G p) (polygon_vertex_seq G) := by
  -- === TCCs for on_seg_from_miter_pt_to_endpoint ===
  -- TCC: on_seg_from_miter_pt_to_endpoint_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d,
  --         i: below(G`num_vertices), e: segment_2d):
  --        (H = polygon...
  have on_seg_from_miter_pt_to_endpoint_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: on_seg_from_miter_pt_to_endpoint_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d,
  --         i: below(G`num_vertices), e: segment_2d):
  --        (H = polygon...
  have on_seg_from_miter_pt_to_endpoint_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: on_seg_from_miter_pt_to_endpoint_TCC3
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d,
  --         i: below(G`num_vertices), e: segment_2d):
  --        (H = polygon...
  have on_seg_from_miter_pt_to_endpoint_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: on_seg_from_miter_pt_to_endpoint_TCC4
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d,
  --         i: below(G`num_vertices), e: segment_2d):
  --        (H = polygon...
  have on_seg_from_miter_pt_to_endpoint_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.on_seg_from_miter_pt_contact_free
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
--         d: posreal):
--   LET H = polygon_vertex_seq(G),
--       q = leftmost_intersection_point(G, p),
--       i = rightward_crossing_index(G,
theorem on_seg_from_miter_pt_contact_free : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (H : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (q : ℝ × ℝ) => fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ max_miter_offset H ∧ segment_falling_p e ∧ q ≠ e.p1 ∧ q ≠ e.p2 → (let mp := vertex_miter_point G i d; point_between_rays_p ((# p1 := mp, p2 := e.p1 #)) ((# p1 := mp, p2 := e.p2 #))
                     (p)
   →
   ∃ (S : segment_endpoint_seq), S.seq 0 = p ∧ S.seq (S.List.length - 1) = mp ∧ contact_free_path_p G(S)) (edges_of_polygon G i) (rightward_crossing_index G p) (leftmost_intersection_point G p) (polygon_vertex_seq G) := by
  -- === TCCs for on_seg_from_miter_pt_contact_free ===
  -- TCC: on_seg_from_miter_pt_contact_free_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d,
  --         i: below(G`num_vertices), e: segment_2d):
  --        (H = polygon...
  have on_seg_from_miter_pt_contact_free_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: on_seg_from_miter_pt_contact_free_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d,
  --         i: below(G`num_vertices), e: segment_2d):
  --        (H = polygon...
  have on_seg_from_miter_pt_contact_free_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.vertex_to_miter_pt_seq_contact_free
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
--         i: below(G`num_vertices), d: posreal):
--   LET H = polygon_vertex_seq(G), q = leftmost_intersection_point(G, p) IN
--     d <= max_mi
theorem vertex_to_miter_pt_seq_contact_free : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (H : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (q : ℝ × ℝ) => d ≤ max_miter_offset H ∧
 q = G.vertices i ∧
  segment_falling_p (edges_of_polygon G(i)) ∧
   i = rightward_crossing_index G p → (let mp := miter_point (G.vertices i) (G.vertices (prev_index G i)) (G.vertices (next_index G i)) d; p = mp ∨
   p.y = mp.y ∨
    ∃ (S : segment_endpoint_seq), S.seq 0 = p ∧ S.seq (S.List.length - 1) = mp ∧ contact_free_path_p G(S)) (leftmost_intersection_point G p) (polygon_vertex_seq G) := by
  -- === TCCs for vertex_to_miter_pt_seq_contact_free ===
  -- TCC: vertex_to_miter_pt_seq_contact_free_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
  --         i: below(G`num_vertices), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d):
  --        (H = polygon_vertex_seq(G))...
  have vertex_to_miter_pt_seq_contact_free_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: vertex_to_miter_pt_seq_contact_free_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
  --         i: below(G`num_vertices), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d):
  --        (H = polygon_vertex_seq(G))...
  have vertex_to_miter_pt_seq_contact_free_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.next_vtx_to_miter_pt_subcase_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, p, q, r, u, v: point_2d, k: posreal):
--        (q = (# x := 0, y := 0 #)) AND (m /= q) AND (m /= v) AND (q /= v)
--    AND (u /= q) AND (u /= r) AND (p`y = 0) AND (p`x < q`x) AND (0 < v`y)
--    AN
theorem next_vtx_to_miter_pt_subcase_zero : ∀ (m : ℝ × ℝ) (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (k : {x : {x : ℝ // x ≥ 0} // x > 0}), (q = (# x := 0, y := 0 #)) ∧ (m ≠ q) ∧ (m ≠ v) ∧ (q ≠ v)
 ∧ (u ≠ q) ∧ (u ≠ r) ∧ (p.y = 0) ∧ (p.x < q.x) ∧ (0 < v.y)
 ∧ (m.y < 0) ∧ (norm (m - v) ≤ norm v / 6)
 ∧ (k ≤ min (norm p) (norm v / 6) / 2) ∧ (u = (# x := -k, y := 0 #))
 ∧ (r = k * ^(m)) ∧ (k < norm m) ∧ (cross v m > 0) → ∀ (y : point_2d), point_on_segment_p y ((# p1 := u, p2 := r #)) →
   ∃ (z : point_2d), point_on_segment_p z ((# p1 := v, p2 := q #)) ∧
      point_on_segment_p y ((# p1 := m, p2 := z #)) ∧ y ≠ z := by
  -- === TCCs for next_vtx_to_miter_pt_subcase_zero ===
  -- TCC: next_vtx_to_miter_pt_subcase_zero_TCC1
  -- Statement: FORALL (m, p, q, r, u, v: point_2d, k: posreal):
  --        (q = (# x := 0, y := 0 #)) AND (m /= q) AND (m /= v) AND (q /= v)
  --    AND (u /= q) AND (u /= r) AND (p`y = 0) AND (p`x < q`x) AND (0 < v`y)
  --   ...
  have next_vtx_to_miter_pt_subcase_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_vtx_to_miter_pt_subcase_zero_TCC2
  -- Statement: FORALL (m, p, q, r, u, v: point_2d, k: posreal):
  --        (q = (# x := 0, y := 0 #)) AND (m /= q) AND (m /= v) AND (q /= v)
  --    AND (u /= q) AND (u /= r) AND (p`y = 0) AND (p`x < q`x) AND (0 < v`y)
  --   ...
  have next_vtx_to_miter_pt_subcase_zero_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_vtx_to_miter_pt_subcase_zero_TCC3
  -- Statement: FORALL (m, p, q, r, u, v: point_2d, k: posreal):
  --        (q = (# x := 0, y := 0 #)) AND (m /= q) AND (m /= v) AND (q /= v)
  --    AND (u /= q) AND (u /= r) AND (p`y = 0) AND (p`x < q`x) AND (0 < v`y)
  --   ...
  have next_vtx_to_miter_pt_subcase_zero_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.next_vtx_to_miter_pt_subcase
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m, p, q, r, u, v: point_2d, k: posreal):
--        (m /= q) AND (m /= v) AND (q /= v) AND (u /= q) AND (u /= r)
--    AND (p`y = q`y) AND (p`x < q`x) AND (q`y < v`y) AND (m`y < p`y)
--    AND (norm(m -
theorem next_vtx_to_miter_pt_subcase : ∀ (m : ℝ × ℝ) (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ) (u : ℝ × ℝ) (v : ℝ × ℝ) (k : {x : {x : ℝ // x ≥ 0} // x > 0}), (m ≠ q) ∧ (m ≠ v) ∧ (q ≠ v) ∧ (u ≠ q) ∧ (u ≠ r)
 ∧ (p.y = q.y) ∧ (p.x < q.x) ∧ (q.y < v.y) ∧ (m.y < p.y)
 ∧ (norm (m - v) ≤ norm (q - v) / 6)
 ∧ (k ≤ min (norm (q - p)) (norm (q - v) / 6) / 2)
 ∧ (u = q - (# x := k, y := 0 #)) ∧ (r = q + k * ^(m - q))
 ∧ (k < norm (m - q)) ∧ (cross (v - q) (m - q) > 0) → ∀ (y : point_2d), point_on_segment_p y ((# p1 := u, p2 := r #)) →
   ∃ (z : point_2d), point_on_segment_p z ((# p1 := v, p2 := q #)) ∧
      point_on_segment_p y ((# p1 := m, p2 := z #)) ∧ y ≠ z := by
  -- === TCCs for next_vtx_to_miter_pt_subcase ===
  -- TCC: next_vtx_to_miter_pt_subcase_TCC1
  -- Statement: FORALL (m, p, q, r, u, v: point_2d, k: posreal):
  --        (m /= q) AND (m /= v) AND (q /= v) AND (u /= q) AND (u /= r)
  --    AND (p`y = q`y) AND (p`x < q`x) AND (q`y < v`y) AND (m`y < p`y)
  --    AND (norm(...
  have next_vtx_to_miter_pt_subcase_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_vtx_to_miter_pt_subcase_TCC2
  -- Statement: FORALL (m, p, q, r, u, v: point_2d, k: posreal):
  --        (m /= q) AND (m /= v) AND (q /= v) AND (u /= q) AND (u /= r)
  --    AND (p`y = q`y) AND (p`x < q`x) AND (q`y < v`y) AND (m`y < p`y)
  --    AND (norm(...
  have next_vtx_to_miter_pt_subcase_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_vtx_to_miter_pt_subcase_TCC3
  -- Statement: FORALL (m, p, q, r, u, v: point_2d, k: posreal):
  --        (m /= q) AND (m /= v) AND (q /= v) AND (u /= q) AND (u /= r)
  --    AND (p`y = q`y) AND (p`x < q`x) AND (q`y < v`y) AND (m`y < p`y)
  --    AND (norm(...
  have next_vtx_to_miter_pt_subcase_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.next_vtx_to_miter_pt_seq_contact_free
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
--         i: below(G`num_vertices), d: posreal):
--   LET H = polygon_vertex_seq(G), q = leftmost_intersection_point(G, p) IN
--     d <= max_mi
theorem next_vtx_to_miter_pt_seq_contact_free : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (H : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (q : ℝ × ℝ) => d ≤ max_miter_offset H ∧
 q = G.vertices (next_index G i) ∧
  segment_falling_p (edges_of_polygon G(i)) ∧
   i = rightward_crossing_index G p → (let mp := vertex_miter_point G i d; p = mp ∨
   p.y = mp.y ∨
    ∃ (S : segment_endpoint_seq), S.seq 0 = p ∧ S.seq (S.List.length - 1) = mp ∧ contact_free_path_p G(S)) (leftmost_intersection_point G p) (polygon_vertex_seq G) := by
  -- === TCCs for next_vtx_to_miter_pt_seq_contact_free ===
  -- TCC: next_vtx_to_miter_pt_seq_contact_free_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
  --         i: below(G`num_vertices), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d):
  --        (H = polygon_vertex_seq(G))...
  have next_vtx_to_miter_pt_seq_contact_free_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: next_vtx_to_miter_pt_seq_contact_free_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
  --         i: below(G`num_vertices), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d):
  --        (H = polygon_vertex_seq(G))...
  have next_vtx_to_miter_pt_seq_contact_free_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.nonvertex_to_miter_pt_seq_contact_free
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
--         d: posreal):
--   LET H = polygon_vertex_seq(G),
--       q = leftmost_intersection_point(G, p),
--       i = rightward_crossing_index(G,
theorem nonvertex_to_miter_pt_seq_contact_free : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (H : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (q : ℝ × ℝ) => fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ max_miter_offset H ∧ segment_falling_p e ∧ point_on_segment_p q e → (let mp := vertex_miter_point G i d; p = mp ∨
   q = G.vertices i ∨
    q = G.vertices (next_index G i) ∨
     ∃ (S : segment_endpoint_seq), S.seq 0 = p ∧
        S.seq (S.List.length - 1) = mp ∧ contact_free_path_p G(S)) (edges_of_polygon G i) (rightward_crossing_index G p) (leftmost_intersection_point G p) (polygon_vertex_seq G) := by
  -- === TCCs for nonvertex_to_miter_pt_seq_contact_free ===
  -- TCC: nonvertex_to_miter_pt_seq_contact_free_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d,
  --         i: below(G`num_vertices), e: segment_2d):
  --        (H = polygon...
  have nonvertex_to_miter_pt_seq_contact_free_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: nonvertex_to_miter_pt_seq_contact_free_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         H: edge_parallel_endpoint_seq, q: point_2d,
  --         i: below(G`num_vertices), e: segment_2d):
  --        (H = polygon...
  have nonvertex_to_miter_pt_seq_contact_free_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.rightward_crossing_falling_edge_cases
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
--         i: below(G`num_vertices)):
--   LET s = edges_of_polygon(G)(prev_index(G, i)),
--       e = edges_of_polygon(G)(i),
--       u = s`p1,
--   
theorem rightward_crossing_falling_edge_cases : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (u : ℝ × ℝ) => fun (v : ℝ × ℝ) => fun (w : {p : ℝ × ℝ // p ≠ e.p1}) => v = leftmost_intersection_point G p ∧
 segment_falling_p e ∧ rightward_crossing_index G p = i → u.y = v.y ∨ u.y < v.y ∧ cross (u - v) (w - v) < 0 e.p2 e.p1 s.p1 (edges_of_polygon G i) (edges_of_polygon G (prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_miter_sequences.path_to_miter_pt_seq_contact_free
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)),
--         d: posreal):
--   LET i = rightward_crossing_index(G, p), H = polygon_vertex_seq(G) IN
--     d <= max_miter_offset(H) AND segment_fal
theorem path_to_miter_pt_seq_contact_free : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => fun (H : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => d ≤ max_miter_offset H ∧ segment_falling_p (edges_of_polygon G(i)) → (let mp := miter_point (G.vertices i) (G.vertices (prev_index G i)) (G.vertices (next_index G i)) d; ∃ (S : segment_endpoint_seq), S.seq 0 = p ∧ S.seq (S.List.length - 1) = mp ∧ contact_free_path_p G(S)) (polygon_vertex_seq G) (rightward_crossing_index G p) := by
  -- === TCCs for path_to_miter_pt_seq_contact_free ===
  -- TCC: path_to_miter_pt_seq_contact_free_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         i: below(G`num_vertices), H: edge_parallel_endpoint_seq):
  --   i = rightward_crossing_index(G, p) AND
  --    H = polygon...
  have path_to_miter_pt_seq_contact_free_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_to_miter_pt_seq_contact_free_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G)), d: posreal,
  --         i: below(G`num_vertices), H: edge_parallel_endpoint_seq):
  --   i = rightward_crossing_index(G, p) AND
  --    H = polygon...
  have path_to_miter_pt_seq_contact_free_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.polygon_miter_sequences