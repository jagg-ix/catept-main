-- Auto-generated by pvs2lean/translator.py
-- PVS theory: polygon_membership
-- PVSLib library: polygons
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: polygon_membership

Auto-generated stubs from PVSLib theory `polygon_membership`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.polygon_membership

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (all_arb_intersections : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (arb_crossing_set : pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (below : pvs_τ12 → pvs_τ13)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (card : pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} (ccw_vertex_order_p : pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (closed_miter_point_seq : pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (counted_crossing_direction_p : pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (dir_parallel_p : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} (edge_of_polygon_p : pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (edges_of_polygon : pvs_τ26 → pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (find_index : pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (find_index_any : pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (has_rightward_crossings_p : pvs_τ32 → pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} (in_out_edge_offset_dist : pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} (inward_edge_offset_dist : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (key_arb_intersections : pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} (leftmost_vertex_val : pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (leftward_segment_offset : pvs_τ41 → pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} (max_miter_offset : pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (miter_point : pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} (mod : pvs_τ50 → pvs_τ51)
variable {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} (next_index : pvs_τ52 → pvs_τ53 → pvs_τ54)
variable {pvs_τ55 : Type*} (norm : pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} (odd_p : pvs_τ56 → Prop)
variable {pvs_τ57 : Type*} (outward_edge_offset_dist : pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} (parallel_intersections : pvs_τ58 → pvs_τ59 → pvs_τ60 → pvs_τ61)
variable {pvs_τ65 : Type*} (perp_left : pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} (perp_right : pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} (point_in_polygon_arb_p : pvs_τ67 → pvs_τ68 → pvs_τ69 → Prop)
variable {pvs_τ70 : Type*} {pvs_τ71 : Type*} (point_in_polygon_p : pvs_τ70 → pvs_τ71 → Prop)
variable {pvs_τ72 : Type*} {pvs_τ73 : Type*} {pvs_τ74 : Type*} (point_on_arb_ray_p : pvs_τ72 → pvs_τ73 → pvs_τ74 → Prop)
variable {pvs_τ75 : Type*} {pvs_τ76 : Type*} (point_on_polygon_perimeter_p : pvs_τ75 → pvs_τ76 → Prop)
variable {pvs_τ77 : Type*} {pvs_τ78 : Type*} (point_on_segment_p : pvs_τ77 → pvs_τ78 → Prop)
variable {pvs_τ79 : Type*} {pvs_τ80 : Type*} (point_on_segment_seq_p : pvs_τ79 → pvs_τ80 → Prop)
variable {pvs_τ85 : Type*} {pvs_τ86 : Type*} (polygon_contained_p : pvs_τ85 → pvs_τ86 → Prop)
variable {pvs_τ87 : Type*} (polygon_vertex_seq : pvs_τ87 → Prop)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} {pvs_τ90 : Type*} (prev_index : pvs_τ88 → pvs_τ89 → pvs_τ90)
variable {pvs_τ91 : Type*} {pvs_τ92 : Type*} (rem : pvs_τ91 → pvs_τ92 → Prop)
variable {pvs_τ93 : Type*} {pvs_τ94 : Type*} (reverse_polygon : pvs_τ93 → pvs_τ94)
variable {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} (rightward_crossing_index : pvs_τ95 → pvs_τ96 → pvs_τ97)
variable {pvs_τ98 : Type*} {pvs_τ99 : Type*} {pvs_τ100 : Type*} (rightward_segment_offset : pvs_τ98 → pvs_τ99 → pvs_τ100)
variable {pvs_τ101 : Type*} (segment_falling_p : pvs_τ101 → Prop)
variable {pvs_τ102 : Type*} (segment_midpoint : pvs_τ102 → Prop)
variable {pvs_τ107 : Type*} (seq : pvs_τ107 → Prop)
variable {pvs_τ110 : Type*} (simple_polygon_2d_p : pvs_τ110 → Prop)
variable {pvs_τ111 : Type*} (topleft_vertex_idx : pvs_τ111 → Prop)
variable {pvs_τ112 : Type*} {pvs_τ113 : Type*} (topleft_vertex_val : pvs_τ112 → pvs_τ113)
variable {pvs_τ117 : Type*} {pvs_τ118 : Type*} (upfrom : pvs_τ117 → pvs_τ118)
variable {pvs_τ119 : Type*} (vertex_p : pvs_τ119 → Prop)
variable {pvs_τ120 : Type*} (vertices : pvs_τ120 → Prop)

-- pvs2lean: polygon_membership.serp_ray_outside?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: below(S`body`length - 1)), (p: point_2d):
--    point_on_segment?(p, (# p1 := S`body`seq(i), p2 := S`body`seq(i + 1) #))
--     IMPLIES NOT point_on_polygon_perimeter?(G)(p))
--  AND
--  (LET origin =
axiom serp_ray_outside_p {pvs_τ108 : Type*} {pvs_τ109 : Type*} : pvs_τ108 → pvs_τ109 → Prop

-- pvs2lean: polygon_membership.point_outside_polygon?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (S: serpentine_ray): serp_ray_outside?(S, G) AND p = S`body`seq(0)
axiom point_outside_polygon_p {pvs_τ83 : Type*} {pvs_τ84 : Type*} : pvs_τ83 → pvs_τ84 → Prop

-- pvs2lean: polygon_membership.perimeter_not_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   NOT point_on_polygon_perimeter?(G)(p) OR NOT point_outside_polygon?(p, G)
theorem perimeter_not_outside : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ point_on_polygon_perimeter_p G(p) ∨ ¬ point_outside_polygon_p p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_on_serp_ray?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (i: below(S`body`length - 1)):
--    point_on_segment?(p,
--                      (# p1 := S`body`seq(i), p2 := S`body`seq(i + 1) #)))
--  OR point_on_arb_ray?(p, S`body`seq(S`body`length - 1), S`tail)
axiom point_on_serp_ray_p {pvs_τ81 : Type*} {pvs_τ82 : Type*} : pvs_τ81 → pvs_τ82 → Prop

-- pvs2lean: polygon_membership.downstream_point_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, S: serpentine_ray):
--   serp_ray_outside?(S, G) IMPLIES
--    FORALL (q: point_2d):
--      point_on_serp_ray?(q, S) IMPLIES point_outside_polygon?(q, G)
theorem downstream_point_outside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (S : {S : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) × ({v : ℝ × ℝ // norm v = 1}) // S.body.List.length > 0 ∧
 ∀ (i : Finset.range S.body.List.length - 1), ¬ S.body.seq i = S.body.seq (1 + i)}), serp_ray_outside_p S G → ∀ (q : point_2d), point_on_serp_ray_p q S → point_outside_polygon_p q G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.pt_inside_imp_inside_rev_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: point_2d):
--   point_in_polygon?(p, G) IMPLIES point_in_polygon?(p, reverse_polygon(G))
theorem pt_inside_imp_inside_rev_polygon : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), point_in_polygon_p p G → point_in_polygon_p p (reverse_polygon G) := by
  -- === TCCs for pt_inside_imp_inside_rev_polygon ===
  -- TCC: pt_inside_imp_inside_rev_polygon_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p: point_2d):
  --   point_in_polygon?(p, G) IMPLIES simple_polygon_2d?(reverse_polygon(G))
  have pt_inside_imp_inside_rev_polygon_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.pt_inside_eqv_inside_rev_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: point_2d):
--   point_in_polygon?(p, G) IFF point_in_polygon?(p, reverse_polygon(G))
theorem pt_inside_eqv_inside_rev_polygon : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), point_in_polygon_p p G ↔ point_in_polygon_p p (reverse_polygon G) := by
  -- === TCCs for pt_inside_eqv_inside_rev_polygon ===
  -- TCC: pt_inside_eqv_inside_rev_polygon_TCC1
  -- Statement: FORALL (G: simple_polygon_2d): simple_polygon_2d?(reverse_polygon(G))
  have pt_inside_eqv_inside_rev_polygon_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.pt_outside_eqv_outside_rev_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: point_2d):
--   point_outside_polygon?(p, G) IFF
--    point_outside_polygon?(p, reverse_polygon(G))
theorem pt_outside_eqv_outside_rev_polygon : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), point_outside_polygon_p p G ↔ point_outside_polygon_p p (reverse_polygon G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.rightward_nearby_points_outside?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET e = edges_of_polygon(G)(i) IN
--   FORALL (d: posreal):
--     d <= outward_edge_offset_dist(G) IMPLIES
--      point_outside_polygon?(rightward_segment_offset(e, d), G)
noncomputable def rightward_nearby_points_outside_p : sorry := by
  sorry  -- pvs2lean: definition stub for LET e = edges_of_polygon(G)(i) IN   FORALL (d: posreal):     d <= outward_edge_o

-- pvs2lean: polygon_membership.leftward_nearby_points_inside?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET e = edges_of_polygon(G)(i) IN
--   FORALL (d: posreal):
--     d <= inward_edge_offset_dist(G) IMPLIES
--      point_in_polygon?(leftward_segment_offset(e, d), G)
noncomputable def leftward_nearby_points_inside_p : sorry := by
  sorry  -- pvs2lean: definition stub for LET e = edges_of_polygon(G)(i) IN   FORALL (d: posreal):     d <= inward_edge_of

-- pvs2lean: polygon_membership.prev_edge_not_crossed_by_perp_left_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, e: segment_2d, i: below(G`num_vertices),
--         p: point_2d, v: Normalized, d: posreal):
--   e = edges_of_polygon(G)(i) AND
--    d <= 1 AND
--     v = ^(perp_left(e`p2 - e`p1))
theorem prev_edge_not_crossed_by_perp_left_segment : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), e = edges_of_polygon G(i) ∧
 d ≤ 1 ∧
  v = ^(perp_left (e.p2 - e.p1)) ∧
   p = segment_midpoint e + d * outward_edge_offset_dist G * v → ¬ point_on_segment_p p (edges_of_polygon G(prev_index G i)) := by
  -- === TCCs for prev_edge_not_crossed_by_perp_left_segment ===
  -- TCC: prev_edge_not_crossed_by_perp_left_segment_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, e: segment_2d, i: below(G`num_vertices),
  --         v: Normalized, d: posreal):
  --   e = edges_of_polygon(G)(i) AND d <= 1 IMPLIES
  --    perp_left(e`p2 - e`p1) /= zero
  have prev_edge_not_crossed_by_perp_left_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.prev_edge_not_crossed_by_perp_right_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, e: segment_2d, i: below(G`num_vertices),
--         p: point_2d, v: Normalized, d: posreal):
--   e = edges_of_polygon(G)(i) AND
--    d <= 1 AND
--     v = ^(perp_right(e`p2 - e`p1)
theorem prev_edge_not_crossed_by_perp_right_segment : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), e = edges_of_polygon G(i) ∧
 d ≤ 1 ∧
  v = ^(perp_right (e.p2 - e.p1)) ∧
   p = segment_midpoint e + d * inward_edge_offset_dist G * v → ¬ point_on_segment_p p (edges_of_polygon G(prev_index G i)) := by
  -- === TCCs for prev_edge_not_crossed_by_perp_right_segment ===
  -- TCC: prev_edge_not_crossed_by_perp_right_segment_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, e: segment_2d, i: below(G`num_vertices),
  --         v: Normalized, d: posreal):
  --   e = edges_of_polygon(G)(i) AND d <= 1 IMPLIES
  --    perp_right(e`p2 - e`p1) /= zero
  have prev_edge_not_crossed_by_perp_right_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.next_edge_not_crossed_by_perp_left_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, e: segment_2d, i: below(G`num_vertices),
--         p: point_2d, v: Normalized, d: posreal):
--   e = edges_of_polygon(G)(i) AND
--    d <= 1 AND
--     v = ^(perp_left(e`p1 - e`p2))
theorem next_edge_not_crossed_by_perp_left_segment : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), e = edges_of_polygon G(i) ∧
 d ≤ 1 ∧
  v = ^(perp_left (e.p1 - e.p2)) ∧
   p = segment_midpoint e + d * inward_edge_offset_dist G * v → ¬ point_on_segment_p p (edges_of_polygon G(next_index G i)) := by
  -- === TCCs for next_edge_not_crossed_by_perp_left_segment ===
  -- TCC: next_edge_not_crossed_by_perp_left_segment_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, e: segment_2d, i: below(G`num_vertices),
  --         v: Normalized, d: posreal):
  --   e = edges_of_polygon(G)(i) AND d <= 1 IMPLIES
  --    perp_left(e`p1 - e`p2) /= zero
  have next_edge_not_crossed_by_perp_left_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.next_edge_not_crossed_by_perp_right_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, e: segment_2d, i: below(G`num_vertices),
--         p: point_2d, v: Normalized, d: posreal):
--   e = edges_of_polygon(G)(i) AND
--    d <= 1 AND
--     v = ^(perp_right(e`p1 - e`p2)
theorem next_edge_not_crossed_by_perp_right_segment : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), e = edges_of_polygon G(i) ∧
 d ≤ 1 ∧
  v = ^(perp_right (e.p1 - e.p2)) ∧
   p = segment_midpoint e + d * outward_edge_offset_dist G * v → ¬ point_on_segment_p p (edges_of_polygon G(next_index G i)) := by
  -- === TCCs for next_edge_not_crossed_by_perp_right_segment ===
  -- TCC: next_edge_not_crossed_by_perp_right_segment_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, e: segment_2d, i: below(G`num_vertices),
  --         v: Normalized, d: posreal):
  --   e = edges_of_polygon(G)(i) AND d <= 1 IMPLIES
  --    perp_right(e`p1 - e`p2) /= zero
  have next_edge_not_crossed_by_perp_right_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.nearby_perp_right_segment_not_on_perim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET e = edges_of_polygon(G)(i),
--       c = inward_edge_offset_dist(G),
--       m = segment_midpoint(e),
--       q = m + d * c * ^(perp
theorem nearby_perp_right_segment_not_on_perim : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (c : {x : {x : ℝ // x ≥ 0} // x > 0}) => fun (m : ℝ × ℝ) => fun (q : ℝ × ℝ) => d ≤ 1 → ¬ point_on_polygon_perimeter_p G(q) (m + d * c * ^(perp_right (e.p2 - e.p1))) (segment_midpoint e) (inward_edge_offset_dist G) (edges_of_polygon G i) := by
  -- === TCCs for nearby_perp_right_segment_not_on_perim ===
  -- TCC: nearby_perp_right_segment_not_on_perim_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), e: segment_2d,
  --         c: posreal, m: point_2d):
  --   e = edges_of_polygon(G)(i) AND
  --    c = inward_edge_offset_dist(G) AND m = segment_midpoint(...
  have nearby_perp_right_segment_not_on_perim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.nearby_perp_left_segment_not_on_perim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET e = edges_of_polygon(G)(i),
--       c = outward_edge_offset_dist(G),
--       m = segment_midpoint(e),
--       p = m + d * c * ^(per
theorem nearby_perp_left_segment_not_on_perim : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (c : {x : {x : ℝ // x ≥ 0} // x > 0}) => fun (m : ℝ × ℝ) => fun (p : ℝ × ℝ) => d ≤ 1 → ¬ point_on_polygon_perimeter_p G(p) (m + d * c * ^(perp_left (e.p2 - e.p1))) (segment_midpoint e) (outward_edge_offset_dist G) (edges_of_polygon G i) := by
  -- === TCCs for nearby_perp_left_segment_not_on_perim ===
  -- TCC: nearby_perp_left_segment_not_on_perim_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), e: segment_2d,
  --         c: posreal, m: point_2d):
  --   e = edges_of_polygon(G)(i) AND
  --    c = outward_edge_offset_dist(G) AND m = segment_midpoint...
  have nearby_perp_left_segment_not_on_perim_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.rightward_offset_pt_not_on_perim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET e = edges_of_polygon(G)(i) IN
--     d <= in_out_edge_offset_dist(G) IMPLIES
--      NOT point_on_polygon_perimeter?(G)(rightward_s
theorem rightward_offset_pt_not_on_perim : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ in_out_edge_offset_dist G → ¬ point_on_polygon_perimeter_p G(rightward_segment_offset e d) (edges_of_polygon G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.leftward_offset_pt_not_on_perim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET e = edges_of_polygon(G)(i) IN
--     d <= in_out_edge_offset_dist(G) IMPLIES
--      NOT point_on_polygon_perimeter?(G)(leftward_se
theorem leftward_offset_pt_not_on_perim : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ in_out_edge_offset_dist G → ¬ point_on_polygon_perimeter_p G(leftward_segment_offset e d) (edges_of_polygon G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.segment_seq_outside?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d):
--   point_on_segment_seq?(p, S) IMPLIES point_outside_polygon?(p, G)
axiom segment_seq_outside_p {pvs_τ105 : Type*} {pvs_τ106 : Type*} : pvs_τ105 → pvs_τ106 → Prop

-- pvs2lean: polygon_membership.segment_seq_inside?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d):
--   point_on_segment_seq?(p, S) IMPLIES point_in_polygon?(p, G)
axiom segment_seq_inside_p {pvs_τ103 : Type*} {pvs_τ104 : Type*} : pvs_τ103 → pvs_τ104 → Prop

-- pvs2lean: polygon_membership.on_perimeter_edge_path?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: u = v AND p = u OR
--  LET i = find_index(G, u),
--      j = find_index(G, v),
--      n = G`num_vertices,
--      len = rem(n)(n + j - i)
--    IN
--    EXISTS (k: below(len)):
--      point_on_segment?(p, edges_of_polyg
noncomputable def on_perimeter_edge_path_p : sorry := by
  sorry  -- pvs2lean: definition stub for u = v AND p = u OR  LET i = find_index(G, u),      j = find_index(G, v),      n 

-- pvs2lean: polygon_membership.on_perimeter_vertex_path?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: u = v AND w = u OR
--  LET i = find_index(G, u),
--      j = find_index(G, v),
--      n = G`num_vertices,
--      len = 1 + rem(n)(n + j - i)
--    IN EXISTS (k: below(len)): w = G`vertices(rem(n)(i + k))
noncomputable def on_perimeter_vertex_path_p : sorry := by
  sorry  -- pvs2lean: definition stub for u = v AND w = u OR  LET i = find_index(G, u),      j = find_index(G, v),      n 

-- pvs2lean: polygon_membership.perimeter_segment_path
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET i = find_index_any(G, p),
--     j = find_index_any(G, q),
--     len =
--       IF i = j AND norm(p - G`vertices(i)) > norm(q - G`vertices(i))
--         THEN G`num_vertices
--       ELSE rem(G`num_vertices)(j 
axiom perimeter_segment_path {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} : pvs_τ62 → pvs_τ63 → pvs_τ64 → Prop

-- pvs2lean: polygon_membership.initial_point_perimeter_segment_path
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(G))):
--   perimeter_segment_path(G, p, q)`seq(0) = p
theorem initial_point_perimeter_segment_path : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), perimeter_segment_path G p q.seq 0 = p := by
  -- === TCCs for initial_point_perimeter_segment_path ===
  -- TCC: initial_point_perimeter_segment_path_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(G))):
  --   0 < perimeter_segment_path(G, p, q)`length
  have initial_point_perimeter_segment_path_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.final_point_perimeter_segment_path
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(G))):
--   LET S = perimeter_segment_path(G, p, q) IN S`seq(S`length - 1) = q
theorem final_point_perimeter_segment_path : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), fun (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) => S.seq (S.List.length - 1) = q (perimeter_segment_path G p q) := by
  -- === TCCs for final_point_perimeter_segment_path ===
  -- TCC: final_point_perimeter_segment_path_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(G)),
  --         S: segment_endpoint_seq):
  --   S = perimeter_segment_path(G, p, q) IMPLIES
  --    S`length - 1 >= 0 AND S`length - 1 < S`length
  have final_point_perimeter_segment_path_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.perim_segment_path_on_perim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p, q: (point_on_polygon_perimeter?(G))):
--   FORALL (u: point_2d):
--     point_on_segment_seq?(u, perimeter_segment_path(G, p, q)) IMPLIES
--      point_on_polygon_perimeter?(G)
theorem perim_segment_path_on_perim : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (q : ℝ × ℝ), ∀ (u : ℝ × ℝ), point_on_segment_seq_p u (perimeter_segment_path G p q) → point_on_polygon_perimeter_p G(u) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.append_path_to_s_ray
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# body := R ^^ (0, R`length - 1) o S`body, tail := S`tail #)
axiom append_path_to_s_ray {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7

-- pvs2lean: polygon_membership.truncated_serp_ray
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET singleton = (# length := 1, seq := LAMBDA (i: below(1)): p #) IN
--   IF EXISTS (d: posreal): p = S`body`seq(S`body`length - 1) + d * S`tail
--     THEN S`body o singleton
--   ELSIF S`body`length = 1 THEN
axiom truncated_serp_ray {pvs_τ114 : Type*} {pvs_τ115 : Type*} {pvs_τ116 : Type*} : pvs_τ114 → pvs_τ115 → pvs_τ116

-- pvs2lean: polygon_membership.initial_point_append_path_to_s_ray
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (R: segment_endpoint_seq,
--         S: {S: serpentine_ray | R`seq(R`length - 1) = S`body`seq(0)}):
--   append_path_to_s_ray(R, S)`body`seq(0) = R`seq(0)
theorem initial_point_append_path_to_s_ray : ∀ (R : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (S : {S : {S : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) × ({v : ℝ × ℝ // norm v = 1}) // S.body.List.length > 0 ∧
 ∀ (i : Finset.range S.body.List.length - 1), ¬ S.body.seq i = S.body.seq (1 + i)} // R.seq (R.List.length - 1) = S.body.seq 0}), append_path_to_s_ray R S.body.seq 0 = R.seq 0 := by
  -- === TCCs for initial_point_append_path_to_s_ray ===
  -- TCC: initial_point_append_path_to_s_ray_TCC1
  -- Statement: FORALL (R: segment_endpoint_seq,
  --         S: {S: serpentine_ray | R`seq(R`length - 1) = S`body`seq(0)}):
  --   0 < append_path_to_s_ray(R, S)`body`length
  have initial_point_append_path_to_s_ray_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: initial_point_append_path_to_s_ray_TCC2
  -- Statement: FORALL (R: segment_endpoint_seq,
  --         S: {S: serpentine_ray | R`seq(R`length - 1) = S`body`seq(0)}):
  --   0 < R`length
  have initial_point_append_path_to_s_ray_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.append_outside_seq_to_s_ray
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, R: segment_endpoint_seq,
--         S: {S: serpentine_ray | R`seq(R`length - 1) = S`body`seq(0)}):
--   (FORALL (p: point_2d):
--      point_on_segment_seq?(p, R) IMPLIES
--       NO
theorem append_outside_seq_to_s_ray : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (R : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (S : {S : {S : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) × ({v : ℝ × ℝ // norm v = 1}) // S.body.List.length > 0 ∧
 ∀ (i : Finset.range S.body.List.length - 1), ¬ S.body.seq i = S.body.seq (1 + i)} // R.seq (R.List.length - 1) = S.body.seq 0}), (∀ (p : point_2d), point_on_segment_seq_p p R →
    ¬ point_on_polygon_perimeter_p G(p))
 ∧ serp_ray_outside_p S G → serp_ray_outside_p (append_path_to_s_ray R S) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.initial_point_of_truncated_s_ray
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: serpentine_ray, p: {p: point_2d | point_on_serp_ray?(p, S)}):
--   truncated_serp_ray(S, p)`seq(0) = S`body`seq(0)
theorem initial_point_of_truncated_s_ray : ∀ (S : {S : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) × ({v : ℝ × ℝ // norm v = 1}) // S.body.List.length > 0 ∧
 ∀ (i : Finset.range S.body.List.length - 1), ¬ S.body.seq i = S.body.seq (1 + i)}) (p : {p : ℝ × ℝ // point_on_serp_ray_p p S}), truncated_serp_ray S p.seq 0 = S.body.seq 0 := by
  -- === TCCs for initial_point_of_truncated_s_ray ===
  -- TCC: initial_point_of_truncated_s_ray_TCC1
  -- Statement: FORALL (S: serpentine_ray, p: {p: point_2d | point_on_serp_ray?(p, S)}):
  --   0 < truncated_serp_ray(S, p)`length
  have initial_point_of_truncated_s_ray_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_on_truncated_s_ray_on_s_ray
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (S: serpentine_ray, p: {p: point_2d | point_on_serp_ray?(p, S)}):
--   FORALL (q: point_2d):
--     point_on_segment_seq?(q, truncated_serp_ray(S, p)) IMPLIES
--      point_on_serp_ray?(q, S)
theorem point_on_truncated_s_ray_on_s_ray : ∀ (S : {S : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) × ({v : ℝ × ℝ // norm v = 1}) // S.body.List.length > 0 ∧
 ∀ (i : Finset.range S.body.List.length - 1), ¬ S.body.seq i = S.body.seq (1 + i)}) (p : {p : ℝ × ℝ // point_on_serp_ray_p p S}), ∀ (q : ℝ × ℝ), point_on_segment_seq_p q (truncated_serp_ray S p) → point_on_serp_ray_p q S := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.truncated_s_ray_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, S: serpentine_ray,
--         p: {p: point_2d | point_on_serp_ray?(p, S)}):
--   serp_ray_outside?(S, G) IMPLIES
--    segment_seq_outside?(truncated_serp_ray(S, p), G)
theorem truncated_s_ray_outside : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (S : {S : ({i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ)) × ({v : ℝ × ℝ // norm v = 1}) // S.body.List.length > 0 ∧
 ∀ (i : Finset.range S.body.List.length - 1), ¬ S.body.seq i = S.body.seq (1 + i)}) (p : {p : ℝ × ℝ // point_on_serp_ray_p p S}), serp_ray_outside_p S G → segment_seq_outside_p (truncated_serp_ray S p) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_in_polygon_conds_equiv_horiz
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   point_in_polygon?(p, G) IFF
--    point_in_polygon_arb?(p, (# x := 1, y := 0 #), G)
theorem point_in_polygon_conds_equiv_horiz : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_in_polygon_p p G ↔ point_in_polygon_arb_p p ((# x := 1, y := 0 #)) G := by
  -- === TCCs for point_in_polygon_conds_equiv_horiz ===
  -- TCC: point_in_polygon_conds_equiv_horiz_TCC1
  -- Statement: norm((# x := 1, y := 0 #)) = 1
  have point_in_polygon_conds_equiv_horiz_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_in_polygon_conds_equiv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   point_in_polygon?(p, G) IFF point_in_polygon_arb?(p, v, G)
theorem point_in_polygon_conds_equiv : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_in_polygon_p p G ↔ point_in_polygon_arb_p p v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.extend_points_inside_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d, G: simple_polygon_2d):
--   point_in_polygon?(p, G) AND
--    p = e`p1 AND
--     (FORALL (q: point_2d):
--        point_on_segment?(q, e) IMPLIES
--         NOT point_on_polygon_
theorem extend_points_inside_polygon : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_in_polygon_p p G ∧
 p = e.p1 ∧
  (∀ (q : point_2d), point_on_segment_p q e → ¬ point_on_polygon_perimeter_p G(q)) → ∀ (q : point_2d), point_on_segment_p q e → point_in_polygon_p q G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.extend_points_inside_polygon_excl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d, G: simple_polygon_2d):
--   point_in_polygon?(p, G) AND
--    p = e`p1 AND
--     (FORALL (q: point_2d):
--        point_on_segment?(q, e) AND q /= e`p2 IMPLIES
--         NOT poi
theorem extend_points_inside_polygon_excl : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_in_polygon_p p G ∧
 p = e.p1 ∧
  (∀ (q : point_2d), point_on_segment_p q e ∧ q ≠ e.p2 →
      ¬ point_on_polygon_perimeter_p G(q)) → ∀ (q : point_2d), point_on_segment_p q e ∧ q ≠ e.p2 → point_in_polygon_p q G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.extend_points_outside_polygon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d, G: simple_polygon_2d):
--   point_outside_polygon?(p, G) AND
--    p = e`p1 AND
--     (FORALL (q: point_2d):
--        point_on_segment?(q, e) IMPLIES
--         NOT point_on_pol
theorem extend_points_outside_polygon : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_outside_polygon_p p G ∧
 p = e.p1 ∧
  (∀ (q : point_2d), point_on_segment_p q e → ¬ point_on_polygon_perimeter_p G(q)) → ∀ (q : point_2d), point_on_segment_p q e → point_outside_polygon_p q G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.extend_points_outside_polygon_excl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, e: segment_2d, G: simple_polygon_2d):
--   point_outside_polygon?(p, G) AND
--    p = e`p1 AND
--     (FORALL (q: point_2d):
--        point_on_segment?(q, e) AND q /= e`p2 IMPLIES
--         NO
theorem extend_points_outside_polygon_excl : ∀ (p : ℝ × ℝ) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_outside_polygon_p p G ∧
 p = e.p1 ∧
  (∀ (q : point_2d), point_on_segment_p q e ∧ q ≠ e.p2 →
      ¬ point_on_polygon_perimeter_p G(q)) → ∀ (q : point_2d), point_on_segment_p q e ∧ q ≠ e.p2 → point_outside_polygon_p q G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_out_pts_on_non_perim_path_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d, S: segment_endpoint_seq):
--   point_outside_polygon?(p, G) AND
--    p = S`seq(0) AND
--     (FORALL (q: point_2d):
--        point_on_segment_seq?(q, S) IMPLIES
--       
theorem point_out_pts_on_non_perim_path_out : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}), point_outside_polygon_p p G ∧
 p = S.seq 0 ∧
  (∀ (q : point_2d), point_on_segment_seq_p q S →
      ¬ point_on_polygon_perimeter_p G(q)) → segment_seq_outside_p S G := by
  -- === TCCs for point_out_pts_on_non_perim_path_out ===
  -- TCC: point_out_pts_on_non_perim_path_out_TCC1
  -- Statement: FORALL (p: point_2d, G: simple_polygon_2d, S: segment_endpoint_seq):
  --   point_outside_polygon?(p, G) IMPLIES 0 < S`length
  have point_out_pts_on_non_perim_path_out_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_in_pts_on_non_perim_path_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d, S: segment_endpoint_seq,
--         n: posnat):
--   point_in_polygon?(p, G) AND
--    p = S`seq(0) AND
--     S`length <= n AND
--      (FORALL (q: point_2d):
--         poin
theorem point_in_pts_on_non_perim_path_in : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}), point_in_polygon_p p G ∧
 p = S.seq 0 ∧
  S.List.length ≤ n ∧
   (∀ (q : point_2d), point_on_segment_seq_p q S →
       ¬ point_on_polygon_perimeter_p G(q)) → segment_seq_inside_p S G := by
  -- === TCCs for point_in_pts_on_non_perim_path_in ===
  -- TCC: point_in_pts_on_non_perim_path_in_TCC1
  -- Statement: FORALL (p: point_2d, G: simple_polygon_2d, S: segment_endpoint_seq):
  --   point_in_polygon?(p, G) IMPLIES 0 < S`length
  have point_in_pts_on_non_perim_path_in_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.topleft_miter_point_in_or_out
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, d: posreal):
--   LET S = polygon_vertex_seq(G),
--       i = topleft_vertex_idx(G),
--       mp =
--         miter_point(G`vertices(i), G`vertices(prev_index(G, i)),
--                
theorem topleft_miter_point_in_or_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => fun (i : {i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices} // G.vertices i.x = leftmost_vertex_val G ∧
 G.vertices i.y = topleft_vertex_val G}) => fun (mp : ℝ × ℝ) => d ≤ max_miter_offset S → if ccw_vertex_order_p G then point_outside_polygon_p mp G
else point_in_polygon_p mp G (miter_point (G.vertices i) (G.vertices (prev_index G i)) (G.vertices (next_index G i)) d) (topleft_vertex_idx G) (polygon_vertex_seq G) := by
  -- === TCCs for topleft_miter_point_in_or_out ===
  -- TCC: topleft_miter_point_in_or_out_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, S: edge_parallel_endpoint_seq,
  --         i:
  --           {i: below(G`num_vertices) |
  --              G`vertices(i)`x = leftmost_vertex_val(G) AND
  --               G`vertices(i)`y...
  have topleft_miter_point_in_or_out_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: topleft_miter_point_in_or_out_TCC2
  -- Statement: FORALL (G: simple_polygon_2d, S: edge_parallel_endpoint_seq,
  --         i:
  --           {i: below(G`num_vertices) |
  --              G`vertices(i)`x = leftmost_vertex_val(G) AND
  --               G`vertices(i)`y...
  have topleft_miter_point_in_or_out_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.polygon_miter_sequence_in_or_out
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, d: posreal):
--   LET S = polygon_vertex_seq(G) IN
--     d <= max_miter_offset(S) IMPLIES
--      LET MS = closed_miter_point_seq(S, d) IN
--        IF ccw_vertex_order?(G) THEN seg
theorem polygon_miter_sequence_in_or_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (S : {S : {S : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → ℝ × ℝ) // S.List.length > 0 ∧
 ∀ (i : Finset.range S.List.length - 1), ¬ S.seq i = S.seq (1 + i)} // (S.seq (loop_back_intercept S) = S.seq (S.List.length - 1) → S.List.length > 3)
 ∧
 mostly_uniq_point_list_p S.List.length (loop_back_intercept S) (S.seq) ∧
  segments_do_not_cross_p S}) => d ≤ max_miter_offset S → (let MS := closed_miter_point_seq S d; if ccw_vertex_order_p G then segment_seq_outside_p MS G
  else segment_seq_inside_p MS G
  ) (polygon_vertex_seq G) := by
  -- === TCCs for polygon_miter_sequence_in_or_out ===
  -- TCC: polygon_miter_sequence_in_or_out_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, d: posreal, S: edge_parallel_endpoint_seq):
  --   S = polygon_vertex_seq(G) AND d <= max_miter_offset(S) IMPLIES
  --    S`seq(0) = S`seq(S`length - 1)
  have polygon_miter_sequence_in_or_out_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.falling_edge_imp_ccw_in_or_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET i = rightward_crossing_index(G, p) IN
--     segment_falling?(edges_of_polygon(G)(i)) IMPLIES
--      IF ccw_vertex_order?(G) THEN poin
theorem falling_edge_imp_ccw_in_or_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => segment_falling_p (edges_of_polygon G(i)) → if ccw_vertex_order_p G then point_outside_polygon_p p G
else point_in_polygon_p p G (rightward_crossing_index G p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.ccw_imp_falling_edge_in_or_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET i = rightward_crossing_index(G, p) IN
--     ccw_vertex_order?(G) IMPLIES
--      IF segment_falling?(edges_of_polygon(G)(i))
--        TH
theorem ccw_imp_falling_edge_in_or_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => ccw_vertex_order_p G → if segment_falling_p (edges_of_polygon G(i))
  then point_outside_polygon_p p G
else point_in_polygon_p p G (rightward_crossing_index G p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_in_has_rightward_crossings
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   point_in_polygon?(p, G) IMPLIES has_rightward_crossings?(G)(p)
theorem point_in_has_rightward_crossings : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_in_polygon_p p G → has_rightward_crossings_p G(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_outside_imp_not_inside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d, n: posnat):
--   (EXISTS (S: serpentine_ray):
--      serp_ray_outside?(S, G) AND p = S`body`seq(0) AND S`body`length <= n)
--    IMPLIES NOT point_in_polygon?(p, G)
theorem point_outside_imp_not_inside : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (n : {i : {i : ℝ // i ≥ 0} // i > 0}), (∃ (S : serpentine_ray), serp_ray_outside_p S G ∧ p = S.body.seq 0 ∧ S.body.List.length ≤ n) → ¬ point_in_polygon_p p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_outside_imp_not_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   point_outside_polygon?(p, G) IMPLIES NOT point_in_polygon?(p, G)
theorem point_outside_imp_not_in : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_outside_polygon_p p G → ¬ point_in_polygon_p p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_not_in_imp_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   NOT point_in_polygon?(p, G) IMPLIES
--    point_on_polygon_perimeter?(G)(p) OR point_outside_polygon?(p, G)
theorem point_not_in_imp_outside : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ point_in_polygon_p p G → point_on_polygon_perimeter_p G(p) ∨ point_outside_polygon_p p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_out_not_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   NOT point_in_polygon?(p, G) IFF
--    (point_on_polygon_perimeter?(G)(p) OR point_outside_polygon?(p, G))
theorem point_out_not_in : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ point_in_polygon_p p G ↔ (point_on_polygon_perimeter_p G(p) ∨ point_outside_polygon_p p G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.same_perim_pts_imp_same_exterior_pts
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (p: point_2d):
--      point_on_polygon_perimeter?(A)(p) IFF
--       point_on_polygon_perimeter?(B)(p))
--    IMPLIES
--    FORALL (q: point_2d):
--      point_outside_po
theorem same_perim_pts_imp_same_exterior_pts : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (p : point_2d), point_on_polygon_perimeter_p A(p) ↔ point_on_polygon_perimeter_p B(p)) → ∀ (q : point_2d), point_outside_polygon_p q A ↔ point_outside_polygon_p q B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.same_perim_pts_imp_same_interior_pts
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (p: point_2d):
--      point_on_polygon_perimeter?(A)(p) IFF
--       point_on_polygon_perimeter?(B)(p))
--    IMPLIES
--    FORALL (q: point_2d): point_in_polygon?(q, 
theorem same_perim_pts_imp_same_interior_pts : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (p : point_2d), point_on_polygon_perimeter_p A(p) ↔ point_on_polygon_perimeter_p B(p)) → ∀ (q : point_2d), point_in_polygon_p q A ↔ point_in_polygon_p q B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.falling_edge_ccw_in_or_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET i = rightward_crossing_index(G, p) IN
--     segment_falling?(edges_of_polygon(G)(i)) IFF
--      IF ccw_vertex_order?(G) THEN point_ou
theorem falling_edge_ccw_in_or_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => segment_falling_p (edges_of_polygon G(i)) ↔ if ccw_vertex_order_p G then point_outside_polygon_p p G
else point_in_polygon_p p G (rightward_crossing_index G p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.ccw_falling_edge_in_or_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: (has_rightward_crossings?(G))):
--   LET i = rightward_crossing_index(G, p) IN
--     ccw_vertex_order?(G) IFF
--      IF segment_falling?(edges_of_polygon(G)(i))
--        THEN p
theorem ccw_falling_edge_in_or_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), fun (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) => ccw_vertex_order_p G ↔ if segment_falling_p (edges_of_polygon G(i))
  then point_outside_polygon_p p G
else point_in_polygon_p p G (rightward_crossing_index G p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.nearest_rightward_crossing_in_or_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem nearest_rightward_crossing_in_or_out : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: polygon_membership.point_in_has_inside_segments
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   point_in_polygon?(p, G) IMPLIES
--    FORALL (q: (point_on_polygon_perimeter?(G))):
--      EXISTS (S: segment_endpoint_seq):
--        S`seq(0) = p AND
--         S`
theorem point_in_has_inside_segments : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_in_polygon_p p G → ∀ (q : (point_on_polygon_perimeter_p G)), ∃ (S : segment_endpoint_seq), S.seq 0 = p ∧
     S.seq (S.List.length - 1) = q ∧
      ∀ (r : point_2d), r ≠ q ∧ point_on_segment_seq_p r S →
         point_in_polygon_p r G := by
  -- === TCCs for point_in_has_inside_segments ===
  -- TCC: point_in_has_inside_segments_TCC1
  -- Statement: FORALL (p: point_2d, G: simple_polygon_2d):
  --   point_in_polygon?(p, G) IMPLIES
  --    FORALL (q: (point_on_polygon_perimeter?(G)), S: segment_endpoint_seq):
  --      S`seq(0) = p IMPLIES S`length - 1 >= 0 A...
  have point_in_has_inside_segments_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_outside_not_in_polygon_arb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, d: posreal):
--   point_outside_polygon?(p + d * v, G) IMPLIES
--    NOT point_in_polygon_arb?(p + d * v, v, G)
theorem point_outside_not_in_polygon_arb : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), point_outside_polygon_p (p + d * v) G → ¬ point_in_polygon_arb_p (p + d * v) v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.point_outside_not_in_polygon_arb_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, d: posreal):
--   point_outside_polygon?(p - d * v, G) IMPLIES
--    NOT point_in_polygon_arb?(p - d * v, v, G)
theorem point_outside_not_in_polygon_arb_neg : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), point_outside_polygon_p (p - d * v) G → ¬ point_in_polygon_arb_p (p - d * v) v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.left_right_midpoints_in_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET e = edges_of_polygon(G)(i) IN
--     d <= in_out_edge_offset_dist(G) AND
--      point_in_polygon?(leftward_segment_offset(e, d), G
theorem left_right_midpoints_in_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ in_out_edge_offset_dist G ∧
 point_in_polygon_p (leftward_segment_offset e d) G → point_outside_polygon_p (rightward_segment_offset e d) G (edges_of_polygon G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.right_left_midpoints_in_out
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET e = edges_of_polygon(G)(i) IN
--     d <= in_out_edge_offset_dist(G) AND
--      point_in_polygon?(rightward_segment_offset(e, d), 
theorem right_left_midpoints_in_out : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ in_out_edge_offset_dist G ∧
 point_in_polygon_p (rightward_segment_offset e d) G → point_outside_polygon_p (leftward_segment_offset e d) G (edges_of_polygon G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.left_right_midpoints_out_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET e = edges_of_polygon(G)(i) IN
--     d <= in_out_edge_offset_dist(G) AND
--      point_outside_polygon?(leftward_segment_offset(e, 
theorem left_right_midpoints_out_in : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ in_out_edge_offset_dist G ∧
 point_outside_polygon_p (leftward_segment_offset e d) G → point_in_polygon_p (rightward_segment_offset e d) G (edges_of_polygon G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.right_left_midpoints_out_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), d: posreal):
--   LET e = edges_of_polygon(G)(i) IN
--     d <= in_out_edge_offset_dist(G) AND
--      point_outside_polygon?(rightward_segment_offset(e,
theorem right_left_midpoints_out_in : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), fun (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => d ≤ in_out_edge_offset_dist G ∧
 point_outside_polygon_p (rightward_segment_offset e d) G → point_in_polygon_p (leftward_segment_offset e d) G (edges_of_polygon G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.counted_crossing_direction_exists
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   point_on_polygon_perimeter?(G)(p) IMPLIES
--    EXISTS (v: Normalized): counted_crossing_direction?(p, G)(v)
theorem counted_crossing_direction_exists : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_polygon_perimeter_p G(p) → ∃ (v : Normalized), counted_crossing_direction_p p G(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.free_ray_point_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(p, G)(v) AND
--    empty?[real](all_arb_intersections(p, v, G))
--    IMPLIES FORALL (d: posreal): point_outside_poly
theorem free_ray_point_outside : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p p G(v) ∧
 empty_p (all_arb_intersections p v G) → ∀ (d : {x : ℝ // 0 < x}), point_outside_polygon_p (p + d * v) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.only_parallel_ray_point_outside
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, d: posreal):
--   counted_crossing_direction?(p, G)(v) AND
--    empty?[real](key_arb_intersections(p, v, G)) AND
--     NOT empty?[real](parallel_inte
theorem only_parallel_ray_point_outside : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), counted_crossing_direction_p p G(v) ∧
 empty_p (key_arb_intersections p v G) ∧
  ¬ empty_p (parallel_intersections p v G) ∧
   d < min (parallel_intersections p v G) → point_outside_polygon_p (p + d * v) G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.one_dir_empty_intersections
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(p, G)(v) AND
--    empty?[real](key_arb_intersections(p, -v, G))
--    IMPLIES NOT empty?[real](key_arb_intersections
theorem one_dir_empty_intersections : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p p G(v) ∧
 empty_p (key_arb_intersections p -v G) → ¬ empty_p (key_arb_intersections p v G) := by
  -- === TCCs for one_dir_empty_intersections ===
  -- TCC: one_dir_empty_intersections_TCC1
  -- Statement: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
  --   counted_crossing_direction?(p, G)(v) IMPLIES norm(-v) = 1
  have one_dir_empty_intersections_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.one_dir_empty_point_outside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(p, G)(v) AND
--    empty?[real](key_arb_intersections(p, -v, G))
--    IMPLIES NOT odd?(card(arb_crossing_set(p, v, G
theorem one_dir_empty_point_outside : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p p G(v) ∧
 empty_p (key_arb_intersections p -v G) → ¬ odd_p (card (arb_crossing_set p v G)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.one_dir_out_other_in_polygon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, d: posreal):
--        counted_crossing_direction?(p, G)(v)
--    AND (NOT odd?(card(arb_crossing_set(p, v, G))))
--    AND (NOT empty?[real](key_arb_i
theorem one_dir_out_other_in_polygon : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), counted_crossing_direction_p p G(v)
 ∧ (¬ odd_p (card (arb_crossing_set p v G)))
 ∧ (¬ empty_p (key_arb_intersections p v G))
 ∧ (d < min (key_arb_intersections p v G))
 ∧ (empty_p (parallel_intersections p v G) ∨
       d < min (parallel_intersections p v G)) → point_in_polygon_arb_p (p + d * v) v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.opposite_sides_of_crossing_arb
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(p, G)(v) AND
--    NOT empty?[real](all_arb_intersections(p, v, G)) AND
--     NOT empty?[real](all_arb_intersections
theorem opposite_sides_of_crossing_arb : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p p G(v) ∧
 ¬ empty_p (all_arb_intersections p v G) ∧
  ¬ empty_p (all_arb_intersections p -v G) → (∀ (d1 : _) (d2 : {x : ℝ // 0 < x}), d1 < min (all_arb_intersections p v G) ∧
    d2 < min (all_arb_intersections p -v G)
    →
    point_in_polygon_arb_p (p - d2 * v) v G ∧
     point_outside_polygon_p (p + d1 * v) G)
 ∨
 (∀ (d1 : _) (d2 : {x : ℝ // 0 < x}), d1 < min (all_arb_intersections p v G) ∧
     d2 < min (all_arb_intersections p -v G)
     →
     point_outside_polygon_p (p - d2 * v) G ∧
      point_in_polygon_arb_p (p + d1 * v) v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.nearby_point_outside_exists_either
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   point_on_polygon_perimeter?(G)(p) IMPLIES
--    EXISTS (a, b: posreal, v: Normalized):
--      ((FORALL (c, d: posreal):
--          c < a AND d < b IMPLIES
--       
theorem nearby_point_outside_exists_either : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_polygon_perimeter_p G(p) → ∃ (a : _) (b : {x : ℝ // 0 < x}) (v : Normalized), ((∀ (c : _) (d : {x : ℝ // 0 < x}), c < a ∧ d < b →
       point_outside_polygon_p (p + c * v) G ∧
        point_in_polygon_arb_p (p - d * v) v G)
    ∨
    (∀ (c : _) (d : {x : ℝ // 0 < x}), c < a ∧ d < b →
        point_in_polygon_arb_p (p + c * v) v G ∧
         point_outside_polygon_p (p - d * v) G))
   ∧ counted_crossing_direction_p p G(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.nearby_point_outside_exists
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   point_on_polygon_perimeter?(G)(p) IMPLIES
--    EXISTS (a, b: posreal, v: Normalized):
--      (FORALL (c, d: posreal):
--         c < a AND d < b IMPLIES
--         
theorem nearby_point_outside_exists : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_polygon_perimeter_p G(p) → ∃ (a : _) (b : {x : ℝ // 0 < x}) (v : Normalized), (∀ (c : _) (d : {x : ℝ // 0 < x}), c < a ∧ d < b →
      point_outside_polygon_p (p + c * v) G ∧
       point_in_polygon_arb_p (p - d * v) v G)
   ∧ counted_crossing_direction_p p G(v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.nearby_point_in_polygon
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, d: nonneg_real, G: simple_polygon_2d):
--   point_in_polygon?(p, G) AND
--    NOT point_on_polygon_perimeter?(G)(p + d * v) AND
--     NOT empty?[point_2d](arb_crossing_set(
theorem nearby_point_in_polygon : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (d : {x : ℝ // x ≥ 0}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_in_polygon_p p G ∧
 ¬ point_on_polygon_perimeter_p G(p + d * v) ∧
  ¬ empty_p (arb_crossing_set p v G) ∧
   d < min (key_arb_intersections p v G) → point_in_polygon_p (p + d * v) G := by
  -- === TCCs for nearby_point_in_polygon ===
  -- TCC: nearby_point_in_polygon_TCC1
  -- Statement: FORALL (p: point_2d, v: Normalized, d: nonneg_real, G: simple_polygon_2d):
  --   point_in_polygon?(p, G) AND
  --    NOT point_on_polygon_perimeter?(G)(p + d * v) AND
  --     NOT empty?[point_2d](arb_crossing_s...
  have nearby_point_in_polygon_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.A_perim_outside_imp_B_perim_inside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d):
--   (FORALL (p: point_2d):
--      point_on_polygon_perimeter?(A)(p) IMPLIES
--       point_outside_polygon?(p, B))
--    AND
--    (EXISTS (u: point_2d):
--       point_in_polygon?(u
theorem A_perim_outside_imp_B_perim_inside : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), (∀ (p : point_2d), point_on_polygon_perimeter_p A(p) → point_outside_polygon_p p B)
 ∧
 (∃ (u : point_2d), point_in_polygon_p u A ∧
     (point_in_polygon_p u B ∨ point_on_polygon_perimeter_p B(u))) → ∀ (q : point_2d), point_on_polygon_perimeter_p B(q) → point_in_polygon_p q A := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.overlap_imp_inside_pt_on_perim
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, B: simple_polygon_2d, p: point_2d):
--   NOT polygon_contained?(B, A) AND
--    point_in_polygon?(p, A) AND point_in_polygon?(p, B)
--    IMPLIES
--    EXISTS (r: (point_on_polygon_perimeter?(A))): poi
theorem overlap_imp_inside_pt_on_perim : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (B : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), ¬ polygon_contained_p B A ∧
 point_in_polygon_p p A ∧ point_in_polygon_p p B → ∃ (r : (point_on_polygon_perimeter_p A)), point_in_polygon_p r B := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.outside_C_inside_A
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, A, C: simple_polygon_2d):
--   point_on_polygon_perimeter?(C)(p) AND point_in_polygon?(p, A) IMPLIES
--    EXISTS (q: point_2d):
--      point_in_polygon?(q, A) AND point_outside_polygon?(
theorem outside_C_inside_A : ∀ (p : ℝ × ℝ) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_polygon_perimeter_p C(p) ∧ point_in_polygon_p p A → ∃ (q : point_2d), point_in_polygon_p q A ∧ point_outside_polygon_p q C := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.container_edges_not_inside
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A, C: simple_polygon_2d):
--   polygon_contained?(A, C) IMPLIES
--    FORALL (p: point_2d), (e: (edge_of_polygon?(C))):
--      point_on_segment?(p, e) IMPLIES NOT point_in_polygon?(p, A)
theorem container_edges_not_inside : ∀ (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p A C → ∀ (p: point_2d), (e: (edge_of_polygon_p C)):
  point_on_segment_p p e → ¬ point_in_polygon_p p A := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.contained_not_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, A, C: simple_polygon_2d):
--   polygon_contained?(A, C) AND point_in_polygon?(p, A) IMPLIES
--    NOT point_on_polygon_perimeter?(C)(p)
theorem contained_not_on_perimeter : ∀ (p : ℝ × ℝ) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p A C ∧ point_in_polygon_p p A → ¬ point_on_polygon_perimeter_p C(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.contained_membership
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, A, C: simple_polygon_2d):
--   polygon_contained?(A, C) AND point_in_polygon?(p, A) IMPLIES
--    point_in_polygon?(p, C)
theorem contained_membership : ∀ (p : ℝ × ℝ) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p A C ∧ point_in_polygon_p p A → point_in_polygon_p p C := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygon_membership.contained_membership_cor
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, A, C: simple_polygon_2d):
--   polygon_contained?(A, C) AND point_outside_polygon?(p, C) IMPLIES
--    point_outside_polygon?(p, A)
theorem contained_membership_cor : ∀ (p : ℝ × ℝ) (A : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (C : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_contained_p A C ∧ point_outside_polygon_p p C → point_outside_polygon_p p A := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.polygon_membership