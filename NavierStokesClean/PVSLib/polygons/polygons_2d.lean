-- Auto-generated by pvs2lean/translator.py
-- PVS theory: polygons_2d
-- PVSLib library: polygons
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: polygons_2d

Auto-generated stubs from PVSLib theory `polygons_2d`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.polygons_2d

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (ELSIF : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (_alt : pvs_τ3 → pvs_τ4)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (are_segments_intersecting_p : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (are_segments_overlapping_p : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (are_segments_strictly_intersecting_p : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (below : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (cross : pvs_τ15 → pvs_τ16 → Prop)
variable {pvs_τ17 : Type*} {pvs_τ18 : Type*} (dir_parallel_p : pvs_τ17 → pvs_τ18 → Prop)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} (is_point_on_segment_p : pvs_τ57 → pvs_τ58 → Prop)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} (length : pvs_τ63 → pvs_τ64)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (norm : pvs_τ69 → pvs_τ70)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} (point_on_arb_ray_p : pvs_τ71 → pvs_τ72 → pvs_τ73 → Prop)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} (point_on_segment_p : pvs_τ76 → pvs_τ77 → Prop)
variable {pvs_τ82 : Type*} {pvs_τ83 : Type*} {pvs_τ84 : Type*} (rem : pvs_τ82 → pvs_τ83 → pvs_τ84)
variable {pvs_τ85 : Type*} {pvs_τ86 : Type*} (segment_endpoint_p : pvs_τ85 → pvs_τ86 → Prop)
variable {pvs_τ93 : Type*} (segment_midpoint : pvs_τ93 → Prop)
variable {pvs_τ98 : Type*} {pvs_τ99 : Type*} (seq : pvs_τ98 → pvs_τ99)
variable {pvs_τ102 : Type*} (uniq_vertex_list_p : pvs_τ102 → Prop)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} (upto : pvs_τ103 → pvs_τ104)
variable {pvs_τ107 : Type*} (vertices : pvs_τ107 → Prop)

-- pvs2lean: polygons_2d.prev_index
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF cur_index = 0 THEN p`num_vertices - 1 ELSE cur_index - 1 ENDIF
axiom prev_index {pvs_τ79 : Type*} {pvs_τ80 : Type*} {pvs_τ81 : Type*} : pvs_τ79 → pvs_τ80 → pvs_τ81

-- pvs2lean: polygons_2d.next_index
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF cur_index = p`num_vertices - 1 THEN 0 ELSE cur_index + 1 ENDIF
axiom next_index {pvs_τ65 : Type*} {pvs_τ66 : Type*} : pvs_τ65 → pvs_τ66 → Prop

-- pvs2lean: polygons_2d.prev_next_is_original
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: polygon_2d, cur_index: below(p`num_vertices)):
--   prev_index(p, next_index(p, cur_index)) = cur_index
theorem prev_next_is_original : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (cur_index : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), prev_index p (next_index p cur_index) = cur_index := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.next_prev_is_original
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: polygon_2d, cur_index: below(p`num_vertices)):
--   next_index(p, prev_index(p, cur_index)) = cur_index
theorem next_prev_is_original : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (cur_index : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), next_index p (prev_index p cur_index) = cur_index := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.rem_diff_eq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: posnat, i, j: below(b)): rem(b)(b + j - i) = 0 IMPLIES j = i
theorem rem_diff_eq_0 : ∀ (b : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : {s : {i : ℝ // i ≥ 0} // s < b}) (j : {s : {i : ℝ // i ≥ 0} // s < b}), rem b(b + j - i) = 0 → j = i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.rem_diff_eq_0_shift
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: posnat, i, j: below(b)): rem(b)(j - i) = 0 IMPLIES j = i
theorem rem_diff_eq_0_shift : ∀ (b : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : {s : {i : ℝ // i ≥ 0} // s < b}) (j : {s : {i : ℝ // i ≥ 0} // s < b}), rem b(j - i) = 0 → j = i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.rem_add1_eq_next_index
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   rem(G`num_vertices)(1 + G`num_vertices + i) = next_index(G, i)
theorem rem_add1_eq_next_index : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), rem G.num_vertices(1 + G.num_vertices + i) = next_index G i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.rem_sum3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b: posnat, i, j, k: int):
--   rem(b)(i + rem(b)(j) + k) = rem(b)(i + j + k)
theorem rem_sum3 : ∀ (b : {i : {i : ℝ // i ≥ 0} // i > 0}) (i : ℝ) (j : ℝ) (k : ℝ), rem b(i + rem b(j) + k) = rem b(i + j + k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (p: point_2d):
--   EXISTS (i: below(polygon`num_vertices)): polygon`vertices(i) = p
axiom vertex_p {pvs_τ105 : Type*} {pvs_τ106 : Type*} : pvs_τ105 → pvs_τ106 → Prop

-- pvs2lean: polygons_2d.vertex_is_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, i: below(polygon`num_vertices)):
--   vertex?(polygon)(polygon`vertices(i))
theorem vertex_is_vertex : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < polygon.num_vertices}), vertex_p polygon (polygon.vertices i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.nonvertex?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (p: point_2d): NOT vertex?(polygon)(p)
axiom nonvertex_p {pvs_τ67 : Type*} {pvs_τ68 : Type*} : pvs_τ67 → pvs_τ68 → Prop

-- pvs2lean: polygons_2d.vertex_is_not_nonvertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, i: below(polygon`num_vertices)):
--   NOT nonvertex?(polygon)(polygon`vertices(i))
theorem vertex_is_not_nonvertex : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < polygon.num_vertices}), ¬(nonvertex_p polygon (polygon.vertices i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.edges_of_polygon
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (i: below(polygon`num_vertices)):
--   (# p1 := polygon`vertices(i),
--      p2 := polygon`vertices(next_index(polygon, i)) #)
axiom edges_of_polygon {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: polygons_2d.edge_of_polygon?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (s: segment_2d):
--   EXISTS (i: below(polygon`num_vertices)): edges_of_polygon(polygon)(i) = s
axiom edge_of_polygon_p {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: polygons_2d.edge_is_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, i: below(polygon`num_vertices)):
--   edge_of_polygon?(polygon)(edges_of_polygon(polygon)(i))
theorem edge_is_edge : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < polygon.num_vertices}), edge_of_polygon_p polygon (edges_of_polygon polygon i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_helper
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF idx = p`num_vertices THEN -1
-- ELSIF p`vertices(idx) = point THEN idx
-- ELSE find_helper(p, idx + 1, point)
-- ENDIF
axiom find_helper {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ26 → pvs_τ27 → pvs_τ28 → Prop

-- pvs2lean: polygons_2d.find_helper_invariant
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: polygon_2d, idx: upto(p`num_vertices), point: point_2d):
--   LET k = find_helper(p, idx, point) IN
--     0 <= k AND p`vertices(k) = point OR
--      k = -1 AND
--       FORALL (j: nat):
--         j < p
theorem find_helper_invariant : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (idx : {s : {i : ℝ // i ≥ 0} // s ≤ p.num_vertices}) (point : ℝ × ℝ), fun (k : {i : ℝ // -1 ≤ i ∧ i < p.num_vertices}) => 0 ≤ k ∧ p.vertices k = point ∨ k = -1 ∧
 ∀ (j : ℕ), j < p.num_vertices - idx →
    p.vertices (p.num_vertices - 1 - j) ≠ point (find_helper p idx point) := by
  -- === TCCs for find_helper_invariant ===
  -- TCC: find_helper_invariant_TCC1
  -- Statement: FORALL (p: polygon_2d, idx: upto(p`num_vertices), point: point_2d,
  --         k: {i: int | -1 <= i AND i < p`num_vertices}):
  --   k = find_helper(p, idx, point) AND 0 <= k IMPLIES
  --    k >= 0 AND k < p`num...
  have find_helper_invariant_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: find_helper_invariant_TCC2
  -- Statement: FORALL (p: polygon_2d, idx: upto(p`num_vertices), point: point_2d,
  --         k: {i: int | -1 <= i AND i < p`num_vertices}):
  --   k = find_helper(p, idx, point) AND
  --    NOT (0 <= k AND p`vertices(k) = poi...
  have find_helper_invariant_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_index
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: find_helper(polygon, 0, point)
axiom find_index {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ29 → pvs_τ30 → Prop

-- pvs2lean: polygons_2d.next_index_distinct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)): next_index(G, i) /= i
theorem next_index_distinct : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), next_index G i ≠ i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_find_helper
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, p: point_2d, i, j: below(G`num_vertices)):
--   G`vertices(i) = p AND i >= j IMPLIES find_helper(G, j, p) >= 0
theorem vertex_find_helper : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices i = p ∧ i ≥ j → find_helper G j p ≥ 0 := by
  -- === TCCs for vertex_find_helper ===
  -- TCC: vertex_find_helper_TCC1
  -- Statement: FORALL (G: polygon_2d, p: point_2d, i, j: below(G`num_vertices)):
  --   G`vertices(i) = p AND i >= j IMPLIES j <= G`num_vertices
  have vertex_find_helper_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_find_index
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, p: point_2d):
--   vertex?(G)(p) IMPLIES find_index(G, p) >= 0
theorem vertex_find_index : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), vertex_p G(p) → find_index G p ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_helper_self
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, u: point_2d, i: below(G`num_vertices)):
--   find_helper(G, i, u) >= 0 IMPLIES G`vertices(find_helper(G, i, u)) = u
theorem find_helper_self : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (u : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), find_helper G i u ≥ 0 → G.vertices (find_helper G i u) = u := by
  -- === TCCs for find_helper_self ===
  -- TCC: find_helper_self_TCC1
  -- Statement: FORALL (G: polygon_2d, i: below(G`num_vertices)): i <= G`num_vertices
  have find_helper_self_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: find_helper_self_TCC2
  -- Statement: FORALL (G: polygon_2d, u: point_2d, i: below(G`num_vertices)):
  --   find_helper(G, i, u) >= 0 IMPLIES find_helper(G, i, u) < G`num_vertices
  have find_helper_self_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_index_self
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, u: point_2d):
--   find_index(G, u) >= 0 IMPLIES G`vertices(find_index(G, u)) = u
theorem find_index_self : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (u : ℝ × ℝ), find_index G u ≥ 0 → G.vertices (find_index G u) = u := by
  -- === TCCs for find_index_self ===
  -- TCC: find_index_self_TCC1
  -- Statement: FORALL (G: polygon_2d, u: point_2d):
  --   find_index(G, u) >= 0 IMPLIES find_index(G, u) < G`num_vertices
  have find_index_self_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_index_self_index
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   find_index(G, G`vertices(i)) = i
theorem find_index_self_index : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), find_index G (G.vertices i) = i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.segment_in_edges
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, G: polygon_2d):
--   find_index(G, u) >= 0 AND v = G`vertices(next_index(G, find_index(G, u)))
--    IMPLIES edge_of_polygon?(G)((# p1 := u, p2 := v #))
theorem segment_in_edges : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), find_index G u ≥ 0 ∧ v = G.vertices (next_index G (find_index G u)) → edge_of_polygon_p G((# p1 := u, p2 := v #)) := by
  -- === TCCs for segment_in_edges ===
  -- TCC: segment_in_edges_TCC1
  -- Statement: FORALL (u, v: point_2d, G: polygon_2d):
  --   find_index(G, u) >= 0 AND v = G`vertices(next_index(G, find_index(G, u)))
  --    IMPLIES v /= u
  have segment_in_edges_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.edge_segment_has_vertices
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, s: segment_2d):
--   edge_of_polygon?(G)(s) IMPLIES vertex?(G)(s`p1) AND vertex?(G)(s`p2)
theorem edge_segment_has_vertices : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), edge_of_polygon_p G(s) → vertex_p G(s.p1) ∧ vertex_p G(s.p2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_index_segment_is_edge
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (u, v: point_2d, G: polygon_2d):
--   find_index(G, u) >= 0 AND v = G`vertices(next_index(G, find_index(G, u)))
--    IMPLIES edges_of_polygon(G)(find_index(G, u)) = (# p1 := u, p2 := v #)
theorem find_index_segment_is_edge : ∀ (u : ℝ × ℝ) (v : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), find_index G u ≥ 0 ∧ v = G.vertices (next_index G (find_index G u)) → edges_of_polygon G(find_index G u) = (# p1 := u, p2 := v #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_neq_next
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   G`vertices(i) /= G`vertices(next_index(G, i))
theorem vertex_neq_next : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices i ≠ G.vertices (next_index G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_neq_next_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   G`vertices(next_index(G, i)) /= G`vertices(i)
theorem vertex_neq_next_2 : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices (next_index G i) ≠ G.vertices i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_neq_prev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   G`vertices(i) /= G`vertices(prev_index(G, i))
theorem vertex_neq_prev : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices i ≠ G.vertices (prev_index G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_neq_prev_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   G`vertices(prev_index(G, i)) /= G`vertices(i)
theorem vertex_neq_prev_2 : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices (prev_index G i) ≠ G.vertices i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.prev_vertex_neq_next_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   G`vertices(prev_index(G, i)) /= G`vertices(next_index(G, i))
theorem prev_vertex_neq_next_vertex : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices (prev_index G i) ≠ G.vertices (next_index G i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertices_diff_next_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   G`vertices(next_index(G, i)) - G`vertices(i) = zero IFF FALSE
theorem vertices_diff_next_nonzero : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices (next_index G i) - G.vertices i = zero ↔ False := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertices_diff_prev_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   G`vertices(i) - G`vertices(prev_index(G, i)) = zero IFF FALSE
theorem vertices_diff_prev_nonzero : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices i - G.vertices (prev_index G i) = zero ↔ False := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.second_edge_does_not_double_back
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v0: point_2d, G: polygon_2d, j0, j: int):
--   j0 = find_index(G, v0) AND
--    j0 >= 0 AND j = find_index(G, G`vertices(next_index(G, j0)))
--    IMPLIES v0 /= G`vertices(next_index(G, j))
theorem second_edge_does_not_double_back : ∀ (v0 : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j0 : ℝ) (j : ℝ), j0 = find_index G v0 ∧
 j0 ≥ 0 ∧ j = find_index G (G.vertices (next_index G j0)) → v0 ≠ G.vertices (next_index G j) := by
  -- === TCCs for second_edge_does_not_double_back ===
  -- TCC: second_edge_does_not_double_back_TCC1
  -- Statement: FORALL (v0: point_2d, G: polygon_2d, j0: int):
  --   j0 = find_index(G, v0) AND j0 >= 0 IMPLIES j0 < G`num_vertices
  have second_edge_does_not_double_back_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: second_edge_does_not_double_back_TCC2
  -- Statement: FORALL (v0: point_2d, G: polygon_2d, j0, j: int):
  --   j0 = find_index(G, v0) AND
  --    j0 >= 0 AND j = find_index(G, G`vertices(next_index(G, j0)))
  --    IMPLIES j >= 0 AND j < G`num_vertices
  have second_edge_does_not_double_back_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_on_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   point_on_segment?(G`vertices(i), edges_of_polygon(G)(i))
theorem vertex_on_edge : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_on_segment_p (G.vertices i) (edges_of_polygon G(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.next_vertex_on_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   point_on_segment?(G`vertices(next_index(G, i)), edges_of_polygon(G)(i))
theorem next_vertex_on_edge : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_on_segment_p (G.vertices (next_index G i)) (edges_of_polygon G(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_on_prev_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   point_on_segment?(G`vertices(i), edges_of_polygon(G)(prev_index(G, i)))
theorem vertex_on_prev_edge : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_on_segment_p (G.vertices i) (edges_of_polygon G(prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.norm_vertex_diff_next_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   norm(G`vertices(next_index(G, i)) - G`vertices(i)) > 0
theorem norm_vertex_diff_next_pos : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), norm (G.vertices (next_index G i) - G.vertices i) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.norm_vertex_diff_prev_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   norm(G`vertices(prev_index(G, i)) - G`vertices(i)) > 0
theorem norm_vertex_diff_prev_pos : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), norm (G.vertices (prev_index G i) - G.vertices i) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.two_adjacent_edges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem two_adjacent_edges : True := by
  -- === TCCs for two_adjacent_edges ===
  -- TCC: two_adjacent_edges_TCC1
  -- Statement: FORALL (p: point_2d, G: polygon_2d, i: below(G`num_vertices),
  --         e1: [# p1: point_2d, p2: point_2d #],
  --         e2: [# p1: point_2d, p2: point_2d #]):
  --   p = G`vertices(i) AND
  --    e1 = (# p1 := G...
  have two_adjacent_edges_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: two_adjacent_edges_TCC2
  -- Statement: FORALL (p: point_2d, G: polygon_2d, i: below(G`num_vertices),
  --         e1: [# p1: point_2d, p2: point_2d #],
  --         e2: [# p1: point_2d, p2: point_2d #]):
  --   p = G`vertices(i) AND
  --    e1 = (# p1 := G...
  have two_adjacent_edges_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: polygons_2d.same_edge_equiv_same_points
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i, j: below(G`num_vertices)):
--   i = j IFF
--    FORALL (u: point_2d):
--      point_on_segment?(u, edges_of_polygon(G)(i)) IFF
--       point_on_segment?(u, edges_of_polygon(G)(j))
theorem same_edge_equiv_same_points : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), i = j ↔ ∀ (u : point_2d), point_on_segment_p u (edges_of_polygon G(i)) ↔
   point_on_segment_p u (edges_of_polygon G(j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.rem_plus_1_eq_next_index
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   rem(G`num_vertices)(i + 1) = next_index(G, i)
theorem rem_plus_1_eq_next_index : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), rem G.num_vertices(i + 1) = next_index G i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.rem_minus_1_eq_prev_index
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i: below(G`num_vertices)):
--   rem(G`num_vertices)(i - 1) = prev_index(G, i)
theorem rem_minus_1_eq_prev_index : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), rem G.num_vertices(i - 1) = prev_index G i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.rem_plus_1_eq_next_index_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, i, j: nat):
--   rem(G`num_vertices)(i + j + 1) = next_index(G, rem(G`num_vertices)(i + j))
theorem rem_plus_1_eq_next_index_2 : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), rem G.num_vertices(i + j + 1) = next_index G (rem G.num_vertices(i + j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.equal_or_adjacent_edge?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (abs(i - j) <= 1) OR (i = n - 1 AND j = 0) OR (i = 0 AND j = n - 1)
axiom equal_or_adjacent_edge_p {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} : pvs_τ23 → pvs_τ24 → pvs_τ25 → Prop

-- pvs2lean: polygons_2d.equal_or_adjacent_edge_reflexive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nonneg_int, i: below(n)): equal_or_adjacent_edge?(n, i, i)
theorem equal_or_adjacent_edge_reflexive : ∀ (n : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < n}), equal_or_adjacent_edge_p n i i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.equal_or_adjacent_edge_symmetric
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nonneg_int, i, j: below(n)):
--   equal_or_adjacent_edge?(n, i, j) = equal_or_adjacent_edge?(n, j, i)
theorem equal_or_adjacent_edge_symmetric : ∀ (n : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < n}) (j : {s : {i : ℝ // i ≥ 0} // s < n}), equal_or_adjacent_edge_p n i j = equal_or_adjacent_edge_p n j i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.equal_or_adjacent_edge_not_transitive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (n: nonneg_int, i, j, k: below(n)):
--   equal_or_adjacent_edge?(n, i, j) AND
--    equal_or_adjacent_edge?(n, j, k) AND NOT equal_or_adjacent_edge?(n, i, k)
theorem equal_or_adjacent_edge_not_transitive : ∃ (n : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < n}) (j : {s : {i : ℝ // i ≥ 0} // s < n}) (k : {s : {i : ℝ // i ≥ 0} // s < n}), equal_or_adjacent_edge_p n i j ∧ equal_or_adjacent_edge_p n j k ∧ ¬ equal_or_adjacent_edge_p n i k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.not_equal_or_adjacent_edge?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT equal_or_adjacent_edge?(n, i, j)
noncomputable def not_equal_or_adjacent_edge_p : sorry := by
  sorry  -- pvs2lean: definition stub for NOT equal_or_adjacent_edge?(n, i, j)

-- pvs2lean: polygons_2d.adjacent_edge?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (abs(i - j) = 1) OR (i = n - 1 AND j = 0) OR (i = 0 AND j = n - 1)
axiom adjacent_edge_p {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: polygons_2d.polygon_edges_do_not_cross?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: below(p`num_vertices)):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     FORALL (q: (is_point_on_segment?(ei))):
--       is_point_on_segment?(ej)(q) IMPLIES i = j OR q
axiom polygon_edges_do_not_cross_p {pvs_τ78 : Type*} : pvs_τ78 → Prop

-- pvs2lean: polygons_2d.polygon_edges_do_not_cross?_alt
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(p`num_vertices), j: {n: below(p`num_vertices) | n /= i}):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     IF equal_or_adjacent_edge?(p`num_vertices, i, j)
--       
noncomputable def polygon_edges_do_not_cross_p_alt : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL (i: below(p`num_vertices), j: {n: below(p`num_vertices) | n /= i}):   LET

-- pvs2lean: polygons_2d.adj_edges_share_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: polygon_2d, i: below(p`num_vertices),
--         j: (adjacent_edge?(p`num_vertices, i))):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     ei`p2 = ej`p1 OR ei`p1 = ej`p2
theorem adj_edges_share_vertex : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ei.p2 = ej.p1 ∨ ei.p1 = ej.p2 (edges_of_polygon p j) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.short_antenna_edge_midpoint_on_longer_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   (s1`p2 = s2`p1) AND (length(s2) <= length(s1)) AND (^(s1) = -^(s2))
--    IMPLIES is_point_on_segment?(s1)(segment_midpoint(s2))
theorem short_antenna_edge_midpoint_on_longer_edge : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s1.p2 = s2.p1) ∧ (List.length s2 ≤ List.length s1) ∧ (^(s1) = -^(s2)) → is_point_on_segment_p s1(segment_midpoint s2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.short_antenna_edge_midpoint_not_endpoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: segment_2d):
--   (s1`p2 = s2`p1) AND (length(s2) <= length(s1)) AND (^(s1) = -^(s2))
--    IMPLIES segment_midpoint(s2) /= s1`p1
theorem short_antenna_edge_midpoint_not_endpoint : ∀ (s1 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s2 : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s1.p2 = s2.p1) ∧ (List.length s2 ≤ List.length s1) ∧ (^(s1) = -^(s2)) → segment_midpoint s2 ≠ s1.p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.antenna_edges_share_many_points
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: polygon_2d, i: below(p`num_vertices),
--         j: (adjacent_edge?(p`num_vertices, i))):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     ^(ei) = -^(ej) IMPLIES
--      EXIS
theorem antenna_edges_share_many_points : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ^(ei) = -^(ej) → ∃ (q : (is_point_on_segment_p ei)), is_point_on_segment_p ej(q) ∧ q ≠ ei.p1 ∧ q ≠ ei.p2 (edges_of_polygon p j) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.only_common_point_is_vertex_means_equal_or_adjacent
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: polygon_2d, i, j: below(p`num_vertices)):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     (EXISTS (p: (is_point_on_segment?(ei))): is_point_on_segment?(ej)(p))
--      AN
theorem only_common_point_is_vertex_means_equal_or_adjacent : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => (∃ (p : (is_point_on_segment_p ei)), is_point_on_segment_p ej(p)) ∧
 (∀ (q : (is_point_on_segment_p ei)), is_point_on_segment_p ej(q) → q = ei.p1 ∨ q = ei.p2)
  ∧
  (∀ (q : (is_point_on_segment_p ej)), is_point_on_segment_p ei(q) → q = ej.p1 ∨ q = ej.p2) → equal_or_adjacent_edge_p p.num_vertices i j (edges_of_polygon p j) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.common_pt_on_adj_edges_must_be_vertex_unless_antenna
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: polygon_2d, i: below(p`num_vertices),
--         j: (adjacent_edge?(p`num_vertices, i))):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     (^(ei) = -^(ej)) OR
--      (FORALL
theorem common_pt_on_adj_edges_must_be_vertex_unless_antenna : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => (^(ei) = -^(ej)) ∨ (∀ (q : (is_point_on_segment_p ei)), is_point_on_segment_p ej(q) → (q = ei.p1 ∨ q = ei.p2)) (edges_of_polygon p j) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.polygon_edges_do_not_cross_defs_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: polygon_2d):
--   polygon_edges_do_not_cross?(p) IFF polygon_edges_do_not_cross?_alt(p)
theorem polygon_edges_do_not_cross_defs_eq : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), polygon_edges_do_not_cross_p p ↔ polygon_edges_do_not_cross_p_alt p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.simple_polygon_2d?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: polygon_edges_do_not_cross?(p)
axiom simple_polygon_2d_p {pvs_τ100 : Type*} {pvs_τ101 : Type*} : pvs_τ100 → pvs_τ101 → Prop

-- pvs2lean: polygons_2d.example_right_triangle
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# num_vertices := 3,
--    vertices
--      := LAMBDA (i: below(3)):
--           IF (i = 0) THEN (# x := 0, y := 0 #)
--           ELSIF (i = 1) THEN (# x := 1, y := 0 #)
--           ELSE (# y := 1, x := 0 #)
--    
noncomputable def example_right_triangle : sorry := by
  sorry  -- pvs2lean: definition stub for (# num_vertices := 3,    vertices      := LAMBDA (i: below(3)):           IF (i 

-- pvs2lean: polygons_2d.example_diamond
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# num_vertices := 4,
--    vertices
--      := LAMBDA (i: below(4)):
--           IF (i = 0) THEN (# x := -1, y := 0 #)
--           ELSIF (i = 1) THEN (# x := 0, y := 1 #)
--           ELSIF (i = 2) THEN (# x := 1
noncomputable def example_diamond : sorry := by
  sorry  -- pvs2lean: definition stub for (# num_vertices := 4,    vertices      := LAMBDA (i: below(4)):           IF (i 

-- pvs2lean: polygons_2d.next_edge_shares_only_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: simple_polygon_2d, i: below(p`num_vertices)):
--   LET ei = edges_of_polygon(p)(i),
--       ej = edges_of_polygon(p)(next_index(p, i))
--     IN
--     FORALL (q: (is_point_on_segment?(ei))):
--       is
theorem next_edge_shares_only_vertex : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ∀ (q : ℝ × ℝ), is_point_on_segment_p ej(q) → q = ei.p2 ∧ q = ej.p1 (edges_of_polygon p (next_index p i)) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.prev_edge_shares_only_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: simple_polygon_2d, i: below(p`num_vertices)):
--   LET ei = edges_of_polygon(p)(i),
--       ej = edges_of_polygon(p)(prev_index(p, i))
--     IN
--     FORALL (q: (is_point_on_segment?(ei))):
--       is
theorem prev_edge_shares_only_vertex : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ∀ (q : ℝ × ℝ), is_point_on_segment_p ej(q) → q = ei.p1 ∧ q = ej.p2 (edges_of_polygon p (prev_index p i)) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.adjacent_edges_share_only_vertex
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: simple_polygon_2d, i: below(p`num_vertices),
--         j: (adjacent_edge?(p`num_vertices, i))):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     FORALL (q: (is_point_on_s
theorem adjacent_edges_share_only_vertex : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ∀ (q : ℝ × ℝ), is_point_on_segment_p ej(q) → q = ei.p2 ∧ q = ej.p1 ∨ q = ei.p1 ∧ q = ej.p2 (edges_of_polygon p j) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.next_edge_not_dir_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: simple_polygon_2d, i: below(p`num_vertices)):
--   LET ei = edges_of_polygon(p)(i),
--       ej = edges_of_polygon(p)(next_index(p, i))
--     IN NOT dir_parallel?(ei`p2 - ei`p1, ej`p1 - ej`p2)
theorem next_edge_not_dir_parallel : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ¬(dir_parallel_p (ei.p2 - ei.p1) (ej.p1 - ej.p2)) (edges_of_polygon p (next_index p i)) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.prev_edge_not_dir_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: simple_polygon_2d, i: below(p`num_vertices)):
--   LET ei = edges_of_polygon(p)(i),
--       ej = edges_of_polygon(p)(prev_index(p, i))
--     IN NOT dir_parallel?(ei`p1 - ei`p2, ej`p2 - ej`p1)
theorem prev_edge_not_dir_parallel : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ¬(dir_parallel_p (ei.p1 - ei.p2) (ej.p2 - ej.p1)) (edges_of_polygon p (prev_index p i)) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.adjacent_edges_not_dir_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: simple_polygon_2d, i: below(p`num_vertices),
--         j: (adjacent_edge?(p`num_vertices, i))):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     NOT dir_parallel?(ei`p2 -
theorem adjacent_edges_not_dir_parallel : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ¬(dir_parallel_p (ei.p2 - ei.p1) (ej.p1 - ej.p2)) (edges_of_polygon p j) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.prev_vertex_not_on_ray_of_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), e: segment_2d):
--   e = edges_of_polygon(G)(i) IMPLIES
--    NOT point_on_arb_ray?(G`vertices(prev_index(G, i)), e`p1, ^(e`p2 - e`p1))
theorem prev_vertex_not_on_ray_of_edge : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), e = edges_of_polygon G(i) → ¬ point_on_arb_ray_p (G.vertices (prev_index G i)) e.p1 (^(e.p2 - e.p1)) := by
  -- === TCCs for prev_vertex_not_on_ray_of_edge ===
  -- TCC: prev_vertex_not_on_ray_of_edge_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, i: below(G`num_vertices), e: segment_2d):
  --   e = edges_of_polygon(G)(i) IMPLIES e`p2 - e`p1 /= zero
  have prev_vertex_not_on_ray_of_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.only_adjacent_edges_intersect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: simple_polygon_2d, i, j: below(p`num_vertices)):
--   LET ei = edges_of_polygon(p)(i), ej = edges_of_polygon(p)(j) IN
--     FORALL (q: point_2d):
--       is_point_on_segment?(ei)(q) AND is_point_o
theorem only_adjacent_edges_intersect : ∀ (p : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}) (j : {s : {i : ℝ // i ≥ 0} // s < p.num_vertices}), fun (ei : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => fun (ej : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) => ∀ (q : ℝ × ℝ), is_point_on_segment_p ei(q) ∧ is_point_on_segment_p ej(q) → equal_or_adjacent_edge_p p.num_vertices i j (edges_of_polygon p j) (edges_of_polygon p i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.adjacent_edges_do_not_fold_back
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   G`vertices(prev_index(G, i)) /= G`vertices(i) AND
--    G`vertices(next_index(G, i)) /= G`vertices(i) AND
--     NOT dir_parallel?(G`vertices(prev_
theorem adjacent_edges_do_not_fold_back : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), G.vertices (prev_index G i) ≠ G.vertices i ∧ G.vertices (next_index G i) ≠ G.vertices i ∧
 ¬ dir_parallel_p (G.vertices (prev_index G i) - G.vertices i) (G.vertices (next_index G i) - G.vertices i) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.points_on_distinct_edges_differ
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d, j, k: below(G`num_vertices)):
--   is_point_on_segment?(edges_of_polygon(G)(j))(p) AND
--    is_point_on_segment?(edges_of_polygon(G)(k))(p) AND j < k
--    IMPLIES
--  
theorem points_on_distinct_edges_differ : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (j : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (k : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), is_point_on_segment_p (edges_of_polygon G(j)) (p) ∧
 is_point_on_segment_p (edges_of_polygon G(k)) (p) ∧ j < k → k = j + 1 ∧ p = edges_of_polygon G(j).p2 ∨
 k = G.num_vertices - 1 ∧ j = 0 ∧ p = edges_of_polygon G(k).p2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.edge_x_left_or_right_imp_cross_neq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   (G`vertices(prev_index(G, i))`x > G`vertices(i)`x AND
--     G`vertices(next_index(G, i))`x > G`vertices(i)`x)
--    OR
--    (G`vertices(prev_index(G
theorem edge_x_left_or_right_imp_cross_neq_0 : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), (G.vertices (prev_index G i).x > G.vertices i.x ∧
  G.vertices (next_index G i).x > G.vertices i.x)
 ∨
 (G.vertices (prev_index G i).x < G.vertices i.x ∧
   G.vertices (next_index G i).x < G.vertices i.x) → cross (G.vertices (prev_index G i) - G.vertices i) (G.vertices (next_index G i) - G.vertices i)
 ≠ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.edge_y_above_or_below_imp_cross_neq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   (G`vertices(prev_index(G, i))`y > G`vertices(i)`y AND
--     G`vertices(next_index(G, i))`y > G`vertices(i)`y)
--    OR
--    (G`vertices(prev_index(G
theorem edge_y_above_or_below_imp_cross_neq_0 : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), (G.vertices (prev_index G i).y > G.vertices i.y ∧
  G.vertices (next_index G i).y > G.vertices i.y)
 ∨
 (G.vertices (prev_index G i).y < G.vertices i.y ∧
   G.vertices (next_index G i).y < G.vertices i.y) → cross (G.vertices (prev_index G i) - G.vertices i) (G.vertices (next_index G i) - G.vertices i)
 ≠ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.shared_endpoints_imp_same_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, s, e: segment_2d):
--   (s`p1 = e`p1 OR s`p2 = e`p2) AND
--    edge_of_polygon?(G)(s) AND edge_of_polygon?(G)(e)
--    IMPLIES s = e
theorem shared_endpoints_imp_same_edge : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), (s.p1 = e.p1 ∨ s.p2 = e.p2) ∧
 edge_of_polygon_p G(s) ∧ edge_of_polygon_p G(e) → s = e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.same_polygon_edge_overlaps_equal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, s, e: segment_2d):
--   are_segments_overlapping?(s)(e) AND
--    edge_of_polygon?(G)(s) AND edge_of_polygon?(G)(e)
--    IMPLIES s = e
theorem same_polygon_edge_overlaps_equal : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), are_segments_overlapping_p s(e) ∧
 edge_of_polygon_p G(s) ∧ edge_of_polygon_p G(e) → s = e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.interior_point_is_nonvertex
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d, t: real,
--         i: below(G`num_vertices)):
--   0 < t AND
--    t < 1 AND p = (1 - t) * G`vertices(i) + t * G`vertices(next_index(G, i))
--    IMPLIES NOT vertex?(G)
theorem interior_point_is_nonvertex : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (t : ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), 0 < t ∧
 t < 1 ∧ p = (1 - t) * G.vertices i + t * G.vertices (next_index G i) → ¬ vertex_p G(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.nonvertex_on_one_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d, e, s: (edge_of_polygon?(G))):
--   nonvertex?(G)(p) AND point_on_segment?(p, e) AND point_on_segment?(p, s)
--    IMPLIES e = s
theorem nonvertex_on_one_edge : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), nonvertex_p G(p) ∧ point_on_segment_p p e ∧ point_on_segment_p p s → e = s := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_is_segment_endpoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d, s: (edge_of_polygon?(G))):
--   vertex?(G)(p) AND point_on_segment?(p, s) IMPLIES p = s`p1 OR p = s`p2
theorem vertex_is_segment_endpoint : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (s : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), vertex_p G(p) ∧ point_on_segment_p p s → p = s.p1 ∨ p = s.p2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_is_edge_endpoint
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, G: simple_polygon_2d):
--   vertex?(G)(p) IFF
--    EXISTS (s: (edge_of_polygon?(G))): segment_endpoint?(s)(p)
theorem vertex_is_edge_endpoint : ∀ (p : ℝ × ℝ) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), vertex_p G(p) ↔ ∃ (s : (edge_of_polygon_p G)), segment_endpoint_p s(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.vertex_on_two_edges
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
theorem vertex_on_two_edges : True := by
  sorry  -- needs_human: automated tactics exhausted

-- pvs2lean: polygons_2d.x_in_range
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF (p2`y - p1`y < 0)
--   THEN (p2`y - p1`y) * (p`x - p1`x) > (p2`x - p1`x) * (p`y - p1`y)
-- ELSE (p2`y - p1`y) * (p`x - p1`x) < (p2`x - p1`x) * (p`y - p1`y)
-- ENDIF
axiom x_in_range {pvs_τ108 : Type*} {pvs_τ109 : Type*} {pvs_τ110 : Type*} : pvs_τ108 → pvs_τ109 → pvs_τ110 → Prop

-- pvs2lean: polygons_2d.y_in_range
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (p1`y <= p`y AND p`y < p2`y) OR (p2`y <= p`y AND p`y < p1`y)
axiom y_in_range {pvs_τ111 : Type*} {pvs_τ112 : Type*} {pvs_τ113 : Type*} : pvs_τ111 → pvs_τ112 → pvs_τ113 → Prop

-- pvs2lean: polygons_2d.y_approx_in_range
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (p1`y + eps <= p`y AND p`y < p2`y - eps) OR
--  (p2`y + eps <= p`y AND p`y < p1`y - eps)
noncomputable def y_approx_in_range : sorry := by
  sorry  -- pvs2lean: definition stub for (p1`y + eps <= p`y AND p`y < p2`y - eps) OR  (p2`y + eps <= p`y AND p`y < p1`y -

-- pvs2lean: polygons_2d.is_right_point_ray_intersection?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: y_in_range(point, segment`p1, segment`p2) AND
--  x_in_range(point, segment`p1, segment`p2)
axiom is_right_point_ray_intersection_p {pvs_τ61 : Type*} {pvs_τ62 : Type*} : pvs_τ61 → pvs_τ62 → Prop

-- pvs2lean: polygons_2d.is_point_in_polygon_recursive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET segment: segment_2d = edges_of_polygon(polygon)(index) IN
--   IF (index = polygon`num_vertices - 1)
--     THEN is_inside XOR is_right_point_ray_intersection?(point, segment)
--   ELSE is_point_in_polygon
axiom is_point_in_polygon_recursive_p {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} : pvs_τ51 → pvs_τ52 → pvs_τ53 → pvs_τ54 → Prop

-- pvs2lean: polygons_2d.is_point_in_polygon_recursive_negation
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, point: point_2d,
--         index: below(polygon`num_vertices)):
--   is_point_in_polygon_recursive?(polygon, point, index, FALSE) =
--    NOT is_point_in_polygon_recursive?(polygo
theorem is_point_in_polygon_recursive_negation : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (point : ℝ × ℝ) (index : {s : {i : ℝ // i ≥ 0} // s < polygon.num_vertices}), is_point_in_polygon_recursive_p polygon point index False = ¬ is_point_in_polygon_recursive_p polygon point index True := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.is_point_on_polygon_perimeter?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (e: (edge_of_polygon?(polygon))): is_point_on_segment?(point, e)
axiom is_point_on_polygon_perimeter_p {pvs_τ55 : Type*} {pvs_τ56 : Type*} : pvs_τ55 → pvs_τ56 → Prop

-- pvs2lean: polygons_2d.is_point_in_polygon_inclusive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF is_point_on_polygon_perimeter?(polygon)(point) THEN TRUE
-- ELSE is_point_in_polygon_recursive?(polygon, point, 0, FALSE)
-- ENDIF
axiom is_point_in_polygon_inclusive_p {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ49 → pvs_τ50 → Prop

-- pvs2lean: polygons_2d.is_point_in_polygon_exclusive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF is_point_on_polygon_perimeter?(polygon)(point) THEN FALSE
-- ELSE is_point_in_polygon_recursive?(polygon, point, 0, FALSE)
-- ENDIF
axiom is_point_in_polygon_exclusive_p {pvs_τ47 : Type*} {pvs_τ48 : Type*} : pvs_τ47 → pvs_τ48 → Prop

-- pvs2lean: polygons_2d.center_of_diamond_is_inside
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET ctr = (# x := 0, y := 0 #) IN
--   is_point_in_polygon_exclusive?(example_diamond)(ctr)
theorem center_of_diamond_is_inside : fun (ctr : {x : {s : {i : ℝ // i ≥ 0} // s < exp2 8} // even_p x} × {x : {s : {i : ℝ // i ≥ 0} // s < exp2 8} // even_p x}) => is_point_in_polygon_exclusive_p example_diamond ctr (# x := 0) (y := 0 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.outside_left_of_diamond_is_outside
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET pt = (# x := -2, y := 0 #) IN
--   NOT is_point_in_polygon_exclusive?(example_diamond)(pt)
theorem outside_left_of_diamond_is_outside : fun (pt : {x : {k : ℝ // -exp2 7 ≤ k ∧ k ≤ exp2 7 - 1} // x < 0 ∧ even_p x} × {x : {s : {i : ℝ // i ≥ 0} // s < exp2 8} // even_p x}) => ¬(is_point_in_polygon_exclusive_p example_diamond pt) (# x := -2) (y := 0 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.above_center_of_diamond_is_inside
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET pt = (# x := 0, y := 0.5 #) IN
--   is_point_in_polygon_exclusive?(example_diamond)(pt)
theorem above_center_of_diamond_is_inside : fun (pt : {x : {s : {i : ℝ // i ≥ 0} // s < exp2 8} // even_p x} × {r : {r : ℝ // r ≥ 0} // r > 0}) => is_point_in_polygon_exclusive_p example_diamond pt (# x := 0) (y := 0.5 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.above_outside_left_of_diamond_is_outside
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET pt = (# x := -2, y := 0.5 #) IN
--   NOT is_point_in_polygon_exclusive?(example_diamond)(pt)
theorem above_outside_left_of_diamond_is_outside : fun (pt : {x : {k : ℝ // -exp2 7 ≤ k ∧ k ≤ exp2 7 - 1} // x < 0 ∧ even_p x} × {r : {r : ℝ // r ≥ 0} // r > 0}) => ¬(is_point_in_polygon_exclusive_p example_diamond pt) (# x := -2) (y := 0.5 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.way_above_diamond_is_outside
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET pt = (# x := 0, y := 50 #) IN
--   NOT is_point_in_polygon_exclusive?(example_diamond)(pt)
theorem way_above_diamond_is_outside : fun (pt : {x : {s : {i : ℝ // i ≥ 0} // s < exp2 8} // even_p x} × {x : {s : {i : ℝ // i ≥ 0} // s < exp2 8} // x > 0 ∧ even_p x}) => ¬(is_point_in_polygon_exclusive_p example_diamond pt) (# x := 0) (y := 50 #) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.is_polygon_inside_polygon?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: (vertex?(polygon2))): is_point_in_polygon_inclusive?(polygon1)(v)
axiom is_polygon_inside_polygon_p {pvs_τ59 : Type*} {pvs_τ60 : Type*} : pvs_τ59 → pvs_τ60 → Prop

-- pvs2lean: polygons_2d.segment_enters_polygon?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (pt: (is_point_on_segment?(segment))):
--   is_point_in_polygon_exclusive?(polygon)(pt)
axiom segment_enters_polygon_p {pvs_τ87 : Type*} {pvs_τ88 : Type*} : pvs_τ87 → pvs_τ88 → Prop

-- pvs2lean: polygons_2d.segment_enters_polygon_alt?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: is_point_in_polygon_exclusive?(polygon)(segment`p1) OR
--  is_point_in_polygon_exclusive?(polygon)(segment`p2) OR
--   EXISTS (e: (edge_of_polygon?(polygon))):
--     are_segments_strictly_intersecting?(e)(seg
noncomputable def segment_enters_polygon_alt_p : sorry := by
  sorry  -- pvs2lean: definition stub for is_point_in_polygon_exclusive?(polygon)(segment`p1) OR  is_point_in_polygon_excl

-- pvs2lean: polygons_2d.polygons_are_inside_themselves
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d): is_polygon_inside_polygon?(polygon, polygon)
theorem polygons_are_inside_themselves : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), is_polygon_inside_polygon_p polygon polygon := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.polygon_intersect?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT (is_polygon_inside_polygon?(A, B) OR is_polygon_inside_polygon?(B, A))
--  AND
--  EXISTS (point: vector_2d):
--    is_point_in_polygon_exclusive?(A)(point) AND
--     is_point_in_polygon_exclusive?(B)(point)
noncomputable def polygon_intersect_p : sorry := by
  sorry  -- pvs2lean: definition stub for NOT (is_polygon_inside_polygon?(A, B) OR is_polygon_inside_polygon?(B, A))  AND 

-- pvs2lean: polygons_2d.polygon_intersect_invalid?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT (is_polygon_inside_polygon?(A, B) OR is_polygon_inside_polygon?(B, A))
--  AND
--  ((EXISTS (vb: (vertex?(B))): is_point_in_polygon_exclusive?(A)(vb)) OR
--    (EXISTS (va: (vertex?(A))): is_point_in_polyg
noncomputable def polygon_intersect_invalid_p : sorry := by
  sorry  -- pvs2lean: definition stub for NOT (is_polygon_inside_polygon?(A, B) OR is_polygon_inside_polygon?(B, A))  AND 

-- pvs2lean: polygons_2d.polygon_intersect_alt?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (ea: (edge_of_polygon?(A))): segment_enters_polygon?(B, ea)
noncomputable def polygon_intersect_alt_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS (ea: (edge_of_polygon?(A))): segment_enters_polygon?(B, ea)

-- pvs2lean: polygons_2d.insert_vertex
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# num_vertices := A`num_vertices + 1,
--    vertices
--      := LAMBDA (n: upto(A`num_vertices)):
--           IF n < i THEN A`vertices(n)
--           ELSIF n = i THEN p
--           ELSE A`vertices(n - 1)
--        
axiom insert_vertex {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} : pvs_τ36 → pvs_τ37 → pvs_τ38 → Prop

-- pvs2lean: polygons_2d.insert_vertex_seq_pre
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: {k: below(seq`length) | k >= j}): nonvertex?(A)(seq`seq(i))
axiom insert_vertex_seq_pre {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} : pvs_τ44 → pvs_τ45 → pvs_τ46 → Prop

-- pvs2lean: polygons_2d.insert_vertex_seq
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF seq`length = 0 THEN A
-- ELSIF j = seq`length - 1 THEN insert_vertex(A, i, seq`seq(j))
-- ELSE insert_vertex_seq(insert_vertex(A, i, seq`seq(j)), i + 1, seq, j + 1)
-- ENDIF
axiom insert_vertex_seq {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} : pvs_τ39 → pvs_τ40 → pvs_τ41 → pvs_τ42 → pvs_τ43

-- pvs2lean: polygons_2d.segment_outside_inclusive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (point: {p: point_2d | is_point_on_segment?(p, s)}):
--   NOT is_point_in_polygon_inclusive?(A)(point)
axiom segment_outside_inclusive_p {pvs_τ96 : Type*} {pvs_τ97 : Type*} : pvs_τ96 → pvs_τ97 → Prop

-- pvs2lean: polygons_2d.polygon_segment_is_not_outside_inclusive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, edge: (edge_of_polygon?(polygon))):
--   NOT segment_outside_inclusive?(polygon)(edge)
theorem polygon_segment_is_not_outside_inclusive : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (edge : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬(segment_outside_inclusive_p polygon edge) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.segment_outside_exclusive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (point: {p: point_2d | is_point_on_segment?(p, s)}):
--   NOT is_point_in_polygon_exclusive?(A)(point)
axiom segment_outside_exclusive_p {pvs_τ94 : Type*} {pvs_τ95 : Type*} : pvs_τ94 → pvs_τ95 → Prop

-- pvs2lean: polygons_2d.polygon_segment_is_outside_exclusive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, edge: (edge_of_polygon?(polygon))):
--   segment_outside_exclusive?(polygon)(edge)
theorem polygon_segment_is_outside_exclusive : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (edge : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), segment_outside_exclusive_p polygon edge := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.segment_inside_inclusive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (point: {p: point_2d | is_point_on_segment?(p, s)}):
--   is_point_in_polygon_inclusive?(A)(point)
axiom segment_inside_inclusive_p {pvs_τ91 : Type*} {pvs_τ92 : Type*} : pvs_τ91 → pvs_τ92 → Prop

-- pvs2lean: polygons_2d.polygon_segment_is_inside_inclusive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, edge: (edge_of_polygon?(polygon))):
--   segment_inside_inclusive?(polygon)(edge)
theorem polygon_segment_is_inside_inclusive : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (edge : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), segment_inside_inclusive_p polygon edge := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.segment_inside_exclusive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (point: {p: point_2d | is_point_on_segment?(p, s)}):
--   is_point_in_polygon_exclusive?(A)(point)
axiom segment_inside_exclusive_p {pvs_τ89 : Type*} {pvs_τ90 : Type*} : pvs_τ89 → pvs_τ90 → Prop

-- pvs2lean: polygons_2d.polygon_segment_is_not_inside_exclusive
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, edge: (edge_of_polygon?(polygon))):
--   NOT segment_inside_exclusive?(polygon)(edge)
theorem polygon_segment_is_not_inside_exclusive : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (edge : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), ¬(segment_inside_exclusive_p polygon edge) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.path_outside_inclusive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: below(p`num_vertices - 1)):
--   LET s: segment_2d = (# p1 := p`vertices(n), p2 := p`vertices(n + 1) #) IN
--     segment_outside_inclusive?(A)(s)
noncomputable def path_outside_inclusive_p : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL (n: below(p`num_vertices - 1)):   LET s: segment_2d = (# p1 := p`vertices

-- pvs2lean: polygons_2d.path_inside_exclusive?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: below(p`num_vertices - 1)):
--   LET s: segment_2d = (# p1 := p`vertices(n), p2 := p`vertices(n + 1) #) IN
--     segment_inside_exclusive?(A)(s)
noncomputable def path_inside_exclusive_p : sorry := by
  sorry  -- pvs2lean: definition stub for FORALL (n: below(p`num_vertices - 1)):   LET s: segment_2d = (# p1 := p`vertices

-- pvs2lean: polygons_2d.point_on_polygon_perimeter?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (e: (edge_of_polygon?(G))): point_on_segment?(p, e)
axiom point_on_polygon_perimeter_p {pvs_τ74 : Type*} {pvs_τ75 : Type*} : pvs_τ74 → pvs_τ75 → Prop

-- pvs2lean: polygons_2d.point_on_polygon_perimeter?_eq_is_point_on_polygon_perimeter?
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polygon: polygon_2d, point: point_2d):
--   point_on_polygon_perimeter?(polygon)(point) IFF
--    is_point_on_polygon_perimeter?(polygon)(point)
theorem point_on_polygon_perimeter_p_eq_is_point_on_polygon_perimeter_p : ∀ (polygon : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (point : ℝ × ℝ), point_on_polygon_perimeter_p polygon(point) ↔ is_point_on_polygon_perimeter_p polygon(point) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_index_any_rec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF p = G`vertices(next_index(G, i)) THEN next_index(G, i)
-- ELSIF point_on_segment?(p, edges_of_polygon(G)(i)) THEN i
-- ELSIF i = 0 THEN -1
-- ELSE find_index_any_rec(G, p, i - 1)
-- ENDIF
axiom find_index_any_rec {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ33 → pvs_τ34 → pvs_τ35 → Prop

-- pvs2lean: polygons_2d.find_index_any
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: find_index_any_rec(G, p, G`num_vertices - 1)
axiom find_index_any {pvs_τ31 : Type*} {pvs_τ32 : Type*} : pvs_τ31 → pvs_τ32 → Prop

-- pvs2lean: polygons_2d.perimeter_find_index_any
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, p: point_2d):
--   point_on_polygon_perimeter?(G)(p) IFF find_index_any(G, p) >= 0
theorem perimeter_find_index_any : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), point_on_polygon_perimeter_p G(p) ↔ find_index_any G p ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.point_on_find_index_segment
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, p: point_2d):
--   find_index_any(G, p) >= 0 IMPLIES
--    point_on_segment?(p, edges_of_polygon(G)(find_index_any(G, p))) AND
--     p /= G`vertices(next_index(G, find_index_any(G, p)))
theorem point_on_find_index_segment : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ), find_index_any G p ≥ 0 → point_on_segment_p p (edges_of_polygon G(find_index_any G p)) ∧
 p ≠ G.vertices (next_index G (find_index_any G p)) := by
  -- === TCCs for point_on_find_index_segment ===
  -- TCC: point_on_find_index_segment_TCC1
  -- Statement: FORALL (G: polygon_2d, p: point_2d):
  --   find_index_any(G, p) >= 0 IMPLIES find_index_any(G, p) < G`num_vertices
  have point_on_find_index_segment_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_index_any_segment_rec
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: point_2d, n: below(G`num_vertices),
--         k: upto(n)):
--   point_on_segment?(p, edges_of_polygon(G)(n - k)) IMPLIES
--    find_index_any_rec(G, p, n) =
--     IF p = G`verti
theorem find_index_any_segment_rec : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (n : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}) (k : {s : {i : ℝ // i ≥ 0} // s ≤ n}), point_on_segment_p p (edges_of_polygon G(n - k)) → find_index_any_rec G p n =
 if p = G.vertices (next_index G (n - k)) then next_index G (n - k)
 else n - k := by
  -- === TCCs for find_index_any_segment_rec ===
  -- TCC: find_index_any_segment_rec_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, n: below(G`num_vertices), k: upto(n)):
  --   n - k >= 0 AND n - k < G`num_vertices
  have find_index_any_segment_rec_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_index_any_segment
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, p: point_2d, i: below(G`num_vertices)):
--   point_on_segment?(p, edges_of_polygon(G)(i)) AND
--    p /= G`vertices(next_index(G, i))
--    IMPLIES find_index_any(G, p) = i
theorem find_index_any_segment : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), point_on_segment_p p (edges_of_polygon G(i)) ∧
 p ≠ G.vertices (next_index G i) → find_index_any G p = i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.find_index_any_self
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, i: below(G`num_vertices)):
--   find_index_any(G, G`vertices(i)) = i
theorem find_index_any_self : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), find_index_any G (G.vertices i) = i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.point_on_segment_rem_endpoints
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, p: point_2d, i: nat):
--   LET j = G`num_vertices + i IN
--     point_on_segment?(p,
--                       (# p1 := G`vertices(rem(G`num_vertices)(j)),
--                          p2 := 
theorem point_on_segment_rem_endpoints : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (i : {i : ℝ // i ≥ 0}), fun (j : {i : {i : ℝ // i ≥ 0} // i > 0}) => point_on_segment_p p ((# p1 := G.vertices (rem G.num_vertices(j)),
                     p2 := G.vertices (rem G.num_vertices(1 + j)) #)) → point_on_segment_p p (edges_of_polygon G(rem G.num_vertices(i))) ∧
 point_on_polygon_perimeter_p G(p) (G.num_vertices + i) := by
  -- === TCCs for point_on_segment_rem_endpoints ===
  -- TCC: point_on_segment_rem_endpoints_TCC1
  -- Statement: FORALL (G: polygon_2d, i: nat, j: posint):
  --   j = G`num_vertices + i IMPLIES
  --    G`vertices(rem(G`num_vertices)(1 + j)) /=
  --     G`vertices(rem(G`num_vertices)(j))
  have point_on_segment_rem_endpoints_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: polygons_2d.point_on_segment_rem_endpoints_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: polygon_2d, p: point_2d, i, j: nat):
--   point_on_segment?(p,
--                     (# p1 := G`vertices(rem(G`num_vertices)(i + j)),
--                        p2 := G`vertices(rem(G`num_vertices)(
theorem point_on_segment_rem_endpoints_2 : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (p : ℝ × ℝ) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), point_on_segment_p p ((# p1 := G.vertices (rem G.num_vertices(i + j)),
                     p2 := G.vertices (rem G.num_vertices(1 + i + j)) #)) → point_on_segment_p p (edges_of_polygon G(rem G.num_vertices(i + j))) ∧
 point_on_polygon_perimeter_p G(p) := by
  -- === TCCs for point_on_segment_rem_endpoints_2 ===
  -- TCC: point_on_segment_rem_endpoints_2_TCC1
  -- Statement: FORALL (G: polygon_2d, i, j: nat):
  --   G`vertices(rem(G`num_vertices)(1 + i + j)) /=
  --    G`vertices(rem(G`num_vertices)(i + j))
  have point_on_segment_rem_endpoints_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.polygons_2d