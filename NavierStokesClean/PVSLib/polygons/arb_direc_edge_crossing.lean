-- Auto-generated by pvs2lean/translator.py
-- PVS theory: arb_direc_edge_crossing
-- PVSLib library: polygons
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: arb_direc_edge_crossing

Auto-generated stubs from PVSLib theory `arb_direc_edge_crossing`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.arb_direc_edge_crossing

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (add : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ14 : Type*} {pvs_τ15 : Type*} (below : pvs_τ14 → pvs_τ15)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (card : pvs_τ19 → pvs_τ20)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (cross : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (difference : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} (edge_of_polygon_p : pvs_τ32 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (member : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (min : pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (next_index : pvs_τ41 → pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} (norm : pvs_τ44 → Prop)
variable {pvs_τ48 : Type*} (odd_p : pvs_τ48 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (parallel_p : pvs_τ56 → pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} (point_on_arb_line_p : pvs_τ58 → pvs_τ59 → pvs_τ60 → Prop)
variable {pvs_τ61 : Type*} {pvs_τ62 : Type*} {pvs_τ63 : Type*} (point_on_arb_ray_p : pvs_τ61 → pvs_τ62 → pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} (point_on_polygon_perimeter_p : pvs_τ64 → pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} (point_on_segment_p : pvs_τ66 → pvs_τ67 → Prop)
variable {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} (prev_index : pvs_τ68 → pvs_τ69 → pvs_τ70)
variable {pvs_τ74 : Type*} {pvs_τ75 : Type*} (subset_p : pvs_τ74 → pvs_τ75 → Prop)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} (union : pvs_τ76 → pvs_τ77 → pvs_τ78)
variable {pvs_τ79 : Type*} {pvs_τ80 : Type*} (vertex_p : pvs_τ79 → pvs_τ80 → Prop)
variable {pvs_τ81 : Type*} (vertices : pvs_τ81 → Prop)

-- pvs2lean: arb_direc_edge_crossing.adjacent_edges_counted?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (i: below(G`num_vertices)):
--   p = G`vertices(i) AND
--    LET prev = G`vertices(prev_index(G, i)),
--        next = G`vertices(next_index(G, i))
--      IN
--      cross(v, prev - p) > 0 AND cross(v, next 
axiom adjacent_edges_counted_p {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ4 → pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: arb_direc_edge_crossing.counted_crossing_arb?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF vertex?(G)(p) THEN adjacent_edges_counted?(p, v, G)
-- ELSE EXISTS (s: (edge_of_polygon?(G))):
--        point_on_segment?(p, s) AND NOT parallel?(s`p2 - s`p1, v)
-- ENDIF
axiom counted_crossing_arb_p {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ21 → pvs_τ22 → pvs_τ23 → Prop

-- pvs2lean: arb_direc_edge_crossing.arb_crossing_set
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {p: point_2d |
--    point_on_arb_ray?(p, r, v) AND counted_crossing_arb?(v, G)(p)}
axiom arb_crossing_set {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13 → Prop

-- pvs2lean: arb_direc_edge_crossing.set_before_p
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {u: point_2d | S(u) AND norm(u - r) < norm(p - r)}
axiom set_before_p {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} : pvs_τ71 → pvs_τ72 → pvs_τ73 → Prop

-- pvs2lean: arb_direc_edge_crossing.norms_from_points
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {d: real | EXISTS (p: point_2d): S(p) AND d = norm(p - r)}
axiom norms_from_points {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} : pvs_τ45 → pvs_τ46 → pvs_τ47

-- pvs2lean: arb_direc_edge_crossing.point_in_polygon_arb?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT point_on_polygon_perimeter?(G)(p) AND
--  odd?(card(arb_crossing_set(p, v, G)))
noncomputable def point_in_polygon_arb_p : sorry := by
  sorry  -- pvs2lean: definition stub for NOT point_on_polygon_perimeter?(G)(p) AND  odd?(card(arb_crossing_set(p, v, G)))

-- pvs2lean: arb_direc_edge_crossing.points_on_ray_diff_norms
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q, r: point_2d, v: Normalized):
--   point_on_arb_ray?(r, p, v) AND
--    point_on_arb_ray?(r, q, v) AND point_on_arb_ray?(q, p, v)
--    IMPLIES norm(r - p) >= norm(q - p)
theorem points_on_ray_diff_norms : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}), point_on_arb_ray_p r p v ∧
 point_on_arb_ray_p r q v ∧ point_on_arb_ray_p q p v → norm (r - p) ≥ norm (q - p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.points_on_ray_norms_on_ray
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q, r: point_2d, v: Normalized):
--   point_on_arb_ray?(r, p, v) AND
--    point_on_arb_ray?(q, p, v) AND norm(r - p) >= norm(q - p)
--    IMPLIES point_on_arb_ray?(r, q, v)
theorem points_on_ray_norms_on_ray : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}), point_on_arb_ray_p r p v ∧
 point_on_arb_ray_p q p v ∧ norm (r - p) ≥ norm (q - p) → point_on_arb_ray_p r q v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.counted_crossing_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_arb?(v, G)(p) IMPLIES point_on_polygon_perimeter?(G)(p)
theorem counted_crossing_on_perimeter : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_arb_p v G(p) → point_on_polygon_perimeter_p G(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.arb_crossings_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   point_on_arb_ray?(q, p, v) IMPLIES
--    subset?(arb_crossing_set(q, v, G), arb_crossing_set(p, v, G))
theorem arb_crossings_subset : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_arb_ray_p q p v → subset_p (arb_crossing_set q v G) (arb_crossing_set p v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.arb_crossings_before_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   point_on_arb_ray?(q, p, v) IMPLIES
--    LET R = arb_crossing_set(p, v, G) IN
--      arb_crossing_set(q, v, G) = difference(R, set_before_p(R
theorem arb_crossings_before_diff : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_arb_ray_p q p v → (let R := arb_crossing_set p v G; arb_crossing_set q v G = difference R (set_before_p R q p)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.member_arb_crossings_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   member(q, arb_crossing_set(p, v, G)) IMPLIES
--    point_on_polygon_perimeter?(G)(q)
theorem member_arb_crossings_on_perimeter : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), member q (arb_crossing_set p v G) → point_on_polygon_perimeter_p G(q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.arb_crossings_min
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   LET R = arb_crossing_set(p, v, G) IN
--     (FORALL (u: (R)): norm(u - p) >= norm(q - p)) AND
--      point_on_arb_ray?(q, p, v)
--      IMPLIES 
theorem arb_crossings_min : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (R : ℝ × ℝ → Bool) => (∀ (u : (R)), norm (u - p) ≥ norm (q - p)) ∧ point_on_arb_ray_p q p v → arb_crossing_set q v G = R (arb_crossing_set p v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.arb_crossings_max
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   LET R = arb_crossing_set(p, v, G) IN
--     (FORALL (u: (R)): norm(u - p) < norm(q - p)) AND
--      point_on_arb_ray?(q, p, v)
--      IMPLIES a
theorem arb_crossings_max : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), fun (R : ℝ × ℝ → Bool) => (∀ (u : (R)), norm (u - p) < norm (q - p)) ∧ point_on_arb_ray_p q p v → arb_crossing_set q v G = emptyset (arb_crossing_set p v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.before_arb_crossings_empty
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   point_on_arb_ray?(q, p, v) AND
--    set_before_p(arb_crossing_set(p, v, G), q, p) = emptyset
--    IMPLIES arb_crossing_set(q, v, G) = arb_cr
theorem before_arb_crossings_empty : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_arb_ray_p q p v ∧
 set_before_p (arb_crossing_set p v G) q p = emptyset → arb_crossing_set q v G = arb_crossing_set p v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.one_adjacent_edge_not_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   adjacent_edges_counted?(p, v, G) IMPLIES
--    EXISTS (i: below(G`num_vertices)):
--      p = G`vertices(i) AND
--       (NOT parallel?(G`vertices(p
theorem one_adjacent_edge_not_parallel : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), adjacent_edges_counted_p p v G → ∃ (i : Finset.range G.num_vertices), p = G.vertices i ∧
   (¬ parallel_p (G.vertices (prev_index G i) - p) v ∨
     ¬ parallel_p (G.vertices (next_index G i) - p) v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.next_arb_intersection?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: point_on_arb_ray?(q, p, v) AND
--  point_on_polygon_perimeter?(G)(q) AND
--   FORALL (u: point_2d):
--     point_on_arb_ray?(u, p, v) AND norm(u - p) < norm(q - p) IMPLIES
--      NOT point_on_polygon_perimeter?(
noncomputable def next_arb_intersection_p : sorry := by
  sorry  -- pvs2lean: definition stub for point_on_arb_ray?(q, p, v) AND  point_on_polygon_perimeter?(G)(q) AND   FORALL (

-- pvs2lean: arb_direc_edge_crossing.key_arb_intersections
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {d: real |
--    EXISTS (s: (edge_of_polygon?(G))), (q: point_2d):
--           point_on_segment?(q, s) AND point_on_arb_ray?(q, p, v)
--       AND (NOT parallel?(s`p2 - s`p1, v)) AND (p /= q)
--       AND (d = n
axiom key_arb_intersections {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} : pvs_τ33 → pvs_τ34 → pvs_τ35 → pvs_τ36 → Prop

-- pvs2lean: arb_direc_edge_crossing.with_real_offset
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {d: real | EXISTS (x: (S)): d = x + r}
axiom with_real_offset {pvs_τ82 : Type*} {pvs_τ83 : Type*} {pvs_τ84 : Type*} : pvs_τ82 → pvs_τ83 → pvs_τ84

-- pvs2lean: arb_direc_edge_crossing.key_arb_intersections_nonneg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: real):
--   key_arb_intersections(p, v, G)(r) IMPLIES r >= 0
theorem key_arb_intersections_nonneg : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : ℝ), key_arb_intersections p v G(r) → r ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.key_arb_intersections_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   point_on_arb_ray?(q, p, v) IMPLIES
--    subset?(with_real_offset(key_arb_intersections(q, v, G), norm(q - p)),
--            key_arb_intersec
theorem key_arb_intersections_subset : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), point_on_arb_ray_p q p v → subset_p (with_real_offset (key_arb_intersections q v G) (norm (q - p))) (key_arb_intersections p v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.only_parallel_edges?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: (edge_of_polygon?(G))):
--   point_on_segment?(p, e) IMPLIES parallel?(e`p2 - e`p1, v)
axiom only_parallel_edges_p {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} : pvs_τ49 → pvs_τ50 → pvs_τ51 → Prop

-- pvs2lean: arb_direc_edge_crossing.on_parallel_not_adj_counted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   only_parallel_edges?(p, v, G) IMPLIES NOT adjacent_edges_counted?(p, v, G)
theorem on_parallel_not_adj_counted : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), only_parallel_edges_p p v G → ¬ adjacent_edges_counted_p p v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.on_parallel_not_counted_crossing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   only_parallel_edges?(p, v, G) IMPLIES NOT counted_crossing_arb?(v, G)(p)
theorem on_parallel_not_counted_crossing : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), only_parallel_edges_p p v G → ¬ counted_crossing_arb_p v G(p) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.parallel_on_arb_line
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q, r: point_2d, v: Normalized):
--   parallel?(q - r, v) IMPLIES point_on_arb_line?(q, r, v)
theorem parallel_on_arb_line : ∀ (q : ℝ × ℝ) (r : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}), parallel_p (q - r) v → point_on_arb_line_p q r v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.parallel_diff_norm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (q, r: point_2d, v: Normalized):
--   parallel?(q - r, v) IMPLIES
--    q - r = norm(q - r) * v OR q - r = -norm(q - r) * v
theorem parallel_diff_norm : ∀ (q : ℝ × ℝ) (r : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}), parallel_p (q - r) v → q - r = norm (q - r) * v ∨ q - r = -norm (q - r) * v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.parallel_vectors_diff_segment_endpoints
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, d: nzreal, v: Normalized, e: segment_2d):
--   parallel?(e`p2 - e`p1, v) AND
--    e`p2 /= p AND point_on_segment?(p + d * v, e)
--    IMPLIES parallel?(e`p2 - p, v)
theorem parallel_vectors_diff_segment_endpoints : ∀ (p : ℝ × ℝ) (d : {r : ℝ // r ≠ 0}) (v : {v : ℝ × ℝ // norm v = 1}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), parallel_p (e.p2 - e.p1) v ∧ e.p2 ≠ p ∧ point_on_segment_p (p + d * v) e → parallel_p (e.p2 - p) v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.parallel_point_diff_norm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, r: nzreal, v: Normalized, e: segment_2d):
--   parallel?(e`p2 - e`p1, v) AND
--    e`p2 /= p AND e`p1 /= p AND point_on_segment?(p + r * v, e)
--    IMPLIES
--    (e`p1 - p = norm(e`p1 - p) *
theorem parallel_point_diff_norm : ∀ (p : ℝ × ℝ) (r : {r : ℝ // r ≠ 0}) (v : {v : ℝ × ℝ // norm v = 1}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), parallel_p (e.p2 - e.p1) v ∧
 e.p2 ≠ p ∧ e.p1 ≠ p ∧ point_on_segment_p (p + r * v) e → (e.p1 - p = norm (e.p1 - p) * v ∨ e.p1 - p = -norm (e.p1 - p) * v) ∧
 (e.p2 - p = norm (e.p2 - p) * v ∨ e.p2 - p = -norm (e.p2 - p) * v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.point_not_on_parallel_segment
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, r: real, v: Normalized, e: segment_2d):
--   0 < r AND
--    r < min(norm(e`p1 - p), norm(e`p2 - p)) AND
--     parallel?(e`p2 - e`p1, v) AND NOT point_on_segment?(p, e)
--    IMPLIES NOT poi
theorem point_not_on_parallel_segment : ∀ (p : ℝ × ℝ) (r : ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})), 0 < r ∧
 r < min (norm (e.p1 - p)) (norm (e.p2 - p)) ∧
  parallel_p (e.p2 - e.p1) v ∧ ¬ point_on_segment_p p e → ¬ point_on_segment_p (p + r * v) e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.below_min_key_arb_not_on
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: real):
--   NOT empty?[real](key_arb_intersections(p, v, G)) AND
--    0 < r AND r < min[real, <=](key_arb_intersections(p, v, G))
--    IMPLIES onl
theorem below_min_key_arb_not_on : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : ℝ), ¬ empty_p (key_arb_intersections p v G) ∧
 0 < r ∧ r < min (key_arb_intersections p v G) → only_parallel_edges_p (p + r * v) v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.below_min_key_arb_crossing
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: real):
--   NOT empty?[real](key_arb_intersections(p, v, G)) AND
--    0 < r AND
--     r < min[real, <=](key_arb_intersections(p, v, G)) AND
--      N
theorem below_min_key_arb_crossing : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : ℝ), ¬ empty_p (key_arb_intersections p v G) ∧
 0 < r ∧
  r < min (key_arb_intersections p v G) ∧
   ¬ point_on_polygon_perimeter_p G(p) → arb_crossing_set (p + r * v) v G = arb_crossing_set p v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.below_min_key_same_crossing_set
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: real):
--   LET kai = key_arb_intersections(p, v, G) IN
--     NOT empty?[real](kai) AND 0 < r AND r < min[real, <=](kai) IMPLIES
--      arb_crossi
theorem below_min_key_same_crossing_set : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : ℝ), fun (kai : ℝ → Bool) => ¬ empty_p kai ∧ 0 < r ∧ r < min kai → arb_crossing_set (p + r * v) v G =
 arb_crossing_set (p + min kai * v) v G (key_arb_intersections p v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.key_intersections_subset_arb_crossings
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   NOT member(p, arb_crossing_set(p, v, G)) IMPLIES
--    subset?(norms_from_points(arb_crossing_set(p, v, G), p),
--            key_arb_intersectio
theorem key_intersections_subset_arb_crossings : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ member p (arb_crossing_set p v G) → subset_p (norms_from_points (arb_crossing_set p v G) p) (key_arb_intersections p v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.key_intersections_empty_arb_crossings
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   NOT member(p, arb_crossing_set(p, v, G)) AND
--    empty?[real](key_arb_intersections(p, v, G))
--    IMPLIES empty?[point_2d](arb_crossing_set(p
theorem key_intersections_empty_arb_crossings : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ member p (arb_crossing_set p v G) ∧
 empty_p (key_arb_intersections p v G) → empty_p (arb_crossing_set p v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_key_arb_only_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   empty?[real](key_arb_intersections(p, v, G)) IMPLIES
--    FORALL (d: posreal): only_parallel_edges?(p + d * v, v, G)
theorem empty_key_arb_only_parallel : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), empty_p (key_arb_intersections p v G) → ∀ (d : {x : ℝ // 0 < x}), only_parallel_edges_p (p + d * v) v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_key_arb_crossings_emptyset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: posreal):
--   empty?[real](key_arb_intersections(p, v, G)) IMPLIES
--    arb_crossing_set(p + r * v, v, G) = emptyset
theorem empty_key_arb_crossings_emptyset : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : {x : {x : ℝ // x ≥ 0} // x > 0}), empty_p (key_arb_intersections p v G) → arb_crossing_set (p + r * v) v G = emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_key_arb_before_empty_crossings
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: posreal):
--   NOT point_on_polygon_perimeter?(G)(p) AND
--    empty?[real](key_arb_intersections(p, v, G))
--    IMPLIES set_before_p(arb_crossing_
theorem empty_key_arb_before_empty_crossings : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : {x : {x : ℝ // x ≥ 0} // x > 0}), ¬ point_on_polygon_perimeter_p G(p) ∧
 empty_p (key_arb_intersections p v G) → set_before_p (arb_crossing_set p v G) (p + r * v) p = emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_key_arb_before_empty_crossings_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: posreal):
--   empty?[real](key_arb_intersections(p, -v, G)) IMPLIES
--    set_before_p(arb_crossing_set(p - r * v, v, G), p, p - r * v) = emptys
theorem empty_key_arb_before_empty_crossings_neg : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : {x : {x : ℝ // x ≥ 0} // x > 0}), empty_p (key_arb_intersections p -v G) → set_before_p (arb_crossing_set (p - r * v) v G) p (p - r * v) = emptyset := by
  -- === TCCs for empty_key_arb_before_empty_crossings_neg ===
  -- TCC: empty_key_arb_before_empty_crossings_neg_TCC1
  -- Statement: FORALL (v: Normalized): norm(-v) = 1
  have empty_key_arb_before_empty_crossings_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_key_arb_no_crossings
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: posreal):
--   NOT point_on_polygon_perimeter?(G)(p) AND
--    empty?[real](key_arb_intersections(p, v, G))
--    IMPLIES arb_crossing_set(p + r * v
theorem empty_key_arb_no_crossings : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : {x : {x : ℝ // x ≥ 0} // x > 0}), ¬ point_on_polygon_perimeter_p G(p) ∧
 empty_p (key_arb_intersections p v G) → arb_crossing_set (p + r * v) v G = arb_crossing_set p v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_key_arb_no_crossings_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: posreal):
--   empty?[real](key_arb_intersections(p, -v, G)) IMPLIES
--    arb_crossing_set(p - r * v, v, G) = arb_crossing_set(p, v, G)
theorem empty_key_arb_no_crossings_neg : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : {x : {x : ℝ // x ≥ 0} // x > 0}), empty_p (key_arb_intersections p -v G) → arb_crossing_set (p - r * v) v G = arb_crossing_set p v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_key_arb_no_crossings_add
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_arb?(v, G)(p) AND
--    empty?[real](key_arb_intersections(p, v, G))
--    IMPLIES arb_crossing_set(p, v, G) = add(p, emptyset)
theorem empty_key_arb_no_crossings_add : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_arb_p v G(p) ∧
 empty_p (key_arb_intersections p v G) → arb_crossing_set p v G = add p emptyset := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.card_add_crossing_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, c: posreal):
--   odd?(card(add(p, arb_crossing_set(p + c * v, v, G)))) OR
--    odd?(card(arb_crossing_set(p + c * v, v, G)))
theorem card_add_crossing_set : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (c : {x : {x : ℝ // x ≥ 0} // x > 0}), odd_p (card (add p (arb_crossing_set (p + c * v) v G))) ∨ odd_p (card (arb_crossing_set (p + c * v) v G)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.point_before_min_key_not_on
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d, r: real):
--        (NOT empty?[real](key_arb_intersections(p, v, G))) AND (0 < r)
--    AND (r < min[real, <=](key_arb_intersections(p, v, G)))
-- 
theorem point_before_min_key_not_on : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : ℝ), (¬ empty_p (key_arb_intersections p v G)) ∧ (0 < r)
 ∧ (r < min (key_arb_intersections p v G))
 ∧ point_on_arb_ray_p q p v ∧ (norm (q - p) ≤ r) ∧ (q ≠ p) → only_parallel_edges_p q v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.point_before_min_neg_key_not_on
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d, r: real):
--        (NOT empty?[real](key_arb_intersections(p, -v, G))) AND (0 < r)
--    AND (r < min[real, <=](key_arb_intersections(p, -v, G))
theorem point_before_min_neg_key_not_on : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : ℝ), (¬ empty_p (key_arb_intersections p -v G)) ∧ (0 < r)
 ∧ (r < min (key_arb_intersections p -v G))
 ∧ point_on_arb_ray_p p q v ∧ (norm (q - p) ≤ r) ∧ (q ≠ p) → only_parallel_edges_p q v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.bisect_vector
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: q + r - 2 * p
axiom bisect_vector {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → Prop

-- pvs2lean: arb_direc_edge_crossing.bisect_vector_nonzero
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q, r: point_2d):
--   q /= p AND r /= p AND NOT parallel?(q - p, r - p) IMPLIES
--    bisect_vector(p, q, r) /= zero
theorem bisect_vector_nonzero : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (r : ℝ × ℝ), q ≠ p ∧ r ≠ p ∧ ¬ parallel_p (q - p) (r - p) → bisect_vector p q r ≠ zero := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.counted_crossing_direction?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: point_on_polygon_perimeter?(G)(p) AND
--  IF vertex?(G)(p)
--    THEN EXISTS (i: below(G`num_vertices)):
--           p = G`vertices(i) AND
--            LET q = G`vertices(prev_index(G, i)),
--                r = G
axiom counted_crossing_direction_p {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: arb_direc_edge_crossing.bisect_not_parallel_prev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Nz_vector, G: simple_polygon_2d,
--         i: below(G`num_vertices)):
--   LET prev = G`vertices(prev_index(G, i)),
--       next = G`vertices(next_index(G, i))
--     IN
--     p = G`vertic
theorem bisect_not_parallel_prev : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // v ≠ zero}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (prev : ℝ × ℝ) => fun (next : ℝ × ℝ) => p = G.vertices i ∧
 v = bisect_vector p prev next ∧ ¬ parallel_p (prev - p) (next - p) → ¬ parallel_p (prev - p) v (G.vertices (next_index G i)) (G.vertices (prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.bisect_not_parallel_next
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Nz_vector, G: simple_polygon_2d,
--         i: below(G`num_vertices)):
--   LET prev = G`vertices(prev_index(G, i)),
--       next = G`vertices(next_index(G, i))
--     IN
--     p = G`vertic
theorem bisect_not_parallel_next : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // v ≠ zero}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (prev : ℝ × ℝ) => fun (next : ℝ × ℝ) => p = G.vertices i ∧
 v = bisect_vector p prev next ∧ ¬ parallel_p (prev - p) (next - p) → ¬ parallel_p (next - p) v (G.vertices (next_index G i)) (G.vertices (prev_index G i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.counted_direction_not_parallel
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(p, G)(v) IMPLIES
--    FORALL (e: (edge_of_polygon?(G))):
--      point_on_segment?(p, e) IMPLIES NOT parallel?(e`p2 
theorem counted_direction_not_parallel : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p p G(v) → ∀ (e : (edge_of_polygon_p G)), point_on_segment_p p e → ¬ parallel_p (e.p2 - e.p1) v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.bisect_not_parallel_cross_nonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: simple_polygon_2d, v: Normalized, i: below(G`num_vertices)):
--   LET p = G`vertices(i),
--       prev = G`vertices(prev_index(G, i)),
--       next = G`vertices(next_index(G, i)),
--       u = prev - 
theorem bisect_not_parallel_cross_nonzero : ∀ (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (v : {v : ℝ × ℝ // norm v = 1}) (i : {s : {i : ℝ // i ≥ 0} // s < G.num_vertices}), fun (p : ℝ × ℝ) => fun (prev : ℝ × ℝ) => fun (next : ℝ × ℝ) => fun (u : ℝ × ℝ) => fun (w : ℝ × ℝ) => ¬ parallel_p u w ∧ v = ^(bisect_vector p prev next) → cross u v > 0 ∧ cross w v < 0 ∨ cross u v < 0 ∧ cross w v > 0 (next - p) (prev - p) (G.vertices (next_index G i)) (G.vertices (prev_index G i)) (G.vertices i) := by
  -- === TCCs for bisect_not_parallel_cross_nonzero ===
  -- TCC: bisect_not_parallel_cross_nonzero_TCC1
  -- Statement: FORALL (G: simple_polygon_2d, v: Normalized, i: below(G`num_vertices),
  --         p: point_2d, prev: point_2d, next: point_2d, u: Vector, w: Vector):
  --        (p = G`vertices(i)) AND (prev = G`vertices(...
  have bisect_not_parallel_cross_nonzero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.key_arb_intersections_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   NOT empty?[real](key_arb_intersections(p, v, G)) IMPLIES
--    min[real, <=](key_arb_intersections(p, v, G)) > 0
theorem key_arb_intersections_pos : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ empty_p (key_arb_intersections p v G) → min (key_arb_intersections p v G) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.counted_key_arb_intersections_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(p, G)(v) AND
--    NOT empty?[real](key_arb_intersections(p, v, G))
--    IMPLIES min[real, <=](key_arb_intersections
theorem counted_key_arb_intersections_pos : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p p G(v) ∧
 ¬ empty_p (key_arb_intersections p v G) → min (key_arb_intersections p v G) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.counted_key_arb_intersections_reverse_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(p, G)(v) AND
--    NOT empty?[real](key_arb_intersections(p, -v, G))
--    IMPLIES min[real, <=](key_arb_intersection
theorem counted_key_arb_intersections_reverse_pos : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p p G(v) ∧
 ¬ empty_p (key_arb_intersections p -v G) → min (key_arb_intersections p -v G) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.crossing_direction_counted
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(q, G)(v) AND point_on_arb_ray?(q, p, v)
--    IMPLIES counted_crossing_arb?(v, G)(q)
theorem crossing_direction_counted : ∀ (p : ℝ × ℝ) (q : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p q G(v) ∧ point_on_arb_ray_p q p v → counted_crossing_arb_p v G(q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.crossing_direction_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   counted_crossing_direction?(p, G)(v) IMPLIES
--    counted_crossing_direction?(p, G)(-v)
theorem crossing_direction_neg : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), counted_crossing_direction_p p G(v) → counted_crossing_direction_p p G(-v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.prev_point_ray_crossing_set
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: real):
--   NOT empty?[real](key_arb_intersections(p, -v, G)) AND
--    0 <= r AND r < min[real, <=](key_arb_intersections(p, -v, G))
--    IMPLIES 
theorem prev_point_ray_crossing_set : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : ℝ), ¬ empty_p (key_arb_intersections p -v G) ∧
 0 ≤ r ∧ r < min (key_arb_intersections p -v G) → arb_crossing_set (p - r * v) v G = arb_crossing_set p v G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.later_point_ray_crossing_set
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, r: real):
--   NOT empty?[real](key_arb_intersections(p, v, G)) AND
--    counted_crossing_direction?(p, G)(v) AND
--     0 < r AND r < min[real, <=](k
theorem later_point_ray_crossing_set : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (r : ℝ), ¬ empty_p (key_arb_intersections p v G) ∧
 counted_crossing_direction_p p G(v) ∧
  0 < r ∧ r < min (key_arb_intersections p v G) → arb_crossing_set p v G = add p (arb_crossing_set (p + r * v) v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.parallel_intersections
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {d: real |
--    EXISTS (s: (edge_of_polygon?(G))), (q: point_2d):
--           (q = s`p1 OR q = s`p2) AND parallel?(s`p2 - s`p1, v)
--       AND point_on_arb_ray?(q, p, v) AND (p /= q) AND (d = norm(q - p))}
axiom parallel_intersections {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} : pvs_τ52 → pvs_τ53 → pvs_τ54 → pvs_τ55

-- pvs2lean: arb_direc_edge_crossing.all_arb_intersections
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: union(key_arb_intersections(p, v, G), parallel_intersections(p, v, G))
axiom all_arb_intersections {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10

-- pvs2lean: arb_direc_edge_crossing.empty_ray_intersection_sets
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   empty?[real](all_arb_intersections(p, v, G)) IFF
--    empty?[real](key_arb_intersections(p, v, G)) AND
--     empty?[real](parallel_intersection
theorem empty_ray_intersection_sets : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), empty_p (all_arb_intersections p v G) ↔ empty_p (key_arb_intersections p v G) ∧
 empty_p (parallel_intersections p v G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.parallel_intersections_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   NOT empty?[real](parallel_intersections(p, v, G)) IMPLIES
--    min[real, <=](parallel_intersections(p, v, G)) > 0
theorem parallel_intersections_pos : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ empty_p (parallel_intersections p v G) → min (parallel_intersections p v G) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.min_all_intersections_key
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   NOT empty?[real](key_arb_intersections(p, v, G)) AND
--    empty?[real](parallel_intersections(p, v, G))
--    IMPLIES
--    min[real, <=](all_arb_i
theorem min_all_intersections_key : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ empty_p (key_arb_intersections p v G) ∧
 empty_p (parallel_intersections p v G) → min (all_arb_intersections p v G) =
 min (key_arb_intersections p v G) := by
  -- === TCCs for min_all_intersections_key ===
  -- TCC: min_all_intersections_key_TCC1
  -- Statement: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
  --   NOT empty?[real](key_arb_intersections(p, v, G)) AND
  --    empty?[real](parallel_intersections(p, v, G))
  --    IMPLIES NOT empty?[real](all_ar...
  have min_all_intersections_key_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.min_all_intersections_parallel
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   empty?[real](key_arb_intersections(p, v, G)) AND
--    NOT empty?[real](parallel_intersections(p, v, G))
--    IMPLIES
--    min[real, <=](all_arb_i
theorem min_all_intersections_parallel : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), empty_p (key_arb_intersections p v G) ∧
 ¬ empty_p (parallel_intersections p v G) → min (all_arb_intersections p v G) =
 min (parallel_intersections p v G) := by
  -- === TCCs for min_all_intersections_parallel ===
  -- TCC: min_all_intersections_parallel_TCC1
  -- Statement: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
  --   empty?[real](key_arb_intersections(p, v, G)) AND
  --    NOT empty?[real](parallel_intersections(p, v, G))
  --    IMPLIES NOT empty?[real](all_ar...
  have min_all_intersections_parallel_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.all_intersections_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   NOT empty?[real](all_arb_intersections(p, v, G)) IMPLIES
--    min[real, <=](all_arb_intersections(p, v, G)) > 0
theorem all_intersections_pos : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), ¬ empty_p (all_arb_intersections p v G) → min (all_arb_intersections p v G) > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.nonempty_min_key_arb_intersections
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, d: posreal):
--   NOT empty?[real](key_arb_intersections(p, v, G)) AND
--    d < min[real, <=](all_arb_intersections(p, v, G))
--    IMPLIES d < min[re
theorem nonempty_min_key_arb_intersections : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), ¬ empty_p (key_arb_intersections p v G) ∧
 d < min (all_arb_intersections p v G) → d < min (key_arb_intersections p v G) := by
  -- === TCCs for nonempty_min_key_arb_intersections ===
  -- TCC: nonempty_min_key_arb_intersections_TCC1
  -- Statement: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
  --   NOT empty?[real](key_arb_intersections(p, v, G)) IMPLIES
  --    NOT empty?[real](all_arb_intersections(p, v, G))
  have nonempty_min_key_arb_intersections_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.nonempty_min_parallel_intersections
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, d: posreal):
--   NOT empty?[real](parallel_intersections(p, v, G)) AND
--    d < min[real, <=](all_arb_intersections(p, v, G))
--    IMPLIES d < min[r
theorem nonempty_min_parallel_intersections : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), ¬ empty_p (parallel_intersections p v G) ∧
 d < min (all_arb_intersections p v G) → d < min (parallel_intersections p v G) := by
  -- === TCCs for nonempty_min_parallel_intersections ===
  -- TCC: nonempty_min_parallel_intersections_TCC1
  -- Statement: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
  --   NOT empty?[real](parallel_intersections(p, v, G)) IMPLIES
  --    NOT empty?[real](all_arb_intersections(p, v, G))
  have nonempty_min_parallel_intersections_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.no_parallel_intersections_not_on_segment
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d,
--         e: (edge_of_polygon?(G)), d: posreal):
--   (empty?[real](parallel_intersections(p, v, G)) OR
--     d < min[real, <=](parallel_intersection
theorem no_parallel_intersections_not_on_segment : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), (empty_p (parallel_intersections p v G) ∨
  d < min (parallel_intersections p v G))
 ∧ ¬ point_on_segment_p p e ∧ parallel_p (e.p2 - e.p1) v → ¬ point_on_segment_p (p + d * v) e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_parallel_intersections_not_on
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d,
--         e: (edge_of_polygon?(G)), d: posreal):
--   (empty?[real](parallel_intersections(p, v, G)) OR
--     d < min[real, <=](parallel_intersection
theorem empty_parallel_intersections_not_on : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (e : (ℝ × ℝ) × ({p : ℝ × ℝ // p ≠ p1})) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), (empty_p (parallel_intersections p v G) ∨
  d < min (parallel_intersections p v G))
 ∧ counted_crossing_direction_p p G(v) ∧ parallel_p (e.p2 - e.p1) v → ¬ point_on_segment_p (p + d * v) e := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.empty_all_not_on_perimeter
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d):
--   empty?[real](all_arb_intersections(p, v, G)) IMPLIES
--    FORALL (d: posreal): NOT point_on_polygon_perimeter?(G)(p + d * v)
theorem empty_all_not_on_perimeter : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)), empty_p (all_arb_intersections p v G) → ∀ (d : {x : ℝ // 0 < x}), ¬ point_on_polygon_perimeter_p G(p + d * v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.below_min_all_arb_not_on
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, d: posreal):
--   counted_crossing_direction?(p, G)(v) AND
--    NOT empty?[real](all_arb_intersections(p, v, G)) AND
--     d < min[real, <=](all_arb_
theorem below_min_all_arb_not_on : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), counted_crossing_direction_p p G(v) ∧
 ¬ empty_p (all_arb_intersections p v G) ∧
  d < min (all_arb_intersections p v G) → ¬ point_on_polygon_perimeter_p G(p + d * v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: arb_direc_edge_crossing.below_min_all_arb_not_on_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: point_2d, v: Normalized, G: simple_polygon_2d, d: posreal):
--   counted_crossing_direction?(p, G)(v) AND
--    NOT empty?[real](all_arb_intersections(p, -v, G)) AND
--     d < min[real, <=](all_arb
theorem below_min_all_arb_not_on_neg : ∀ (p : ℝ × ℝ) (v : {v : ℝ × ℝ // norm v = 1}) (G : {s : ℝ // s ≥ 3} × ({s : {i : ℝ // i ≥ 0} // s < num_vertices} → ℝ × ℝ)) (d : {x : {x : ℝ // x ≥ 0} // x > 0}), counted_crossing_direction_p p G(v) ∧
 ¬ empty_p (all_arb_intersections p -v G) ∧
  d < min (all_arb_intersections p -v G) → ¬ point_on_polygon_perimeter_p G(p - d * v) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.arb_direc_edge_crossing