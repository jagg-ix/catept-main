-- Auto-generated by pvs2lean/translator.py
-- PVS theory: ODEs_equiv
-- PVSLib library: dL
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: ODEs_equiv

Auto-generated stubs from PVSLib theory `ODEs_equiv`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.ODEs_equiv

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (pvs_1 : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (pvs_2 : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ23 : Type*} (SS : pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (VectorN : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} (below : pvs_τ26 → pvs_τ27)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} (car : pvs_τ28 → pvs_τ29)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} (closed_interval : pvs_τ33 → pvs_τ34 → pvs_τ35)
variable {pvs_τ36 : Type*} (closed_interval_p : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (cons : pvs_τ37 → pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} (cons_p : pvs_τ40 → Prop)
variable {pvs_τ50 : Type*} (dd_p : pvs_τ50 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (derivable_odes_p : pvs_τ51 → pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} (derivable_p : pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} (dlvar_index : pvs_τ54 → pvs_τ55)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} (env : pvs_τ56 → pvs_τ57)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} {pvs_τ62 : Type*} (env_at_t : pvs_τ58 → pvs_τ59 → pvs_τ60 → pvs_τ61 → pvs_τ62)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} {pvs_τ73 : Type*} (fs : pvs_τ71 → pvs_τ72 → pvs_τ73)
variable {pvs_τ88 : Type*} (hp : pvs_τ88 → Prop)
variable {pvs_τ89 : Type*} {pvs_τ90 : Type*} (length : pvs_τ89 → pvs_τ90)
variable {pvs_τ91 : Type*} (mapexpr_inj : pvs_τ91 → Prop)
variable {pvs_τ92 : Type*} {pvs_τ93 : Type*} {pvs_τ94 : Type*} (max : pvs_τ92 → pvs_τ93 → pvs_τ94)
variable {pvs_τ99 : Type*} (nonempty_p : pvs_τ99 → Prop)
variable {pvs_τ100 : Type*} {pvs_τ101 : Type*} {pvs_τ102 : Type*} (norm_inf : pvs_τ100 → pvs_τ101 → pvs_τ102)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} {pvs_τ105 : Type*} (nth : pvs_τ103 → pvs_τ104 → pvs_τ105)
variable {pvs_τ109 : Type*} {pvs_τ110 : Type*} (re : pvs_τ109 → pvs_τ110)
variable {pvs_τ111 : Type*} {pvs_τ112 : Type*} {pvs_τ113 : Type*} {pvs_τ114 : Type*} (solution_odes_p : pvs_τ111 → pvs_τ112 → pvs_τ113 → pvs_τ114 → Prop)
variable {pvs_τ115 : Type*} {pvs_τ116 : Type*} {pvs_τ117 : Type*} {pvs_τ118 : Type*} (solution_odes_u_p : pvs_τ115 → pvs_τ116 → pvs_τ117 → pvs_τ118 → Prop)
variable {pvs_τ119 : Type*} {pvs_τ120 : Type*} {pvs_τ121 : Type*} (y1 : pvs_τ119 → pvs_τ120 → pvs_τ121)
variable {pvs_τ122 : Type*} {pvs_τ123 : Type*} {pvs_τ124 : Type*} (y2 : pvs_τ122 → pvs_τ123 → pvs_τ124)

-- pvs2lean: ODEs_equiv.Re
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(odes) = 0 THEN null
-- ELSE LET (i, re) = car(odes) IN cons(re(env), Re(cdr(odes))(env))
-- ENDIF
axiom Re {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ20 → pvs_τ21 → pvs_τ22

-- pvs2lean: ODEs_equiv.Re_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (odes: ODEs, env: Environment, k: below(length(odes))):
--   nth(Re(odes)(env), k) = nth(odes, k)`2(env)
theorem Re_nth : ∀ (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (env : {i : ℝ // i ≥ 0} → ℝ) (k : {s : {i : ℝ // i ≥ 0} // s < List.length odes}), nth (Re odes(env)) k = nth odes k.2 env := by
  -- === TCCs for Re_nth ===
  -- TCC: Re_nth_TCC1
  -- Statement: FORALL (odes: ODEs, env: Environment, k: below(length[MapExpr](odes))):
  --   k < length[real](Re(odes)(env))
  have re_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: ODEs_equiv.env_vec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF length(odes) = 0 THEN null
-- ELSE cons(env(dlvar_index(car(odes)`1)), env_vec(cdr(odes))(env))
-- ENDIF
axiom env_vec {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} : pvs_τ68 → pvs_τ69 → pvs_τ70

-- pvs2lean: ODEs_equiv.env_vec_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (odes: ODEs, env: Environment):
--   FORALL (i: below(length(odes))):
--     nth(env_vec(odes)(env), i) = env(dlvar_index(nth(odes, i)`1))
theorem env_vec_nth : ∀ (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (env : {i : ℝ // i ≥ 0} → ℝ), ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length odes}), nth (env_vec odes(env)) i = env (dlvar_index (nth odes i.1)) := by
  -- === TCCs for env_vec_nth ===
  -- TCC: env_vec_nth_TCC1
  -- Statement: FORALL (odes: ODEs, env: Environment, i: below(length[MapExpr](odes))):
  --   i < length[real](env_vec(odes)(env))
  have env_vec_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.r2v_2_fs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ext_d[(d)](nth(v, i))(r)
axiom r2v_2_fs {pvs_τ106 : Type*} {pvs_τ107 : Type*} {pvs_τ108 : Type*} : pvs_τ106 → pvs_τ107 → pvs_τ108

-- pvs2lean: ODEs_equiv.fs_2_r2v
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 1
--   THEN restrict[real, (d), (cons?[real])](LAMBDA (r: real): (: fs(0)(r) :))
-- ELSE cons[(d)]
--          (restrict[real, (d), real](fs(0)),
--           fs_2_r2v(n - 1, d,
--                    LAMBDA (
axiom fs_2_r2v {pvs_τ74 : Type*} {pvs_τ75 : Type*} {pvs_τ76 : Type*} : pvs_τ74 → pvs_τ75 → pvs_τ76 → Prop

-- pvs2lean: ODEs_equiv.fs_2_f2v_nth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, d: (closed_interval?), fs: [below(n) -> [real -> real]],
--         i: below(n)):
--   nth(fs_2_r2v(n, d, fs), i) = restrict[real, (d), real](fs(i))
theorem fs_2_f2v_nth : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (d : ℝ → Bool) (fs : {s : {i : ℝ // i ≥ 0} // s < n} → ℝ → ℝ) (i : {s : {i : ℝ // i ≥ 0} // s < n}), nth (fs_2_r2v n d fs) i = restrict (fs i) := by
  -- === TCCs for fs_2_f2v_nth ===
  -- TCC: fs_2_f2v_nth_TCC1
  -- Statement: FORALL (n: posnat, d: (closed_interval?), fs: [below(n) -> [real -> real]],
  --         i: below(n)):
  --   i < length[d](fs_2_r2v(n, d, fs))
  have fs_2_f2v_nth_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.fs_2_f2v_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, r: posreal, y: [real -> VectorN(n)],
--         fs: [below(n) -> [real -> real]]):
--   (derivable?[closed_interval(0, r)])
--       (restrict[real, (closed_interval(0, r)), VectorN(n)](y))
-- 
theorem fs_2_f2v_eq : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (r : {x : {x : ℝ // x ≥ 0} // x > 0}) (y : ℝ → {l : List ℝ // List.length l = n}) (fs : {s : {i : ℝ // i ≥ 0} // s < n} → ℝ → ℝ), (derivable_p[closed_interval 0 r])
    (restrict y) → ((fun (t : (closed_interval 0 r))=> y t) =
  fs_2_r2v n (closed_interval 0 r) fs
  ↔
  (∀ (i : Finset.range n) (t : closed_interval 0 r), fs i(t) =
      r2v_2_fs (closed_interval 0 r) (restrict y)
              (i) (t))) := by
  -- === TCCs for fs_2_f2v_eq ===
  -- TCC: fs_2_f2v_eq_TCC1
  -- Statement: FORALL (n: posnat, r: posreal, y: [real -> VectorN(n)]): 0 < r
  have fs_2_f2v_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fs_2_f2v_eq_TCC2
  -- Statement: FORALL (n: posnat, r: posreal, y: [real -> VectorN(n)]):
  --   nonempty?[real](closed_interval(0, r))
  have fs_2_f2v_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fs_2_f2v_eq_TCC3
  -- Statement: FORALL (n: posnat, r: posreal, y: [real -> VectorN(n)]):
  --   connected?[(closed_interval(0, r))]
  have fs_2_f2v_eq_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fs_2_f2v_eq_TCC4
  -- Statement: FORALL (n: posnat, r: posreal, y: [real -> VectorN(n)]):
  --   not_one_element?[(closed_interval(0, r))]
  have fs_2_f2v_eq_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fs_2_f2v_eq_TCC5
  -- Statement: FORALL (n: posnat, r: posreal, y: [real -> VectorN(n)]):
  --   (derivable?[closed_interval(0, r)])
  --       (restrict[real, (closed_interval(0, r)), VectorN(n)](y))
  --    IMPLIES closed_interval?(closed_inte...
  have fs_2_f2v_eq_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fs_2_f2v_eq_TCC6
  -- Statement: FORALL (n: posnat, r: posreal, y: [real -> VectorN(n)]):
  --   (derivable?[closed_interval(0, r)])
  --       (restrict[real, (closed_interval(0, r)), VectorN(n)](y))
  --    IMPLIES
  --    FORALL (i: below(n), t: c...
  have fs_2_f2v_eq_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.Odes_f
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Re(odes)
--   (env_at_t(odes,
--             LAMBDA (i: below(length(odes))): LAMBDA (r: real): nth(v, i),
--             envi)
--            (t))
axiom Odes_f {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19

-- pvs2lean: ODEs_equiv.cont_f?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(odes) = 0 OR
--  (r = 0 OR
--    cont_d?[closed_interval(0, r)]
--        (length(odes))(Odes_f(closed_interval(0, r), odes, envi)))
axiom cont_f_p {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} : pvs_τ47 → pvs_τ48 → pvs_τ49 → Prop

-- pvs2lean: ODEs_equiv.Lip?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(odes) = 0 OR
--  (r = 0 OR
--    Lip_d?[closed_interval(0, r)]
--        (length(odes))(Odes_f(closed_interval(0, r), odes, envi)))
axiom Lip_p {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ10 → pvs_τ11 → pvs_τ12 → Prop

-- pvs2lean: ODEs_equiv.const?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i: below(k)): EXISTS (c: real): FORALL (x: real): f(i)(x) = c
axiom const_p {pvs_τ41 : Type*} {pvs_τ42 : Type*} : pvs_τ41 → pvs_τ42 → Prop

-- pvs2lean: ODEs_equiv.max_diff
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF k = 1 THEN abs(y1(0)(0) - y2(0)(0))
-- ELSE max(abs(y1(0)(0) - y2(0)(0)),
--          max_diff(k - 1)
--                  ((LAMBDA (i: below(k - 1)): y1(i + 1)),
--                   (LAMBDA (i: below(k - 1)):
axiom max_diff {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} {pvs_τ98 : Type*} : pvs_τ95 → pvs_τ96 → pvs_τ97 → pvs_τ98

-- pvs2lean: ODEs_equiv.max_diff_norm_inf
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat, y1, y2: VectorN(k)):
--   max_diff(k)
--           (LAMBDA (i: below(k)): (LAMBDA (r: real): nth(y1, i)),
--            LAMBDA (i: below(k)): (LAMBDA (r: real): nth(y2, i)))
--    = norm_inf(k)
theorem max_diff_norm_inf : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (y1 : {l : List ℝ // List.length l = k}) (y2 : {l : List ℝ // List.length l = k}), max_diff k
        (fun (i : Finset.range k)=> (fun (r : ℝ)=> nth y1 i),
         fun (i : Finset.range k)=> (fun (r : ℝ)=> nth y2 i)) = norm_inf k(y1 - y2) := by
  -- === TCCs for max_diff_norm_inf ===
  -- TCC: max_diff_norm_inf_TCC1
  -- Statement: FORALL (k: posnat, y1, y2: VectorN(k), i: below(k)): i < length[real](y1)
  have max_diff_norm_inf_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_diff_norm_inf_TCC2
  -- Statement: FORALL (k: posnat, y1, y2: VectorN(k)):
  --   const?(k)(LAMBDA (i: below(k)): LAMBDA (r: real): nth[real](y1, i))
  have max_diff_norm_inf_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_diff_norm_inf_TCC3
  -- Statement: FORALL (k: posnat, y1, y2: VectorN(k), i: below(k)): i < length[real](y2)
  have max_diff_norm_inf_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_diff_norm_inf_TCC4
  -- Statement: FORALL (k: posnat, y1, y2: VectorN(k)):
  --   const?(k)(LAMBDA (i: below(k)): (LAMBDA (r: real): nth[real](y2, i)))
  have max_diff_norm_inf_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: max_diff_norm_inf_TCC5
  -- Statement: FORALL (k: posnat, y1, y2: VectorN(k)): cons?[real](y1 - y2)
  have max_diff_norm_inf_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.cdr
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (i: below(k - 1)): f(i + 1)
axiom cdr {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} : pvs_τ30 → pvs_τ31 → pvs_τ32

-- pvs2lean: ODEs_equiv.cdr_const
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posnat, f: [below(k) -> [real -> real]]):
--   const?(k)(f) IMPLIES const?(k - 1)(cdr(k)(f))
theorem cdr_const : ∀ (k : {i : {i : ℝ // i ≥ 0} // i > 0}) (f : {s : {i : ℝ // i ≥ 0} // s < k} → ℝ → ℝ), const_p k(f) → const_p (k - 1) (cdr k(f)) := by
  -- === TCCs for cdr_const ===
  -- TCC: cdr_const_TCC1
  -- Statement: FORALL (k: posnat, f: [below(k) -> [real -> real]]):
  --   const?(k)(f) IMPLIES k - 1 >= 0
  have cdr_const_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.Lip_ODEs?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(odes) > 0 IMPLIES
--  (FORALL (k: below(length(odes))):
--     EXISTS (L: nnreal):
--       FORALL (t: {tt: nnreal | tt <= r}):
--         FORALL (y1, y2: (const?(length(odes)))):
--           abs(nth(odes, k
axiom Lip_ODEs_p {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: ODEs_equiv.Lip_DDL_ODEs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (odes: ODEs, envi: Environment, r: posreal):
--   Lip_ODEs?(odes, envi, r) IFF Lip?(odes, envi, r)
theorem Lip_DDL_ODEs : ∀ (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (envi : {i : ℝ // i ≥ 0} → ℝ) (r : {x : {x : ℝ // x ≥ 0} // x > 0}), Lip_ODEs_p odes envi r ↔ Lip_p odes envi r := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.cont_ODEs?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: below(length(odes))):
--   FORALL (eps: posreal, y: (const?(length(odes)))):
--     EXISTS (del: posreal):
--       FORALL (t1: {tt: nnreal | tt <= r}):
--         abs(t1 - t) < del IMPLIES
--          ab
axiom cont_ODEs_p {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} : pvs_τ43 → pvs_τ44 → pvs_τ45 → pvs_τ46 → Prop

-- pvs2lean: ODEs_equiv.cont_DDL_ODEs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (odes: ODEs, envi: Environment, r: nnreal):
--   cont_ODEs?(odes, envi, r) IFF cont_f?(odes, envi, r)
theorem cont_DDL_ODEs : ∀ (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (envi : {i : ℝ // i ≥ 0} → ℝ) (r : {x : ℝ // x ≥ 0}), cont_ODEs_p odes envi r ↔ cont_f_p odes envi r := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.diff_sol_Lip
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (odes: ODEs, envi: Environment, r: posreal):
--   (cont_f?(odes, envi, r) AND Lip?(odes, envi, r)) IMPLIES
--    EXISTS (y: (derivable_odes?(closed_interval(0, r), length(odes)))):
--      solution_odes
theorem diff_sol_Lip : ∀ (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (envi : {i : ℝ // i ≥ 0} → ℝ) (r : {x : {x : ℝ // x ≥ 0} // x > 0}), (cont_f_p odes envi r ∧ Lip_p odes envi r) → ∃ (y : (derivable_odes_p (closed_interval 0 r) (List.length odes))), solution_odes_u_p (closed_interval 0 r) odes envi(y) := by
  -- === TCCs for diff_sol_Lip ===
  -- TCC: diff_sol_Lip_TCC1
  -- Statement: FORALL (odes: ODEs, envi: Environment, r: posreal):
  --   cont_f?(odes, envi, r) AND Lip?(odes, envi, r) IMPLIES 0 < r
  have diff_sol_lip_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: diff_sol_Lip_TCC2
  -- Statement: FORALL (odes: ODEs, envi: Environment, r: posreal):
  --   cont_f?(odes, envi, r) AND Lip?(odes, envi, r) IMPLIES
  --    dd?(closed_interval(0, r))
  have diff_sol_lip_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.diff_sol_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (odes: ODEs, envi: Environment, r: posreal):
--   (cont_ODEs?(odes, envi, r) AND Lip_ODEs?(odes, envi, r)) IMPLIES
--    EXISTS (fs: (derivable_odes?(closed_interval(0, r), length(odes)))):
--      solu
theorem diff_sol_eq : ∀ (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (envi : {i : ℝ // i ≥ 0} → ℝ) (r : {x : {x : ℝ // x ≥ 0} // x > 0}), (cont_ODEs_p odes envi r ∧ Lip_ODEs_p odes envi r) → ∃ (fs : (derivable_odes_p (closed_interval 0 r) (List.length odes))), solution_odes_u_p (closed_interval 0 r) odes envi(fs) := by
  -- === TCCs for diff_sol_eq ===
  -- TCC: diff_sol_eq_TCC1
  -- Statement: FORALL (odes: ODEs, envi: Environment, r: posreal):
  --   cont_ODEs?(odes, envi, r) AND Lip_ODEs?(odes, envi, r) IMPLIES 0 < r
  have diff_sol_eq_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: diff_sol_eq_TCC2
  -- Statement: FORALL (odes: ODEs, envi: Environment, r: posreal):
  --   cont_ODEs?(odes, envi, r) AND Lip_ODEs?(odes, envi, r) IMPLIES
  --    dd?(closed_interval(0, r))
  have diff_sol_eq_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.ODEs_s?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (fs: (derivable_odes?(D, length(odes)))):
--   solution_odes_u?(D, odes, envi)(fs)
axiom ODEs_s_p {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ13 → pvs_τ14 → pvs_τ15 → Prop

-- pvs2lean: ODEs_equiv.fs_sol
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom fs_sol {pvs_τ77 : Type*} {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} {pvs_τ81 : Type*} {pvs_τ82 : Type*} : pvs_τ77 → pvs_τ78 → pvs_τ79 → pvs_τ80 → pvs_τ81 → pvs_τ82

-- pvs2lean: ODEs_equiv.env_sol
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: env_at_t(odes, fs_sol(D, envi, odes), envi)(t)
axiom env_sol {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} : pvs_τ63 → pvs_τ64 → pvs_τ65 → pvs_τ66 → pvs_τ67

-- pvs2lean: ODEs_equiv.ODEs_s_sub
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
--         r: {rr: (D) | rr /= 0}):
--   ODEs_s?(D, envi)(odes) IMPLIES
--    ODEs_s?((closed_interval(0, r)), envi)(odes)
theorem ODEs_s_sub : ∀ (D : ℝ → Bool) (envi : {i : ℝ // i ≥ 0} → ℝ) (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (r : {rr : ℝ // rr ≠ 0}), ODEs_s_p D envi(odes) → ODEs_s_p ((closed_interval 0 r)) envi(odes) := by
  -- === TCCs for ODEs_s_sub ===
  -- TCC: ODEs_s_sub_TCC1
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}):
  --   ODEs_s?(D, envi)(odes) IMPLIES 0 < r
  have odes_s_sub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ODEs_s_sub_TCC2
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}):
  --   ODEs_s?(D, envi)(odes) IMPLIES dd?((closed_interval(0, r)))
  have odes_s_sub_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.fs_sol_sub
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
--         r: {rr: (D) | rr /= 0}, k: below(length(odes)),
--         x: (closed_interval(0, r))):
--   fs_sol(D, envi, odes)(k)(x) =
--    fs_sol((c
theorem fs_sol_sub : ∀ (D : ℝ → Bool) (envi : {i : ℝ // i ≥ 0} → ℝ) (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (r : {rr : ℝ // rr ≠ 0}) (k : {s : {i : ℝ // i ≥ 0} // s < List.length odes}) (x : ℝ), fs_sol D envi odes(k) (x) = fs_sol ((closed_interval 0 r)) envi odes(k) (x) := by
  -- === TCCs for fs_sol_sub ===
  -- TCC: fs_sol_sub_TCC1
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}, k: below(length[MapExpr](odes))):
  --   0 < r
  have fs_sol_sub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fs_sol_sub_TCC2
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}, k: below(length[MapExpr](odes)),
  --         x: (closed_interval(0, r))):
  --   dd?((closed_interval(0, r)))
  have fs_sol_sub_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: fs_sol_sub_TCC3
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}, k: below(length[MapExpr](odes)),
  --         x: (closed_interval(0, r))):
  --   ODEs_s?((closed_interval(0, r)...
  have fs_sol_sub_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.env_sol_sub
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
--         r: {rr: (D) | rr /= 0}, x: (closed_interval(0, r))):
--   env_sol(D, envi, odes)(x) =
--    env_sol((closed_interval(0, r)), envi, odes
theorem env_sol_sub : ∀ (D : ℝ → Bool) (envi : {i : ℝ // i ≥ 0} → ℝ) (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (r : {rr : ℝ // rr ≠ 0}) (x : ℝ), env_sol D envi odes(x) = env_sol ((closed_interval 0 r)) envi odes(x) := by
  -- === TCCs for env_sol_sub ===
  -- TCC: env_sol_sub_TCC1
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}):
  --   0 < r
  have env_sol_sub_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: env_sol_sub_TCC2
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}, x: (closed_interval(0, r))):
  --   D(x)
  have env_sol_sub_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: env_sol_sub_TCC3
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}, x: (closed_interval(0, r))):
  --   dd?((closed_interval(0, r)))
  have env_sol_sub_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: env_sol_sub_TCC4
  -- Statement: FORALL (D: (dd?), envi: Environment, odes: (ODEs_s?(D, envi)),
  --         r: {rr: (D) | rr /= 0}, x: (closed_interval(0, r))):
  --   ODEs_s?((closed_interval(0, r)), envi)(odes)
  have env_sol_sub_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.solution_odes_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (D: (dd?), odes: ODEs, envi: Environment,
--         fs: [below(length(odes)) -> [real -> real]],
--         S: {SS: (dd?) | FORALL (x: real): SS(x) IMPLIES D(x)}):
--   solution_odes?(D, odes, envi)(fs
theorem solution_odes_sub : ∀ (D : ℝ → Bool) (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (envi : {i : ℝ // i ≥ 0} → ℝ) (fs : {s : {i : ℝ // i ≥ 0} // s < List.length odes} → ℝ → ℝ) (S : {SS : ℝ → Bool // ∀ (x : ℝ), SS x → D x}), solution_odes_p D odes envi(fs) → solution_odes_p S odes envi(fs) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.solution_odes_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (D: (dd?), odes: ODEs, envi: Environment,
--         fs: [below(length(odes)) -> [real -> real]],
--         S: {SS: (dd?) | FORALL (x: real): SS(x) IMPLIES D(x)}):
--   solution_odes_u?(D, odes, envi)(
theorem solution_odes_subset : ∀ (D : ℝ → Bool) (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (envi : {i : ℝ // i ≥ 0} → ℝ) (fs : {s : {i : ℝ // i ≥ 0} // s < List.length odes} → ℝ → ℝ) (S : {SS : ℝ → Bool // ∀ (x : ℝ), SS x → D x}), solution_odes_u_p D odes envi(fs) → solution_odes_u_p S odes envi(fs) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.odes_hp_s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (envi: Environment,
--         odes: ODEs
--             | cont_ODEs?(hp(0), odes, envi) AND
--                Lip_ODEs?(hp(0), odes, envi)):
--   FORALL (b: posreal): ODEs_s?(closed_interval(0, b), envi)(
theorem odes_hp_s : ∀ (envi : {i : ℝ // i ≥ 0} → ℝ) (odes : {odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ)) // cont_ODEs_p (hp 0) odes envi ∧ Lip_ODEs_p (hp 0) odes envi}), ∀ (b : {x : {x : ℝ // x ≥ 0} // x > 0}), ODEs_s_p (closed_interval 0 b) envi odes := by
  -- === TCCs for odes_hp_s ===
  -- TCC: odes_hp_s_TCC1
  -- Statement: FORALL (odes: ODEs): dd?(hp(0))
  have odes_hp_s_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: odes_hp_s_TCC2
  -- Statement: FORALL (envi: Environment,
  --         odes: ODEs
  --             | cont_ODEs?(hp(0), odes, envi) AND
  --                Lip_ODEs?(hp(0), odes, envi),
  --         b: posreal):
  --   0 < b
  have odes_hp_s_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: odes_hp_s_TCC3
  -- Statement: FORALL (envi: Environment,
  --         odes: ODEs
  --             | cont_ODEs?(hp(0), odes, envi) AND
  --                Lip_ODEs?(hp(0), odes, envi),
  --         b: posreal):
  --   dd?(closed_interval(0, b))
  have odes_hp_s_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.fs_sol_hp
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF t < 0 THEN fs_sol(closed_interval(0, 1), envi, odes)(k)(t)
-- ELSE fs_sol(closed_interval(0, t + 1), envi, odes)(k)(t)
-- ENDIF
axiom fs_sol_hp {pvs_τ83 : Type*} {pvs_τ84 : Type*} {pvs_τ85 : Type*} {pvs_τ86 : Type*} {pvs_τ87 : Type*} : pvs_τ83 → pvs_τ84 → pvs_τ85 → pvs_τ86 → pvs_τ87

-- pvs2lean: ODEs_equiv.fs_sol_hp_fs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (envi: Environment,
--         odes: ODEs
--             | cont_ODEs?(hp(0), odes, envi) AND
--                Lip_ODEs?(hp(0), odes, envi),
--         b: posreal):
--   FORALL (k: below(length(odes)), b2: (c
theorem fs_sol_hp_fs : ∀ (envi : {i : ℝ // i ≥ 0} → ℝ) (odes : {odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ)) // cont_ODEs_p (hp 0) odes envi ∧ Lip_ODEs_p (hp 0) odes envi}) (b : {x : {x : ℝ // x ≥ 0} // x > 0}), ∀ (k : {s : {i : ℝ // i ≥ 0} // s < List.length odes}) (b2 : ℝ), fs_sol_hp envi odes(k) (b2) = fs_sol (closed_interval 0 b) envi odes(k) (b2) := by
  -- === TCCs for fs_sol_hp_fs ===
  -- TCC: fs_sol_hp_fs_TCC1
  -- Statement: FORALL (envi: Environment,
  --         odes: ODEs
  --             | cont_ODEs?(hp(0), odes, envi) AND
  --                Lip_ODEs?(hp(0), odes, envi),
  --         b: posreal, k: below(length[MapExpr](odes)),
  --      ...
  have fs_sol_hp_fs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.fs_sol_hp_sol
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (envi: Environment,
--         odes: ODEs
--             | cont_ODEs?(hp(0), odes, envi) AND
--                Lip_ODEs?(hp(0), odes, envi)):
--   solution_odes?(hp(0), odes, envi)(fs_sol_hp(envi, odes))
theorem fs_sol_hp_sol : ∀ (envi : {i : ℝ // i ≥ 0} → ℝ) (odes : {odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ)) // cont_ODEs_p (hp 0) odes envi ∧ Lip_ODEs_p (hp 0) odes envi}), solution_odes_p (hp 0) odes envi (fs_sol_hp envi odes) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.fs_sol_hp_sol_u
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (envi: Environment,
--         odes: ODEs
--             | cont_ODEs?(hp(0), odes, envi) AND
--                Lip_ODEs?(hp(0), odes, envi)):
--   solution_odes_u?(hp(0), odes, envi)(fs_sol_hp(envi, odes))
theorem fs_sol_hp_sol_u : ∀ (envi : {i : ℝ // i ≥ 0} → ℝ) (odes : {odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ)) // cont_ODEs_p (hp 0) odes envi ∧ Lip_ODEs_p (hp 0) odes envi}), solution_odes_u_p (hp 0) odes envi (fs_sol_hp envi odes) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: ODEs_equiv.diff_sol_eq_dd
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (odes: ODEs, envi: Environment, D: (dd?)):
--   (cont_ODEs?(D, odes, envi) AND Lip_ODEs?(D, odes, envi)) IMPLIES
--    EXISTS (fs: (derivable_odes?(D, length(odes)))):
--      solution_odes_u?(D, odes, 
theorem diff_sol_eq_dd : ∀ (odes : List (dLVar × (({i : ℝ // i ≥ 0} → ℝ) → ℝ))) (envi : {i : ℝ // i ≥ 0} → ℝ) (D : ℝ → Bool), (cont_ODEs_p D odes envi ∧ Lip_ODEs_p D odes envi) → ∃ (fs : (derivable_odes_p D (List.length odes))), solution_odes_u_p D odes envi(fs) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.ODEs_equiv