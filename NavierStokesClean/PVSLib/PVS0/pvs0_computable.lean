-- Auto-generated by pvs2lean/translator.py
-- PVS theory: pvs0_computable
-- PVSLib library: PVS0
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: pvs0_computable

Auto-generated stubs from PVSLib theory `pvs0_computable`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.pvs0_computable

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (choose : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (length : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} (nonempty_p : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (nth : pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (op1 : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (op2 : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} (pvs0_eval : pvs_τ16 → pvs_τ17 → pvs_τ18)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (semantic_rel : pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} (surjective_p : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} (terminating_p : pvs_τ30 → Prop)
variable {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} (tuple2nat : pvs_τ31 → pvs_τ32 → pvs_τ33)

-- pvs2lean: pvs0_computable.pvs0_level
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: pvs0`3 = false_val AND
--  IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--  ELSE (EXISTS (p: (pvs0_level(n - 1))):
--          length(p`1) + 1 = length(pvs0`1) AND
--           FORALL (i: below(length(pvs0`1))):
axiom pvs0_level {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: pvs0_computable.pvs0_level_weak_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [nat -> [PVS0[nat] -> boolean]]):
--   (FORALL (n: nat, pvs0: PVS0):
--      pvs0`3 = false_val AND
--       IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--       ELSE (EXISTS (p: (P(n - 1))):
--        
theorem pvs0_level_weak_induction : ∀ (P : {i : ℝ // i ≥ 0} → (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ → Bool), ∀ (n : ℕ) (pvs0 : PVS0), pvs0.3 = false_val ∧
   if n = 0 then pvs0.1 = lop1 ∧ pvs0.2 = lop2
   else (∃ (p : (P (n - 1))), List.length p.1 + 1 = List.length pvs0.1 ∧
            ∀ (i : Finset.range List.length pvs0.1), if i < List.length p.1 then nth pvs0.1 i = nth p.1 i
              else ∀ (env : ℕ), semantic_rel p(env, nth pvs0.1 i(env))
              )
         ∧
         (∃ (p : (P (n - 1))), List.length p.2 + 1 = List.length pvs0.2 ∧
             ∀ (i : Finset.range List.length pvs0.2), if i < List.length p.2 then nth pvs0.2 i = nth p.2 i
               else ∀ (env1 : _) (env2 : ℕ), semantic_rel p
                                  (tuple2nat env1 env2,
                                   nth pvs0.2 i(env1, env2))
               )
   
   → P n(pvs0) → ∀ (n : ℕ) (pvs0 : PVS0), pvs0_level n(pvs0) → P n(pvs0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pvs0_computable.pvs0_level_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [nat -> [PVS0[nat] -> boolean]]):
--   (FORALL (n: nat, pvs0: PVS0):
--      pvs0`3 = false_val AND
--       IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--       ELSE (EXISTS (p:
--                     
theorem pvs0_level_induction : ∀ (P : {i : ℝ // i ≥ 0} → (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ → Bool), ∀ (n : ℕ) (pvs0 : PVS0), pvs0.3 = false_val ∧
   if n = 0 then pvs0.1 = lop1 ∧ pvs0.2 = lop2
   else (∃ (p : ({pvs0: PVS0 |
                       pvs0_level (n - 1) (pvs0) ∧ P (n - 1) (pvs0)})), List.length p.1 + 1 = List.length pvs0.1 ∧
            ∀ (i : Finset.range List.length pvs0.1), if i < List.length p.1 then nth pvs0.1 i = nth p.1 i
              else ∀ (env : ℕ), semantic_rel p(env, nth pvs0.1 i(env))
              )
         ∧
         (∃ (p : ({pvs0: PVS0 |
                        pvs0_level (n - 1) (pvs0) ∧ P (n - 1) (pvs0)})), List.length p.2 + 1 = List.length pvs0.2 ∧
             ∀ (i : Finset.range List.length pvs0.2), if i < List.length p.2 then nth pvs0.2 i = nth p.2 i
               else ∀ (env1 : _) (env2 : ℕ), semantic_rel p
                                  (tuple2nat env1 env2,
                                   nth pvs0.2 i(env1, env2))
               )
   
   → P n(pvs0) → ∀ (n : ℕ) (pvs0 : PVS0), pvs0_level n(pvs0) → P n(pvs0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pvs0_computable.level_surjective
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat): EXISTS (f: [nat -> (pvs0_level(n))]): surjective?(f)
theorem level_surjective : ∀ (n : {i : ℝ // i ≥ 0}), ∃ (f : {i : ℝ // i ≥ 0} → (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ), surjective_p f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pvs0_computable.level_surjection
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: choose({f: [nat -> (pvs0_level(n))] | surjective?(f)})
noncomputable def level_surjection : sorry := by
  sorry  -- pvs2lean: definition stub for choose({f: [nat -> (pvs0_level(n))] | surjective?(f)})

-- pvs2lean: pvs0_computable.representantion
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (f: [nat -> PVS0_partial_recursive]): surjective?(f)
theorem representantion : ∃ (f : {i : ℝ // i ≥ 0} → {pvs0 : (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ // ∃ (n : ℕ), pvs0_level n(pvs0)}), surjective_p f := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pvs0_computable.pvs0_level_computable
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: pvs0`3 = false_val AND
--  terminating?(pvs0) AND
--   IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--   ELSE (EXISTS (p: (pvs0_level_computable(n - 1))):
--           length(p`1) + 1 = length(pvs0`1) AND
--       
axiom pvs0_level_computable {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ23 → pvs_τ24 → Prop

-- pvs2lean: pvs0_computable.pvs0_level_computable_weak_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [nat -> [PVS0[nat] -> boolean]]):
--   (FORALL (n: nat, pvs0: PVS0):
--      pvs0`3 = false_val AND
--       terminating?(pvs0) AND
--        IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--        ELSE (
theorem pvs0_level_computable_weak_induction : ∀ (P : {i : ℝ // i ≥ 0} → (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ → Bool), ∀ (n : ℕ) (pvs0 : PVS0), pvs0.3 = false_val ∧
   terminating_p pvs0 ∧
    if n = 0 then pvs0.1 = lop1 ∧ pvs0.2 = lop2
    else (∃ (p : (P (n - 1))), List.length p.1 + 1 = List.length pvs0.1 ∧
             ∀ (i : Finset.range List.length pvs0.1), if i < List.length p.1 then nth pvs0.1 i = nth p.1 i
               else ∀ (env : ℕ), pvs0_eval p(env) = nth pvs0.1 i(env)
               )
          ∧
          (∃ (p : (P (n - 1))), List.length p.2 + 1 = List.length pvs0.2 ∧
              ∀ (i : Finset.range List.length pvs0.2), if i < List.length p.2 then nth pvs0.2 i = nth p.2 i
                else ∀ (env1 : _) (env2 : ℕ), pvs0_eval p(tuple2nat env1 env2) =
                        nth pvs0.2 i(env1, env2)
                )
    
   → P n(pvs0) → ∀ (n : ℕ) (pvs0 : PVS0), pvs0_level_computable n(pvs0) → P n(pvs0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pvs0_computable.pvs0_level_computable_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [nat -> [PVS0[nat] -> boolean]]):
--   (FORALL (n: nat, pvs0: PVS0):
--      pvs0`3 = false_val AND
--       terminating?(pvs0) AND
--        IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--        ELSE (
theorem pvs0_level_computable_induction : ∀ (P : {i : ℝ // i ≥ 0} → (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ → Bool), ∀ (n : ℕ) (pvs0 : PVS0), pvs0.3 = false_val ∧
   terminating_p pvs0 ∧
    if n = 0 then pvs0.1 = lop1 ∧ pvs0.2 = lop2
    else (∃ (p : ({pvs0: PVS0 |
                        pvs0_level_computable (n - 1) (pvs0) ∧
                         P (n - 1) (pvs0)})), List.length p.1 + 1 = List.length pvs0.1 ∧
             ∀ (i : Finset.range List.length pvs0.1), if i < List.length p.1 then nth pvs0.1 i = nth p.1 i
               else ∀ (env : ℕ), pvs0_eval p(env) = nth pvs0.1 i(env)
               )
          ∧
          (∃ (p : ({pvs0: PVS0 |
                         pvs0_level_computable (n - 1) (pvs0) ∧
                          P (n - 1) (pvs0)})), List.length p.2 + 1 = List.length pvs0.2 ∧
              ∀ (i : Finset.range List.length pvs0.2), if i < List.length p.2 then nth pvs0.2 i = nth p.2 i
                else ∀ (env1 : _) (env2 : ℕ), pvs0_eval p(tuple2nat env1 env2) =
                        nth pvs0.2 i(env1, env2)
                )
    
   → P n(pvs0) → ∀ (n : ℕ) (pvs0 : PVS0), pvs0_level_computable n(pvs0) → P n(pvs0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pvs0_computable.pvs0_level2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: pvs0`3 = false_val AND
--  IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--  ELSE (EXISTS (m: below(n), p: (pvs0_level2(m))):
--          length(p`1) + 1 = length(pvs0`1) AND
--           FORALL (i: below(length(
axiom pvs0_level2 {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ21 → pvs_τ22 → Prop

-- pvs2lean: pvs0_computable.pvs0_level2_weak_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [nat -> [PVS0[nat] -> boolean]]):
--   (FORALL (n: nat, pvs0: PVS0):
--      pvs0`3 = false_val AND
--       IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--       ELSE (EXISTS (m: below(n), p: (P(m)))
theorem pvs0_level2_weak_induction : ∀ (P : {i : ℝ // i ≥ 0} → (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ → Bool), ∀ (n : ℕ) (pvs0 : PVS0), pvs0.3 = false_val ∧
   if n = 0 then pvs0.1 = lop1 ∧ pvs0.2 = lop2
   else (∃ (m : Finset.range n) (p : (P m)), List.length p.1 + 1 = List.length pvs0.1 ∧
            ∀ (i : Finset.range List.length pvs0.1), if i < List.length p.1 then nth pvs0.1 i = nth p.1 i
              else ∀ (env : ℕ), semantic_rel p(env, nth pvs0.1 i(env))
              )
         ∧
         (∃ (m : Finset.range n) (p : (P m)), List.length p.2 + 1 = List.length pvs0.2 ∧
             ∀ (i : Finset.range List.length pvs0.2), if i < List.length p.2 then nth pvs0.2 i = nth p.2 i
               else ∀ (env1 : _) (env2 : ℕ), semantic_rel p
                                  (tuple2nat env1 env2,
                                   nth pvs0.2 i(env1, env2))
               )
   
   → P n(pvs0) → ∀ (n : ℕ) (pvs0 : PVS0), pvs0_level2 n(pvs0) → P n(pvs0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pvs0_computable.pvs0_level2_induction
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (P: [nat -> [PVS0[nat] -> boolean]]):
--   (FORALL (n: nat, pvs0: PVS0):
--      pvs0`3 = false_val AND
--       IF n = 0 THEN pvs0`1 = lop1 AND pvs0`2 = lop2
--       ELSE (EXISTS (m: below(n),
--           
theorem pvs0_level2_induction : ∀ (P : {i : ℝ // i ≥ 0} → (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ → Bool), ∀ (n : ℕ) (pvs0 : PVS0), pvs0.3 = false_val ∧
   if n = 0 then pvs0.1 = lop1 ∧ pvs0.2 = lop2
   else (∃ (m : Finset.range n) (p : ({pvs0: PVS0 | pvs0_level2 m(pvs0) ∧ P m(pvs0)})), List.length p.1 + 1 = List.length pvs0.1 ∧
            ∀ (i : Finset.range List.length pvs0.1), if i < List.length p.1 then nth pvs0.1 i = nth p.1 i
              else ∀ (env : ℕ), semantic_rel p(env, nth pvs0.1 i(env))
              )
         ∧
         (∃ (m : Finset.range n) (p : ({pvs0: PVS0 | pvs0_level2 m(pvs0) ∧ P m(pvs0)})), List.length p.2 + 1 = List.length pvs0.2 ∧
             ∀ (i : Finset.range List.length pvs0.2), if i < List.length p.2 then nth pvs0.2 i = nth p.2 i
               else ∀ (env1 : _) (env2 : ℕ), semantic_rel p
                                  (tuple2nat env1 env2,
                                   nth pvs0.2 i(env1, env2))
               )
   
   → P n(pvs0) → ∀ (n : ℕ) (pvs0 : PVS0), pvs0_level2 n(pvs0) → P n(pvs0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pvs0_computable.pvs0_level1_is_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, pvs0: (pvs0_level(n))):
--   EXISTS (m: nat): pvs0_level2(m)(pvs0)
theorem pvs0_level1_is_2 : ∀ (n : {i : ℝ // i ≥ 0}) (pvs0 : (List ({i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × (List ({i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0})) × {i : ℝ // i ≥ 0} × PVS0Expr ℝ), ∃ (m : {i : ℝ // i ≥ 0}), pvs0_level2 m pvs0 := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.pvs0_computable