-- Auto-generated by pvs2lean/translator.py
-- PVS theory: gcd
-- PVSLib library: ints
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: gcd

Auto-generated stubs from PVSLib theory `gcd`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.gcd

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (divides : pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (max : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (min : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (mod : pvs_τ15 → pvs_τ16 → Prop)

-- pvs2lean: gcd.gcd
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: max({k: posnat | divides(k, i) AND divides(k, j)})
axiom gcd {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: gcd.gcd_divides
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) IMPLIES divides(gcd(i, j), i) AND divides(gcd(i, j), j)
theorem gcd_divides : (i ≠ 0 ∨ j ≠ 0) → divides (gcd i j) i ∧ divides (gcd i j) j := by
  -- === TCCs for gcd_divides ===
  -- TCC: gcd_divides_TCC1
  -- Statement: FORALL (i, j: int): (i /= 0 OR j /= 0) IMPLIES i = 0 => j /= 0
  have gcd_divides_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_is_max
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) AND divides(kk, i) AND divides(kk, j) IMPLIES
--  kk <= gcd(i, j)
theorem gcd_is_max : (i ≠ 0 ∨ j ≠ 0) ∧ divides kk i ∧ divides kk j → kk ≤ gcd i j := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) IMPLIES
--  (gcd(i, j) = nn IFF
--    ((divides(nn, i) AND divides(nn, j)) AND
--      (FORALL mm: divides(mm, i) AND divides(mm, j) IMPLIES mm <= nn)))
theorem gcd_def : (i ≠ 0 ∨ j ≠ 0) → (gcd i j = nn ↔
  ((divides nn i ∧ divides nn j) ∧
    (∀ mm,  divides mm i ∧ divides mm j → mm ≤ nn))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_0_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: gcd(0, mm) = mm
theorem gcd_0_pos : gcd 0 mm = mm := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) IMPLIES gcd(i, j) = gcd(abs(i), abs(j))
theorem gcd_abs : (i ≠ 0 ∨ j ≠ 0) → gcd i j = gcd |i| |j| := by
  -- === TCCs for gcd_abs ===
  -- TCC: gcd_abs_TCC1
  -- Statement: FORALL (i, j: int): (i /= 0 OR j /= 0) IMPLIES abs(i) = 0 => abs(j) /= 0
  have gcd_abs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_0_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: gcd(0, -mm) = mm
theorem gcd_0_neg : gcd 0 -mm = mm := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_sym
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) IMPLIES gcd(i, j) = gcd(j, i)
theorem gcd_sym : (i ≠ 0 ∨ j ≠ 0) → gcd i j = gcd j i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_lt_nat
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (n /= 0) IMPLIES gcd(n, m) <= n
theorem gcd_lt_nat : (n ≠ 0) → gcd n m ≤ n := by
  -- === TCCs for gcd_lt_nat ===
  -- TCC: gcd_lt_nat_TCC1
  -- Statement: FORALL (m, n: nat): (n /= 0) IMPLIES n = 0 => m /= 0
  have gcd_lt_nat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_lt
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 AND j /= 0) IMPLIES gcd(i, j) <= min(abs(i), abs(j))
theorem gcd_lt : (i ≠ 0 ∧ j ≠ 0) → gcd i j ≤ min |i| |j| := by
  -- === TCCs for gcd_lt ===
  -- TCC: gcd_lt_TCC1
  -- Statement: FORALL (i, j: int): j /= 0 AND i /= 0 IMPLIES i = 0 => j /= 0
  have gcd_lt_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: gcd(0, ii) = abs(ii)
theorem gcd_0 : gcd 0 ii = |ii| := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_mod
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0) IMPLIES gcd(mod(j, i), i) = gcd(i, j)
theorem gcd_mod : (i ≠ 0) → gcd (j := by
  -- === TCCs for gcd_mod ===
  -- TCC: gcd_mod_TCC1
  -- Statement: FORALL (i, j: int): (i /= 0) IMPLIES mod(j, i) = 0 => i /= 0
  have gcd_mod_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: gcd_mod_TCC2
  -- Statement: FORALL (i, j: int): (i /= 0) IMPLIES i = 0 => j /= 0
  have gcd_mod_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_mod_div
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0) IMPLIES divides(gcd(i, j), mod(j, i))
theorem gcd_mod_div : (i ≠ 0) → divides (gcd i j) (j := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_factors_nat
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (n /= 0 OR m /= 0) IMPLIES EXISTS ip, jp: gcd(n, m) = ip * n + jp * m
theorem gcd_factors_nat : (n ≠ 0 ∨ m ≠ 0) → ∃ ip jp,  gcd n m = ip * n + jp * m := by
  -- === TCCs for gcd_factors_nat ===
  -- TCC: gcd_factors_nat_TCC1
  -- Statement: FORALL (m, n: nat): (n /= 0 OR m /= 0) IMPLIES n = 0 => m /= 0
  have gcd_factors_nat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_factors
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) IMPLIES EXISTS ip, jp: gcd(i, j) = ip * i + jp * j
theorem gcd_factors : (i ≠ 0 ∨ j ≠ 0) → ∃ ip jp,  gcd i j = ip * i + jp * j := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.divides_gcd
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) AND divides(kk, i) AND divides(kk, j) IMPLIES
--  divides(kk, gcd(i, j))
theorem divides_gcd : (i ≠ 0 ∨ j ≠ 0) ∧ divides kk i ∧ divides kk j → divides kk (gcd i j) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_same
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: gcd(p, p) = p
theorem gcd_same : gcd p p = p := by
  -- === TCCs for gcd_same ===
  -- TCC: gcd_same_TCC1
  -- Statement: FORALL (p: posint): p = 0 => p /= 0
  have gcd_same_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_minus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: p /= q IMPLIES gcd(p, q) = gcd(p, q - p)
theorem gcd_minus : p ≠ q → gcd p q = gcd p (q - p) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.gcd_times
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: gcd(p * l, q * l) = gcd(p, q) * l
theorem gcd_times : gcd (p * l) (q * l) = gcd p q * l := by
  -- === TCCs for gcd_times ===
  -- TCC: gcd_times_TCC1
  -- Statement: FORALL (l, p, q: posint): p * l = 0 => q * l /= 0
  have gcd_times_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: gcd.rel_prime
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (gcd(i, j) = 1)
axiom rel_prime {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ17 → pvs_τ18 → Prop

-- pvs2lean: gcd.rel_prime_lem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) IMPLIES
--  (rel_prime(i, j) IFF (EXISTS (n, m: int): 1 = m * i + n * j))
theorem rel_prime_lem : (i ≠ 0 ∨ j ≠ 0) → (rel_prime i j ↔ (∃ (n : _) (m : ℤ), 1 = m * i + n * j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd.rel_prime_div_prod
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) IMPLIES
--  rel_prime(i, j) AND divides(i, j * k) IMPLIES divides(i, k)
theorem rel_prime_div_prod : (i ≠ 0 ∨ j ≠ 0) → rel_prime i j ∧ divides i (j * k) → divides i k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd.rel_prime_sym
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) IMPLIES rel_prime(i, j) = rel_prime(j, i)
theorem rel_prime_sym : (i ≠ 0 ∨ j ≠ 0) → rel_prime i j = rel_prime j i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd.rel_prime_mult_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) AND
--  (i /= 0 OR k /= 0) AND rel_prime(i, j) AND rel_prime(i, k)
--  IMPLIES rel_prime(i, j * k)
theorem rel_prime_mult_right : (i ≠ 0 ∨ j ≠ 0) ∧
 (i ≠ 0 ∨ k ≠ 0) ∧ rel_prime i j ∧ rel_prime i k → rel_prime i (j * k) := by
  -- === TCCs for rel_prime_mult_right ===
  -- TCC: rel_prime_mult_right_TCC1
  -- Statement: FORALL (i, j, k: int):
  --   rel_prime(i, j) AND (i /= 0 OR k /= 0) AND (i /= 0 OR j /= 0) IMPLIES
  --    i = 0 => k /= 0
  have rel_prime_mult_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: rel_prime_mult_right_TCC2
  -- Statement: FORALL (i, j, k: int):
  --   rel_prime(i, k) AND
  --    rel_prime(i, j) AND (i /= 0 OR k /= 0) AND (i /= 0 OR j /= 0)
  --    IMPLIES i = 0 => j * k /= 0
  have rel_prime_mult_right_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd.rel_prime_mult_left
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (i /= 0 OR j /= 0) AND
--  (j /= 0 OR k /= 0) AND rel_prime(i, j) AND rel_prime(k, j)
--  IMPLIES rel_prime(i * k, j)
theorem rel_prime_mult_left : (i ≠ 0 ∨ j ≠ 0) ∧
 (j ≠ 0 ∨ k ≠ 0) ∧ rel_prime i j ∧ rel_prime k j → rel_prime (i * k) j := by
  -- === TCCs for rel_prime_mult_left ===
  -- TCC: rel_prime_mult_left_TCC1
  -- Statement: FORALL (i, j, k: int):
  --   rel_prime(i, j) AND (j /= 0 OR k /= 0) AND (i /= 0 OR j /= 0) IMPLIES
  --    k = 0 => j /= 0
  have rel_prime_mult_left_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: rel_prime_mult_left_TCC2
  -- Statement: FORALL (i, j, k: int):
  --   rel_prime(k, j) AND
  --    rel_prime(i, j) AND (j /= 0 OR k /= 0) AND (i /= 0 OR j /= 0)
  --    IMPLIES i * k = 0 => j /= 0
  have rel_prime_mult_left_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: gcd.compute_gcd
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 0 THEN compute_gcd(-i, j)
-- ELSIF j < 0 THEN compute_gcd(i, -j)
-- ELSIF i = 0 THEN j
-- ELSIF j = 0 THEN i
-- ELSIF i < j THEN compute_gcd(j, i)
-- ELSE (LET rem = mod(i, j) IN
--         IF rem = 0 THEN j ELS
axiom compute_gcd {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5

-- pvs2lean: gcd.rel_prime_inverse
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: m > 1 AND rel_prime(n, m) IMPLIES
--  FORALL (q: below(m)): EXISTS (k: below(m)): mod(n * k, m) = q
theorem rel_prime_inverse : m > 1 ∧ rel_prime n m → ∀ (q : Finset.range m), ∃ (k : Finset.range m), (n * k := by
  -- === TCCs for rel_prime_inverse ===
  -- TCC: rel_prime_inverse_TCC1
  -- Statement: FORALL (m, n: nat): m > 1 IMPLIES n = 0 => m /= 0
  have rel_prime_inverse_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: rel_prime_inverse_TCC2
  -- Statement: FORALL (m, n: nat):
  --   rel_prime(n, m) AND m > 1 IMPLIES
  --    FORALL (q: below(m), k: below(m)): m /= 0
  have rel_prime_inverse_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.gcd