-- Auto-generated by pvs2lean/translator.py
-- PVS theory: tdiv
-- PVSLib library: ints
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: tdiv

Auto-generated stubs from PVSLib theory `tdiv`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.tdiv

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (floor : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (integer_pred : pvs_τ3 → Prop)

-- pvs2lean: tdiv.tdiv
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: floor(i / j)
axiom tdiv {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: tdiv.sgn
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x >= 0 THEN 1 ELSE -1 ENDIF
axiom sgn {pvs_τ4 : Type*} : pvs_τ4 → Prop

-- pvs2lean: tdiv.tdiv_nat
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(n, m) = IF n >= m THEN 1 + tdiv(n - m, m) ELSE 0 ENDIF
theorem tdiv_nat : tdiv n m = if n ≥ m then 1 + tdiv (n - m) m else 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_neg_d
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(i, -j) =
--  IF integer_pred(i / j) THEN -tdiv(i, j) ELSE -tdiv(i, j) - 1 ENDIF
theorem tdiv_neg_d : tdiv i -j = if integer_pred (i / j) then -tdiv i j else -tdiv i j - 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tdiv.tdiv_neg
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(-i, j) =
--  IF integer_pred(i / j) THEN -tdiv(i, j) ELSE -tdiv(i, j) - 1 ENDIF
theorem tdiv_neg : tdiv -i j = if integer_pred (i / j) then -tdiv i j else -tdiv i j - 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tdiv.tdiv_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(i, m) =
--  IF i >= m THEN 1 + tdiv(i - m, m)
--  ELSIF i >= 0 THEN 0
--  ELSIF integer_pred(i / m) THEN -tdiv(-i, m)
--  ELSE -1 - tdiv(-i, m)
--  ENDIF
theorem tdiv_def : tdiv i m = if i ≥ m then 1 + tdiv (i - m) m
ELSIF i ≥ 0 then 0
ELSIF integer_pred (i / m) then -tdiv -i m
else -1 - tdiv -i m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tdiv.tdiv_sgn
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(i, -j) = tdiv(-i, j)
theorem tdiv_sgn : tdiv i -j = tdiv -i j := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_value
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: i >= k * j AND i < (k + 1) * j IMPLIES tdiv(i, j) = k
theorem tdiv_value : i ≥ k * j ∧ i < (k + 1) * j → tdiv i j = k := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(0, j) = 0
theorem tdiv_zero : tdiv 0 j = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_one
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(j) > 1 IMPLIES tdiv(1, j) = IF j >= 0 THEN 0 ELSE -1 ENDIF
theorem tdiv_one : |j| > 1 → tdiv 1 j = if j ≥ 0 then 0 else -1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_eq_arg
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(j, j) = 1
theorem tdiv_eq_arg : tdiv j j = 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_by_one
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(i, 1) = i
theorem tdiv_by_one : tdiv i 1 = i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_eq_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(i, j) = 0 IMPLIES i = 0 OR (sgn(i) = sgn(j) AND abs(i) < abs(j))
theorem tdiv_eq_0 : tdiv i j = 0 → i = 0 ∨ (sgn i = sgn j ∧ |i| < |j|) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_lt
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(i) < abs(j) IMPLIES
--  tdiv(i, j) = IF i = 0 OR sgn(i) = sgn(j) THEN 0 ELSE -1 ENDIF
theorem tdiv_lt : |i| < |j| → tdiv i j = if i = 0 ∨ sgn i = sgn j then 0 else -1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_parity
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sgn(i) = sgn(j) IMPLIES tdiv(i, j) >= 0
theorem tdiv_parity : sgn i = sgn j → tdiv i j ≥ 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_parity_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sgn(i) /= sgn(j) IMPLIES tdiv(i, j) <= 0
theorem tdiv_parity_neg : sgn i ≠ sgn j → tdiv i j ≤ 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_smaller
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: m * tdiv(n, m) <= n
theorem tdiv_smaller : m * tdiv n m ≤ n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_abs
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(tdiv(i, j)) =
--  IF integer_pred(i / j) OR sgn(i) = sgn(j) THEN tdiv(abs(i), abs(j))
--  ELSE tdiv(abs(i), abs(j)) + 1
--  ENDIF
theorem tdiv_abs : |tdiv i j| = if integer_pred (i / j) ∨ sgn i = sgn j then tdiv |i| |j|
else tdiv |i| |j| + 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tdiv.tdiv_max
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(j) * abs(tdiv(i, j)) <= abs(i) + abs(j)
theorem tdiv_max : |j| * |tdiv i j| ≤ |i| + |j| := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_multiple
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(k * j, j) = k
theorem tdiv_multiple : tdiv (k * j) j = k := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: tdiv.tdiv_sum
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tdiv(i + k * j, j) = tdiv(i, j) + k
theorem tdiv_sum : tdiv (i + k * j) j = tdiv i j + k := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.tdiv