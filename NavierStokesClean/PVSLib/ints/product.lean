-- Auto-generated by pvs2lean/translator.py
-- PVS theory: product
-- PVSLib library: ints
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: product

Auto-generated stubs from PVSLib theory `product`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.product

noncomputable section

variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (subrange : pvs_τ5 → pvs_τ6 → pvs_τ7)

-- pvs2lean: product.T_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
axiom T_pred {pvs_τ1 : Type*} : pvs_τ1 → Prop

-- pvs2lean: product.connected_domain
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (x, y: T), (z: integer): x <= z AND z <= y IMPLIES T_pred(z))
theorem connected_domain : ∀ (x : ℝ) (y : ℝ) (z : ℝ), x ≤ z ∧ z ≤ y → T_pred z := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: product.T_low?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: {i: int | EXISTS (j: T): j <= i}
noncomputable def T_low_p : sorry := by
  sorry  -- pvs2lean: definition stub for {i: int | EXISTS (j: T): j <= i}

-- pvs2lean: product.T_high?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: {i: int | EXISTS (j: T): i <= j}
noncomputable def T_high_p : sorry := by
  sorry  -- pvs2lean: definition stub for {i: int | EXISTS (j: T): i <= j}

-- pvs2lean: product.T_pred_lem
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: low <= z AND z <= high IMPLIES T_pred(z)
theorem T_pred_lem : low ≤ z ∧ z ≤ high → T_pred z := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: product.high_low_rewrite
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (p: pred[[T_high, T_low]]):
--    (FORALL (high: T_high), (low: T_low):
--       IF high < low THEN p(high, low)
--       ELSE (FORALL (n: subrange(low, high)): p(n, low))
--       ENDIF)
--     IMPLIES (FOR
theorem high_low_rewrite : ∀ (p : {i : ℝ // T_pred i ∨ ∃ (j : T), i ≤ j} × {i : ℝ // T_pred i ∨ ∃ (j : T), j ≤ i} → Bool), (∀ (high: T_high), (low: T_low):
   if high < low then p high low
   else (∀ (n : Set.Icc low high), p n low)
   ) → (∀ high low,  p high low) := by
  -- === TCCs for high_low_rewrite ===
  -- TCC: high_low_rewrite_TCC1
  -- Statement: FORALL (high: T_high, low: T_low):
  --   NOT high < low IMPLIES
  --    FORALL (n: subrange(low, high)): T_pred(n) OR EXISTS (j: T): n <= j
  have high_low_rewrite_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: product.product
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF low > high THEN 1 ELSE F(high) * product(low, high - 1, F) ENDIF
noncomputable def product : sorry := by
  sorry  -- pvs2lean: definition stub for IF low > high THEN 1 ELSE F(high) * product(low, high - 1, F) ENDIF

-- pvs2lean: product.product_spl
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: T_pred(low + nn + rng) IMPLIES
--  product(low, low + nn + rng, F) =
--   product(low, low + nn, F) * product(low + nn + 1, low + nn + rng, F)
theorem product_spl : T_pred (low + nn + rng) → Finset.prod (Finset.Icc low low + nn + rng) (F) =
 Finset.prod (Finset.Icc low low + nn) (F) * Finset.prod (Finset.Icc low + nn + 1 low + nn + rng) (F) := by
  -- === TCCs for product_spl ===
  -- TCC: product_spl_TCC1
  -- Statement: FORALL (low: T_low, nn, rng: nat):
  --   T_pred(low + nn + rng) IMPLIES
  --    T_pred(low + nn + rng) OR EXISTS (j: T): low + nn + rng <= j
  have product_spl_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_spl_TCC2
  -- Statement: FORALL (low: T_low, nn, rng: nat):
  --   T_pred(low + nn + rng) IMPLIES
  --    T_pred(low + nn) OR EXISTS (j: T): low + nn <= j
  have product_spl_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_spl_TCC3
  -- Statement: FORALL (low: T_low, nn, rng: nat):
  --   T_pred(low + nn + rng) IMPLIES
  --    T_pred(low + nn + 1) OR EXISTS (j: T): j <= low + nn + 1
  have product_spl_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: product.product_split
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: low - 1 <= z AND z <= high IMPLIES
--  product(low, high, F) = product(low, z, F) * product(z + 1, high, F)
theorem product_split : low - 1 ≤ z ∧ z ≤ high → Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low z) (F) * Finset.prod (Finset.Icc z + 1 high) (F) := by
  -- === TCCs for product_split ===
  -- TCC: product_split_TCC1
  -- Statement: FORALL (high: T_high, low: T_low, z: int):
  --   z <= high AND low - 1 <= z IMPLIES T_pred(z) OR EXISTS (j: T): z <= j
  have product_split_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_split_TCC2
  -- Statement: FORALL (high: T_high, low: T_low, z: int):
  --   z <= high AND low - 1 <= z IMPLIES
  --    T_pred(z + 1) OR EXISTS (j: T): j <= z + 1
  have product_split_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_div
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: low - 1 <= z AND z <= high AND product(low, z, F) /= 0 IMPLIES
--  product(low, high, F) / product(low, z, F) = product(z + 1, high, F)
theorem product_div : low - 1 ≤ z ∧ z ≤ high ∧ Finset.prod (Finset.Icc low z) (F) ≠ 0 → Finset.prod (Finset.Icc low high) (F) / Finset.prod (Finset.Icc low z) (F) = Finset.prod (Finset.Icc z + 1 high) (F) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_div_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: low - 1 <= z AND
--  z <= high AND product(low, high, F) /= 0 AND product(z + 1, high, F) /= 0
--  IMPLIES
--  product(low, z, F) / product(low, high, F) = 1 / product(z + 1, high, F)
theorem product_div_neg : low - 1 ≤ z ∧
 z ≤ high ∧ Finset.prod (Finset.Icc low high) (F) ≠ 0 ∧ Finset.prod (Finset.Icc z + 1 high) (F) ≠ 0 → Finset.prod (Finset.Icc low z) (F) / Finset.prod (Finset.Icc low high) (F) = 1 / Finset.prod (Finset.Icc z + 1 high) (F) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_eq_arg
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: product(m, m, F) = F(m)
theorem product_eq_arg : Finset.prod (Finset.Icc m m) (F) = F m := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_first
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: high >= low IMPLIES
--  product(low, high, F) = F(low) * product(low + 1, high, F)
theorem product_first : high ≥ low → Finset.prod (Finset.Icc low high) (F) = F low * Finset.prod (Finset.Icc low + 1 high) (F) := by
  -- === TCCs for product_first ===
  -- TCC: product_first_TCC1
  -- Statement: FORALL (high: T_high, low: T_low): high >= low IMPLIES T_pred(low)
  have product_first_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_first_TCC2
  -- Statement: FORALL (high: T_high, low: T_low):
  --   high >= low IMPLIES T_pred(low + 1) OR EXISTS (j: T): j <= low + 1
  have product_first_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_last
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: high >= low IMPLIES
--  product(low, high, F) = product(low, high - 1, F) * F(high)
theorem product_last : high ≥ low → Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low high - 1) (F) * F high := by
  -- === TCCs for product_last ===
  -- TCC: product_last_TCC1
  -- Statement: FORALL (high: T_high, low: T_low):
  --   high >= low IMPLIES T_pred(high - 1) OR EXISTS (j: T): high - 1 <= j
  have product_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_last_TCC2
  -- Statement: FORALL (high: T_high, low: T_low): high >= low IMPLIES T_pred(high)
  have product_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_middle
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: high >= i AND i >= low IMPLIES
--  product(low, high, F) =
--   product(low, i - 1, F) * F(i) * product(i + 1, high, F)
theorem product_middle : high ≥ i ∧ i ≥ low → Finset.prod (Finset.Icc low high) (F) =
 Finset.prod (Finset.Icc low i - 1) (F) * F i * Finset.prod (Finset.Icc i + 1 high) (F) := by
  -- === TCCs for product_middle ===
  -- TCC: product_middle_TCC1
  -- Statement: FORALL (high: T_high, i: T, low: T_low):
  --   i >= low AND high >= i IMPLIES T_pred(i - 1) OR EXISTS (j: T): i - 1 <= j
  have product_middle_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_middle_TCC2
  -- Statement: FORALL (high: T_high, i: T, low: T_low):
  --   i >= low AND high >= i IMPLIES T_pred(i + 1) OR EXISTS (j: T): j <= i + 1
  have product_middle_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_const
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: product(low, high, (LAMBDA i: nzr)) =
--  IF high >= low THEN nzr ^ (high - low + 1) ELSE 1 ENDIF
theorem product_const : Finset.prod (Finset.Icc low high) ((fun i=>  nzr)) = if high ≥ low then nzr ^ (high - low + 1) else 1 := by
  -- === TCCs for product_const ===
  -- TCC: product_const_TCC1
  -- Statement: FORALL (high: T_high, low: T_low, nzr: nzint):
  --   high >= low IMPLIES nzr /= 0 OR (high - low + 1) >= 0
  have product_const_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: low <= high IMPLIES product(low, high, (LAMBDA i: 0)) = 0
theorem product_zero : low ≤ high → Finset.prod (Finset.Icc low high) ((fun i=>  0)) = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_scal
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: low <= high AND a /= 0 IMPLIES
--  product(low, high, (LAMBDA i: a * F(i))) =
--   a ^ (high - low + 1) * product(low, high, F)
theorem product_scal : low ≤ high ∧ a ≠ 0 → Finset.prod (Finset.Icc low high) ((fun i=>  a * F i)) =
 a ^ (high - low + 1) * Finset.prod (Finset.Icc low high) (F) := by
  -- === TCCs for product_scal ===
  -- TCC: product_scal_TCC1
  -- Statement: FORALL (a: int, high: T_high, low: T_low):
  --   a /= 0 AND low <= high IMPLIES a /= 0 OR (high - low + 1) >= 0
  have product_scal_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_ge_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (n: subrange(low, high)): F(n) >= 0) IMPLIES
--  product(low, high, F) >= 0
theorem product_ge_0 : (∀ (n : Set.Icc low high), F n ≥ 0) → Finset.prod (Finset.Icc low high) (F) ≥ 0 := by
  -- === TCCs for product_ge_0 ===
  -- TCC: product_ge_0_TCC1
  -- Statement: FORALL (high: T_high, low: T_low, n: subrange(low, high)): T_pred(n)
  have product_ge_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_gt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (n: subrange(low, high)): F(n) > 0) IMPLIES
--  product(low, high, F) > 0
theorem product_gt_0 : (∀ (n : Set.Icc low high), F n > 0) → Finset.prod (Finset.Icc low high) (F) > 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_shift_T
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: T): T_pred(i + z)) IMPLIES
--  product(low + z, high + z, F) =
--   product(low, high, (LAMBDA (i: T): F(i + z)))
theorem product_shift_T : (∀ (i : T), T_pred (i + z)) → Finset.prod (Finset.Icc low + z high + z) (F) =
 Finset.prod (Finset.Icc low high) ((fun (i : T)=> F (i + z))) := by
  -- === TCCs for product_shift_T ===
  -- TCC: product_shift_T_TCC1
  -- Statement: FORALL (low: T_low, z: int):
  --   (FORALL (i: T): T_pred(i + z)) IMPLIES
  --    T_pred(low + z) OR EXISTS (j: T): j <= low + z
  have product_shift_t_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_shift_T_TCC2
  -- Statement: FORALL (high: T_high, z: int):
  --   (FORALL (i: T): T_pred(i + z)) IMPLIES
  --    T_pred(high + z) OR EXISTS (j: T): high + z <= j
  have product_shift_t_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_shift_T_TCC3
  -- Statement: FORALL (z: int):
  --   (FORALL (i: T): T_pred(i + z)) IMPLIES FORALL (i: T): T_pred(i + z)
  have product_shift_t_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: product.product_shift_T2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: T_pred(low + z) AND T_pred(high + z) IMPLIES
--  product(low + z, high + z, F) =
--   product(low, high,
--           (LAMBDA (i: T): IF T_pred(i + z) THEN F(i + z) ELSE 1 ENDIF))
theorem product_shift_T2 : T_pred (low + z) ∧ T_pred (high + z) → Finset.prod (Finset.Icc low + z high + z) (F) =
 Finset.prod (Finset.Icc low high) ((fun (i : T)=> if T_pred (i + z) then F (i + z) else 1 )) := by
  -- === TCCs for product_shift_T2 ===
  -- TCC: product_shift_T2_TCC1
  -- Statement: FORALL (high: T_high, low: T_low, z: int):
  --   T_pred(high + z) AND T_pred(low + z) IMPLIES
  --    T_pred(low + z) OR EXISTS (j: T): j <= low + z
  have product_shift_t2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_shift_T2_TCC2
  -- Statement: FORALL (high: T_high, low: T_low, z: int):
  --   T_pred(high + z) AND T_pred(low + z) IMPLIES
  --    T_pred(high + z) OR EXISTS (j: T): high + z <= j
  have product_shift_t2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: product.product_prod
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: product(low, high, F) * product(low, high, G) =
--  product(low, high, (LAMBDA i: F(i) * G(i)))
theorem product_prod : Finset.prod (Finset.Icc low high) (F) * Finset.prod (Finset.Icc low high) (G) = Finset.prod (Finset.Icc low high) ((fun i=>  F i * G i)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.restrict
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA i: IF i < low OR i > high THEN 0 ELSE F(i) ENDIF)
axiom restrict {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ2 → pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: product.product_restrict
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: l <= low AND h >= high IMPLIES
--  product(low, high, F) = product(low, high, restrict(F, l, h))
theorem product_restrict : l ≤ low ∧ h ≥ high → Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low high) (restrict F l h) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_restrict_to
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: product(low, high, F) = product(low, high, restrict(F, low, high))
theorem product_restrict_to : Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low high) (restrict F low high) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_restrict_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: restrict(F, low, high) = restrict(G, low, high) IMPLIES
--  product(low, high, F) = product(low, high, G)
theorem product_restrict_eq : restrict F low high = restrict G low high → Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low high) (G) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_eq
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (n: subrange(low, high)): F(n) = G(n)) IMPLIES
--  product(low, high, F) = product(low, high, G)
theorem product_eq : (∀ (n : Set.Icc low high), F n = G n) → Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low high) (G) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_with
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: low <= i AND i <= high AND G(i) /= 0 AND F = G WITH [i := a] IMPLIES
--  product(low, high, F) = a * product(low, high, G) / G(i)
theorem product_with : low ≤ i ∧ i ≤ high ∧ G i ≠ 0 ∧ F = G WITH[i := a] → Finset.prod (Finset.Icc low high) (F) = a * Finset.prod (Finset.Icc low high) (G) / G i := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product.product_nonneg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL i: F(i) >= 0) IMPLIES product(low, high, F) >= 0
theorem product_nonneg : (∀ i,  F i ≥ 0) → Finset.prod (Finset.Icc low high) (F) ≥ 0 := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.product