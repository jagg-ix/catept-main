-- Auto-generated by pvs2lean/translator.py
-- PVS theory: number_util
-- PVSLib library: ints
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: number_util

Auto-generated stubs from PVSLib theory `number_util`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.number_util

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (divides : pvs_τ1 → pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (gcd : pvs_τ3 → pvs_τ4 → Prop)

-- pvs2lean: number_util.prime_divides
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat): n /= 1 IMPLIES EXISTS (p: (prime?)): divides(p, n)
theorem prime_divides : ∀ (n : {i : ℝ // i ≥ 0}), n ≠ 1 → ∃ (p : (prime_p)), divides p n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.posnat_divides_posnat
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: posnat, c: int): a = b * c => c > 0
theorem posnat_divides_posnat : ∀ (a : {i : {i : ℝ // i ≥ 0} // i > 0}) (b : {i : {i : ℝ // i ≥ 0} // i > 0}) (c : ℝ), a = b * c → c > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.divisor_posnat_strict_smaller
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: posnat, b: nat | b > 1, c: posnat): a = b * c => c < a
theorem divisor_posnat_strict_smaller : ∀ (a : {i : {i : ℝ // i ≥ 0} // i > 0}) (b : {b : {i : ℝ // i ≥ 0} // b > 1}) (c : {i : {i : ℝ // i ≥ 0} // i > 0}), a = b * c → c < a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: number_util.prime_sq_decomp_eq_prime_prod
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: int | a > 1, b: int | b > 1, p: (prime?)):
--   p * p = a * b => a = p AND b = p
theorem prime_sq_decomp_eq_prime_prod : ∀ (a : {a : ℝ // a > 1}) (b : {b : ℝ // b > 1}) (p : ℝ), p * p = a * b → a = p ∧ b = p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.prime_gcd_rel
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: int, p: (prime?)): NOT divides(p, a) => gcd(p, a) = 1
theorem prime_gcd_rel : ∀ (a : ℝ) (p : ℝ), ¬ divides p a → gcd p a = 1 := by
  -- === TCCs for prime_gcd_rel ===
  -- TCC: prime_gcd_rel_TCC1
  -- Statement: FORALL (a: int, p: (prime?)): NOT divides(p, a) IMPLIES p = 0 => a /= 0
  have prime_gcd_rel_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.gcd_one_left
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: int): gcd(1, n) = 1
theorem gcd_one_left : ∀ (n : ℝ), gcd 1 n = 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: number_util.gcd_one_right
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: int): gcd(n, 1) = 1
theorem gcd_one_right : ∀ (n : ℝ), gcd n 1 = 1 := by
  -- === TCCs for gcd_one_right ===
  -- TCC: gcd_one_right_TCC1
  -- Statement: FORALL (n: int): n = 0 => 1 /= 0
  have gcd_one_right_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: number_util.gcd_dif_primes_eq_one
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p, q: (prime?)): p /= q => gcd(p, q) = 1
theorem gcd_dif_primes_eq_one : ∀ (p : ℝ) (q : ℝ), p ≠ q → gcd p q = 1 := by
  -- === TCCs for gcd_dif_primes_eq_one ===
  -- TCC: gcd_dif_primes_eq_one_TCC1
  -- Statement: FORALL (p, q: (prime?)): p /= q IMPLIES p = 0 => q /= 0
  have gcd_dif_primes_eq_one_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.product_eq_gcd_one
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c, d: int):
--   (a /= 0 OR c /= 0) AND gcd(a, c) = 1 AND a * b = c * d => divides(a, d)
theorem product_eq_gcd_one : ∀ (a : ℝ) (b : ℝ) (c : ℝ) (d : ℝ), (a ≠ 0 ∨ c ≠ 0) ∧ gcd a c = 1 ∧ a * b = c * d → divides a d := by
  -- === TCCs for product_eq_gcd_one ===
  -- TCC: product_eq_gcd_one_TCC1
  -- Statement: FORALL (a, c: int): (a /= 0 OR c /= 0) IMPLIES a = 0 => c /= 0
  have product_eq_gcd_one_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.gcd_one_divides_prod
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c: int):
--   (a /= 0 OR b /= 0) AND gcd(a, b) = 1 AND divides(a, b * c) =>
--    divides(a, c)
theorem gcd_one_divides_prod : ∀ (a : ℝ) (b : ℝ) (c : ℝ), (a ≠ 0 ∨ b ≠ 0) ∧ gcd a b = 1 ∧ divides a (b * c) → divides a c := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.gcd_one_prod_eq_one
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c: int):
--   (b /= 0 AND c /= 0) AND gcd(a, b) = 1 AND gcd(a, c) = 1 =>
--    gcd(a, b * c) = 1
theorem gcd_one_prod_eq_one : ∀ (a : ℝ) (b : ℝ) (c : ℝ), (b ≠ 0 ∧ c ≠ 0) ∧ gcd a b = 1 ∧ gcd a c = 1 → gcd a (b * c) = 1 := by
  -- === TCCs for gcd_one_prod_eq_one ===
  -- TCC: gcd_one_prod_eq_one_TCC1
  -- Statement: FORALL (a, b, c: int): b /= 0 AND c /= 0 IMPLIES a = 0 => b /= 0
  have gcd_one_prod_eq_one_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: gcd_one_prod_eq_one_TCC2
  -- Statement: FORALL (a, b, c: int):
  --   b /= 0 AND c /= 0 AND gcd(a, b) = 1 IMPLIES a = 0 => c /= 0
  have gcd_one_prod_eq_one_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: gcd_one_prod_eq_one_TCC3
  -- Statement: FORALL (a, b, c: int):
  --   b /= 0 AND c /= 0 AND gcd(a, b) = 1 AND gcd(a, c) = 1 IMPLIES
  --    a = 0 => b * c /= 0
  have gcd_one_prod_eq_one_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.gcd_one_pot_eq_one
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: nzint, i, j: nat): gcd(a, b) = 1 => gcd(a ^ i, b ^ j) = 1
theorem gcd_one_pot_eq_one : ∀ (a : {i : ℝ // i ≠ 0}) (b : {i : ℝ // i ≠ 0}) (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), gcd a b = 1 → gcd (a ^ i) (b ^ j) = 1 := by
  -- === TCCs for gcd_one_pot_eq_one ===
  -- TCC: gcd_one_pot_eq_one_TCC1
  -- Statement: FORALL (a, b: nzint): a = 0 => b /= 0
  have gcd_one_pot_eq_one_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: gcd_one_pot_eq_one_TCC2
  -- Statement: FORALL (a, b: nzint, i: nat): gcd(a, b) = 1 IMPLIES a /= 0 OR i >= 0
  have gcd_one_pot_eq_one_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: gcd_one_pot_eq_one_TCC3
  -- Statement: FORALL (a, b: nzint, j: nat): gcd(a, b) = 1 IMPLIES b /= 0 OR j >= 0
  have gcd_one_pot_eq_one_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: gcd_one_pot_eq_one_TCC4
  -- Statement: FORALL (a, b: nzint, i, j: nat):
  --   gcd(a, b) = 1 IMPLIES a ^ i = 0 => b ^ j /= 0
  have gcd_one_pot_eq_one_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.divides_gcd_one
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b, c: int):
--   (a /= 0 OR b /= 0) AND gcd(a, b) = 1 AND divides(a, c) AND divides(b, c)
--    => divides(a * b, c)
theorem divides_gcd_one : ∀ (a : ℝ) (b : ℝ) (c : ℝ), (a ≠ 0 ∨ b ≠ 0) ∧ gcd a b = 1 ∧ divides a c ∧ divides b c → divides (a * b) c := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: number_util.prod_selection_prop
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat):
--   FORALL (a: sequence[int], i: nat | i <= n):
--     product(0, n, a) =
--      a(i) * product(0, n, LAMBDA (j: nat): IF j = i THEN 1 ELSE a(j) ENDIF)
theorem prod_selection_prop : ∀ (n : {i : ℝ // i ≥ 0}), ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (i : {i : {i : ℝ // i ≥ 0} // i ≤ n}), Finset.prod (Finset.Icc 0 n) (a) = a i * Finset.prod (Finset.Icc 0 n) (fun (j : ℕ)=> if j = i then 1 else a j ) := by
  -- === TCCs for prod_selection_prop ===
  -- TCC: prod_selection_prop_TCC1
  -- Statement: FORALL (n: nat, i: nat | i <= n): 0 >= 0 OR EXISTS (j: nat): j <= 0
  have prod_selection_prop_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: prod_selection_prop_TCC2
  -- Statement: FORALL (n: nat, i: nat | i <= n): n >= 0 OR EXISTS (j: nat): n <= j
  have prod_selection_prop_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: prod_selection_prop_TCC3
  -- Statement: FORALL (n: nat, i: nat | i <= n):
  --   (FORALL (x, y: nat), (z: integer): x <= z AND z <= y IMPLIES z >= 0)
  have prod_selection_prop_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.number_util