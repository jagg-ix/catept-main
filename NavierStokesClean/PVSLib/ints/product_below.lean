-- Auto-generated by pvs2lean/translator.py
-- PVS theory: product_below
-- PVSLib library: ints
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: product_below

Auto-generated stubs from PVSLib theory `product_below`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.product_below

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)

-- pvs2lean: product_below.product_first_ge
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: high >= low IMPLIES
--  product(low, high, F) = F(low) * product(low + 1, high, F)
theorem product_first_ge : high ≥ low → Finset.prod (Finset.Icc low high) (F) = F low * Finset.prod (Finset.Icc low + 1 high) (F) := by
  -- === TCCs for product_first_ge ===
  -- TCC: product_first_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat): high >= low IMPLIES low < N
  have product_first_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_first_ge_TCC1
  -- Statement: FORALL (high, low: upto[N]):
  --   high >= low IMPLIES
  --    high >= 0 AND high <= N OR EXISTS (j: upto[N]): high <= j
  have product_first_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_below.product_last_ge
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: high >= low IMPLIES
--  product(low, high, F) = product(low, high - 1, F) * F(high)
theorem product_last_ge : high ≥ low → Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low high - 1) (F) * F high := by
  -- === TCCs for product_last_ge ===
  -- TCC: product_last_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat):
  --   high >= low IMPLIES
  --    high - 1 >= 0 AND high - 1 < N OR EXISTS (j: below(N)): high - 1 <= j
  have product_last_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_last_ge_TCC2
  -- Statement: FORALL (high: int_below, low: nat):
  --   high >= low IMPLIES high >= 0 AND high < N
  have product_last_ge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_last_ge_TCC1
  -- Statement: FORALL (high, low: upto[N]):
  --   high >= low IMPLIES
  --    high - 1 >= 0 AND high - 1 <= N OR EXISTS (j: upto[N]): high - 1 <= j
  have product_last_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_below.product_split_ge
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: low - 1 <= m AND m <= high IMPLIES
--  product(low, high, F) = product(low, m, F) * product(m + 1, high, F)
theorem product_split_ge : low - 1 ≤ m ∧ m ≤ high → Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low m) (F) * Finset.prod (Finset.Icc m + 1 high) (F) := by
  -- === TCCs for product_split_ge ===
  -- TCC: product_split_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat, m: below[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    m >= 0 AND m < N OR EXISTS (j: below(N)): m <= j
  have product_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_split_ge_TCC1
  -- Statement: FORALL (high, i: int, low: nat):
  --   i <= high AND low - 1 <= i IMPLIES
  --    i + 1 >= 0 OR EXISTS (j: nat): j <= i + 1
  have product_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_split_ge_TCC1
  -- Statement: FORALL (high, low, m: upto[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    high >= 0 AND high <= N OR EXISTS (j: upto[N]): high <= j
  have product_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_split_ge_TCC2
  -- Statement: FORALL (high, low, m: upto[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    m >= 0 AND m <= N OR EXISTS (j: upto[N]): m <= j
  have product_split_ge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_below.product_0_neg
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: product(0, ing, F) = 1
theorem product_0_neg : Finset.prod (Finset.Icc 0 ing) (F) = 1 := by
  -- === TCCs for product_0_neg ===
  -- TCC: product_0_neg_TCC1
  -- Statement: FORALL (ing: negint): ing >= 0 AND ing < N OR EXISTS (j: below(N)): ing <= j
  have product_0_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_0_neg_TCC1
  -- Statement: FORALL (ing: negint): ing >= 0 AND ing <= N OR EXISTS (j: upto[N]): ing <= j
  have product_0_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.product_below