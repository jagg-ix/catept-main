-- Auto-generated by pvs2lean/translator.py
-- PVS theory: product_nat
-- PVSLib library: ints
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: product_nat

Auto-generated stubs from PVSLib theory `product_nat`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.product_nat

noncomputable section

-- pvs2lean: product_nat.product_shift
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: product(low + m, high + m, F) =
--  product(low, high, (LAMBDA (n: nat): F(n + m)))
theorem product_shift : Finset.prod (Finset.Icc low + m high + m) (F) = Finset.prod (Finset.Icc low high) ((fun (n : ℕ)=> F (n + m))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_nat.product_shift_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: low - m >= 0 IMPLIES
--  product(low - m, high - m, F) =
--   product(low, high, (LAMBDA n: IF n - m < 0 THEN 0 ELSE F(n - m) ENDIF))
theorem product_shift_neg : low - m ≥ 0 → Finset.prod (Finset.Icc low - m high - m) (F) =
 Finset.prod (Finset.Icc low high) ((fun n=>  if n - m < 0 then 0 else F (n - m) )) := by
  -- === TCCs for product_shift_neg ===
  -- TCC: product_shift_neg_TCC1
  -- Statement: FORALL (low, m: nat):
  --   low - m >= 0 IMPLIES low - m >= 0 OR EXISTS (j: nat): j <= low - m
  have product_shift_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_shift_neg_TCC2
  -- Statement: FORALL (low, m: nat):
  --   low - m >= 0 IMPLIES FORALL (n: nat): NOT n - m < 0 IMPLIES n - m >= 0
  have product_shift_neg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_shift_neg_TCC1
  -- Statement: FORALL (low, m: posnat):
  --   low - m > 0 IMPLIES
  --    low - m >= 0 AND low - m > 0 OR EXISTS (j: posnat): j <= low - m
  have product_shift_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_shift_neg_TCC2
  -- Statement: FORALL (low, m: posnat):
  --   low - m > 0 IMPLIES
  --    FORALL (n: posnat): NOT n - m <= 0 IMPLIES n - m >= 0 AND n - m > 0
  have product_shift_neg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_nat.product_shift_ng2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: low - m >= 0 IMPLIES
--  product(low - m, high - m, F) = product(low, high, (LAMBDA n: F(n ~ m)))
theorem product_shift_ng2 : low - m ≥ 0 → Finset.prod (Finset.Icc low - m high - m) (F) = Finset.prod (Finset.Icc low high) ((fun n=>  F (n ~ m))) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_nat.product_shift_i
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 <= low + i IMPLIES
--  product(low + i, high + i, F) =
--   product(low, high,
--           (LAMBDA (n: nat): IF n < low THEN 0 ELSE F(n + i) ENDIF))
theorem product_shift_i : 0 ≤ low + i → Finset.prod (Finset.Icc low + i high + i) (F) =
 Finset.prod (Finset.Icc low high) ((fun (n : ℕ)=> if n < low then 0 else F (n + i) )) := by
  -- === TCCs for product_shift_i ===
  -- TCC: product_shift_i_TCC1
  -- Statement: FORALL (i: int, low: nat):
  --   0 <= low + i IMPLIES low + i >= 0 OR EXISTS (j: nat): j <= low + i
  have product_shift_i_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_shift_i_TCC2
  -- Statement: FORALL (i: int, low: nat):
  --   0 <= low + i IMPLIES FORALL (n: nat): NOT n < low IMPLIES n + i >= 0
  have product_shift_i_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_nat.product_first_ge
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: high >= low IMPLIES
--  product(low, high, F) = F(low) * product(low + 1, high, F)
theorem product_first_ge : high ≥ low → Finset.prod (Finset.Icc low high) (F) = F low * Finset.prod (Finset.Icc low + 1 high) (F) := by
  -- === TCCs for product_first_ge ===
  -- TCC: product_first_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat): high >= low IMPLIES low < N
  have product_first_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_first_ge_TCC1
  -- Statement: FORALL (high, low: upto[N]):
  --   high >= low IMPLIES
  --    high >= 0 AND high <= N OR EXISTS (j: upto[N]): high <= j
  have product_first_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_nat.product_split_ge
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: low - 1 <= i AND i <= high IMPLIES
--  product(low, high, F) = product(low, i, F) * product(i + 1, high, F)
theorem product_split_ge : low - 1 ≤ i ∧ i ≤ high → Finset.prod (Finset.Icc low high) (F) = Finset.prod (Finset.Icc low i) (F) * Finset.prod (Finset.Icc i + 1 high) (F) := by
  -- === TCCs for product_split_ge ===
  -- TCC: product_split_ge_TCC1
  -- Statement: FORALL (high: int_below, low: nat, m: below[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    m >= 0 AND m < N OR EXISTS (j: below(N)): m <= j
  have product_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_split_ge_TCC1
  -- Statement: FORALL (high, i: int, low: nat):
  --   i <= high AND low - 1 <= i IMPLIES
  --    i + 1 >= 0 OR EXISTS (j: nat): j <= i + 1
  have product_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_split_ge_TCC1
  -- Statement: FORALL (high, low, m: upto[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    high >= 0 AND high <= N OR EXISTS (j: upto[N]): high <= j
  have product_split_ge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_split_ge_TCC2
  -- Statement: FORALL (high, low, m: upto[N]):
  --   m <= high AND low - 1 <= m IMPLIES
  --    m >= 0 AND m <= N OR EXISTS (j: upto[N]): m <= j
  have product_split_ge_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: product_nat.product_0_neg
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: product(0, ing, F) = 1
theorem product_0_neg : Finset.prod (Finset.Icc 0 ing) (F) = 1 := by
  -- === TCCs for product_0_neg ===
  -- TCC: product_0_neg_TCC1
  -- Statement: FORALL (ing: negint): ing >= 0 AND ing < N OR EXISTS (j: below(N)): ing <= j
  have product_0_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: product_0_neg_TCC1
  -- Statement: FORALL (ing: negint): ing >= 0 AND ing <= N OR EXISTS (j: upto[N]): ing <= j
  have product_0_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.product_nat