-- Auto-generated by pvs2lean/translator.py
-- PVS theory: integral_split_fun
-- PVSLib library: analysis
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: integral_split_fun

Auto-generated stubs from PVSLib theory `integral_split_fun`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.integral_split_fun

noncomputable section

-- pvs2lean: integral_split_fun.T_pred
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def T_pred : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: integral_split_fun.deriv_domain
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv_domain?[T]
theorem deriv_domain : deriv_domain_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.connected_domain
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?[T]
theorem connected_domain : connected_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.not_one_element
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: not_one_element?[T]
theorem not_one_element : not_one_element_p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.split_fun
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (t: T): IF (t <= tSplit) THEN f1(t) ELSE f2(t) ENDIF
noncomputable def split_fun : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA (t: T): IF (t <= tSplit) THEN f1(t) ELSE f2(t) ENDIF

-- pvs2lean: integral_split_fun.split_fun?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (tSplit: T, r1, r2: [T -> real]):
--   f = LAMBDA (t: T): IF (t <= tSplit) THEN r1(t) ELSE r2(t) ENDIF
noncomputable def split_fun_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS (tSplit: T, r1, r2: [T -> real]):   f = LAMBDA (t: T): IF (t <= tSplit) T

-- pvs2lean: integral_split_fun.split_fun?_split_fun
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: split_fun?(split_fun(tSplit, r1, r2))
theorem split_fun_p_split_fun : split_fun_p (split_fun ((tSplit, r1, r2))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.split_fun_Integrable
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: t0 <= tSplit AND
--  tSplit <= t1 AND
--   Integrable?(t0, tSplit, r1) AND Integrable?(tSplit, t1, r2)
--  IMPLIES Integrable?(t0, t1, split_fun(tSplit, r1, r2))
theorem split_fun_Integrable : t0 ≤ tSplit ∧
 tSplit ≤ t1 ∧
  Integrable_p t0 tSplit r1 ∧ Integrable_p tSplit t1 r2 → Integrable_p t0 t1 (split_fun tSplit r1 r2) := by
  -- === TCCs for split_fun_Integrable ===
  -- TCC: split_fun_Integrable_TCC1
  -- Statement: FORALL (t0, t1, tSplit: T):
  --   tSplit <= t1 AND t0 <= tSplit IMPLIES connected?[T]
  have split_fun_integrable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: split_fun_Integrable_TCC2
  -- Statement: FORALL (t0, t1, tSplit: T):
  --   tSplit <= t1 AND t0 <= tSplit IMPLIES not_one_element?[T]
  have split_fun_integrable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.Integral_split_fun
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (t0 <= tSplit) AND (tSplit <= t1) AND (t0 <= tt) AND (tt <= t1)
--  AND Integrable?(t0, tSplit, r1) AND Integrable?(tSplit, t1, r2)
--  IMPLIES
--  Integrable?(t0, t1, split_fun(tSplit, r1, r2)) AND
--   Int
theorem Integral_split_fun : (t0 ≤ tSplit) ∧ (tSplit ≤ t1) ∧ (t0 ≤ tt) ∧ (tt ≤ t1)
 ∧ Integrable_p t0 tSplit r1 ∧ Integrable_p tSplit t1 r2 → Integrable_p t0 t1 (split_fun tSplit r1 r2) ∧
 Integral t0 tt (split_fun tSplit r1 r2) =
  if tt ≤ tSplit then Integral t0 tt r1
  else Integral t0 tSplit r1 + Integral tSplit tt r2 := by
  -- === TCCs for Integral_split_fun ===
  -- TCC: Integral_split_fun_TCC1
  -- Statement: FORALL (r1, r2: [T -> real], t0, t1, tSplit, tt: T):
  --        Integrable?(t0, t1, split_fun(tSplit, r1, r2))
  --    AND Integrable?(tSplit, t1, r2) AND Integrable?(t0, tSplit, r1)
  --    AND (tt <= t1) AND (...
  have integral_split_fun_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun_TCC2
  -- Statement: FORALL (r1, r2: [T -> real], t0, t1, tSplit, tt: T):
  --        (tt <= tSplit) AND Integrable?(t0, t1, split_fun(tSplit, r1, r2))
  --    AND Integrable?(tSplit, t1, r2) AND Integrable?(t0, tSplit, r1)
  --    A...
  have integral_split_fun_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun_TCC3
  -- Statement: FORALL (r1, r2: [T -> real], t0, t1, tSplit, tt: T):
  --        (NOT tt <= tSplit) AND Integrable?(t0, t1, split_fun(tSplit, r1, r2))
  --    AND Integrable?(tSplit, t1, r2) AND Integrable?(t0, tSplit, r1)
  -- ...
  have integral_split_fun_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.split_fun2
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (t: T):
--   IF (t <= tSplit1) THEN f1(t)
--   ELSIF (t <= tSplit2) THEN f2(t)
--   ELSE f3(t)
--   ENDIF
noncomputable def split_fun2 : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA (t: T):   IF (t <= tSplit1) THEN f1(t)   ELSIF (t <= tSplit2) THEN f2(t) 

-- pvs2lean: integral_split_fun.split_fun2?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (tSplit1, tSplit2: T, r1, r2, r3: [T -> real]):
--   f =
--    LAMBDA (t: T):
--      IF (t <= tSplit1) THEN r1(t)
--      ELSIF (t <= tSplit2) THEN r2(t)
--      ELSE r3(t)
--      ENDIF
noncomputable def split_fun2_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS (tSplit1, tSplit2: T, r1, r2, r3: [T -> real]):   f =    LAMBDA (t: T):  

-- pvs2lean: integral_split_fun.split_fun2?_split_fun2
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: split_fun2?(split_fun2(tSplit1, tSplit2, r1, r2, r3))
theorem split_fun2_p_split_fun2 : split_fun2_p (split_fun2 ((tSplit1, tSplit2, r1, r2, r3))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.in_order?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: t0 <= tSplit1 AND tSplit1 <= tSplit2 AND tSplit2 <= t1
noncomputable def in_order_p : sorry := by
  sorry  -- pvs2lean: definition stub for t0 <= tSplit1 AND tSplit1 <= tSplit2 AND tSplit2 <= t1

-- pvs2lean: integral_split_fun.split_fun2_Integrable
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: in_order?(t0, tSplit1, tSplit2, t1) AND
--  Integrable?(t0, tSplit1, r1) AND
--   Integrable?(tSplit1, tSplit2, r2) AND Integrable?(tSplit2, t1, r3)
--  IMPLIES Integrable?(t0, t1, split_fun2(tSplit1, tSplit2,
theorem split_fun2_Integrable : in_order_p t0 tSplit1 tSplit2 t1 ∧
 Integrable_p t0 tSplit1 r1 ∧
  Integrable_p tSplit1 tSplit2 r2 ∧ Integrable_p tSplit2 t1 r3 → Integrable_p t0 t1 (split_fun2 tSplit1 tSplit2 r1 r2 r3) := by
  -- === TCCs for split_fun2_Integrable ===
  -- TCC: split_fun2_Integrable_TCC1
  -- Statement: FORALL (t0, t1, tSplit1, tSplit2: T):
  --   in_order?(t0, tSplit1, tSplit2, t1) IMPLIES connected?[T]
  have split_fun2_integrable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: split_fun2_Integrable_TCC2
  -- Statement: FORALL (t0, t1, tSplit1, tSplit2: T):
  --   in_order?(t0, tSplit1, tSplit2, t1) IMPLIES not_one_element?[T]
  have split_fun2_integrable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.Integral_split_fun2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      in_order?(t0, tSplit1, tSplit2, t1) AND (t0 <= tt) AND (tt <= t1)
--  AND Integrable?(t0, tSplit1, r1) AND Integrable?(tSplit1, tSplit2, r2)
--  AND Integrable?(tSplit2, t1, r3)
--  IMPLIES
--  Integrable?(t
theorem Integral_split_fun2 : in_order_p t0 tSplit1 tSplit2 t1 ∧ (t0 ≤ tt) ∧ (tt ≤ t1)
 ∧ Integrable_p t0 tSplit1 r1 ∧ Integrable_p tSplit1 tSplit2 r2
 ∧ Integrable_p tSplit2 t1 r3 → Integrable_p t0 t1 (split_fun2 tSplit1 tSplit2 r1 r2 r3) ∧
 Integral t0 tt (split_fun2 tSplit1 tSplit2 r1 r2 r3) =
  if tt ≤ tSplit1 then Integral t0 tt r1
  ELSIF tt ≤ tSplit2
    then Integral t0 tSplit1 r1 + Integral tSplit1 tt r2
  else Integral t0 tSplit1 r1 + Integral tSplit1 tSplit2 r2 +
        Integral tSplit2 tt r3 := by
  -- === TCCs for Integral_split_fun2 ===
  -- TCC: Integral_split_fun2_TCC1
  -- Statement: FORALL (r1, r2, r3: [T -> real], t0, t1, tSplit1, tSplit2, tt: T):
  --        Integrable?(t0, t1, split_fun2(tSplit1, tSplit2, r1, r2, r3))
  --    AND Integrable?(tSplit2, t1, r3) AND Integrable?(tSplit1, ...
  have integral_split_fun2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun2_TCC2
  -- Statement: FORALL (r1, r2, r3: [T -> real], t0, t1, tSplit1, tSplit2, tt: T):
  --        (tt <= tSplit1)
  --    AND Integrable?(t0, t1, split_fun2(tSplit1, tSplit2, r1, r2, r3))
  --    AND Integrable?(tSplit2, t1, r3) AN...
  have integral_split_fun2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun2_TCC3
  -- Statement: FORALL (r1, r2, r3: [T -> real], t0, t1, tSplit1, tSplit2, tt: T):
  --        (tt <= tSplit2) AND (NOT tt <= tSplit1)
  --    AND Integrable?(t0, t1, split_fun2(tSplit1, tSplit2, r1, r2, r3))
  --    AND Integra...
  have integral_split_fun2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun2_TCC4
  -- Statement: FORALL (r1, r2, r3: [T -> real], t0, t1, tSplit1, tSplit2, tt: T):
  --        (NOT tt <= tSplit2) AND (NOT tt <= tSplit1)
  --    AND Integrable?(t0, t1, split_fun2(tSplit1, tSplit2, r1, r2, r3))
  --    AND Int...
  have integral_split_fun2_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.split_fun3
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: LAMBDA (t: T):
--   IF (t <= tSplit1) THEN f1(t)
--   ELSIF (t <= tSplit2) THEN f2(t)
--   ELSIF (t <= tSplit3) THEN f3(t)
--   ELSE f4(t)
--   ENDIF
noncomputable def split_fun3 : sorry := by
  sorry  -- pvs2lean: definition stub for LAMBDA (t: T):   IF (t <= tSplit1) THEN f1(t)   ELSIF (t <= tSplit2) THEN f2(t) 

-- pvs2lean: integral_split_fun.split_fun3?
-- pvs_status: definition  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (tSplit1, tSplit2, tSplit3: T, r1, r2, r3, r4: [T -> real]):
--   f =
--    LAMBDA (t: T):
--      IF (t <= tSplit1) THEN r1(t)
--      ELSIF (t <= tSplit2) THEN r2(t)
--      ELSIF (t <= tSplit3) THEN r3(t)
-- 
noncomputable def split_fun3_p : sorry := by
  sorry  -- pvs2lean: definition stub for EXISTS (tSplit1, tSplit2, tSplit3: T, r1, r2, r3, r4: [T -> real]):   f =    LAM

-- pvs2lean: integral_split_fun.split_fun3?_split_fun3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: split_fun3?(split_fun3(tSplit1, tSplit2, tSplit3, r1, r2, r3, r4))
theorem split_fun3_p_split_fun3 : split_fun3_p (split_fun3 ((tSplit1, tSplit2, tSplit3, r1, r2, r3, r4))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.in_order?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: t0 <= tSplit1 AND
--  tSplit1 <= tSplit2 AND tSplit2 <= tSplit3 AND tSplit3 <= t1
noncomputable def in_order_p : sorry := by
  sorry  -- pvs2lean: definition stub for t0 <= tSplit1 AND  tSplit1 <= tSplit2 AND tSplit2 <= tSplit3 AND tSplit3 <= t1

-- pvs2lean: integral_split_fun.split_middle_fun3
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt: tSplit1 <= tSplit2 AND tSplit2 <= tSplit3 IMPLIES
--  split_fun3(tSplit1, tSplit2, tSplit3, r1, r2, r3, r4) =
--   split_fun(tSplit2, split_fun(tSplit1, r1, r2), split_fun(tSplit3, r3, r4))
theorem split_middle_fun3 : tSplit1 ≤ tSplit2 ∧ tSplit2 ≤ tSplit3 → split_fun3 tSplit1 tSplit2 tSplit3 r1 r2 r3 r4 =
 split_fun tSplit2 (split_fun tSplit1 r1 r2) (split_fun tSplit3 r3 r4) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.split_fun3_Integrable
-- pvs_status: unproved  lean_tactic_class: mathlib_exact
-- stmt_confidence: high  [ast]
-- pvs_stmt:      in_order?(t0, tSplit1, tSplit2, tSplit3, t1)
--  AND Integrable?(t0, tSplit1, r1) AND Integrable?(tSplit1, tSplit2, r2)
--  AND Integrable?(tSplit2, tSplit3, r3) AND Integrable?(tSplit3, t1, r4)
--  IMPLI
theorem split_fun3_Integrable : in_order_p t0 tSplit1 tSplit2 tSplit3 t1
 ∧ Integrable_p t0 tSplit1 r1 ∧ Integrable_p tSplit1 tSplit2 r2
 ∧ Integrable_p tSplit2 tSplit3 r3 ∧ Integrable_p tSplit3 t1 r4 → Integrable_p t0 t1 (split_fun3 tSplit1 tSplit2 tSplit3 r1 r2 r3 r4) := by
  -- === TCCs for split_fun3_Integrable ===
  -- TCC: split_fun3_Integrable_TCC1
  -- Statement: FORALL (t0, t1, tSplit1, tSplit2, tSplit3: T):
  --   in_order?(t0, tSplit1, tSplit2, tSplit3, t1) IMPLIES connected?[T]
  have split_fun3_integrable_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: split_fun3_Integrable_TCC2
  -- Statement: FORALL (t0, t1, tSplit1, tSplit2, tSplit3: T):
  --   in_order?(t0, tSplit1, tSplit2, tSplit3, t1) IMPLIES not_one_element?[T]
  have split_fun3_integrable_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: integral_split_fun.Integral_split_fun3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt:      in_order?(t0, tSplit1, tSplit2, tSplit3, t1) AND (t0 <= tt)
--  AND (tt <= t1) AND Integrable?(t0, tSplit1, r1)
--  AND Integrable?(tSplit1, tSplit2, r2) AND Integrable?(tSplit2, tSplit3, r3)
--  AND Inte
theorem Integral_split_fun3 : in_order_p t0 tSplit1 tSplit2 tSplit3 t1 ∧ (t0 ≤ tt)
 ∧ (tt ≤ t1) ∧ Integrable_p t0 tSplit1 r1
 ∧ Integrable_p tSplit1 tSplit2 r2 ∧ Integrable_p tSplit2 tSplit3 r3
 ∧ Integrable_p tSplit3 t1 r4 → Integrable_p t0 t1 (split_fun3 tSplit1 tSplit2 tSplit3 r1 r2 r3 r4)
 ∧
 Integral t0 tt (split_fun3 tSplit1 tSplit2 tSplit3 r1 r2 r3 r4) =
  if tt ≤ tSplit1 then Integral t0 tt r1
  ELSIF tt ≤ tSplit2
    then Integral t0 tSplit1 r1 + Integral tSplit1 tt r2
  ELSIF tt ≤ tSplit3
    then Integral t0 tSplit1 r1 + Integral tSplit1 tSplit2 r2 +
          Integral tSplit2 tt r3
  else Integral t0 tSplit1 r1 + Integral tSplit1 tSplit2 r2 +
        Integral tSplit2 tSplit3 r3
        + Integral tSplit3 tt r4 := by
  -- === TCCs for Integral_split_fun3 ===
  -- TCC: Integral_split_fun3_TCC1
  -- Statement: FORALL (r1, r2, r3, r4: [T -> real],
  --         t0, t1, tSplit1, tSplit2, tSplit3, tt: T):
  --        Integrable?(t0, t1,
  --                    split_fun3(tSplit1, tSplit2, tSplit3, r1, r2, r3, r4))
  --    AND I...
  have integral_split_fun3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun3_TCC2
  -- Statement: FORALL (r1, r2, r3, r4: [T -> real],
  --         t0, t1, tSplit1, tSplit2, tSplit3, tt: T):
  --        (tt <= tSplit1)
  --    AND Integrable?(t0, t1,
  --                    split_fun3(tSplit1, tSplit2, tSplit3, r1...
  have integral_split_fun3_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun3_TCC3
  -- Statement: FORALL (r1, r2, r3, r4: [T -> real],
  --         t0, t1, tSplit1, tSplit2, tSplit3, tt: T):
  --        (tt <= tSplit2) AND (NOT tt <= tSplit1)
  --    AND Integrable?(t0, t1,
  --                    split_fun3(tSpli...
  have integral_split_fun3_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun3_TCC4
  -- Statement: FORALL (r1, r2, r3, r4: [T -> real],
  --         t0, t1, tSplit1, tSplit2, tSplit3, tt: T):
  --        (tt <= tSplit3) AND (NOT tt <= tSplit2) AND (NOT tt <= tSplit1)
  --    AND Integrable?(t0, t1,
  --            ...
  have integral_split_fun3_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: Integral_split_fun3_TCC5
  -- Statement: FORALL (r1, r2, r3, r4: [T -> real],
  --         t0, t1, tSplit1, tSplit2, tSplit3, tt: T):
  --        (NOT tt <= tSplit3) AND (NOT tt <= tSplit2) AND (NOT tt <= tSplit1)
  --    AND Integrable?(t0, t1,
  --        ...
  have integral_split_fun3_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.integral_split_fun