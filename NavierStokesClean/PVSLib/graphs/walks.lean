-- Auto-generated by pvs2lean/translator.py
-- PVS theory: walks
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: walks

Auto-generated stubs from PVSLib theory `walks`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.walks

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Seq : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Walk : pvs_τ3 → pvs_τ4)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} (below : pvs_τ8 → pvs_τ9)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} {pvs_τ12 : Type*} (dbl : pvs_τ10 → pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (edge_p : pvs_τ13 → pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (edges : pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (is_finite : pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} (length : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (nth : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (rev : pvs_τ33 → pvs_τ34)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (seq : pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} (subset_p : pvs_τ37 → pvs_τ38 → Prop)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (vert : pvs_τ41 → pvs_τ42 → Prop)

-- pvs2lean: walks.verts_in?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: below(length(s))): vert(G)(seq(s)(i)))
axiom verts_in_p {pvs_τ43 : Type*} {pvs_τ44 : Type*} : pvs_τ43 → pvs_τ44 → Prop

-- pvs2lean: walks.walk?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: verts_in?(G, ps) AND
--  (FORALL n:
--     n < length(ps) - 1 IMPLIES
--      edge?(G)(finseq_appl[T](ps)(n), finseq_appl[T](ps)(n + 1)))
axiom walk_p {pvs_τ51 : Type*} {pvs_τ52 : Type*} : pvs_τ51 → pvs_τ52 → Prop

-- pvs2lean: walks.list2prewalk
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := length(L), seq := (LAMBDA (x: below[length(L)]): nth(L, x)) #)
noncomputable def list2prewalk : sorry := by
  sorry  -- pvs2lean: definition stub for (# length := length(L), seq := (LAMBDA (x: below[length(L)]): nth(L, x)) #)

-- pvs2lean: walks.from?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: seq(ps)(0) = u AND seq(ps)(length(ps) - 1) = v
noncomputable def from_p : sorry := by
  sorry  -- pvs2lean: definition stub for seq(ps)(0) = u AND seq(ps)(length(ps) - 1) = v

-- pvs2lean: walks.walk_from?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: seq(ps)(0) = u AND seq(ps)(length(ps) - 1) = v AND walk?(G, ps)
axiom walk_from_p {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50 → Prop

-- pvs2lean: walks.verts_of
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {t: T | (EXISTS (i: below(length(ww))): finseq_appl[T](ww)(i) = t)}
axiom verts_of {pvs_τ45 : Type*} {pvs_τ46 : Type*} : pvs_τ45 → pvs_τ46

-- pvs2lean: walks.edges_of
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {e: doubleton[T] |
--    EXISTS (i: below(length(ww) - 1)):
--      e = dbl(finseq_appl[T](ww)(i), finseq_appl[T](ww)(i + 1))}
axiom edges_of {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ18 → pvs_τ19

-- pvs2lean: walks.pre_circuit?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk?(G, w) AND finseq_appl[T](w)(0) = finseq_appl[T](w)(length(w) - 1)
noncomputable def pre_circuit_p : sorry := by
  sorry  -- pvs2lean: definition stub for walk?(G, w) AND finseq_appl[T](w)(0) = finseq_appl[T](w)(length(w) - 1)

-- pvs2lean: walks.verts_in?_concat
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s1, s2: Seq(G)): verts_in?(G, s1 o s2)
theorem verts_in_p_concat : ∀ (s1 : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}) (s2 : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}), verts_in_p G (s1 ∘ s2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.verts_in?_caret
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (j: below(length(ps))):
--   i <= j IMPLIES verts_in?(G, ps) IMPLIES verts_in?(G, ps ^ (i, j))
theorem verts_in_p_caret : ∀ (j : {s : {i : ℝ // i ≥ 0} // s < List.length ps}), i ≤ j → verts_in_p G ps → verts_in_p G (ps ^ (i, j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.vert_seq_lem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Seq(G)): n < length(w) IMPLIES vert(G)(finseq_appl[T](w)(n))
theorem vert_seq_lem : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}), n < List.length w → vert G(finseq_appl w(n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.verts_of_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Seq(G)): subset?(verts_of(w), vert(G))
theorem verts_of_subset : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}), subset_p (verts_of w) (vert G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.edges_of_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk?(G, ww) IMPLIES subset?(edges_of(ww), edges(G))
theorem edges_of_subset : walk_p G ww → subset_p (edges_of ww) (edges G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk_verts_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk?(G, ps) IMPLIES verts_in?(G, ps)
theorem walk_verts_in : walk_p G ps → verts_in_p G ps := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk_from_vert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: prewalk, v1, v2: T):
--   walk_from?(G, w, v1, v2) IMPLIES vert(G)(v1) AND vert(G)(v2)
theorem walk_from_vert : ∀ (w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0}) (v1 : T) (v2 : T), walk_from_p G w v1 v2 → vert G(v1) ∧ vert G(v2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk_edge_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk?(G, ww) AND
--  subset?(edges_of(ww), edges(GG)) AND subset?(verts_of(ww), vert(GG))
--  IMPLIES walk?(GG, ww)
theorem walk_edge_in : walk_p G ww ∧
 subset_p (edges_of ww) (edges GG) ∧ subset_p (verts_of ww) (vert GG) → walk_p GG ww := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.gen_seq1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := 1, seq := (LAMBDA (i: below(1)): u) #)
axiom gen_seq1 {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ20 → pvs_τ21 → pvs_τ22

-- pvs2lean: walks.gen_seq2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := 2,
--    seq := (LAMBDA (i: below(2)): IF i = 0 THEN u ELSE v ENDIF) #)
axiom gen_seq2 {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26

-- pvs2lean: walks.trunc1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: p ^ (0, length(p) - 2)
axiom trunc1 {pvs_τ39 : Type*} {pvs_τ40 : Type*} : pvs_τ39 → pvs_τ40

-- pvs2lean: walks.add1
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# length := length(ww) + 1,
--    seq
--      := (LAMBDA (ii: below(length(ww) + 1)):
--            IF ii < length(ww) THEN seq(ww)(ii) ELSE x ENDIF) #)
axiom add1 {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7

-- pvs2lean: walks.gen_seq1_is_walk
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: vert(G)(x) IMPLIES walk?(G, gen_seq1(G, x))
theorem gen_seq1_is_walk : vert G(x) → walk_p G (gen_seq1 G x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.edge_to_walk
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: u /= v AND edges(G)(edg[T](u, v)) IMPLIES walk?(G, gen_seq2(G, u, v))
theorem edge_to_walk : u ≠ v ∧ edges G(edg u v) → walk_p G (gen_seq2 G u v) := by
  -- === TCCs for edge_to_walk ===
  -- TCC: edge_to_walk_TCC1
  -- Statement: FORALL (u, v: T): u /= v IMPLIES v /= u
  have edge_to_walk_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: edge_to_walk_TCC2
  -- Statement: FORALL (G: graph[T], u, v: T):
  --   edges(G)(edg[T](u, v)) AND u /= v IMPLIES vert(G)(u)
  have edge_to_walk_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: edge_to_walk_TCC3
  -- Statement: FORALL (G: graph[T], u, v: T):
  --   edges(G)(edg[T](u, v)) AND u /= v IMPLIES vert(G)(v)
  have edge_to_walk_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk?_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk?(G, ps) IMPLIES walk?(G, rev(ps))
theorem walk_p_rev : walk_p G ps → walk_p G (rev ps) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk?_reverse
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk_from?(G, w1, v1, v2) IMPLIES
--  (EXISTS (w: Walk(G)): walk_from?(G, w, v2, v1))
theorem walk_p_reverse : walk_from_p G w1 v1 v2 → (∃ (w : Walk G), walk_from_p G w v2 v1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk?_caret
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i <= j AND j < length(ps) AND walk?(G, ps) IMPLIES walk?(G, ps ^ (i, j))
theorem walk_p_caret : i ≤ j ∧ j < List.length ps ∧ walk_p G ps → walk_p G (ps ^ (i, j)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.l_trunc1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(ww) > 1 IMPLIES length(trunc1(ww)) = length(ww) - 1
theorem l_trunc1 : List.length ww > 1 → List.length (trunc1 ww) = List.length ww - 1 := by
  -- === TCCs for l_trunc1 ===
  -- TCC: l_trunc1_TCC1
  -- Statement: FORALL (ww: prewalk): length(ww) > 1 IMPLIES length(ww) >= 2
  have l_trunc1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: walks.walk?_add1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk?(G, ww) AND vert(G)(x) AND edge?(G)(seq(ww)(length(ww) - 1), x)
--  IMPLIES walk?(G, add1(ww, x))
theorem walk_p_add1 : walk_p G ww ∧ vert G(x) ∧ edge_p G(seq ww(List.length ww - 1), x) → walk_p G (add1 ww x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk_concat_edge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk_from?(G, w1, u1, v1) AND
--  walk_from?(G, w2, u2, v2) AND edge?(G)(v1, u2)
--  IMPLIES walk_from?(G, w1 o w2, u1, v2)
theorem walk_concat_edge : walk_from_p G w1 u1 v1 ∧ walk_from_p G w2 u2 v2 ∧ edge_p G(v1, u2) → walk_from_p G (w1 ∘ w2) u1 v2 := by
  -- === TCCs for walk_concat_edge ===
  -- TCC: walk_concat_edge_TCC1
  -- Statement: FORALL (G: graph[T], u1, u2, v1, v2: T, w1, w2: prewalk):
  --   edge?(G)(v1, u2) AND
  --    walk_from?(G, w2, u2, v2) AND walk_from?(G, w1, u1, v1)
  --    IMPLIES length(o[T](w1, w2)) > 0
  have walk_concat_edge_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk_concat
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(w1) > 1 AND walk_from?(G, w1, u1, v) AND walk_from?(G, w2, u2, v)
--  IMPLIES walk_from?(G, w1 ^ (0, length(w1) - 2) o rev(w2), u1, u2)
theorem walk_concat : List.length w1 > 1 ∧ walk_from_p G w1 u1 v ∧ walk_from_p G w2 u2 v → walk_from_p G (w1 ^ (0, List.length w1 - 2) ∘ rev w2) u1 u2 := by
  -- === TCCs for walk_concat ===
  -- TCC: walk_concat_TCC1
  -- Statement: FORALL (G: graph[T], u1, u2, v: T, w1, w2: prewalk):
  --   walk_from?(G, w2, u2, v) AND walk_from?(G, w1, u1, v) AND length(w1) > 1
  --    IMPLIES length(w1) - 2 >= 0
  have walk_concat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walk_concat_TCC2
  -- Statement: FORALL (G: graph[T], u1, u2, v: T, w1, w2: prewalk):
  --   walk_from?(G, w2, u2, v) AND walk_from?(G, w1, u1, v) AND length(w1) > 1
  --    IMPLIES length(o[T](^[T](w1, (0, length(w1) - 2)), rev[T](w2))) > 0
  have walk_concat_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk?_cut
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j: below(length(ps))):
--   i < j AND seq(ps)(i) = seq(ps)(j) AND walk_from?(G, ps, u, v) IMPLIES
--    walk_from?(G, ps ^ (0, i) o ps ^ (j + 1, length(ps) - 1), u, v)
theorem walk_p_cut : ∀ (i : {s : {i : ℝ // i ≥ 0} // s < List.length ps}) (j : {s : {i : ℝ // i ≥ 0} // s < List.length ps}), i < j ∧ seq ps(i) = seq ps(j) ∧ walk_from_p G ps u v → walk_from_p G (ps ^ (0, i) ∘ ps ^ (j + 1, List.length ps - 1)) u v := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: walks.walk_merge
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: walk_from?(G, p1, v, yt) AND walk_from?(G, p2, u, yt) IMPLIES
--  (EXISTS (p: prewalk): walk_from?(G, p, u, v))
theorem walk_merge : walk_from_p G p1 v yt ∧ walk_from_p G p2 u yt → (∃ (p : prewalk), walk_from_p G p u v) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.walks