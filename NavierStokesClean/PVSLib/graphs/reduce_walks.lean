-- Auto-generated by pvs2lean/translator.py
-- PVS theory: reduce_walks
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: reduce_walks

Auto-generated stubs from PVSLib theory `reduce_walks`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.reduce_walks

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Long_walk : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Walk : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (below : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} (length : pvs_τ7 → Prop)

-- pvs2lean: reduce_walks.reduce
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF finseq_appl[T](w)(k - 1) = finseq_appl[T](w)(k + 1)
--   THEN (# length := length(w) - 2,
--           seq
--             := (LAMBDA (i: below(length(w) - 2)):
--                   IF i < k THEN finseq_appl[T]
axiom reduce {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10 → Prop

-- pvs2lean: reduce_walks.reduce_lem
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Walk(G), k: {n: nat | n > 0 AND n < length(w) - 1}):
--   finseq_appl[T](w)(k - 1) = finseq_appl[T](w)(k + 1) AND length(w) > 3
--    IMPLIES reduce(G, w, k) = w ^ (0, k - 1) o w ^ (k + 2, length
theorem reduce_lem : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // walk_p G w}) (k : {n : {i : ℝ // i ≥ 0} // n > 0 ∧ n < List.length w - 1}), finseq_appl w(k - 1) = finseq_appl w(k + 1) ∧ List.length w > 3 → reduce G w k = w ^ (0, k - 1) ∘ w ^ (k + 2, List.length w - 1) := by
  -- === TCCs for reduce_lem ===
  -- TCC: reduce_lem_TCC1
  -- Statement: FORALL (G: graph[T], w: Walk[T](G),
  --         k: {n: nat | n > 0 AND n < length(w) - 1}):
  --   k - 1 >= 0 AND k - 1 < length(w)
  have reduce_lem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: reduce_lem_TCC2
  -- Statement: FORALL (G: graph[T], w: Walk[T](G),
  --         k: {n: nat | n > 0 AND n < length(w) - 1}):
  --   k + 1 < length(w)
  have reduce_lem_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: reduce_lem_TCC3
  -- Statement: FORALL (G: graph[T], w: Walk[T](G),
  --         k: {n: nat | n > 0 AND n < length(w) - 1}):
  --   finseq_appl[T](w)(k - 1) = finseq_appl[T](w)(k + 1) AND length(w) > 3
  --    IMPLIES k - 1 >= 0
  have reduce_lem_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: reduce_lem_TCC4
  -- Statement: FORALL (G: graph[T], w: Walk[T](G),
  --         k: {n: nat | n > 0 AND n < length(w) - 1}):
  --   finseq_appl[T](w)(k - 1) = finseq_appl[T](w)(k + 1) AND length(w) > 3
  --    IMPLIES length(w) - 1 >= 0
  have reduce_lem_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.reduce_walks