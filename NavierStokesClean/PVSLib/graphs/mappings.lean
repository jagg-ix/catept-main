-- Auto-generated by pvs2lean/translator.py
-- PVS theory: mappings
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: mappings

Auto-generated stubs from PVSLib theory `mappings`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.mappings

noncomputable section

variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (G_from : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (Seq : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (Subgraph : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} (XOR : pvs_τ15 → pvs_τ16)
variable {pvs_τ17 : Type*} (card : pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (connected_p : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} (cycle_p : pvs_τ19 → pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (dbl : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (deg : pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} (del_edge : pvs_τ26 → pvs_τ27 → pvs_τ28)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (del_vert : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (edge_p : pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (edges : pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} (empty_p : pvs_τ37 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} (incident_edges : pvs_τ40 → pvs_τ41 → pvs_τ42)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (is_finite : pvs_τ44 → pvs_τ45)
variable {pvs_τ46 : Type*} (length : pvs_τ46 → Prop)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} (lsth : pvs_τ47 → pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} (map : pvs_τ49 → pvs_τ50 → pvs_τ51)
variable {pvs_τ52 : Type*} (num_edges : pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} (path_connected_p : pvs_τ53 → Prop)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} (path_from_p : pvs_τ54 → pvs_τ55 → pvs_τ56 → pvs_τ57 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} (path_p : pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} (phi : pvs_τ60 → Prop)
variable {pvs_τ63 : Type*} (size : pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} (subgraph : pvs_τ64 → pvs_τ65 → pvs_τ66)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (subgraph_p : pvs_τ67 → pvs_τ68 → Prop)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} (subset_p : pvs_τ69 → pvs_τ70 → Prop)
variable {pvs_τ71 : Type*} (tree_p : pvs_τ71 → Prop)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} (vert : pvs_τ73 → pvs_τ74 → Prop)
variable {pvs_τ75 : Type*} {pvs_τ76 : Type*} {pvs_τ77 : Type*} {pvs_τ78 : Type*} (walk_from_p : pvs_τ75 → pvs_τ76 → pvs_τ77 → pvs_τ78 → Prop)

-- pvs2lean: mappings.proper?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (x, y: T): edge?(G1)(x, y) IMPLIES edge?(G2)(M(x), M(y)))
noncomputable def proper_p : sorry := by
  sorry  -- pvs2lean: definition stub for (FORALL (x, y: T): edge?(G1)(x, y) IMPLIES edge?(G2)(M(x), M(y)))

-- pvs2lean: mappings.injective?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: injective?(M)
axiom injective_p {pvs_τ43 : Type*} : pvs_τ43 → Prop

-- pvs2lean: mappings.fixed?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (v: (vert(G))): v /= phi(v)) IMPLIES
--  (EXISTS (x, y: (vert(G))): y = phi(x) AND x = phi(y) AND edge?(G)(x, y))
axiom fixed_p {pvs_τ38 : Type*} {pvs_τ39 : Type*} : pvs_τ38 → pvs_τ39 → Prop

-- pvs2lean: mappings.reachable
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {y: T | vert(G)(y) AND (EXISTS (w: Seq(G)): walk_from?(G, w, x, y))}
axiom reachable {pvs_τ61 : Type*} {pvs_τ62 : Type*} : pvs_τ61 → pvs_τ62 → Prop

-- pvs2lean: mappings.reachable_subset
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(reachable(G, x), vert(G))
theorem reachable_subset : subset_p (reachable G x) (vert G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.path_reach1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: path_connected?(G) IFF
--  (EXISTS (x: T): vert(G)(x) AND reachable(G, x) = vert(G))
theorem path_reach1 : path_connected_p G ↔ (∃ (x : T), vert G(x) ∧ reachable G x = vert G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.path_reach2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: path_connected?(G) IFF
--  (FORALL (x: T): vert(G)(x) IMPLIES reachable(G, x) = vert(G)) AND
--   NOT empty?(G)
theorem path_reach2 : path_connected_p G ↔ (∀ (x : T), vert G(x) → reachable G x = vert G) ∧
 ¬ empty_p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.reachable_conn
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: (vert(G))): connected?(subgraph(G, reachable(G, x)))
theorem reachable_conn : ∀ (x : T), connected_p (subgraph G (reachable G x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.sub_tree_k
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL k, H, G:
--    subgraph?(H, G) AND tree?(G) AND connected?(H) AND size(G) = size(H) + k
--     IMPLIES tree?(H))
--  IMPLIES
--  (FORALL H, G:
--     subgraph?(H, G) AND tree?(G) AND connected?(H) IMPLIES tr
theorem sub_tree_k : (∀ k H G, 
   subgraph_p H G ∧ tree_p G ∧ connected_p H ∧ size G = size H + k
    → tree_p H) → (∀ H G, 
   subgraph_p H G ∧ tree_p G ∧ connected_p H → tree_p H) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.sub_tree_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: tree?(G) AND subgraph?(H, G) AND size(H) = size(G) AND connected?(H)
--  IMPLIES H = G
theorem sub_tree_0 : tree_p G ∧ subgraph_p H G ∧ size H = size G ∧ connected_p H → H = G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.sub_tree_k_lemm
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL k, H, G:
--    subgraph?(H, G) AND tree?(G) AND connected?(H) AND size(G) = size(H) + k
--     IMPLIES tree?(H))
theorem sub_tree_k_lemm : ∀ (k : {i : ℝ // i ≥ 0}) (H : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)}) (G : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)}), subgraph_p H G ∧ tree_p G ∧ connected_p H ∧ size G = size H + k → tree_p H := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.sub_tree_all
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: tree?(G) AND subgraph?(H, G) AND connected?(H) IMPLIES tree?(H)
theorem sub_tree_all : tree_p G ∧ subgraph_p H G ∧ connected_p H → tree_p H := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.path_reach4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: tree?(G) AND edges(G)(e) IMPLIES NOT path_connected?(del_edge(G, e))
theorem path_reach4 : tree_p G ∧ edges G(e) → ¬ path_connected_p (del_edge G e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.size_subgraph_path
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: path?(G, p) IMPLIES size(G_from(G, p)) = length(p)
theorem size_subgraph_path : path_p G p → size (G_from G p) = List.length p := by
  -- === TCCs for size_subgraph_path ===
  -- TCC: size_subgraph_path_TCC1
  -- Statement: FORALL (G: graph[T], p: prewalk[T]): path?(G, p) IMPLIES walk?[T](G, p)
  have size_subgraph_path_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.path_gap
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      tree?(G) AND edges(G)(e) AND e(x) AND e(y)
--  AND walk_from?(del_edge(G, e), w, x, y)
--  IMPLIES x = y
theorem path_gap : tree_p G ∧ edges G(e) ∧ e x ∧ e y
 ∧ walk_from_p (del_edge G e) w x y → x = y := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.set_card_less
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (A: finite_set[T], B: finite_set[T]):
--   subset?(A, B) AND B(x) IMPLIES card(A) < card(B) OR A(x)
theorem set_card_less : ∀ (A : T → Bool) (B : T → Bool), subset_p A B ∧ B x → card A < card B ∨ A x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.conn_compon
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: (vert(G))):
--   EXISTS H:
--     (subgraph?(H, G) AND
--       connected?(H) AND
--        vert(H)(x) AND FORALL (y: (vert(H))): (deg(y, G) = deg(y, H)))
theorem conn_compon : ∀ (x : T), ∃ (H : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)}), subgraph_p H G ∧ connected_p H ∧
 vert H(x) ∧ ∀ (y : (vert H)), (deg y G = deg y H) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.Bush
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subgraph(Tr, reachable(del_edge(Tr, e), v))
axiom Bush {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4

-- pvs2lean: mappings.Bush_less
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Tr: Tree, v, z: (vert(Tr)), e: (incident_edges(v, Tr)),
--         f: (incident_edges(z, Tr))):
--   f(v) AND NOT e(z) IMPLIES size(Bush(Tr, z, f)) < size(Bush(Tr, v, e))
theorem Bush_less : ∀ (Tr : {G : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // tree_p G}) (v : T) (z : T) (e : {S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y}) (f : {S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y}), f v ∧ ¬ e z → size (Bush Tr z f) < size (Bush Tr v e) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.Fox
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS w:
--         path?(Tr, w) AND (length(w) > 1)
--     AND (phi(finseq_appl[T](w)(0)) = finseq_appl[T](w)(length(w) - 1))
--     AND (G = G_from(Tr, w))
--     AND (H =
--           Bush(Tr, finseq_appl[T](w)
axiom Fox {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: mappings.short_hound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Tr: Tree, phi: map(Tr, Tr)):
--   path_from?(Tr, w, x, phi(x)) AND length(w) = 1 IMPLIES fixed?(Tr, phi)
theorem short_hound : ∀ (Tr : {G : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // tree_p G}) (phi : {phi : {psi : T → T // ∀ (x : T), vert Tr(x) → vert Tr(psi x)} // ∀ (x : _) (y : T), edge_p Tr(x, y) → edge_p Tr(phi x, phi y) ∨ phi x = phi y}), path_from_p Tr w x (phi x) ∧ List.length w = 1 → fixed_p Tr phi := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.back_hound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Tr: Tree, phi: map(Tr, Tr)):
--   path_from?(Tr, w, x, phi(x)) AND
--    length(w) = 2 AND
--     phi(finseq_appl[T](w)(1)) = finseq_appl[T](w)(length(w) - 2)
--    IMPLIES fixed?(Tr, phi)
theorem back_hound : ∀ (Tr : {G : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // tree_p G}) (phi : {phi : {psi : T → T // ∀ (x : T), vert Tr(x) → vert Tr(psi x)} // ∀ (x : _) (y : T), edge_p Tr(x, y) → edge_p Tr(phi x, phi y) ∨ phi x = phi y}), path_from_p Tr w x (phi x) ∧
 List.length w = 2 ∧
  phi (finseq_appl w(1)) = finseq_appl w(List.length w - 2) → fixed_p Tr phi := by
  -- === TCCs for back_hound ===
  -- TCC: back_hound_TCC1
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND length(w) = 2 IMPLIES 1 < length(w)
  have back_hound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: back_hound_TCC2
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND length(w) = 2 IMPLIES
  --    length(w) - 2 >= 0 AND length(w) - 2 < length(w)
  have back_hound_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.stuck_hound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Tr: Tree, phi: map(Tr, Tr)):
--   path_from?(Tr, w, x, phi(x)) AND
--    length(w) > 1 AND
--     phi(finseq_appl[T](w)(1)) = finseq_appl[T](w)(length(w) - 1)
--    IMPLIES
--    EXISTS (p: prewalk):
--      pa
theorem stuck_hound : ∀ (Tr : {G : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // tree_p G}) (phi : {phi : {psi : T → T // ∀ (x : T), vert Tr(x) → vert Tr(psi x)} // ∀ (x : _) (y : T), edge_p Tr(x, y) → edge_p Tr(phi x, phi y) ∨ phi x = phi y}), path_from_p Tr w x (phi x) ∧
 List.length w > 1 ∧
  phi (finseq_appl w(1)) = finseq_appl w(List.length w - 1) → ∃ (p : prewalk), path_from_p Tr p (finseq_appl w(1)) (phi (finseq_appl w(1))) ∧
   List.length p < List.length w ∧
    ((List.length p > 1 ∧
       finseq_appl p(List.length p - 2) = finseq_appl w(List.length w - 2))
      ∨ fixed_p Tr phi) := by
  -- === TCCs for stuck_hound ===
  -- TCC: stuck_hound_TCC1
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND length(w) > 1 IMPLIES 1 < length(w)
  have stuck_hound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: stuck_hound_TCC2
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND length(w) > 1 IMPLIES
  --    length(w) - 1 >= 0 AND length(w) - 1 < length(w)
  have stuck_hound_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: stuck_hound_TCC3
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND
  --    length(w) > 1 AND
  --     phi(finseq_appl[T](w)(1)) = finseq_appl[T](w)(length(w) - 1)
  --    IMPLIES
  --    F...
  have stuck_hound_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: stuck_hound_TCC4
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND
  --    length(w) > 1 AND
  --     phi(finseq_appl[T](w)(1)) = finseq_appl[T](w)(length(w) - 1)
  --    IMPLIES
  --    F...
  have stuck_hound_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.whole_hound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Tr: Tree, phi: map(Tr, Tr)):
--   path_from?(Tr, w, x, phi(x)) AND
--    length(w) > 2 AND
--     phi(finseq_appl[T](w)(1)) = finseq_appl[T](w)(length(w) - 2)
--    IMPLIES
--    EXISTS (p: prewalk):
--      pa
theorem whole_hound : ∀ (Tr : {G : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // tree_p G}) (phi : {phi : {psi : T → T // ∀ (x : T), vert Tr(x) → vert Tr(psi x)} // ∀ (x : _) (y : T), edge_p Tr(x, y) → edge_p Tr(phi x, phi y) ∨ phi x = phi y}), path_from_p Tr w x (phi x) ∧
 List.length w > 2 ∧
  phi (finseq_appl w(1)) = finseq_appl w(List.length w - 2) → ∃ (p : prewalk), path_from_p Tr p (finseq_appl w(1)) (phi (finseq_appl w(1))) ∧
   List.length p < List.length w ∧
    (List.length p > 1 →
      finseq_appl p(List.length p - 2) = finseq_appl w(List.length w - 3)) := by
  -- === TCCs for whole_hound ===
  -- TCC: whole_hound_TCC1
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND length(w) > 2 IMPLIES 1 < length(w)
  have whole_hound_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: whole_hound_TCC2
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND length(w) > 2 IMPLIES
  --    length(w) - 2 >= 0 AND length(w) - 2 < length(w)
  have whole_hound_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: whole_hound_TCC3
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND
  --    length(w) > 2 AND
  --     phi(finseq_appl[T](w)(1)) = finseq_appl[T](w)(length(w) - 2)
  --    IMPLIES
  --    F...
  have whole_hound_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: whole_hound_TCC4
  -- Statement: FORALL (w: prewalk[T], x: T, Tr: Tree[T], phi: map(Tr, Tr)):
  --   path_from?(Tr, w, x, phi(x)) AND
  --    length(w) > 2 AND
  --     phi(finseq_appl[T](w)(1)) = finseq_appl[T](w)(length(w) - 2)
  --    IMPLIES
  --    F...
  have whole_hound_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.fixed_fox
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Tr: Tree, phi: map(Tr, Tr)):
--   (EXISTS (G, H: Subgraph(Tr)): Fox(Tr, G, H, phi)) OR fixed?(Tr, phi)
theorem fixed_fox : ∀ (Tr : {G : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // tree_p G}) (phi : {phi : {psi : T → T // ∀ (x : T), vert Tr(x) → vert Tr(psi x)} // ∀ (x : _) (y : T), edge_p Tr(x, y) → edge_p Tr(phi x, phi y) ∨ phi x = phi y}), (∃ (G : _) (H : Subgraph Tr), Fox Tr G H phi) ∨ fixed_p Tr phi := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.small_fox
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (Tr: Tree, phi: map(Tr, Tr), (G, H: Subgraph(Tr))):
--   Fox(Tr, G, H, phi) IMPLIES
--    (EXISTS (GG, HH: Subgraph(Tr)):
--       Fox(Tr, GG, HH, phi) AND
--        lsth((size(GG), size(HH)), (size(G), si
theorem small_fox : ∀ (Tr : {G : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // tree_p G}) (phi : {phi : {psi : T → T // ∀ (x : T), vert Tr(x) → vert Tr(psi x)} // ∀ (x : _) (y : T), edge_p Tr(x, y) → edge_p Tr(phi x, phi y) ∨ phi x = phi y}) (G : {S : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // subgraph_p S Tr}) (H : {S : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // subgraph_p S Tr}), Fox Tr G H phi → (∃ (GG : _) (HH : Subgraph Tr), Fox Tr GG HH phi ∧ lsth ((size GG, size HH)) ((size G, size H)))
 ∨ fixed_p Tr phi := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.num_edge_tree
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?(G) AND num_edges(G) < size(G) IMPLIES tree?(G)
theorem num_edge_tree : connected_p G ∧ num_edges G < size G → tree_p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.uniq_paths?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t, p, q):
--   path_from?(G, p, s, t) AND path_from?(G, q, s, t) IMPLIES p = q
axiom uniq_paths_p {pvs_τ72 : Type*} : pvs_τ72 → Prop

-- pvs2lean: mappings.uniq_del_vert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: (vert(G))): uniq_paths?(G) IMPLIES uniq_paths?(del_vert(G, v))
theorem uniq_del_vert : ∀ (v : T), uniq_paths_p G → uniq_paths_p (del_vert G v) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.del_edge_uniq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (e: (edges(G))):
--   connected?(del_edge(G, e)) IMPLIES NOT uniq_paths?(G)
theorem del_edge_uniq : ∀ (e : {S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y}), connected_p (del_edge G e) → ¬ uniq_paths_p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.charact_tree
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?(G) AND uniq_paths?(G) IMPLIES tree?(G)
theorem charact_tree : connected_p G ∧ uniq_paths_p G → tree_p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.iff_tree
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: tree?(G) IFF connected?(G) AND uniq_paths?(G)
theorem iff_tree : tree_p G ↔ connected_p G ∧ uniq_paths_p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.tree_num_iff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?(G) AND num_edges(G) = size(G) - 1 IFF tree?(G)
theorem tree_num_iff : connected_p G ∧ num_edges G = size G - 1 ↔ tree_p G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.tree_map
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (phi: map(G, G)): tree?(G) IMPLIES fixed?(G, phi)
theorem tree_map : ∀ (phi : {phi : {psi : T → T // ∀ (x : T), vert G(x) → vert G(psi x)} // ∀ (x : _) (y : T), edge_p G(x, y) → edge_p G(phi x, phi y) ∨ phi x = phi y}), tree_p G → fixed_p G phi := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.sub_cycle
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Seq(H)): subgraph?(H, G) AND cycle?(H, w) IMPLIES cycle?(G, w)
theorem sub_cycle : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p H w}), subgraph_p H G ∧ cycle_p H w → cycle_p G w := by
  -- === TCCs for sub_cycle ===
  -- TCC: sub_cycle_TCC1
  -- Statement: FORALL (G, H: graph[T], w: Seq[T](H)):
  --   subgraph?(H, G) AND cycle?(H, w) IMPLIES verts_in?[T](G, w)
  have sub_cycle_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sub_cycle_TCC1
  -- Statement: FORALL (G, H: graph[T], w: Seq[T](H)):
  --   subgraph?(H, G) AND cycle?(H, w) IMPLIES verts_in?[T](G, w)
  have sub_cycle_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.exclus_cycl
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: connected?(G) IMPLIES tree?(G) XOR (EXISTS (w: Seq(G)): cycle?(G, w))
theorem exclus_cycl : connected_p G → tree_p G XOR (∃ (w : Seq G), cycle_p G w) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: mappings.deg_gt_1_cycle
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (v: T): vert(G)(v) IMPLIES deg(v, G) > 1) IMPLIES
--  (EXISTS (w: Seq(G)): cycle?(G, w)) OR empty?(G)
theorem deg_gt_1_cycle : (∀ (v : T), vert G(v) → deg v G > 1) → (∃ (w : Seq G), cycle_p G w) ∨ empty_p G := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.mappings