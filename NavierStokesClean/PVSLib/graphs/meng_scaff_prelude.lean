-- Auto-generated by pvs2lean/translator.py
-- PVS theory: meng_scaff_prelude
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: meng_scaff_prelude

Auto-generated stubs from PVSLib theory `meng_scaff_prelude`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.meng_scaff_prelude

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Long_walk_from : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (dbl : pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (edge_p : pvs_τ12 → pvs_τ13 → pvs_τ14 → Prop)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (from_p : pvs_τ15 → pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} (in_p : pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → Prop)
variable {pvs_τ23 : Type*} {pvs_τ24 : Type*} (independent_p : pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ35 : Type*} (length : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} (meng : pvs_τ36 → pvs_τ37 → pvs_τ38 → pvs_τ39 → pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} {pvs_τ45 : Type*} (path_from_p : pvs_τ42 → pvs_τ43 → pvs_τ44 → pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} (path_p : pvs_τ46 → pvs_τ47 → Prop)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} (rev : pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (sep_num : pvs_τ50 → pvs_τ51 → pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} (separable_p : pvs_τ53 → pvs_τ54 → pvs_τ55 → Prop)
variable {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} (separates : pvs_τ56 → pvs_τ57 → pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (seq : pvs_τ60 → pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} (size : pvs_τ62 → Prop)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} (trunc1 : pvs_τ63 → pvs_τ64)
variable {pvs_τ65 : Type*} {pvs_τ66 : Type*} (vert : pvs_τ65 → pvs_τ66 → Prop)
variable {pvs_τ67 : Type*} {pvs_τ68 : Type*} (verts_of : pvs_τ67 → pvs_τ68 → Prop)
variable {pvs_τ69 : Type*} {pvs_τ70 : Type*} {pvs_τ71 : Type*} {pvs_τ72 : Type*} (walk_from_p : pvs_τ69 → pvs_τ70 → pvs_τ71 → pvs_τ72 → Prop)
variable {pvs_τ73 : Type*} {pvs_τ74 : Type*} (walk_p : pvs_τ73 → pvs_τ74 → Prop)

-- pvs2lean: meng_scaff_prelude.induction_step
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (GG: graph[T]):
--   size(GG) < size(G) IMPLIES
--    FORALL (s: T, t: T):
--      separable?(GG, s, t) AND
--       sep_num(GG, s, t) = 2 AND vert(GG)(s) AND vert(GG)(t)
--       IMPLIES
--       (EXISTS (p1, p
axiom induction_step {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ25 → pvs_τ26 → pvs_τ27 → Prop

-- pvs2lean: meng_scaff_prelude.line20
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt:      separable?(G, s, t) AND in?(G, s, t, w1, w2) AND (sep_num(G, s, t) = 2)
--  AND separates(G, dbl(w1, w2), s, t) AND disjoint?(s, t, w1, w2)
--  AND (MM = meng(G, s, t, w1, w2)) AND induction_step(G, s,
theorem line20 : separable_p G s t ∧ in_p G s t w1 w2 ∧ (sep_num G s t = 2)
 ∧ separates G (dbl w1 w2) s t ∧ disjoint_p s t w1 w2
 ∧ (MM = meng G s t w1 w2) ∧ induction_step G s t
 ∧ ¬ (edge_p G(w1, t) ∧ edge_p G(w2, t)) → (∃ (P1 : _) (P2 : prewalk), path_from_p MM P1 s t ∧ (List.length P1 > 2)
    ∧ path_from_p MM P2 s t ∧ (List.length P2 > 2)
    ∧ (seq P1(List.length P1 - 2) = w1) ∧ (¬ verts_of P1(w2))
    ∧ (seq P2(List.length P2 - 2) = w2) ∧ (¬ verts_of P2(w1))
    ∧ independent_p P1 P2) := by
  -- === TCCs for line20 ===
  -- TCC: line20_TCC1
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND separates(G, dbl(w1, w2), s, t)
  --    AND (sep_num(G, s, t) = 2) AND in?(G, s, t, w1, w2)
  --    AND separable?(G, s, t)
  --    IMPLIE...
  have line20_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line20_TCC2
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND separates(G, dbl(w1, w2), s, t)
  --    AND (sep_num(G, s, t) = 2) AND in?(G, s, t, w1, w2)
  --    AND separable?(G, s, t)
  --    IMPLIE...
  have line20_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line20_TCC3
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND separates(G, dbl(w1, w2), s, t)
  --    AND (sep_num(G, s, t) = 2) AND in?(G, s, t, w1, w2)
  --    AND separable?(G, s, t)
  --    IMPLIE...
  have line20_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line20_TCC4
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND separates(G, dbl(w1, w2), s, t)
  --    AND (sep_num(G, s, t) = 2) AND in?(G, s, t, w1, w2)
  --    AND separable?(G, s, t)
  --    IMPLIE...
  have line20_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line20_TCC5
  -- Statement: FORALL (G, MM: graph[T], s, t, w1, w2: T):
  --        (NOT (edge?(G)(w1, t) AND edge?(G)(w2, t)))
  --    AND induction_step(G, s, t) AND (MM = meng(G, s, t, w1, w2))
  --    AND disjoint?(s, t, w1, w2) AND sepa...
  have line20_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line20_TCC6
  -- Statement: FORALL (G, MM: graph[T], s, t, w1, w2: T):
  --        (NOT (edge?(G)(w1, t) AND edge?(G)(w2, t)))
  --    AND induction_step(G, s, t) AND (MM = meng(G, s, t, w1, w2))
  --    AND disjoint?(s, t, w1, w2) AND sepa...
  have line20_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.join2
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: p1 ^ (0, length(p1) - 2) o rev(p2)
axiom join2 {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ28 → pvs_τ29 → pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34

-- pvs2lean: meng_scaff_prelude.path_meng_t
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: path_from?(G, p, s, t) AND length(p) > 0 IMPLIES
--  (i < length(p) AND seq(p)(i) = t IMPLIES i = length(p) - 1)
theorem path_meng_t : path_from_p G p s t ∧ List.length p > 0 → (i < List.length p ∧ seq p(i) = t → i = List.length p - 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.path_H_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: path?(H(G, s, w1, w2), p) AND j < length(p) IMPLIES
--  walk?(del_verts[T](G, dbl[T](w1, w2)), p ^ (0, j))
theorem path_H_def : path_p (H G s w1 w2) p ∧ j < List.length p → walk_p (del_verts G (dbl[T](w1, w2))) (p ^ (0, j)) := by
  -- === TCCs for path_H_def ===
  -- TCC: path_H_def_TCC1
  -- Statement: FORALL (G: graph[T], j: nat, p: prewalk[T], s, w1, w2: T):
  --   j < length(p) AND path?(H(G, s, w1, w2), p) IMPLIES
  --    length(^[T](p, (0, j))) > 0
  have path_h_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.path_H_ind
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (length(p1) > 1) AND (length(p2) > 1)
--  AND path?(H(G, s, w1, w2), trunc1(p1))
--  AND path?(H(G, t, w1, w2), trunc1(p2)) AND separates(G, dbl(w1, w2), s, t)
--  AND (seq(p1)(0) = s) AND (seq(p2)(0) = t
theorem path_H_ind : (List.length p1 > 1) ∧ (List.length p2 > 1)
 ∧ path_p (H G s w1 w2) (trunc1 p1)
 ∧ path_p (H G t w1 w2) (trunc1 p2) ∧ separates G (dbl w1 w2) s t
 ∧ (seq p1(0) = s) ∧ (seq p2(0) = t) → independent_p p1 p2 := by
  -- === TCCs for path_H_ind ===
  -- TCC: path_H_ind_TCC1
  -- Statement: FORALL (p1, p2: prewalk[T]):
  --   length(p2) > 1 AND length(p1) > 1 IMPLIES length(p1) >= 2
  have path_h_ind_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_H_ind_TCC2
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, w1, w2: T):
  --   path?(H(G, s, w1, w2), trunc1(p1)) AND length(p2) > 1 AND length(p1) > 1
  --    IMPLIES length(p2) >= 2
  have path_h_ind_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_H_ind_TCC3
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, w1, w2: T):
  --        separates(G, dbl(w1, w2), s, t)
  --    AND path?(H(G, t, w1, w2), trunc1(p2))
  --    AND path?(H(G, s, w1, w2), trunc1(p1)) AND (length(p2)...
  have path_h_ind_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_H_ind_TCC4
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, w1, w2: T):
  --        (seq(p1)(0) = s) AND separates(G, dbl(w1, w2), s, t)
  --    AND path?(H(G, t, w1, w2), trunc1(p2))
  --    AND path?(H(G, s, w1, w2), trunc1...
  have path_h_ind_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.path_comps_ind3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (length(p1) > 2) AND (length(p2) > 2)
--  AND path?(H(G, s, w1, w2), trunc1(trunc1(p1)))
--  AND path?(H(G, t, w1, w2), trunc1(trunc1(p2)))
--  AND separates(G, dbl(w1, w2), s, t) AND (seq(p1)(0) = s)
--  AN
theorem path_comps_ind3 : (List.length p1 > 2) ∧ (List.length p2 > 2)
 ∧ path_p (H G s w1 w2) (trunc1 (trunc1 p1))
 ∧ path_p (H G t w1 w2) (trunc1 (trunc1 p2))
 ∧ separates G (dbl w1 w2) s t ∧ (seq p1(0) = s)
 ∧ (seq p2(0) = t) → independent_p (trunc1 p1) (trunc1 p2) := by
  -- === TCCs for path_comps_ind3 ===
  -- TCC: path_comps_ind3_TCC1
  -- Statement: FORALL (p1, p2: prewalk[T]):
  --   length(p2) > 2 AND length(p1) > 2 IMPLIES length(p1) >= 2
  have path_comps_ind3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_comps_ind3_TCC2
  -- Statement: FORALL (p1, p2: prewalk[T]):
  --   length(p2) > 2 AND length(p1) > 2 IMPLIES length(trunc1[T](p1)) >= 2
  have path_comps_ind3_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_comps_ind3_TCC3
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, w1, w2: T):
  --   path?(H(G, s, w1, w2), trunc1(trunc1(p1))) AND
  --    length(p2) > 2 AND length(p1) > 2
  --    IMPLIES length(p2) >= 2
  have path_comps_ind3_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_comps_ind3_TCC4
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, w1, w2: T):
  --   path?(H(G, s, w1, w2), trunc1(trunc1(p1))) AND
  --    length(p2) > 2 AND length(p1) > 2
  --    IMPLIES length(trunc1[T](p2)) >= 2
  have path_comps_ind3_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_comps_ind3_TCC5
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, w1, w2: T):
  --        separates(G, dbl(w1, w2), s, t)
  --    AND path?(H(G, t, w1, w2), trunc1(trunc1(p2)))
  --    AND path?(H(G, s, w1, w2), trunc1(trunc1(p1)))...
  have path_comps_ind3_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_comps_ind3_TCC6
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, w1, w2: T):
  --        (seq(p1)(0) = s) AND separates(G, dbl(w1, w2), s, t)
  --    AND path?(H(G, t, w1, w2), trunc1(trunc1(p2)))
  --    AND path?(H(G, s, w1, w2)...
  have path_comps_ind3_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.path_H_trunc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (length(p) >= 2) AND path?(H(G, s, w1, w2), trunc1(p))
--  AND from?(p, s, w1)
--  AND edge?(G)
--           (finseq_appl[T](p)(length(p) - 2),
--            finseq_appl[T](p)(length(p) - 1))
--  AND vert(G)(s)
theorem path_H_trunc : (List.length p ≥ 2) ∧ path_p (H G s w1 w2) (trunc1 p)
 ∧ from_p p s w1
 ∧ edge_p G
          (finseq_appl p(List.length p - 2),
           finseq_appl p(List.length p - 1))
 ∧ vert G(s) ∧ (w1 ≠ s) ∧ vert G(t) ∧ (w2 ≠ s) ∧ vert G(w1)
 ∧ vert G(w2) → path_from_p G p s w1 := by
  -- === TCCs for path_H_trunc ===
  -- TCC: path_H_trunc_TCC1
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, w1, w2: T):
  --   from?(p, s, w1) AND path?(H(G, s, w1, w2), trunc1(p)) AND length(p) >= 2
  --    IMPLIES length(p) - 2 >= 0 AND length(p) - 2 < length(p)
  have path_h_trunc_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_H_trunc_TCC2
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, w1, w2: T):
  --   from?(p, s, w1) AND path?(H(G, s, w1, w2), trunc1(p)) AND length(p) >= 2
  --    IMPLIES length(p) - 1 >= 0 AND length(p) - 1 < length(p)
  have path_h_trunc_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.meng_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (length(p) > 2) AND vert(G)(s) AND (w1 /= s) AND (w1 /= t)
--  AND vert(G)(t) AND (w2 /= s) AND (w2 /= t) AND vert(G)(w1) AND vert(G)(w2)
--  AND path_from?(meng(G, s, t, w1, w2), p, s, t)
--  IMPLIES edg
theorem meng_last : (List.length p > 2) ∧ vert G(s) ∧ (w1 ≠ s) ∧ (w1 ≠ t)
 ∧ vert G(t) ∧ (w2 ≠ s) ∧ (w2 ≠ t) ∧ vert G(w1) ∧ vert G(w2)
 ∧ path_from_p (meng G s t w1 w2) p s t → edge_p G(seq p(List.length p - 3), seq p(List.length p - 2)) := by
  -- === TCCs for meng_last ===
  -- TCC: meng_last_TCC1
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --        vert(G)(w2) AND vert(G)(w1) AND (w2 /= t) AND (w2 /= s)
  --    AND vert(G)(t) AND (w1 /= t) AND (w1 /= s) AND vert(G)(s)
  --    AND (length(p) >...
  have meng_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meng_last_TCC2
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --        vert(G)(w2) AND vert(G)(w1) AND (w2 /= t) AND (w2 /= s)
  --    AND vert(G)(t) AND (w1 /= t) AND (w1 /= s) AND vert(G)(s)
  --    AND (length(p) >...
  have meng_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meng_last_TCC3
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --        path_from?(meng(G, s, t, w1, w2), p, s, t) AND vert(G)(w2)
  --    AND vert(G)(w1) AND (w2 /= t) AND (w2 /= s) AND vert(G)(t) AND (w1 /= t)
  --  ...
  have meng_last_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meng_last_TCC4
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --        path_from?(meng(G, s, t, w1, w2), p, s, t) AND vert(G)(w2)
  --    AND vert(G)(w1) AND (w2 /= t) AND (w2 /= s) AND vert(G)(t) AND (w1 /= t)
  --  ...
  have meng_last_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.ind_verts_W
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      path_from?(G, p1, s, t) AND path_from?(G, p2, t, s)
--  AND (length(p1) > 2) AND (seq(p1)(length(p1) - 2) = w1)
--  AND (length(p2) > 2) AND (seq(p2)(length(p2) - 2) = w2)
--  AND independent?(p1, p2) AND
theorem ind_verts_W : path_from_p G p1 s t ∧ path_from_p G p2 t s
 ∧ (List.length p1 > 2) ∧ (seq p1(List.length p1 - 2) = w1)
 ∧ (List.length p2 > 2) ∧ (seq p2(List.length p2 - 2) = w2)
 ∧ independent_p p1 p2 ∧ (w1 ≠ s) ∧ (w1 ≠ t) ∧ (w2 ≠ s)
 ∧ (w2 ≠ t) → ¬ verts_of p1(w2) ∧ ¬ verts_of p2(w1) := by
  -- === TCCs for ind_verts_W ===
  -- TCC: ind_verts_W_TCC1
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t: T):
  --   length(p1) > 2 AND path_from?(G, p2, t, s) AND path_from?(G, p1, s, t)
  --    IMPLIES length(p1) - 2 >= 0 AND length(p1) - 2 < p1`length
  have ind_verts_w_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ind_verts_W_TCC2
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, w1: T):
  --        (length(p2) > 2) AND (seq(p1)(length(p1) - 2) = w1)
  --    AND (length(p1) > 2) AND path_from?(G, p2, t, s)
  --    AND path_from?(G, p1, s, t)
  -- ...
  have ind_verts_w_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.ind_verts_w
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      path_from?(G, p1, s, t) AND path_from?(G, p2, s, t)
--  AND (length(p1) > 2) AND (seq(p1)(length(p1) - 2) = w1)
--  AND (length(p2) > 2) AND (seq(p2)(length(p2) - 2) = w2)
--  AND independent?(p1, p2) AND
theorem ind_verts_w : path_from_p G p1 s t ∧ path_from_p G p2 s t
 ∧ (List.length p1 > 2) ∧ (seq p1(List.length p1 - 2) = w1)
 ∧ (List.length p2 > 2) ∧ (seq p2(List.length p2 - 2) = w2)
 ∧ independent_p p1 p2 ∧ (w1 ≠ s) ∧ (w1 ≠ t) ∧ (w2 ≠ s)
 ∧ (w2 ≠ t) → ¬ verts_of p1(w2) ∧ ¬ verts_of p2(w1) := by
  -- === TCCs for ind_verts_w ===
  -- TCC: ind_verts_w_TCC1
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t: T):
  --   length(p1) > 2 AND path_from?(G, p2, s, t) AND path_from?(G, p1, s, t)
  --    IMPLIES length(p1) - 2 >= 0 AND length(p1) - 2 < p1`length
  have ind_verts_w_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ind_verts_w_TCC2
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, w1: T):
  --        (length(p2) > 2) AND (seq(p1)(length(p1) - 2) = w1)
  --    AND (length(p1) > 2) AND path_from?(G, p2, s, t)
  --    AND path_from?(G, p1, s, t)
  -- ...
  have ind_verts_w_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.path_meng_in
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (length(p) > 2) AND vert(G)(s) AND (w1 /= s) AND (w1 /= t)
--  AND vert(G)(t) AND (w2 /= s) AND (w2 /= t) AND vert(G)(w1) AND vert(G)(w2)
--  AND path?(meng(G, s, t, w1, w2), p) AND from?(p, s, t)
--  AND
theorem path_meng_in : (List.length p > 2) ∧ vert G(s) ∧ (w1 ≠ s) ∧ (w1 ≠ t)
 ∧ vert G(t) ∧ (w2 ≠ s) ∧ (w2 ≠ t) ∧ vert G(w1) ∧ vert G(w2)
 ∧ path_p (meng G s t w1 w2) p ∧ from_p p s t
 ∧ ((seq p(List.length p - 2) = w1 ∧ ¬ verts_of p(w2)) ∨
       (seq p(List.length p - 2) = w2 ∧ ¬ verts_of p(w1))) → path_p (H G s w1 w2) (trunc1 (trunc1 p)) := by
  -- === TCCs for path_meng_in ===
  -- TCC: path_meng_in_TCC1
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --        from?(p, s, t) AND path?(meng(G, s, t, w1, w2), p) AND vert(G)(w2)
  --    AND vert(G)(w1) AND (w2 /= t) AND (w2 /= s) AND vert(G)(t) AND (w1...
  have path_meng_in_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_meng_in_TCC2
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --        ((seq(p)(length(p) - 2) = w1 AND NOT verts_of(p)(w2)) OR
  --          (seq(p)(length(p) - 2) = w2 AND NOT verts_of(p)(w1)))
  --    AND from?(p, ...
  have path_meng_in_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: path_meng_in_TCC3
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --        ((seq(p)(length(p) - 2) = w1 AND NOT verts_of(p)(w2)) OR
  --          (seq(p)(length(p) - 2) = w2 AND NOT verts_of(p)(w1)))
  --    AND from?(p, ...
  have path_meng_in_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.disjoint?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i, j: nat):
--    i < length(w1) AND j < length(w2) IMPLIES seq(w1)(i) /= seq(w2)(j))
axiom disjoint_p {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: meng_scaff_prelude.join2_lem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      path_from?(G, p1, s, v) AND path_from?(G, p2, t, v) AND (s /= v)
--  AND (t /= v) AND (s /= t) AND disjoint?(trunc1(p1), trunc1(p2))
--  IMPLIES path_from?(G, join2(G, s, t, v, p1, p2), s, t)
theorem join2_lem : path_from_p G p1 s v ∧ path_from_p G p2 t v ∧ (s ≠ v)
 ∧ (t ≠ v) ∧ (s ≠ t) ∧ disjoint_p (trunc1 p1) (trunc1 p2) → path_from_p G (join2 G s t v p1 p2) s t := by
  -- === TCCs for join2_lem ===
  -- TCC: join2_lem_TCC1
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, v: T):
  --        (s /= t) AND (t /= v) AND (s /= v) AND path_from?(G, p2, t, v)
  --    AND path_from?(G, p1, s, v)
  --    IMPLIES length(p1) >= 2
  have join2_lem_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: join2_lem_TCC2
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, v: T):
  --        (s /= t) AND (t /= v) AND (s /= v) AND path_from?(G, p2, t, v)
  --    AND path_from?(G, p1, s, v)
  --    IMPLIES length(p2) >= 2
  have join2_lem_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: join2_lem_TCC3
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, v: T):
  --        disjoint?(trunc1(p1), trunc1(p2)) AND (s /= t) AND (t /= v)
  --    AND (s /= v) AND path_from?(G, p2, t, v) AND path_from?(G, p1, s, v)
  --    I...
  have join2_lem_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: join2_lem_TCC4
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, v: T):
  --        disjoint?(trunc1(p1), trunc1(p2)) AND (s /= t) AND (t /= v)
  --    AND (s /= v) AND path_from?(G, p2, t, v) AND path_from?(G, p1, s, v)
  --    I...
  have join2_lem_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.path_from_to_walk_from
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: path_from?(G, p, s, t) IMPLIES walk_from?(G, p, s, t)
theorem path_from_to_walk_from : path_from_p G p s t → walk_from_p G p s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.paths_H_disj
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      path?(H(G, t, w1, w2), p2) AND path?(H(G, s, w1, w2), p1)
--  AND separates(G, dbl(w1, w2), s, t) AND (seq(p1)(0) = s)
--  AND (seq(p2)(0) = t) AND (s /= t)
--  IMPLIES disjoint?(p1, p2)
theorem paths_H_disj : path_p (H G t w1 w2) p2 ∧ path_p (H G s w1 w2) p1
 ∧ separates G (dbl w1 w2) s t ∧ (seq p1(0) = s)
 ∧ (seq p2(0) = t) ∧ (s ≠ t) → disjoint_p p1 p2 := by
  -- === TCCs for paths_H_disj ===
  -- TCC: paths_H_disj_TCC1
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, w1, w2: T):
  --   separates(G, dbl(w1, w2), s, t) AND
  --    path?(H(G, s, w1, w2), p1) AND path?(H(G, t, w1, w2), p2)
  --    IMPLIES 0 < p1`length
  have paths_h_disj_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: paths_H_disj_TCC2
  -- Statement: FORALL (G: graph[T], p1, p2: prewalk[T], s, t, w1, w2: T):
  --   seq(p1)(0) = s AND
  --    separates(G, dbl(w1, w2), s, t) AND
  --     path?(H(G, s, w1, w2), p1) AND path?(H(G, t, w1, w2), p2)
  --    IMPLIES 0 < p...
  have paths_h_disj_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff_prelude.prelude
-- pvs_status: unproved  lean_tactic_class: induction
-- stmt_confidence: high  [ast]
-- pvs_stmt:      separates(G, dbl(w1, w2), s, t) AND separable?(G, s, t)
--  AND (sep_num(G, s, t) = 2) AND induction_step(G, s, t)
--  AND in?(G, s, t, w1, w2) AND disjoint?(s, t, w1, w2)
--  AND (NOT (edge?(G)(w1, s) AN
theorem prelude : separates G (dbl w1 w2) s t ∧ separable_p G s t
 ∧ (sep_num G s t = 2) ∧ induction_step G s t
 ∧ in_p G s t w1 w2 ∧ disjoint_p s t w1 w2
 ∧ (¬ (edge_p G(w1, s) ∧ edge_p G(w2, s)))
 ∧ ¬ (edge_p G(w1, t) ∧ edge_p G(w2, t)) → (∃ (p1 : _) (p2 : prewalk), path_from_p G p1 s t ∧
    path_from_p G p2 s t ∧ independent_p p1 p2) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.meng_scaff_prelude