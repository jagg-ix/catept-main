-- Auto-generated by pvs2lean/translator.py
-- PVS theory: meng_scaff
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: meng_scaff

Auto-generated stubs from PVSLib theory `meng_scaff`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.meng_scaff

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (dbl : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (disjoint_p : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (edge_p : pvs_τ8 → pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (in_p : pvs_τ13 → pvs_τ14 → pvs_τ15 → pvs_τ16 → pvs_τ17 → Prop)
variable {pvs_τ18 : Type*} (length : pvs_τ18 → Prop)
variable {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (meng : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (min : pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} (nonempty_p : pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (path_from_p : pvs_τ28 → pvs_τ29 → pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (ps_ptype : pvs_τ32 → pvs_τ33)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} (sep_num : pvs_τ34 → pvs_τ35 → pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} (separable_p : pvs_τ37 → pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} (separates : pvs_τ40 → pvs_τ41 → pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (seq : pvs_τ44 → pvs_τ45 → Prop)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} (singleton : pvs_τ46 → pvs_τ47)
variable {pvs_τ48 : Type*} (size : pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} (vert : pvs_τ49 → pvs_τ50 → Prop)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} (verts_of : pvs_τ51 → pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} (walk_from_p : pvs_τ53 → pvs_τ54 → pvs_τ55 → pvs_τ56 → Prop)

-- pvs2lean: meng_scaff.first
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min({i: nat | i < length(ps) AND P(seq(ps)(i))})
axiom first {pvs_τ11 : Type*} {pvs_τ12 : Type*} : pvs_τ11 → pvs_τ12 → Prop

-- pvs2lean: meng_scaff.first_lem
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ps: ps_ptype(P)): P(seq(ps)(first(P, ps)))
theorem first_lem : ∀ (ps : {ps : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // ∃ (i : Finset.range List.length ps), P (seq ps(i))}), P (seq ps (first P ps)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.first_not
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ps: ps_ptype(P)): i < first(P, ps) IMPLIES NOT P(seq(ps)(i))
theorem first_not : ∀ (ps : {ps : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // ∃ (i : Finset.range List.length ps), P (seq ps(i))}), i < first P ps → ¬ P (seq ps(i)) := by
  -- === TCCs for first_not ===
  -- TCC: first_not_TCC1
  -- Statement: FORALL (P: pred[T], i: nat, ps: ps_ptype(P)):
  --   i < first(P, ps) IMPLIES i < ps`length
  have first_not_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.meng_to_G_sep
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      walk_from?(G, p, s, t) AND in?(G, s, t, w1, w2)
--  AND disjoint?(s, t, w1, w2)
--  AND separates(meng(G, s, t, w1, w2), singleton(z), s, t)
--  AND separable?(G, s, t) AND separates(G, dbl(w1, w2), s, t)
theorem meng_to_G_sep : walk_from_p G p s t ∧ in_p G s t w1 w2
 ∧ disjoint_p s t w1 w2
 ∧ separates (meng G s t w1 w2) (singleton z) s t
 ∧ separable_p G s t ∧ separates G (dbl w1 w2) s t
 ∧ ¬ verts_of p(z) → sep_num G s t ≤ 1 := by
  -- === TCCs for meng_to_G_sep ===
  -- TCC: meng_to_G_sep_TCC1
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --   disjoint?(s, t, w1, w2) AND
  --    in?(G, s, t, w1, w2) AND walk_from?(G, p, s, t)
  --    IMPLIES vert(G)(s)
  have meng_to_g_sep_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meng_to_G_sep_TCC2
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --   disjoint?(s, t, w1, w2) AND
  --    in?(G, s, t, w1, w2) AND walk_from?(G, p, s, t)
  --    IMPLIES vert(G)(t)
  have meng_to_g_sep_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meng_to_G_sep_TCC3
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --   disjoint?(s, t, w1, w2) AND
  --    in?(G, s, t, w1, w2) AND walk_from?(G, p, s, t)
  --    IMPLIES vert(G)(w1) AND w1 /= t
  have meng_to_g_sep_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: meng_to_G_sep_TCC4
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --   disjoint?(s, t, w1, w2) AND
  --    in?(G, s, t, w1, w2) AND walk_from?(G, p, s, t)
  --    IMPLIES vert(G)(w2) AND w2 /= t
  have meng_to_g_sep_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.prep_14
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      vert(G)(s) AND vert(G)(t) AND vert(G)(w1) AND vert(G)(w2)
--  AND disjoint?(s, t, w1, w2) AND (sep_num(meng(G, s, t, w1, w2), s, t) <= 1)
--  AND separates(meng(G, s, t, w1, w2), dbl(w1, w2), s, t)
--  AN
theorem prep_14 : vert G(s) ∧ vert G(t) ∧ vert G(w1) ∧ vert G(w2)
 ∧ disjoint_p s t w1 w2 ∧ (sep_num (meng G s t w1 w2) s t ≤ 1)
 ∧ separates (meng G s t w1 w2) (dbl w1 w2) s t
 ∧ separable_p G s t ∧ separates G (dbl w1 w2) s t
 ∧ (sep_num G s t = 2) → sep_num G s t ≤ 1 := by
  -- === TCCs for prep_14 ===
  -- TCC: prep_14_TCC1
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND vert(G)(w2) AND vert(G)(w1)
  --    AND vert(G)(t) AND vert(G)(s)
  --    IMPLIES vert(G)(w1) AND w1 /= t
  have prep_14_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: prep_14_TCC2
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND vert(G)(w2) AND vert(G)(w1)
  --    AND vert(G)(t) AND vert(G)(s)
  --    IMPLIES vert(G)(w2) AND w2 /= t
  have prep_14_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.line14
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      separable?(G, s, t) AND in?(G, s, t, w1, w2) AND (sep_num(G, s, t) = 2)
--  AND separates(G, dbl(w1, w2), s, t) AND disjoint?(s, t, w1, w2)
--  IMPLIES sep_num(meng(G, s, t, w1, w2), s, t) = 2
theorem line14 : separable_p G s t ∧ in_p G s t w1 w2 ∧ (sep_num G s t = 2)
 ∧ separates G (dbl w1 w2) s t ∧ disjoint_p s t w1 w2 → sep_num (meng G s t w1 w2) s t = 2 := by
  -- === TCCs for line14 ===
  -- TCC: line14_TCC1
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND separates(G, dbl(w1, w2), s, t)
  --    AND (sep_num(G, s, t) = 2) AND in?(G, s, t, w1, w2)
  --    AND separable?(G, s, t)
  --    IMPLIE...
  have line14_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line14_TCC2
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND separates(G, dbl(w1, w2), s, t)
  --    AND (sep_num(G, s, t) = 2) AND in?(G, s, t, w1, w2)
  --    AND separable?(G, s, t)
  --    IMPLIE...
  have line14_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line14_TCC3
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND separates(G, dbl(w1, w2), s, t)
  --    AND (sep_num(G, s, t) = 2) AND in?(G, s, t, w1, w2)
  --    AND separable?(G, s, t)
  --    IMPLIE...
  have line14_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line14_TCC4
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --        disjoint?(s, t, w1, w2) AND separates(G, dbl(w1, w2), s, t)
  --    AND (sep_num(G, s, t) = 2) AND in?(G, s, t, w1, w2)
  --    AND separable?(G, s, t)
  --    IMPLIE...
  have line14_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.line15ab
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separable?(G, s, t) AND
--  in?(G, s, t, w1, w2) AND
--   sep_num(G, s, t) = 2 AND separates(G, dbl(w1, w2), s, t)
--  IMPLIES
--  ((EXISTS (Psw1: prewalk): path_from?(G, Psw1, s, w1)) AND
--    (EXISTS (Psw2: prewa
theorem line15ab : separable_p G s t ∧
 in_p G s t w1 w2 ∧
  sep_num G s t = 2 ∧ separates G (dbl w1 w2) s t → ((∃ (Psw1 : prewalk), path_from_p G Psw1 s w1) ∧
  (∃ (Psw2 : prewalk), path_from_p G Psw2 s w2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.prep_16
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      separates(G, dbl(w1, w2), s, t) AND disjoint?(s, t, w1, w2)
--  AND (FORALL p: path_from?(G, p, t, w1) IMPLIES verts_of(p)(w2))
--  AND vert(G)(s) AND vert(G)(t)
--  IMPLIES sep_num(G, s, t) <= 1
theorem prep_16 : separates G (dbl w1 w2) s t ∧ disjoint_p s t w1 w2
 ∧ (∀ p,  path_from_p G p t w1 → verts_of p(w2))
 ∧ vert G(s) ∧ vert G(t) → sep_num G s t ≤ 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.line16
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (NOT edge?(G)(w1, t)) AND disjoint?(s, t, w1, w2)
--  AND separates(G, dbl(w1, w2), s, t) AND (sep_num(G, s, t) = 2)
--  AND vert(G)(s) AND vert(G)(t)
--  IMPLIES EXISTS (yt: T): vert(H(G, t, w1, w2))(yt)
theorem line16 : (¬ edge_p G(w1, t)) ∧ disjoint_p s t w1 w2
 ∧ separates G (dbl w1 w2) s t ∧ (sep_num G s t = 2)
 ∧ vert G(s) ∧ vert G(t) → ∃ (yt : T), vert (H G t w1 w2) (yt) ∧ yt ≠ t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.line19
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      separable?(G, s, t) AND in?(G, s, t, w1, w2)
--  AND disjoint?(s, t, w1, w2) AND (MM = meng(G, s, t, w1, w2))
--  AND separates(G, dbl(w1, w2), s, t) AND path_from?(MM, p, s, t)
--  IMPLIES
--  length(p) > 2
theorem line19 : separable_p G s t ∧ in_p G s t w1 w2
 ∧ disjoint_p s t w1 w2 ∧ (MM = meng G s t w1 w2)
 ∧ separates G (dbl w1 w2) s t ∧ path_from_p MM p s t → List.length p > 2 ∧ (seq p(List.length p - 2) = w1 ∨ seq p(List.length p - 2) = w2) := by
  -- === TCCs for line19 ===
  -- TCC: line19_TCC1
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --   disjoint?(s, t, w1, w2) AND in?(G, s, t, w1, w2) AND separable?(G, s, t)
  --    IMPLIES vert(G)(s)
  have line19_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line19_TCC2
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --   disjoint?(s, t, w1, w2) AND in?(G, s, t, w1, w2) AND separable?(G, s, t)
  --    IMPLIES vert(G)(t)
  have line19_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line19_TCC3
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --   disjoint?(s, t, w1, w2) AND in?(G, s, t, w1, w2) AND separable?(G, s, t)
  --    IMPLIES vert(G)(w1) AND w1 /= t
  have line19_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line19_TCC4
  -- Statement: FORALL (G: graph[T], s, t, w1, w2: T):
  --   disjoint?(s, t, w1, w2) AND in?(G, s, t, w1, w2) AND separable?(G, s, t)
  --    IMPLIES vert(G)(w2) AND w2 /= t
  have line19_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line19_TCC5
  -- Statement: FORALL (G, MM: graph[T], p: prewalk[T], s, t, w1, w2: T):
  --        (length(p) > 2) AND path_from?(MM, p, s, t)
  --    AND separates(G, dbl(w1, w2), s, t) AND (MM = meng(G, s, t, w1, w2))
  --    AND disjoint?...
  have line19_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.line17_prep
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      vert(G)(s) AND vert(G)(t) AND disjoint?(s, t, w1, w2)
--  AND vert(H(G, t, w1, w2))(yt) AND separates(G, dbl(w1, w2), s, t)
--  IMPLIES NOT vert(H(G, s, w1, w2))(yt)
theorem line17_prep : vert G(s) ∧ vert G(t) ∧ disjoint_p s t w1 w2
 ∧ vert (H G t w1 w2) (yt) ∧ separates G (dbl w1 w2) s t → ¬ vert (H G s w1 w2) (yt) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.line17
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      vert(H(G, t, w1, w2))(yt) AND in?(G, s, t, w1, w2)
--  AND disjoint?(s, t, w1, w2) AND (yt /= t)
--  AND separates(G, dbl(w1, w2), s, t)
--  IMPLIES size(meng(G, s, t, w1, w2)) < size(G)
theorem line17 : vert (H G t w1 w2) (yt) ∧ in_p G s t w1 w2
 ∧ disjoint_p s t w1 w2 ∧ (yt ≠ t)
 ∧ separates G (dbl w1 w2) s t → size (meng G s t w1 w2) < size G := by
  -- === TCCs for line17 ===
  -- TCC: line17_TCC1
  -- Statement: FORALL (G: graph[T], s, t, w1, w2, yt: T):
  --        separates(G, dbl(w1, w2), s, t) AND (yt /= t)
  --    AND disjoint?(s, t, w1, w2) AND in?(G, s, t, w1, w2)
  --    AND vert(H(G, t, w1, w2))(yt)
  --    IMPLIES v...
  have line17_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line17_TCC2
  -- Statement: FORALL (G: graph[T], s, t, w1, w2, yt: T):
  --        separates(G, dbl(w1, w2), s, t) AND (yt /= t)
  --    AND disjoint?(s, t, w1, w2) AND in?(G, s, t, w1, w2)
  --    AND vert(H(G, t, w1, w2))(yt)
  --    IMPLIES v...
  have line17_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line17_TCC3
  -- Statement: FORALL (G: graph[T], s, t, w1, w2, yt: T):
  --        separates(G, dbl(w1, w2), s, t) AND (yt /= t)
  --    AND disjoint?(s, t, w1, w2) AND in?(G, s, t, w1, w2)
  --    AND vert(H(G, t, w1, w2))(yt)
  --    IMPLIES v...
  have line17_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: line17_TCC4
  -- Statement: FORALL (G: graph[T], s, t, w1, w2, yt: T):
  --        separates(G, dbl(w1, w2), s, t) AND (yt /= t)
  --    AND disjoint?(s, t, w1, w2) AND in?(G, s, t, w1, w2)
  --    AND vert(H(G, t, w1, w2))(yt)
  --    IMPLIES v...
  have line17_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: meng_scaff.pre20
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      separable?(G, s, t) AND in?(G, s, t, w1, w2) AND (sep_num(G, s, t) = 2)
--  AND separates(G, dbl(w1, w2), s, t) AND disjoint?(s, t, w1, w2)
--  IMPLIES
--  (separable?(meng(G, s, t, w1, w2), s, t) AND
--    
theorem pre20 : separable_p G s t ∧ in_p G s t w1 w2 ∧ (sep_num G s t = 2)
 ∧ separates G (dbl w1 w2) s t ∧ disjoint_p s t w1 w2 → (separable_p (meng G s t w1 w2) s t ∧
  sep_num (meng G s t w1 w2) s t = 2 ∧
   vert (meng G s t w1 w2) (s) ∧ vert (meng G s t w1 w2) (t)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.meng_scaff