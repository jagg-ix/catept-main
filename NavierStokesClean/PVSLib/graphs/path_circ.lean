-- Auto-generated by pvs2lean/translator.py
-- PVS theory: path_circ
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: path_circ

Auto-generated stubs from PVSLib theory `path_circ`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.path_circ

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Path : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (Seq : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (add1 : pvs_τ5 → pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (circuit_p : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cyclically_reduced_p : pvs_τ9 → pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (finseq_appl : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (from_p : pvs_τ13 → pvs_τ14 → pvs_τ15 → Prop)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} (gen_seq2 : pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} (length : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (path_from_p : pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (pre_circuit_p : pvs_τ25 → pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (reduced_p : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} (rev : pvs_τ29 → Prop)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} (seq : pvs_τ30 → pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} (trunc1 : pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} {pvs_τ34 : Type*} (vert : pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} (walk_p : pvs_τ35 → pvs_τ36 → Prop)

-- pvs2lean: path_circ.path_reduc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (p: Path(G)): reduced?(G, p)
theorem path_reduc : ∀ (p : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}), reduced_p G p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.trunc_long
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(u) >= 2 AND length(w) >= 2 IMPLIES length(trunc1(u) o w) >= 1
theorem trunc_long : List.length u ≥ 2 ∧ List.length w ≥ 2 → List.length (trunc1 u ∘ w) ≥ 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: path_circ.trunc_walk
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(u) >= 2 AND walk?(G, u) IMPLIES walk?(G, trunc1(u))
theorem trunc_walk : List.length u ≥ 2 ∧ walk_p G u → walk_p G (trunc1 u) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.walks_concat_red
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      walk?(G, u) AND walk?(G, w) AND (length(u) >= 2) AND (length(w) >= 2)
--  AND reduced?(G, u) AND reduced?(G, w)
--  AND (finseq_appl[T](u)(length(u) - 1) = finseq_appl[T](w)(0))
--  AND (finseq_appl[T](u)
theorem walks_concat_red : walk_p G u ∧ walk_p G w ∧ (List.length u ≥ 2) ∧ (List.length w ≥ 2)
 ∧ reduced_p G u ∧ reduced_p G w
 ∧ (finseq_appl u(List.length u - 1) = finseq_appl w(0))
 ∧ (finseq_appl u(List.length u - 2) ≠ finseq_appl w(1)) → reduced_p G (trunc1 u ∘ w) := by
  -- === TCCs for walks_concat_red ===
  -- TCC: walks_concat_red_TCC1
  -- Statement: FORALL (G: graph[T], u, w: prewalk[T]):
  --   length(w) >= 2 AND length(u) >= 2 AND walk?(G, w) AND walk?(G, u) IMPLIES
  --    verts_in?[T](G, u)
  have walks_concat_red_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walks_concat_red_TCC2
  -- Statement: FORALL (G: graph[T], u, w: prewalk[T]):
  --        reduced?(G, u) AND (length(w) >= 2) AND (length(u) >= 2)
  --    AND walk?(G, w) AND walk?(G, u)
  --    IMPLIES verts_in?[T](G, w)
  have walks_concat_red_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walks_concat_red_TCC3
  -- Statement: FORALL (G: graph[T], u, w: prewalk[T]):
  --        reduced?(G, w) AND reduced?(G, u) AND (length(w) >= 2)
  --    AND (length(u) >= 2) AND walk?(G, w) AND walk?(G, u)
  --    IMPLIES length(u) - 1 >= 0 AND lengt...
  have walks_concat_red_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walks_concat_red_TCC4
  -- Statement: FORALL (G: graph[T], u, w: prewalk[T]):
  --        reduced?(G, w) AND reduced?(G, u) AND (length(w) >= 2)
  --    AND (length(u) >= 2) AND walk?(G, w) AND walk?(G, u)
  --    IMPLIES 0 < length(w)
  have walks_concat_red_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walks_concat_red_TCC5
  -- Statement: FORALL (G: graph[T], u, w: prewalk[T]):
  --        (finseq_appl[T](u)(length(u) - 1) = finseq_appl[T](w)(0))
  --    AND reduced?(G, w) AND reduced?(G, u) AND (length(w) >= 2)
  --    AND (length(u) >= 2) AND wa...
  have walks_concat_red_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walks_concat_red_TCC6
  -- Statement: FORALL (G: graph[T], u, w: prewalk[T]):
  --        (finseq_appl[T](u)(length(u) - 1) = finseq_appl[T](w)(0))
  --    AND reduced?(G, w) AND reduced?(G, u) AND (length(w) >= 2)
  --    AND (length(u) >= 2) AND wa...
  have walks_concat_red_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walks_concat_red_TCC7
  -- Statement: FORALL (G: graph[T], u, w: prewalk[T]):
  --        (finseq_appl[T](u)(length(u) - 2) /= finseq_appl[T](w)(1))
  --    AND (finseq_appl[T](u)(length(u) - 1) = finseq_appl[T](w)(0))
  --    AND reduced?(G, w) AND ...
  have walks_concat_red_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.index_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(q) >= 2 IMPLIES
--  length(rev(q)) = length(q) AND
--   finseq_appl(rev(q))(0) = finseq_appl[T](q)(length(q) - 1) AND
--    finseq_appl(rev(q))(1) = finseq_appl[T](q)(length(q) - 2)
theorem index_rev : List.length q ≥ 2 → List.length (rev q) = List.length q ∧
 finseq_appl (rev q) (0) = finseq_appl q(List.length q - 1) ∧
  finseq_appl (rev q) (1) = finseq_appl q(List.length q - 2) := by
  -- === TCCs for index_rev ===
  -- TCC: index_rev_TCC1
  -- Statement: FORALL (q: prewalk[T]):
  --   length(rev(q)) = length(q) AND length(q) >= 2 IMPLIES
  --    0 < length(rev[T](q))
  have index_rev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: index_rev_TCC2
  -- Statement: FORALL (q: prewalk[T]):
  --   length(rev(q)) = length(q) AND length(q) >= 2 IMPLIES
  --    length(q) - 1 >= 0 AND length(q) - 1 < length(q)
  have index_rev_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: index_rev_TCC3
  -- Statement: FORALL (q: prewalk[T]):
  --   finseq_appl(rev(q))(0) = finseq_appl[T](q)(length(q) - 1) AND
  --    length(rev(q)) = length(q) AND length(q) >= 2
  --    IMPLIES 1 < length(rev[T](q))
  have index_rev_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: index_rev_TCC4
  -- Statement: FORALL (q: prewalk[T]):
  --   finseq_appl(rev(q))(0) = finseq_appl[T](q)(length(q) - 1) AND
  --    length(rev(q)) = length(q) AND length(q) >= 2
  --    IMPLIES length(q) - 2 >= 0 AND length(q) - 2 < length(q)
  have index_rev_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.rev_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: rev(rev(q)) = q
theorem rev_rev : rev (rev q) = q := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: path_circ.ind_rev_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: length(q) >= 2 IMPLIES
--  finseq_appl(rev(q))(length(q) - 1) = finseq_appl[T](q)(0) AND
--   finseq_appl(rev(q))(length(q) - 2) = finseq_appl[T](q)(1)
theorem ind_rev_rev : List.length q ≥ 2 → finseq_appl (rev q) (List.length q - 1) = finseq_appl q(0) ∧
 finseq_appl (rev q) (List.length q - 2) = finseq_appl q(1) := by
  -- === TCCs for ind_rev_rev ===
  -- TCC: ind_rev_rev_TCC1
  -- Statement: FORALL (q: prewalk[T]):
  --   length(q) >= 2 IMPLIES
  --    length(q) - 1 >= 0 AND length(q) - 1 < length(rev[T](q))
  have ind_rev_rev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ind_rev_rev_TCC2
  -- Statement: FORALL (q: prewalk[T]): length(q) >= 2 IMPLIES 0 < length(q)
  have ind_rev_rev_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ind_rev_rev_TCC3
  -- Statement: FORALL (q: prewalk[T]):
  --   finseq_appl(rev(q))(length(q) - 1) = finseq_appl[T](q)(0) AND
  --    length(q) >= 2
  --    IMPLIES length(q) - 2 >= 0 AND length(q) - 2 < length(rev[T](q))
  have ind_rev_rev_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: ind_rev_rev_TCC4
  -- Statement: FORALL (q: prewalk[T]):
  --   finseq_appl(rev(q))(length(q) - 1) = finseq_appl[T](q)(0) AND
  --    length(q) >= 2
  --    IMPLIES 1 < length(q)
  have ind_rev_rev_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.second_in_cat
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s /= t AND from?(p, s, t) AND from?(q, s, t) IMPLIES
--  finseq_appl[T](p)(1) = finseq_appl((trunc1(p) o rev(q)))(1)
theorem second_in_cat : s ≠ t ∧ from_p p s t ∧ from_p q s t → finseq_appl p(1) = finseq_appl ((trunc1 p ∘ rev q)) (1) := by
  -- === TCCs for second_in_cat ===
  -- TCC: second_in_cat_TCC1
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) AND s /= t IMPLIES 1 < length(p)
  have second_in_cat_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: second_in_cat_TCC2
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) AND s /= t IMPLIES
  --    1 < length((o[T](trunc1[T](p), rev[T](q))))
  have second_in_cat_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: second_in_cat_TCC3
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) AND s /= t IMPLIES length(p) >= 2
  have second_in_cat_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.sec_from_last
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s /= t AND from?(p, s, t) AND from?(q, s, t) IMPLIES
--  finseq_appl[T](q)(1) =
--   finseq_appl((trunc1(p) o rev(q)))(length(trunc1(p) o rev(q)) - 2)
theorem sec_from_last : s ≠ t ∧ from_p p s t ∧ from_p q s t → finseq_appl q(1) =
 finseq_appl ((trunc1 p ∘ rev q)) (List.length (trunc1 p ∘ rev q) - 2) := by
  -- === TCCs for sec_from_last ===
  -- TCC: sec_from_last_TCC1
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) AND s /= t IMPLIES 1 < length(q)
  have sec_from_last_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sec_from_last_TCC2
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) AND s /= t IMPLIES
  --    length(o[T](trunc1[T](p), rev[T](q))) - 2 >= 0 AND
  --     length(o[T](trunc1[T](p), rev[T](q))) - 2 <
  --     ...
  have sec_from_last_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.prewalk_same
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (s = t) AND (length(p) = 1) AND (length(q) = 1) AND from?(p, s, t)
--  AND from?(q, s, t)
--  IMPLIES p = q
theorem prewalk_same : (s = t) ∧ (List.length p = 1) ∧ (List.length q = 1) ∧ from_p p s t
 ∧ from_p q s t → p = q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.compose_long
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s /= t AND
--  from?(p, s, t) AND
--   from?(q, s, t) AND
--    finseq_appl[T](p)(length(p) - 2) /= finseq_appl[T](q)(length(q) - 2)
--  IMPLIES length(trunc1(p) o rev(q)) > 2
theorem compose_long : s ≠ t ∧
 from_p p s t ∧
  from_p q s t ∧
   finseq_appl p(List.length p - 2) ≠ finseq_appl q(List.length q - 2) → List.length (trunc1 p ∘ rev q) > 2 := by
  -- === TCCs for compose_long ===
  -- TCC: compose_long_TCC1
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) AND s /= t IMPLIES
  --    length(p) - 2 >= 0 AND length(p) - 2 < length(p)
  have compose_long_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: compose_long_TCC2
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) AND s /= t IMPLIES
  --    length(q) - 2 >= 0 AND length(q) - 2 < length(q)
  have compose_long_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.red_compos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s /= t AND
--  path_from?(G, p, s, t) AND
--   path_from?(G, q, s, t) AND
--    finseq_appl[T](p)(length(p) - 2) /= finseq_appl[T](q)(length(q) - 2)
--  IMPLIES reduced?(G, trunc1(p) o rev(q))
theorem red_compos : s ≠ t ∧
 path_from_p G p s t ∧
  path_from_p G q s t ∧
   finseq_appl p(List.length p - 2) ≠ finseq_appl q(List.length q - 2) → reduced_p G (trunc1 p ∘ rev q) := by
  -- === TCCs for red_compos ===
  -- TCC: red_compos_TCC1
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND path_from?(G, p, s, t) AND s /= t IMPLIES
  --    length(p) - 2 >= 0 AND length(p) - 2 < length(p)
  have red_compos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: red_compos_TCC2
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND path_from?(G, p, s, t) AND s /= t IMPLIES
  --    length(q) - 2 >= 0 AND length(q) - 2 < length(q)
  have red_compos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: red_compos_TCC3
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --   finseq_appl[T](p)(length(p) - 2) /= finseq_appl[T](q)(length(q) - 2) AND
  --    path_from?(G, q, s, t) AND path_from?(G, p, s, t) AND s /= t
  --    IMPLIE...
  have red_compos_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: red_compos_TCC4
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --   finseq_appl[T](p)(length(p) - 2) /= finseq_appl[T](q)(length(q) - 2) AND
  --    path_from?(G, q, s, t) AND path_from?(G, p, s, t) AND s /= t
  --    IMPLIE...
  have red_compos_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.cycl_red_compos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (s /= t) AND path_from?(G, p, s, t) AND path_from?(G, q, s, t)
--  AND (finseq_appl[T](p)(length(p) - 2) /= finseq_appl[T](q)(length(q) - 2))
--  AND (finseq_appl[T](p)(1) /= finseq_appl[T](q)(1))
--  IMP
theorem cycl_red_compos : (s ≠ t) ∧ path_from_p G p s t ∧ path_from_p G q s t
 ∧ (finseq_appl p(List.length p - 2) ≠ finseq_appl q(List.length q - 2))
 ∧ (finseq_appl p(1) ≠ finseq_appl q(1)) → cyclically_reduced_p G (trunc1 p ∘ rev q) := by
  -- === TCCs for cycl_red_compos ===
  -- TCC: cycl_red_compos_TCC1
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --   finseq_appl[T](p)(length(p) - 2) /= finseq_appl[T](q)(length(q) - 2) AND
  --    path_from?(G, q, s, t) AND path_from?(G, p, s, t) AND s /= t
  --    IMPLIE...
  have cycl_red_compos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cycl_red_compos_TCC2
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --   finseq_appl[T](p)(length(p) - 2) /= finseq_appl[T](q)(length(q) - 2) AND
  --    path_from?(G, q, s, t) AND path_from?(G, p, s, t) AND s /= t
  --    IMPLIE...
  have cycl_red_compos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.walkers
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: finseq_appl(p)(0) = finseq_appl(q)(0) AND
--  finseq_appl(p)(length(p) - 1) = finseq_appl(q)(length(q) - 1)
--  IMPLIES
--  from?(p, finseq_appl(p)(0), finseq_appl(p)(length(p) - 1)) AND
--   from?(q, finseq_appl
theorem walkers : finseq_appl p(0) = finseq_appl q(0) ∧
 finseq_appl p(List.length p - 1) = finseq_appl q(List.length q - 1) → from_p p (finseq_appl p(0)) (finseq_appl p(List.length p - 1)) ∧
 from_p q (finseq_appl p(0)) (finseq_appl p(List.length p - 1)) := by
  -- === TCCs for walkers ===
  -- TCC: walkers_TCC1
  -- Statement: FORALL (p: prewalk[T]): 0 < length(p)
  have walkers_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walkers_TCC2
  -- Statement: FORALL (p, q: prewalk[T]):
  --   finseq_appl(p)(0) = finseq_appl(q)(0) IMPLIES
  --    length(p) - 1 >= 0 AND length(p) - 1 < length(p)
  have walkers_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: walkers_TCC3
  -- Statement: FORALL (p, q: prewalk[T]):
  --   finseq_appl(p)(0) = finseq_appl(q)(0) IMPLIES
  --    length(q) - 1 >= 0 AND length(q) - 1 < length(q)
  have walkers_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.path_one
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s = t AND path_from?(G, p, s, t) IMPLIES length(p) = 1 OR circuit?(G, p)
theorem path_one : s = t ∧ path_from_p G p s t → List.length p = 1 ∨ circuit_p G p := by
  -- === TCCs for path_one ===
  -- TCC: path_one_TCC1
  -- Statement: FORALL (G: graph[T], p: prewalk[T], s, t: T):
  --   NOT length(p) = 1 AND path_from?(G, p, s, t) AND s = t IMPLIES
  --    verts_in?[T](G, p)
  have path_one_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.rev_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: rev(p) = rev(q) IMPLIES p = q
theorem rev_eq : rev p = rev q → p = q := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: path_circ.front_back
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: from?(p, s, t) AND from?(q, s, t) IMPLIES
--  finseq_appl[T](p)(0) = finseq_appl[T](q)(0) AND
--   finseq_appl[T](p)(length(p) - 1) = finseq_appl[T](q)(length(q) - 1)
theorem front_back : from_p p s t ∧ from_p q s t → finseq_appl p(0) = finseq_appl q(0) ∧
 finseq_appl p(List.length p - 1) = finseq_appl q(List.length q - 1) := by
  -- === TCCs for front_back ===
  -- TCC: front_back_TCC1
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) IMPLIES 0 < length(q)
  have front_back_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.walk_from_l
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: from?(p, s, t) AND s /= t IMPLIES length(p) >= 2
theorem walk_from_l : from_p p s t ∧ s ≠ t → List.length p ≥ 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.plus_up
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s /= t AND from?(p, s, t) AND from?(q, s, t) IMPLIES
--  (trunc1(p) = trunc1(q) IMPLIES p = q)
theorem plus_up : s ≠ t ∧ from_p p s t ∧ from_p q s t → (trunc1 p = trunc1 q → p = q) := by
  -- === TCCs for plus_up ===
  -- TCC: plus_up_TCC1
  -- Statement: FORALL (p, q: prewalk[T], s, t: T):
  --   from?(q, s, t) AND from?(p, s, t) AND s /= t IMPLIES length(q) >= 2
  have plus_up_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.two_walks
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (s /= t) AND walk?(G, p) AND walk?(G, q) AND from?(p, s, t)
--  AND from?(q, s, t)
--  IMPLIES pre_circuit?(G, trunc1(p) o rev(q))
theorem two_walks : (s ≠ t) ∧ walk_p G p ∧ walk_p G q ∧ from_p p s t
 ∧ from_p q s t → pre_circuit_p G (trunc1 p ∘ rev q) := by
  -- === TCCs for two_walks ===
  -- TCC: two_walks_TCC1
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --        from?(q, s, t) AND from?(p, s, t) AND walk?(G, q) AND walk?(G, p)
  --    AND (s /= t)
  --    IMPLIES length(o[T](trunc1[T](p), rev[T](q))) > 0
  have two_walks_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.back_short
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s /= t AND path_from?(G, p, s, t) AND path_from?(G, q, s, t) IMPLIES
--  (finseq_appl[T](p)(length(p) - 2) = finseq_appl[T](q)(length(q) - 2) AND
--    p /= q
--    IMPLIES
--    (EXISTS (pp, qq: prewalk):
--       
theorem back_short : s ≠ t ∧ path_from_p G p s t ∧ path_from_p G q s t → (finseq_appl p(List.length p - 2) = finseq_appl q(List.length q - 2) ∧
  p ≠ q
  →
  (∃ (pp : _) (qq : prewalk), (pp ≠ qq)
      ∧ path_from_p G pp s (finseq_appl p(List.length p - 2))
      ∧ path_from_p G qq s (finseq_appl p(List.length p - 2))
      ∧ (List.length pp = List.length p - 1) ∧ (List.length qq = List.length q - 1))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.from_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: from?(p, s, t) IMPLIES from?(rev(p), t, s)
theorem from_rev : from_p p s t → from_p (rev p) t s := by
  -- === TCCs for from_rev ===
  -- TCC: from_rev_TCC1
  -- Statement: FORALL (p: prewalk[T], s, t: T):
  --   from?(p, s, t) IMPLIES length(rev[T](p)) > 0
  have from_rev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.front_short
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: s /= t AND path_from?(G, p, s, t) AND path_from?(G, q, s, t) IMPLIES
--  (finseq_appl[T](p)(1) = finseq_appl[T](q)(1) AND p /= q IMPLIES
--    (EXISTS (pp, qq: prewalk):
--            (pp /= qq) AND path_from?
theorem front_short : s ≠ t ∧ path_from_p G p s t ∧ path_from_p G q s t → (finseq_appl p(1) = finseq_appl q(1) ∧ p ≠ q →
  (∃ (pp : _) (qq : prewalk), (pp ≠ qq) ∧ path_from_p G pp t (finseq_appl p(1))
      ∧ path_from_p G qq t (finseq_appl p(1))
      ∧ (List.length pp = List.length p - 1) ∧ (List.length qq = List.length q - 1))) := by
  -- === TCCs for front_short ===
  -- TCC: front_short_TCC1
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND path_from?(G, p, s, t) AND s /= t IMPLIES
  --    1 < length(p)
  have front_short_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: front_short_TCC2
  -- Statement: FORALL (G: graph[T], p, q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND path_from?(G, p, s, t) AND s /= t IMPLIES
  --    1 < length(q)
  have front_short_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: path_circ.add1_rev
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Seq(G), x: (vert(G))):
--   length(w) > 1 AND x /= seq(w)(length(w) - 1) IMPLIES
--    add1(w, x) = trunc1(w) o rev(gen_seq2(G, x, seq(w)(length(w) - 1)))
theorem add1_rev : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}) (x : T), List.length w > 1 ∧ x ≠ seq w(List.length w - 1) → add1 w x = trunc1 w ∘ rev (gen_seq2 G x (seq w(List.length w - 1))) := by
  -- === TCCs for add1_rev ===
  -- TCC: add1_rev_TCC1
  -- Statement: FORALL (G: graph[T], w: Seq[T](G), x: (vert(G))):
  --   length(w) > 1 IMPLIES length(w) - 1 >= 0 AND length(w) - 1 < w`length
  have add1_rev_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: add1_rev_TCC2
  -- Statement: FORALL (G: graph[T], w: Seq[T](G), x: (vert(G))):
  --   length(w) > 1 AND x /= seq(w)(length(w) - 1) IMPLIES length(w) >= 2
  have add1_rev_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: add1_rev_TCC3
  -- Statement: FORALL (G: graph[T], w: Seq[T](G), x: (vert(G))):
  --   length(w) > 1 AND x /= seq(w)(length(w) - 1) IMPLIES
  --    vert(G)(seq(w)(length(w) - 1))
  have add1_rev_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.path_circ