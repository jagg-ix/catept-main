-- Auto-generated by pvs2lean/translator.py
-- PVS theory: k_menger
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: k_menger

Auto-generated stubs from PVSLib theory `k_menger`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.k_menger

noncomputable section

variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (add : pvs_τ28 → pvs_τ29 → pvs_τ30)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (below : pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} (card : pvs_τ40 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (dbl : pvs_τ45 → pvs_τ46 → pvs_τ47)
variable {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} (del_vert : pvs_τ48 → pvs_τ49 → pvs_τ50)
variable {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} (del_verts : pvs_τ51 → pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} (difference : pvs_τ54 → pvs_τ55 → pvs_τ56)
variable {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} (edge_p : pvs_τ57 → pvs_τ58 → pvs_τ59 → Prop)
variable {pvs_τ60 : Type*} {pvs_τ61 : Type*} (edges : pvs_τ60 → pvs_τ61)
variable {pvs_τ62 : Type*} (empty_p : pvs_τ62 → Prop)
variable {pvs_τ63 : Type*} (fq : pvs_τ63 → Prop)
variable {pvs_τ64 : Type*} {pvs_τ65 : Type*} {pvs_τ66 : Type*} (gen_seq1 : pvs_τ64 → pvs_τ65 → pvs_τ66)
variable {pvs_τ76 : Type*} {pvs_τ77 : Type*} (image : pvs_τ76 → pvs_τ77 → Prop)
variable {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} {pvs_τ81 : Type*} (ind_path_set_p : pvs_τ78 → pvs_τ79 → pvs_τ80 → pvs_τ81 → Prop)
variable {pvs_τ83 : Type*} {pvs_τ84 : Type*} (independent_p : pvs_τ83 → pvs_τ84 → Prop)
variable {pvs_τ85 : Type*} (injective_p : pvs_τ85 → Prop)
variable {pvs_τ86 : Type*} {pvs_τ87 : Type*} (internal_verts : pvs_τ86 → pvs_τ87)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (intersection : pvs_τ88 → pvs_τ89 → Prop)
variable {pvs_τ90 : Type*} (ips : pvs_τ90 → Prop)
variable {pvs_τ91 : Type*} (ipss : pvs_τ91 → Prop)
variable {pvs_τ92 : Type*} (ipst : pvs_τ92 → Prop)
variable {pvs_τ93 : Type*} {pvs_τ94 : Type*} (is_finite : pvs_τ93 → pvs_τ94)
variable {pvs_τ98 : Type*} (length : pvs_τ98 → Prop)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} {pvs_τ105 : Type*} {pvs_τ106 : Type*} (path_from_p : pvs_τ103 → pvs_τ104 → pvs_τ105 → pvs_τ106 → Prop)
variable {pvs_τ107 : Type*} {pvs_τ108 : Type*} {pvs_τ109 : Type*} (path_verts : pvs_τ107 → pvs_τ108 → pvs_τ109)
variable {pvs_τ110 : Type*} {pvs_τ111 : Type*} {pvs_τ112 : Type*} (remove : pvs_τ110 → pvs_τ111 → pvs_τ112)
variable {pvs_τ113 : Type*} (second : pvs_τ113 → Prop)
variable {pvs_τ116 : Type*} {pvs_τ117 : Type*} {pvs_τ118 : Type*} (sep_num : pvs_τ116 → pvs_τ117 → pvs_τ118 → Prop)
variable {pvs_τ119 : Type*} {pvs_τ120 : Type*} {pvs_τ121 : Type*} (separable_p : pvs_τ119 → pvs_τ120 → pvs_τ121 → Prop)
variable {pvs_τ122 : Type*} {pvs_τ123 : Type*} {pvs_τ124 : Type*} {pvs_τ125 : Type*} (separates : pvs_τ122 → pvs_τ123 → pvs_τ124 → pvs_τ125 → Prop)
variable {pvs_τ126 : Type*} {pvs_τ127 : Type*} {pvs_τ128 : Type*} (seq : pvs_τ126 → pvs_τ127 → pvs_τ128)
variable {pvs_τ129 : Type*} {pvs_τ130 : Type*} {pvs_τ131 : Type*} {pvs_τ132 : Type*} (set_of_paths : pvs_τ129 → pvs_τ130 → pvs_τ131 → pvs_τ132)
variable {pvs_τ133 : Type*} (size : pvs_τ133 → Prop)
variable {pvs_τ138 : Type*} {pvs_τ139 : Type*} {pvs_τ140 : Type*} (subgraph : pvs_τ138 → pvs_τ139 → pvs_τ140)
variable {pvs_τ141 : Type*} {pvs_τ142 : Type*} (subgraph_p : pvs_τ141 → pvs_τ142 → Prop)
variable {pvs_τ143 : Type*} {pvs_τ144 : Type*} (subset_p : pvs_τ143 → pvs_τ144 → Prop)
variable {pvs_τ145 : Type*} {pvs_τ146 : Type*} (tau : pvs_τ145 → pvs_τ146)
variable {pvs_τ151 : Type*} {pvs_τ152 : Type*} {pvs_τ153 : Type*} (union : pvs_τ151 → pvs_τ152 → pvs_τ153)
variable {pvs_τ154 : Type*} {pvs_τ155 : Type*} (vert : pvs_τ154 → pvs_τ155 → Prop)
variable {pvs_τ156 : Type*} {pvs_τ157 : Type*} (verts_of : pvs_τ156 → pvs_τ157 → Prop)
variable {pvs_τ158 : Type*} {pvs_τ159 : Type*} {pvs_τ160 : Type*} {pvs_τ161 : Type*} (walk_from_p : pvs_τ158 → pvs_τ159 → pvs_τ160 → pvs_τ161 → Prop)
variable {pvs_τ162 : Type*} {pvs_τ163 : Type*} (walk_p : pvs_τ162 → pvs_τ163 → Prop)

-- pvs2lean: k_menger.good?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separable?(G, s, t) AND vert(G)(s) AND vert(G)(t)
axiom good_p {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} : pvs_τ67 → pvs_τ68 → pvs_τ69 → Prop

-- pvs2lean: k_menger.green?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NOT V(s) AND NOT V(t)
axiom green_p {pvs_τ73 : Type*} {pvs_τ74 : Type*} {pvs_τ75 : Type*} : pvs_τ73 → pvs_τ74 → pvs_τ75 → Prop

-- pvs2lean: k_menger.B
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND (FORALL V: separates(G, V, s, t) IMPLIES card(V) >= k)
axiom B {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: k_menger.ind_prewalk_set?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (p1, p2):
--    ips(p1) AND ips(p2) AND p1 /= p2 IMPLIES independent?(p1, p2))
axiom ind_prewalk_set_p {pvs_τ82 : Type*} : pvs_τ82 → Prop

-- pvs2lean: k_menger.st_path_set?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL p1: ips(p1) IMPLIES path_from?(G, p1, s, t))
axiom st_path_set_p {pvs_τ134 : Type*} {pvs_τ135 : Type*} {pvs_τ136 : Type*} {pvs_τ137 : Type*} : pvs_τ134 → pvs_τ135 → pvs_τ136 → pvs_τ137 → Prop

-- pvs2lean: k_menger.many_ind
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: EXISTS (ips):
--   ind_prewalk_set?(ips) AND
--    st_path_set?(G, s, t, ips) AND card[prewalk[T]](ips) = j
axiom many_ind {pvs_τ99 : Type*} {pvs_τ100 : Type*} {pvs_τ101 : Type*} {pvs_τ102 : Type*} : pvs_τ99 → pvs_τ100 → pvs_τ101 → pvs_τ102 → Prop

-- pvs2lean: k_menger.B_many
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: B(G, s, t, k) IMPLIES many_ind(G, s, t, k)
axiom B_many {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8 → Prop

-- pvs2lean: k_menger.sep_num_B
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: B(G, s, t, k) IMPLIES sep_num(G, s, t) >= k
theorem sep_num_B : B G s t k → sep_num G s t ≥ k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.sep_num_implies
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND sep_num(G, s, t) = k IMPLIES B(G, s, t, k)
theorem sep_num_implies : good_p G s t ∧ sep_num G s t = k → B G s t k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.easy_menger_B
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND many_ind(G, s, t, k) IMPLIES B(G, s, t, k)
theorem easy_menger_B : good_p G s t ∧ many_ind G s t k → B G s t k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.C
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL k: B_many(G, s, t, k))
axiom C {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: k_menger.sep_set_small
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: B(G, s, t, k) AND green?(V, s, t) AND card(V) < k IMPLIES
--  EXISTS p: path_from?(del_verts(G, V), p, s, t)
theorem sep_set_small : B G s t k ∧ green_p V s t ∧ card V < k → ∃ p,  path_from_p (del_verts G V) p s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.B_many_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: B_many(G, s, t, 1)
theorem B_many_1 : B_many G s t 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.tight?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(W, intersection(path_verts(G, s), path_verts(G, t)))
axiom tight_p {pvs_τ147 : Type*} {pvs_τ148 : Type*} {pvs_τ149 : Type*} {pvs_τ150 : Type*} : pvs_τ147 → pvs_τ148 → pvs_τ149 → pvs_τ150 → Prop

-- pvs2lean: k_menger.sub_tight
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subset?(V, W) AND tight?(G, s, t, W) IMPLIES tight?(G, s, t, V)
theorem sub_tight : subset_p V W ∧ tight_p G s t W → tight_p G s t V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.smaller_tight
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separates(G, W, s, t) IMPLIES
--  tight?(G, s, t, W) OR
--   EXISTS V: subset?(V, W) AND NOT V = W AND separates(G, V, s, t)
theorem smaller_tight : separates G W s t → tight_p G s t W ∨
 ∃ V,  subset_p V W ∧ ¬ V = W ∧ separates G V s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.small_tight
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separates(G, W, s, t) IMPLIES
--  EXISTS V: subset?(V, W) AND separates(G, V, s, t) AND tight?(G, s, t, V)
theorem small_tight : separates G W s t → ∃ V,  subset_p V W ∧ separates G V s t ∧ tight_p G s t V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.exists_tight
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) IMPLIES
--  EXISTS V: separates(G, V, s, t) AND tight?(G, s, t, V)
theorem exists_tight : good_p G s t → ∃ V,  separates G V s t ∧ tight_p G s t V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.close?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: T):
--   W(w) IMPLIES
--    EXISTS p:
--      path_from?(G, p, s, t) AND
--       intersection(W, internal_verts(p)) = singleton[T](w)
axiom close_p {pvs_τ41 : Type*} {pvs_τ42 : Type*} {pvs_τ43 : Type*} {pvs_τ44 : Type*} : pvs_τ41 → pvs_τ42 → pvs_τ43 → pvs_τ44 → Prop

-- pvs2lean: k_menger.close_tight
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: close?(G, s, t, W) IMPLIES tight?(G, s, t, W)
theorem close_tight : close_p G s t W → tight_p G s t W := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.smaller_close
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separates(G, W, s, t) AND tight?(G, s, t, W) IMPLIES
--  close?(G, s, t, W) OR
--   EXISTS V:
--     subset?(V, W) AND
--      NOT V = W AND separates(G, V, s, t) AND tight?(G, s, t, V)
theorem smaller_close : separates G W s t ∧ tight_p G s t W → close_p G s t W ∨
 ∃ V, 
   subset_p V W ∧
    ¬ V = W ∧ separates G V s t ∧ tight_p G s t V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.small_close
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separates(G, W, s, t) IMPLIES
--  EXISTS V: subset?(V, W) AND separates(G, V, s, t) AND close?(G, s, t, V)
theorem small_close : separates G W s t → ∃ V,  subset_p V W ∧ separates G V s t ∧ close_p G s t V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.exists_close
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) IMPLIES
--  EXISTS V: separates(G, V, s, t) AND close?(G, s, t, V)
theorem exists_close : good_p G s t → ∃ V,  separates G V s t ∧ close_p G s t V := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.closed_minimal
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separates(G, W, s, t) AND
--  close?(G, s, t, W) AND subset?(V, W) AND separates(G, V, s, t)
--  IMPLIES V = W
theorem closed_minimal : separates G W s t ∧
 close_p G s t W ∧ subset_p V W ∧ separates G V s t → V = W := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.closed_verts
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: close?(G, s, t, W) IMPLIES subset?(W, vert(G))
theorem closed_verts : close_p G s t W → subset_p W (vert G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.attached?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL w: W(w) IMPLIES edge?(G)(s, w)) OR
--  (FORALL w: W(w) IMPLIES edge?(G)(t, w))
axiom attached_p {pvs_τ34 : Type*} {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} : pvs_τ34 → pvs_τ35 → pvs_τ36 → pvs_τ37 → Prop

-- pvs2lean: k_menger.attach_edges
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: {e: doubleton[T] | EXISTS w: W(w) AND e = dbl[T](w, x)}
axiom attach_edges {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ31 → pvs_τ32 → pvs_τ33

-- pvs2lean: k_menger.Hverts
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: path_verts(del_verts(G, W), x)
axiom Hverts {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19

-- pvs2lean: k_menger.Kgraph
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subgraph(G, difference[T](vert(G), Hverts(G, W, x)))
axiom Kgraph {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23

-- pvs2lean: k_menger.Mgraph
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (# vert := add(x, vert(Kgraph(G, W, x))),
--    edges
--      := union(edges(Kgraph(G, W, x)),
--               attach_edges(intersection(vert(G), W), x)) #)
axiom Mgraph {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27

-- pvs2lean: k_menger.Hst_intersect
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separates(G, W, s, t) IMPLIES
--  empty?(intersection(Hverts(G, W, s), Hverts(G, W, t)))
theorem Hst_intersect : separates G W s t → empty_p (intersection (Hverts G W s) (Hverts G W t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.subset_Ws
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND separates(G, W, s, t) AND NOT attached?(G, s, t, W)
--  IMPLIES subset?(vert(Mgraph(G, W, s)), vert(G))
theorem subset_Ws : good_p G s t ∧ separates G W s t ∧ ¬ attached_p G s t W → subset_p (vert (Mgraph G W s)) (vert G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.subset_Wt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND separates(G, W, s, t) AND NOT attached?(G, s, t, W)
--  IMPLIES subset?(vert(Mgraph(G, W, t)), vert(G))
theorem subset_Wt : good_p G s t ∧ separates G W s t ∧ ¬ attached_p G s t W → subset_p (vert (Mgraph G W t)) (vert G) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.size_Ws
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  separates(G, W, s, t) AND close?(G, s, t, W) AND NOT attached?(G, s, t, W)
--  IMPLIES size(Mgraph(G, W, s)) < size(G)
theorem size_Ws : good_p G s t ∧
 separates G W s t ∧ close_p G s t W ∧ ¬ attached_p G s t W → size (Mgraph G W s) < size G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.size_Wt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  separates(G, W, s, t) AND close?(G, s, t, W) AND NOT attached?(G, s, t, W)
--  IMPLIES size(Mgraph(G, W, t)) < size(G)
theorem size_Wt : good_p G s t ∧
 separates G W s t ∧ close_p G s t W ∧ ¬ attached_p G s t W → size (Mgraph G W t) < size G := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.C_induct
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL GG: size(GG) < size(G) IMPLIES C(GG, s, t)) IMPLIES C(G, s, t)
axiom C_induct {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ12 → pvs_τ13 → pvs_τ14 → pvs_τ15

-- pvs2lean: k_menger.Menger_hard
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL G: C_induct(G, s, t)) IMPLIES (FORALL G: C(G, s, t))
theorem Menger_hard : (∀ G,  C_induct G s t) → (∀ G,  C G s t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.subgraph_walks
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subgraph?(GG, G) AND walk?(GG, p1) IMPLIES walk?(G, p1)
theorem subgraph_walks : subgraph_p GG G ∧ walk_p GG p1 → walk_p G p1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.subgraph_paths
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subgraph?(GG, G) AND path_from?(GG, p1, s, t) IMPLIES
--  path_from?(G, p1, s, t)
theorem subgraph_paths : subgraph_p GG G ∧ path_from_p GG p1 s t → path_from_p G p1 s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.greatest?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(p) AND
--  V(seq(p)(i)) AND FORALL j: (j < length(p) AND V(seq(p)(j)) IMPLIES j <= i)
axiom greatest_p {pvs_τ70 : Type*} {pvs_τ71 : Type*} {pvs_τ72 : Type*} : pvs_τ70 → pvs_τ71 → pvs_τ72 → Prop

-- pvs2lean: k_menger.least?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(p) AND
--  V(seq(p)(i)) AND FORALL j: (j < length(p) AND V(seq(p)(j)) IMPLIES j >= i)
axiom least_p {pvs_τ95 : Type*} {pvs_τ96 : Type*} {pvs_τ97 : Type*} : pvs_τ95 → pvs_τ96 → pvs_τ97 → Prop

-- pvs2lean: k_menger.greatest_is
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(p) AND V(seq(p)(i)) IMPLIES EXISTS k: greatest?(p, V, k)
theorem greatest_is : i < List.length p ∧ V (seq p(i)) → ∃ k,  greatest_p p V k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.least_is
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i < length(p) AND V(seq(p)(i)) IMPLIES EXISTS k: least?(p, V, k)
theorem least_is : i < List.length p ∧ V (seq p(i)) → ∃ k,  least_p p V k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.set_small_s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      B(G, s, t, k) AND good?(G, s, t) AND separates(G, W, s, t)
--  AND green?(V, s, t) AND (card(V) < k)
--  IMPLIES EXISTS p: path_from?(del_verts(Mgraph(G, W, s), V), p, s, t)
theorem set_small_s : B G s t k ∧ good_p G s t ∧ separates G W s t
 ∧ green_p V s t ∧ (card V < k) → ∃ p,  path_from_p (del_verts (Mgraph G W s) V) p s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.set_small_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      B(G, s, t, k) AND good?(G, s, t) AND separates(G, W, s, t)
--  AND green?(V, s, t) AND (card(V) < k)
--  IMPLIES EXISTS p: path_from?(del_verts(Mgraph(G, W, t), V), p, s, t)
theorem set_small_t : B G s t k ∧ good_p G s t ∧ separates G W s t
 ∧ green_p V s t ∧ (card V < k) → ∃ p,  path_from_p (del_verts (Mgraph G W t) V) p s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.separates_Ws
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: B(G, s, t, k) AND separates(G, W, s, t) IMPLIES B(Mgraph(G, W, s), s, t, k)
theorem separates_Ws : B G s t k ∧ separates G W s t → B (Mgraph G W s) s t k := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: k_menger.separates_Wt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: B(G, s, t, k) AND separates(G, W, s, t) IMPLIES B(Mgraph(G, W, t), s, t, k)
theorem separates_Wt : B G s t k ∧ separates G W s t → B (Mgraph G W t) s t k := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: k_menger.separ_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subgraph?(GG, G) AND separates(G, V, s, t) IMPLIES separates(GG, V, s, t)
theorem separ_sub : subgraph_p GG G ∧ separates G V s t → separates GG V s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.separ_plus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separates(del_vert(G, a), V, s, t) IMPLIES
--  separates(G, add(a, V), s, t) OR a = s OR a = t
theorem separ_plus : separates (del_vert G a) V s t → separates G (add a V) s t ∨ a = s ∨ a = t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.super_separ
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  subset?(V, W) AND green?(W, s, t) AND separates(G, V, s, t)
--  IMPLIES separates(G, W, s, t)
theorem super_separ : good_p G s t ∧
 subset_p V W ∧ green_p W s t ∧ separates G V s t → separates G W s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.intermediate_subgraph
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (good?(G, s, t) AND
--   subgraph?(H, G) AND
--    green?(vert(H), s, t) AND size(H) <= k AND k < size(G) - 1)
--  IMPLIES
--  (EXISTS HH:
--     (green?(vert(HH), s, t) AND
--       subgraph?(H, HH) AND size(HH) = k A
theorem intermediate_subgraph : (good_p G s t ∧
  subgraph_p H G ∧
   green_p (vert H) s t ∧ size H ≤ k ∧ k < size G - 1) → (∃ HH, 
   (green_p (vert HH) s t ∧
     subgraph_p H HH ∧ size HH = k ∧ subgraph_p HH G)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.intermediate_verts
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND green?(V, s, t) AND subset?(V, vert(G))
--  AND (card(V) <= k) AND (k < size(G) - 1)
--  IMPLIES
--  (EXISTS W:
--     subset?(V, W) AND
--      card(W) = k AND green?(W, s, t) AND subset?(W,
theorem intermediate_verts : good_p G s t ∧ green_p V s t ∧ subset_p V (vert G)
 ∧ (card V ≤ k) ∧ (k < size G - 1) → (∃ W, 
   subset_p V W ∧
    card W = k ∧ green_p W s t ∧ subset_p W (vert G)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.one_wedge_s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND path_from?(G, q, s, t) IMPLIES
--  EXISTS k:
--    k < length(q) - 1 AND
--     edge?(G)(s, seq(q)(k)) AND
--      (FORALL j: j < length(q) AND edge?(G)(s, seq(q)(j)) IMPLIES j <= k)
--       AND 
theorem one_wedge_s : good_p G s t ∧ path_from_p G q s t → ∃ k, 
  k < List.length q - 1 ∧
   edge_p G(s, seq q(k)) ∧
    (∀ j,  j < List.length q ∧ edge_p G(s, seq q(j)) → j ≤ k) ∧
     path_from_p G (gen_seq1 G s ∘ q ^ (k, List.length q - 1)) s t := by
  -- === TCCs for one_wedge_s ===
  -- TCC: one_wedge_s_TCC1
  -- Statement: FORALL (G: graph[T], q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND good?(G, s, t) IMPLIES
  --    FORALL (k: nat): k < length(q) - 1 IMPLIES k < q`length
  have one_wedge_s_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: one_wedge_s_TCC2
  -- Statement: FORALL (G: graph[T], q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND good?(G, s, t) IMPLIES
  --    FORALL (k: nat):
  --      (k < length(q) - 1 AND
  --        edge?(G)(s, seq(q)(k)) AND
  --         FORALL (j...
  have one_wedge_s_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: one_wedge_s_TCC3
  -- Statement: FORALL (G: graph[T], q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND good?(G, s, t) IMPLIES
  --    FORALL (k: nat):
  --      (k < length(q) - 1 AND
  --        edge?(G)(s, seq(q)(k)) AND
  --         FORALL (j...
  have one_wedge_s_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: one_wedge_s_TCC4
  -- Statement: FORALL (G: graph[T], q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND good?(G, s, t) IMPLIES
  --    FORALL (k: nat):
  --      (k < length(q) - 1 AND
  --        edge?(G)(s, seq(q)(k)) AND
  --         FORALL (j...
  have one_wedge_s_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.one_edge_s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND path_from?(G, q, s, t) IMPLIES
--  EXISTS p:
--    path_from?(G, p, s, t) AND
--     FORALL (i: below(length(p))):
--       (edge?(G)(s, seq(p)(i)) IMPLIES i = 1) AND
--        subset?(verts_of(p)
theorem one_edge_s : good_p G s t ∧ path_from_p G q s t → ∃ p, 
  path_from_p G p s t ∧
   ∀ (i : Finset.range List.length p), (edge_p G(s, seq p(i)) → i = 1) ∧
      subset_p (verts_of p) (verts_of q) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.short_path_k
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: k >= 1 AND B(G, s, t, k) IMPLIES
--  EXISTS p:
--    path_from?(G, p, s, t) AND
--     FORALL (i: below(length(p))):
--       ((edge?(G)(s, seq(p)(i)) IMPLIES i = 1) AND
--         (edge?(G)(seq(p)(i), t) IMPLIES i 
theorem short_path_k : k ≥ 1 ∧ B G s t k → ∃ p, 
  path_from_p G p s t ∧
   ∀ (i : Finset.range List.length p), ((edge_p G(s, seq p(i)) → i = 1) ∧
       (edge_p G(seq p(i), t) → i = List.length p - 2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.indep_path_sub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: subgraph?(GG, G) AND many_ind(GG, s, t, j) IMPLIES many_ind(G, s, t, j)
theorem indep_path_sub : subgraph_p GG G ∧ many_ind GG s t j → many_ind G s t j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.ips_lem1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL q, q1:
--    (ips(q) IMPLIES V(second(q))) AND
--     (ips(q) AND ips(q1) AND q /= q1 IMPLIES second(q) /= second(q1)))
--  IMPLIES
--  subset?(image(second, ips), V) AND
--   injective?(restrict[prewalk[T],
theorem ips_lem1 : (∀ q q1, 
   (ips q → V (second q)) ∧
    (ips q ∧ ips q1 ∧ q ≠ q1 → second q ≠ second q1)) → subset_p (image second ips) V ∧
 injective_p (restrict second) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.ips_lem2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL q, q1:
--    (ips(q) IMPLIES V(second(q))) AND
--     (ips(q) AND ips(q1) AND q /= q1 IMPLIES second(q) /= second(q1)))
--  AND card[prewalk[T]](ips) = card[T](V)
--  IMPLIES image(second, ips) = V
theorem ips_lem2 : (∀ q q1, 
   (ips q → V (second q)) ∧
    (ips q ∧ ips q1 ∧ q ≠ q1 → second q ≠ second q1))
 ∧ card ips = card V → image second ips = V := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: k_menger.secoo
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: finseq_appl[T](p)(1)
axiom secoo {pvs_τ114 : Type*} : pvs_τ114 → Prop

-- pvs2lean: k_menger.secpp
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: finseq_appl[T](p)(length(p) - 2)
axiom secpp {pvs_τ115 : Type*} : pvs_τ115 → Prop

-- pvs2lean: k_menger.inj_ips
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (q, q1):
--    (ips(q) IMPLIES V(second(q))) AND
--     (ips(q) AND ips(q1) AND q /= q1 IMPLIES second(q) /= second(q1)))
--  AND card[prewalk[T]](ips) = card[T](V)
--  IMPLIES
--  EXISTS (fq: [(V) -> prewal
theorem inj_ips : (∀ (q : _) (q1 : _), (ips q → V (second q)) ∧
    (ips q ∧ ips q1 ∧ q ≠ q1 → second q ≠ second q1))
 ∧ card ips = card V → ∃ (fq: [(V):
  image fq (restrict V) = ips ∧
   (∀ (v : (V)), second (fq v) = v) ∧
    (∀ q2,  ips q2 → fq (second q2) = q2) := by
  -- === TCCs for inj_ips ===
  -- TCC: inj_ips_TCC1
  -- Statement: FORALL (V: finite_set[T], ips: set_of_prewalks, second: [prewalk[T] -> T]):
  --   (FORALL (q: prewalk[T], q1: prewalk[T]):
  --      (ips(q) IMPLIES V(second(q))) AND
  --       (ips(q) AND ips(q1) AND q /= q1 I...
  have inj_ips_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.long_ipss_paths
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND st_path_set?(Mgraph(G, W, s), s, t, ipss) AND ipss(p)
--  IMPLIES length(p) > 1
theorem long_ipss_paths : good_p G s t ∧ st_path_set_p (Mgraph G W s) s t ipss ∧ ipss p → List.length p > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.ipss_W
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  st_path_set?(Mgraph(G, W, s), s, t, ipss) AND
--   separates(G, W, s, t) AND ipss(p)
--  IMPLIES W(secoo(p))
theorem ipss_W : good_p G s t ∧
 st_path_set_p (Mgraph G W s) s t ipss ∧
  separates G W s t ∧ ipss p → W (secoo p) := by
  -- === TCCs for ipss_W ===
  -- TCC: ipss_W_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss: set_of_prewalks,
  --         p: prewalk[T], s, t: T):
  --   ipss(p) AND
  --    separates(G, W, s, t) AND
  --     st_path_set?(Mgraph(G, W, s), s, t, ipss) AND good?(G, ...
  have ipss_w_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.long_ipst_paths
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND st_path_set?(Mgraph(G, W, t), s, t, ipss) AND ipss(p)
--  IMPLIES length(p) > 1
theorem long_ipst_paths : good_p G s t ∧ st_path_set_p (Mgraph G W t) s t ipss ∧ ipss p → List.length p > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.ipst_W
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  st_path_set?(Mgraph(G, W, t), s, t, ipss) AND
--   separates(G, W, s, t) AND ipss(p)
--  IMPLIES W(secpp(p))
theorem ipst_W : good_p G s t ∧
 st_path_set_p (Mgraph G W t) s t ipss ∧
  separates G W s t ∧ ipss p → W (secpp p) := by
  -- === TCCs for ipst_W ===
  -- TCC: ipst_W_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss: set_of_prewalks,
  --         p: prewalk[T], s, t: T):
  --   ipss(p) AND
  --    separates(G, W, s, t) AND
  --     st_path_set?(Mgraph(G, W, t), s, t, ipss) AND good?(G, ...
  have ipst_w_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.long_ipss_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  st_path_set?(Mgraph(G, W, s), s, t, ipss) AND
--   ipss(p) AND separates(G, W, s, t)
--  IMPLIES length(p) > 2
theorem long_ipss_2 : good_p G s t ∧
 st_path_set_p (Mgraph G W s) s t ipss ∧
  ipss p ∧ separates G W s t → List.length p > 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.long_ipst_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  st_path_set?(Mgraph(G, W, t), s, t, ipss) AND
--   ipss(p) AND separates(G, W, s, t)
--  IMPLIES length(p) > 2
theorem long_ipst_2 : good_p G s t ∧
 st_path_set_p (Mgraph G W t) s t ipss ∧
  ipss p ∧ separates G W s t → List.length p > 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.ind_path_set_secoo
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND (card[T](W) = card[prewalk[T]](ipss))
--  AND separates(G, W, s, t) AND ind_prewalk_set?(ipss)
--  AND st_path_set?(Mgraph(G, W, s), s, t, ipss)
--  IMPLIES
--  image(secoo,
--        restric
theorem ind_path_set_secoo : good_p G s t ∧ (card W = card ipss)
 ∧ separates G W s t ∧ ind_prewalk_set_p ipss
 ∧ st_path_set_p (Mgraph G W s) s t ipss → image secoo (restrict ipss)
 = W
 ∧
 ∀ (q : _) (q1 : _), (ipss q ∧ ipss q1 ∧ q ≠ q1 → secoo q ≠ secoo q1) := by
  -- === TCCs for ind_path_set_secoo ===
  -- TCC: ind_path_set_secoo_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss: set_of_prewalks, s, t: T):
  --        (image(secoo,
  --               restrict[prewalk[T], {p: prewalk[T] | length(p) > 1}, boolean]
  --                   (ipss))
  --  ...
  have ind_path_set_secoo_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.ind_path_set_secpp
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND (card[T](W) = card[prewalk[T]](ipst))
--  AND separates(G, W, s, t) AND ind_prewalk_set?(ipst)
--  AND st_path_set?(Mgraph(G, W, t), s, t, ipst)
--  IMPLIES
--  image(secpp,
--        restric
theorem ind_path_set_secpp : good_p G s t ∧ (card W = card ipst)
 ∧ separates G W s t ∧ ind_prewalk_set_p ipst
 ∧ st_path_set_p (Mgraph G W t) s t ipst → image secpp (restrict ipst)
 = W
 ∧
 ∀ (q : _) (q1 : _), (ipst q ∧ ipst q1 ∧ q ≠ q1 → secpp q ≠ secpp q1) := by
  -- === TCCs for ind_path_set_secpp ===
  -- TCC: ind_path_set_secpp_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipst: set_of_prewalks, s, t: T):
  --        (image(secpp,
  --               restrict[prewalk[T], {p: prewalk[T] | length(p) > 1}, boolean]
  --                   (ipst))
  --  ...
  have ind_path_set_secpp_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.uniq_w_ipss
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND (card[T](W) = card[prewalk[T]](ipss))
--  AND ind_prewalk_set?(ipss) AND st_path_set?(Mgraph(G, W, s), s, t, ipss)
--  AND separates(G, W, s, t) AND ipss(p) AND verts_of(p)(w) AND W(
theorem uniq_w_ipss : good_p G s t ∧ (card W = card ipss)
 ∧ ind_prewalk_set_p ipss ∧ st_path_set_p (Mgraph G W s) s t ipss
 ∧ separates G W s t ∧ ipss p ∧ verts_of p(w) ∧ W w → w = secoo p := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.uniq_w_ipst
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND (card[T](W) = card[prewalk[T]](ipst))
--  AND ind_prewalk_set?(ipst) AND st_path_set?(Mgraph(G, W, t), s, t, ipst)
--  AND separates(G, W, s, t) AND ipst(q) AND verts_of(q)(w) AND W(
theorem uniq_w_ipst : good_p G s t ∧ (card W = card ipst)
 ∧ ind_prewalk_set_p ipst ∧ st_path_set_p (Mgraph G W t) s t ipst
 ∧ separates G W s t ∧ ipst q ∧ verts_of q(w) ∧ W w → w = secpp q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.low_card_sep
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: B(G, s, t, k) IMPLIES
--  EXISTS W:
--    separates(G, W, s, t) AND
--     card(W) >= k AND
--      FORALL V: (separates(G, V, s, t) IMPLIES card(V) >= card(W))
theorem low_card_sep : B G s t k → ∃ W, 
  separates G W s t ∧
   card W ≥ k ∧
    ∀ V,  (separates G V s t → card V ≥ card W) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: k_menger.smaller_ips
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: j <= card[prewalk[T]](ips) IMPLIES
--  EXISTS ips1: subset?(ips1, ips) AND card[prewalk[T]](ips1) = j
theorem smaller_ips : j ≤ card ips → ∃ ips1,  subset_p ips1 ips ∧ card ips1 = j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.few_many
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: i >= j AND many_ind(G, s, t, i) IMPLIES many_ind(G, s, t, j)
theorem few_many : i ≥ j ∧ many_ind G s t i → many_ind G s t j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.min_B
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: B(G, s, t, k) IMPLIES
--  EXISTS (i, W):
--    i >= k AND B(G, s, t, i) AND separates(G, W, s, t) AND card(W) = i
theorem min_B : B G s t k → ∃ (i : _) (W : _), i ≥ k ∧ B G s t i ∧ separates G W s t ∧ card W = i := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.no_sep_req
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL k, W:
--    B(G, s, t, k) AND separates(G, W, s, t) AND card(W) = k IMPLIES
--     many_ind(G, s, t, k))
--  IMPLIES (FORALL k: B(G, s, t, k) IMPLIES many_ind(G, s, t, k))
theorem no_sep_req : (∀ k W, 
   B G s t k ∧ separates G W s t ∧ card W = k →
    many_ind G s t k) → (∀ k,  B G s t k → many_ind G s t k) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.k_sep_close
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL k, W:
--   B(G, s, t, k) AND separates(G, W, s, t) AND card(W) = k IMPLIES
--    close?(G, s, t, W)
theorem k_sep_close : ∀ (k : {i : ℝ // i ≥ 0}) (W : T → Bool), B G s t k ∧ separates G W s t ∧ card W = k → close_p G s t W := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.p_Ht
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND separates(G, W, s, t)
--  AND path_from?(Mgraph(G, W, s), p, s, t) AND (length(p) > 2)
--  AND (FORALL w: W(w) AND verts_of(p)(w) IMPLIES w = secoo(p))
--  IMPLIES subset?(verts_of(p ^ 
theorem p_Ht : good_p G s t ∧ separates G W s t
 ∧ path_from_p (Mgraph G W s) p s t ∧ (List.length p > 2)
 ∧ (∀ w,  W w ∧ verts_of p(w) → w = secoo p) → subset_p (verts_of (p ^ (2, List.length p - 1))) (Hverts G W t) := by
  -- === TCCs for p_Ht ===
  -- TCC: p_Ht_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], p: prewalk[T], s, t: T):
  --   length(p) > 2 AND
  --    path_from?(Mgraph(G, W, s), p, s, t) AND
  --     separates(G, W, s, t) AND good?(G, s, t)
  --    IMPLIES FORALL (w: T)...
  have p_ht_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: p_Ht_TCC2
  -- Statement: FORALL (G: graph[T], W: finite_set[T], p: prewalk[T], s, t: T):
  --        (FORALL (w: T): W(w) AND verts_of(p)(w) IMPLIES w = secoo(p))
  --    AND (length(p) > 2) AND path_from?(Mgraph(G, W, s), p, s, t)
  -- ...
  have p_ht_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: p_Ht_TCC3
  -- Statement: FORALL (G: graph[T], W: finite_set[T], p: prewalk[T], s, t: T):
  --        (FORALL (w: T): W(w) AND verts_of(p)(w) IMPLIES w = secoo(p))
  --    AND (length(p) > 2) AND path_from?(Mgraph(G, W, s), p, s, t)
  -- ...
  have p_ht_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.q_Hs
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND separates(G, W, s, t)
--  AND path_from?(Mgraph(G, W, t), q, s, t) AND (length(q) > 2)
--  AND (FORALL w: W(w) AND verts_of(q)(w) IMPLIES w = secpp(q))
--  IMPLIES subset?(verts_of(q ^ 
theorem q_Hs : good_p G s t ∧ separates G W s t
 ∧ path_from_p (Mgraph G W t) q s t ∧ (List.length q > 2)
 ∧ (∀ w,  W w ∧ verts_of q(w) → w = secpp q) → subset_p (verts_of (q ^ (0, List.length q - 3))) (Hverts G W s) := by
  -- === TCCs for q_Hs ===
  -- TCC: q_Hs_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], q: prewalk[T], s, t: T):
  --   length(q) > 2 AND
  --    path_from?(Mgraph(G, W, t), q, s, t) AND
  --     separates(G, W, s, t) AND good?(G, s, t)
  --    IMPLIES FORALL (w: T)...
  have q_hs_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: q_Hs_TCC2
  -- Statement: FORALL (G: graph[T], W: finite_set[T], q: prewalk[T], s, t: T):
  --        (FORALL (w: T): W(w) AND verts_of(q)(w) IMPLIES w = secpp(q))
  --    AND (length(q) > 2) AND path_from?(Mgraph(G, W, t), q, s, t)
  -- ...
  have q_hs_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: q_Hs_TCC3
  -- Statement: FORALL (G: graph[T], W: finite_set[T], q: prewalk[T], s, t: T):
  --        (FORALL (w: T): W(w) AND verts_of(q)(w) IMPLIES w = secpp(q))
  --    AND (length(q) > 2) AND path_from?(Mgraph(G, W, t), q, s, t)
  -- ...
  have q_hs_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.disjoint_M_paths
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND close?(G, s, t, W) AND separates(G, W, s, t)
--  AND (card[T](W) = card[prewalk[T]](ipss))
--  AND (card[T](W) = card[prewalk[T]](ipst))
--  AND st_path_set?(Mgraph(G, W, s), s, t, ipss
theorem disjoint_M_paths : good_p G s t ∧ close_p G s t W ∧ separates G W s t
 ∧ (card W = card ipss)
 ∧ (card W = card ipst)
 ∧ st_path_set_p (Mgraph G W s) s t ipss
 ∧ st_path_set_p (Mgraph G W t) s t ipst ∧ ind_prewalk_set_p ipss
 ∧ ind_prewalk_set_p ipst ∧ ipss p ∧ ipst q → (secoo p = secpp q ∧
  intersection (verts_of (p ^ (1, List.length p - 1))) (verts_of (q ^ (0, List.length q - 2)))
   = singleton (secoo p)
   ∧ W (secoo p))
 ∨
 (secoo p ≠ secpp q ∧
   empty_p (intersection (verts_of (p ^ (1, List.length p - 1))) (verts_of (q ^ (0, List.length q - 2))))) := by
  -- === TCCs for disjoint_M_paths ===
  -- TCC: disjoint_M_paths_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        ipst(q) AND ipss(p) AND ind_prewalk_set?(ipst)
  --    AND ind_prewalk_set?(ipss) AND st_pa...
  have disjoint_m_paths_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: disjoint_M_paths_TCC2
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        (secoo(p) = secpp(q)) AND ipst(q) AND ipss(p)
  --    AND ind_prewalk_set?(ipst) AND ind_pr...
  have disjoint_m_paths_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: disjoint_M_paths_TCC3
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        (secoo(p) = secpp(q)) AND ipst(q) AND ipss(p)
  --    AND ind_prewalk_set?(ipst) AND ind_pr...
  have disjoint_m_paths_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: disjoint_M_paths_TCC4
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        (secoo(p) = secpp(q)) AND ipst(q) AND ipss(p)
  --    AND ind_prewalk_set?(ipst) AND ind_pr...
  have disjoint_m_paths_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: disjoint_M_paths_TCC5
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        (secoo(p) = secpp(q)) AND ipst(q) AND ipss(p)
  --    AND ind_prewalk_set?(ipst) AND ind_pr...
  have disjoint_m_paths_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: disjoint_M_paths_TCC6
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        (secoo(p) /= secpp(q))
  --    AND (NOT (secoo(p) = secpp(q) AND
  --               intersection...
  have disjoint_m_paths_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: disjoint_M_paths_TCC7
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        (secoo(p) /= secpp(q))
  --    AND (NOT (secoo(p) = secpp(q) AND
  --               intersection...
  have disjoint_m_paths_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: disjoint_M_paths_TCC8
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        (secoo(p) /= secpp(q))
  --    AND (NOT (secoo(p) = secpp(q) AND
  --               intersection...
  have disjoint_m_paths_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: disjoint_M_paths_TCC9
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss, ipst: set_of_prewalks,
  --         p, q: prewalk[T], s, t: T):
  --        (secoo(p) /= secpp(q))
  --    AND (NOT (secoo(p) = secpp(q) AND
  --               intersection...
  have disjoint_m_paths_tcc9 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.sec_image
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  separates(G, W, s, t) AND
--   image(second, ipss) = W AND injective?(LAMBDA (s: (ipss)): second(s))
--  IMPLIES
--  EXISTS (tau: [(W) -> prewalk]):
--    image(tau, restrict[T, (W), boolean](
theorem sec_image : good_p G s t ∧
 separates G W s t ∧
  image second ipss = W ∧ injective_p (fun (s : (ipss))=> second s) → ∃ (tau: [(W):
  image tau (restrict W) = ipss ∧
   ∀ w,  (W w → second (tau w) = w) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.Map_s
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND separates(G, W, s, t)
--  AND (card[T](W) = card[prewalk[T]](ipss)) AND ind_prewalk_set?(ipss)
--  AND st_path_set?(Mgraph(G, W, s), s, t, ipss)
--  IMPLIES
--  EXISTS (tau: [(W) -> prewal
theorem Map_s : good_p G s t ∧ separates G W s t
 ∧ (card W = card ipss) ∧ ind_prewalk_set_p ipss
 ∧ st_path_set_p (Mgraph G W s) s t ipss → ∃ (tau: [(W):
  image tau (restrict W) = ipss ∧
   ∀ w,  (W w → secoo (tau w) = w) := by
  -- === TCCs for Map_s ===
  -- TCC: Map_s_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipss: set_of_prewalks, s, t: T):
  --        st_path_set?(Mgraph(G, W, s), s, t, ipss) AND ind_prewalk_set?(ipss)
  --    AND (card[T](W) = card[prewalk[T]](ipss)) AND ...
  have map_s_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.Map_t
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND separates(G, W, s, t)
--  AND (card[T](W) = card[prewalk[T]](ipst)) AND ind_prewalk_set?(ipst)
--  AND st_path_set?(Mgraph(G, W, t), s, t, ipst)
--  IMPLIES
--  EXISTS (tau: [(W) -> prewal
theorem Map_t : good_p G s t ∧ separates G W s t
 ∧ (card W = card ipst) ∧ ind_prewalk_set_p ipst
 ∧ st_path_set_p (Mgraph G W t) s t ipst → ∃ (tau: [(W):
  image tau (restrict W) = ipst ∧
   ∀ w,  (W w → secpp (tau w) = w) := by
  -- === TCCs for Map_t ===
  -- TCC: Map_t_TCC1
  -- Statement: FORALL (G: graph[T], W: finite_set[T], ipst: set_of_prewalks, s, t: T):
  --        st_path_set?(Mgraph(G, W, t), s, t, ipst) AND ind_prewalk_set?(ipst)
  --    AND (card[T](W) = card[prewalk[T]](ipst)) AND ...
  have map_t_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.Mapper_ips
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND close?(G, s, t, W) AND separates(G, W, s, t)
--  AND (card[T](W) = card[prewalk[T]](ipss))
--  AND (card[T](W) = card[prewalk[T]](ipst))
--  AND st_path_set?(Mgraph(G, W, s), s, t, ipss
theorem Mapper_ips : good_p G s t ∧ close_p G s t W ∧ separates G W s t
 ∧ (card W = card ipss)
 ∧ (card W = card ipst)
 ∧ st_path_set_p (Mgraph G W s) s t ipss
 ∧ st_path_set_p (Mgraph G W t) s t ipst ∧ ind_prewalk_set_p ipss
 ∧ ind_prewalk_set_p ipst → ∃ (phi: [(W):
  card (image phi (restrict[T, (W), boolean](W))) = card W
   ∧
   st_path_set_p G s t (image phi (restrict W)) ∧
    ind_prewalk_set_p (image phi (restrict W)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.non_attached_induct
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      good?(G, s, t) AND separates(G, W, s, t) AND B(G, s, t, k)
--  AND (card[T](W) = k)
--  AND (FORALL GG: size(GG) < size(G) IMPLIES C(GG, s, t))
--  IMPLIES many_ind(G, s, t, k) OR attached?(G, s, t, W)
theorem non_attached_induct : good_p G s t ∧ separates G W s t ∧ B G s t k
 ∧ (card W = k)
 ∧ (∀ GG,  size GG < size G → C GG s t) → many_ind G s t k ∨ attached_p G s t W := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.plus_sep_vert
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND separates(del_vert(G, a), W, s, t) IMPLIES
--  separates(G, add(a, W), s, t) OR a = s OR a = t
theorem plus_sep_vert : good_p G s t ∧ separates (del_vert G a) W s t → separates G (add a W) s t ∨ a = s ∨ a = t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.separ_del_vert
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: separates(G, W, s, t) IMPLIES separates(G, remove(x, W), s, t) OR vert(G)(x)
theorem separ_del_vert : separates G W s t → separates G (remove x W) s t ∨ vert G(x) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: k_menger.plus_path_set
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  path_from?(G, q, s, t) AND
--   st_path_set?(del_verts(G, verts_of(q ^ (1, length(q) - 2))), s, t, ips)
--    AND ind_prewalk_set?(ips)
--  IMPLIES
--  EXISTS ipss:
--    card[prewalk[T]](ipss) =
theorem plus_path_set : good_p G s t ∧
 path_from_p G q s t ∧
  st_path_set_p (del_verts G (verts_of (q ^ (1, List.length q - 2)))) s t ips
   ∧ ind_prewalk_set_p ips → ∃ ipss, 
  card ipss = card ips + 1 ∧
   st_path_set_p G s t ipss ∧ ind_prewalk_set_p ipss := by
  -- === TCCs for plus_path_set ===
  -- TCC: plus_path_set_TCC1
  -- Statement: FORALL (G: graph[T], q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND good?(G, s, t) IMPLIES length(q) - 2 >= 0
  have plus_path_set_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: plus_path_set_TCC2
  -- Statement: FORALL (G: graph[T], q: prewalk[T], s, t: T):
  --   path_from?(G, q, s, t) AND good?(G, s, t) IMPLIES
  --    length(^[T](q, (1, length(q) - 2))) > 0
  have plus_path_set_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.short_path_attach
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      B(G, s, t, k) AND separates(G, W, s, t) AND (card[T](W) = k)
--  AND (FORALL V:
--         separates(G, V, s, t) AND card[T](V) = k IMPLIES
--          attached?(G, s, t, V))
--  AND (FORALL GG: size(GG) < s
theorem short_path_attach : B G s t k ∧ separates G W s t ∧ (card W = k)
 ∧ (∀ V, 
        separates G V s t ∧ card V = k →
         attached_p G s t V)
 ∧ (∀ GG,  size GG < size G → C GG s t) → many_ind G s t k ∨
 ∃ a, 
   edge_p G(s, a) ∧
    (edge_p G(a, t) ∨ ∃ c,  edge_p G(a, c) ∧ edge_p G(c, t)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.bridge_one
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: edge?(G)(s, a) AND
--  edge?(G)(a, t) AND (FORALL GG: size(GG) < size(G) IMPLIES C(GG, s, t))
--  IMPLIES B_many(G, s, t, k)
theorem bridge_one : edge_p G(s, a) ∧
 edge_p G(a, t) ∧ (∀ GG,  size GG < size G → C GG s t) → B_many G s t k := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.bridge_two
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (k >= 1) AND edge?(G)(s, a) AND edge?(G)(a, c) AND edge?(G)(c, t)
--  AND B(del_verts(G, dbl[T](a, c)), s, t, k - 1)
--  AND (FORALL GG: size(GG) < size(G) IMPLIES C(GG, s, t))
--  IMPLIES B_many(G, s, t,
theorem bridge_two : (k ≥ 1) ∧ edge_p G(s, a) ∧ edge_p G(a, c) ∧ edge_p G(c, t)
 ∧ B (del_verts G (dbl a c)) s t (k - 1)
 ∧ (∀ GG,  size GG < size G → C GG s t) → B_many G s t k := by
  -- === TCCs for bridge_two ===
  -- TCC: bridge_two_TCC1
  -- Statement: FORALL (G: graph[T], a, c: T, k: nat, s, t: T):
  --   edge?(G)(c, t) AND edge?(G)(a, c) AND edge?(G)(s, a) AND k >= 1 IMPLIES
  --    k - 1 >= 0
  have bridge_two_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.bridge_three
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt:      (k >= 1) AND edge?(G)(s, a) AND edge?(G)(a, c) AND edge?(G)(c, t)
--  AND B(G, s, t, k) AND separates(G, W, s, t) AND (card[T](W) = k)
--  AND (FORALL V:
--         separates(G, V, s, t) AND card[T](V) = 
theorem bridge_three : (k ≥ 1) ∧ edge_p G(s, a) ∧ edge_p G(a, c) ∧ edge_p G(c, t)
 ∧ B G s t k ∧ separates G W s t ∧ (card W = k)
 ∧ (∀ V, 
        separates G V s t ∧ card V = k →
         attached_p G s t V) → B (del_verts G (dbl a c)) s t (k - 1) ∨
 edge_p G(s, c) ∨ edge_p G(a, t) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.C_induct_lemma
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: C_induct(G, s, t)
theorem C_induct_lemma : C_induct G s t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.Menger_k_hard
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: C(G, s, t)
theorem Menger_k_hard : C G s t := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: k_menger.Many_indep_paths
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: good?(G, s, t) AND
--  k <= size(G) - 2 AND
--   (FORALL W:
--      card[T](W) = k AND subset?(W, difference(vert(G), dbl(s, t))) IMPLIES
--       EXISTS q: walk_from?(del_verts(G, W), q, s, t))
--  IMPLIES many_ind
theorem Many_indep_paths : good_p G s t ∧
 k ≤ size G - 2 ∧
  (∀ W, 
     card W = k ∧ subset_p W (difference (vert G) (dbl s t)) →
      ∃ q,  walk_from_p (del_verts G W) q s t) → many_ind G s t (k + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: k_menger.hard_menger_trad
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: separable?(G, s, t) AND sep_num(G, s, t) = k AND vert(G)(s) AND vert(G)(t)
--  IMPLIES
--  (EXISTS (ips: set_of_paths(G, s, t)):
--     card(ips) = k AND ind_path_set?(G, s, t, ips))
theorem hard_menger_trad : separable_p G s t ∧ sep_num G s t = k ∧ vert G(s) ∧ vert G(t) → (∃ (ips : set_of_paths G s t), card ips = k ∧ ind_path_set_p G s t ips) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.k_menger