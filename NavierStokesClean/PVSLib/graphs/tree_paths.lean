-- Auto-generated by pvs2lean/translator.py
-- PVS theory: tree_paths
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: tree_paths

Auto-generated stubs from PVSLib theory `tree_paths`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.tree_paths

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (PROJ_1 : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (PROJ_2 : pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} (Path : pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (cycle_p : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ15 : Type*} (length : pvs_τ15 → Prop)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (path_from_p : pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (rev : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} (tree_p : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (trunc1 : pvs_τ27 → pvs_τ28)

-- pvs2lean: tree_paths.dual_paths?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: p /= q AND
--  finseq_appl[T](p)(0) = finseq_appl[T](q)(0) AND
--   finseq_appl[T](p)(length(p) - 1) = finseq_appl[T](q)(length(q) - 1)
axiom dual_paths_p {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ9 → pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: tree_paths.min_dual_paths
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: min[[Path(G), Path(G)],
--     (LAMBDA (r: Path(G), s: Path(G)): length(r) + length(s)),
--     (LAMBDA (r: Path(G), s: Path(G)): dual_paths?(G, r, s))]
axiom min_dual_paths {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19

-- pvs2lean: tree_paths.is_min_dual?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: dual_paths?(G, p, q) AND
--  FORALL (r: Path(G), s: Path(G)):
--    dual_paths?(G, r, s) IMPLIES
--     length(r) + length(s) >= length(p) + length(q)
axiom is_min_dual_p {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} : pvs_τ12 → pvs_τ13 → pvs_τ14 → Prop

-- pvs2lean: tree_paths.min_dual_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: Graph[T], r: Path(G), s: Path(G)):
--   dual_paths?(G, r, s) IMPLIES
--    is_min_dual?(G, PROJ_1(min_dual_paths(G, r, s)),
--                 PROJ_2(min_dual_paths(G, r, s)))
--     AND
--     dual_paths
theorem min_dual_def : ∀ (G : {g : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // nonempty_p (vert g)}) (r : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}) (s : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}), dual_paths_p G r s → is_min_dual_p G (PROJ_1 (min_dual_paths G r s)) (PROJ_2 (min_dual_paths G r s))
 ∧
 dual_paths_p G (PROJ_1 (min_dual_paths G r s)) (PROJ_2 (min_dual_paths G r s)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tree_paths.min_dual_exists
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: Graph[T], r: Path(G), s: Path(G)):
--   dual_paths?(G, r, s) IMPLIES EXISTS (p, q: Path(G)): is_min_dual?(G, p, q)
theorem min_dual_exists : ∀ (G : {g : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // nonempty_p (vert g)}) (r : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}) (s : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}), dual_paths_p G r s → ∃ (p : _) (q : Path G), is_min_dual_p G p q := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tree_paths.dual_path_trunc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: Graph[T], p: Path(G), q: Path(G)):
--   dual_paths?(G, p, q) IMPLIES
--         (length(p) > 1) AND (length(q) > 1)
--     AND path?[T](G, p ^ (0, length(p) - 2))
--     AND path?[T](G, q ^ (0, length(
theorem dual_path_trunc : ∀ (G : {g : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // nonempty_p (vert g)}) (p : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}) (q : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}), dual_paths_p G p q → (List.length p > 1) ∧ (List.length q > 1)
 ∧ path_p G (p ^ (0, List.length p - 2))
 ∧ path_p G (q ^ (0, List.length q - 2))
 ∧ path_p G (p ^ (1, List.length p - 1))
 ∧ path_p G (q ^ (1, List.length q - 1)) := by
  -- === TCCs for dual_path_trunc ===
  -- TCC: dual_path_trunc_TCC1
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   dual_paths?(G, p, q) AND length(p) > 1 AND length(q) > 1 IMPLIES
  --    length(p) - 2 >= 0
  have dual_path_trunc_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_path_trunc_TCC2
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   dual_paths?(G, p, q) AND length(p) > 1 AND length(q) > 1 IMPLIES
  --    length(^[T](p, (0, length(p) - 2))) > 0
  have dual_path_trunc_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_path_trunc_TCC3
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   dual_paths?(G, p, q) AND
  --    length(p) > 1 AND length(q) > 1 AND path?[T](G, p ^ (0, length(p) - 2))
  --    IMPLIES length(q) - 2 >= 0
  have dual_path_trunc_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_path_trunc_TCC4
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   dual_paths?(G, p, q) AND
  --    length(p) > 1 AND length(q) > 1 AND path?[T](G, p ^ (0, length(p) - 2))
  --    IMPLIES length(^[T](q, (0, length(q) - 2...
  have dual_path_trunc_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_path_trunc_TCC5
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --        dual_paths?(G, p, q) AND (length(p) > 1) AND (length(q) > 1)
  --    AND path?[T](G, p ^ (0, length(p) - 2))
  --    AND path?[T](G, q ^ (0, length(...
  have dual_path_trunc_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_path_trunc_TCC6
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --        dual_paths?(G, p, q) AND (length(p) > 1) AND (length(q) > 1)
  --    AND path?[T](G, p ^ (0, length(p) - 2))
  --    AND path?[T](G, q ^ (0, length(...
  have dual_path_trunc_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_path_trunc_TCC7
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --        dual_paths?(G, p, q) AND (length(p) > 1) AND (length(q) > 1)
  --    AND path?[T](G, p ^ (0, length(p) - 2))
  --    AND path?[T](G, q ^ (0, length(...
  have dual_path_trunc_tcc7 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_path_trunc_TCC8
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --        dual_paths?(G, p, q) AND (length(p) > 1) AND (length(q) > 1)
  --    AND path?[T](G, p ^ (0, length(p) - 2))
  --    AND path?[T](G, q ^ (0, length(...
  have dual_path_trunc_tcc8 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tree_paths.dual_path_length
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: Graph[T], p: Path(G), q: Path(G)):
--   dual_paths?(G, p, q) IMPLIES length(p) > 2 OR length(q) > 2
theorem dual_path_length : ∀ (G : {g : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // nonempty_p (vert g)}) (p : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}) (q : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}), dual_paths_p G p q → List.length p > 2 ∨ List.length q > 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tree_paths.min_dual_reduc
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: Graph[T], p: Path(G), q: Path(G)):
--   is_min_dual?(G, p, q) IMPLIES
--    finseq_appl[T](p)(1) /= finseq_appl[T](q)(1) AND
--     finseq_appl[T](p)(length(p) - 2) /= finseq_appl[T](q)(length(q) - 
theorem min_dual_reduc : ∀ (G : {g : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // nonempty_p (vert g)}) (p : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}) (q : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}), is_min_dual_p G p q → finseq_appl p(1) ≠ finseq_appl q(1) ∧
 finseq_appl p(List.length p - 2) ≠ finseq_appl q(List.length q - 2) := by
  -- === TCCs for min_dual_reduc ===
  -- TCC: min_dual_reduc_TCC1
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   is_min_dual?(G, p, q) IMPLIES 1 < length(p)
  have min_dual_reduc_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_dual_reduc_TCC2
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   is_min_dual?(G, p, q) IMPLIES 1 < length(q)
  have min_dual_reduc_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_dual_reduc_TCC3
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   is_min_dual?(G, p, q) AND finseq_appl[T](p)(1) /= finseq_appl[T](q)(1)
  --    IMPLIES length(p) - 2 >= 0 AND length(p) - 2 < length(p)
  have min_dual_reduc_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_dual_reduc_TCC4
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   is_min_dual?(G, p, q) AND finseq_appl[T](p)(1) /= finseq_appl[T](q)(1)
  --    IMPLIES length(q) - 2 >= 0 AND length(q) - 2 < length(q)
  have min_dual_reduc_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tree_paths.min_dual_distin
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: Graph[T], p: Path(G), q: Path(G)):
--   is_min_dual?(G, p, q) IMPLIES
--    (FORALL (i, j: nat):
--       i < length(p) - 1 AND
--        j < length(q) - 1 AND finseq_appl[T](p)(i) = finseq_appl[T](q)(
theorem min_dual_distin : ∀ (G : {g : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // nonempty_p (vert g)}) (p : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}) (q : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}), is_min_dual_p G p q → (∀ (i : _) (j : ℕ), i < List.length p - 1 ∧
    j < List.length q - 1 ∧ finseq_appl p(i) = finseq_appl q(j)
    → (i = 0 ∧ j = 0)) := by
  -- === TCCs for min_dual_distin ===
  -- TCC: min_dual_distin_TCC1
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   is_min_dual?(G, p, q) IMPLIES
  --    FORALL (i, j: nat):
  --      i < length(p) - 1 AND j < length(q) - 1 IMPLIES i < length(p)
  have min_dual_distin_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: min_dual_distin_TCC2
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   is_min_dual?(G, p, q) IMPLIES
  --    FORALL (i, j: nat):
  --      i < length(p) - 1 AND j < length(q) - 1 IMPLIES j < length(q)
  have min_dual_distin_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tree_paths.dual_cycle
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (G: Graph[T], p: Path(G), q: Path(G)):
--   dual_paths?(G, p, q) AND
--    (FORALL (i, j: nat):
--       i < length(p) - 1 AND
--        j < length(q) - 1 AND finseq_appl[T](p)(i) = finseq_appl[T](q)(j)
--   
theorem dual_cycle : ∀ (G : {g : {g : ({S : T → Bool // ∃ (x: T), (y: T): x ≠ y ∧ S = dbl x y} → Bool) × (T → Bool) // ∀ (e : doubleton T), edges g(e) → ∀ (x : T), e x → vert g(x)} // nonempty_p (vert g)}) (p : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}) (q : {p : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // path_p G p}), dual_paths_p G p q ∧
 (∀ (i : _) (j : ℕ), i < List.length p - 1 ∧
     j < List.length q - 1 ∧ finseq_appl p(i) = finseq_appl q(j)
     → (i = 0 ∧ j = 0)) → cycle_p G (trunc1 p ∘ rev q) := by
  -- === TCCs for dual_cycle ===
  -- TCC: dual_cycle_TCC1
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   dual_paths?(G, p, q) IMPLIES
  --    FORALL (i, j: nat):
  --      i < length(p) - 1 AND j < length(q) - 1 IMPLIES i < length(p)
  have dual_cycle_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_cycle_TCC2
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   dual_paths?(G, p, q) IMPLIES
  --    FORALL (i, j: nat):
  --      i < length(p) - 1 AND j < length(q) - 1 IMPLIES j < length(q)
  have dual_cycle_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_cycle_TCC3
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   (dual_paths?(G, p, q) AND
  --     FORALL (i, j: nat):
  --       i < length(p) - 1 AND
  --        j < length(q) - 1 AND finseq_appl[T](p)(i) = finseq_appl[T...
  have dual_cycle_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: dual_cycle_TCC4
  -- Statement: FORALL (G: Graph[T], p: Path[T](G), q: Path[T](G)):
  --   (dual_paths?(G, p, q) AND
  --     FORALL (i, j: nat):
  --       i < length(p) - 1 AND
  --        j < length(q) - 1 AND finseq_appl[T](p)(i) = finseq_appl[T...
  have dual_cycle_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tree_paths.tree_one_path
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: tree?(G) AND path_from?(G, p, u, v) AND path_from?(G, q, u, v) IMPLIES p = q
theorem tree_one_path : tree_p G ∧ path_from_p G p u v ∧ path_from_p G q u v → p = q := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.tree_paths