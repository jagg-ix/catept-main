-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cycles
-- PVSLib library: graphs
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cycles

Auto-generated stubs from PVSLib theory `cycles`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cycles

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Seq : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} (below : pvs_τ3 → pvs_τ4)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (endpoint_p : pvs_τ7 → pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} (length : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (path_p : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} (pre_circuit_p : pvs_τ12 → pvs_τ13 → Prop)

-- pvs2lean: cycles.cycle?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pre_circuit?(G, w) AND
--  length(w) > 1 AND
--   finseq_appl[T](w)(1) /= finseq_appl[T](w)(length(w) - 2) AND
--    (FORALL (i, j: below(length(w))):
--       finseq_appl[T](w)(i) = finseq_appl[T](w)(j) IMPLIES
-- 
axiom cycle_p {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ5 → pvs_τ6 → Prop

-- pvs2lean: cycles.cycle_l_gt_3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (w: Seq(G)): cycle?(G, w) IMPLIES length(w) > 3)
theorem cycle_l_gt_3 : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}), cycle_p G w → List.length w > 3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cycles.cycle_has_path
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Seq(G)):
--   cycle?(G, w) IMPLIES (EXISTS (j: below(length(w))): path?(G, w ^ (0, j)))
theorem cycle_has_path : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}), cycle_p G w → (∃ (j : Finset.range List.length w), path_p G (w ^ (0, j))) := by
  -- === TCCs for cycle_has_path ===
  -- TCC: cycle_has_path_TCC1
  -- Statement: FORALL (G: graph[T], w: Seq[T](G)):
  --   cycle?(G, w) IMPLIES
  --    FORALL (j: below(length(w))): length(^[T](w, (0, j))) > 0
  have cycle_has_path_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cycles.cycle_gt3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Seq(G)): cycle?(G, w) IMPLIES length(w) > 3
theorem cycle_gt3 : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}), cycle_p G w → List.length w > 3 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cycles.cycle_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (w: Seq(G)):
--   cycle?(G, w) IFF
--    pre_circuit?(G, w) AND
--     length(w) > 3 AND
--      (FORALL (i, j: below(length(w))):
--         finseq_appl[T](w)(i) = finseq_appl[T](w)(j) IMPLIES
--          (i = 
theorem cycle_def : ∀ (w : {w : {w : {i : ℝ // i ≥ 0} × ({s : {i : ℝ // i ≥ 0} // s < List.length} → T) // List.length w > 0} // verts_in_p G w}), cycle_p G w ↔ pre_circuit_p G w ∧
 List.length w > 3 ∧
  (∀ (i : _) (j : Finset.range List.length w), finseq_appl w(i) = finseq_appl w(j) →
      (i = j ∨ (endpoint_p i w ∧ endpoint_p j w))) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cycles