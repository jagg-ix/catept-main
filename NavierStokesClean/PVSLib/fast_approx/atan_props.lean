-- Auto-generated by pvs2lean/translator.py
-- PVS theory: atan_props
-- PVSLib library: fast_approx
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: atan_props

Auto-generated stubs from PVSLib theory `atan_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.atan_props

noncomputable section

variable {pvs_τ1 : Type*} (atan : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (atan_alt : pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} (atan_approx_iter_alt : pvs_τ5 → pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (atan_approx_pos : pvs_τ11 → pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (atan_approx_pos_le1_alt : pvs_τ15 → pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} (atan_fast_approx_pihalf : pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} (atan_series_n : pvs_τ22 → pvs_τ23 → pvs_τ24)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} (below : pvs_τ25 → pvs_τ26)
variable {pvs_τ34 : Type*} (ceiling : pvs_τ34 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (pi_fast_approx : pvs_τ38 → pvs_τ39)
variable {pvs_τ40 : Type*} {pvs_τ41 : Type*} (pi_fast_approx_br : pvs_τ40 → pvs_τ41)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (pihalf_fast_approx : pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} (pihalf_fast_approx_br : pvs_τ44 → pvs_τ45)
variable {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} (reduce_approx : pvs_τ46 → pvs_τ47 → pvs_τ48)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} (reduce_fun : pvs_τ49 → pvs_τ50)

-- pvs2lean: atan_props.reduce_less
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, eps: posreal): reduce_approx(x, eps) <= reduce_fun(x)
theorem reduce_less : ∀ (x : {x : ℝ // x ≥ 0}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), reduce_approx x eps ≤ reduce_fun x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.reduce_close
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, eps: posreal):
--   reduce_fun(x) - reduce_approx(x, eps) <= x * eps / 4
theorem reduce_close : ∀ (x : {x : ℝ // x ≥ 0}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), reduce_fun x - reduce_approx x eps ≤ x * eps / 4 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.atan_of_sum
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: nnreal): atan(x) <= atan(x + y) AND atan(x + y) <= atan(x) + y
theorem atan_of_sum : ∀ (x : {x : ℝ // x ≥ 0}) (y : {x : ℝ // x ≥ 0}), atan x ≤ atan (x + y) ∧ atan (x + y) ≤ atan x + y := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.atan_reduce_close
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: nnreal, eps: posreal):
--   2 * atan(reduce_approx(x, eps)) <= atan(x) AND
--    atan(x) <= 2 * (atan(reduce_approx(x, eps))) + x * eps / 2
theorem atan_reduce_close : ∀ (x : {x : ℝ // x ≥ 0}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), 2 * atan (reduce_approx x eps) ≤ atan x ∧ atan x ≤ 2 * (atan (reduce_approx x eps)) + x * eps / 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.eps_sig
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(1, n, LAMBDA (k: nat): eps / 2 ^ k)
axiom eps_sig {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} : pvs_τ35 → pvs_τ36 → pvs_τ37

-- pvs2lean: atan_props.eps_sig_next
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (eps: posreal, n: posnat):
--   eps_sig(eps, n) + eps / 2 ^ (n + 1) = eps_sig(eps, n + 1)
theorem eps_sig_next : ∀ (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : {i : ℝ // i ≥ 0} // i > 0}), eps_sig eps n + eps / 2 ^ (n + 1) = eps_sig eps (n + 1) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.atan_iter_alt_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x <= 1), eps: posreal, n: nat,
--         (b: real
--              | LET B = IF n = 0 THEN 0 ELSE atan_series_n(x, n - 1) ENDIF
--                  IN
--                  B <= b AND
--          
theorem atan_iter_alt_series : ∀ (x : {x : {x : ℝ // x ≥ 0} // x ≤ 1}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : ℝ // i ≥ 0}) (b : {b : ℝ // (let B := if n = 0 then 0 else atan_series_n x (n - 1) ; B ≤ b ∧ b ≤ B + if n = 0 then 0 else eps_sig eps n )}) (c : {c : {x : ℝ // x ≥ 0} // c ≤ 1 ∧
 c = x ^ (2 * n + 1) ∧
  c / (2 * n + 1) > eps ∧ c * x ^ 2 / (2 * n + 3) ≤ eps}), ∃ (epsum : {epsum : {x : ℝ // x ≥ 0} // epsum ≤ eps_sig eps (n + 1)}), atan_approx_iter_alt x eps n b c = atan_series_n x n + epsum := by
  -- === TCCs for atan_iter_alt_series ===
  -- TCC: atan_iter_alt_series_TCC1
  -- Statement: FORALL ((x: nnreal | x <= 1), n: nat): NOT n = 0 IMPLIES n - 1 >= 0
  have atan_iter_alt_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_iter_alt_series_TCC2
  -- Statement: FORALL ((x: nnreal | x <= 1), n: nat, b: real, B: real):
  --   B = IF n = 0 THEN 0 ELSE atan_series_n(x, n - 1) ENDIF AND
  --    B <= b AND NOT n = 0
  --    IMPLIES n > 0
  have atan_iter_alt_series_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_iter_alt_series_TCC3
  -- Statement: FORALL ((x: nnreal | x <= 1), eps: posreal, n: nat,
  --         (b: real
  --              | LET B = IF n = 0 THEN 0 ELSE atan_series_n(x, n - 1) ENDIF
  --                  IN
  --                  B <= b AND
  --       ...
  have atan_iter_alt_series_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_iter_alt_series_TCC4
  -- Statement: FORALL ((x: nnreal | x <= 1), eps: posreal, n: nat,
  --         (b: real
  --              | LET B = IF n = 0 THEN 0 ELSE atan_series_n(x, n - 1) ENDIF
  --                  IN
  --                  B <= b AND
  --       ...
  have atan_iter_alt_series_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.atan_iter_alt_series2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x <= 1), eps: posreal, n: nat, m: posnat,
--         (b: real
--              | LET B = IF n = 0 THEN 0 ELSE atan_series_n(x, n - 1) ENDIF
--                  IN
--                  B <= b AN
theorem atan_iter_alt_series2 : ∀ (x : {x : {x : ℝ // x ≥ 0} // x ≤ 1}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : ℝ // i ≥ 0}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (b : {b : ℝ // (let B := if n = 0 then 0 else atan_series_n x (n - 1) ; B ≤ b ∧ b ≤ B + if n = 0 then 0 else eps_sig eps n )}) (c : {c : {x : ℝ // x ≥ 0} // c ≤ 1 ∧
 c = x ^ (2 * n + 1) ∧
  (∀ (mm : Finset.range m), c * x ^ (2 * mm) / (2 * (n + mm) + 1) > eps) ∧
   c * x ^ (2 * m) / (2 * (n + m) + 1) ≤ eps}), ∃ (epsum : {epsum : {x : ℝ // x ≥ 0} // epsum ≤ eps_sig eps (n + m)}), atan_approx_iter_alt x eps n b c = atan_series_n x ((n + m - 1)) + epsum := by
  -- === TCCs for atan_iter_alt_series2 ===
  -- TCC: atan_iter_alt_series2_TCC1
  -- Statement: FORALL ((x: nnreal | x <= 1), eps: posreal, n: nat, m: posnat,
  --         (b: real
  --              | LET B = IF n = 0 THEN 0 ELSE atan_series_n(x, n - 1) ENDIF
  --                  IN
  --                  B <= b...
  have atan_iter_alt_series2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_iter_alt_series2_TCC2
  -- Statement: FORALL ((x: nnreal | x <= 1), eps: posreal, n: nat, m: posnat,
  --         (b: real
  --              | LET B = IF n = 0 THEN 0 ELSE atan_series_n(x, n - 1) ENDIF
  --                  IN
  --                  B <= b...
  have atan_iter_alt_series2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_iter_alt_series2_TCC3
  -- Statement: FORALL ((x: nnreal | x <= 1), eps: posreal, n: nat, m: posnat,
  --         (b: real
  --              | LET B = IF n = 0 THEN 0 ELSE atan_series_n(x, n - 1) ENDIF
  --                  IN
  --                  B <= b...
  have atan_iter_alt_series2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.atan_alt_series
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x <= 1), eps: posreal,
--         (n: posnat
--              | (FORALL (m: below(n)): x ^ (2 * m + 1) / (2 * m + 1) > eps)
--                 AND x ^ (2 * n + 1) / (2 * n + 1) <= eps)):
--   
theorem atan_alt_series : ∀ (x : {x : {x : ℝ // x ≥ 0} // x ≤ 1}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {n : {i : {i : ℝ // i ≥ 0} // i > 0} // (∀ (m : Finset.range n), x ^ (2 * m + 1) / (2 * m + 1) > eps) ∧
 x ^ (2 * n + 1) / (2 * n + 1) ≤ eps}), ∃ (delt : {delt : {x : ℝ // x ≥ 0} // delt ≤ eps}), atan_approx_iter_alt x eps 0 0 x = atan_series_n x (n - 1) + delt := by
  -- === TCCs for atan_alt_series ===
  -- TCC: atan_alt_series_TCC1
  -- Statement: FORALL ((x: nnreal | x <= 1), n: posnat, m: below(n)):
  --   x /= 0 OR (2 * m + 1) >= 0
  have atan_alt_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_alt_series_TCC2
  -- Statement: FORALL ((x: nnreal | x <= 1), eps: posreal, n: posnat):
  --   (FORALL (m: below(n)): x ^ (2 * m + 1) / (2 * m + 1) > eps) IMPLIES
  --    x /= 0 OR (2 * n + 1) >= 0
  have atan_alt_series_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_alt_series_TCC3
  -- Statement: FORALL ((x: nnreal | x <= 1), eps: posreal,
  --         (n: posnat
  --              | (FORALL (m: below(n)): x ^ (2 * m + 1) / (2 * m + 1) > eps)
  --                 AND x ^ (2 * n + 1) / (2 * n + 1) <= eps),
  -- ...
  have atan_alt_series_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.bounding?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ (2 * n + 1) / (2 * n + 1) <= eps
axiom bounding_p {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} : pvs_τ31 → pvs_τ32 → pvs_τ33 → Prop

-- pvs2lean: atan_props.bound_iter
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF bounding?(x, eps, n + 1) THEN n + 1 ELSE bound_iter(x, eps, n + 1) ENDIF
axiom bound_iter {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ27 → pvs_τ28 → pvs_τ29 → pvs_τ30

-- pvs2lean: atan_props.iter_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x <= 1), eps: posreal):
--   x <= eps OR
--    (EXISTS (n: posnat):
--       (FORALL (m: below(n)): x ^ (2 * m + 1) / (2 * m + 1) > eps) AND
--        x ^ (2 * n + 1) / (2 * n + 1) <= eps)
theorem iter_bound : ∀ (x : {x : {x : ℝ // x ≥ 0} // x ≤ 1}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), x ≤ eps ∨ (∃ (n : {n : ℕ // 0 < n}), (∀ (m : Finset.range n), x ^ (2 * m + 1) / (2 * m + 1) > eps) ∧
    x ^ (2 * n + 1) / (2 * n + 1) ≤ eps) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_props.atan_alt_bound1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x <= 1), eps: posreal):
--   abs(atan(x) - atan_alt(x, eps)) <= eps
theorem atan_alt_bound1 : ∀ (x : {x : {x : ℝ // x ≥ 0} // x ≤ 1}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), |atan x - atan_alt x eps| ≤ eps := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.atan_alt_bound2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: posreal | x <= 1), eps: posreal):
--   abs(atan(x) - atan_approx_pos_le1_alt(x, eps)) <= eps
theorem atan_alt_bound2 : ∀ (x : {x : {x : {x : ℝ // x ≥ 0} // x > 0} // x ≤ 1}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), |atan x - atan_approx_pos_le1_alt x eps| ≤ eps := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.atan_alt_bound3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, eps: posreal,
--         (pihalf: posreal | abs(pi / 2 - pihalf) <= eps / 2)):
--   abs(atan(x) - atan_approx_pos(x, eps, pihalf)) <= eps
theorem atan_alt_bound3 : ∀ (x : {x : {x : ℝ // x ≥ 0} // x > 0}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (pihalf : {pihalf : {x : {x : ℝ // x ≥ 0} // x > 0} // |pi / 2 - pihalf| ≤ eps / 2}), |atan x - atan_approx_pos x eps pihalf| ≤ eps := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.atan_fast_approx_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real, eps: posreal,
--         (pihalf: posreal | abs(pi / 2 - pihalf) <= eps / 2)):
--   abs(atan(x) - atan_fast_approx_pihalf(x, eps, pihalf)) <= eps
theorem atan_fast_approx_bound : ∀ (x : ℝ) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (pihalf : {pihalf : {x : {x : ℝ // x ≥ 0} // x > 0} // |pi / 2 - pihalf| ≤ eps / 2}), |atan x - atan_fast_approx_pihalf x eps pihalf| ≤ eps := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.pi_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (eps: posreal): abs(pi - pi_fast_approx(eps)) <= eps
theorem pi_bound : ∀ (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), |pi - pi_fast_approx eps| ≤ eps := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.pi_br_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (eps: posreal): abs(pi - pi_fast_approx_br(eps)) <= eps
theorem pi_br_bound : ∀ (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), |pi - pi_fast_approx_br eps| ≤ eps := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.pihalf_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (eps: posreal): abs(pi / 2 - pihalf_fast_approx(eps)) <= eps / 2
theorem pihalf_bound : ∀ (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), |pi / 2 - pihalf_fast_approx eps| ≤ eps / 2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_props.pihalf_br_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (eps: posreal): abs(pi / 2 - pihalf_fast_approx_br(eps)) <= eps / 2
theorem pihalf_br_bound : ∀ (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), |pi / 2 - pihalf_fast_approx_br eps| ≤ eps / 2 := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.atan_props