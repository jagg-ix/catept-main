-- Auto-generated by pvs2lean/translator.py
-- PVS theory: sin_props
-- PVSLib library: fast_approx
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: sin_props

Auto-generated stubs from PVSLib theory `sin_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.sin_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ10 : Type*} (factorial : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (lex2 : pvs_τ11 → pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} (log_nat : pvs_τ13 → pvs_τ14 → pvs_τ15)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (sin : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (sin_approx : pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} (sin_approx_iter_alt : pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (sin_fast_approx : pvs_τ27 → pvs_τ28 → pvs_τ29)

-- pvs2lean: sin_props.sin_iter_alt_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x < 7), eps: posreal, n: nat,
--         (b: real | b = IF n = 0 THEN 0 ELSE sin_approx(x, n - 1) ENDIF),
--         (c: nnreal
--              | c = x ^ (2 * n + 1) / factorial(2 * n + 1)
theorem sin_iter_alt_series : ∀ (x : {x : {x : ℝ // x ≥ 0} // x < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : ℝ // i ≥ 0}) (b : {b : ℝ // b = if n = 0 then 0 else sin_approx x (n - 1)}) (c : {c : {x : ℝ // x ≥ 0} // c = x ^ (2 * n + 1) / factorial (2 * n + 1) ∧
 c > eps ∧ c * x ^ 2 / ((2 * n + 3) * (2 * n + 2)) ≤ eps}), sin_approx_iter_alt x eps n b c = sin_approx x n := by
  -- === TCCs for sin_iter_alt_series ===
  -- TCC: sin_iter_alt_series_TCC1
  -- Statement: FORALL ((x: nnreal | x < 7), n: nat): NOT n = 0 IMPLIES n - 1 >= 0
  have sin_iter_alt_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sin_iter_alt_series_TCC2
  -- Statement: FORALL ((x: nnreal | x < 7), n: nat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE sin_approx(x, n - 1) ENDIF)):
  --   x /= 0 OR (2 * n + 1) >= 0
  have sin_iter_alt_series_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sin_iter_alt_series_TCC3
  -- Statement: FORALL ((x: nnreal | x < 7), eps: posreal, n: nat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE sin_approx(x, n - 1) ENDIF),
  --         c: nnreal):
  --   c = x ^ (2 * n + 1) / factorial(2 * n + 1) AND c > ...
  have sin_iter_alt_series_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sin_props.sin_iter_alt_series2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x < 7), eps: posreal, n: nat, m: posnat,
--         (b: real | b = IF n = 0 THEN 0 ELSE sin_approx(x, n - 1) ENDIF),
--         (c: nnreal
--              | c = x ^ (2 * n + 1) AND
--        
theorem sin_iter_alt_series2 : ∀ (x : {x : {x : ℝ // x ≥ 0} // x < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : ℝ // i ≥ 0}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (b : {b : ℝ // b = if n = 0 then 0 else sin_approx x (n - 1)}) (c : {c : {x : ℝ // x ≥ 0} // c = x ^ (2 * n + 1) ∧
 (∀ (mm : Finset.range m), c * x ^ (2 * mm) / factorial (2 * (n + mm) + 1) > eps)
  ∧ c * x ^ (2 * m) / factorial (2 * (n + m) + 1) ≤ eps}), sin_approx_iter_alt x eps n b (c / factorial (2 * n + 1)) = sin_approx x ((n + m - 1)) := by
  -- === TCCs for sin_iter_alt_series2 ===
  -- TCC: sin_iter_alt_series2_TCC1
  -- Statement: FORALL ((x: nnreal | x < 7), n: nat, m: posnat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE sin_approx(x, n - 1) ENDIF),
  --         c: nnreal):
  --   c = x ^ (2 * n + 1) IMPLIES FORALL (mm: below(m)): x /...
  have sin_iter_alt_series2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sin_iter_alt_series2_TCC2
  -- Statement: FORALL ((x: nnreal | x < 7), eps: posreal, n: nat, m: posnat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE sin_approx(x, n - 1) ENDIF),
  --         c: nnreal):
  --   c = x ^ (2 * n + 1) AND
  --    (FORALL (mm: ...
  have sin_iter_alt_series2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sin_iter_alt_series2_TCC3
  -- Statement: FORALL ((x: nnreal | x < 7), eps: posreal, n: nat, m: posnat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE sin_approx(x, n - 1) ENDIF),
  --         (c: nnreal
  --              | c = x ^ (2 * n + 1) AND
  --     ...
  have sin_iter_alt_series2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sin_props.sin_iter_alt_series3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x < 7), eps: posreal,
--         (n: posnat
--              | (FORALL (m: below(n)):
--                   x ^ (2 * m + 1) / factorial(2 * m + 1) > eps)
--                 AND x ^ (2 * n + 1) 
theorem sin_iter_alt_series3 : ∀ (x : {x : {x : ℝ // x ≥ 0} // x < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {n : {i : {i : ℝ // i ≥ 0} // i > 0} // (∀ (m : Finset.range n), x ^ (2 * m + 1) / factorial (2 * m + 1) > eps) ∧
 x ^ (2 * n + 1) / factorial (2 * n + 1) ≤ eps}), sin_approx_iter_alt x eps 0 0 x = sin_approx x (n - 1) := by
  -- === TCCs for sin_iter_alt_series3 ===
  -- TCC: sin_iter_alt_series3_TCC1
  -- Statement: FORALL ((x: nnreal | x < 7), n: posnat, m: below(n)):
  --   x /= 0 OR (2 * m + 1) >= 0
  have sin_iter_alt_series3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sin_iter_alt_series3_TCC2
  -- Statement: FORALL ((x: nnreal | x < 7), eps: posreal, n: posnat):
  --   (FORALL (m: below(n)): x ^ (2 * m + 1) / factorial(2 * m + 1) > eps)
  --    IMPLIES x /= 0 OR (2 * n + 1) >= 0
  have sin_iter_alt_series3_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sin_iter_alt_series3_TCC3
  -- Statement: FORALL ((x: nnreal | x < 7), eps: posreal,
  --         (n: posnat
  --              | (FORALL (m: below(n)):
  --                   x ^ (2 * m + 1) / factorial(2 * m + 1) > eps)
  --                 AND x ^ (2 * n + ...
  have sin_iter_alt_series3_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: sin_props.bounding?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ (2 * n + 1) / factorial(2 * n + 1) <= eps
axiom bounding_p {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: sin_props.bound_iter
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF bounding?(x, eps, n + 1) THEN n + 1 ELSE bound_iter(x, eps, n + 1) ENDIF
axiom bound_iter {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6

-- pvs2lean: sin_props.iter_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: nnreal | x < 7), eps: posreal):
--   x <= eps OR
--    (EXISTS (nn: posnat):
--       (FORALL (m: below(nn)): x ^ (2 * m + 1) / factorial(2 * m + 1) > eps)
--        AND x ^ (2 * nn + 1) / factorial(2
theorem iter_bound : ∀ (x : {x : {x : ℝ // x ≥ 0} // x < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), x ≤ eps ∨ (∃ (nn : {n : ℕ // 0 < n}), (∀ (m : Finset.range nn), x ^ (2 * m + 1) / factorial (2 * m + 1) > eps)
    ∧ x ^ (2 * nn + 1) / factorial (2 * nn + 1) ≤ eps) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: sin_props.sin_fast_approx_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: real | abs(x) < 7), eps: posreal):
--   abs(sin(x) - sin_fast_approx(x, eps)) <= eps
theorem sin_fast_approx_bound : ∀ (x : {x : ℝ // |x| < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), |sin x - sin_fast_approx x eps| ≤ eps := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.sin_props