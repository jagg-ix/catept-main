-- Auto-generated by pvs2lean/translator.py
-- PVS theory: cos_props
-- PVSLib library: fast_approx
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: cos_props

Auto-generated stubs from PVSLib theory `cos_props`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.cos_props

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (cos : pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (cos_approx : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} (cos_approx_iter_alt : pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20)
variable {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} (cos_fast_approx : pvs_τ21 → pvs_τ22 → pvs_τ23)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (cos_term : pvs_τ24 → pvs_τ25)
variable {pvs_τ26 : Type*} (factorial : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} {pvs_τ28 : Type*} (lex2 : pvs_τ27 → pvs_τ28 → Prop)
variable {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} (log_nat : pvs_τ29 → pvs_τ30 → pvs_τ31)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} (pm1 : pvs_τ32 → pvs_τ33)

-- pvs2lean: cos_props.cos_iter_alt_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: posreal | x < 7), eps: posreal, n: nat,
--         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF),
--         (c: nnreal
--              | c = x ^ (2 * n) / factorial(2 * n) AND
--   
theorem cos_iter_alt_series : ∀ (x : {x : {x : {x : ℝ // x ≥ 0} // x > 0} // x < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : ℝ // i ≥ 0}) (b : {b : ℝ // b = if n = 0 then 0 else cos_approx x (n - 1)}) (c : {c : {x : ℝ // x ≥ 0} // c = x ^ (2 * n) / factorial (2 * n) ∧
 c > eps ∧ c * x ^ 2 / ((2 * n + 1) * (2 * n + 2)) ≤ eps}), fun (N : {i : ℝ // i ≥ 0}) => cos_approx_iter_alt x eps n b c = cos_approx x N (IF (b + pm1 n * c ≠ 0) n (n + 1)) := by
  -- === TCCs for cos_iter_alt_series ===
  -- TCC: cos_iter_alt_series_TCC1
  -- Statement: FORALL ((x: posreal | x < 7), n: nat): NOT n = 0 IMPLIES n - 1 >= 0
  have cos_iter_alt_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series_TCC2
  -- Statement: FORALL ((x: posreal | x < 7), n: nat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF)):
  --   x /= 0 OR (2 * n) >= 0
  have cos_iter_alt_series_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series_TCC3
  -- Statement: FORALL ((x: posreal | x < 7), eps: posreal, n: nat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF),
  --         c: nnreal):
  --   c = x ^ (2 * n) / factorial(2 * n) AND c > eps IMP...
  have cos_iter_alt_series_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series_TCC4
  -- Statement: FORALL ((x: posreal | x < 7), eps: posreal, n: nat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF),
  --         (c: nnreal
  --              | c = x ^ (2 * n) / factorial(2 * n) AND...
  have cos_iter_alt_series_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cos_props.cos_iter_alt_series2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: posreal | x < 7), eps: posreal, n: nat, m: posnat,
--         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF),
--         (c: nnreal
--              | c = x ^ (2 * n) AND
--           
theorem cos_iter_alt_series2 : ∀ (x : {x : {x : {x : ℝ // x ≥ 0} // x > 0} // x < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {i : ℝ // i ≥ 0}) (m : {i : {i : ℝ // i ≥ 0} // i > 0}) (b : {b : ℝ // b = if n = 0 then 0 else cos_approx x (n - 1)}) (c : {c : {x : ℝ // x ≥ 0} // c = x ^ (2 * n) ∧
 (∀ (mm : Finset.range m), c * x ^ (2 * mm) / factorial (2 * (n + mm)) > eps)
  ∧ c * x ^ (2 * m) / factorial (2 * (n + m)) ≤ eps}), fun (N : ℝ) => cos_approx_iter_alt x eps n b (c / factorial (2 * n)) = cos_approx x N (IF (b + Finset.sum (Finset.Icc n n + m - 1) (cos_term x) ≠ 0) (n + m - 1) (n + m)) := by
  -- === TCCs for cos_iter_alt_series2 ===
  -- TCC: cos_iter_alt_series2_TCC1
  -- Statement: FORALL ((x: posreal | x < 7), n: nat, m: posnat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF),
  --         c: nnreal):
  --   c = x ^ (2 * n) IMPLIES FORALL (mm: below(m)): x /= 0...
  have cos_iter_alt_series2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series2_TCC2
  -- Statement: FORALL ((x: posreal | x < 7), eps: posreal, n: nat, m: posnat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF),
  --         c: nnreal):
  --   c = x ^ (2 * n) AND
  --    (FORALL (mm: bel...
  have cos_iter_alt_series2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series2_TCC3
  -- Statement: FORALL ((x: posreal | x < 7), eps: posreal, n: nat, m: posnat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF),
  --         (c: nnreal
  --              | c = x ^ (2 * n) AND
  --        ...
  have cos_iter_alt_series2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series2_TCC4
  -- Statement: FORALL ((x: posreal | x < 7), eps: posreal, n: nat, m: posnat,
  --         (b: real | b = IF n = 0 THEN 0 ELSE cos_approx(x, n - 1) ENDIF),
  --         (c: nnreal
  --              | c = x ^ (2 * n) AND
  --        ...
  have cos_iter_alt_series2_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cos_props.cos_iter_alt_series3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: posreal | x < 7), eps: posreal,
--         (n: posnat
--              | (FORALL (m: below(n)): x ^ (2 * m) / factorial(2 * m) > eps)
--                 AND x ^ (2 * n) / factorial(2 * n) <= eps)):
-- 
theorem cos_iter_alt_series3 : ∀ (x : {x : {x : {x : ℝ // x ≥ 0} // x > 0} // x < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}) (n : {n : {i : {i : ℝ // i ≥ 0} // i > 0} // (∀ (m : Finset.range n), x ^ (2 * m) / factorial (2 * m) > eps) ∧
 x ^ (2 * n) / factorial (2 * n) ≤ eps}), fun (N : ℝ) => cos_approx_iter_alt x eps 0 0 1 = cos_approx x N (IF (cos_approx x (n - 1) ≠ 0) (n - 1) n) := by
  -- === TCCs for cos_iter_alt_series3 ===
  -- TCC: cos_iter_alt_series3_TCC1
  -- Statement: FORALL ((x: posreal | x < 7), n: posnat, m: below(n)):
  --   x /= 0 OR (2 * m) >= 0
  have cos_iter_alt_series3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series3_TCC2
  -- Statement: FORALL ((x: posreal | x < 7), eps: posreal, n: posnat):
  --   (FORALL (m: below(n)): x ^ (2 * m) / factorial(2 * m) > eps) IMPLIES
  --    x /= 0 OR (2 * n) >= 0
  have cos_iter_alt_series3_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series3_TCC3
  -- Statement: FORALL ((x: posreal | x < 7), eps: posreal,
  --         (n: posnat
  --              | (FORALL (m: below(n)): x ^ (2 * m) / factorial(2 * m) > eps)
  --                 AND x ^ (2 * n) / factorial(2 * n) <= eps)...
  have cos_iter_alt_series3_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_iter_alt_series3_TCC4
  -- Statement: FORALL ((x: posreal | x < 7), eps: posreal,
  --         (n: posnat
  --              | (FORALL (m: below(n)): x ^ (2 * m) / factorial(2 * m) > eps)
  --                 AND x ^ (2 * n) / factorial(2 * n) <= eps)...
  have cos_iter_alt_series3_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: cos_props.bounding?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x ^ (2 * n) / factorial(2 * n) <= eps
axiom bounding_p {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ7 → pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: cos_props.bound_iter
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF bounding?(x, eps, n + 1) THEN n + 1 ELSE bound_iter(x, eps, n + 1) ENDIF
axiom bound_iter {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} : pvs_τ3 → pvs_τ4 → pvs_τ5 → pvs_τ6

-- pvs2lean: cos_props.iter_bound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: posreal | x < 7), eps: posreal):
--   1 <= eps OR
--    (EXISTS (nn: posnat):
--       (FORALL (m: below(nn)): x ^ (2 * m) / factorial(2 * m) > eps) AND
--        x ^ (2 * nn) / factorial(2 * nn) <= e
theorem iter_bound : ∀ (x : {x : {x : {x : ℝ // x ≥ 0} // x > 0} // x < 7}) (eps : {x : {x : ℝ // x ≥ 0} // x > 0}), 1 ≤ eps ∨ (∃ (nn : {n : ℕ // 0 < n}), (∀ (m : Finset.range nn), x ^ (2 * m) / factorial (2 * m) > eps) ∧
    x ^ (2 * nn) / factorial (2 * nn) ≤ eps) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: cos_props.cos_fast_approx_bound
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL ((x: real | abs(x) < 7), (eps: posreal | eps < 1)):
--   abs(cos(x) - cos_fast_approx(x, eps)) <= eps
theorem cos_fast_approx_bound : ∀ (x : {x : ℝ // |x| < 7}) (eps : {eps : {x : {x : ℝ // x ≥ 0} // x > 0} // eps < 1}), |cos x - cos_fast_approx x eps| ≤ eps := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.cos_props