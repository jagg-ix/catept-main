-- Auto-generated by pvs2lean/translator.py
-- PVS theory: merten
-- PVSLib library: trig
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: merten

Auto-generated stubs from PVSLib theory `merten`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.merten

noncomputable section

variable {pvs_τ1 : Type*} (conv_series_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} (inf_sum : pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (series : pvs_τ3 → pvs_τ4 → pvs_τ5)

-- pvs2lean: merten.series_prod
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, i, LAMBDA (j: nat): IF j <= i THEN a(j) * b(i - j) ELSE 0 ENDIF)
axiom series_prod {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} : pvs_τ6 → pvs_τ7 → pvs_τ8

-- pvs2lean: merten.merten_init1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: series(series_prod(a, b))(n) =
--  sigma(0, n,
--        LAMBDA (i): IF i <= n THEN a(n - i) * series(b)(i) ELSE 0 ENDIF)
theorem merten_init1 : series (series_prod a b) (n) = Finset.sum (Finset.Icc 0 n) (fun (i : _)=> if i ≤ n then a (n - i) * series b(i) else 0 ) := by
  -- === TCCs for merten_init1 ===
  -- TCC: merten_init1_TCC1
  -- Statement: FORALL (n, i: nat): i <= n IMPLIES n - i >= 0
  have merten_init1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merten.merten_init2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (B: real):
--   series(series_prod(a, b))(n) =
--    series(a)(n) * B +
--     sigma(0, n,
--           LAMBDA (i):
--             IF i <= n THEN a(n - i) * (series(b)(i) - B) ELSE 0 ENDIF)
theorem merten_init2 : ∀ (B : ℝ), series (series_prod a b) (n) = series a(n) * B +
 Finset.sum (Finset.Icc 0 n) (fun (i : _)=> if i ≤ n then a (n - i) * (series b(i) - B) else 0 ) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merten.merten_init3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (conv_series?(abs(a)) AND conv_series?(b)) IMPLIES
--  EXISTS (N: posnat):
--    FORALL (n):
--      n >= N IMPLIES
--       abs(series(b)(n) - inf_sum(b)) <= epsil / (3 * (inf_sum(abs(a)) + 1))
theorem merten_init3 : (conv_series_p |a| ∧ conv_series_p b) → ∃ (N : {n : ℕ // 0 < n}), ∀ (n : _), n ≥ N →
     |series b(n) - inf_sum b| ≤ epsil / (3 * (inf_sum |a| + 1)) := by
  -- === TCCs for merten_init3 ===
  -- TCC: merten_init3_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   conv_series?(b) AND conv_series?(abs(a)) IMPLIES
  --    FORALL (N: posnat, n: nat):
  --      n >= N IMPLIES (3 * (inf_sum(abs(a)) + 1)) /= 0
  have merten_init3_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merten.merten_init4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (conv_series?(abs(a)) AND conv_series?(b)) IMPLIES
--  FORALL (Np):
--    EXISTS (M: nat, X: posreal):
--      FORALL (n):
--        n >= M IMPLIES
--         FORALL (i):
--           i <= Np IMPLIES
--            (abs(se
theorem merten_init4 : (conv_series_p |a| ∧ conv_series_p b) → ∀ (Np : _), ∃ (M : ℕ) (X : {x : ℝ // 0 < x}), ∀ (n : _), n ≥ M →
       ∀ (i : _), i ≤ Np →
          (|series b(i) - inf_sum b| < X ∧
            |a n| ≤ epsil / (3 * Np * (X + 1))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merten.merten_init5
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (conv_series?(abs(a)) AND conv_series?(b)) IMPLIES
--  EXISTS (L: posnat):
--    FORALL (n):
--      n >= L IMPLIES
--       abs(series(a)(n) - inf_sum(a)) <= epsil / (3 * (abs(inf_sum(b)) + 1))
theorem merten_init5 : (conv_series_p |a| ∧ conv_series_p b) → ∃ (L : {n : ℕ // 0 < n}), ∀ (n : _), n ≥ L →
     |series a(n) - inf_sum a| ≤ epsil / (3 * (|inf_sum b| + 1)) := by
  -- === TCCs for merten_init5 ===
  -- TCC: merten_init5_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   conv_series?(b) AND conv_series?(abs(a)) IMPLIES
  --    FORALL (L: posnat, n: nat): n >= L IMPLIES conv_series?(a)
  have merten_init5_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merten.merten1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (conv_series?(abs(a)) AND conv_series?(b)) IMPLIES
--  conv_series?(series_prod(a, b)) AND
--   inf_sum(series_prod(a, b)) = inf_sum(a) * inf_sum(b)
theorem merten1 : (conv_series_p |a| ∧ conv_series_p b) → conv_series_p (series_prod a b) ∧
 inf_sum (series_prod a b) = inf_sum a * inf_sum b := by
  -- === TCCs for merten1 ===
  -- TCC: merten1_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   conv_series?(series_prod(a, b)) AND
  --    conv_series?(b) AND conv_series?(abs(a))
  --    IMPLIES conv_series?(a)
  have merten1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: merten.merten
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (conv_series?(a) AND conv_series?(b)) AND
--  (conv_series?(abs(a)) OR conv_series?(abs(b)))
--  IMPLIES
--  conv_series?(series_prod(a, b)) AND
--   inf_sum(series_prod(a, b)) = inf_sum(a) * inf_sum(b)
theorem merten : (conv_series_p a ∧ conv_series_p b) ∧
 (conv_series_p |a| ∨ conv_series_p |b|) → conv_series_p (series_prod a b) ∧
 inf_sum (series_prod a b) = inf_sum a * inf_sum b := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.merten