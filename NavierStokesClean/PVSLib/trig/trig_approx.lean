-- Auto-generated by pvs2lean/translator.py
-- PVS theory: trig_approx
-- PVSLib library: trig
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: trig_approx

Auto-generated stubs from PVSLib theory `trig_approx`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.trig_approx

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (below : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} (cos : pvs_τ3 → Prop)
variable {pvs_τ12 : Type*} (even_p : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (factorial : pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} (odd_p : pvs_τ15 → Prop)
variable {pvs_τ17 : Type*} (pi_lbn : pvs_τ17 → Prop)
variable {pvs_τ20 : Type*} (pi_ubn : pvs_τ20 → Prop)
variable {pvs_τ21 : Type*} (sin : pvs_τ21 → Prop)
variable {pvs_τ31 : Type*} (strict_decreasing_p : pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} (strict_increasing_p : pvs_τ32 → Prop)
variable {pvs_τ33 : Type*} (tends_to_0_p : pvs_τ33 → Prop)

-- pvs2lean: trig_approx.sin_term
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (-1) ^ n * a ^ (2 * n + 1) / factorial(2 * n + 1)
axiom sin_term {pvs_τ26 : Type*} {pvs_τ27 : Type*} : pvs_τ26 → pvs_τ27 → Prop

-- pvs2lean: trig_approx.sin_term_next
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_term(a)(n + 1) =
--  sin_term(a)(n) * (-1) * a * a / ((2 * n + 3) * (2 * n + 2))
theorem sin_term_next : sin_term a(n + 1) = sin_term a(n) * (-1) * a * a / ((2 * n + 3) * (2 * n + 2)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_term_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_term(-a)(n) = -sin_term(a)(n)
theorem sin_term_neg : sin_term -a(n) = -sin_term a(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_terms_alternate
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 /= a IMPLIES (sin_term(a)(n + 1) < 0 IFF sin_term(a)(n) > 0)
theorem sin_terms_alternate : 0 ≠ a → (sin_term a(n + 1) < 0 ↔ sin_term a(n) > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_term_nonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_term(a)(n) /= 0 IFF a /= 0
theorem sin_term_nonzero : sin_term a(n) ≠ 0 ↔ a ≠ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_term_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_term(a)(n) = 0 IFF a = 0
theorem sin_term_zero : sin_term a(n) = 0 ↔ a = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_term_gt0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_term(a)(n) > 0 IFF ((even?(n) AND a > 0) OR (odd?(n) AND a < 0))
theorem sin_term_gt0 : sin_term a(n) > 0 ↔ ((even_p n ∧ a > 0) ∨ (odd_p n ∧ a < 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_term_lt0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_term(a)(n) < 0 IFF ((even?(n) AND a < 0) OR (odd?(n) AND a > 0))
theorem sin_term_lt0 : sin_term a(n) < 0 ↔ ((even_p n ∧ a < 0) ∨ (odd_p n ∧ a > 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_tends_to_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: tends_to_0?(sin_term(a))
theorem sin_tends_to_0 : tends_to_0_p (sin_term a) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_term
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN 1 ELSE (-1) ^ n * a ^ (2 * n) / factorial(2 * n) ENDIF
axiom cos_term {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9 → Prop

-- pvs2lean: trig_approx.cos_term_next
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_term(a)(n + 1) =
--  cos_term(a)(n) * (-1) * a * a / ((2 * n + 2) * (2 * n + 1))
theorem cos_term_next : cos_term a(n + 1) = cos_term a(n) * (-1) * a * a / ((2 * n + 2) * (2 * n + 1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_term_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_term(-a)(n) = cos_term(a)(n)
theorem cos_term_neg : cos_term -a(n) = cos_term a(n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_terms_alternate
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 /= a IMPLIES (cos_term(a)(n + 1) < 0 IFF cos_term(a)(n) > 0)
theorem cos_terms_alternate : 0 ≠ a → (cos_term a(n + 1) < 0 ↔ cos_term a(n) > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_term_nonzero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_term(a)(n) /= 0 IFF (n = 0 OR a /= 0)
theorem cos_term_nonzero : cos_term a(n) ≠ 0 ↔ (n = 0 ∨ a ≠ 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_term_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_term(a)(n) = 0 IFF (n /= 0 AND a = 0)
theorem cos_term_zero : cos_term a(n) = 0 ↔ (n ≠ 0 ∧ a = 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_term_gt0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_term(a)(n) > 0 IFF (even?(n) AND (a /= 0 OR n = 0))
theorem cos_term_gt0 : cos_term a(n) > 0 ↔ (even_p n ∧ (a ≠ 0 ∨ n = 0)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_term_lt0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_term(a)(n) < 0 IFF (odd?(n) AND a /= 0)
theorem cos_term_lt0 : cos_term a(n) < 0 ↔ (odd_p n ∧ a ≠ 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_tends_to_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: tends_to_0?(cos_term(n0x))
theorem cos_tends_to_0 : tends_to_0_p (cos_term n0x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_term_deriv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[real](LAMBDA a: sin_term(a)(n)) AND
--  deriv[real](LAMBDA a: sin_term(a)(n)) = (LAMBDA a: cos_term(a)(n))
theorem sin_term_deriv : derivable_p (fun a=>  sin_term a(n)) ∧ deriv (fun a=>  sin_term a(n)) = (fun a=>  cos_term a(n)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_term_deriv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[real](LAMBDA a: cos_term(a)(n)) AND
--  deriv[real](LAMBDA a: cos_term(a)(n)) =
--   (LAMBDA a: IF n = 0 THEN 0 ELSE -sin_term(a)(n - 1) ENDIF)
theorem cos_term_deriv : derivable_p (fun a=>  cos_term a(n)) ∧ deriv (fun a=>  cos_term a(n)) =
 (fun a=>  if n = 0 then 0 else -sin_term a(n - 1) ) := by
  -- === TCCs for cos_term_deriv ===
  -- TCC: cos_term_deriv_TCC1
  -- Statement: FORALL (n: nat):
  --   derivable?[real](LAMBDA (a: real): cos_term(a)(n)) IMPLIES
  --    NOT n = 0 IMPLIES n - 1 >= 0
  have cos_term_deriv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_approx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, n, sin_term(a))
axiom sin_approx {pvs_τ22 : Type*} {pvs_τ23 : Type*} : pvs_τ22 → pvs_τ23 → Prop

-- pvs2lean: trig_approx.sin_approx_a0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_approx(0, n) = 0
theorem sin_approx_a0 : sin_approx 0 n = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_approx_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_approx(-a, n) = -sin_approx(a, n)
theorem sin_approx_neg : sin_approx -a n = -sin_approx a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_approx_next
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_approx(a, n + 2) - sin_approx(a, n) =
--  sin_term(a)(n + 1) * (1 - a * a / ((2 * n + 5) * (2 * n + 4)))
theorem sin_approx_next : sin_approx a (n + 2) - sin_approx a n = sin_term a(n + 1) * (1 - a * a / ((2 * n + 5) * (2 * n + 4))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_approx_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: a * a = (2 * n + 5) * (2 * n + 4) IMPLIES
--  sin_approx(a, n + 2) = sin_approx(a, n)
theorem sin_approx_eq : a * a = (2 * n + 5) * (2 * n + 4) → sin_approx a (n + 2) = sin_approx a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_approx_sin
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(sin(a) - sin_approx(a, n)) <= abs(sin_term(a)(n + 1))
theorem sin_approx_sin : |sin a - sin_approx a n| ≤ |sin_term a(n + 1)| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_approx
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, n, cos_term(a))
axiom cos_approx {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ4 → pvs_τ5 → Prop

-- pvs2lean: trig_approx.cos_approx_a0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_approx(0, n) = 1
theorem cos_approx_a0 : cos_approx 0 n = 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_approx_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_approx(-a, n) = cos_approx(a, n)
theorem cos_approx_neg : cos_approx -a n = cos_approx a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_approx_cos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(cos(a) - cos_approx(a, n)) <= abs(cos_term(a)(n + 1))
theorem cos_approx_cos : |cos a - cos_approx a n| ≤ |cos_term a(n + 1)| := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_approx_next
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_approx(a, n + 2) - cos_approx(a, n) =
--  cos_term(a)(n + 1) * (1 - a * a / ((2 * n + 4) * (2 * n + 3)))
theorem cos_approx_next : cos_approx a (n + 2) - cos_approx a n = cos_term a(n + 1) * (1 - a * a / ((2 * n + 4) * (2 * n + 3))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_approx_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: a * a = (2 * n + 4) * (2 * n + 3) IMPLIES
--  cos_approx(a, n + 2) = cos_approx(a, n)
theorem cos_approx_eq : a * a = (2 * n + 4) * (2 * n + 3) → cos_approx a (n + 2) = cos_approx a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_approx_deriv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[real](LAMBDA a: sin_approx(a, n)) AND
--  deriv[real](LAMBDA a: sin_approx(a, n)) = (LAMBDA a: cos_approx(a, n))
theorem sin_approx_deriv : derivable_p (fun a=>  sin_approx a n) ∧ deriv (fun a=>  sin_approx a n) = (fun a=>  cos_approx a n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_approx_deriv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[real](LAMBDA a: cos_approx(a, n)) AND
--  deriv[real](LAMBDA a: cos_approx(a, n)) =
--   (LAMBDA a: IF n = 0 THEN 0 ELSE -sin_approx(a, n - 1) ENDIF)
theorem cos_approx_deriv : derivable_p (fun a=>  cos_approx a n) ∧ deriv (fun a=>  cos_approx a n) =
 (fun a=>  if n = 0 then 0 else -sin_approx a (n - 1) ) := by
  -- === TCCs for cos_approx_deriv ===
  -- TCC: cos_approx_deriv_TCC1
  -- Statement: FORALL (n: nat):
  --   derivable?[real](LAMBDA (a: real): cos_approx(a, n)) IMPLIES
  --    NOT n = 0 IMPLIES n - 1 >= 0
  have cos_approx_deriv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_ub
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF a < 0 THEN sin_approx(a, 2 * n + 1) ELSE sin_approx(a, 2 * n) ENDIF
axiom sin_ub {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} : pvs_τ28 → pvs_τ29 → pvs_τ30

-- pvs2lean: trig_approx.sin_lb
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF a < 0 THEN sin_approx(a, 2 * n) ELSE sin_approx(a, 2 * n + 1) ENDIF
axiom sin_lb {pvs_τ24 : Type*} {pvs_τ25 : Type*} : pvs_τ24 → pvs_τ25 → Prop

-- pvs2lean: trig_approx.sin_lb_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_lb(-a, n) = -sin_ub(a, n)
theorem sin_lb_neg : sin_lb -a n = -sin_ub a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_ub_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_ub(-a, n) = -sin_lb(a, n)
theorem sin_ub_neg : sin_ub -a n = -sin_lb a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_a0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_lb(0, n) = 0
theorem sin_lb_a0 : sin_lb 0 n = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_ub_a0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_ub(0, n) = 0
theorem sin_ub_a0 : sin_ub 0 n = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_ub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_lb(n0x, n) < sin_ub(n0x, n)
theorem sin_lb_ub : sin_lb n0x n < sin_ub n0x n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_inc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px * px < (4 * n + 7) * (4 * n + 6) => sin_lb(px, n) < sin_lb(px, n + pm)
theorem sin_lb_inc : px * px < (4 * n + 7) * (4 * n + 6) → sin_lb px n < sin_lb px (n + pm) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_ub_dec
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px * px < (4 * n + 5) * (4 * n + 4) => sin_ub(px, n + pm) < sin_ub(px, n)
theorem sin_ub_dec : px * px < (4 * n + 5) * (4 * n + 4) → sin_ub px (n + pm) < sin_ub px n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px * px < (4 * n + 7) * (4 * n + 6) => sin_lb(px, n) < sin(px)
theorem sin_lb_lt : px * px < (4 * n + 7) * (4 * n + 6) → sin_lb px n < sin px := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_ub_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px * px < (4 * n + 5) * (4 * n + 4) => sin(px) < sin_ub(px, n)
theorem sin_ub_lt : px * px < (4 * n + 5) * (4 * n + 4) → sin px < sin_ub px n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_gt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px * px > (4 * n + 7) * (4 * n + 6) => sin_lb(px, n) < -1
theorem sin_lb_gt : px * px > (4 * n + 7) * (4 * n + 6) → sin_lb px n < -1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_ub_gt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px * px > (4 * n + 5) * (4 * n + 4) => sin_ub(px, n) > 1
theorem sin_ub_gt : px * px > (4 * n + 5) * (4 * n + 4) → sin_ub px n > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_bounds
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_lb(a, n) <= sin(a) AND sin(a) <= sin_ub(a, n)
theorem sin_bounds : sin_lb a n ≤ sin a ∧ sin a ≤ sin_ub a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_gt0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: px * px < 6 => sin_lb(px, n) > 0
theorem sin_lb_gt0 : px * px < 6 → sin_lb px n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_approx(a, 2 * n)
axiom cos_ub {pvs_τ10 : Type*} {pvs_τ11 : Type*} : pvs_τ10 → pvs_τ11 → Prop

-- pvs2lean: trig_approx.cos_lb
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_approx(a, 2 * n + 1)
axiom cos_lb {pvs_τ6 : Type*} {pvs_τ7 : Type*} : pvs_τ6 → pvs_τ7 → Prop

-- pvs2lean: trig_approx.cos_lb_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_lb(-a, n) = cos_lb(a, n)
theorem cos_lb_neg : cos_lb -a n = cos_lb a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_ub(-a, n) = cos_ub(a, n)
theorem cos_ub_neg : cos_ub -a n = cos_ub a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_a0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_lb(0, n) = 1
theorem cos_lb_a0 : cos_lb 0 n = 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_a0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_ub(0, n) = 1
theorem cos_ub_a0 : cos_ub 0 n = 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_ub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_lb(n0x, n) < cos_ub(n0x, n)
theorem cos_lb_ub : cos_lb n0x n < cos_ub n0x n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_inc
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n0x * n0x < (4 * n + 6) * (4 * n + 5) =>
--  cos_lb(n0x, n) < cos_lb(n0x, n + pm)
theorem cos_lb_inc : n0x * n0x < (4 * n + 6) * (4 * n + 5) → cos_lb n0x n < cos_lb n0x (n + pm) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_dec
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n0x * n0x < (4 * n + 4) * (4 * n + 3) =>
--  cos_ub(n0x, n + pm) < cos_ub(n0x, n)
theorem cos_ub_dec : n0x * n0x < (4 * n + 4) * (4 * n + 3) → cos_ub n0x (n + pm) < cos_ub n0x n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n0x * n0x < (4 * n + 6) * (4 * n + 5) => cos_lb(n0x, n) < cos(n0x)
theorem cos_lb_lt : n0x * n0x < (4 * n + 6) * (4 * n + 5) → cos_lb n0x n < cos n0x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_lt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n0x * n0x < (4 * n + 4) * (4 * n + 3) => cos(n0x) < cos_ub(n0x, n)
theorem cos_ub_lt : n0x * n0x < (4 * n + 4) * (4 * n + 3) → cos n0x < cos_ub n0x n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_gt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n0x * n0x > (4 * n + 6) * (4 * n + 5) => cos_lb(n0x, n) < -1
theorem cos_lb_gt : n0x * n0x > (4 * n + 6) * (4 * n + 5) → cos_lb n0x n < -1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_gt
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n0x * n0x > (4 * n + 4) * (4 * n + 3) & n >= 1 => cos_ub(n0x, n) > 1
theorem cos_ub_gt : n0x * n0x > (4 * n + 4) * (4 * n + 3) & n ≥ 1 → cos_ub n0x n > 1 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_bounds
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_lb(a, n) <= cos(a) AND cos(a) <= cos_ub(a, n)
theorem cos_bounds : cos_lb a n ≤ cos a ∧ cos a ≤ cos_ub a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_deriv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[nnreal](LAMBDA nnx: sin_lb(nnx, n)) AND
--  deriv[nnreal](LAMBDA nnx: sin_lb(nnx, n)) = (LAMBDA nnx: cos_lb(nnx, n))
theorem sin_lb_deriv : derivable_p (fun nnx=>  sin_lb nnx n) ∧ deriv (fun nnx=>  sin_lb nnx n) = (fun nnx=>  cos_lb nnx n) := by
  -- === TCCs for sin_lb_deriv ===
  -- TCC: sin_lb_deriv_TCC1
  -- Statement: deriv_domain?[nnreal]
  have sin_lb_deriv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sin_lb_deriv_TCC2
  -- Statement: not_one_element?[nnreal]
  have sin_lb_deriv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_ub_deriv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[nnreal](LAMBDA nnx: sin_ub(nnx, n)) AND
--  deriv[nnreal](LAMBDA nnx: sin_ub(nnx, n)) = (LAMBDA nnx: cos_ub(nnx, n))
theorem sin_ub_deriv : derivable_p (fun nnx=>  sin_ub nnx n) ∧ deriv (fun nnx=>  sin_ub nnx n) = (fun nnx=>  cos_ub nnx n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_deriv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[real](LAMBDA a: cos_lb(a, n)) AND
--  deriv[real](LAMBDA a: cos_lb(a, n)) =
--   (LAMBDA a: IF a < 0 THEN -sin_lb(a, n) ELSE -sin_ub(a, n) ENDIF)
theorem cos_lb_deriv : derivable_p (fun a=>  cos_lb a n) ∧ deriv (fun a=>  cos_lb a n) =
 (fun a=>  if a < 0 then -sin_lb a n else -sin_ub a n ) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_deriv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?[real](LAMBDA a: cos_ub(a, n)) AND
--  deriv[real](LAMBDA a: cos_ub(a, n)) =
--   (LAMBDA a:
--      IF n = 0 THEN 0
--      ELSIF a < 0 THEN -sin_ub(a, n - 1)
--      ELSE -sin_lb(a, n - 1)
--      ENDIF)
theorem cos_ub_deriv : derivable_p (fun a=>  cos_ub a n) ∧ deriv (fun a=>  cos_ub a n) =
 (fun a=> 
    if n = 0 then 0
    ELSIF a < 0 then -sin_ub a (n - 1)
    else -sin_lb a (n - 1)
    ) := by
  -- === TCCs for cos_ub_deriv ===
  -- TCC: cos_ub_deriv_TCC1
  -- Statement: FORALL (n: nat):
  --   derivable?[real](LAMBDA (a: real): cos_ub(a, n)) IMPLIES
  --    FORALL (a: real): NOT n = 0 AND a < 0 IMPLIES n - 1 >= 0
  have cos_ub_deriv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_ub_deriv_TCC2
  -- Statement: FORALL (n: nat):
  --   derivable?[real](LAMBDA (a: real): cos_ub(a, n)) IMPLIES
  --    FORALL (a: real): NOT n = 0 AND NOT a < 0 IMPLIES n - 1 >= 0
  have cos_ub_deriv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_nn_strict_decreasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_decreasing?(LAMBDA (x: {x: nnreal | x <= pi}): cos_lb(x, n))
theorem cos_lb_nn_strict_decreasing : strict_decreasing_p (fun (x : {x : {x : ℝ // x ≥ 0} // x ≤ pi}) => cos_lb x n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_np_strict_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_increasing?(LAMBDA (x: {x: npreal | x >= -pi}): cos_lb(x, n))
theorem cos_lb_np_strict_increasing : strict_increasing_p (fun (x : {x : {x : ℝ // x ≤ 0} // x ≥ -pi}) => cos_lb x n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_px
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 / 500 > px => sin(px) > px * (1 - 1 / 1500000)
theorem sin_px : 1 / 500 > px → sin px > px * (1 - 1 / 1500000) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_approx.Cos_pos_n
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 5
noncomputable def Cos_pos_n : sorry := by
  sorry  -- pvs2lean: definition stub for 5

-- pvs2lean: trig_approx.cos_term_pi_lb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 5 IMPLIES cos_term(pi_lbn(n) / 2)(2 * n + 2) < cos(pi_lbn(n) / 2)
theorem cos_term_pi_lb : n ≥ 5 → cos_term (pi_lbn n / 2) (2 * n + 2) < cos (pi_lbn n / 2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_pi2_eps
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: px <= pi / 2 AND abs(cos_term(pi / 2 - px)(2 * n + 2)) < sin(px) =>
--  cos_lb(pi / 2 - px, n) > 0
theorem cos_lb_pi2_eps : px ≤ pi / 2 ∧ |cos_term (pi / 2 - px) (2 * n + 2)| < sin px → cos_lb (pi / 2 - px) n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_pi2_pos0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: n = 0 => NOT cos_lb(pi_lbn(n) / 2, n) > 0
theorem cos_lb_pi2_pos0 : n = 0 → ¬ cos_lb (pi_lbn n / 2) n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_pi2_pos1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: n = 1 => NOT cos_lb(pi_lbn(n) / 2, n) > 0
theorem cos_lb_pi2_pos1 : n = 1 → ¬ cos_lb (pi_lbn n / 2) n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_pi2_pos2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: n = 2 => NOT cos_lb(pi_lbn(n) / 2, n) > 0
theorem cos_lb_pi2_pos2 : n = 2 → ¬ cos_lb (pi_lbn n / 2) n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_lb_pi2_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 5 IMPLIES cos_lb(pi_lbn(n) / 2, n) > 0
theorem cos_lb_pi2_pos : n ≥ 5 → cos_lb (pi_lbn n / 2) n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.Sin_pos_n
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 11
noncomputable def Sin_pos_n : sorry := by
  sorry  -- pvs2lean: definition stub for 11

-- pvs2lean: trig_approx.sin_term_pi_lb
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 11 IMPLIES sin_term(pi_lbn(n + 1))(2 * n + 2) < sin(pi_lbn(n + 1))
theorem sin_term_pi_lb : n ≥ 11 → sin_term (pi_lbn (n + 1)) (2 * n + 2) < sin (pi_lbn (n + 1)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_pi_not_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (b9: below(11)): NOT sin_lb(pi_lbn(b9 + 1), b9) > 0
theorem sin_lb_pi_not_pos : ∀ (b9 : {s : {i : ℝ // i ≥ 0} // s < 11}), ¬(sin_lb (pi_lbn (b9 + 1)) b9 > 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_pi_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 11 IMPLIES sin_lb(pi_lbn(n + 1), n) > 0
theorem sin_lb_pi_pos : n ≥ 11 → sin_lb (pi_lbn (n + 1)) n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.pi_lb_est
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n < 12 THEN pi_lbn(n) - 2 / (n ^ 3 + 1) ELSE pi_lbn(n) ENDIF
axiom pi_lb_est {pvs_τ16 : Type*} : pvs_τ16 → Prop

-- pvs2lean: trig_approx.pi_lb_est_le
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pi_lb_est(n) <= pi_lbn(n) AND pi_lb_est(n) <= pi
theorem pi_lb_est_le : pi_lb_est n ≤ pi_lbn n ∧ pi_lb_est n ≤ pi := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_pi_est_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_lb(pi_lb_est(n + 1), n) > 0
theorem sin_lb_pi_est_pos : sin_lb (pi_lb_est (n + 1)) n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_pi_range_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: px <= pi_lb_est(n + 1) IMPLIES sin_lb(px, n) > 0
theorem sin_lb_pi_range_pos : px ≤ pi_lb_est (n + 1) → sin_lb px n > 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_nn_strict_decreasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 1 IMPLIES
--  strict_decreasing?(LAMBDA (x: {x: nnreal | x <= pi_lb_est(n)}):
--                       cos_ub(x, n))
theorem cos_ub_nn_strict_decreasing : n ≥ 1 → strict_decreasing_p (fun (x : {x : NNReal // x ≤ pi_lb_est n})=> cos_ub x n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_np_strict_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 1 IMPLIES
--  strict_increasing?(LAMBDA (x: {x: npreal | x >= -pi_lb_est(n)}):
--                       cos_ub(x, n))
theorem cos_ub_np_strict_increasing : n ≥ 1 → strict_increasing_p (fun (x : {x : npreal // x ≥ -pi_lb_est n})=> cos_ub x n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_increasing?(LAMBDA (x:
--                              {x: real |
--                                 -pi_lb_est(n) / 2 <= x AND
--                                  x <= pi_lb_est(n) / 2}):
--              
theorem sin_lb_increasing : strict_increasing_p (fun (x : {x : ℝ // -pi_lb_est n / 2 ≤ x ∧ x ≤ pi_lb_est n / 2}) => sin_lb x n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_ub_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_increasing?(LAMBDA (x:
--                              {x: real |
--                                 -pi_lb_est(n) / 2 <= x AND
--                                  x <= pi_lb_est(n) / 2}):
--              
theorem sin_ub_increasing : strict_increasing_p (fun (x : {x : ℝ // -pi_lb_est n / 2 ≤ x ∧ x ≤ pi_lb_est n / 2}) => sin_ub x n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.sin_lb_nn_decreasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_decreasing?(LAMBDA (x: {x: real | pi / 2 <= x AND x <= pi}):
--                      sin_lb(x, n))
theorem sin_lb_nn_decreasing : strict_decreasing_p (fun (x : {x : ℝ // pi / 2 ≤ x ∧ x ≤ pi}) => sin_lb x n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_term_pi_ub
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 5 IMPLIES -cos_term(pi_ubn(n) / 2)(2 * n + 1) < -cos(pi_ubn(n) / 2)
theorem cos_term_pi_ub : n ≥ 5 → -cos_term (pi_ubn n / 2) (2 * n + 1) < -cos (pi_ubn n / 2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.cos_ub_pi2_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 5 IMPLIES cos_ub(pi_ubn(n) / 2, n) < 0
theorem cos_ub_pi2_neg : n ≥ 5 → cos_ub (pi_ubn n / 2) n < 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_approx.pi_ub_est
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n < 5 THEN pi_ubn(n) + 1 / (n + 1) ^ 4 ELSE pi_ubn(n) ENDIF
axiom pi_ub_est {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ18 → pvs_τ19

-- pvs2lean: trig_approx.sin_ub_nn_decreasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: n >= 1 IMPLIES
--  strict_decreasing?(LAMBDA (x:
--                               {x: real |
--                                  pi_ub_est(n) / 2 <= x AND
--                                   x <= pi_lb_est(n)}):
theorem sin_ub_nn_decreasing : n ≥ 1 → strict_decreasing_p (fun (x : {x: real |
                                pi_ub_est n / 2 ≤ x ∧
                                 x ≤ pi_lb_est n})=> sin_ub x n) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.trig_approx