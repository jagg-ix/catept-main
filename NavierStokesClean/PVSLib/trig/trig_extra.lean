-- Auto-generated by pvs2lean/translator.py
-- PVS theory: trig_extra
-- PVSLib library: trig
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: trig_extra

Auto-generated stubs from PVSLib theory `trig_extra`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.trig_extra

noncomputable section

variable {pvs_τ1 : Type*} (Tan_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} (atan2 : pvs_τ2 → pvs_τ3 → pvs_τ4)
variable {pvs_τ5 : Type*} (cos : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (expt : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (sin : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} (sq : pvs_τ10 → Prop)
variable {pvs_τ11 : Type*} (tan : pvs_τ11 → Prop)

-- pvs2lean: trig_extra.sin_minus_2pi
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a - 2 * pi) = sin(a)
theorem sin_minus_2pi : sin (a - 2 * pi) = sin a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_minus_2pi
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a - 2 * pi) = cos(a)
theorem cos_minus_2pi : cos (a - 2 * pi) = cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_minus_2pi
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) /= 0 IMPLIES tan(a - 2 * pi) = tan(a)
theorem tan_minus_2pi : cos a ≠ 0 → tan (a - 2 * pi) = tan a := by
  -- === TCCs for tan_minus_2pi ===
  -- TCC: tan_minus_2pi_TCC1
  -- Statement: FORALL (a: real): cos(a) /= 0 IMPLIES Tan?(a - 2 * pi)
  have tan_minus_2pi_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_minus_2pi_TCC2
  -- Statement: FORALL (a: real): cos(a) /= 0 IMPLIES Tan?(a)
  have tan_minus_2pi_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_minus_3pi2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a - 3 * pi / 2) = cos(a)
theorem sin_minus_3pi2 : sin (a - 3 * pi / 2) = cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_minus_3pi2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a - 3 * pi / 2) = -sin(a)
theorem cos_minus_3pi2 : cos (a - 3 * pi / 2) = -sin a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_minus_3pi2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) /= 0 AND cos(a) /= 0 IMPLIES tan(a - 3 * pi / 2) = -1 / tan(a)
theorem tan_minus_3pi2 : sin a ≠ 0 ∧ cos a ≠ 0 → tan (a - 3 * pi / 2) = -1 / tan a := by
  -- === TCCs for tan_minus_3pi2 ===
  -- TCC: tan_minus_3pi2_TCC1
  -- Statement: FORALL (a: real): cos(a) /= 0 AND sin(a) /= 0 IMPLIES Tan?(a - 3 * pi / 2)
  have tan_minus_3pi2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_minus_3pi2_TCC2
  -- Statement: FORALL (a: real): cos(a) /= 0 AND sin(a) /= 0 IMPLIES tan(a) /= 0
  have tan_minus_3pi2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_minus_pi
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a - pi) = -sin(a)
theorem sin_minus_pi : sin (a - pi) = -sin a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_minus_pi
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a - pi) = -cos(a)
theorem cos_minus_pi : cos (a - pi) = -cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_minus_pi
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) /= 0 IMPLIES tan(a - pi) = tan(a)
theorem tan_minus_pi : cos a ≠ 0 → tan (a - pi) = tan a := by
  -- === TCCs for tan_minus_pi ===
  -- TCC: tan_minus_pi_TCC1
  -- Statement: FORALL (a: real): cos(a) /= 0 IMPLIES Tan?(a - pi)
  have tan_minus_pi_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_minus_pi2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a - pi / 2) = -cos(a)
theorem sin_minus_pi2 : sin (a - pi / 2) = -cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_minus_pi2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a - pi / 2) = sin(a)
theorem cos_minus_pi2 : cos (a - pi / 2) = sin a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_minus_pi2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) /= 0 AND cos(a) /= 0 IMPLIES tan(a - pi / 2) = -1 / tan(a)
theorem tan_minus_pi2 : sin a ≠ 0 ∧ cos a ≠ 0 → tan (a - pi / 2) = -1 / tan a := by
  -- === TCCs for tan_minus_pi2 ===
  -- TCC: tan_minus_pi2_TCC1
  -- Statement: FORALL (a: real): cos(a) /= 0 AND sin(a) /= 0 IMPLIES Tan?(a - pi / 2)
  have tan_minus_pi2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_2pi_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(2 * pi - a) = -sin(a)
theorem sin_2pi_minus : sin (2 * pi - a) = -sin a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_2pi_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(2 * pi - a) = cos(a)
theorem cos_2pi_minus : cos (2 * pi - a) = cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_2pi_minus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) /= 0 AND cos(a) /= 0 IMPLIES tan(2 * pi - a) = -tan(a)
theorem tan_2pi_minus : sin a ≠ 0 ∧ cos a ≠ 0 → tan (2 * pi - a) = -tan a := by
  -- === TCCs for tan_2pi_minus ===
  -- TCC: tan_2pi_minus_TCC1
  -- Statement: FORALL (a: real): cos(a) /= 0 AND sin(a) /= 0 IMPLIES Tan?(2 * pi - a)
  have tan_2pi_minus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_3pi2_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(3 * pi / 2 - a) = -cos(a)
theorem sin_3pi2_minus : sin (3 * pi / 2 - a) = -cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_3pi2_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(3 * pi / 2 - a) = -sin(a)
theorem cos_3pi2_minus : cos (3 * pi / 2 - a) = -sin a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_3pi2_minus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) /= 0 AND cos(a) /= 0 IMPLIES tan(3 * pi / 2 - a) = 1 / tan(a)
theorem tan_3pi2_minus : sin a ≠ 0 ∧ cos a ≠ 0 → tan (3 * pi / 2 - a) = 1 / tan a := by
  -- === TCCs for tan_3pi2_minus ===
  -- TCC: tan_3pi2_minus_TCC1
  -- Statement: FORALL (a: real): cos(a) /= 0 AND sin(a) /= 0 IMPLIES Tan?(3 * pi / 2 - a)
  have tan_3pi2_minus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_pi_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(pi - a) = sin(a)
theorem sin_pi_minus : sin (pi - a) = sin a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_pi_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(pi - a) = -cos(a)
theorem cos_pi_minus : cos (pi - a) = -cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_pi_minus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) /= 0 IMPLIES tan(pi - a) = -tan(a)
theorem tan_pi_minus : cos a ≠ 0 → tan (pi - a) = -tan a := by
  -- === TCCs for tan_pi_minus ===
  -- TCC: tan_pi_minus_TCC1
  -- Statement: FORALL (a: real): cos(a) /= 0 IMPLIES Tan?(pi - a)
  have tan_pi_minus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_pi2_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(pi / 2 - a) = cos(a)
theorem sin_pi2_minus : sin (pi / 2 - a) = cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_pi2_minus
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(pi / 2 - a) = sin(a)
theorem cos_pi2_minus : cos (pi / 2 - a) = sin a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_pi2_minus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) /= 0 AND cos(a) /= 0 IMPLIES tan(pi / 2 - a) = 1 / tan(a)
theorem tan_pi2_minus : sin a ≠ 0 ∧ cos a ≠ 0 → tan (pi / 2 - a) = 1 / tan a := by
  -- === TCCs for tan_pi2_minus ===
  -- TCC: tan_pi2_minus_TCC1
  -- Statement: FORALL (a: real): cos(a) /= 0 AND sin(a) /= 0 IMPLIES Tan?(pi / 2 - a)
  have tan_pi2_minus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_times_cos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) * cos(b) = (sin(a + b) + sin(a - b)) / 2
theorem sin_times_cos : sin a * cos b = (sin (a + b) + sin (a - b)) / 2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_times_cos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) * cos(b) = (cos(a + b) + cos(a - b)) / 2
theorem cos_times_cos : cos a * cos b = (cos (a + b) + cos (a - b)) / 2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_times_sin
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) * sin(b) = (cos(a - b) - cos(a + b)) / 2
theorem sin_times_sin : sin a * sin b = (cos (a - b) - cos (a + b)) / 2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_sum
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) + sin(b) = 2 * sin((a + b) / 2) * cos((a - b) / 2)
theorem sin_sum : sin a + sin b = 2 * sin ((a + b) / 2) * cos ((a - b) / 2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) - sin(b) = 2 * cos((a + b) / 2) * sin((a - b) / 2)
theorem sin_diff : sin a - sin b = 2 * cos ((a + b) / 2) * sin ((a - b) / 2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_sum
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) + cos(b) = 2 * cos((a + b) / 2) * cos((a - b) / 2)
theorem cos_sum : cos a + cos b = 2 * cos ((a + b) / 2) * cos ((a - b) / 2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) - cos(b) = -2 * sin((a + b) / 2) * sin((a - b) / 2)
theorem cos_diff : cos a - cos b = -2 * sin ((a + b) / 2) * sin ((a - b) / 2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_diff
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Tan?(a) AND Tan?(b) IMPLIES tan(a) - tan(b) = sin(a - b) / (cos(a) * cos(b))
theorem tan_diff : Tan_p a ∧ Tan_p b → tan a - tan b = sin (a - b) / (cos a * cos b) := by
  -- === TCCs for tan_diff ===
  -- TCC: tan_diff_TCC1
  -- Statement: FORALL (a, b: real): Tan?(b) AND Tan?(a) IMPLIES (cos(a) * cos(b)) /= 0
  have tan_diff_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_extra.sq_sin
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sq(sin(a)) = (1 - cos(2 * a)) / 2
theorem sq_sin : (sin a^2) = (1 - cos (2 * a)) / 2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sq_cos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sq(cos(a)) = (1 + cos(2 * a)) / 2
theorem sq_cos : (cos a^2) = (1 + cos (2 * a)) / 2 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_half_zeroes
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a / 2) = 0 IFF 1 - cos(a) = 0
theorem sin_half_zeroes : sin (a / 2) = 0 ↔ 1 - cos a = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_half_zeroes
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a / 2) = 0 IFF 1 + cos(a) = 0
theorem cos_half_zeroes : cos (a / 2) = 0 ↔ 1 + cos a = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_half_zeroes2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a / 2) = 0 IMPLIES sin(a) = 0
theorem cos_half_zeroes2 : cos (a / 2) = 0 → sin a = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sq_tan
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (1 + cos(2 * a)) /= 0 IMPLIES
--  sq(tan(a)) = (1 - cos(2 * a)) / (1 + cos(2 * a))
theorem sq_tan : (1 + cos (2 * a)) ≠ 0 → (tan a^2) = (1 - cos (2 * a)) / (1 + cos (2 * a)) := by
  -- === TCCs for sq_tan ===
  -- TCC: sq_tan_TCC1
  -- Statement: FORALL (a: real): (1 + cos(2 * a)) /= 0 IMPLIES Tan?(a)
  have sq_tan_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_half
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (0 <= a / 2 AND a / 2 <= pi IMPLIES sin(a / 2) = sqrt((1 - cos(a)) / 2))
--  AND
--  (pi <= a / 2 AND a / 2 <= 2 * pi IMPLIES
--    sin(a / 2) = -sqrt((1 - cos(a)) / 2))
theorem sin_half : (0 ≤ a / 2 ∧ a / 2 ≤ pi → sin (a / 2) = Real.sqrt ((1 - cos a) / 2)) ∧ (pi ≤ a / 2 ∧ a / 2 ≤ 2 * pi →
  sin (a / 2) = -Real.sqrt ((1 - cos a) / 2)) := by
  -- === TCCs for sin_half ===
  -- TCC: sin_half_TCC1
  -- Statement: FORALL (a: real): a / 2 <= pi AND 0 <= a / 2 IMPLIES (1 - cos(a)) / 2 >= 0
  have sin_half_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sin_half_TCC2
  -- Statement: FORALL (a: real):
  --   a / 2 <= 2 * pi AND
  --    pi <= a / 2 AND
  --     (0 <= a / 2 AND a / 2 <= pi IMPLIES
  --       sin(a / 2) = sqrt((1 - cos(a)) / 2))
  --    IMPLIES (1 - cos(a)) / 2 >= 0
  have sin_half_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_half
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (0 <= a / 2 AND a / 2 <= pi / 2 IMPLIES
--   cos(a / 2) = sqrt((1 + cos(a)) / 2))
--  AND
--  (pi / 2 <= a / 2 AND a / 2 <= 3 * pi / 2 IMPLIES
--    cos(a / 2) = -sqrt((1 + cos(a)) / 2))
--   AND
--   (3 * pi / 2 <= a 
theorem cos_half : (0 ≤ a / 2 ∧ a / 2 ≤ pi / 2 → cos (a / 2) = Real.sqrt ((1 + cos a) / 2)) ∧ (pi / 2 ≤ a / 2 ∧ a / 2 ≤ 3 * pi / 2 →
  cos (a / 2) = -Real.sqrt ((1 + cos a) / 2))
 ∧
 (3 * pi / 2 ≤ a / 2 ∧ a / 2 ≤ 2 * pi →
   cos (a / 2) = Real.sqrt ((1 + cos a) / 2)) := by
  -- === TCCs for cos_half ===
  -- TCC: cos_half_TCC1
  -- Statement: FORALL (a: real):
  --   a / 2 <= pi / 2 AND 0 <= a / 2 IMPLIES (1 + cos(a)) / 2 >= 0
  have cos_half_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_half_TCC2
  -- Statement: FORALL (a: real):
  --   a / 2 <= 3 * pi / 2 AND
  --    pi / 2 <= a / 2 AND
  --     (0 <= a / 2 AND a / 2 <= pi / 2 IMPLIES
  --       cos(a / 2) = sqrt((1 + cos(a)) / 2))
  --    IMPLIES (1 + cos(a)) / 2 >= 0
  have cos_half_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos_half_TCC3
  -- Statement: FORALL (a: real):
  --   a / 2 <= 2 * pi AND
  --    3 * pi / 2 <= a / 2 AND
  --     (pi / 2 <= a / 2 AND a / 2 <= 3 * pi / 2 IMPLIES
  --       cos(a / 2) = -sqrt((1 + cos(a)) / 2))
  --      AND
  --      (0 <= a / 2 AND a /...
  have cos_half_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_half
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) /= -1 IMPLIES tan(a / 2) = sin(a) / (1 + cos(a))
theorem tan_half : cos a ≠ -1 → tan (a / 2) = sin a / (1 + cos a) := by
  -- === TCCs for tan_half ===
  -- TCC: tan_half_TCC1
  -- Statement: FORALL (a: real): cos(a) /= -1 IMPLIES Tan?(a / 2)
  have tan_half_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_half_TCC2
  -- Statement: FORALL (a: real): cos(a) /= -1 IMPLIES (1 + cos(a)) /= 0
  have tan_half_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_half2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) /= 0 IMPLIES tan(a / 2) = (1 - cos(a)) / sin(a)
theorem tan_half2 : sin a ≠ 0 → tan (a / 2) = (1 - cos a) / sin a := by
  -- === TCCs for tan_half2 ===
  -- TCC: tan_half2_TCC1
  -- Statement: FORALL (a: real): sin(a) /= 0 IMPLIES Tan?(a / 2)
  have tan_half2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_3a
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(3 * a) = 3 * sin(a) - 4 * expt(sin(a), 3)
theorem sin_3a : sin (3 * a) = 3 * sin a - 4 * expt (sin a) 3 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_3a
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(3 * a) = 4 * expt(cos(a), 3) - 3 * cos(a)
theorem cos_3a : cos (3 * a) = 4 * expt (cos a) 3 - 3 * cos a := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_eq_sin
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) = sin(b) <=>
--  (EXISTS (k: int): (a = b + 2 * k * pi) OR (a = pi - b + 2 * k * pi))
theorem sin_eq_sin : sin a = sin b <=> (∃ (k : ℤ), (a = b + 2 * k * pi) ∨ (a = pi - b + 2 * k * pi)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_eq_cos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) = cos(b) <=>
--  (EXISTS (k: int): (a = b + 2 * k * pi) OR (a = -b + 2 * k * pi))
theorem cos_eq_cos : cos a = cos b <=> (∃ (k : ℤ), (a = b + 2 * k * pi) ∨ (a = -b + 2 * k * pi)) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_eq_tan
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Tan?(a) AND Tan?(b) IMPLIES
--  (tan(a) = tan(b) <=> (EXISTS (k: int): (a = b + k * pi)))
theorem tan_eq_tan : Tan_p a ∧ Tan_p b → (tan a = tan b ≤> (∃ (k : ℤ), (a = b + k * pi))) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_extra.sin_cos_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: {x: nnreal | x < 2 * pi}):
--   sin(a) = sin(b) AND cos(a) = cos(b) IMPLIES a = b
theorem sin_cos_eq : ∀ (a : {x : {x : ℝ // x ≥ 0} // x < 2 * pi}) (b : {x : {x : ℝ // x ≥ 0} // x < 2 * pi}), sin a = sin b ∧ cos a = cos b → a = b := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_extra.sin_eq_cos
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) = cos(a) IMPLIES (EXISTS (i: int): a = pi / 4 + i * pi)
theorem sin_eq_cos : sin a = cos a → (∃ (i : ℤ), a = pi / 4 + i * pi) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.tan_eq_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Tan?(a) AND tan(a) = 1 IMPLIES (EXISTS (i: int): a = pi / 4 + i * pi)
theorem tan_eq_1 : Tan_p a ∧ tan a = 1 → (∃ (i : ℤ), a = pi / 4 + i * pi) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_extra.sin_eq_pm1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(a) = (-1) ^ i IFF (EXISTS j: a = (i + 2 * j) * pi + pi / 2)
theorem sin_eq_pm1 : sin a = (-1) ^ i ↔ (∃ j,  a = (i + 2 * j) * pi + pi / 2) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.cos_eq_pm1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(a) = (-1) ^ i IFF (EXISTS j: a = (i + 2 * j) * pi)
theorem cos_eq_pm1 : cos a = (-1) ^ i ↔ (∃ j,  a = (i + 2 * j) * pi) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.sin_plus_cos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sq(C) = sq(A) + sq(B) AND sin(del) = B / C AND cos(del) = A / C IMPLIES
--  A * sin(x) + B * cos(x) = C * sin(x + del)
theorem sin_plus_cos : (C)^2 = (A)^2 + (B)^2 ∧ sin del = B / C ∧ cos del = A / C → A * sin x + B * cos x = C * sin (x + del) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: trig_extra.spc_tan_prep
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: A /= 0 AND Tan?(del) AND tan(del) = B / A AND sq(C) = sq(A) + sq(B) IMPLIES
--  sq(sin(del)) = sq(B / C) AND sq(cos(del)) = sq(A / C)
theorem spc_tan_prep : A ≠ 0 ∧ Tan_p del ∧ tan del = B / A ∧ (C)^2 = (A)^2 + (B)^2 → (sin del^2) = (B / C)^2 ∧ (cos del^2) = (A / C)^2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: trig_extra.sin_plus_cos_atan2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= 0 OR y /= 0 IMPLIES
--  x * sin(alpha) + y * cos(alpha) =
--   sqrt(sq(x) + sq(y)) * sin(alpha + atan2(x, y))
theorem sin_plus_cos_atan2 : x ≠ 0 ∨ y ≠ 0 → x * sin alpha + y * cos alpha =
 Real.sqrt ((x)^2 + (y)^2) * sin (alpha + atan2 x y) := by
  -- === TCCs for sin_plus_cos_atan2 ===
  -- TCC: sin_plus_cos_atan2_TCC1
  -- Statement: FORALL (x, y: real): x /= 0 OR y /= 0 IMPLIES x = 0 => y /= 0
  have sin_plus_cos_atan2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.trig_extra