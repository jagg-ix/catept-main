-- Auto-generated by pvs2lean/translator.py
-- PVS theory: atan_integral
-- PVSLib library: trig
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: atan_integral

Auto-generated stubs from PVSLib theory `atan_integral`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.atan_integral

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (Integrable_p : pvs_τ1 → pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} {pvs_τ7 : Type*} (Integral : pvs_τ4 → pvs_τ5 → pvs_τ6 → pvs_τ7)
variable {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} (between : pvs_τ30 → pvs_τ31 → pvs_τ32)
variable {pvs_τ33 : Type*} (bijective_p : pvs_τ33 → Prop)
variable {pvs_τ34 : Type*} {pvs_τ35 : Type*} (const_fun : pvs_τ34 → pvs_τ35)
variable {pvs_τ36 : Type*} (continuous_p : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} (deriv : pvs_τ37 → Prop)
variable {pvs_τ38 : Type*} {pvs_τ39 : Type*} (derivable_n_times_p : pvs_τ38 → pvs_τ39 → Prop)
variable {pvs_τ40 : Type*} (derivable_p : pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} (even_p : pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (factorial : pvs_τ42 → pvs_τ43)
variable {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} (harmonic_poly_real : pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47)
variable {pvs_τ48 : Type*} (integer_pred : pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} (nderiv : pvs_τ49 → pvs_τ50 → pvs_τ51 → Prop)
variable {pvs_τ52 : Type*} (odd_p : pvs_τ52 → Prop)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} (polynomial : pvs_τ53 → pvs_τ54 → pvs_τ55)
variable {pvs_τ56 : Type*} (strict_increasing_p : pvs_τ56 → Prop)

-- pvs2lean: atan_integral.atan_deriv_fn
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 1 / (1 + x * x)
noncomputable def atan_deriv_fn : sorry := by
  sorry  -- pvs2lean: definition stub for 1 / (1 + x * x)

-- pvs2lean: atan_integral.one_over_one_plus_t_sq_cont
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: continuous?[real](atan_deriv_fn)
theorem one_over_one_plus_t_sq_cont : continuous_p atan_deriv_fn := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_value
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: Integral[real](0, x, atan_deriv_fn)
axiom atan_value {pvs_τ29 : Type*} : pvs_τ29 → Prop

-- pvs2lean: atan_integral.atan_value_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan_value(0) = 0
theorem atan_value_0 : atan_value 0 = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_neg_value
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan_value(-x) = -atan_value(x)
theorem atan_neg_value : atan_value -x = -atan_value x := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_inv_value
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan_value(1 / px) = 2 * atan_value(1) - atan_value(px)
theorem atan_inv_value : atan_value (1 / px) = 2 * atan_value 1 - atan_value px := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_inv_neg_value
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan_value(1 / nx) = -2 * atan_value(1) - atan_value(nx)
theorem atan_inv_neg_value : atan_value (1 / nx) = -2 * atan_value 1 - atan_value nx := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_value_strict_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_increasing?(atan_value)
theorem atan_value_strict_increasing : strict_increasing_p atan_value := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_value_minus_x1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: -1 / py < x IMPLIES
--  atan_value(x) - atan_value(py) = atan_value((x - py) / (1 + x * py))
theorem atan_value_minus_x1 : -1 / py < x → atan_value x - atan_value py = atan_value ((x - py) / (1 + x * py)) := by
  -- === TCCs for atan_value_minus_x1 ===
  -- TCC: atan_value_minus_x1_TCC1
  -- Statement: FORALL (py: posreal, x: real): -1 / py < x IMPLIES (1 + x * py) /= 0
  have atan_value_minus_x1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_value_minus_x2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: x < -1 / py IMPLIES
--  4 * atan_value(1) + atan_value(x) - atan_value(py) =
--   atan_value((x - py) / (1 + x * py))
theorem atan_value_minus_x2 : x < -1 / py → 4 * atan_value 1 + atan_value x - atan_value py =
 atan_value ((x - py) / (1 + x * py)) := by
  -- === TCCs for atan_value_minus_x2 ===
  -- TCC: atan_value_minus_x2_TCC1
  -- Statement: FORALL (py: posreal, x: real): x < -1 / py IMPLIES (1 + x * py) /= 0
  have atan_value_minus_x2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_value_minus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (-1 < x * y =>
--   atan_value(x) - atan_value(y) = atan_value((x - y) / (1 + x * y)))
--  AND
--  (x * y < -1 & y > 0 =>
--    atan_value(x) - atan_value(y) + 4 * atan_value(1) =
--     atan_value((x - y) / (1 + x 
theorem atan_value_minus : (-1 < x * y →
  atan_value x - atan_value y = atan_value ((x - y) / (1 + x * y))) ∧ (x * y < -1 & y > 0 →
  atan_value x - atan_value y + 4 * atan_value 1 =
   atan_value ((x - y) / (1 + x * y)))
 ∧
 (x * y < -1 & y < 0 →
   atan_value x - atan_value y - 4 * atan_value 1 =
    atan_value ((x - y) / (1 + x * y))) := by
  -- === TCCs for atan_value_minus ===
  -- TCC: atan_value_minus_TCC1
  -- Statement: FORALL (x, y: real): -1 < x * y IMPLIES (1 + x * y) /= 0
  have atan_value_minus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_value_minus_TCC2
  -- Statement: FORALL (x, y: real):
  --   y > 0 AND
  --    x * y < -1 AND
  --     (-1 < x * y =>
  --       atan_value(x) - atan_value(y) = atan_value((x - y) / (1 + x * y)))
  --    IMPLIES (1 + x * y) /= 0
  have atan_value_minus_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_value_minus_TCC3
  -- Statement: FORALL (x, y: real):
  --   y < 0 AND
  --    x * y < -1 AND
  --     (x * y < -1 & y > 0 =>
  --       atan_value(x) - atan_value(y) + 4 * atan_value(1) =
  --        atan_value((x - y) / (1 + x * y)))
  --      AND
  --      (-1 <...
  have atan_value_minus_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.pi
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 4 * atan_value(1)
noncomputable def pi : sorry := by
  sorry  -- pvs2lean: definition stub for 4 * atan_value(1)

-- pvs2lean: atan_integral.atan
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan_value(x)
axiom atan {pvs_τ10 : Type*} : pvs_τ10 → Prop

-- pvs2lean: atan_integral.pi_value
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: pi = 4 * atan(1)
theorem pi_value : pi = 4 * atan 1 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_strict_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: strict_increasing?(atan)
theorem atan_strict_increasing : strict_increasing_p atan := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_bij
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: bijective?[real, real_abs_lt_pi2](atan)
theorem atan_bij : bijective_p atan := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(0) = 0
theorem atan_0 : atan 0 = 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_inv
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(1 / px) = pi / 2 - atan(px)
theorem atan_inv : atan (1 / px) = pi / 2 - atan px := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_inv_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(1 / nx) = -pi / 2 - atan(nx)
theorem atan_inv_neg : atan (1 / nx) = -pi / 2 - atan nx := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_def
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(x) = Integral(0, x, (LAMBDA (x: real): 1 / (1 + x * x)))
theorem atan_def : atan x = Integral 0 x ((fun (x : ℝ)=> 1 / (1 + x * x))) := by
  -- === TCCs for atan_def ===
  -- TCC: atan_def_TCC1
  -- Statement: FORALL (x: real_abs_lt_pi2): Tan?(x)
  have atan_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_def_TCC1
  -- Statement: FORALL (x1: real):
  --   Integrable?[real](0, x1, (LAMBDA (x: real): 1 / (1 + x * x)))
  have atan_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.acot
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(1 / nzx)
axiom acot {pvs_τ8 : Type*} {pvs_τ9 : Type*} : pvs_τ8 → pvs_τ9

-- pvs2lean: atan_integral.atan_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(-x) = -atan(x)
theorem atan_neg : atan -x = -atan x := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.acot_neg
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: acot(-nzx) = -acot(nzx)
theorem acot_neg : acot -nzx = -acot nzx := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_minus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (-1 < x * y => atan(x) - atan(y) = atan((x - y) / (1 + x * y))) AND
--  (x * y < -1 & y > 0 =>
--    atan(x) - atan(y) + pi = atan((x - y) / (1 + x * y)))
--   AND
--   (x * y < -1 & y < 0 =>
--     atan(x) - atan(y
theorem atan_minus : (-1 < x * y → atan x - atan y = atan ((x - y) / (1 + x * y))) ∧ (x * y < -1 & y > 0 →
  atan x - atan y + pi = atan ((x - y) / (1 + x * y)))
 ∧
 (x * y < -1 & y < 0 →
   atan x - atan y - pi = atan ((x - y) / (1 + x * y))) := by
  -- === TCCs for atan_minus ===
  -- TCC: atan_minus_TCC1
  -- Statement: FORALL (x, y: real):
  --   y > 0 AND
  --    x * y < -1 AND
  --     (-1 < x * y => atan(x) - atan(y) = atan((x - y) / (1 + x * y)))
  --    IMPLIES (1 + x * y) /= 0
  have atan_minus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_minus_TCC2
  -- Statement: FORALL (x, y: real):
  --   y < 0 AND
  --    x * y < -1 AND
  --     (x * y < -1 & y > 0 =>
  --       atan(x) - atan(y) + pi = atan((x - y) / (1 + x * y)))
  --      AND (-1 < x * y => atan(x) - atan(y) = atan((x - y) / ...
  have atan_minus_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_plus
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x * y < 1 => atan(x) + atan(y) = atan((x + y) / (1 - x * y))) AND
--  (1 < x * y & y > 0 =>
--    atan(x) + atan(y) - pi = atan((x + y) / (1 - x * y)))
--   AND
--   (1 < x * y & y < 0 =>
--     atan(x) + atan(y) +
theorem atan_plus : (x * y < 1 → atan x + atan y = atan ((x + y) / (1 - x * y))) ∧ (1 < x * y & y > 0 → atan x + atan y - pi = atan ((x + y) / (1 - x * y)))
 ∧
 (1 < x * y & y < 0 → atan x + atan y + pi = atan ((x + y) / (1 - x * y))) := by
  -- === TCCs for atan_plus ===
  -- TCC: atan_plus_TCC1
  -- Statement: FORALL (x, y: real): x * y < 1 IMPLIES (1 - x * y) /= 0
  have atan_plus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_plus_TCC2
  -- Statement: FORALL (x, y: real):
  --   y > 0 AND
  --    1 < x * y AND
  --     (x * y < 1 => atan(x) + atan(y) = atan((x + y) / (1 - x * y)))
  --    IMPLIES (1 - x * y) /= 0
  have atan_plus_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_plus_TCC3
  -- Statement: FORALL (x, y: real):
  --   y < 0 AND
  --    1 < x * y AND
  --     (1 < x * y & y > 0 =>
  --       atan(x) + atan(y) - pi = atan((x + y) / (1 - x * y)))
  --      AND (x * y < 1 => atan(x) + atan(y) = atan((x + y) / (1 ...
  have atan_plus_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_plus_TCC1
  -- Statement: FORALL (x, y: real): x * y < 1 IMPLIES (1 - x * y) /= 0
  have atan_plus_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_plus_TCC2
  -- Statement: FORALL (x, y: real):
  --   y > 0 AND
  --    1 < x * y AND
  --     (x * y < 1 => atan(x) + atan(y) = atan((x + y) / (1 - x * y)))
  --    IMPLIES (1 - x * y) /= 0
  have atan_plus_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_plus_TCC3
  -- Statement: FORALL (x, y: real):
  --   y < 0 AND
  --    1 < x * y AND
  --     (1 < x * y & y > 0 =>
  --       atan(x) + atan(y) - pi = atan((x + y) / (1 - x * y)))
  --      AND (x * y < 1 => atan(x) + atan(y) = atan((x + y) / (1 ...
  have atan_plus_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_sub_swap
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x /= 0 AND y /= 0 IMPLIES
--  atan(x / y) - atan(y / x) = atan((x ^ 2 - y ^ 2) / (2 * x * y))
theorem atan_sub_swap : x ≠ 0 ∧ y ≠ 0 → atan (x / y) - atan (y / x) = atan ((x ^ 2 - y ^ 2) / (2 * x * y)) := by
  -- === TCCs for atan_sub_swap ===
  -- TCC: atan_sub_swap_TCC1
  -- Statement: FORALL (x, y: real): y /= 0 AND x /= 0 IMPLIES x /= 0 OR 2 >= 0
  have atan_sub_swap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_sub_swap_TCC2
  -- Statement: FORALL (x, y: real): y /= 0 AND x /= 0 IMPLIES y /= 0 OR 2 >= 0
  have atan_sub_swap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_sub_swap_TCC3
  -- Statement: FORALL (x, y: real): y /= 0 AND x /= 0 IMPLIES (2 * x * y) /= 0
  have atan_sub_swap_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_sub_swap_TCC1
  -- Statement: FORALL (x, y: real): y /= 0 AND x /= 0 IMPLIES x /= 0 OR 2 >= 0
  have atan_sub_swap_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_sub_swap_TCC2
  -- Statement: FORALL (x, y: real): y /= 0 AND x /= 0 IMPLIES y /= 0 OR 2 >= 0
  have atan_sub_swap_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_sub_swap_TCC3
  -- Statement: FORALL (x, y: real): y /= 0 AND x /= 0 IMPLIES (2 * x * y) /= 0
  have atan_sub_swap_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.deriv_atan_fun
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?(atan) AND deriv(atan) = (LAMBDA (x: real): 1 / (1 + x * x))
theorem deriv_atan_fun : derivable_p atan ∧ deriv atan = (fun (x : ℝ)=> 1 / (1 + x * x)) := by
  -- === TCCs for deriv_atan_fun ===
  -- TCC: deriv_atan_fun_TCC1
  -- Statement: derivable?(atan) IMPLIES FORALL (x: real): (1 + x * x) /= 0
  have deriv_atan_fun_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.continuous_atan
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: continuous?[real](atan)
theorem continuous_atan : continuous_p atan := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan(1) = 4 * atan(1 / 5) - atan(1 / 239)
theorem atan_1 : atan 1 = 4 * atan (1 / 5) - atan (1 / 239) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_bnds
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: px / (1 + px * px) < atan(px) AND atan(px) < px
theorem atan_bnds : px / (1 + px * px) < atan px ∧ atan px < px := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.pi_bnds
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 306 / 100 < pi AND pi < 319 / 100
theorem pi_bnds : 306 / 100 < pi ∧ pi < 319 / 100 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atanS
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: harmonic_poly_real(2 * n + 1, 1, x)
axiom atanS {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} : pvs_τ20 → pvs_τ21 → pvs_τ22

-- pvs2lean: atan_integral.atanD
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (1 + x * x) ^ (2 * n + 1)
axiom atanD {pvs_τ11 : Type*} {pvs_τ12 : Type*} {pvs_τ13 : Type*} : pvs_τ11 → pvs_τ12 → pvs_τ13

-- pvs2lean: atan_integral.atanF
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i > 2 * n OR odd?(i) THEN 0
-- ELSE (-1) ^ (i / 2 + n) * factorial(2 * n) * C(2 * n + 1, i)
-- ENDIF
axiom atanF {pvs_τ14 : Type*} {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ14 → pvs_τ15 → pvs_τ16

-- pvs2lean: atan_integral.atanN
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: polynomial(atanF(n), 2 * n)
axiom atanN {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} : pvs_τ17 → pvs_τ18 → pvs_τ19

-- pvs2lean: atan_integral.atan_taylors_prep1
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(atanS(n)(x)) <= (1 + x * x) ^ n * sqrt(1 + x * x)
theorem atan_taylors_prep1 : |atanS n(x)| ≤ (1 + x * x) ^ n * Real.sqrt (1 + x * x) := by
  -- === TCCs for atan_taylors_prep1 ===
  -- TCC: atan_taylors_prep1_TCC1
  -- Statement: FORALL (n: nat, x: real): (1 + x * x) /= 0 OR n >= 0
  have atan_taylors_prep1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep1_TCC2
  -- Statement: FORALL (x: real): 1 + x * x >= 0
  have atan_taylors_prep1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep1_TCC1
  -- Statement: FORALL (x: real): 1 + x * x >= 0
  have atan_taylors_prep1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_taylors_prep2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (-1) ^ n * factorial(2 * n) * atanS(n) = atanN(n)
theorem atan_taylors_prep2 : (-1) ^ n * factorial (2 * n) * atanS n = atanN n := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_taylors_prep3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable_n_times?(atanN(n), m) AND derivable_n_times?(atanD(n), m)
theorem atan_taylors_prep3 : derivable_n_times_p (atanN n) m ∧ derivable_n_times_p (atanD n) m := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atanN_derivable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?(atanN(n))
theorem atanN_derivable : derivable_p (atanN n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atanD_derivable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable?(atanD(n))
theorem atanD_derivable : derivable_p (atanD n) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_taylors_prep4
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv(atanN(n)) =
--  IF n = 0 THEN const_fun(0)
--  ELSE polynomial(LAMBDA (i: nat): (i + 1) * atanF(n)(i + 1), 2 * n - 1)
--  ENDIF
theorem atan_taylors_prep4 : deriv (atanN n) = if n = 0 then const_fun 0
else polynomial (fun (i : ℕ)=> (i + 1) * atanF n(i + 1)) (2 * n - 1) := by
  -- === TCCs for atan_taylors_prep4 ===
  -- TCC: atan_taylors_prep4_TCC1
  -- Statement: FORALL (n: nat): derivable?[real](atanN(n))
  have atan_taylors_prep4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep4_TCC2
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES 2 * n - 1 >= 0
  have atan_taylors_prep4_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep4_TCC1
  -- Statement: FORALL (n: nat): derivable?[real](atanN(n))
  have atan_taylors_prep4_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep4_TCC2
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES 2 * n - 1 >= 0
  have atan_taylors_prep4_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_taylors_prep5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv(atanD(n)) =
--  (LAMBDA (x: real): (4 * n + 2) * x * (1 + x * x) ^ (2 * n))
theorem atan_taylors_prep5 : deriv (atanD n) = (fun (x : ℝ)=> (4 * n + 2) * x * (1 + x * x) ^ (2 * n)) := by
  -- === TCCs for atan_taylors_prep5 ===
  -- TCC: atan_taylors_prep5_TCC1
  -- Statement: FORALL (n: nat): derivable?[real](atanD(n))
  have atan_taylors_prep5_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep5_TCC2
  -- Statement: FORALL (n: nat, x: real): (1 + x * x) /= 0 OR (2 * n) >= 0
  have atan_taylors_prep5_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep5_TCC1
  -- Statement: FORALL (n: nat): derivable?[real](atanD(n))
  have atan_taylors_prep5_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_taylors_prep6
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv(deriv(atanN(n))) =
--  IF n = 0 THEN const_fun(0)
--  ELSE polynomial(LAMBDA (i: nat): (i + 2) * (i + 1) * atanF(n)(i + 2),
--                  2 * n - 2)
--  ENDIF
theorem atan_taylors_prep6 : deriv (deriv (atanN n)) = if n = 0 then const_fun 0
else polynomial (fun (i : ℕ)=> (i + 2) * (i + 1) * atanF n(i + 2)) (2 * n - 2) := by
  -- === TCCs for atan_taylors_prep6 ===
  -- TCC: atan_taylors_prep6_TCC1
  -- Statement: FORALL (n: nat): derivable?[real](deriv[real](atanN(n)))
  have atan_taylors_prep6_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep6_TCC2
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES 2 * n - 2 >= 0
  have atan_taylors_prep6_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep6_TCC1
  -- Statement: FORALL (n: nat): derivable?[real](deriv[real](atanN(n)))
  have atan_taylors_prep6_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep6_TCC2
  -- Statement: FORALL (n: nat): NOT n = 0 IMPLIES 2 * n - 2 >= 0
  have atan_taylors_prep6_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_taylors_prep7
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (f: [real -> real], g: [real -> nzreal]):
--   derivable?(f) AND derivable?(g) IMPLIES
--    derivable?(f / g ^ pn) AND
--     deriv(f / g ^ pn) = (deriv(f) * g - pn * f * deriv(g)) / (g ^ (pn + 1))
theorem atan_taylors_prep7 : ∀ (f : ℝ → ℝ) (g : ℝ → {r : ℝ // r ≠ 0}), derivable_p f ∧ derivable_p g → derivable_p (f / g ^ pn) ∧
 deriv (f / g ^ pn) = (deriv f * g - pn * f * deriv g) / (g ^ (pn + 1)) := by
  -- === TCCs for atan_taylors_prep7 ===
  -- TCC: atan_taylors_prep7_TCC1
  -- Statement: FORALL (pn: posnat, f: [real -> real], g: [real -> nzreal]):
  --   derivable?(f) AND derivable?(g) IMPLIES
  --    FORALL (x1: real): (^[real](g, pn))(x1) /= 0
  have atan_taylors_prep7_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep7_TCC2
  -- Statement: FORALL (pn: posnat, f: [real -> real], g: [real -> nzreal]):
  --   derivable?(f) AND derivable?(g) AND derivable?(f / g ^ pn) IMPLIES
  --    FORALL (x1: real): (^[real](g, (pn + 1)))(x1) /= 0
  have atan_taylors_prep7_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep7_TCC1
  -- Statement: FORALL (pn: posnat, f: [real -> real], g: [real -> nzreal]):
  --   derivable?(f) AND derivable?(g) IMPLIES
  --    FORALL (x1: real): (^[real](g, pn))(x1) /= 0
  have atan_taylors_prep7_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep7_TCC2
  -- Statement: FORALL (pn: posnat, f: [real -> real], g: [real -> nzreal]):
  --   derivable?(f) AND derivable?(g) AND derivable?(f / g ^ pn) IMPLIES
  --    FORALL (x1: real): (^[real](g, (pn + 1)))(x1) /= 0
  have atan_taylors_prep7_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_taylors_prep8
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: deriv(deriv(atanN(n) / atanD(n))) = atanN(n + 1) / atanD(n + 1)
theorem atan_taylors_prep8 : deriv (deriv (atanN n / atanD n)) = atanN (n + 1) / atanD (n + 1) := by
  -- === TCCs for atan_taylors_prep8 ===
  -- TCC: atan_taylors_prep8_TCC1
  -- Statement: FORALL (n: nat, x1: real): atanD(n)(x1) /= 0
  have atan_taylors_prep8_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep8_TCC2
  -- Statement: FORALL (n: nat): derivable?[real](/[real](atanN(n), atanD(n)))
  have atan_taylors_prep8_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep8_TCC3
  -- Statement: FORALL (n: nat): derivable?[real](deriv[real](/[real](atanN(n), atanD(n))))
  have atan_taylors_prep8_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep8_TCC4
  -- Statement: FORALL (n: nat, x1: real): atanD(n + 1)(x1) /= 0
  have atan_taylors_prep8_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep8_TCC1
  -- Statement: FORALL (n: nat, x1: real): atanD(n)(x1) /= 0
  have atan_taylors_prep8_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep8_TCC2
  -- Statement: FORALL (n: nat): derivable?[real](/[real](atanN(n), atanD(n)))
  have atan_taylors_prep8_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep8_TCC3
  -- Statement: FORALL (n: nat): derivable?[real](deriv[real](/[real](atanN(n), atanD(n))))
  have atan_taylors_prep8_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_prep8_TCC4
  -- Statement: FORALL (n: nat, x1: real): atanD(n + 1)(x1) /= 0
  have atan_taylors_prep8_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_series_prep4
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable_n_times?(atanN(n) / atanD(n), 2 * m)
theorem atan_series_prep4 : derivable_n_times_p (atanN n / atanD n) (2 * m) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_series_prep5
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nderiv(2 * m, atanN(n) / atanD(n)) = atanN(n + m) / atanD(n + m)
theorem atan_series_prep5 : nderiv (2 * m) (atanN n / atanD n) = atanN (n + m) / atanD (n + m) := by
  -- === TCCs for atan_series_prep5 ===
  -- TCC: atan_series_prep5_TCC1
  -- Statement: FORALL (m, n: nat):
  --   derivable_n_times?[real](/[real](atanN(n), atanD(n)), 2 * m)
  have atan_series_prep5_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_series_prep5_TCC2
  -- Statement: FORALL (m, n: nat, x1: real): atanD(n + m)(x1) /= 0
  have atan_series_prep5_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_series_prep5_TCC1
  -- Statement: FORALL (m, n: nat):
  --   derivable_n_times?[real](/[real](atanN(n), atanD(n)), 2 * m)
  have atan_series_prep5_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_series_prep5_TCC2
  -- Statement: FORALL (m, n: nat, x1: real): atanD(n + m)(x1) /= 0
  have atan_series_prep5_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_series_prep6
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable_n_times?(LAMBDA (x: real): 1 / (1 + x * x), 2 * n) AND
--  nderiv(2 * n, LAMBDA (x: real): 1 / (1 + x * x)) = atanN(n) / atanD(n)
theorem atan_series_prep6 : derivable_n_times_p (fun (x : ℝ)=> 1 / (1 + x * x)) (2 * n) ∧ nderiv (2 * n) (fun (x : ℝ)=> 1 / (1 + x * x)) = atanN n / atanD n := by
  -- === TCCs for atan_series_prep6 ===
  -- TCC: atan_series_prep6_TCC1
  -- Statement: FORALL (x: real): (1 + x * x) /= 0
  have atan_series_prep6_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_nderiv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nderiv(n, atan) =
--  IF n = 0 THEN atan
--  ELSIF even?(n)
--    THEN (deriv(atanN(n / 2 - 1)) * (LAMBDA (x: real): (1 + x * x)) -
--           atanN(n / 2 - 1) * (LAMBDA (x: real): (2 * n - 2) * x))
--          / 
theorem atan_nderiv : nderiv n atan = if n = 0 then atan
ELSIF even_p n
  then (deriv (atanN (n / 2 - 1)) * (fun (x : ℝ)=> (1 + x * x)) -
         atanN (n / 2 - 1) * (fun (x : ℝ)=> (2 * n - 2) * x))
        / (atanD (n / 2 - 1) * (fun (x : ℝ)=> (1 + x * x)))
else atanN ((n - 1) / 2) / atanD ((n - 1) / 2) := by
  -- === TCCs for atan_nderiv ===
  -- TCC: atan_nderiv_TCC1
  -- Statement: FORALL (n: nat): derivable_n_times?[real](atan, n)
  have atan_nderiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC2
  -- Statement: FORALL (n: nat):
  --   even?(n) AND NOT n = 0 IMPLIES integer_pred(n / 2 - 1) AND n / 2 - 1 >= 0
  have atan_nderiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC3
  -- Statement: FORALL (n: nat):
  --   even?(n) AND NOT n = 0 IMPLIES derivable?[real](atanN(n / 2 - 1))
  have atan_nderiv_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC4
  -- Statement: FORALL (n: nat):
  --   even?(n) AND NOT n = 0 IMPLIES
  --    FORALL (x1: real):
  --      (*[real](atanD(n / 2 - 1), (LAMBDA (x: real): (1 + x * x))))(x1) /= 0
  have atan_nderiv_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC5
  -- Statement: FORALL (n: nat):
  --   NOT even?(n) AND NOT n = 0 IMPLIES
  --    integer_pred((n - 1) / 2) AND (n - 1) / 2 >= 0
  have atan_nderiv_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC6
  -- Statement: FORALL (n: nat):
  --   NOT even?(n) AND NOT n = 0 IMPLIES
  --    FORALL (x1: real): atanD((n - 1) / 2)(x1) /= 0
  have atan_nderiv_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC1
  -- Statement: FORALL (n: nat): derivable_n_times?[real](atan, n)
  have atan_nderiv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC2
  -- Statement: FORALL (n: nat):
  --   even?(n) AND NOT n = 0 IMPLIES integer_pred(n / 2 - 1) AND n / 2 - 1 >= 0
  have atan_nderiv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC3
  -- Statement: FORALL (n: nat):
  --   even?(n) AND NOT n = 0 IMPLIES derivable?[real](atanN(n / 2 - 1))
  have atan_nderiv_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC4
  -- Statement: FORALL (n: nat):
  --   even?(n) AND NOT n = 0 IMPLIES
  --    FORALL (x1: real):
  --      (*[real](atanD(n / 2 - 1), (LAMBDA (x: real): (1 + x * x))))(x1) /= 0
  have atan_nderiv_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC5
  -- Statement: FORALL (n: nat):
  --   NOT even?(n) AND NOT n = 0 IMPLIES
  --    integer_pred((n - 1) / 2) AND (n - 1) / 2 >= 0
  have atan_nderiv_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_TCC6
  -- Statement: FORALL (n: nat):
  --   NOT even?(n) AND NOT n = 0 IMPLIES
  --    FORALL (x1: real): atanD((n - 1) / 2)(x1) /= 0
  have atan_nderiv_tcc6 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_nderiv_0
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: nderiv(n, atan)(0) =
--  IF even?(n) THEN 0 ELSE (-1) ^ ((n - 1) / 2) * factorial(n - 1) ENDIF
theorem atan_nderiv_0 : nderiv n atan(0) = if even_p n then 0 else (-1) ^ ((n - 1) / 2) * factorial (n - 1) := by
  -- === TCCs for atan_nderiv_0 ===
  -- TCC: atan_nderiv_0_TCC1
  -- Statement: FORALL (n: nat): NOT even?(n) IMPLIES integer_pred(((n - 1) / 2))
  have atan_nderiv_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_0_TCC2
  -- Statement: FORALL (n: nat): NOT even?(n) IMPLIES n - 1 >= 0
  have atan_nderiv_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_0_TCC1
  -- Statement: FORALL (n: nat): NOT even?(n) IMPLIES integer_pred(((n - 1) / 2))
  have atan_nderiv_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_nderiv_0_TCC2
  -- Statement: FORALL (n: nat): NOT even?(n) IMPLIES n - 1 >= 0
  have atan_nderiv_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_n_times_derivable
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: derivable_n_times?(atan, n)
theorem atan_n_times_derivable : derivable_n_times_p atan n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_series_prep7
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(atanN(n)(x)) <= factorial(2 * n) * (1 + x * x) ^ (2 * n + 1)
theorem atan_series_prep7 : |atanN n(x)| ≤ factorial (2 * n) * (1 + x * x) ^ (2 * n + 1) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_series_prep8
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(nderiv(2 * n + 1, atan)(x)) <= factorial(2 * n)
theorem atan_series_prep8 : |nderiv (2 * n + 1) atan(x)| ≤ factorial (2 * n) := by
  -- === TCCs for atan_series_prep8 ===
  -- TCC: atan_series_prep8_TCC1
  -- Statement: FORALL (n: nat): derivable_n_times?[real](atan, 1 + 2 * n)
  have atan_series_prep8_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_series_prep8_TCC1
  -- Statement: FORALL (n: nat): derivable_n_times?[real](atan, 1 + 2 * n)
  have atan_series_prep8_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan_integral.atan_series_coef
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: ((-1) ^ n) / (2 * n + 1)
axiom atan_series_coef {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ23 → pvs_τ24

-- pvs2lean: atan_integral.atan_series_term
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (LAMBDA (n: nat): x ^ (2 * n + 1) * atan_series_coef(n))
axiom atan_series_term {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ27 → pvs_τ28

-- pvs2lean: atan_integral.atan_series_n
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sigma(0, n, atan_series_term(x))
axiom atan_series_n {pvs_τ25 : Type*} {pvs_τ26 : Type*} : pvs_τ25 → pvs_τ26 → Prop

-- pvs2lean: atan_integral.atan_series_n_increasing
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x, y: nnreal):
--   x <= y AND y <= 1 IMPLIES atan_series_n(x, n) <= atan_series_n(y, n)
theorem atan_series_n_increasing : ∀ (x : {x : ℝ // x ≥ 0}) (y : {x : ℝ // x ≥ 0}), x ≤ y ∧ y ≤ 1 → atan_series_n x n ≤ atan_series_n y n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_series_n_a0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan_series_n(0, n) = 0
theorem atan_series_n_a0 : atan_series_n 0 n = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_series_eqv
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan_series_n(x, n) =
--  sigma(0, 2 * n + 2,
--        LAMBDA (nn: nat):
--          IF nn > 2 * n + 2 OR nn = 0 THEN 0
--          ELSE nderiv(nn, atan)(0) * x ^ nn / factorial(nn)
--          ENDIF)
theorem atan_series_eqv : atan_series_n x n = Finset.sum (Finset.Icc 0 2 * n + 2) (fun (nn : ℕ)=> if nn > 2 * n + 2 ∨ nn = 0 then 0
        else nderiv nn atan(0) * x ^ nn / factorial nn
        ) := by
  -- === TCCs for atan_series_eqv ===
  -- TCC: atan_series_eqv_TCC1
  -- Statement: FORALL (n, nn: nat):
  --   NOT nn > 2 * n + 2 AND NOT nn = 0 IMPLIES
  --    derivable_n_times?[real](atan, nn)
  have atan_series_eqv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_series_eqv_TCC2
  -- Statement: FORALL (n: nat, x: real, nn: nat):
  --   NOT nn > 2 * n + 2 AND NOT nn = 0 IMPLIES x /= 0 OR nn >= 0
  have atan_series_eqv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_series_eqv_TCC1
  -- Statement: FORALL (n, nn: nat):
  --   NOT nn > 2 * n + 2 AND NOT nn = 0 IMPLIES
  --    derivable_n_times?[real](atan, nn)
  have atan_series_eqv_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_series_eqv_TCC2
  -- Statement: FORALL (n: nat, x: real, nn: nat):
  --   NOT nn > 2 * n + 2 AND NOT nn = 0 IMPLIES x /= 0 OR nn >= 0
  have atan_series_eqv_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_taylors
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (EXISTS (c: between(0, x)):
--    atan(x) =
--     atan_series_n(x, n) +
--      nderiv(2 * n + 3, atan)(c) * x ^ (2 * n + 3) / factorial(2 * n + 3))
theorem atan_taylors : ∃ (c : {x_1 : ℝ // (0 < x → 0 < x_1 ∧ x_1 < x) ∧
 (x < 0 → x < x_1 ∧ x_1 < 0) ∧ (0 = x → x_1 = 0)}), atan x = atan_series_n x n +
 nderiv (2 * n + 3) atan(c) * x ^ (2 * n + 3) / factorial (2 * n + 3) := by
  -- === TCCs for atan_taylors ===
  -- TCC: atan_taylors_TCC1
  -- Statement: FORALL (n: nat, x: real, c: between[real](0, x)):
  --   derivable_n_times?[real](atan, 3 + 2 * n)
  have atan_taylors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_TCC2
  -- Statement: FORALL (n: nat, x: real, c: between[real](0, x)): x /= 0 OR (2 * n + 3) >= 0
  have atan_taylors_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_TCC1
  -- Statement: FORALL (n: nat, x: real, c: between[real](0, x)):
  --   derivable_n_times?[real](atan, 3 + 2 * n)
  have atan_taylors_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_taylors_TCC2
  -- Statement: FORALL (n: nat, x: real, c: between[real](0, x)): x /= 0 OR (2 * n + 3) >= 0
  have atan_taylors_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: atan_integral.atan_series
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: abs(atan(x) - atan_series_n(x, n)) <= abs(x ^ (2 * n + 3)) / (2 * n + 3)
theorem atan_series : |atan x - atan_series_n x n| ≤ |x ^ (2 * n + 3)| / (2 * n + 3) := by
  -- === TCCs for atan_series ===
  -- TCC: atan_series_TCC1
  -- Statement: FORALL (n: nat, x: real): x /= 0 OR (2 * n + 3) >= 0
  have atan_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan_series_TCC1
  -- Statement: FORALL (n: nat, x: real): x /= 0 OR (2 * n + 3) >= 0
  have atan_series_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.atan_integral