-- Auto-generated by pvs2lean/translator.py
-- PVS theory: pi_def
-- PVSLib library: trig
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: pi_def

Auto-generated stubs from PVSLib theory `pi_def`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.pi_def

noncomputable section

variable {pvs_τ1 : Type*} (conv_series_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} (convergent_p : pvs_τ2 → Prop)
variable {pvs_τ3 : Type*} (cos : pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (factorial : pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} (inf_sum : pvs_τ6 → Prop)
variable {pvs_τ7 : Type*} (limit : pvs_τ7 → Prop)
variable {pvs_τ8 : Type*} (nonempty_p : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (series : pvs_τ9 → pvs_τ10)
variable {pvs_τ11 : Type*} {pvs_τ12 : Type*} (sin : pvs_τ11 → pvs_τ12)
variable {pvs_τ13 : Type*} (singleton_p : pvs_τ13 → Prop)

-- pvs2lean: pi_def.pi_lb_new
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 31415926 / 10000000
noncomputable def pi_lb_new : sorry := by
  sorry  -- pvs2lean: definition stub for 31415926 / 10000000

-- pvs2lean: pi_def.pi_ub_new
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 31415927 / 10000000
noncomputable def pi_ub_new : sorry := by
  sorry  -- pvs2lean: definition stub for 31415927 / 10000000

-- pvs2lean: pi_def.inf_sum_odd_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real]):
--   (convergent?(series(a)) AND
--     (FORALL (k: nat): b(k) = a(2 * k) AND a(2 * k + 1) = 0))
--    IMPLIES convergent?(series(b)) AND inf_sum(a) = inf_sum(b)
theorem inf_sum_odd_0 : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ), (convergent_p (series a) ∧
  (∀ (k : ℕ), b k = a (2 * k) ∧ a (2 * k + 1) = 0)) → convergent_p (series b) ∧ inf_sum a = inf_sum b := by
  -- === TCCs for inf_sum_odd_0 ===
  -- TCC: inf_sum_odd_0_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(a)) AND
  --    (FORALL (k: nat): b(k) = a(2 * k) AND a(2 * k + 1) = 0) AND
  --     convergent?(series(b))
  --    IMPLIES conv_series?(a)
  have inf_sum_odd_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inf_sum_odd_0_TCC2
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(a)) AND
  --    (FORALL (k: nat): b(k) = a(2 * k) AND a(2 * k + 1) = 0) AND
  --     convergent?(series(b))
  --    IMPLIES conv_series?(b)
  have inf_sum_odd_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.inf_sum_even_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a, b: sequence[real]):
--   (convergent?(series(a)) AND
--     (FORALL (k: nat): b(k) = a(2 * k + 1) AND a(2 * k) = 0))
--    IMPLIES convergent?(series(b)) AND inf_sum(a) = inf_sum(b)
theorem inf_sum_even_0 : ∀ (a : {i : ℝ // i ≥ 0} → ℝ) (b : {i : ℝ // i ≥ 0} → ℝ), (convergent_p (series a) ∧
  (∀ (k : ℕ), b k = a (2 * k + 1) ∧ a (2 * k) = 0)) → convergent_p (series b) ∧ inf_sum a = inf_sum b := by
  -- === TCCs for inf_sum_even_0 ===
  -- TCC: inf_sum_even_0_TCC1
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(a)) AND
  --    (FORALL (k: nat): b(k) = a(2 * k + 1) AND a(2 * k) = 0) AND
  --     convergent?(series(b))
  --    IMPLIES conv_series?(a)
  have inf_sum_even_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: inf_sum_even_0_TCC2
  -- Statement: FORALL (a, b: sequence[real]):
  --   convergent?(series(a)) AND
  --    (FORALL (k: nat): b(k) = a(2 * k + 1) AND a(2 * k) = 0) AND
  --     convergent?(series(b))
  --    IMPLIES conv_series?(b)
  have inf_sum_even_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.cos1_gt_0_prep
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real):
--   LET S =
--         LAMBDA (n: nat):
--           x ^ (4 * n) / factorial(4 * n) -
--            x ^ (4 * n + 2) / factorial(4 * n + 2)
--     IN conv_series?(S) AND cos(x) = inf_sum(S)
theorem cos1_gt_0_prep : ∀ (x : ℝ), fun (S : {i : ℝ // i ≥ 0} → ℝ) => conv_series_p S ∧ cos x = inf_sum S (fun (n : {i : ℝ // i ≥ 0}) => x ^ (4 * n) / factorial (4 * n) - x ^ (4 * n + 2) / factorial (4 * n + 2)) := by
  -- === TCCs for cos1_gt_0_prep ===
  -- TCC: cos1_gt_0_prep_TCC1
  -- Statement: FORALL (x: real, n: nat): x /= 0 OR (4 * n) >= 0
  have cos1_gt_0_prep_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: cos1_gt_0_prep_TCC2
  -- Statement: FORALL (x: real, n: nat): x /= 0 OR (4 * n + 2) >= 0
  have cos1_gt_0_prep_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.series_first_aux
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: sequence[real]):
--   convergent?(series(a)) IMPLIES
--    limit(series(a)) = a(0) + limit(series(LAMBDA (n: nat): a(n + 1)))
theorem series_first_aux : ∀ (a : {i : ℝ // i ≥ 0} → ℝ), convergent_p (series a) → limit (series a) = a 0 + limit (series (fun (n : ℕ)=> a (n + 1))) := by
  -- === TCCs for series_first_aux ===
  -- TCC: series_first_aux_TCC1
  -- Statement: FORALL (a: sequence[real]):
  --   convergent?(series(a)) IMPLIES
  --    convergent?(series(LAMBDA (n: nat): a(n + 1)))
  have series_first_aux_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.cos1_gt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(1) > 0
theorem cos1_gt_0 : cos 1 > 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: pi_def.cos2_lt_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(2) < 0
theorem cos2_lt_0 : cos 2 < 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: pi_def.poly_monincr_quad1
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: nat, x, y: nonneg_real): x > y IMPLIES x ^ (n + 1) > y ^ (n + 1)
theorem poly_monincr_quad1 : ∀ (n : {i : ℝ // i ≥ 0}) (x : {x : ℝ // x ≥ 0}) (y : {x : ℝ // x ≥ 0}), x > y → x ^ (n + 1) > y ^ (n + 1) := by
  -- === TCCs for poly_monincr_quad1 ===
  -- TCC: poly_monincr_quad1_TCC1
  -- Statement: FORALL (n: nat, x, y: nonneg_real): x > y IMPLIES x /= 0 OR (n + 1) >= 0
  have poly_monincr_quad1_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: poly_monincr_quad1_TCC2
  -- Statement: FORALL (n: nat, x, y: nonneg_real): x > y IMPLIES y /= 0 OR (n + 1) >= 0
  have poly_monincr_quad1_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.cos_mondecr_from1to2_prep
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (c: {x: real | 1 < x AND x < 2}): sin(c) > 0
theorem cos_mondecr_from1to2_prep : ∀ (c : {x : ℝ // 1 < x ∧ x < 2}), sin c > 0 := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: pi_def.cos_mondecr_from1to2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (s, t: {x: real | 1 < x AND x < 2}): s < t IMPLIES cos(s) > cos(t)
theorem cos_mondecr_from1to2 : ∀ (s : {x : ℝ // 1 < x ∧ x < 2}) (t : {x : ℝ // 1 < x ∧ x < 2}), s < t → cos s > cos t := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.pi_bound_prep
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos(1 / 2 * pi_lb_new) > 0 AND cos(1 / 2 * pi_ub_new) < 0
theorem pi_bound_prep : cos (1 / 2 * pi_lb_new) > 0 ∧ cos (1 / 2 * pi_ub_new) < 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.piby2_prep
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: nonempty?[real]({x: real | x > 1 AND x < 2 AND cos(x) = 0})
theorem piby2_prep : nonempty_p (fun (x : ℝ) => x > 1 ∧ x < 2 ∧ cos x = 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.pi_unique
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: singleton?({x: real | x > 1 AND x < 2 AND cos(x) = 0})
theorem pi_unique : singleton_p (fun (x : ℝ) => x > 1 ∧ x < 2 ∧ cos x = 0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: pi_def.pi
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: NIL
noncomputable def pi : sorry := by
  sorry  -- pvs2lean: definition stub for NIL

-- pvs2lean: pi_def.pi_div8_lb
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 31415926 / 80000000
noncomputable def pi_div8_lb : sorry := by
  sorry  -- pvs2lean: definition stub for 31415926 / 80000000

-- pvs2lean: pi_def.pi_div8_ub
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 31415927 / 80000000
noncomputable def pi_div8_ub : sorry := by
  sorry  -- pvs2lean: definition stub for 31415927 / 80000000

-- pvs2lean: pi_def.sin_pi2_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin(pi / 2) > 0
theorem sin_pi2_pos : sin (pi / 2) > 0 := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.pi_def