-- Auto-generated by pvs2lean/translator.py
-- PVS theory: atan2
-- PVSLib library: trig
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: atan2

Auto-generated stubs from PVSLib theory `atan2`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.atan2

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (atan : pvs_τ1 → pvs_τ2)
variable {pvs_τ5 : Type*} (cos : pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (sign : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (sin : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (sq : pvs_τ9 → pvs_τ10)

-- pvs2lean: atan2.atan2
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF x > 0 THEN IF y >= 0 THEN atan(y / x) ELSE 2 * pi + atan(y / x) ENDIF
-- ELSIF x = 0 THEN IF y > 0 THEN pi / 2 ELSE 3 * pi / 2 ENDIF
-- ELSE pi + atan(y / x)
-- ENDIF
axiom atan2 {pvs_τ3 : Type*} {pvs_τ4 : Type*} : pvs_τ3 → pvs_τ4 → Prop

-- pvs2lean: atan2.atan2_0_2pi
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x /= 0 OR y /= 0) IMPLIES
--  (((x >= 0 AND y >= 0) => (0 <= atan2(x, y) AND atan2(x, y) <= pi / 2)) AND
--    ((x < 0 AND y >= 0) => (pi / 2 < atan2(x, y) AND atan2(x, y) <= pi)) AND
--     ((x < 0 AND y < 0
theorem atan2_0_2pi : (x ≠ 0 ∨ y ≠ 0) → (((x ≥ 0 ∧ y ≥ 0) → (0 ≤ atan2 x y ∧ atan2 x y ≤ pi / 2)) ∧
  ((x < 0 ∧ y ≥ 0) → (pi / 2 < atan2 x y ∧ atan2 x y ≤ pi)) ∧
   ((x < 0 ∧ y < 0) → (pi < atan2 x y ∧ atan2 x y < 3 * pi / 2))
    ∧
    ((x ≥ 0 ∧ y < 0) →
      (3 * pi / 2 ≤ atan2 x y ∧ atan2 x y < 2 * pi))) := by
  -- === TCCs for atan2_0_2pi ===
  -- TCC: atan2_0_2pi_TCC1
  -- Statement: FORALL (x, y: real):
  --   y >= 0 AND x >= 0 AND (x /= 0 OR y /= 0) IMPLIES x = 0 => y /= 0
  have atan2_0_2pi_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_0_2pi_TCC2
  -- Statement: FORALL (x, y: real):
  --   y >= 0 AND
  --    x < 0 AND
  --     ((x >= 0 AND y >= 0) => (0 <= atan2(x, y) AND atan2(x, y) <= pi / 2))
  --      AND (x /= 0 OR y /= 0)
  --    IMPLIES x = 0 => y /= 0
  have atan2_0_2pi_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_0_2pi_TCC3
  -- Statement: FORALL (x, y: real):
  --        (y < 0) AND (x < 0)
  --    AND ((x < 0 AND y >= 0) => (pi / 2 < atan2(x, y) AND atan2(x, y) <= pi))
  --    AND ((x >= 0 AND y >= 0) => (0 <= atan2(x, y) AND atan2(x, y) <= pi / ...
  have atan2_0_2pi_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_0_2pi_TCC4
  -- Statement: FORALL (x, y: real):
  --        (y < 0) AND (x >= 0)
  --    AND ((x < 0 AND y < 0) =>
  --          (pi < atan2(x, y) AND atan2(x, y) < 3 * pi / 2))
  --    AND ((x < 0 AND y >= 0) => (pi / 2 < atan2(x, y) AND atan2...
  have atan2_0_2pi_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.atan2_ge_0_lt_2pi
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x /= 0 OR y /= 0) IMPLIES atan2(x, y) >= 0 AND atan2(x, y) < 2 * pi
theorem atan2_ge_0_lt_2pi : (x ≠ 0 ∨ y ≠ 0) → atan2 x y ≥ 0 ∧ atan2 x y < 2 * pi := by
  -- === TCCs for atan2_ge_0_lt_2pi ===
  -- TCC: atan2_ge_0_lt_2pi_TCC1
  -- Statement: FORALL (x, y: real): (x /= 0 OR y /= 0) IMPLIES x = 0 => y /= 0
  have atan2_ge_0_lt_2pi_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.atan2_cancel_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x /= 0 OR y /= 0) IMPLIES atan2(x * pz, y * pz) = atan2(x, y)
theorem atan2_cancel_pos : (x ≠ 0 ∨ y ≠ 0) → atan2 (x * pz) (y * pz) = atan2 x y := by
  -- === TCCs for atan2_cancel_pos ===
  -- TCC: atan2_cancel_pos_TCC1
  -- Statement: FORALL (pz: posreal, x, y: real):
  --   (x /= 0 OR y /= 0) IMPLIES x * pz = 0 => y * pz /= 0
  have atan2_cancel_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.atan2_cancel_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x /= 0 OR y /= 0) IMPLIES
--  atan2(x * nz, y * nz) =
--   IF y > 0 OR (y = 0 AND x > 0) THEN atan2(x, y) + pi
--   ELSE atan2(x, y) - pi
--   ENDIF
theorem atan2_cancel_neg : (x ≠ 0 ∨ y ≠ 0) → atan2 (x * nz) (y * nz) =
 if y > 0 ∨ (y = 0 ∧ x > 0) then atan2 x y + pi
 else atan2 x y - pi := by
  -- === TCCs for atan2_cancel_neg ===
  -- TCC: atan2_cancel_neg_TCC1
  -- Statement: FORALL (nz: negreal, x, y: real):
  --   (x /= 0 OR y /= 0) IMPLIES x * nz = 0 => y * nz /= 0
  have atan2_cancel_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.atan2_swap_pos
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x * y > 0 IMPLIES
--  atan2(x, y) = atan2(y, x) + atan((y ^ 2 - x ^ 2) / (2 * (x * y)))
theorem atan2_swap_pos : x * y > 0 → atan2 x y = atan2 y x + atan ((y ^ 2 - x ^ 2) / (2 * (x * y))) := by
  -- === TCCs for atan2_swap_pos ===
  -- TCC: atan2_swap_pos_TCC1
  -- Statement: FORALL (x, y: real): x * y > 0 IMPLIES x = 0 => y /= 0
  have atan2_swap_pos_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_swap_pos_TCC2
  -- Statement: FORALL (x, y: real): x * y > 0 IMPLIES y /= 0 OR 2 >= 0
  have atan2_swap_pos_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_swap_pos_TCC3
  -- Statement: FORALL (x, y: real): x * y > 0 IMPLIES x /= 0 OR 2 >= 0
  have atan2_swap_pos_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_swap_pos_TCC4
  -- Statement: FORALL (x, y: real): x * y > 0 IMPLIES (2 * (x * y)) /= 0
  have atan2_swap_pos_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.atan2_swap_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: x * y < 0 IMPLIES
--  atan2(x, y) =
--   atan2(y, x) + atan((y ^ 2 - x ^ 2) / (2 * (x * y))) + sign(x) * pi
theorem atan2_swap_neg : x * y < 0 → atan2 x y =
 atan2 y x + atan ((y ^ 2 - x ^ 2) / (2 * (x * y))) + sign x * pi := by
  -- === TCCs for atan2_swap_neg ===
  -- TCC: atan2_swap_neg_TCC1
  -- Statement: FORALL (x, y: real): x * y < 0 IMPLIES x = 0 => y /= 0
  have atan2_swap_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_swap_neg_TCC2
  -- Statement: FORALL (x, y: real): x * y < 0 IMPLIES y /= 0 OR 2 >= 0
  have atan2_swap_neg_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_swap_neg_TCC3
  -- Statement: FORALL (x, y: real): x * y < 0 IMPLIES x /= 0 OR 2 >= 0
  have atan2_swap_neg_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_swap_neg_TCC4
  -- Statement: FORALL (x, y: real): x * y < 0 IMPLIES (2 * (x * y)) /= 0
  have atan2_swap_neg_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.atan2_swap_zero
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x = 0 AND y /= 0 IMPLIES atan2(x, y) = atan2(y, x) + pi / 2) AND
--  (y = 0 AND x /= 0 IMPLIES atan2(x, y) = atan2(y, x) - pi / 2)
theorem atan2_swap_zero : (x = 0 ∧ y ≠ 0 → atan2 x y = atan2 y x + pi / 2) ∧ (y = 0 ∧ x ≠ 0 → atan2 x y = atan2 y x - pi / 2) := by
  -- === TCCs for atan2_swap_zero ===
  -- TCC: atan2_swap_zero_TCC1
  -- Statement: FORALL (x, y: real): y /= 0 AND x = 0 IMPLIES x = 0 => y /= 0
  have atan2_swap_zero_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: atan2_swap_zero_TCC2
  -- Statement: FORALL (x, y: real):
  --   x /= 0 AND
  --    y = 0 AND (x = 0 AND y /= 0 IMPLIES atan2(x, y) = atan2(y, x) + pi / 2)
  --    IMPLIES x = 0 => y /= 0
  have atan2_swap_zero_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.atan2_cos_sin
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: atan2(cos(a), sin(a)) = a
theorem atan2_cos_sin : atan2 (cos a) (sin a) = a := by
  -- === TCCs for atan2_cos_sin ===
  -- TCC: atan2_cos_sin_TCC1
  -- Statement: FORALL (a: {z: nnreal | z < 2 * pi}): cos(a) = 0 => sin(a) /= 0
  have atan2_cos_sin_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.sin_atan2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x /= 0 OR y /= 0) IMPLIES
--  sin(atan2(x, y)) =
--   IF x = 0 THEN IF y > 0 THEN 1 ELSE -1 ENDIF
--   ELSE LET yx = y / x IN
--          LET r = yx / sqrt((sq(yx) + 1)) IN IF x > 0 THEN r ELSE -r ENDIF
--   ENDIF
theorem sin_atan2 : (x ≠ 0 ∨ y ≠ 0) → sin (atan2 x y) =
 if x = 0 then if y > 0 then 1 else -1 
 else (let yx := y / x; (let r := yx / Real.sqrt (((yx)^2 + 1)); if x > 0 then r else -r 
 )) := by
  -- === TCCs for sin_atan2 ===
  -- TCC: sin_atan2_TCC1
  -- Statement: FORALL (x, y: real): NOT x = 0 AND (x /= 0 OR y /= 0) IMPLIES x /= 0
  have sin_atan2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.cos_atan2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (x /= 0 OR y /= 0) IMPLIES
--  cos(atan2(x, y)) =
--   IF x = 0 THEN 0
--   ELSE LET yx = y / x IN
--          IF x > 0 THEN 1 / sqrt((sq(yx) + 1))
--          ELSE -1 / sqrt((sq(yx) + 1))
--          ENDIF
--   ENDIF
theorem cos_atan2 : (x ≠ 0 ∨ y ≠ 0) → cos (atan2 x y) =
 if x = 0 then 0
 else (let yx := y / x; if x > 0 then 1 / Real.sqrt (((yx)^2 + 1))
        else -1 / Real.sqrt (((yx)^2 + 1))
        
 ) := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: atan2.atan2_scal_lt2pi
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (k: posreal, a: nnreal_lt_2pi): atan2(k * cos(a), k * sin(a)) = a
theorem atan2_scal_lt2pi : ∀ (k : {x : {x : ℝ // x ≥ 0} // x > 0}) (a : {x : {x : ℝ // x ≥ 0} // x < 2 * pi}), atan2 (k * cos a) (k * sin a) = a := by
  -- === TCCs for atan2_scal_lt2pi ===
  -- TCC: atan2_scal_lt2pi_TCC1
  -- Statement: FORALL (k: posreal, a: nnreal_lt_2pi): k * cos(a) = 0 => k * sin(a) /= 0
  have atan2_scal_lt2pi_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.atan2