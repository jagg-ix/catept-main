-- Auto-generated by pvs2lean/translator.py
-- PVS theory: tan_approx
-- PVSLib library: trig
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: tan_approx

Auto-generated stubs from PVSLib theory `tan_approx`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.tan_approx

noncomputable section

variable {pvs_τ1 : Type*} (Tan_p : pvs_τ1 → Prop)
variable {pvs_τ2 : Type*} {pvs_τ3 : Type*} (cos_lb : pvs_τ2 → pvs_τ3 → Prop)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} (cos_ub : pvs_τ4 → pvs_τ5 → Prop)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} (sin_lb : pvs_τ6 → pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (sin_ub : pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ12 : Type*} (tan : pvs_τ12 → Prop)

-- pvs2lean: tan_approx.tan_lb_ub
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_lb(a, n) / cos_ub(a, n)
axiom tan_lb_ub {pvs_τ15 : Type*} {pvs_τ16 : Type*} : pvs_τ15 → pvs_τ16 → Prop

-- pvs2lean: tan_approx.tan_ub_lb
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_ub(a, n) / cos_lb(a, n)
axiom tan_ub_lb {pvs_τ17 : Type*} {pvs_τ18 : Type*} : pvs_τ17 → pvs_τ18 → Prop

-- pvs2lean: tan_approx.tan_lb_lb
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_lb(a, n) / cos_lb(a, n)
axiom tan_lb_lb {pvs_τ13 : Type*} {pvs_τ14 : Type*} : pvs_τ13 → pvs_τ14 → Prop

-- pvs2lean: tan_approx.tan_ub_ub
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sin_ub(a, n) / cos_ub(a, n)
axiom tan_ub_ub {pvs_τ19 : Type*} {pvs_τ20 : Type*} : pvs_τ19 → pvs_τ20 → Prop

-- pvs2lean: tan_approx.tan_lb_ub_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_ub(a, n) /= 0 IMPLIES tan_lb_ub(-a, n) = -tan_ub_ub(a, n)
theorem tan_lb_ub_neg : cos_ub a n ≠ 0 → tan_lb_ub -a n = -tan_ub_ub a n := by
  -- === TCCs for tan_lb_ub_neg ===
  -- TCC: tan_lb_ub_neg_TCC1
  -- Statement: FORALL (a: real, n: nat): cos_ub(a, n) /= 0 IMPLIES cos_ub(-a, n) /= 0
  have tan_lb_ub_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tan_approx.tan_ub_lb_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_lb(a, n) /= 0 IMPLIES tan_ub_lb(-a, n) = -tan_lb_lb(a, n)
theorem tan_ub_lb_neg : cos_lb a n ≠ 0 → tan_ub_lb -a n = -tan_lb_lb a n := by
  -- === TCCs for tan_ub_lb_neg ===
  -- TCC: tan_ub_lb_neg_TCC1
  -- Statement: FORALL (a: real, n: nat): cos_lb(a, n) /= 0 IMPLIES cos_lb(-a, n) /= 0
  have tan_ub_lb_neg_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tan_approx.tan_lb_lb_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_lb(a, n) /= 0 IMPLIES tan_lb_lb(-a, n) = -tan_ub_lb(a, n)
theorem tan_lb_lb_neg : cos_lb a n ≠ 0 → tan_lb_lb -a n = -tan_ub_lb a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tan_approx.tan_ub_ub_neg
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: cos_ub(a, n) /= 0 IMPLIES tan_ub_ub(-a, n) = -tan_lb_ub(a, n)
theorem tan_ub_ub_neg : cos_ub a n ≠ 0 → tan_ub_ub -a n = -tan_lb_ub a n := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tan_approx.tan_bounds_0_pi2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: 0 <= a AND a < pi / 2 AND cos_lb(a, n) > 0 IMPLIES
--  tan_lb_ub(a, n) <= tan(a) AND tan(a) <= tan_ub_lb(a, n)
theorem tan_bounds_0_pi2 : 0 ≤ a ∧ a < pi / 2 ∧ cos_lb a n > 0 → tan_lb_ub a n ≤ tan a ∧ tan a ≤ tan_ub_lb a n := by
  -- === TCCs for tan_bounds_0_pi2 ===
  -- TCC: tan_bounds_0_pi2_TCC1
  -- Statement: FORALL (a: real, n: nat):
  --   cos_lb(a, n) > 0 AND a < pi / 2 AND 0 <= a IMPLIES cos_ub(a, n) /= 0
  have tan_bounds_0_pi2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_bounds_0_pi2_TCC2
  -- Statement: FORALL (a: real, n: nat):
  --   cos_lb(a, n) > 0 AND a < pi / 2 AND 0 <= a IMPLIES Tan?(a)
  have tan_bounds_0_pi2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_bounds_0_pi2_TCC3
  -- Statement: FORALL (a: real, n: nat):
  --   tan_lb_ub(a, n) <= tan(a) AND cos_lb(a, n) > 0 AND a < pi / 2 AND 0 <= a
  --    IMPLIES cos_lb(a, n) /= 0
  have tan_bounds_0_pi2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tan_approx.tan_bounds_npi2_0
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -pi / 2 < a AND a <= 0 AND cos_lb(a, n) > 0 IMPLIES
--  tan_lb_lb(a, n) <= tan(a) AND tan(a) <= tan_ub_ub(a, n)
theorem tan_bounds_npi2_0 : -pi / 2 < a ∧ a ≤ 0 ∧ cos_lb a n > 0 → tan_lb_lb a n ≤ tan a ∧ tan a ≤ tan_ub_ub a n := by
  -- === TCCs for tan_bounds_npi2_0 ===
  -- TCC: tan_bounds_npi2_0_TCC1
  -- Statement: FORALL (a: real, n: nat):
  --   cos_lb(a, n) > 0 AND a <= 0 AND -pi / 2 < a IMPLIES cos_lb(a, n) /= 0
  have tan_bounds_npi2_0_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_bounds_npi2_0_TCC2
  -- Statement: FORALL (a: real, n: nat):
  --   cos_lb(a, n) > 0 AND a <= 0 AND -pi / 2 < a IMPLIES Tan?(a)
  have tan_bounds_npi2_0_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_bounds_npi2_0_TCC3
  -- Statement: FORALL (a: real, n: nat):
  --   tan_lb_lb(a, n) <= tan(a) AND cos_lb(a, n) > 0 AND a <= 0 AND -pi / 2 < a
  --    IMPLIES cos_ub(a, n) /= 0
  have tan_bounds_npi2_0_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tan_approx.tan_bounds_pi2_pi
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: pi / 2 < a AND a <= pi AND cos_ub(a, n) < 0 IMPLIES
--  tan_ub_ub(a, n) <= tan(a) AND tan(a) <= tan_lb_lb(a, n)
theorem tan_bounds_pi2_pi : pi / 2 < a ∧ a ≤ pi ∧ cos_ub a n < 0 → tan_ub_ub a n ≤ tan a ∧ tan a ≤ tan_lb_lb a n := by
  -- === TCCs for tan_bounds_pi2_pi ===
  -- TCC: tan_bounds_pi2_pi_TCC1
  -- Statement: FORALL (a: real, n: nat):
  --   cos_ub(a, n) < 0 AND a <= pi AND pi / 2 < a IMPLIES cos_ub(a, n) /= 0
  have tan_bounds_pi2_pi_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_bounds_pi2_pi_TCC2
  -- Statement: FORALL (a: real, n: nat):
  --   cos_ub(a, n) < 0 AND a <= pi AND pi / 2 < a IMPLIES Tan?(a)
  have tan_bounds_pi2_pi_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_bounds_pi2_pi_TCC3
  -- Statement: FORALL (a: real, n: nat):
  --   tan_ub_ub(a, n) <= tan(a) AND cos_ub(a, n) < 0 AND a <= pi AND pi / 2 < a
  --    IMPLIES cos_lb(a, n) /= 0
  have tan_bounds_pi2_pi_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: tan_approx.tan_bounds_npi_npi2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: -pi <= a AND a < -pi / 2 AND cos_ub(a, n) < 0 IMPLIES
--  tan_ub_lb(a, n) <= tan(a) AND tan(a) <= tan_lb_ub(a, n)
theorem tan_bounds_npi_npi2 : -pi ≤ a ∧ a < -pi / 2 ∧ cos_ub a n < 0 → tan_ub_lb a n ≤ tan a ∧ tan a ≤ tan_lb_ub a n := by
  -- === TCCs for tan_bounds_npi_npi2 ===
  -- TCC: tan_bounds_npi_npi2_TCC1
  -- Statement: FORALL (a: real, n: nat):
  --   cos_ub(a, n) < 0 AND a < -pi / 2 AND -pi <= a IMPLIES cos_lb(a, n) /= 0
  have tan_bounds_npi_npi2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_bounds_npi_npi2_TCC2
  -- Statement: FORALL (a: real, n: nat):
  --   cos_ub(a, n) < 0 AND a < -pi / 2 AND -pi <= a IMPLIES Tan?(a)
  have tan_bounds_npi_npi2_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: tan_bounds_npi_npi2_TCC3
  -- Statement: FORALL (a: real, n: nat):
  --   tan_ub_lb(a, n) <= tan(a) AND
  --    cos_ub(a, n) < 0 AND a < -pi / 2 AND -pi <= a
  --    IMPLIES cos_ub(a, n) /= 0
  have tan_bounds_npi_npi2_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.tan_approx