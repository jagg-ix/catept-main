-- Auto-generated by pvs2lean/translator.py
-- PVS theory: poly_families
-- PVSLib library: Tarski
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: poly_families

Auto-generated stubs from PVSLib theory `poly_families`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.poly_families

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} (pvs_1 : pvs_τ1 → pvs_τ2 → pvs_τ3)
variable {pvs_τ4 : Type*} {pvs_τ5 : Type*} {pvs_τ6 : Type*} (pvs_2 : pvs_τ4 → pvs_τ5 → pvs_τ6)
variable {pvs_τ7 : Type*} {pvs_τ8 : Type*} (pvs_3 : pvs_τ7 → pvs_τ8)
variable {pvs_τ9 : Type*} (Aset : pvs_τ9 → Prop)
variable {pvs_τ10 : Type*} {pvs_τ11 : Type*} (GF : pvs_τ10 → pvs_τ11 → Prop)
variable {pvs_τ12 : Type*} (GP : pvs_τ12 → Prop)
variable {pvs_τ13 : Type*} {pvs_τ14 : Type*} (KF : pvs_τ13 → pvs_τ14)
variable {pvs_τ36 : Type*} {pvs_τ37 : Type*} (RelF : pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} (RelF1 : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} (RelF2 : pvs_τ39 → pvs_τ40)
variable {pvs_τ41 : Type*} {pvs_τ42 : Type*} (RelF6 : pvs_τ41 → pvs_τ42)
variable {pvs_τ43 : Type*} (RelFF : pvs_τ43 → Prop)
variable {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} {pvs_τ61 : Type*} {pvs_τ62 : Type*} (TQ : pvs_τ58 → pvs_τ59 → pvs_τ60 → pvs_τ61 → pvs_τ62)
variable {pvs_τ63 : Type*} {pvs_τ64 : Type*} (TQRow : pvs_τ63 → pvs_τ64)
variable {pvs_τ71 : Type*} {pvs_τ72 : Type*} (above : pvs_τ71 → pvs_τ72)
variable {pvs_τ73 : Type*} (anspoly : pvs_τ73 → Prop)
variable {pvs_τ81 : Type*} {pvs_τ82 : Type*} {pvs_τ83 : Type*} (base_n : pvs_τ81 → pvs_τ82 → pvs_τ83 → Prop)
variable {pvs_τ84 : Type*} {pvs_τ85 : Type*} (below : pvs_τ84 → pvs_τ85)
variable {pvs_τ86 : Type*} {pvs_τ87 : Type*} (bpolyinit : pvs_τ86 → pvs_τ87)
variable {pvs_τ88 : Type*} {pvs_τ89 : Type*} (card : pvs_τ88 → pvs_τ89)
variable {pvs_τ95 : Type*} (integer_pred : pvs_τ95 → Prop)
variable {pvs_τ96 : Type*} {pvs_τ97 : Type*} (is_finite : pvs_τ96 → pvs_τ97)
variable {pvs_τ98 : Type*} {pvs_τ99 : Type*} (mod : pvs_τ98 → pvs_τ99 → Prop)
variable {pvs_τ100 : Type*} {pvs_τ101 : Type*} {pvs_τ102 : Type*} (nth : pvs_τ100 → pvs_τ101 → pvs_τ102)
variable {pvs_τ103 : Type*} {pvs_τ104 : Type*} {pvs_τ105 : Type*} {pvs_τ106 : Type*} (polynomial : pvs_τ103 → pvs_τ104 → pvs_τ105 → pvs_τ106)
variable {pvs_τ107 : Type*} {pvs_τ108 : Type*} {pvs_τ109 : Type*} {pvs_τ110 : Type*} {pvs_τ111 : Type*} {pvs_τ112 : Type*} (polynomial_prod : pvs_τ107 → pvs_τ108 → pvs_τ109 → pvs_τ110 → pvs_τ111 → pvs_τ112)
variable {pvs_τ122 : Type*} (rational_pred : pvs_τ122 → Prop)
variable {pvs_τ123 : Type*} {pvs_τ124 : Type*} (setseq : pvs_τ123 → pvs_τ124 → Prop)
variable {pvs_τ136 : Type*} {pvs_τ137 : Type*} (sign_ext : pvs_τ136 → pvs_τ137)
variable {pvs_τ156 : Type*} {pvs_τ157 : Type*} {pvs_τ158 : Type*} (subrange : pvs_τ156 → pvs_τ157 → pvs_τ158)
variable {pvs_τ159 : Type*} {pvs_τ160 : Type*} (tpolyinit : pvs_τ159 → pvs_τ160)
variable {pvs_τ168 : Type*} {pvs_τ169 : Type*} (upto : pvs_τ168 → pvs_τ169)

-- pvs2lean: poly_families.poly1
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i = 0 THEN 1 ELSE 0 ENDIF
noncomputable def poly1 : sorry := by
  sorry  -- pvs2lean: definition stub for IF i = 0 THEN 1 ELSE 0 ENDIF

-- pvs2lean: poly_families.polynomial_prod_int
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: rational_pred(polynomial_prod(a, d, g, k)(i)) AND
--  integer_pred(polynomial_prod(a, d, g, k)(i))
theorem polynomial_prod_int : rational_pred (polynomial_prod a d g k(i)) ∧ integer_pred (polynomial_prod a d g k(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sigma_j
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (GP: [nat -> subrange(0, 2)], KF: [nat -> nat], k: nat):
--   rational_pred(sigma(0, k, *(KF, GP))) AND
--    integer_pred(sigma(0, k, *(KF, GP))) AND sigma(0, k, *(KF, GP)) >= 0
theorem sigma_j : ∀ (GP : {i : ℝ // i ≥ 0} → {k : ℝ // 0 ≤ k ∧ k ≤ 2}) (KF : {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0}) (k : {i : ℝ // i ≥ 0}), rational_pred (Finset.sum (Finset.Icc 0 k) (*(KF, GP))) ∧ integer_pred (Finset.sum (Finset.Icc 0 k) (*(KF, GP))) ∧ Finset.sum (Finset.Icc 0 k) (*(KF, GP)) ≥ 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.prod_polynomials
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET tpoly =
--       (IF GP(k) = 0 THEN poly1
--        ELSIF GP(k) = 1
--          THEN (LAMBDA (i: nat): IF i <= KF(k) THEN GF(k)(i) ELSE 0 ENDIF)
--        ELSE polynomial_prod(GF(k), KF(k), GF(k), KF(k))
--     
axiom prod_polynomials {pvs_τ113 : Type*} {pvs_τ114 : Type*} {pvs_τ115 : Type*} {pvs_τ116 : Type*} {pvs_τ117 : Type*} : pvs_τ113 → pvs_τ114 → pvs_τ115 → pvs_τ116 → pvs_τ117 → Prop

-- pvs2lean: poly_families.prod_polynomials_list
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET tpolyinit =
--       (IF GP(k) = 0 THEN poly1
--        ELSIF GP(k) = 1 THEN GF(k)
--        ELSE polynomial_prod(GF(k), KF(k), GF(k), KF(k))
--        ENDIF),
--     tdeg =
--       IF GP(k) = 0 THEN 0 ELSIF GP(k)
axiom prod_polynomials_list {pvs_τ118 : Type*} {pvs_τ119 : Type*} {pvs_τ120 : Type*} {pvs_τ121 : Type*} : pvs_τ118 → pvs_τ119 → pvs_τ120 → pvs_τ121 → Prop

-- pvs2lean: poly_families.TQ_fam
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET ll = prod_polynomials_list(GF, KF, GP, k),
--     d = length[int](ll) - 1,
--     g = (LAMBDA (i): IF i <= d THEN nth(ll, i) ELSE 0 ENDIF)
--   IN TQ(a, n, g, d)
axiom TQ_fam {pvs_τ65 : Type*} {pvs_τ66 : Type*} {pvs_τ67 : Type*} {pvs_τ68 : Type*} {pvs_τ69 : Type*} {pvs_τ70 : Type*} : pvs_τ65 → pvs_τ66 → pvs_τ67 → pvs_τ68 → pvs_τ69 → pvs_τ70 → Prop

-- pvs2lean: poly_families.TQ_fam_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND (FORALL (i: upto(k)): GF(i)(KF(i)) /= 0) IMPLIES
--  TQ_fam(k, a, n, GF, KF, GP) =
--   TQ(a, n, prod_polynomials(GF, KF, GP, k), sigma(0, k, KF * GP))
theorem TQ_fam_def : a n ≠ 0 ∧ (∀ (i : {k : ℕ // k ≤ k}), GF i(KF i) ≠ 0) → TQ_fam k a n GF KF GP =
 TQ a n (prod_polynomials GF KF GP k) (Finset.sum (Finset.Icc 0 k) (KF * GP)) := by
  -- === TCCs for TQ_fam_def ===
  -- TCC: TQ_fam_def_TCC1
  -- Statement: FORALL (GF: [nat -> [nat -> int]], GP: [nat -> subrange(0, 2)],
  --         KF: [nat -> nat], a: [nat -> int], k: nat, n: posnat):
  --   (FORALL (i: upto(k)): GF(i)(KF(i)) /= 0) AND a(n) /= 0 IMPLIES
  --    ra...
  have tq_fam_def_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.Sol
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: {r: real |
--    polynomial(a, n)(r) = 0 AND
--     (FORALL (i):
--        i <= k IMPLIES
--         (IF RelF(i) = -1 THEN polynomial(GF(i), KF(i))(r) < 0
--          ELSIF RelF(i) = 1 THEN polynomial(GF(i), KF(i))(
axiom Sol {pvs_τ44 : Type*} {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} {pvs_τ50 : Type*} : pvs_τ44 → pvs_τ45 → pvs_τ46 → pvs_τ47 → pvs_τ48 → pvs_τ49 → pvs_τ50

-- pvs2lean: poly_families.Sol_all
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: {r: real |
--    polynomial(a, n)(r) = 0 AND
--     (FORALL (i):
--        i <= k IMPLIES
--         (IF RelF6(i) = 0 THEN polynomial(GF(i), KF(i))(r) = 0
--          ELSIF RelF6(i) = 1 THEN polynomial(GF(i), KF(i))
axiom Sol_all {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} {pvs_τ54 : Type*} {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} : pvs_τ51 → pvs_τ52 → pvs_τ53 → pvs_τ54 → pvs_τ55 → pvs_τ56 → pvs_τ57

-- pvs2lean: poly_families.NSol
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: card(Sol(k, a, n, GF, KF, RelF))
axiom NSol {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} {pvs_τ18 : Type*} {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} : pvs_τ15 → pvs_τ16 → pvs_τ17 → pvs_τ18 → pvs_τ19 → pvs_τ20 → pvs_τ21

-- pvs2lean: poly_families.NSol_all
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: card(Sol_all(k, a, n, GF, KF, RelF6))
axiom NSol_all {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} : pvs_τ22 → pvs_τ23 → pvs_τ24 → pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28

-- pvs2lean: poly_families.sign_prod
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: product[nat](0, k, LAMBDA (i: nat): RelF(i) ^ GP(i))
axiom sign_prod {pvs_τ138 : Type*} {pvs_τ139 : Type*} {pvs_τ140 : Type*} : pvs_τ138 → pvs_τ139 → pvs_τ140 → Prop

-- pvs2lean: poly_families.sign_prod_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (i: nat): i <= k IMPLIES RelF1(i) = RelF2(i)) IMPLIES
--  sign_prod(k, GP, RelF1) = sign_prod(k, GP, RelF2)
theorem sign_prod_eq : (∀ (i : ℕ), i ≤ k → RelF1 i = RelF2 i) → sign_prod k GP RelF1 = sign_prod k GP RelF2 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sign_ext_pow
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sign_ext(x ^ k) = sign_ext(x) ^ k
theorem sign_ext_pow : sign_ext (x ^ k) = sign_ext x ^ k := by
  -- === TCCs for sign_ext_pow ===
  -- TCC: sign_ext_pow_TCC1
  -- Statement: FORALL (k: nat, x: real): x /= 0 OR k >= 0
  have sign_ext_pow_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: sign_ext_pow_TCC2
  -- Statement: FORALL (k: nat, x: real): sign_ext(x) /= 0 OR k >= 0
  have sign_ext_pow_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sign_ext_prod_polynomials
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET RelFF = (LAMBDA (i: nat): sign_ext(polynomial(GF(i), KF(i))(x))) IN
--   sign_ext(polynomial(prod_polynomials(GF, KF, GP, k), sigma(0, k, KF * GP))
--                      (x))
--    = sign_prod(k, GP, Rel
theorem sign_ext_prod_polynomials : fun (RelFF : _ → {sig : ℝ // (sig = -1 ∨ sig = 1 ∨ sig = 0) ∧
 sig * polynomial (GF i) (KF i) (x) = |polynomial (GF i) (KF i) (x)|}) => sign_ext (polynomial (prod_polynomials GF KF GP k) (Finset.sum (Finset.Icc 0 k) (KF * GP))
                   (x)) = sign_prod k GP RelFF (fun (i : {i : ℝ // i ≥ 0}) => sign_ext (polynomial (GF i) (KF i) x)) := by
  -- === TCCs for sign_ext_prod_polynomials ===
  -- TCC: sign_ext_prod_polynomials_TCC1
  -- Statement: FORALL (GF: [nat -> [nat -> int]], KF: [nat -> nat], x: real,
  --         RelFF:
  --           [i: nat ->
  --              {sig: real |
  --                 (sig = -1 OR sig = 1 OR sig = 0) AND
  --                  sig...
  have sign_ext_prod_polynomials_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.atest
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < 6 THEN i ELSIF i <= 9 THEN i + 4 ELSE 0 ENDIF
noncomputable def atest : sorry := by
  sorry  -- pvs2lean: definition stub for IF i < 6 THEN i ELSIF i <= 9 THEN i + 4 ELSE 0 ENDIF

-- pvs2lean: poly_families.ntest
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 9
noncomputable def ntest : sorry := by
  sorry  -- pvs2lean: definition stub for 9

-- pvs2lean: poly_families.GFtest
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i < j THEN 1 + j
-- ELSIF i + j <= 6 THEN i * j
-- ELSIF j <= 7 THEN 2
-- ELSE 0
-- ENDIF
noncomputable def GFtest : sorry := by
  sorry  -- pvs2lean: definition stub for IF i < j THEN 1 + j ELSIF i + j <= 6 THEN i * j ELSIF j <= 7 THEN 2 ELSE 0 ENDIF

-- pvs2lean: poly_families.KFtest
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: 7
noncomputable def KFtest : sorry := by
  sorry  -- pvs2lean: definition stub for 7

-- pvs2lean: poly_families.GPtest
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i <= 3 THEN 2 ELSIF i <= 5 THEN 1 ELSE 0 ENDIF
noncomputable def GPtest : sorry := by
  sorry  -- pvs2lean: definition stub for IF i <= 3 THEN 2 ELSIF i <= 5 THEN 1 ELSE 0 ENDIF

-- pvs2lean: poly_families.sig
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF ii = 0 THEN 0 ELSIF ii = 1 THEN 1 ELSE -1 ENDIF
axiom sig {pvs_τ125 : Type*} {pvs_τ126 : Type*} : pvs_τ125 → pvs_τ126

-- pvs2lean: poly_families.unsig
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: mod(ii, 3)
axiom unsig {pvs_τ164 : Type*} {pvs_τ165 : Type*} : pvs_τ164 → pvs_τ165

-- pvs2lean: poly_families.sig_unsig
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ii: subrange(-1, 1)): sig(unsig(ii)) = ii
theorem sig_unsig : ∀ (ii : {k : ℝ // -1 ≤ k ∧ k ≤ 1}), sig (unsig ii) = ii := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: poly_families.sig_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sig(GP(i))
axiom sig_seq {pvs_τ127 : Type*} : pvs_τ127 → Prop

-- pvs2lean: poly_families.unsig_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: unsig(RelF(i))
axiom unsig_seq {pvs_τ166 : Type*} {pvs_τ167 : Type*} : pvs_τ166 → pvs_τ167

-- pvs2lean: poly_families.sig_seq_unsig_seq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: sig_seq(unsig_seq(RelF)) = RelF
theorem sig_seq_unsig_seq : sig_seq (unsig_seq RelF) = RelF := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.base_3_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (IF p <= k THEN base_n(3, i)(k - p) ELSE 0 ENDIF)
axiom base_3_seq {pvs_τ76 : Type*} {pvs_τ77 : Type*} : pvs_τ76 → pvs_τ77 → Prop

-- pvs2lean: poly_families.base_6_seq
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (IF p <= k THEN base_n(6, i)(k - p) ELSE 0 ENDIF)
axiom base_6_seq {pvs_τ78 : Type*} {pvs_τ79 : Type*} {pvs_τ80 : Type*} : pvs_τ78 → pvs_τ79 → pvs_τ80 → Prop

-- pvs2lean: poly_families.sig_seq_base_3_onto
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (RelF, k):
--   EXISTS (i: below(3 ^ (k + 1))):
--     sig_seq(base_3_seq(k, i)) =
--      (LAMBDA (i: nat): IF i <= k THEN RelF(i) ELSE 0 ENDIF)
theorem sig_seq_base_3_onto : ∀ (RelF : {i : ℝ // i ≥ 0} → {k : ℝ // -1 ≤ k ∧ k ≤ 1}) (k : {i : ℝ // i ≥ 0}), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < 3 ^ (1 + k)}), sig_seq (base_3_seq k i) = (fun (i : ℕ)=> if i ≤ k then RelF i else 0 ) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sig_seq_base_3_onto_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (RelF, k):
--   EXISTS (i: below(3 ^ (k + 1))):
--     sig_seq(base_n(3, i)) =
--      (LAMBDA (i: nat): IF i <= k THEN RelF(i) ELSE 0 ENDIF)
theorem sig_seq_base_3_onto_2 : ∀ (RelF : {i : ℝ // i ≥ 0} → {k : ℝ // -1 ≤ k ∧ k ≤ 1}) (k : {i : ℝ // i ≥ 0}), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < 3 ^ (1 + k)}), sig_seq (base_n 3 i) = (fun (i : ℕ)=> if i ≤ k then RelF i else 0 ) := by
  -- === TCCs for sig_seq_base_3_onto_2 ===
  -- TCC: sig_seq_base_3_onto_2_TCC1
  -- Statement: FORALL (RelF: [nat -> subrange(-1, 1)], k: nat, i: below(3 ^ (1 + k))):
  --   FORALL (x1: nat): 0 <= base_n(3, i)(x1) AND base_n(3, i)(x1) <= 2
  have sig_seq_base_3_onto_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sig_seq_base_6_onto
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (RelF, k):
--   EXISTS (i: below(6 ^ (k + 1))):
--     (FORALL (j: nat): base_6_seq(k, i)(j) <= 2) AND
--      sig_seq(base_6_seq(k, i)) =
--       (LAMBDA (i: nat): IF i <= k THEN RelF(i) ELSE 0 ENDIF)
theorem sig_seq_base_6_onto : ∀ (RelF : {i : ℝ // i ≥ 0} → {k : ℝ // -1 ≤ k ∧ k ≤ 1}) (k : {i : ℝ // i ≥ 0}), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < 6 ^ (1 + k)}), (∀ (j : ℕ), base_6_seq k i(j) ≤ 2) ∧ sig_seq (base_6_seq k i) =
 (fun (i : ℕ)=> if i ≤ k then RelF i else 0 ) := by
  -- === TCCs for sig_seq_base_6_onto ===
  -- TCC: sig_seq_base_6_onto_TCC1
  -- Statement: FORALL (RelF: [nat -> subrange(-1, 1)], k: nat, i: below(6 ^ (1 + k))):
  --   (FORALL (j: nat): base_6_seq(k, i)(j) <= 2) IMPLIES
  --    FORALL (x1: nat): 0 <= base_6_seq(k, i)(x1) AND base_6_seq(k, i)(x1)...
  have sig_seq_base_6_onto_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sig_seq_base_6_onto_2
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (RelF, k):
--   EXISTS (i: below(6 ^ (k + 1))):
--     (FORALL (j: nat): base_n(6, i)(j) <= 2) AND
--      sig_seq(base_n(6, i)) =
--       (LAMBDA (i: nat): IF i <= k THEN RelF(i) ELSE 0 ENDIF)
theorem sig_seq_base_6_onto_2 : ∀ (RelF : {i : ℝ // i ≥ 0} → {k : ℝ // -1 ≤ k ∧ k ≤ 1}) (k : {i : ℝ // i ≥ 0}), ∃ (i : {s : {i : ℝ // i ≥ 0} // s < 6 ^ (1 + k)}), (∀ (j : ℕ), base_n 6 i(j) ≤ 2) ∧ sig_seq (base_n 6 i) =
 (fun (i : ℕ)=> if i ≤ k then RelF i else 0 ) := by
  -- === TCCs for sig_seq_base_6_onto_2 ===
  -- TCC: sig_seq_base_6_onto_2_TCC1
  -- Statement: FORALL (RelF: [nat -> subrange(-1, 1)], k: nat, i: below(6 ^ (1 + k))):
  --   (FORALL (j: nat): base_n(6, i)(j) <= 2) IMPLIES
  --    FORALL (x1: nat): 0 <= base_n(6, i)(x1) AND base_n(6, i)(x1) <= 2
  have sig_seq_base_6_onto_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.base_3_seq_unique
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j):
--   i < 3 ^ (k + 1) AND j < 3 ^ (k + 1) AND i /= j IMPLIES
--    base_3_seq(k, i) /= base_3_seq(k, j)
theorem base_3_seq_unique : ∀ (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), i < 3 ^ (k + 1) ∧ j < 3 ^ (k + 1) ∧ i ≠ j → base_3_seq k i ≠ base_3_seq k j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.base_n_3_seq_unique
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (i, j):
--   i < 3 ^ (k + 1) AND j < 3 ^ (k + 1) AND i /= j IMPLIES
--    base_n(3, i) /= base_n(3, j)
theorem base_n_3_seq_unique : ∀ (i : {i : ℝ // i ≥ 0}) (j : {i : ℝ // i ≥ 0}), i < 3 ^ (k + 1) ∧ j < 3 ^ (k + 1) ∧ i ≠ j → base_n 3 i ≠ base_n 3 j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.union_upto
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: (IF k = 0 THEN setseq(0)
--  ELSE union[real](union_upto(k - 1, setseq), setseq(k))
--  ENDIF)
axiom union_upto {pvs_τ161 : Type*} {pvs_τ162 : Type*} {pvs_τ163 : Type*} : pvs_τ161 → pvs_τ162 → pvs_τ163

-- pvs2lean: poly_families.union_upto_finite
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (FORALL (setseq: [nat -> set[real]]):
--    (FORALL (i: upto(k)): is_finite[real](setseq(i))) IMPLIES
--     is_finite[real](union_upto(k, setseq)))
theorem union_upto_finite : ∀ (setseq : {i : ℝ // i ≥ 0} → ℝ → Bool), (∀ (i : {k : ℕ // k ≤ k}), is_finite (setseq i)) → is_finite (union_upto k setseq) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.real_set_disj_union_cards
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (setseq: [nat -> set[real]]):
--   (FORALL (i: nat): is_finite[real](setseq(i))) AND
--    is_finite[real](union_upto(k, setseq)) AND
--     (FORALL (i, j: nat):
--        i /= j IMPLIES disjoint?[real](se
theorem real_set_disj_union_cards : ∀ (setseq : {i : ℝ // i ≥ 0} → ℝ → Bool), (∀ (i : ℕ), is_finite (setseq i)) ∧
 is_finite (union_upto k setseq) ∧
  (∀ (i : _) (j : ℕ), i ≠ j → disjoint_p (setseq i) (setseq j)) → card (union_upto k setseq) =
 Finset.sum (Finset.Icc 0 k) (fun (i : _)=> card (setseq i)) := by
  -- === TCCs for real_set_disj_union_cards ===
  -- TCC: real_set_disj_union_cards_TCC1
  -- Statement: FORALL (k: nat, setseq: [nat -> set[real]]):
  --   ((FORALL (i: nat): is_finite[real](setseq(i))) AND
  --     is_finite[real](union_upto(k, setseq)) AND
  --      FORALL (i, j: nat):
  --        i /= j IMPLIES disjo...
  have real_set_disj_union_cards_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.mult_tarski_query_simple
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND (FORALL (i: upto(k)): GF(i)(KF(i)) /= 0) IMPLIES
--  TQ_fam(k, a, n, GF, KF, GP) =
--   sigma(0, 3 ^ (k + 1) - 1,
--         LAMBDA (i: nat):
--           sign_prod(k, GP, sig_seq(base_n(3, i))) *
--  
theorem mult_tarski_query_simple : a n ≠ 0 ∧ (∀ (i : {k : ℕ // k ≤ k}), GF i(KF i) ≠ 0) → TQ_fam k a n GF KF GP =
 sigma 0 (3 ^ (k + 1) - 1) (fun (i : ℕ)=> sign_prod k GP (sig_seq (base_n 3 i)) *
          NSol k a n GF KF (sig_seq (base_n 3 i))) := by
  -- === TCCs for mult_tarski_query_simple ===
  -- TCC: mult_tarski_query_simple_TCC1
  -- Statement: FORALL (GF: [nat -> [nat -> int]], KF: [nat -> nat], a: [nat -> int],
  --         k: nat, n: posnat):
  --   (FORALL (i: upto(k)): GF(i)(KF(i)) /= 0) AND a(n) /= 0 IMPLIES
  --    FORALL (i: nat):
  --      FORALL (x...
  have mult_tarski_query_simple_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.split_index_fun_entry
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF F(i)`1 < 3 THEN F
-- ELSIF F(i)`1 = 3 THEN F WITH [i := (1, 2)]
-- ELSIF F(i)`1 = 4 THEN F WITH [i := (0, 1)]
-- ELSE F WITH [i := (0, 2)]
-- ENDIF
axiom split_index_fun_entry {pvs_τ152 : Type*} {pvs_τ153 : Type*} {pvs_τ154 : Type*} {pvs_τ155 : Type*} : pvs_τ152 → pvs_τ153 → pvs_τ154 → pvs_τ155

-- pvs2lean: poly_families.split_index_fun
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF n = 0 THEN split_index_fun_entry(F, n)
-- ELSE split_index_fun(split_index_fun_entry(F, n), n - 1)
-- ENDIF
axiom split_index_fun {pvs_τ149 : Type*} {pvs_τ150 : Type*} {pvs_τ151 : Type*} : pvs_τ149 → pvs_τ150 → pvs_τ151

-- pvs2lean: poly_families.fun_to_pairsfun
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (F(i), 99)
axiom fun_to_pairsfun {pvs_τ90 : Type*} {pvs_τ91 : Type*} {pvs_τ92 : Type*} : pvs_τ90 → pvs_τ91 → pvs_τ92

-- pvs2lean: poly_families.base6_pairs
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: fun_to_pairsfun(base_n(6, k))(i)
axiom base6_pairs {pvs_τ74 : Type*} {pvs_τ75 : Type*} : pvs_τ74 → pvs_τ75

-- pvs2lean: poly_families.split_base6
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: split_index_fun(base6_pairs(k), n - 1)
axiom split_base6 {pvs_τ145 : Type*} {pvs_τ146 : Type*} {pvs_τ147 : Type*} {pvs_τ148 : Type*} : pvs_τ145 → pvs_τ146 → pvs_τ147 → pvs_τ148

-- pvs2lean: poly_families.splits_to_base3
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (n: posnat, k: nat, i: below(n)):
--   split_base6(n, k)(i) =
--    IF base_n(6, k)(i) < 3 THEN (base_n(6, k)(i), 99)
--    ELSIF base_n(6, k)(i) = 3 THEN (1, 2)
--    ELSIF base_n(6, k)(i) = 4 THEN (0, 1)
theorem splits_to_base3 : ∀ (n : {i : {i : ℝ // i ≥ 0} // i > 0}) (k : {i : ℝ // i ≥ 0}) (i : {s : {i : ℝ // i ≥ 0} // s < n}), split_base6 n k(i) = if base_n 6 k(i) < 3 then (base_n 6 k(i), 99)
ELSIF base_n 6 k(i) = 3 then (1, 2)
ELSIF base_n 6 k(i) = 4 then (0, 1)
else (0, 2) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sign_prod_coeff6
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i >= 6 ^ (k + 1) OR (EXISTS (p: upto(k)): base_n(6, i)(p) >= 3) THEN 0
-- ELSE sign_prod(k, GP, sig_seq(base_n(6, i)))
-- ENDIF
axiom sign_prod_coeff6 {pvs_τ141 : Type*} {pvs_τ142 : Type*} {pvs_τ143 : Type*} {pvs_τ144 : Type*} : pvs_τ141 → pvs_τ142 → pvs_τ143 → pvs_τ144

-- pvs2lean: poly_families.NSol_seq6
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i >= 6 ^ (k + 1) OR (EXISTS (p: upto(k)): base_n(6, i)(p) >= 3) THEN 0
-- ELSE NSol(k, a, n, GF, KF, sig_seq(base_n(6, i)))
-- ENDIF
axiom NSol_seq6 {pvs_τ29 : Type*} {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} {pvs_τ35 : Type*} : pvs_τ29 → pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34 → pvs_τ35

-- pvs2lean: poly_families.mult_tarski_query_simple_6_0_to_3
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND (FORALL (i: upto(k)): GF(i)(KF(i)) /= 0) IMPLIES
--  TQ_fam(k, a, n, GF, KF, GP) =
--   sigma(0, 6 ^ (k + 1) - 1,
--         LAMBDA (i: nat):
--           sign_prod_coeff6(k, GP, i) * NSol_seq6(k, a
theorem mult_tarski_query_simple_6_0_to_3 : a n ≠ 0 ∧ (∀ (i : {k : ℕ // k ≤ k}), GF i(KF i) ≠ 0) → TQ_fam k a n GF KF GP =
 sigma 0 (6 ^ (k + 1) - 1) (fun (i : ℕ)=> sign_prod_coeff6 k GP i * NSol_seq6 k a n GF KF(i)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sign_coeff6_zero_entry
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET bij = base_n(6, i)(j) IN
--   IF TQRow(j) = 0 THEN (IF bij >= 3 THEN 0 ELSE 1 ENDIF)
--   ELSIF TQRow(j) = 1
--     THEN (IF bij = 1 THEN 1 ELSIF bij = 2 THEN -1 ELSE 0 ENDIF)
--   ELSIF TQRow(j) = 2 THEN (IF
axiom sign_coeff6_zero_entry {pvs_τ132 : Type*} {pvs_τ133 : Type*} {pvs_τ134 : Type*} {pvs_τ135 : Type*} : pvs_τ132 → pvs_τ133 → pvs_τ134 → pvs_τ135 → Prop

-- pvs2lean: poly_families.sign_coeff6_zero_entry_eq
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: base_n(6, i)(j) = base_n(6, d)(j) IMPLIES
--  sign_coeff6_zero_entry(k, TQRow, i, j) =
--   sign_coeff6_zero_entry(k, TQRow, d, j)
theorem sign_coeff6_zero_entry_eq : base_n 6 i(j) = base_n 6 d(j) → sign_coeff6_zero_entry k TQRow i j =
 sign_coeff6_zero_entry k TQRow d j := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.sign_coeff6_zero
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF i >= 6 ^ (k + 1) THEN 0
-- ELSE product[nat](0, k, LAMBDA (j): sign_coeff6_zero_entry(k, TQRow, i, j))
-- ENDIF
axiom sign_coeff6_zero {pvs_τ128 : Type*} {pvs_τ129 : Type*} {pvs_τ130 : Type*} {pvs_τ131 : Type*} : pvs_τ128 → pvs_τ129 → pvs_τ130 → pvs_τ131

-- pvs2lean: poly_families.in6?
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: (j = vk`1 OR
--        (j = vk`2) OR (j = vk`3) OR (j = vk`4) OR (j = vk`5) OR (j = vk`6))
axiom in6_p {pvs_τ93 : Type*} {pvs_τ94 : Type*} : pvs_τ93 → pvs_τ94 → Prop

-- pvs2lean: poly_families.sigma_disjoinction_6
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (v: [nat -> [nat, nat, nat, nat, nat, nat]]):
--   (FORALL (j):
--      j <= 6 ^ (k + 1) - 1 IMPLIES
--       EXISTS (i): i <= 6 ^ k - 1 AND in6?(v(i), j))
--    AND
--    (FORALL (i):
--       i <= 6 ^ k - 1 IM
theorem sigma_disjoinction_6 : ∀ (v : {i : ℝ // i ≥ 0} → {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0} × {i : ℝ // i ≥ 0}), (∀ (j : _), j ≤ 6 ^ (k + 1) - 1 →
    ∃ (i : _), i ≤ 6 ^ k - 1 ∧ in6_p (v i) j)
 ∧
 (∀ (i : _), i ≤ 6 ^ k - 1 →
     (v i.1 < v i.2 ∧
            (v i.2 < v i.3) ∧ (v i.3 < v i.4) ∧ (v i.4 < v i.5)
        ∧ (v i.5 < v i.6) ∧ (v i.6 ≤ 6 ^ (k + 1) - 1)))
  ∧
  (∀ (i : _) (d : _) (j : _), (i ≤ 6 ^ k - 1) ∧ (d ≤ 6 ^ k - 1) ∧ (j ≤ 6 ^ (k + 1) - 1)
      ∧ in6_p (v i) j ∧ in6_p (v d) j
      → i = d)
   ∧
   (∀ (i : _), i ≤ 6 ^ k - 1 →
       (g (v i.1) + g (v i.2) + g (v i.3) + g (v i.4) + g (v i.5) +
         g (v i.6))
        = 0) → sigma 0 (6 ^ (k + 1) - 1) g = 0 := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: poly_families.mult_tarski_query_simple_6_above_2
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND
--  (FORALL (i: upto(k)): GF(i)(KF(i)) /= 0) AND
--   (EXISTS (i: upto(k)): TQRow(i) > 2)
--  IMPLIES
--  sigma(0, 6 ^ (k + 1) - 1,
--        LAMBDA (i: nat):
--          sign_coeff6_zero(k, TQRow, i) *
--  
theorem mult_tarski_query_simple_6_above_2 : a n ≠ 0 ∧
 (∀ (i : {k : ℕ // k ≤ k}), GF i(KF i) ≠ 0) ∧
  (∃ (i : {k : ℕ // k ≤ k}), TQRow i > 2) → sigma 0 (6 ^ (k + 1) - 1) (fun (i : ℕ)=> sign_coeff6_zero k TQRow i *
         NSol_all k a n GF KF (base_n 6 i))
 = 0 := by
  -- === TCCs for mult_tarski_query_simple_6_above_2 ===
  -- TCC: mult_tarski_query_simple_6_above_2_TCC1
  -- Statement: FORALL (GF: [nat -> [nat -> int]], KF: [nat -> nat],
  --         TQRow: [nat -> subrange(0, 5)], a: [nat -> int], k: nat,
  --         n: posnat):
  --   (EXISTS (i: upto(k)): TQRow(i) > 2) AND
  --    (FORALL (i: up...
  have mult_tarski_query_simple_6_above_2_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.poly_families