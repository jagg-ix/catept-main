-- Auto-generated by pvs2lean/translator.py
-- PVS theory: compute_sturm_tarski
-- PVSLib library: Tarski
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: compute_sturm_tarski

Auto-generated stubs from PVSLib theory `compute_sturm_tarski`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.compute_sturm_tarski

noncomputable section

variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} {pvs_τ8 : Type*} {pvs_τ9 : Type*} {pvs_τ10 : Type*} {pvs_τ11 : Type*} (NSol : pvs_τ6 → pvs_τ7 → pvs_τ8 → pvs_τ9 → pvs_τ10 → pvs_τ11)
variable {pvs_τ16 : Type*} {pvs_τ17 : Type*} (below : pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} (bijective_p : pvs_τ18 → Prop)
variable {pvs_τ25 : Type*} {pvs_τ26 : Type*} {pvs_τ27 : Type*} {pvs_τ28 : Type*} {pvs_τ29 : Type*} (constructed_sturm_sequence_p : pvs_τ25 → pvs_τ26 → pvs_τ27 → pvs_τ28 → pvs_τ29 → Prop)
variable {pvs_τ35 : Type*} (even_p : pvs_τ35 → Prop)
variable {pvs_τ36 : Type*} (integer_pred : pvs_τ36 → Prop)
variable {pvs_τ37 : Type*} {pvs_τ38 : Type*} {pvs_τ39 : Type*} {pvs_τ40 : Type*} (is_neg_remainder_list_p : pvs_τ37 → pvs_τ38 → pvs_τ39 → pvs_τ40 → Prop)
variable {pvs_τ41 : Type*} (length : pvs_τ41 → Prop)
variable {pvs_τ42 : Type*} {pvs_τ43 : Type*} (max : pvs_τ42 → pvs_τ43 → Prop)
variable {pvs_τ44 : Type*} (newa : pvs_τ44 → Prop)
variable {pvs_τ45 : Type*} {pvs_τ46 : Type*} (nschighlow : pvs_τ45 → pvs_τ46)
variable {pvs_τ47 : Type*} {pvs_τ48 : Type*} {pvs_τ49 : Type*} (nth : pvs_τ47 → pvs_τ48 → pvs_τ49)
variable {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} (number_sign_changes : pvs_τ50 → pvs_τ51 → pvs_τ52)
variable {pvs_τ53 : Type*} {pvs_τ54 : Type*} (poly_deriv : pvs_τ53 → pvs_τ54)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} {pvs_τ57 : Type*} {pvs_τ58 : Type*} {pvs_τ59 : Type*} {pvs_τ60 : Type*} (polynomial_prod : pvs_τ55 → pvs_τ56 → pvs_τ57 → pvs_τ58 → pvs_τ59 → pvs_τ60)
variable {pvs_τ61 : Type*} (rational_pred : pvs_τ61 → Prop)
variable {pvs_τ62 : Type*} {pvs_τ63 : Type*} {pvs_τ64 : Type*} {pvs_τ65 : Type*} (remainder_seq : pvs_τ62 → pvs_τ63 → pvs_τ64 → pvs_τ65 → Prop)
variable {pvs_τ66 : Type*} {pvs_τ67 : Type*} (upto : pvs_τ66 → pvs_τ67)

-- pvs2lean: compute_sturm_tarski.der_prod
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: polynomial_prod(g, k, poly_deriv(a), n - 1)(i)
axiom der_prod {pvs_τ30 : Type*} {pvs_τ31 : Type*} {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} : pvs_τ30 → pvs_τ31 → pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop

-- pvs2lean: compute_sturm_tarski.finite_bij_real_remove_one
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, A: set[real], bij: [below(m) -> (A)]):
--   bijective?(bij) AND A(x) IMPLIES
--    m - 1 >= 0 AND
--     EXISTS (bijec: [below(m - 1) -> {r: (A) | r /= x}]): bijective?(bijec)
theorem finite_bij_real_remove_one : ∀ (m : {i : ℝ // i ≥ 0}) (A : ℝ → Bool) (bij : {s : {i : ℝ // i ≥ 0} // s < m} → ℝ), bijective_p bij ∧ A x → m - 1 ≥ 0 ∧
 ∃ (bijec : Finset.range m - 1 → {x : (A) // x ≠ x}), bijective_p bijec := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm_tarski.finite_bij_real_remove_two
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (m: nat, A: set[real], bij: [below(m) -> (A)]):
--   bijective?(bij) AND A(x) AND A(y) AND x /= y IMPLIES
--    m - 2 >= 0 AND
--     EXISTS (bijec: [below(m - 2) -> {r: (A) | r /= x AND r /= y}]):
--     
theorem finite_bij_real_remove_two : ∀ (m : {i : ℝ // i ≥ 0}) (A : ℝ → Bool) (bij : {s : {i : ℝ // i ≥ 0} // s < m} → ℝ), bijective_p bij ∧ A x ∧ A y ∧ x ≠ y → m - 2 ≥ 0 ∧
 ∃ (bijec : Finset.range m - 2 → {x : (A) // x ≠ x ∧ x ≠ y}), bijective_p bijec := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm_tarski.computed_sturm_spec
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND g(k) /= 0 AND (FORALL (i: nat): i > n IMPLIES a(i) = 0)
--  IMPLIES
--  LET sl = remainder_seq(a, n, der_prod(a, n, g, k), n - 1 + k),
--      P: [nat -> [nat -> int]] =
--        (LAMBDA (j: nat):
-- 
theorem computed_sturm_spec : a n ≠ 0 ∧ g k ≠ 0 ∧ (∀ (i : ℕ), i > n → a i = 0) → (let sl := remainder_seq a n (der_prod a n g k) (n - 1 + k),
    P: [nat -> [nat -> int]] =
      (fun (j : ℕ)=> if j < List.length sl
           then list2array 0(nth sl (List.length sl - 1 - j))
         else (fun (i : ℕ)=> 0)
         ),
    N: [nat -> nat] =
      (fun (j : ℕ)=> if j < List.length sl
           then max (List.length (nth sl (List.length sl - 1 - j)) - 1) 0
         else 0
         ),
    M: nat = List.length sl - 1; constructed_sturm_sequence_p P N g k M ∧ P 0 = a ∧ N 0 = n) := by
  -- === TCCs for computed_sturm_spec ===
  -- TCC: computed_sturm_spec_TCC1
  -- Statement: FORALL (a, g: [nat -> int], k: nat, n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND g(k) /= 0 AND a(n) /= 0
  --    IMPLIES n - 1 + k >= 0 AND der_prod(a, n, g, k)(n - 1 + k) /= 0
  have computed_sturm_spec_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: computed_sturm_spec_TCC2
  -- Statement: FORALL (a, g: [nat -> int], k: nat, n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND g(k) /= 0 AND a(n) /= 0
  --    IMPLIES
  --    FORALL (sl:
  --              {crem:
  --                 (is_neg_remaind...
  have computed_sturm_spec_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: computed_sturm_spec_TCC3
  -- Statement: FORALL (a, g: [nat -> int], k: nat, n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND g(k) /= 0 AND a(n) /= 0
  --    IMPLIES
  --    FORALL (sl:
  --              {crem:
  --                 (is_neg_remaind...
  have computed_sturm_spec_tcc3 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: computed_sturm_spec_TCC4
  -- Statement: FORALL (a, g: [nat -> int], k: nat, n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND g(k) /= 0 AND a(n) /= 0
  --    IMPLIES
  --    FORALL (sl:
  --              {crem:
  --                 (is_neg_remaind...
  have computed_sturm_spec_tcc4 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: computed_sturm_spec_TCC5
  -- Statement: FORALL (a, g: [nat -> int], k: nat, n: posnat):
  --   (FORALL (i: nat): i > n IMPLIES a(i) = 0) AND g(k) /= 0 AND a(n) /= 0
  --    IMPLIES
  --    FORALL (sl:
  --              {crem:
  --                 (is_neg_remaind...
  have computed_sturm_spec_tcc5 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: compute_sturm_tarski.Eq_computed_remainder?
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: sl =
--  remainder_seq((LAMBDA (i: nat): IF i <= n THEN a(i) ELSE 0 ENDIF), n,
--                der_prod((LAMBDA (i: nat):
--                            IF i <= n THEN a(i) ELSE 0 ENDIF),
--                     
axiom Eq_computed_remainder_p {pvs_τ1 : Type*} {pvs_τ2 : Type*} {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} : pvs_τ1 → pvs_τ2 → pvs_τ3 → pvs_τ4 → pvs_τ5 → Prop

-- pvs2lean: compute_sturm_tarski.compute_TQ_param
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET P: [nat -> [nat -> int]] =
--       (LAMBDA (j: nat):
--          IF j < length(sl)
--            THEN list2array[int](0)(nth(sl, length(sl) - 1 - j))
--          ELSE (LAMBDA (i: nat): 0)
--          ENDIF),
--   
axiom compute_TQ_param {pvs_τ19 : Type*} {pvs_τ20 : Type*} {pvs_τ21 : Type*} {pvs_τ22 : Type*} {pvs_τ23 : Type*} {pvs_τ24 : Type*} : pvs_τ19 → pvs_τ20 → pvs_τ21 → pvs_τ22 → pvs_τ23 → pvs_τ24

-- pvs2lean: compute_sturm_tarski.TQ
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET newa = (LAMBDA (i: nat): IF i <= n THEN a(i) ELSE 0 ENDIF),
--     sl = remainder_seq(newa, n, der_prod(newa, n, g, k), n - 1 + k)
--   IN compute_TQ_param(newa, n, g, k, sl)
axiom TQ {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} {pvs_τ15 : Type*} : pvs_τ12 → pvs_τ13 → pvs_τ14 → pvs_τ15 → Prop

-- pvs2lean: compute_sturm_tarski.TQ_def
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND g(k) /= 0 IMPLIES
--  TQ(a, n, g, k) = NSol(a, n, g, k, >) - NSol(a, n, g, k, <)
theorem TQ_def : a n ≠ 0 ∧ g k ≠ 0 → TQ a n g k = NSol a n g k > - NSol a n g k < := by
  sorry  -- phase2_high: type translated, proof pending

-- pvs2lean: compute_sturm_tarski.TQ_eq_g
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: a(n) /= 0 AND
--  g1(k) /= 0 AND g2(k) /= 0 AND (FORALL (i: upto(k)): g1(i) = g2(i))
--  IMPLIES TQ(a, n, g1, k) = TQ(a, n, g2, k)
theorem TQ_eq_g : a n ≠ 0 ∧
 g1 k ≠ 0 ∧ g2 k ≠ 0 ∧ (∀ (i : {k : ℕ // k ≤ k}), g1 i = g2 i) → TQ a n g1 k = TQ a n g2 k := by
  sorry  -- phase2_high: type translated, proof pending

end

end NavierStokesClean.PVSLib.compute_sturm_tarski