-- Auto-generated by pvs2lean/translator.py
-- PVS theory: dnf_strategy
-- PVSLib library: Tarski
-- DO NOT EDIT manually; re-run the pvs2lean pipeline to regenerate.

import Mathlib

/-!
# PVSLib → Lean4: dnf_strategy

Auto-generated stubs from PVSLib theory `dnf_strategy`.
Phase-2: real Lean4 type signatures (translated from PVS statements).
Run `lake build` to find which stubs the automated tactics close.
-/

-- Stubs: allow auto-implicit to handle undeclared PVS type names
set_option autoImplicit true

namespace NavierStokesClean.PVSLib.dnf_strategy

noncomputable section

variable {pvs_τ1 : Type*} {pvs_τ2 : Type*} (Some : pvs_τ1 → pvs_τ2)
variable {pvs_τ3 : Type*} {pvs_τ4 : Type*} {pvs_τ5 : Type*} (append : pvs_τ3 → pvs_τ4 → pvs_τ5)
variable {pvs_τ6 : Type*} {pvs_τ7 : Type*} (below : pvs_τ6 → pvs_τ7)
variable {pvs_τ8 : Type*} (car : pvs_τ8 → Prop)
variable {pvs_τ9 : Type*} {pvs_τ10 : Type*} (cdr : pvs_τ9 → pvs_τ10)
variable {pvs_τ12 : Type*} {pvs_τ13 : Type*} {pvs_τ14 : Type*} (cons : pvs_τ12 → pvs_τ13 → pvs_τ14)
variable {pvs_τ15 : Type*} {pvs_τ16 : Type*} {pvs_τ17 : Type*} (degr_rec : pvs_τ15 → pvs_τ16 → pvs_τ17)
variable {pvs_τ18 : Type*} {pvs_τ19 : Type*} (dnf : pvs_τ18 → pvs_τ19)
variable {pvs_τ20 : Type*} {pvs_τ21 : Type*} (eval : pvs_τ20 → pvs_τ21 → Prop)
variable {pvs_τ22 : Type*} {pvs_τ23 : Type*} (eval_atm : pvs_τ22 → pvs_τ23 → Prop)
variable {pvs_τ24 : Type*} {pvs_τ25 : Type*} (eval_atm_list : pvs_τ24 → pvs_τ25 → Prop)
variable {pvs_τ26 : Type*} (length : pvs_τ26 → Prop)
variable {pvs_τ27 : Type*} (none_p : pvs_τ27 → Prop)
variable {pvs_τ28 : Type*} {pvs_τ29 : Type*} {pvs_τ30 : Type*} (nth : pvs_τ28 → pvs_τ29 → pvs_τ30)
variable {pvs_τ31 : Type*} (null_p : pvs_τ31 → Prop)
variable {pvs_τ32 : Type*} {pvs_τ33 : Type*} {pvs_τ34 : Type*} (polynomial : pvs_τ32 → pvs_τ33 → pvs_τ34 → Prop)
variable {pvs_τ35 : Type*} {pvs_τ36 : Type*} {pvs_τ37 : Type*} (pq : pvs_τ35 → pvs_τ36 → pvs_τ37)
variable {pvs_τ38 : Type*} (rel : pvs_τ38 → Prop)
variable {pvs_τ39 : Type*} {pvs_τ40 : Type*} {pvs_τ41 : Type*} {pvs_τ42 : Type*} (rel5 : pvs_τ39 → pvs_τ40 → pvs_τ41 → pvs_τ42)
variable {pvs_τ48 : Type*} (some_p : pvs_τ48 → Prop)
variable {pvs_τ49 : Type*} {pvs_τ50 : Type*} {pvs_τ51 : Type*} {pvs_τ52 : Type*} {pvs_τ53 : Type*} (tarski : pvs_τ49 → pvs_τ50 → pvs_τ51 → pvs_τ52 → pvs_τ53)
variable {pvs_τ54 : Type*} (to_nat : pvs_τ54 → Prop)
variable {pvs_τ55 : Type*} {pvs_τ56 : Type*} (upto : pvs_τ55 → pvs_τ56)
variable {pvs_τ57 : Type*} (val : pvs_τ57 → Prop)

-- pvs2lean: dnf_strategy.remove_constants_rec
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: IF null?(l) THEN Some(a)
-- ELSIF car(l)`d < 1
--   THEN IF eval_atm(car(l))(0) THEN remove_constants_rec(cdr(l), a)
--        ELSE None
--        ENDIF
-- ELSE remove_constants_rec(cdr(l), cons(car(l), a))
-- ENDIF
axiom remove_constants_rec {pvs_τ45 : Type*} {pvs_τ46 : Type*} {pvs_τ47 : Type*} : pvs_τ45 → pvs_τ46 → pvs_τ47

-- pvs2lean: dnf_strategy.when_not_none
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l1, l2, l3: list[DNF_Atom]):
--   some?(remove_constants_rec(l1, l2)) IMPLIES
--    some?(remove_constants_rec(l1, l3))
theorem when_not_none : ∀ (l1 : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}) (l2 : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}) (l3 : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), some_p (remove_constants_rec l1 l2) → some_p (remove_constants_rec l1 l3) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.relation_remove_constants_rec
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l, l2: list[DNF_Atom]):
--   FORALL (a: DNF_Atom):
--     (some?(remove_constants_rec(l, append(l2, (: a :)))) AND
--       some?(remove_constants_rec(l, l2)))
--      IMPLIES
--      val(remove_constants_re
theorem relation_remove_constants_rec : ∀ (l : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}) (l2 : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), ∀ (a : {dnf_atm : {i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel // dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0}), (some_p (remove_constants_rec l (append l2 ((: a :)))) ∧
  some_p (remove_constants_rec l l2)) → val (remove_constants_rec l (append l2 ((: a :)))) =
 append (val (remove_constants_rec l l2)) ((: a :)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.remove_constants
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: remove_constants_rec(l, null)
axiom remove_constants {pvs_τ44 : Type*} : pvs_τ44 → Prop

-- pvs2lean: dnf_strategy.relation_remove_constants_single
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: DNF_Atom):
--   some?(remove_constants((: a :))) IMPLIES
--    (val(remove_constants((: a :))) = (: :) OR
--      val(remove_constants((: a :))) = (: a :))
theorem relation_remove_constants_single : ∀ (a : {dnf_atm : {i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel // dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0}), some_p (remove_constants ((: a :))) → (val (remove_constants ((: a :))) = (: :) ∨
  val (remove_constants ((: a :))) = (: a :)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.relation_remove_constants_null
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[DNF_Atom]):
--   FORALL (a: DNF_Atom):
--     (some?(remove_constants(cons(a, l))) AND
--       val(remove_constants((: a :))) = (: :))
--      IMPLIES val(remove_constants(cons(a, l))) = val(remo
theorem relation_remove_constants_null : ∀ (l : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), ∀ (a : {dnf_atm : {i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel // dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0}), (some_p (remove_constants (cons a l)) ∧
  val (remove_constants ((: a :))) = (: :)) → val (remove_constants (cons a l)) = val (remove_constants l) := by
  -- === TCCs for relation_remove_constants_null ===
  -- TCC: relation_remove_constants_null_TCC1
  -- Statement: FORALL (l: list[DNF_Atom], a: DNF_Atom):
  --   some?(remove_constants(cons(a, l))) IMPLIES
  --    some?[list[DNF_Atom]](remove_constants((: a :)))
  have relation_remove_constants_null_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: relation_remove_constants_null_TCC2
  -- Statement: FORALL (l: list[DNF_Atom], a: DNF_Atom):
  --   some?(remove_constants(cons(a, l))) AND
  --    val(remove_constants((: a :))) = (: :)
  --    IMPLIES some?[list[DNF_Atom]](remove_constants(l))
  have relation_remove_constants_null_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.relation_remove_constants
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[DNF_Atom]):
--   FORALL (a: DNF_Atom):
--     (some?(remove_constants(cons(a, l))) AND
--       val(remove_constants((: a :))) = (: a :))
--      IMPLIES
--      val(remove_constants(cons(a, l))) =
--  
theorem relation_remove_constants : ∀ (l : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), ∀ (a : {dnf_atm : {i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel // dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0}), (some_p (remove_constants (cons a l)) ∧
  val (remove_constants ((: a :))) = (: a :)) → val (remove_constants (cons a l)) =
 append (val (remove_constants l)) ((: a :)) := by
  -- === TCCs for relation_remove_constants ===
  -- TCC: relation_remove_constants_TCC1
  -- Statement: FORALL (l: list[DNF_Atom], a: DNF_Atom):
  --   some?(remove_constants(cons(a, l))) AND
  --    val(remove_constants((: a :))) = (: a :)
  --    IMPLIES some?[list[DNF_Atom]](remove_constants(l))
  have relation_remove_constants_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.eval_truth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real):
--   FORALL (l: list[DNF_Atom]):
--     FORALL (a: DNF_Atom):
--       eval_atm_list(cons(a, l))(x) IFF
--        (eval_atm(a)(x) AND eval_atm_list(l)(x))
theorem eval_truth : ∀ (x : ℝ), ∀ (l : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), ∀ (a : {dnf_atm : {i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel // dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0}), eval_atm_list (cons a l) (x) ↔ (eval_atm a(x) ∧ eval_atm_list l(x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.degr_lemma
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (polyn: [nat -> rat]):
--   FORALL (x: real):
--     FORALL (deg: nat):
--       degr_rec(polyn, deg) = 0 IMPLIES
--        polynomial(polyn, deg)(x) = polynomial(polyn, deg)(0)
theorem degr_lemma : ∀ (polyn : {i : ℝ // i ≥ 0} → ℝ), ∀ (x : ℝ), ∀ (deg : {i : ℝ // i ≥ 0}), degr_rec polyn deg = 0 → polynomial polyn deg(x) = polynomial polyn deg(0) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.eval_remove_constants_none
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (l: list[DNF_Atom]):
--   none?(remove_constants(l)) IMPLIES
--    (FORALL (x: real): eval_atm_list(l)(x) = FALSE)
theorem eval_remove_constants_none : ∀ (l : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), none_p (remove_constants l) → (∀ (x : ℝ), eval_atm_list l(x) = False) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.eval_remove_constants
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (x: real):
--   FORALL (l: list[DNF_Atom]):
--     some?(remove_constants(l)) IMPLIES
--      (eval_atm_list(val(remove_constants(l)))(x) = eval_atm_list(l)(x))
theorem eval_remove_constants : ∀ (x : ℝ), ∀ (l : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), some_p (remove_constants l) → (eval_atm_list (val (remove_constants l)) (x) = eval_atm_list l(x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.conj_to_tarski
-- pvs_status: definition  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET rc = remove_constants(ll) IN
--   IF rc = None THEN FALSE
--   ELSIF length(val(rc)) = 0 THEN TRUE
--   ELSE LET l = val(rc),
--            k = length(l) - 1,
--            pq =
--              LAMBDA (j: nat):
--    
axiom conj_to_tarski {pvs_τ11 : Type*} : pvs_τ11 → Prop

-- pvs2lean: dnf_strategy.satisfiability_relation
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (a: DNF_Atom):
--   a`d > 0 IMPLIES
--    (FORALL (x: real):
--       (rel5(to_nat(a`rel))(polynomial(a`pn, a`d)(x), 0)) IFF
--        (eval_atm(a)(x)))
theorem satisfiability_relation : ∀ (a : {dnf_atm : {i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel // dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0}), a.d > 0 → (∀ (x : ℝ), (rel5 (to_nat a.rel) (polynomial a.pn a.d(x), 0)) ↔ (eval_atm a(x))) := by
  -- === TCCs for satisfiability_relation ===
  -- TCC: satisfiability_relation_TCC1
  -- Statement: FORALL (a: DNF_Atom):
  --   a`d > 0 IMPLIES 0 <= to_nat(a`rel) AND to_nat(a`rel) <= 5
  have satisfiability_relation_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.satisfiability_relation_system
-- pvs_status: unproved  lean_tactic_class: arithmetic_norm_num
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: list[DNF_Atom]):
--   (some?(remove_constants(ll)) AND length(val(remove_constants(ll))) > 0)
--    IMPLIES
--    (((FORALL (j: upto(length(val(remove_constants(ll))) - 1)):
--         ((nth(val(remov
theorem satisfiability_relation_system : ∀ (ll : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), (some_p (remove_constants ll) ∧ List.length (val (remove_constants ll)) > 0) → (((∀ (j : {k : ℕ // k ≤ List.length (val (remove_constants ll})) (((nth (val (remove_constants ll)) j.pn)
          (nth (val (remove_constants ll)) j.d)
       ≠ 0
       ∧ (nth (val (remove_constants ll)) j.d > 0)))
   →
   (∀ (x : ℝ)) ((∀ (j : {k : ℕ // k ≤ List.length (val (remove_constants ll})) (rel5 (to_nat (nth (val (remove_constants ll)) j.rel))
             (polynomial (nth (val (remove_constants ll)) j.pn) (nth (val (remove_constants ll)) j.d)
                        (x),
              0))
       ↔ eval_atm_list ll(x))))) := by
  -- === TCCs for satisfiability_relation_system ===
  -- TCC: satisfiability_relation_system_TCC1
  -- Statement: FORALL (ll: list[DNF_Atom]):
  --   some?(remove_constants(ll)) AND length(val(remove_constants(ll))) > 0
  --    IMPLIES
  --    FORALL (j:
  --              upto(length[DNF_Atom]
  --                       (val[list[DNF_...
  have satisfiability_relation_system_tcc1 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  -- TCC: satisfiability_relation_system_TCC2
  -- Statement: FORALL (ll: list[DNF_Atom]):
  --   (some?(remove_constants(ll)) AND
  --     length(val(remove_constants(ll))) > 0 AND
  --      FORALL (j: upto(length(val(remove_constants(ll))) - 1)):
  --        ((nth(val(remove_c...
  have satisfiability_relation_system_tcc2 : True := by
    trivial  -- pvs2lean tcc_discharge: replace with norm_num/decide if needed
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.conj_to_tarski_sound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (ll: list[DNF_Atom]):
--   conj_to_tarski(ll) IFF (EXISTS (x: real): eval_atm_list(ll)(x))
theorem conj_to_tarski_sound : ∀ (ll : {x : List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel) // every ({dnf_atm: DNF_At |
         dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
     (x)}), conj_to_tarski ll ↔ (∃ (x : ℝ), eval_atm_list ll(x)) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.rel_to_tarski
-- pvs_status: definition  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: LET d = dnf(rel) IN EXISTS (i: below(length(d))): conj_to_tarski(nth(d, i))
axiom rel_to_tarski {pvs_τ43 : Type*} : pvs_τ43 → Prop

-- pvs2lean: dnf_strategy.dnf_eval_truth
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (d: DNF):
--   FORALL (x: real):
--     eval(d)(x) IFF EXISTS (i: below(length(d))): eval_atm_list(nth(d, i))(x)
theorem dnf_eval_truth : ∀ (d : {x : List (List ({i : ℝ // i ≥ 0} × ({i : ℝ // i ≥ 0} → ℝ) × TarskiRel)) // every (fun (x : List DNF_At)=> every ({dnf_atm: DNF_At |
                 dnf_atm.d > 0 → (dnf_atm.pn) (dnf_atm.d) ≠ 0})
             (x))
     (x)}), ∀ (x : ℝ), eval d(x) ↔ ∃ (i : Finset.range List.length d), eval_atm_list (nth d i) (x) := by
  sorry  -- phase2_medium: type translated, proof pending

-- pvs2lean: dnf_strategy.rel_to_tarski_sound
-- pvs_status: unproved  lean_tactic_class: needs_human
-- stmt_confidence: high  [ast]
-- pvs_stmt: FORALL (rel: PolyRelExpr):
--   rel_to_tarski(rel) IFF (EXISTS (x: real): eval(rel)(x))
theorem rel_to_tarski_sound : ∀ (rel : PolyRelExpr), rel_to_tarski rel ↔ (∃ (x : ℝ), eval rel(x)) := by
  sorry  -- phase2_medium: type translated, proof pending

end

end NavierStokesClean.PVSLib.dnf_strategy